import { computeFocusTargets, deckDataFieldChanged, aiChangeAnimationCss, changeGhostStyle, createChatHistoryStore, collectStoredChats, exportAiChatLogs, applyElementUpdate } from './chunk-3Y63XPIN.mjs';
import { useViewerOptions, useReadOnlyRecommendationState, useCompatibilityToastsState, useReducedMotion, useViewerState, useIsMobile, useResizablePanels, useDerivedSlideState, useZoomViewport, useEditorHistory, usePresentationSetup, useTouchGestures, useViewerDialogs, useEditorOperations, useViewerIntegration, useViewPreferencesSync, useLayoutSwitching, ViewerOptionsContext, useYjsDocumentSync, useCollaborationLivePatch, useFollowMode, useBroadcastFollower, safePrompt, scopeLayoutOptionsToActiveSlide, useToolbarVisibility, useKeyboardInsets, useModalDismissDrag, useCollaborativeState, useViewerOptionsContext, useVirtualizedSlides, useSwipeNavigation, useSheetDismissDrag, detectTargetType, parseEmailUrl, parseSlideFromUrl, ACTION_VERB_MAP, syncSelectionHandleOverlay } from './chunk-PCCM7NVY.mjs';
import { MIN_ELEMENT_SIZE, DEFAULT_STROKE_COLOR, getConnectorPathGeometry, buildLineShadowCss, buildLineGlowFilter, renderConnectorMarker, getShapeVisualStyle, getTextStyleForElement, DEFAULT_TEXT_COLOR, renderVectorShape, isConnectorOrLineElement, build3DExtrusionData, getImageRenderStyle, cn, getImageSurfaceStyle, getTextLayoutStyle, renderTableElement, renderMediaElement, shouldRenderFallbackLabel, SHAPE_PRESETS, ANIMATION_PRESET_OPTIONS, DEFAULT_FILL_COLOR, hasDagDuotoneEffect, renderDagDuotoneSvgFilter, getImageSurfaceMaskStyle, getCropShapeClipPath, getImageTilingStyle, getDuotoneColors, getTextWarpStyle, renderTextSegments, buildReactChartViewModel, renderChartElement, buildTextBody3DSceneStyle, SLIDE_NAV_THUMBNAIL_WIDTH, DEFAULT_TEXT_FONT_SIZE, DEFAULT_FONT_FAMILY, TOOLBAR_SECTIONS, DEFAULT_SECTION_GROUP_ID, SHORTCUT_REFERENCE_ITEMS as SHORTCUT_REFERENCE_ITEMS$1, HYPERLINK_COLOR, GRID_SIZE, CONNECTOR_ARROW_OPTIONS, ARROW_SIZE_OPTIONS, startMotionPathPreview, startPreviewAnimation, stopPreviewAnimation, STROKE_DASH_OPTIONS } from './chunk-NTWFXLZX.mjs';
import { hasShapeProperties, normalizeHexColor, colorWithOpacity, normalizeStrokeDashType, getSvgStrokeDasharray, getCompoundLineOffsets, getCompoundLineWidths, svgLineCap, hasTextProperties, connectorWrapperTransform, getAriaRoleDescription, getAriaLabel, getAriaRole, resolveElementInteractivity, isEditableTextElement, buildTextStyleOverrideCss, isElementRendered, resolveElementAriaAttributes, getImageEffectsOpacity, getImageEffectsFilter, inlineElementPointerEvents, isHollowShapeElement, LINK_TOOLTIP_HOST_CLASS, ACTION_BUTTON_PRESETS, mediaTransportVisible, isInkElement, getGroupChildParentFill, getElementLabel, SWITCHABLE_LAYOUT_TYPES, compileEquationTemplateMathMl, buildUserFontFaceStyles, readStoredViewerPrefs, resolveThemeCatalogEntry, THEME_CATALOG, writeStoredViewerPrefs, openPptxFile, readBackstageRecentFile, resolveAutosaveActivation, buildThemeColorMap, viewerOptionsToPreferences, applyPreferenceToOptions, listAutosaveSnapshots, deleteAutosaveSnapshot, resolveExpiredAutosaveSnapshots, resolveHistoryDepth, applyAutoCorrect, resolveImageResolutionScale, resolveAutosaveIntervalMs, resolveAutosaveIntervalSeconds, shouldShowAutosaveRecoveryPrompt, resolveOptionRootClasses, resolve3DRenderingFlags, resolveSlideSizeSelection, buildDeckSaveOptions, paintedStrokeWidth, buildCssGradientFromShapeStyle, ensureActionAffordanceStyles, resolveElementActionAffordance, actionAffordanceLabels, resolveElementHighlightClick, applyHighlightClickStyle, getComputedEffectStyle, getSoftEdgeSvgFilter, getEffectStyleSource, buildStrokeOutline, buildHollowHitOutline, buildSubpathFillOverlay, isImageLikeElement, strokeOutlineViewBox, getElementTransform, resolveRevealedSmartArtNodes, resolveRevealedDrawingShapes, buildSmartArtA11y, canDrillDown, computeSmartArtElementLayout, flattenNodes, buildSummaryZoomView, isImageTiled, getImageColorWashStyle, readEditableText, placeCaretAtEnd, getPendingSelectionRestore, restoreSegmentSelection, getTextCompensationTransform, resolveChartKind, subscribeBarFacePicturePixelSamples, getBarFacePicturePixelSampleVersion, ensureChartInteractionStyles, applyChartPartHighlight, resolveRevealedChartData, formatAxisValue, buildInkGroupStrokes, getInkReplayStyles, INK_REPLAY_KEYFRAMES, buildContentPartStrokes, getContentPartReplayStyles, resolveOleType, getOleAriaLabel, getOleTypeColor, getOleTypeLabel, filterRenderedElements, resolveGroupChildFill, isLinkedTextBox, getOverflowSegments, shouldUseSvgWarp, getTextBodyRotationTransform, placeholderPromptDescriptor, RULER_THICKNESS, RULER_FONT_SIZE, buildPreviewElements, deriveSlideFieldContext, SLIDE_TRANSITION_OPTIONS, hiddenSlideCue, HIDDEN_SLIDE_SLASH_GRADIENT, HIDDEN_SLIDE_LABEL_KEY, visibleTemplateElements, themeToCssVars, shouldClearAutosaveCacheOnClose, masterViewCrudActions, applyMasterViewCrudAction, masterViewCrudFailureKey, applyMotionPathPreset, resetSlideLayoutPath, DEFAULT_INSERT_CHART_KIND, hasCopyableFormat, templateSchemeFromTheme, createBackstagePresentation, playFeedbackSound, shouldDiscardAutosaveOnSuccessfulSave, setMasterViewBackgroundColor, compatToastStackStyle, printPropertiesFrameSlides, printPropertiesSlidesPerPage, VIEWER_OPTIONS_TABS, DEFAULT_QUICK_ACCESS_COMMAND_IDS, isHeaderFooterDateTextVisible, isHeaderFooterHeaderTextVisible, isHeaderFooterFooterTextVisible, buildJoinCollaborationConfig, buildCreateCollaborationConfig, generateBroadcastRoomId, DEFAULT_BROADCAST_SERVER_URL, buildBroadcastViewerUrl, resolveTransportForServerUrl, elementIdSelector, ACTION_INDICATOR_CLASS, ACTION_INDICATOR_ICON_PATH, LINK_TOOLTIP_CLASS, LINK_TOOLTIP_PANEL_CLASS, LINK_TOOLTIP_LABEL_CLASS, LINK_TOOLTIP_HINT_CLASS, DEFAULT_PALETTE2, resolvePalette, computeDrawingViewBox, projectDrawingShapes, resolveRevealedDrawingShapeNodeIds, styleShadowFilter, buildChromeStyle, modelDataToBlobUrl, getImageSvgFilters, buildDuotoneCacheKey, getDuotoneCachedResult, applyDuotone, setDuotoneCachedResult, buildCacheKey, DEFAULT_COLOR_CHANGE_TOLERANCE, getCachedResult, applyColorChange, setCachedResult, getInlineEditorSelection, findChartPartTarget, buildSurfaceChart3DDataForElement, buildBarChart3DDataForElement, buildLineChart3DDataForElement, buildAreaChart3DDataForElement, buildPieChart3DDataForElement, withChartTitle, buildSmartArt3DModel, collectCoherent3DOffNodeIds, isBrowserOpenableMime, formatBytes, getOleDisplayName, getOleBadgeLabel, openUrlInNewTab, getSubstituteFontFamily, hasGlyphEnvelope, buildGlyphEnvelope, getWarpPath, isWedgeCalloutPresetShape, isElementActionable, getSlideBackgroundStyle, pushRecentColor, mruColorsPatch, filterCommands, resolveTitleBarStatusKey, TITLE_BAR_CLASSES, TITLE_BAR_DEFAULT_FILE_KEY, resolveScreenTip, masterViewBackgroundColor, buildFieldSubstitutionContext, shouldConfirmExternalHyperlink, safeOpenUrl, resolvePresentationAction, isPpactionUrl, parsePpactionUrl, isPresentationAdvanceClick, toggleBlackboard, PRESENT_TOOLBAR_CLASSES, buildContextMenuEntries, themeColorSchemesEqual, EXPANDED_MAX_HEIGHT, STATUS_BAR_CLASSES, buildBarActions, formatVersionTimestamp, formatRelativeTime, scanAvailableFontFamilies, PRESETS, CATEGORIES, convertOmmlToLatex, compileLatexEquation, convertLatexToOmml, EQUATION_TEMPLATES, clampPercent2, resolveDefaultPrintSettings, HANDOUT_OPTIONS, TOOLBAR_TABS, SHORTCUT_REFERENCE_ITEMS, resolveViewerAddinRows, availableQuickAccessCommands, addQuickAccessCommand, removeQuickAccessCommand, moveQuickAccessCommand, CUSTOM_FONT_ACCEPT, activateModalFocus, buildCollaborationShareUrl, buildActiveSessionUsers, SORTER_MIN_ZOOM, SORTER_ZOOM_STEP, SORTER_MAX_ZOOM, canGoPrevious, formatSlideCounter, READING_VIEW_COUNTER_ATTR, canGoNext, READING_VIEW_ATTR, OUTLINE_LEVEL_ATTR, OUTLINE_SLIDE_ATTR, OUTLINE_ROW_ATTR, OUTLINE_VIEW_ATTR, mobileElapsedSince, nextPresentedSlide, presenterPrevDisabled, presenterNextDisabled, formatMobileElapsed, mobileSlideCounter, presenterPaneAdvancesOnClick, presenterTimerProgress, PRESENTER_CONSOLE_CLASSES, formatElapsed, centeredSvgTextLines, computeInlineEditorRect, findSmartArtNodeText, setSmartArtNodeStyle, rebuildDrawingShapesIfCleared, shouldCommitSmartArtNodeText, updateSmartArtNodeText, advanceChartMarkDrag, advanceChartValueDrag, buildChartMarkDragGeometry, beginChartMarkDrag, beginChartValueDrag, groupIntoParagraphs, substituteFieldText, extraQuickAccessCommands, TAB_ROW_ACTION_CLASSES, listBackstageRecentFiles, BACKSTAGE_NAV, COMMON_FONT_SIZES, textFontSizePtToPx, INSERT_CHART_TYPES, CHARACTER_SPACING_OPTIONS, OFFICE_COLOR_SWATCHES, shapeFillChange, shapeOutlineChange, readRibbonTransitionDraft, ribbonTransitionUpdates, readSoundFileAsDataUrl, applyTransitionSoundFile, playSlideTransitionPreview, RIBBON_TRANSITION_PRESETS, transitionSoundOptions, transitionSoundSelectedValue, DEFAULT_MOTION_PATH_PRESET_ID, SLIDE_SHOW_OPTIONS, slideShowOptionChange, readSlideShowOption, SLIDE_VIRTUALIZATION_THRESHOLD, PRESENTATION_HIT_TEST_CSS, PRESENTATION_STAGE_ATTRIBUTE, getSlideTransitionAnimations, MORPH_CROSSFADE_GROUP_STYLE, MORPH_CROSSFADE_HALF_STYLE, SLIDE_TRANSITION_KEYFRAMES, buildStrokePathD, cursorForTool, annotationOverlayZIndex, presentationInkPath, resolveNotesSchematicBodyFontSizePx, motionPathFor, setMotionPath, getShapeAdjustmentHandleDescriptors, shouldShowElementHandles, PRESENTER_NAVIGATOR_LABEL_KEYS, PEN_COLORS, HIGHLIGHTER_COLORS, isBlackboardActive, scaleSlidesForSizeChange, slideSizeToCanvasPx, resolveSelectionPaneRename, PRESET_THEMES, resolveNotesSegments, segmentsToEditorHtml, DEBOUNCE_MS, createPlainNotesSegments, parseSegmentsFromRichEditor, segmentsToPlainText, getCurrentParagraphIndex, segmentsToParagraphs, paragraphsToSegments, MAX_INDENT_LEVEL, INDENT_PX, isUrlSafe, buildNotesPrintHtml, toggleSheet, SMARTART_PREVIEW_ELEMENT_HEIGHT, SMARTART_PREVIEW_ELEMENT_WIDTH, buildSmartArtPreviewElement, sanitizeMathMl, computeHandoutLayout, getPrintableArea, generateNoteLineCount, computeAllNotesPages, getNotesPrintableArea, downloadBlob, clampOptionNumber, QUICK_ACCESS_COMMAND_CATALOG, registerCustomFont, openReadingView, applyReadingViewCommand, createPresentationKeyBuffer, readingViewFitScale, READING_VIEW_STAGE_ATTR, buildOutline, applyOutlineEdit, mapOutlineKey, notesSegmentsToSpans, erasePresentationInkAt, movePresenterPointer, appendPresentationInkPoint, PRESENTER_CONSOLE_CONTROLS, NOTES_FONT_SIZE_DEFAULT, formatTime, NOTES_FONT_SIZE_MIN, clampNotesFontSize, NOTES_FONT_SIZE_STEP, NOTES_FONT_SIZE_MAX, withChartPointValue, BACKSTAGE_TEMPLATES, formatBackstageDate, formatBackstageSize, backstageCardsFor, DEFAULT_VIEWER_PROFILE, resolveProfileInitial, AVATAR_COLOR_SWATCHES, resolveDefaultFontFamily, textFontSizePxToPt, buildFontCatalog, buildThemeColorSwatchGrid, themeColorSwatchRows, findSelectedThemeSwatch, themeSwatchCommit, RIBBON_SHAPE_SWATCHES, canGroupSelection, canUngroupSelection, canSetStrokeWidth, strokeWidthOf, TRANSITION_SOUND_OTHER_VALUE, TRANSITION_SOUND_NONE_VALUE, clearTransitionSound, ENTRANCE_PRESET_VALUES, EMPHASIS_PRESET_VALUES, EXIT_PRESET_VALUES, MOTION_PATH_FAMILIES, motionPathFamilyLabelKey, motionPathPresetsByFamily, motionPathPresetLabelKey, masterViewPseudoSlide, authorConnectorBetweenSites, findEraserHitElementId, generateTicks, rulerDragToGuidePosition, getElementTransformWithoutRotation, getActiveXControlOverlayView, buildCommentMarkers, COMMENT_MARKER_SIZE, collectConnectorSiteCandidates, getConnectorEndpointHandles, findConnectorSiteNear, motionPathToSvgD, motionPathEndPixel, isEditableMotionPath, setMotionPathEnd, applyTableStyleDelete, applyTableStyleMapChange, themeColorLabel, buildThemeColorGrid, THEME_COLOR_SCHEME_KEYS, COMMON_FONTS, handleReadingViewKey, getLocalStorageUsageSummary, saveViewerProfile, clearAllLocalViewerData, isCurrentLayout, SLIDE_TEMPLATES, computeAnchoredPopupPosition, buildLiveInkStrokeView, CONNECTOR_SITE_SNAP_PX, resolveConnectorEndpointUpdate, withConnectorEndpointUpdate, getShapeConnectionSites, isElementLocked, mergeSlideTransition, mapSlideSorterKey, isEditorTextInputTarget, clampSorterZoom, buildLayoutPreviewGeometry, buildSlideTemplateContent, hasTiltData, elementLockTogglePatch, buildTableDataGrid, appendTableElementRow, removeLastTableElementRow, appendTableElementColumn, removeLastTableElementColumn, removeTableElementColumn, removeTableElementRow, setTableElementCellText, TABLE_STYLE_PRESETS, applyTableStylePreset, tableStyleAssignmentUpdate, evenColumnWidths, redistributeColumnWidth, evenRowHeights, patchChartData, collapseChartTitleRunsForEdit, chartGridlinesState, chartGridlinesPatch, seriesSecondaryAxisPatch, setChartCategoryLabel, setChartCellValue, addChartCategory, removeChartCategory, addChartSeries, removeChartSeries, setChartAxisLogScale, setChartAxisTitleStyle, setChartAxisGridlineStyle, setChartSeriesMarker, setChartSeriesChartType, setChartDataPointFill, setChartDataPointExplosion, setChartDataPointMarker, setChartDataPointLabel, describeSmartArtBounds, canAddTopLevelNode, classifyExtraConnections, siblingIndex, siblingCount, canRemoveTopLevelNode, canInteractWithElement, buildOleObjectNamePatch, getOleObjectTypeLabel, sanitizeGradientStops, shouldShowAccessibilitySection, pptxActionToElementAction, withFrameSlides, withSlidesPerPage, applyThemeOverrideToSlide, COLOR_MAP_ALIAS_KEYS, DEFAULT_COLOR_MAP, schemaLabel, THEME_COLOR_SLOT_LABEL_KEYS, SLIDE_SIZE_PRESETS, TRANSITION_VALID_DIRECTIONS, TRANSITION_ORIENTATION_TYPES, clampTransitionNumber, TRANSITION_SPEED_OPTIONS, TRANSITION_MORPH_OPTIONS, flattenTagCollections, resolveTemplateBackgroundRows, formatCommentTimestamp, commentMentionQuery, matchCommentMentionAuthors, buildAnimationTimelineRows, clearMotionPath, getEffectSoundState, setEffectSound, setAfterAnimation, setAfterAnimationColor, DIRECTIONAL_PRESETS, buildAnimationTimelineBars, motionPathPresetIdForPath, normalizeTableStyleGuid, describeTableStyleEditor, TABLE_STYLE_EDITOR_PARTS, computeMergeCellRight, computeMergeCellDown, computeSplitCell, resolveDisplayedChartType, CHART_TYPE_OPTIONS, BAR3D_SHAPE_OPTIONS, bar3DShapePatch, RADAR_STYLE_OPTIONS, radarStylePatch, SURFACE_WIREFRAME_OPTIONS, surfaceWireframePatch, isSeriesUsingSecondaryAxis, listChartUserShapeRows, withChartUserShapeAdded, createDefaultChartUserShape, withChartUserShapeRowRemoved, withChartUserShapeRowTextUpdated, SMARTART_COLOR_SCHEME_LABEL_KEYS, SMARTART_STYLE_LABEL_KEYS, addSmartArtNodeAsChild, removeSmartArtNode, addSiblingAfter, removeEmptyNode, demote, promote, reorder, clampCropValue, DUOTONE_PRESETS, ARTISTIC_EFFECTS, mediaTrimEndSeconds, trimmedMediaDurationMs, mediaTrimEndAbsoluteMs, mediaTrimEndMsFromAbsoluteMs, getNonVisualDescriptionFields, resolveActionType, ELEMENT_ACTION_TYPE_OPTIONS, elementActionToPptxAction, canCommitActionType, slideSizeFromPreset, withSlideSizeOrientation, TRANSITION_DIR_ARROWS, buildDirectionGrid, updateTagInCollections, deleteTagFromCollections, addTagToCollections, commentTextSegments, COMMENT_MENTION_CLASS, COMMENT_MENTION_ATTRIBUTE, insertCommentMention, reorderAnimationTimelineRows, applyAnimationTimelineOrder, animationEffectLabel, TABLE_STYLE_BORDER_SIDES, applyTableStyleFieldEdit, createTableStyleEntry, addTableStyleToMap, deleteTableStyleFromMap, LBL, SEL, FILL_MODE_OPTIONS, SECTION_HEADING, NUM, getChartUserShapeGroupTransform, withChartUserShapeGroupChildAdded, createDefaultChartUserShapeGroupChild, withChartUserShapeRowUpdated, getChartUserShapeRowChartBox, withChartUserShapeRowFlipUpdated, withChartUserShapeRowRotationUpdated, withChartUserShapeRowChartBoxUpdated, switchSmartArtLayout, mediaTrimEndMsFromSeconds, validateMediaTrimRange, SHAPE_QUICK_STYLES, quickStyleSwatchCss, PATTERN_PRESET_OPTIONS, getPatternSvg, COMPOUND_LINE_OPTIONS, LINE_JOIN_OPTIONS, LINE_CAP_OPTIONS, TEXT_WARP_PRESETS, warpPreviewPath, text3dPtToEmu, text3dEmuToPt, MATERIAL_PRESETS, toSlideIndex, TABLE_STYLE_BORDER_SIDE_LABEL_KEYS, TABLE_STYLE_DASH_PRESETS, GRADIENT_TYPE_OPTIONS, PATTERN_OPTIONS, FILL_PATTERN_LABEL_KEYS, openNativeEyeDropper, toggleText3dExtrusion, BEVEL_PRESETS } from './chunk-VQX7G75Z.mjs';
import { LOCALE_CATALOG, translationsEn } from './chunk-JBSAKP4Z.mjs';
import React20, { createContext, useMemo, useState, useCallback, lazy, forwardRef, useEffect, useRef, Suspense, useLayoutEffect, useContext, useSyncExternalStore, useId, Fragment as Fragment$1, useDeferredValue } from 'react';
import { jsxs, jsx, Fragment } from 'react/jsx-runtime';
import { useTranslation } from 'react-i18next';
import { LuRotateCw, LuMessageSquare, LuEyeOff, LuHistory, LuTrash2, LuSettings, LuX, LuCalendarDays, LuClock, LuHash, LuFileText, LuText, LuCast, LuSave, LuUndo, LuRedo, LuSearch, LuShieldCheck, LuPlus, LuPanelLeftClose, LuStickyNote, LuMonitor, LuColumns2, LuPresentation, LuMinus, LuSettings2, LuLayers, LuDownload, LuCheck, LuLock, LuEye, LuType, LuLoader, LuShieldAlert, LuSignature, LuInfo, LuTriangleAlert, LuPrinter, LuPenTool, LuBug, LuChevronUp, LuChevronDown, LuUpload, LuWifi, LuWifiOff, LuUsers, LuCopy, LuChevronLeft, LuChevronRight, LuPlay, LuPause, LuZoomOut, LuZoomIn, LuSpellCheck, LuFileDown, LuMenu, LuSparkles, LuShare2, LuPanelLeft, LuPanelRight, LuArrowLeft, LuFolderOpen, LuClipboardPaste, LuScissors, LuPaintbrush, LuSquare, LuImage, LuVideo, LuDatabase, LuAArrowUp, LuAArrowDown, LuRemoveFormatting, LuHighlighter, LuList, LuListOrdered, LuIndentDecrease, LuIndentIncrease, LuReplace, LuMousePointer2, LuShapes, LuPaintBucket, LuPenLine, LuPalette, LuPencil, LuStar, LuMoveRight, LuMousePointerClick, LuClock3, LuMonitorPlay, LuListVideo, LuCaptions, LuCamera, LuEraser, LuRotateCcw, LuCircleHelp, LuBookOpen, LuLanguages, LuGlobe, LuGitCompare, LuMessageSquarePlus, LuLockKeyhole, LuPanelTop, LuLayoutGrid, LuGrid3X3, LuPipette, LuRuler, LuMaximize2, LuCode, LuCaseSensitive, LuTimer, LuGripVertical, LuLoaderCircle, LuBold, LuItalic, LuUnderline, LuStrikethrough, LuLink, LuGrid2X2, LuCopyPlus, LuScan, LuMonitorOff, LuCirclePlay, LuCirclePause, LuArrowLeftRight, LuClipboardCopy, LuWand, LuTextCursorInput, LuFile, LuEllipsis, LuUserRound, LuFilePlus2, LuHouse, LuFileJson, LuBox, LuFileImage, LuUser, LuHardDrive, LuLogIn, LuLayoutTemplate, LuFolderPlus, LuAlignLeft, LuAlignCenter, LuAlignRight, LuAlignJustify, LuColumns3, LuSpline, LuAlignHorizontalSpaceAround, LuAlignVerticalSpaceAround, LuGroup, LuUngroup, LuMove, LuRadio, LuLockOpen, LuArrowDown, LuArrowUp, LuArrowRight, LuReply, LuBookmark } from 'react-icons/lu';

var ViewerThemeContext = createContext(void 0);
function ViewerThemeProvider({ theme, children }) {
  return /* @__PURE__ */ jsx(ViewerThemeContext.Provider, { value: theme, children });
}
function useViewerTheme() {
  return useContext(ViewerThemeContext);
}
function useThemeStyle(theme) {
  return useMemo(() => {
    if (!theme) {
      return void 0;
    }
    const vars = themeToCssVars(theme);
    if (Object.keys(vars).length === 0) {
      return void 0;
    }
    return vars;
  }, [theme]);
}
var CollaborationContext = createContext(null);
function useCollaboration() {
  return useContext(CollaborationContext);
}
function CollaborationProvider({
  config,
  canvasWidth,
  canvasHeight,
  children
}) {
  const value = useCollaborativeState({
    config,
    canvasWidth,
    canvasHeight
  });
  return /* @__PURE__ */ jsx(CollaborationContext.Provider, { value, children });
}
function RemoteUserCursors({
  remoteUsers,
  activeSlideIndex,
  canvasWidth,
  canvasHeight
}) {
  const visibleUsers = remoteUsers.filter((u) => u.activeSlideIndex === activeSlideIndex);
  if (visibleUsers.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "svg",
    {
      "data-testid": "remote-user-cursors",
      "data-export-ignore": "true",
      className: "absolute inset-0 pointer-events-none",
      style: { zIndex: 9999 },
      width: canvasWidth,
      height: canvasHeight,
      viewBox: `0 0 ${canvasWidth} ${canvasHeight}`,
      "aria-hidden": "true",
      children: visibleUsers.map((user) => /* @__PURE__ */ jsxs(
        "g",
        {
          style: {
            transform: `translate(${user.cursorX}px, ${user.cursorY}px)`,
            transition: "transform 90ms linear",
            willChange: "transform"
          },
          "data-testid": `remote-cursor-${user.clientId}`,
          "data-pptx-remote-cursor": user.clientId,
          children: [
            /* @__PURE__ */ jsx(
              "path",
              {
                d: "M0 0 L0 16 L4.5 12.5 L8 20 L10.5 19 L7 11.5 L12 11 Z",
                fill: user.userColor,
                stroke: "#fff",
                strokeWidth: 1,
                opacity: 0.9
              }
            ),
            /* @__PURE__ */ jsxs("g", { transform: "translate(14, 18)", children: [
              /* @__PURE__ */ jsx(
                "rect",
                {
                  rx: 3,
                  ry: 3,
                  x: -2,
                  y: -10,
                  width: Math.min(user.userName.length * 7 + 8, 150),
                  height: 16,
                  fill: user.userColor,
                  opacity: 0.85
                }
              ),
              /* @__PURE__ */ jsx(
                "text",
                {
                  fill: "#fff",
                  fontSize: 10,
                  fontFamily: "system-ui, sans-serif",
                  fontWeight: 500,
                  dominantBaseline: "central",
                  y: -2,
                  x: 2,
                  children: user.userName.length > 20 ? `${user.userName.slice(0, 18)}...` : user.userName
                }
              )
            ] })
          ]
        },
        user.clientId
      ))
    }
  );
}
function getInitials(name) {
  const parts = name.trim().split(/\s+/u);
  if (parts.length >= 2) {
    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
  }
  return name.slice(0, 2).toUpperCase();
}
function AvatarCircle({
  name,
  color,
  avatar,
  isLocal
}) {
  const { t } = useTranslation();
  const initials = getInitials(name);
  const title = isLocal ? t("pptx.collaboration.youLabel", { name }) : name;
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "relative w-7 h-7 rounded-full flex items-center justify-center text-[10px] font-semibold text-white border-2 -ml-1 first:ml-0",
      style: {
        backgroundColor: color,
        borderColor: isLocal ? "#fff" : color
      },
      title,
      "aria-label": title,
      children: avatar ? /* @__PURE__ */ jsx(
        "img",
        {
          src: avatar,
          alt: "",
          className: "w-full h-full rounded-full object-cover",
          onError: (e) => {
            e.target.style.display = "none";
          }
        }
      ) : initials
    }
  );
}
function UserAvatarBar({
  remoteUsers,
  localUserName,
  localUserColor,
  localUserAvatar,
  status,
  maxVisible = 5
}) {
  const { t } = useTranslation();
  if (status === "disconnected" || status === "error") {
    return null;
  }
  const allUsers = [
    { name: localUserName, color: localUserColor, avatar: localUserAvatar, isLocal: true },
    ...remoteUsers.map((u) => ({
      name: u.userName,
      color: u.userColor,
      avatar: u.userAvatar,
      isLocal: false
    }))
  ];
  const visible = allUsers.slice(0, maxVisible);
  const overflow = allUsers.length - maxVisible;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "user-avatar-bar",
      className: "flex items-center px-2",
      "aria-label": t("pptx.collaboration.usersConnected", { count: allUsers.length }),
      children: [
        visible.map((user, i) => /* @__PURE__ */ jsx(
          AvatarCircle,
          {
            name: user.name,
            color: user.color,
            avatar: user.avatar,
            isLocal: user.isLocal
          },
          user.isLocal ? "local" : `remote-${i}`
        )),
        overflow > 0 && /* @__PURE__ */ jsxs(
          "div",
          {
            className: "w-7 h-7 rounded-full flex items-center justify-center text-[10px] font-semibold text-gray-300 bg-gray-700 border-2 border-gray-600 -ml-1",
            title: t("pptx.collaboration.moreUsers", { count: overflow }),
            children: [
              "+",
              overflow
            ]
          }
        )
      ]
    }
  );
}
var STATUS_STYLES = {
  connected: {
    dot: "bg-green-400",
    text: "text-green-400",
    label: "pptx.collaboration.status.connected"
  },
  connecting: {
    dot: "bg-yellow-400 animate-pulse",
    text: "text-yellow-400",
    label: "pptx.collaboration.status.connecting"
  },
  disconnected: {
    dot: "bg-gray-500",
    text: "text-gray-500",
    label: "pptx.collaboration.status.disconnected"
  },
  error: {
    dot: "bg-red-400",
    text: "text-red-400",
    label: "pptx.collaboration.status.error"
  }
};
function CollaborationStatusIndicator({
  status,
  connectedCount,
  onRetry
}) {
  const { t } = useTranslation();
  const style = STATUS_STYLES[status];
  return /* @__PURE__ */ jsxs(
    "div",
    {
      role: "status",
      "data-testid": "collaboration-status",
      className: "flex items-center gap-1.5",
      "aria-label": t("pptx.collaboration.statusAriaLabel", {
        status: t(`pptx.collaboration.status.${status}`),
        count: connectedCount
      }),
      children: [
        /* @__PURE__ */ jsx("span", { className: `inline-block w-2 h-2 rounded-full ${style.dot}`, "aria-hidden": "true" }),
        /* @__PURE__ */ jsx("span", { className: `text-[10px] ${style.text}`, children: status === "connected" ? t("pptx.collaboration.userCount", { count: connectedCount }) : t(`pptx.collaboration.status.${status}`) }),
        status === "error" && onRetry && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onRetry,
            className: "text-[10px] text-blue-400 hover:text-blue-300 underline underline-offset-2 transition-colors",
            "aria-label": t("pptx.collaboration.retry"),
            children: t("pptx.collaboration.retry")
          }
        )
      ]
    }
  );
}
function getInitials2(name) {
  const parts = name.trim().split(/\s+/u);
  if (parts.length >= 2 && parts[0] && parts[parts.length - 1]) {
    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
  }
  return name.slice(0, 2).toUpperCase() || "?";
}
function FollowModeBar({
  presences,
  followedClientId,
  onFollow
}) {
  const { t } = useTranslation();
  if (presences.length === 0) {
    return null;
  }
  const followedPeer = followedClientId === null ? null : presences.find((p) => p.clientId === followedClientId) ?? null;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "follow-mode-bar",
      "data-export-ignore": "true",
      className: "flex flex-wrap items-center gap-3 rounded-lg border border-border bg-card/95 px-2.5 py-1.5 text-xs text-foreground shadow-lg",
      children: [
        /* @__PURE__ */ jsx(
          "span",
          {
            "data-testid": "follow-status",
            className: "inline-flex items-center gap-1.5 whitespace-nowrap text-muted-foreground",
            children: followedPeer ? /* @__PURE__ */ jsxs(Fragment, { children: [
              t("pptx.followMode.following"),
              /* @__PURE__ */ jsx("strong", { className: "text-foreground", children: followedPeer.userName }),
              /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  "data-testid": "follow-stop",
                  onClick: () => onFollow(null),
                  title: t("pptx.followMode.stopFollowing"),
                  className: "cursor-pointer rounded-md border border-border bg-transparent px-2 py-0.5 text-[11px] text-foreground hover:bg-muted",
                  children: t("pptx.followMode.stop")
                }
              )
            ] }) : t("pptx.followMode.followCollaborator")
          }
        ),
        /* @__PURE__ */ jsx("ul", { className: "m-0 flex list-none items-center gap-1.5 p-0", children: presences.map((peer) => {
          const isFollowing = peer.clientId === followedClientId;
          return /* @__PURE__ */ jsx("li", { children: /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              "data-testid": "follow-peer",
              "data-client-id": peer.clientId,
              "aria-pressed": isFollowing,
              title: isFollowing ? t("pptx.followMode.stopFollowingUser", { name: peer.userName }) : t("pptx.followMode.followUser", { name: peer.userName }),
              onClick: () => onFollow(isFollowing ? null : peer.clientId),
              className: `inline-flex cursor-pointer items-center gap-1.5 rounded-full border py-0.5 pl-0.5 pr-2 text-foreground hover:bg-muted ${isFollowing ? "border-primary bg-primary/30" : "border-transparent bg-muted/60"}`,
              children: [
                /* @__PURE__ */ jsx(
                  "span",
                  {
                    "data-testid": "follow-avatar",
                    className: "inline-flex h-[22px] w-[22px] items-center justify-center rounded-full text-[10px] font-semibold leading-none text-white",
                    style: { backgroundColor: peer.userColor },
                    children: getInitials2(peer.userName)
                  }
                ),
                /* @__PURE__ */ jsx("span", { className: "max-w-[120px] overflow-hidden text-ellipsis whitespace-nowrap", children: peer.userName })
              ]
            }
          ) }, peer.clientId);
        }) })
      ]
    }
  );
}
function CollaborationCursorOverlay({
  activeSlideIndex,
  canvasWidth,
  canvasHeight,
  selectedElementId
}) {
  const collab = useCollaboration();
  const containerRef = useRef(null);
  const prevSelectionRef = useRef(selectedElementId);
  useEffect(() => {
    if (!collab || selectedElementId === prevSelectionRef.current) {
      return;
    }
    prevSelectionRef.current = selectedElementId;
    collab.broadcastPresence({
      selectedElementId: selectedElementId ?? void 0,
      activeSlideIndex
    });
  }, [collab, selectedElementId, activeSlideIndex]);
  useEffect(() => {
    if (!collab) {
      return;
    }
    const parent = containerRef.current?.parentElement;
    if (!parent) {
      return;
    }
    const handler = (e) => {
      const rect = parent.getBoundingClientRect();
      const x = (e.clientX - rect.left) / rect.width * canvasWidth;
      const y = (e.clientY - rect.top) / rect.height * canvasHeight;
      collab.broadcastPresence({
        cursorX: x,
        cursorY: y,
        activeSlideIndex
      });
    };
    parent.addEventListener("pointermove", handler);
    return () => parent.removeEventListener("pointermove", handler);
  }, [collab, canvasWidth, canvasHeight, activeSlideIndex]);
  if (!collab) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref: containerRef,
      "data-testid": "collab-pointer-tracker",
      "data-export-ignore": "true",
      style: { display: "contents" },
      children: /* @__PURE__ */ jsx(
        RemoteUserCursors,
        {
          remoteUsers: collab.remoteUsers,
          activeSlideIndex,
          canvasWidth,
          canvasHeight
        }
      )
    }
  );
}
function RemoteSelectionOverlay({
  elements,
  activeSlideIndex
}) {
  const collab = useCollaboration();
  if (!collab) {
    return null;
  }
  const elementMap = /* @__PURE__ */ new Map();
  for (const el of elements) {
    elementMap.set(el.id, el);
  }
  const selections = [];
  for (const user of collab.remoteUsers) {
    if (user.activeSlideIndex === activeSlideIndex && user.selectedElementId) {
      const el = elementMap.get(user.selectedElementId);
      if (el) {
        selections.push({
          userName: user.userName,
          userColor: user.userColor,
          element: el
        });
      }
    }
  }
  if (selections.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx(Fragment, { children: selections.map((sel) => /* @__PURE__ */ jsx(
    "div",
    {
      "data-testid": `remote-selection-${sel.element.id}`,
      "data-pptx-remote-selection": sel.element.id,
      "data-export-ignore": "true",
      className: "absolute pointer-events-none",
      style: {
        left: sel.element.x,
        top: sel.element.y,
        width: sel.element.width,
        height: sel.element.height,
        zIndex: 9997,
        border: `2px solid ${sel.userColor}`,
        borderRadius: 2
      },
      children: /* @__PURE__ */ jsx(
        "span",
        {
          className: "absolute -top-5 left-0 px-1 py-0.5 text-[9px] font-medium text-white rounded-sm whitespace-nowrap leading-none",
          style: { backgroundColor: sel.userColor },
          children: sel.userName
        }
      )
    },
    `remote-sel-${sel.element.id}`
  )) });
}
var ChartPartSelectionContext = createContext({
  selection: null,
  setSelection: () => {
  }
});
function ChartPartSelectionProvider({
  children
}) {
  const [selection, setSelection] = useState(null);
  const value = useMemo(() => ({ selection, setSelection }), [selection]);
  return /* @__PURE__ */ jsx(ChartPartSelectionContext.Provider, { value, children });
}
function useChartPartSelection() {
  return useContext(ChartPartSelectionContext);
}
var AreaChart3DContext = createContext(false);
function LoadingState({ className }) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(
    "div",
    {
      role: "status",
      "aria-live": "polite",
      "aria-atomic": "true",
      className: cn(
        "h-full w-full flex items-center justify-center text-muted-foreground",
        className
      ),
      children: /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "animate-spin rounded-full h-6 w-6 border-b-2 border-primary",
            "aria-hidden": "true"
          }
        ),
        /* @__PURE__ */ jsx("span", { children: t("pptx.viewer.loading") })
      ] })
    }
  );
}
function FailedToLoad() {
  return null;
}
var LazyArea3DChartScene = React20.lazy(
  async () => {
    try {
      return await import('./Area3DChartScene-JM3CQ3WQ.mjs');
    } catch {
      return { default: FailedToLoad };
    }
  }
);
var SceneErrorBoundary = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function Area3DChartRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = useMemo(
    () => buildAreaChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = /* @__PURE__ */ jsx(Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return /* @__PURE__ */ jsx(LoadingState, {});
  }
  return /* @__PURE__ */ jsx(SceneErrorBoundary, { fallback: svgFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: /* @__PURE__ */ jsx(LoadingState, {}), children: /* @__PURE__ */ jsx(
    LazyArea3DChartScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
var BarChart3DContext = createContext(false);
function FailedToLoad2() {
  return null;
}
var LazyBar3DChartScene = React20.lazy(
  async () => {
    try {
      return await import('./Bar3DChartScene-VNFWI2QG.mjs');
    } catch {
      return { default: FailedToLoad2 };
    }
  }
);
var SceneErrorBoundary2 = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function Bar3DChartRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = useMemo(
    () => buildBarChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = /* @__PURE__ */ jsx(Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return /* @__PURE__ */ jsx(LoadingState, {});
  }
  return /* @__PURE__ */ jsx(SceneErrorBoundary2, { fallback: svgFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: /* @__PURE__ */ jsx(LoadingState, {}), children: /* @__PURE__ */ jsx(
    LazyBar3DChartScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}

// src/viewer/components/elements/build-chart3d-part-interaction.ts
function buildChart3DPartInteraction({
  element,
  canEdit,
  onUpdateElement,
  selection,
  setSelection,
  setDragValue
}) {
  if (!canEdit) {
    return void 0;
  }
  const onSelect = (part) => {
    if (part) {
      setSelection({ elementId: element.id, part });
    } else if (selection?.elementId === element.id) {
      setSelection(null);
    }
  };
  const onValueDragPreview = (_part, value) => {
    setDragValue(value);
  };
  const onValueDragCommit = (part, value) => {
    setDragValue(null);
    if (!onUpdateElement || !element.chartData || part.pointIndex === void 0) {
      return;
    }
    onUpdateElement({
      chartData: withChartPointValue(element.chartData, part.seriesIndex, part.pointIndex, value)
    });
  };
  return { onSelect, onValueDragPreview, onValueDragCommit };
}
var LineChart3DContext = createContext(false);
function FailedToLoad3() {
  return null;
}
var LazyLine3DChartScene = React20.lazy(
  async () => {
    try {
      return await import('./Line3DChartScene-GXOWGIFJ.mjs');
    } catch {
      return { default: FailedToLoad3 };
    }
  }
);
var SceneErrorBoundary3 = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function Line3DChartRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = useMemo(
    () => buildLineChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = /* @__PURE__ */ jsx(Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return /* @__PURE__ */ jsx(LoadingState, {});
  }
  return /* @__PURE__ */ jsx(SceneErrorBoundary3, { fallback: svgFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: /* @__PURE__ */ jsx(LoadingState, {}), children: /* @__PURE__ */ jsx(
    LazyLine3DChartScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
var PieChart3DContext = createContext(false);
function FailedToLoad4() {
  return null;
}
var LazyPieChart3DScene = React20.lazy(
  async () => {
    try {
      return await import('./PieChart3DScene-WXJEDYMK.mjs');
    } catch {
      return { default: FailedToLoad4 };
    }
  }
);
var SceneErrorBoundary4 = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function PieChart3DRenderer({
  element,
  interaction,
  selectedPart
}) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = useMemo(
    () => buildPieChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = /* @__PURE__ */ jsx(Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return /* @__PURE__ */ jsx(LoadingState, {});
  }
  return /* @__PURE__ */ jsx(SceneErrorBoundary4, { fallback: svgFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: /* @__PURE__ */ jsx(LoadingState, {}), children: /* @__PURE__ */ jsx(
    LazyPieChart3DScene,
    {
      options,
      interaction,
      selectedPart
    }
  ) }) });
}
var SurfaceChart3DContext = createContext(false);
function FailedToLoad5() {
  return null;
}
var LazySurfaceChart3DScene = React20.lazy(
  async () => {
    try {
      return await import('./SurfaceChart3DScene-IKPE2SVE.mjs');
    } catch {
      return { default: FailedToLoad5 };
    }
  }
);
var SceneErrorBoundary5 = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function SurfaceChart3DRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = useMemo(
    () => buildSurfaceChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = /* @__PURE__ */ jsx(Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return /* @__PURE__ */ jsx(LoadingState, {});
  }
  return /* @__PURE__ */ jsx(SceneErrorBoundary5, { fallback: svgFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: /* @__PURE__ */ jsx(LoadingState, {}), children: /* @__PURE__ */ jsx(
    LazySurfaceChart3DScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
function useChartMarkInteraction({
  element,
  canEdit,
  onUpdateElement,
  viewModel,
  wrapperRef,
  setSelection
}) {
  const dragRef = useRef(null);
  const markDragRef = useRef(null);
  const [previewData, setPreviewData] = useState(null);
  const [dragValue, setDragValue] = useState(null);
  const endDrag = (commit) => {
    const active = dragRef.current;
    const markActive = markDragRef.current;
    dragRef.current = null;
    markDragRef.current = null;
    setPreviewData(null);
    setDragValue(null);
    if (!commit || !onUpdateElement) {
      return;
    }
    if (active?.moved && active.lastData) {
      onUpdateElement({ chartData: active.lastData });
    } else if (markActive?.moved && markActive.lastData) {
      onUpdateElement({ chartData: markActive.lastData });
    }
  };
  useEffect(() => {
    if (dragValue === null) {
      return;
    }
    const onKeyDown = (e) => {
      if (e.key === "Escape") {
        endDrag(false);
      }
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  });
  const handlePointerDown = (e) => {
    if (!canEdit) {
      return;
    }
    const part = findChartPartTarget(e.target);
    if (!part) {
      return;
    }
    e.stopPropagation();
    setSelection({ elementId: element.id, part });
    if (!viewModel || !element.chartData) {
      return;
    }
    let captured = false;
    const chartKind = resolveChartKind(element.chartData.chartType ?? "bar");
    if (part.pointIndex !== void 0 && chartKind !== "unsupported") {
      const markGeometry = buildChartMarkDragGeometry({
        kind: chartKind,
        element,
        chartData: element.chartData,
        categoryLabels: element.chartData.categories,
        seriesIndex: part.seriesIndex,
        pointIndex: part.pointIndex
      });
      const startedMark = beginChartMarkDrag({
        part,
        geometry: markGeometry,
        chartData: element.chartData,
        svgWidth: viewModel.svgWidth,
        svgHeight: viewModel.svgHeight,
        clientX: e.clientX,
        clientY: e.clientY
      });
      if (startedMark) {
        markDragRef.current = startedMark;
        captured = true;
      }
    }
    if (!captured) {
      const started = beginChartValueDrag({
        part,
        viewModel,
        chartData: element.chartData,
        clientY: e.clientY
      });
      if (started) {
        dragRef.current = started;
        captured = true;
      }
    }
    if (!captured) {
      return;
    }
    e.preventDefault();
    try {
      e.currentTarget.setPointerCapture?.(e.pointerId);
    } catch {
    }
  };
  const handlePointerMove = (e) => {
    const markActive = markDragRef.current;
    if (markActive) {
      const rect = wrapperRef.current?.querySelector("svg")?.getBoundingClientRect();
      if (!rect) {
        return;
      }
      const step2 = advanceChartMarkDrag(markActive, e.clientX, e.clientY, rect);
      if (!step2) {
        return;
      }
      setPreviewData(step2.chartData);
      setDragValue(step2.value);
      return;
    }
    const active = dragRef.current;
    if (!active) {
      return;
    }
    const height = wrapperRef.current?.querySelector("svg")?.getBoundingClientRect().height ?? 0;
    const step = advanceChartValueDrag(active, e.clientY, height);
    if (!step) {
      return;
    }
    setPreviewData(step.chartData);
    setDragValue(step.value);
  };
  const handlePointerUp = () => {
    if (dragRef.current || markDragRef.current) {
      endDrag(true);
    }
  };
  return {
    previewData,
    dragValue,
    setDragValue,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp
  };
}
function ChartElementView({
  element,
  editable,
  onUpdateElement,
  animationState
}) {
  const wrapperRef = useRef(null);
  const { selection, setSelection } = useChartPartSelection();
  const [titleDraft, setTitleDraft] = useState(null);
  const selectedPart = selection?.elementId === element.id ? selection.part : null;
  const canEdit = editable && Boolean(onUpdateElement);
  const use3D = useContext(SurfaceChart3DContext);
  const isSurfaceKind = resolveChartKind(element.chartData?.chartType ?? "bar") === "surface";
  const use3DBar = useContext(BarChart3DContext);
  const isBar3DKind = element.chartData?.chartType === "bar3D";
  const use3DLine = useContext(LineChart3DContext);
  const isLine3DKind = element.chartData?.chartType === "line3D";
  const use3DArea = useContext(AreaChart3DContext);
  const isArea3DKind = element.chartData?.chartType === "area3D";
  const use3DPie = useContext(PieChart3DContext);
  const isPie3DKind = element.chartData?.chartType === "pie3D";
  const barFacePictureSampleVersion = useSyncExternalStore(
    subscribeBarFacePicturePixelSamples,
    getBarFacePicturePixelSampleVersion,
    getBarFacePicturePixelSampleVersion
  );
  const viewModel = useMemo(() => {
    return canEdit ? buildReactChartViewModel(element) : null;
  }, [canEdit, element, barFacePictureSampleVersion]);
  useEffect(ensureChartInteractionStyles, []);
  useEffect(() => {
    if (!onUpdateElement) {
      return;
    }
    if (!canEdit && selection?.elementId === element.id) {
      setSelection(null);
    }
  }, [canEdit, selection, element.id, setSelection, onUpdateElement]);
  useEffect(() => {
    applyChartPartHighlight(wrapperRef.current, selectedPart);
  });
  const {
    previewData,
    dragValue,
    setDragValue,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp
  } = useChartMarkInteraction({
    element,
    canEdit,
    onUpdateElement,
    viewModel,
    wrapperRef,
    setSelection
  });
  const chart3DInteraction = buildChart3DPartInteraction({
    element,
    canEdit,
    onUpdateElement,
    selection,
    setSelection,
    setDragValue
  });
  const handleDoubleClick = (e) => {
    if (!canEdit || !canDrillDown(element)) {
      return;
    }
    const target = e.target;
    if (typeof target.closest !== "function") {
      return;
    }
    if (target.closest("[data-chart-part='title']")) {
      e.stopPropagation();
      setTitleDraft(element.chartData?.title ?? "");
      return;
    }
    if (findChartPartTarget(e.target)) {
      e.stopPropagation();
    }
  };
  const commitTitle = () => {
    if (titleDraft !== null && element.chartData && onUpdateElement) {
      onUpdateElement({
        chartData: withChartTitle(element.chartData, titleDraft)
      });
    }
    setTitleDraft(null);
  };
  const baseChartData = previewData ?? element.chartData;
  const revealedChartData = baseChartData ? resolveRevealedChartData(baseChartData, animationState) : baseChartData;
  const renderedElement = revealedChartData === element.chartData ? element : { ...element, chartData: revealedChartData };
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ref: wrapperRef,
      className: `relative w-full h-full ${canEdit ? "pptx-chart-interactive" : ""}`,
      onPointerDown: handlePointerDown,
      onPointerMove: handlePointerMove,
      onPointerUp: handlePointerUp,
      onDoubleClick: handleDoubleClick,
      children: [
        use3D && isSurfaceKind ? /* @__PURE__ */ jsx(
          SurfaceChart3DRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState?.textStyle
          }
        ) : use3DBar && isBar3DKind ? /* @__PURE__ */ jsx(
          Bar3DChartRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState?.textStyle
          }
        ) : use3DLine && isLine3DKind ? /* @__PURE__ */ jsx(
          Line3DChartRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState?.textStyle
          }
        ) : use3DArea && isArea3DKind ? /* @__PURE__ */ jsx(
          Area3DChartRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState?.textStyle
          }
        ) : use3DPie && isPie3DKind ? /* @__PURE__ */ jsx(
          PieChart3DRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart
          }
        ) : renderChartElement(renderedElement),
        dragValue !== null && /* @__PURE__ */ jsx("div", { className: "absolute top-1 right-1 z-10 rounded bg-primary/90 px-1.5 py-0.5 text-[10px] font-medium text-white pointer-events-none", children: formatAxisValue(dragValue) }),
        titleDraft !== null && /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            autoFocus: true,
            value: titleDraft,
            className: "absolute left-1/2 top-0.5 z-10 w-3/5 -translate-x-1/2 rounded border border-border bg-background px-1 py-0.5 text-center text-[11px] text-foreground shadow",
            onChange: (e) => setTitleDraft(e.target.value),
            onPointerDown: (e) => e.stopPropagation(),
            onDoubleClick: (e) => e.stopPropagation(),
            onKeyDown: (e) => {
              if (e.key === "Enter") {
                commitTitle();
              } else if (e.key === "Escape") {
                setTitleDraft(null);
              }
              e.stopPropagation();
            },
            onBlur: commitTitle
          }
        )
      ]
    }
  );
}
function ColorChangedImage({
  src,
  clrChange,
  tolerancePct,
  style,
  className,
  alt
}) {
  const tolerance = tolerancePct ?? DEFAULT_COLOR_CHANGE_TOLERANCE;
  const cacheKey = buildCacheKey(
    src,
    clrChange.clrFrom,
    clrChange.clrTo,
    tolerance,
    Boolean(clrChange.clrToTransparent)
  );
  const cached = getCachedResult(cacheKey);
  const [processedSrc, setProcessedSrc] = useState(cached ?? null);
  useEffect(() => {
    if (getCachedResult(cacheKey)) {
      setProcessedSrc(getCachedResult(cacheKey) ?? null);
      return;
    }
    let cancelled = false;
    applyColorChange(
      src,
      clrChange.clrFrom,
      clrChange.clrTo,
      tolerance,
      Boolean(clrChange.clrToTransparent)
    ).then((result) => {
      if (!cancelled) {
        setCachedResult(cacheKey, result.dataUrl);
        setProcessedSrc(result.dataUrl);
      }
      return void 0;
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [src, cacheKey, clrChange.clrFrom, clrChange.clrTo, tolerance, clrChange.clrToTransparent]);
  return /* @__PURE__ */ jsx(
    "img",
    {
      src: processedSrc ?? src,
      alt,
      className,
      style,
      draggable: false,
      onError: (e) => {
        e.currentTarget.style.display = "none";
      }
    }
  );
}
function DuotoneImage({
  src,
  duotone,
  style,
  className,
  alt
}) {
  const cacheKey = buildDuotoneCacheKey(src, duotone.color1, duotone.color2);
  const cached = getDuotoneCachedResult(cacheKey);
  const [processedSrc, setProcessedSrc] = useState(cached ?? null);
  useEffect(() => {
    const cachedResult = getDuotoneCachedResult(cacheKey);
    if (cachedResult) {
      setProcessedSrc(cachedResult);
      return;
    }
    let cancelled = false;
    applyDuotone(src, duotone.color1, duotone.color2).then((result) => {
      if (!cancelled) {
        setDuotoneCachedResult(cacheKey, result.dataUrl);
        setProcessedSrc(result.dataUrl);
      }
      return void 0;
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [src, cacheKey, duotone.color1, duotone.color2]);
  return /* @__PURE__ */ jsx(
    "img",
    {
      src: processedSrc ?? src,
      alt,
      className,
      style,
      draggable: false,
      onError: (e) => {
        e.currentTarget.style.display = "none";
      }
    }
  );
}
function renderImageEffectDefinitions(element, excludeDuotone = false) {
  return getImageSvgFilters(element).filter((definition) => !excludeDuotone || definition.id !== `duotone-${element.id}`).map((definition) => /* @__PURE__ */ jsx(
    "svg",
    {
      width: 0,
      height: 0,
      style: { position: "absolute", overflow: "hidden" },
      "aria-hidden": "true",
      children: /* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx(
        "filter",
        {
          id: definition.id,
          colorInterpolationFilters: "sRGB",
          dangerouslySetInnerHTML: { __html: definition.markup }
        }
      ) })
    },
    definition.id
  ));
}
function imgSrc(el) {
  if ("svgData" in el && el.svgData) {
    return el.svgData;
  }
  return "imageData" in el ? el.imageData : void 0;
}
function renderImg(el, style, filter, alt, opacity) {
  const src = imgSrc(el);
  if (!src) {
    const imagePath = "imagePath" in el ? el.imagePath : void 0;
    if (imagePath) {
      const ext = imagePath.split(".").pop()?.toLowerCase();
      if (ext === "emf" || ext === "wmf") {
        return /* @__PURE__ */ jsxs("div", { className: "w-full h-full flex flex-col items-center justify-center text-[11px] text-gray-400 bg-gray-50 border border-dashed border-gray-200 rounded", children: [
          /* @__PURE__ */ jsxs(
            "svg",
            {
              xmlns: "http://www.w3.org/2000/svg",
              width: "24",
              height: "24",
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "1.5",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              className: "mb-1 text-gray-300",
              children: [
                /* @__PURE__ */ jsx("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2", ry: "2" }),
                /* @__PURE__ */ jsx("circle", { cx: "8.5", cy: "8.5", r: "1.5" }),
                /* @__PURE__ */ jsx("polyline", { points: "21 15 16 10 5 21" })
              ]
            }
          ),
          /* @__PURE__ */ jsxs("span", { children: [
            ext.toUpperCase(),
            " metafile"
          ] })
        ] });
      }
    }
    return /* @__PURE__ */ jsx("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-gray-500 bg-gray-100", children: translationsEn["pptx.elementType.image"] });
  }
  const effectStyles = {
    ...filter ? { filter } : {},
    ...opacity !== void 0 ? { opacity } : {}
  };
  if (isImageTiled(el)) {
    const tileSrc = imgSrc(el);
    if (!tileSrc) {
      return /* @__PURE__ */ jsx("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-gray-500 bg-gray-100", children: "Image" });
    }
    return /* @__PURE__ */ jsxs(Fragment, { children: [
      renderImageEffectDefinitions(el),
      /* @__PURE__ */ jsx(
        "div",
        {
          className: "pointer-events-none select-none w-full h-full",
          style: { ...getImageTilingStyle(el), ...effectStyles }
        }
      )
    ] });
  }
  const colorWash = getImageColorWashStyle(
    isImageLikeElement(el) ? el.imageEffects?.colorWash : void 0
  );
  const clrChange = "imageEffects" in el ? el.imageEffects?.clrChange : void 0;
  const duotoneColors = getDuotoneColors(el);
  const useDuotoneCanvas = Boolean(duotoneColors && !clrChange);
  const filterForImg = useDuotoneCanvas ? getImageEffectsFilter(el, { excludeDuotone: true }) : filter;
  const canvasEffectStyles = {
    ...filterForImg ? { filter: filterForImg } : {},
    ...opacity !== void 0 ? { opacity } : {}
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    renderImageEffectDefinitions(el, useDuotoneCanvas),
    useDuotoneCanvas && duotoneColors ? /* @__PURE__ */ jsx(
      DuotoneImage,
      {
        src,
        duotone: duotoneColors,
        alt,
        className: "pointer-events-none select-none",
        style: { ...style, ...canvasEffectStyles }
      }
    ) : clrChange ? /* @__PURE__ */ jsx(
      ColorChangedImage,
      {
        src,
        clrChange,
        alt,
        className: "pointer-events-none select-none",
        style: { ...style, ...effectStyles }
      }
    ) : /* @__PURE__ */ jsx(
      "img",
      {
        src,
        alt,
        className: "pointer-events-none select-none",
        style: { ...style, ...effectStyles },
        draggable: false,
        onError: (e) => {
          e.currentTarget.style.display = "none";
        }
      }
    ),
    colorWash && /* @__PURE__ */ jsx(
      "div",
      {
        className: "pointer-events-none absolute inset-0",
        style: {
          backgroundColor: colorWash.backgroundColor,
          opacity: colorWash.opacity
        }
      }
    )
  ] });
}
function useActionAffordance(element, canInteract) {
  const { t } = useTranslation();
  ensureActionAffordanceStyles();
  return useMemo(
    () => resolveElementActionAffordance(element, {
      canInteract,
      // Wrapped rather than passed by reference: i18next's `TFunction` is
      // heavily overloaded and does not assign to a plain key -> string.
      labels: actionAffordanceLabels((key) => t(key))
    }),
    [element, canInteract, t]
  );
}
function ActionIndicator({ title }) {
  return /* @__PURE__ */ jsx("div", { className: ACTION_INDICATOR_CLASS, title, children: /* @__PURE__ */ jsx("svg", { viewBox: "0 0 24 24", fill: "currentColor", "aria-hidden": "true", children: /* @__PURE__ */ jsx("path", { d: ACTION_INDICATOR_ICON_PATH }) }) });
}
function LinkTooltip({ label, hint }) {
  return /* @__PURE__ */ jsx("div", { className: LINK_TOOLTIP_CLASS, children: /* @__PURE__ */ jsxs("div", { className: LINK_TOOLTIP_PANEL_CLASS, children: [
    /* @__PURE__ */ jsx("div", { className: LINK_TOOLTIP_LABEL_CLASS, children: label }),
    /* @__PURE__ */ jsx("div", { className: LINK_TOOLTIP_HINT_CLASS, children: hint })
  ] }) });
}
function ActionAffordances({
  affordance
}) {
  if (!affordance.showIndicator && !affordance.showLinkTooltip) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    affordance.showIndicator && /* @__PURE__ */ jsx(ActionIndicator, { title: affordance.indicatorTitle }),
    affordance.showLinkTooltip && /* @__PURE__ */ jsx(LinkTooltip, { label: affordance.linkTooltipLabel, hint: affordance.linkTooltipHint })
  ] });
}
function ConnectorTextOverlay({
  connectorText,
  connectorTextSegments,
  connectorTextStyle
}) {
  if (!connectorText || !connectorTextSegments || connectorTextSegments.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "absolute inset-0 flex items-center justify-center overflow-hidden",
      style: {
        pointerEvents: "none",
        textAlign: (connectorTextStyle?.align === "justLow" || connectorTextStyle?.align === "dist" || connectorTextStyle?.align === "thaiDist" ? "justify" : connectorTextStyle?.align) ?? "center"
      },
      children: /* @__PURE__ */ jsx(
        "div",
        {
          className: "px-1",
          style: {
            fontFamily: connectorTextStyle?.fontFamily ?? "inherit",
            fontSize: connectorTextStyle?.fontSize ? `${connectorTextStyle.fontSize}px` : "10px",
            color: connectorTextStyle?.color ?? "#000000",
            fontWeight: connectorTextStyle?.bold ? "bold" : "normal",
            fontStyle: connectorTextStyle?.italic ? "italic" : "normal",
            textDecoration: connectorTextStyle?.underline ? "underline" : "none",
            whiteSpace: "pre-wrap",
            lineHeight: 1.2,
            maxWidth: "100%"
          },
          children: connectorTextSegments.map((seg, idx) => /* @__PURE__ */ jsx(
            "span",
            {
              style: {
                fontFamily: seg.style?.fontFamily ?? connectorTextStyle?.fontFamily ?? "inherit",
                fontSize: seg.style?.fontSize ? `${seg.style.fontSize}px` : void 0,
                color: seg.style?.color ?? connectorTextStyle?.color ?? "#000000",
                fontWeight: seg.style?.bold ? "bold" : connectorTextStyle?.bold ? "bold" : "normal",
                fontStyle: seg.style?.italic ? "italic" : connectorTextStyle?.italic ? "italic" : "normal",
                textDecoration: seg.style?.underline ? "underline" : "none"
              },
              children: seg.text
            },
            idx
          ))
        }
      )
    }
  );
}
var HANDLE_TOUCH_ACTION = { touchAction: "none" };
var CORNER_HANDLES = [
  {
    handle: "nw",
    posClass: "-left-1.5 -top-1.5 max-md:-left-2.5 max-md:-top-2.5",
    cursor: "cursor-nwse-resize"
  },
  {
    handle: "ne",
    posClass: "-right-1.5 -top-1.5 max-md:-right-2.5 max-md:-top-2.5",
    cursor: "cursor-nesw-resize"
  },
  {
    handle: "sw",
    posClass: "-left-1.5 -bottom-1.5 max-md:-left-2.5 max-md:-bottom-2.5",
    cursor: "cursor-nesw-resize"
  },
  {
    handle: "se",
    posClass: "-right-1.5 -bottom-1.5 max-md:-right-2.5 max-md:-bottom-2.5",
    cursor: "cursor-nwse-resize"
  }
];
var EDGE_HANDLES = [
  {
    handle: "n",
    posClass: "top-0 left-1/2 -translate-x-1/2 -translate-y-1/2",
    cursor: "cursor-ns-resize",
    sizeClass: "w-5 h-2 max-md:w-8 max-md:h-3 rounded-sm"
  },
  {
    handle: "s",
    posClass: "bottom-0 left-1/2 -translate-x-1/2 translate-y-1/2",
    cursor: "cursor-ns-resize",
    sizeClass: "w-5 h-2 max-md:w-8 max-md:h-3 rounded-sm"
  },
  {
    handle: "e",
    posClass: "right-0 top-1/2 translate-x-1/2 -translate-y-1/2",
    cursor: "cursor-ew-resize",
    sizeClass: "w-2 h-5 max-md:w-3 max-md:h-8 rounded-sm"
  },
  {
    handle: "w",
    posClass: "left-0 top-1/2 -translate-x-1/2 -translate-y-1/2",
    cursor: "cursor-ew-resize",
    sizeClass: "w-2 h-5 max-md:w-3 max-md:h-8 rounded-sm"
  }
];
function ResizeHandles({
  elementId,
  adjustmentHandles,
  onResizePointerDown,
  onAdjustmentPointerDown,
  forcePointerEvents,
  rotation,
  nonRotationTransform,
  onRotate
}) {
  const { t } = useTranslation();
  const peStyle = forcePointerEvents ? { ...HANDLE_TOUCH_ACTION, pointerEvents: "auto" } : HANDLE_TOUCH_ACTION;
  const handleResizePointer = (e, handle) => {
    if (e.pointerType === "mouse") {
      return;
    }
    e.stopPropagation();
    e.currentTarget.setPointerCapture?.(e.pointerId);
    onResizePointerDown(elementId, e, handle);
  };
  const startRotate = (btn, pointerId) => {
    const scope = btn.closest("[data-pptx-viewport]") ?? document;
    const wrapper = scope.querySelector(elementIdSelector(elementId));
    if (!wrapper) {
      return;
    }
    const rect = wrapper.getBoundingClientRect();
    const cx = rect.left + rect.width / 2;
    const cy = rect.top + rect.height / 2;
    const base = nonRotationTransform ? ` ${nonRotationTransform}` : "";
    const startDeg = rotation ?? 0;
    let last = startDeg;
    if (pointerId !== void 0) {
      btn.setPointerCapture?.(pointerId);
    }
    const apply = (clientX, clientY, shift) => {
      let deg = Math.atan2(clientY - cy, clientX - cx) * 180 / Math.PI + 90;
      if (shift) {
        deg = Math.round(deg / 15) * 15;
      }
      deg = Math.round((deg % 360 + 360) % 360);
      last = deg;
      const transform = `rotate(${deg}deg)${base}`;
      wrapper.style.transform = transform;
      syncSelectionHandleOverlay(wrapper, elementId, { transform });
    };
    const onPointerMove = (ev) => apply(ev.clientX, ev.clientY, ev.shiftKey);
    const end = () => {
      window.removeEventListener("pointermove", onPointerMove);
      window.removeEventListener("pointerup", end);
      window.removeEventListener("pointercancel", end);
      if (last !== startDeg) {
        onRotate?.(elementId, last);
      }
    };
    window.addEventListener("pointermove", onPointerMove);
    window.addEventListener("pointerup", end);
    window.addEventListener("pointercancel", end);
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    CORNER_HANDLES.map(({ handle, posClass, cursor }) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.selectionOverlay.resize", { handle }),
        className: cn("absolute z-10 group", posClass, cursor),
        style: peStyle,
        onPointerDown: (e) => handleResizePointer(e, handle),
        onMouseDown: (e) => {
          e.stopPropagation();
          onResizePointerDown(elementId, e, handle);
        },
        children: [
          /* @__PURE__ */ jsx("div", { className: "w-3 h-3 max-md:w-5.5 max-md:h-5.5 rounded-full border border-white bg-primary shadow" }),
          /* @__PURE__ */ jsx("div", { className: "absolute -inset-1.5 max-md:-inset-1" })
        ]
      },
      handle
    )),
    EDGE_HANDLES.map(({ handle, posClass, cursor, sizeClass }) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.selectionOverlay.resize", { handle }),
        className: cn("absolute z-10", posClass, cursor),
        style: peStyle,
        onPointerDown: (e) => handleResizePointer(e, handle),
        onMouseDown: (e) => {
          e.stopPropagation();
          onResizePointerDown(elementId, e, handle);
        },
        children: [
          /* @__PURE__ */ jsx("div", { className: cn(sizeClass, "border border-white bg-primary shadow") }),
          /* @__PURE__ */ jsx("div", { className: "absolute -inset-2 max-md:-inset-1" })
        ]
      },
      handle
    )),
    onRotate ? /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.selectionOverlay.rotate"),
        "data-pptx-compact": true,
        className: "absolute left-1/2 top-0 -translate-x-1/2 -translate-y-1/2 z-20 flex items-center justify-center w-5 h-5 max-md:w-7 max-md:h-7 rounded-full border border-white bg-primary text-white shadow cursor-grab active:cursor-grabbing",
        style: peStyle,
        onPointerDown: (e) => {
          if (e.pointerType === "mouse") {
            return;
          }
          e.stopPropagation();
          startRotate(e.currentTarget, e.pointerId);
        },
        onMouseDown: (e) => {
          e.stopPropagation();
          startRotate(e.currentTarget);
        },
        children: [
          /* @__PURE__ */ jsx(LuRotateCw, { className: "w-3 h-3 max-md:w-4 max-md:h-4" }),
          /* @__PURE__ */ jsx("span", { className: "absolute -inset-2 max-md:-inset-1", "aria-hidden": "true" })
        ]
      }
    ) : null,
    adjustmentHandles.map((adjH) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.canvas.adjustShape"),
        "data-pptx-adjust-key": adjH.key,
        "data-pptx-compact": true,
        className: "absolute h-2.5 w-2.5 max-md:h-4 max-md:w-4 rotate-45 border border-amber-700 bg-amber-300 shadow z-10",
        style: {
          left: adjH.left - 5,
          top: adjH.top - 5,
          cursor: adjH.cursor,
          ...HANDLE_TOUCH_ACTION,
          ...forcePointerEvents ? { pointerEvents: "auto" } : {}
        },
        onPointerDown: (e) => {
          if (e.pointerType === "mouse") {
            return;
          }
          e.stopPropagation();
          e.currentTarget.setPointerCapture?.(e.pointerId);
          onAdjustmentPointerDown(elementId, e, adjH);
        },
        onMouseDown: (e) => {
          e.stopPropagation();
          onAdjustmentPointerDown(elementId, e, adjH);
        }
      },
      adjH.key
    ))
  ] });
}
var ConnectorElementRenderer = React20.memo(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the memo component its displayName
  function ConnectorElementRendererInner({
    el,
    isSelected,
    canInteract,
    showResizeHandles,
    showHoverBorder,
    selectionColorClass: selClr,
    opacity,
    zIndex,
    adjustmentHandles: adjH,
    onResizePointerDown,
    onAdjustmentPointerDown,
    animationState,
    textStyleOverrideCss
  }) {
    const shapeEl = hasShapeProperties(el) ? el : void 0;
    const boxWidth = Math.max(el.width, MIN_ELEMENT_SIZE);
    const boxHeight = Math.max(el.height, MIN_ELEMENT_SIZE);
    const viewWidth = Math.max(el.width, 0);
    const viewHeight = Math.max(el.height, 0);
    const ss = shapeEl?.shapeStyle;
    const strokeWidth = Math.max(0, ss?.strokeWidth ?? 2);
    const strokeColor = normalizeHexColor(ss?.strokeColor, DEFAULT_STROKE_COLOR);
    const strokePaint = colorWithOpacity(strokeColor, ss?.strokeOpacity);
    const dashType = normalizeStrokeDashType(ss?.strokeDash);
    const dashArray = getSvgStrokeDasharray(
      dashType,
      Math.max(strokeWidth, 1),
      ss?.customDashSegments
    );
    const startArrow = ss?.connectorStartArrow;
    const endArrow = ss?.connectorEndArrow;
    const compoundLine = ss?.compoundLine;
    const markerSeed = el.id.replace(/[^a-zA-Z0-9_-]/gu, "_");
    const startMarkerId = `${markerSeed}-sel-start`;
    const endMarkerId = `${markerSeed}-sel-end`;
    const compoundOffsets = getCompoundLineOffsets(compoundLine, strokeWidth);
    const compoundWidths = getCompoundLineWidths(compoundLine, strokeWidth);
    const strokeCap = svgLineCap(ss?.lineCap);
    const textEl = hasTextProperties(el) ? el : void 0;
    const connectorText = textEl?.text?.trim() ?? "";
    const connectorTextSegments = textEl?.textSegments;
    const connectorTextStyle = textEl?.textStyle;
    const pathGeometry = shapeEl ? getConnectorPathGeometry(shapeEl) : {
      pathData: `M 0 0 L ${viewWidth} ${viewHeight}`,
      startX: 0,
      startY: 0,
      endX: viewWidth,
      endY: viewHeight
    };
    const hitTargetWidth = Math.max(strokeWidth * 3, 14);
    const selColor = selClr === "blue-400" ? "#60a5fa" : "#3b82f6";
    const lineShadow = buildLineShadowCss(el);
    const lineGlow = buildLineGlowFilter(el);
    return /* @__PURE__ */ jsxs(
      "div",
      {
        "data-pptx-element": "true",
        "data-element-id": el.id,
        role: getAriaRole(el, { actionable: false }),
        "aria-label": getAriaLabel(el),
        "aria-roledescription": getAriaRoleDescription(el),
        "aria-selected": isSelected ? true : void 0,
        className: "absolute",
        style: {
          left: el.x,
          top: el.y,
          width: boxWidth,
          height: boxHeight,
          transform: connectorWrapperTransform(el),
          transformOrigin: "center",
          background: "transparent",
          border: "none",
          pointerEvents: "none",
          opacity,
          zIndex,
          visibility: animationState?.visible === false ? "hidden" : "visible",
          animation: animationState?.cssAnimation,
          ...lineGlow ? { filter: lineGlow } : {}
        },
        children: [
          textStyleOverrideCss && /* @__PURE__ */ jsx("style", { children: textStyleOverrideCss }),
          /* @__PURE__ */ jsxs(
            "svg",
            {
              viewBox: `0 0 ${boxWidth} ${boxHeight}`,
              className: "w-full h-full",
              preserveAspectRatio: "none",
              style: { overflow: "visible", pointerEvents: "none" },
              children: [
                /* @__PURE__ */ jsxs("defs", { children: [
                  renderConnectorMarker(
                    startMarkerId,
                    startArrow,
                    strokePaint,
                    ss?.connectorStartArrowWidth,
                    ss?.connectorStartArrowLength
                  ),
                  renderConnectorMarker(
                    endMarkerId,
                    endArrow,
                    strokePaint,
                    ss?.connectorEndArrowWidth,
                    ss?.connectorEndArrowLength
                  ),
                  lineShadow && /* @__PURE__ */ jsx("filter", { id: `${markerSeed}-line-shadow`, x: "-50%", y: "-50%", width: "200%", height: "200%", children: /* @__PURE__ */ jsx(
                    "feDropShadow",
                    {
                      dx: ss?.lineShadowOffsetX ?? 2,
                      dy: ss?.lineShadowOffsetY ?? 2,
                      stdDeviation: Math.max(0, (ss?.lineShadowBlur ?? 4) / 2),
                      floodColor: ss?.lineShadowColor ?? "#000000",
                      floodOpacity: ss?.lineShadowOpacity ?? 0.35
                    }
                  ) })
                ] }),
                isSelected && /* @__PURE__ */ jsx(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: selColor,
                    strokeWidth: Math.max(strokeWidth, 2) + 6,
                    strokeOpacity: 0.35,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    vectorEffect: "non-scaling-stroke",
                    style: { pointerEvents: "none" }
                  }
                ),
                !isSelected && showHoverBorder && /* @__PURE__ */ jsx(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: "#93c5fd",
                    strokeWidth: Math.max(strokeWidth, 2) + 4,
                    strokeOpacity: 0,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    vectorEffect: "non-scaling-stroke",
                    className: "transition-[stroke-opacity] duration-150 group-hover:stroke-opacity-40",
                    style: { pointerEvents: "none" }
                  }
                ),
                /* @__PURE__ */ jsx(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: "transparent",
                    strokeWidth: hitTargetWidth,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    style: {
                      pointerEvents: "stroke",
                      cursor: canInteract ? "move" : "default"
                    }
                  }
                ),
                compoundOffsets.map((offset, idx) => /* @__PURE__ */ jsx(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: strokePaint,
                    strokeWidth: Math.max(compoundWidths[idx] ?? strokeWidth, 1),
                    strokeDasharray: dashArray,
                    strokeLinecap: strokeCap,
                    strokeLinejoin: "round",
                    markerStart: idx === 0 && startArrow && startArrow !== "none" ? `url(#${startMarkerId})` : void 0,
                    markerEnd: idx === compoundOffsets.length - 1 && endArrow && endArrow !== "none" ? `url(#${endMarkerId})` : void 0,
                    vectorEffect: "non-scaling-stroke",
                    filter: idx === 0 && lineShadow ? `url(#${markerSeed}-line-shadow)` : void 0,
                    style: {
                      pointerEvents: "none",
                      ...offset !== 0 ? {
                        transform: `translate(0, ${offset}px)`
                      } : {}
                    }
                  },
                  idx
                )),
                isSelected && /* @__PURE__ */ jsxs(Fragment, { children: [
                  /* @__PURE__ */ jsx(
                    "circle",
                    {
                      cx: pathGeometry.startX,
                      cy: pathGeometry.startY,
                      r: 4,
                      fill: selColor,
                      stroke: "white",
                      strokeWidth: 1.5,
                      style: { pointerEvents: "none" }
                    }
                  ),
                  /* @__PURE__ */ jsx(
                    "circle",
                    {
                      cx: pathGeometry.endX,
                      cy: pathGeometry.endY,
                      r: 4,
                      fill: selColor,
                      stroke: "white",
                      strokeWidth: 1.5,
                      style: { pointerEvents: "none" }
                    }
                  )
                ] })
              ]
            }
          ),
          connectorTextSegments && /* @__PURE__ */ jsx(
            ConnectorTextOverlay,
            {
              connectorText,
              connectorTextSegments,
              connectorTextStyle
            }
          ),
          showResizeHandles && /* @__PURE__ */ jsx(
            ResizeHandles,
            {
              elementId: el.id,
              adjustmentHandles: adjH,
              onResizePointerDown,
              onAdjustmentPointerDown,
              forcePointerEvents: true
            }
          )
        ]
      }
    );
  }
);
ConnectorElementRenderer.displayName = "ConnectorElementRenderer";

// src/viewer/components/elements/element-interaction-props.ts
function getElementInteractionProps({
  element,
  isEditableText,
  canInteract,
  isInlineEditing,
  isActionable,
  isPresentationPassive,
  onInlineEditCancel,
  onActionClick
}) {
  const hasHoverAction = Boolean(element.actionHover);
  const highlight = resolveElementHighlightClick(element.actionClick, element.actionHover);
  const hoverLeaveStyle = highlight.hover?.leaveStyle;
  return {
    onKeyDown: (event) => {
      if (event.key === "Enter" && isEditableText && canInteract && !isInlineEditing) {
        event.preventDefault();
        event.stopPropagation();
        event.currentTarget.dispatchEvent(new MouseEvent("dblclick", { bubbles: true }));
      } else if (event.key === "Escape" && isInlineEditing) {
        event.preventDefault();
        event.stopPropagation();
        onInlineEditCancel();
      } else if ((event.key === "Enter" || event.key === " ") && isActionable) {
        event.preventDefault();
        event.stopPropagation();
        event.currentTarget.click();
      }
    },
    onClick: (event) => {
      if (!element.actionClick || !onActionClick) {
        return;
      }
      const shouldTrigger = !canInteract || event.ctrlKey || event.metaKey;
      if (!shouldTrigger) {
        return;
      }
      event.stopPropagation();
      event.preventDefault();
      if (highlight.click) {
        const target = event.currentTarget;
        const { style, clearStyle, durationMs } = highlight.click;
        applyHighlightClickStyle(target, style);
        window.setTimeout(() => {
          applyHighlightClickStyle(target, clearStyle);
        }, durationMs);
      }
      onActionClick(element.id, element.actionClick);
    },
    onMouseEnter: (event) => {
      if (highlight.hover) {
        applyHighlightClickStyle(event.currentTarget, highlight.hover.enterStyle);
      }
      if (isPresentationPassive && element.actionHover && onActionClick && (element.actionHover.url || element.actionHover.targetSlideIndex !== void 0)) {
        onActionClick(element.id, element.actionHover);
      }
    },
    onMouseLeave: hasHoverAction && hoverLeaveStyle ? (event) => {
      applyHighlightClickStyle(event.currentTarget, hoverLeaveStyle);
    } : void 0,
    title: canInteract && element.actionClick ? void 0 : element.actionClick?.tooltip || element.actionHover?.tooltip || void 0
  };
}

// src/viewer/components/elements/element-renderer-helpers.tsx
function renderDagDuotoneFilterForElement(el) {
  if (!hasDagDuotoneEffect(el)) {
    return null;
  }
  const duotone = el.shapeStyle?.dagDuotone;
  if (!duotone) {
    return null;
  }
  return renderDagDuotoneSvgFilter(el.id, duotone.color1, duotone.color2);
}
function getContainerStyle({
  el,
  isFullscreenMedia,
  isImg,
  zIndex,
  opacity,
  animationState,
  shapeVisualStyle,
  has3DExtrusion,
  templateEditing
}) {
  const ss = hasShapeProperties(el) ? el.shapeStyle : void 0;
  const isPicture = el.type === "picture" || el.type === "image";
  const blurGrowVisible = Boolean(
    ss?.blurGrow && typeof ss.blurRadius === "number" && ss.blurRadius > 0
  );
  const overflowValue = has3DExtrusion || blurGrowVisible || isPicture ? "visible" : isImg || el.type === "media" ? "hidden" : void 0;
  return {
    left: isFullscreenMedia ? 0 : el.x,
    top: isFullscreenMedia ? 0 : el.y,
    width: isFullscreenMedia ? "100%" : Math.max(el.width, MIN_ELEMENT_SIZE),
    height: isFullscreenMedia ? "100%" : Math.max(el.height, MIN_ELEMENT_SIZE),
    transform: isFullscreenMedia ? "none" : getElementTransform(el),
    transformOrigin: "center",
    overflow: overflowValue,
    clipPath: isPicture ? void 0 : isImg && !has3DExtrusion ? getCropShapeClipPath(el) : void 0,
    zIndex: isFullscreenMedia ? 20 : zIndex,
    visibility: animationState?.visible === false ? "hidden" : "visible",
    animation: animationState?.cssAnimation,
    background: isFullscreenMedia ? "#000" : void 0,
    transition: isFullscreenMedia ? "left 0.3s ease, top 0.3s ease, width 0.3s ease, height 0.3s ease" : void 0,
    borderColor: isFullscreenMedia ? "transparent" : void 0,
    ...shapeVisualStyle,
    ...isPicture ? {
      backgroundColor: "transparent",
      backgroundImage: void 0,
      backgroundRepeat: void 0,
      backgroundSize: void 0,
      backgroundPosition: void 0,
      borderRadius: void 0,
      clipPath: void 0,
      overflow: "visible"
    } : {},
    // COMPOSE the effect alpha (`a:alphaModFix` on the effect DAG) with the
    // element opacity instead of letting the spread clobber it. The other four
    // bindings multiply the two; React spread the shape style over its own
    // `opacity` key, so a half-transparent element carrying a DAG alpha
    // rendered at the DAG's alpha alone.
    opacity: typeof shapeVisualStyle.opacity === "number" ? (opacity ?? 1) * shapeVisualStyle.opacity : opacity,
    // Editable-template affordance: a distinct amber dashed ring + slight
    // transparency so inherited master/layout shapes read as "template" while
    // edit-template mode is on. Applied after the shape style so it wins; never
    // set for normal slide content or while the mode is off.
    ...templateEditing ? { outline: "2px dashed rgb(217, 119, 6)", outlineOffset: "1px", opacity: opacity ?? 0.95 } : {}
  };
}

// src/viewer/components/elements/element-shape-params.ts
function shapeParams(element) {
  const style = hasShapeProperties(element) ? element.shapeStyle : void 0;
  const strokeWidth = paintedStrokeWidth(style);
  const strokeColor = normalizeHexColor(style?.strokeColor, DEFAULT_STROKE_COLOR);
  const fillColor = normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR);
  const hasFill = style?.fillColor !== void 0 && style.fillColor !== "transparent" || Boolean(buildCssGradientFromShapeStyle(style) || style?.fillGradient) || style?.fillMode === "pattern" && Boolean(style.fillPatternPreset);
  return { hf: hasFill, fc: fillColor, sw: strokeWidth, sc: strokeColor };
}
var Extrusion3DOverlay = React20.memo(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the memo component its displayName
  function Extrusion3DOverlayInner({ data }) {
    if (!data.hasExtrusion || data.panels.length === 0) {
      return null;
    }
    return /* @__PURE__ */ jsxs("div", { className: "extrusion-3d-wrapper", style: data.wrapperStyle, "aria-hidden": "true", children: [
      data.panels.map((panel) => /* @__PURE__ */ jsx(
        "div",
        {
          className: `extrusion-3d-panel extrusion-3d-panel--${panel.side}`,
          style: panel.style
        },
        panel.side
      )),
      data.materialOverlay && /* @__PURE__ */ jsx(
        "div",
        {
          className: "extrusion-3d-material-overlay",
          style: {
            position: "absolute",
            inset: 0,
            backgroundImage: data.materialOverlay,
            pointerEvents: "none",
            borderRadius: "inherit",
            transform: data.frontFaceStyle.transform,
            transformStyle: "preserve-3d",
            backfaceVisibility: "hidden",
            mixBlendMode: "normal"
          }
        }
      )
    ] });
  }
);
Extrusion3DOverlay.displayName = "Extrusion3DOverlay";

// src/viewer/components/elements/scoped-element-handlers.ts
function getScopedElementHandlers(elementId, options) {
  const cellSelectHandler = options.onTableCellSelect ? (cell) => options.onTableCellSelect?.(cell, elementId) : void 0;
  const cellCommitHandler = options.onCommitCellEdit ? (rowIndex, colIndex, text) => options.onCommitCellEdit?.(elementId, rowIndex, colIndex, text) : void 0;
  const colResizeHandler = options.onResizeTableColumns ? (newWidths) => options.onResizeTableColumns?.(elementId, newWidths) : void 0;
  const rowResizeHandler = options.onResizeTableRow ? (rowIndex, newHeight) => options.onResizeTableRow?.(elementId, rowIndex, newHeight) : void 0;
  const smartArtUpdateHandler = options.onUpdateSmartArtElement ? (updates) => options.onUpdateSmartArtElement?.(elementId, updates) : void 0;
  return {
    cellSelectHandler,
    cellCommitHandler,
    colResizeHandler,
    rowResizeHandler,
    smartArtUpdateHandler
  };
}
function renderOutlinePaint(paint) {
  if (paint.kind === "pattern") {
    return /* @__PURE__ */ jsx(
      "pattern",
      {
        id: paint.id,
        width: paint.width,
        height: paint.height,
        patternUnits: "userSpaceOnUse",
        children: /* @__PURE__ */ jsx("image", { href: paint.href, width: paint.width, height: paint.height })
      }
    );
  }
  if (paint.kind === "rectPath") {
    return /* @__PURE__ */ jsx("pattern", { id: paint.id, patternUnits: "objectBoundingBox", width: 1, height: 1, children: /* @__PURE__ */ jsx("image", { href: paint.href, x: 0, y: 0, width: 1, height: 1, preserveAspectRatio: "none" }) });
  }
  const stops = paint.stops.map((stop, idx) => /* @__PURE__ */ jsx(
    "stop",
    {
      offset: stop.offset,
      stopColor: stop.color,
      stopOpacity: typeof stop.opacity === "number" ? stop.opacity : void 0
    },
    idx
  ));
  if (paint.kind === "radial") {
    return /* @__PURE__ */ jsx("radialGradient", { id: paint.id, cx: paint.cx, cy: paint.cy, r: paint.r, children: stops });
  }
  return /* @__PURE__ */ jsx("linearGradient", { id: paint.id, x1: paint.x1, y1: paint.y1, x2: paint.x2, y2: paint.y2, children: stops });
}
function ShapeEffectOverlay({
  element,
  animatesFill = false,
  animatesStroke = false,
  suppressReflection = false
}) {
  const fx = getComputedEffectStyle(element);
  const overlay = fx.fillOverlay;
  const reflection = suppressReflection ? void 0 : fx.reflection;
  const softEdge = getSoftEdgeSvgFilter(getEffectStyleSource(element), element.id);
  const strokeOutline = buildStrokeOutline(element);
  const hollowHit = buildHollowHitOutline(element);
  const subpathFill = buildSubpathFillOverlay(element);
  if (!overlay && !softEdge && !strokeOutline && !hollowHit && !subpathFill && !reflection) {
    return null;
  }
  const fillOverlayStyle = overlay ? {
    position: "absolute",
    inset: 0,
    ...isImageLikeElement(element) ? getImageSurfaceMaskStyle(element) : {},
    background: overlay.color,
    mixBlendMode: overlay.blendMode,
    pointerEvents: "none"
  } : void 0;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    subpathFill ? /* @__PURE__ */ jsx(
      "svg",
      {
        className: "pptx-react-subpath-fill",
        "aria-hidden": "true",
        viewBox: `0 0 ${subpathFill.viewBoxWidth} ${subpathFill.viewBoxHeight}`,
        preserveAspectRatio: "none",
        style: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%"
        },
        children: subpathFill.paints.map((paint, idx) => /* @__PURE__ */ jsx(
          "path",
          {
            d: paint.d,
            fill: animatesFill ? "inherit" : paint.fill,
            stroke: "none"
          },
          idx
        ))
      }
    ) : null,
    softEdge ? /* @__PURE__ */ jsx(
      "svg",
      {
        width: 0,
        height: 0,
        "aria-hidden": "true",
        style: {
          position: "absolute",
          width: 0,
          height: 0,
          overflow: "hidden"
        },
        children: /* @__PURE__ */ jsx("defs", { dangerouslySetInnerHTML: { __html: softEdge.filterMarkup } })
      }
    ) : null,
    fillOverlayStyle ? /* @__PURE__ */ jsx("div", { className: "pptx-react-fill-overlay", "aria-hidden": "true", style: fillOverlayStyle }) : null,
    strokeOutline ? /* @__PURE__ */ jsxs(
      "svg",
      {
        className: "pptx-react-gradient-outline",
        "aria-hidden": "true",
        viewBox: strokeOutlineViewBox(element),
        preserveAspectRatio: "none",
        style: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%",
          overflow: "visible",
          pointerEvents: "none"
        },
        children: [
          strokeOutline.paint ? /* @__PURE__ */ jsx("defs", { children: renderOutlinePaint(strokeOutline.paint) }) : null,
          strokeOutline.strands.map((strand, idx) => /* @__PURE__ */ jsx(
            "path",
            {
              d: strokeOutline.d,
              fill: "none",
              stroke: animatesStroke ? "inherit" : strokeOutline.stroke,
              strokeWidth: strand.strokeWidth,
              strokeDasharray: strokeOutline.dashArray,
              strokeLinecap: strokeOutline.lineCap,
              strokeLinejoin: strokeOutline.lineJoin,
              style: strand.offset !== 0 ? { transform: `translate(0, ${strand.offset}px)` } : void 0
            },
            idx
          ))
        ]
      }
    ) : null,
    hollowHit ? /* @__PURE__ */ jsx(
      "svg",
      {
        "aria-hidden": "true",
        viewBox: strokeOutlineViewBox(element),
        preserveAspectRatio: "none",
        style: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%",
          overflow: "visible",
          pointerEvents: "none"
        },
        children: /* @__PURE__ */ jsx(
          "path",
          {
            d: hollowHit.d,
            fill: "none",
            stroke: "transparent",
            strokeWidth: hollowHit.strokeWidth,
            style: { pointerEvents: "stroke" }
          }
        )
      }
    ) : null,
    reflection ? /* @__PURE__ */ jsx(
      "div",
      {
        className: "pptx-react-reflection",
        "aria-hidden": "true",
        style: reflection,
        children: /* @__PURE__ */ jsx(StaticElementRenderer, { element, positioned: false, suppressReflection: true })
      }
    ) : null
  ] });
}
var ElementRenderer = React20.memo(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the memo component its displayName
  function ElementRendererInner({
    element: el,
    activeSlide,
    isSelected,
    isInlineEditing,
    inlineEditingText,
    canInteract,
    presenting,
    spellCheckEnabled,
    mediaDataUrls,
    tableEditorState,
    selectionColorClass: selClr,
    showHoverBorder,
    opacity,
    templateEditing,
    zIndex,
    imageAltText,
    showResizeHandles,
    renderInk: doInk,
    renderGroups: doGrp,
    adjustmentHandles: adjH,
    onResizePointerDown,
    onAdjustmentPointerDown,
    onInlineEditChange,
    onInlineEditCommit,
    onInlineEditCancel,
    onTableCellSelect,
    onCommitCellEdit,
    onUpdateSmartArtElement,
    onFormatText,
    onResizeTableColumns,
    onResizeTableRow,
    findHighlights,
    onActionClick,
    onHyperlinkClick,
    animationState,
    presentationElementStates,
    allSlides,
    onZoomClick,
    sourceSlideIndex,
    fieldContext,
    tableStyleContext
  }) {
    const {
      cellSelectHandler,
      cellCommitHandler,
      colResizeHandler,
      rowResizeHandler,
      smartArtUpdateHandler
    } = getScopedElementHandlers(el.id, {
      onTableCellSelect,
      onCommitCellEdit,
      onResizeTableColumns,
      onResizeTableRow,
      onUpdateSmartArtElement
    });
    const chartUpdateHandler = smartArtUpdateHandler;
    const { hf, fc, sw, sc } = shapeParams(el);
    const allow = resolveElementInteractivity(el);
    const backgroundAnimationState = presentationElementStates?.get(`${el.id}::pptx-bg`);
    const visualAnimationState = backgroundAnimationState ?? animationState;
    const isTxt = isEditableTextElement(el) && allow.textEditable;
    const txtSE = hasTextProperties(el) ? el.textStyle : void 0;
    const textStyleOverrideCss = buildTextStyleOverrideCss(el.id, visualAnimationState?.textStyle);
    const ss = getShapeVisualStyle(
      el,
      hf,
      fc,
      sw,
      sc,
      visualAnimationState?.animatesFill,
      visualAnimationState?.animatesStroke
    );
    const ts = getTextStyleForElement(el, DEFAULT_TEXT_COLOR);
    const isImg = el.type === "picture" || el.type === "image";
    const vs = renderVectorShape(
      el,
      isImg ? false : hf,
      fc,
      sw,
      sc,
      visualAnimationState?.animatesFill,
      visualAnimationState?.animatesStroke
    );
    const isModel3D = el.type === "model3d";
    const isConn = isConnectorOrLineElement(el);
    const shapeStyle3d = hasShapeProperties(el) ? el.shapeStyle : void 0;
    const extrusionData = useMemo(
      () => build3DExtrusionData(shapeStyle3d?.shape3d, shapeStyle3d?.scene3d, fc, el.width, el.height),
      [shapeStyle3d?.shape3d, shapeStyle3d?.scene3d, fc, el.width, el.height]
    );
    const actionAffordance = useActionAffordance(el, canInteract);
    const [isMediaPlaying, setIsMediaPlaying] = useState(false);
    const handleMediaPlayStateChange = useCallback((playing) => {
      setIsMediaPlaying(playing);
    }, []);
    if (!isElementRendered(el)) {
      return null;
    }
    if (isConn) {
      return /* @__PURE__ */ jsx(
        ConnectorElementRenderer,
        {
          el,
          isSelected,
          canInteract,
          showResizeHandles: showResizeHandles && allow.resizable,
          showHoverBorder,
          selectionColorClass: selClr,
          opacity,
          zIndex,
          adjustmentHandles: adjH,
          onResizePointerDown,
          onAdjustmentPointerDown,
          animationState,
          textStyleOverrideCss
        }
      );
    }
    const effectiveCanInteract = canInteract && allow.selectable;
    const effectiveIsInlineEditing = isInlineEditing && allow.textEditable;
    const canEditSmartArt = effectiveCanInteract && allow.textEditable;
    const canEditChart = effectiveCanInteract;
    const hasAction = Boolean(el.actionClick && onActionClick);
    const isZoom = el.type === "zoom" && Boolean(onZoomClick);
    const aria = resolveElementAriaAttributes(el, {
      hasActionHandler: Boolean(onActionClick),
      hasHyperlinkHandler: Boolean(onHyperlinkClick),
      hasZoomHandler: Boolean(onZoomClick)
    });
    const isActionable = aria.actionable;
    const selB = isSelected ? `outline-1 -outline-offset-1 outline-${selClr} ring-2 ring-${selClr}/50` : showHoverBorder ? "outline-1 -outline-offset-1 outline-transparent hover:outline-primary/40" : "";
    const cur = effectiveIsInlineEditing ? "cursor-text" : effectiveCanInteract ? !allow.movable ? "cursor-default" : "cursor-move" : hasAction || isZoom ? "cursor-pointer" : "";
    const isPresentationPassive = presenting === true;
    const isFullscreenMedia = el.type === "media" && Boolean(el.fullScreen) && isPresentationPassive && isMediaPlaying;
    const isFocusable = effectiveCanInteract || isActionable;
    const interactionProps = getElementInteractionProps({
      element: el,
      isEditableText: isTxt,
      canInteract: effectiveCanInteract,
      isInlineEditing: effectiveIsInlineEditing,
      isActionable,
      isPresentationPassive,
      onInlineEditCancel,
      onActionClick
    });
    return /* @__PURE__ */ jsxs(
      "div",
      {
        "data-pptx-element": "true",
        "data-element-id": el.id,
        "data-pptx-action": isActionable ? "click" : void 0,
        role: aria.role,
        "aria-label": aria.label,
        "aria-roledescription": aria.roleDescription,
        "aria-hidden": aria.hidden ? true : void 0,
        "aria-selected": isSelected ? true : void 0,
        tabIndex: isFocusable ? 0 : -1,
        className: cn(
          "absolute",
          "focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-500",
          cur,
          // During a show this must stay EMPTY: `PRESENTATION_HIT_TEST_CSS`
          // (injected by `PresentationStage`) owns hit-testing there, because
          // it is the only form that can re-enable an action shape nested
          // inside inert scenery - an inline `pointer-events: none` on the
          // group could never do that, and React was the one binding that
          // wrote it. Off the show stage the inline rule still applies.
          inlineElementPointerEvents({
            interactive: effectiveCanInteract || isActionable,
            presenting: presenting === true
          }) === "none" ? "pointer-events-none" : "",
          // An unfilled, textless shape is a FRAME: PowerPoint hit-tests it on its
          // outline only, so its interior must not swallow clicks meant for what it
          // is drawn over. ShapeEffectOverlay paints a transparent
          // pointer-events:stroke band that opts the outline back in.
          isHollowShapeElement(el) ? "pointer-events-none" : "",
          isFullscreenMedia ? "pointer-events-auto" : "",
          selB,
          // Shared class the tooltip's `:hover` rule keys off (see
          // `ACTION_AFFORDANCE_CSS`), replacing React's Tailwind `group/link`
          // so the four non-Tailwind bindings reveal it the same way.
          actionAffordance.showLinkTooltip && LINK_TOOLTIP_HOST_CLASS
        ),
        style: getContainerStyle({
          el,
          isFullscreenMedia,
          isImg: isImg || isModel3D,
          zIndex,
          opacity,
          animationState,
          shapeVisualStyle: backgroundAnimationState ? {} : ss,
          has3DExtrusion: extrusionData.hasExtrusion,
          templateEditing
        }),
        ...interactionProps,
        children: [
          renderDagDuotoneFilterForElement(el),
          textStyleOverrideCss && /* @__PURE__ */ jsx("style", { children: textStyleOverrideCss }),
          backgroundAnimationState ? /* @__PURE__ */ jsxs(
            "div",
            {
              "data-pptx-animation-layer": "background",
              style: {
                ...ss,
                position: "absolute",
                inset: 0,
                transformOrigin: "center",
                visibility: backgroundAnimationState.visible === false ? "hidden" : "visible",
                animation: backgroundAnimationState.cssAnimation,
                pointerEvents: "none"
              },
              children: [
                /* @__PURE__ */ jsx(
                  ShapeEffectOverlay,
                  {
                    element: el,
                    animatesFill: visualAnimationState?.animatesFill,
                    animatesStroke: visualAnimationState?.animatesStroke
                  }
                ),
                extrusionData.hasExtrusion && /* @__PURE__ */ jsx(Extrusion3DOverlay, { data: extrusionData }),
                isImg ? null : vs
              ]
            }
          ) : /* @__PURE__ */ jsxs(Fragment, { children: [
            /* @__PURE__ */ jsx(
              ShapeEffectOverlay,
              {
                element: el,
                animatesFill: visualAnimationState?.animatesFill,
                animatesStroke: visualAnimationState?.animatesStroke
              }
            ),
            extrusionData.hasExtrusion && /* @__PURE__ */ jsx(Extrusion3DOverlay, { data: extrusionData })
          ] }),
          renderBody({
            el,
            isImg,
            isEditing: effectiveIsInlineEditing,
            editText: inlineEditingText,
            spellCheck: spellCheckEnabled,
            txtSE,
            txtS: ts,
            vecShape: backgroundAnimationState && !isImg ? null : vs,
            imgStyle: getImageRenderStyle(el),
            imgFilter: getImageEffectsFilter(el),
            imgOpacity: getImageEffectsOpacity(el),
            imgAlt: imageAltText,
            isTxtEl: isTxt,
            media: mediaDataUrls,
            tableSt: tableEditorState,
            isSel: isSelected,
            doInk,
            doGrp,
            renderGroupChild: (child, index) => /* @__PURE__ */ jsx(
              StaticElementRenderer,
              {
                element: child,
                activeSlide,
                allSlides,
                mediaDataUrls,
                sourceSlideIndex,
                zIndex: index,
                animation: presentationElementStates?.get(child.id)?.cssAnimation,
                exposeElementId: true,
                parentGroupFill: getGroupChildParentFill(el),
                onActionClick: el.actionClick ? void 0 : onActionClick,
                actionRequiresModifier: effectiveCanInteract
              },
              child.id
            ),
            onEditChange: onInlineEditChange,
            onCommit: onInlineEditCommit,
            onCancel: onInlineEditCancel,
            onCellSel: cellSelectHandler,
            onCellCommit: cellCommitHandler,
            onColResize: colResizeHandler,
            onRowResize: rowResizeHandler,
            findHl: findHighlights,
            onHyperlinkClick,
            isPresentationPassive,
            // Same rule as the other four bindings: the hint belongs to the
            // editable canvas, not to a read-only viewer or the show stage.
            placeholderPromptMode: canInteract && !isPresentationPassive ? "edit" : "present",
            handleMediaPlayStateChange,
            presentationElementStates,
            slideElements: activeSlide?.elements,
            allSlides,
            onZoomClick,
            sourceSlideIndex,
            fieldContext,
            tableStyleContext,
            canEditSmartArt,
            onUpdateSmartArtElement: smartArtUpdateHandler,
            canEditChart,
            onUpdateChartElement: chartUpdateHandler,
            onFormatText
          }),
          /* @__PURE__ */ jsx(ActionAffordances, { affordance: actionAffordance })
        ]
      }
    );
  }
);
function renderPressureStroke(circles, color, opacity, keyPrefix) {
  return /* @__PURE__ */ jsx("g", { opacity, children: circles.map((c, j) => /* @__PURE__ */ jsx("circle", { cx: c.cx, cy: c.cy, r: c.r, fill: color }, `${keyPrefix}-pc-${j}`)) });
}
function renderNibMarkStroke(marks, color, opacity, keyPrefix) {
  return /* @__PURE__ */ jsx("g", { opacity, children: marks.map((m, j) => /* @__PURE__ */ jsx(
    "ellipse",
    {
      cx: m.cx,
      cy: m.cy,
      rx: m.rPerp,
      ry: m.rTilt,
      transform: `rotate(${m.rotationDeg} ${m.cx} ${m.cy})`,
      fill: color
    },
    `${keyPrefix}-nib-${j}`
  )) });
}
function renderStrokeView(view, pressureSensitive, replayStyle, key) {
  if (view.nibMarks) {
    return /* @__PURE__ */ jsx("g", { children: renderNibMarkStroke(view.nibMarks, view.color, view.opacity, key) }, key);
  }
  if (pressureSensitive && view.circles) {
    return /* @__PURE__ */ jsx("g", { children: renderPressureStroke(view.circles, view.color, view.opacity, key) }, key);
  }
  return /* @__PURE__ */ jsx(
    "path",
    {
      d: view.d,
      fill: "none",
      stroke: view.color,
      strokeWidth: view.width,
      strokeOpacity: view.opacity,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      vectorEffect: "non-scaling-stroke",
      ...replayStyle ? {
        strokeDasharray: replayStyle.strokeDasharray,
        strokeDashoffset: replayStyle.strokeDashoffset,
        style: {
          animation: replayStyle.animation,
          "--ink-path-length": replayStyle.pathLength
        }
      } : {}
    },
    key
  );
}
function renderInk(el, options) {
  const replay = options?.replay ?? false;
  const strokes = buildInkGroupStrokes(el, { color: "#000", width: 3 });
  strokes.some((s) => s.circles || s.nibMarks);
  const pressureSensitive = options?.pressureSensitive;
  const replayStyles = replay ? getInkReplayStyles(el, options?.replayConfig) : null;
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      className: "w-full h-full pointer-events-none",
      viewBox: `0 0 ${Math.max(el.width, 1)} ${Math.max(el.height, 1)}`,
      preserveAspectRatio: "none",
      children: [
        replay && /* @__PURE__ */ jsx("style", { children: INK_REPLAY_KEYFRAMES }),
        strokes.map(
          (s, i) => renderStrokeView(s, pressureSensitive, replayStyles?.[i], `${el.id}-ink-${i}`)
        )
      ]
    }
  );
}
function renderGroup(children, parentGroupFill) {
  return /* @__PURE__ */ jsx("div", { className: "relative w-full h-full pointer-events-none", children: filterRenderedElements(children).map((c, childIndex) => {
    const { hf, fc, sw, sc } = shapeParams(c);
    const baseSs = getShapeVisualStyle(c, hf, fc, sw, sc);
    const inheritedFill = resolveGroupChildFill(c, parentGroupFill);
    const ss = inheritedFill ? {
      ...baseSs,
      backgroundColor: inheritedFill.backgroundColor,
      backgroundImage: inheritedFill.backgroundImage,
      backgroundRepeat: inheritedFill.backgroundRepeat,
      backgroundSize: inheritedFill.backgroundSize,
      backgroundPosition: inheritedFill.backgroundPosition
    } : baseSs;
    const isI = c.type === "picture" || c.type === "image";
    const vs = renderVectorShape(c, isI ? false : hf, fc, sw, sc);
    const ts = getTextStyleForElement(c, DEFAULT_TEXT_COLOR);
    const isTxt = isEditableTextElement(c);
    return /* @__PURE__ */ jsxs(
      "div",
      {
        "data-element-id": c.id,
        className: "absolute",
        style: {
          left: c.x,
          top: c.y,
          width: Math.max(c.width, MIN_ELEMENT_SIZE),
          height: Math.max(c.height, MIN_ELEMENT_SIZE),
          transform: getElementTransform(c),
          transformOrigin: "center",
          ...ss,
          ...isI ? {
            backgroundColor: "transparent",
            backgroundImage: void 0,
            backgroundRepeat: void 0,
            backgroundSize: void 0,
            backgroundPosition: void 0,
            borderRadius: void 0,
            clipPath: void 0,
            overflow: "visible"
          } : {},
          // Explicit z-index preserves document order stacking within the
          // group: later children in the array (= later in p:grpSp XML)
          // render on top, matching PowerPoint's painter's algorithm.
          // Placed after ...ss to ensure it is never overwritten.
          zIndex: childIndex
        },
        children: [
          /* @__PURE__ */ jsx(ShapeEffectOverlay, { element: c }),
          c.type === "group" ? (
            // A nested group: recurse, chaining the inherited fill so a
            // `grpFill` shape under a fill-less sub-group still paints.
            renderGroup(c.children, getGroupChildParentFill(c, parentGroupFill))
          ) : isI && ("svgData" in c && c.svgData || "imageData" in c && c.imageData) ? /* @__PURE__ */ jsxs("div", { className: "absolute inset-0 pointer-events-none", style: getImageSurfaceStyle(c), children: [
            isImageTiled(c) ? /* @__PURE__ */ jsx(
              "div",
              {
                className: "pointer-events-none select-none w-full h-full",
                style: getImageTilingStyle(c)
              }
            ) : /* @__PURE__ */ jsx(
              "img",
              {
                src: "svgData" in c && c.svgData ? c.svgData : c.imageData,
                alt: translationsEn["pptx.ink.groupChildAlt"],
                className: "pointer-events-none select-none",
                style: getImageRenderStyle(c),
                draggable: false
              }
            ),
            vs ? /* @__PURE__ */ jsx("div", { className: "pointer-events-none absolute inset-0", children: vs }) : null
          ] }) : /* @__PURE__ */ jsxs(Fragment, { children: [
            vs,
            isTxt ? /* @__PURE__ */ jsx(
              "div",
              {
                className: "relative z-10 w-full h-full pointer-events-none whitespace-pre-wrap break-words leading-[1.3]",
                style: ts,
                children: renderTextSegments(c, DEFAULT_TEXT_COLOR)
              }
            ) : null
          ] })
        ]
      },
      c.id
    );
  }) });
}
function renderContentPart(el, options) {
  if (el.inkStrokes && el.inkStrokes.length > 0) {
    const replay = options?.replay ?? false;
    const pressureSensitive = options?.pressureSensitive ?? true;
    const strokes = buildContentPartStrokes(el);
    const replayStyles = replay ? getContentPartReplayStyles(el.inkStrokes, options?.replayConfig) : null;
    return /* @__PURE__ */ jsxs(
      "svg",
      {
        className: "w-full h-full pointer-events-none",
        viewBox: `0 0 ${Math.max(el.width, 1)} ${Math.max(el.height, 1)}`,
        preserveAspectRatio: "none",
        children: [
          replay && /* @__PURE__ */ jsx("style", { children: INK_REPLAY_KEYFRAMES }),
          strokes.map(
            (s, i) => renderStrokeView(s, pressureSensitive, replayStyles?.[i], `${el.id}-cp-ink-${i}`)
          )
        ]
      }
    );
  }
  return /* @__PURE__ */ jsx("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none", children: translationsEn["pptx.ink.contentPartFallback"] });
}
function ExcelIcon(color, size = 32) {
  return /* @__PURE__ */ jsxs("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "3",
        y: "3",
        width: "18",
        height: "18",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    /* @__PURE__ */ jsx("line", { x1: "3", y1: "9", x2: "21", y2: "9", stroke: color, strokeWidth: "1" }),
    /* @__PURE__ */ jsx("line", { x1: "3", y1: "15", x2: "21", y2: "15", stroke: color, strokeWidth: "1" }),
    /* @__PURE__ */ jsx("line", { x1: "9", y1: "3", x2: "9", y2: "21", stroke: color, strokeWidth: "1" }),
    /* @__PURE__ */ jsx("line", { x1: "15", y1: "3", x2: "15", y2: "21", stroke: color, strokeWidth: "1" })
  ] });
}
function WordIcon(color, size = 32) {
  return /* @__PURE__ */ jsxs("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "4",
        y: "2",
        width: "16",
        height: "20",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    /* @__PURE__ */ jsx("line", { x1: "7", y1: "7", x2: "17", y2: "7", stroke: color, strokeWidth: "1.5", strokeLinecap: "round" }),
    /* @__PURE__ */ jsx("line", { x1: "7", y1: "11", x2: "17", y2: "11", stroke: color, strokeWidth: "1.5", strokeLinecap: "round" }),
    /* @__PURE__ */ jsx("line", { x1: "7", y1: "15", x2: "13", y2: "15", stroke: color, strokeWidth: "1.5", strokeLinecap: "round" })
  ] });
}
function PdfIcon(color, size = 32) {
  return /* @__PURE__ */ jsxs("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "4",
        y: "2",
        width: "16",
        height: "20",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    /* @__PURE__ */ jsx("text", { x: "12", y: "14", textAnchor: "middle", fill: color, fontSize: "7", fontWeight: "bold", children: translationsEn["pptx.file.pdf"] })
  ] });
}
function VisioIcon(color, size = 32) {
  return /* @__PURE__ */ jsxs("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    /* @__PURE__ */ jsx("rect", { x: "8", y: "2", width: "8", height: "5", rx: "1", stroke: color, strokeWidth: "1.5", fill: "none" }),
    /* @__PURE__ */ jsx("line", { x1: "12", y1: "7", x2: "12", y2: "10", stroke: color, strokeWidth: "1.5" }),
    /* @__PURE__ */ jsx("line", { x1: "6", y1: "10", x2: "18", y2: "10", stroke: color, strokeWidth: "1.5" }),
    /* @__PURE__ */ jsx("line", { x1: "6", y1: "10", x2: "6", y2: "13", stroke: color, strokeWidth: "1.5" }),
    /* @__PURE__ */ jsx("line", { x1: "18", y1: "10", x2: "18", y2: "13", stroke: color, strokeWidth: "1.5" }),
    /* @__PURE__ */ jsx("rect", { x: "2", y: "13", width: "8", height: "5", rx: "1", stroke: color, strokeWidth: "1.5", fill: "none" }),
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "14",
        y: "13",
        width: "8",
        height: "5",
        rx: "1",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    )
  ] });
}
function MathIcon(color, size = 32) {
  return /* @__PURE__ */ jsxs("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "2",
        y: "4",
        width: "20",
        height: "16",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    /* @__PURE__ */ jsx(
      "text",
      {
        x: "12",
        y: "15",
        textAnchor: "middle",
        fill: color,
        fontSize: "9",
        fontStyle: "italic",
        fontWeight: "bold",
        children: "f(x)"
      }
    )
  ] });
}
function GenericOleIcon(color, size = 32) {
  return /* @__PURE__ */ jsxs("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "2",
        y: "5",
        width: "9",
        height: "7",
        rx: "1.5",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    /* @__PURE__ */ jsx(
      "rect",
      {
        x: "13",
        y: "12",
        width: "9",
        height: "7",
        rx: "1.5",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    /* @__PURE__ */ jsx(
      "line",
      {
        x1: "11",
        y1: "8.5",
        x2: "13",
        y2: "15.5",
        stroke: color,
        strokeWidth: "1.5",
        strokeLinecap: "round"
      }
    )
  ] });
}
function getOleIcon(type, color, size = 32) {
  switch (type) {
    case "excel":
      return ExcelIcon(color, size);
    case "word":
      return WordIcon(color, size);
    case "pdf":
      return PdfIcon(color, size);
    case "visio":
      return VisioIcon(color, size);
    case "mathtype":
      return MathIcon(color, size);
    case "unknown":
    default:
      return GenericOleIcon(color, size);
  }
}
function renderOleBadge(oleType) {
  const color = getOleTypeColor(oleType);
  const shortLabel = getOleBadgeLabel(oleType);
  return /* @__PURE__ */ jsxs("svg", { width: "24", height: "24", viewBox: "0 0 24 24", className: "absolute bottom-1 right-1 z-10", children: [
    /* @__PURE__ */ jsx("rect", { x: "2", y: "2", width: "20", height: "20", rx: "3", fill: color }),
    /* @__PURE__ */ jsx(
      "text",
      {
        x: "12",
        y: "16",
        textAnchor: "middle",
        fill: "white",
        fontSize: shortLabel.length > 4 ? "6" : "10",
        fontWeight: "bold",
        children: shortLabel
      }
    )
  ] });
}
function InlineTextEditor({
  initialText,
  spellCheck,
  rtl,
  textDirection: _textDirection,
  textStyle,
  textStyleRaw,
  layoutStyle,
  element,
  onCommit,
  onCancel,
  onEditChange,
  onFormatText
}) {
  const editorRef = useRef(null);
  const seedRef = useRef(null);
  if (seedRef.current === null) {
    seedRef.current = {
      initialText,
      hasRichSegments: Boolean(
        hasTextProperties(element) && element.textSegments && element.textSegments.length > 0
      )
    };
  }
  const seed = seedRef.current;
  const extractText = useCallback(() => {
    const el = editorRef.current;
    if (!el) {
      return seed.initialText;
    }
    return readEditableText(el);
  }, [seed]);
  const handleInput = useCallback(() => {
    onEditChange(extractText());
  }, [extractText, onEditChange]);
  const trimTrailingSpaceBeforeCaret = useCallback(() => {
    const selection = window.getSelection();
    if (!selection || !selection.isCollapsed || selection.rangeCount === 0) {
      return;
    }
    const range = selection.getRangeAt(0);
    const { startContainer, startOffset } = range;
    if (startContainer.nodeType !== Node.TEXT_NODE || startOffset === 0) {
      return;
    }
    const text = startContainer.textContent ?? "";
    if (text.charAt(startOffset - 1) !== " ") {
      return;
    }
    const trimRange = document.createRange();
    trimRange.setStart(startContainer, startOffset - 1);
    trimRange.setEnd(startContainer, startOffset);
    trimRange.deleteContents();
  }, []);
  useEffect(() => {
    const el = editorRef.current;
    if (!el) {
      return;
    }
    el.focus();
    placeCaretAtEnd(el);
  }, []);
  const mountedRef = useRef(false);
  useLayoutEffect(() => {
    if (!mountedRef.current) {
      mountedRef.current = true;
      return;
    }
    const pending = getPendingSelectionRestore();
    if (!pending || !editorRef.current) {
      return;
    }
    restoreSegmentSelection(
      editorRef.current,
      pending.startSegIdx,
      pending.startOffset,
      pending.endSegIdx,
      pending.endOffset
    );
  });
  const warpStyle = getTextWarpStyle(textStyleRaw);
  const compensationTransform = getTextCompensationTransform(element);
  const warpTransform = warpStyle?.transform;
  const mergedTransform = [compensationTransform, warpTransform].filter(Boolean).join(" ") || void 0;
  const wrapperStyle = {
    ...layoutStyle,
    ...textStyle,
    ...warpStyle,
    transform: mergedTransform,
    transformOrigin: warpStyle?.transformOrigin || "center"
  };
  const nextInlineStyleValue = (property) => {
    if (!hasTextProperties(element)) {
      return true;
    }
    const selection = getInlineEditorSelection(element.textSegments);
    const segment = selection ? element.textSegments?.[selection.startSegIdx] : element.textSegments?.[0];
    const style = segment?.style ?? element.textStyle;
    return !style?.[property];
  };
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref: editorRef,
      contentEditable: true,
      suppressContentEditableWarning: true,
      "data-inline-editor": true,
      spellCheck,
      dir: rtl ? "rtl" : "ltr",
      className: "relative z-10 w-full h-full whitespace-pre-wrap break-words leading-[1.3] outline-none",
      style: {
        ...wrapperStyle,
        cursor: "text",
        minHeight: "1em"
      },
      onPointerDown: (e) => e.stopPropagation(),
      onMouseDown: (e) => e.stopPropagation(),
      onClick: (e) => e.stopPropagation(),
      onInput: handleInput,
      onBlur: () => {
        onEditChange(extractText());
        onCommit();
      },
      onKeyDown: (e) => {
        if ((e.ctrlKey || e.metaKey) && !e.shiftKey && onFormatText) {
          const key = e.key.toLowerCase();
          if (key === "b" || key === "i" || key === "u") {
            e.preventDefault();
            e.stopPropagation();
            switch (key) {
              case "b":
                onFormatText({ bold: nextInlineStyleValue("bold") });
                break;
              case "i":
                onFormatText({ italic: nextInlineStyleValue("italic") });
                break;
              case "u":
                onFormatText({ underline: nextInlineStyleValue("underline") });
                break;
            }
            return;
          }
        }
        if (e.key === "Escape") {
          e.preventDefault();
          onCancel();
          return;
        }
        if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
          e.preventDefault();
          onEditChange(extractText());
          onCommit();
          return;
        }
        if (e.key === "Enter") {
          trimTrailingSpaceBeforeCaret();
        }
      },
      onPaste: (e) => {
        e.preventDefault();
        const text = e.clipboardData.getData("text/plain");
        document.execCommand("insertText", false, text);
      },
      children: seed.hasRichSegments ? renderTextSegments(element, DEFAULT_TEXT_COLOR) : seed.initialText
    }
  );
}
function FailedToLoad6() {
  return null;
}
var LazyModel3DScene = React20.lazy(
  async () => {
    try {
      return await import('./Model3DScene-WJ3MMIZH.mjs');
    } catch {
      return { default: FailedToLoad6 };
    }
  }
);
function dataUrlToBlobUrl(dataUrl, mimeType) {
  return modelDataToBlobUrl(dataUrl, mimeType);
}
function PosterFallback({
  element,
  width,
  height
}) {
  const src = element.posterImage ?? element.imageData;
  if (src) {
    return /* @__PURE__ */ jsx(
      "img",
      {
        src,
        alt: "3D Model",
        className: "pointer-events-none select-none",
        style: {
          width,
          height,
          objectFit: "contain"
        },
        draggable: false
      }
    );
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "w-full h-full flex flex-col items-center justify-center text-[11px] text-gray-400 bg-gray-50 border border-dashed border-gray-200 rounded",
      style: { width, height },
      children: [
        /* @__PURE__ */ jsxs(
          "svg",
          {
            xmlns: "http://www.w3.org/2000/svg",
            width: "24",
            height: "24",
            viewBox: "0 0 24 24",
            fill: "none",
            stroke: "currentColor",
            strokeWidth: "1.5",
            strokeLinecap: "round",
            strokeLinejoin: "round",
            className: "mb-1 text-gray-300",
            children: [
              /* @__PURE__ */ jsx("path", { d: "M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" }),
              /* @__PURE__ */ jsx("polyline", { points: "3.27 6.96 12 12.01 20.73 6.96" }),
              /* @__PURE__ */ jsx("line", { x1: "12", y1: "22.08", x2: "12", y2: "12" })
            ]
          }
        ),
        /* @__PURE__ */ jsx("span", { children: "3D Model" })
      ]
    }
  );
}
var Model3DErrorBoundary = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    if (this.state.hasError) {
      return this.props.fallback;
    }
    return this.props.children;
  }
};
function Model3DRenderer({ element, width, height, interactive }) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const blobUrl = useMemo(
    () => dataUrlToBlobUrl(element.modelData, element.modelMimeType),
    [element.modelData, element.modelMimeType]
  );
  useEffect(() => {
    return () => {
      if (blobUrl) {
        URL.revokeObjectURL(blobUrl);
      }
    };
  }, [blobUrl]);
  const posterFallback = /* @__PURE__ */ jsx(PosterFallback, { element, width, height });
  if (threeAvailable === null) {
    return posterFallback;
  }
  if (!threeAvailable || !blobUrl) {
    return posterFallback;
  }
  return /* @__PURE__ */ jsx(Model3DErrorBoundary, { fallback: posterFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: posterFallback, children: /* @__PURE__ */ jsx(
    LazyModel3DScene,
    {
      modelUrl: blobUrl,
      interactive,
      width,
      height
    }
  ) }) });
}
function buildEmbedInfo(el) {
  const canDownload = Boolean(el.oleEmbeddedData);
  return {
    fileName: el.oleEmbeddedFileName ?? el.fileName,
    displayName: getOleDisplayName(el),
    readableSize: formatBytes(el.oleEmbeddedByteSize),
    progId: el.oleProgId,
    typeLabel: getOleTypeLabel(resolveOleType(el)),
    canDownload,
    canOpenInBrowser: canDownload && isBrowserOpenableMime(el.oleEmbeddedMimeType)
  };
}
function buildTooltip(info) {
  const parts = [info.displayName, info.typeLabel];
  if (info.readableSize) {
    parts.push(info.readableSize);
  }
  if (info.progId) {
    parts.push(info.progId);
  }
  return Array.from(new Set(parts)).join(" \xB7 ");
}
function stopPointer(e) {
  e.stopPropagation();
}
function OleActionFooter({ el, info }) {
  const { t } = useTranslation();
  if (!info.canDownload || !el.oleEmbeddedData) {
    return null;
  }
  const downloadName = info.fileName ?? "embedded-object";
  const sizeSuffix = info.readableSize ? ` (${info.readableSize})` : "";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "absolute inset-x-0 bottom-0 z-20 flex items-center justify-between gap-2 px-2 py-1 bg-black/70 text-white text-[10px] opacity-0 transition-opacity group-hover:opacity-100 focus-within:opacity-100",
      onPointerDown: stopPointer,
      onMouseDown: stopPointer,
      onClick: stopPointer,
      children: [
        /* @__PURE__ */ jsxs("span", { className: "min-w-0 truncate", title: downloadName, children: [
          downloadName,
          sizeSuffix
        ] }),
        /* @__PURE__ */ jsxs("span", { className: "flex shrink-0 items-center gap-1", children: [
          /* @__PURE__ */ jsx(
            "a",
            {
              href: el.oleEmbeddedData,
              download: downloadName,
              className: "rounded px-1.5 py-0.5 bg-white/15 hover:bg-white/25 focus-visible:outline focus-visible:outline-1 focus-visible:outline-white",
              title: `Download ${downloadName}`,
              children: t("pptx.ole.download")
            }
          ),
          info.canOpenInBrowser && el.oleEmbeddedData && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: () => openUrlInNewTab(el.oleEmbeddedData),
              className: "rounded px-1.5 py-0.5 bg-white/15 hover:bg-white/25 focus-visible:outline focus-visible:outline-1 focus-visible:outline-white",
              title: `Open ${downloadName} in a new tab`,
              children: t("pptx.ole.open")
            }
          )
        ] })
      ]
    }
  );
}
function OleRenderer({ element: el }) {
  const oleType = resolveOleType(el);
  const ariaLabel = getOleAriaLabel(el);
  const info = buildEmbedInfo(el);
  const tooltip = buildTooltip(info);
  if (el.previewImageData) {
    return /* @__PURE__ */ jsxs(
      "div",
      {
        className: "group relative w-full h-full",
        role: "img",
        "aria-label": ariaLabel,
        title: tooltip,
        children: [
          /* @__PURE__ */ jsx(
            "img",
            {
              src: el.previewImageData,
              alt: ariaLabel,
              className: "pointer-events-none select-none w-full h-full object-contain",
              draggable: false
            }
          ),
          renderOleBadge(oleType),
          /* @__PURE__ */ jsx(OleActionFooter, { el, info })
        ]
      }
    );
  }
  const color = getOleTypeColor(oleType);
  const label = getOleTypeLabel(oleType);
  const displayName = info.displayName;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "group relative w-full h-full flex flex-col items-center justify-center",
      role: "img",
      "aria-label": ariaLabel,
      title: tooltip,
      style: {
        border: `2px solid ${color}33`,
        borderRadius: 6,
        backgroundColor: `${color}0D`
      },
      children: [
        getOleIcon(oleType, color, 36),
        /* @__PURE__ */ jsx("span", { className: "mt-2 text-[12px] font-medium max-w-[90%] truncate", style: { color }, children: displayName }),
        /* @__PURE__ */ jsx("span", { className: "mt-0.5 text-[10px] text-white/50 max-w-[90%] truncate", children: info.readableSize ? `${label} \xB7 ${info.readableSize}` : label }),
        /* @__PURE__ */ jsx(OleActionFooter, { el, info })
      ]
    }
  );
}
var SmartArt3DContext = createContext(false);

// src/viewer/utils/smartart-helpers.tsx
function resolvePalette2(el) {
  return el.type === "smartArt" ? resolvePalette(el.smartArtData) : DEFAULT_PALETTE2;
}
function resolveStyle(el) {
  if (el.type !== "smartArt" || !el.smartArtData) {
    return "flat";
  }
  return el.smartArtData.style ?? "flat";
}
function resolveSmartArtDataPalette(data) {
  return resolvePalette(data);
}
var LINE_HEIGHT_RATIO = 1.2;
var DEFAULT_FONT_SIZE = 12;
function SmartArtInlineNodeEditor({
  initialText,
  rect,
  fontSize = DEFAULT_FONT_SIZE,
  onCommit,
  onCancel
}) {
  const { t } = useTranslation();
  const ref = React20.useRef(null);
  const [value, setValue] = React20.useState(initialText);
  const settled = React20.useRef(false);
  React20.useEffect(() => {
    const el = ref.current;
    if (el) {
      el.focus();
      el.select();
    }
  }, []);
  const commit = () => {
    if (settled.current) {
      return;
    }
    settled.current = true;
    onCommit(value);
  };
  const cancel = () => {
    if (settled.current) {
      return;
    }
    settled.current = true;
    onCancel();
  };
  return /* @__PURE__ */ jsx(
    "textarea",
    {
      ref,
      value,
      "aria-label": t("pptx.smartArt.editNodeText"),
      spellCheck: false,
      className: "absolute z-20 resize-none rounded-sm border border-primary bg-white/95 text-center text-black shadow outline-none",
      style: {
        left: rect.left,
        top: rect.top,
        width: rect.width,
        height: rect.height,
        fontSize,
        lineHeight: `${fontSize * LINE_HEIGHT_RATIO}px`
      },
      onChange: (e) => setValue(e.target.value),
      onMouseDown: (e) => e.stopPropagation(),
      onClick: (e) => e.stopPropagation(),
      onDoubleClick: (e) => e.stopPropagation(),
      onBlur: commit,
      onKeyDown: (e) => {
        if (e.key === "Enter" && !e.shiftKey) {
          e.preventDefault();
          commit();
        } else if (e.key === "Escape") {
          e.preventDefault();
          cancel();
        }
        e.stopPropagation();
      }
    }
  );
}
function SmartArtNodeStyleBar({
  palette,
  onPickFill
}) {
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "flex gap-1.5 px-2 py-1.5 bg-white/90 shadow-sm rounded-full border border-border",
      onMouseDown: (e) => e.stopPropagation(),
      onClick: (e) => e.stopPropagation(),
      children: palette.slice(0, 6).map((color) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          "aria-label": `Set fill to ${color}`,
          "data-pptx-compact": true,
          className: "w-5 h-5 rounded-full border border-black/10 hover:scale-125 transition-transform",
          style: { background: color },
          onClick: () => onPickFill(color)
        },
        color
      ))
    }
  );
}
var NODE_ID_ATTR = "data-smartart-node-id";
function findNodeIdFromEvent(target) {
  let el = target instanceof Element ? target : null;
  while (el) {
    if (el.hasAttribute(NODE_ID_ATTR)) {
      return el;
    }
    el = el.parentElement;
  }
  return null;
}
function useSmartArtHoverState(containerRef) {
  const [hoveredNodeId, setHoveredNodeId] = React20.useState(null);
  const [hoveredNodeRect, setHoveredNodeRect] = React20.useState(null);
  const hideTimeoutRef = React20.useRef(null);
  const cancelPendingHide = React20.useCallback(() => {
    if (hideTimeoutRef.current !== null) {
      window.clearTimeout(hideTimeoutRef.current);
      hideTimeoutRef.current = null;
    }
  }, []);
  const handleMouseMove = React20.useCallback(
    (e, ignoreRef) => {
      const nodeEl = findNodeIdFromEvent(e.target);
      const container = containerRef.current;
      if (nodeEl && container) {
        cancelPendingHide();
        setHoveredNodeId(nodeEl.getAttribute(NODE_ID_ATTR));
        setHoveredNodeRect(
          computeInlineEditorRect(
            nodeEl.getBoundingClientRect(),
            container.getBoundingClientRect()
          )
        );
        return;
      }
      if (ignoreRef?.current && e.target instanceof Node && ignoreRef.current.contains(e.target)) {
        cancelPendingHide();
        return;
      }
      cancelPendingHide();
      hideTimeoutRef.current = window.setTimeout(() => {
        setHoveredNodeId(null);
        setHoveredNodeRect(null);
        hideTimeoutRef.current = null;
      }, 150);
    },
    [containerRef, cancelPendingHide]
  );
  const clearHover = React20.useCallback(() => {
    cancelPendingHide();
    setHoveredNodeId(null);
    setHoveredNodeRect(null);
  }, [cancelPendingHide]);
  React20.useEffect(() => cancelPendingHide, [cancelPendingHide]);
  return { hoveredNodeId, hoveredNodeRect, handleMouseMove, clearHover };
}
var EDITOR_PADDING = 4;
var LINE_HEIGHT_RATIO2 = 1.2;
var STYLE_BAR_WIDTH = 168;
var STYLE_BAR_HEIGHT = 40;
function SmartArtEditableLayer({
  smartArtData,
  canEdit,
  onCommitNodeText,
  palette,
  onChangeNodeStyle,
  children
}) {
  const containerRef = React20.useRef(null);
  const styleBarRef = React20.useRef(null);
  const [edit, setEdit] = React20.useState(null);
  const { hoveredNodeId, hoveredNodeRect, handleMouseMove, clearHover } = useSmartArtHoverState(containerRef);
  const openEditor = React20.useCallback(
    (target) => {
      const nodeEl = findNodeIdFromEvent(target);
      const container = containerRef.current;
      if (!nodeEl || !container) {
        return;
      }
      const nodeId = nodeEl.getAttribute(NODE_ID_ATTR);
      if (!nodeId) {
        return;
      }
      const textEl = nodeEl.querySelector("text");
      const textRect = textEl?.getBoundingClientRect();
      const hasTextRect = textRect !== void 0 && textRect.width > 0 && textRect.height > 0;
      const sourceRect = hasTextRect ? textRect : nodeEl.getBoundingClientRect();
      const rect = computeInlineEditorRect(sourceRect, container.getBoundingClientRect());
      const paddedRect = {
        left: rect.left - EDITOR_PADDING,
        top: rect.top - EDITOR_PADDING,
        width: rect.width + EDITOR_PADDING * 2,
        height: rect.height + EDITOR_PADDING * 2
      };
      const lineCount = Math.max(1, textEl?.querySelectorAll("tspan").length ?? 1);
      const fontSize = hasTextRect ? Math.max(8, textRect.height / (lineCount * LINE_HEIGHT_RATIO2)) : void 0;
      clearHover();
      setEdit({ nodeId, rect: paddedRect, fontSize });
    },
    [clearHover]
  );
  if (!canEdit) {
    return children;
  }
  const initialText = edit ? findSmartArtNodeText(smartArtData, edit.nodeId) ?? "" : "";
  const showStyleBar = !edit && hoveredNodeId !== null && hoveredNodeRect !== null && palette !== void 0 && onChangeNodeStyle !== void 0;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ref: containerRef,
      className: "relative h-full w-full",
      style: { cursor: hoveredNodeId ? "text" : void 0 },
      onMouseMove: (e) => handleMouseMove(e, styleBarRef),
      onMouseLeave: clearHover,
      onDoubleClick: (e) => {
        const nodeEl = findNodeIdFromEvent(e.target);
        if (nodeEl) {
          e.stopPropagation();
          openEditor(e.target);
        }
      },
      children: [
        !edit && /* @__PURE__ */ jsx("style", { children: `[data-smartart-node-id]:hover { outline: 2px solid rgba(96,165,250,0.6); outline-offset: 1px; }` }),
        children,
        showStyleBar && (() => {
          const container = containerRef.current;
          const maxLeft = Math.max(
            0,
            (container?.clientWidth ?? STYLE_BAR_WIDTH) - STYLE_BAR_WIDTH
          );
          const maxTop = Math.max(
            0,
            (container?.clientHeight ?? STYLE_BAR_HEIGHT) - STYLE_BAR_HEIGHT
          );
          return /* @__PURE__ */ jsx(
            "div",
            {
              ref: styleBarRef,
              style: {
                position: "absolute",
                left: Math.min(
                  maxLeft,
                  Math.max(0, hoveredNodeRect.left + hoveredNodeRect.width - STYLE_BAR_WIDTH)
                ),
                top: Math.min(maxTop, Math.max(0, hoveredNodeRect.top - 22)),
                zIndex: 10
              },
              children: /* @__PURE__ */ jsx(
                SmartArtNodeStyleBar,
                {
                  palette,
                  onPickFill: (color) => onChangeNodeStyle(hoveredNodeId, color)
                }
              )
            }
          );
        })(),
        edit && /* @__PURE__ */ jsx(
          SmartArtInlineNodeEditor,
          {
            initialText,
            rect: edit.rect,
            fontSize: edit.fontSize,
            onCommit: (text) => {
              onCommitNodeText(edit.nodeId, text);
              setEdit(null);
            },
            onCancel: () => setEdit(null)
          },
          edit.nodeId
        )
      ]
    }
  );
}
var LABEL_WIDTH_FRACTION = 0.82;
function smartArtNodeGroupProps(nodeId, shadow, label) {
  const props = {
    "data-smartart-node-id": nodeId,
    style: { filter: shadow, pointerEvents: "auto" }
  };
  if (label) {
    props.role = "img";
    props["aria-label"] = label;
  }
  return props;
}
function SmartArtGradient({ gradient }) {
  const stops = gradient.stops.map((stop, i) => /* @__PURE__ */ jsx(
    "stop",
    {
      offset: stop.offset,
      stopColor: stop.color,
      ...stop.opacity !== void 0 ? { stopOpacity: stop.opacity } : {}
    },
    `${gradient.id}-s${i}`
  ));
  return gradient.kind === "radial" ? /* @__PURE__ */ jsx("radialGradient", { id: gradient.id, cx: gradient.cx, cy: gradient.cy, r: gradient.r, children: stops }) : /* @__PURE__ */ jsx(
    "linearGradient",
    {
      id: gradient.id,
      x1: gradient.x1,
      y1: gradient.y1,
      x2: gradient.x2,
      y2: gradient.y2,
      children: stops
    }
  );
}
function SmartArtNodeText({
  text,
  lines: positionedLines,
  x,
  y,
  fill,
  fontSize,
  fontWeight,
  fontStyle,
  fontFamily,
  className,
  textAnchor = "middle",
  anchor = "middle",
  maxWidth
}) {
  if (positionedLines) {
    return /* @__PURE__ */ jsx(
      "text",
      {
        x,
        textAnchor,
        dominantBaseline: "central",
        fill,
        fontSize,
        fontWeight,
        fontStyle,
        fontFamily,
        className,
        children: positionedLines.map((line, i) => /* @__PURE__ */ jsx("tspan", { x, y: line.y, children: line.text }, i))
      }
    );
  }
  const source = text ?? "", lines = maxWidth !== void 0 ? centeredSvgTextLines(source, fontSize, { maxWidth: maxWidth * LABEL_WIDTH_FRACTION }).map(
    (line) => line.text
  ) : source.split("\n").filter((l) => l.length > 0), lineHeight = fontSize * 1.2;
  let startY, dominantBaseline;
  if (anchor === "bottom") {
    startY = lines.length > 0 ? y - (lines.length - 1) * lineHeight : y;
    dominantBaseline = "auto";
  } else if (anchor === "top") {
    startY = y;
    dominantBaseline = "hanging";
  } else {
    const totalHeight = lines.length * lineHeight;
    startY = lines.length > 0 ? y - totalHeight / 2 + lineHeight / 2 : y;
    dominantBaseline = "central";
  }
  return /* @__PURE__ */ jsx(
    "text",
    {
      x,
      textAnchor,
      dominantBaseline,
      fill,
      fontSize,
      fontWeight,
      fontStyle,
      fontFamily,
      className,
      children: lines.map((line, i) => /* @__PURE__ */ jsx("tspan", { x, y: startY + i * lineHeight, children: line }, i))
    }
  );
}
function chromeStyleToReactCss(style) {
  const react = {};
  for (const [key, value] of Object.entries(style)) {
    react[key.replace(/-([a-z])/gu, (_match, letter) => letter.toUpperCase())] = value;
  }
  return react;
}
function wrapChrome(chrome, content, className, a11y) {
  const wrapperStyle = chromeStyleToReactCss(buildChromeStyle(chrome));
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: `w-full h-full ${className}`,
      style: wrapperStyle,
      role: a11y?.role,
      "aria-label": a11y?.label,
      children: content
    }
  );
}
function shapeBody(shape) {
  switch (shape.kind) {
    case "image":
      return /* @__PURE__ */ jsx(
        "image",
        {
          x: shape.x,
          y: shape.y,
          width: shape.width,
          height: shape.height,
          href: shape.imageUrl,
          preserveAspectRatio: "xMidYMid meet",
          transform: shape.transform
        }
      );
    case "ellipse":
      return /* @__PURE__ */ jsx(
        "ellipse",
        {
          cx: shape.cx,
          cy: shape.cy,
          rx: shape.width / 2,
          ry: shape.height / 2,
          fill: shape.fill,
          stroke: shape.stroke,
          strokeWidth: shape.strokeWidth,
          transform: shape.transform
        }
      );
    case "path":
      return /* @__PURE__ */ jsx(
        "path",
        {
          d: shape.pathData,
          fill: shape.fill,
          stroke: shape.stroke,
          strokeWidth: shape.strokeWidth,
          transform: shape.pathTransform
        }
      );
    default:
      return /* @__PURE__ */ jsx(
        "rect",
        {
          x: shape.x,
          y: shape.y,
          width: shape.width,
          height: shape.height,
          rx: shape.rx,
          fill: shape.fill,
          stroke: shape.stroke,
          strokeWidth: shape.strokeWidth,
          transform: shape.transform
        }
      );
  }
}
function DrawingShapeRenderer({
  elementId,
  shapes,
  allShapes = shapes,
  style,
  palette,
  nodes,
  nodeLabels
}) {
  const viewBox = computeDrawingViewBox(allShapes);
  const rendered = projectDrawingShapes(elementId, shapes, viewBox, palette, style);
  const nodeIds = nodes ? resolveRevealedDrawingShapeNodeIds(allShapes, shapes, nodes) : void 0;
  const shadow = styleShadowFilter(style);
  return /* @__PURE__ */ jsx(
    "svg",
    {
      viewBox: `0 0 ${viewBox.width} ${viewBox.height}`,
      className: "w-full h-full pointer-events-none",
      preserveAspectRatio: "xMidYMid meet",
      "data-testid": "smartart-drawing-shapes",
      children: rendered.map((shape, i) => {
        const nodeId = nodeIds?.[i];
        const nodeLabel = nodeId ? nodeLabels?.get(nodeId) : void 0;
        const groupProps = nodeId ? smartArtNodeGroupProps(nodeId, shadow, nodeLabel) : { style: { filter: shadow } };
        return /* @__PURE__ */ jsxs("g", { ...groupProps, children: [
          nodeLabel ? /* @__PURE__ */ jsx("title", { children: nodeLabel }) : null,
          shape.gradient ? /* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx(SmartArtGradient, { gradient: shape.gradient }) }) : null,
          shapeBody(shape),
          shape.textLines.length > 0 ? /* @__PURE__ */ jsx(
            SmartArtNodeText,
            {
              x: shape.textX,
              y: shape.textY,
              lines: shape.textLines,
              fill: shape.fontColor,
              fontSize: shape.fontSize,
              fontFamily: shape.fontFamily,
              fontWeight: shape.fontWeight,
              fontStyle: shape.fontStyle,
              className: "pointer-events-none"
            }
          ) : null
        ] }, shape.key);
      })
    }
  );
}
var CONNECTOR_STROKE = "#94a3b8";
var CONNECTOR_WIDTH = 1.5;
var CONNECTOR_OPACITY = 0.5;
var DEFAULT_LABEL_COLOR = "white";
function ConnectorPath({ connector }) {
  return /* @__PURE__ */ jsx(
    "path",
    {
      d: connector.d,
      fill: "none",
      stroke: connector.stroke ?? CONNECTOR_STROKE,
      strokeWidth: connector.strokeWidth ?? CONNECTOR_WIDTH,
      opacity: connector.opacity ?? CONNECTOR_OPACITY,
      strokeDasharray: connector.dash
    }
  );
}
function NodeShape({ node }) {
  if (node.kind === "circle") {
    return /* @__PURE__ */ jsx(
      "circle",
      {
        cx: node.cx,
        cy: node.cy,
        r: node.r,
        fill: node.fill,
        stroke: node.stroke,
        strokeWidth: node.strokeWidth,
        opacity: node.opacity
      }
    );
  }
  if (node.kind === "polygon") {
    return /* @__PURE__ */ jsx(
      "polygon",
      {
        points: node.points,
        fill: node.fill,
        stroke: node.stroke,
        strokeWidth: node.strokeWidth,
        opacity: node.opacity
      }
    );
  }
  return /* @__PURE__ */ jsx(
    "rect",
    {
      x: node.x,
      y: node.y,
      width: node.width,
      height: node.height,
      rx: node.rx,
      fill: node.fill,
      stroke: node.stroke,
      strokeWidth: node.strokeWidth,
      opacity: node.opacity
    }
  );
}
function labelAnchor(node) {
  if (node.kind === "circle") {
    return {
      x: node.textX ?? node.cx,
      y: node.textY ?? node.cy,
      textAnchor: node.textAnchor ?? "middle",
      baseline: node.textBaseline ?? "middle"
    };
  }
  return { x: node.textX, y: node.textY, textAnchor: "middle", baseline: "middle" };
}
function SmartArtLayoutSvg({
  layout,
  nodeIds,
  nodeLabels
}) {
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      className: "w-full h-full pointer-events-none",
      viewBox: layout.viewBox,
      preserveAspectRatio: "xMidYMid meet",
      "data-testid": `smartart-${layout.family}`,
      "data-layout-family": layout.family,
      children: [
        layout.connectors.map((connector) => /* @__PURE__ */ jsx(ConnectorPath, { connector }, connector.key)),
        layout.nodes.map((node, i) => {
          const nodeId = nodeIds[i];
          const label = nodeId ? nodeLabels?.get(nodeId) : void 0;
          const anchor = labelAnchor(node);
          return /* @__PURE__ */ jsxs(
            "g",
            {
              ...smartArtNodeGroupProps(nodeId ?? node.key, layout.shadowFilter, label),
              children: [
                label ? /* @__PURE__ */ jsx("title", { children: label }) : null,
                /* @__PURE__ */ jsx(NodeShape, { node }),
                node.text.length > 0 ? /* @__PURE__ */ jsx(
                  SmartArtNodeText,
                  {
                    x: anchor.x,
                    y: anchor.y,
                    text: node.text,
                    fill: node.fontColor ?? DEFAULT_LABEL_COLOR,
                    fontSize: node.fontSize,
                    fontWeight: node.fontWeight,
                    fontStyle: node.fontStyle,
                    textAnchor: anchor.textAnchor,
                    anchor: anchor.baseline,
                    className: "pointer-events-none"
                  }
                ) : null
              ]
            },
            node.key
          );
        })
      ]
    }
  );
}
function SmartArtRendererImpl({
  element,
  className = "",
  canEdit = false,
  onUpdateElement,
  animationState
}) {
  const { t } = useTranslation();
  if (element.type !== "smartArt" || !element.smartArtData) {
    return /* @__PURE__ */ jsx(
      "div",
      {
        className: `w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none ${className}`,
        children: t("pptx.smartArt.placeholder")
      }
    );
  }
  const smartArtData = element.smartArtData;
  const { nodes, drawingShapes, chrome } = smartArtData;
  const { nodes: revealedNodes } = resolveRevealedSmartArtNodes(
    nodes,
    animationState,
    smartArtData.presLayoutVars
  );
  const revealedShapes = drawingShapes && drawingShapes.length > 0 ? resolveRevealedDrawingShapes(drawingShapes, nodes, animationState) : drawingShapes;
  if (nodes.length === 0) {
    return /* @__PURE__ */ jsx(
      "div",
      {
        className: `w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none ${className}`,
        children: t("pptx.smartArt.placeholder")
      }
    );
  }
  const palette = resolvePalette2(element);
  const style = resolveStyle(element);
  const a11y = buildSmartArtA11y(smartArtData);
  const nodeLabels = new Map(a11y.nodes.map((n) => [n.id, n.label]));
  const editable = canEdit && Boolean(onUpdateElement) && canDrillDown(element);
  const handleCommitNodeText = (nodeId, text) => {
    if (!onUpdateElement || !shouldCommitSmartArtNodeText(smartArtData, nodeId, text)) {
      return;
    }
    const updated = updateSmartArtNodeText(smartArtData, nodeId, text);
    const box = { width: element.width, height: element.height };
    const reflowed = rebuildDrawingShapesIfCleared(
      updated,
      smartArtData.layout,
      resolveSmartArtDataPalette(updated),
      style,
      element.id,
      box
    );
    onUpdateElement({ smartArtData: reflowed });
  };
  const handleChangeNodeStyle = (nodeId, fill) => {
    if (!onUpdateElement) {
      return;
    }
    const next = setSmartArtNodeStyle(smartArtData, nodeId, { fillColor: fill });
    if (next !== smartArtData) {
      const box = { width: element.width, height: element.height };
      const reflowed = rebuildDrawingShapesIfCleared(
        next,
        smartArtData.layout,
        resolveSmartArtDataPalette(next),
        style,
        element.id,
        box
      );
      onUpdateElement({ smartArtData: reflowed });
    }
  };
  let content;
  if (revealedShapes && revealedShapes.length > 0) {
    content = /* @__PURE__ */ jsx(
      DrawingShapeRenderer,
      {
        elementId: element.id,
        shapes: revealedShapes,
        allShapes: drawingShapes,
        style,
        palette,
        nodes,
        nodeLabels
      }
    );
  } else {
    const layout = computeSmartArtElementLayout(
      smartArtData,
      revealedNodes,
      { width: element.width, height: element.height },
      palette,
      style,
      element.id
    );
    const nodeIds = flattenNodes(revealedNodes).map((n) => n.id);
    content = /* @__PURE__ */ jsx(SmartArtLayoutSvg, { layout, nodeIds, nodeLabels });
  }
  const body = editable ? /* @__PURE__ */ jsx(
    SmartArtEditableLayer,
    {
      smartArtData,
      canEdit: editable,
      onCommitNodeText: handleCommitNodeText,
      palette,
      onChangeNodeStyle: handleChangeNodeStyle,
      children: content
    }
  ) : content;
  return wrapChrome(chrome, body, className, { role: a11y.role, label: a11y.label });
}
function arePropsEqual(prev, next) {
  if (prev.className !== next.className) {
    return false;
  }
  if (prev.canEdit !== next.canEdit) {
    return false;
  }
  if (prev.onUpdateElement !== next.onUpdateElement) {
    return false;
  }
  if (prev.element.id !== next.element.id) {
    return false;
  }
  if (prev.element.type !== next.element.type) {
    return false;
  }
  if (prev.element.width !== next.element.width || prev.element.height !== next.element.height) {
    return false;
  }
  if (prev.element.x !== next.element.x || prev.element.y !== next.element.y) {
    return false;
  }
  const prevData = prev.element.type === "smartArt" ? prev.element.smartArtData : void 0;
  const nextData = next.element.type === "smartArt" ? next.element.smartArtData : void 0;
  if (prevData !== nextData) {
    return false;
  }
  const prevBuild = prev.animationState?.build;
  const nextBuild = next.animationState?.build;
  if (prevBuild?.kind !== nextBuild?.kind || prevBuild?.mode !== nextBuild?.mode || prevBuild?.progress !== nextBuild?.progress) {
    return false;
  }
  return sameDiagramReveal(prev.animationState?.diagramReveal, next.animationState?.diagramReveal);
}
function sameDiagramReveal(prev, next) {
  if (prev === next) {
    return true;
  }
  if (!prev || !next) {
    return false;
  }
  if (prev.mode !== next.mode || prev.descriptor.background !== next.descriptor.background) {
    return false;
  }
  const prevIds = prev.descriptor.nodeIds;
  const nextIds = next.descriptor.nodeIds;
  if (prevIds.size !== nextIds.size) {
    return false;
  }
  for (const id of prevIds) {
    if (!nextIds.has(id)) {
      return false;
    }
  }
  return true;
}
var SmartArtRenderer = React20.memo(SmartArtRendererImpl, arePropsEqual);
function FailedToLoad7() {
  return null;
}
var LazySmartArt3DScene = React20.lazy(
  async () => {
    try {
      return await import('./SmartArt3DScene-ODKCGJBO.mjs');
    } catch {
      return { default: FailedToLoad7 };
    }
  }
);
var SceneErrorBoundary6 = class extends React20.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function SmartArt3DRenderer({
  element,
  className,
  interactive = false,
  canEdit,
  onUpdateElement,
  animationState
}) {
  const [threeAvailable, setThreeAvailable] = useState(null);
  useEffect(() => {
    let cancelled = false;
    import('./three.module-AC7ESV4W.mjs').then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const model = useMemo(() => {
    if (element.type !== "smartArt" || !element.smartArtData) {
      return null;
    }
    const { nodes, chrome } = element.smartArtData;
    if (nodes.length === 0) {
      return null;
    }
    const palette = resolvePalette2(element);
    const style = resolveStyle(element);
    const layoutResult = computeSmartArtElementLayout(
      element.smartArtData,
      nodes,
      { width: element.width, height: element.height },
      palette,
      style,
      element.id
    );
    return buildSmartArt3DModel(layoutResult, {
      background: chrome?.backgroundColor,
      spatial: true,
      coherent3DOffNodeIds: collectCoherent3DOffNodeIds(nodes)
    });
  }, [element]);
  const svgFallback = /* @__PURE__ */ jsx(SmartArtRenderer, { element, className });
  if (!model || model.meshes.length === 0 || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return svgFallback;
  }
  const sceneNode = /* @__PURE__ */ jsx(SceneErrorBoundary6, { fallback: svgFallback, children: /* @__PURE__ */ jsx(Suspense, { fallback: svgFallback, children: /* @__PURE__ */ jsx(
    LazySmartArt3DScene,
    {
      model,
      width: element.width,
      height: element.height,
      interactive,
      textStyle: animationState?.textStyle
    }
  ) }) });
  const editEnabled = canEdit && Boolean(onUpdateElement);
  const smartArtData = element.type === "smartArt" ? element.smartArtData : void 0;
  if (!editEnabled || !smartArtData) {
    return sceneNode;
  }
  const handleCommitNodeText = (nodeId, text) => {
    if (!shouldCommitSmartArtNodeText(smartArtData, nodeId, text)) {
      return;
    }
    onUpdateElement({
      smartArtData: updateSmartArtNodeText(smartArtData, nodeId, text)
    });
  };
  return /* @__PURE__ */ jsxs("div", { className: "relative", style: { width: element.width, height: element.height }, children: [
    sceneNode,
    /* @__PURE__ */ jsx(
      SmartArtEditableLayer,
      {
        smartArtData,
        canEdit: true,
        onCommitNodeText: handleCommitNodeText,
        children: /* @__PURE__ */ jsx("div", { className: "absolute inset-0 opacity-0", children: /* @__PURE__ */ jsx(SmartArtRenderer, { element, canEdit: false }) })
      }
    )
  ] });
}
function SmartArtElement({
  element,
  className,
  canEdit,
  onUpdateElement,
  animationState
}) {
  const use3D = useContext(SmartArt3DContext);
  if (use3D) {
    return /* @__PURE__ */ jsx(
      SmartArt3DRenderer,
      {
        element,
        className,
        canEdit,
        onUpdateElement,
        animationState
      }
    );
  }
  return /* @__PURE__ */ jsx(
    SmartArtRenderer,
    {
      element,
      className,
      canEdit,
      onUpdateElement,
      animationState
    }
  );
}
function groupIntoParagraphs2(element, fieldContext) {
  return groupIntoParagraphs(
    element,
    fieldContext ? (seg) => {
      if (seg.fieldType) {
        const substituted = substituteFieldText(seg.text, seg.fieldType, fieldContext);
        if (substituted !== seg.text) {
          return { ...seg, text: substituted };
        }
      }
      return seg;
    } : void 0
  );
}
function getAlignmentProps(align) {
  switch (align) {
    case "center":
      return { startOffset: "50%", textAnchor: "middle" };
    case "right":
      return { startOffset: "100%", textAnchor: "end" };
    case "left":
    case "justify":
    default:
      return { startOffset: "0%", textAnchor: "start" };
  }
}
function getSegmentTspanProps(segment, element, fallbackColor) {
  const s = segment.style || {};
  const decos = [];
  if (s.underline || s.hyperlink) {
    decos.push("underline");
  }
  if (s.strikethrough) {
    decos.push("line-through");
  }
  const fill = s.hyperlink ? normalizeHexColor(s.color || element.textStyle?.color, HYPERLINK_COLOR) : normalizeHexColor(s.color || element.textStyle?.color, fallbackColor);
  return {
    fill,
    fontSize: s.fontSize ?? element.textStyle?.fontSize ?? DEFAULT_TEXT_FONT_SIZE,
    fontWeight: s.bold ? 700 : 400,
    fontStyle: s.italic ? "italic" : void 0,
    fontFamily: s.fontFamily || element.textStyle?.fontFamily ? getSubstituteFontFamily(s.fontFamily || element.textStyle?.fontFamily || "") : DEFAULT_FONT_FAMILY,
    textDecoration: decos.length > 0 ? decos.join(" ") : void 0
  };
}
function resolveSegmentFont(segment, element) {
  const s = segment.style || {};
  const family = s.fontFamily || element.textStyle?.fontFamily;
  return {
    fontFamily: family ? getSubstituteFontFamily(family) : DEFAULT_FONT_FAMILY,
    fontSizePx: s.fontSize ?? element.textStyle?.fontSize ?? DEFAULT_TEXT_FONT_SIZE,
    bold: s.bold ?? element.textStyle?.bold,
    italic: s.italic ?? element.textStyle?.italic
  };
}
function EnvelopeGlyph({
  glyphKey,
  glyph,
  tspanProps
}) {
  if (!glyph.slices || glyph.slices.length <= 1) {
    return /* @__PURE__ */ jsx("text", { x: glyph.x, y: glyph.y, transform: glyph.transform, ...tspanProps, children: glyph.char });
  }
  return /* @__PURE__ */ jsx("g", { "data-glyph-slices": glyph.slices.length, children: glyph.slices.map((slice, si) => {
    const clipId = `${glyphKey}-s${si}`;
    return /* @__PURE__ */ jsxs(React20.Fragment, { children: [
      /* @__PURE__ */ jsx("clipPath", { id: clipId, clipPathUnits: "userSpaceOnUse", children: /* @__PURE__ */ jsx(
        "rect",
        {
          x: slice.clipX0,
          y: -1e5,
          width: Math.max(0, slice.clipX1 - slice.clipX0),
          height: 2e5
        }
      ) }),
      /* @__PURE__ */ jsx(
        "text",
        {
          x: glyph.x,
          y: glyph.y,
          transform: slice.transform,
          clipPath: `url(#${clipId})`,
          ...tspanProps,
          children: glyph.char
        }
      )
    ] }, clipId);
  }) });
}
function EnvelopeLine({
  paraSegments,
  glyphs,
  element,
  fallbackColor,
  lineIdPrefix
}) {
  return /* @__PURE__ */ jsx(Fragment, { children: glyphs.map((g, i) => /* @__PURE__ */ jsx(
    EnvelopeGlyph,
    {
      glyphKey: `${lineIdPrefix}-g${i}`,
      glyph: g,
      tspanProps: getSegmentTspanProps(paraSegments[g.segmentIndex], element, fallbackColor)
    },
    i
  )) });
}
function WarpedText({
  element,
  width,
  height,
  fallbackColor,
  fieldContext
}) {
  if (!hasTextProperties(element)) {
    return null;
  }
  const textEl = element;
  const preset = textEl.textStyle?.textWarpPreset;
  if (!preset || !shouldUseSvgWarp(preset)) {
    return null;
  }
  const paragraphs = groupIntoParagraphs2(textEl, fieldContext);
  if (paragraphs.length === 0) {
    return null;
  }
  const lineCount = paragraphs.length;
  const pathIdPrefix = `warp-${element.id}`;
  const warpAdj = textEl.textStyle?.textWarpAdj;
  const warpAdj2 = textEl.textStyle?.textWarpAdj2;
  const align = textEl.textStyle?.align ?? "center";
  const { startOffset, textAnchor } = getAlignmentProps(align);
  const baseFontSize = textEl.textStyle?.fontSize ?? DEFAULT_TEXT_FONT_SIZE;
  const baseFontFamily = textEl.textStyle?.fontFamily ? getSubstituteFontFamily(textEl.textStyle.fontFamily) : DEFAULT_FONT_FAMILY;
  const baseFill = normalizeHexColor(textEl.textStyle?.color, fallbackColor);
  if (hasGlyphEnvelope(preset)) {
    return /* @__PURE__ */ jsx(
      "svg",
      {
        width,
        height,
        viewBox: `0 0 ${width} ${height}`,
        xmlns: "http://www.w3.org/2000/svg",
        style: { overflow: "visible", position: "absolute", inset: 0, pointerEvents: "none" },
        "aria-hidden": "true",
        children: paragraphs.map((para, paraIdx) => {
          const segsInput = para.segments.map((seg, i) => ({
            text: seg.text,
            font: resolveSegmentFont(seg, textEl),
            segmentIndex: i
          }));
          const glyphs = buildGlyphEnvelope(
            preset,
            segsInput,
            width,
            height,
            align,
            warpAdj,
            warpAdj2,
            paraIdx,
            lineCount
          );
          return /* @__PURE__ */ jsx(
            EnvelopeLine,
            {
              paraSegments: para.segments,
              glyphs,
              element: textEl,
              fallbackColor,
              lineIdPrefix: `warp-${element.id}-l${paraIdx}`
            },
            `envelope-line-${paraIdx}`
          );
        })
      }
    );
  }
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      width,
      height,
      viewBox: `0 0 ${width} ${height}`,
      xmlns: "http://www.w3.org/2000/svg",
      style: {
        overflow: "visible",
        position: "absolute",
        inset: 0,
        pointerEvents: "none"
      },
      "aria-hidden": "true",
      children: [
        /* @__PURE__ */ jsx("defs", { children: paragraphs.map((_para, i) => /* @__PURE__ */ jsx(
          "path",
          {
            id: `${pathIdPrefix}-${i}`,
            d: getWarpPath(preset, width, height, i, lineCount, warpAdj, warpAdj2),
            fill: "none"
          },
          `${pathIdPrefix}-def-${i}`
        )) }),
        paragraphs.map((para, paraIdx) => /* @__PURE__ */ jsx(
          "text",
          {
            fontSize: baseFontSize,
            fontFamily: baseFontFamily,
            fill: baseFill,
            fontWeight: textEl.textStyle?.bold ? 700 : 400,
            fontStyle: textEl.textStyle?.italic ? "italic" : "normal",
            children: /* @__PURE__ */ jsx(
              "textPath",
              {
                href: `#${pathIdPrefix}-${paraIdx}`,
                startOffset,
                textAnchor,
                children: para.segments.map((seg, segIdx) => /* @__PURE__ */ jsx(
                  "tspan",
                  {
                    ...getSegmentTspanProps(seg, textEl, fallbackColor),
                    children: seg.text
                  },
                  `${pathIdPrefix}-ts-${paraIdx}-${segIdx}`
                ))
              }
            )
          },
          `${pathIdPrefix}-txt-${paraIdx}`
        ))
      ]
    }
  );
}
var GLYPH_BY_SHAPE = Object.fromEntries(
  ACTION_BUTTON_PRESETS.map((p) => [p.shapeType, p.iconPath])
);
GLYPH_BY_SHAPE["actionButtonForwardOrNext"] = GLYPH_BY_SHAPE["actionButtonForwardNext"];
GLYPH_BY_SHAPE["actionButtonBackOrPrevious"] = GLYPH_BY_SHAPE["actionButtonBackPrevious"];
function isActionButtonShape(shapeType) {
  return Boolean(shapeType && shapeType in GLYPH_BY_SHAPE);
}
function getActionButtonGlyphPath(shapeType) {
  if (!shapeType) {
    return void 0;
  }
  const path = GLYPH_BY_SHAPE[shapeType];
  return path && path.length > 0 ? path : void 0;
}
function ActionButtonGlyphOverlay({
  element,
  color
}) {
  const shapeType = "shapeType" in element ? element.shapeType : void 0;
  const path = getActionButtonGlyphPath(shapeType);
  if (!path) {
    return null;
  }
  const stroke = color ?? ("textStyle" in element && element.textStyle?.color || "#ffffff");
  return /* @__PURE__ */ jsx(
    "svg",
    {
      viewBox: "0 0 24 24",
      width: "100%",
      height: "100%",
      preserveAspectRatio: "xMidYMid meet",
      style: {
        position: "absolute",
        inset: 0,
        pointerEvents: "none",
        padding: "20%"
      },
      "aria-hidden": "true",
      children: /* @__PURE__ */ jsx(
        "path",
        {
          d: path,
          fill: "none",
          stroke,
          strokeWidth: 2,
          strokeLinecap: "round",
          strokeLinejoin: "round"
        }
      )
    }
  );
}
function renderTextElementBody(options) {
  const {
    el,
    vecShape,
    isTxtEl,
    txtS,
    txtSE,
    findHl,
    onHyperlinkClick,
    fieldContext,
    presentationElementStates,
    isPresentationPassive,
    placeholderPromptMode = "present",
    slideElements
  } = options;
  const isLinkedTextBox2 = isLinkedTextBox(el);
  const linkedSegments = getOverflowSegments(el, slideElements);
  const useSvgWarp = shouldUseSvgWarp(
    hasTextProperties(el) ? el.textStyle?.textWarpPreset : void 0
  );
  const scene3dStyle = hasTextProperties(el) ? buildTextBody3DSceneStyle(el.textStyle, { width: el.width, height: el.height }) : void 0;
  const rotationTransform = getTextBodyRotationTransform(el);
  const composedTransform = [rotationTransform, getTextCompensationTransform(el), scene3dStyle?.transform].filter(Boolean).join(" ") || void 0;
  const transformStyle = {
    transform: composedTransform,
    // A COM-measured homography (`scene3dStyle.transformOrigin === '0 0'`)
    // MUST win over the default centred origin, or the matrix3d's baked-in
    // translation (relative to the element's own top-left) is applied from
    // the wrong pivot and the projection is wrong (see
    // `visual-3d-camera-homography`'s module doc comment). This used to be
    // hardcoded to `'center'` unconditionally, silently clobbering
    // `scene3dStyle.transformOrigin` the same way Svelte's `ElementRenderer`
    // once re-clobbered `pointerEvents` from its own interactive flag.
    transformOrigin: scene3dStyle?.transformOrigin ?? "center",
    ...scene3dStyle?.perspective ? { perspective: scene3dStyle.perspective } : {},
    // COM-calibrated off-axis correction for a handful of camera presets
    // (see `visual-3d-camera`'s module doc comment); only reachable when an
    // explicit `a:camera/a:rot`/`@fov`/`@zoom` override sits alongside one of
    // those presets, since the homography path above never sets it.
    ...scene3dStyle?.perspectiveOrigin ? { perspectiveOrigin: scene3dStyle.perspectiveOrigin } : {},
    ...scene3dStyle?.transformStyle ? { transformStyle: scene3dStyle.transformStyle } : {},
    ...isLinkedTextBox2 ? { overflow: "hidden" } : {}
  };
  const shapeType = "shapeType" in el ? el.shapeType : void 0;
  const placeholderPrompt = placeholderPromptDescriptor(el, placeholderPromptMode);
  const shouldRenderText = isTxtEl || placeholderPrompt !== null;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    vecShape,
    isActionButtonShape(shapeType) && /* @__PURE__ */ jsx(ActionButtonGlyphOverlay, { element: el }),
    shouldRenderText && (useSvgWarp ? /* @__PURE__ */ jsx(
      "div",
      {
        className: cn(
          "relative z-10 w-full h-full",
          onHyperlinkClick ? "" : "pointer-events-none"
        ),
        style: { ...getTextLayoutStyle(el), ...transformStyle },
        children: /* @__PURE__ */ jsx(
          WarpedText,
          {
            element: el,
            width: el.width,
            height: el.height,
            fallbackColor: DEFAULT_TEXT_COLOR,
            findHighlights: findHl,
            fieldContext
          }
        )
      }
    ) : /* @__PURE__ */ jsx(
      "div",
      {
        className: cn(
          "relative z-10 w-full h-full whitespace-pre-wrap break-words leading-[1.3]",
          onHyperlinkClick ? "" : "pointer-events-none"
        ),
        style: {
          ...getTextLayoutStyle(el),
          ...txtS,
          ...getTextWarpStyle(txtSE),
          ...transformStyle
        },
        children: renderTextSegments(
          el,
          DEFAULT_TEXT_COLOR,
          void 0,
          findHl,
          onHyperlinkClick,
          fieldContext,
          presentationElementStates,
          linkedSegments ?? void 0,
          !isPresentationPassive,
          placeholderPrompt
        )
      }
    ))
  ] });
}
function ZoomElementRenderer({
  element,
  slides,
  isPresentationMode,
  onZoomClick,
  sourceSlideIndex
}) {
  const { t } = useTranslation();
  const summaryView = buildSummaryZoomView(
    element,
    (index) => {
      const slide = slides?.[index];
      return slide ? {
        slideNumber: slide.slideNumber,
        sectionName: slide.sectionName,
        backgroundColor: slide.backgroundColor
      } : void 0;
    },
    // The tile captions and aria-labels are built in shared; without this the
    // slide subtitle and the container's aria-label stay English.
    (key, params) => t(key, params ?? {})
  );
  if (summaryView) {
    return /* @__PURE__ */ jsxs(
      "div",
      {
        className: "zoom-element-container zoom-summary-container",
        style: summaryView.containerStyle,
        "data-zoom-type": "summary",
        "data-testid": "zoom-element",
        role: "group",
        "aria-label": summaryView.ariaLabel,
        children: [
          summaryView.tiles.map((tile) => /* @__PURE__ */ jsx(
            "div",
            {
              className: "zoom-summary-tile",
              style: {
                ...tile.style,
                backgroundColor: tile.backgroundColor,
                overflow: "hidden",
                border: "1px solid rgba(0, 0, 0, 0.12)",
                cursor: isPresentationMode ? "pointer" : "default"
              },
              "data-zoom-target": tile.targetSlideIndex,
              "data-section-id": tile.sectionId,
              role: isPresentationMode ? "button" : void 0,
              tabIndex: isPresentationMode ? 0 : -1,
              "aria-label": tile.ariaLabel,
              onClick: (event) => {
                if (isPresentationMode && onZoomClick) {
                  event.stopPropagation();
                  onZoomClick(tile.targetSlideIndex, sourceSlideIndex ?? 0);
                }
              },
              onKeyDown: (event) => {
                if (isPresentationMode && onZoomClick && (event.key === "Enter" || event.key === " ")) {
                  event.preventDefault();
                  event.stopPropagation();
                  onZoomClick(tile.targetSlideIndex, sourceSlideIndex ?? 0);
                }
              },
              children: tile.imageSrc ? /* @__PURE__ */ jsx(
                "img",
                {
                  src: tile.imageSrc,
                  alt: tile.ariaLabel,
                  style: { width: "100%", height: "100%", objectFit: "contain" }
                }
              ) : /* @__PURE__ */ jsxs(Fragment, { children: [
                /* @__PURE__ */ jsx("div", { children: tile.label }),
                /* @__PURE__ */ jsx("div", { children: tile.slideLabel })
              ] })
            },
            tile.key
          )),
          /* @__PURE__ */ jsx("div", { style: { position: "absolute", right: 4, bottom: 4, fontSize: 9 }, children: t("pptx.zoom.summaryZoom") })
        ]
      }
    );
  }
  const targetSlide = slides?.[element.targetSlideIndex];
  const handleClick = (e) => {
    if (!isPresentationMode || !onZoomClick) {
      return;
    }
    e.stopPropagation();
    onZoomClick(element.targetSlideIndex, sourceSlideIndex ?? 0);
  };
  const handleKeyDown = (e) => {
    if (!isPresentationMode || !onZoomClick) {
      return;
    }
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      e.stopPropagation();
      onZoomClick(element.targetSlideIndex, sourceSlideIndex ?? 0);
    }
  };
  const hasPreviewImage = Boolean(element.imageData);
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "zoom-element-container",
      style: {
        width: "100%",
        height: "100%",
        position: "relative",
        cursor: isPresentationMode ? "pointer" : "default",
        overflow: "hidden",
        borderRadius: "4px",
        boxShadow: "0 2px 8px rgba(0, 0, 0, 0.15)",
        transition: "box-shadow 0.2s ease, transform 0.15s ease"
      },
      "data-zoom-type": element.zoomType,
      "data-zoom-target": element.targetSlideIndex,
      "data-testid": "zoom-element",
      onClick: handleClick,
      onKeyDown: handleKeyDown,
      role: isPresentationMode ? "button" : void 0,
      tabIndex: isPresentationMode ? 0 : -1,
      "aria-label": `Zoom to slide ${element.targetSlideIndex + 1}${element.zoomType === "section" && element.targetSectionId ? ` (section: ${element.targetSectionId})` : ""}`,
      children: [
        hasPreviewImage ? renderImg(
          element,
          {
            width: "100%",
            height: "100%",
            objectFit: "contain"
          },
          void 0,
          `Preview of slide ${element.targetSlideIndex + 1}`
        ) : /* @__PURE__ */ jsx(ZoomSlideThumbnail, { targetSlide, slideIndex: element.targetSlideIndex }),
        isPresentationMode && /* @__PURE__ */ jsx(
          "div",
          {
            className: "zoom-hover-overlay",
            style: {
              position: "absolute",
              inset: 0,
              background: "transparent",
              transition: "background 0.2s ease",
              pointerEvents: "none"
            }
          }
        ),
        /* @__PURE__ */ jsx(
          "div",
          {
            style: {
              position: "absolute",
              bottom: 4,
              right: 4,
              fontSize: "9px",
              padding: "1px 4px",
              borderRadius: "2px",
              backgroundColor: "rgba(0, 0, 0, 0.5)",
              color: "#fff",
              pointerEvents: "none",
              lineHeight: "1.4"
            },
            children: element.zoomType === "section" ? t("pptx.zoom.sectionZoom") : t("pptx.zoom.slideZoom")
          }
        )
      ]
    }
  );
}
function ZoomSlideThumbnail({
  targetSlide,
  slideIndex
}) {
  const bgColor = targetSlide?.backgroundColor || "#f0f0f0";
  const slideLabel = targetSlide ? `Slide ${targetSlide.slideNumber}` : `Slide ${slideIndex + 1}`;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "zoom-thumbnail",
      style: {
        width: "100%",
        height: "100%",
        backgroundColor: bgColor,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        border: "1px solid rgba(0, 0, 0, 0.1)"
      },
      children: [
        /* @__PURE__ */ jsx(
          "div",
          {
            style: {
              fontSize: "14px",
              fontWeight: 600,
              color: "rgba(0, 0, 0, 0.5)",
              marginBottom: "4px"
            },
            children: slideLabel
          }
        ),
        targetSlide?.sectionName && /* @__PURE__ */ jsx(
          "div",
          {
            style: {
              fontSize: "10px",
              color: "rgba(0, 0, 0, 0.4)"
            },
            children: targetSlide.sectionName
          }
        )
      ]
    }
  );
}
function renderBody(options) {
  const {
    el,
    isImg,
    isEditing,
    editText,
    spellCheck,
    txtSE,
    txtS,
    vecShape,
    imgStyle,
    imgFilter,
    imgOpacity,
    imgAlt,
    isTxtEl,
    media,
    tableSt,
    isSel,
    doInk,
    doGrp,
    renderGroupChild,
    onEditChange,
    onCommit,
    onCancel,
    onCellSel,
    onCellCommit,
    onColResize,
    onRowResize,
    isPresentationPassive,
    isStaticSurface,
    handleMediaPlayStateChange,
    allSlides,
    onZoomClick,
    sourceSlideIndex,
    tableStyleContext,
    onFormatText,
    canEditSmartArt,
    onUpdateSmartArtElement,
    canEditChart,
    onUpdateChartElement,
    presentationElementStates
  } = options;
  const animationState = presentationElementStates?.get(el.id);
  if (el.type === "model3d") {
    return /* @__PURE__ */ jsx(
      Model3DRenderer,
      {
        element: el,
        width: el.width,
        height: el.height,
        interactive: !isPresentationPassive
      }
    );
  }
  if (el.type === "zoom") {
    return /* @__PURE__ */ jsx(
      ZoomElementRenderer,
      {
        element: el,
        slides: allSlides,
        isPresentationMode: isPresentationPassive,
        onZoomClick,
        sourceSlideIndex
      }
    );
  }
  if (isImg) {
    return /* @__PURE__ */ jsxs("div", { className: "absolute inset-0 pointer-events-none", style: getImageSurfaceStyle(el), children: [
      renderImg(el, imgStyle, imgFilter, imgAlt, imgOpacity),
      vecShape ? /* @__PURE__ */ jsx("div", { className: "pointer-events-none absolute inset-0", children: vecShape }) : null
    ] });
  }
  if (isEditing) {
    return /* @__PURE__ */ jsxs(Fragment, { children: [
      vecShape,
      /* @__PURE__ */ jsx(
        InlineTextEditor,
        {
          initialText: editText,
          spellCheck,
          rtl: txtSE?.rtl,
          textDirection: txtSE?.textDirection,
          textStyle: txtS,
          textStyleRaw: txtSE,
          layoutStyle: getTextLayoutStyle(el),
          element: el,
          onCommit,
          onCancel,
          onEditChange,
          onFormatText
        }
      )
    ] });
  }
  if (el.type === "table") {
    return renderTableElement(el, txtS, {
      editable: isSel,
      selectedCell: isSel ? tableSt : null,
      onSelectCell: onCellSel,
      onCommitCellEdit: onCellCommit,
      onResizeColumns: onColResize,
      onResizeRow: onRowResize,
      styleCtx: tableStyleContext
    });
  }
  if (el.type === "chart") {
    return /* @__PURE__ */ jsx(
      ChartElementView,
      {
        element: el,
        editable: Boolean(isSel && canEditChart),
        onUpdateElement: onUpdateChartElement,
        animationState
      }
    );
  }
  if (el.type === "smartArt") {
    return /* @__PURE__ */ jsx(
      SmartArtElement,
      {
        element: el,
        canEdit: canEditSmartArt,
        onUpdateElement: onUpdateSmartArtElement,
        animationState
      }
    );
  }
  if (el.type === "media") {
    return renderMediaElement(el, media, {
      // Present mode autoplays every media element on the active slide,
      // not only ones persisted with their own `autoPlay` flag (matching
      // the other four bindings' unconditional `presenting`-gated
      // `startMediaAutoplay` call): a clip inserted via Insert > Media
      // never gets that flag, so gating on `el.autoPlay` left it silently
      // paused the moment the show reached its slide.
      autoPlay: isPresentationPassive === true,
      fullScreen: isPresentationPassive && Boolean(el.fullScreen),
      isPresentationMode: isPresentationPassive,
      // A still of a slide never carries a transport, whatever the canvas
      // does. The rule is shared so the five bindings cannot drift on it.
      showTransport: mediaTransportVisible({
        presenting: isPresentationPassive === true,
        preview: isStaticSurface === true,
        canvasTransport: true
      }),
      // ...and never the play badge / placeholder box either: a still is
      // slide content, and the transition overlay paints one (issue #147).
      preview: isStaticSurface === true,
      onPlayStateChange: handleMediaPlayStateChange
    });
  }
  if (doInk && isInkElement(el)) {
    return renderInk(el, {
      replay: isPresentationPassive,
      pressureSensitive: true
    });
  }
  if (el.type === "contentPart") {
    return renderContentPart(el, {
      replay: isPresentationPassive
    });
  }
  if (el.type === "ole") {
    return /* @__PURE__ */ jsx(OleRenderer, { element: el });
  }
  if (doGrp && el.type === "group" && el.children) {
    if (renderGroupChild) {
      return /* @__PURE__ */ jsx("div", { className: "relative w-full h-full pointer-events-none", children: el.children.map(renderGroupChild) });
    }
    return renderGroup(el.children, getGroupChildParentFill(el));
  }
  if (shouldRenderFallbackLabel(el, isTxtEl)) {
    return /* @__PURE__ */ jsx("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none", children: getElementLabel(el) });
  }
  return renderTextElementBody(options);
}

// src/viewer/components/static-element-renderer-utils.ts
function defaultTextColorForFill(fillColor) {
  const hex = String(fillColor ?? "").replace(/^#/u, "");
  if (!/^[0-9a-f]{6}$/iu.test(hex)) {
    return DEFAULT_TEXT_COLOR;
  }
  const channels = [0, 2, 4].map((offset) => Number.parseInt(hex.slice(offset, offset + 2), 16) / 255).map((channel) => channel <= 0.04045 ? channel / 12.92 : ((channel + 0.055) / 1.055) ** 2.4);
  const luminance = channels[0] * 0.2126 + channels[1] * 0.7152 + channels[2] * 0.0722;
  return luminance > 0.179 ? DEFAULT_TEXT_COLOR : "#ffffff";
}
function getStaticElementVisualState(element, parentGroupFill) {
  const style = hasShapeProperties(element) ? element.shapeStyle : void 0;
  const hasFill = style?.fillColor !== void 0 && style.fillColor !== "transparent" || Boolean(buildCssGradientFromShapeStyle(style) || style?.fillGradient) || style?.fillMode === "pattern" && Boolean(style.fillPatternPreset);
  const fill = normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR);
  const strokeWidth = paintedStrokeWidth(style);
  const stroke = normalizeHexColor(style?.strokeColor, DEFAULT_STROKE_COLOR);
  const baseVisualStyle = getShapeVisualStyle(element, hasFill, fill, strokeWidth, stroke);
  const inheritedFill = resolveGroupChildFill(element, parentGroupFill);
  const visualStyle = inheritedFill ? {
    ...baseVisualStyle,
    backgroundColor: inheritedFill.backgroundColor,
    backgroundImage: inheritedFill.backgroundImage,
    backgroundRepeat: inheritedFill.backgroundRepeat,
    backgroundSize: inheritedFill.backgroundSize,
    backgroundPosition: inheritedFill.backgroundPosition
  } : baseVisualStyle;
  const textStyle = getTextStyleForElement(
    element,
    element.type === "shape" && hasFill ? defaultTextColorForFill(fill) : DEFAULT_TEXT_COLOR
  );
  const isImage = element.type === "picture" || element.type === "image";
  const letsTextOverflow = hasTextProperties(element) && element.textStyle?.autoFitMode === "shrink" && (element.type === "text" || element.locks?.txBox === true);
  const shapeType = hasShapeProperties(element) ? element.shapeType : void 0;
  const isCallout = isWedgeCalloutPresetShape(shapeType) || String(shapeType ?? "").toLowerCase().includes("callout");
  return {
    hasFill,
    fill,
    strokeWidth,
    stroke,
    visualStyle,
    textStyle,
    isImage,
    letsTextOverflow,
    isCallout
  };
}
function getStaticElementInteractionState(element, hasActionHandler, exposeElementId) {
  const action = element.actionClick;
  const isActionable = Boolean(action && hasActionHandler);
  const contractRole = exposeElementId ? getAriaRole(element, { actionable: isElementActionable(element) }) : isActionable ? "button" : void 0;
  return { action, isActionable, contractRole };
}
function getStaticElementWrapperClassName(element, visual, positioned, isActionable) {
  const overflowClass = visual.isImage || element.type === "group" || isConnectorOrLineElement(element) || visual.isCallout || visual.letsTextOverflow ? "" : "overflow-hidden";
  const pointerClass = isActionable ? "pointer-events-auto cursor-pointer" : "pointer-events-none";
  return `${positioned ? "absolute" : "relative"} ${overflowClass} ${pointerClass}`;
}
var noop = () => {
};
var EMPTY_MEDIA_DATA_URLS = /* @__PURE__ */ new Map();
function StaticElementRendererImpl({
  element,
  activeSlide,
  allSlides,
  mediaDataUrls = EMPTY_MEDIA_DATA_URLS,
  sourceSlideIndex,
  zIndex,
  positioned = true,
  fieldContext,
  tableStyleContext,
  parentGroupFill,
  onActionClick,
  actionRequiresModifier = false,
  animation,
  imageAnimation,
  exposeElementId = false,
  suppressReflection = false
}) {
  if (!isElementRendered(element)) {
    return null;
  }
  const {
    hasFill,
    fill,
    strokeWidth,
    stroke,
    visualStyle,
    textStyle,
    isImage,
    letsTextOverflow,
    isCallout
  } = getStaticElementVisualState(element, parentGroupFill);
  const { action, isActionable, contractRole } = getStaticElementInteractionState(
    element,
    Boolean(onActionClick),
    exposeElementId
  );
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-static-element-type": element.type,
      "data-element-id": exposeElementId ? element.id : void 0,
      "data-pptx-element": exposeElementId ? "true" : void 0,
      "data-pptx-action": isActionable ? "click" : void 0,
      className: getStaticElementWrapperClassName(
        element,
        { isImage, letsTextOverflow, isCallout },
        positioned,
        isActionable
      ),
      role: contractRole,
      "aria-label": exposeElementId ? getAriaLabel(element) : void 0,
      "aria-roledescription": exposeElementId ? getAriaRoleDescription(element) : void 0,
      tabIndex: isActionable ? 0 : void 0,
      title: isActionable ? action?.tooltip || action?.url || void 0 : void 0,
      onClick: isActionable ? (event) => {
        if (actionRequiresModifier && !event.ctrlKey && !event.metaKey) {
          return;
        }
        event.stopPropagation();
        event.preventDefault();
        onActionClick?.(element.id, action);
      } : void 0,
      onKeyDown: isActionable ? (event) => {
        if (event.key !== "Enter" && event.key !== " ") {
          return;
        }
        event.preventDefault();
        event.stopPropagation();
        onActionClick?.(element.id, action);
      } : void 0,
      style: {
        left: positioned ? element.x : void 0,
        top: positioned ? element.y : void 0,
        width: positioned ? Math.max(element.width, 1) : "100%",
        height: positioned ? Math.max(element.height, 1) : "100%",
        transform: positioned ? getElementTransform(element) : void 0,
        transformOrigin: "center",
        zIndex,
        ...visualStyle,
        ...isImage ? {
          backgroundColor: "transparent",
          backgroundImage: void 0,
          backgroundRepeat: void 0,
          backgroundSize: void 0,
          backgroundPosition: void 0,
          borderRadius: void 0,
          clipPath: void 0,
          overflow: "visible"
        } : {},
        animation
      },
      children: [
        /* @__PURE__ */ jsx(ShapeEffectOverlay, { element, suppressReflection }),
        element.type === "group" ? /* @__PURE__ */ jsx("div", { className: "relative w-full h-full", children: (element.children ?? []).map((child, index) => /* @__PURE__ */ jsx(
          StaticElementRenderer,
          {
            element: child,
            activeSlide,
            allSlides,
            mediaDataUrls,
            sourceSlideIndex,
            zIndex: index,
            fieldContext,
            tableStyleContext,
            parentGroupFill: getGroupChildParentFill(element, parentGroupFill),
            onActionClick,
            actionRequiresModifier,
            exposeElementId
          },
          child.id
        )) }) : renderBody({
          el: element,
          isImg: isImage,
          isEditing: false,
          editText: "",
          spellCheck: false,
          txtSE: hasTextProperties(element) ? element.textStyle : void 0,
          txtS: textStyle,
          vecShape: renderVectorShape(
            element,
            isImage ? false : hasFill,
            fill,
            strokeWidth,
            stroke
          ),
          imgStyle: imageAnimation ? { ...getImageRenderStyle(element), animation: imageAnimation } : getImageRenderStyle(element),
          imgFilter: getImageEffectsFilter(element),
          imgOpacity: getImageEffectsOpacity(element),
          imgAlt: "",
          isTxtEl: isEditableTextElement(element),
          media: mediaDataUrls,
          tableSt: null,
          isSel: false,
          doInk: true,
          doGrp: false,
          onEditChange: noop,
          onCommit: noop,
          onCancel: noop,
          isPresentationPassive: false,
          // This renderer only ever paints a STILL of a slide (presenter
          // console panes, thumbnails, previews). It is not in presentation
          // mode, so without saying so a video here would carry Chrome's
          // scrubber over a slide nobody can play.
          isStaticSurface: true,
          placeholderPromptMode: "thumbnail",
          slideElements: activeSlide?.elements,
          allSlides,
          sourceSlideIndex,
          fieldContext,
          tableStyleContext,
          canEditSmartArt: false,
          canEditChart: false
        })
      ]
    }
  );
}
var StaticElementRenderer = React20.memo(StaticElementRendererImpl);
StaticElementRenderer.displayName = "StaticElementRenderer";
var PREVIEW_CANVAS_WIDTH = 1280;
var PREVIEW_CANVAS_HEIGHT = 720;
var PREVIEW_TILE_WIDTH = 144;
var PREVIEW_SCALE = PREVIEW_TILE_WIDTH / PREVIEW_CANVAS_WIDTH;
var PREVIEW_TILE_HEIGHT = Math.round(PREVIEW_CANVAS_HEIGHT * PREVIEW_SCALE);
function SlideTemplatePreview({
  templateId,
  scheme
}) {
  const previewSlide = useMemo(() => {
    const content = buildSlideTemplateContent(templateId, {
      slideWidth: PREVIEW_CANVAS_WIDTH,
      slideHeight: PREVIEW_CANVAS_HEIGHT,
      ...scheme ? { scheme } : {},
      idFor: (index) => `tpl-preview-${templateId}-${index}`
    });
    return {
      id: `tpl-preview-${templateId}`,
      rId: "",
      slideNumber: 1,
      elements: content.elements,
      ...content.backgroundColor ? { backgroundColor: content.backgroundColor } : {}
    };
  }, [templateId, scheme]);
  return /* @__PURE__ */ jsx(
    "div",
    {
      "aria-hidden": "true",
      className: "overflow-hidden pointer-events-none rounded",
      style: {
        width: PREVIEW_TILE_WIDTH,
        height: PREVIEW_TILE_HEIGHT,
        backgroundColor: previewSlide.backgroundColor ?? "#FFFFFF"
      },
      children: /* @__PURE__ */ jsx(
        "div",
        {
          style: {
            position: "relative",
            width: PREVIEW_CANVAS_WIDTH,
            height: PREVIEW_CANVAS_HEIGHT,
            transform: `scale(${PREVIEW_SCALE})`,
            transformOrigin: "top left"
          },
          children: previewSlide.elements.map((element, index) => /* @__PURE__ */ jsx(
            StaticElementRenderer,
            {
              element,
              activeSlide: previewSlide,
              allSlides: [previewSlide],
              zIndex: index
            },
            element.id
          ))
        }
      )
    }
  );
}
function useModalFocus(open, panelRef, onClose, initialFocusRef) {
  const onCloseRef = useRef(onClose);
  useEffect(() => {
    onCloseRef.current = onClose;
  }, [onClose]);
  useEffect(() => {
    const panel = panelRef.current;
    if (!open || !panel) {
      return;
    }
    return activateModalFocus(panel, {
      initialFocus: initialFocusRef?.current,
      onEscape: () => onCloseRef.current()
    });
  }, [initialFocusRef, open, panelRef]);
}
function SlideTemplateGalleryDialog({
  isOpen,
  onClose,
  onInsert,
  scheme
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const [selected, setSelected] = useState(null);
  const dialogRef = useRef(null);
  useModalFocus(isOpen, dialogRef, onClose);
  const handleInsert = useCallback(() => {
    if (!selected) {
      return;
    }
    onInsert(selected);
    onClose();
  }, [selected, onInsert, onClose]);
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        onClick: onClose,
        "aria-label": t("pptx.slideTemplates.close")
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            ref: dialogRef,
            style: panelStyle,
            className: "pointer-events-auto w-[640px] max-w-[92vw] max-h-[80vh] rounded-lg border border-border bg-background shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.slideTemplates.galleryTitle"),
            tabIndex: -1,
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-4 py-3 border-b border-border touch-none",
                  children: [
                    /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-0.5", children: [
                      /* @__PURE__ */ jsx("h2", { className: "text-sm font-medium text-foreground", children: t("pptx.slideTemplates.galleryTitle") }),
                      /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.slideTemplates.galleryDescription") })
                    ] }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "p-1 rounded hover:bg-muted transition-colors",
                        "aria-label": t("pptx.slideTemplates.close"),
                        children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsx("div", { className: "flex-1 p-3 overflow-y-auto", children: /* @__PURE__ */ jsx(
                "div",
                {
                  className: "grid grid-cols-3 gap-2",
                  role: "listbox",
                  "aria-label": t("pptx.slideTemplates.gallery"),
                  children: SLIDE_TEMPLATES.map((spec) => /* @__PURE__ */ jsxs(
                    "button",
                    {
                      type: "button",
                      role: "option",
                      "aria-selected": selected === spec.id,
                      "aria-label": t(spec.nameKey),
                      title: t(spec.descriptionKey),
                      onClick: () => setSelected(spec.id),
                      onDoubleClick: () => {
                        onInsert(spec.id);
                        onClose();
                      },
                      className: cn(
                        "flex flex-col items-center gap-1 p-2 rounded border transition-colors",
                        selected === spec.id ? "border-primary bg-primary/20" : "border-border hover:border-border hover:bg-muted/50"
                      ),
                      children: [
                        /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center bg-muted rounded", children: /* @__PURE__ */ jsx(SlideTemplatePreview, { templateId: spec.id, scheme }) }),
                        /* @__PURE__ */ jsx("span", { className: "text-[10px] text-foreground text-center leading-tight", children: t(spec.nameKey) })
                      ]
                    },
                    spec.id
                  ))
                }
              ) }),
              /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 px-4 py-3 border-t border-border", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 text-xs rounded bg-muted hover:bg-accent text-foreground transition-colors",
                    children: t("pptx.slideTemplates.cancel")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: handleInsert,
                    disabled: !selected,
                    className: cn(
                      "px-3 py-1.5 text-xs rounded transition-colors",
                      selected ? "bg-primary hover:bg-primary/80 text-white" : "bg-muted text-muted-foreground cursor-not-allowed"
                    ),
                    children: t("pptx.slideTemplates.insert")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
var CATEGORIES2 = [
  {
    group: "entrance",
    labelKey: "pptx.animation.entrance",
    tone: "text-emerald-500",
    presets: ENTRANCE_PRESET_VALUES
  },
  {
    group: "emphasis",
    labelKey: "pptx.animation.emphasis",
    tone: "text-amber-500",
    presets: EMPHASIS_PRESET_VALUES
  },
  {
    group: "exit",
    labelKey: "pptx.animation.exit",
    tone: "text-red-500",
    presets: EXIT_PRESET_VALUES
  }
];
function AnimationPresetGallery({
  disabled,
  onAddAnimation
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "flex max-h-[62px] items-start gap-2 overflow-y-auto rounded-sm border border-border/60 bg-muted/30 px-1.5 py-1",
      "aria-label": t("pptx.animations.galleryAria"),
      children: CATEGORIES2.map((category) => /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: "text-[9px] font-semibold leading-3 text-muted-foreground", children: t(category.labelKey) }),
        /* @__PURE__ */ jsx("div", { className: "flex max-w-[150px] flex-wrap gap-0.5", children: category.presets.map((preset) => {
          const label = t(`pptx.animation.preset.${preset}`);
          return /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled,
              onClick: () => onAddAnimation?.(preset, category.group),
              title: label,
              className: "inline-flex items-center gap-0.5 rounded-sm px-1 py-0.5 text-[9px] leading-3 text-foreground transition-colors hover:bg-accent disabled:opacity-35",
              children: [
                /* @__PURE__ */ jsx(
                  LuStar,
                  {
                    className: `h-2.5 w-2.5 fill-current ${category.tone}`,
                    "aria-hidden": "true"
                  }
                ),
                /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap", children: label })
              ]
            },
            preset
          );
        }) })
      ] }, category.group))
    }
  );
}
function MotionPathGallery({
  disabled,
  onApplyMotionPath
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "flex max-h-[62px] items-start gap-2 overflow-y-auto rounded-sm border border-border/60 bg-muted/30 px-1.5 py-1",
      "aria-label": t("pptx.animations.motionPathGalleryAria"),
      children: MOTION_PATH_FAMILIES.map((family) => /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: "text-[9px] font-semibold leading-3 text-muted-foreground", children: t(motionPathFamilyLabelKey(family)) }),
        /* @__PURE__ */ jsx("div", { className: "flex max-w-[150px] flex-wrap gap-0.5", children: motionPathPresetsByFamily(family).map((preset) => {
          const label = t(motionPathPresetLabelKey(preset.id));
          return /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled,
              onClick: () => onApplyMotionPath?.(preset.id),
              title: label,
              className: "inline-flex items-center gap-0.5 rounded-sm px-1 py-0.5 text-[9px] leading-3 text-foreground transition-colors hover:bg-accent disabled:opacity-35",
              children: [
                /* @__PURE__ */ jsx(LuMoveRight, { className: "h-2.5 w-2.5 text-sky-500", "aria-hidden": "true" }),
                /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap", children: label })
              ]
            },
            preset.id
          );
        }) })
      ] }, family))
    }
  );
}
function RibbonGroup({
  label,
  children,
  className
}) {
  return /* @__PURE__ */ jsxs(
    "section",
    {
      className: cn(
        "relative flex min-h-[78px] shrink-0 items-start gap-1 border-r border-border/60 px-2 pb-4 pt-1 last:border-r-0",
        className
      ),
      "aria-label": label,
      children: [
        children,
        /* @__PURE__ */ jsx("span", { className: "pointer-events-none absolute inset-x-1 bottom-0.5 truncate text-center text-[9px] leading-3 text-muted-foreground", children: label })
      ]
    }
  );
}
function RibbonCommand({
  label,
  icon,
  onClick,
  disabled,
  active,
  compact = false,
  title,
  pressed
}) {
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      onClick,
      disabled,
      title: title ?? label,
      "aria-pressed": pressed,
      className: cn(
        "inline-flex rounded-sm text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-35",
        active && "bg-primary/15 text-primary ring-1 ring-primary/35",
        compact ? "h-[26px] min-w-[88px] items-center gap-1.5 px-1.5 text-left text-[10px] leading-3" : "h-[58px] min-w-[54px] max-w-[72px] flex-col items-center justify-start gap-0.5 px-1 py-1 text-center text-[9px] leading-[11px]"
      ),
      children: [
        /* @__PURE__ */ jsx(
          "span",
          {
            className: cn(
              "grid shrink-0 place-items-center text-primary",
              compact ? "h-4 w-4 [&>svg]:h-4 [&>svg]:w-4" : "h-7 w-7 [&>svg]:h-6 [&>svg]:w-6"
            ),
            "aria-hidden": "true",
            children: icon
          }
        ),
        /* @__PURE__ */ jsx("span", { children: label })
      ]
    }
  );
}
function RibbonCommandStack({
  children
}) {
  return /* @__PURE__ */ jsx("div", { className: "flex flex-col justify-start gap-0.5", children });
}
function RibbonToggle({
  label,
  checked,
  onChange,
  disabled,
  title
}) {
  return /* @__PURE__ */ jsxs(
    "label",
    {
      title,
      className: cn(
        "flex h-[19px] items-center gap-1 whitespace-nowrap rounded-sm px-1 text-[10px] text-foreground",
        checked && "bg-primary/15 text-primary"
      ),
      children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            checked,
            disabled,
            onChange: (event) => onChange?.(event.target.checked),
            className: "h-3 w-3 accent-primary disabled:opacity-35"
          }
        ),
        label
      ]
    }
  );
}
function AnimationsSection(p) {
  const { t } = useTranslation();
  const [previewActive, setPreviewActive] = useState(false);
  const disabled = !p.canEdit || p.selectedElement === null;
  const preview = () => {
    if (disabled) {
      return;
    }
    setPreviewActive(true);
    setTimeout(() => setPreviewActive(false), 1200);
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(RibbonGroup, { label: t("pptx.animations.preview"), children: /* @__PURE__ */ jsx(
      RibbonCommand,
      {
        label: t("pptx.animations.preview"),
        icon: /* @__PURE__ */ jsx(LuPlay, {}),
        onClick: preview,
        disabled,
        active: previewActive,
        title: t("pptx.animations.previewTooltip")
      }
    ) }),
    /* @__PURE__ */ jsx(
      RibbonGroup,
      {
        label: t("pptx.animations.animation", { defaultValue: "Animation" }),
        className: "max-w-[500px] overflow-hidden",
        children: /* @__PURE__ */ jsx(AnimationPresetGallery, { disabled, onAddAnimation: p.onAddAnimation })
      }
    ),
    /* @__PURE__ */ jsx(RibbonGroup, { label: t("pptx.animation.motionPath"), className: "max-w-[420px] overflow-hidden", children: /* @__PURE__ */ jsx(
      MotionPathGallery,
      {
        disabled,
        onApplyMotionPath: (presetId) => p.onAddAnimation?.(presetId, "motionPath")
      }
    ) }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.animations.advanced", { defaultValue: "Advanced Animation" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.animations.exitEffects", { defaultValue: "Exit Effects" }),
          icon: /* @__PURE__ */ jsx(LuStar, { className: "text-red-500" }),
          onClick: () => p.onAddAnimation?.("fadeOut", "exit"),
          disabled
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.animations.pathAnimation", { defaultValue: "Path Animation" }),
          icon: /* @__PURE__ */ jsx(LuMoveRight, {}),
          onClick: () => p.onAddAnimation?.(DEFAULT_MOTION_PATH_PRESET_ID, "motionPath"),
          disabled
        }
      ),
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.effectOptions", { defaultValue: "Effect Options" }),
            icon: /* @__PURE__ */ jsx(LuSparkles, {}),
            onClick: p.onOpenAnimationPanel ?? p.onToggleInspector,
            disabled
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.animationPanel"),
            icon: /* @__PURE__ */ jsx(LuPanelRight, {}),
            onClick: p.onOpenAnimationPanel ?? p.onToggleInspector,
            active: p.isInspectorPaneOpen,
            title: t("pptx.animations.openPanelTooltip")
          }
        )
      ] }),
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.trigger", { defaultValue: "Trigger" }),
            icon: /* @__PURE__ */ jsx(LuMousePointerClick, {}),
            onClick: p.onOpenAnimationPanel ?? p.onToggleInspector,
            disabled
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.painter", { defaultValue: "Animation Painter" }),
            icon: /* @__PURE__ */ jsx(LuPaintbrush, {}),
            disabled: true
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.animations.remove"),
          icon: /* @__PURE__ */ jsx(LuTrash2, {}),
          onClick: p.onRemoveAnimation,
          disabled,
          title: t("pptx.animations.removeTooltip")
        }
      )
    ] }),
    /* @__PURE__ */ jsx(RibbonGroup, { label: t("pptx.animations.timing", { defaultValue: "Timing" }), children: /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[48px_82px] items-center gap-x-1 gap-y-1 text-[10px]", children: [
      /* @__PURE__ */ jsx("label", { htmlFor: "pptx-animation-start", children: t("pptx.animations.start", { defaultValue: "Start" }) }),
      /* @__PURE__ */ jsxs(
        "select",
        {
          id: "pptx-animation-start",
          disabled: true,
          className: "h-6 rounded-sm border border-border bg-muted px-1 text-[10px]",
          children: [
            /* @__PURE__ */ jsx("option", { children: t("pptx.animations.onClick", { defaultValue: "On Click" }) }),
            /* @__PURE__ */ jsx("option", { children: t("pptx.animations.withPrevious", { defaultValue: "With Previous" }) }),
            /* @__PURE__ */ jsx("option", { children: t("pptx.animations.afterPrevious", { defaultValue: "After Previous" }) })
          ]
        }
      ),
      /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx(LuClock3, {}),
        " ",
        t("pptx.animations.duration", { defaultValue: "Duration" })
      ] }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "number",
          min: "0",
          step: "0.1",
          defaultValue: "0.5",
          disabled: true,
          "aria-label": t("pptx.animations.duration", { defaultValue: "Duration" }),
          className: "h-6 rounded-sm border border-border bg-muted px-1 text-[10px]"
        }
      )
    ] }) })
  ] });
}
var _b = "inline-flex items-center justify-center px-2.5 py-1.5 max-md:min-h-[44px] max-md:min-w-[44px] active:scale-95 active:opacity-80";
var gB = `${_b} border-r border-border hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed`;
var gL = `${_b} hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed`;
var grp = "inline-flex items-center rounded bg-muted text-xs overflow-hidden";
var pill = "inline-flex items-center gap-1.5 px-2.5 py-1.5 max-md:min-h-[44px] rounded bg-muted hover:bg-accent text-xs transition-colors active:scale-95 active:opacity-80 disabled:opacity-40 disabled:cursor-not-allowed";
var sep = /* @__PURE__ */ jsx("div", { className: "w-px self-stretch bg-border/40 mx-1 max-md:hidden" });
var ic = "w-4 h-4";
var ics = "w-3.5 h-3.5";
var ALIGN_BTNS = [
  { k: "left", el: /* @__PURE__ */ jsx(LuAlignLeft, { className: ic }) },
  { k: "center", el: /* @__PURE__ */ jsx(LuAlignCenter, { className: ic }) },
  { k: "right", el: /* @__PURE__ */ jsx(LuAlignRight, { className: ic }) },
  { k: "top", el: /* @__PURE__ */ jsx(LuChevronUp, { className: ic }) },
  { k: "middle", el: /* @__PURE__ */ jsx(LuAlignCenter, { className: `${ic} rotate-90` }) },
  { k: "bottom", el: /* @__PURE__ */ jsx(LuChevronDown, { className: ic }) }
];
var DISTRIBUTE_BTNS = [
  { k: "horizontal", el: /* @__PURE__ */ jsx(LuAlignHorizontalSpaceAround, { className: ic }) },
  { k: "vertical", el: /* @__PURE__ */ jsx(LuAlignVerticalSpaceAround, { className: ic }) }
];
var DRAW_TOOLS = [
  {
    id: "select",
    icon: /* @__PURE__ */ jsx(LuMoveRight, { className: ic }),
    labelKey: "pptx.ribbon.tool.select",
    ac: "bg-primary text-white"
  },
  {
    id: "pen",
    icon: /* @__PURE__ */ jsx(LuPencil, { className: ic }),
    labelKey: "pptx.ribbon.tool.pen",
    ac: "bg-primary text-white"
  },
  {
    id: "highlighter",
    icon: /* @__PURE__ */ jsx(LuType, { className: ic }),
    labelKey: "pptx.ribbon.tool.highlighter",
    ac: "bg-yellow-600 text-white"
  },
  {
    id: "eraser",
    icon: /* @__PURE__ */ jsx(LuMinus, { className: ic }),
    labelKey: "pptx.ribbon.tool.eraser",
    ac: "bg-red-600 text-white"
  },
  {
    id: "freeform",
    icon: /* @__PURE__ */ jsx(LuSpline, { className: ic }),
    labelKey: "pptx.ribbon.tool.freeform",
    ac: "bg-primary text-white"
  }
];
var OV = [
  {
    k: "png",
    labelKey: "pptx.ribbon.exportPng",
    i: /* @__PURE__ */ jsx(LuDownload, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "pdf",
    labelKey: "pptx.ribbon.exportPdf",
    i: /* @__PURE__ */ jsx(LuFileText, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "video",
    labelKey: "pptx.ribbon.exportVideo",
    i: /* @__PURE__ */ jsx(LuVideo, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "gif",
    labelKey: "pptx.ribbon.exportGif",
    i: /* @__PURE__ */ jsx(LuImage, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "pptx",
    labelKey: "pptx.file.saveAsPptxTooltip",
    i: /* @__PURE__ */ jsx(LuDownload, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "ppsx",
    labelKey: "pptx.file.saveAsPpsxTooltip",
    i: /* @__PURE__ */ jsx(LuPlay, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "pptm",
    labelKey: "pptx.file.saveAsPptmTooltip",
    i: /* @__PURE__ */ jsx(LuDatabase, { className: `${ics} text-muted-foreground` })
  },
  { k: "---0", labelKey: "", i: null },
  {
    k: "print",
    labelKey: "pptx.print.printButton",
    i: /* @__PURE__ */ jsx(LuPrinter, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "copyImg",
    labelKey: "pptx.file.copyImageTooltip",
    i: /* @__PURE__ */ jsx(LuCopy, { className: `${ics} text-muted-foreground` })
  },
  { k: "---", labelKey: "", i: null },
  {
    k: "a11y",
    labelKey: "pptx.ribbon.accessibilityCheck",
    i: /* @__PURE__ */ jsx(LuCheck, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "shortcuts",
    labelKey: "pptx.settings.keyboardShortcuts",
    i: /* @__PURE__ */ jsx(LuSearch, { className: `${ics} text-muted-foreground` })
  },
  { k: "---2", labelKey: "", i: null },
  {
    k: "versionHistory",
    labelKey: "pptx.ribbon.versionHistory",
    i: /* @__PURE__ */ jsx(LuClock, { className: `${ics} text-muted-foreground` })
  },
  { k: "---3", labelKey: "", i: null },
  {
    k: "documentProperties",
    labelKey: "pptx.ribbon.documentProperties",
    i: /* @__PURE__ */ jsx(LuInfo, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "passwordProtection",
    labelKey: "pptx.security.protectPresentation",
    i: /* @__PURE__ */ jsx(LuLock, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "fontEmbedding",
    labelKey: "pptx.ribbon.embedFonts",
    i: /* @__PURE__ */ jsx(LuType, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "digitalSignatures",
    labelKey: "pptx.viewer.digitalSignatures",
    i: /* @__PURE__ */ jsx(LuShieldAlert, { className: `${ics} text-muted-foreground` })
  }
];
var FMT = [
  { id: "bold", i: /* @__PURE__ */ jsx(LuBold, { className: ic }), labelKey: "pptx.textPanel.bold" },
  { id: "italic", i: /* @__PURE__ */ jsx(LuItalic, { className: ic }), labelKey: "pptx.textPanel.italic" },
  { id: "underline", i: /* @__PURE__ */ jsx(LuUnderline, { className: ic }), labelKey: "pptx.textPanel.underline" },
  {
    id: "strikethrough",
    i: /* @__PURE__ */ jsx(LuStrikethrough, { className: ic }),
    labelKey: "pptx.textPanel.strikethrough"
  }
];
var ATXT = [
  { id: "left", i: /* @__PURE__ */ jsx(LuAlignLeft, { className: ic }), labelKey: "pptx.ribbon.alignLeft" },
  { id: "center", i: /* @__PURE__ */ jsx(LuAlignCenter, { className: ic }), labelKey: "pptx.ribbon.alignCenter" },
  { id: "right", i: /* @__PURE__ */ jsx(LuAlignRight, { className: ic }), labelKey: "pptx.ribbon.alignRight" },
  { id: "justify", i: /* @__PURE__ */ jsx(LuAlignJustify, { className: ic }), labelKey: "pptx.ribbon.justify" }
];
function ShapeArrangeExtras(p) {
  const { t } = useTranslation();
  const canGroup = canGroupSelection(p.canEdit, p.selectedCount, p.selectionGroupable);
  const canUngroup = canUngroupSelection(p.canEdit, p.selectedElement);
  const canStrokeWidth = canSetStrokeWidth(p.canEdit, p.selectedElement);
  const strokeWidth = strokeWidthOf(p.selectedElement);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: grp, children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: p.onGroupElements,
          disabled: !canGroup,
          className: gB,
          title: t("pptx.contextMenu.group"),
          "aria-label": t("pptx.contextMenu.group"),
          children: /* @__PURE__ */ jsx(LuGroup, { className: ic })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: p.onUngroupElement,
          disabled: !canUngroup,
          className: gL,
          title: t("pptx.contextMenu.ungroup"),
          "aria-label": t("pptx.contextMenu.ungroup"),
          children: /* @__PURE__ */ jsx(LuUngroup, { className: ic })
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "number",
        min: "0",
        max: "120",
        step: "0.5",
        disabled: !canStrokeWidth,
        "aria-label": t("pptx.ribbon.strokeWidth"),
        title: t("pptx.ribbon.strokeWidth"),
        value: strokeWidth,
        onChange: (event) => {
          const next = Number(event.target.value);
          if (Number.isFinite(next)) {
            p.onUpdateElementStyle({ strokeWidth: Math.max(0, next) });
          }
        },
        className: "h-[26px] w-[52px] rounded border border-border bg-muted px-1 text-center text-[11px] text-foreground disabled:opacity-40"
      }
    )
  ] });
}
function ArrangeSection(p) {
  const { t } = useTranslation();
  const hasSel = Boolean(p.selectedElement);
  const canMut = hasSel && p.canEdit;
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
      /* @__PURE__ */ jsx("div", { className: grp, children: ALIGN_BTNS.map((a, i, arr) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: () => p.onAlignElements(a.k),
          disabled: !canMut,
          className: i < arr.length - 1 ? gB : gL,
          title: t("pptx.arrange.align", { direction: a.k }),
          children: a.el
        },
        a.k
      )) }),
      /* @__PURE__ */ jsx("div", { className: grp, children: DISTRIBUTE_BTNS.map((d, i, arr) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: () => p.onDistributeElements(d.k),
          disabled: !p.canEdit || !p.canDistribute,
          className: i < arr.length - 1 ? gB : gL,
          title: t(`pptx.arrange.distribute${d.k.charAt(0).toUpperCase()}${d.k.slice(1)}`),
          children: d.el
        },
        d.k
      )) }),
      p.onToggleFormatPainter && /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          onClick: p.onToggleFormatPainter,
          disabled: !p.canEdit || p.canActivateFormatPainter === false && !p.formatPainterActive,
          "data-testid": "format-painter-toggle",
          "data-active": p.formatPainterActive ? "true" : "false",
          className: cn(
            pill,
            p.formatPainterActive ? "bg-amber-600 hover:bg-amber-500 text-amber-50" : ""
          ),
          title: t("pptx.arrange.formatPainter"),
          children: [
            /* @__PURE__ */ jsx(LuPaintbrush, { className: ic }),
            t("pptx.arrange.format")
          ]
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: grp, children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: () => p.onFlip("horizontal"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.flipHorizontally"),
            children: t("pptx.arrange.flipH")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: () => p.onFlip("vertical"),
            disabled: !canMut,
            className: gL,
            title: t("pptx.arrange.flipVertically"),
            children: t("pptx.arrange.flipV")
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        ShapeArrangeExtras,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          selectedCount: p.selectedCount,
          selectionGroupable: p.selectionGroupable,
          onGroupElements: p.onGroupElements,
          onUngroupElement: p.onUngroupElement,
          onUpdateElementStyle: p.onUpdateElementStyle
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: grp, children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            onClick: () => p.onMoveLayer("backward"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.sendBackward"),
            children: /* @__PURE__ */ jsx(LuChevronDown, { className: ic })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            onClick: () => p.onMoveLayer("forward"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.bringForward"),
            children: /* @__PURE__ */ jsx(LuChevronUp, { className: ic })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            onClick: () => p.onMoveLayerToEdge("back"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.sendToBack"),
            children: t("pptx.arrange.back")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            onClick: () => p.onMoveLayerToEdge("front"),
            disabled: !canMut,
            className: gL,
            title: t("pptx.arrange.bringToFront"),
            children: t("pptx.arrange.front")
          }
        )
      ] }),
      /* @__PURE__ */ jsxs(
        "button",
        {
          onClick: p.onDuplicate,
          disabled: !canMut,
          className: pill,
          title: t("pptx.arrange.duplicate"),
          children: [
            /* @__PURE__ */ jsx(LuCopy, { className: ic }),
            t("pptx.arrange.duplicate")
          ]
        }
      ),
      /* @__PURE__ */ jsxs(
        "button",
        {
          onClick: p.onDelete,
          disabled: !canMut,
          className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded bg-red-700/80 hover:bg-red-600 disabled:opacity-40 disabled:cursor-not-allowed text-xs transition-colors",
          title: t("pptx.arrange.delete"),
          children: [
            /* @__PURE__ */ jsx(LuTrash2, { className: ic }),
            t("pptx.arrange.delete")
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: t("pptx.arrange.groupLabel") })
  ] });
}
function TransitionsSection(p) {
  const { t } = useTranslation();
  const canEdit = p.canEdit !== false;
  const draft = React20.useMemo(() => readRibbonTransitionDraft(p.activeSlide), [p.activeSlide]);
  const [durationBuffer, setDurationBuffer] = React20.useState(null);
  const [advanceBuffer, setAdvanceBuffer] = React20.useState(null);
  const commit = React20.useCallback(
    (changes) => {
      p.onTransitionChange(ribbonTransitionUpdates({ ...draft, ...changes }));
    },
    [draft, p]
  );
  const soundFileInputRef = React20.useRef(null);
  const handleSoundFilePicked = React20.useCallback(
    (file) => {
      void readSoundFileAsDataUrl(file).then((dataUrl) => {
        if (dataUrl) {
          p.onTransitionChange(applyTransitionSoundFile({ name: file.name, dataUrl }));
        }
        return void 0;
      });
    },
    [p]
  );
  const handleSoundSelectChange = (event) => {
    const value = event.target.value;
    if (value === TRANSITION_SOUND_OTHER_VALUE) {
      soundFileInputRef.current?.click();
      event.target.value = transitionSoundSelectedValue(p.activeSlide?.transition);
      return;
    }
    if (value === TRANSITION_SOUND_NONE_VALUE) {
      p.onTransitionChange(clearTransitionSound());
    }
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: pill,
        title: t("pptx.ribbon.previewTransition"),
        onClick: () => playSlideTransitionPreview(p.activeSlide?.transition, document),
        children: [
          /* @__PURE__ */ jsx(LuPlay, { className: ics }),
          t("pptx.ribbon.preview")
        ]
      }
    ),
    sep,
    /* @__PURE__ */ jsx("div", { className: "inline-flex items-center gap-0.5 overflow-x-auto max-w-[420px]", children: RIBBON_TRANSITION_PRESETS.map((preset) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        onClick: () => commit({ type: preset.type }),
        className: cn(
          "flex-shrink-0 px-2 py-1 max-md:min-h-[44px] rounded border text-[11px] leading-tight transition-colors",
          draft.type === preset.type ? "border-primary bg-primary/10 text-primary font-medium" : "border-border bg-muted hover:bg-accent text-foreground"
        ),
        title: t("pptx.ribbon.transitionTitle", { name: t(preset.labelKey) }),
        children: t(preset.labelKey)
      },
      preset.type
    )) }),
    sep,
    /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-1.5 text-xs text-muted-foreground", children: [
      /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.duration") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "number",
          min: 0,
          max: 20,
          step: 0.25,
          disabled: !canEdit,
          value: durationBuffer ?? String(draft.durationSec),
          onChange: (e) => {
            setDurationBuffer(e.target.value);
            const seconds = Number(e.target.value);
            if (Number.isFinite(seconds) && e.target.value !== "") {
              commit({ durationSec: seconds });
            }
          },
          onBlur: () => setDurationBuffer(null),
          className: "w-16 px-1.5 py-1 rounded border border-border bg-muted text-xs text-foreground text-center",
          title: t("pptx.ribbon.transitionDurationTitle")
        }
      )
    ] }),
    sep,
    /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-1.5 text-xs text-muted-foreground", children: [
      /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.sound") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.ribbon.sound"),
          className: "w-24 px-1.5 py-1 rounded border border-border bg-muted text-xs text-foreground disabled:opacity-50",
          disabled: !canEdit,
          value: transitionSoundSelectedValue(p.activeSlide?.transition),
          onChange: handleSoundSelectChange,
          children: transitionSoundOptions(p.activeSlide?.transition).map((option) => /* @__PURE__ */ jsx("option", { value: option.value, children: option.i18nKey ? t(option.i18nKey) : option.label }, option.value))
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          ref: soundFileInputRef,
          type: "file",
          accept: "audio/*",
          className: "hidden",
          onChange: (e) => {
            const file = e.target.files?.[0];
            if (file) {
              handleSoundFilePicked(file);
            }
            e.target.value = "";
          }
        }
      )
    ] }),
    sep,
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: pill,
        title: t("pptx.ribbon.applyTransitionToAll"),
        onClick: p.onApplyTransitionToAll,
        children: [
          /* @__PURE__ */ jsx(LuCopy, { className: ics }),
          t("pptx.headerFooter.applyToAll")
        ]
      }
    ),
    sep,
    /* @__PURE__ */ jsxs("div", { className: "inline-flex flex-col gap-1 text-xs text-muted-foreground", children: [
      /* @__PURE__ */ jsx("span", { className: "text-[10px] font-medium text-foreground", children: t("pptx.ribbon.advanceSlide") }),
      /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-1.5 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: draft.advanceOnClick,
            onChange: (e) => commit({ advanceOnClick: e.target.checked }),
            className: "accent-primary h-3 w-3"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.onMouseClick") })
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-1.5 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            "aria-label": t("pptx.ribbon.afterDuration"),
            disabled: !canEdit,
            checked: draft.advanceAfter,
            onChange: (e) => commit({ advanceAfter: e.target.checked }),
            className: "accent-primary h-3 w-3"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.afterDuration") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            "aria-label": t("pptx.ribbon.advanceAfterSeconds"),
            value: advanceBuffer ?? draft.advanceAfterText,
            onChange: (e) => {
              setAdvanceBuffer(e.target.value);
              commit({ advanceAfter: true, advanceAfterText: e.target.value });
            },
            onBlur: () => setAdvanceBuffer(null),
            disabled: !canEdit || !draft.advanceAfter,
            className: "w-16 px-1 py-0.5 rounded border border-border bg-muted text-xs text-foreground text-center disabled:opacity-50",
            title: t("pptx.ribbon.advanceAfterSeconds")
          }
        )
      ] })
    ] }),
    sep,
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: p.onToggleInspector,
        className: cn(
          pill,
          p.isInspectorPaneOpen ? "bg-primary hover:bg-primary/80 text-white" : ""
        ),
        title: t("pptx.ribbon.openInspectorTransitions"),
        children: [
          /* @__PURE__ */ jsx(LuPanelRight, { className: ic }),
          t("pptx.ribbon.inspector")
        ]
      }
    )
  ] });
}
function DesignSection(p) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onToggleThemeGallery,
        disabled: !p.canEdit,
        className: cn(
          pill,
          p.isThemeGalleryOpen ? "bg-primary hover:bg-primary/80 text-white" : ""
        ),
        title: t("pptx.ribbon.browseThemesTitle"),
        children: [
          /* @__PURE__ */ jsx(LuPalette, { className: ics }),
          t("pptx.ribbon.browseThemes")
        ]
      }
    ),
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onToggleThemeEditor,
        disabled: !p.canEdit,
        className: cn(pill, p.isThemeEditorOpen ? "bg-primary hover:bg-primary/80 text-white" : ""),
        title: t("pptx.ribbon.editThemeTitle"),
        children: [
          /* @__PURE__ */ jsx(LuPencil, { className: ics }),
          t("pptx.ribbon.editTheme")
        ]
      }
    ),
    sep,
    (p.onOpenSlideSize ?? p.onOpenDocumentProperties) && /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onOpenSlideSize ?? p.onOpenDocumentProperties,
        className: pill,
        title: t("pptx.ribbon.slideSizeTitle"),
        children: [
          /* @__PURE__ */ jsx(LuMonitor, { className: ics }),
          t("pptx.ribbon.slideSize")
        ]
      }
    ),
    p.onToggleInspector && /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onToggleInspector,
        className: cn(
          pill,
          p.isInspectorPaneOpen ? "bg-primary hover:bg-primary/80 text-white" : ""
        ),
        title: t("pptx.ribbon.formatBackgroundTitle"),
        children: [
          /* @__PURE__ */ jsx(LuPaintBucket, { className: ics }),
          t("pptx.ribbon.formatBackground")
        ]
      }
    )
  ] });
}
var DEFAULT_VALUE = {
  recentColors: [],
  pushColor: () => {
  }
};
var RecentColorsContext = createContext(DEFAULT_VALUE);
var RecentColorsProvider = RecentColorsContext.Provider;
function useRecentColors() {
  return useContext(RecentColorsContext);
}
function DrawSection(p) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const colorRef = useRef(null);
  useEffect(() => {
    const el = colorRef.current;
    if (!el) {
      return;
    }
    const handler = () => pushColor(el.value);
    el.addEventListener("change", handler);
    return () => el.removeEventListener("change", handler);
  }, [pushColor]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("div", { className: grp, children: DRAW_TOOLS.map((tool, i, a) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: () => p.onSetActiveTool(tool.id),
        className: cn(
          i < a.length - 1 ? gB : gL,
          p.activeTool === tool.id ? tool.ac ?? "bg-accent text-foreground" : ""
        ),
        title: t(tool.labelKey),
        children: tool.icon
      },
      tool.id
    )) }),
    /* @__PURE__ */ jsxs("div", { className: "inline-flex items-center gap-2 text-xs", children: [
      /* @__PURE__ */ jsxs(
        "label",
        {
          className: "inline-flex items-center gap-1 text-muted-foreground",
          title: t("pptx.ribbon.penColour"),
          children: [
            t("pptx.ribbon.colour"),
            /* @__PURE__ */ jsx(
              "input",
              {
                ref: colorRef,
                type: "color",
                value: p.drawingColor,
                onChange: (e) => p.onSetDrawingColor(e.target.value),
                className: "w-6 h-6 rounded border border-border bg-transparent cursor-pointer"
              }
            )
          ]
        }
      ),
      /* @__PURE__ */ jsxs(
        "label",
        {
          className: "inline-flex items-center gap-1 text-muted-foreground",
          title: t("pptx.ribbon.strokeWidth"),
          children: [
            t("pptx.ribbon.width"),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "range",
                min: 1,
                max: 12,
                value: p.drawingWidth,
                onChange: (e) => p.onSetDrawingWidth(Number(e.target.value)),
                className: "w-16 h-1 accent-primary"
              }
            ),
            /* @__PURE__ */ jsx("span", { className: "text-foreground w-4 text-right", children: p.drawingWidth })
          ]
        }
      )
    ] })
  ] });
}
var AccountAuthContext = createContext(void 0);
var cardClass = "border border-border bg-card p-6 text-card-foreground";
function AccountPage() {
  const { t } = useTranslation();
  const accountAuth = useContext(AccountAuthContext);
  const [profile, setProfile] = useState(
    () => readStoredViewerPrefs().profile ?? DEFAULT_VIEWER_PROFILE
  );
  const updateProfile = (patch) => {
    const next = { ...profile, ...patch };
    setProfile(next);
    saveViewerProfile(next);
  };
  const [usage, setUsage] = useState(null);
  const [cleared, setCleared] = useState(false);
  const refreshUsage = () => {
    void getLocalStorageUsageSummary().then(setUsage);
  };
  useEffect(refreshUsage, []);
  const handleClear = () => {
    if (!window.confirm(t("pptx.account.storage.clearConfirm"))) {
      return;
    }
    void clearAllLocalViewerData().then(() => {
      setCleared(true);
      refreshUsage();
      setTimeout(() => setCleared(false), 4e3);
      return void 0;
    });
  };
  const version = "3.8.0" ;
  return /* @__PURE__ */ jsxs("div", { className: "mt-8 max-w-[700px] space-y-6", children: [
    /* @__PURE__ */ jsxs("section", { className: cardClass, children: [
      /* @__PURE__ */ jsxs("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        /* @__PURE__ */ jsx(LuUser, { className: "text-primary" }),
        " ",
        t("pptx.account.profile.title")
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "mt-4 flex items-center gap-4", children: [
        /* @__PURE__ */ jsx(
          "span",
          {
            className: "grid size-14 shrink-0 place-items-center rounded-full text-xl font-semibold text-white",
            style: { background: profile.avatarColor },
            children: resolveProfileInitial(profile)
          }
        ),
        /* @__PURE__ */ jsxs("div", { className: "min-w-0 flex-1", children: [
          /* @__PURE__ */ jsx("label", { htmlFor: "pptx-account-name", className: "text-xs text-muted-foreground", children: t("pptx.account.profile.nameLabel") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              id: "pptx-account-name",
              type: "text",
              value: profile.displayName,
              onChange: (e) => updateProfile({ displayName: e.target.value }),
              placeholder: t("pptx.account.profile.namePlaceholder"),
              className: "mt-1 h-9 w-full border border-input bg-background px-3 text-sm outline-none focus:border-ring"
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ jsx("p", { className: "mt-4 text-xs text-muted-foreground", children: t("pptx.account.profile.avatarColorLabel") }),
      /* @__PURE__ */ jsx("div", { className: "mt-2 flex gap-2", children: AVATAR_COLOR_SWATCHES.map((color) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          "aria-label": color,
          "aria-pressed": profile.avatarColor === color,
          onClick: () => updateProfile({ avatarColor: color }),
          className: cn(
            "size-7 rounded-full border-2 transition-transform",
            profile.avatarColor === color ? "border-foreground scale-110" : "border-transparent hover:scale-105"
          ),
          style: { background: color }
        },
        color
      )) })
    ] }),
    /* @__PURE__ */ jsxs("section", { className: cardClass, children: [
      /* @__PURE__ */ jsxs("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        /* @__PURE__ */ jsx(LuHardDrive, { className: "text-primary" }),
        " ",
        t("pptx.account.storage.title")
      ] }),
      /* @__PURE__ */ jsx("p", { className: "mt-3 text-sm text-muted-foreground", children: usage && usage.presentationCount > 0 ? t("pptx.account.storage.usage", {
        count: usage.presentationCount,
        size: formatBackstageSize(usage.totalBytes)
      }) : t("pptx.account.storage.usageEmpty") }),
      /* @__PURE__ */ jsx("p", { className: "mt-3 text-xs leading-5 text-muted-foreground", children: t("pptx.account.storage.privacyBlurb") }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: handleClear,
          className: "mt-4 border border-destructive/40 px-4 py-2 text-xs font-semibold text-destructive hover:bg-destructive/10",
          children: t("pptx.account.storage.clear")
        }
      ),
      cleared && /* @__PURE__ */ jsx("p", { className: "mt-2 text-xs text-primary", children: t("pptx.account.storage.clearedNotice") })
    ] }),
    /* @__PURE__ */ jsxs("section", { className: cardClass, children: [
      /* @__PURE__ */ jsxs("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        /* @__PURE__ */ jsx(LuInfo, { className: "text-primary" }),
        " ",
        t("pptx.account.about.title")
      ] }),
      /* @__PURE__ */ jsxs("p", { className: "mt-3 text-sm text-muted-foreground", children: [
        "pptx-react-viewer",
        ` \xB7 ${t("pptx.account.about.version", { version })}` 
      ] })
    ] }),
    accountAuth?.enabled && /* @__PURE__ */ jsxs("section", { className: cardClass, children: [
      /* @__PURE__ */ jsxs("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        /* @__PURE__ */ jsx(LuLogIn, { className: "text-primary" }),
        " ",
        t("pptx.account.signIn.title")
      ] }),
      accountAuth.signedInUser ? /* @__PURE__ */ jsx("p", { className: "mt-3 text-sm text-muted-foreground", children: t("pptx.account.signIn.signedInAs", { name: accountAuth.signedInUser.name }) }) : /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx("p", { className: "mt-3 text-sm text-muted-foreground", children: t("pptx.account.signIn.description") }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: accountAuth.onSignIn,
            className: "mt-4 bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90",
            children: t("pptx.account.signIn.button")
          }
        )
      ] })
    ] })
  ] });
}
var actionClass = "group flex min-h-28 items-start gap-4 border border-border bg-card p-5 text-left text-card-foreground transition hover:border-primary hover:shadow-md";
function BackstageAction(props) {
  return /* @__PURE__ */ jsxs("button", { type: "button", className: actionClass, onClick: props.onClick, children: [
    /* @__PURE__ */ jsx("span", { className: "mt-0.5 grid size-10 shrink-0 place-items-center bg-accent text-xl text-primary", children: props.icon }),
    /* @__PURE__ */ jsxs("span", { children: [
      /* @__PURE__ */ jsx("strong", { className: "block text-[15px] font-semibold", children: props.title }),
      /* @__PURE__ */ jsx("span", { className: "mt-1 block text-[12px] leading-5 text-muted-foreground", children: props.body })
    ] })
  ] });
}
function BackstageNewGallery(props) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("h2", { className: "mt-7 text-[17px] font-semibold", children: t("pptx.backstage.newHeading") }),
    /* @__PURE__ */ jsx("div", { className: "mt-5 grid grid-cols-[repeat(auto-fill,minmax(150px,1fr))] gap-6", children: BACKSTAGE_TEMPLATES.map((template) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "text-left",
        onClick: () => props.onCreate(template.id),
        children: [
          /* @__PURE__ */ jsx(
            "span",
            {
              className: "block aspect-[16/9] border border-border bg-card shadow-sm transition hover:-translate-y-0.5 hover:border-primary hover:shadow-lg",
              style: { background: template.preview }
            }
          ),
          /* @__PURE__ */ jsx("strong", { className: "mt-2 block truncate text-[12px] font-medium", children: t(template.nameKey, { defaultValue: template.name }) }),
          /* @__PURE__ */ jsx("span", { className: "block truncate text-[10px] text-muted-foreground", children: t(template.descriptionKey, { defaultValue: template.description }) })
        ]
      },
      template.id
    )) })
  ] });
}
function BackstageRecentList(props) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "mt-5 border-t border-border", children: [
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[1fr_120px_90px] px-3 py-2 text-[11px] font-semibold text-muted-foreground", children: [
      /* @__PURE__ */ jsx("span", { children: t("pptx.backstage.columnName") }),
      /* @__PURE__ */ jsx("span", { children: t("pptx.backstage.columnModified") }),
      /* @__PURE__ */ jsx("span", { children: t("pptx.backstage.columnSize") })
    ] }),
    props.files.map((file) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "grid w-full grid-cols-[1fr_120px_90px] items-center border-t border-border px-3 py-3 text-left hover:bg-accent",
        onClick: () => props.onOpen?.(file.key),
        children: [
          /* @__PURE__ */ jsxs("span", { className: "flex min-w-0 items-center gap-3", children: [
            /* @__PURE__ */ jsx("span", { className: "grid size-8 shrink-0 place-items-center bg-primary font-bold text-primary-foreground", children: "P" }),
            /* @__PURE__ */ jsxs("span", { className: "min-w-0", children: [
              /* @__PURE__ */ jsx("strong", { className: "block truncate text-[13px] font-normal", children: file.name }),
              /* @__PURE__ */ jsx("small", { className: "block truncate text-[11px] text-muted-foreground", children: file.location })
            ] })
          ] }),
          /* @__PURE__ */ jsx("span", { className: "text-[11px] text-muted-foreground", children: formatBackstageDate(file.timestamp, Date.now(), t) }),
          /* @__PURE__ */ jsx("span", { className: "text-[11px] text-muted-foreground", children: formatBackstageSize(file.size) })
        ]
      },
      file.key
    )),
    props.files.length === 0 && /* @__PURE__ */ jsx("div", { className: "border-t border-border px-3 py-10 text-center text-sm text-muted-foreground", children: t("pptx.backstage.noRecent") })
  ] });
}
var ICONS = {
  protect: LuLock,
  inspect: LuInfo,
  embedFonts: LuType,
  signatures: LuShieldAlert,
  versionHistory: LuClock3,
  saveAsPptx: LuDownload,
  saveAsPpsx: LuPlay,
  saveAsPptm: LuFileText,
  pdf: LuFileText,
  png: LuFileImage,
  video: LuVideo,
  gif: LuBox,
  json: LuFileJson,
  copyImage: LuCopy,
  print: LuPrinter,
  share: LuShare2
};
function BackstageCards({
  page,
  props,
  run
}) {
  const { t } = useTranslation();
  const handlers = {
    protect: props.onOpenPasswordProtection,
    inspect: props.onOpenDocumentProperties,
    embedFonts: props.onOpenFontEmbedding,
    signatures: props.onOpenDigitalSignatures,
    versionHistory: props.onOpenVersionHistory,
    saveAsPptx: props.onSaveAsPptx,
    saveAsPpsx: props.onSaveAsPpsx,
    saveAsPptm: props.onSaveAsPptm,
    pdf: props.onExportPdf,
    png: props.onExportPng,
    video: props.onExportVideo,
    gif: props.onExportGif,
    json: props.onExportJson,
    copyImage: props.onCopySlideAsImage,
    print: props.onPrint,
    share: props.onOpenShareDialog
  };
  const cards = backstageCardsFor(page).filter(
    (card) => card.id !== "saveAsPptm" || props.hasMacros
  );
  if (cards.length === 0) {
    return null;
  }
  const single = cards.length === 1;
  return /* @__PURE__ */ jsx("div", { className: `mt-8 grid gap-5 ${single ? "max-w-[700px]" : "max-w-[900px] grid-cols-2"}`, children: cards.map((card) => {
    const Icon = ICONS[card.id];
    return /* @__PURE__ */ jsx(
      BackstageAction,
      {
        icon: /* @__PURE__ */ jsx(Icon, {}),
        title: t(card.titleKey, { defaultValue: card.title }),
        body: t(card.bodyKey, { defaultValue: card.body }),
        onClick: () => run(handlers[card.id])
      },
      card.id
    );
  }) });
}
var ICONS2 = {
  home: LuHouse,
  new: LuFilePlus2,
  open: LuFolderOpen,
  info: LuInfo,
  save: LuSave,
  saveAs: LuDownload,
  print: LuPrinter,
  share: LuShare2,
  export: LuUpload,
  close: LuX,
  account: LuUserRound,
  options: LuSettings
};
function BackstageNavIcon({ page }) {
  const Icon = ICONS2[page];
  return Icon ? /* @__PURE__ */ jsx(Icon, { className: "size-[17px] shrink-0", "aria-hidden": "true" }) : null;
}
function FileSection(p) {
  const { t } = useTranslation();
  const [page, setPage] = useState("home");
  const [query, setQuery] = useState("");
  const [recent, setRecent] = useState([]);
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  const exportHidden = isHidden("export");
  useEffect(() => {
    void listBackstageRecentFiles(t, p.recentPresentationsCount).then(setRecent);
  }, [t, p.recentPresentationsCount]);
  const visibleRecent = useMemo(() => {
    const q = query.trim().toLowerCase();
    return q ? recent.filter((file) => `${file.name} ${file.location}`.toLowerCase().includes(q)) : recent;
  }, [query, recent]);
  const run = (action) => {
    action?.();
    if (action) {
      p.onClose();
    }
  };
  const current = BACKSTAGE_NAV.find((item) => item.id === page);
  const title = current ? t(current.labelKey, { defaultValue: current.label }) : t("pptx.backstage.nav.home");
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "fixed inset-0 z-[200] flex bg-background font-[Aptos,Segoe_UI,sans-serif] text-foreground max-md:flex-col",
      role: "dialog",
      "aria-modal": "true",
      "aria-label": t("pptx.backstage.title"),
      children: [
        /* @__PURE__ */ jsxs("aside", { className: "flex w-[148px] shrink-0 flex-col border-r border-border bg-secondary max-md:w-full max-md:flex-row max-md:items-center max-md:overflow-x-auto max-md:border-r-0 max-md:border-b", children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "aria-label": t("pptx.backstage.back"),
              onClick: p.onClose,
              className: "grid h-47px min-h-[48px] place-items-center border-b border-border text-xl hover:bg-accent max-md:min-w-[48px] max-md:shrink-0 max-md:border-b-0 max-md:border-r",
              children: /* @__PURE__ */ jsx(LuArrowLeft, {})
            }
          ),
          /* @__PURE__ */ jsxs("nav", { className: "flex min-h-0 flex-1 flex-col py-2 max-md:flex-row max-md:items-center max-md:py-0", children: [
            BACKSTAGE_NAV.filter(
              (item) => !item.group && !(item.id === "export" && exportHidden)
            ).map((item) => /* @__PURE__ */ jsxs(
              "button",
              {
                type: "button",
                onClick: () => item.id === "close" ? p.onClose() : item.id === "save" ? run(p.onSaveAsPptx) : setPage(item.id),
                className: `flex min-h-10 items-center gap-3 border-l-2 px-4 text-left text-[12px] max-md:shrink-0 max-md:whitespace-nowrap max-md:border-l-0 max-md:border-b-2 max-md:px-3 ${page === item.id ? "border-primary bg-card text-primary" : "border-transparent hover:bg-accent"}`,
                children: [
                  /* @__PURE__ */ jsx(BackstageNavIcon, { page: item.id }),
                  t(item.labelKey, { defaultValue: item.label })
                ]
              },
              item.id
            )),
            /* @__PURE__ */ jsx("div", { className: "flex-1 max-md:hidden" }),
            BACKSTAGE_NAV.filter((item) => item.group).map((item) => /* @__PURE__ */ jsxs(
              "button",
              {
                type: "button",
                onClick: () => item.id === "options" && p.onOpenSettings ? run(p.onOpenSettings) : setPage(item.id),
                className: `flex min-h-10 items-center gap-3 border-l-2 px-4 text-left text-[12px] max-md:shrink-0 max-md:whitespace-nowrap max-md:border-l-0 max-md:border-b-2 max-md:px-3 ${page === item.id ? "border-primary bg-card text-primary" : "border-transparent hover:bg-accent"}`,
                children: [
                  /* @__PURE__ */ jsx(BackstageNavIcon, { page: item.id }),
                  t(item.labelKey, { defaultValue: item.label })
                ]
              },
              item.id
            ))
          ] })
        ] }),
        /* @__PURE__ */ jsxs("main", { className: "min-w-0 flex-1 overflow-y-auto bg-background px-[clamp(32px,4vw,72px)] py-5 max-md:px-4 max-md:py-4", children: [
          /* @__PURE__ */ jsx("h1", { className: "text-[24px] font-semibold", children: page === "home" ? t("pptx.backstage.greeting") : title }),
          (page === "home" || page === "new") && /* @__PURE__ */ jsx(
            BackstageNewGallery,
            {
              onCreate: (templateId) => run(() => p.onCreatePresentation(templateId))
            }
          ),
          (page === "home" || page === "open") && /* @__PURE__ */ jsxs(Fragment, { children: [
            /* @__PURE__ */ jsxs("div", { className: "mt-8 flex max-w-[540px] items-center border border-input bg-card px-3 focus-within:border-ring", children: [
              /* @__PURE__ */ jsx(LuSearch, { className: "text-muted-foreground" }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  value: query,
                  onChange: (event) => setQuery(event.target.value),
                  placeholder: t("pptx.backstage.searchPlaceholder"),
                  className: "h-10 min-w-0 flex-1 bg-transparent px-3 text-[13px] outline-none"
                }
              )
            ] }),
            page === "open" && /* @__PURE__ */ jsxs(
              "button",
              {
                type: "button",
                className: "mt-4 inline-flex items-center gap-2 bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90",
                onClick: () => run(p.onOpenFile),
                children: [
                  /* @__PURE__ */ jsx(LuFolderOpen, {}),
                  " ",
                  t("pptx.backstage.browseDevice")
                ]
              }
            ),
            /* @__PURE__ */ jsx("h2", { className: "mt-6 text-[16px] font-semibold", children: t("pptx.backstage.recentHeading") }),
            /* @__PURE__ */ jsx(
              BackstageRecentList,
              {
                files: visibleRecent,
                onOpen: (key) => run(() => p.onOpenRecentFile?.(key))
              }
            )
          ] }),
          !(page === "export" && exportHidden) && /* @__PURE__ */ jsx(BackstageCards, { page, props: p, run }),
          page === "account" && /* @__PURE__ */ jsx(AccountPage, {}),
          page === "options" && /* @__PURE__ */ jsxs("div", { className: "mt-8 max-w-[760px] border border-border bg-card p-7 text-card-foreground", children: [
            /* @__PURE__ */ jsx(LuSettings, { className: "text-3xl text-primary" }),
            /* @__PURE__ */ jsx("h2", { className: "mt-4 text-lg font-semibold", children: t("pptx.backstage.optionsTitle") }),
            /* @__PURE__ */ jsx("p", { className: "mt-2 text-sm text-muted-foreground", children: t("pptx.backstage.optionsBody") }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "mt-6 bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90",
                onClick: () => run(p.onOpenSettings),
                children: t("pptx.backstage.openOptions")
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("p", { className: "mt-12 text-[11px] text-muted-foreground", children: [
            p.fileName || t("pptx.backstage.untitled"),
            " \xB7 ",
            t("pptx.backstage.savedToBrowser")
          ] })
        ] })
      ]
    }
  );
}
function RibbonMenu({
  anchorRef,
  className,
  children
}) {
  const [pos, setPos] = useState(null);
  useEffect(() => {
    const anchor = anchorRef.current;
    if (!anchor) {
      return;
    }
    const update = () => {
      const { left, top } = computeAnchoredPopupPosition(anchor.getBoundingClientRect());
      setPos(
        (prev) => prev && prev.left === left && prev.top === top ? prev : { left: left ?? 0, top }
      );
    };
    update();
    anchor.addEventListener("mouseenter", update);
    window.addEventListener("resize", update);
    document.addEventListener("scroll", update, true);
    return () => {
      anchor.removeEventListener("mouseenter", update);
      window.removeEventListener("resize", update);
      document.removeEventListener("scroll", update, true);
    };
  }, [anchorRef]);
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: cn("fixed z-50", className),
      style: pos ? { left: pos.left, top: pos.top } : { visibility: "hidden" },
      children
    }
  );
}
function FontFamilyMenu(p) {
  const { t } = useTranslation();
  const groups = buildFontCatalog({
    themeFonts: p.themeFonts,
    embeddedFonts: p.embeddedFonts,
    customFonts: p.customFonts
  });
  return /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: p.anchorRef, className: "flex flex-col w-64 pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 max-h-80 overflow-y-auto", children: groups.map((group, groupIndex) => /* @__PURE__ */ jsxs(React20.Fragment, { children: [
    /* @__PURE__ */ jsx(
      "div",
      {
        className: `px-3 pb-1 pt-2 text-[10px] font-semibold uppercase tracking-wide text-muted-foreground${groupIndex > 0 ? " border-t border-border/60 mt-1" : ""}`,
        children: t(group.labelKey)
      }
    ),
    group.entries.map((entry) => /* @__PURE__ */ jsx(FontRow, { entry, onSelect: p.onSelect }, `${group.id}-${entry.family}`))
  ] }, group.id)) }) });
}
function FontRow({
  entry,
  onSelect
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      className: "flex w-full items-center justify-between gap-3 px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
      style: { fontFamily: entry.family },
      onClick: () => onSelect(entry.family),
      children: [
        /* @__PURE__ */ jsx("span", { className: "truncate", children: entry.family }),
        entry.themeRole && /* @__PURE__ */ jsx("span", { className: "shrink-0 text-[10px] text-muted-foreground", children: t(`pptx.font.role.${entry.themeRole}`) })
      ]
    }
  );
}
function useLayoutPreviews(loadPreviews, enabled) {
  const [previews, setPreviews] = useState(/* @__PURE__ */ new Map());
  useEffect(() => {
    if (!enabled || !loadPreviews) {
      return;
    }
    let cancelled = false;
    void loadPreviews().then((loaded) => {
      if (!cancelled) {
        setPreviews(new Map(loaded.map((preview) => [preview.path, preview])));
      }
      return void 0;
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [enabled, loadPreviews]);
  return previews;
}
var THUMB_WIDTH = 128;
var THUMB_HEIGHT = 72;
var MAX_PREVIEW_ELEMENTS = 100;
function LayoutGalleryMenu(p) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: p.anchorRef, className: "flex flex-col w-[620px] pt-1", children: /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "layout-gallery-menu",
      className: "grid grid-cols-4 gap-2 rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-3 max-h-[520px] overflow-y-auto",
      children: [
        p.layoutOptions.length === 0 && /* @__PURE__ */ jsx("p", { className: "col-span-4 px-2 py-3 text-xs text-muted-foreground", children: t("pptx.layoutGallery.empty") }),
        p.layoutOptions.map((layout) => {
          const isCurrent = isCurrentLayout(layout, p.currentLayoutPath);
          return /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              "aria-current": isCurrent ? "true" : void 0,
              title: isCurrent ? `${layout.name} (${t("pptx.layoutGallery.current")})` : layout.name,
              className: cn(
                "relative flex min-w-0 flex-col items-center gap-1 rounded border-2 p-1 text-xs text-foreground transition-colors hover:bg-muted",
                isCurrent ? "border-primary bg-primary/10" : "border-transparent"
              ),
              onClick: () => p.onSelect(layout),
              children: [
                /* @__PURE__ */ jsx(LayoutThumbnail, { preview: p.previews.get(layout.path), name: layout.name }),
                /* @__PURE__ */ jsx("span", { className: "w-full truncate text-center", children: layout.name })
              ]
            },
            layout.path
          );
        })
      ]
    }
  ) });
}
function LayoutThumbnail({ preview, name }) {
  const geometry = buildLayoutPreviewGeometry(preview, THUMB_WIDTH, THUMB_HEIGHT);
  const slide = {
    id: `layout-preview-${preview?.path ?? name}`,
    rId: "",
    slideNumber: 0,
    elements: preview?.elements ?? [],
    backgroundColor: geometry.backgroundColor
  };
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "relative shrink-0 overflow-hidden rounded-sm border border-border/70 shadow-sm",
      style: {
        width: geometry.boxWidth,
        height: geometry.boxHeight,
        backgroundColor: geometry.backgroundColor
      },
      children: /* @__PURE__ */ jsxs(
        "div",
        {
          className: "absolute left-0 top-0 origin-top-left overflow-hidden",
          style: {
            width: geometry.surfaceWidth,
            height: geometry.surfaceHeight,
            transform: `scale(${geometry.scale})`,
            backgroundColor: geometry.backgroundColor
          },
          children: [
            (preview?.elements ?? []).slice(0, MAX_PREVIEW_ELEMENTS).map((element, index) => /* @__PURE__ */ jsx(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index
              },
              element.id
            )),
            geometry.frames.map((frame) => /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute border-dashed border-muted-foreground/70 bg-background/20",
                style: {
                  left: frame.left,
                  top: frame.top,
                  width: frame.width,
                  height: frame.height,
                  borderWidth: geometry.frameBorderWidth,
                  borderStyle: "dashed"
                }
              },
              frame.key
            ))
          ]
        }
      )
    }
  );
}
function SlidesGroup(p) {
  const { t } = useTranslation();
  const [newSlideMenuOpen, setNewSlideMenuOpen] = useState(false);
  const [layoutMenuOpen, setLayoutMenuOpen] = useState(false);
  const [templateGalleryOpen, setTemplateGalleryOpen] = useState(false);
  const newSlideMenuRef = useRef(null);
  const layoutMenuRef = useRef(null);
  const previews = useLayoutPreviews(p.loadLayoutPreviews, newSlideMenuOpen || layoutMenuOpen);
  const handleNewSlide = useCallback(() => {
    if (p.layoutOptions.length > 0) {
      const first = p.layoutOptions[0];
      p.onInsertSlideFromLayout(first.path, first.name);
    }
  }, [p]);
  useEffect(() => {
    if (!newSlideMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (newSlideMenuRef.current && !newSlideMenuRef.current.contains(e.target)) {
        setNewSlideMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [newSlideMenuOpen]);
  useEffect(() => {
    if (!layoutMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (layoutMenuRef.current && !layoutMenuRef.current.contains(e.target)) {
        setLayoutMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [layoutMenuOpen]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "relative inline-flex items-center", ref: newSlideMenuRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              onClick: handleNewSlide,
              disabled: !p.canEdit || p.layoutOptions.length === 0,
              className: cn(
                pill,
                "whitespace-nowrap",
                p.layoutOptions.length > 0 ? "rounded-r-none" : ""
              ),
              title: t("pptx.home.newSlide"),
              children: [
                /* @__PURE__ */ jsx(LuPlus, { className: ic }),
                t("pptx.home.newSlide")
              ]
            }
          ),
          p.layoutOptions.length > 0 && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !p.canEdit,
              className: "inline-flex items-center justify-center self-stretch px-1 rounded-r bg-muted hover:bg-accent text-xs transition-colors border-l border-border/40 active:scale-95 active:opacity-80",
              title: t("pptx.home.chooseLayout"),
              onClick: () => setNewSlideMenuOpen((v) => !v),
              children: /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3" })
            }
          ),
          newSlideMenuOpen && /* @__PURE__ */ jsx(
            LayoutGalleryMenu,
            {
              anchorRef: newSlideMenuRef,
              layoutOptions: p.layoutOptions,
              previews,
              onSelect: (layout) => {
                p.onInsertSlideFromLayout(layout.path, layout.name);
                setNewSlideMenuOpen(false);
              }
            }
          )
        ] }),
        p.onInsertSlideFromTemplate && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            disabled: !p.canEdit,
            className: pill,
            title: t("pptx.home.slideTemplates"),
            onClick: () => setTemplateGalleryOpen(true),
            children: [
              /* @__PURE__ */ jsx(LuLayoutTemplate, { className: ic }),
              t("pptx.home.slideTemplates")
            ]
          }
        ),
        /* @__PURE__ */ jsxs("div", { className: "relative inline-flex items-center", ref: layoutMenuRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled: !p.canEdit || p.layoutOptions.length === 0,
              className: pill,
              title: t("pptx.master.layout"),
              onClick: () => setLayoutMenuOpen((v) => !v),
              children: [
                /* @__PURE__ */ jsx(LuLayoutGrid, { className: ic }),
                t("pptx.master.layout")
              ]
            }
          ),
          layoutMenuOpen && /* @__PURE__ */ jsx(
            LayoutGalleryMenu,
            {
              anchorRef: layoutMenuRef,
              layoutOptions: p.layoutOptions,
              previews,
              currentLayoutPath: p.currentLayoutPath,
              onSelect: (layout) => {
                p.onApplyLayout?.(layout.path);
                setLayoutMenuOpen(false);
              }
            }
          )
        ] }),
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            disabled: !p.canEdit,
            className: pill,
            title: t("pptx.sections.resetSlideTitle"),
            onClick: p.onResetSlide,
            children: [
              /* @__PURE__ */ jsx(LuRotateCcw, { className: ic }),
              t("pptx.animations.reset")
            ]
          }
        ),
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            disabled: !p.canEdit,
            className: pill,
            title: t("pptx.sections.addSection"),
            onClick: p.onAddSection,
            children: [
              /* @__PURE__ */ jsx(LuFolderPlus, { className: ic }),
              t("pptx.sections.sectionButtonLabel")
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Slides" })
    ] }),
    sep,
    p.onInsertSlideFromTemplate && /* @__PURE__ */ jsx(
      SlideTemplateGalleryDialog,
      {
        isOpen: templateGalleryOpen,
        onClose: () => setTemplateGalleryOpen(false),
        onInsert: (templateId) => p.onInsertSlideFromTemplate?.(templateId),
        scheme: p.templateScheme
      }
    )
  ] });
}
function extractFontInfo(element, themeFonts) {
  const placeholderType = element?.placeholderType;
  const defaults = {
    fontFamily: resolveDefaultFontFamily(placeholderType, themeFonts),
    fontSize: "24"
  };
  if (!element || !hasTextProperties(element)) {
    return defaults;
  }
  const segStyle = element.textSegments?.[0]?.style;
  const textStyle = element.textStyle;
  const fontFamily = segStyle?.fontFamily ?? textStyle?.fontFamily ?? defaults.fontFamily;
  const fontSize = segStyle?.fontSize ?? textStyle?.fontSize;
  return {
    fontFamily,
    fontSize: fontSize !== void 0 && fontSize !== null ? String(textFontSizePxToPt(fontSize)) : defaults.fontSize
  };
}
function HomeSection(p) {
  const { t } = useTranslation();
  const [fontMenuOpen, setFontMenuOpen] = useState(false);
  const [sizeMenuOpen, setSizeMenuOpen] = useState(false);
  const [copiedFeedback, setCopiedFeedback] = useState(false);
  const [cutFeedback, setCutFeedback] = useState(false);
  const fontMenuRef = useRef(null);
  const sizeMenuRef = useRef(null);
  const { fontFamily, fontSize } = extractFontInfo(p.selectedElement, p.themeFonts);
  const hasSelection = Boolean(p.selectedElement);
  useEffect(() => {
    if (!fontMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (fontMenuRef.current && !fontMenuRef.current.contains(e.target)) {
        setFontMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [fontMenuOpen]);
  useEffect(() => {
    if (!sizeMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (sizeMenuRef.current && !sizeMenuRef.current.contains(e.target)) {
        setSizeMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [sizeMenuOpen]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: grp, children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: p.onPaste,
            disabled: !p.clipboardPayload || !p.canEdit,
            className: gB,
            title: t("pptx.arrange.paste"),
            children: /* @__PURE__ */ jsx(LuClipboardPaste, { className: ic })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: () => {
              p.onCut();
              setCutFeedback(true);
              setTimeout(() => setCutFeedback(false), 600);
            },
            disabled: !p.canEdit || !hasSelection,
            className: cn(gB, cutFeedback && "bg-green-600/20 text-green-400"),
            title: t("pptx.arrange.cut"),
            children: /* @__PURE__ */ jsx(LuScissors, { className: ic })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: () => {
              p.onCopy();
              setCopiedFeedback(true);
              setTimeout(() => setCopiedFeedback(false), 600);
            },
            disabled: !hasSelection,
            className: cn(gB, copiedFeedback && "bg-green-600/20 text-green-400"),
            title: t("pptx.arrange.copy"),
            children: /* @__PURE__ */ jsx(LuCopy, { className: ic })
          }
        ),
        p.onToggleFormatPainter && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: p.onToggleFormatPainter,
            disabled: !p.canEdit || p.canActivateFormatPainter === false && !p.formatPainterActive,
            "data-testid": "format-painter-toggle",
            "data-active": p.formatPainterActive ? "true" : "false",
            className: cn(
              gL,
              p.formatPainterActive ? "bg-amber-600 hover:bg-amber-500 text-amber-50" : ""
            ),
            title: t("pptx.arrange.formatPainter"),
            children: /* @__PURE__ */ jsx(LuPaintbrush, { className: ic })
          }
        )
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: t("pptx.ribbon.clipboard") })
    ] }),
    sep,
    /* @__PURE__ */ jsx(
      SlidesGroup,
      {
        canEdit: p.canEdit,
        layoutOptions: p.layoutOptions,
        currentLayoutPath: p.currentLayoutPath,
        loadLayoutPreviews: p.loadLayoutPreviews,
        onInsertSlideFromLayout: p.onInsertSlideFromLayout,
        onInsertSlideFromTemplate: p.onInsertSlideFromTemplate,
        templateScheme: p.templateScheme,
        onApplyLayout: p.onApplyLayout,
        onResetSlide: p.onResetSlide,
        onAddSection: p.onAddSection
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "relative", ref: fontMenuRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              onClick: () => setFontMenuOpen((v) => !v),
              "aria-label": t("pptx.ribbon.fontFamily"),
              className: "inline-flex items-center justify-between px-2 py-1 rounded-sm border border-border/60 bg-background/60 text-[11px] text-foreground min-w-[120px] truncate hover:bg-accent/40 transition-colors cursor-pointer",
              children: [
                /* @__PURE__ */ jsx("span", { className: "truncate", children: fontFamily }),
                /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3 ml-1 shrink-0 text-muted-foreground" })
              ]
            }
          ),
          fontMenuOpen && /* @__PURE__ */ jsx(
            FontFamilyMenu,
            {
              anchorRef: fontMenuRef,
              themeFonts: p.themeFonts,
              embeddedFonts: p.embeddedFontFamilies,
              customFonts: p.customFontFamilies,
              onSelect: (family) => {
                p.onUpdateTextStyle?.({ fontFamily: family });
                setFontMenuOpen(false);
              }
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative", ref: sizeMenuRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              onClick: () => setSizeMenuOpen((v) => !v),
              "aria-label": t("pptx.ribbon.fontSize"),
              className: "inline-flex items-center justify-between px-2 py-1 rounded-sm border border-border/60 bg-background/60 text-[11px] text-foreground min-w-[50px] text-center hover:bg-accent/40 transition-colors cursor-pointer",
              children: [
                /* @__PURE__ */ jsx("span", { className: "truncate", children: fontSize }),
                /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3 ml-1 shrink-0 text-muted-foreground" })
              ]
            }
          ),
          sizeMenuOpen && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: sizeMenuRef, className: "flex flex-col w-48 pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 max-h-60 overflow-y-auto", children: COMMON_FONT_SIZES.map((s) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
              onClick: () => {
                p.onUpdateTextStyle?.({
                  fontSize: p.selectedElement && hasTextProperties(p.selectedElement) ? textFontSizePtToPx(s) : s
                });
                setSizeMenuOpen(false);
              },
              children: s
            },
            s
          )) }) })
        ] })
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Font" })
    ] }),
    sep
  ] });
}
function InsertHyperlinkButton(p) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      onClick: p.onOpenHyperlinkDialog,
      disabled: !p.hasSelection,
      className: pill,
      title: t("pptx.hyperlinkDialog.title"),
      children: [
        /* @__PURE__ */ jsx(LuLink, { className: ic }),
        t("pptx.hyperlinkDialog.title")
      ]
    }
  );
}
function InsertSection(p) {
  const { t } = useTranslation();
  const { canEdit } = p;
  const [datePickerOpen, setDatePickerOpen] = useState(false);
  const [datePickerValue, setDatePickerValue] = useState("");
  const [dateFormat, setDateFormat] = useState("locale");
  const [newChartKind, setNewChartKind] = useState(DEFAULT_INSERT_CHART_KIND);
  const datePickerRef = useRef(null);
  const actionMenuRef = useRef(null);
  const fieldMenuRef = useRef(null);
  const openDatePicker = useCallback(() => {
    const now = /* @__PURE__ */ new Date();
    const pad = (n) => String(n).padStart(2, "0");
    setDatePickerValue(
      `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}T${pad(now.getHours())}:${pad(now.getMinutes())}`
    );
    setDateFormat("locale");
    setDatePickerOpen(true);
  }, []);
  const confirmDatePicker = useCallback(() => {
    if (!p.onInsertField) {
      return;
    }
    const d = new Date(datePickerValue);
    if (isNaN(d.getTime())) {
      return;
    }
    let formatted;
    switch (dateFormat) {
      case "iso":
        formatted = d.toISOString().slice(0, 10);
        break;
      case "long":
        formatted = d.toLocaleDateString(void 0, {
          weekday: "long",
          year: "numeric",
          month: "long",
          day: "numeric"
        });
        break;
      case "short":
        formatted = d.toLocaleDateString(void 0, {
          year: "numeric",
          month: "short",
          day: "numeric"
        });
        break;
      case "time":
        formatted = d.toLocaleString();
        break;
      default:
        formatted = d.toLocaleDateString();
        break;
    }
    p.onInsertField("datetime", formatted);
    setDatePickerOpen(false);
  }, [datePickerValue, dateFormat, p]);
  useEffect(() => {
    if (!datePickerOpen) {
      return;
    }
    const handler = (e) => {
      if (datePickerRef.current && !datePickerRef.current.contains(e.target)) {
        setDatePickerOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [datePickerOpen]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onAddTextBox,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.addTextBox"),
        children: [
          /* @__PURE__ */ jsx(LuType, { className: ic }),
          t("pptx.ribbon.textBox")
        ]
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: grp, children: [
      /* @__PURE__ */ jsx(
        "select",
        {
          value: p.newShapeType,
          onChange: (e) => p.onSetNewShapeType(e.target.value),
          className: "bg-transparent py-1.5 pl-2 pr-1 outline-none text-xs",
          title: t("pptx.insert.shapeType"),
          children: SHAPE_PRESETS.map((sp) => /* @__PURE__ */ jsx("option", { value: sp.type, className: "bg-background", children: t(sp.i18nKey) }, sp.type))
        }
      ),
      /* @__PURE__ */ jsxs(
        "button",
        {
          onClick: p.onAddShape,
          disabled: !canEdit,
          className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 border-l border-border hover:bg-accent transition-colors text-xs",
          title: t("pptx.insert.addShape"),
          children: [
            SHAPE_PRESETS.find((sp) => sp.type === p.newShapeType)?.icon || /* @__PURE__ */ jsx(LuSquare, { className: ic }),
            t("pptx.insert.shape")
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onOpenImagePicker,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.ribbon.insertImage"),
        children: [
          /* @__PURE__ */ jsx(LuImage, { className: ic }),
          t("pptx.ribbon.image")
        ]
      }
    ),
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onOpenMediaPicker,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.ribbon.insertMedia"),
        children: [
          /* @__PURE__ */ jsx(LuVideo, { className: ic }),
          t("pptx.ribbon.media")
        ]
      }
    ),
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onAddTable,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.insertTable"),
        children: [
          /* @__PURE__ */ jsx(LuDatabase, { className: ic }),
          t("pptx.ribbon.table")
        ]
      }
    ),
    p.onAddChart && /* @__PURE__ */ jsxs("div", { className: grp, children: [
      /* @__PURE__ */ jsx(
        "select",
        {
          value: newChartKind,
          onChange: (e) => {
            const kind = INSERT_CHART_TYPES.find((ct) => ct.id === e.target.value)?.id;
            setNewChartKind(kind ?? DEFAULT_INSERT_CHART_KIND);
          },
          className: "bg-transparent py-1.5 pl-2 pr-1 outline-none text-xs",
          title: t("pptx.ribbon.chartType"),
          children: INSERT_CHART_TYPES.map((ct) => /* @__PURE__ */ jsx("option", { value: ct.id, className: "bg-background", children: t(ct.labelKey) }, ct.id))
        }
      ),
      /* @__PURE__ */ jsxs(
        "button",
        {
          onClick: () => p.onAddChart(newChartKind),
          disabled: !canEdit,
          className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 border-l border-border hover:bg-accent transition-colors text-xs",
          title: t("pptx.ribbon.insertChart"),
          children: [
            /* @__PURE__ */ jsxs(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: [
                  /* @__PURE__ */ jsx("path", { d: "M3 3v18h18" }),
                  /* @__PURE__ */ jsx("rect", { x: "7", y: "11", width: "3", height: "6" }),
                  /* @__PURE__ */ jsx("rect", { x: "12", y: "7", width: "3", height: "10" }),
                  /* @__PURE__ */ jsx("rect", { x: "17", y: "13", width: "3", height: "4" })
                ]
              }
            ),
            t("pptx.ribbon.chart")
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onAddSmartArt,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.insertSmartArt"),
        children: [
          /* @__PURE__ */ jsx(LuLayers, { className: ic }),
          t("pptx.ribbon.smartArt")
        ]
      }
    ),
    /* @__PURE__ */ jsxs(
      "button",
      {
        onClick: p.onAddEquation,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.insertEquation"),
        children: [
          /* @__PURE__ */ jsx(
            "svg",
            {
              className: ic,
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "2",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              children: /* @__PURE__ */ jsx("path", { d: "M4 17h6M7 14v6M14 7l4.5 10M15.5 14h5" })
            }
          ),
          t("pptx.ribbon.equation")
        ]
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "relative group inline-flex items-center", ref: actionMenuRef, children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: pill,
          title: t("pptx.ribbon.insertActionButton"),
          children: [
            /* @__PURE__ */ jsxs(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: [
                  /* @__PURE__ */ jsx("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2" }),
                  /* @__PURE__ */ jsx("path", { d: "M13 7l4 5-4 5" })
                ]
              }
            ),
            t("pptx.ribbon.action"),
            /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3" })
          ]
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonMenu,
        {
          anchorRef: actionMenuRef,
          className: "hidden group-hover:flex flex-col w-40 pt-1",
          children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: ACTION_BUTTON_PRESETS.map((preset) => /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled: !canEdit,
              className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
              onClick: () => p.onAddActionButton(preset.shapeType),
              children: [
                /* @__PURE__ */ jsx(
                  "svg",
                  {
                    className: "w-4 h-4 flex-shrink-0",
                    viewBox: "0 0 24 24",
                    fill: "none",
                    stroke: "currentColor",
                    strokeWidth: "2",
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    children: /* @__PURE__ */ jsx("path", { d: preset.iconPath })
                  }
                ),
                preset.label
              ]
            },
            preset.shapeType
          )) })
        }
      )
    ] }),
    p.onInsertField && /* @__PURE__ */ jsxs("div", { className: "relative group inline-flex items-center", ref: fieldMenuRef, children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: pill,
          title: t("pptx.field.insertField"),
          children: [
            /* @__PURE__ */ jsxs(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: [
                  /* @__PURE__ */ jsx("path", { d: "M4 7h16M4 12h10M4 17h12" }),
                  /* @__PURE__ */ jsx("circle", { cx: "19", cy: "15", r: "3" })
                ]
              }
            ),
            t("pptx.field.field"),
            /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3" })
          ]
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonMenu,
        {
          anchorRef: fieldMenuRef,
          className: "hidden group-hover:flex flex-col w-44 pt-1",
          children: /* @__PURE__ */ jsxs("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => p.onInsertField("slidenum"),
                children: t("pptx.field.slideNumber")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: openDatePicker,
                children: t("pptx.field.dateTime")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => p.onInsertField("header"),
                children: t("pptx.field.header")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => p.onInsertField("footer"),
                children: t("pptx.field.footer")
              }
            )
          ] })
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      InsertHyperlinkButton,
      {
        hasSelection: p.hasSelection,
        onOpenHyperlinkDialog: p.onOpenHyperlinkDialog
      }
    ),
    p.onOpenHeaderFooter && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: cn(pill, "whitespace-nowrap"),
        onClick: p.onOpenHeaderFooter,
        children: t("pptx.headerFooter.title")
      }
    ),
    datePickerOpen && /* @__PURE__ */ jsx(
      "div",
      {
        ref: datePickerRef,
        className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/30",
        onMouseDown: (e) => {
          if (e.target === e.currentTarget) {
            setDatePickerOpen(false);
          }
        },
        children: /* @__PURE__ */ jsxs("div", { className: "rounded-lg border border-border bg-popover shadow-2xl p-4 w-72 space-y-3", children: [
          /* @__PURE__ */ jsx("div", { className: "text-sm font-medium text-foreground", children: t("pptx.field.dateTime") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "datetime-local",
              className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              value: datePickerValue,
              onChange: (e) => setDatePickerValue(e.target.value)
            }
          ),
          /* @__PURE__ */ jsxs("div", { children: [
            /* @__PURE__ */ jsx("label", { className: "block text-[11px] text-muted-foreground mb-1", children: t("pptx.field.format", "Format") }),
            /* @__PURE__ */ jsxs(
              "select",
              {
                className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                value: dateFormat,
                onChange: (e) => setDateFormat(e.target.value),
                children: [
                  /* @__PURE__ */ jsx("option", { value: "locale", children: new Date(datePickerValue || Date.now()).toLocaleDateString() }),
                  /* @__PURE__ */ jsx("option", { value: "long", children: new Date(datePickerValue || Date.now()).toLocaleDateString(void 0, {
                    weekday: "long",
                    year: "numeric",
                    month: "long",
                    day: "numeric"
                  }) }),
                  /* @__PURE__ */ jsx("option", { value: "short", children: new Date(datePickerValue || Date.now()).toLocaleDateString(void 0, {
                    year: "numeric",
                    month: "short",
                    day: "numeric"
                  }) }),
                  /* @__PURE__ */ jsx("option", { value: "iso", children: new Date(datePickerValue || Date.now()).toISOString().slice(0, 10) }),
                  /* @__PURE__ */ jsx("option", { value: "time", children: new Date(datePickerValue || Date.now()).toLocaleString() })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 pt-1", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "px-3 py-1.5 text-xs rounded border border-border text-foreground hover:bg-muted transition-colors",
                onClick: () => setDatePickerOpen(false),
                children: t("pptx.common.cancel", "Cancel")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "px-3 py-1.5 text-xs rounded bg-primary text-white hover:bg-primary/90 transition-colors",
                onClick: confirmDatePicker,
                children: t("pptx.common.insert", "Insert")
              }
            )
          ] })
        ] })
      }
    )
  ] });
}
function ReviewSection(p) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.review.proofing", { defaultValue: "Proofing" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.spelling"),
          icon: /* @__PURE__ */ jsx(LuSpellCheck, {}),
          onClick: () => p.onSetSpellCheckEnabled(!p.spellCheckEnabled),
          active: p.spellCheckEnabled,
          title: t("pptx.review.toggleSpellCheck")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.thesaurus", { defaultValue: "Thesaurus" }),
          icon: /* @__PURE__ */ jsx(LuBookOpen, {}),
          disabled: true
        }
      )
    ] }),
    /* @__PURE__ */ jsx(RibbonGroup, { label: t("pptx.review.accessibility", { defaultValue: "Accessibility" }), children: /* @__PURE__ */ jsx(
      RibbonCommand,
      {
        label: t("pptx.review.accessibilityCheck"),
        icon: /* @__PURE__ */ jsx(LuShieldCheck, {}),
        onClick: p.onOpenAccessibilityCheck
      }
    ) }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.review.language"), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.translate", { defaultValue: "Translate" }),
          icon: /* @__PURE__ */ jsx(LuLanguages, {}),
          disabled: true
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.language"),
          icon: /* @__PURE__ */ jsx(LuGlobe, {}),
          onClick: p.onSetLanguage
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.review.changes", { defaultValue: "Changes" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.markAllRead", { defaultValue: "Mark All as Read" }),
          icon: /* @__PURE__ */ jsx(LuCopy, {}),
          disabled: true
        }
      ),
      p.onCompare && /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.ribbon.compare"),
          icon: /* @__PURE__ */ jsx(LuGitCompare, {}),
          onClick: p.onCompare,
          disabled: !p.canEdit,
          title: t("pptx.ribbon.compareTitle")
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.toolbar.comments"), children: [
      p.onToggleComments && /* @__PURE__ */ jsxs("div", { className: "relative", children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            label: t("pptx.toolbar.comments"),
            icon: /* @__PURE__ */ jsx(LuMessageSquarePlus, {}),
            onClick: p.onToggleComments,
            active: p.isCommentsPanelOpen,
            title: t("pptx.review.toggleComments")
          }
        ),
        Boolean(p.slideCommentCount) && /* @__PURE__ */ jsx("span", { className: "absolute right-0 top-0 rounded-full bg-primary px-1 text-[9px] text-white", children: p.slideCommentCount })
      ] }),
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.common.delete", { defaultValue: "Delete" }),
            icon: /* @__PURE__ */ jsx(LuTrash2, {}),
            disabled: true
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.common.previous", { defaultValue: "Previous" }),
            icon: /* @__PURE__ */ jsx(LuChevronLeft, {}),
            disabled: true
          }
        )
      ] }),
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.common.next", { defaultValue: "Next" }),
            icon: /* @__PURE__ */ jsx(LuChevronRight, {}),
            disabled: true
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.review.showComments", { defaultValue: "Show Comments" }),
            icon: /* @__PURE__ */ jsx(LuMessageSquare, {}),
            onClick: p.onToggleComments
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.review.protect", { defaultValue: "Protect" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.readOnly", { defaultValue: "Always Open Read-only" }),
          icon: /* @__PURE__ */ jsx(LuLockKeyhole, {}),
          disabled: true
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.restrictPermission", { defaultValue: "Restrict Permission" }),
          icon: /* @__PURE__ */ jsx(LuShieldCheck, {}),
          disabled: true
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.review.hideInk", { defaultValue: "Hide Ink" }),
          icon: /* @__PURE__ */ jsx(LuEyeOff, {}),
          disabled: true
        }
      )
    ] })
  ] });
}
function CustomShowsControls({
  customShows,
  activeCustomShowId,
  canEdit,
  isCurrentSlideInActiveShow,
  onSetActiveCustomShowId,
  onCreateCustomShow,
  onRenameActiveCustomShow,
  onDeleteActiveCustomShow,
  onToggleCurrentSlideInActiveShow
}) {
  const { t } = useTranslation();
  if (customShows.length > 0) {
    return /* @__PURE__ */ jsxs(Fragment, { children: [
      sep,
      /* @__PURE__ */ jsxs(
        "select",
        {
          value: activeCustomShowId ?? "",
          onChange: (e) => onSetActiveCustomShowId(e.target.value || null),
          className: "h-6 px-1.5 text-[11px] rounded bg-muted text-foreground border border-border hover:bg-accent transition-colors cursor-pointer",
          title: t("pptx.customShows.customShowTooltip"),
          "aria-label": t("pptx.customShows.selectCustomShow"),
          children: [
            /* @__PURE__ */ jsx("option", { value: "", children: t("pptx.customShows.allSlides") }),
            customShows.map((cs) => /* @__PURE__ */ jsx("option", { value: cs.id, children: cs.name }, cs.id))
          ]
        }
      ),
      canEdit && /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onCreateCustomShow,
            className: "px-2 py-1 rounded bg-muted hover:bg-accent text-[11px] transition-colors",
            title: t("pptx.customShows.createTooltip"),
            children: "+ Show"
          }
        ),
        activeCustomShowId && /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onRenameActiveCustomShow,
              className: "px-2 py-1 rounded bg-muted hover:bg-accent text-[11px] transition-colors",
              title: t("pptx.customShows.renameTooltip"),
              children: t("pptx.sections.rename")
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onDeleteActiveCustomShow,
              className: "px-2 py-1 rounded bg-red-700/80 hover:bg-red-600 text-[11px] transition-colors",
              title: t("pptx.customShows.deleteTooltip"),
              children: t("pptx.customShows.delete")
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onToggleCurrentSlideInActiveShow,
              className: cn(
                "px-2 py-1 rounded text-[11px] transition-colors",
                isCurrentSlideInActiveShow ? "bg-primary text-white" : "bg-muted hover:bg-accent"
              ),
              title: t("pptx.customShows.toggleSlideTooltip"),
              children: isCurrentSlideInActiveShow ? t("pptx.customShows.inShow") : t("pptx.customShows.addSlide")
            }
          )
        ] })
      ] })
    ] });
  }
  if (canEdit) {
    return /* @__PURE__ */ jsxs(Fragment, { children: [
      sep,
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onCreateCustomShow,
          className: "px-2 py-1 rounded bg-muted hover:bg-accent text-[11px] transition-colors",
          title: t("pptx.customShows.createTooltip"),
          children: "+ Show"
        }
      )
    ] });
  }
  return null;
}
function SlideShowSection(p) {
  const { t } = useTranslation();
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  const [showsOpen, setShowsOpen] = useState(false);
  const showsRef = useRef(null);
  useEffect(() => {
    if (!showsOpen) {
      return;
    }
    const handler = (event) => {
      if (showsRef.current && !showsRef.current.contains(event.target)) {
        setShowsOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [showsOpen]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.slideShow.start", { defaultValue: "Start Slide Show" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.fromBeginning"),
          icon: /* @__PURE__ */ jsx(LuPlay, {}),
          onClick: p.onPresentFromBeginning ?? (() => p.onSetMode("present")),
          title: t("pptx.slideShow.fromBeginningTooltip")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.fromCurrent"),
          icon: /* @__PURE__ */ jsx(LuMonitorPlay, {}),
          onClick: p.onPresent,
          title: t("pptx.slideShow.fromCurrentTooltip")
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.slideShow.present", { defaultValue: "Present" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.presenterView"),
          icon: /* @__PURE__ */ jsx(LuPresentation, {}),
          onClick: p.onEnterPresenterView,
          title: t("pptx.slideShow.presenterViewTooltip")
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: "relative", ref: showsRef, children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            label: t("pptx.slideShow.customShow", { defaultValue: "Custom Show" }),
            icon: /* @__PURE__ */ jsx(LuListVideo, {}),
            onClick: () => setShowsOpen((open) => !open),
            active: showsOpen,
            title: t("pptx.customShows.customShowTooltip")
          }
        ),
        showsOpen && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: showsRef, className: "pt-1", children: /* @__PURE__ */ jsx("div", { className: "flex items-center gap-1 rounded-lg border border-border bg-popover p-2 shadow-2xl", children: /* @__PURE__ */ jsx(CustomShowsControls, { ...p.customShowControls }) }) })
      ] }),
      !isHidden("broadcast") && /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.broadcast"),
          icon: /* @__PURE__ */ jsx(LuCast, {}),
          onClick: p.onOpenBroadcastDialog,
          title: t("pptx.slideShow.broadcastTooltip")
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.slideShow.setUpGroup", { defaultValue: "Set Up" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.rehearseCoach", { defaultValue: "Rehearse with Coach" }),
          icon: /* @__PURE__ */ jsx(LuVideo, {}),
          disabled: true
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.setUp"),
          icon: /* @__PURE__ */ jsx(LuSettings2, {}),
          onClick: p.onOpenSetUpSlideShow,
          title: t("pptx.slideShow.setUpTooltip")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.hideSlide", { defaultValue: "Hide Slide" }),
          icon: /* @__PURE__ */ jsx(LuEyeOff, {}),
          onClick: p.onToggleHideSlide,
          active: p.activeSlideHidden,
          pressed: p.activeSlideHidden
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideShow.rehearseTimings"),
          icon: /* @__PURE__ */ jsx(LuClock3, {}),
          onClick: p.onEnterRehearsalMode,
          title: t("pptx.slideShow.rehearseTimingsTooltip")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.titleBar.record"),
          icon: /* @__PURE__ */ jsx(LuVideo, {}),
          onClick: p.onEnterRehearsalMode
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.slideShow.options", { defaultValue: "Options" }), children: [
      /* @__PURE__ */ jsx(RibbonCommandStack, { children: SLIDE_SHOW_OPTIONS.slice(0, 3).map((option) => /* @__PURE__ */ jsx(
        RibbonToggle,
        {
          label: t(option.labelKey),
          checked: readSlideShowOption(p.presentationProperties, option.id),
          disabled: option.unsupported,
          onChange: option.unsupported ? void 0 : (next) => {
            const change = slideShowOptionChange(option.id, next);
            if (change) {
              p.onPresentationPropertiesChange?.(change);
            }
          }
        },
        option.id
      )) }),
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonToggle,
          {
            label: t(SLIDE_SHOW_OPTIONS[3].labelKey),
            checked: readSlideShowOption(p.presentationProperties, SLIDE_SHOW_OPTIONS[3].id),
            disabled: SLIDE_SHOW_OPTIONS[3].unsupported
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonToggle,
          {
            label: t("pptx.slideShow.subtitles"),
            checked: p.showSubtitles,
            onChange: () => p.onToggleSubtitles(),
            title: t("pptx.slideShow.subtitlesTooltip")
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.slideShow.subtitleSettings", { defaultValue: "Subtitle Settings" }),
            icon: /* @__PURE__ */ jsx(LuCaptions, {}),
            onClick: p.onToggleSubtitles
          }
        )
      ] })
    ] })
  ] });
}
function RecentColorsRow({
  prefix,
  disabled,
  onCommit
}) {
  const { t } = useTranslation();
  const { recentColors, pushColor } = useRecentColors();
  if (recentColors.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "pptx-color-recent",
      "aria-label": t("pptx.colorPicker.recentColors"),
      className: "mt-1 flex flex-wrap items-center gap-1",
      children: [
        /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground", children: t("pptx.colorPicker.recentColors") }),
        recentColors.map((c) => /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-compact": true,
            className: "h-4 w-4 rounded border border-primary",
            style: { backgroundColor: c },
            title: c,
            "aria-label": `Recent ${c}`,
            disabled,
            onClick: () => {
              onCommit(c);
              pushColor(c);
            }
          },
          `${prefix}-recent-${c}`
        ))
      ]
    }
  );
}
var ThemeColorMapContext = createContext(void 0);
var ThemeColorMapProvider = ThemeColorMapContext.Provider;
function useThemeColorMap() {
  return useContext(ThemeColorMapContext);
}
function ThemeColorSwatchGrid({
  prefix,
  disabled,
  selectedRef,
  selectedHex,
  onPick
}) {
  const { t } = useTranslation();
  const themeColorMap = useThemeColorMap();
  const columns = useMemo(() => buildThemeColorSwatchGrid(themeColorMap), [themeColorMap]);
  const rows = useMemo(() => themeColorSwatchRows(columns), [columns]);
  const selected = useMemo(
    () => findSelectedThemeSwatch(columns, selectedRef, selectedHex),
    [columns, selectedRef, selectedHex]
  );
  if (columns.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: "mt-1", children: [
    /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.colorPicker.themeColors") }),
    /* @__PURE__ */ jsx("div", { className: "flex flex-col gap-0.5", children: rows.map((row, rowIndex) => (
      // eslint-disable-next-line react/no-array-index-key
      /* @__PURE__ */ jsx("div", { className: "flex gap-0.5", children: row.map(
        (swatch, colIndex) => swatch ? /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled,
            "data-pptx-compact": true,
            title: swatch.label,
            "aria-label": swatch.label,
            className: `h-4 w-4 rounded-sm border transition-transform hover:scale-110 disabled:opacity-40 disabled:cursor-not-allowed ${selected === swatch ? "border-primary ring-1 ring-primary" : "border-border"}`,
            style: { backgroundColor: swatch.hex },
            onClick: () => onPick(themeSwatchCommit(swatch))
          },
          `${prefix}-theme-${swatch.ref.scheme}-${rowIndex}`
        ) : /* @__PURE__ */ jsx("div", { className: "h-4 w-4" }, `${prefix}-theme-empty-${rowIndex}-${colIndex}`)
      ) }, `${prefix}-theme-row-${rowIndex}`)
    )) })
  ] });
}
var LINE_SPACING_OPTIONS = [
  { label: "1.0", value: 1 },
  { label: "1.15", value: 1.15 },
  { label: "1.5", value: 1.5 },
  { label: "2.0", value: 2 },
  { label: "2.5", value: 2.5 },
  { label: "3.0", value: 3 }
];
var TEXT_DIRECTION_OPTIONS = [
  { label: "Horizontal", value: "horizontal" },
  { label: "Rotate 90\xB0", value: "vertical" },
  { label: "Rotate 270\xB0", value: "vertical270" },
  { label: "Stacked", value: "wordArtVert" }
];
var COLUMN_OPTIONS = [
  { label: "1 Column", value: 1 },
  { label: "2 Columns", value: 2 },
  { label: "3 Columns", value: 3 }
];
function useCloseOnClickOutside(ref, open, setOpen) {
  useEffect(() => {
    if (!open) {
      return;
    }
    const handler = (e) => {
      if (ref.current && !ref.current.contains(e.target)) {
        setOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [open, ref, setOpen]);
}
function LineSpacingDropdown(p) {
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);
  const ref = useRef(null);
  useCloseOnClickOutside(ref, open, setOpen);
  return /* @__PURE__ */ jsxs("div", { className: "relative", ref, children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !p.canMut,
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => setOpen((v) => !v),
        className: pill,
        title: t("pptx.paragraph.lineSpacing"),
        "aria-label": t("pptx.paragraph.lineSpacing"),
        children: /* @__PURE__ */ jsxs("svg", { className: ic, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", children: [
          /* @__PURE__ */ jsx("line", { x1: "5", y1: "5", x2: "19", y2: "5" }),
          /* @__PURE__ */ jsx("line", { x1: "5", y1: "12", x2: "19", y2: "12" }),
          /* @__PURE__ */ jsx("line", { x1: "5", y1: "19", x2: "19", y2: "19" }),
          /* @__PURE__ */ jsx("path", { d: "M2 8 L2 3 M2 3 L3.5 4.5 M2 3 L0.5 4.5" }),
          /* @__PURE__ */ jsx("path", { d: "M2 16 L2 21 M2 21 L3.5 19.5 M2 21 L0.5 19.5" })
        ] })
      }
    ),
    open && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: ref, className: "pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-28", children: LINE_SPACING_OPTIONS.map((opt) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => {
          if (!p.canFormat) {
            return;
          }
          p.onUpdateTextStyle({ lineSpacing: opt.value });
          setOpen(false);
        },
        children: [
          opt.label,
          p.effectiveTs?.lineSpacing === opt.value && /* @__PURE__ */ jsx("span", { className: "ml-auto text-primary", children: "\u2022" })
        ]
      },
      opt.value
    )) }) })
  ] });
}
function TextDirectionDropdown(p) {
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);
  const ref = useRef(null);
  useCloseOnClickOutside(ref, open, setOpen);
  return /* @__PURE__ */ jsxs("div", { className: "relative", ref, children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !p.canMut,
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => setOpen((v) => !v),
        className: pill,
        title: t("pptx.paragraph.textDirection"),
        "aria-label": t("pptx.paragraph.textDirection"),
        children: /* @__PURE__ */ jsxs("svg", { className: ic, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", children: [
          /* @__PURE__ */ jsx("text", { x: "3", y: "16", fontSize: "12", fill: "currentColor", stroke: "none", children: "A" }),
          /* @__PURE__ */ jsx("path", { d: "M18 6 C21 6 21 10 18 10" }),
          /* @__PURE__ */ jsx("path", { d: "M18 10 L19.5 8.5 M18 10 L16.5 8.5" })
        ] })
      }
    ),
    open && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: ref, className: "pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-36", children: TEXT_DIRECTION_OPTIONS.map((opt) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => {
          if (!p.canFormat) {
            return;
          }
          p.onUpdateTextStyle({ textDirection: opt.value });
          setOpen(false);
        },
        children: opt.label
      },
      opt.value
    )) }) })
  ] });
}
function ColumnsDropdown(p) {
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);
  const ref = useRef(null);
  useCloseOnClickOutside(ref, open, setOpen);
  return /* @__PURE__ */ jsxs("div", { className: "relative", ref, children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !p.canMut,
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => setOpen((v) => !v),
        className: pill,
        title: t("pptx.paragraph.columns"),
        children: /* @__PURE__ */ jsx(LuColumns3, { className: ic })
      }
    ),
    open && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: ref, className: "pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-28", children: COLUMN_OPTIONS.map((opt) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => {
          if (!p.canFormat) {
            return;
          }
          p.onUpdateTextStyle({ columnCount: opt.value });
          setOpen(false);
        },
        children: opt.label
      },
      opt.value
    )) }) })
  ] });
}
function getEffectiveTextStyle(element, tableEditorState) {
  if (!element) {
    return void 0;
  }
  if (hasTextProperties(element)) {
    return element.textStyle;
  }
  if (element.type === "table" && tableEditorState && element.tableData) {
    const cell = element.tableData.rows[tableEditorState.rowIndex]?.cells[tableEditorState.columnIndex];
    return cell?.style;
  }
  return void 0;
}
var HIGHLIGHT_COLOR_PRESETS = [
  "#ffff00",
  "#00ff00",
  "#00ffff",
  "#ff00ff",
  "#0000ff",
  "#ff0000",
  "#000080",
  "#008080",
  "#008000",
  "#800080"
];
function TextSection(p) {
  const { t } = useTranslation();
  const hasSel = Boolean(p.selectedElement);
  const canMut = hasSel && p.canEdit;
  const isTextEl = hasSel && p.selectedElement !== null && hasTextProperties(p.selectedElement);
  const isTable = hasSel && p.selectedElement?.type === "table";
  const canFormat = isTextEl || isTable;
  const effectiveTs = getEffectiveTextStyle(p.selectedElement, p.tableEditorState);
  const currentColor = isTextEl && p.selectedElement && hasTextProperties(p.selectedElement) ? p.selectedElement.textSegments?.[0]?.style?.color ?? p.selectedElement.textStyle?.color ?? "#000000" : effectiveTs?.color ?? "#000000";
  const currentColorThemeRef = isTextEl && p.selectedElement && hasTextProperties(p.selectedElement) ? p.selectedElement.textSegments?.[0]?.style?.colorRef ?? p.selectedElement.textStyle?.colorRef : void 0;
  const currentHighlight = isTextEl && p.selectedElement && hasTextProperties(p.selectedElement) ? p.selectedElement.textSegments?.[0]?.style?.highlightColor ?? p.selectedElement.textStyle?.highlightColor ?? "#ffff00" : "#ffff00";
  const colorInputRef = useRef(null);
  const highlightInputRef = useRef(null);
  const charSpacingRef = useRef(null);
  const changeCaseRef = useRef(null);
  const fontColorRef = useRef(null);
  const highlightMenuRef = useRef(null);
  const { pushColor } = useRecentColors();
  const handleColorChange = useCallback(
    (color, ref) => {
      if (!canFormat) {
        return;
      }
      p.onUpdateTextStyle({ color, colorRef: ref });
      pushColor(color);
    },
    [canFormat, p, pushColor]
  );
  const handleHighlightChange = useCallback(
    (highlightColor) => {
      if (!canFormat) {
        return;
      }
      p.onUpdateTextStyle({ highlightColor });
      pushColor(highlightColor);
    },
    [canFormat, p, pushColor]
  );
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx("div", { className: grp, children: FMT.map((b, i, a) => {
          const handleClick = () => {
            if (!canFormat || !p.selectedElement) {
              return;
            }
            const ts = effectiveTs;
            switch (b.id) {
              case "bold":
                p.onUpdateTextStyle({ bold: !ts?.bold });
                break;
              case "italic":
                p.onUpdateTextStyle({ italic: !ts?.italic });
                break;
              case "underline":
                p.onUpdateTextStyle({
                  underline: !ts?.underline
                });
                break;
              case "strikethrough":
                p.onUpdateTextStyle({
                  strikethrough: !ts?.strikethrough
                });
                break;
            }
          };
          return /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: handleClick,
              className: i < a.length - 1 ? gB : gL,
              title: t(b.labelKey),
              children: b.i
            },
            b.id
          );
        }) }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canMut,
            onMouseDown: (e) => e.preventDefault(),
            onClick: () => {
              if (!canFormat) {
                return;
              }
              const hasShadow = Boolean(effectiveTs?.textShadowColor);
              p.onUpdateTextStyle(
                hasShadow ? {
                  textShadowColor: void 0,
                  textShadowBlur: void 0,
                  textShadowOffsetX: void 0,
                  textShadowOffsetY: void 0
                } : {
                  textShadowColor: "#000000",
                  textShadowBlur: 2,
                  textShadowOffsetX: 1,
                  textShadowOffsetY: 1,
                  textShadowOpacity: 0.5
                }
              );
            },
            className: pill,
            title: t("pptx.textEffects.shadow"),
            "aria-label": t("pptx.textEffects.shadow"),
            children: /* @__PURE__ */ jsxs(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                children: [
                  /* @__PURE__ */ jsx("text", { x: "6", y: "17", fontSize: "16", fontWeight: "bold", fill: "currentColor", stroke: "none", children: "S" }),
                  /* @__PURE__ */ jsx(
                    "text",
                    {
                      x: "7.5",
                      y: "18.5",
                      fontSize: "16",
                      fontWeight: "bold",
                      fill: "none",
                      stroke: "currentColor",
                      strokeWidth: "0.5",
                      opacity: "0.4",
                      children: "S"
                    }
                  )
                ]
              }
            )
          }
        ),
        /* @__PURE__ */ jsxs("div", { className: grp, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = effectiveTs?.fontSize ?? (isTextEl ? textFontSizePtToPx(18) : 18);
                const delta = isTextEl ? textFontSizePtToPx(2) : 2;
                p.onUpdateTextStyle({ fontSize: current + delta });
              },
              className: gB,
              title: t("pptx.text.increaseFontSize"),
              children: /* @__PURE__ */ jsx(LuAArrowUp, { className: ic })
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = effectiveTs?.fontSize ?? (isTextEl ? textFontSizePtToPx(18) : 18);
                const delta = isTextEl ? textFontSizePtToPx(2) : 2;
                const minimum = isTextEl ? textFontSizePtToPx(1) : 1;
                p.onUpdateTextStyle({ fontSize: Math.max(minimum, current - delta) });
              },
              className: gB,
              title: t("pptx.text.decreaseFontSize"),
              children: /* @__PURE__ */ jsx(LuAArrowDown, { className: ic })
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat) {
                  return;
                }
                p.onUpdateTextStyle({
                  bold: false,
                  italic: false,
                  underline: false,
                  strikethrough: false,
                  highlightColor: void 0
                });
              },
              className: gL,
              title: t("pptx.text.clearFormatting"),
              children: /* @__PURE__ */ jsx(LuRemoveFormatting, { className: ic })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative group", ref: charSpacingRef, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.characterSpacing"),
              "aria-label": t("pptx.text.characterSpacing"),
              children: /* @__PURE__ */ jsxs(
                "svg",
                {
                  className: ic,
                  viewBox: "0 0 24 24",
                  fill: "none",
                  stroke: "currentColor",
                  strokeWidth: "1.5",
                  children: [
                    /* @__PURE__ */ jsx(
                      "text",
                      {
                        x: "4",
                        y: "16",
                        fontSize: "11",
                        fontWeight: "bold",
                        fill: "currentColor",
                        stroke: "none",
                        children: "AV"
                      }
                    ),
                    /* @__PURE__ */ jsx("path", { d: "M3 20 L1 20 M3 20 L5 20", strokeWidth: "1.5" }),
                    /* @__PURE__ */ jsx("path", { d: "M21 20 L19 20 M21 20 L23 20", strokeWidth: "1.5" })
                  ]
                }
              )
            }
          ),
          /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: charSpacingRef, className: "hidden group-hover:block pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-32", children: CHARACTER_SPACING_OPTIONS.map((opt) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat) {
                  return;
                }
                p.onUpdateTextStyle({ characterSpacing: opt.value });
              },
              children: t(opt.i18nKey)
            },
            opt.i18nKey
          )) }) })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative group", ref: changeCaseRef, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.changeCase"),
              "aria-label": t("pptx.text.changeCase"),
              children: /* @__PURE__ */ jsx(
                "svg",
                {
                  className: ic,
                  viewBox: "0 0 24 24",
                  fill: "none",
                  stroke: "currentColor",
                  strokeWidth: "1.5",
                  children: /* @__PURE__ */ jsx(
                    "text",
                    {
                      x: "2",
                      y: "16",
                      fontSize: "13",
                      fontWeight: "bold",
                      fill: "currentColor",
                      stroke: "none",
                      children: "Aa"
                    }
                  )
                }
              )
            }
          ),
          /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: changeCaseRef, className: "hidden group-hover:block pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-44", children: [
            { label: t("pptx.text.changeCaseSentence"), value: "sentence" },
            { label: t("pptx.text.changeCaseLower"), value: "lower" },
            { label: t("pptx.text.changeCaseUpper"), value: "upper" },
            { label: t("pptx.text.changeCaseCapitalize"), value: "capitalize" },
            { label: t("pptx.text.changeCaseToggle"), value: "toggle" }
          ].map((opt) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat) {
                  return;
                }
                if (isTable) {
                  p.onUpdateTextStyle({
                    textCaps: opt.value === "upper" ? "all" : "none"
                  });
                  return;
                }
                p.onTransformTextCase(opt.value);
              },
              children: opt.label
            },
            opt.value
          )) }) })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative group", ref: fontColorRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.fontColor"),
              "aria-label": t("pptx.text.fontColor"),
              children: [
                /* @__PURE__ */ jsx(
                  "svg",
                  {
                    className: ic,
                    viewBox: "0 0 24 24",
                    fill: "none",
                    stroke: "currentColor",
                    strokeWidth: "2",
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    children: /* @__PURE__ */ jsx("path", { d: "M6 20h12M9.5 4h5L18 16H6L9.5 4z" })
                  }
                ),
                /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: "w-4 h-1 rounded-sm -mt-0.5",
                    style: { backgroundColor: currentColor }
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: fontColorRef, className: "hidden group-hover:block pt-1", children: /* @__PURE__ */ jsxs("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-2 w-48", children: [
            /* @__PURE__ */ jsx(
              ThemeColorSwatchGrid,
              {
                prefix: "font-color",
                disabled: !canMut,
                selectedRef: currentColorThemeRef,
                selectedHex: currentColor,
                onPick: (c) => handleColorChange(c.hex, c.ref)
              }
            ),
            /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mt-1 mb-1", children: t("pptx.colorPicker.standardColors") }),
            /* @__PURE__ */ jsx("div", { className: "grid grid-cols-5 gap-1.5 mb-2", children: OFFICE_COLOR_SWATCHES.map((c) => /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                "aria-label": c.label,
                title: c.label,
                "data-pptx-compact": true,
                className: `w-5 h-5 rounded-full border transition-transform hover:scale-125 ${currentColor?.toLowerCase() === c.hex ? "border-primary ring-1 ring-primary" : "border-border"}`,
                style: { backgroundColor: c.hex },
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => handleColorChange(c.hex)
              },
              c.hex
            )) }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "w-full text-[10px] text-muted-foreground hover:text-foreground py-1 transition-colors",
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => colorInputRef.current?.click(),
                children: t("pptx.ribbon.customColour")
              }
            ),
            /* @__PURE__ */ jsx(
              "input",
              {
                ref: colorInputRef,
                type: "color",
                className: "sr-only",
                value: currentColor,
                onChange: (e) => handleColorChange(e.target.value)
              }
            ),
            /* @__PURE__ */ jsx(
              RecentColorsRow,
              {
                prefix: "font-color",
                disabled: !canMut,
                onCommit: handleColorChange
              }
            )
          ] }) })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative group", ref: highlightMenuRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.highlightColor"),
              "aria-label": t("pptx.text.highlightColor"),
              children: [
                /* @__PURE__ */ jsx(LuHighlighter, { className: ic }),
                /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: "w-4 h-1 rounded-sm -mt-0.5",
                    style: { backgroundColor: currentHighlight }
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: highlightMenuRef, className: "hidden group-hover:block pt-1", children: /* @__PURE__ */ jsxs("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-2 w-36", children: [
            /* @__PURE__ */ jsx("div", { className: "grid grid-cols-5 gap-1.5 mb-2", children: HIGHLIGHT_COLOR_PRESETS.map((c) => /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                "aria-label": c,
                "data-pptx-compact": true,
                className: `w-5 h-5 rounded-full border transition-transform hover:scale-125 ${currentHighlight?.toLowerCase() === c ? "border-primary ring-1 ring-primary" : "border-border"}`,
                style: { backgroundColor: c },
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => handleHighlightChange(c)
              },
              c
            )) }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "w-full text-[10px] text-muted-foreground hover:text-foreground py-1 transition-colors",
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => highlightInputRef.current?.click(),
                children: t("pptx.ribbon.customColour")
              }
            ),
            /* @__PURE__ */ jsx(
              "input",
              {
                ref: highlightInputRef,
                type: "color",
                className: "sr-only",
                value: currentHighlight,
                onChange: (e) => handleHighlightChange(e.target.value)
              }
            )
          ] }) })
        ] })
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Font" })
    ] }),
    sep,
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsxs("div", { className: grp, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                p.onUpdateTextStyle({
                  listType: effectiveTs?.listType === "bullet" ? "none" : "bullet"
                });
              },
              className: gB,
              title: t("pptx.text.bulletList"),
              children: /* @__PURE__ */ jsx(LuList, { className: ic })
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                p.onUpdateTextStyle({
                  listType: effectiveTs?.listType === "numbered" ? "none" : "numbered"
                });
              },
              className: gL,
              title: t("pptx.text.numberedList"),
              children: /* @__PURE__ */ jsx(LuListOrdered, { className: ic })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("div", { className: grp, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = effectiveTs?.paragraphMarginLeft ?? 0;
                p.onUpdateTextStyle({
                  paragraphMarginLeft: Math.max(0, current - 24)
                });
              },
              className: gB,
              title: t("pptx.text.decreaseIndent"),
              children: /* @__PURE__ */ jsx(LuIndentDecrease, { className: ic })
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = effectiveTs?.paragraphMarginLeft ?? 0;
                p.onUpdateTextStyle({
                  paragraphMarginLeft: current + 24
                });
              },
              className: gL,
              title: t("pptx.text.increaseIndent"),
              children: /* @__PURE__ */ jsx(LuIndentIncrease, { className: ic })
            }
          )
        ] }),
        /* @__PURE__ */ jsx("div", { className: grp, children: ATXT.map((b, i, a) => {
          const handleClick = () => {
            if (!canFormat) {
              return;
            }
            p.onUpdateTextStyle({ align: b.id });
          };
          return /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: handleClick,
              className: i < a.length - 1 ? gB : gL,
              title: t(b.labelKey),
              children: b.i
            },
            b.id
          );
        }) }),
        /* @__PURE__ */ jsx(
          LineSpacingDropdown,
          {
            canMut,
            canFormat,
            effectiveTs,
            onUpdateTextStyle: p.onUpdateTextStyle
          }
        ),
        /* @__PURE__ */ jsx(
          TextDirectionDropdown,
          {
            canMut,
            canFormat,
            onUpdateTextStyle: p.onUpdateTextStyle
          }
        ),
        /* @__PURE__ */ jsx(
          ColumnsDropdown,
          {
            canMut,
            canFormat,
            onUpdateTextStyle: p.onUpdateTextStyle
          }
        )
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Paragraph" })
    ] })
  ] });
}
function ViewSection(p) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.view.presentationViews"), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.view.normal"),
          icon: /* @__PURE__ */ jsx(LuPanelTop, {}),
          title: t("pptx.statusBar.normalView")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.slideSorter.title"),
          icon: /* @__PURE__ */ jsx(LuLayoutGrid, {}),
          onClick: p.onToggleSlideSorter,
          title: t("pptx.view.slideSorterTooltip")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.view.outlineView"),
          icon: /* @__PURE__ */ jsx(LuIndentIncrease, {}),
          onClick: p.onOpenOutlineView,
          title: t("pptx.view.outlineViewTooltip")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.view.readingView"),
          icon: /* @__PURE__ */ jsx(LuBookOpen, {}),
          onClick: p.onOpenReadingView,
          title: t("pptx.view.readingView")
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.view.masterViews"), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.master.title"),
          icon: /* @__PURE__ */ jsx(LuPresentation, {}),
          onClick: p.onEnterMasterView,
          disabled: !p.canEdit,
          title: t("pptx.view.slideMasterTooltip")
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.master.handoutMasterTitle", { defaultValue: "Handout Master" }),
          icon: /* @__PURE__ */ jsx(LuGrid3X3, {}),
          disabled: true
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.master.notesMasterTitle", { defaultValue: "Notes Master" }),
          icon: /* @__PURE__ */ jsx(LuStickyNote, {}),
          disabled: true
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.view.show", { defaultValue: "Show" }), children: [
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonToggle,
          {
            label: t("pptx.ruler.rulers"),
            checked: p.showRulers,
            onChange: p.onSetShowRulers
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonToggle,
          {
            label: t("pptx.grid.grid"),
            checked: p.showGrid,
            onChange: p.onSetShowGrid,
            title: t("pptx.grid.toggleGrid")
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonToggle,
          {
            label: t("pptx.view.guides", { defaultValue: "Guides" }),
            checked: p.showGuides,
            onChange: p.onSetShowGuides,
            title: t("pptx.ribbon.toggleGuides")
          }
        ),
        /* @__PURE__ */ jsx(RibbonToggle, { label: "Snap to grid", checked: p.snapToGrid, onChange: p.onSetSnapToGrid })
      ] }),
      /* @__PURE__ */ jsxs(RibbonCommandStack, { children: [
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.view.selection"),
            icon: /* @__PURE__ */ jsx(LuList, {}),
            onClick: p.onToggleSelectionPane,
            active: p.isSelectionPaneOpen,
            title: t("pptx.selectionPane.title")
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.ribbon.eyedropper"),
            icon: /* @__PURE__ */ jsx(LuPipette, {}),
            onClick: p.onToggleEyedropper,
            active: p.eyedropperActive,
            disabled: !p.canEdit
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.view.snapToShape"),
            icon: /* @__PURE__ */ jsx(LuGrid3X3, {}),
            onClick: () => p.onSetSnapToShape(!p.snapToShape),
            active: p.snapToShape,
            title: t("pptx.view.snapToShape")
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: "H Guide",
            icon: /* @__PURE__ */ jsx(LuRuler, {}),
            onClick: () => p.onAddGuide("h"),
            title: t("pptx.view.addHorizontalGuide")
          }
        ),
        /* @__PURE__ */ jsx(
          RibbonCommand,
          {
            compact: true,
            label: "V Guide",
            icon: /* @__PURE__ */ jsx(LuRuler, {}),
            onClick: () => p.onAddGuide("v"),
            title: t("pptx.view.addVerticalGuide")
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.slideSorter.zoom"), children: [
      /* @__PURE__ */ jsx(RibbonCommand, { label: t("pptx.slideSorter.zoom"), icon: /* @__PURE__ */ jsx(LuZoomIn, {}), disabled: true }),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.view.zoomToFit"),
          icon: /* @__PURE__ */ jsx(LuMaximize2, {}),
          onClick: p.onZoomToFit
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: t("pptx.view.window", { defaultValue: "Window" }), children: [
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t(p.editTemplateMode ? "pptx.ribbon.templatesOn" : "pptx.ribbon.templatesOff"),
          icon: /* @__PURE__ */ jsx(LuRuler, {}),
          onClick: () => p.onSetEditTemplateMode(!p.editTemplateMode),
          active: p.editTemplateMode,
          disabled: !p.canEdit,
          title: t("pptx.view.templateEditingTooltip", {
            defaultValue: "Toggle template/master element editing"
          })
        }
      ),
      /* @__PURE__ */ jsx(
        RibbonCommand,
        {
          label: t("pptx.view.macros", { defaultValue: "Macros" }),
          icon: /* @__PURE__ */ jsx(LuCode, {}),
          disabled: true
        }
      )
    ] })
  ] });
}
function MobileSheet({
  open,
  onClose,
  title,
  children,
  heightFraction = 0.6,
  fullScreen = false,
  autoHeight = false,
  className,
  headerRight
}) {
  const sheetRef = useRef(null);
  const { dragY, dragging, handlers } = useSheetDismissDrag(onClose);
  useModalFocus(open, sheetRef, onClose);
  if (!open) {
    return null;
  }
  const heightStyle = autoHeight ? { maxHeight: "calc(85dvh - env(safe-area-inset-top))" } : fullScreen ? { height: "calc(100dvh - env(safe-area-inset-top))" } : { height: `${Math.round(heightFraction * 100)}dvh` };
  return /* @__PURE__ */ jsxs("div", { className: "fixed inset-0 z-50 flex flex-col justify-end", children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        "aria-label": "Close",
        className: "absolute inset-0 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150",
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsxs(
      "div",
      {
        ref: sheetRef,
        role: "dialog",
        "aria-modal": "true",
        "aria-label": typeof title === "string" ? title : "Mobile panel",
        tabIndex: -1,
        className: cn(
          "relative bg-background border-t border-border rounded-t-2xl shadow-2xl flex flex-col overflow-hidden",
          "animate-in slide-in-from-bottom duration-200",
          className
        ),
        style: {
          ...heightStyle,
          transform: dragY > 0 ? `translateY(${dragY}px)` : void 0,
          transition: dragging ? "none" : "transform 150ms ease-out"
        },
        children: [
          /* @__PURE__ */ jsxs(
            "div",
            {
              className: "cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: handlers.onPointerDown,
              onPointerMove: handlers.onPointerMove,
              onPointerUp: handlers.onPointerUp,
              onPointerCancel: handlers.onPointerCancel,
              children: [
                /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center pt-2 pb-1", children: /* @__PURE__ */ jsx("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" }) }),
                (title || headerRight) && /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2 px-4 pb-2 border-b border-border/60", children: [
                  /* @__PURE__ */ jsx("div", { className: "text-sm font-semibold text-foreground truncate", children: title }),
                  headerRight,
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      "aria-label": "Close",
                      onPointerDown: (event) => event.stopPropagation(),
                      onClick: onClose,
                      className: "inline-flex h-8 w-8 items-center justify-center rounded text-xl text-muted-foreground hover:bg-accent hover:text-foreground",
                      children: "\xD7"
                    }
                  )
                ] })
              ]
            }
          ),
          /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-y-auto overscroll-contain", children })
        ]
      }
    )
  ] });
}
var MENU_ITEMS = [
  { key: "home", label: "pptx.ribbon.tab.home", icon: LuClipboardCopy },
  { key: "insert", label: "pptx.ribbon.tab.insert", icon: LuPlus },
  { key: "text", label: "pptx.ribbon.tab.text", icon: LuType },
  { key: "draw", label: "pptx.ribbon.tab.draw", icon: LuPaintbrush },
  { key: "arrange", label: "pptx.ribbon.tab.arrange", icon: LuShapes },
  { key: "design", label: "pptx.ribbon.tab.design", icon: LuLayoutGrid },
  { key: "transitions", label: "pptx.ribbon.tab.transitions", icon: LuSparkles },
  { key: "animations", label: "pptx.ribbon.tab.animations", icon: LuWand },
  { key: "slideShow", label: "pptx.ribbon.tab.slideShow", icon: LuPresentation },
  { key: "review", label: "pptx.ribbon.tab.review", icon: LuTextCursorInput },
  { key: "view", label: "pptx.ribbon.tab.view", icon: LuSettings },
  { key: "file", label: "pptx.ribbon.tab.file", icon: LuFile }
];
function MobileMenuSheet(props) {
  const { t } = useTranslation();
  const { open, onClose } = props;
  const [active, setActive] = useState("home");
  const { isTabVisible } = useToolbarVisibility(props.hiddenActions);
  return /* @__PURE__ */ jsx(MobileSheet, { open, onClose, autoHeight: true, title: t("pptx.mobileToolbar.menu"), children: /* @__PURE__ */ jsxs("div", { className: "flex flex-col", children: [
    /* @__PURE__ */ jsx("div", { className: "sticky top-0 z-10 bg-background border-b border-border", children: /* @__PURE__ */ jsx("div", { className: "flex flex-wrap gap-1.5 px-3 py-2", children: MENU_ITEMS.filter(({ key }) => isTabVisible(key)).map(({ key, label, icon: Icon }) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: () => setActive(active === key ? null : key),
        className: cn(
          "inline-flex items-center gap-1.5 shrink-0 px-3 py-2 rounded-full text-[12px] font-medium border transition-colors min-h-[36px]",
          active === key ? "bg-primary text-white border-primary" : "border-border text-muted-foreground hover:text-foreground hover:bg-accent/40"
        ),
        children: [
          /* @__PURE__ */ jsx(Icon, { className: "w-4 h-4" }),
          t(label)
        ]
      },
      key
    )) }) }),
    /* @__PURE__ */ jsx("div", { className: "p-3", children: /* @__PURE__ */ jsx(MobileSectionBody, { active, ...props }) })
  ] }) });
}
function MobileSectionBody({
  active,
  ...p
}) {
  const wrap = "flex flex-wrap items-center gap-2";
  switch (active) {
    case "home":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        HomeSection,
        {
          canEdit: p.canEdit,
          clipboardPayload: p.clipboardPayload,
          formatPainterActive: p.formatPainterActive,
          canActivateFormatPainter: p.canActivateFormatPainter,
          onCopy: p.onCopy,
          onCut: p.onCut,
          onPaste: p.onPaste,
          onToggleFormatPainter: p.onToggleFormatPainter,
          layoutOptions: p.layoutOptions,
          onInsertSlideFromLayout: p.onInsertSlideFromLayout,
          onInsertSlideFromTemplate: p.onInsertSlideFromTemplate,
          templateScheme: p.templateScheme,
          selectedElement: p.selectedElement,
          onUpdateTextStyle: p.onUpdateTextStyle
        }
      ) });
    case "insert":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        InsertSection,
        {
          canEdit: p.canEdit,
          newShapeType: p.newShapeType,
          onSetNewShapeType: p.onSetNewShapeType,
          onAddTextBox: p.onAddTextBox,
          onAddShape: p.onAddShape,
          onAddTable: p.onAddTable,
          onAddChart: p.onAddChart,
          onAddSmartArt: p.onAddSmartArt,
          onAddEquation: p.onAddEquation,
          onAddActionButton: p.onAddActionButton,
          onInsertField: p.onInsertField,
          onOpenImagePicker: p.onOpenImagePicker,
          onOpenMediaPicker: p.onOpenMediaPicker,
          hasSelection: Boolean(p.selectedElement),
          onOpenHyperlinkDialog: p.onOpenHyperlinkDialog
        }
      ) });
    case "text":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        TextSection,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          tableEditorState: p.tableEditorState,
          onUpdateTextStyle: p.onUpdateTextStyle,
          onTransformTextCase: p.onTransformTextCase
        }
      ) });
    case "draw":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        DrawSection,
        {
          activeTool: p.activeTool,
          drawingColor: p.drawingColor,
          drawingWidth: p.drawingWidth,
          onSetActiveTool: p.onSetActiveTool,
          onSetDrawingColor: p.onSetDrawingColor,
          onSetDrawingWidth: p.onSetDrawingWidth
        }
      ) });
    case "arrange":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        ArrangeSection,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          selectedCount: p.selectedCount,
          selectionGroupable: p.selectionGroupable,
          onAlignElements: p.onAlignElements,
          onDistributeElements: p.onDistributeElements,
          canDistribute: p.canDistribute,
          onFlip: p.onFlip,
          onMoveLayer: p.onMoveLayer,
          onMoveLayerToEdge: p.onMoveLayerToEdge,
          onGroupElements: p.onGroupElements,
          onUngroupElement: p.onUngroupElement,
          onUpdateElementStyle: p.onUpdateElementStyle,
          onDuplicate: p.onDuplicate,
          onDelete: p.onDelete,
          formatPainterActive: p.formatPainterActive,
          onToggleFormatPainter: p.onToggleFormatPainter,
          canActivateFormatPainter: p.canActivateFormatPainter
        }
      ) });
    case "design":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        DesignSection,
        {
          canEdit: p.canEdit,
          onToggleThemeGallery: p.onToggleThemeGallery,
          isThemeGalleryOpen: p.isThemeGalleryOpen,
          onToggleThemeEditor: p.onToggleThemeEditor,
          isThemeEditorOpen: p.isThemeEditorOpen,
          onOpenDocumentProperties: p.onOpenDocumentProperties,
          onOpenSlideSize: p.onOpenSlideSize,
          onToggleInspector: p.onToggleInspector,
          isInspectorPaneOpen: p.isInspectorPaneOpen
        }
      ) });
    case "transitions":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        TransitionsSection,
        {
          isInspectorPaneOpen: p.isInspectorPaneOpen,
          onToggleInspector: p.onToggleInspector,
          canEdit: p.canEdit,
          activeSlide: p.activeSlide,
          onTransitionChange: p.onTransitionChange,
          onApplyTransitionToAll: p.onApplyTransitionToAll
        }
      ) });
    case "animations":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        AnimationsSection,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          isInspectorPaneOpen: p.isInspectorPaneOpen,
          onToggleInspector: p.onToggleInspector,
          onOpenAnimationPanel: p.onOpenAnimationPanel,
          onAddAnimation: p.onAddAnimation,
          onRemoveAnimation: p.onRemoveAnimation
        }
      ) });
    case "slideShow":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        SlideShowSection,
        {
          onPresent: () => p.onSetMode("present"),
          onPresentFromBeginning: p.onPresentFromBeginning,
          onEnterPresenterView: p.onEnterPresenterView ?? (() => {
          }),
          onEnterRehearsalMode: p.onEnterRehearsalMode ?? (() => {
          }),
          onOpenSetUpSlideShow: p.onOpenSetUpSlideShow ?? (() => {
          }),
          onToggleHideSlide: p.onToggleHideSlide ?? (() => {
          }),
          activeSlideHidden: p.activeSlideHidden ?? false,
          onOpenBroadcastDialog: p.onOpenBroadcastDialog ?? (() => {
          }),
          onToggleSubtitles: p.onToggleSubtitles ?? (() => {
          }),
          showSubtitles: p.showSubtitles ?? false,
          onSetMode: p.onSetMode,
          customShowControls: p,
          hiddenActions: p.hiddenActions
        }
      ) });
    case "review":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        ReviewSection,
        {
          canEdit: p.canEdit,
          spellCheckEnabled: p.spellCheckEnabled,
          onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
          onToggleComments: p.onToggleComments,
          isCommentsPanelOpen: p.isCommentsPanelOpen,
          slideCommentCount: p.slideCommentCount,
          onCompare: p.onCompare
        }
      ) });
    case "view":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        ViewSection,
        {
          canEdit: p.canEdit,
          editTemplateMode: p.editTemplateMode,
          onSetEditTemplateMode: p.onSetEditTemplateMode,
          spellCheckEnabled: p.spellCheckEnabled,
          onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
          showGrid: p.showGrid,
          showRulers: p.showRulers,
          showGuides: p.showGuides,
          onSetShowGuides: p.onSetShowGuides,
          snapToGrid: p.snapToGrid,
          snapToShape: p.snapToShape,
          onSetShowGrid: p.onSetShowGrid,
          onSetShowRulers: p.onSetShowRulers,
          onSetSnapToGrid: p.onSetSnapToGrid,
          onSetSnapToShape: p.onSetSnapToShape,
          onAddGuide: p.onAddGuide,
          onEnterMasterView: p.onEnterMasterView,
          isSelectionPaneOpen: p.isSelectionPaneOpen,
          onToggleSelectionPane: p.onToggleSelectionPane,
          eyedropperActive: p.eyedropperActive,
          onToggleEyedropper: p.onToggleEyedropper,
          onToggleSlideSorter: p.onToggleSlideSorter,
          onOpenReadingView: p.onOpenReadingView
        }
      ) });
    case "file":
      return /* @__PURE__ */ jsx("div", { className: wrap, children: /* @__PURE__ */ jsx(
        FileSection,
        {
          fileName: p.fileName,
          onClose: () => p.onSetToolbarSection("home"),
          onCreatePresentation: p.onCreatePresentation,
          onOpenFile: p.onOpenFile,
          onOpenRecentFile: p.onOpenRecentFile,
          onExportPng: p.onExportPng,
          onExportPdf: p.onExportPdf,
          onExportVideo: p.onExportVideo,
          onExportGif: p.onExportGif,
          onExportJson: p.onExportJson,
          onSaveAsPptx: p.onSaveAsPptx,
          onSaveAsPpsx: p.onSaveAsPpsx,
          onSaveAsPptm: p.onSaveAsPptm,
          hasMacros: p.hasMacros,
          onCopySlideAsImage: p.onCopySlideAsImage,
          onPrint: p.onPrint,
          onOpenSettings: p.onOpenSettings,
          onOpenShareDialog: p.onOpenShareDialog,
          onOpenDocumentProperties: p.onOpenDocumentProperties,
          onOpenPasswordProtection: p.onOpenPasswordProtection,
          onOpenFontEmbedding: p.onOpenFontEmbedding,
          onOpenDigitalSignatures: p.onOpenDigitalSignatures,
          onOpenVersionHistory: p.onToggleVersionHistory,
          hiddenActions: p.hiddenActions,
          recentPresentationsCount: p.recentPresentationsCount
        }
      ) });
    default:
      return /* @__PURE__ */ jsxs("div", { className: "text-center text-sm text-muted-foreground py-8", children: [
        /* @__PURE__ */ jsx(LuChevronRight, { className: "w-5 h-5 inline-block opacity-50" }),
        " Select a section above"
      ] });
  }
}
function MobileToolbar(props) {
  const { t } = useTranslation();
  const { mode, canUndo, canRedo, onUndo, onRedo, onSetMode, onSaveAsPptx } = props;
  const [menuOpen, setMenuOpen] = useState(false);
  const { isHidden } = useToolbarVisibility(props.hiddenActions);
  const showEdit = mode === "edit" || mode === "master";
  const btn = "inline-flex items-center justify-center min-w-[44px] min-h-[44px] rounded-md text-foreground/80 hover:bg-accent/60 disabled:opacity-40 disabled:cursor-not-allowed active:scale-95 transition-transform";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      role: "toolbar",
      "aria-label": t("pptx.mobileToolbar.toolbar"),
      className: "relative z-20 flex items-center gap-1 px-2 py-1 border-b border-border bg-secondary/50 min-h-[52px] pt-[max(env(safe-area-inset-top),0px)]",
      children: [
        showEdit && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: () => setMenuOpen(true),
            className: btn,
            title: t("pptx.mobileToolbar.menu"),
            "aria-label": t("pptx.mobileToolbar.menu"),
            children: /* @__PURE__ */ jsx(LuMenu, { className: "w-5 h-5" })
          }
        ),
        showEdit && !isHidden("undo") && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onUndo,
            disabled: !canUndo,
            className: btn,
            title: t("pptx.toolbar.undo"),
            "aria-label": t("pptx.toolbar.undo"),
            children: /* @__PURE__ */ jsx(LuUndo, { className: "w-5 h-5" })
          }
        ),
        showEdit && !isHidden("redo") && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onRedo,
            disabled: !canRedo,
            className: btn,
            title: t("pptx.toolbar.redo"),
            "aria-label": t("pptx.toolbar.redo"),
            children: /* @__PURE__ */ jsx(LuRedo, { className: "w-5 h-5" })
          }
        ),
        /* @__PURE__ */ jsx("div", { className: "flex-1" }),
        showEdit && props.aiEnabled && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: props.onToggleAiPanel,
            className: cn(btn, props.isAiPanelOpen ? "text-primary" : void 0),
            title: t("pptx.toolbar.toggleAiAssistant"),
            "aria-label": t("pptx.toolbar.toggleAiAssistant"),
            children: /* @__PURE__ */ jsx(LuSparkles, { className: "w-5 h-5" })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onSaveAsPptx,
            className: btn,
            title: t("pptx.toolbar.save", "Save"),
            "aria-label": t("pptx.toolbar.save", "Save"),
            children: /* @__PURE__ */ jsx(LuDownload, { className: "w-5 h-5" })
          }
        ),
        !isHidden("fullscreen") && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: () => onSetMode("present"),
            className: cn(btn, "text-primary"),
            title: t("pptx.toolbar.present"),
            "aria-label": t("pptx.toolbar.present"),
            children: /* @__PURE__ */ jsx(LuPresentation, { className: "w-5 h-5" })
          }
        ),
        showEdit && !isHidden("share") && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: props.onOpenShareDialog,
            className: cn(btn, "bg-primary text-white hover:bg-primary/90 px-3"),
            title: t("pptx.toolbar.share"),
            "aria-label": t("pptx.toolbar.share"),
            children: /* @__PURE__ */ jsx(LuShare2, { className: "w-4 h-4" })
          }
        ),
        /* @__PURE__ */ jsx(MobileMenuSheet, { open: menuOpen, onClose: () => setMenuOpen(false), ...props })
      ]
    }
  );
}
function ShapeColorPopover({
  icon,
  title,
  prefix,
  anchorRef,
  open,
  onToggle,
  disabled,
  swatchAriaLabel,
  selectedRef,
  selectedHex,
  onApply,
  onClose
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "relative", ref: anchorRef, children: [
    /* @__PURE__ */ jsx("button", { type: "button", disabled, className: pill, title, onClick: onToggle, children: icon }),
    open && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef, className: "pt-1", children: /* @__PURE__ */ jsxs("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-2", children: [
      /* @__PURE__ */ jsx(
        ThemeColorSwatchGrid,
        {
          prefix,
          disabled,
          selectedRef,
          selectedHex,
          onPick: (c) => {
            onApply(c.hex, c.ref);
            onClose();
          }
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "mt-1 text-[10px] text-muted-foreground mb-1", children: t("pptx.colorPicker.standardColors") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-6 gap-1", children: RIBBON_SHAPE_SWATCHES.map((c) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          "aria-label": `${swatchAriaLabel} ${c}`,
          "data-pptx-compact": true,
          className: "w-5 h-5 rounded border border-border/60 hover:scale-110 transition-transform",
          style: { backgroundColor: c },
          title: c,
          onClick: () => {
            onApply(c);
            onClose();
          }
        },
        c
      )) }),
      /* @__PURE__ */ jsx(
        RecentColorsRow,
        {
          prefix,
          disabled,
          onCommit: (c) => {
            onApply(c);
            onClose();
          }
        }
      )
    ] }) })
  ] });
}
var TOP_SHAPES = SHAPE_PRESETS.slice(0, 12);
function DrawingGroup(p) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const selectedShapeStyle = p.selectedElement && hasShapeProperties(p.selectedElement) ? p.selectedElement.shapeStyle : void 0;
  const [shapesOpen, setShapesOpen] = useState(false);
  const [arrangeOpen, setArrangeOpen] = useState(false);
  const [fillOpen, setFillOpen] = useState(false);
  const [outlineOpen, setOutlineOpen] = useState(false);
  const shapesRef = useRef(null);
  const arrangeRef = useRef(null);
  const fillRef = useRef(null);
  const outlineRef = useRef(null);
  useEffect(() => {
    if (!shapesOpen) {
      return;
    }
    const handler = (e) => {
      if (shapesRef.current && !shapesRef.current.contains(e.target)) {
        setShapesOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [shapesOpen]);
  useEffect(() => {
    if (!arrangeOpen) {
      return;
    }
    const handler = (e) => {
      if (arrangeRef.current && !arrangeRef.current.contains(e.target)) {
        setArrangeOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [arrangeOpen]);
  useEffect(() => {
    if (!fillOpen) {
      return;
    }
    const handler = (e) => {
      if (fillRef.current && !fillRef.current.contains(e.target)) {
        setFillOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [fillOpen]);
  useEffect(() => {
    if (!outlineOpen) {
      return;
    }
    const handler = (e) => {
      if (outlineRef.current && !outlineRef.current.contains(e.target)) {
        setOutlineOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [outlineOpen]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "relative", ref: shapesRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled: !p.canEdit,
              className: pill,
              title: t("pptx.drawing.shapes"),
              onClick: () => setShapesOpen((v) => !v),
              children: [
                /* @__PURE__ */ jsx(LuShapes, { className: ic }),
                t("pptx.drawing.shapes")
              ]
            }
          ),
          shapesOpen && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: shapesRef, className: "flex flex-col w-52 pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 max-h-60 overflow-y-auto", children: TOP_SHAPES.map((s) => /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: cn(
                "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                p.newShapeType === s.type && "bg-accent"
              ),
              onClick: () => {
                p.onSetNewShapeType(s.type);
                p.onAddShape();
                setShapesOpen(false);
              },
              children: [
                s.icon,
                t(s.i18nKey)
              ]
            },
            s.type
          )) }) })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative", ref: arrangeRef, children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              disabled: !p.canEdit || !p.selectedElement,
              className: pill,
              title: t("pptx.ribbon.arrange"),
              onClick: () => setArrangeOpen((v) => !v),
              children: [
                /* @__PURE__ */ jsx(LuLayers, { className: ic }),
                t("pptx.ribbon.arrange")
              ]
            }
          ),
          arrangeOpen && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: arrangeRef, className: "flex flex-col w-44 pt-1", children: /* @__PURE__ */ jsxs("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayer("forward");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.bringForward")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayer("backward");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.sendBackward")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayerToEdge("front");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.bringToFront")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayerToEdge("back");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.sendToBack")
              }
            )
          ] }) })
        ] }),
        /* @__PURE__ */ jsx(
          ShapeColorPopover,
          {
            icon: /* @__PURE__ */ jsx(LuPaintBucket, { className: ic }),
            title: t("pptx.drawing.shapeFill"),
            prefix: "shape-fill",
            anchorRef: fillRef,
            open: fillOpen,
            onToggle: () => setFillOpen((v) => !v),
            disabled: !p.canEdit || !p.selectedElement,
            swatchAriaLabel: "Fill colour",
            selectedRef: selectedShapeStyle?.fillColorRef,
            selectedHex: selectedShapeStyle?.fillColor,
            onApply: (c, ref) => {
              p.onUpdateElementStyle?.(shapeFillChange(c, ref));
              pushColor(c);
            },
            onClose: () => setFillOpen(false)
          }
        ),
        /* @__PURE__ */ jsx(
          ShapeColorPopover,
          {
            icon: /* @__PURE__ */ jsx(LuPenLine, { className: ic }),
            title: t("pptx.drawing.shapeOutline"),
            prefix: "shape-outline",
            anchorRef: outlineRef,
            open: outlineOpen,
            onToggle: () => setOutlineOpen((v) => !v),
            disabled: !p.canEdit || !p.selectedElement,
            swatchAriaLabel: "Outline colour",
            selectedRef: selectedShapeStyle?.strokeColorRef,
            selectedHex: selectedShapeStyle?.strokeColor,
            onApply: (c, ref) => {
              p.onUpdateElementStyle?.(shapeOutlineChange(c, ref));
              pushColor(c);
            },
            onClose: () => setOutlineOpen(false)
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: true,
            className: pill,
            title: t("pptx.drawing.shapeEffectsUnavailable"),
            children: /* @__PURE__ */ jsx(LuSparkles, { className: ic })
          }
        )
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Drawing" })
    ] }),
    sep
  ] });
}
function EditingSection(p) {
  const { t } = useTranslation();
  const [selectMenuOpen, setSelectMenuOpen] = useState(false);
  const menuRef = useRef(null);
  useEffect(() => {
    if (!selectMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (menuRef.current && !menuRef.current.contains(e.target)) {
        setSelectMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [selectMenuOpen]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    sep,
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsxs("div", { className: grp, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onMouseDown: (e) => e.preventDefault(),
              onClick: p.onToggleFindReplace,
              className: gB,
              title: t("pptx.editing.find"),
              children: /* @__PURE__ */ jsx(LuSearch, { className: ic })
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onMouseDown: (e) => e.preventDefault(),
              onClick: p.onToggleFindReplace,
              className: gL,
              title: t("pptx.ribbon.replace"),
              children: /* @__PURE__ */ jsx(LuReplace, { className: ic })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative", ref: menuRef, children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => setSelectMenuOpen((v) => !v),
              className: pill,
              title: t("pptx.ribbon.tool.select"),
              children: /* @__PURE__ */ jsx(LuMousePointer2, { className: ic })
            }
          ),
          selectMenuOpen && /* @__PURE__ */ jsx(RibbonMenu, { anchorRef: menuRef, className: "pt-1", children: /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-32", children: /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                p.onSelectAll?.();
                setSelectMenuOpen(false);
              },
              children: t("pptx.editing.selectAll")
            }
          ) }) })
        ] })
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Editing" })
    ] })
  ] });
}
function HelpSection(p) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onOpenSettings ?? p.onToggleShortcuts,
        className: pill,
        title: t("pptx.settings.title"),
        children: t("pptx.settings.title")
      }
    ),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onToggleShortcuts,
        className: pill,
        title: t("pptx.settings.keyboardShortcuts"),
        children: t("pptx.settings.keyboardShortcuts")
      }
    ),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onRunAccessibilityCheck,
        className: pill,
        title: t("pptx.ribbon.accessibilityCheck"),
        children: t("pptx.ribbon.accessibilityCheck")
      }
    )
  ] });
}
function RecordSection({
  onRecordFromBeginning,
  onRecordFromCurrent
}) {
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(RibbonGroup, { label: "Camera", children: /* @__PURE__ */ jsx(RibbonCommand, { icon: /* @__PURE__ */ jsx(LuCamera, {}), label: "Cameo", disabled: true }) }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: "Record", children: [
      /* @__PURE__ */ jsx(RibbonCommand, { icon: /* @__PURE__ */ jsx(LuVideo, {}), label: "From Beginning", onClick: onRecordFromBeginning }),
      /* @__PURE__ */ jsx(RibbonCommand, { icon: /* @__PURE__ */ jsx(LuPlay, {}), label: "From Current Slide", onClick: onRecordFromCurrent })
    ] }),
    /* @__PURE__ */ jsxs(RibbonGroup, { label: "Manage", children: [
      /* @__PURE__ */ jsx(RibbonCommand, { icon: /* @__PURE__ */ jsx(LuEraser, {}), label: "Clear", disabled: true }),
      /* @__PURE__ */ jsx(RibbonCommand, { icon: /* @__PURE__ */ jsx(LuRotateCcw, {}), label: "Reset to Cameo", disabled: true })
    ] }),
    /* @__PURE__ */ jsx(RibbonGroup, { label: "Help", children: /* @__PURE__ */ jsx(RibbonCommand, { icon: /* @__PURE__ */ jsx(LuCircleHelp, {}), label: "Learn More", disabled: true }) })
  ] });
}
function TabRowActions(p) {
  const { t } = useTranslation();
  const collab = useCollaboration();
  const isCollaborating = collab && collab.status === "connected";
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  return /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 pr-1", children: [
    p.onEnterRehearsalMode && !isHidden("record") && /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: p.onEnterRehearsalMode,
        className: TAB_ROW_ACTION_CLASSES.record,
        title: t("pptx.titleBar.record"),
        "aria-label": t("pptx.titleBar.record"),
        children: [
          /* @__PURE__ */ jsx("span", { className: TAB_ROW_ACTION_CLASSES.recordDot, "aria-hidden": "true" }),
          /* @__PURE__ */ jsx("span", { children: t("pptx.titleBar.record") })
        ]
      }
    ),
    !isHidden("share") && /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: p.onOpenShareDialog,
        className: cn(
          "relative inline-flex items-center gap-1 px-2.5 py-1 rounded-sm text-[11px] font-medium transition-colors whitespace-nowrap",
          isCollaborating ? "bg-green-600 hover:bg-green-500 text-white" : "bg-primary hover:bg-primary/90 text-white"
        ),
        title: isCollaborating ? t("pptx.toolbar.sharingUsers", { count: collab.connectedCount }) : t("pptx.toolbar.share"),
        "aria-label": t("pptx.toolbar.share"),
        children: [
          /* @__PURE__ */ jsx(LuShare2, { className: "w-3 h-3" }),
          /* @__PURE__ */ jsx("span", { children: isCollaborating ? t("pptx.toolbar.sharingCount", { count: collab.connectedCount }) : t("pptx.toolbar.share") })
        ]
      }
    )
  ] });
}
var QUICK_ACCESS_ICONS = {
  save: LuSave,
  undo: LuUndo,
  redo: LuRedo,
  play: LuPlay,
  printer: LuPrinter,
  fileDown: LuFileDown,
  plus: LuPlus,
  spellCheck: LuSpellCheck,
  zoomIn: LuZoomIn,
  zoomOut: LuZoomOut
};
function TitleBarQuickExtras(p) {
  const { t } = useTranslation();
  if (!p.quickAccess.visible) {
    return null;
  }
  const commands = extraQuickAccessCommands(p.quickAccess.commandIds);
  if (commands.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx(Fragment, { children: commands.map((command) => {
    const Icon = QUICK_ACCESS_ICONS[command.icon] ?? LuPlay;
    const label = t(command.labelKey);
    return /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: () => p.onCommand?.(command.id),
        className: TITLE_BAR_CLASSES.quickButton,
        title: label,
        "aria-label": label,
        children: /* @__PURE__ */ jsx(Icon, { className: "w-3.5 h-3.5" })
      },
      command.id
    );
  }) });
}
function PresentDropdown({
  isActive,
  onPresent,
  onPresenterView,
  onRehearse,
  onSetUpSlideShow,
  onBroadcast,
  onToggleSubtitles,
  showSubtitles
}) {
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);
  return /* @__PURE__ */ jsxs("div", { className: "relative", children: [
    /* @__PURE__ */ jsxs("div", { className: "inline-flex items-stretch rounded border border-border bg-muted text-xs overflow-hidden", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onPresent,
          className: cn(
            "px-2 py-1 transition-colors",
            isActive ? "bg-primary text-white" : "hover:bg-accent text-foreground"
          ),
          title: t("pptx.present.presentTooltip"),
          children: t("pptx.toolbar.present")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: () => setOpen((prev) => !prev),
          className: cn(
            "inline-flex items-center px-1 transition-colors border-l border-border",
            open ? "bg-primary text-white" : "hover:bg-accent text-foreground"
          ),
          title: t("pptx.present.optionsTooltip"),
          "aria-label": t("pptx.present.optionsTooltip"),
          children: /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3" })
        }
      )
    ] }),
    open && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "fixed inset-0 z-40",
          "aria-label": t("pptx.overflow.closeMenu"),
          onClick: () => setOpen(false)
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: "absolute right-0 top-full mt-1 z-50 w-52 rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: [
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onPresent();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              /* @__PURE__ */ jsx(LuPlay, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.toolbar.present")
            ]
          }
        ),
        onPresenterView && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onPresenterView();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              /* @__PURE__ */ jsx(LuMonitor, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.slideShow.presenterView")
            ]
          }
        ),
        onRehearse && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onRehearse();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              /* @__PURE__ */ jsx(LuClock, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.slideShow.rehearseTimings")
            ]
          }
        ),
        /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border/60" }),
        onSetUpSlideShow && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onSetUpSlideShow();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              /* @__PURE__ */ jsx(LuSettings, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.slideShow.setUp")
            ]
          }
        ),
        onBroadcast && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onBroadcast();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              /* @__PURE__ */ jsx(LuRadio, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.present.presentOnline")
            ]
          }
        ),
        onToggleSubtitles && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onToggleSubtitles();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              /* @__PURE__ */ jsx(LuCaptions, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              /* @__PURE__ */ jsx("span", { className: "flex-1 text-left", children: "Subtitles" }),
              showSubtitles && /* @__PURE__ */ jsx(LuCheck, { className: "w-3 h-3 text-primary" })
            ]
          }
        )
      ] })
    ] })
  ] });
}
function ModeSwitcher({
  mode,
  onSetMode,
  onCloseMasterView,
  onEnterPresenterView,
  onEnterRehearsalMode,
  onOpenSetUpSlideShow,
  onOpenBroadcastDialog,
  onToggleSubtitles,
  showSubtitles
}) {
  const { t } = useTranslation();
  if (mode === "master") {
    return /* @__PURE__ */ jsxs("div", { className: "inline-flex items-center gap-1.5", children: [
      /* @__PURE__ */ jsx("span", { className: "inline-flex items-center px-2 py-0.5 rounded-sm bg-amber-600/90 text-[10px] text-amber-50", children: t("pptx.mode.masterView") }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onCloseMasterView,
          className: "px-2 py-0.5 rounded-sm hover:bg-accent text-[10px] text-foreground transition-colors",
          title: t("pptx.mode.closeMasterViewTooltip"),
          children: t("pptx.common.close")
        }
      )
    ] });
  }
  return /* @__PURE__ */ jsx(
    PresentDropdown,
    {
      isActive: mode === "present",
      onPresent: () => onSetMode("present"),
      onPresenterView: onEnterPresenterView,
      onRehearse: onEnterRehearsalMode,
      onSetUpSlideShow: onOpenSetUpSlideShow,
      onBroadcast: onOpenBroadcastDialog,
      onToggleSubtitles,
      showSubtitles
    }
  );
}
function OverflowMenu(p) {
  const { t } = useTranslation();
  const ovAct = (k) => {
    p.onSetOverflowMenuOpen(false);
    ({
      png: p.onExportPng,
      pdf: p.onExportPdf,
      video: p.onExportVideo,
      gif: p.onExportGif,
      pptx: p.onSaveAsPptx,
      ppsx: p.onSaveAsPpsx,
      pptm: p.onSaveAsPptm,
      print: p.onPrint,
      copyImg: p.onCopySlideAsImage,
      a11y: p.onRunAccessibilityCheck,
      shortcuts: p.onToggleShortcuts,
      versionHistory: p.onToggleVersionHistory,
      documentProperties: p.onOpenDocumentProperties,
      passwordProtection: p.onOpenPasswordProtection,
      fontEmbedding: p.onOpenFontEmbedding,
      digitalSignatures: p.onOpenDigitalSignatures
    })[k]?.();
  };
  return /* @__PURE__ */ jsxs("div", { className: "relative", children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: () => p.onSetOverflowMenuOpen(!p.isOverflowMenuOpen),
        className: cn(
          "p-1.5 rounded transition-colors",
          p.isOverflowMenuOpen ? "bg-primary/80 text-white" : "bg-muted hover:bg-accent"
        ),
        title: t("pptx.ribbon.moreActions"),
        "aria-label": t("pptx.ribbon.moreActions"),
        children: /* @__PURE__ */ jsx(LuEllipsis, { className: ic })
      }
    ),
    p.isOverflowMenuOpen && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "fixed inset-0 z-40",
          "aria-label": t("pptx.overflow.closeMenu"),
          onClick: () => p.onSetOverflowMenuOpen(false)
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "absolute right-0 top-full mt-1 z-50 w-44 rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: OV.filter((o) => o.k !== "pptm" || p.hasMacros).map(
        (o) => o.k.startsWith("---") ? /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border/60" }, o.k) : /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: () => ovAct(o.k),
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              o.i,
              t(o.labelKey)
            ]
          },
          o.k
        )
      ) })
    ] })
  ] });
}
function ToolbarPrimaryRow(p) {
  const { t } = useTranslation();
  const {
    mode,
    canEdit,
    isSidebarCollapsed,
    isInspectorPaneOpen,
    onToggleSidebar,
    onToggleInspector
  } = p;
  const collab = useCollaboration();
  const qab = "p-1 max-md:p-2 max-md:min-h-[40px] max-md:min-w-[40px] rounded-sm transition-colors hover:bg-accent/60 disabled:opacity-40 disabled:cursor-not-allowed active:scale-90 active:opacity-70";
  return /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-0.5 max-md:gap-0 px-1.5 py-0.5 max-md:px-1", children: [
    mode !== "present" && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onToggleSidebar,
        className: cn(qab, !isSidebarCollapsed ? "text-foreground" : "text-muted-foreground"),
        title: t("pptx.toolbar.toggleSlidesPanel"),
        "aria-label": t("pptx.toolbar.toggleSlidesPanel"),
        children: /* @__PURE__ */ jsx(LuPanelLeft, { className: ic })
      }
    ),
    /* @__PURE__ */ jsx("div", { className: "flex-1 min-w-2 max-md:min-w-1" }),
    (mode === "edit" || mode === "master") && /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: p.onToggleComments,
        className: cn(
          qab,
          "relative max-md:hidden",
          p.isCommentsPanelOpen ? "text-foreground" : "text-muted-foreground"
        ),
        title: t("pptx.toolbar.comments"),
        "aria-label": t("pptx.toolbar.comments"),
        children: [
          /* @__PURE__ */ jsx(LuMessageSquare, { className: ics }),
          (p.slideCommentCount ?? 0) > 0 && /* @__PURE__ */ jsx("span", { className: "absolute -top-0.5 -right-0.5 flex items-center justify-center w-3.5 h-3.5 rounded-full bg-primary text-[8px] text-white leading-none", children: p.slideCommentCount })
        ]
      }
    ),
    collab && (collab.status === "connected" || collab.status === "connecting") && collab.remoteUsers.length > 0 && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onOpenShareDialog,
        className: "mx-1 rounded-sm px-1 py-0.5 hover:bg-accent/60 transition-colors cursor-pointer",
        title: t("pptx.toolbar.sharingUsers", { count: collab.connectedCount }),
        children: /* @__PURE__ */ jsx(
          UserAvatarBar,
          {
            remoteUsers: collab.remoteUsers,
            localUserName: collab.config.userName,
            localUserColor: collab.config.userColor ?? "#6366f1",
            localUserAvatar: collab.config.userAvatar,
            status: collab.status
          }
        )
      }
    ),
    /* @__PURE__ */ jsx(
      ModeSwitcher,
      {
        mode: p.mode,
        onSetMode: p.onSetMode,
        onCloseMasterView: p.onCloseMasterView,
        onToggleSlideSorter: p.onToggleSlideSorter,
        onEnterPresenterView: p.onEnterPresenterView,
        onEnterRehearsalMode: p.onEnterRehearsalMode,
        onOpenSetUpSlideShow: p.onOpenSetUpSlideShow,
        onOpenBroadcastDialog: p.onOpenBroadcastDialog,
        onToggleSubtitles: p.onToggleSubtitles,
        showSubtitles: p.showSubtitles
      }
    ),
    /* @__PURE__ */ jsx(
      CustomShowsControls,
      {
        customShows: p.customShows,
        activeCustomShowId: p.activeCustomShowId,
        canEdit: p.canEdit,
        isCurrentSlideInActiveShow: p.isCurrentSlideInActiveShow,
        onSetActiveCustomShowId: p.onSetActiveCustomShowId,
        onCreateCustomShow: p.onCreateCustomShow,
        onRenameActiveCustomShow: p.onRenameActiveCustomShow,
        onDeleteActiveCustomShow: p.onDeleteActiveCustomShow,
        onToggleCurrentSlideInActiveShow: p.onToggleCurrentSlideInActiveShow
      }
    ),
    sep,
    (mode === "edit" || mode === "master") && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onToggleInspector,
        className: cn(qab, isInspectorPaneOpen ? "text-foreground" : "text-muted-foreground"),
        title: t("pptx.toolbar.toggleInspector"),
        "aria-label": t("pptx.toolbar.toggleInspector"),
        children: /* @__PURE__ */ jsx(LuPanelRight, { className: ic })
      }
    ),
    p.aiEnabled && (mode === "edit" || mode === "master") && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onToggleAiPanel,
        className: cn(qab, p.isAiPanelOpen ? "text-primary" : "text-muted-foreground"),
        title: t("pptx.toolbar.toggleAiAssistant"),
        "aria-label": t("pptx.toolbar.toggleAiAssistant"),
        children: /* @__PURE__ */ jsx(LuSparkles, { className: ic })
      }
    ),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onOpenSettings ?? p.onToggleShortcuts,
        className: cn(qab, "text-muted-foreground"),
        title: t("pptx.toolbar.settingsShortcuts"),
        "aria-label": t("pptx.toolbar.settings"),
        children: /* @__PURE__ */ jsx(LuSettings, { className: ics })
      }
    ),
    !canEdit && p.isProtectedView && p.onEnableEditing ? /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: p.onEnableEditing,
        className: "inline-flex items-center px-2 py-0.5 rounded-sm bg-amber-600/90 text-[10px] text-amber-50 transition-colors hover:bg-amber-600",
        title: t("pptx.options.trust.protectedViewInfo"),
        children: t("pptx.security.enableEditing")
      }
    ) : !canEdit && /* @__PURE__ */ jsx("span", { className: "inline-flex items-center px-2 py-0.5 rounded-sm bg-amber-600/90 text-[10px] text-amber-50", children: t("pptx.toolbar.readOnly") }),
    /* @__PURE__ */ jsx(OverflowMenu, { ...p })
  ] });
}
function Toolbar(p) {
  const { mode, isNarrowViewport, isCompactToolbarOpen, toolbarSection, onSetToolbarSection } = p;
  const { t } = useTranslation();
  const { isTabVisible } = useToolbarVisibility(p.hiddenActions);
  const viewerOptions = useViewerOptionsContext();
  if (isNarrowViewport && mode !== "present") {
    return /* @__PURE__ */ jsx(MobileToolbar, { ...p });
  }
  const sFil = toolbarSection === "file";
  const sHome = toolbarSection === "home";
  const sIns = toolbarSection === "insert";
  const sTxt = sHome || toolbarSection === "text";
  const sArr = sHome || toolbarSection === "arrange";
  const sDrw = toolbarSection === "draw";
  const sDes = toolbarSection === "design";
  const sTrn = toolbarSection === "transitions";
  const sAni = toolbarSection === "animations";
  const sSlw = toolbarSection === "slideShow";
  const sRec = toolbarSection === "record";
  const sRev = toolbarSection === "review";
  const sViw = toolbarSection === "view";
  const sHlp = toolbarSection === "help";
  const showRibbon = mode === "edit" || mode === "master";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      role: "toolbar",
      "aria-label": t("pptx.toolbar.presentationToolbarAria"),
      className: "relative z-20 border-b border-border bg-secondary/50 overflow-visible",
      children: [
        /* @__PURE__ */ jsx(ToolbarPrimaryRow, { ...p }),
        showRibbon && /* @__PURE__ */ jsxs(
          "div",
          {
            role: "tablist",
            className: "flex items-center border-b border-border/60 px-1 max-md:overflow-x-auto max-md:scrollbar-none",
            children: [
              TOOLBAR_SECTIONS.filter((s) => isTabVisible(s.id)).map((s) => /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  role: "tab",
                  "aria-selected": toolbarSection === s.id,
                  title: resolveScreenTip(viewerOptions, t(s.labelKey)),
                  onClick: () => onSetToolbarSection(s.id),
                  className: cn(
                    "relative px-3.5 py-2 text-[12px] font-medium whitespace-nowrap transition-colors max-md:min-h-[36px] max-md:px-3",
                    toolbarSection === s.id ? s.id === "file" ? "text-white bg-primary/80 rounded-sm" : "text-foreground after:absolute after:-bottom-px after:left-0 after:right-0 after:h-[2.5px] after:bg-primary" : s.id === "file" ? "text-primary hover:bg-primary/15 rounded-sm" : "text-muted-foreground hover:text-foreground hover:bg-accent/30"
                  ),
                  children: t(s.labelKey)
                },
                s.id
              )),
              /* @__PURE__ */ jsx("div", { className: "flex-1" }),
              /* @__PURE__ */ jsx(
                TabRowActions,
                {
                  onEnterRehearsalMode: p.canEdit ? p.onEnterRehearsalMode : void 0,
                  onOpenShareDialog: p.onOpenShareDialog,
                  hiddenActions: p.hiddenActions
                }
              ),
              isNarrowViewport && /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  onClick: p.onToggleCompactToolbar,
                  className: cn(
                    "px-2 py-1 rounded text-[11px] transition-colors mr-1",
                    isCompactToolbarOpen ? "bg-primary/80 text-white" : "text-muted-foreground hover:text-foreground"
                  ),
                  title: t(
                    isCompactToolbarOpen ? "pptx.ribbon.collapseRibbon" : "pptx.ribbon.expandRibbon"
                  ),
                  children: t(isCompactToolbarOpen ? "pptx.ribbon.collapseRibbon" : "pptx.ribbon.expandRibbon")
                }
              )
            ]
          }
        ),
        showRibbon && viewerOptions.quickAccess.position === "below" && /* @__PURE__ */ jsx("div", { className: "flex items-center gap-0.5 border-b border-border/60 px-2 py-1", children: /* @__PURE__ */ jsx(
          TitleBarQuickExtras,
          {
            quickAccess: viewerOptions.quickAccess,
            onCommand: p.onQuickCommand
          }
        ) }),
        showRibbon && /* @__PURE__ */ jsxs(
          "div",
          {
            className: cn(
              /*
               * `items-stretch` (not `items-center`): `RibbonGroup` pins its
               * caption to `bottom-0.5` of ITS OWN box (`position: relative`), so
               * a group left at its un-stretched intrinsic height sits centered in
               * the row with its caption floating well above the row's true bottom
               * edge whenever a taller sibling sets the row's height (e.g. View's
               * five-row "Show" group next to its two-row "Zoom" group). Stretching
               * every direct child to the row's full height lines up every group's
               * caption and right-hand divider at the same baseline. File's
               * backstage is unaffected: it renders `position: fixed`, outside this
               * row's flex layout entirely.
               */
              "flex min-h-[82px] items-stretch gap-0 overflow-x-auto px-1 py-0.5 max-md:min-h-0 max-md:px-1 max-md:py-0.5 flex-nowrap [&>*]:shrink-0",
              isNarrowViewport && !isCompactToolbarOpen && "hidden"
            ),
            children: [
              sFil && /* @__PURE__ */ jsx(
                FileSection,
                {
                  fileName: p.fileName,
                  onClose: () => p.onSetToolbarSection("home"),
                  onCreatePresentation: p.onCreatePresentation,
                  onOpenFile: p.onOpenFile,
                  onOpenRecentFile: p.onOpenRecentFile,
                  onExportPng: p.onExportPng,
                  onExportPdf: p.onExportPdf,
                  onExportVideo: p.onExportVideo,
                  onExportGif: p.onExportGif,
                  onExportJson: p.onExportJson,
                  onSaveAsPptx: p.onSaveAsPptx,
                  onSaveAsPpsx: p.onSaveAsPpsx,
                  onSaveAsPptm: p.onSaveAsPptm,
                  hasMacros: p.hasMacros,
                  onCopySlideAsImage: p.onCopySlideAsImage,
                  onPrint: p.onPrint,
                  onOpenSettings: p.onOpenSettings,
                  onOpenShareDialog: p.onOpenShareDialog,
                  onOpenDocumentProperties: p.onOpenDocumentProperties,
                  onOpenPasswordProtection: p.onOpenPasswordProtection,
                  onOpenFontEmbedding: p.onOpenFontEmbedding,
                  onOpenDigitalSignatures: p.onOpenDigitalSignatures,
                  onOpenVersionHistory: p.onToggleVersionHistory,
                  hiddenActions: p.hiddenActions,
                  recentPresentationsCount: p.recentPresentationsCount
                }
              ),
              sHome && /* @__PURE__ */ jsx(
                HomeSection,
                {
                  canEdit: p.canEdit,
                  clipboardPayload: p.clipboardPayload,
                  formatPainterActive: p.formatPainterActive,
                  canActivateFormatPainter: p.canActivateFormatPainter,
                  onCopy: p.onCopy,
                  onCut: p.onCut,
                  onPaste: p.onPaste,
                  onToggleFormatPainter: p.onToggleFormatPainter,
                  layoutOptions: p.layoutOptions,
                  currentLayoutPath: p.currentLayoutPath,
                  loadLayoutPreviews: p.loadLayoutPreviews,
                  themeFonts: p.themeFonts,
                  embeddedFontFamilies: p.embeddedFontFamilies,
                  customFontFamilies: p.customFontFamilies,
                  onInsertSlideFromLayout: p.onInsertSlideFromLayout,
                  onApplyLayout: p.onApplyLayout,
                  onInsertSlideFromTemplate: p.onInsertSlideFromTemplate,
                  templateScheme: p.templateScheme,
                  selectedElement: p.selectedElement,
                  onUpdateTextStyle: p.onUpdateTextStyle,
                  onResetSlide: p.onResetSlide,
                  onAddSection: p.onAddSection
                }
              ),
              sIns && /* @__PURE__ */ jsx(
                InsertSection,
                {
                  canEdit: p.canEdit,
                  newShapeType: p.newShapeType,
                  onSetNewShapeType: p.onSetNewShapeType,
                  onAddTextBox: p.onAddTextBox,
                  onAddShape: p.onAddShape,
                  onAddTable: p.onAddTable,
                  onAddChart: p.onAddChart,
                  onAddSmartArt: p.onAddSmartArt,
                  onAddEquation: p.onAddEquation,
                  onAddActionButton: p.onAddActionButton,
                  onInsertField: p.onInsertField,
                  onOpenHeaderFooter: p.onOpenHeaderFooter,
                  onOpenImagePicker: p.onOpenImagePicker,
                  onOpenMediaPicker: p.onOpenMediaPicker,
                  hasSelection: Boolean(p.selectedElement),
                  onOpenHyperlinkDialog: p.onOpenHyperlinkDialog
                }
              ),
              sTxt && /* @__PURE__ */ jsx(
                TextSection,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  tableEditorState: p.tableEditorState,
                  onUpdateTextStyle: p.onUpdateTextStyle,
                  onTransformTextCase: p.onTransformTextCase
                }
              ),
              sHome && /* @__PURE__ */ jsx(
                EditingSection,
                {
                  onToggleFindReplace: p.onToggleFindReplace,
                  onSelectAll: p.onSelectAll
                }
              ),
              sHome && /* @__PURE__ */ jsx(
                DrawingGroup,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  newShapeType: p.newShapeType,
                  onSetNewShapeType: p.onSetNewShapeType,
                  onAddShape: p.onAddShape,
                  onMoveLayer: p.onMoveLayer,
                  onMoveLayerToEdge: p.onMoveLayerToEdge,
                  onUpdateElementStyle: p.onUpdateElementStyle
                }
              ),
              sDrw && /* @__PURE__ */ jsx(
                DrawSection,
                {
                  activeTool: p.activeTool,
                  drawingColor: p.drawingColor,
                  drawingWidth: p.drawingWidth,
                  onSetActiveTool: p.onSetActiveTool,
                  onSetDrawingColor: p.onSetDrawingColor,
                  onSetDrawingWidth: p.onSetDrawingWidth
                }
              ),
              sArr && /* @__PURE__ */ jsx(
                ArrangeSection,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  selectedCount: p.selectedCount,
                  selectionGroupable: p.selectionGroupable,
                  onAlignElements: p.onAlignElements,
                  onDistributeElements: p.onDistributeElements,
                  canDistribute: p.canDistribute,
                  onFlip: p.onFlip,
                  onMoveLayer: p.onMoveLayer,
                  onMoveLayerToEdge: p.onMoveLayerToEdge,
                  onGroupElements: p.onGroupElements,
                  onUngroupElement: p.onUngroupElement,
                  onUpdateElementStyle: p.onUpdateElementStyle,
                  onDuplicate: p.onDuplicate,
                  onDelete: p.onDelete,
                  formatPainterActive: p.formatPainterActive,
                  onToggleFormatPainter: p.onToggleFormatPainter,
                  canActivateFormatPainter: p.canActivateFormatPainter
                }
              ),
              sDes && /* @__PURE__ */ jsx(
                DesignSection,
                {
                  canEdit: p.canEdit,
                  onToggleThemeGallery: p.onToggleThemeGallery,
                  isThemeGalleryOpen: p.isThemeGalleryOpen,
                  onToggleThemeEditor: p.onToggleThemeEditor,
                  isThemeEditorOpen: p.isThemeEditorOpen,
                  onOpenDocumentProperties: p.onOpenDocumentProperties,
                  onOpenSlideSize: p.onOpenSlideSize,
                  onToggleInspector: p.onToggleInspector,
                  isInspectorPaneOpen: p.isInspectorPaneOpen
                }
              ),
              sTrn && /* @__PURE__ */ jsx(
                TransitionsSection,
                {
                  isInspectorPaneOpen: p.isInspectorPaneOpen,
                  onToggleInspector: p.onToggleInspector,
                  canEdit: p.canEdit,
                  activeSlide: p.activeSlide,
                  onTransitionChange: p.onTransitionChange,
                  onApplyTransitionToAll: p.onApplyTransitionToAll
                }
              ),
              sAni && /* @__PURE__ */ jsx(
                AnimationsSection,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  isInspectorPaneOpen: p.isInspectorPaneOpen,
                  onToggleInspector: p.onToggleInspector,
                  onOpenAnimationPanel: p.onOpenAnimationPanel,
                  onAddAnimation: p.onAddAnimation,
                  onRemoveAnimation: p.onRemoveAnimation
                }
              ),
              sSlw && /* @__PURE__ */ jsx(
                SlideShowSection,
                {
                  onPresent: () => p.onSetMode("present"),
                  onPresentFromBeginning: p.onPresentFromBeginning,
                  onEnterPresenterView: p.onEnterPresenterView ?? (() => {
                  }),
                  onEnterRehearsalMode: p.onEnterRehearsalMode ?? (() => {
                  }),
                  onOpenSetUpSlideShow: p.onOpenSetUpSlideShow ?? (() => {
                  }),
                  onToggleHideSlide: p.onToggleHideSlide ?? (() => {
                  }),
                  activeSlideHidden: p.activeSlideHidden ?? false,
                  onOpenBroadcastDialog: p.onOpenBroadcastDialog ?? (() => {
                  }),
                  onToggleSubtitles: p.onToggleSubtitles ?? (() => {
                  }),
                  showSubtitles: p.showSubtitles ?? false,
                  onSetMode: p.onSetMode,
                  customShowControls: p,
                  hiddenActions: p.hiddenActions,
                  presentationProperties: p.presentationProperties,
                  onPresentationPropertiesChange: p.onPresentationPropertiesChange
                }
              ),
              sRec && /* @__PURE__ */ jsx(
                RecordSection,
                {
                  onRecordFromBeginning: p.onEnterRehearsalMode ?? (() => {
                  }),
                  onRecordFromCurrent: p.onEnterRehearsalMode ?? (() => {
                  })
                }
              ),
              sRev && /* @__PURE__ */ jsx(
                ReviewSection,
                {
                  canEdit: p.canEdit,
                  spellCheckEnabled: p.spellCheckEnabled,
                  onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
                  onToggleComments: p.onToggleComments,
                  isCommentsPanelOpen: p.isCommentsPanelOpen,
                  slideCommentCount: p.slideCommentCount,
                  onCompare: p.onCompare,
                  onOpenAccessibilityCheck: p.onRunAccessibilityCheck,
                  onSetLanguage: p.onOpenSettings
                }
              ),
              sViw && /* @__PURE__ */ jsx(
                ViewSection,
                {
                  canEdit: p.canEdit,
                  editTemplateMode: p.editTemplateMode,
                  onSetEditTemplateMode: p.onSetEditTemplateMode,
                  spellCheckEnabled: p.spellCheckEnabled,
                  onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
                  showGrid: p.showGrid,
                  showRulers: p.showRulers,
                  showGuides: p.showGuides,
                  snapToGrid: p.snapToGrid,
                  snapToShape: p.snapToShape,
                  onSetShowGrid: p.onSetShowGrid,
                  onSetShowRulers: p.onSetShowRulers,
                  onSetShowGuides: p.onSetShowGuides,
                  onSetSnapToGrid: p.onSetSnapToGrid,
                  onSetSnapToShape: p.onSetSnapToShape,
                  onAddGuide: p.onAddGuide,
                  onEnterMasterView: p.onEnterMasterView,
                  isSelectionPaneOpen: p.isSelectionPaneOpen,
                  onToggleSelectionPane: p.onToggleSelectionPane,
                  eyedropperActive: p.eyedropperActive,
                  onToggleEyedropper: p.onToggleEyedropper,
                  onToggleSlideSorter: p.onToggleSlideSorter,
                  onOpenReadingView: p.onOpenReadingView,
                  onOpenOutlineView: p.onOpenOutlineView,
                  onZoomToFit: p.onZoomToFit
                }
              ),
              sHlp && /* @__PURE__ */ jsx(
                HelpSection,
                {
                  onOpenSettings: p.onOpenSettings,
                  onToggleShortcuts: p.onToggleShortcuts,
                  onRunAccessibilityCheck: p.onRunAccessibilityCheck
                }
              )
            ]
          }
        )
      ]
    }
  );
}

// src/viewer/utils/slide-background-style.ts
function getReactSlideBackgroundStyle(slide, slideSize) {
  const style = getSlideBackgroundStyle(slide, slideSize);
  return {
    backgroundColor: style["background-color"],
    backgroundImage: slide?.backgroundImage ? void 0 : style["background-image"],
    backgroundSize: style["background-size"],
    backgroundRepeat: style["background-repeat"]
  };
}
function CanvasGuides({ guides, onDeleteGuide, onStartGuideDrag }) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(Fragment, { children: guides.map((guide) => /* @__PURE__ */ jsx(
    "div",
    {
      className: "absolute z-[49] pointer-events-auto",
      style: guide.axis === "h" ? {
        left: 0,
        right: 0,
        top: guide.position,
        height: 1,
        backgroundColor: "rgba(250, 204, 21, 0.9)",
        cursor: "row-resize"
      } : {
        top: 0,
        bottom: 0,
        left: guide.position,
        width: 1,
        backgroundColor: "rgba(250, 204, 21, 0.9)",
        cursor: "col-resize"
      },
      onDoubleClick: (e) => {
        e.stopPropagation();
        onDeleteGuide?.(guide.id);
      },
      onPointerDown: (e) => {
        e.stopPropagation();
        e.currentTarget.setPointerCapture(e.pointerId);
        onStartGuideDrag({
          id: guide.id,
          axis: guide.axis,
          pointerId: e.pointerId
        });
      },
      title: t("pptx.canvas.dragGuideTooltip")
    },
    guide.id
  )) });
}
function MarqueeOverlay({ marqueeSelectionState }) {
  if (!marqueeSelectionState) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "absolute border border-primary bg-primary/10 pointer-events-none z-50",
      style: {
        left: Math.min(marqueeSelectionState.startX, marqueeSelectionState.currentX),
        top: Math.min(marqueeSelectionState.startY, marqueeSelectionState.currentY),
        width: Math.abs(marqueeSelectionState.currentX - marqueeSelectionState.startX),
        height: Math.abs(marqueeSelectionState.currentY - marqueeSelectionState.startY)
      }
    }
  );
}
function SnapLinesOverlay({ snapLines }) {
  return /* @__PURE__ */ jsx(Fragment, { children: snapLines.map((line, i) => /* @__PURE__ */ jsx(
    "div",
    {
      className: "absolute bg-red-500 pointer-events-none z-50",
      style: line.axis === "x" ? {
        left: line.position,
        top: 0,
        width: 1,
        height: "100%"
      } : {
        left: 0,
        top: line.position,
        width: "100%",
        height: 1
      }
    },
    i
  )) });
}
function CommentMarkersOverlay({
  comments,
  canvasSize,
  onCommentMarkerClick
}) {
  const { t } = useTranslation(), half = COMMENT_MARKER_SIZE / 2, markers = buildCommentMarkers(
    comments,
    canvasSize.width,
    canvasSize.height,
    t("pptx.comments.unknownAuthor")
  );
  return /* @__PURE__ */ jsx("div", { className: "absolute inset-0 pointer-events-none z-[45]", children: markers.map((marker) => /* @__PURE__ */ jsx(
    "div",
    {
      className: "absolute pointer-events-auto cursor-pointer",
      style: {
        left: marker.x - half,
        top: marker.y - half,
        width: COMMENT_MARKER_SIZE,
        height: COMMENT_MARKER_SIZE,
        borderRadius: "50%",
        backgroundColor: "rgba(255, 165, 0, 0.9)",
        border: "2px solid #fff",
        boxShadow: "0 1px 3px rgba(0,0,0,0.3)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 10,
        fontWeight: 700,
        color: "#fff",
        lineHeight: 1
      },
      title: marker.title,
      onClick: (e) => {
        e.stopPropagation();
        onCommentMarkerClick?.(marker.commentId);
      },
      children: marker.label
    },
    marker.commentId
  )) });
}
function ConnectorEndpointOverlay({
  connector,
  elements,
  editorScale,
  canvasStageRef,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const [drag, setDrag] = useState(null);
  const candidates = useMemo(
    () => collectConnectorSiteCandidates(elements.filter((el) => el.id !== connector.id)),
    [elements, connector.id]
  );
  const handles = getConnectorEndpointHandles(connector);
  const snap = drag ? findConnectorSiteNear(candidates, drag.x, drag.y) : null;
  const toSlidePoint = useCallback(
    (clientX, clientY) => {
      const rect = canvasStageRef.current?.getBoundingClientRect();
      const scale = editorScale || 1;
      return {
        x: (clientX - (rect?.left ?? 0)) / scale,
        y: (clientY - (rect?.top ?? 0)) / scale
      };
    },
    [canvasStageRef, editorScale]
  );
  useEffect(() => {
    if (!drag) {
      return;
    }
    const onMove = (event) => {
      const point2 = toSlidePoint(event.clientX, event.clientY);
      setDrag((prev) => prev ? { ...prev, ...point2 } : prev);
    };
    const onUp = (event) => {
      const point2 = toSlidePoint(event.clientX, event.clientY);
      const target = findConnectorSiteNear(candidates, point2.x, point2.y, CONNECTOR_SITE_SNAP_PX);
      const update = resolveConnectorEndpointUpdate(connector, elements, drag.kind, point2, target);
      const next = withConnectorEndpointUpdate(connector, update);
      onUpdateElement(connector.id, {
        x: next.x,
        y: next.y,
        width: next.width,
        height: next.height,
        flipHorizontal: next.flipHorizontal,
        flipVertical: next.flipVertical,
        // Sent whole: a DETACHED end has had its binding key deleted, so a
        // merge of only the surviving keys would leave the stale one behind.
        shapeStyle: next.shapeStyle
      });
      setDrag(null);
    };
    window.addEventListener("pointermove", onMove);
    window.addEventListener("pointerup", onUp);
    window.addEventListener("pointercancel", onUp);
    return () => {
      window.removeEventListener("pointermove", onMove);
      window.removeEventListener("pointerup", onUp);
      window.removeEventListener("pointercancel", onUp);
    };
  }, [drag, candidates, connector, elements, onUpdateElement, toSlidePoint]);
  return /* @__PURE__ */ jsxs("div", { className: "absolute inset-0 z-[56] pointer-events-none", "data-pptx-connector-endpoints": true, children: [
    drag ? candidates.map((site) => /* @__PURE__ */ jsx(
      "div",
      {
        "aria-hidden": "true",
        "data-pptx-connection-site": true,
        className: "absolute rounded-full border-2 border-blue-500",
        style: {
          left: site.x - 4,
          top: site.y - 4,
          width: 8,
          height: 8,
          background: snap?.elementId === site.elementId && snap.siteIndex === site.siteIndex ? "#3b82f6" : "rgba(96,165,250,0.6)"
        }
      },
      `${site.elementId}-${site.siteIndex}`
    )) : null,
    handles.map((handle) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        "data-pptx-connector-endpoint": handle.kind,
        "data-pptx-connector-attached": String(handle.attached),
        "aria-label": t(
          handle.kind === "start" ? "pptx.canvas.connectorEndpointStart" : "pptx.canvas.connectorEndpointEnd"
        ),
        className: "absolute rounded-full border-2 border-white shadow",
        style: {
          left: (drag?.kind === handle.kind ? drag.x : handle.x) - 5,
          top: (drag?.kind === handle.kind ? drag.y : handle.y) - 5,
          width: 10,
          height: 10,
          // A bound end is filled, a loose one hollow, so "is this
          // connector actually attached?" is answerable at a glance.
          background: handle.attached ? "#16a34a" : "#ffffff",
          boxShadow: "0 0 0 1px #16a34a",
          cursor: "crosshair",
          pointerEvents: "auto",
          touchAction: "none"
        },
        onPointerDown: (event) => {
          event.stopPropagation();
          event.preventDefault();
          const point2 = toSlidePoint(event.clientX, event.clientY);
          setDrag({ kind: handle.kind, ...point2 });
        }
      },
      handle.kind
    ))
  ] });
}
function ConnectorOverlay({
  activeSlide,
  canvasSize,
  zoom,
  connectorDragState,
  onConnectionSiteDown,
  onConnectorDragMove,
  onConnectionSiteDrop,
  onConnectorDragEnd
}) {
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "absolute inset-0 z-[55] pointer-events-none",
      onMouseMove: onConnectorDragMove,
      onMouseUp: onConnectorDragEnd,
      style: { pointerEvents: connectorDragState ? "auto" : "none" },
      children: [
        collectConnectorSiteCandidates(activeSlide.elements).map((site) => /* @__PURE__ */ jsx(
          "div",
          {
            "data-pptx-connection-site": true,
            className: "absolute rounded-full border-2 border-blue-500 bg-blue-400/60 hover:bg-blue-500 hover:scale-125 transition-transform cursor-crosshair",
            style: {
              left: site.x - 5,
              top: site.y - 5,
              width: 10,
              height: 10,
              pointerEvents: "auto",
              zIndex: 56
            },
            onMouseDown: (e) => onConnectionSiteDown(site.elementId, site.siteIndex, e),
            onMouseUp: () => connectorDragState ? onConnectionSiteDrop(site.elementId, site.siteIndex) : void 0
          },
          `${site.elementId}-site-${site.siteIndex}`
        )),
        connectorDragState && /* @__PURE__ */ jsx(
          ConnectorDragPreview,
          {
            activeSlide,
            canvasSize,
            zoom,
            connectorDragState
          }
        )
      ]
    }
  );
}
function ConnectorDragPreview({
  activeSlide,
  canvasSize,
  zoom,
  connectorDragState
}) {
  const startEl = activeSlide.elements.find((el) => el.id === connectorDragState.startElementId);
  if (!startEl) {
    return null;
  }
  const startSites = getShapeConnectionSites(startEl);
  const startSite = startSites[connectorDragState.startSiteIndex] ?? startSites[0];
  const sx = startEl.x + startSite.x;
  const sy = startEl.y + startSite.y;
  const stage = zoom.canvasStageRef.current;
  if (!stage) {
    return null;
  }
  const rect = stage.getBoundingClientRect();
  const scale = zoom.editorScale || 1;
  const ex = (connectorDragState.currentX - rect.left) / scale;
  const ey = (connectorDragState.currentY - rect.top) / scale;
  return /* @__PURE__ */ jsx(
    "svg",
    {
      className: "absolute inset-0",
      style: {
        width: canvasSize.width,
        height: canvasSize.height,
        pointerEvents: "none"
      },
      viewBox: `0 0 ${canvasSize.width} ${canvasSize.height}`,
      children: /* @__PURE__ */ jsx(
        "line",
        {
          x1: sx,
          y1: sy,
          x2: ex,
          y2: ey,
          stroke: "#3b82f6",
          strokeWidth: 2,
          strokeDasharray: "6 4",
          strokeLinecap: "round"
        }
      )
    }
  );
}
function DrawingOverlaySvg({
  canvasSize,
  activeTool,
  drawingColor,
  drawingWidth,
  isStrokeActive,
  liveStrokeD,
  liveStrokeView,
  onPointerDown,
  onPointerMove,
  onPointerUp
}) {
  return /* @__PURE__ */ jsx(
    "svg",
    {
      className: "absolute inset-0 z-[60]",
      style: {
        width: canvasSize.width,
        height: canvasSize.height,
        cursor: "crosshair",
        touchAction: "none"
      },
      viewBox: `0 0 ${canvasSize.width} ${canvasSize.height}`,
      onPointerDown,
      onPointerMove,
      onPointerUp,
      children: isStrokeActive && liveStrokeView ? renderStrokeView(liveStrokeView, true, void 0, "ink-live-preview") : isStrokeActive && liveStrokeD && /* @__PURE__ */ jsx(
        "path",
        {
          d: liveStrokeD,
          fill: "none",
          stroke: drawingColor,
          strokeWidth: drawingWidth,
          strokeOpacity: activeTool === "highlighter" ? 0.4 : 1,
          strokeLinecap: "round",
          strokeLinejoin: "round"
        }
      )
    }
  );
}
function GridOverlay({ canvasSize, gridSpacingPx = GRID_SIZE, visible }) {
  const spacing = Math.max(gridSpacingPx, 2);
  const patternId = useMemo(() => `grid-dot-pattern-${Math.random().toString(36).slice(2, 8)}`, []);
  if (!visible) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      className: "absolute inset-0 pointer-events-none z-[2]",
      width: canvasSize.width,
      height: canvasSize.height,
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        /* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx("pattern", { id: patternId, width: spacing, height: spacing, patternUnits: "userSpaceOnUse", children: /* @__PURE__ */ jsx("circle", { cx: spacing / 2, cy: spacing / 2, r: 0.6, fill: "rgba(156, 163, 175, 0.55)" }) }) }),
        /* @__PURE__ */ jsx("rect", { width: canvasSize.width, height: canvasSize.height, fill: `url(#${patternId})` })
      ]
    }
  );
}
function MotionPathOverlay({
  element,
  path,
  canvasSize,
  scale,
  canEdit,
  onChangePath
}) {
  const { t } = useTranslation();
  const dragRef = useRef(null);
  const frame = {
    originX: element.x + element.width / 2,
    originY: element.y + element.height / 2,
    slideWidth: canvasSize.width,
    slideHeight: canvasSize.height
  };
  const d = motionPathToSvgD(path, frame);
  const end = motionPathEndPixel(path, frame);
  const editable = canEdit && Boolean(onChangePath) && isEditableMotionPath(path);
  const handlePointerDown = useCallback(
    (event) => {
      if (!editable) {
        return;
      }
      event.stopPropagation();
      event.preventDefault();
      event.currentTarget.setPointerCapture(event.pointerId);
      dragRef.current = {
        pointerId: event.pointerId,
        startX: event.clientX,
        startY: event.clientY
      };
    },
    [editable]
  );
  const handlePointerMove = useCallback(
    (event) => {
      const drag = dragRef.current;
      if (!drag || drag.pointerId !== event.pointerId || !onChangePath) {
        return;
      }
      event.stopPropagation();
      const dxPx = (event.clientX - drag.startX) / (scale || 1);
      const dyPx = (event.clientY - drag.startY) / (scale || 1);
      const nextX = (end.x + dxPx - frame.originX) / frame.slideWidth;
      const nextY = (end.y + dyPx - frame.originY) / frame.slideHeight;
      const next = setMotionPathEnd(path, nextX, nextY);
      if (next !== path) {
        dragRef.current = {
          pointerId: event.pointerId,
          startX: event.clientX,
          startY: event.clientY
        };
        onChangePath(next);
      }
    },
    [
      end.x,
      end.y,
      frame.originX,
      frame.originY,
      frame.slideWidth,
      frame.slideHeight,
      onChangePath,
      path,
      scale
    ]
  );
  const handlePointerUp = useCallback((event) => {
    if (dragRef.current?.pointerId === event.pointerId) {
      event.currentTarget.releasePointerCapture(event.pointerId);
      dragRef.current = null;
    }
  }, []);
  if (!d) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      className: "pointer-events-none absolute left-0 top-0 z-[45]",
      width: canvasSize.width,
      height: canvasSize.height,
      role: "img",
      "aria-label": t("pptx.animation.motionPath.overlay"),
      "data-pptx-motion-path-overlay": "true",
      children: [
        /* @__PURE__ */ jsx(
          "path",
          {
            d,
            fill: "none",
            stroke: "#0ea5e9",
            strokeWidth: 2,
            strokeDasharray: "6 4",
            vectorEffect: "non-scaling-stroke"
          }
        ),
        /* @__PURE__ */ jsx("circle", { cx: frame.originX, cy: frame.originY, r: 5, fill: "#0ea5e9", opacity: 0.55 }),
        /* @__PURE__ */ jsx(
          "circle",
          {
            cx: end.x,
            cy: end.y,
            r: 7,
            fill: "#ffffff",
            stroke: "#0ea5e9",
            strokeWidth: 2,
            className: editable ? "pointer-events-auto cursor-move" : "",
            "aria-label": t("pptx.animation.motionPath.endHandle"),
            "data-pptx-motion-path-handle": "end",
            onPointerDown: handlePointerDown,
            onPointerMove: handlePointerMove,
            onPointerUp: handlePointerUp,
            onPointerCancel: handlePointerUp
          }
        )
      ]
    }
  );
}
function HorizontalRulerView({ ticks, widthPx, highlight }) {
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      width: widthPx,
      height: RULER_THICKNESS,
      className: "block select-none",
      "aria-hidden": "true",
      "data-pptx-ruler": "h",
      children: [
        /* @__PURE__ */ jsx("rect", { width: widthPx, height: RULER_THICKNESS, className: "fill-gray-100 dark:fill-gray-800" }),
        /* @__PURE__ */ jsx(
          "line",
          {
            x1: 0,
            y1: RULER_THICKNESS - 0.5,
            x2: widthPx,
            y2: RULER_THICKNESS - 0.5,
            className: "stroke-border",
            strokeWidth: 1
          }
        ),
        highlight && /* @__PURE__ */ jsx(
          "rect",
          {
            x: highlight.start,
            y: 0,
            width: Math.max(highlight.end - highlight.start, 1),
            height: RULER_THICKNESS,
            className: "fill-primary/20"
          }
        ),
        ticks.map((tick, i) => {
          const tickHeight = tick.isMajor ? RULER_THICKNESS * 0.6 : RULER_THICKNESS * 0.3;
          return /* @__PURE__ */ jsxs("g", { children: [
            /* @__PURE__ */ jsx(
              "line",
              {
                x1: tick.position,
                y1: RULER_THICKNESS,
                x2: tick.position,
                y2: RULER_THICKNESS - tickHeight,
                className: "stroke-gray-400 dark:stroke-gray-500",
                strokeWidth: tick.isMajor ? 1 : 0.5
              }
            ),
            tick.label && /* @__PURE__ */ jsx(
              "text",
              {
                x: tick.position + 2,
                y: RULER_FONT_SIZE + 1,
                fontSize: RULER_FONT_SIZE,
                className: "fill-gray-500 dark:fill-gray-400",
                style: { fontFamily: "system-ui, sans-serif" },
                children: tick.label
              }
            )
          ] }, i);
        })
      ]
    }
  );
}
var HorizontalRuler = React20.memo(HorizontalRulerView);
HorizontalRuler.displayName = "HorizontalRuler";
function VerticalRulerView({ ticks, heightPx, highlight }) {
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      width: RULER_THICKNESS,
      height: heightPx,
      className: "block select-none",
      "aria-hidden": "true",
      "data-pptx-ruler": "v",
      children: [
        /* @__PURE__ */ jsx(
          "rect",
          {
            width: RULER_THICKNESS,
            height: heightPx,
            className: "fill-gray-100 dark:fill-gray-800"
          }
        ),
        /* @__PURE__ */ jsx(
          "line",
          {
            x1: RULER_THICKNESS - 0.5,
            y1: 0,
            x2: RULER_THICKNESS - 0.5,
            y2: heightPx,
            className: "stroke-border",
            strokeWidth: 1
          }
        ),
        highlight && /* @__PURE__ */ jsx(
          "rect",
          {
            x: 0,
            y: highlight.start,
            width: RULER_THICKNESS,
            height: Math.max(highlight.end - highlight.start, 1),
            className: "fill-primary/20"
          }
        ),
        ticks.map((tick, i) => {
          const tickWidth = tick.isMajor ? RULER_THICKNESS * 0.6 : RULER_THICKNESS * 0.3;
          return /* @__PURE__ */ jsxs("g", { children: [
            /* @__PURE__ */ jsx(
              "line",
              {
                x1: RULER_THICKNESS,
                y1: tick.position,
                x2: RULER_THICKNESS - tickWidth,
                y2: tick.position,
                className: "stroke-gray-400 dark:stroke-gray-500",
                strokeWidth: tick.isMajor ? 1 : 0.5
              }
            ),
            tick.label && /* @__PURE__ */ jsx(
              "text",
              {
                x: 2,
                y: tick.position + RULER_FONT_SIZE + 2,
                fontSize: RULER_FONT_SIZE,
                className: "fill-gray-500 dark:fill-gray-400",
                style: { fontFamily: "system-ui, sans-serif" },
                children: tick.label
              }
            )
          ] }, i);
        })
      ]
    }
  );
}
var VerticalRuler = React20.memo(VerticalRulerView);
VerticalRuler.displayName = "VerticalRuler";
function Ruler({
  canvasSize,
  editorScale,
  unit,
  visible,
  selectedBounds,
  onCreateGuideFromRuler
}) {
  const hTicks = useMemo(
    () => generateTicks(canvasSize.width, editorScale, unit),
    [canvasSize.width, editorScale, unit]
  );
  const vTicks = useMemo(
    () => generateTicks(canvasSize.height, editorScale, unit),
    [canvasSize.height, editorScale, unit]
  );
  const hHighlight = useMemo(() => {
    if (!selectedBounds) {
      return null;
    }
    return {
      start: selectedBounds.x * editorScale,
      end: (selectedBounds.x + selectedBounds.width) * editorScale
    };
  }, [selectedBounds, editorScale]);
  const vHighlight = useMemo(() => {
    if (!selectedBounds) {
      return null;
    }
    return {
      start: selectedBounds.y * editorScale,
      end: (selectedBounds.y + selectedBounds.height) * editorScale
    };
  }, [selectedBounds, editorScale]);
  const [rulerDrag, setRulerDrag] = useState(null);
  const hRulerRef = useRef(null);
  const vRulerRef = useRef(null);
  const handleHRulerPointerDown = useCallback(
    (e) => {
      if (!onCreateGuideFromRuler) {
        return;
      }
      e.preventDefault();
      e.currentTarget.setPointerCapture(e.pointerId);
      setRulerDrag({ axis: "h", pointerId: e.pointerId });
    },
    [onCreateGuideFromRuler]
  );
  const handleVRulerPointerDown = useCallback(
    (e) => {
      if (!onCreateGuideFromRuler) {
        return;
      }
      e.preventDefault();
      e.currentTarget.setPointerCapture(e.pointerId);
      setRulerDrag({ axis: "v", pointerId: e.pointerId });
    },
    [onCreateGuideFromRuler]
  );
  const handleRulerPointerUp = useCallback(
    (e) => {
      if (!rulerDrag || !onCreateGuideFromRuler) {
        return;
      }
      try {
        e.currentTarget.releasePointerCapture(rulerDrag.pointerId);
      } catch {
      }
      const horizontal = rulerDrag.axis === "h";
      const rulerEl = horizontal ? hRulerRef.current : vRulerRef.current;
      if (!rulerEl) {
        setRulerDrag(null);
        return;
      }
      const rect = rulerEl.getBoundingClientRect();
      const posScaled = horizontal ? e.clientY - rect.top : e.clientX - rect.left;
      const positionPx = rulerDragToGuidePosition(
        posScaled,
        editorScale,
        horizontal ? canvasSize.height : canvasSize.width
      );
      if (positionPx !== null) {
        onCreateGuideFromRuler(rulerDrag.axis, positionPx);
      }
      setRulerDrag(null);
    },
    [rulerDrag, onCreateGuideFromRuler, editorScale, canvasSize]
  );
  if (!visible) {
    return null;
  }
  const scaledWidth = canvasSize.width * editorScale;
  const scaledHeight = canvasSize.height * editorScale;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "div",
      {
        className: "absolute z-[51] bg-gray-100 dark:bg-gray-800 border-r border-b border-border",
        style: {
          top: 0,
          left: 0,
          width: RULER_THICKNESS,
          height: RULER_THICKNESS
        }
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        ref: hRulerRef,
        className: "absolute z-[50] overflow-visible",
        style: {
          top: 0,
          left: RULER_THICKNESS,
          width: scaledWidth,
          height: RULER_THICKNESS,
          cursor: onCreateGuideFromRuler ? "row-resize" : void 0
        },
        onPointerDown: handleHRulerPointerDown,
        onPointerUp: handleRulerPointerUp,
        children: /* @__PURE__ */ jsx(HorizontalRuler, { ticks: hTicks, widthPx: scaledWidth, highlight: hHighlight })
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        ref: vRulerRef,
        className: "absolute z-[50] overflow-visible",
        style: {
          top: RULER_THICKNESS,
          left: 0,
          width: RULER_THICKNESS,
          height: scaledHeight,
          cursor: onCreateGuideFromRuler ? "col-resize" : void 0
        },
        onPointerDown: handleVRulerPointerDown,
        onPointerUp: handleRulerPointerUp,
        children: /* @__PURE__ */ jsx(VerticalRuler, { ticks: vTicks, heightPx: scaledHeight, highlight: vHighlight })
      }
    )
  ] });
}
function SelectionHandleOverlay({
  element,
  adjustmentHandles,
  onResizePointerDown,
  onAdjustmentPointerDown,
  onRotate,
  onClick,
  onDoubleClick,
  onContextMenu
}) {
  const allow = resolveElementInteractivity(element);
  return /* @__PURE__ */ jsx(
    "div",
    {
      "data-pptx-handle-for": element.id,
      "data-pptx-selection-handle-host": "true",
      style: {
        position: "absolute",
        left: element.x,
        top: element.y,
        width: Math.max(element.width, MIN_ELEMENT_SIZE),
        height: Math.max(element.height, MIN_ELEMENT_SIZE),
        // Every slide element carries an explicit numeric `zIndex` (see
        // `getContainerStyle`), so a sibling with no z-index at all paints
        // at the CSS "z-index: 0" level regardless of DOM order: any
        // element other than the very bottom one (index 0) then renders
        // ABOVE this host, hiding its own resize/rotate handles behind its
        // own fill (worst on a rotated shape, where the rotate knob
        // deliberately overlaps the box) and stealing their clicks. Pinned
        // above the stage's other interaction overlays
        // (marquee z-50, connector overlay z-55/56) to match, well clear of
        // any realistic per-slide element count.
        zIndex: 58,
        transform: getElementTransform(element),
        transformOrigin: "center",
        // Transparent everywhere a handle button is not, so a click that
        // misses every handle still reaches the shape (or whatever is
        // behind it) exactly as if this host did not exist. `ResizeHandles`
        // renders with `forcePointerEvents` so its own buttons opt back in.
        pointerEvents: "none"
      },
      onClick: (e) => onClick(element.id, e),
      onDoubleClick: (e) => onDoubleClick(element.id, e),
      onContextMenu: (e) => onContextMenu(element.id, e),
      children: /* @__PURE__ */ jsx(
        ResizeHandles,
        {
          elementId: element.id,
          adjustmentHandles,
          onResizePointerDown,
          onAdjustmentPointerDown,
          rotation: element.rotation,
          nonRotationTransform: getElementTransformWithoutRotation(element),
          onRotate: allow.rotatable ? onRotate : void 0,
          forcePointerEvents: true
        }
      )
    }
  );
}

// src/viewer/components/canvas/canvas-types.ts
function getElementIdFromEvent(e) {
  let target = e.target;
  while (target) {
    if (target.dataset?.pptxElement === "true") {
      return target.dataset.elementId || null;
    }
    target = target.parentElement;
  }
  return null;
}

// src/viewer/components/canvas/useCanvasEventHandlers.ts
var DOUBLE_TAP_MS = 300;
function isViewportBackgroundMouseDownTarget(target, currentTarget) {
  return target === currentTarget;
}
function useCanvasEventHandlers({
  cbRef,
  onCanvasMouseDown,
  findResults,
  findResultIndex,
  activeSlideIndex,
  selectedElement,
  zoom,
  onMoveGuide
}) {
  const elementFindHighlightsMap = useMemo(() => {
    const map = /* @__PURE__ */ new Map();
    if (!findResults || findResults.length === 0 || activeSlideIndex === null) {
      return map;
    }
    for (let i = 0; i < findResults.length; i++) {
      const r = findResults[i];
      if (r.slideIndex !== activeSlideIndex) {
        continue;
      }
      if (!map.has(r.elementId)) {
        map.set(r.elementId, /* @__PURE__ */ new Map());
      }
      const elMap = map.get(r.elementId);
      if (!elMap.has(r.segmentIndex)) {
        elMap.set(r.segmentIndex, []);
      }
      elMap.get(r.segmentIndex).push({
        startOffset: r.startOffset,
        length: r.length,
        isCurrent: i === (findResultIndex ?? -1)
      });
    }
    return map;
  }, [findResults, findResultIndex, activeSlideIndex]);
  const selectedBounds = useMemo(() => {
    if (!selectedElement) {
      return null;
    }
    return {
      x: selectedElement.x,
      y: selectedElement.y,
      width: selectedElement.width,
      height: selectedElement.height
    };
  }, [selectedElement]);
  const handleStageClick = useCallback(
    (e) => {
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onClick(id, e);
      }
    },
    [cbRef]
  );
  const handleStageDblClick = useCallback(
    (e) => {
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onDoubleClick(id, e);
      }
    },
    [cbRef]
  );
  const suppressNextMouseDownRef = useRef(false);
  const handleStageMouseDown = useCallback(
    (e) => {
      if (suppressNextMouseDownRef.current) {
        suppressNextMouseDownRef.current = false;
        return;
      }
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onMouseDown(id, e);
        return;
      }
      onCanvasMouseDown?.(e);
    },
    [cbRef, onCanvasMouseDown]
  );
  const handleViewportMouseDown = useCallback(
    (e) => {
      if (!isViewportBackgroundMouseDownTarget(e.target, e.currentTarget)) {
        return;
      }
      onCanvasMouseDown?.(e);
    },
    [onCanvasMouseDown]
  );
  const lastTapRef = useRef(null);
  const handleStagePointerDown = useCallback(
    (e) => {
      if (e.pointerType === "mouse") {
        return;
      }
      suppressNextMouseDownRef.current = true;
      e.currentTarget.setPointerCapture?.(e.pointerId);
      const id = getElementIdFromEvent(e);
      if (id) {
        const now = e.timeStamp || Date.now();
        const last = lastTapRef.current;
        if (last && last.id === id && now - last.time < DOUBLE_TAP_MS) {
          lastTapRef.current = null;
          cbRef.current.onDoubleClick(id, e);
          return;
        }
        lastTapRef.current = { id, time: now };
        cbRef.current.onMouseDown(id, e);
        return;
      }
      lastTapRef.current = null;
      onCanvasMouseDown?.(e);
    },
    [cbRef, onCanvasMouseDown]
  );
  const handleStageContextMenu = useCallback(
    (e) => {
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onContextMenu(id, e);
      }
    },
    [cbRef]
  );
  const [draggingGuide, setDraggingGuide] = useState(null);
  const handleStagePointerMove = useCallback(
    (e) => {
      if (!draggingGuide || !onMoveGuide) {
        return;
      }
      const stage = zoom.canvasStageRef.current;
      if (!stage) {
        return;
      }
      const rect = stage.getBoundingClientRect();
      const scale = zoom.editorScale || 1;
      const rawPosition = draggingGuide.axis === "h" ? (e.clientY - rect.top) / scale : (e.clientX - rect.left) / scale;
      onMoveGuide(draggingGuide.id, rawPosition);
    },
    [draggingGuide, onMoveGuide, zoom.canvasStageRef, zoom.editorScale]
  );
  const handleStagePointerUp = useCallback(
    (e) => {
      if (!draggingGuide) {
        return;
      }
      try {
        e.currentTarget.releasePointerCapture(draggingGuide.pointerId);
      } catch {
      }
      setDraggingGuide(null);
    },
    [draggingGuide]
  );
  return {
    elementFindHighlightsMap,
    selectedBounds,
    handleStageClick,
    handleStageDblClick,
    handleStageMouseDown,
    handleViewportMouseDown,
    handleStagePointerDown,
    handleStageContextMenu,
    draggingGuide,
    setDraggingGuide,
    handleStagePointerMove,
    handleStagePointerUp
  };
}
function useConnectorCreation({
  activeSlide,
  zoom: _zoom,
  onCreateConnector
}) {
  const [connectorDragState, setConnectorDragState] = useState(null);
  const handleConnectionSiteDown = useCallback(
    (elementId, siteIndex, e) => {
      e.stopPropagation();
      e.preventDefault();
      setConnectorDragState({
        startElementId: elementId,
        startSiteIndex: siteIndex,
        currentX: e.clientX,
        currentY: e.clientY
      });
    },
    []
  );
  const handleConnectorDragMove = useCallback(
    (e) => {
      if (!connectorDragState) {
        return;
      }
      setConnectorDragState(
        (prev) => prev ? { ...prev, currentX: e.clientX, currentY: e.clientY } : null
      );
    },
    [connectorDragState]
  );
  const handleConnectionSiteDrop = useCallback(
    (targetElementId, targetSiteIndex) => {
      if (!connectorDragState || !onCreateConnector) {
        return;
      }
      const startEl = activeSlide?.elements.find(
        (el) => el.id === connectorDragState.startElementId
      );
      const endEl = activeSlide?.elements.find((el) => el.id === targetElementId);
      if (!startEl || !endEl) {
        setConnectorDragState(null);
        return;
      }
      const authored = authorConnectorBetweenSites(
        { element: startEl, siteIndex: connectorDragState.startSiteIndex },
        { element: endEl, siteIndex: targetSiteIndex }
      );
      if (!authored) {
        setConnectorDragState(null);
        return;
      }
      const newConnector = {
        id: `conn-new-${Date.now()}`,
        type: "connector",
        x: authored.x,
        y: authored.y,
        width: authored.width,
        height: authored.height,
        shapeType: authored.shapeType,
        shapeStyle: {
          strokeColor: "#4472C4",
          strokeWidth: 2,
          connectorStartConnection: authored.startConnection,
          connectorEndConnection: authored.endConnection
        }
      };
      onCreateConnector(newConnector);
      setConnectorDragState(null);
    },
    [connectorDragState, onCreateConnector, activeSlide?.elements]
  );
  const handleConnectorDragEnd = useCallback(() => {
    setConnectorDragState(null);
  }, []);
  return {
    connectorDragState,
    handleConnectionSiteDown,
    handleConnectorDragMove,
    handleConnectionSiteDrop,
    handleConnectorDragEnd
  };
}

// src/viewer/components/canvas/canvas-path.ts
function buildCanvasPathD(points) {
  if (points.length === 0) {
    return "";
  }
  const parts = [`M ${points[0].x} ${points[0].y}`];
  for (let i = 1; i < points.length; i++) {
    parts.push(`L ${points[i].x} ${points[i].y}`);
  }
  return parts.join(" ");
}

// src/viewer/components/canvas/finishDrawStroke.ts
function finishDrawStroke(input) {
  const { tool, points, pressures, tiltX, tiltY, color, width } = input;
  if (points.length < 2) {
    return null;
  }
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const pt of points) {
    if (pt.x < minX) {
      minX = pt.x;
    }
    if (pt.y < minY) {
      minY = pt.y;
    }
    if (pt.x > maxX) {
      maxX = pt.x;
    }
    if (pt.y > maxY) {
      maxY = pt.y;
    }
  }
  const pad = width;
  minX -= pad;
  minY -= pad;
  maxX += pad;
  maxY += pad;
  const w = Math.max(maxX - minX, 1);
  const h = Math.max(maxY - minY, 1);
  const relPoints = points.map((pt) => ({ x: pt.x - minX, y: pt.y - minY }));
  if (tool === "freeform") {
    return {
      kind: "freeform",
      element: buildFreeformShape(relPoints, { x: minX, y: minY, w, h }, color, width)
    };
  }
  return {
    kind: "ink",
    element: buildInkElement(
      relPoints,
      { x: minX, y: minY, w, h },
      tool,
      { pressures, tiltX, tiltY },
      color,
      width
    )
  };
}
function buildFreeformShape(relPoints, box, color, width) {
  const COORD_SCALE = 100;
  const segments = [];
  for (let i = 0; i < relPoints.length; i++) {
    const scaledPt = {
      x: Math.round(relPoints[i].x * COORD_SCALE),
      y: Math.round(relPoints[i].y * COORD_SCALE)
    };
    segments.push(i === 0 ? { type: "moveTo", pt: scaledPt } : { type: "lineTo", pt: scaledPt });
  }
  if (segments.length > 2) {
    segments.push({ type: "close" });
  }
  return {
    id: `shape-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    type: "shape",
    x: box.x,
    y: box.y,
    width: box.w,
    height: box.h,
    shapeType: "custom",
    shapeStyle: { fillColor: "transparent", strokeColor: color, strokeWidth: width },
    customGeometryPaths: [
      { width: Math.round(box.w * COORD_SCALE), height: Math.round(box.h * COORD_SCALE), segments }
    ]
  };
}
function buildInkElement(relPoints, box, tool, channels, color, width) {
  const pathD = buildCanvasPathD(relPoints);
  const isHighlighter = tool === "highlighter";
  const { pressures, tiltX, tiltY } = channels;
  const hasPressure = pressures.length >= 2 && pressures.some((p) => Math.abs(p - pressures[0]) > 0.01);
  const hasTilt = hasTiltData(tiltX, tiltY);
  return {
    id: `ink-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    type: "ink",
    x: box.x,
    y: box.y,
    width: box.w,
    height: box.h,
    inkPaths: [pathD],
    inkColors: [color],
    inkWidths: [width],
    inkOpacities: [isHighlighter ? 0.4 : 1],
    inkTool: isHighlighter ? "highlighter" : "pen",
    ...hasPressure ? { inkPointPressures: [pressures] } : {},
    ...hasTilt ? { inkPointTiltX: [tiltX], inkPointTiltY: [tiltY] } : {}
  };
}
function useLiveInkPreview(isStrokeActive, activeTool, points, pressures, tiltX, tiltY, color, width) {
  const liveStrokeD = useMemo(
    () => isStrokeActive ? buildCanvasPathD(points) : "",
    [isStrokeActive, points]
  );
  const liveStrokeView = useMemo(() => {
    if (!isStrokeActive || activeTool === "select" || activeTool === "eraser") {
      return null;
    }
    const inkPoints = points.map((pt, i) => ({
      x: pt.x,
      y: pt.y,
      pressure: pressures[i],
      tiltX: tiltX[i],
      tiltY: tiltY[i]
    }));
    return buildLiveInkStrokeView({ points: inkPoints, color, width, tool: activeTool });
  }, [isStrokeActive, activeTool, points, pressures, tiltX, tiltY, color, width]);
  return { liveStrokeD, liveStrokeView };
}

// src/viewer/components/canvas/useDrawingOverlay.ts
function useDrawingOverlay({
  activeTool,
  activeSlide,
  zoom,
  drawingColor,
  drawingWidth,
  isDrawingRef,
  onAddInkElement,
  onAddFreeformShape,
  onEraseInkElement
}) {
  const isDrawing = activeTool !== "select";
  const [currentStrokePoints, setCurrentStrokePoints] = useState(
    []
  );
  const [currentStrokePressures, setCurrentStrokePressures] = useState([]);
  const [currentStrokeTiltX, setCurrentStrokeTiltX] = useState([]);
  const [currentStrokeTiltY, setCurrentStrokeTiltY] = useState([]);
  const [isStrokeActive, setIsStrokeActive] = useState(false);
  const pointerToCanvasCoords = useCallback(
    (e) => {
      const stage = zoom.canvasStageRef.current;
      if (!stage) {
        return null;
      }
      const rect = stage.getBoundingClientRect();
      const scale = zoom.editorScale || 1;
      return {
        x: (e.clientX - rect.left) / scale,
        y: (e.clientY - rect.top) / scale
      };
    },
    [zoom.canvasStageRef, zoom.editorScale]
  );
  const handleDrawPointerDown = useCallback(
    (e) => {
      if (activeTool === "select") {
        return;
      }
      if (activeTool === "eraser" && activeSlide) {
        const pt2 = pointerToCanvasCoords(e);
        if (!pt2) {
          return;
        }
        const hitId = findEraserHitElementId(activeSlide.elements, pt2);
        if (hitId) {
          onEraseInkElement?.(hitId);
        }
        return;
      }
      const pt = pointerToCanvasCoords(e);
      if (!pt) {
        return;
      }
      e.preventDefault();
      e.target.setPointerCapture(e.pointerId);
      setCurrentStrokePoints([pt]);
      setCurrentStrokePressures([e.pressure]);
      setCurrentStrokeTiltX([e.tiltX ?? 0]);
      setCurrentStrokeTiltY([e.tiltY ?? 0]);
      setIsStrokeActive(true);
      if (isDrawingRef) {
        isDrawingRef.current = true;
      }
    },
    [activeTool, activeSlide, pointerToCanvasCoords, isDrawingRef, onEraseInkElement]
  );
  const handleDrawPointerMove = useCallback(
    (e) => {
      if (!isStrokeActive || activeTool === "select" || activeTool === "eraser") {
        return;
      }
      const pt = pointerToCanvasCoords(e);
      if (!pt) {
        return;
      }
      setCurrentStrokePoints((prev) => [...prev, pt]);
      setCurrentStrokePressures((prev) => [...prev, e.pressure]);
      setCurrentStrokeTiltX((prev) => [...prev, e.tiltX ?? 0]);
      setCurrentStrokeTiltY((prev) => [...prev, e.tiltY ?? 0]);
    },
    [isStrokeActive, activeTool, pointerToCanvasCoords]
  );
  const handleDrawPointerUp = useCallback(
    (e) => {
      if (!isStrokeActive || activeTool === "select" || activeTool === "eraser") {
        return;
      }
      e.target.releasePointerCapture(e.pointerId);
      setIsStrokeActive(false);
      if (isDrawingRef) {
        isDrawingRef.current = false;
      }
      const finished = finishDrawStroke({
        tool: activeTool,
        points: currentStrokePoints,
        pressures: currentStrokePressures,
        tiltX: currentStrokeTiltX,
        tiltY: currentStrokeTiltY,
        color: drawingColor,
        width: drawingWidth
      });
      if (finished?.kind === "freeform") {
        onAddFreeformShape?.(finished.element);
      } else if (finished?.kind === "ink") {
        onAddInkElement?.(finished.element);
      }
      setCurrentStrokePoints([]);
      setCurrentStrokePressures([]);
      setCurrentStrokeTiltX([]);
      setCurrentStrokeTiltY([]);
    },
    [
      isStrokeActive,
      activeTool,
      currentStrokePoints,
      currentStrokeTiltX,
      currentStrokeTiltY,
      drawingColor,
      drawingWidth,
      isDrawingRef,
      onAddInkElement,
      onAddFreeformShape,
      currentStrokePressures
    ]
  );
  const { liveStrokeD, liveStrokeView } = useLiveInkPreview(
    isStrokeActive,
    activeTool,
    currentStrokePoints,
    currentStrokePressures,
    currentStrokeTiltX,
    currentStrokeTiltY,
    drawingColor,
    drawingWidth
  );
  return {
    isDrawing,
    isStrokeActive,
    liveStrokeD,
    liveStrokeView,
    currentStrokePoints,
    handleDrawPointerDown,
    handleDrawPointerMove,
    handleDrawPointerUp
  };
}
function useStableCallbacks(callbacks) {
  const cbRef = useRef(callbacks);
  cbRef.current = callbacks;
  const stableResizePointerDown = useCallback(
    (elementId, e, handle) => cbRef.current.onResizePointerDown(elementId, e, handle),
    []
  );
  const stableAdjustmentPointerDown = useCallback(
    (elementId, e, descriptor) => cbRef.current.onAdjustmentPointerDown(elementId, e, descriptor),
    []
  );
  const stableRotate = useCallback(
    (elementId, rotationDeg) => cbRef.current.onRotate?.(elementId, rotationDeg),
    []
  );
  const stableInlineEditChange = useCallback(
    (text) => cbRef.current.onInlineEditChange(text),
    []
  );
  const stableInlineEditCommit = useCallback(() => cbRef.current.onInlineEditCommit(), []);
  const stableInlineEditCancel = useCallback(() => cbRef.current.onInlineEditCancel(), []);
  const stableTableCellSelect = useCallback(
    (cell, elementId) => cbRef.current.onTableCellSelect(cell, elementId),
    []
  );
  const stableCommitCellEdit = useCallback(
    (elementId, rowIndex, colIndex, text) => cbRef.current.onCommitCellEdit?.(elementId, rowIndex, colIndex, text),
    []
  );
  const stableUpdateSmartArtElement = useCallback(
    (elementId, updates) => cbRef.current.onUpdateSmartArtElement?.(elementId, updates),
    []
  );
  const stableFormatText = useCallback(
    (updates) => cbRef.current.onFormatText?.(updates),
    []
  );
  const stableResizeTableColumns = useCallback(
    (elementId, newWidths) => cbRef.current.onResizeTableColumns?.(elementId, newWidths),
    []
  );
  const stableResizeTableRow = useCallback(
    (elementId, rowIndex, newHeight) => cbRef.current.onResizeTableRow?.(elementId, rowIndex, newHeight),
    []
  );
  return {
    cbRef,
    stableResizePointerDown,
    stableAdjustmentPointerDown,
    stableRotate,
    stableInlineEditChange,
    stableInlineEditCommit,
    stableInlineEditCancel,
    stableTableCellSelect,
    stableCommitCellEdit,
    stableUpdateSmartArtElement,
    stableFormatText,
    stableResizeTableColumns,
    stableResizeTableRow
  };
}
function ActiveXControlOverlay({
  controls,
  canvasSize,
  resolveFallbackImage
}) {
  if (controls.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx("div", { className: "absolute inset-0 pointer-events-none z-[40]", "data-testid": "pptx-activex-overlay", children: controls.map((control, idx) => {
    const view = getActiveXControlOverlayView(control, canvasSize, idx, resolveFallbackImage);
    if (view.className === "image" && view.imageUrl) {
      return /* @__PURE__ */ jsx(
        "img",
        {
          src: view.imageUrl,
          alt: view.label,
          title: `ActiveX control: ${view.label}`,
          className: "absolute",
          style: { left: view.left, top: view.top, width: view.width, height: view.height }
        },
        `${control.relId}-${idx}`
      );
    }
    return /* @__PURE__ */ jsxs(
      "div",
      {
        className: "absolute",
        title: `ActiveX control: ${view.label} (interactive controls are not supported in the viewer)`,
        style: {
          left: view.left,
          top: view.top,
          width: view.width,
          height: view.height,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          gap: 6,
          padding: "2px 6px",
          boxSizing: "border-box",
          border: "1px dashed rgba(100, 116, 139, 0.8)",
          borderRadius: 4,
          background: "rgba(148, 163, 184, 0.14)",
          color: "rgb(51, 65, 85)",
          fontSize: 11,
          fontWeight: 600,
          lineHeight: 1.2,
          overflow: "hidden",
          textOverflow: "ellipsis",
          whiteSpace: "nowrap"
        },
        children: [
          /* @__PURE__ */ jsx("span", { "aria-hidden": "true", children: "\u2699" }),
          /* @__PURE__ */ jsx("span", { style: { overflow: "hidden", textOverflow: "ellipsis" }, children: view.label })
        ]
      },
      `${control.relId}-${idx}`
    );
  }) });
}
function SlideBackgroundImageLayer({
  slide
}) {
  if (!slide?.backgroundImage) {
    return null;
  }
  const properties = slide.backgroundImageProperties ?? {};
  const originalEffects = properties.imageEffects;
  const renderEffects = originalEffects ? { ...originalEffects } : void 0;
  if (renderEffects) {
    delete renderEffects.alphaModFix;
    delete renderEffects.alphaModFixRawXml;
  }
  const idPart = String(slide.id || "slide").replace(/[^A-Za-z0-9_-]/gu, "-");
  const backgroundElement = {
    id: `slide-background-${idPart}`,
    type: "picture",
    x: 0,
    y: 0,
    width: 1,
    height: 1,
    ...properties,
    imageData: slide.backgroundImage,
    imageEffects: renderEffects
  };
  const opacity = typeof originalEffects?.alphaModFix === "number" ? Math.max(0, Math.min(1, originalEffects.alphaModFix / 100)) : void 0;
  return /* @__PURE__ */ jsx(
    "div",
    {
      "aria-hidden": "true",
      className: "absolute inset-0 overflow-hidden pointer-events-none select-none",
      style: { zIndex: 0 },
      children: renderImg(
        backgroundElement,
        getImageRenderStyle(backgroundElement),
        getImageEffectsFilter(backgroundElement),
        "",
        opacity
      )
    }
  );
}
var EMPTY_ADJUSTMENT_HANDLES = [];
function SlideCanvas({
  activeSlide,
  templateElements,
  canvasSize,
  zoom,
  mode,
  canEdit,
  editTemplateMode,
  selectedElementIdSet,
  selectedElement,
  inlineEditingElementId,
  inlineEditingText,
  spellCheckEnabled,
  mediaDataUrls,
  tableEditorState,
  marqueeSelectionState,
  snapLines,
  showGrid,
  gridSpacingPx,
  showRulers,
  rulerUnit = "inches",
  guides,
  presentationElementStates,
  presentationKeyframesCss,
  onClick,
  onDoubleClick,
  onMouseDown,
  onContextMenu,
  onCanvasMouseDown,
  onResizePointerDown,
  onAdjustmentPointerDown,
  onRotate,
  onInlineEditChange,
  onInlineEditCommit,
  onInlineEditCancel,
  onTableCellSelect,
  onCommitCellEdit,
  onUpdateSmartArtElement,
  onFormatText,
  onResizeTableColumns,
  onResizeTableRow,
  findResults,
  findResultIndex,
  activeSlideIndex,
  activeTool = "select",
  drawingColor = "#000000",
  drawingWidth = 3,
  isDrawingRef,
  onAddInkElement,
  onAddFreeformShape,
  onEraseInkElement,
  onActionClick,
  onHyperlinkClick,
  comments,
  showCommentMarkers = false,
  onCommentMarkerClick,
  onMoveGuide,
  onDeleteGuide,
  onCreateGuideFromRuler,
  connectorCreationMode = false,
  onCreateConnector,
  onUpdateSlideAnimations,
  allSlides,
  onZoomClick,
  sourceSlideIndex,
  fieldContext,
  tableStyleContext,
  collaborationOverlay,
  aiActive = false
}) {
  const isEditableCanvas = (mode === "edit" || mode === "master") && canEdit;
  const {
    cbRef,
    stableResizePointerDown,
    stableAdjustmentPointerDown,
    stableRotate,
    stableInlineEditChange,
    stableInlineEditCommit,
    stableInlineEditCancel,
    stableTableCellSelect,
    stableCommitCellEdit,
    stableUpdateSmartArtElement,
    stableFormatText,
    stableResizeTableColumns,
    stableResizeTableRow
  } = useStableCallbacks({
    onClick,
    onDoubleClick,
    onMouseDown,
    onContextMenu,
    onResizePointerDown,
    onAdjustmentPointerDown,
    onRotate,
    onInlineEditChange,
    onInlineEditCommit,
    onInlineEditCancel,
    onTableCellSelect,
    onCommitCellEdit,
    onUpdateSmartArtElement,
    onFormatText,
    onResizeTableColumns,
    onResizeTableRow
  });
  const {
    elementFindHighlightsMap,
    selectedBounds,
    handleStageClick,
    handleStageDblClick,
    handleStageMouseDown,
    handleViewportMouseDown,
    handleStagePointerDown,
    handleStageContextMenu,
    setDraggingGuide,
    handleStagePointerMove,
    handleStagePointerUp
  } = useCanvasEventHandlers({
    cbRef,
    onCanvasMouseDown,
    findResults,
    findResultIndex,
    activeSlideIndex,
    selectedElement,
    zoom,
    onMoveGuide
  });
  const selectedMotionPath = selectedElement ? motionPathFor(activeSlide?.animations ?? [], selectedElement.id) : void 0;
  const handleMotionPathChange = useCallback(
    (path) => {
      if (!selectedElement || !onUpdateSlideAnimations) {
        return;
      }
      onUpdateSlideAnimations(
        setMotionPath(activeSlide?.animations ?? [], selectedElement.id, path)
      );
    },
    [activeSlide?.animations, onUpdateSlideAnimations, selectedElement]
  );
  const {
    connectorDragState,
    handleConnectionSiteDown,
    handleConnectorDragMove,
    handleConnectionSiteDrop,
    handleConnectorDragEnd
  } = useConnectorCreation({ activeSlide, zoom, onCreateConnector });
  const {
    isDrawing,
    isStrokeActive,
    liveStrokeD,
    liveStrokeView,
    handleDrawPointerDown,
    handleDrawPointerMove,
    handleDrawPointerUp
  } = useDrawingOverlay({
    activeTool,
    activeSlide,
    zoom,
    drawingColor,
    drawingWidth,
    isDrawingRef,
    onAddInkElement,
    onAddFreeformShape,
    onEraseInkElement
  });
  const rulerOffset = showRulers ? RULER_THICKNESS : 0;
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref: zoom.canvasViewportRef,
      "data-pptx-viewport": true,
      className: "flex-1 flex overflow-auto relative",
      style: { touchAction: "pan-x pan-y" },
      onMouseDown: handleViewportMouseDown,
      children: /* @__PURE__ */ jsxs(
        "div",
        {
          ref: zoom.editWrapperRef,
          className: "relative m-auto",
          style: {
            width: canvasSize.width * zoom.editorScale + rulerOffset,
            height: canvasSize.height * zoom.editorScale + rulerOffset
          },
          children: [
            /* @__PURE__ */ jsx(
              Ruler,
              {
                canvasSize,
                editorScale: zoom.editorScale,
                unit: rulerUnit,
                visible: showRulers,
                selectedBounds,
                onCreateGuideFromRuler
              }
            ),
            /* @__PURE__ */ jsxs(
              "div",
              {
                ref: zoom.canvasStageRef,
                role: "region",
                "aria-label": `Slide ${(activeSlideIndex ?? 0) + 1}`,
                "aria-roledescription": "slide",
                "data-pptx-ai-active": aiActive ? "true" : void 0,
                className: "relative shadow-2xl",
                style: {
                  width: canvasSize.width,
                  height: canvasSize.height,
                  transform: `scale(${zoom.editorScale})`,
                  transformOrigin: "top left",
                  // Motion-path keyframes translate by a fraction of the SLIDE, so
                  // the stage publishes its own size for those calc() offsets.
                  ["--pptx-slide-w"]: `${canvasSize.width}px`,
                  ["--pptx-slide-h"]: `${canvasSize.height}px`,
                  marginTop: rulerOffset,
                  marginLeft: rulerOffset,
                  // In edit/master mode the stage must own all touch gestures so
                  // drag/resize/marquee aren't stolen by the browser for panning or
                  // pinch-zoom. View/present mode keeps the default so the slide can
                  // still be scrolled and swipe-navigated.
                  touchAction: isEditableCanvas ? "none" : void 0,
                  ...getReactSlideBackgroundStyle(activeSlide, {
                    widthPx: canvasSize.width,
                    heightPx: canvasSize.height
                  })
                },
                onClick: handleStageClick,
                onDoubleClick: handleStageDblClick,
                onMouseDown: handleStageMouseDown,
                onPointerDown: isEditableCanvas ? handleStagePointerDown : void 0,
                onContextMenu: handleStageContextMenu,
                onPointerMove: handleStagePointerMove,
                onPointerUp: handleStagePointerUp,
                children: [
                  /* @__PURE__ */ jsx(SlideBackgroundImageLayer, { slide: activeSlide }),
                  presentationKeyframesCss && /* @__PURE__ */ jsx("style", { children: presentationKeyframesCss }),
                  /* @__PURE__ */ jsx(GridOverlay, { canvasSize, gridSpacingPx, visible: showGrid }),
                  /* @__PURE__ */ jsx(
                    CanvasGuides,
                    {
                      guides,
                      onDeleteGuide,
                      onStartGuideDrag: setDraggingGuide
                    }
                  ),
                  templateElements.map((element, index) => /* @__PURE__ */ jsx(
                    ElementRenderer,
                    {
                      element,
                      activeSlide,
                      isSelected: selectedElementIdSet.has(element.id),
                      isInlineEditing: inlineEditingElementId === element.id,
                      inlineEditingText,
                      canInteract: (mode === "edit" || mode === "master") && canEdit && editTemplateMode,
                      spellCheckEnabled,
                      mediaDataUrls,
                      selectionColorClass: "blue-400",
                      showHoverBorder: false,
                      templateEditing: editTemplateMode,
                      zIndex: index,
                      imageAltText: "Template element",
                      showResizeHandles: shouldShowElementHandles(
                        isEditableCanvas,
                        selectedElementIdSet.has(element.id),
                        selectedElementIdSet.size
                      ),
                      renderInk: false,
                      renderGroups: true,
                      adjustmentHandles: isEditableCanvas && selectedElement?.id === element.id ? getShapeAdjustmentHandleDescriptors(element) : EMPTY_ADJUSTMENT_HANDLES,
                      onResizePointerDown: stableResizePointerDown,
                      onAdjustmentPointerDown: stableAdjustmentPointerDown,
                      onRotate: stableRotate,
                      onInlineEditChange: stableInlineEditChange,
                      onInlineEditCommit: stableInlineEditCommit,
                      onInlineEditCancel: stableInlineEditCancel,
                      onUpdateSmartArtElement: stableUpdateSmartArtElement,
                      onFormatText: stableFormatText,
                      onActionClick,
                      onHyperlinkClick,
                      animationState: presentationElementStates?.get(element.id),
                      presentationElementStates,
                      allSlides,
                      onZoomClick,
                      sourceSlideIndex,
                      fieldContext,
                      tableStyleContext
                    },
                    `tpl-${element.id}`
                  )),
                  activeSlide?.elements.map((element, index) => /* @__PURE__ */ jsx(
                    ElementRenderer,
                    {
                      element,
                      activeSlide,
                      isSelected: selectedElementIdSet.has(element.id),
                      isInlineEditing: inlineEditingElementId === element.id,
                      inlineEditingText,
                      canInteract: isEditableCanvas,
                      spellCheckEnabled,
                      mediaDataUrls,
                      tableEditorState,
                      selectionColorClass: "blue-500",
                      showHoverBorder: true,
                      zIndex: templateElements.length + index,
                      imageAltText: "Slide element",
                      showResizeHandles: shouldShowElementHandles(
                        isEditableCanvas,
                        selectedElementIdSet.has(element.id),
                        selectedElementIdSet.size
                      ),
                      renderInk: true,
                      renderGroups: true,
                      adjustmentHandles: isEditableCanvas && selectedElement?.id === element.id ? getShapeAdjustmentHandleDescriptors(element) : EMPTY_ADJUSTMENT_HANDLES,
                      onResizePointerDown: stableResizePointerDown,
                      onAdjustmentPointerDown: stableAdjustmentPointerDown,
                      onRotate: stableRotate,
                      onInlineEditChange: stableInlineEditChange,
                      onInlineEditCommit: stableInlineEditCommit,
                      onInlineEditCancel: stableInlineEditCancel,
                      onUpdateSmartArtElement: stableUpdateSmartArtElement,
                      onFormatText: stableFormatText,
                      onTableCellSelect: stableTableCellSelect,
                      onCommitCellEdit: stableCommitCellEdit,
                      onResizeTableColumns: stableResizeTableColumns,
                      onResizeTableRow: stableResizeTableRow,
                      findHighlights: elementFindHighlightsMap.get(element.id),
                      onActionClick,
                      onHyperlinkClick,
                      animationState: presentationElementStates?.get(element.id),
                      presentationElementStates,
                      allSlides,
                      onZoomClick,
                      sourceSlideIndex,
                      fieldContext,
                      tableStyleContext
                    },
                    element.id
                  )),
                  selectedElement && shouldShowElementHandles(isEditableCanvas, true, selectedElementIdSet.size) && !isConnectorOrLineElement(selectedElement) && /* @__PURE__ */ jsx(
                    SelectionHandleOverlay,
                    {
                      element: selectedElement,
                      adjustmentHandles: getShapeAdjustmentHandleDescriptors(selectedElement),
                      onResizePointerDown: stableResizePointerDown,
                      onAdjustmentPointerDown: stableAdjustmentPointerDown,
                      onRotate: stableRotate,
                      onClick,
                      onDoubleClick,
                      onContextMenu
                    }
                  ),
                  /* @__PURE__ */ jsx(MarqueeOverlay, { marqueeSelectionState }),
                  activeSlide?.activeXControls && activeSlide.activeXControls.length > 0 && /* @__PURE__ */ jsx(ActiveXControlOverlay, { controls: activeSlide.activeXControls, canvasSize }),
                  showCommentMarkers && comments && comments.length > 0 && /* @__PURE__ */ jsx(
                    CommentMarkersOverlay,
                    {
                      comments,
                      canvasSize,
                      onCommentMarkerClick
                    }
                  ),
                  /* @__PURE__ */ jsx(SnapLinesOverlay, { snapLines }),
                  isEditableCanvas && selectedElement?.type === "connector" && activeSlide && /* @__PURE__ */ jsx(
                    ConnectorEndpointOverlay,
                    {
                      connector: selectedElement,
                      elements: activeSlide.elements,
                      editorScale: zoom.editorScale,
                      canvasStageRef: zoom.canvasStageRef,
                      onUpdateElement: stableUpdateSmartArtElement
                    }
                  ),
                  connectorCreationMode && activeSlide && /* @__PURE__ */ jsx(
                    ConnectorOverlay,
                    {
                      activeSlide,
                      canvasSize,
                      zoom,
                      connectorDragState,
                      onConnectionSiteDown: handleConnectionSiteDown,
                      onConnectorDragMove: handleConnectorDragMove,
                      onConnectionSiteDrop: handleConnectionSiteDrop,
                      onConnectorDragEnd: handleConnectorDragEnd
                    }
                  ),
                  isEditableCanvas && selectedElement && selectedMotionPath && /* @__PURE__ */ jsx(
                    MotionPathOverlay,
                    {
                      element: selectedElement,
                      path: selectedMotionPath,
                      canvasSize,
                      scale: zoom.editorScale,
                      canEdit,
                      onChangePath: handleMotionPathChange
                    }
                  ),
                  isDrawing && /* @__PURE__ */ jsx(
                    DrawingOverlaySvg,
                    {
                      canvasSize,
                      activeTool,
                      drawingColor,
                      drawingWidth,
                      isStrokeActive,
                      liveStrokeD,
                      liveStrokeView,
                      onPointerDown: handleDrawPointerDown,
                      onPointerMove: handleDrawPointerMove,
                      onPointerUp: handleDrawPointerUp
                    }
                  ),
                  collaborationOverlay
                ]
              }
            )
          ]
        }
      )
    }
  );
}
function ErrorState({ className, error }) {
  return /* @__PURE__ */ jsx(
    "div",
    {
      role: "alert",
      "aria-live": "assertive",
      className: cn(
        "h-full w-full flex items-center justify-center p-6 text-center text-red-400",
        className
      ),
      children: error
    }
  );
}
function ShortcutPanel({ isOpen, onClose }) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-pptx-shortcuts-panel": "true",
      className: "absolute top-14 right-3 z-40 w-[min(24rem,calc(100%-1.5rem))] rounded border border-border bg-popover shadow-2xl",
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
          /* @__PURE__ */ jsx("span", { className: "text-xs uppercase tracking-wide text-foreground", children: t("pptx.shortcuts.title") }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "rounded px-2 py-1 text-[11px] text-foreground hover:bg-muted hover:text-foreground",
              children: t("pptx.shortcuts.close")
            }
          )
        ] }),
        /* @__PURE__ */ jsx("div", { className: "max-h-64 overflow-y-auto p-2 space-y-1", children: SHORTCUT_REFERENCE_ITEMS$1.map((shortcut) => /* @__PURE__ */ jsxs(
          "div",
          {
            className: "flex items-center justify-between gap-3 rounded bg-muted/80 px-2 py-1.5",
            children: [
              /* @__PURE__ */ jsx("span", { className: "text-xs text-foreground", children: t(shortcut.actionKey) }),
              /* @__PURE__ */ jsx("span", { className: "font-mono text-[11px] text-foreground whitespace-nowrap", children: shortcut.shortcut })
            ]
          },
          shortcut.actionKey
        )) })
      ]
    }
  );
}
function AccessibilityPanel({
  isOpen,
  issues,
  onClose,
  reducedMotion,
  onToggleReducedMotion
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      role: "dialog",
      "aria-label": t("pptx.accessibility.title"),
      className: "absolute top-14 right-3 z-40 w-[min(28rem,calc(100%-1.5rem))] rounded border border-border bg-popover shadow-2xl",
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
          /* @__PURE__ */ jsx("span", { className: "text-xs uppercase tracking-wide text-foreground", children: t("pptx.accessibility.title") }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.accessibility.issueCount", { count: issues.length }) }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                "aria-label": t("pptx.accessibility.closePanel"),
                className: "rounded px-2 py-1 text-[11px] text-foreground hover:bg-muted hover:text-foreground",
                children: t("pptx.accessibility.close")
              }
            )
          ] })
        ] }),
        onToggleReducedMotion !== void 0 && /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
          /* @__PURE__ */ jsx("label", { htmlFor: "reduced-motion-toggle", className: "text-xs text-foreground", children: t("pptx.accessibility.reduceMotion") }),
          /* @__PURE__ */ jsx(
            "button",
            {
              id: "reduced-motion-toggle",
              type: "button",
              role: "switch",
              "aria-label": t("pptx.accessibility.reduceMotion"),
              "aria-checked": reducedMotion ?? false,
              onClick: onToggleReducedMotion,
              className: cn(
                "relative inline-flex h-5 w-9 items-center rounded-full transition-colors",
                reducedMotion ? "bg-primary" : "bg-muted-foreground/30"
              ),
              children: /* @__PURE__ */ jsx(
                "span",
                {
                  className: cn(
                    "inline-block h-3.5 w-3.5 rounded-full bg-white transition-transform",
                    reducedMotion ? "translate-x-4" : "translate-x-0.5"
                  )
                }
              )
            }
          )
        ] }),
        /* @__PURE__ */ jsx(
          "div",
          {
            role: "list",
            "aria-label": t("pptx.accessibility.issuesList"),
            className: "max-h-72 overflow-y-auto p-2 space-y-1",
            children: issues.length === 0 ? /* @__PURE__ */ jsx("div", { role: "listitem", className: "text-center text-xs text-muted-foreground py-4", children: t("pptx.accessibility.noIssues") }) : issues.map((issue, idx) => /* @__PURE__ */ jsxs(
              "div",
              {
                role: "listitem",
                className: cn(
                  "flex items-start gap-2 rounded px-2 py-1.5 text-xs",
                  /* Severity-based background and text colouring */
                  issue.severity === "error" ? "bg-red-900/30 text-red-300" : issue.severity === "warning" ? "bg-amber-900/30 text-amber-300" : "bg-muted/50 text-muted-foreground"
                ),
                children: [
                  /* @__PURE__ */ jsx("span", { className: "shrink-0 mt-0.5", "aria-hidden": "true", children: issue.severity === "error" ? "\u25CF" : issue.severity === "warning" ? "\u25B2" : "\u2139" }),
                  /* @__PURE__ */ jsxs("span", { children: [
                    issue.severity === "error" ? t("pptx.accessibility.error") : issue.severity === "warning" ? t("pptx.accessibility.warning") : t("pptx.accessibility.info"),
                    issue.message
                  ] })
                ]
              },
              idx
            ))
          }
        )
      ]
    }
  );
}
function ExportProgressModal({
  isOpen,
  title,
  progress,
  statusMessage,
  onCancel
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  const clampedProgress = clampPercent2(progress);
  return /* @__PURE__ */ jsx(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/60 backdrop-blur-sm",
      children: /* @__PURE__ */ jsxs("div", { className: "w-96 max-w-[calc(100%-2rem)] rounded-xl border border-border bg-background p-6 shadow-2xl", children: [
        /* @__PURE__ */ jsx("h3", { className: "mb-4 text-sm font-semibold text-foreground", children: title }),
        /* @__PURE__ */ jsx("div", { className: "mb-3 h-2.5 w-full overflow-hidden rounded-full bg-accent", children: /* @__PURE__ */ jsx(
          "div",
          {
            className: "h-full rounded-full bg-primary transition-all duration-300 ease-out",
            style: { width: `${clampedProgress}%` }
          }
        ) }),
        /* @__PURE__ */ jsxs("div", { className: "mb-4 flex items-center justify-between text-xs text-muted-foreground", children: [
          /* @__PURE__ */ jsx("span", { children: statusMessage ?? t("pptx.export.processing") }),
          /* @__PURE__ */ jsxs("span", { className: "tabular-nums", children: [
            clampedProgress,
            "%"
          ] })
        ] }),
        /* @__PURE__ */ jsx("div", { className: "flex justify-end", children: /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onCancel,
            className: "rounded-md border border-border bg-muted px-4 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
            children: t("pptx.export.cancel")
          }
        ) })
      ] })
    }
  );
}
function formatAutosaveAge(timestamp, t) {
  const diff = Date.now() - timestamp;
  const minutes = Math.floor(diff / 6e4);
  if (minutes < 1) {
    return t("pptx.autosave.justNow");
  }
  if (minutes === 1) {
    return t("pptx.autosave.oneMinAgo");
  }
  return t("pptx.autosave.minutesAgo", { count: minutes });
}
function StatusBar({
  slideCount,
  activeSlideIndex,
  isDirty,
  autosaveStatus,
  scale,
  onZoomIn,
  onZoomOut,
  onZoomToFit,
  isNotesExpanded,
  onToggleNotes,
  mode,
  onSetMode,
  onToggleSlideSorter,
  collaborationSlot,
  hideZoomControls = false,
  hideNotesToggle = false,
  hideFullscreenToggle = false
}) {
  const { t } = useTranslation();
  let statusText;
  if (autosaveStatus?.state === "saving") {
    statusText = t("pptx.autosave.saving");
  } else if (autosaveStatus?.state === "saved") {
    statusText = t("pptx.autosave.saved", {
      time: formatAutosaveAge(autosaveStatus.timestamp, t)
    });
  } else if (autosaveStatus?.state === "error") {
    statusText = t("pptx.autosave.error");
  } else if (isDirty) {
    statusText = t("pptx.statusBar.unsavedChanges");
  } else {
    statusText = t("pptx.statusBar.allSaved");
  }
  const vb = "p-1 rounded-sm transition-colors hover:bg-accent/60 text-muted-foreground active:scale-95 active:opacity-80";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "w-full px-2 py-0.5 border-t border-border bg-secondary/50 text-[10px] text-muted-foreground flex items-center gap-1",
        // Pins the row height to the shared metric instead of letting it fall
        // out of the padding + button box, which is how the Vanilla and Svelte
        // ports ended up 2px shorter than this one.
        STATUS_BAR_CLASSES.container
      ),
      children: [
        /* @__PURE__ */ jsx("span", { className: "shrink-0", children: slideCount > 0 ? t("pptx.statusBar.slideOf", {
          current: Math.min(activeSlideIndex + 1, slideCount),
          total: slideCount
        }) : t("pptx.statusBar.noSlides") }),
        /* @__PURE__ */ jsx("div", { className: "w-px h-3 bg-border/40 mx-1 max-md:hidden" }),
        /* @__PURE__ */ jsx("span", { className: "shrink-0 max-md:hidden text-[10px]", children: t("pptx.statusBar.language") }),
        /* @__PURE__ */ jsx("div", { className: "w-px h-3 bg-border/60 mx-1 max-md:hidden" }),
        /* @__PURE__ */ jsx(
          "span",
          {
            className: cn(
              "shrink-0 max-md:hidden",
              autosaveStatus?.state === "error" ? "text-red-400" : autosaveStatus?.state === "saving" ? "text-yellow-400" : ""
            ),
            children: statusText
          }
        ),
        /* @__PURE__ */ jsx("div", { className: "flex-1" }),
        onToggleNotes && !hideNotesToggle && /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick: onToggleNotes,
            className: cn(
              vb,
              "flex items-center gap-1 text-[10px]",
              isNotesExpanded && "text-primary"
            ),
            title: t("pptx.statusBar.toggleNotes"),
            "aria-label": t("pptx.statusBar.toggleNotes"),
            children: [
              /* @__PURE__ */ jsx(LuStickyNote, { className: "w-3 h-3" }),
              /* @__PURE__ */ jsx("span", { className: "max-md:hidden", children: t("pptx.notes.title") })
            ]
          }
        ),
        /* @__PURE__ */ jsx("div", { className: "w-px h-3 bg-border/60 mx-0.5" }),
        onSetMode && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-0.5", children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: () => onSetMode("edit"),
              className: cn(vb, mode === "edit" && "text-primary"),
              title: t("pptx.statusBar.normalView"),
              "aria-label": t("pptx.statusBar.normalView"),
              children: /* @__PURE__ */ jsx(LuMonitor, { className: "w-3.5 h-3.5" })
            }
          ),
          onToggleSlideSorter && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onToggleSlideSorter,
              className: vb,
              title: t("pptx.statusBar.slideSorter"),
              "aria-label": t("pptx.statusBar.slideSorter"),
              children: /* @__PURE__ */ jsx(LuColumns2, { className: "w-3.5 h-3.5" })
            }
          ),
          !hideFullscreenToggle && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: () => onSetMode("present"),
              className: cn(vb, mode === "present" && "text-primary"),
              title: t("pptx.statusBar.slideShow"),
              "aria-label": t("pptx.statusBar.slideShow"),
              children: /* @__PURE__ */ jsx(LuPresentation, { className: "w-3.5 h-3.5" })
            }
          )
        ] }),
        collaborationSlot && /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx("div", { className: "w-px h-3 bg-border/40 mx-0.5" }),
          collaborationSlot
        ] }),
        scale !== void 0 && !hideZoomControls && /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx("div", { className: "w-px h-3 bg-border/60 mx-0.5" }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-0.5", children: [
            onZoomOut && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onZoomOut,
                className: vb,
                title: t("pptx.statusBar.zoomOut"),
                "aria-label": t("pptx.statusBar.zoomOut"),
                children: /* @__PURE__ */ jsx(LuMinus, { className: "w-3 h-3" })
              }
            ),
            /* @__PURE__ */ jsxs(
              "button",
              {
                type: "button",
                onClick: onZoomToFit,
                className: "px-1.5 py-0.5 rounded-sm hover:bg-accent/60 text-[10px] text-muted-foreground tabular-nums min-w-[3rem] text-center transition-colors",
                title: t("pptx.statusBar.zoomToFit"),
                children: [
                  Math.round((scale ?? 1) * 100),
                  "%"
                ]
              }
            ),
            onZoomIn && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onZoomIn,
                className: vb,
                title: t("pptx.statusBar.zoomIn"),
                "aria-label": t("pptx.statusBar.zoomIn"),
                children: /* @__PURE__ */ jsx(LuPlus, { className: "w-3 h-3" })
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function ResizeHandle({
  direction,
  onResize,
  onResizeEnd,
  className
}) {
  const startPos = useRef(0);
  const handlePointerDown = useCallback(
    (e) => {
      e.preventDefault();
      e.stopPropagation();
      const target = e.currentTarget;
      target.setPointerCapture(e.pointerId);
      startPos.current = direction === "horizontal" ? e.clientX : e.clientY;
      const handlePointerMove = (ev) => {
        const current = direction === "horizontal" ? ev.clientX : ev.clientY;
        const delta = current - startPos.current;
        startPos.current = current;
        onResize(delta);
      };
      const handlePointerUp = () => {
        target.removeEventListener("pointermove", handlePointerMove);
        target.removeEventListener("pointerup", handlePointerUp);
        target.removeEventListener("pointercancel", handlePointerUp);
        onResizeEnd?.();
      };
      target.addEventListener("pointermove", handlePointerMove);
      target.addEventListener("pointerup", handlePointerUp);
      target.addEventListener("pointercancel", handlePointerUp);
    },
    [direction, onResize, onResizeEnd]
  );
  return /* @__PURE__ */ jsx(
    "div",
    {
      role: "separator",
      "aria-orientation": direction,
      className: cn(
        // Outer container: generous hit area for easy clicking.
        // The visible indicator is rendered via the inner pseudo-element (::after).
        "relative flex-shrink-0 z-20 group",
        direction === "horizontal" ? "w-3 cursor-col-resize -mx-1" : "h-3 cursor-row-resize -my-1",
        className
      ),
      onPointerDown: handlePointerDown,
      children: /* @__PURE__ */ jsx(
        "div",
        {
          className: cn(
            "absolute transition-colors",
            direction === "horizontal" ? "top-0 bottom-0 left-1/2 -translate-x-1/2 w-0.5 group-hover:w-1 group-hover:bg-primary/40 group-active:w-1 group-active:bg-primary/60" : "left-0 right-0 top-1/2 -translate-y-1/2 h-0.5 group-hover:h-1 group-hover:bg-primary/40 group-active:h-1 group-active:bg-primary/60"
          )
        }
      )
    }
  );
}
function SlideThumbnailImpl({
  slide,
  templateElements,
  canvasSize,
  fieldContext,
  tableStyleContext
}) {
  const safeCanvasWidth = Math.max(canvasSize.width, 1);
  const safeCanvasHeight = Math.max(canvasSize.height, 1);
  const scale = SLIDE_NAV_THUMBNAIL_WIDTH / safeCanvasWidth;
  const previewHeight = Math.max(56, Math.round(safeCanvasHeight * scale));
  const previewElements = buildPreviewElements(slide, templateElements);
  const slideFieldContext = deriveSlideFieldContext(fieldContext, slide);
  const { t } = useTranslation();
  const transitionLabel = slide.transition ? (() => {
    const opt = SLIDE_TRANSITION_OPTIONS.find((o) => o.value === slide.transition?.type);
    return opt ? t(opt.i18nKey) : slide.transition.type;
  })() : void 0;
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "relative w-full overflow-hidden rounded border border-border bg-white",
      style: {
        height: previewHeight,
        ...getReactSlideBackgroundStyle(slide, {
          widthPx: safeCanvasWidth,
          heightPx: safeCanvasHeight
        })
      },
      children: /* @__PURE__ */ jsxs(
        "div",
        {
          className: "absolute top-0 left-0 origin-top-left",
          style: {
            width: safeCanvasWidth,
            height: safeCanvasHeight,
            transform: `scale(${scale})`,
            transformOrigin: "top left"
          },
          children: [
            /* @__PURE__ */ jsx(SlideBackgroundImageLayer, { slide }),
            slide.transition && slide.transition.type !== "none" && slide.transition.type !== "cut" && /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute top-0.5 right-0.5 z-10 px-1 py-px rounded bg-primary/80 text-[7px] text-white leading-tight pointer-events-none",
                title: t("pptx.slideThumbnail.transitionTooltip", { name: transitionLabel }),
                children: transitionLabel
              }
            ),
            previewElements.map((element, index) => /* @__PURE__ */ jsx(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index,
                fieldContext: slideFieldContext,
                tableStyleContext
              },
              element.id
            ))
          ]
        }
      )
    }
  );
}
function arePropsEqual2(prev, next) {
  if (prev.slide.id !== next.slide.id) {
    return false;
  }
  if (prev.slide.isDirty !== next.slide.isDirty) {
    return false;
  }
  if (prev.slide.hidden !== next.slide.hidden) {
    return false;
  }
  if (prev.slide.elements !== next.slide.elements) {
    return false;
  }
  if (prev.slide.backgroundColor !== next.slide.backgroundColor) {
    return false;
  }
  if (prev.slide.backgroundImage !== next.slide.backgroundImage) {
    return false;
  }
  if (prev.slide.backgroundGradient !== next.slide.backgroundGradient) {
    return false;
  }
  if (prev.slide.backgroundPattern !== next.slide.backgroundPattern) {
    return false;
  }
  if (prev.templateElements !== next.templateElements) {
    return false;
  }
  if (prev.fieldContext !== next.fieldContext) {
    return false;
  }
  if (prev.tableStyleContext !== next.tableStyleContext) {
    return false;
  }
  if (prev.canvasSize.width !== next.canvasSize.width || prev.canvasSize.height !== next.canvasSize.height) {
    return false;
  }
  return true;
}
var SlideThumbnail = React20.memo(SlideThumbnailImpl, arePropsEqual2);

// src/viewer/components/context-menu-dispatch.ts
function tableCell(props) {
  const { selectedElement, tableEditorState } = props;
  if (selectedElement?.type !== "table" || tableEditorState === null) {
    return null;
  }
  const rows = selectedElement.tableData?.rows;
  const cell = rows?.[tableEditorState.rowIndex]?.cells[tableEditorState.columnIndex];
  return {
    hasMultiCellSelection: Array.isArray(tableEditorState.selectedCells) && tableEditorState.selectedCells.length >= 2,
    isMergedCell: Boolean(cell && ((cell.gridSpan ?? 1) > 1 || (cell.rowSpan ?? 1) > 1))
  };
}
function contextMenuContext(props) {
  return {
    elementType: props.selectedElement?.type ?? null,
    table: tableCell(props),
    hasMultiSelection: Boolean(props.hasMultiSelection),
    selectionGroupable: props.selectionGroupable,
    // Both AI entries appear together or not at all, so a viewer that wired
    // only one of them does not produce a menu the other bindings cannot match.
    aiEnabled: Boolean(props.onAskAi) && Boolean(props.onFixAi)
  };
}
function contextMenuHandlers(props) {
  const { onAction, onClose } = props;
  const andClose = (run) => run && (() => {
    run();
    onClose();
  });
  return {
    copy: andClose(() => onAction("copy")),
    cut: andClose(() => onAction("cut")),
    paste: andClose(() => onAction("paste")),
    duplicate: andClose(() => onAction("duplicate")),
    "bring-forward": andClose(() => onAction("bring-forward")),
    "send-backward": andClose(() => onAction("send-backward")),
    "bring-front": andClose(() => onAction("bring-front")),
    "send-back": andClose(() => onAction("send-back")),
    "ai-ask": andClose(props.onAskAi),
    "ai-fix": andClose(props.onFixAi),
    comment: andClose(() => onAction("comment")),
    hyperlink: andClose(() => onAction("editHyperlink")),
    "table-insert-row-above": andClose(() => props.onInsertTableRow("above")),
    "table-insert-row-below": andClose(() => props.onInsertTableRow("below")),
    "table-delete-row": andClose(props.onDeleteTableRow),
    "table-insert-col-left": andClose(() => props.onInsertTableColumn("left")),
    "table-insert-col-right": andClose(() => props.onInsertTableColumn("right")),
    "table-delete-col": andClose(props.onDeleteTableColumn),
    "table-merge-selected": andClose(props.onMergeSelectedCells),
    "table-merge-right": andClose(props.onMergeCellRight),
    "table-merge-down": andClose(props.onMergeCellDown),
    "table-split": andClose(props.onSplitCell),
    group: andClose(() => onAction("group")),
    ungroup: andClose(() => onAction("ungroup")),
    delete: andClose(() => onAction("delete"))
  };
}
var ITEM_CLASS = "w-full px-3 py-1.5 text-left hover:bg-muted";
var DANGER_ITEM_CLASS = "w-full px-3 py-1.5 text-left text-red-300 hover:bg-red-900/40";
var DISABLED_CLASS = " opacity-40 pointer-events-none";
function ContextMenuItem({
  onSelect,
  danger,
  disabled,
  children
}) {
  return /* @__PURE__ */ jsx(
    "button",
    {
      type: "button",
      role: "menuitem",
      disabled,
      className: `${danger ? DANGER_ITEM_CLASS : ITEM_CLASS}${disabled ? DISABLED_CLASS : ""}`,
      onClick: onSelect,
      children
    }
  );
}
function ContextMenuSeparator() {
  return /* @__PURE__ */ jsx("div", { role: "separator", "aria-orientation": "horizontal", className: "my-1 border-t border-border" });
}
function ContextMenu(props) {
  const { contextMenuState, mode, onClose } = props;
  const { t } = useTranslation();
  if (!contextMenuState || mode !== "edit") {
    return null;
  }
  const handlers = contextMenuHandlers(props);
  const entries = buildContextMenuEntries(contextMenuContext(props));
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "div",
      {
        className: "fixed inset-0 z-[119]",
        onClick: onClose,
        onContextMenu: (e) => {
          e.preventDefault();
          onClose();
        }
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        "data-pptx-context-menu": "true",
        role: "menu",
        "aria-label": t("pptx.contextMenu.ariaLabel"),
        className: "fixed z-[120] min-w-[180px] rounded border border-border bg-popover shadow-2xl py-1.5 text-xs text-foreground",
        style: {
          left: Math.max(contextMenuState.x, 8),
          top: Math.max(contextMenuState.y, 8)
        },
        children: entries.map((entry) => {
          const run = handlers[entry.id];
          return /* @__PURE__ */ jsxs(Fragment$1, { children: [
            entry.separatorBefore && /* @__PURE__ */ jsx(ContextMenuSeparator, {}),
            /* @__PURE__ */ jsx(
              ContextMenuItem,
              {
                danger: entry.danger,
                disabled: entry.disabled || !run,
                onSelect: () => run?.(),
                children: t(entry.labelKey)
              }
            )
          ] }, entry.id);
        })
      }
    )
  ] });
}
var ic2 = "w-3.5 h-3.5";
var btnBase = "inline-flex items-center justify-center rounded p-1 transition-colors disabled:opacity-40 disabled:cursor-not-allowed";
var btnGhost = `${btnBase} hover:bg-accent text-muted-foreground hover:text-foreground`;
var btnAction = `${btnBase} px-2 py-1 bg-muted hover:bg-accent text-foreground text-xs`;
function FindReplacePanel({
  findQuery,
  replaceQuery,
  findMatchCase,
  findResults,
  findResultIndex,
  onSetFindQuery,
  onSetReplaceQuery,
  onSetFindMatchCase,
  onPerformFind,
  onNavigateResult,
  onReplace,
  onReplaceAll,
  onClose
}) {
  const { t } = useTranslation();
  const searchInputRef = useRef(null);
  useEffect(() => {
    searchInputRef.current?.focus();
  }, []);
  useEffect(() => {
    onPerformFind();
  }, [findQuery, findMatchCase]);
  const handleSearchKeyDown = useCallback(
    (event) => {
      if (event.key === "Escape") {
        onClose();
      } else if (event.key === "Enter") {
        if (event.shiftKey) {
          onNavigateResult(-1);
        } else {
          onNavigateResult(1);
        }
      }
    },
    [onClose, onNavigateResult]
  );
  const handleReplaceKeyDown = useCallback(
    (event) => {
      if (event.key === "Escape") {
        onClose();
      } else if (event.key === "Enter") {
        onReplace();
      }
    },
    [onClose, onReplace]
  );
  const hasResults = findResults.length > 0;
  const matchCountLabel = hasResults ? t("pptx.findReplace.matchCount", { current: findResultIndex + 1, total: findResults.length }) : findQuery.length > 0 ? t("pptx.findReplace.noMatches") : "";
  return /* @__PURE__ */ jsxs("div", { className: "absolute top-2 right-2 z-40 bg-popover border border-border rounded-lg shadow-lg p-3 text-xs text-foreground w-80 backdrop-blur", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between mb-2", children: [
      /* @__PURE__ */ jsxs("span", { className: "font-semibold text-sm inline-flex items-center gap-1.5", children: [
        /* @__PURE__ */ jsx(LuSearch, { className: ic2 }),
        t("pptx.findReplace.title")
      ] }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: btnGhost,
          onClick: onClose,
          title: t("pptx.findReplace.closeEscape"),
          "aria-label": t("pptx.findReplace.closeAriaLabel"),
          children: /* @__PURE__ */ jsx(LuX, { className: ic2 })
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 mb-1.5", children: [
      /* @__PURE__ */ jsxs("div", { className: "relative flex-1", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            ref: searchInputRef,
            type: "text",
            value: findQuery,
            onChange: (e) => onSetFindQuery(e.target.value),
            onKeyDown: handleSearchKeyDown,
            placeholder: t("pptx.findReplace.findPlaceholder"),
            className: "w-full bg-muted border border-border rounded px-2 py-1 pr-7 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:outline-none",
            "aria-label": t("pptx.findReplace.searchText")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: `absolute right-1 top-1/2 -translate-y-1/2 p-0.5 rounded transition-colors ${findMatchCase ? "bg-primary/80 text-white" : "text-muted-foreground hover:text-foreground hover:bg-accent"}`,
            onClick: () => onSetFindMatchCase(!findMatchCase),
            title: t("pptx.findReplace.matchCase"),
            "aria-label": t("pptx.findReplace.toggleMatchCase"),
            "aria-pressed": findMatchCase,
            children: /* @__PURE__ */ jsx(LuCaseSensitive, { className: ic2 })
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: btnGhost,
          onClick: () => onNavigateResult(-1),
          disabled: !hasResults,
          title: t("pptx.findReplace.previousMatch"),
          "aria-label": t("pptx.findReplace.previousMatch"),
          children: /* @__PURE__ */ jsx(LuChevronUp, { className: ic2 })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: btnGhost,
          onClick: () => onNavigateResult(1),
          disabled: !hasResults,
          title: t("pptx.findReplace.nextMatch"),
          "aria-label": t("pptx.findReplace.nextMatch"),
          children: /* @__PURE__ */ jsx(LuChevronDown, { className: ic2 })
        }
      )
    ] }),
    matchCountLabel && /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground mb-2 pl-0.5 tabular-nums", children: matchCountLabel }),
    /* @__PURE__ */ jsx("div", { className: "flex items-center gap-1 mb-2", children: /* @__PURE__ */ jsxs("div", { className: "relative flex-1", children: [
      /* @__PURE__ */ jsx(LuReplace, { className: "absolute left-2 top-1/2 -translate-y-1/2 w-3 h-3 text-muted-foreground" }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          value: replaceQuery,
          onChange: (e) => onSetReplaceQuery(e.target.value),
          onKeyDown: handleReplaceKeyDown,
          placeholder: t("pptx.findReplace.replacePlaceholder"),
          className: "w-full bg-muted border border-border rounded pl-7 pr-2 py-1 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:outline-none",
          "aria-label": t("pptx.findReplace.replacementText")
        }
      )
    ] }) }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1.5", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: btnAction,
          onClick: onReplace,
          disabled: !hasResults,
          title: t("pptx.findReplace.replaceCurrent"),
          children: t("pptx.findReplace.replace")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: btnAction,
          onClick: onReplaceAll,
          disabled: !hasResults,
          title: t("pptx.findReplace.replaceAllMatches"),
          children: t("pptx.findReplace.replaceAll")
        }
      )
    ] })
  ] });
}
function SectionContextMenu({
  state,
  sectionGroups,
  totalSlides,
  onStartRename,
  onDeleteSection,
  onMoveSectionUp,
  onMoveSectionDown,
  onAddSection,
  onClose
}) {
  const { t } = useTranslation();
  const handleAddSectionAfter = () => {
    const group = sectionGroups.find((g) => g.id === state.sectionId);
    if (!group) {
      return;
    }
    const lastSlideIndex = group.slideIndexes[group.slideIndexes.length - 1] ?? 0;
    const nextSlideIndex = Math.min(lastSlideIndex + 1, totalSlides - 1);
    onAddSection?.(t("pptx.sections.defaultName"), nextSlideIndex);
    onClose();
  };
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "fixed z-50 min-w-[160px] rounded-md border border-border bg-popover py-1 shadow-xl",
      style: { left: state.x, top: state.y },
      onClick: (e) => e.stopPropagation(),
      children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
            onClick: () => {
              const group = sectionGroups.find((g) => g.id === state.sectionId);
              if (group) {
                onStartRename(state.sectionId, group.label);
              }
            },
            children: t("pptx.sections.rename")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
            onClick: () => {
              onDeleteSection?.(state.sectionId);
              onClose();
            },
            children: t("pptx.sections.delete")
          }
        ),
        /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border" }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted disabled:opacity-40 disabled:cursor-not-allowed",
            disabled: state.sectionIndex === 0,
            onClick: () => {
              onMoveSectionUp?.(state.sectionId);
              onClose();
            },
            children: t("pptx.sections.moveUp")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted disabled:opacity-40 disabled:cursor-not-allowed",
            disabled: state.sectionIndex >= state.totalSections - 1,
            onClick: () => {
              onMoveSectionDown?.(state.sectionId);
              onClose();
            },
            children: t("pptx.sections.moveDown")
          }
        ),
        /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border" }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
            onClick: handleAddSectionAfter,
            children: t("pptx.sections.addAfter")
          }
        )
      ]
    }
  );
}
function SectionHeader({
  sectionId,
  label,
  slideCount,
  isCollapsed,
  isRenaming,
  renameValue,
  canEdit,
  sectionIndex,
  totalSections,
  renameInputRef,
  onToggle,
  onContextMenu,
  onStartRename,
  onRenameValueChange,
  onCommitRename,
  onCancelRename
}) {
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      className: "flex w-full items-center gap-1 rounded px-1.5 py-1 text-[11px] uppercase tracking-wide text-muted-foreground hover:bg-muted/60 hover:text-foreground",
      onClick: () => onToggle(sectionId, isCollapsed),
      onContextMenu: (e) => canEdit ? onContextMenu(e, sectionId, sectionIndex, totalSections) : void 0,
      onDoubleClick: (e) => {
        if (!canEdit) {
          return;
        }
        e.stopPropagation();
        onStartRename(sectionId, label);
      },
      children: [
        /* @__PURE__ */ jsx(
          LuChevronDown,
          {
            className: cn(
              "h-3 w-3 flex-shrink-0 transition-transform",
              isCollapsed ? "-rotate-90" : "rotate-0"
            )
          }
        ),
        isRenaming ? /* @__PURE__ */ jsx(
          "input",
          {
            ref: renameInputRef,
            type: "text",
            className: "flex-1 bg-muted text-[11px] text-foreground rounded px-1 py-0.5 outline-none border border-primary",
            value: renameValue,
            onChange: (e) => onRenameValueChange(e.target.value),
            onKeyDown: (e) => {
              if (e.key === "Enter") {
                e.preventDefault();
                onCommitRename();
              } else if (e.key === "Escape") {
                e.preventDefault();
                onCancelRename();
              }
              e.stopPropagation();
            },
            onClick: (e) => e.stopPropagation(),
            onBlur: onCommitRename
          }
        ) : /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx("span", { className: "truncate text-left", children: label }),
          /* @__PURE__ */ jsx("span", { className: "ml-auto text-[10px] text-muted-foreground", children: slideCount })
        ] })
      ]
    }
  );
}
function SlideContextMenu({
  state,
  onAddSection,
  onClose
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "fixed z-50 min-w-[180px] rounded-md border border-border bg-popover py-1 shadow-xl",
      style: { left: state.x, top: state.y },
      onClick: (e) => e.stopPropagation(),
      children: /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
          onClick: () => {
            onAddSection?.(t("pptx.sections.defaultName"), state.slideIndex);
            onClose();
          },
          children: t("pptx.sections.addBefore")
        }
      )
    }
  );
}
function LazyThumbnailInner({
  slide,
  templateElements,
  canvasSize,
  previewHeight,
  fieldContext,
  tableStyleContext
}) {
  const containerRef = useRef(null);
  const [isVisible, setIsVisible] = useState(false);
  useEffect(() => {
    const el = containerRef.current;
    if (!el) {
      return;
    }
    if (typeof IntersectionObserver === "undefined") {
      setIsVisible(true);
      return;
    }
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry?.isIntersecting) {
          setIsVisible(true);
          observer.disconnect();
        }
      },
      {
        // Pre-render thumbnails 200px before they enter the viewport
        rootMargin: "200px 0px"
      }
    );
    observer.observe(el);
    return () => {
      observer.disconnect();
    };
  }, []);
  return /* @__PURE__ */ jsx("div", { ref: containerRef, children: isVisible ? /* @__PURE__ */ jsx(
    SlideThumbnail,
    {
      slide,
      templateElements,
      canvasSize,
      fieldContext,
      tableStyleContext
    }
  ) : /* @__PURE__ */ jsx(
    "div",
    {
      className: "relative w-full overflow-hidden rounded border border-border bg-muted/30 animate-pulse",
      style: {
        height: previewHeight,
        minWidth: SLIDE_NAV_THUMBNAIL_WIDTH
      }
    }
  ) });
}
var LazyThumbnail = React20.memo(LazyThumbnailInner);

// src/viewer/components/slides-pane/utils.ts
function formatTimingMs(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
}
function buildFlatPaneItems(sectionGroups, showSectionHeaders, collapsedSections) {
  const items = [];
  for (let si = 0; si < sectionGroups.length; si++) {
    const section = sectionGroups[si];
    if (showSectionHeaders) {
      items.push({
        type: "section",
        sectionIndex: si,
        sectionId: section.id
      });
    }
    const isCollapsed = collapsedSections[section.id] ?? false;
    if (!isCollapsed) {
      for (const idx of section.slideIndexes) {
        items.push({ type: "slide", slideIndex: idx });
      }
    }
  }
  return items;
}
function estimateSlideItemHeight(canvasWidth, canvasHeight) {
  const safeW = Math.max(canvasWidth, 1);
  const safeH = Math.max(canvasHeight, 1);
  const scale = SLIDE_NAV_THUMBNAIL_WIDTH / safeW;
  const previewHeight = Math.max(56, Math.round(safeH * scale));
  return previewHeight + 30;
}
function SlideItemInner({
  slide,
  templateElements,
  slideIndex,
  isActive,
  canvasSize,
  canEdit,
  rehearsalTimings,
  presenceUsers,
  fieldContext,
  tableStyleContext,
  onSelectSlide,
  onSlideContextMenu,
  onAddSection,
  onOpenSlideCtxMenu,
  onDragStart,
  onDragOver,
  onDrop,
  slideRef
}) {
  const { t } = useTranslation();
  const isHidden = Boolean(slide.hidden);
  const cue = hiddenSlideCue(slide.hidden, "rail", slideIndex);
  const handleContextMenu = useCallback(
    (e) => {
      if (canEdit && onAddSection) {
        e.preventDefault();
        e.stopPropagation();
        onOpenSlideCtxMenu(e.clientX, e.clientY, slideIndex);
      } else {
        onSlideContextMenu(e, slideIndex);
      }
    },
    [canEdit, onAddSection, onOpenSlideCtxMenu, onSlideContextMenu, slideIndex]
  );
  const safeCanvasWidth = Math.max(canvasSize.width, 1);
  const safeCanvasHeight = Math.max(canvasSize.height, 1);
  const scale = SLIDE_NAV_THUMBNAIL_WIDTH / safeCanvasWidth;
  const previewHeight = Math.max(56, Math.round(safeCanvasHeight * scale));
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      ref: slideRef,
      "aria-label": `Go to slide ${slideIndex + 1}`,
      "aria-current": isActive ? "true" : void 0,
      "aria-describedby": cue.labelId,
      "data-pptx-slide-hidden": cue.marker,
      className: cn(
        "group relative flex w-full items-center gap-1 cursor-pointer border-0 bg-transparent py-0.5 px-1 text-left transition-all",
        isActive && "bg-accent/40 before:absolute before:left-0 before:top-1 before:bottom-1 before:w-[3px] before:bg-primary before:rounded-r",
        isHidden && "opacity-50"
      ),
      draggable: canEdit,
      onClick: () => onSelectSlide(slideIndex),
      onContextMenu: handleContextMenu,
      onDragStart: (e) => onDragStart(e, slideIndex),
      onDragOver,
      onDrop: (e) => onDrop(e, slideIndex),
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5 w-5 shrink-0", children: [
          /* @__PURE__ */ jsx(
            "span",
            {
              className: cn(
                "text-[10px] tabular-nums text-right select-none w-full",
                isActive ? "text-primary font-medium" : "text-muted-foreground"
              ),
              style: isHidden ? { backgroundImage: HIDDEN_SLIDE_SLASH_GRADIENT } : void 0,
              children: slideIndex + 1
            }
          ),
          presenceUsers && presenceUsers.length > 0 && /* @__PURE__ */ jsx("div", { className: "flex flex-wrap justify-center gap-px", children: presenceUsers.slice(0, 4).map((u, i) => /* @__PURE__ */ jsx(
            "span",
            {
              className: "w-[6px] h-[6px] rounded-full",
              style: { backgroundColor: u.userColor },
              title: u.userName
            },
            i
          )) })
        ] }),
        /* @__PURE__ */ jsxs(
          "div",
          {
            className: cn(
              "relative flex-1 overflow-hidden border transition-colors bg-white",
              isActive ? "border-primary/60" : "border-transparent group-hover:border-border/40"
            ),
            children: [
              isHidden && /* @__PURE__ */ jsx("div", { className: "absolute inset-0 pointer-events-none z-10 bg-[repeating-linear-gradient(135deg,transparent,transparent_4px,rgba(255,255,255,0.04)_4px,rgba(255,255,255,0.04)_8px)]" }),
              /* @__PURE__ */ jsx(
                LazyThumbnail,
                {
                  slide,
                  templateElements,
                  canvasSize,
                  previewHeight,
                  fieldContext,
                  tableStyleContext
                }
              ),
              (slide.comments?.length ?? 0) > 0 && /* @__PURE__ */ jsxs("div", { className: "absolute top-0.5 right-0.5 flex items-center gap-0.5 rounded bg-amber-500/90 px-1 py-0.5 text-[8px] font-medium text-white leading-none z-10", children: [
                /* @__PURE__ */ jsx(LuMessageSquare, { className: "w-2 h-2" }),
                slide.comments?.length
              ] }),
              isHidden && /* @__PURE__ */ jsxs("div", { className: "absolute bottom-0.5 right-0.5 z-10", id: cue.labelId, children: [
                /* @__PURE__ */ jsx(LuEyeOff, { className: "w-3 h-3 text-muted-foreground" }),
                /* @__PURE__ */ jsx("span", { className: "sr-only", children: t(HIDDEN_SLIDE_LABEL_KEY) })
              ] }),
              rehearsalTimings && typeof rehearsalTimings[slideIndex] === "number" && /* @__PURE__ */ jsx("div", { className: "absolute bottom-0.5 left-0.5 z-10", children: /* @__PURE__ */ jsx("span", { className: "text-[8px] font-mono text-amber-400/80 tabular-nums bg-black/50 px-0.5 rounded", children: formatTimingMs(rehearsalTimings[slideIndex]) }) })
            ]
          }
        )
      ]
    }
  );
}
var SlideItem = React20.memo(SlideItemInner);
function useSlidePaneCallbacks(onMoveSlide, onRenameSection, onToggleSectionCollapse) {
  const [collapsedSections, setCollapsedSections] = useState({});
  const [renamingSectionId, setRenamingSectionId] = useState(null);
  const [renameValue, setRenameValue] = useState("");
  const [sectionContextMenu, setSectionContextMenu] = useState(
    null
  );
  const [slideCtxMenu, setSlideCtxMenu] = useState(null);
  useEffect(() => {
    if (!sectionContextMenu && !slideCtxMenu) {
      return;
    }
    const handler = () => {
      setSectionContextMenu(null);
      setSlideCtxMenu(null);
    };
    document.addEventListener("click", handler);
    return () => document.removeEventListener("click", handler);
  }, [sectionContextMenu, slideCtxMenu]);
  const handleDragStart = useCallback((e, slideIndex) => {
    e.dataTransfer.setData("text/plain", String(slideIndex));
    e.dataTransfer.effectAllowed = "move";
  }, []);
  const handleDragOver = useCallback((e) => {
    e.preventDefault();
    e.dataTransfer.dropEffect = "move";
  }, []);
  const handleDrop = useCallback(
    (e, toIndex) => {
      e.preventDefault();
      const fromStr = e.dataTransfer.getData("text/plain");
      const fromIndex = parseInt(fromStr, 10);
      if (!isNaN(fromIndex) && fromIndex !== toIndex) {
        onMoveSlide(fromIndex, toIndex);
      }
    },
    [onMoveSlide]
  );
  const toggleSection = useCallback(
    (sectionId, isCollapsed) => {
      setCollapsedSections((prev) => ({
        ...prev,
        [sectionId]: !isCollapsed
      }));
      onToggleSectionCollapse?.(sectionId);
    },
    [onToggleSectionCollapse]
  );
  const startRename = useCallback((sectionId, currentLabel) => {
    setRenamingSectionId(sectionId);
    setRenameValue(currentLabel);
    setSectionContextMenu(null);
  }, []);
  const commitRename = useCallback(() => {
    if (renamingSectionId && renameValue.trim().length > 0) {
      onRenameSection?.(renamingSectionId, renameValue.trim());
    }
    setRenamingSectionId(null);
    setRenameValue("");
  }, [renamingSectionId, renameValue, onRenameSection]);
  const cancelRename = useCallback(() => {
    setRenamingSectionId(null);
    setRenameValue("");
  }, []);
  const handleSectionContextMenu = useCallback(
    (e, sectionId, sectionIndex, totalSections) => {
      e.preventDefault();
      e.stopPropagation();
      setSectionContextMenu({
        x: e.clientX,
        y: e.clientY,
        sectionId,
        sectionIndex,
        totalSections
      });
    },
    []
  );
  const handleOpenSlideCtxMenu = useCallback((x, y, slideIndex) => {
    setSlideCtxMenu({ x, y, slideIndex });
  }, []);
  const closeSectionContextMenu = useCallback(() => {
    setSectionContextMenu(null);
  }, []);
  const closeSlideCtxMenu = useCallback(() => {
    setSlideCtxMenu(null);
  }, []);
  return {
    collapsedSections,
    renamingSectionId,
    renameValue,
    sectionContextMenu,
    slideCtxMenu,
    setRenameValue,
    handleDragStart,
    handleDragOver,
    handleDrop,
    toggleSection,
    startRename,
    commitRename,
    cancelRename,
    handleSectionContextMenu,
    handleOpenSlideCtxMenu,
    closeSectionContextMenu,
    closeSlideCtxMenu
  };
}
var EMPTY_TEMPLATE_ELEMENTS = [];
var VIRTUALIZATION_THRESHOLD = SLIDE_VIRTUALIZATION_THRESHOLD;
function SlidesPaneSidebar({
  slides,
  templateElementsBySlideId,
  activeSlideIndex,
  canvasSize,
  sectionGroups,
  isOpen,
  canEdit,
  onSelectSlide,
  onSlideContextMenu,
  onMoveSlide,
  onAddSlide,
  onCollapse: _onCollapse,
  onAddSection,
  onRenameSection,
  onDeleteSection,
  onMoveSectionUp,
  onMoveSectionDown,
  onToggleSectionCollapse,
  rehearsalTimings,
  panelWidth,
  fieldContext,
  tableStyleContext
}) {
  const { t } = useTranslation();
  const collab = useCollaboration();
  const slideRefs = useRef(/* @__PURE__ */ new Map());
  const renameInputRef = useRef(null);
  const slidePresenceMap = useMemo(() => {
    if (!collab || collab.remoteUsers.length === 0) {
      return void 0;
    }
    const map = /* @__PURE__ */ new Map();
    for (const user of collab.remoteUsers) {
      const idx = user.activeSlideIndex;
      const existing = map.get(idx);
      const entry = { userName: user.userName, userColor: user.userColor };
      if (existing) {
        existing.push(entry);
      } else {
        map.set(idx, [entry]);
      }
    }
    return map;
  }, [collab]);
  const estimatedItemHeight = useMemo(
    () => estimateSlideItemHeight(canvasSize.width, canvasSize.height),
    [canvasSize.width, canvasSize.height]
  );
  const showSectionHeaders = sectionGroups.some((group) => group.id !== DEFAULT_SECTION_GROUP_ID);
  const {
    collapsedSections,
    renamingSectionId,
    renameValue,
    sectionContextMenu,
    slideCtxMenu,
    setRenameValue,
    handleDragStart,
    handleDragOver,
    handleDrop,
    toggleSection,
    startRename,
    commitRename,
    cancelRename,
    handleSectionContextMenu,
    handleOpenSlideCtxMenu,
    closeSectionContextMenu,
    closeSlideCtxMenu
  } = useSlidePaneCallbacks(onMoveSlide, onRenameSection, onToggleSectionCollapse);
  const effectiveCollapsed = useMemo(() => {
    const map = {};
    for (const group of sectionGroups) {
      map[group.id] = collapsedSections[group.id] ?? group.defaultCollapsed ?? false;
    }
    return map;
  }, [sectionGroups, collapsedSections]);
  const flatItems = useMemo(
    () => buildFlatPaneItems(sectionGroups, showSectionHeaders, effectiveCollapsed),
    [sectionGroups, showSectionHeaders, effectiveCollapsed]
  );
  const shouldVirtualize = slides.length >= VIRTUALIZATION_THRESHOLD;
  const { startIndex, endIndex, totalHeight, offsetY, scrollContainerRef, scrollToIndex } = useVirtualizedSlides({
    totalItems: shouldVirtualize ? flatItems.length : 0,
    itemHeight: estimatedItemHeight
  });
  useEffect(() => {
    if (shouldVirtualize) {
      const flatIdx = flatItems.findIndex(
        (item) => item.type === "slide" && item.slideIndex === activeSlideIndex
      );
      if (flatIdx >= 0) {
        scrollToIndex(flatIdx);
      }
    } else {
      const el = slideRefs.current.get(activeSlideIndex);
      if (el) {
        el.scrollIntoView({ behavior: "smooth", block: "nearest" });
      }
    }
  }, [activeSlideIndex, shouldVirtualize, scrollToIndex, flatItems]);
  useEffect(() => {
    if (renamingSectionId && renameInputRef.current) {
      renameInputRef.current.focus();
      renameInputRef.current.select();
    }
  }, [renamingSectionId]);
  const setSlideRef = useCallback(
    (idx) => (el) => {
      if (el) {
        slideRefs.current.set(idx, el);
      } else {
        slideRefs.current.delete(idx);
      }
    },
    []
  );
  if (!isOpen) {
    return null;
  }
  const renderVirtualized = () => {
    const visibleItems = flatItems.slice(startIndex, endIndex + 1);
    return /* @__PURE__ */ jsx("div", { ref: scrollContainerRef, className: "flex-1 overflow-y-auto px-1.5 pb-2", children: /* @__PURE__ */ jsx("div", { style: { height: totalHeight, position: "relative" }, children: /* @__PURE__ */ jsx(
      "div",
      {
        style: {
          position: "absolute",
          top: offsetY,
          left: 0,
          right: 0
        },
        children: /* @__PURE__ */ jsx("div", { className: "space-y-1", children: visibleItems.map((item) => {
          if (item.type === "section") {
            const section = sectionGroups[item.sectionIndex];
            if (!section) {
              return null;
            }
            const isCollapsed = effectiveCollapsed[section.id] ?? false;
            return /* @__PURE__ */ jsx(
              SectionHeader,
              {
                sectionId: section.id,
                label: section.label,
                slideCount: section.slideIndexes.length,
                isCollapsed,
                isRenaming: renamingSectionId === section.id,
                renameValue,
                canEdit,
                sectionIndex: item.sectionIndex,
                totalSections: sectionGroups.length,
                renameInputRef,
                onToggle: toggleSection,
                onContextMenu: handleSectionContextMenu,
                onStartRename: startRename,
                onRenameValueChange: setRenameValue,
                onCommitRename: commitRename,
                onCancelRename: cancelRename
              },
              `section-${section.id}`
            );
          }
          const slide = slides[item.slideIndex];
          if (!slide) {
            return null;
          }
          return /* @__PURE__ */ jsx(
            SlideItem,
            {
              slide,
              templateElements: templateElementsBySlideId[slide.id] ?? EMPTY_TEMPLATE_ELEMENTS,
              slideIndex: item.slideIndex,
              isActive: item.slideIndex === activeSlideIndex,
              canvasSize,
              canEdit,
              rehearsalTimings,
              presenceUsers: slidePresenceMap?.get(item.slideIndex),
              fieldContext,
              tableStyleContext,
              onSelectSlide,
              onSlideContextMenu,
              onAddSection,
              onOpenSlideCtxMenu: handleOpenSlideCtxMenu,
              onDragStart: handleDragStart,
              onDragOver: handleDragOver,
              onDrop: handleDrop,
              slideRef: setSlideRef(item.slideIndex)
            },
            slide.id ?? item.slideIndex
          );
        }) })
      }
    ) }) });
  };
  const renderNonVirtualized = () => /* @__PURE__ */ jsx("div", { className: "flex-1 space-y-1 overflow-y-auto px-1.5 pb-2", children: sectionGroups.map((section, sectionIndex) => {
    const isCollapsed = effectiveCollapsed[section.id] ?? false;
    return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
      showSectionHeaders && /* @__PURE__ */ jsx(
        SectionHeader,
        {
          sectionId: section.id,
          label: section.label,
          slideCount: section.slideIndexes.length,
          isCollapsed,
          isRenaming: renamingSectionId === section.id,
          renameValue,
          canEdit,
          sectionIndex,
          totalSections: sectionGroups.length,
          renameInputRef,
          onToggle: toggleSection,
          onContextMenu: handleSectionContextMenu,
          onStartRename: startRename,
          onRenameValueChange: setRenameValue,
          onCommitRename: commitRename,
          onCancelRename: cancelRename
        }
      ),
      !isCollapsed && section.slideIndexes.map((idx) => {
        const slide = slides[idx];
        if (!slide) {
          return null;
        }
        return /* @__PURE__ */ jsx(
          SlideItem,
          {
            slide,
            templateElements: templateElementsBySlideId[slide.id] ?? EMPTY_TEMPLATE_ELEMENTS,
            slideIndex: idx,
            isActive: idx === activeSlideIndex,
            canvasSize,
            canEdit,
            rehearsalTimings,
            presenceUsers: slidePresenceMap?.get(idx),
            fieldContext,
            tableStyleContext,
            onSelectSlide,
            onSlideContextMenu,
            onAddSection,
            onOpenSlideCtxMenu: handleOpenSlideCtxMenu,
            onDragStart: handleDragStart,
            onDragOver: handleDragOver,
            onDrop: handleDrop,
            slideRef: setSlideRef(idx)
          },
          slide.id ?? idx
        );
      })
    ] }, section.id);
  }) });
  return /* @__PURE__ */ jsxs(
    "aside",
    {
      role: "navigation",
      "aria-label": t("pptx.sections.slides"),
      className: "flex h-full flex-col border-r border-border bg-secondary/30",
      style: panelWidth ? { width: panelWidth, flexShrink: 0 } : void 0,
      children: [
        shouldVirtualize ? renderVirtualized() : renderNonVirtualized(),
        canEdit && /* @__PURE__ */ jsx("div", { className: "border-t border-border/60 px-2 py-1.5", children: /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            className: "flex w-full items-center justify-center gap-1 rounded-sm px-2 py-1 text-[11px] text-muted-foreground hover:bg-accent hover:text-foreground transition-colors disabled:cursor-not-allowed disabled:opacity-40",
            disabled: !canEdit,
            onClick: onAddSlide,
            children: [
              /* @__PURE__ */ jsx(LuPlus, { className: "h-3 w-3" }),
              t("pptx.sections.addSlide")
            ]
          }
        ) }),
        sectionContextMenu && /* @__PURE__ */ jsx(
          SectionContextMenu,
          {
            state: sectionContextMenu,
            sectionGroups,
            totalSlides: slides.length,
            onStartRename: startRename,
            onDeleteSection,
            onMoveSectionUp,
            onMoveSectionDown,
            onAddSection,
            onClose: closeSectionContextMenu
          }
        ),
        slideCtxMenu && /* @__PURE__ */ jsx(
          SlideContextMenu,
          {
            state: slideCtxMenu,
            onAddSection,
            onClose: closeSlideCtxMenu
          }
        )
      ]
    }
  );
}
var HANDOUT_SLIDES_PER_PAGE_OPTIONS = [1, 2, 3, 4, 6, 9];
function HandoutMasterPanel({
  handoutMaster,
  slidesPerPage,
  onSlidesPerPageChange,
  onBackgroundChange
}) {
  const { t } = useTranslation();
  if (!handoutMaster) {
    return /* @__PURE__ */ jsx("div", { className: "px-2 py-4 text-center text-xs text-muted-foreground", children: t("pptx.master.noHandoutMaster") });
  }
  return /* @__PURE__ */ jsxs("div", { className: "space-y-2 px-1", children: [
    /* @__PURE__ */ jsxs("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1.5", children: t("pptx.master.handoutSlidesPerPage") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-3 gap-1", children: HANDOUT_SLIDES_PER_PAGE_OPTIONS.map((count) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          "aria-pressed": slidesPerPage === count,
          className: cn(
            "px-2 py-1.5 rounded text-[11px] font-medium transition-colors",
            slidesPerPage === count ? "bg-primary text-white" : "bg-accent/50 text-muted-foreground hover:bg-accent/70 hover:text-foreground"
          ),
          onClick: () => onSlidesPerPageChange(count),
          children: count
        },
        count
      )) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.master.handoutBackground") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          "aria-label": t("pptx.master.backgroundColorLabel"),
          className: "w-full h-8 rounded border border-border",
          value: handoutMaster.backgroundColor ?? "#ffffff",
          onChange: (event) => onBackgroundChange(event.target.value)
        }
      )
    ] }),
    handoutMaster.placeholders && handoutMaster.placeholders.length > 0 && /* @__PURE__ */ jsxs("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1.5", children: t("pptx.master.handoutPlaceholders") }),
      /* @__PURE__ */ jsx("div", { className: "space-y-1", children: handoutMaster.placeholders.map((ph) => /* @__PURE__ */ jsxs(
        "div",
        {
          className: "flex items-center gap-2 rounded px-1.5 py-1 bg-background/50 text-[10px] text-foreground",
          children: [
            /* @__PURE__ */ jsx("span", { className: "w-2 h-2 rounded-full bg-purple-500/60 flex-shrink-0" }),
            ph.type
          ]
        },
        `${ph.type}-${ph.idx ?? "default"}`
      )) })
    ] })
  ] });
}
function NotesMasterPanel({
  notesMaster,
  onBackgroundChange
}) {
  const { t } = useTranslation();
  const placeholderLabels = useMemo(() => {
    if (!notesMaster?.placeholders) {
      return [];
    }
    return notesMaster.placeholders.map((ph) => {
      const typeLabel = ph.type === "body" ? t("pptx.master.notesMasterBody") : ph.type === "sldImg" ? t("pptx.master.notesMasterSlideImage") : ph.type === "hdr" ? t("pptx.master.notesMasterHeader") : ph.type === "ftr" ? t("pptx.master.notesMasterFooter") : ph.type === "dt" ? t("pptx.master.notesMasterDate") : ph.type === "sldNum" ? t("pptx.master.notesMasterPageNumber") : ph.type;
      return { type: ph.type, label: typeLabel, idx: ph.idx };
    });
  }, [notesMaster?.placeholders, t]);
  if (!notesMaster) {
    return /* @__PURE__ */ jsx("div", { className: "px-2 py-4 text-center text-xs text-muted-foreground", children: t("pptx.master.noNotesMaster") });
  }
  return /* @__PURE__ */ jsxs("div", { className: "space-y-2 px-1", children: [
    /* @__PURE__ */ jsxs("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.master.notesMasterBackground") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          "aria-label": t("pptx.master.backgroundColorLabel"),
          className: "w-full h-8 rounded border border-border",
          value: notesMaster.backgroundColor ?? "#ffffff",
          onChange: (event) => onBackgroundChange(event.target.value)
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1.5", children: t("pptx.master.notesMasterPlaceholders") }),
      placeholderLabels.length > 0 ? /* @__PURE__ */ jsx("div", { className: "space-y-1", children: placeholderLabels.map((ph) => /* @__PURE__ */ jsxs(
        "div",
        {
          className: "flex items-center gap-2 rounded px-1.5 py-1 bg-background/50 text-[10px] text-foreground",
          children: [
            /* @__PURE__ */ jsx("span", { className: "w-2 h-2 rounded-full bg-green-500/60 flex-shrink-0" }),
            ph.label
          ]
        },
        `${ph.type}-${ph.idx ?? "default"}`
      )) }) : /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.master.noPlaceholders") })
    ] })
  ] });
}
function partToSlide(master, layoutIndex) {
  return masterViewPseudoSlide(
    { slideMasters: [master] },
    { tab: "slides", masterIndex: 0, layoutIndex }
  );
}
var EMPTY_SLIDE = { id: "", rId: "", slideNumber: 0, elements: [] };
function SlideMastersList({
  slideMasters,
  activeMasterIndex,
  activeLayoutIndex,
  canvasSize,
  onSelectMaster,
  onSelectLayout
}) {
  const activeRef = useRef(null);
  useEffect(() => {
    activeRef.current?.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }, [activeMasterIndex, activeLayoutIndex]);
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    slideMasters.map((master, masterIdx) => {
      const isMasterActive = masterIdx === activeMasterIndex && activeLayoutIndex === null;
      const layouts = master.layouts ?? [];
      return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            ref: isMasterActive ? activeRef : void 0,
            "aria-pressed": isMasterActive,
            "aria-label": master.name || t("pptx.master.master"),
            className: cn(
              "group relative block w-full cursor-pointer rounded-lg border-2 p-1 text-left transition-all",
              isMasterActive ? "border-amber-500 bg-amber-500/10" : "border-border bg-background/40 hover:border-border"
            ),
            onClick: () => onSelectMaster(masterIdx),
            children: [
              /* @__PURE__ */ jsx("div", { className: "relative overflow-hidden rounded bg-white", children: /* @__PURE__ */ jsx(
                SlideThumbnail,
                {
                  slide: partToSlide(master, null) ?? EMPTY_SLIDE,
                  templateElements: [],
                  canvasSize
                }
              ) }),
              /* @__PURE__ */ jsx("div", { className: "mt-1 px-1", children: /* @__PURE__ */ jsx(
                "span",
                {
                  className: cn(
                    "text-[10px] font-medium",
                    isMasterActive ? "text-amber-400" : "text-muted-foreground"
                  ),
                  children: master.name || t("pptx.master.master")
                }
              ) })
            ]
          }
        ),
        layouts.length > 0 && /* @__PURE__ */ jsx("div", { className: "ml-3 space-y-1 border-l border-border/40 pl-2", children: layouts.map((layout, layoutIdx) => {
          const isLayoutActive = masterIdx === activeMasterIndex && layoutIdx === activeLayoutIndex;
          return /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              ref: isLayoutActive ? activeRef : void 0,
              "aria-pressed": isLayoutActive,
              "aria-label": layout.name || t("pptx.master.layout"),
              className: cn(
                "group relative block w-full cursor-pointer rounded-md border-2 p-0.5 text-left transition-all",
                isLayoutActive ? "border-primary bg-primary/10" : "border-border bg-background/40 hover:border-border"
              ),
              onClick: () => onSelectLayout(masterIdx, layoutIdx),
              children: [
                /* @__PURE__ */ jsx("div", { className: "relative overflow-hidden rounded bg-white", children: /* @__PURE__ */ jsx(
                  SlideThumbnail,
                  {
                    slide: partToSlide(master, layoutIdx) ?? EMPTY_SLIDE,
                    templateElements: [],
                    canvasSize
                  }
                ) }),
                /* @__PURE__ */ jsx("div", { className: "mt-0.5 px-0.5", children: /* @__PURE__ */ jsx(
                  "span",
                  {
                    className: cn(
                      "text-[9px]",
                      isLayoutActive ? "text-primary" : "text-muted-foreground"
                    ),
                    children: layout.name || t("pptx.master.layout")
                  }
                ) })
              ]
            },
            layout.path
          );
        }) })
      ] }, master.path);
    }),
    slideMasters.length === 0 && /* @__PURE__ */ jsx("div", { className: "px-2 py-4 text-center text-xs text-muted-foreground", children: t("pptx.master.noMasters") })
  ] });
}
var TABS = [
  { key: "slides", labelKey: "pptx.sections.slides" },
  { key: "notes", labelKey: "pptx.notes.title" },
  { key: "handout", labelKey: "pptx.masterView.tabHandout" }
];
function MasterViewSidebar({
  slideMasters,
  activeMasterIndex,
  activeLayoutIndex,
  canvasSize,
  masterViewTab,
  notesMaster,
  handoutMaster,
  handoutSlidesPerPage,
  canEdit,
  onSelectMaster,
  onSelectLayout,
  onCollapse,
  onTabChange,
  crudActions,
  onCrudAction,
  onHandoutSlidesPerPageChange,
  onNotesMasterBackgroundChange,
  onHandoutMasterBackgroundChange,
  onSlidesBackgroundChange
}) {
  const scrollRef = useRef(null);
  const { t } = useTranslation();
  const slidesBackground = masterViewBackgroundColor(
    { slideMasters },
    { tab: "slides", masterIndex: activeMasterIndex, layoutIndex: activeLayoutIndex }
  );
  const handleMasterClick = useCallback(
    (index) => {
      onSelectMaster(index);
    },
    [onSelectMaster]
  );
  const handleLayoutClick = useCallback(
    (masterIdx, layoutIdx) => {
      onSelectLayout(masterIdx, layoutIdx);
    },
    [onSelectLayout]
  );
  return /* @__PURE__ */ jsxs(
    "aside",
    {
      "aria-label": t("pptx.view.masterViews"),
      className: "flex h-full flex-col border-r border-border/80 bg-background/70 backdrop-blur-sm w-56",
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-3 py-2", children: [
          /* @__PURE__ */ jsx("span", { className: "text-xs uppercase tracking-wide text-muted-foreground", children: masterViewTab === "slides" ? t("pptx.master.title") : masterViewTab === "notes" ? t("pptx.master.notesMasterTitle") : t("pptx.master.handoutMasterTitle") }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "rounded p-1 text-muted-foreground hover:bg-muted hover:text-foreground",
              title: t("pptx.master.collapseMasterPane"),
              onClick: onCollapse,
              children: /* @__PURE__ */ jsx(LuPanelLeftClose, { className: "h-3.5 w-3.5" })
            }
          )
        ] }),
        /* @__PURE__ */ jsx(
          "div",
          {
            role: "tablist",
            "aria-label": t("pptx.mode.masterView"),
            className: "flex border-b border-border/60 px-1",
            children: TABS.map((tab) => /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                role: "tab",
                "aria-selected": masterViewTab === tab.key,
                className: cn(
                  "flex-1 px-1 py-1.5 text-[10px] font-medium transition-colors border-b-2",
                  masterViewTab === tab.key ? "border-amber-500 text-amber-400" : "border-transparent text-muted-foreground hover:text-foreground"
                ),
                onClick: () => onTabChange(tab.key),
                children: t(tab.labelKey)
              },
              tab.key
            ))
          }
        ),
        /* @__PURE__ */ jsxs("div", { ref: scrollRef, className: "flex-1 space-y-2 overflow-y-auto px-1.5 pb-2 pt-1", children: [
          masterViewTab === "slides" && canEdit && /* @__PURE__ */ jsxs("section", { className: "space-y-1.5 rounded-md border border-border/60 p-2", children: [
            /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: t("pptx.master.notesMasterBackground") }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "color",
                "aria-label": t("pptx.master.backgroundColorLabel"),
                className: "h-7 w-full cursor-pointer rounded border border-border/60 bg-transparent",
                value: slidesBackground ?? "#ffffff",
                onChange: (event) => onSlidesBackgroundChange(event.target.value)
              }
            )
          ] }),
          masterViewTab === "slides" && canEdit && crudActions.length > 0 && /* @__PURE__ */ jsx("section", { className: "grid grid-cols-2 gap-1 rounded-md border border-border/60 p-2", children: crudActions.map((action) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "data-testid": `pptx-master-crud-${action.id}`,
              disabled: !action.enabled,
              title: action.disabledReasonKey ? t(action.disabledReasonKey) : void 0,
              onClick: () => onCrudAction(action.id),
              className: "rounded border border-border/60 px-1.5 py-1 text-[10px] hover:bg-accent/60 disabled:cursor-not-allowed disabled:opacity-40",
              children: t(action.labelKey)
            },
            action.id
          )) }),
          masterViewTab === "slides" && /* @__PURE__ */ jsx(
            SlideMastersList,
            {
              slideMasters,
              activeMasterIndex,
              activeLayoutIndex,
              canvasSize,
              onSelectMaster: handleMasterClick,
              onSelectLayout: handleLayoutClick
            }
          ),
          masterViewTab === "notes" && /* @__PURE__ */ jsx(
            NotesMasterPanel,
            {
              notesMaster,
              onBackgroundChange: onNotesMasterBackgroundChange
            }
          ),
          masterViewTab === "handout" && /* @__PURE__ */ jsx(
            HandoutMasterPanel,
            {
              handoutMaster,
              slidesPerPage: handoutSlidesPerPage,
              onSlidesPerPageChange: onHandoutSlidesPerPageChange,
              onBackgroundChange: onHandoutMasterBackgroundChange
            }
          )
        ] })
      ]
    }
  );
}
var DEFAULT_NOTES_WIDTH = 720;
var DEFAULT_NOTES_HEIGHT = 960;
var PLACEHOLDER_LABEL_KEYS = {
  sldImg: "pptx.master.notesMasterSlideImage",
  body: "pptx.master.notesMasterBody",
  hdr: "pptx.master.notesMasterHeader",
  ftr: "pptx.master.notesMasterFooter",
  dt: "pptx.master.notesMasterDate",
  sldNum: "pptx.master.notesMasterPageNumber"
};
var DEFAULT_POSITIONS = {
  sldImg: { x: 0.1, y: 0.05, w: 0.8, h: 0.4 },
  body: { x: 0.1, y: 0.5, w: 0.8, h: 0.4 },
  hdr: { x: 0, y: 0, w: 0.4, h: 0.04 },
  ftr: { x: 0, y: 0.96, w: 0.4, h: 0.04 },
  dt: { x: 0.6, y: 0, w: 0.4, h: 0.04 },
  sldNum: { x: 0.6, y: 0.96, w: 0.4, h: 0.04 }
};
function elementText(element) {
  if ("textSegments" in element && element.textSegments?.length) {
    return element.textSegments.map((segment) => segment.text).join("");
  }
  return "text" in element ? element.text ?? "" : "";
}
function NotesMasterCanvas({
  notesMaster,
  canvasSize,
  notesCanvasSize,
  slideThumbnail,
  notesText,
  slideNumber
}) {
  const { t } = useTranslation();
  const pageWidth = notesCanvasSize?.width ?? DEFAULT_NOTES_WIDTH;
  const pageHeight = notesCanvasSize?.height ?? DEFAULT_NOTES_HEIGHT;
  const scale = useMemo(() => {
    const scaleX = canvasSize.width / pageWidth;
    const scaleY = canvasSize.height / pageHeight;
    return Math.min(scaleX, scaleY, 1) * 0.85;
  }, [canvasSize.width, canvasSize.height, pageWidth, pageHeight]);
  const scaledWidth = pageWidth * scale;
  const scaledHeight = pageHeight * scale;
  const bodyFontSize = useMemo(
    () => resolveNotesSchematicBodyFontSizePx(notesMaster?.notesStyle, scale),
    [notesMaster?.notesStyle, scale]
  );
  const placeholders = useMemo(() => {
    if (!notesMaster?.placeholders) {
      return [
        { type: "sldImg", idx: void 0 },
        { type: "body", idx: void 0 },
        { type: "hdr", idx: void 0 },
        { type: "ftr", idx: void 0 },
        { type: "dt", idx: void 0 },
        { type: "sldNum", idx: void 0 }
      ];
    }
    return notesMaster.placeholders;
  }, [notesMaster?.placeholders]);
  if (!notesMaster) {
    return /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-full text-muted-foreground text-sm", children: t("pptx.master.noNotesMaster") });
  }
  return /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-full w-full", children: /* @__PURE__ */ jsxs(
    "div",
    {
      className: "relative bg-white shadow-lg border border-border rounded",
      style: { width: scaledWidth, height: scaledHeight },
      children: [
        notesMaster.backgroundColor && /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute inset-0 rounded",
            style: { backgroundColor: notesMaster.backgroundColor }
          }
        ),
        placeholders.map((ph) => {
          const pos = DEFAULT_POSITIONS[ph.type];
          if (!pos) {
            return null;
          }
          const labelKey = PLACEHOLDER_LABEL_KEYS[ph.type] ?? ph.type;
          const isSlideArea = ph.type === "sldImg";
          const isBody = ph.type === "body";
          const isPageNum = ph.type === "sldNum";
          if (isSlideArea && slideThumbnail) {
            return /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute overflow-hidden border border-solid border-gray-300",
                style: {
                  left: pos.x * scaledWidth,
                  top: pos.y * scaledHeight,
                  width: pos.w * scaledWidth,
                  height: pos.h * scaledHeight
                },
                children: /* @__PURE__ */ jsx(
                  "img",
                  {
                    src: slideThumbnail,
                    alt: slideNumber ? `Slide ${slideNumber}` : "Slide",
                    className: "w-full h-full object-contain"
                  }
                )
              },
              `${ph.type}-${ph.idx ?? "default"}`
            );
          }
          if (isBody && notesText !== void 0) {
            return /* @__PURE__ */ jsx(
              "div",
              {
                "data-region": ph.type,
                className: "absolute overflow-auto border border-solid border-gray-200 p-2",
                style: {
                  left: pos.x * scaledWidth,
                  top: pos.y * scaledHeight,
                  width: pos.w * scaledWidth,
                  height: pos.h * scaledHeight,
                  fontSize: bodyFontSize,
                  lineHeight: 1.4,
                  color: "#374151",
                  whiteSpace: "pre-wrap"
                },
                children: notesText || t("pptx.notes.noNotes")
              },
              `${ph.type}-${ph.idx ?? "default"}`
            );
          }
          if (isPageNum && slideNumber !== void 0) {
            return /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute flex items-center justify-center",
                style: {
                  left: pos.x * scaledWidth,
                  top: pos.y * scaledHeight,
                  width: pos.w * scaledWidth,
                  height: pos.h * scaledHeight,
                  fontSize: Math.max(8, Math.min(12, scaledWidth * 0.018)),
                  color: "rgba(156, 163, 175, 0.6)"
                },
                children: slideNumber
              },
              `${ph.type}-${ph.idx ?? "default"}`
            );
          }
          return /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute flex items-center justify-center border border-dashed",
              style: {
                left: pos.x * scaledWidth,
                top: pos.y * scaledHeight,
                width: pos.w * scaledWidth,
                height: pos.h * scaledHeight,
                borderColor: isSlideArea ? "rgba(59, 130, 246, 0.5)" : isBody ? "rgba(34, 197, 94, 0.5)" : "rgba(156, 163, 175, 0.4)",
                backgroundColor: isSlideArea ? "rgba(59, 130, 246, 0.05)" : isBody ? "rgba(34, 197, 94, 0.05)" : "transparent"
              },
              children: /* @__PURE__ */ jsx(
                "span",
                {
                  className: "text-center px-1",
                  style: {
                    fontSize: Math.max(8, Math.min(12, scaledWidth * 0.018)),
                    color: isSlideArea ? "rgba(59, 130, 246, 0.7)" : isBody ? "rgba(34, 197, 94, 0.7)" : "rgba(156, 163, 175, 0.6)"
                  },
                  children: t(labelKey)
                }
              )
            },
            `${ph.type}-${ph.idx ?? "default"}`
          );
        }),
        notesMaster.elements?.map((element) => /* @__PURE__ */ jsx(
          "div",
          {
            "data-pptx-element": "true",
            "data-element-id": element.id,
            className: "absolute overflow-hidden",
            style: {
              left: element.x * scale,
              top: element.y * scale,
              width: element.width * scale,
              height: element.height * scale,
              zIndex: 2
            },
            children: elementText(element)
          },
          element.id
        ))
      ]
    }
  ) });
}
var PAGE_WIDTH = 720;
var PAGE_HEIGHT = 960;
var MARGIN = 0.06;
function elementText2(element) {
  if ("textSegments" in element && element.textSegments?.length) {
    return element.textSegments.map((segment) => segment.text).join("");
  }
  return "text" in element ? element.text ?? "" : "";
}
function computeSlotLayout(slidesPerPage, slideAspect) {
  const mx = MARGIN;
  const my = MARGIN;
  const contentW = 1 - 2 * mx;
  const contentH = 1 - 2 * my;
  const GAP = 0.02;
  switch (slidesPerPage) {
    case 1: {
      const w = contentW * 0.8;
      const h = w / slideAspect;
      return [{ x: mx + (contentW - w) / 2, y: my + (contentH - h) / 2, w, h }];
    }
    case 2: {
      const w = contentW * 0.75;
      const h = w / slideAspect;
      const totalH = h * 2 + GAP;
      const startY = my + (contentH - totalH) / 2;
      return [0, 1].map((i) => ({
        x: mx + (contentW - w) / 2,
        y: startY + i * (h + GAP),
        w,
        h
      }));
    }
    case 3: {
      const w = contentW * 0.5;
      const h = w / slideAspect;
      const totalH = h * 3 + GAP * 2;
      const startY = my + (contentH - totalH) / 2;
      return [0, 1, 2].map((i) => ({
        x: mx,
        y: startY + i * (h + GAP),
        w,
        h
      }));
    }
    case 4: {
      const cols = 2;
      const rows = 2;
      const w = (contentW - GAP) / cols;
      const h = w / slideAspect;
      const totalH = h * rows + GAP * (rows - 1);
      const startY = my + (contentH - totalH) / 2;
      return Array.from({ length: 4 }, (_, i) => ({
        x: mx + i % cols * (w + GAP),
        y: startY + Math.floor(i / cols) * (h + GAP),
        w,
        h
      }));
    }
    case 6: {
      const cols = 2;
      const rows = 3;
      const w = (contentW - GAP) / cols;
      const h = w / slideAspect;
      const totalH = h * rows + GAP * (rows - 1);
      const startY = my + (contentH - totalH) / 2;
      return Array.from({ length: 6 }, (_, i) => ({
        x: mx + i % cols * (w + GAP),
        y: startY + Math.floor(i / cols) * (h + GAP),
        w,
        h
      }));
    }
    case 9: {
      const cols = 3;
      const rows = 3;
      const w = (contentW - GAP * 2) / cols;
      const h = w / slideAspect;
      const totalH = h * rows + GAP * (rows - 1);
      const startY = my + (contentH - totalH) / 2;
      return Array.from({ length: 9 }, (_, i) => ({
        x: mx + i % cols * (w + GAP),
        y: startY + Math.floor(i / cols) * (h + GAP),
        w,
        h
      }));
    }
    default:
      return [];
  }
}
function HandoutMasterCanvas({
  handoutMaster,
  canvasSize,
  slidesPerPage,
  slideThumbnails,
  pageNumber
}) {
  const { t } = useTranslation();
  const slideAspect = 4 / 3;
  const scale = useMemo(() => {
    const scaleX = canvasSize.width / PAGE_WIDTH;
    const scaleY = canvasSize.height / PAGE_HEIGHT;
    return Math.min(scaleX, scaleY, 1) * 0.85;
  }, [canvasSize.width, canvasSize.height]);
  const scaledWidth = PAGE_WIDTH * scale;
  const scaledHeight = PAGE_HEIGHT * scale;
  const slots = useMemo(
    () => computeSlotLayout(slidesPerPage, slideAspect),
    [slidesPerPage, slideAspect]
  );
  if (!handoutMaster) {
    return /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-full text-muted-foreground text-sm", children: t("pptx.master.noHandoutMaster") });
  }
  return /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-full w-full", children: /* @__PURE__ */ jsxs(
    "div",
    {
      className: "relative bg-white shadow-lg border border-gray-300 rounded",
      style: { width: scaledWidth, height: scaledHeight },
      children: [
        handoutMaster.backgroundColor && /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute inset-0 rounded",
            style: { backgroundColor: handoutMaster.backgroundColor }
          }
        ),
        slots.map((slot, i) => {
          const thumbnail = slideThumbnails?.[i];
          return /* @__PURE__ */ jsx(
            "div",
            {
              className: `absolute flex items-center justify-center overflow-hidden ${thumbnail ? "border border-solid border-gray-300" : "border border-dashed border-blue-400/50 bg-blue-50/30"}`,
              style: {
                left: slot.x * scaledWidth,
                top: slot.y * scaledHeight,
                width: slot.w * scaledWidth,
                height: slot.h * scaledHeight
              },
              children: thumbnail ? /* @__PURE__ */ jsx(
                "img",
                {
                  src: thumbnail,
                  alt: `Slide ${(pageNumber ? (pageNumber - 1) * slidesPerPage : 0) + i + 1}`,
                  className: "w-full h-full object-contain"
                }
              ) : /* @__PURE__ */ jsx(
                "span",
                {
                  className: "text-blue-400/60 font-medium",
                  style: {
                    fontSize: Math.max(8, Math.min(14, slot.w * scaledWidth * 0.08))
                  },
                  children: t("pptx.master.handoutSlideSlot", { number: i + 1 })
                }
              )
            },
            i
          );
        }),
        handoutMaster.elements?.map((element) => /* @__PURE__ */ jsx(
          "div",
          {
            "data-pptx-element": "true",
            "data-element-id": element.id,
            className: "absolute overflow-hidden",
            style: {
              left: element.x * scale,
              top: element.y * scale,
              width: element.width * scale,
              height: element.height * scale,
              zIndex: 2
            },
            children: elementText2(element)
          },
          element.id
        )),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute left-0 top-0 px-1 text-gray-400/50 border-b border-r border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: t("pptx.master.notesMasterHeader")
          }
        ),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute right-0 top-0 px-1 text-gray-400/50 border-b border-l border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: t("pptx.master.notesMasterDate")
          }
        ),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute left-0 bottom-0 px-1 text-gray-400/50 border-t border-r border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: t("pptx.master.notesMasterFooter")
          }
        ),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute right-0 bottom-0 px-1 text-gray-400/50 border-t border-l border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: pageNumber !== void 0 ? String(pageNumber) : t("pptx.master.notesMasterPageNumber")
          }
        )
      ]
    }
  ) });
}
function SectionBlock({
  section,
  slides,
  gridCols,
  zoomScale: _zoomScale,
  renderSlideCard
}) {
  const [collapsed, setCollapsed] = useState(section.defaultCollapsed ?? false);
  return /* @__PURE__ */ jsxs("div", { className: "mb-4", children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "flex w-full items-center gap-1.5 rounded px-2 py-1.5 text-[11px] uppercase tracking-wide text-muted-foreground hover:bg-muted/60 hover:text-foreground mb-2",
        onClick: () => setCollapsed((p) => !p),
        children: [
          section.color && /* @__PURE__ */ jsx(
            "span",
            {
              className: "inline-block h-2.5 w-2.5 shrink-0 rounded-full",
              style: { backgroundColor: section.color }
            }
          ),
          /* @__PURE__ */ jsx(
            LuChevronDown,
            {
              className: cn("h-3 w-3 transition-transform", collapsed ? "-rotate-90" : "rotate-0")
            }
          ),
          /* @__PURE__ */ jsx("span", { className: "truncate text-left", children: section.label }),
          /* @__PURE__ */ jsx("span", { className: "ml-auto text-[10px] text-muted-foreground", children: section.slideIndexes.length })
        ]
      }
    ),
    !collapsed && /* @__PURE__ */ jsx(
      "div",
      {
        className: "grid gap-4",
        style: {
          gridTemplateColumns: `repeat(${gridCols}, minmax(0, 1fr))`
        },
        children: section.slideIndexes.map((idx) => {
          const slide = slides[idx];
          if (!slide) {
            return null;
          }
          return renderSlideCard(slide, idx);
        })
      }
    )
  ] });
}
function SlideCardImpl({
  slide,
  index,
  isActive,
  isDragTarget,
  isSelected,
  selectedCount,
  selectionOrder,
  canvasSize,
  canEdit,
  onSlideClick,
  onDoubleClick,
  onContextMenu,
  onDragStart,
  onDragOver,
  onDragLeave,
  onDrop
}) {
  const { t } = useTranslation();
  const cue = hiddenSlideCue(slide.hidden, "sorter", index);
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "group relative cursor-pointer rounded-lg border-2 p-1 transition-all",
        isDragTarget ? "border-primary bg-primary/20" : isSelected ? "border-primary bg-primary/10 ring-1 ring-primary/50" : isActive ? "border-primary/50 bg-primary/5" : "border-border bg-background/50 hover:border-border",
        slide.hidden && "opacity-40"
      ),
      "data-pptx-slide-hidden": cue.marker,
      "aria-describedby": cue.labelId,
      onClick: (e) => onSlideClick(e, index),
      onDoubleClick: () => onDoubleClick(index),
      onContextMenu: (e) => onContextMenu(e, index),
      draggable: canEdit,
      onDragStart: (e) => onDragStart(e, index),
      onDragOver: (e) => onDragOver(e, index),
      onDragLeave,
      onDrop: (e) => onDrop(e, index),
      children: [
        /* @__PURE__ */ jsx("div", { className: "aspect-video overflow-hidden rounded bg-white", children: /* @__PURE__ */ jsx(SlideThumbnail, { slide, templateElements: [], canvasSize }) }),
        /* @__PURE__ */ jsxs("div", { className: "mt-1 flex items-center justify-between px-0.5", children: [
          /* @__PURE__ */ jsx(
            "span",
            {
              className: cn(
                "text-[11px] font-medium px-0.5",
                isSelected ? "text-primary" : isActive ? "text-primary/70" : "text-muted-foreground"
              ),
              style: cue.hidden ? { backgroundImage: HIDDEN_SLIDE_SLASH_GRADIENT } : void 0,
              children: index + 1
            }
          ),
          cue.hidden && /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1", id: cue.labelId, children: [
            /* @__PURE__ */ jsx(LuEyeOff, { className: "h-3 w-3 text-muted-foreground" }),
            /* @__PURE__ */ jsx("span", { className: "text-[9px] uppercase tracking-wide text-muted-foreground", children: t(HIDDEN_SLIDE_LABEL_KEY) })
          ] })
        ] }),
        isSelected && selectedCount > 1 && /* @__PURE__ */ jsx("div", { className: "absolute top-2 right-2 flex h-5 w-5 items-center justify-center rounded-full bg-primary text-white text-[10px] font-bold", children: selectionOrder })
      ]
    }
  );
}
function arePropsEqual3(prev, next) {
  if (prev.slide.id !== next.slide.id) {
    return false;
  }
  if (prev.slide.isDirty !== next.slide.isDirty) {
    return false;
  }
  if (prev.slide.hidden !== next.slide.hidden) {
    return false;
  }
  if (prev.slide.elements !== next.slide.elements) {
    return false;
  }
  if (prev.index !== next.index) {
    return false;
  }
  if (prev.isActive !== next.isActive) {
    return false;
  }
  if (prev.isDragTarget !== next.isDragTarget) {
    return false;
  }
  if (prev.isSelected !== next.isSelected) {
    return false;
  }
  if (prev.selectedCount !== next.selectedCount) {
    return false;
  }
  if (prev.selectionOrder !== next.selectionOrder) {
    return false;
  }
  if (prev.canEdit !== next.canEdit) {
    return false;
  }
  if (prev.canvasSize.width !== next.canvasSize.width || prev.canvasSize.height !== next.canvasSize.height) {
    return false;
  }
  if (prev.onSlideClick !== next.onSlideClick || prev.onDoubleClick !== next.onDoubleClick || prev.onContextMenu !== next.onContextMenu || prev.onDragStart !== next.onDragStart || prev.onDragOver !== next.onDragOver || prev.onDragLeave !== next.onDragLeave || prev.onDrop !== next.onDrop) {
    return false;
  }
  return true;
}
var SlideCard = React20.memo(SlideCardImpl, arePropsEqual3);
function SorterContextMenu({
  x,
  y,
  selectedCount,
  hasClipboard,
  hasHiddenInSelection,
  hasVisibleInSelection,
  sectionGroups: _sectionGroups,
  onDelete,
  onDuplicate,
  onCopy,
  onPaste,
  onToggleHide,
  onClose
}) {
  const { t } = useTranslation();
  const menuX = Math.max(8, Math.min(x, window.innerWidth - 220));
  const menuY = Math.max(8, Math.min(y, window.innerHeight - 300));
  const countLabel = selectedCount > 1 ? ` (${selectedCount})` : "";
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "div",
      {
        className: "fixed inset-0 z-[119]",
        onClick: onClose,
        onContextMenu: (e) => {
          e.preventDefault();
          onClose();
        }
      }
    ),
    /* @__PURE__ */ jsxs(
      "div",
      {
        className: "fixed z-[120] min-w-[200px] rounded border border-border bg-popover shadow-2xl py-1.5 text-xs text-foreground",
        style: { left: menuX, top: menuY },
        children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onCopy,
              children: [
                /* @__PURE__ */ jsx(LuCopy, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.copy"),
                countLabel
              ]
            }
          ),
          hasClipboard && /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onPaste,
              children: [
                /* @__PURE__ */ jsx(LuClipboardPaste, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.paste")
              ]
            }
          ),
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onDuplicate,
              children: [
                /* @__PURE__ */ jsx(LuCopyPlus, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.duplicate"),
                countLabel
              ]
            }
          ),
          /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border" }),
          hasVisibleInSelection && /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onToggleHide,
              children: [
                /* @__PURE__ */ jsx(LuEyeOff, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.hideSlides"),
                countLabel
              ]
            }
          ),
          hasHiddenInSelection && /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onToggleHide,
              children: [
                /* @__PURE__ */ jsx(LuEye, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.showSlides"),
                countLabel
              ]
            }
          ),
          /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border" }),
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left text-red-300 hover:bg-red-900/40",
              onClick: onDelete,
              children: [
                /* @__PURE__ */ jsx(LuTrash2, { className: "h-3.5 w-3.5" }),
                t("pptx.slideSorter.contextMenu.delete"),
                countLabel
              ]
            }
          )
        ]
      }
    )
  ] });
}

// src/viewer/components/slide-sorter/types.ts
var MIN_ZOOM = SORTER_MIN_ZOOM;
var MAX_ZOOM = SORTER_MAX_ZOOM;
var DEFAULT_ZOOM = 100;
var ZOOM_STEP = SORTER_ZOOM_STEP;
function useKeyboardShortcuts(params) {
  const paramsRef = useRef(params);
  paramsRef.current = params;
  useEffect(() => {
    const handleKeyDown = (e) => {
      const current = paramsRef.current;
      if (current.contextMenu) {
        current.setContextMenu(null);
      }
      const { action } = mapSlideSorterKey(e, {
        canEdit: current.canEdit,
        hasMultiSelection: current.selectedSlideIds.length > 1,
        isTextInputTarget: isEditorTextInputTarget(e.target)
      });
      if (action === null) {
        return;
      }
      switch (action) {
        case "collapseSelection": {
          e.stopPropagation();
          const activeSlide = current.slides[current.activeSlideIndex];
          current.setSelectedSlideIds(activeSlide?.id ? [activeSlide.id] : []);
          return;
        }
        case "close":
          e.stopPropagation();
          current.onClose();
          return;
        case "delete":
          e.preventDefault();
          if (current.selectedIndexes.length > 0) {
            current.handleDeleteSelected();
          }
          return;
        case "copy":
          e.preventDefault();
          current.handleCopySelected();
          return;
        case "paste":
          e.preventDefault();
          current.handlePaste();
          return;
        case "duplicate":
          e.preventDefault();
          current.handleDuplicateSelected();
          return;
        case "selectAll":
          e.preventDefault();
          current.handleSelectAll();
          return;
        case "zoomIn":
          e.preventDefault();
          current.setZoom((z) => clampSorterZoom(z + ZOOM_STEP));
          return;
        case "zoomOut":
          e.preventDefault();
          current.setZoom((z) => clampSorterZoom(z - ZOOM_STEP));
          break;
      }
    };
    window.addEventListener("keydown", handleKeyDown);
    return () => window.removeEventListener("keydown", handleKeyDown);
  }, []);
}

// src/viewer/components/slide-sorter/useSlideSorterState.ts
function useSlideSorterState(params) {
  const {
    slides,
    activeSlideIndex,
    canEdit,
    sectionGroups,
    onSelectSlide,
    onMoveSlide,
    onDeleteSlides,
    onDuplicateSlides,
    onToggleHideSlides,
    onClose
  } = params;
  const [dragOverIndex, setDragOverIndex] = useState(null);
  const [selectedSlideIds, setSelectedSlideIds] = useState(() => {
    const activeSlide = slides[activeSlideIndex];
    return activeSlide?.id ? [activeSlide.id] : [];
  });
  const [lastClickedIndex, setLastClickedIndex] = useState(activeSlideIndex);
  const [contextMenu, setContextMenu] = useState(null);
  const [zoom, setZoom] = useState(DEFAULT_ZOOM);
  const [clipboardSlideIds, setClipboardSlideIds] = useState([]);
  const backdropRef = useRef(null);
  const slideIdToIndex = useMemo(() => {
    const map = /* @__PURE__ */ new Map();
    slides.forEach((s, i) => map.set(s.id, i));
    return map;
  }, [slides]);
  const selectedIndexes = useMemo(
    () => selectedSlideIds.map((id) => slideIdToIndex.get(id)).filter((i) => i !== void 0).sort((a, b) => a - b),
    [selectedSlideIds, slideIdToIndex]
  );
  const isSelected = useCallback(
    (slideId) => selectedSlideIds.includes(slideId),
    [selectedSlideIds]
  );
  const handleSlideClick = useCallback(
    (e, index) => {
      const slide = slides[index];
      if (!slide) {
        return;
      }
      if (e.ctrlKey || e.metaKey) {
        setSelectedSlideIds(
          (prev) => prev.includes(slide.id) ? prev.filter((id) => id !== slide.id) : [...prev, slide.id]
        );
        setLastClickedIndex(index);
      } else if (e.shiftKey) {
        const start = Math.min(lastClickedIndex, index);
        const end = Math.max(lastClickedIndex, index);
        const rangeIds = [];
        for (let i = start; i <= end; i++) {
          if (slides[i]?.id) {
            rangeIds.push(slides[i].id);
          }
        }
        setSelectedSlideIds(rangeIds);
      } else {
        setSelectedSlideIds([slide.id]);
        setLastClickedIndex(index);
      }
    },
    [slides, lastClickedIndex]
  );
  const handleContextMenu = useCallback(
    (e, index) => {
      e.preventDefault();
      e.stopPropagation();
      const slide = slides[index];
      if (!slide) {
        return;
      }
      if (!selectedSlideIds.includes(slide.id)) {
        setSelectedSlideIds([slide.id]);
        setLastClickedIndex(index);
      }
      setContextMenu({ x: e.clientX, y: e.clientY, slideIndex: index });
    },
    [slides, selectedSlideIds]
  );
  const closeContextMenu = useCallback(() => {
    setContextMenu(null);
  }, []);
  const handleDeleteSelected = useCallback(() => {
    if (selectedIndexes.length === 0) {
      return;
    }
    onDeleteSlides(selectedIndexes);
    setSelectedSlideIds([]);
    closeContextMenu();
  }, [selectedIndexes, onDeleteSlides, closeContextMenu]);
  const handleDuplicateSelected = useCallback(() => {
    if (selectedIndexes.length === 0) {
      return;
    }
    onDuplicateSlides(selectedIndexes);
    closeContextMenu();
  }, [selectedIndexes, onDuplicateSlides, closeContextMenu]);
  const handleCopySelected = useCallback(() => {
    setClipboardSlideIds([...selectedSlideIds]);
    closeContextMenu();
  }, [selectedSlideIds, closeContextMenu]);
  const handlePaste = useCallback(() => {
    if (clipboardSlideIds.length === 0) {
      return;
    }
    const indexes = clipboardSlideIds.map((id) => slideIdToIndex.get(id)).filter((i) => i !== void 0).sort((a, b) => a - b);
    if (indexes.length > 0) {
      onDuplicateSlides(indexes);
    }
    closeContextMenu();
  }, [clipboardSlideIds, slideIdToIndex, onDuplicateSlides, closeContextMenu]);
  const handleToggleHideSelected = useCallback(() => {
    if (selectedIndexes.length === 0) {
      return;
    }
    onToggleHideSlides(selectedIndexes);
    closeContextMenu();
  }, [selectedIndexes, onToggleHideSlides, closeContextMenu]);
  const handleSelectAll = useCallback(() => {
    setSelectedSlideIds(slides.map((s) => s.id));
  }, [slides]);
  useKeyboardShortcuts({
    slides,
    activeSlideIndex,
    canEdit,
    selectedSlideIds,
    selectedIndexes,
    contextMenu,
    setContextMenu,
    setSelectedSlideIds,
    setZoom,
    onClose,
    handleDeleteSelected,
    handleCopySelected,
    handlePaste,
    handleDuplicateSelected,
    handleSelectAll
  });
  const handleBackdropClick = useCallback(
    (e) => {
      if (e.target === backdropRef.current) {
        onClose();
      }
    },
    [onClose]
  );
  const handleDragStart = useCallback((e, index) => {
    e.dataTransfer.setData("text/plain", String(index));
    e.dataTransfer.effectAllowed = "move";
  }, []);
  const handleDragOver = useCallback((e, index) => {
    e.preventDefault();
    e.dataTransfer.dropEffect = "move";
    setDragOverIndex(index);
  }, []);
  const handleDragLeave = useCallback(() => setDragOverIndex(null), []);
  const handleDrop = useCallback(
    (e, toIndex) => {
      e.preventDefault();
      setDragOverIndex(null);
      const fromIndex = parseInt(e.dataTransfer.getData("text/plain"), 10);
      if (!isNaN(fromIndex) && fromIndex !== toIndex) {
        onMoveSlide(fromIndex, toIndex);
      }
    },
    [onMoveSlide]
  );
  const handleDoubleClick = useCallback((index) => onSelectSlide(index), [onSelectSlide]);
  const zoomScale = zoom / 100;
  const gridCols = useMemo(() => {
    if (zoomScale >= 1.8) {
      return 2;
    }
    if (zoomScale >= 1.4) {
      return 3;
    }
    if (zoomScale >= 1) {
      return 4;
    }
    if (zoomScale >= 0.7) {
      return 5;
    }
    return 6;
  }, [zoomScale]);
  const showSectionHeaders = sectionGroups.length > 1;
  const hasHiddenInSelection = useMemo(
    () => selectedIndexes.some((i) => slides[i]?.hidden),
    [selectedIndexes, slides]
  );
  const hasVisibleInSelection = useMemo(
    () => selectedIndexes.some((i) => !slides[i]?.hidden),
    [selectedIndexes, slides]
  );
  return {
    dragOverIndex,
    selectedSlideIds,
    contextMenu,
    zoom,
    setZoom,
    clipboardSlideIds,
    backdropRef,
    selectedIndexes,
    isSelected,
    handleSlideClick,
    handleContextMenu,
    closeContextMenu,
    handleDeleteSelected,
    handleDuplicateSelected,
    handleCopySelected,
    handlePaste,
    handleToggleHideSelected,
    handleBackdropClick,
    handleDragStart,
    handleDragOver,
    handleDragLeave,
    handleDrop,
    handleDoubleClick,
    zoomScale,
    gridCols,
    showSectionHeaders,
    hasHiddenInSelection,
    hasVisibleInSelection
  };
}
function SlideSorterOverlay({
  slides,
  activeSlideIndex,
  canvasSize,
  canEdit,
  sectionGroups,
  onSelectSlide,
  onMoveSlide,
  onDeleteSlides,
  onDuplicateSlides,
  onToggleHideSlides,
  onClose
}) {
  const { t } = useTranslation();
  const state = useSlideSorterState({
    slides,
    activeSlideIndex,
    canEdit,
    sectionGroups,
    onSelectSlide,
    onMoveSlide,
    onDeleteSlides,
    onDuplicateSlides,
    onToggleHideSlides,
    onClose
  });
  const renderSlideCard = (slide, index) => /* @__PURE__ */ jsx(
    SlideCard,
    {
      slide,
      index,
      isActive: index === activeSlideIndex,
      isDragTarget: state.dragOverIndex === index,
      isSelected: state.isSelected(slide.id),
      selectedCount: state.selectedSlideIds.length,
      selectionOrder: state.selectedIndexes.indexOf(index) + 1,
      canvasSize,
      canEdit,
      onSlideClick: state.handleSlideClick,
      onDoubleClick: state.handleDoubleClick,
      onContextMenu: state.handleContextMenu,
      onDragStart: state.handleDragStart,
      onDragOver: state.handleDragOver,
      onDragLeave: state.handleDragLeave,
      onDrop: state.handleDrop
    },
    slide.id || index
  );
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ref: state.backdropRef,
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex flex-col bg-black/70 backdrop-blur-sm",
      onClick: state.handleBackdropClick,
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-6 py-3 border-b border-border/50", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3", children: [
            /* @__PURE__ */ jsx("h2", { className: "text-sm font-medium text-foreground", children: t("pptx.slideSorter.title") }),
            state.selectedSlideIds.length > 1 && /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: t("pptx.slideSorter.selectedCount", {
              count: state.selectedSlideIds.length
            }) })
          ] }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "flex h-8 w-8 items-center justify-center rounded-full bg-muted text-foreground hover:bg-accent hover:text-white transition-colors",
              "aria-label": t("pptx.slideSorter.close"),
              children: /* @__PURE__ */ jsxs(
                "svg",
                {
                  xmlns: "http://www.w3.org/2000/svg",
                  width: "16",
                  height: "16",
                  viewBox: "0 0 24 24",
                  fill: "none",
                  stroke: "currentColor",
                  strokeWidth: "2",
                  strokeLinecap: "round",
                  strokeLinejoin: "round",
                  children: [
                    /* @__PURE__ */ jsx("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
                    /* @__PURE__ */ jsx("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
                  ]
                }
              )
            }
          )
        ] }),
        /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-y-auto px-6 py-6", children: /* @__PURE__ */ jsx(
          "div",
          {
            className: "mx-auto",
            style: {
              maxWidth: `${Math.max(600, state.gridCols * 220 * state.zoomScale)}px`
            },
            children: state.showSectionHeaders ? sectionGroups.map((section) => /* @__PURE__ */ jsx(
              SectionBlock,
              {
                section,
                slides,
                gridCols: state.gridCols,
                zoomScale: state.zoomScale,
                renderSlideCard
              },
              section.id
            )) : /* @__PURE__ */ jsx(
              "div",
              {
                className: "grid gap-4",
                style: {
                  gridTemplateColumns: `repeat(${state.gridCols}, minmax(0, 1fr))`
                },
                children: slides.map((slide, index) => renderSlideCard(slide, index))
              }
            )
          }
        ) }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-6 py-2.5 border-t border-border/50 bg-background/50", children: [
          /* @__PURE__ */ jsx("div", { className: "text-xs text-muted-foreground", children: t("pptx.slideSorter.slideCount", { count: slides.length }) }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "rounded p-1 text-muted-foreground hover:bg-accent hover:text-foreground disabled:opacity-40",
                disabled: state.zoom <= MIN_ZOOM,
                onClick: () => state.setZoom((z) => Math.max(MIN_ZOOM, z - ZOOM_STEP)),
                "aria-label": t("pptx.slideSorter.zoomOut"),
                children: /* @__PURE__ */ jsx(LuMinus, { className: "h-3.5 w-3.5" })
              }
            ),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "range",
                min: MIN_ZOOM,
                max: MAX_ZOOM,
                step: ZOOM_STEP,
                value: state.zoom,
                onChange: (e) => state.setZoom(Number(e.target.value)),
                className: "h-1 w-24 cursor-pointer appearance-none rounded-full bg-accent accent-primary",
                "aria-label": t("pptx.slideSorter.zoom")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "rounded p-1 text-muted-foreground hover:bg-accent hover:text-foreground disabled:opacity-40",
                disabled: state.zoom >= MAX_ZOOM,
                onClick: () => state.setZoom((z) => Math.min(MAX_ZOOM, z + ZOOM_STEP)),
                "aria-label": t("pptx.slideSorter.zoomIn"),
                children: /* @__PURE__ */ jsx(LuPlus, { className: "h-3.5 w-3.5" })
              }
            ),
            /* @__PURE__ */ jsxs("span", { className: "min-w-[3ch] text-right text-xs text-muted-foreground", children: [
              state.zoom,
              "%"
            ] })
          ] })
        ] }),
        state.contextMenu && canEdit && /* @__PURE__ */ jsx(
          SorterContextMenu,
          {
            x: state.contextMenu.x,
            y: state.contextMenu.y,
            selectedCount: state.selectedSlideIds.length,
            hasClipboard: state.clipboardSlideIds.length > 0,
            hasHiddenInSelection: state.hasHiddenInSelection,
            hasVisibleInSelection: state.hasVisibleInSelection,
            sectionGroups: state.showSectionHeaders ? sectionGroups : [],
            onDelete: state.handleDeleteSelected,
            onDuplicate: state.handleDuplicateSelected,
            onCopy: state.handleCopySelected,
            onPaste: state.handlePaste,
            onToggleHide: state.handleToggleHideSelected,
            onClose: state.closeContextMenu
          }
        )
      ]
    }
  );
}
function useOutlineView(input) {
  const { slides, canvasSize, canEdit, setSlides, setActiveSlideIndex, bumpHistory } = input;
  const rows = useMemo(() => buildOutline(slides), [slides]);
  const containerRef = useRef(null);
  const pendingFocusRef = useRef(null);
  const run = useCallback(
    (edit) => {
      if (!canEdit) {
        return;
      }
      const result = applyOutlineEdit(slides, edit, { canvas: canvasSize });
      if (!result.changed) {
        return;
      }
      setSlides(result.slides);
      setActiveSlideIndex(result.activeSlideIndex);
      bumpHistory();
      pendingFocusRef.current = result.focusKey;
    },
    [bumpHistory, canEdit, canvasSize, setActiveSlideIndex, setSlides, slides]
  );
  const onRowKeyDown = useCallback(
    (event, rowKey) => {
      const { edit, preventDefault } = mapOutlineKey(event, rowKey);
      if (preventDefault) {
        event.preventDefault();
      }
      if (edit) {
        run(edit);
      }
    },
    [run]
  );
  useEffect(() => {
    const key = pendingFocusRef.current;
    if (!key) {
      return;
    }
    pendingFocusRef.current = null;
    const selector = `[${OUTLINE_ROW_ATTR}="${CSS.escape(key)}"]`;
    const target = containerRef.current?.querySelector(selector);
    if (target && document.activeElement !== target) {
      target.focus();
      target.setSelectionRange(target.value.length, target.value.length);
    }
  }, [rows]);
  return { rows, containerRef, run, onRowKeyDown };
}
var INDENT_PX2 = 22;
function OutlineViewOverlay({
  slides,
  canvasSize,
  canEdit,
  setSlides,
  setActiveSlideIndex,
  bumpHistory,
  onClose
}) {
  const { t } = useTranslation();
  const { rows, containerRef, run, onRowKeyDown } = useOutlineView({
    slides,
    canvasSize,
    canEdit,
    setSlides,
    setActiveSlideIndex,
    bumpHistory
  });
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ...{ [OUTLINE_VIEW_ATTR]: "true" },
      role: "region",
      "aria-label": t("pptx.view.outlineView"),
      className: "fixed inset-0 z-[1300] flex flex-col bg-neutral-900 text-neutral-100",
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 border-b border-white/10 px-4 py-2", children: [
          /* @__PURE__ */ jsx("span", { className: "text-sm font-semibold", children: t("pptx.view.outlineView") }),
          /* @__PURE__ */ jsx("span", { className: "flex-1 truncate text-[11px] text-white/50", children: t("pptx.outline.hint") }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "inline-flex h-8 w-8 items-center justify-center rounded text-white/80 transition-colors hover:bg-white/15 hover:text-white",
              "aria-label": t("pptx.statusBar.normalView"),
              title: t("pptx.statusBar.normalView"),
              onClick: onClose,
              children: /* @__PURE__ */ jsx(LuX, {})
            }
          )
        ] }),
        /* @__PURE__ */ jsx("div", { ref: containerRef, className: "min-h-0 flex-1 overflow-auto px-4 py-3", children: rows.map((row) => /* @__PURE__ */ jsxs(
          "div",
          {
            className: "flex items-center gap-2 py-0.5",
            style: { paddingLeft: row.level * INDENT_PX2 },
            children: [
              /* @__PURE__ */ jsx("span", { className: "w-6 shrink-0 text-right text-[10px] tabular-nums text-white/40", children: row.kind === "title" ? row.slideIndex + 1 : "" }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  ...{
                    [OUTLINE_ROW_ATTR]: row.key,
                    [OUTLINE_SLIDE_ATTR]: String(row.slideIndex + 1),
                    [OUTLINE_LEVEL_ATTR]: String(row.level)
                  },
                  type: "text",
                  value: row.text,
                  readOnly: !canEdit,
                  "aria-label": t(
                    row.kind === "title" ? "pptx.outline.titleLine" : "pptx.outline.bodyLine"
                  ),
                  className: row.kind === "title" ? "w-full rounded bg-transparent px-1 py-0.5 text-sm font-semibold outline-none focus:bg-white/10" : "w-full rounded bg-transparent px-1 py-0.5 text-[13px] text-white/80 outline-none focus:bg-white/10",
                  onChange: (event) => run({ type: "setText", key: row.key, text: event.target.value }),
                  onKeyDown: (event) => onRowKeyDown(event, row.key)
                }
              )
            ]
          },
          row.key
        )) })
      ]
    }
  );
}
function ReadingViewStage({
  slide,
  templateElements,
  canvasSize,
  scale
}) {
  if (scale <= 0) {
    return null;
  }
  const width = Math.max(canvasSize.width, 1);
  const height = Math.max(canvasSize.height, 1);
  const elements = [...templateElements, ...slide.elements];
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ...{ [READING_VIEW_STAGE_ATTR]: "true" },
      "aria-roledescription": "slide",
      className: "relative overflow-hidden bg-white shadow-2xl",
      style: { width: width * scale, height: height * scale },
      children: [
        slide.backgroundColor && slide.backgroundColor !== "transparent" && /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute inset-0",
            style: { backgroundColor: normalizeHexColor(slide.backgroundColor, "#ffffff") }
          }
        ),
        slide.backgroundImage && /* @__PURE__ */ jsx(
          "img",
          {
            src: slide.backgroundImage,
            alt: "",
            className: "absolute inset-0 h-full w-full object-cover",
            draggable: false
          }
        ),
        slide.backgroundGradient && /* @__PURE__ */ jsx("div", { className: "absolute inset-0", style: { backgroundImage: slide.backgroundGradient } }),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute left-0 top-0 origin-top-left",
            style: { width, height, transform: `scale(${scale})` },
            children: elements.map((element, index) => /* @__PURE__ */ jsx(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index
              },
              element.id
            ))
          }
        )
      ]
    }
  );
}
var READING_VIEW_PADDING = 24;
function useReadingView(input) {
  const { slideCount, canvasSize, initialSlideIndex, onExit } = input;
  const [state, setState] = useState(
    () => openReadingView(initialSlideIndex, slideCount)
  );
  const viewportRef = useRef(null);
  const [viewport, setViewport] = useState({ width: 0, height: 0 });
  const onExitRef = useRef(onExit);
  onExitRef.current = onExit;
  const run = useCallback(
    (command) => {
      setState((previous) => {
        const next = applyReadingViewCommand(previous, command, slideCount);
        if (previous.open && !next.open) {
          onExitRef.current(previous.slideIndex);
        }
        return next;
      });
    },
    [slideCount]
  );
  useEffect(() => {
    const buffer = createPresentationKeyBuffer();
    const handle = (event) => {
      const { command, swallow, preventDefault } = handleReadingViewKey(event, buffer);
      if (swallow) {
        event.stopPropagation();
      }
      if (preventDefault) {
        event.preventDefault();
      }
      if (command.command !== "none") {
        run(command);
      }
    };
    window.addEventListener("keydown", handle, true);
    return () => window.removeEventListener("keydown", handle, true);
  }, [run]);
  useEffect(() => {
    const element = viewportRef.current;
    if (!element) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      const rect = entries[0]?.contentRect;
      if (rect) {
        setViewport({ width: rect.width, height: rect.height });
      }
    });
    observer.observe(element);
    return () => observer.disconnect();
  }, []);
  const scale = useMemo(
    () => readingViewFitScale(canvasSize, viewport, READING_VIEW_PADDING),
    [canvasSize, viewport]
  );
  return { state, viewportRef, scale, run };
}
function ReadingViewOverlay({
  slides,
  templateElements,
  canvasSize,
  activeSlideIndex,
  onExit
}) {
  const { t } = useTranslation();
  const { state, viewportRef, scale, run } = useReadingView({
    slideCount: slides.length,
    canvasSize,
    initialSlideIndex: activeSlideIndex,
    onExit
  });
  const slide = slides[state.slideIndex];
  if (!state.open || !slide) {
    return null;
  }
  const control = "inline-flex h-8 w-8 items-center justify-center rounded text-white/80 transition-colors hover:bg-white/15 hover:text-white disabled:cursor-default disabled:opacity-30 disabled:hover:bg-transparent";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ...{ [READING_VIEW_ATTR]: "true" },
      role: "region",
      "aria-label": t("pptx.view.readingView"),
      className: "fixed inset-0 z-[1300] flex flex-col bg-neutral-900",
      children: [
        /* @__PURE__ */ jsx("div", { ref: viewportRef, className: "flex min-h-0 flex-1 items-center justify-center", children: /* @__PURE__ */ jsx(
          ReadingViewStage,
          {
            slide,
            templateElements,
            canvasSize,
            scale
          }
        ) }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-center gap-3 border-t border-white/10 px-4 py-2", children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: control,
              "aria-label": t("pptx.common.previous"),
              title: t("pptx.common.previous"),
              disabled: !canGoPrevious(state),
              onClick: () => run({ command: "previous" }),
              children: /* @__PURE__ */ jsx(LuChevronLeft, {})
            }
          ),
          /* @__PURE__ */ jsx(
            "span",
            {
              ...{ [READING_VIEW_COUNTER_ATTR]: "true" },
              className: "min-w-16 text-center text-xs tabular-nums text-white/70",
              children: formatSlideCounter(state.slideIndex, slides.length)
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: control,
              "aria-label": t("pptx.common.next"),
              title: t("pptx.common.next"),
              disabled: !canGoNext(state, slides.length),
              onClick: () => run({ command: "next" }),
              children: /* @__PURE__ */ jsx(LuChevronRight, {})
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: control,
              "aria-label": t("pptx.statusBar.normalView"),
              title: t("pptx.statusBar.normalView"),
              onClick: () => run({ command: "exit" }),
              children: /* @__PURE__ */ jsx(LuX, {})
            }
          )
        ] })
      ]
    }
  );
}
function renderRichNotesSegments(segments) {
  const paragraphs = segmentsToParagraphs(segments);
  const nodes = [];
  let numberedCounter = 0;
  paragraphs.forEach((para, pIdx) => {
    if (para.bulletType === "numbered") {
      numberedCounter++;
    } else {
      numberedCounter = 0;
    }
    const divStyle = {};
    if (para.indentLevel > 0) {
      divStyle.paddingLeft = para.indentLevel * INDENT_PX;
    }
    const children = [];
    if (para.bulletType === "bullet") {
      children.push(
        /* @__PURE__ */ jsx("span", { style: { marginRight: 6, color: "#9ca3af" }, children: "\u2022" }, "bullet")
      );
    } else if (para.bulletType === "numbered") {
      children.push(
        /* @__PURE__ */ jsxs("span", { style: { marginRight: 6, color: "#9ca3af" }, children: [
          numberedCounter,
          "."
        ] }, "num")
      );
    }
    para.segments.forEach((segment, sIdx) => {
      if (segment.isParagraphBreak) {
        return;
      }
      const style = {};
      if (segment.style.bold) {
        style.fontWeight = "bold";
      }
      if (segment.style.italic) {
        style.fontStyle = "italic";
      }
      if (segment.style.underline) {
        style.textDecoration = "underline";
      }
      if (segment.style.strikethrough) {
        style.textDecoration = `${style.textDecoration ? `${style.textDecoration} ` : ""}line-through`;
      }
      if (segment.style.color) {
        style.color = segment.style.color;
      }
      if (segment.style.fontSize) {
        style.fontSize = `${segment.style.fontSize}pt`;
      }
      if (segment.style.fontFamily) {
        style.fontFamily = segment.style.fontFamily;
      }
      if (segment.style.hyperlink && isUrlSafe(segment.style.hyperlink)) {
        const safeHref = segment.style.hyperlink;
        style.color = "#4a9eff";
        style.textDecoration = "underline";
        style.cursor = "pointer";
        children.push(
          /* @__PURE__ */ jsx(
            "a",
            {
              href: safeHref,
              style,
              onClick: (e) => {
                e.preventDefault();
                safeOpenUrl(safeHref);
              },
              children: segment.text
            },
            `seg-${pIdx}-${sIdx}`
          )
        );
      } else {
        children.push(
          /* @__PURE__ */ jsx("span", { style, children: segment.text }, `seg-${pIdx}-${sIdx}`)
        );
      }
    });
    nodes.push(
      /* @__PURE__ */ jsx("div", { style: divStyle, children: children.length > 0 ? children : /* @__PURE__ */ jsx("br", {}) }, `p-${pIdx}`)
    );
  });
  return nodes;
}
function NotesPrintDialog({
  slides,
  onClose,
  notesStyle
}) {
  const { t } = useTranslation();
  const printFrameRef = useRef(null);
  const handlePrint = useCallback(() => {
    const iframe = printFrameRef.current;
    if (!iframe?.contentWindow) {
      return;
    }
    const doc = iframe.contentWindow.document;
    doc.open();
    doc.write(buildNotesPrintHtml(slides, (n) => t("pptx.notes.slideN", { n }), notesStyle));
    doc.close();
    setTimeout(() => {
      iframe.contentWindow?.print();
    }, 200);
  }, [slides, t, notesStyle]);
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-50 flex items-center justify-center bg-black/60", children: /* @__PURE__ */ jsxs("div", { className: "bg-background border border-border rounded-lg shadow-xl w-[640px] max-h-[80vh] flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-4 py-3 border-b border-border", children: [
      /* @__PURE__ */ jsx("span", { className: "text-sm font-medium text-foreground", children: t("pptx.notes.printNotes") }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "text-muted-foreground hover:text-foreground text-sm",
          children: t("pptx.common.close")
        }
      )
    ] }),
    /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-y-auto p-4 space-y-4", children: slides.map((slide) => {
      const segs = resolveNotesSegments(slide, notesStyle);
      const hasText = segmentsToPlainText(segs).trim().length > 0;
      return /* @__PURE__ */ jsxs("div", { className: "border border-border/50 rounded p-3", children: [
        /* @__PURE__ */ jsx("div", { className: "text-xs font-medium text-muted-foreground mb-2", children: t("pptx.notes.slideN", { n: slide.slideNumber }) }),
        /* @__PURE__ */ jsx("div", { className: "w-full aspect-video bg-muted rounded mb-2 flex items-center justify-center text-muted-foreground text-sm", children: t("pptx.notes.slideN", { n: slide.slideNumber }) }),
        /* @__PURE__ */ jsx("div", { className: "text-xs text-foreground whitespace-pre-wrap", children: hasText ? renderRichNotesSegments(segs) : /* @__PURE__ */ jsx("span", { className: "italic text-muted-foreground", children: t("pptx.notes.noNotes") }) })
      ] }, slide.id);
    }) }),
    /* @__PURE__ */ jsx("div", { className: "flex justify-end px-4 py-3 border-t border-border", children: /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: handlePrint,
        className: "px-3 py-1.5 text-xs bg-primary hover:bg-primary/80 text-white rounded",
        children: t("pptx.notes.print")
      }
    ) }),
    /* @__PURE__ */ jsx(
      "iframe",
      {
        ref: printFrameRef,
        title: "print-notes",
        className: "hidden",
        sandbox: "allow-same-origin"
      }
    )
  ] }) });
}
function HyperlinkPopover({
  initialText,
  onInsert,
  onClose
}) {
  const { t } = useTranslation();
  const [url, setUrl] = useState("");
  const [displayText, setDisplayText] = useState(initialText);
  const urlRef = useRef(null);
  useEffect(() => {
    urlRef.current?.focus();
  }, []);
  const handleSubmit = useCallback(
    (e) => {
      e.preventDefault();
      if (url.trim().length === 0) {
        return;
      }
      const finalUrl = url.startsWith("http") ? url : `https://${url}`;
      onInsert(finalUrl, displayText || finalUrl);
    },
    [url, displayText, onInsert]
  );
  return /* @__PURE__ */ jsx("div", { className: "absolute bottom-full left-0 mb-1 z-10 bg-muted border border-border rounded-lg shadow-lg p-3 w-72", children: /* @__PURE__ */ jsxs("form", { onSubmit: handleSubmit, className: "space-y-2", children: [
    /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-[10px] text-muted-foreground mb-0.5", children: t("pptx.notes.linkUrl") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          ref: urlRef,
          type: "text",
          value: url,
          onChange: (e) => setUrl(e.target.value),
          placeholder: "https://...",
          className: "w-full px-2 py-1 text-xs bg-background border border-border rounded text-foreground focus:outline-none focus:border-primary"
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-[10px] text-muted-foreground mb-0.5", children: t("pptx.notes.linkDisplayText") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          value: displayText,
          onChange: (e) => setDisplayText(e.target.value),
          placeholder: t("pptx.notes.linkDisplayText"),
          className: "w-full px-2 py-1 text-xs bg-background border border-border rounded text-foreground focus:outline-none focus:border-primary"
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "px-2 py-1 text-[10px] text-muted-foreground hover:text-foreground",
          children: t("pptx.common.cancel")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "submit",
          className: "px-2 py-1 text-[10px] bg-primary hover:bg-primary/80 text-white rounded",
          children: t("pptx.notes.insertLink")
        }
      )
    ] })
  ] }) });
}
function NotesToolbar({
  isRichEditEnabled,
  showLinkPopover,
  savedSelectionText,
  hasAllSlides,
  onApplyRichCommand,
  onToggleBulletList,
  onToggleNumberedList,
  onIndent,
  onOutdent,
  onLinkButtonClick,
  onInsertLink,
  onCloseLinkPopover,
  onPrintClick,
  onToggleRichEdit
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "mb-1 flex items-center justify-between gap-2", children: [
    /* @__PURE__ */ jsxs("div", { className: "inline-flex items-center rounded bg-muted text-xs overflow-hidden border border-border/60 relative", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.bold"),
          onClick: () => onApplyRichCommand("bold"),
          children: /* @__PURE__ */ jsx(LuBold, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.italic"),
          onClick: () => onApplyRichCommand("italic"),
          children: /* @__PURE__ */ jsx(LuItalic, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.underline"),
          onClick: () => onApplyRichCommand("underline"),
          children: /* @__PURE__ */ jsx(LuUnderline, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.strikethrough"),
          onClick: () => onApplyRichCommand("strikeThrough"),
          children: /* @__PURE__ */ jsx(LuStrikethrough, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "w-px h-4 bg-border mx-0.5" }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.bulletList"),
          onClick: onToggleBulletList,
          children: /* @__PURE__ */ jsx(LuList, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.numberedList"),
          onClick: onToggleNumberedList,
          children: /* @__PURE__ */ jsx(LuListOrdered, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "w-px h-4 bg-border mx-0.5" }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.indent"),
          onClick: onIndent,
          children: /* @__PURE__ */ jsx(LuIndentIncrease, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.outdent"),
          onClick: onOutdent,
          children: /* @__PURE__ */ jsx(LuIndentDecrease, { className: "w-3.5 h-3.5" })
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "w-px h-4 bg-border mx-0.5" }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.insertLink"),
          onClick: onLinkButtonClick,
          children: /* @__PURE__ */ jsx(LuLink, { className: "w-3.5 h-3.5" })
        }
      ),
      hasAllSlides && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.printNotes"),
          onClick: onPrintClick,
          children: /* @__PURE__ */ jsx(LuPrinter, { className: "w-3.5 h-3.5" })
        }
      ),
      showLinkPopover && /* @__PURE__ */ jsx(
        HyperlinkPopover,
        {
          initialText: savedSelectionText,
          onInsert: onInsertLink,
          onClose: onCloseLinkPopover
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: "text-[10px] px-2 py-1 rounded bg-muted hover:bg-accent border border-border/60",
        onClick: onToggleRichEdit,
        title: t("pptx.notes.toggleRichPlainEditor"),
        children: isRichEditEnabled ? t("pptx.notes.plainEditor") : t("pptx.notes.richEditor")
      }
    )
  ] });
}
function useSlideNotes({
  activeSlide,
  isExpanded,
  canEdit,
  onToggle,
  onUpdateNotes,
  notesStyle
}) {
  const initialSegments = useMemo(
    () => resolveNotesSegments(activeSlide, notesStyle),
    [activeSlide, notesStyle]
  );
  const [draft, setDraft] = useState(activeSlide?.notes ?? "");
  const [draftSegments, setDraftSegments] = useState(initialSegments);
  const [isRichEditEnabled, setIsRichEditEnabled] = useState(initialSegments.length > 0);
  const [showLinkPopover, setShowLinkPopover] = useState(false);
  const [showPrintDialog, setShowPrintDialog] = useState(false);
  const debounceRef = useRef(null);
  const textareaRef = useRef(null);
  const richEditorRef = useRef(null);
  const savedSelectionRef = useRef(null);
  const isInternalInputRef = useRef(false);
  const lastSavedTextRef = useRef(null);
  const activeSlideRef = useRef(activeSlide);
  activeSlideRef.current = activeSlide;
  useEffect(() => {
    const nextText = activeSlide?.notes ?? "";
    if (nextText === lastSavedTextRef.current) {
      return;
    }
    const nextSegments = resolveNotesSegments(activeSlideRef.current, notesStyle);
    setDraft(nextText);
    setDraftSegments(nextSegments);
    setIsRichEditEnabled(nextSegments.length > 0);
  }, [activeSlide?.id, activeSlide?.notes, activeSlide?.notesSegments, notesStyle]);
  useEffect(() => {
    if (!isRichEditEnabled || !isExpanded || !canEdit || !richEditorRef.current) {
      return;
    }
    if (isInternalInputRef.current) {
      isInternalInputRef.current = false;
      return;
    }
    richEditorRef.current.innerHTML = segmentsToEditorHtml(draftSegments);
  }, [canEdit, draftSegments, isExpanded, isRichEditEnabled]);
  const flush = useCallback(
    (value, segments) => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
        debounceRef.current = null;
      }
      lastSavedTextRef.current = value;
      onUpdateNotes(value, segments);
    },
    [onUpdateNotes]
  );
  const scheduleSave = useCallback(
    (value, segments) => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
      }
      debounceRef.current = setTimeout(() => {
        lastSavedTextRef.current = value;
        onUpdateNotes(value, segments);
        debounceRef.current = null;
      }, DEBOUNCE_MS);
    },
    [onUpdateNotes]
  );
  useEffect(() => {
    return () => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
      }
    };
  }, []);
  const handlePlainChange = useCallback(
    (e) => {
      const value = e.target.value;
      const segments = createPlainNotesSegments(value);
      setDraft(value);
      setDraftSegments(segments);
      scheduleSave(value, segments);
    },
    [scheduleSave]
  );
  const handleKeyDownPlain = useCallback(
    (e) => {
      e.stopPropagation();
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
        flush(draft, draftSegments);
        onToggle();
      }
    },
    [draft, draftSegments, flush, onToggle]
  );
  const handleRichInput = useCallback(() => {
    if (!richEditorRef.current) {
      return;
    }
    isInternalInputRef.current = true;
    const segments = parseSegmentsFromRichEditor(richEditorRef.current);
    const text = segmentsToPlainText(segments);
    setDraft(text);
    setDraftSegments(segments);
    scheduleSave(text, segments);
  }, [scheduleSave]);
  const handleBlur = useCallback(() => {
    flush(draft, draftSegments);
  }, [draft, draftSegments, flush]);
  const updateParagraphProperty = useCallback(
    (updater) => {
      if (!richEditorRef.current) {
        return;
      }
      const paraIdx = getCurrentParagraphIndex(richEditorRef.current, draftSegments);
      const paragraphs = segmentsToParagraphs(draftSegments);
      if (paraIdx >= 0 && paraIdx < paragraphs.length) {
        paragraphs[paraIdx] = updater(paragraphs[paraIdx], paraIdx);
      }
      const newSegments = paragraphsToSegments(paragraphs);
      const newText = segmentsToPlainText(newSegments);
      setDraft(newText);
      setDraftSegments(newSegments);
      scheduleSave(newText, newSegments);
    },
    [draftSegments, scheduleSave]
  );
  const applyRichCommand = useCallback(
    (command) => {
      document.execCommand(command);
      handleRichInput();
      richEditorRef.current?.focus();
    },
    [handleRichInput]
  );
  const toggleBulletList = useCallback(() => {
    updateParagraphProperty((para) => ({
      ...para,
      bulletType: para.bulletType === "bullet" ? "none" : "bullet"
    }));
    richEditorRef.current?.focus();
  }, [updateParagraphProperty]);
  const toggleNumberedList = useCallback(() => {
    updateParagraphProperty((para) => ({
      ...para,
      bulletType: para.bulletType === "numbered" ? "none" : "numbered"
    }));
    richEditorRef.current?.focus();
  }, [updateParagraphProperty]);
  const handleIndent = useCallback(() => {
    updateParagraphProperty((para) => ({
      ...para,
      indentLevel: Math.min(MAX_INDENT_LEVEL, para.indentLevel + 1)
    }));
    richEditorRef.current?.focus();
  }, [updateParagraphProperty]);
  const handleOutdent = useCallback(() => {
    updateParagraphProperty((para) => ({
      ...para,
      indentLevel: Math.max(0, para.indentLevel - 1)
    }));
    richEditorRef.current?.focus();
  }, [updateParagraphProperty]);
  const handleKeyDownRich = useCallback(
    (e) => {
      e.stopPropagation();
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
        flush(draft, draftSegments);
        onToggle();
        return;
      }
      if (e.key === "Tab") {
        e.preventDefault();
        if (e.shiftKey) {
          handleOutdent();
        } else {
          handleIndent();
        }
      }
    },
    [draft, draftSegments, flush, onToggle, handleIndent, handleOutdent]
  );
  const handleLinkButtonClick = useCallback(() => {
    const sel = window.getSelection();
    savedSelectionRef.current = { text: sel?.toString() ?? "" };
    setShowLinkPopover(true);
  }, []);
  const handleInsertLink = useCallback(
    (url, displayText) => {
      setShowLinkPopover(false);
      if (!richEditorRef.current) {
        return;
      }
      richEditorRef.current.focus();
      const sel = window.getSelection();
      if (sel && sel.rangeCount > 0) {
        const range = sel.getRangeAt(0);
        range.deleteContents();
        const anchor = document.createElement("a");
        anchor.href = url;
        anchor.textContent = displayText;
        anchor.style.color = "#4a9eff";
        anchor.style.textDecoration = "underline";
        anchor.style.cursor = "pointer";
        anchor.setAttribute("data-hyperlink", url);
        range.insertNode(anchor);
        range.setStartAfter(anchor);
        range.collapse(true);
        sel.removeAllRanges();
        sel.addRange(range);
      }
      handleRichInput();
    },
    [handleRichInput]
  );
  const handleEditorClick = useCallback((e) => {
    const target = e.target;
    if (!(target instanceof HTMLAnchorElement)) {
      return;
    }
    const href = target.getAttribute("data-hyperlink") || target.getAttribute("href");
    if (href && (e.ctrlKey || e.metaKey)) {
      e.preventDefault();
      safeOpenUrl(href);
    }
  }, []);
  return {
    draft,
    draftSegments,
    isRichEditEnabled,
    setIsRichEditEnabled,
    showLinkPopover,
    setShowLinkPopover,
    showPrintDialog,
    setShowPrintDialog,
    textareaRef,
    richEditorRef,
    savedSelectionRef,
    handlePlainChange,
    handleRichInput,
    handleBlur,
    handleKeyDownPlain,
    handleKeyDownRich,
    applyRichCommand,
    toggleBulletList,
    toggleNumberedList,
    handleIndent,
    handleOutdent,
    handleLinkButtonClick,
    handleInsertLink,
    handleEditorClick
  };
}
function SlideNotesPanel({
  activeSlide,
  allSlides,
  isExpanded,
  canEdit,
  onToggle,
  onUpdateNotes,
  panelHeight,
  notesStyle
}) {
  const { t } = useTranslation();
  const {
    draft,
    draftSegments,
    isRichEditEnabled,
    setIsRichEditEnabled,
    showLinkPopover,
    setShowLinkPopover,
    showPrintDialog,
    setShowPrintDialog,
    textareaRef,
    richEditorRef,
    savedSelectionRef,
    handlePlainChange,
    handleRichInput,
    handleBlur,
    handleKeyDownPlain,
    handleKeyDownRich,
    applyRichCommand,
    toggleBulletList,
    toggleNumberedList,
    handleIndent,
    handleOutdent,
    handleLinkButtonClick,
    handleInsertLink,
    handleEditorClick
  } = useSlideNotes({
    activeSlide,
    isExpanded,
    canEdit,
    onToggle,
    onUpdateNotes,
    notesStyle
  });
  const hasNotes = draft.trim().length > 0;
  const slideLabel = activeSlide ? t("pptx.notes.slideN", { n: activeSlide.slideNumber }) : t("pptx.notes.noSlide");
  const { dragY, handlers: dragHandlers } = useSheetDismissDrag(onToggle);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    isExpanded && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.common.close"),
        onClick: onToggle,
        className: "md:hidden fixed inset-0 z-20 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150"
      }
    ),
    /* @__PURE__ */ jsxs(
      "div",
      {
        className: cn(
          "flex flex-col border-t border-border/60 bg-background select-none",
          // On mobile, hide the entire notes strip when collapsed: the
          // MobileBottomBar's Notes button is the entry point instead.
          !isExpanded && "max-md:hidden",
          // On mobile, expanded notes render as a swipe-dismissable bottom
          // sheet (mirrors the Format/Comments sheets), not an inline strip.
          "max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:z-30 max-md:max-h-[75dvh] max-md:rounded-t-2xl max-md:border max-md:border-border max-md:shadow-2xl max-md:pb-[max(env(safe-area-inset-bottom),0px)]"
        ),
        style: dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : void 0,
        children: [
          isExpanded && /* @__PURE__ */ jsx(
            "div",
            {
              className: "md:hidden flex items-center justify-center pt-3 pb-2 cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: dragHandlers.onPointerDown,
              onPointerMove: dragHandlers.onPointerMove,
              onPointerUp: dragHandlers.onPointerUp,
              onPointerCancel: dragHandlers.onPointerCancel,
              children: /* @__PURE__ */ jsx("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" })
            }
          ),
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              onClick: onToggle,
              className: "flex items-center gap-1.5 px-3 py-1 text-[11px] text-muted-foreground hover:text-foreground hover:bg-accent/30 transition-colors w-full text-left shrink-0 max-md:hidden",
              "aria-expanded": isExpanded,
              "aria-controls": "slide-notes-content",
              children: [
                "Notes",
                !isExpanded && hasNotes && /* @__PURE__ */ jsx("span", { className: "text-muted-foreground/50 text-[10px]", children: "(has notes)" })
              ]
            }
          ),
          isExpanded && /* @__PURE__ */ jsxs(
            "div",
            {
              id: "slide-notes-content",
              className: "px-3 pb-2 overflow-y-auto",
              style: { maxHeight: panelHeight ?? EXPANDED_MAX_HEIGHT + 40 },
              children: [
                /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: slideLabel }),
                canEdit ? /* @__PURE__ */ jsxs(Fragment, { children: [
                  /* @__PURE__ */ jsx(
                    NotesToolbar,
                    {
                      isRichEditEnabled,
                      showLinkPopover,
                      savedSelectionText: savedSelectionRef.current?.text ?? "",
                      hasAllSlides: allSlides !== void 0 && allSlides.length > 0,
                      onApplyRichCommand: applyRichCommand,
                      onToggleBulletList: toggleBulletList,
                      onToggleNumberedList: toggleNumberedList,
                      onIndent: handleIndent,
                      onOutdent: handleOutdent,
                      onLinkButtonClick: handleLinkButtonClick,
                      onInsertLink: handleInsertLink,
                      onCloseLinkPopover: () => setShowLinkPopover(false),
                      onPrintClick: () => setShowPrintDialog(true),
                      onToggleRichEdit: () => setIsRichEditEnabled((prev) => !prev)
                    }
                  ),
                  isRichEditEnabled ? /* @__PURE__ */ jsx(
                    "div",
                    {
                      ref: richEditorRef,
                      contentEditable: true,
                      suppressContentEditableWarning: true,
                      onInput: handleRichInput,
                      onBlur: handleBlur,
                      onKeyDown: handleKeyDownRich,
                      onClick: handleEditorClick,
                      className: "w-full overflow-y-auto rounded-md border border-border/50 bg-muted/60 px-2.5 py-1.5 text-xs text-foreground focus:border-primary/50 focus:outline-none focus:ring-1 focus:ring-primary/30 transition-colors whitespace-pre-wrap",
                      style: { maxHeight: EXPANDED_MAX_HEIGHT - 8, minHeight: 72 }
                    }
                  ) : /* @__PURE__ */ jsx(
                    "textarea",
                    {
                      ref: textareaRef,
                      name: "slide-notes",
                      value: draft,
                      onChange: handlePlainChange,
                      onBlur: handleBlur,
                      onKeyDown: handleKeyDownPlain,
                      placeholder: t("pptx.notes.clickToAddNotes"),
                      rows: 4,
                      className: "w-full resize-none rounded-md border border-border/50 bg-muted/60 px-2.5 py-1.5 text-xs text-foreground placeholder:text-muted-foreground focus:border-primary/50 focus:outline-none focus:ring-1 focus:ring-primary/30 transition-colors",
                      style: { maxHeight: EXPANDED_MAX_HEIGHT - 8 }
                    }
                  )
                ] }) : /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: "w-full rounded-md border border-border/30 bg-muted/40 px-2.5 py-1.5 text-xs text-muted-foreground overflow-y-auto whitespace-pre-wrap",
                    style: { maxHeight: EXPANDED_MAX_HEIGHT - 32, minHeight: 60 },
                    children: hasNotes ? renderRichNotesSegments(draftSegments) : /* @__PURE__ */ jsx("span", { className: "italic text-muted-foreground", children: t("pptx.notes.noNotes") })
                  }
                )
              ]
            }
          ),
          showPrintDialog && allSlides && /* @__PURE__ */ jsx(
            NotesPrintDialog,
            {
              slides: allSlides,
              onClose: () => setShowPrintDialog(false),
              notesStyle
            }
          )
        ]
      }
    )
  ] });
}
var DB_NAME = "pptx-viewer-autosave";
var DB_VERSION = 1;
var STORE_NAME = "recoveryVersions";
function openAutosaveDb() {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);
    req.onupgradeneeded = () => {
      const db = req.result;
      if (!db.objectStoreNames.contains(STORE_NAME)) {
        db.createObjectStore(STORE_NAME, { keyPath: "key" });
      }
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}
async function getVersions(filePath) {
  try {
    const db = await openAutosaveDb();
    return new Promise((resolve) => {
      const tx = db.transaction(STORE_NAME, "readonly");
      const store = tx.objectStore(STORE_NAME);
      const req = store.get(filePath);
      req.onsuccess = () => {
        db.close();
        const result = req.result;
        resolve(result ? [result] : []);
      };
      req.onerror = () => {
        db.close();
        resolve([]);
      };
    });
  } catch {
    return [];
  }
}
async function deleteVersion(filePath) {
  try {
    const db = await openAutosaveDb();
    return new Promise((resolve) => {
      const tx = db.transaction(STORE_NAME, "readwrite");
      const store = tx.objectStore(STORE_NAME);
      store.delete(filePath);
      tx.oncomplete = () => {
        db.close();
        resolve();
      };
      tx.onerror = () => {
        db.close();
        resolve();
      };
    });
  } catch {
  }
}
function formatFileSize(bytes) {
  if (bytes < 1024) {
    return `${bytes} B`;
  }
  if (bytes < 1024 * 1024) {
    return `${(bytes / 1024).toFixed(1)} KB`;
  }
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
function VersionHistoryPanel({
  isOpen,
  filePath,
  onClose,
  onRestore
}) {
  const { t } = useTranslation();
  const [versions, setVersions] = useState([]);
  const [loading, setLoading] = useState(false);
  const [restoringKey, setRestoringKey] = useState(null);
  const [deletingKey, setDeletingKey] = useState(null);
  const fetchVersions = useCallback(async () => {
    if (!filePath) {
      return;
    }
    setLoading(true);
    try {
      const result = await getVersions(filePath);
      setVersions(result);
    } catch {
      setVersions([]);
    } finally {
      setLoading(false);
    }
  }, [filePath]);
  useEffect(() => {
    if (isOpen) {
      void fetchVersions();
    }
  }, [isOpen, fetchVersions]);
  const handleRestore = useCallback(
    async (version) => {
      setRestoringKey(version.key);
      try {
        if (version.data) {
          onRestore(version.data);
          onClose();
        }
      } finally {
        setRestoringKey(null);
      }
    },
    // `onRestore`/`onClose` ARE read above (inside the `try`), but the analyzer
    // doesn't see through a try/finally block and flags them as unused;
    // verified as a false positive with a minimal repro (see useLayoutSwitching.ts).
    // oxlint-disable-next-line react/memo-dependencies -- see comment above
    [onRestore, onClose]
  );
  const handleDelete = useCallback(
    async (version) => {
      setDeletingKey(version.key);
      try {
        await deleteVersion(version.key);
        await fetchVersions();
      } finally {
        setDeletingKey(null);
      }
    },
    // `fetchVersions` IS read above (inside the `try`), but the analyzer
    // doesn't see through an `await` call wrapped in try/finally and flags it
    // as unused; verified as a false positive with a minimal repro (see
    // useLayoutSwitching.ts).
    // oxlint-disable-next-line react/memo-dependencies -- see comment above
    [fetchVersions]
  );
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: "absolute inset-y-0 right-0 w-80 bg-background border-l border-border z-50 flex flex-col shadow-xl", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-3 py-2 border-b border-border", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-sm font-medium text-foreground", children: [
        /* @__PURE__ */ jsx(LuClock, { className: "w-4 h-4" }),
        t("pptx.versionHistory.title")
      ] }),
      /* @__PURE__ */ jsx(
        "button",
        {
          onClick: onClose,
          className: "p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground",
          children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex-1 overflow-y-auto", children: [
      loading && /* @__PURE__ */ jsx("div", { className: "px-3 py-8 text-center text-xs text-muted-foreground", children: t("pptx.common.loading") }),
      !loading && versions.length === 0 && /* @__PURE__ */ jsx("div", { className: "px-3 py-8 text-center text-xs text-muted-foreground", children: t("pptx.versionHistory.noVersions") }),
      !loading && versions.map((version) => /* @__PURE__ */ jsxs(
        "div",
        {
          className: "px-3 py-2.5 border-b border-border hover:bg-muted/50 group",
          children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
              /* @__PURE__ */ jsx("div", { className: "text-xs text-foreground", children: formatVersionTimestamp(version.timestamp) }),
              /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground", children: formatRelativeTime(version.timestamp) })
            ] }),
            /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mt-0.5", children: formatFileSize(version.size) }),
            /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 mt-1.5 opacity-0 group-hover:opacity-100 transition-opacity", children: [
              /* @__PURE__ */ jsxs(
                "button",
                {
                  onClick: () => void handleRestore(version),
                  disabled: restoringKey === version.key,
                  className: "inline-flex items-center gap-1 px-2 py-1 rounded text-[10px] bg-primary/20 text-primary hover:bg-primary/30 disabled:opacity-40",
                  children: [
                    /* @__PURE__ */ jsx(LuDownload, { className: "w-3 h-3" }),
                    restoringKey === version.key ? t("pptx.common.loading") : t("pptx.versionHistory.restore")
                  ]
                }
              ),
              /* @__PURE__ */ jsxs(
                "button",
                {
                  onClick: () => void handleDelete(version),
                  disabled: deletingKey === version.key,
                  className: "inline-flex items-center gap-1 px-2 py-1 rounded text-[10px] bg-red-600/20 text-red-400 hover:bg-red-600/30 disabled:opacity-40",
                  children: [
                    /* @__PURE__ */ jsx(LuTrash2, { className: "w-3 h-3" }),
                    t("pptx.common.delete")
                  ]
                }
              )
            ] })
          ]
        },
        version.key
      ))
    ] })
  ] });
}
function ChangeKindIcon({ kind }) {
  switch (kind) {
    case "added":
      return /* @__PURE__ */ jsx(LuPlus, { className: "w-3 h-3 text-green-400" });
    case "removed":
      return /* @__PURE__ */ jsx(LuMinus, { className: "w-3 h-3 text-red-400" });
    case "moved":
      return /* @__PURE__ */ jsx(LuMove, { className: "w-3 h-3 text-primary" });
    case "resized":
      return /* @__PURE__ */ jsx(LuMove, { className: "w-3 h-3 text-amber-400" });
    case "textChanged":
      return /* @__PURE__ */ jsx(LuType, { className: "w-3 h-3 text-purple-400" });
  }
}
function SlideDiffRow({
  diff,
  diffIndex,
  canvasSize,
  accepted,
  rejected,
  onAccept,
  onReject
}) {
  const { t } = useTranslation();
  const [expanded, setExpanded] = useState(diff.status === "changed");
  const statusLabel = (() => {
    switch (diff.status) {
      case "added":
        return t("pptx.compare.statusAdded");
      case "removed":
        return t("pptx.compare.statusRemoved");
      case "changed":
        return t("pptx.compare.statusChanged");
      case "unchanged":
        return t("pptx.compare.statusUnchanged");
    }
  })();
  const statusColor = (() => {
    switch (diff.status) {
      case "added":
        return "text-green-400 bg-green-900/30";
      case "removed":
        return "text-red-400 bg-red-900/30";
      case "changed":
        return "text-amber-400 bg-amber-900/30";
      case "unchanged":
        return "text-muted-foreground bg-muted/30";
    }
  })();
  if (diff.status === "unchanged") {
    return null;
  }
  const isResolved = accepted || rejected;
  const slideNumber = diff.baseIndex >= 0 ? diff.baseIndex + 1 : diff.compareIndex + 1;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "rounded-lg border transition-colors",
        isResolved ? "border-border/60 bg-card/40 opacity-60" : "border-border bg-background/70"
      ),
      children: [
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            className: "flex items-center gap-2 w-full px-3 py-2 text-left",
            onClick: () => setExpanded((p) => !p),
            children: [
              expanded ? /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3.5 h-3.5 text-muted-foreground flex-shrink-0" }) : /* @__PURE__ */ jsx(LuChevronRight, { className: "w-3.5 h-3.5 text-muted-foreground flex-shrink-0" }),
              /* @__PURE__ */ jsx("span", { className: "text-xs text-foreground", children: t("pptx.compare.slideNumber", { number: slideNumber }) }),
              /* @__PURE__ */ jsx("span", { className: cn("rounded-full px-2 py-0.5 text-[10px] font-medium", statusColor), children: statusLabel }),
              diff.changes.length > 0 && /* @__PURE__ */ jsxs("span", { className: "text-[10px] text-muted-foreground", children: [
                diff.changes.length,
                " ",
                diff.changes.length === 1 ? "change" : "changes"
              ] }),
              /* @__PURE__ */ jsx("span", { className: "flex-1" }),
              isResolved && /* @__PURE__ */ jsx(
                "span",
                {
                  className: cn(
                    "text-[10px] font-medium",
                    accepted ? "text-green-400" : "text-muted-foreground"
                  ),
                  children: accepted ? t("pptx.compare.accepted") : t("pptx.compare.rejected")
                }
              )
            ]
          }
        ),
        expanded && /* @__PURE__ */ jsxs("div", { className: "px-3 pb-3 space-y-2", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
            diff.baseSlide && /* @__PURE__ */ jsxs("div", { className: "flex-1", children: [
              /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.compare.current") }),
              /* @__PURE__ */ jsx("div", { className: "rounded border border-border overflow-hidden", children: /* @__PURE__ */ jsx(
                SlideThumbnail,
                {
                  slide: diff.baseSlide,
                  templateElements: [],
                  canvasSize
                }
              ) })
            ] }),
            diff.compareSlide && /* @__PURE__ */ jsxs("div", { className: "flex-1", children: [
              /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.compare.incoming") }),
              /* @__PURE__ */ jsx(
                "div",
                {
                  className: cn(
                    "rounded border overflow-hidden",
                    diff.status === "added" ? "border-green-700/60" : diff.status === "changed" ? "border-amber-700/60" : "border-border"
                  ),
                  children: /* @__PURE__ */ jsx(
                    SlideThumbnail,
                    {
                      slide: diff.compareSlide,
                      templateElements: [],
                      canvasSize
                    }
                  )
                }
              )
            ] })
          ] }),
          diff.changes.length > 0 && /* @__PURE__ */ jsx("div", { className: "space-y-1", children: diff.changes.map((change, ci) => /* @__PURE__ */ jsxs(
            "div",
            {
              className: "flex items-start gap-2 rounded bg-muted/60 px-2 py-1.5 text-[11px]",
              children: [
                /* @__PURE__ */ jsx(ChangeKindIcon, { kind: change.kind }),
                /* @__PURE__ */ jsx("span", { className: "text-foreground", children: change.description })
              ]
            },
            `${change.elementId}-${change.kind}-${ci}`
          )) }),
          !isResolved && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 pt-1", children: [
            /* @__PURE__ */ jsxs(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-green-700/80 px-2.5 py-1 text-[11px] text-green-50 hover:bg-green-600 transition-colors",
                onClick: () => onAccept(diffIndex),
                children: [
                  /* @__PURE__ */ jsx(LuCheck, { className: "w-3 h-3" }),
                  t("pptx.compare.accept")
                ]
              }
            ),
            /* @__PURE__ */ jsxs(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-accent px-2.5 py-1 text-[11px] text-foreground hover:bg-accent/80 transition-colors",
                onClick: () => onReject(diffIndex),
                children: [
                  /* @__PURE__ */ jsx(LuX, { className: "w-3 h-3" }),
                  t("pptx.compare.reject")
                ]
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function ComparePanel({
  isOpen,
  compareResult,
  canvasSize,
  onClose,
  onAcceptSlide,
  onRejectSlide,
  onAcceptAll
}) {
  const { t } = useTranslation();
  const [accepted, setAccepted] = useState({});
  const [rejected, setRejected] = useState({});
  const handleAccept = useCallback(
    (index) => {
      setAccepted((p) => ({ ...p, [index]: true }));
      setRejected((p) => {
        const next = { ...p };
        delete next[index];
        return next;
      });
      onAcceptSlide(index);
    },
    [onAcceptSlide]
  );
  const handleReject = useCallback(
    (index) => {
      setRejected((p) => ({ ...p, [index]: true }));
      setAccepted((p) => {
        const next = { ...p };
        delete next[index];
        return next;
      });
      onRejectSlide(index);
    },
    [onRejectSlide]
  );
  const handleAcceptAll = useCallback(() => {
    if (!compareResult) {
      return;
    }
    const acc = {};
    compareResult.diffs.forEach((d, i) => {
      if (d.status !== "unchanged") {
        acc[i] = true;
      }
    });
    setAccepted(acc);
    setRejected({});
    onAcceptAll();
  }, [compareResult, onAcceptAll]);
  if (!isOpen || !compareResult) {
    return null;
  }
  const nonTrivialDiffs = compareResult.diffs.filter((d) => d.status !== "unchanged");
  return /* @__PURE__ */ jsxs("div", { className: "fixed inset-y-0 right-0 z-50 flex flex-col w-[440px] max-w-full border-l border-border bg-popover backdrop-blur-lg shadow-2xl", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-4 py-3 border-b border-border", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("h3", { className: "text-sm font-medium text-foreground", children: t("pptx.compare.title") }),
        /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground mt-0.5", children: t("pptx.compare.summary", {
          added: compareResult.addedCount,
          removed: compareResult.removedCount,
          changed: compareResult.changedCount
        }) })
      ] }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "rounded p-1.5 text-muted-foreground hover:bg-muted hover:text-foreground transition-colors",
          onClick: onClose,
          title: t("pptx.compare.close"),
          children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
        }
      )
    ] }),
    nonTrivialDiffs.length > 0 && /* @__PURE__ */ jsx("div", { className: "px-4 py-2 border-b border-border/60", children: /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "inline-flex items-center gap-1.5 rounded bg-green-700/80 px-3 py-1.5 text-xs text-green-50 hover:bg-green-600 transition-colors",
        onClick: handleAcceptAll,
        children: [
          /* @__PURE__ */ jsx(LuCheck, { className: "w-3.5 h-3.5" }),
          t("pptx.compare.acceptAll")
        ]
      }
    ) }),
    /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-y-auto px-3 py-3 space-y-2", children: nonTrivialDiffs.length === 0 ? /* @__PURE__ */ jsx("div", { className: "text-center py-8 text-xs text-muted-foreground", children: t("pptx.compare.noDifferences") }) : compareResult.diffs.map((diff, i) => /* @__PURE__ */ jsx(
      SlideDiffRow,
      {
        diff,
        diffIndex: i,
        canvasSize,
        accepted: Boolean(accepted[i]),
        rejected: Boolean(rejected[i]),
        onAccept: handleAccept,
        onReject: handleReject
      },
      `diff-${i}-${diff.status}`
    )) })
  ] });
}
function getPasswordStrength(password) {
  if (!password) {
    return 0;
  }
  let score = 0;
  if (password.length >= 8) {
    score++;
  }
  if (password.length >= 12) {
    score++;
  }
  if (/[A-Z]/u.test(password) && /[a-z]/u.test(password)) {
    score++;
  }
  if (/\d/u.test(password)) {
    score++;
  }
  if (/[^A-Za-z0-9]/u.test(password)) {
    score++;
  }
  return Math.min(score, 4);
}
var STRENGTH_COLORS = [
  "bg-red-500",
  "bg-orange-500",
  "bg-yellow-500",
  "bg-lime-500",
  "bg-green-500"
];
function PasswordProtectionDialog({
  isOpen,
  isCurrentlyProtected,
  onClose,
  onSetPassword,
  onRemovePassword
}) {
  const { t } = useTranslation();
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");
  const [showPassword, setShowPassword] = useState(false);
  const [error, setError] = useState("");
  const dialogRef = useRef(null);
  const passwordRef = useRef(null);
  const strength = useMemo(() => getPasswordStrength(password), [password]);
  const strengthLabel = useMemo(() => {
    const labels = [
      t("pptx.security.strengthVeryWeak"),
      t("pptx.security.strengthWeak"),
      t("pptx.security.strengthFair"),
      t("pptx.security.strengthStrong"),
      t("pptx.security.strengthVeryStrong")
    ];
    return password ? labels[strength] : "";
  }, [strength, password, t]);
  const handleSubmit = useCallback(() => {
    setError("");
    if (!password) {
      setError(t("pptx.security.errorPasswordRequired"));
      return;
    }
    if (password !== confirmPassword) {
      setError(t("pptx.security.errorPasswordMismatch"));
      return;
    }
    if (password.length < 4) {
      setError(t("pptx.security.errorPasswordTooShort"));
      return;
    }
    onSetPassword(password);
    setPassword("");
    setConfirmPassword("");
    onClose();
  }, [password, confirmPassword, onSetPassword, onClose, t]);
  const handleRemove = useCallback(() => {
    onRemovePassword();
    setPassword("");
    setConfirmPassword("");
    onClose();
  }, [onRemovePassword, onClose]);
  const handleClose = useCallback(() => {
    setPassword("");
    setConfirmPassword("");
    setError("");
    onClose();
  }, [onClose]);
  useModalFocus(isOpen, dialogRef, handleClose, passwordRef);
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: handleClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            ref: dialogRef,
            role: "dialog",
            "aria-modal": "true",
            "aria-labelledby": "pptx-password-title",
            tabIndex: -1,
            className: "pointer-events-auto w-[420px] overscroll-contain rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
                /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
                  /* @__PURE__ */ jsx(LuLock, { className: "w-5 h-5 text-primary", "aria-hidden": "true" }),
                  /* @__PURE__ */ jsx("h2", { id: "pptx-password-title", className: "text-sm font-semibold text-foreground", children: t("pptx.security.protectPresentation") })
                ] }),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: handleClose,
                    className: "p-1 rounded hover:bg-accent transition-colors",
                    "aria-label": t("pptx.common.close"),
                    children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4 text-muted-foreground", "aria-hidden": "true" })
                  }
                )
              ] }),
              /* @__PURE__ */ jsxs("div", { className: "px-5 py-4 space-y-4", children: [
                isCurrentlyProtected && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 rounded-lg bg-green-900/30 border border-green-700/40 px-3 py-2", children: [
                  /* @__PURE__ */ jsx(LuShieldCheck, { className: "w-4 h-4 text-green-400 shrink-0", "aria-hidden": "true" }),
                  /* @__PURE__ */ jsx("span", { className: "text-xs text-green-300", children: t("pptx.security.currentlyProtected") })
                ] }),
                /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", children: t("pptx.security.description") }),
                /* @__PURE__ */ jsxs("div", { children: [
                  /* @__PURE__ */ jsx("label", { htmlFor: "pptx-password", className: "block text-xs text-foreground mb-1", children: t("pptx.security.password") }),
                  /* @__PURE__ */ jsxs("div", { className: "relative", children: [
                    /* @__PURE__ */ jsx(
                      "input",
                      {
                        ref: passwordRef,
                        id: "pptx-password",
                        name: "presentation-password",
                        autoComplete: "new-password",
                        "aria-invalid": Boolean(error),
                        "aria-describedby": error ? "pptx-password-error" : "pptx-password-strength",
                        type: showPassword ? "text" : "password",
                        className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                        placeholder: t("pptx.security.passwordPlaceholder"),
                        value: password,
                        onChange: (e) => {
                          setPassword(e.target.value);
                          setError("");
                        }
                      }
                    ),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: () => setShowPassword(!showPassword),
                        className: "absolute right-2 top-1/2 -translate-y-1/2 p-1 text-muted-foreground hover:text-foreground",
                        "aria-label": showPassword ? t("pptx.security.hidePassword") : t("pptx.security.showPassword"),
                        children: showPassword ? /* @__PURE__ */ jsx(LuEyeOff, { className: "w-4 h-4", "aria-hidden": "true" }) : /* @__PURE__ */ jsx(LuEye, { className: "w-4 h-4", "aria-hidden": "true" })
                      }
                    )
                  ] })
                ] }),
                password && /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
                  /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: Array.from({ length: 5 }).map((_, i) => /* @__PURE__ */ jsx(
                    "div",
                    {
                      className: `h-1 flex-1 rounded-full transition-colors ${i <= strength ? STRENGTH_COLORS[strength] : "bg-accent"}`
                    },
                    `str-${i}`
                  )) }),
                  /* @__PURE__ */ jsx(
                    "p",
                    {
                      id: "pptx-password-strength",
                      className: "text-[11px] text-muted-foreground",
                      role: "status",
                      "aria-live": "polite",
                      children: strengthLabel
                    }
                  )
                ] }),
                /* @__PURE__ */ jsxs("div", { children: [
                  /* @__PURE__ */ jsx("label", { htmlFor: "pptx-password-confirm", className: "block text-xs text-foreground mb-1", children: t("pptx.security.confirmPassword") }),
                  /* @__PURE__ */ jsx(
                    "input",
                    {
                      id: "pptx-password-confirm",
                      name: "presentation-password-confirmation",
                      autoComplete: "new-password",
                      "aria-invalid": Boolean(error),
                      "aria-describedby": error ? "pptx-password-error" : void 0,
                      type: showPassword ? "text" : "password",
                      className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                      placeholder: t("pptx.security.confirmPasswordPlaceholder"),
                      value: confirmPassword,
                      onChange: (e) => {
                        setConfirmPassword(e.target.value);
                        setError("");
                      }
                    }
                  )
                ] }),
                error && /* @__PURE__ */ jsx("p", { id: "pptx-password-error", className: "text-xs text-red-400", role: "alert", children: error })
              ] }),
              /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-3 border-t border-border/60", children: [
                /* @__PURE__ */ jsx("div", { children: isCurrentlyProtected && /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: handleRemove,
                    className: "text-xs text-red-400 hover:text-red-300 transition-colors",
                    children: t("pptx.security.removePassword")
                  }
                ) }),
                /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      onClick: handleClose,
                      className: "px-3 py-1.5 text-xs rounded-lg border border-border text-foreground hover:bg-muted transition-colors",
                      children: t("pptx.common.cancel")
                    }
                  ),
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      onClick: handleSubmit,
                      className: "px-3 py-1.5 text-xs rounded-lg bg-primary text-white hover:bg-primary/80 transition-colors",
                      children: isCurrentlyProtected ? t("pptx.security.updatePassword") : t("pptx.security.setPassword")
                    }
                  )
                ] })
              ] })
            ]
          }
        )
      }
    )
  ] });
}
var CUSTOM_PROPERTY_TYPES = [
  { value: "lpwstr", labelKey: "pptx.documentProperties.custom.typeText" },
  { value: "i4", labelKey: "pptx.documentProperties.custom.typeNumber" },
  { value: "filetime", labelKey: "pptx.documentProperties.custom.typeDate" },
  { value: "bool", labelKey: "pptx.documentProperties.custom.typeYesNo" }
];
function DocumentPropertiesCustomTab({
  customProperties,
  onUpdate
}) {
  const { t } = useTranslation();
  const handleAdd = useCallback(() => {
    onUpdate([...customProperties, { name: "", value: "", type: "lpwstr" }]);
  }, [customProperties, onUpdate]);
  const handleDelete = useCallback(
    (index) => {
      onUpdate(customProperties.filter((_, i) => i !== index));
    },
    [customProperties, onUpdate]
  );
  const handleChangeName = useCallback(
    (index, name) => {
      onUpdate(customProperties.map((prop, i) => i === index ? { ...prop, name } : prop));
    },
    [customProperties, onUpdate]
  );
  const handleChangeValue = useCallback(
    (index, value) => {
      onUpdate(customProperties.map((prop, i) => i === index ? { ...prop, value } : prop));
    },
    [customProperties, onUpdate]
  );
  const handleChangeType = useCallback(
    (index, type) => {
      onUpdate(customProperties.map((prop, i) => i === index ? { ...prop, type } : prop));
    },
    [customProperties, onUpdate]
  );
  return /* @__PURE__ */ jsxs("div", { className: "space-y-3", children: [
    /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", children: t("pptx.documentProperties.custom.description") }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[1fr_1fr_100px_32px] gap-1 text-[11px] font-medium text-muted-foreground px-1", children: [
      /* @__PURE__ */ jsx("span", { children: t("pptx.documentProperties.custom.name") }),
      /* @__PURE__ */ jsx("span", { children: t("pptx.documentProperties.custom.value") }),
      /* @__PURE__ */ jsx("span", { children: t("pptx.documentProperties.custom.type") }),
      /* @__PURE__ */ jsx("span", {})
    ] }),
    /* @__PURE__ */ jsx("div", { className: "max-h-[240px] overflow-y-auto space-y-1", children: customProperties.map((prop, index) => /* @__PURE__ */ jsxs(
      "div",
      {
        className: "grid grid-cols-[1fr_1fr_100px_32px] gap-1 items-center",
        children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "text",
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              placeholder: t("pptx.documentProperties.custom.namePlaceholder"),
              value: prop.name,
              onChange: (e) => handleChangeName(index, e.target.value)
            }
          ),
          prop.type === "bool" ? /* @__PURE__ */ jsxs(
            "select",
            {
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              value: prop.value,
              onChange: (e) => handleChangeValue(index, e.target.value),
              children: [
                /* @__PURE__ */ jsx("option", { value: "true", children: t("pptx.documentProperties.custom.yes") }),
                /* @__PURE__ */ jsx("option", { value: "false", children: t("pptx.documentProperties.custom.no") })
              ]
            }
          ) : /* @__PURE__ */ jsx(
            "input",
            {
              type: prop.type === "i4" ? "number" : "text",
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              placeholder: t("pptx.documentProperties.custom.valuePlaceholder"),
              value: prop.value,
              onChange: (e) => handleChangeValue(index, e.target.value)
            }
          ),
          /* @__PURE__ */ jsx(
            "select",
            {
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              value: prop.type,
              onChange: (e) => handleChangeType(index, e.target.value),
              children: CUSTOM_PROPERTY_TYPES.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: () => handleDelete(index),
              className: "p-1 rounded hover:bg-red-500/20 text-muted-foreground hover:text-red-400 transition-colors",
              "aria-label": t("pptx.documentProperties.custom.deleteProperty"),
              children: /* @__PURE__ */ jsx(LuTrash2, { className: "w-3.5 h-3.5" })
            }
          )
        ]
      },
      `custom-prop-${index}`
    )) }),
    customProperties.length === 0 && /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground/60 text-center py-4", children: t("pptx.documentProperties.custom.empty") }),
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: handleAdd,
        className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 text-xs rounded-lg border border-border text-foreground hover:bg-muted transition-colors",
        children: [
          /* @__PURE__ */ jsx(LuPlus, { className: "w-3.5 h-3.5" }),
          t("pptx.documentProperties.custom.addProperty")
        ]
      }
    )
  ] });
}
var STATISTICS_FIELDS = [
  {
    key: "application",
    labelKey: "pptx.documentProperties.statistics.application",
    editable: false
  },
  {
    key: "appVersion",
    labelKey: "pptx.documentProperties.statistics.appVersion",
    editable: false
  },
  {
    key: "presentationFormat",
    labelKey: "pptx.documentProperties.statistics.presentationFormat",
    editable: false
  },
  {
    key: "slides",
    labelKey: "pptx.documentProperties.statistics.slides",
    editable: false
  },
  {
    key: "hiddenSlides",
    labelKey: "pptx.documentProperties.statistics.hiddenSlides",
    editable: false
  },
  {
    key: "notes",
    labelKey: "pptx.documentProperties.statistics.notes",
    editable: false
  },
  {
    key: "totalTime",
    labelKey: "pptx.documentProperties.statistics.totalTime",
    editable: false
  },
  {
    key: "words",
    labelKey: "pptx.documentProperties.statistics.words",
    editable: false
  },
  {
    key: "paragraphs",
    labelKey: "pptx.documentProperties.statistics.paragraphs",
    editable: false
  },
  {
    key: "template",
    labelKey: "pptx.documentProperties.statistics.template",
    editable: false
  },
  {
    key: "company",
    labelKey: "pptx.documentProperties.summary.company",
    editable: true
  },
  {
    key: "manager",
    labelKey: "pptx.documentProperties.summary.manager",
    editable: true
  }
];
function formatTotalTime(minutes, t) {
  if (minutes === void 0 || minutes === null) {
    return t("pptx.documentProperties.statistics.notAvailable");
  }
  const hours = Math.floor(minutes / 60);
  const remainingMinutes = minutes % 60;
  return t("pptx.documentProperties.statistics.hoursMinutes", {
    hours,
    minutes: remainingMinutes
  });
}
function formatFieldValue(key, value, t) {
  if (key === "totalTime") {
    return formatTotalTime(value, t);
  }
  if (value === void 0 || value === null) {
    return t("pptx.documentProperties.statistics.notAvailable");
  }
  if (typeof value === "boolean") {
    return t(value ? "pptx.documentProperties.custom.yes" : "pptx.documentProperties.custom.no");
  }
  return String(value);
}
function DocumentPropertiesStatisticsTab({
  appProperties,
  onUpdateAppProperties,
  canEdit
}) {
  const { t } = useTranslation();
  const handleEditableChange = useCallback(
    (key, value) => {
      onUpdateAppProperties({ [key]: value });
    },
    [onUpdateAppProperties]
  );
  return /* @__PURE__ */ jsx("div", { className: "space-y-3", children: STATISTICS_FIELDS.map(({ key, labelKey, editable }) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3", children: [
    /* @__PURE__ */ jsx("label", { className: "block text-xs text-foreground w-[140px] shrink-0", children: t(labelKey) }),
    editable && canEdit ? /* @__PURE__ */ jsx(
      "input",
      {
        type: "text",
        className: "flex-1 rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
        value: String(appProperties[key] ?? ""),
        onChange: (e) => handleEditableChange(key, e.target.value)
      }
    ) : /* @__PURE__ */ jsx("span", { className: "flex-1 px-3 py-2 text-sm text-muted-foreground", children: formatFieldValue(key, appProperties[key], t) })
  ] }, key)) });
}
var SUMMARY_FIELDS = [
  { key: "title", labelKey: "pptx.properties.titleLabel" },
  { key: "subject", labelKey: "pptx.properties.subject" },
  { key: "creator", labelKey: "pptx.properties.author" },
  { key: "keywords", labelKey: "pptx.properties.keywords" },
  {
    key: "description",
    labelKey: "pptx.documentProperties.summary.description",
    multiline: true
  },
  { key: "category", labelKey: "pptx.documentProperties.summary.category" }
];
function DocumentPropertiesDialog({
  isOpen,
  coreProperties,
  customProperties,
  appProperties,
  onClose,
  onSave
}) {
  const { t } = useTranslation();
  const [activeTab, setActiveTab] = useState("summary");
  const [draftCore, setDraftCore] = useState({});
  const [draftCustom, setDraftCustom] = useState([]);
  const [draftApp, setDraftApp] = useState({});
  const [prevOpen, setPrevOpen] = useState(false);
  if (isOpen && !prevOpen) {
    setDraftCore({ ...coreProperties });
    setDraftCustom(customProperties.map((p) => ({ ...p })));
    setDraftApp({
      company: appProperties?.company,
      manager: appProperties?.manager
    });
    setActiveTab("summary");
  }
  if (isOpen !== prevOpen) {
    setPrevOpen(isOpen);
  }
  const handleCoreFieldChange = useCallback((key, value) => {
    setDraftCore((prev) => ({ ...prev, [key]: value }));
  }, []);
  const isDirty = useMemo(() => {
    const coreChanged = SUMMARY_FIELDS.some(
      ({ key }) => (draftCore[key] ?? "") !== (coreProperties[key] ?? "")
    );
    if (coreChanged) {
      return true;
    }
    const appCompanyChanged = (draftApp.company ?? "") !== (appProperties?.company ?? "");
    const appManagerChanged = (draftApp.manager ?? "") !== (appProperties?.manager ?? "");
    if (appCompanyChanged || appManagerChanged) {
      return true;
    }
    if (draftCustom.length !== customProperties.length) {
      return true;
    }
    return draftCustom.some(
      (p, i) => p.name !== customProperties[i]?.name || p.value !== customProperties[i]?.value || p.type !== customProperties[i]?.type
    );
  }, [draftCore, draftCustom, draftApp, coreProperties, customProperties, appProperties]);
  const handleUpdateDraftApp = useCallback((updates) => {
    setDraftApp((prev) => ({ ...prev, ...updates }));
  }, []);
  const handleSave = useCallback(() => {
    const appChanged = (draftApp.company ?? "") !== (appProperties?.company ?? "") || (draftApp.manager ?? "") !== (appProperties?.manager ?? "");
    onSave(draftCore, draftCustom, appChanged ? draftApp : void 0);
    onClose();
  }, [draftCore, draftCustom, draftApp, appProperties, onSave, onClose]);
  const handleClose = useCallback(() => {
    onClose();
  }, [onClose]);
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(handleClose);
  if (!isOpen) {
    return null;
  }
  const TABS2 = [
    { id: "summary", labelKey: "pptx.documentProperties.tabs.general" },
    { id: "custom", labelKey: "pptx.documentProperties.tabs.custom" },
    { id: "statistics", labelKey: "pptx.documentProperties.tabs.statistics" }
  ];
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: handleClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            style: panelStyle,
            className: "pointer-events-auto w-[520px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-5 py-4 border-b border-border/60 touch-none",
                  children: [
                    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
                      /* @__PURE__ */ jsx(LuFileText, { className: "w-5 h-5 text-primary" }),
                      /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.documentProperties.dialogTitle") })
                    ] }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: handleClose,
                        className: "p-1 rounded hover:bg-accent transition-colors",
                        "aria-label": t("pptx.common.close"),
                        children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4 text-muted-foreground" })
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsx("div", { className: "flex border-b border-border/60", children: TABS2.map((tab) => /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  onClick: () => setActiveTab(tab.id),
                  className: `px-4 py-2 text-xs font-medium transition-colors ${activeTab === tab.id ? "text-primary border-b-2 border-primary" : "text-muted-foreground hover:text-foreground"}`,
                  children: t(tab.labelKey)
                },
                tab.id
              )) }),
              /* @__PURE__ */ jsxs("div", { className: "px-5 py-4 min-h-[280px]", children: [
                activeTab === "summary" && /* @__PURE__ */ jsx("div", { className: "space-y-3", children: SUMMARY_FIELDS.map(({ key, labelKey, multiline }) => /* @__PURE__ */ jsxs("div", { children: [
                  /* @__PURE__ */ jsx("label", { className: "block text-xs text-foreground mb-1", children: t(labelKey) }),
                  multiline ? /* @__PURE__ */ jsx(
                    "textarea",
                    {
                      className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none resize-none",
                      rows: 3,
                      value: draftCore[key] ?? "",
                      onChange: (e) => handleCoreFieldChange(key, e.target.value)
                    }
                  ) : /* @__PURE__ */ jsx(
                    "input",
                    {
                      type: "text",
                      className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                      value: draftCore[key] ?? "",
                      onChange: (e) => handleCoreFieldChange(key, e.target.value)
                    }
                  )
                ] }, key)) }),
                activeTab === "custom" && /* @__PURE__ */ jsx(
                  DocumentPropertiesCustomTab,
                  {
                    customProperties: draftCustom,
                    onUpdate: setDraftCustom
                  }
                ),
                activeTab === "statistics" && /* @__PURE__ */ jsx(
                  DocumentPropertiesStatisticsTab,
                  {
                    appProperties: {
                      ...appProperties,
                      company: draftApp.company,
                      manager: draftApp.manager
                    },
                    onUpdateAppProperties: handleUpdateDraftApp,
                    canEdit: true
                  }
                )
              ] }),
              /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 px-5 py-3 border-t border-border/60", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: handleClose,
                    className: "px-3 py-1.5 text-xs rounded-lg border border-border text-foreground hover:bg-muted transition-colors",
                    children: t("pptx.common.cancel")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: handleSave,
                    disabled: !isDirty,
                    className: "px-3 py-1.5 text-xs rounded-lg bg-primary text-white hover:bg-primary/80 transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
                    children: t("pptx.common.save")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function FontEmbeddingPanel({
  isOpen,
  embedFontsEnabled,
  usedFontFamilies,
  embeddedFonts,
  canEmbedFonts = true,
  embedUnavailableKey,
  onClose,
  onToggleEmbedFonts
}) {
  const { t } = useTranslation();
  const [availableFamilies, setAvailableFamilies] = useState(/* @__PURE__ */ new Set());
  const [scanning, setScanning] = useState(false);
  const [scanned, setScanned] = useState(false);
  const scanFonts = useCallback(async () => {
    setScanning(true);
    try {
      setAvailableFamilies(await scanAvailableFontFamilies(usedFontFamilies));
      setScanned(true);
    } catch {
    } finally {
      setScanning(false);
    }
  }, [usedFontFamilies]);
  useEffect(() => {
    if (isOpen && !scanned) {
      void scanFonts();
    }
  }, [isOpen, scanned, scanFonts]);
  if (!isOpen) {
    return null;
  }
  const missingFamilies = usedFontFamilies.filter((f) => !availableFamilies.has(f));
  const embeddedSet = new Set(embeddedFonts);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs("div", { className: "pointer-events-auto w-[460px] max-h-[80vh] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60 shrink-0", children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsx(LuType, { className: "w-5 h-5 text-primary" }),
              /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.fonts.embedFonts") })
            ] }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "px-5 py-4 space-y-4 overflow-y-auto flex-1", children: [
            /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", children: t("pptx.fonts.embedDescription") }),
            /* @__PURE__ */ jsxs(
              "label",
              {
                className: `flex items-center gap-3 ${canEmbedFonts ? "cursor-pointer" : "cursor-not-allowed opacity-60"}`,
                children: [
                  /* @__PURE__ */ jsxs("div", { className: "relative", children: [
                    /* @__PURE__ */ jsx(
                      "input",
                      {
                        type: "checkbox",
                        className: "sr-only",
                        checked: embedFontsEnabled,
                        disabled: !canEmbedFonts,
                        onChange: (e) => onToggleEmbedFonts(e.target.checked)
                      }
                    ),
                    /* @__PURE__ */ jsx(
                      "div",
                      {
                        className: `w-9 h-5 rounded-full transition-colors ${embedFontsEnabled ? "bg-primary" : "bg-muted-foreground"}`
                      }
                    ),
                    /* @__PURE__ */ jsx(
                      "div",
                      {
                        className: `absolute top-0.5 left-0.5 w-4 h-4 rounded-full bg-white transition-transform ${embedFontsEnabled ? "translate-x-4" : ""}`
                      }
                    )
                  ] }),
                  /* @__PURE__ */ jsx("span", { className: "text-xs text-foreground", children: t("pptx.fonts.enableEmbedding") })
                ]
              }
            ),
            /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: canEmbedFonts ? t("pptx.fonts.embedKeepsExisting") : t(embedUnavailableKey ?? "pptx.fonts.embedUnavailable") }),
            /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
              /* @__PURE__ */ jsxs("h3", { className: "text-xs font-medium text-foreground", children: [
                t("pptx.fonts.usedFonts"),
                " (",
                usedFontFamilies.length,
                ")"
              ] }),
              scanning ? /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 py-4 justify-center", children: [
                /* @__PURE__ */ jsx(LuLoader, { className: "w-4 h-4 text-muted-foreground animate-spin" }),
                /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: t("pptx.fonts.scanning") })
              ] }) : /* @__PURE__ */ jsx("div", { className: "space-y-1 max-h-[280px] overflow-y-auto", children: usedFontFamilies.map((family) => {
                const found = availableFamilies.has(family);
                const embedded = embeddedSet.has(family);
                return /* @__PURE__ */ jsxs(
                  "div",
                  {
                    className: "flex items-center justify-between px-3 py-2 rounded-lg bg-muted/60",
                    children: [
                      /* @__PURE__ */ jsx("span", { className: "text-xs text-foreground", children: family }),
                      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
                        embedded && /* @__PURE__ */ jsx("span", { className: "text-[10px] px-1.5 py-0.5 rounded bg-green-900/40 text-green-400 border border-green-700/40", children: t("pptx.fonts.embedded") }),
                        found ? /* @__PURE__ */ jsx(LuCheck, { className: "w-3.5 h-3.5 text-green-400" }) : /* @__PURE__ */ jsx("span", { className: "text-[10px] text-yellow-400", children: t("pptx.fonts.notFound") })
                      ] })
                    ]
                  },
                  family
                );
              }) })
            ] }),
            missingFamilies.length > 0 && !scanning && /* @__PURE__ */ jsx("p", { className: "text-[11px] text-yellow-400/80", children: t("pptx.fonts.missingWarning", {
              count: missingFamilies.length
            }) })
          ] }),
          /* @__PURE__ */ jsx("div", { className: "flex items-center justify-end px-5 py-3 border-t border-border/60 shrink-0", children: /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 text-xs rounded-lg bg-primary text-white hover:bg-primary/80 transition-colors",
              children: t("pptx.common.done")
            }
          ) })
        ] })
      }
    )
  ] });
}
function DigitalSignaturesDialog({
  isOpen,
  onClose,
  hasSignatures,
  signatureCount
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs("div", { className: "pointer-events-auto w-[420px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
              hasSignatures ? /* @__PURE__ */ jsx(LuShieldCheck, { className: "w-5 h-5 text-green-400" }) : /* @__PURE__ */ jsx(LuShieldAlert, { className: "w-5 h-5 text-amber-400" }),
              /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.digitalSignatures.title") })
            ] }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          /* @__PURE__ */ jsx("div", { className: "px-5 py-6 space-y-4", children: hasSignatures ? /* @__PURE__ */ jsxs(Fragment, { children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-start gap-3 rounded-lg bg-green-900/20 border border-green-700/30 px-4 py-3", children: [
              /* @__PURE__ */ jsx(LuSignature, { className: "w-5 h-5 text-green-400 shrink-0 mt-0.5" }),
              /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
                /* @__PURE__ */ jsx("p", { className: "text-xs text-green-200", children: t("pptx.digitalSignatures.signed") }),
                /* @__PURE__ */ jsx("p", { className: "text-[11px] text-green-300/70", children: t("pptx.digitalSignatures.signatureCount", {
                  count: signatureCount
                }) })
              ] })
            ] }),
            /* @__PURE__ */ jsxs("div", { className: "flex items-start gap-3 rounded-lg bg-amber-900/20 border border-amber-700/30 px-4 py-3", children: [
              /* @__PURE__ */ jsx(LuInfo, { className: "w-5 h-5 text-amber-400 shrink-0 mt-0.5" }),
              /* @__PURE__ */ jsx("p", { className: "text-xs text-amber-200", children: t("pptx.digitalSignatures.editWarning") })
            ] })
          ] }) : /* @__PURE__ */ jsx(Fragment, { children: /* @__PURE__ */ jsxs("div", { className: "flex items-start gap-3 rounded-lg bg-muted/30 border border-border/40 px-4 py-3", children: [
            /* @__PURE__ */ jsx(LuInfo, { className: "w-5 h-5 text-muted-foreground shrink-0 mt-0.5" }),
            /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", children: t("pptx.digitalSignatures.noSignatures") })
          ] }) }) }),
          /* @__PURE__ */ jsx("div", { className: "flex items-center justify-end px-5 py-3 border-t border-border/60", children: /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 text-xs rounded-lg bg-accent text-foreground hover:bg-accent/80 transition-colors",
              children: t("pptx.common.close")
            }
          ) })
        ] })
      }
    )
  ] });
}
function SignatureStatusBadge({
  hasSignatures,
  signatureCount,
  onClick
}) {
  const { t } = useTranslation();
  if (!hasSignatures || signatureCount === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      onClick,
      className: "inline-flex items-center gap-1 px-2 py-0.5 text-[11px] font-medium rounded-md bg-green-900/30 border border-green-700/40 text-green-300 hover:bg-green-900/50 transition-colors",
      title: t("pptx.digitalSignatures.signatureCount", {
        count: signatureCount
      }),
      children: [
        /* @__PURE__ */ jsx(LuShieldCheck, { className: "w-3.5 h-3.5" }),
        /* @__PURE__ */ jsx("span", { children: t("pptx.digitalSignatures.badge") })
      ]
    }
  );
}
function SignatureStrippedDialog({
  isOpen,
  signatureCount,
  onConfirm,
  onCancel
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onCancel
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs("div", { className: "pointer-events-auto w-[440px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsx(LuTriangleAlert, { className: "w-5 h-5 text-amber-400" }),
              /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.digitalSignatures.strippedTitle") })
            ] }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onCancel,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          /* @__PURE__ */ jsx("div", { className: "px-5 py-6", children: /* @__PURE__ */ jsxs("div", { className: "flex items-start gap-3 rounded-lg bg-amber-900/20 border border-amber-700/30 px-4 py-3", children: [
            /* @__PURE__ */ jsx(LuTriangleAlert, { className: "w-5 h-5 text-amber-400 shrink-0 mt-0.5" }),
            /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsx("p", { className: "text-xs text-amber-200", children: t("pptx.digitalSignatures.strippedMessage", {
                count: signatureCount
              }) }),
              /* @__PURE__ */ jsx("p", { className: "text-[11px] text-amber-300/70", children: t("pptx.digitalSignatures.editWarning") })
            ] })
          ] }) }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 px-5 py-3 border-t border-border/60", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onCancel,
                className: "px-3 py-1.5 text-xs rounded-lg bg-accent text-foreground hover:bg-accent/80 transition-colors",
                children: t("pptx.common.cancel")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onConfirm,
                className: "px-3 py-1.5 text-xs rounded-lg bg-amber-600 text-white hover:bg-amber-500 transition-colors",
                children: t("pptx.digitalSignatures.strippedConfirm")
              }
            )
          ] })
        ] })
      }
    )
  ] });
}
function EncryptedFileDialog({
  isOpen,
  onClose
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs("div", { className: "pointer-events-auto w-[420px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsx(LuLock, { className: "w-5 h-5 text-red-400" }),
              /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.encryptedFile.title") })
            ] }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          /* @__PURE__ */ jsx("div", { className: "px-5 py-6 space-y-4", children: /* @__PURE__ */ jsxs("div", { className: "flex items-start gap-3 rounded-lg bg-red-900/20 border border-red-700/30 px-4 py-3", children: [
            /* @__PURE__ */ jsx(LuInfo, { className: "w-5 h-5 text-red-400 shrink-0 mt-0.5" }),
            /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsx("p", { className: "text-xs text-red-200", children: t("pptx.encryptedFile.message") }),
              /* @__PURE__ */ jsx("p", { className: "text-[11px] text-red-300/70", children: t("pptx.encryptedFile.instructions") })
            ] })
          ] }) }),
          /* @__PURE__ */ jsx("div", { className: "flex items-center justify-end px-5 py-3 border-t border-border/60", children: /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 text-xs rounded-lg bg-accent text-foreground hover:bg-accent/80 transition-colors",
              children: t("pptx.common.close")
            }
          ) })
        ] })
      }
    )
  ] });
}
function PresenterSlideFrame({
  canvasSize,
  zoomScale = 1,
  zoomOriginX = 0.5,
  zoomOriginY = 0.5,
  inkProps,
  children
}) {
  const paneRef = useRef(null);
  const [pane, setPane] = useState(null);
  useEffect(() => {
    const el = paneRef.current;
    if (!el) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      const rect = entries[0]?.contentRect;
      if (rect && rect.width > 0 && rect.height > 0) {
        setPane({ width: rect.width, height: rect.height });
      }
    });
    observer.observe(el);
    const initial = el.getBoundingClientRect();
    if (initial.width > 0 && initial.height > 0) {
      setPane({ width: initial.width, height: initial.height });
    }
    return () => {
      observer.disconnect();
    };
  }, []);
  const safeWidth = Math.max(canvasSize.width, 1);
  const safeHeight = Math.max(canvasSize.height, 1);
  const fittedWidth = pane ? Math.max(1, Math.min(pane.width, pane.height * safeWidth / safeHeight)) : 0;
  return /* @__PURE__ */ jsx("div", { ref: paneRef, className: "flex min-h-0 w-full flex-1 items-center justify-center", children: /* @__PURE__ */ jsx(
    "div",
    {
      className: "relative transition-transform duration-200",
      style: {
        width: fittedWidth || void 0,
        transform: `scale(${zoomScale})`,
        transformOrigin: `${zoomOriginX * 100}% ${zoomOriginY * 100}%`,
        touchAction: "none"
      },
      ...inkProps,
      children: fittedWidth > 0 && children
    }
  ) });
}
function PresentationAudienceEffects({ snapshot }) {
  const strokes = snapshot.inkStrokes?.filter((stroke) => stroke.slideIndex === snapshot.slideIndex) ?? [];
  return /* @__PURE__ */ jsxs("div", { className: "pointer-events-none absolute inset-0 z-[75] overflow-hidden", children: [
    snapshot.blackout !== "none" && /* @__PURE__ */ jsx(
      "div",
      {
        "data-pptx-blackout": true,
        className: `absolute inset-0 ${snapshot.blackout === "black" ? "bg-black" : "bg-white"}`
      }
    ),
    /* @__PURE__ */ jsx(
      "svg",
      {
        className: "absolute inset-0 h-full w-full",
        viewBox: "0 0 100 100",
        preserveAspectRatio: "none",
        "aria-hidden": "true",
        children: strokes.map((stroke) => /* @__PURE__ */ jsx(
          "path",
          {
            d: presentationInkPath(stroke.points),
            fill: "none",
            stroke: stroke.color,
            strokeWidth: stroke.width / 10,
            strokeLinecap: "round",
            strokeLinejoin: "round",
            opacity: stroke.tool === "highlighter" ? 0.42 : 1
          },
          stroke.id
        ))
      }
    ),
    snapshot.pointer?.tool === "laser" && /* @__PURE__ */ jsx(
      "span",
      {
        className: "absolute h-5 w-5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-500 shadow-[0_0_20px_8px_rgba(239,68,68,.55)]",
        style: { left: `${snapshot.pointer.x * 100}%`, top: `${snapshot.pointer.y * 100}%` }
      }
    ),
    snapshot.subtitlesVisible && snapshot.caption && /* @__PURE__ */ jsx("div", { className: "absolute inset-x-[10%] bottom-8 rounded-lg bg-black/80 px-6 py-3 text-center text-xl font-medium text-white", children: snapshot.caption })
  ] });
}
function PresentationEndOverlay({ onExit }) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(
    "button",
    {
      type: "button",
      "data-pptx-end-of-show": "",
      onClick: onExit,
      className: "absolute inset-0 z-[90] flex cursor-default items-start bg-black text-left",
      children: /* @__PURE__ */ jsx("span", { className: "px-4 py-3 text-[12px] text-white/70", children: t("pptx.presentation.endOfSlideShow", "End of slide show, click to exit.") })
    }
  );
}
function PresentationSubtitleBar({
  visible,
  onCaptionChange
}) {
  const { t } = useTranslation();
  const [captionText, setCaptionText] = useState("");
  const [supportState, setSupportState] = useState(
    "unknown"
  );
  const recognitionRef = useRef(null);
  const shouldRunRef = useRef(false);
  useEffect(() => {
    if (!visible) {
      shouldRunRef.current = false;
      recognitionRef.current?.stop();
      recognitionRef.current = null;
      setCaptionText("");
      return;
    }
    shouldRunRef.current = true;
    const speechWindow = window;
    const RecognitionCtor = speechWindow.SpeechRecognition ?? speechWindow.webkitSpeechRecognition;
    if (!RecognitionCtor) {
      setSupportState("unsupported");
      return;
    }
    setSupportState("supported");
    const recognition = new RecognitionCtor();
    recognition.continuous = true;
    recognition.interimResults = true;
    recognition.lang = navigator.language || "en-US";
    recognition.onresult = (event) => {
      let finalText = "";
      let interimText = "";
      for (let index = event.resultIndex; index < event.results.length; index += 1) {
        const result = event.results[index];
        const fragment = result?.[0]?.transcript ?? "";
        if (result?.isFinal) {
          finalText += fragment;
        } else {
          interimText += fragment;
        }
      }
      const merged = `${finalText} ${interimText}`.trim();
      if (merged.length > 0) {
        setCaptionText(merged);
        onCaptionChange?.(merged);
      }
    };
    recognition.onerror = () => {
    };
    recognition.onend = () => {
      if (!shouldRunRef.current) {
        return;
      }
      try {
        recognition.start();
      } catch {
      }
    };
    recognitionRef.current = recognition;
    try {
      recognition.start();
    } catch {
      setSupportState("unsupported");
    }
    return () => {
      shouldRunRef.current = false;
      recognition.stop();
      recognitionRef.current = null;
    };
  }, [visible, onCaptionChange]);
  if (!visible) {
    return null;
  }
  const renderedText = supportState === "unsupported" ? t("pptx.subtitles.notSupported") : captionText.length > 0 ? captionText : t("pptx.subtitles.listening");
  return /* @__PURE__ */ jsx("div", { className: "absolute bottom-14 left-1/2 -translate-x-1/2 z-[70] max-w-[80%] min-w-[300px]", children: /* @__PURE__ */ jsx("div", { className: "px-6 py-3 rounded-lg bg-black/75 backdrop-blur-sm border border-white/10", children: /* @__PURE__ */ jsx("p", { className: "text-center text-[15px] text-white/70 italic", children: renderedText }) }) });
}
function renderNotesSegments(segments) {
  return notesSegmentsToSpans(segments).map((span) => {
    if (span.kind === "break") {
      return React20.createElement("br", { key: span.key });
    }
    return React20.createElement(
      "span",
      { key: span.key, style: span.style },
      span.text
    );
  });
}
var CONSOLE_ICONS = {
  "arrow-left-right": LuArrowLeftRight,
  captions: LuCaptions,
  "circle-pause": LuCirclePause,
  "circle-play": LuCirclePlay,
  eraser: LuEraser,
  "grid-2x2": LuGrid2X2,
  highlighter: LuHighlighter,
  monitor: LuMonitor,
  "monitor-off": LuMonitorOff,
  "mouse-pointer-2": LuMousePointer2,
  "pen-tool": LuPenTool,
  "rotate-ccw": LuRotateCcw,
  scan: LuScan,
  x: LuX,
  "zoom-in": LuZoomIn,
  "zoom-out": LuZoomOut
};
var POINTER_TOOLS = {
  laser: "laser",
  pen: "pen",
  highlighter: "highlighter",
  eraser: "eraser"
};
function slotState(control, props) {
  const snapshot = props.snapshot;
  const pointerTool = snapshot.pointer?.tool ?? "none";
  const tool = POINTER_TOOLS[control.id];
  if (tool) {
    return {
      active: pointerTool === tool,
      onClick: () => props.onPointerTool(pointerTool === tool ? "none" : tool)
    };
  }
  switch (control.id) {
    case "timer-toggle":
      return { active: Boolean(snapshot.paused), onClick: props.onToggleTimer };
    case "timer-reset":
      return { active: false, onClick: props.onResetTimer };
    case "all-slides":
      return { active: false, onClick: props.onShowSlides };
    case "zoom-in":
      return { active: (snapshot.zoom?.scale ?? 1) > 1, onClick: () => props.onStepZoom(1) };
    case "zoom-out":
      return { active: false, onClick: () => props.onStepZoom(-1) };
    case "zoom-reset":
      return { active: false, onClick: props.onResetZoom };
    case "blackout-black":
      return {
        active: snapshot.blackout === "black",
        onClick: () => props.onBlackout(snapshot.blackout === "black" ? "none" : "black")
      };
    case "blackout-white":
      return {
        active: snapshot.blackout === "white",
        onClick: () => props.onBlackout(snapshot.blackout === "white" ? "none" : "white")
      };
    case "captions":
      return { active: Boolean(snapshot.subtitlesVisible), onClick: props.onToggleSubtitles };
    case "audience":
      return { active: props.audienceOpen, onClick: props.onToggleAudience };
    case "swap-displays":
      return { active: false, onClick: props.onSwapDisplays, disabled: !props.audienceOpen };
    case "end":
      return { active: false, onClick: props.onExit };
    default:
      return { active: false, onClick: () => void 0 };
  }
}
function PresenterConsoleToolbar(props) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx("div", { className: PRESENTER_CONSOLE_CLASSES.strip, "data-pptx-presenter-toolbar": true, children: PRESENTER_CONSOLE_CONTROLS.map((control) => {
    if (control.kind === "divider" || control.kind === "spacer") {
      return /* @__PURE__ */ jsx(
        "span",
        {
          "data-pptx-presenter-control": control.id,
          className: control.kind === "divider" ? PRESENTER_CONSOLE_CLASSES.divider : PRESENTER_CONSOLE_CLASSES.spacer
        },
        control.id
      );
    }
    const state = slotState(control, props);
    const labelKey = state.active && control.activeLabelKey ? control.activeLabelKey : control.labelKey;
    const iconName = state.active && control.activeIcon ? control.activeIcon : control.icon;
    const Icon = iconName === void 0 ? void 0 : CONSOLE_ICONS[iconName];
    const label = labelKey === void 0 ? void 0 : t(labelKey);
    return /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        "data-pptx-presenter-control": control.id,
        className: state.active ? PRESENTER_CONSOLE_CLASSES.controlActive : PRESENTER_CONSOLE_CLASSES.control,
        onClick: state.onClick,
        disabled: state.disabled,
        "aria-label": label,
        title: label,
        "aria-pressed": control.kind === "toggle" ? state.active : void 0,
        children: [
          Icon ? /* @__PURE__ */ jsx(Icon, {}) : null,
          control.glyph
        ]
      },
      control.id
    );
  }) });
}
function ScaledSlidePreviewImpl({
  slide,
  templateElements,
  canvasSize,
  className
}) {
  const containerRef = useRef(null);
  const [containerWidth, setContainerWidth] = useState(0);
  useEffect(() => {
    const el = containerRef.current;
    if (!el) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      const entry = entries[0];
      if (entry) {
        setContainerWidth(entry.contentRect.width);
      }
    });
    observer.observe(el);
    return () => observer.disconnect();
  }, []);
  const safeCanvasWidth = Math.max(canvasSize.width, 1);
  const safeCanvasHeight = Math.max(canvasSize.height, 1);
  const scale = containerWidth > 0 ? containerWidth / safeCanvasWidth : 0.2;
  const previewHeight = Math.max(40, Math.round(safeCanvasHeight * scale));
  const previewElements = [
    ...visibleTemplateElements(slide, templateElements),
    ...slide.elements
  ].slice(0, 80);
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ref: containerRef,
      className: `relative w-full overflow-hidden rounded border border-border bg-white ${className ?? ""}`,
      style: { height: previewHeight },
      children: [
        slide.backgroundColor && slide.backgroundColor !== "transparent" && /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute inset-0",
            style: {
              backgroundColor: normalizeHexColor(slide.backgroundColor, "#ffffff")
            }
          }
        ),
        slide.backgroundImage && /* @__PURE__ */ jsx(
          "img",
          {
            src: slide.backgroundImage,
            alt: "",
            className: "absolute inset-0 w-full h-full object-cover pointer-events-none",
            draggable: false
          }
        ),
        slide.backgroundGradient && /* @__PURE__ */ jsx("div", { className: "absolute inset-0", style: { backgroundImage: slide.backgroundGradient } }),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute top-0 left-0 origin-top-left",
            style: {
              width: safeCanvasWidth,
              height: safeCanvasHeight,
              transform: `scale(${scale})`,
              transformOrigin: "top left"
            },
            children: previewElements.map((element, index) => /* @__PURE__ */ jsx(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index
              },
              element.id
            ))
          }
        )
      ]
    }
  );
}
function arePropsEqual4(prev, next) {
  if (prev.slide.id !== next.slide.id) {
    return false;
  }
  if (prev.slide.isDirty !== next.slide.isDirty) {
    return false;
  }
  if (prev.slide.hidden !== next.slide.hidden) {
    return false;
  }
  if (prev.slide.elements !== next.slide.elements) {
    return false;
  }
  if (prev.slide.backgroundColor !== next.slide.backgroundColor) {
    return false;
  }
  if (prev.slide.backgroundImage !== next.slide.backgroundImage) {
    return false;
  }
  if (prev.slide.backgroundGradient !== next.slide.backgroundGradient) {
    return false;
  }
  if (prev.templateElements !== next.templateElements) {
    return false;
  }
  if (prev.canvasSize.width !== next.canvasSize.width || prev.canvasSize.height !== next.canvasSize.height) {
    return false;
  }
  if (prev.className !== next.className) {
    return false;
  }
  return true;
}
var ScaledSlidePreview = React20.memo(ScaledSlidePreviewImpl, arePropsEqual4);
function PresenterNotesRail({
  slides,
  current,
  canvasSize,
  templateElements,
  now,
  elapsed,
  activeCustomShow,
  authoredRange,
  onMove,
  onUpdateNotes
}) {
  const { t } = useTranslation();
  const slide = slides[current];
  const nextSlide = nextPresentedSlide(slides, current, activeCustomShow, authoredRange);
  const notesText = slide?.notes ?? "";
  const notesSegments = slide?.notesSegments;
  const [notesDraft, setNotesDraft] = useState(notesText);
  const [fontSize, setFontSize] = useState(NOTES_FONT_SIZE_DEFAULT);
  useEffect(() => setNotesDraft(notesText), [current, notesText]);
  const notes = useMemo(
    () => notesSegments?.length ? renderNotesSegments(notesSegments) : notesText.trim() ? notesText : /* @__PURE__ */ jsx("span", { className: "italic text-muted-foreground", children: t("pptx.presenter.noNotes") }),
    [notesSegments, notesText, t]
  );
  return /* @__PURE__ */ jsxs("aside", { className: PRESENTER_CONSOLE_CLASSES.rail, children: [
    /* @__PURE__ */ jsxs("header", { className: "flex items-center justify-between border-b border-border/60 px-4 py-3", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("div", { className: PRESENTER_CONSOLE_CLASSES.railHeading, children: t("pptx.presenter.currentTime") }),
        /* @__PURE__ */ jsx("div", { className: "font-mono text-lg tabular-nums", children: formatTime(new Date(now)) })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "text-right", children: [
        /* @__PURE__ */ jsx("div", { className: PRESENTER_CONSOLE_CLASSES.railHeading, children: t("pptx.presenter.elapsed") }),
        /* @__PURE__ */ jsx("div", { className: "font-mono text-lg tabular-nums text-primary", children: formatElapsed(elapsed) })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("nav", { className: "flex items-center justify-between border-b border-border/60 px-4 py-2", children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          onClick: () => onMove(-1),
          disabled: presenterPrevDisabled(current),
          "data-pptx-presenter-control": "prev",
          className: "inline-flex items-center gap-1 rounded bg-muted px-3 py-1.5 text-xs disabled:opacity-40",
          children: [
            /* @__PURE__ */ jsx(LuChevronLeft, {}),
            " ",
            t("pptx.presenter.prev")
          ]
        }
      ),
      /* @__PURE__ */ jsxs("span", { className: "font-mono text-sm tabular-nums", children: [
        current + 1,
        " / ",
        slides.length
      ] }),
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          onClick: () => onMove(1),
          disabled: presenterNextDisabled(),
          "data-pptx-presenter-control": "next",
          className: "inline-flex items-center gap-1 rounded bg-muted px-3 py-1.5 text-xs disabled:opacity-40",
          children: [
            t("pptx.presenter.next"),
            " ",
            /* @__PURE__ */ jsx(LuChevronRight, {})
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("section", { className: "border-b border-border/60 px-4 py-3", "data-pptx-presenter-next-preview": true, children: [
      /* @__PURE__ */ jsx("div", { className: `mb-2 ${PRESENTER_CONSOLE_CLASSES.railHeading}`, children: t("pptx.presenter.nextSlidePreview") }),
      nextSlide ? /* @__PURE__ */ jsx(
        ScaledSlidePreview,
        {
          slide: nextSlide,
          templateElements,
          canvasSize
        }
      ) : /* @__PURE__ */ jsx("div", { className: "flex h-16 items-center justify-center rounded bg-muted/40 text-xs italic text-muted-foreground", children: t("pptx.presenter.endOfPresentation") })
    ] }),
    /* @__PURE__ */ jsxs("section", { className: "flex min-h-0 flex-1 flex-col px-4 py-3", "data-pptx-presenter-notes": true, children: [
      /* @__PURE__ */ jsxs("div", { className: "mb-2 flex items-center justify-between", children: [
        /* @__PURE__ */ jsx("div", { className: PRESENTER_CONSOLE_CLASSES.railHeading, children: t("pptx.presenter.speakerNotes") }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: () => setFontSize(clampNotesFontSize(fontSize - NOTES_FONT_SIZE_STEP)),
              disabled: fontSize <= NOTES_FONT_SIZE_MIN,
              "data-pptx-presenter-control": "notes-font-decrease",
              "aria-label": t("pptx.presenter.decreaseFontSize"),
              children: /* @__PURE__ */ jsx(LuMinus, {})
            }
          ),
          /* @__PURE__ */ jsxs("span", { className: "min-w-8 text-center font-mono text-[10px]", children: [
            fontSize,
            "px"
          ] }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: () => setFontSize(clampNotesFontSize(fontSize + NOTES_FONT_SIZE_STEP)),
              disabled: fontSize >= NOTES_FONT_SIZE_MAX,
              "data-pptx-presenter-control": "notes-font-increase",
              "aria-label": t("pptx.presenter.increaseFontSize"),
              children: /* @__PURE__ */ jsx(LuPlus, {})
            }
          )
        ] })
      ] }),
      onUpdateNotes ? /* @__PURE__ */ jsx(
        "textarea",
        {
          className: "min-h-0 flex-1 resize-none rounded border border-border/30 bg-muted/40 px-3 py-2 leading-relaxed",
          style: { fontSize },
          value: notesDraft,
          onChange: (event) => setNotesDraft(event.target.value),
          onBlur: () => onUpdateNotes(notesDraft),
          "aria-label": t("pptx.presenter.speakerNotes")
        }
      ) : /* @__PURE__ */ jsx(
        "div",
        {
          className: "min-h-0 flex-1 overflow-y-auto whitespace-pre-wrap rounded border border-border/30 bg-muted/40 px-3 py-2 leading-relaxed",
          style: { fontSize },
          children: notes
        }
      )
    ] })
  ] });
}
function PresenterSlideNavigator(props) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: `${PRESENTER_CONSOLE_CLASSES.navigator} text-foreground`, children: [
    /* @__PURE__ */ jsxs("header", { className: "mb-4 flex items-center justify-between border-b border-border pb-4", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("p", { className: "text-xs uppercase tracking-[0.22em] text-sky-300", children: t(PRESENTER_NAVIGATOR_LABEL_KEYS.title) }),
        /* @__PURE__ */ jsx("h2", { className: "text-xl font-semibold", children: t(PRESENTER_NAVIGATOR_LABEL_KEYS.subtitle) })
      ] }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "rounded-md bg-muted px-4 py-2 hover:bg-accent",
          onClick: props.onClose,
          children: t(PRESENTER_NAVIGATOR_LABEL_KEYS.close)
        }
      )
    ] }),
    /* @__PURE__ */ jsx("div", { className: PRESENTER_CONSOLE_CLASSES.navigatorGrid, children: props.slides.map((slide, index) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: `group text-left ${index === props.current ? "ring-2 ring-sky-400" : ""} ${slide.hidden ? "opacity-45" : ""}`,
        onClick: () => props.onSelect(index),
        children: [
          /* @__PURE__ */ jsx(
            ScaledSlidePreview,
            {
              slide,
              templateElements: props.templateElements,
              canvasSize: props.canvasSize
            }
          ),
          /* @__PURE__ */ jsxs("span", { className: "mt-2 block text-xs tabular-nums text-muted-foreground", children: [
            index + 1,
            slide.hidden ? " - hidden" : ""
          ] })
        ]
      },
      slide.id ?? index
    )) })
  ] });
}
function point(event) {
  const rect = event.currentTarget.getBoundingClientRect();
  return {
    x: (event.clientX - rect.left) / rect.width,
    y: (event.clientY - rect.top) / rect.height
  };
}
function usePresenterInk(snapshot, onUpdate) {
  const drawingId = useRef(null);
  const onPointerDown = useCallback(
    (event) => {
      const pointer = snapshot.pointer;
      if (!pointer || pointer.tool === "none" || pointer.tool === "laser") {
        return;
      }
      const nextPoint = point(event);
      if (pointer.tool === "eraser") {
        onUpdate({
          inkStrokes: erasePresentationInkAt(
            snapshot.inkStrokes ?? [],
            snapshot.slideIndex,
            nextPoint
          )
        });
        return;
      }
      event.currentTarget.setPointerCapture(event.pointerId);
      const stroke = {
        id: crypto.randomUUID(),
        slideIndex: snapshot.slideIndex,
        tool: pointer.tool,
        color: pointer.color,
        width: pointer.tool === "highlighter" ? 12 : 4,
        points: [nextPoint]
      };
      drawingId.current = stroke.id;
      onUpdate({ inkStrokes: [...snapshot.inkStrokes ?? [], stroke] });
    },
    [onUpdate, snapshot]
  );
  const onPointerMove = useCallback(
    (event) => {
      const nextPoint = point(event);
      const pointer = snapshot.pointer;
      if (!pointer) {
        return;
      }
      if (pointer.tool === "laser") {
        onUpdate({ pointer: movePresenterPointer(pointer, nextPoint.x, nextPoint.y) });
      }
      if (pointer.tool === "eraser" && event.buttons === 1) {
        onUpdate({
          inkStrokes: erasePresentationInkAt(
            snapshot.inkStrokes ?? [],
            snapshot.slideIndex,
            nextPoint
          )
        });
      }
      if (drawingId.current && event.buttons === 1) {
        onUpdate({
          inkStrokes: (snapshot.inkStrokes ?? []).map(
            (stroke) => stroke.id === drawingId.current ? appendPresentationInkPoint(stroke, nextPoint) : stroke
          )
        });
      }
    },
    [onUpdate, snapshot]
  );
  const onPointerUp = useCallback(() => {
    drawingId.current = null;
  }, []);
  return { onPointerDown, onPointerMove, onPointerUp };
}
function PresenterView({
  slides,
  currentSlideIndex,
  canvasSize,
  templateElements,
  presentationStartTime,
  activeCustomShow,
  authoredRange,
  onMovePresentationSlide,
  onExit,
  onOpenAudienceWindow,
  onCloseAudienceWindow,
  isAudienceWindowOpen,
  endOfShowVisible,
  snapshot,
  onNavigateToSlide,
  onToggleTimer,
  onResetTimer,
  onStepZoom,
  onResetZoom,
  onSetBlackout,
  onUpdateSnapshot,
  onToggleSubtitles,
  onSwapDisplays,
  onUpdateNotes
}) {
  const { t } = useTranslation();
  const [now, setNow] = useState(Date.now());
  useEffect(() => {
    const interval = window.setInterval(() => setNow(Date.now()), 1e3);
    return () => window.clearInterval(interval);
  }, []);
  const elapsed = snapshot.elapsedMs ?? (presentationStartTime ? now - presentationStartTime : 0);
  const [showSlides, setShowSlides] = useState(false);
  const ink = usePresenterInk(snapshot, onUpdateSnapshot);
  const paneAdvancesOnClick = presenterPaneAdvancesOnClick(snapshot.pointer?.tool) && !endOfShowVisible;
  const handleSlidePaneClick = useCallback(() => {
    if (!paneAdvancesOnClick) {
      return;
    }
    onMovePresentationSlide(1, "click");
  }, [paneAdvancesOnClick, onMovePresentationSlide]);
  const handleCaptionChange = useCallback(
    (caption) => onUpdateSnapshot({ caption }),
    [onUpdateSnapshot]
  );
  const currentSlide = slides[currentSlideIndex];
  if (!currentSlide) {
    return /* @__PURE__ */ jsx("div", { className: "absolute inset-0 z-50 flex items-center justify-center bg-card text-muted-foreground", children: t("pptx.presenter.noSlides") });
  }
  const timerProgress = presenterTimerProgress(elapsed);
  return /* @__PURE__ */ jsxs("div", { className: PRESENTER_CONSOLE_CLASSES.root, children: [
    /* @__PURE__ */ jsx(
      PresenterConsoleToolbar,
      {
        snapshot,
        audienceOpen: Boolean(isAudienceWindowOpen),
        onToggleAudience: () => isAudienceWindowOpen ? onCloseAudienceWindow?.() : onOpenAudienceWindow?.(),
        onSwapDisplays,
        onToggleTimer,
        onResetTimer,
        onShowSlides: () => setShowSlides(true),
        onStepZoom,
        onResetZoom,
        onBlackout: onSetBlackout,
        onPointerTool: (tool) => onUpdateSnapshot({
          pointer: { ...snapshot.pointer ?? { x: 0.5, y: 0.5, color: "#ef4444" }, tool }
        }),
        onToggleSubtitles,
        onExit
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: PRESENTER_CONSOLE_CLASSES.body, children: [
      /* @__PURE__ */ jsxs(
        "div",
        {
          role: "presentation",
          "data-pptx-presenter-slide": true,
          onClick: handleSlidePaneClick,
          className: `${PRESENTER_CONSOLE_CLASSES.main} ${paneAdvancesOnClick ? "cursor-pointer" : ""}`,
          children: [
            endOfShowVisible && /* @__PURE__ */ jsx(PresentationEndOverlay, { onExit: () => onMovePresentationSlide(1) }),
            /* @__PURE__ */ jsxs(
              PresenterSlideFrame,
              {
                canvasSize,
                zoomScale: snapshot.zoom?.scale ?? 1,
                zoomOriginX: snapshot.zoom?.originX ?? 0.5,
                zoomOriginY: snapshot.zoom?.originY ?? 0.5,
                inkProps: ink,
                children: [
                  /* @__PURE__ */ jsx(
                    ScaledSlidePreview,
                    {
                      slide: currentSlide,
                      templateElements,
                      canvasSize
                    }
                  ),
                  /* @__PURE__ */ jsx(PresentationAudienceEffects, { snapshot })
                ]
              }
            ),
            /* @__PURE__ */ jsx("div", { className: "mt-3 text-xs font-mono tabular-nums text-white/50 select-none", children: t("pptx.presenter.slideLabel", {
              current: currentSlideIndex + 1,
              total: slides.length,
              defaultValue: `Slide ${currentSlideIndex + 1} of ${slides.length}`
            }) })
          ]
        }
      ),
      /* @__PURE__ */ jsx(
        PresenterNotesRail,
        {
          slides,
          current: currentSlideIndex,
          canvasSize,
          templateElements,
          activeCustomShow,
          authoredRange,
          now,
          elapsed,
          onMove: onMovePresentationSlide,
          onUpdateNotes
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      PresentationSubtitleBar,
      {
        visible: Boolean(snapshot.subtitlesVisible),
        onCaptionChange: handleCaptionChange
      }
    ),
    showSlides && /* @__PURE__ */ jsx(
      PresenterSlideNavigator,
      {
        slides,
        current: currentSlideIndex,
        canvasSize,
        templateElements,
        onSelect: (index) => {
          onNavigateToSlide(index);
          setShowSlides(false);
        },
        onClose: () => setShowSlides(false)
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        className: PRESENTER_CONSOLE_CLASSES.progressTrack,
        role: "progressbar",
        "aria-valuenow": Math.round(timerProgress.percent),
        "aria-valuemin": 0,
        "aria-valuemax": 100,
        "aria-label": t("pptx.presenter.timerProgress"),
        title: t("pptx.presenter.timerTitle", {
          elapsed: formatElapsed(elapsed),
          segment: timerProgress.segment + 1
        }),
        children: /* @__PURE__ */ jsx(
          "div",
          {
            className: PRESENTER_CONSOLE_CLASSES.progressFill,
            style: { width: `${timerProgress.percent}%` }
          }
        )
      }
    )
  ] });
}
function MobilePresenterView({
  slides,
  currentSlideIndex,
  canvasSize,
  templateElements,
  activeCustomShow,
  authoredRange,
  presentationStartTime,
  onMovePresentationSlide,
  onExit
}) {
  const { t } = useTranslation();
  const [now, setNow] = useState(Date.now());
  useEffect(() => {
    const interval = window.setInterval(() => setNow(Date.now()), 1e3);
    return () => window.clearInterval(interval);
  }, []);
  const elapsed = mobileElapsedSince(presentationStartTime, now);
  const currentSlide = slides[currentSlideIndex];
  const nextSlide = nextPresentedSlide(slides, currentSlideIndex, activeCustomShow, authoredRange);
  if (!currentSlide) {
    return /* @__PURE__ */ jsx("div", { className: "absolute inset-0 z-50 flex items-center justify-center bg-card text-muted-foreground", children: t("pptx.presenter.noSlides") });
  }
  const notesText = currentSlide.notes ?? "";
  const notesSegments = currentSlide.notesSegments;
  const hasRichNotes = notesSegments && notesSegments.length > 0;
  const prevDisabled = presenterPrevDisabled(currentSlideIndex);
  const nextDisabled = presenterNextDisabled();
  const insetStyle = {
    paddingTop: "env(safe-area-inset-top, 0px)",
    paddingBottom: "env(safe-area-inset-bottom, 0px)",
    paddingLeft: "env(safe-area-inset-left, 0px)",
    paddingRight: "env(safe-area-inset-right, 0px)"
  };
  return /* @__PURE__ */ jsxs("div", { className: "absolute inset-0 z-50 flex flex-col bg-card text-foreground", style: insetStyle, children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2 px-4 py-2 border-b border-border/60", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex flex-col", children: [
        /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wider", children: t("pptx.presenter.elapsed") }),
        /* @__PURE__ */ jsx("span", { className: "text-lg font-mono tabular-nums text-primary", children: formatMobileElapsed(elapsed) })
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-sm font-mono tabular-nums text-foreground", children: mobileSlideCounter(currentSlideIndex, slides.length) }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onExit,
          className: "flex items-center justify-center w-11 h-11 min-w-11 min-h-11 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors",
          title: t("pptx.presenter.endPresentation"),
          "aria-label": t("pptx.presenter.endPresentation"),
          children: /* @__PURE__ */ jsx(LuX, { className: "w-5 h-5" })
        }
      )
    ] }),
    /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center bg-black px-3 py-3", children: /* @__PURE__ */ jsx("div", { className: "w-full max-w-[640px]", children: /* @__PURE__ */ jsx(
      ScaledSlidePreview,
      {
        slide: currentSlide,
        templateElements,
        canvasSize
      }
    ) }) }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 px-4 py-2 border-b border-border/60", children: [
      /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wider whitespace-nowrap", children: t("pptx.presenter.nextSlidePreview") }),
      /* @__PURE__ */ jsx("div", { className: "w-[132px] flex-shrink-0", children: nextSlide ? /* @__PURE__ */ jsx(
        ScaledSlidePreview,
        {
          slide: nextSlide,
          templateElements,
          canvasSize
        }
      ) : /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-12 rounded border border-border/30 bg-muted/40 text-[10px] text-muted-foreground italic", children: t("pptx.presenter.endOfPresentation") }) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex-1 flex flex-col min-h-0 px-4 py-2", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground uppercase tracking-wider mb-1", children: t("pptx.presenter.speakerNotes") }),
      /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-y-auto rounded border border-border/30 bg-muted/40 px-3 py-2 text-foreground whitespace-pre-wrap leading-relaxed text-[15px]", children: hasRichNotes ? renderNotesSegments(notesSegments) : notesText.trim().length > 0 ? notesText : /* @__PURE__ */ jsx("span", { className: "italic text-muted-foreground", children: t("pptx.presenter.noNotes") }) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-3 px-4 py-2 border-t border-border/60", children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          "data-pptx-presenter-control": "prev",
          onClick: () => onMovePresentationSlide(-1),
          disabled: prevDisabled,
          className: "inline-flex items-center justify-center gap-1.5 flex-1 h-11 rounded bg-muted hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed text-sm transition-colors",
          title: t("pptx.presenter.previousSlide"),
          "aria-label": t("pptx.presenter.previousSlide"),
          children: [
            /* @__PURE__ */ jsx(LuChevronLeft, { className: "w-5 h-5" }),
            t("pptx.presenter.prev")
          ]
        }
      ),
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          "data-pptx-presenter-control": "next",
          onClick: () => onMovePresentationSlide(1),
          disabled: nextDisabled,
          className: "inline-flex items-center justify-center gap-1.5 flex-1 h-11 rounded bg-muted hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed text-sm transition-colors",
          title: t("pptx.presenter.nextSlide"),
          "aria-label": t("pptx.presenter.nextSlide"),
          children: [
            t("pptx.presenter.next"),
            /* @__PURE__ */ jsx(LuChevronRight, { className: "w-5 h-5" })
          ]
        }
      )
    ] })
  ] });
}
function formatMs(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
}
function RehearseTimingsHud({
  presentationStartTime,
  slideStartTime,
  paused,
  onTogglePause
}) {
  const { t } = useTranslation();
  const [tick, setTick] = useState(0);
  useEffect(() => {
    if (paused) {
      return;
    }
    const id = window.setInterval(() => setTick((p) => p + 1), 250);
    return () => window.clearInterval(id);
  }, [paused]);
  const now = Date.now();
  const slideElapsed = slideStartTime !== null ? now - slideStartTime : 0;
  const totalElapsed = presentationStartTime !== null ? now - presentationStartTime : 0;
  return /* @__PURE__ */ jsxs("div", { className: "fixed bottom-4 left-4 z-[9999] flex items-center gap-3 rounded-lg bg-black/80 px-4 py-2 text-white shadow-xl backdrop-blur-sm select-none", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col text-xs leading-tight", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.rehearse.slideTime") }),
      /* @__PURE__ */ jsx("span", { className: "text-lg font-mono tabular-nums", children: formatMs(slideElapsed) })
    ] }),
    /* @__PURE__ */ jsx("div", { className: "w-px h-8 bg-border" }),
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col text-xs leading-tight", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.rehearse.totalTime") }),
      /* @__PURE__ */ jsx("span", { className: "text-lg font-mono tabular-nums", children: formatMs(totalElapsed) })
    ] }),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onTogglePause,
        className: "ml-1 rounded p-1.5 hover:bg-white/20 transition-colors",
        title: paused ? t("pptx.rehearse.resume") : t("pptx.rehearse.pause"),
        "aria-label": paused ? t("pptx.rehearse.resume") : t("pptx.rehearse.pause"),
        children: paused ? /* @__PURE__ */ jsx(LuPlay, { className: "w-4 h-4" }) : /* @__PURE__ */ jsx(LuPause, { className: "w-4 h-4" })
      }
    )
  ] });
}
function formatMs2(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
}
function RehearseTimingsSummary({
  slides,
  canvasSize,
  recordedTimings,
  onSave,
  onDiscard
}) {
  const { t } = useTranslation();
  const totalMs = Object.values(recordedTimings).reduce((sum, ms) => sum + ms, 0);
  const sortedEntries = Object.entries(recordedTimings).map(([idx, ms]) => ({ slideIndex: Number(idx), ms })).sort((a, b) => a.slideIndex - b.slideIndex);
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 backdrop-blur-sm", children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-lg rounded-xl border border-border bg-background shadow-2xl", children: [
    /* @__PURE__ */ jsxs("div", { className: "border-b border-border px-5 py-4", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-base font-semibold text-foreground", children: t("pptx.rehearse.summaryTitle") }),
      /* @__PURE__ */ jsxs("p", { className: "mt-1 text-sm text-muted-foreground", children: [
        t("pptx.rehearse.totalPresentationTime"),
        ":",
        " ",
        /* @__PURE__ */ jsx("span", { className: "font-mono text-foreground", children: formatMs2(totalMs) })
      ] })
    ] }),
    /* @__PURE__ */ jsx("div", { className: "max-h-72 overflow-y-auto px-5 py-3", children: /* @__PURE__ */ jsxs("table", { className: "w-full text-xs", children: [
      /* @__PURE__ */ jsx("thead", { children: /* @__PURE__ */ jsxs("tr", { className: "text-muted-foreground uppercase tracking-wide", children: [
        /* @__PURE__ */ jsx("th", { className: "pb-2 text-left", children: "#" }),
        /* @__PURE__ */ jsx("th", { className: "pb-2 text-left", children: t("pptx.rehearse.slide") }),
        /* @__PURE__ */ jsx("th", { className: "pb-2 text-right", children: t("pptx.rehearse.time") })
      ] }) }),
      /* @__PURE__ */ jsx("tbody", { children: sortedEntries.map(({ slideIndex, ms }) => {
        const slide = slides[slideIndex];
        return /* @__PURE__ */ jsxs("tr", { className: "border-t border-border", children: [
          /* @__PURE__ */ jsx("td", { className: "py-2 text-muted-foreground tabular-nums", children: slideIndex + 1 }),
          /* @__PURE__ */ jsx("td", { className: "py-2", children: slide && /* @__PURE__ */ jsx("div", { className: "w-20 rounded overflow-hidden bg-white", children: /* @__PURE__ */ jsx(
            SlideThumbnail,
            {
              slide,
              templateElements: [],
              canvasSize
            }
          ) }) }),
          /* @__PURE__ */ jsx("td", { className: "py-2 text-right font-mono text-foreground tabular-nums", children: formatMs2(ms) })
        ] }, slideIndex);
      }) })
    ] }) }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 border-t border-border px-5 py-3", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onDiscard,
          className: "rounded-lg px-4 py-2 text-sm text-foreground hover:bg-muted transition-colors",
          children: t("pptx.rehearse.discard")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onSave,
          className: "rounded-lg bg-primary px-4 py-2 text-sm text-white hover:bg-primary/80 transition-colors",
          children: t("pptx.rehearse.saveTimings")
        }
      )
    ] })
  ] }) });
}

// src/viewer/components/selection-pane-utils.ts
var TYPE_LABELS = {
  text: "Text Box",
  shape: "Shape",
  connector: "Connector",
  image: "Image",
  picture: "Picture",
  chart: "Chart",
  table: "Table",
  smartArt: "SmartArt",
  media: "Media",
  group: "Group",
  ink: "Ink",
  ole: "Object",
  unknown: "Object"
};
function getElementDisplayName(element, index) {
  if (element.name && element.name.trim().length > 0) {
    return element.name.trim();
  }
  if (hasTextProperties(element) && element.text && element.text.trim().length > 0) {
    return element.text.trim().slice(0, 32);
  }
  const label = TYPE_LABELS[element.type] ?? "Object";
  return `${label} ${index + 1}`;
}
function SelectionPane({
  slides,
  activeSlideIndex,
  selectedElementId,
  selectedElementIds,
  canEdit,
  setSelectedElementId,
  setSelectedElementIds,
  setSlides,
  markDirty,
  onClose
}) {
  const { t } = useTranslation();
  const activeSlide = slides[activeSlideIndex];
  const elements = activeSlide?.elements ?? [];
  const [editingId, setEditingId] = useState(null);
  const [editingName, setEditingName] = useState("");
  const inputRef = useRef(null);
  const editSeedRef = useRef("");
  const [dragIndex, setDragIndex] = useState(null);
  const [dragOverIndex, setDragOverIndex] = useState(null);
  const handleSelect = useCallback(
    (elementId) => {
      setSelectedElementId(elementId);
      setSelectedElementIds([elementId]);
    },
    [setSelectedElementId, setSelectedElementIds]
  );
  const handleToggleVisibility = useCallback(
    (elementId) => {
      setSlides((prevSlides) => {
        const nextSlides = [...prevSlides];
        const slide = nextSlides[activeSlideIndex];
        if (!slide) {
          return prevSlides;
        }
        const nextElements = [...slide.elements ?? []];
        const idx = nextElements.findIndex((e) => e.id === elementId);
        if (idx === -1) {
          return prevSlides;
        }
        nextElements[idx] = {
          ...nextElements[idx],
          hidden: !nextElements[idx].hidden
        };
        nextSlides[activeSlideIndex] = { ...slide, elements: nextElements };
        return nextSlides;
      });
      markDirty();
    },
    [activeSlideIndex, setSlides, markDirty]
  );
  const handleDoubleClick = useCallback(
    (element, displayName) => {
      if (!canEdit) {
        return;
      }
      setEditingId(element.id);
      setEditingName(displayName);
      editSeedRef.current = displayName;
      setTimeout(() => inputRef.current?.select(), 0);
    },
    [canEdit]
  );
  const commitRename = useCallback(() => {
    const elementId = editingId;
    setEditingId(null);
    if (elementId === null) {
      return;
    }
    const commit = resolveSelectionPaneRename(editSeedRef.current, editingName);
    if (!commit) {
      return;
    }
    setSlides((prevSlides) => {
      const nextSlides = [...prevSlides];
      const slide = nextSlides[activeSlideIndex];
      if (!slide) {
        return prevSlides;
      }
      const nextElements = [...slide.elements ?? []];
      const idx = nextElements.findIndex((e) => e.id === elementId);
      if (idx === -1) {
        return prevSlides;
      }
      nextElements[idx] = {
        ...nextElements[idx],
        name: commit.name
      };
      nextSlides[activeSlideIndex] = { ...slide, elements: nextElements };
      return nextSlides;
    });
    markDirty();
  }, [editingId, editingName, activeSlideIndex, setSlides, markDirty]);
  const handleDragStart = useCallback((index) => {
    setDragIndex(index);
  }, []);
  const handleDragOver = useCallback(
    (e, index) => {
      e.preventDefault();
      if (dragIndex !== null && dragIndex !== index) {
        setDragOverIndex(index);
      }
    },
    [dragIndex]
  );
  const handleDrop = useCallback(
    (targetIndex) => {
      if (dragIndex === null || dragIndex === targetIndex) {
        setDragIndex(null);
        setDragOverIndex(null);
        return;
      }
      setSlides((prevSlides) => {
        const nextSlides = [...prevSlides];
        const slide = nextSlides[activeSlideIndex];
        if (!slide) {
          return prevSlides;
        }
        const nextElements = [...slide.elements ?? []];
        const [moved] = nextElements.splice(dragIndex, 1);
        nextElements.splice(targetIndex, 0, moved);
        nextSlides[activeSlideIndex] = { ...slide, elements: nextElements };
        return nextSlides;
      });
      markDirty();
      setDragIndex(null);
      setDragOverIndex(null);
    },
    [dragIndex, activeSlideIndex, setSlides, markDirty]
  );
  const reversed = [...elements].reverse();
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-pptx-selection-pane": true,
      className: "flex flex-col h-full bg-popover border-l border-border w-56",
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-3 py-2 border-b border-border", children: [
          /* @__PURE__ */ jsx("span", { className: "text-xs font-medium text-foreground", children: t("pptx.selectionPane.title") }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "text-muted-foreground hover:text-foreground text-xs",
              title: t("pptx.selectionPane.close"),
              children: "\xD7"
            }
          )
        ] }),
        /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-y-auto py-1", children: reversed.length === 0 ? /* @__PURE__ */ jsx("div", { className: "px-3 py-4 text-xs text-muted-foreground italic", children: t("pptx.selectionPane.empty") }) : reversed.map((element, reversedIdx) => {
          const realIndex = elements.length - 1 - reversedIdx;
          const isSelected = selectedElementId === element.id || selectedElementIds.includes(element.id);
          const displayName = getElementDisplayName(element, realIndex);
          const isEditing = editingId === element.id;
          return /* @__PURE__ */ jsxs(
            "div",
            {
              draggable: canEdit,
              onDragStart: () => handleDragStart(realIndex),
              onDragOver: (e) => handleDragOver(e, realIndex),
              onDrop: () => handleDrop(realIndex),
              onDragEnd: () => {
                setDragIndex(null);
                setDragOverIndex(null);
              },
              className: cn(
                "flex items-center gap-1.5 px-2 py-1 text-xs cursor-pointer transition-colors",
                isSelected ? "bg-primary/30 text-primary" : "hover:bg-muted text-foreground",
                dragOverIndex === realIndex ? "border-t-2 border-primary" : ""
              ),
              onClick: () => handleSelect(element.id),
              onDoubleClick: () => handleDoubleClick(element, displayName),
              children: [
                canEdit && /* @__PURE__ */ jsx(LuGripVertical, { className: "w-3 h-3 text-muted-foreground flex-shrink-0 cursor-grab" }),
                /* @__PURE__ */ jsx("span", { "data-pptx-selection-name": true, className: "flex-1 truncate", children: isEditing ? /* @__PURE__ */ jsx(
                  "input",
                  {
                    ref: inputRef,
                    type: "text",
                    "aria-label": t("pptx.selectionPane.renameElement"),
                    value: editingName,
                    onChange: (e) => setEditingName(e.target.value),
                    onBlur: commitRename,
                    onKeyDown: (e) => {
                      if (e.key === "Enter") {
                        commitRename();
                      }
                      if (e.key === "Escape") {
                        setEditingId(null);
                      }
                    },
                    className: "w-full bg-muted text-xs px-1 py-0.5 rounded border border-border outline-none",
                    onClick: (e) => e.stopPropagation()
                  }
                ) : displayName }),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: "text-muted-foreground hover:text-foreground flex-shrink-0",
                    title: element.hidden ? t("pptx.selectionPane.show") : t("pptx.selectionPane.hide"),
                    onClick: (e) => {
                      e.stopPropagation();
                      handleToggleVisibility(element.id);
                    },
                    children: element.hidden ? /* @__PURE__ */ jsx(LuEyeOff, { className: "w-3.5 h-3.5" }) : /* @__PURE__ */ jsx(LuEye, { className: "w-3.5 h-3.5 opacity-50" })
                  }
                )
              ]
            },
            element.id
          );
        }) })
      ]
    }
  );
}
var INPUT_CLS = "bg-muted border border-border rounded px-2 py-1 w-full text-[11px]";
var SELECT_CLS = "bg-muted border border-border rounded px-2 py-1 w-full text-[11px]";
var ENTRANCE_PRESETS = ANIMATION_PRESET_OPTIONS.filter(
  (o) => o.value === "fadeIn" || o.value === "flyIn" || o.value === "zoomIn"
);
var EXIT_PRESETS = ANIMATION_PRESET_OPTIONS.filter(
  (o) => o.value === "fadeOut" || o.value === "flyOut" || o.value === "zoomOut"
);
var EMPHASIS_PRESETS = ANIMATION_PRESET_OPTIONS.filter(
  (o) => o.value === "spin" || o.value === "pulse" || o.value === "colorWave" || o.value === "bounce" || o.value === "flash" || o.value === "growShrink" || o.value === "teeter"
);
var TRIGGER_OPTIONS = [
  { value: "onClick", labelKey: "pptx.animation.trigger.onClick" },
  { value: "onShapeClick", labelKey: "pptx.animation.trigger.onShapeClick" },
  { value: "onHover", labelKey: "pptx.animation.trigger.onHover" },
  { value: "afterPrevious", labelKey: "pptx.animation.trigger.afterPrevious" },
  { value: "withPrevious", labelKey: "pptx.animation.trigger.withPrevious" }
];
var TIMING_CURVE_OPTIONS = [
  { value: "ease", labelKey: "pptx.animation.timingCurve.ease" },
  { value: "ease-in", labelKey: "pptx.animation.timingCurve.easeIn" },
  { value: "ease-out", labelKey: "pptx.animation.timingCurve.easeOut" },
  { value: "linear", labelKey: "pptx.animation.timingCurve.linear" }
];
var REPEAT_MODE_OPTIONS = [
  { value: "none", labelKey: "pptx.animation.repeatUntil.none" },
  {
    value: "untilNextClick",
    labelKey: "pptx.animation.repeatUntil.untilNextClick"
  },
  {
    value: "untilEndOfSlide",
    labelKey: "pptx.animation.repeatUntil.untilEndOfSlide"
  }
];
var DIRECTION_OPTIONS = [
  {
    value: "fromTop",
    labelKey: "pptx.animation.direction.fromTop",
    icon: LuArrowDown
  },
  {
    value: "fromBottom",
    labelKey: "pptx.animation.direction.fromBottom",
    icon: LuArrowUp
  },
  {
    value: "fromLeft",
    labelKey: "pptx.animation.direction.fromLeft",
    icon: LuArrowRight
  },
  {
    value: "fromRight",
    labelKey: "pptx.animation.direction.fromRight",
    icon: LuArrowLeft
  }
];
var SEQUENCE_OPTIONS = [
  { value: "asOne", labelKey: "pptx.animation.sequence.asOne" },
  { value: "byParagraph", labelKey: "pptx.animation.sequence.byParagraph" },
  { value: "byWord", labelKey: "pptx.animation.sequence.byWord" },
  { value: "byLetter", labelKey: "pptx.animation.sequence.byLetter" }
];
var AFTER_ANIMATION_OPTIONS = [
  { value: "none", labelKey: "pptx.animation.afterAnimation.none" },
  { value: "dimToColor", labelKey: "pptx.animation.afterAnimation.dimToColor" },
  { value: "hideAfterAnimation", labelKey: "pptx.animation.afterAnimation.hideAfterAnimation" },
  { value: "hideOnNextClick", labelKey: "pptx.animation.afterAnimation.hideOnNextClick" }
];
function AfterAnimationRow({
  action,
  color,
  canEdit,
  onActionChange,
  onColorChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.afterAnimation") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.animation.afterAnimation"),
          value: action,
          onChange: (event) => onActionChange(event.target.value),
          disabled: !canEdit,
          className: SELECT_CLS,
          children: AFTER_ANIMATION_OPTIONS.map((option) => /* @__PURE__ */ jsx("option", { value: option.value, children: t(option.labelKey) }, option.value))
        }
      )
    ] }),
    action === "dimToColor" && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.afterAnimation.color") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          "aria-label": t("pptx.animation.afterAnimation.color"),
          value: color ?? "#808080",
          onChange: (event) => {
            onColorChange(event.target.value);
            pushColor(event.target.value);
          },
          disabled: !canEdit,
          className: "h-6 w-10 rounded border border-border bg-transparent p-0"
        }
      )
    ] })
  ] });
}
function animationTypeColor(anim) {
  if (anim.entrance) {
    return "bg-green-500/60";
  }
  if (anim.emphasis) {
    return "bg-yellow-500/60";
  }
  if (anim.exit) {
    return "bg-red-500/60";
  }
  return "bg-muted-foreground/40";
}
function AnimationTimelineSection({
  selectedElementId,
  canEdit,
  handlers
}) {
  const { t } = useTranslation();
  const effectLabel = (anim) => animationEffectLabel(anim, (key) => t(key));
  const {
    sortedAnimations,
    timelineRows,
    dragIndex,
    dragOverIndex,
    timelineBarData,
    handleAnimationHover,
    handleAnimationHoverEnd,
    handleDragStart,
    handleDragOver,
    handleDragEnter,
    handleDragLeave,
    handleDrop,
    handleDragEnd,
    handleMoveUp,
    handleMoveDown,
    getTimelineLabel,
    getNativeRowLabel
  } = handlers;
  if (timelineRows.length === 0 && timelineBarData.length === 0) {
    return null;
  }
  const animationByElementId = new Map(sortedAnimations.map((anim) => [anim.elementId, anim]));
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    timelineBarData.length > 0 && /* @__PURE__ */ jsxs("div", { className: "mt-2 pt-2 border-t border-border", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] uppercase tracking-wide text-muted-foreground mb-1", children: t("pptx.animation.timelineBar") }),
      /* @__PURE__ */ jsx("div", { className: "relative h-6 rounded bg-muted/50 border border-border overflow-hidden", children: timelineBarData.map((bar) => /* @__PURE__ */ jsx(
        "div",
        {
          className: cn(
            "absolute top-0.5 bottom-0.5 rounded-sm transition-colors",
            animationTypeColor(bar.anim),
            bar.anim.elementId === selectedElementId && "ring-1 ring-primary"
          ),
          style: {
            left: `${bar.leftPercent}%`,
            width: `${Math.max(bar.widthPercent, 2)}%`
          },
          title: `${getTimelineLabel(bar.anim)} - ${effectLabel(bar.anim)} (${bar.anim.durationMs ?? 500}ms)`,
          onMouseEnter: () => handleAnimationHover(bar.anim),
          onMouseLeave: handleAnimationHoverEnd
        },
        bar.anim.elementId
      )) })
    ] }),
    timelineRows.length > 0 && /* @__PURE__ */ jsxs("div", { className: "mt-2 pt-2 border-t border-border", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] uppercase tracking-wide text-muted-foreground mb-1", children: t("pptx.animation.timeline") }),
      /* @__PURE__ */ jsx("div", { className: "space-y-0.5 max-h-40 overflow-y-auto", children: timelineRows.map((row, index) => {
        const isDragOver = dragOverIndex === index;
        if (row.kind === "native") {
          return /* @__PURE__ */ jsxs(
            "div",
            {
              onDragOver: (e) => handleDragOver(index, e),
              onDragEnter: () => handleDragEnter(index),
              onDragLeave: handleDragLeave,
              onDrop: (e) => handleDrop(index, e),
              title: t("pptx.animation.nativeEffectHint"),
              className: cn(
                "flex items-center gap-1 px-1 py-0.5 rounded text-[10px] italic",
                "bg-muted/20 text-muted-foreground/70",
                isDragOver && "border-t-2 border-primary"
              ),
              children: [
                /* @__PURE__ */ jsx("span", { className: "w-3 h-3 shrink-0" }),
                /* @__PURE__ */ jsxs("span", { className: "text-muted-foreground/70 w-4 shrink-0", children: [
                  index + 1,
                  "."
                ] }),
                /* @__PURE__ */ jsxs("span", { className: "truncate flex-1", children: [
                  t("pptx.animation.nativeEffect"),
                  ": ",
                  getNativeRowLabel(row.targetIds)
                ] })
              ]
            },
            row.key
          );
        }
        const anim = animationByElementId.get(row.elementId);
        if (!anim) {
          return null;
        }
        const isSelected = selectedElementId === anim.elementId;
        const isDragging = dragIndex === index;
        return /* @__PURE__ */ jsxs(
          "div",
          {
            draggable: canEdit,
            onDragStart: (e) => handleDragStart(index, e),
            onDragOver: (e) => handleDragOver(index, e),
            onDragEnter: () => handleDragEnter(index),
            onDragLeave: handleDragLeave,
            onDrop: (e) => handleDrop(index, e),
            onDragEnd: handleDragEnd,
            onMouseEnter: () => handleAnimationHover(anim),
            onMouseLeave: handleAnimationHoverEnd,
            className: cn(
              "flex items-center gap-1 px-1 py-0.5 rounded text-[10px] cursor-grab transition-colors",
              isSelected ? "bg-primary/30 text-primary" : "bg-muted/50 text-muted-foreground",
              isDragging && "opacity-40",
              isDragOver && "border-t-2 border-primary"
            ),
            children: [
              canEdit && /* @__PURE__ */ jsx(LuGripVertical, { className: "w-3 h-3 text-muted-foreground/50 shrink-0" }),
              /* @__PURE__ */ jsxs("span", { className: "text-muted-foreground w-4 shrink-0", children: [
                index + 1,
                "."
              ] }),
              /* @__PURE__ */ jsx("span", { className: "truncate flex-1", children: getTimelineLabel(anim) }),
              anim.entrance && /* @__PURE__ */ jsx(LuMoveRight, { className: "w-3 h-3 text-green-400/70 shrink-0" }),
              anim.emphasis && /* @__PURE__ */ jsx(LuRotateCw, { className: "w-3 h-3 text-yellow-400/70 shrink-0" }),
              anim.exit && /* @__PURE__ */ jsx(LuMoveRight, { className: "w-3 h-3 text-red-400/70 rotate-180 shrink-0" }),
              canEdit && /* @__PURE__ */ jsxs("div", { className: "flex gap-0.5 shrink-0", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    disabled: index === 0,
                    className: "text-muted-foreground hover:text-foreground disabled:opacity-30 transition-colors",
                    onClick: (e) => {
                      e.stopPropagation();
                      handleMoveUp(index);
                    },
                    title: t("pptx.animation.moveUp"),
                    children: /* @__PURE__ */ jsx(LuChevronUp, { className: "w-3 h-3" })
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    disabled: index === timelineRows.length - 1,
                    className: "text-muted-foreground hover:text-foreground disabled:opacity-30 transition-colors",
                    onClick: (e) => {
                      e.stopPropagation();
                      handleMoveDown(index);
                    },
                    title: t("pptx.animation.moveDown"),
                    children: /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3" })
                  }
                )
              ] })
            ]
          },
          row.key
        );
      }) })
    ] })
  ] });
}
function EffectSoundRow({
  soundState,
  canEdit,
  onPick
}) {
  const { t } = useTranslation();
  const fileInputRef = React20.useRef(null);
  const handleSelectChange = (event) => {
    if (event.target.value === "custom") {
      fileInputRef.current?.click();
      return;
    }
    onPick(void 0);
  };
  const handleFileChange = (event) => {
    const file = event.target.files?.[0];
    event.target.value = "";
    if (!file) {
      return;
    }
    const reader = new FileReader();
    reader.onload = () => {
      if (typeof reader.result === "string") {
        onPick({ dataUrl: reader.result, fileName: file.name });
      }
    };
    reader.readAsDataURL(file);
  };
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.sound") }),
    /* @__PURE__ */ jsxs(
      "select",
      {
        "aria-label": t("pptx.animation.sound"),
        value: soundState.hasSound ? "custom" : "none",
        onChange: handleSelectChange,
        disabled: !canEdit,
        className: SELECT_CLS,
        children: [
          /* @__PURE__ */ jsx("option", { value: "none", children: t("pptx.animation.sound.none") }),
          /* @__PURE__ */ jsx("option", { value: "custom", children: soundState.hasSound && soundState.fileName ? soundState.fileName : t("pptx.animation.sound.custom") })
        ]
      }
    ),
    /* @__PURE__ */ jsx(
      "input",
      {
        ref: fileInputRef,
        type: "file",
        accept: "audio/*",
        "aria-label": t("pptx.animation.sound.chooseFile"),
        className: "hidden",
        onChange: handleFileChange,
        tabIndex: -1
      }
    )
  ] });
}
function MotionPathRow({
  motionPath,
  canEdit,
  onChange
}) {
  const { t } = useTranslation();
  const presetId = motionPathPresetIdForPath(motionPath);
  const isCustom = Boolean(motionPath) && !presetId;
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.motionPath.label") }),
    /* @__PURE__ */ jsxs(
      "select",
      {
        "aria-label": t("pptx.animation.motionPath.label"),
        value: isCustom ? "custom" : presetId ?? "none",
        onChange: (event) => onChange(event.target.value),
        disabled: !canEdit,
        className: SELECT_CLS,
        children: [
          /* @__PURE__ */ jsx("option", { value: "none", children: t("pptx.animation.motionPath.none") }),
          isCustom && /* @__PURE__ */ jsx("option", { value: "custom", children: t("pptx.animation.motionPath.custom") }),
          MOTION_PATH_FAMILIES.map((family) => /* @__PURE__ */ jsx("optgroup", { label: t(motionPathFamilyLabelKey(family)), children: motionPathPresetsByFamily(family).map((preset) => /* @__PURE__ */ jsx("option", { value: preset.id, children: t(motionPathPresetLabelKey(preset.id)) }, preset.id)) }, family))
        ]
      }
    ),
    motionPath && /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.animation.motionPath.editHint") })
  ] });
}
function useAnimationDragDrop({
  canEdit,
  rows,
  updateAnimations
}) {
  const [dragIndex, setDragIndex] = useState(null);
  const [dragOverIndex, setDragOverIndex] = useState(null);
  const dragCounterRef = useRef(0);
  const handleDragStart = useCallback(
    (index, event) => {
      if (!canEdit || rows[index]?.kind !== "editor") {
        return;
      }
      setDragIndex(index);
      event.dataTransfer.effectAllowed = "move";
      event.dataTransfer.setData("text/plain", String(index));
    },
    [canEdit, rows]
  );
  const handleDragOver = useCallback((_index, event) => {
    event.preventDefault();
    event.dataTransfer.dropEffect = "move";
    setDragOverIndex(_index);
  }, []);
  const handleDragEnter = useCallback((index) => {
    dragCounterRef.current++;
    setDragOverIndex(index);
  }, []);
  const handleDragLeave = useCallback(() => {
    dragCounterRef.current--;
    if (dragCounterRef.current <= 0) {
      setDragOverIndex(null);
      dragCounterRef.current = 0;
    }
  }, []);
  const reorderRows = useCallback(
    (sourceIndex, targetIndex) => {
      const sourceRow = rows[sourceIndex];
      if (!sourceRow || sourceRow.kind !== "editor") {
        return;
      }
      const nextRows = reorderAnimationTimelineRows(rows, sourceRow.key, targetIndex);
      updateAnimations((anims) => applyAnimationTimelineOrder(anims, nextRows));
    },
    [rows, updateAnimations]
  );
  const handleDrop = useCallback(
    (targetIndex, event) => {
      event.preventDefault();
      dragCounterRef.current = 0;
      const sourceIndex = dragIndex;
      setDragIndex(null);
      setDragOverIndex(null);
      if (sourceIndex === null || sourceIndex === targetIndex) {
        return;
      }
      reorderRows(sourceIndex, targetIndex);
    },
    [dragIndex, reorderRows]
  );
  const handleDragEnd = useCallback(() => {
    setDragIndex(null);
    setDragOverIndex(null);
    dragCounterRef.current = 0;
  }, []);
  const handleMoveUp = useCallback(
    (rowIndex) => {
      if (rowIndex <= 0) {
        return;
      }
      reorderRows(rowIndex, rowIndex - 1);
    },
    [reorderRows]
  );
  const handleMoveDown = useCallback(
    (rowIndex) => {
      reorderRows(rowIndex, rowIndex + 1);
    },
    [reorderRows]
  );
  return {
    dragIndex,
    dragOverIndex,
    handleDragStart,
    handleDragOver,
    handleDragEnter,
    handleDragLeave,
    handleDrop,
    handleDragEnd,
    handleMoveUp,
    handleMoveDown
  };
}
function useAnimationPreview({
  selectedElementId,
  selectedElementAnimation
}) {
  const handleAnimationHover = useCallback((anim) => {
    if (anim.motionPath) {
      startMotionPathPreview(anim.elementId, anim.motionPath, {
        durationMs: anim.durationMs,
        delayMs: anim.delayMs,
        timingCurve: anim.timingCurve
      });
      return;
    }
    const preset = anim.entrance ?? anim.emphasis ?? anim.exit;
    if (!preset || preset === "none") {
      return;
    }
    startPreviewAnimation(anim.elementId, preset, {
      direction: anim.direction,
      durationMs: anim.durationMs ?? 500,
      timingCurve: anim.timingCurve
    });
  }, []);
  const handleAnimationHoverEnd = useCallback(() => stopPreviewAnimation(), []);
  const handlePreviewClick = useCallback(() => {
    if (!selectedElementAnimation) {
      return;
    }
    if (selectedElementAnimation.motionPath) {
      startMotionPathPreview(selectedElementId, selectedElementAnimation.motionPath, {
        durationMs: selectedElementAnimation.durationMs,
        delayMs: selectedElementAnimation.delayMs,
        timingCurve: selectedElementAnimation.timingCurve
      });
      return;
    }
    const preset = selectedElementAnimation.entrance ?? selectedElementAnimation.emphasis ?? selectedElementAnimation.exit;
    if (!preset || preset === "none") {
      return;
    }
    startPreviewAnimation(selectedElementId, preset, {
      direction: selectedElementAnimation.direction,
      durationMs: selectedElementAnimation.durationMs ?? 500,
      timingCurve: selectedElementAnimation.timingCurve
    });
  }, [selectedElementId, selectedElementAnimation]);
  return {
    handleAnimationHover,
    handleAnimationHoverEnd,
    handlePreviewClick
  };
}

// src/viewer/components/inspector/useAnimationHandlers.ts
function useAnimationHandlers({
  selectedElement,
  activeSlide,
  canEdit,
  onUpdateSlide
}) {
  const selectedElementAnimation = useMemo(
    () => (activeSlide.animations ?? []).find((a) => a.elementId === selectedElement.id),
    [activeSlide, selectedElement]
  );
  const sortedAnimations = useMemo(
    () => [...activeSlide.animations ?? []].sort((a, b) => (a.order ?? 0) - (b.order ?? 0)),
    [activeSlide.animations]
  );
  const timelineRows = useMemo(
    () => buildAnimationTimelineRows(
      activeSlide.animations ?? [],
      activeSlide.animationTimelineAnchors ?? []
    ),
    [activeSlide.animations, activeSlide.animationTimelineAnchors]
  );
  const updateAnimations = useCallback(
    (updater) => {
      if (!canEdit) {
        return;
      }
      const current = activeSlide.animations ?? [];
      onUpdateSlide({ animations: updater([...current]) });
    },
    [canEdit, activeSlide.animations, onUpdateSlide]
  );
  const updateAnimationField = useCallback(
    (field, value) => {
      updateAnimations(
        (anims) => anims.map((a) => a.elementId === selectedElement.id ? { ...a, [field]: value } : a)
      );
    },
    [updateAnimations, selectedElement.id]
  );
  const setAnimationPreset = useCallback(
    (entrance, exit, emphasis) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => {
        const idx = anims.findIndex((a) => a.elementId === selectedElement.id);
        const hasEffect = entrance || exit || emphasis;
        if (idx >= 0) {
          if (!hasEffect && !anims[idx].motionPath) {
            return anims.filter((a) => a.elementId !== selectedElement.id);
          }
          anims[idx] = {
            ...anims[idx],
            entrance,
            exit,
            emphasis
          };
        } else if (hasEffect) {
          anims.push({
            elementId: selectedElement.id,
            entrance,
            exit,
            emphasis,
            durationMs: 500,
            order: anims.length,
            trigger: "onClick"
          });
        }
        return anims;
      });
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleEntranceChange = useCallback(
    (event) => {
      const v = event.target.value;
      setAnimationPreset(
        v === "none" ? void 0 : v,
        selectedElementAnimation?.exit,
        selectedElementAnimation?.emphasis
      );
    },
    [setAnimationPreset, selectedElementAnimation]
  );
  const handleExitChange = useCallback(
    (event) => {
      const v = event.target.value;
      setAnimationPreset(
        selectedElementAnimation?.entrance,
        v === "none" ? void 0 : v,
        selectedElementAnimation?.emphasis
      );
    },
    [setAnimationPreset, selectedElementAnimation]
  );
  const handleEmphasisChange = useCallback(
    (event) => {
      const v = event.target.value;
      setAnimationPreset(
        selectedElementAnimation?.entrance,
        selectedElementAnimation?.exit,
        v === "none" ? void 0 : v
      );
    },
    [setAnimationPreset, selectedElementAnimation]
  );
  const handleMotionPathChange = useCallback(
    (presetId) => {
      if (!canEdit) {
        return;
      }
      if (presetId === "custom") {
        return;
      }
      updateAnimations(
        (anims) => presetId === "none" ? clearMotionPath(anims, selectedElement.id) : applyMotionPathPreset(anims, selectedElement.id, presetId)
      );
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleTriggerChange = useCallback(
    (event) => {
      const newTrigger = event.target.value;
      updateAnimationField("trigger", newTrigger);
      if (newTrigger !== "onShapeClick") {
        updateAnimationField("triggerShapeId", void 0);
      }
    },
    [updateAnimationField]
  );
  const handleTriggerShapeChange = useCallback(
    (e) => updateAnimationField("triggerShapeId", e.target.value || void 0),
    [updateAnimationField]
  );
  const handleTimingCurveChange = useCallback(
    (e) => updateAnimationField("timingCurve", e.target.value),
    [updateAnimationField]
  );
  const handleDurationChange = useCallback(
    (e) => updateAnimationField(
      "durationMs",
      Math.max(100, Math.min(1e4, Number(e.target.value) || 450))
    ),
    [updateAnimationField]
  );
  const handleDelayChange = useCallback(
    (e) => updateAnimationField("delayMs", Math.max(0, Math.min(1e4, Number(e.target.value) || 0))),
    [updateAnimationField]
  );
  const handleRepeatCountChange = useCallback(
    (e) => updateAnimationField("repeatCount", Math.max(1, Math.min(100, Number(e.target.value) || 1))),
    [updateAnimationField]
  );
  const handleRepeatModeChange = useCallback(
    (e) => {
      const v = e.target.value;
      updateAnimationField("repeatMode", v === "none" ? void 0 : v);
    },
    [updateAnimationField]
  );
  const handleDirectionChange = useCallback(
    (dir) => updateAnimationField("direction", dir),
    [updateAnimationField]
  );
  const handleSequenceChange = useCallback(
    (e) => updateAnimationField("sequence", e.target.value),
    [updateAnimationField]
  );
  const effectSoundState = useMemo(
    () => getEffectSoundState(activeSlide.animations ?? [], selectedElement.id),
    [activeSlide.animations, selectedElement.id]
  );
  const handleEffectSoundPick = useCallback(
    (pick) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => setEffectSound(anims, selectedElement.id, pick));
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleAfterAnimationChange = useCallback(
    (action) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => setAfterAnimation(anims, selectedElement.id, action));
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleAfterAnimationColorChange = useCallback(
    (color) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => setAfterAnimationColor(anims, selectedElement.id, color));
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const preview = useAnimationPreview({
    selectedElementId: selectedElement.id,
    selectedElementAnimation
  });
  const dragDrop = useAnimationDragDrop({ canEdit, rows: timelineRows, updateAnimations });
  const getTimelineLabel = useCallback(
    (anim) => {
      const el = activeSlide.elements?.find((e) => e.id === anim.elementId);
      if (!el) {
        return anim.elementId.slice(0, 8);
      }
      const text = hasTextProperties(el) ? el.text : void 0;
      return text || getElementLabel(el);
    },
    [activeSlide.elements]
  );
  const getNativeRowLabel = useCallback(
    (targetIds) => targetIds.map((id) => {
      const el = activeSlide.elements?.find((e) => e.id === id);
      if (!el) {
        return id.slice(0, 8);
      }
      const text = hasTextProperties(el) ? el.text : void 0;
      return text || getElementLabel(el);
    }).join(", "),
    [activeSlide.elements]
  );
  const hasAnimation = Boolean(
    selectedElementAnimation?.entrance || selectedElementAnimation?.exit || selectedElementAnimation?.emphasis || selectedElementAnimation?.motionPath
  );
  const showDirectionPicker = hasAnimation && (DIRECTIONAL_PRESETS.has(selectedElementAnimation?.entrance ?? "") || DIRECTIONAL_PRESETS.has(selectedElementAnimation?.exit ?? ""));
  const timelineBarData = useMemo(() => {
    const bars = buildAnimationTimelineBars(sortedAnimations);
    const barsByElementId = new Map(bars.map((bar) => [bar.elementId, bar]));
    return sortedAnimations.flatMap((anim) => {
      const bar = barsByElementId.get(anim.elementId);
      return bar ? [{ anim, leftPercent: bar.leftPercent, widthPercent: bar.widthPercent }] : [];
    });
  }, [sortedAnimations]);
  return {
    selectedElementAnimation,
    sortedAnimations,
    timelineRows,
    hasAnimation,
    showDirectionPicker,
    timelineBarData,
    handleEntranceChange,
    handleExitChange,
    handleEmphasisChange,
    handleTriggerChange,
    handleTriggerShapeChange,
    handleTimingCurveChange,
    handleDurationChange,
    handleDelayChange,
    handleRepeatCountChange,
    handleRepeatModeChange,
    handleDirectionChange,
    handleSequenceChange,
    handleMotionPathChange,
    effectSoundState,
    handleEffectSoundPick,
    handleAfterAnimationChange,
    handleAfterAnimationColorChange,
    getTimelineLabel,
    getNativeRowLabel,
    ...preview,
    ...dragDrop
  };
}
function AnimationPanel({
  selectedElement,
  activeSlide,
  canEdit,
  onUpdateSlide
}) {
  const { t } = useTranslation();
  const handlers = useAnimationHandlers({
    selectedElement,
    activeSlide,
    canEdit,
    onUpdateSlide
  });
  const {
    selectedElementAnimation,
    hasAnimation,
    showDirectionPicker,
    handleEntranceChange,
    handleExitChange,
    handleEmphasisChange,
    handleTriggerChange,
    handleTriggerShapeChange,
    handleTimingCurveChange,
    handleDurationChange,
    handleDelayChange,
    handleRepeatCountChange,
    handleRepeatModeChange,
    handleDirectionChange,
    handleSequenceChange,
    handleMotionPathChange,
    effectSoundState,
    handleEffectSoundPick,
    handleAfterAnimationChange,
    handleAfterAnimationColorChange,
    handlePreviewClick
  } = handlers;
  return /* @__PURE__ */ jsxs("div", { className: "rounded border border-border bg-card p-2 space-y-2", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.animation.title") }),
      hasAnimation && /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          className: "flex items-center gap-1 text-[10px] text-primary hover:text-primary/80 transition-colors",
          onClick: handlePreviewClick,
          title: t("pptx.animation.preview"),
          children: [
            /* @__PURE__ */ jsx(LuPlay, { className: "w-3 h-3" }),
            t("pptx.animation.preview")
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.entrance") }),
      /* @__PURE__ */ jsxs(
        "select",
        {
          "aria-label": t("pptx.animation.entrance"),
          value: selectedElementAnimation?.entrance ?? "none",
          onChange: handleEntranceChange,
          disabled: !canEdit,
          className: SELECT_CLS,
          children: [
            /* @__PURE__ */ jsx("option", { value: "none", children: t("pptx.animation.none") }),
            ENTRANCE_PRESETS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(`pptx.animation.preset.${o.value}`) }, o.value))
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.emphasis") }),
      /* @__PURE__ */ jsxs(
        "select",
        {
          "aria-label": t("pptx.animation.emphasis"),
          value: selectedElementAnimation?.emphasis ?? "none",
          onChange: handleEmphasisChange,
          disabled: !canEdit,
          className: SELECT_CLS,
          children: [
            /* @__PURE__ */ jsx("option", { value: "none", children: t("pptx.animation.none") }),
            EMPHASIS_PRESETS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(`pptx.animation.preset.${o.value}`) }, o.value))
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.exit") }),
      /* @__PURE__ */ jsxs(
        "select",
        {
          "aria-label": t("pptx.animation.exit"),
          value: selectedElementAnimation?.exit ?? "none",
          onChange: handleExitChange,
          disabled: !canEdit,
          className: SELECT_CLS,
          children: [
            /* @__PURE__ */ jsx("option", { value: "none", children: t("pptx.animation.none") }),
            EXIT_PRESETS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(`pptx.animation.preset.${o.value}`) }, o.value))
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      MotionPathRow,
      {
        motionPath: selectedElementAnimation?.motionPath,
        canEdit,
        onChange: handleMotionPathChange
      }
    ),
    hasAnimation && /* @__PURE__ */ jsxs(Fragment, { children: [
      showDirectionPicker && /* @__PURE__ */ jsxs("div", { className: "pt-1 border-t border-border", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px] block mb-1", children: t("pptx.animation.direction") }),
        /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: DIRECTION_OPTIONS.map((opt) => {
          const Icon = opt.icon;
          const isActive = selectedElementAnimation?.direction === opt.value;
          return /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canEdit,
              className: cn(
                "flex items-center justify-center w-7 h-7 rounded border transition-colors",
                isActive ? "border-primary bg-primary/20 text-primary" : "border-border bg-muted text-muted-foreground hover:bg-accent"
              ),
              onClick: () => handleDirectionChange(opt.value),
              title: t(opt.labelKey),
              children: /* @__PURE__ */ jsx(Icon, { className: "w-3.5 h-3.5" })
            },
            opt.value
          );
        }) })
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.sequence") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.animation.sequence"),
            value: selectedElementAnimation?.sequence ?? "asOne",
            onChange: handleSequenceChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: SEQUENCE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "pt-1 border-t border-border flex flex-col gap-2", children: [
        /* @__PURE__ */ jsx(
          EffectSoundRow,
          {
            soundState: effectSoundState,
            canEdit,
            onPick: handleEffectSoundPick
          }
        ),
        /* @__PURE__ */ jsx(
          AfterAnimationRow,
          {
            action: selectedElementAnimation?.afterAnimation ?? "none",
            color: selectedElementAnimation?.afterAnimationColor,
            canEdit,
            onActionChange: handleAfterAnimationChange,
            onColorChange: handleAfterAnimationColorChange
          }
        )
      ] }),
      /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground pt-1 border-t border-border", children: t("pptx.animation.timing") }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.trigger") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.animation.trigger"),
            value: selectedElementAnimation?.trigger ?? "onClick",
            onChange: handleTriggerChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: TRIGGER_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] }),
      selectedElementAnimation?.trigger === "onShapeClick" && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.trigger.shapeLabel") }),
        /* @__PURE__ */ jsxs(
          "select",
          {
            "aria-label": t("pptx.animation.trigger.shapeLabel"),
            value: selectedElementAnimation?.triggerShapeId ?? "",
            onChange: handleTriggerShapeChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: [
              /* @__PURE__ */ jsx("option", { value: "", children: t("pptx.animation.trigger.selectShape") }),
              activeSlide.elements.filter((el) => el.id !== selectedElement.id).map((el) => /* @__PURE__ */ jsx("option", { value: el.id, children: getElementLabel(el) }, el.id))
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.duration") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 100,
            max: 1e4,
            step: 50,
            disabled: !canEdit,
            value: selectedElementAnimation?.durationMs ?? 450,
            onChange: handleDurationChange,
            className: INPUT_CLS
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.delay") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 1e4,
            step: 50,
            disabled: !canEdit,
            value: selectedElementAnimation?.delayMs ?? 0,
            onChange: handleDelayChange,
            className: INPUT_CLS
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.timingCurve") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.animation.timingCurve"),
            value: selectedElementAnimation?.timingCurve ?? "ease",
            onChange: handleTimingCurveChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: TIMING_CURVE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.repeatCount") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 1,
            max: 100,
            step: 1,
            disabled: !canEdit,
            value: selectedElementAnimation?.repeatCount ?? 1,
            onChange: handleRepeatCountChange,
            className: INPUT_CLS
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.repeatUntil") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.animation.repeatUntil"),
            value: selectedElementAnimation?.repeatMode ?? "none",
            onChange: handleRepeatModeChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: REPEAT_MODE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx(
      AnimationTimelineSection,
      {
        selectedElementId: selectedElement.id,
        canEdit,
        handlers
      }
    )
  ] });
}
var INSPECTOR_TABS = [
  { key: "elements", label: "Elements", icon: LuLayers },
  { key: "properties", label: "Properties", icon: LuSettings2 },
  { key: "comments", label: "Comments", icon: LuMessageSquare }
];
var HEADING = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD = "rounded border border-border bg-card p-2 space-y-2";
var INPUT = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var POS_FIELDS = [
  ["X", "x"],
  ["Y", "y"],
  ["W", "width"],
  ["H", "height"]
];
var URL_LABEL_KEY = {
  url: "pptx.action.gotoUrl",
  openFile: "pptx.hyperlink.actionOpenFile",
  openPresentation: "pptx.hyperlink.actionOpenPresentation"
};
function ActionTargetFields({
  type,
  canEdit,
  slideCount,
  customShows,
  url,
  slideIndex,
  customShowId,
  returnAfter,
  onChange
}) {
  const { t } = useTranslation();
  const urlLabelKey = URL_LABEL_KEY[type];
  if (urlLabelKey) {
    return /* @__PURE__ */ jsx(
      "input",
      {
        type: "text",
        disabled: !canEdit,
        "aria-label": t(urlLabelKey),
        className: cn(INPUT, "w-full"),
        placeholder: "https://...",
        value: url ?? "",
        onChange: (e) => onChange({ url: e.target.value })
      }
    );
  }
  if (type === "slide") {
    return /* @__PURE__ */ jsx(
      "input",
      {
        type: "number",
        disabled: !canEdit,
        "aria-label": t("pptx.action.gotoSlide"),
        className: cn(INPUT, "w-full"),
        placeholder: t("pptx.action.slideNumberPlaceholder"),
        min: 1,
        max: slideCount,
        value: (slideIndex ?? 0) + 1,
        onChange: (e) => {
          const idx = toSlideIndex(Number(e.target.value), slideCount);
          if (idx !== void 0) {
            onChange({ slideIndex: idx });
          }
        }
      }
    );
  }
  if (type === "customShow") {
    return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
      /* @__PURE__ */ jsxs(
        "select",
        {
          "data-testid": "pptx-action-custom-show",
          disabled: !canEdit,
          "aria-label": t("pptx.hyperlink.customShowLabel"),
          className: cn(INPUT, "w-full"),
          value: customShowId ?? "",
          onChange: (e) => onChange({ customShowId: e.target.value, returnAfter }),
          children: [
            /* @__PURE__ */ jsx("option", { value: "", children: t("pptx.hyperlink.customShowLabel") }),
            customShows.map((show) => /* @__PURE__ */ jsx("option", { value: show.id, children: show.name }, show.id))
          ]
        }
      ),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1.5 text-muted-foreground", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            "data-testid": "pptx-action-custom-show-return",
            disabled: !canEdit,
            checked: returnAfter ?? false,
            onChange: (e) => onChange({ customShowId, returnAfter: e.target.checked })
          }
        ),
        t("pptx.hyperlink.customShowReturn")
      ] })
    ] });
  }
  return null;
}
function ActionSettingsPanel({
  selectedElement,
  slides,
  canEdit,
  customShows,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const clickAction = selectedElement.actionClick ? pptxActionToElementAction(selectedElement.actionClick, "click") : void 0;
  const hoverAction = selectedElement.actionHover ? pptxActionToElementAction(selectedElement.actionHover, "hover") : void 0;
  const updateAction = (trigger, type, patch) => {
    const ea = { trigger, type, ...patch };
    const pa = elementActionToPptxAction(ea);
    if (trigger === "click") {
      onUpdateElement({ actionClick: pa });
    } else {
      onUpdateElement({ actionHover: pa });
    }
  };
  const changeType = (trigger, type) => {
    const current = trigger === "click" ? clickAction : hoverAction;
    const target = {
      url: current?.url,
      slideIndex: current?.slideIndex,
      customShowId: current?.customShowId
    };
    if (canCommitActionType(type, target)) {
      updateAction(trigger, type, target);
    }
  };
  return /* @__PURE__ */ jsxs("div", { className: CARD, "data-pptx-action-settings": true, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.action.title", "Action") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-2 text-[11px]", children: [
      /* @__PURE__ */ jsx(
        ActionTriggerSection,
        {
          label: t("pptx.action.onClick", "On Click"),
          trigger: "click",
          activeType: clickAction?.type,
          action: clickAction,
          fallbackUrl: selectedElement.actionClick?.url,
          fallbackSlideIndex: selectedElement.actionClick?.targetSlideIndex,
          canEdit,
          slideCount: slides.length,
          customShows,
          onChangeType: (type) => changeType("click", type),
          onChangeTarget: (type, patch) => updateAction("click", type, patch)
        },
        `click-${selectedElement.id}`
      ),
      /* @__PURE__ */ jsx(
        ActionTriggerSection,
        {
          label: t("pptx.action.onHover", "On Hover"),
          trigger: "hover",
          activeType: hoverAction?.type,
          action: hoverAction,
          fallbackUrl: selectedElement.actionHover?.url,
          fallbackSlideIndex: selectedElement.actionHover?.targetSlideIndex,
          canEdit,
          slideCount: slides.length,
          customShows,
          onChangeType: (type) => changeType("hover", type),
          onChangeTarget: (type, patch) => updateAction("hover", type, patch)
        },
        `hover-${selectedElement.id}`
      )
    ] })
  ] });
}
function ActionTriggerSection({
  label,
  trigger,
  activeType,
  action,
  fallbackUrl,
  fallbackSlideIndex,
  canEdit,
  slideCount,
  customShows,
  onChangeType,
  onChangeTarget
}) {
  const { t } = useTranslation();
  const [pendingType, setPendingType] = useState(void 0);
  const effectiveType = resolveActionType(pendingType, activeType);
  const changeType = (type) => {
    setPendingType(type);
    onChangeType(type);
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", "data-pptx-action-trigger": trigger, children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground font-medium", children: label }),
    /* @__PURE__ */ jsx(
      "select",
      {
        disabled: !canEdit,
        "aria-label": label,
        className: cn(INPUT, "w-full"),
        value: effectiveType,
        onChange: (e) => changeType(e.target.value),
        children: ELEMENT_ACTION_TYPE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.labelKey) }, o.value))
      }
    ),
    /* @__PURE__ */ jsx(
      ActionTargetFields,
      {
        type: effectiveType,
        canEdit,
        slideCount,
        customShows,
        url: action?.url ?? fallbackUrl,
        slideIndex: action?.slideIndex ?? fallbackSlideIndex,
        customShowId: action?.customShowId,
        returnAfter: action?.returnAfter,
        onChange: (patch) => onChangeTarget(effectiveType, patch)
      }
    )
  ] });
}

// src/viewer/components/inspector/chart-panel-constants.ts
var HEADING2 = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD2 = "rounded border border-border bg-card p-2 space-y-2";
var INPUT2 = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN2 = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var CELL_INPUT = "bg-muted border border-border rounded px-1 py-0.5 text-[11px] w-full text-center";
var GROUPING_OPTIONS = [
  { value: "clustered", labelKey: "pptx.chart.groupingClustered" },
  { value: "stacked", labelKey: "pptx.chart.groupingStacked" },
  { value: "percentStacked", labelKey: "pptx.chart.groupingPercentStacked" }
];
var LEGEND_POSITION_OPTIONS = [
  { value: "t", labelKey: "pptx.chart.legendTop" },
  { value: "b", labelKey: "pptx.chart.legendBottom" },
  { value: "l", labelKey: "pptx.chart.legendLeft" },
  { value: "r", labelKey: "pptx.chart.legendRight" }
];
var TICK_LABEL_POSITION_OPTIONS = [
  { value: "nextTo", labelKey: "pptx.chart.tickNextTo" },
  { value: "high", labelKey: "pptx.chart.tickHigh" },
  { value: "low", labelKey: "pptx.chart.tickLow" },
  { value: "none", labelKey: "pptx.chart.tickNone" }
];
var DISPLAY_UNITS_OPTIONS = [
  { value: "", labelKey: "pptx.chart.unitsNone" },
  { value: "hundreds", labelKey: "pptx.chart.unitsHundreds" },
  { value: "thousands", labelKey: "pptx.chart.unitsThousands" },
  { value: "tenThousands", labelKey: "pptx.chart.unitsTenThousands" },
  { value: "hundredThousands", labelKey: "pptx.chart.unitsHundredThousands" },
  { value: "millions", labelKey: "pptx.chart.unitsMillions" },
  { value: "tenMillions", labelKey: "pptx.chart.unitsTenMillions" },
  { value: "hundredMillions", labelKey: "pptx.chart.unitsHundredMillions" },
  { value: "billions", labelKey: "pptx.chart.unitsBillions" },
  { value: "trillions", labelKey: "pptx.chart.unitsTrillions" }
];
var DATA_LABEL_CONTENT_OPTIONS = [
  { key: "showValue", labelKey: "pptx.chart.labelValue" },
  { key: "showCategory", labelKey: "pptx.chart.labelCategory" },
  { key: "showSeriesName", labelKey: "pptx.chart.labelSeriesName" },
  { key: "showPercent", labelKey: "pptx.chart.labelPercent" },
  { key: "showLegendKey", labelKey: "pptx.chart.labelLegendKey" }
];
var DATA_LABEL_POSITION_OPTIONS = [
  { value: "", labelKey: "pptx.chart.labelPosDefault" },
  { value: "ctr", labelKey: "pptx.chart.labelPosCenter" },
  { value: "inEnd", labelKey: "pptx.chart.labelPosInsideEnd" },
  { value: "inBase", labelKey: "pptx.chart.labelPosInsideBase" },
  { value: "outEnd", labelKey: "pptx.chart.labelPosOutsideEnd" },
  { value: "bestFit", labelKey: "pptx.chart.labelPosBestFit" }
];
var TRENDLINE_TYPE_OPTIONS = [
  { value: "", labelKey: "pptx.chart.trendlineNone" },
  { value: "linear", labelKey: "pptx.chart.trendlineLinear" },
  { value: "exponential", labelKey: "pptx.chart.trendlineExponential" },
  { value: "logarithmic", labelKey: "pptx.chart.trendlineLogarithmic" },
  { value: "polynomial", labelKey: "pptx.chart.trendlinePolynomial" },
  { value: "power", labelKey: "pptx.chart.trendlinePower" },
  { value: "movingAvg", labelKey: "pptx.chart.trendlineMovingAvg" }
];
var TRENDLINE_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "bar",
  "line",
  "area",
  "scatter",
  "bubble"
]);
var ERROR_BAR_VALTYPE_OPTIONS = [
  { value: "", labelKey: "pptx.chart.errorBarNone" },
  { value: "fixedVal", labelKey: "pptx.chart.errorBarFixed" },
  { value: "percentage", labelKey: "pptx.chart.errorBarPercentage" },
  { value: "stdDev", labelKey: "pptx.chart.errorBarStdDev" },
  { value: "stdErr", labelKey: "pptx.chart.errorBarStdErr" }
];
var ERROR_BAR_TYPE_OPTIONS = [
  { value: "both", labelKey: "pptx.chart.errorBarBoth" },
  { value: "plus", labelKey: "pptx.chart.errorBarPlus" },
  { value: "minus", labelKey: "pptx.chart.errorBarMinus" }
];
var ERROR_BAR_VALUE_TYPES = /* @__PURE__ */ new Set(["fixedVal", "percentage", "stdDev"]);
var ERROR_BAR_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "bar",
  "line",
  "area",
  "scatter",
  "bubble"
]);
var GROUPING_SUPPORTED_TYPES = /* @__PURE__ */ new Set(["bar", "line", "area"]);
var MARKER_SYMBOL_OPTIONS = [
  { value: "", labelKey: "pptx.chart.markerAuto" },
  { value: "none", labelKey: "pptx.chart.markerNone" },
  { value: "circle", labelKey: "pptx.chart.markerCircle" },
  { value: "square", labelKey: "pptx.chart.markerSquare" },
  { value: "diamond", labelKey: "pptx.chart.markerDiamond" },
  { value: "triangle", labelKey: "pptx.chart.markerTriangle" },
  { value: "x", labelKey: "pptx.chart.markerX" },
  { value: "star", labelKey: "pptx.chart.markerStar" },
  { value: "plus", labelKey: "pptx.chart.markerPlus" },
  { value: "dot", labelKey: "pptx.chart.markerDot" },
  { value: "dash", labelKey: "pptx.chart.markerDash" }
];
var MARKER_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "line",
  "scatter",
  "bubble",
  "radar"
]);
var GRIDLINE_DASH_OPTIONS = [
  { value: "", labelKey: "pptx.chart.dashDefault" },
  { value: "solid", labelKey: "pptx.chart.dashSolid" },
  { value: "dash", labelKey: "pptx.chart.dashDash" },
  { value: "dot", labelKey: "pptx.chart.dashDot" },
  { value: "dashDot", labelKey: "pptx.chart.dashDashDot" },
  { value: "lgDash", labelKey: "pptx.chart.dashLong" }
];
var COMBO_SERIES_TYPE_OPTIONS = [
  { value: "", labelKey: "pptx.chart.comboDefault" },
  { value: "bar", labelKey: "pptx.chart.typeBar" },
  { value: "line", labelKey: "pptx.chart.typeLine" },
  { value: "area", labelKey: "pptx.chart.typeArea" },
  { value: "scatter", labelKey: "pptx.chart.typeScatter" }
];
var COMBO_SUPPORTED_TYPES = /* @__PURE__ */ new Set(["bar", "line", "area", "combo"]);
var EXPLOSION_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "pie",
  "pie3D",
  "doughnut",
  "ofPie"
]);
var AXIS_ROWS = [
  { type: "valAx", labelKey: "pptx.chart.valueAxis", hasScale: true },
  { type: "dateAx", labelKey: "pptx.chart.dateAxis", hasScale: true },
  { type: "catAx", labelKey: "pptx.chart.categoryAxis", hasScale: false }
];
function ChartAxisOptions({ axes, canEdit, onUpdateAxis }) {
  const { t } = useTranslation();
  const rows = AXIS_ROWS.map((row) => ({
    ...row,
    axis: axes?.find((a) => a.axisType === row.type)
  })).filter((row) => Boolean(row.axis));
  if (rows.length === 0) {
    return null;
  }
  const numberField = (axisType, labelKey, value, key) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
    /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t(labelKey) }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "number",
        disabled: !canEdit,
        className: INPUT2,
        value: value ?? "",
        placeholder: t("pptx.chart.auto"),
        onChange: (e) => {
          const raw = e.target.value;
          if (raw === "") {
            onUpdateAxis(axisType, { [key]: void 0 });
            return;
          }
          const num = Number.parseFloat(raw);
          if (Number.isFinite(num)) {
            onUpdateAxis(axisType, { [key]: num });
          }
        }
      }
    )
  ] });
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.axes") }),
    rows.map(({ type, labelKey, hasScale, axis }) => /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[11px] font-medium", children: t(labelKey) }),
      hasScale && /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 ml-2", children: [
        numberField(type, "pptx.chart.min", axis.min, "min"),
        numberField(type, "pptx.chart.max", axis.max, "max"),
        numberField(type, "pptx.chart.majorUnit", axis.majorUnit, "majorUnit"),
        numberField(type, "pptx.chart.minorUnit", axis.minorUnit, "minorUnit"),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.displayUnits") }),
          /* @__PURE__ */ jsx(
            "select",
            {
              "aria-label": t("pptx.chart.displayUnits"),
              disabled: !canEdit,
              className: INPUT2,
              value: axis.displayUnits ?? "",
              onChange: (e) => onUpdateAxis(type, {
                displayUnits: e.target.value || void 0
              }),
              children: DISPLAY_UNITS_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 ml-2", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.axisTitle") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "text",
              disabled: !canEdit,
              className: INPUT2,
              value: axis.titleText ?? "",
              placeholder: t("pptx.chart.axisTitlePlaceholder"),
              onChange: (e) => onUpdateAxis(type, { titleText: e.target.value })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.numberFormat") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "text",
              disabled: !canEdit,
              className: INPUT2,
              value: axis.numFmt?.formatCode ?? "",
              placeholder: t("pptx.settings.general"),
              onChange: (e) => onUpdateAxis(type, {
                numFmt: e.target.value ? { formatCode: e.target.value, sourceLinked: false } : void 0
              })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.tickLabels") }),
          /* @__PURE__ */ jsx(
            "select",
            {
              "aria-label": t("pptx.chart.tickLabels"),
              disabled: !canEdit,
              className: INPUT2,
              value: axis.tickLblPos ?? "nextTo",
              onChange: (e) => onUpdateAxis(type, {
                tickLblPos: e.target.value
              }),
              children: TICK_LABEL_POSITION_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: axis.majorGridlines ?? false,
              onChange: (e) => onUpdateAxis(type, { majorGridlines: e.target.checked }),
              className: "accent-primary"
            }
          ),
          /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.majorGridlines") })
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: axis.minorGridlines ?? false,
              onChange: (e) => onUpdateAxis(type, { minorGridlines: e.target.checked }),
              className: "accent-primary"
            }
          ),
          /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.minorGridlines") })
        ] })
      ] })
    ] }, type))
  ] });
}
var SCALE_AXES = [
  { type: "valAx", labelKey: "pptx.chart.valueAxis" },
  { type: "dateAx", labelKey: "pptx.chart.dateAxis" },
  { type: "catAx", labelKey: "pptx.chart.categoryAxis" }
];
function ChartAxisStyleOptions({
  axes,
  canEdit,
  onSetLogScale,
  onSetTitleStyle,
  onSetGridlineStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const rows = SCALE_AXES.map((row) => ({
    ...row,
    axis: axes?.find((a) => a.axisType === row.type)
  })).filter((row) => Boolean(row.axis));
  if (rows.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.axisStyling") }),
    rows.map(({ type, labelKey, axis }) => {
      const hasScale = type === "valAx" || type === "dateAx";
      return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
        /* @__PURE__ */ jsx("div", { className: "text-[11px] font-medium", children: t(labelKey) }),
        /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 ml-2", children: [
          hasScale && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "checkbox",
                  disabled: !canEdit,
                  checked: axis.logScale ?? false,
                  onChange: (e) => onSetLogScale(type, { enabled: e.target.checked, base: axis.logBase }),
                  className: "accent-primary"
                }
              ),
              /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.logScale") })
            ] }),
            axis.logScale && /* @__PURE__ */ jsx(
              "input",
              {
                type: "number",
                min: 2,
                disabled: !canEdit,
                title: t("pptx.chart.logBase"),
                className: "w-16 bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
                value: axis.logBase ?? 10,
                onChange: (e) => {
                  const num = Number.parseFloat(e.target.value);
                  if (Number.isFinite(num) && num > 1) {
                    onSetLogScale(type, { enabled: true, base: num });
                  }
                }
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
            /* @__PURE__ */ jsx("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.titleFont") }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                disabled: !canEdit,
                className: INPUT2,
                placeholder: t("pptx.chart.auto"),
                value: axis.fontFamily ?? "",
                onChange: (e) => onSetTitleStyle(type, { fontFamily: e.target.value || null })
              }
            ),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "number",
                min: 4,
                max: 96,
                disabled: !canEdit,
                title: t("pptx.chart.fontSize"),
                className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
                value: axis.fontSize ?? "",
                placeholder: t("pptx.chart.auto"),
                onChange: (e) => {
                  const raw = e.target.value;
                  const num = Number.parseFloat(raw);
                  onSetTitleStyle(type, { fontSize: Number.isFinite(num) ? num : null });
                }
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 text-[11px]", children: [
            /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 cursor-pointer", children: [
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "checkbox",
                  disabled: !canEdit,
                  checked: axis.fontBold ?? false,
                  onChange: (e) => onSetTitleStyle(type, { fontBold: e.target.checked }),
                  className: "accent-primary"
                }
              ),
              /* @__PURE__ */ jsx("span", { children: t("pptx.chart.bold") })
            ] }),
            /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.titleColor") }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "color",
                  disabled: !canEdit,
                  className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
                  value: axis.fontColor ?? "#000000",
                  onChange: (e) => {
                    onSetTitleStyle(type, { fontColor: e.target.value });
                    pushColor(e.target.value);
                  }
                }
              )
            ] })
          ] }),
          ["major", "minor"].map((which) => {
            const spPr = which === "major" ? axis.majorGridlinesSpPr : axis.minorGridlinesSpPr;
            const enabled = which === "major" ? axis.majorGridlines : axis.minorGridlines;
            if (!enabled) {
              return null;
            }
            return /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
              /* @__PURE__ */ jsx("span", { className: "w-12 text-muted-foreground shrink-0", children: t(
                which === "major" ? "pptx.chart.majorGridlines" : "pptx.chart.minorGridlines"
              ) }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "color",
                  disabled: !canEdit,
                  title: t("pptx.chart.gridlineColor"),
                  className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
                  value: spPr?.strokeColor ?? "#d9d9d9",
                  onChange: (e) => {
                    onSetGridlineStyle(type, which, { color: e.target.value });
                    pushColor(e.target.value);
                  }
                }
              ),
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "number",
                  min: 0.25,
                  step: 0.25,
                  disabled: !canEdit,
                  title: t("pptx.chart.gridlineWidth"),
                  className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
                  value: spPr?.strokeWidth ?? "",
                  placeholder: t("pptx.chart.auto"),
                  onChange: (e) => {
                    const num = Number.parseFloat(e.target.value);
                    onSetGridlineStyle(type, which, {
                      width: Number.isFinite(num) ? num : null
                    });
                  }
                }
              ),
              /* @__PURE__ */ jsx(
                "select",
                {
                  disabled: !canEdit,
                  title: t("pptx.chart.gridlineDash"),
                  className: INPUT2,
                  value: spPr?.strokeDashStyle ?? "",
                  onChange: (e) => onSetGridlineStyle(type, which, { dashStyle: e.target.value || null }),
                  children: GRIDLINE_DASH_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
                }
              )
            ] }, which);
          })
        ] })
      ] }, type);
    })
  ] });
}
function ChartComboTypeOptions({
  chartType,
  series,
  canEdit,
  onSetSeriesType
}) {
  const { t } = useTranslation();
  if (!COMBO_SUPPORTED_TYPES.has(chartType) || series.length < 2) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.comboTypes") }),
    /* @__PURE__ */ jsx("div", { className: "space-y-1.5", children: series.map((s, i) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
      /* @__PURE__ */ jsx(
        "select",
        {
          disabled: !canEdit,
          className: INPUT2,
          value: s.seriesChartType ?? "",
          onChange: (e) => {
            const value = e.target.value;
            onSetSeriesType(i, value === "" ? null : value);
          },
          children: COMBO_SERIES_TYPE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
        }
      )
    ] }, `${s.name}-${i}`)) })
  ] });
}
function ChartDataGrid({
  series,
  categories,
  canEdit,
  highlightCell,
  onUpdateSeries,
  onUpdateCategoryLabel,
  onUpdateValue,
  onAddCategory,
  onRemoveCategory,
  onAddSeries,
  onRemoveSeries
}) {
  const { t } = useTranslation();
  const highlightRef = useRef(null);
  useEffect(() => {
    highlightRef.current?.scrollIntoView({ block: "nearest", inline: "nearest" });
  }, [highlightCell?.seriesIndex, highlightCell?.pointIndex]);
  const highlightClass = " ring-1 ring-primary";
  const isSeriesHighlight = (si) => highlightCell?.seriesIndex === si && highlightCell.pointIndex === void 0;
  const isValueHighlight = (si, ci) => highlightCell?.seriesIndex === si && highlightCell.pointIndex === ci;
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.data") }),
      canEdit && /* @__PURE__ */ jsxs("div", { className: "flex gap-1", children: [
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            className: BTN2,
            title: t("pptx.chart.addCategory"),
            onClick: onAddCategory,
            children: [
              /* @__PURE__ */ jsx(LuPlus, { className: "inline w-3 h-3 mr-0.5" }),
              t("pptx.chart.cat")
            ]
          }
        ),
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            className: BTN2,
            title: t("pptx.chart.addSeries"),
            onClick: onAddSeries,
            children: [
              /* @__PURE__ */ jsx(LuPlus, { className: "inline w-3 h-3 mr-0.5" }),
              t("pptx.chart.seriesShort")
            ]
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx("div", { className: "overflow-x-auto", children: /* @__PURE__ */ jsxs("table", { className: "w-full text-[11px] border-collapse", children: [
      /* @__PURE__ */ jsx("thead", { children: /* @__PURE__ */ jsxs("tr", { children: [
        /* @__PURE__ */ jsx(
          "th",
          {
            "aria-label": t("pptx.chart.categories"),
            className: "text-muted-foreground p-0.5 text-left min-w-[60px]"
          }
        ),
        series.map((s, si) => /* @__PURE__ */ jsx("th", { className: "p-0.5 font-normal min-w-[72px]", children: /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-0.5", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "text",
              disabled: !canEdit,
              ref: isSeriesHighlight(si) ? highlightRef : null,
              className: `${CELL_INPUT}${isSeriesHighlight(si) ? highlightClass : ""}`,
              value: s.name,
              onChange: (e) => onUpdateSeries(si, { name: e.target.value })
            }
          ),
          canEdit && series.length > 1 && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "text-muted-foreground hover:text-red-400 shrink-0",
              title: t("pptx.chart.removeSeries"),
              onClick: () => onRemoveSeries(si),
              children: /* @__PURE__ */ jsx(LuTrash2, { className: "w-3 h-3" })
            }
          )
        ] }) }, si))
      ] }) }),
      /* @__PURE__ */ jsx("tbody", { children: categories.map((cat, ci) => /* @__PURE__ */ jsxs("tr", { children: [
        /* @__PURE__ */ jsx("td", { className: "p-0.5", children: /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-0.5", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "text",
              disabled: !canEdit,
              className: CELL_INPUT,
              value: cat,
              onChange: (e) => onUpdateCategoryLabel(ci, e.target.value)
            }
          ),
          canEdit && categories.length > 1 && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "text-muted-foreground hover:text-red-400 shrink-0",
              title: t("pptx.chart.removeCategory"),
              onClick: () => onRemoveCategory(ci),
              children: /* @__PURE__ */ jsx(LuTrash2, { className: "w-3 h-3" })
            }
          )
        ] }) }),
        series.map((s, si) => /* @__PURE__ */ jsx("td", { className: "p-0.5", children: /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            "aria-label": `${s.name} value ${ci + 1}`,
            disabled: !canEdit,
            ref: isValueHighlight(si, ci) ? highlightRef : null,
            className: `${CELL_INPUT}${isValueHighlight(si, ci) ? highlightClass : ""}`,
            value: s.values[ci] ?? 0,
            onChange: (e) => onUpdateValue(si, ci, e.target.value)
          }
        ) }, si))
      ] }, ci)) })
    ] }) })
  ] });
}
function ChartDataLabelOptions({
  style,
  canEdit,
  onUpdateStyle
}) {
  const { t } = useTranslation();
  if (!style?.hasDataLabels) {
    return null;
  }
  const labels = style.dataLabels ?? {};
  const patchLabels = (patch) => onUpdateStyle({ dataLabels: { ...labels, ...patch } });
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.dataLabels") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      DATA_LABEL_CONTENT_OPTIONS.map((opt) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: labels[opt.key] ?? false,
            onChange: (e) => patchLabels({ [opt.key]: e.target.checked }),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t(opt.labelKey) })
      ] }, opt.key)),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.labelPosition") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.chart.labelPosition"),
            disabled: !canEdit,
            className: INPUT2,
            value: labels.position ?? "",
            onChange: (e) => patchLabels({
              position: e.target.value || void 0
            }),
            children: DATA_LABEL_POSITION_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] })
    ] })
  ] });
}
var SYMBOL_OPTIONS = MARKER_SYMBOL_OPTIONS.filter((o) => o.value !== "");
function ChartDataPointMarkerOptions({
  chartType,
  categories,
  series,
  canEdit,
  onSetPointMarker
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const [seriesIndex, setSeriesIndex] = useState(0);
  if (!MARKER_SUPPORTED_TYPES.has(chartType) || series.length === 0 || categories.length === 0) {
    return null;
  }
  const activeSeries = series[Math.min(seriesIndex, series.length - 1)];
  const pointFor = (idx) => activeSeries.dataPoints?.find((p) => p.idx === idx);
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.pointMarkers") }),
    series.length > 1 && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.series") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.chart.series"),
          disabled: !canEdit,
          className: "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full",
          value: Math.min(seriesIndex, series.length - 1),
          onChange: (e) => setSeriesIndex(Number.parseInt(e.target.value, 10)),
          children: series.map((s, i) => /* @__PURE__ */ jsx("option", { value: i, children: s.name }, `${s.name}-${i}`))
        }
      )
    ] }),
    /* @__PURE__ */ jsx("div", { className: "space-y-2", children: categories.map((cat, idx) => {
      const point2 = pointFor(idx);
      const hasOverride = point2?.marker !== void 0;
      return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: cat, children: cat }),
          /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 shrink-0", children: [
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: hasOverride,
                onChange: (e) => {
                  onSetPointMarker(
                    seriesIndex,
                    idx,
                    e.target.checked ? { symbol: "circle" } : null
                  );
                }
              }
            ),
            /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.markerOverride") })
          ] })
        ] }),
        hasOverride && point2?.marker && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 ml-2 flex-wrap", children: [
          /* @__PURE__ */ jsx(
            "select",
            {
              disabled: !canEdit,
              className: "bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
              value: point2.marker.symbol,
              onChange: (e) => onSetPointMarker(seriesIndex, idx, {
                symbol: e.target.value
              }),
              children: SYMBOL_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          ),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 1,
              max: 20,
              disabled: !canEdit,
              title: t("pptx.chart.markerSize"),
              className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
              value: point2.marker.size ?? "",
              placeholder: t("pptx.chart.auto"),
              onChange: (e) => {
                const num = Number.parseInt(e.target.value, 10);
                onSetPointMarker(seriesIndex, idx, {
                  size: Number.isFinite(num) ? num : void 0
                });
              }
            }
          ),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              title: t("pptx.chart.markerFill"),
              className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
              value: point2.marker.spPr?.fillColor ?? "#4472c4",
              onChange: (e) => {
                onSetPointMarker(seriesIndex, idx, { fillColor: e.target.value });
                pushColor(e.target.value);
              }
            }
          )
        ] })
      ] }, `${cat}-${idx}`);
    }) })
  ] });
}
function ChartDataPointOptions({
  chartType,
  categories,
  series,
  canEdit,
  onSetPointFill,
  onSetPointExplosion,
  onSetPointLabel
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const [seriesIndex, setSeriesIndex] = useState(0);
  if (series.length === 0 || categories.length === 0) {
    return null;
  }
  const activeSeries = series[Math.min(seriesIndex, series.length - 1)];
  const showExplosion = EXPLOSION_SUPPORTED_TYPES.has(chartType);
  const pointFor = (idx) => activeSeries.dataPoints?.find((p) => p.idx === idx);
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.dataPoints") }),
    series.length > 1 && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.series") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.chart.series"),
          disabled: !canEdit,
          className: INPUT2,
          value: Math.min(seriesIndex, series.length - 1),
          onChange: (e) => setSeriesIndex(Number.parseInt(e.target.value, 10)),
          children: series.map((s, i) => /* @__PURE__ */ jsx("option", { value: i, children: s.name }, `${s.name}-${i}`))
        }
      )
    ] }),
    /* @__PURE__ */ jsx("div", { className: "space-y-1.5", children: categories.map((cat, idx) => {
      const point2 = pointFor(idx);
      return /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: cat, children: cat }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            disabled: !canEdit,
            title: t("pptx.chart.pointLabelOverride"),
            className: "w-20 bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
            value: activeSeries.dataLabels?.find((l) => l.idx === idx)?.text ?? "",
            placeholder: t("pptx.chart.auto"),
            onChange: (e) => {
              const raw = e.target.value;
              onSetPointLabel(seriesIndex, idx, raw === "" ? null : raw);
            }
          }
        ),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            title: t("pptx.chart.pointFill"),
            className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
            value: point2?.spPr?.fillColor ?? activeSeries.color ?? "#4472c4",
            onChange: (e) => {
              onSetPointFill(seriesIndex, idx, e.target.value);
              pushColor(e.target.value);
            }
          }
        ),
        point2?.spPr?.fillColor && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            title: t("pptx.chart.pointFillClear"),
            className: "text-muted-foreground hover:text-foreground",
            onClick: () => onSetPointFill(seriesIndex, idx, null),
            children: "\xD7"
          }
        ),
        showExplosion && /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 100,
            disabled: !canEdit,
            title: t("pptx.chart.pointExplosion"),
            className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
            value: point2?.explosion ?? "",
            placeholder: "0",
            onChange: (e) => {
              const raw = e.target.value;
              if (raw === "") {
                onSetPointExplosion(seriesIndex, idx, null);
                return;
              }
              const num = Number.parseInt(raw, 10);
              if (Number.isFinite(num)) {
                onSetPointExplosion(seriesIndex, idx, num);
              }
            }
          }
        )
      ] }, `${cat}-${idx}`);
    }) })
  ] });
}
function ChartDisplayOptions({
  style,
  canEdit,
  onUpdateStyle,
  hasGridlines,
  onToggleGridlines
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.display") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: style?.hasTitle ?? false,
            onChange: (e) => onUpdateStyle({ hasTitle: e.target.checked }),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.showTitle") })
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: style?.hasLegend ?? false,
            onChange: (e) => onUpdateStyle({ hasLegend: e.target.checked }),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.showLegend") })
      ] }),
      style?.hasLegend && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px] ml-4", children: [
        /* @__PURE__ */ jsx("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.legendPosition") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.chart.legendPosition"),
            disabled: !canEdit,
            className: INPUT2,
            value: style.legendPosition ?? "b",
            onChange: (e) => onUpdateStyle({
              legendPosition: e.target.value
            }),
            children: LEGEND_POSITION_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: hasGridlines,
            onChange: (e) => onToggleGridlines(e.target.checked),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.showGridlines") })
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: style?.hasDataLabels ?? false,
            onChange: (e) => onUpdateStyle({
              hasDataLabels: e.target.checked
            }),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t("pptx.chart.showDataLabels") })
      ] })
    ] })
  ] });
}
function ChartErrorBarOptions({
  chartType,
  series,
  canEdit,
  onSetErrorBars
}) {
  const { t } = useTranslation();
  if (!ERROR_BAR_SUPPORTED_TYPES.has(chartType) || series.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.errorBars") }),
    /* @__PURE__ */ jsx("div", { className: "space-y-2", children: series.map((s, i) => {
      const bars = s.errBars?.[0];
      const valType = bars?.valType ?? "";
      const showValue = ERROR_BAR_VALUE_TYPES.has(valType);
      return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
          /* @__PURE__ */ jsx(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: valType,
              onChange: (e) => {
                const value = e.target.value;
                if (!value) {
                  onSetErrorBars(i, null);
                  return;
                }
                onSetErrorBars(i, {
                  direction: bars?.direction ?? "y",
                  barType: bars?.barType ?? "both",
                  valType: value,
                  val: bars?.val
                });
              },
              children: ERROR_BAR_VALTYPE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        bars && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 ml-2", children: [
          /* @__PURE__ */ jsx(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: bars.barType,
              onChange: (e) => onSetErrorBars(i, {
                ...bars,
                barType: e.target.value
              }),
              children: ERROR_BAR_TYPE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          ),
          showValue && /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              disabled: !canEdit,
              className: INPUT2,
              value: bars.val ?? "",
              placeholder: t("pptx.chart.errorBarAmount"),
              onChange: (e) => {
                const raw = e.target.value;
                const num = raw === "" ? void 0 : Number.parseFloat(raw);
                if (raw !== "" && !Number.isFinite(num)) {
                  return;
                }
                onSetErrorBars(i, { ...bars, val: num });
              }
            }
          )
        ] })
      ] }, `${s.name}-${i}`);
    }) })
  ] });
}
function ChartMarkerOptions({
  chartType,
  series,
  canEdit,
  onSetMarker
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  if (!MARKER_SUPPORTED_TYPES.has(chartType) || series.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.markers") }),
    /* @__PURE__ */ jsx("div", { className: "space-y-2", children: series.map((s, i) => {
      const marker = s.marker;
      return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
          /* @__PURE__ */ jsx(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: marker?.symbol ?? "",
              onChange: (e) => {
                const value = e.target.value;
                if (value === "") {
                  onSetMarker(i, null);
                  return;
                }
                onSetMarker(i, { symbol: value });
              },
              children: MARKER_SYMBOL_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        marker && marker.symbol !== "none" && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 ml-2", children: [
          /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 text-[11px]", children: [
            /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.markerSize") }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "number",
                min: 2,
                max: 72,
                disabled: !canEdit,
                className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
                value: marker.size ?? "",
                placeholder: t("pptx.chart.auto"),
                onChange: (e) => {
                  const raw = e.target.value;
                  const num = Number.parseInt(raw, 10);
                  onSetMarker(i, { size: Number.isFinite(num) ? num : void 0 });
                }
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 text-[11px]", children: [
            /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.markerFill") }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "color",
                disabled: !canEdit,
                className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
                value: marker.spPr?.fillColor ?? "#4472c4",
                onChange: (e) => {
                  onSetMarker(i, { fillColor: e.target.value });
                  pushColor(e.target.value);
                }
              }
            )
          ] })
        ] })
      ] }, `${s.name}-${i}`);
    }) })
  ] });
}
function DebouncedColorInput({
  value,
  disabled,
  className,
  ariaLabel,
  onCommit
}) {
  const [local, setLocal] = useState(value);
  const commitRef = useRef(onCommit);
  commitRef.current = onCommit;
  const { pushColor } = useRecentColors();
  useEffect(() => {
    setLocal(value);
  }, [value]);
  const handleChange = useCallback(
    (e) => {
      const next = e.target.value;
      setLocal(next);
      commitRef.current(next);
      pushColor(next);
    },
    [pushColor]
  );
  return /* @__PURE__ */ jsx(
    "input",
    {
      type: "color",
      "aria-label": ariaLabel ?? "Color",
      disabled,
      value: local,
      className,
      onChange: handleChange
    }
  );
}
var DEFAULT_SWATCH = "#4472c4";
function ChartSeriesColorOptions({
  chartData,
  canEdit,
  onSetColor,
  onToggleSecondaryAxis
}) {
  const { t } = useTranslation();
  const series = chartData.series;
  if (series.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.seriesColors") }),
    /* @__PURE__ */ jsx("div", { className: "space-y-1", children: series.map((s, i) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 text-muted-foreground shrink-0", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: isSeriesUsingSecondaryAxis(chartData, i),
            onChange: (e) => onToggleSecondaryAxis(i, e.target.checked),
            className: "accent-primary"
          }
        ),
        t("pptx.chart.secondaryAxis")
      ] }),
      /* @__PURE__ */ jsx(
        DebouncedColorInput,
        {
          value: s.color ?? DEFAULT_SWATCH,
          disabled: !canEdit,
          ariaLabel: t("pptx.chart.seriesColor", { name: s.name }),
          className: "h-6 w-8 cursor-pointer rounded border border-border bg-muted p-0",
          onCommit: (hex) => onSetColor(i, hex)
        }
      ),
      canEdit && s.color && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "text-muted-foreground hover:text-red-400 shrink-0",
          title: t("pptx.chart.clearSeriesColor"),
          onClick: () => onSetColor(i, null),
          children: /* @__PURE__ */ jsx(LuX, { className: "w-3 h-3" })
        }
      )
    ] }, `${s.name}-${i}`)) })
  ] });
}
function ChartSubtypeOptions({
  chartData,
  canEdit,
  onUpdateChartData
}) {
  const { t } = useTranslation();
  if (chartData.chartType !== "bar3D" && chartData.chartType !== "radar" && chartData.chartType !== "surface") {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.display") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      chartData.chartType === "bar3D" && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.bar3DShapeLabel") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "data-testid": "pptx-chart-bar3d-shape",
            "aria-label": t("pptx.chart.bar3DShapeLabel"),
            disabled: !canEdit,
            className: INPUT2,
            value: chartData.barShape ?? "box",
            onChange: (e) => onUpdateChartData(bar3DShapePatch(chartData, e.target.value)),
            children: BAR3D_SHAPE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] }),
      chartData.chartType === "radar" && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.radarStyleLabel") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "data-testid": "pptx-chart-radar-style",
            "aria-label": t("pptx.chart.radarStyleLabel"),
            disabled: !canEdit,
            className: INPUT2,
            value: chartData.radarStyle ?? "standard",
            onChange: (e) => onUpdateChartData(
              radarStylePatch(
                chartData,
                e.target.value
              )
            ),
            children: RADAR_STYLE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] }),
      chartData.chartType === "surface" && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.surfaceWireframeLabel") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "data-testid": "pptx-chart-surface-wireframe",
            "aria-label": t("pptx.chart.surfaceWireframeLabel"),
            disabled: !canEdit,
            className: INPUT2,
            value: chartData.wireframe ? "true" : "false",
            onChange: (e) => onUpdateChartData(surfaceWireframePatch(chartData, e.target.value === "true")),
            children: SURFACE_WIREFRAME_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] })
    ] })
  ] });
}
function ChartTrendlineOptions({
  chartType,
  series,
  canEdit,
  onSetTrendline
}) {
  const { t } = useTranslation();
  if (!TRENDLINE_SUPPORTED_TYPES.has(chartType) || series.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.trendlines") }),
    /* @__PURE__ */ jsx("div", { className: "space-y-2", children: series.map((s, i) => {
      const trendline = s.trendlines?.[0];
      return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
          /* @__PURE__ */ jsx(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: trendline?.trendlineType ?? "",
              onChange: (e) => {
                const value = e.target.value;
                if (!value) {
                  onSetTrendline(i, null);
                  return;
                }
                onSetTrendline(i, {
                  ...trendline,
                  trendlineType: value
                });
              },
              children: TRENDLINE_TYPE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        trendline && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 ml-2", children: [
          /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 text-[11px] cursor-pointer", children: [
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: trendline.displayEq ?? false,
                onChange: (e) => onSetTrendline(i, { ...trendline, displayEq: e.target.checked }),
                className: "accent-primary"
              }
            ),
            /* @__PURE__ */ jsx("span", { children: t("pptx.chart.trendlineEquation") })
          ] }),
          /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 text-[11px] cursor-pointer", children: [
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: trendline.displayRSq ?? false,
                onChange: (e) => onSetTrendline(i, { ...trendline, displayRSq: e.target.checked }),
                className: "accent-primary"
              }
            ),
            /* @__PURE__ */ jsx("span", { children: t("pptx.chart.trendlineRSquared") })
          ] })
        ] })
      ] }, `${s.name}-${i}`);
    }) })
  ] });
}
function ChartTypeSelector({
  title,
  chartType,
  chartData,
  grouping,
  seriesCount,
  categoryCount,
  canEdit,
  onUpdateChartData,
  onTitleChange
}) {
  const { t } = useTranslation();
  const supportsGrouping = GROUPING_SUPPORTED_TYPES.has(chartType);
  const displayedType = resolveDisplayedChartType(chartData);
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.heading") }),
    /* @__PURE__ */ jsxs("div", { className: "text-[11px] text-muted-foreground mb-1", children: [
      seriesCount,
      " ",
      t("pptx.chart.series"),
      " \xB7 ",
      categoryCount,
      " ",
      t("pptx.chart.categories")
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "w-10 text-muted-foreground shrink-0", children: t("pptx.chart.title") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          disabled: !canEdit,
          className: INPUT2,
          value: title ?? "",
          onChange: (e) => onTitleChange(e.target.value)
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "w-10 text-muted-foreground shrink-0", children: t("pptx.chart.type") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.chart.type"),
          disabled: !canEdit,
          className: INPUT2,
          value: displayedType,
          onChange: (e) => onUpdateChartData({
            chartType: e.target.value
          }),
          children: CHART_TYPE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
        }
      )
    ] }),
    supportsGrouping && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "w-10 text-muted-foreground shrink-0", children: t("pptx.chart.grouping") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.chart.grouping"),
          disabled: !canEdit,
          className: INPUT2,
          value: grouping ?? "clustered",
          onChange: (e) => onUpdateChartData({
            grouping: e.target.value
          }),
          children: GROUPING_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
        }
      )
    ] })
  ] });
}
function NumberField({ value, canEdit, fraction, nonNegative, onChange }) {
  return /* @__PURE__ */ jsx(
    "input",
    {
      type: "number",
      step: fraction ? "0.01" : void 0,
      min: fraction || nonNegative ? 0 : void 0,
      max: fraction ? 1 : void 0,
      disabled: !canEdit,
      className: INPUT2,
      value,
      onChange: (e) => onChange(Number(e.target.value))
    }
  );
}
function RotationField({
  row,
  canEdit,
  onRotationPatch
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeRotation") }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "number",
        step: "1",
        "aria-label": t("pptx.chart.userShapeRotation"),
        disabled: !canEdit,
        className: INPUT2,
        value: row.rotation ?? 0,
        onChange: (e) => onRotationPatch(row.path, Number(e.target.value) || void 0)
      }
    )
  ] });
}
function FlipFields({
  row,
  canEdit,
  onFlipPatch
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          "aria-label": t("pptx.arrange.flipHorizontally"),
          disabled: !canEdit,
          className: "accent-primary",
          checked: row.flipH ?? false,
          onChange: (e) => onFlipPatch(row.path, { flipH: e.target.checked })
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.arrange.flipHorizontally") })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          "aria-label": t("pptx.arrange.flipVertically"),
          disabled: !canEdit,
          className: "accent-primary",
          checked: row.flipV ?? false,
          onChange: (e) => onFlipPatch(row.path, { flipV: e.target.checked })
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.arrange.flipVertically") })
    ] })
  ] });
}
function ChartUserShapePositionFields({
  row,
  userShapes,
  canEdit,
  onPatch,
  onBoxPatch,
  onRotationPatch,
  onFlipPatch
}) {
  const { t } = useTranslation();
  const update = (patch) => onPatch(row.path, patch);
  if (row.depth === 0) {
    const from = row.from;
    return /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-center gap-1 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeFrom") }),
      /* @__PURE__ */ jsx(
        NumberField,
        {
          fraction: true,
          canEdit,
          value: from.x,
          onChange: (x) => update({ from: { ...from, x } })
        }
      ),
      /* @__PURE__ */ jsx(
        NumberField,
        {
          fraction: true,
          canEdit,
          value: from.y,
          onChange: (y) => update({ from: { ...from, y } })
        }
      ),
      row.anchor === "rel" && row.to ? /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeTo") }),
        /* @__PURE__ */ jsx(
          NumberField,
          {
            fraction: true,
            canEdit,
            value: row.to.x,
            onChange: (x) => update({ to: { ...row.to, x } })
          }
        ),
        /* @__PURE__ */ jsx(
          NumberField,
          {
            fraction: true,
            canEdit,
            value: row.to.y,
            onChange: (y) => update({ to: { ...row.to, y } })
          }
        )
      ] }) : null,
      row.anchor === "abs" && row.ext ? /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeSize") }),
        /* @__PURE__ */ jsx(
          NumberField,
          {
            nonNegative: true,
            canEdit,
            value: row.ext.cx,
            onChange: (cx) => update({ ext: { ...row.ext, cx } })
          }
        ),
        /* @__PURE__ */ jsx(
          NumberField,
          {
            nonNegative: true,
            canEdit,
            value: row.ext.cy,
            onChange: (cy) => update({ ext: { ...row.ext, cy } })
          }
        )
      ] }) : null,
      /* @__PURE__ */ jsx(RotationField, { row, canEdit, onRotationPatch }),
      /* @__PURE__ */ jsx(FlipFields, { row, canEdit, onFlipPatch })
    ] });
  }
  const box = getChartUserShapeRowChartBox(userShapes, row.path);
  if (!box) {
    return null;
  }
  const patchBox = (next) => onBoxPatch(row.path, { from: box.from, to: box.to, ...next });
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-center gap-1 text-[11px]", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeFrom") }),
    /* @__PURE__ */ jsx(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.from.x,
        onChange: (x) => patchBox({ from: { ...box.from, x } })
      }
    ),
    /* @__PURE__ */ jsx(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.from.y,
        onChange: (y) => patchBox({ from: { ...box.from, y } })
      }
    ),
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeTo") }),
    /* @__PURE__ */ jsx(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.to.x,
        onChange: (x) => patchBox({ to: { ...box.to, x } })
      }
    ),
    /* @__PURE__ */ jsx(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.to.y,
        onChange: (y) => patchBox({ to: { ...box.to, y } })
      }
    ),
    /* @__PURE__ */ jsx(RotationField, { row, canEdit, onRotationPatch }),
    /* @__PURE__ */ jsx(FlipFields, { row, canEdit, onFlipPatch })
  ] });
}
var PATH_KEY = (path) => path.join(",");
function ChartUserShapeOptions({
  chartData,
  canEdit,
  onUpdateChartData
}) {
  const { t } = useTranslation();
  const shapes = chartData.userShapes;
  const rows = listChartUserShapeRows(shapes);
  const kindLabel = (kind) => {
    const key = `pptx.chart.userShapeKind${kind.charAt(0).toUpperCase()}${kind.slice(1)}`;
    return t(key);
  };
  const update = (path, patch) => onUpdateChartData({ userShapes: withChartUserShapeRowUpdated(shapes, path, patch) });
  const updateBox = (path, box) => onUpdateChartData({ userShapes: withChartUserShapeRowChartBoxUpdated(shapes, path, box) });
  const updateRotation = (path, rotation) => onUpdateChartData({ userShapes: withChartUserShapeRowRotationUpdated(shapes, path, rotation) });
  const updateFlip = (path, flip) => onUpdateChartData({ userShapes: withChartUserShapeRowFlipUpdated(shapes, path, flip) });
  const addIntoGroup = (path) => {
    const transform = getChartUserShapeGroupTransform(shapes, path);
    if (!transform) {
      return;
    }
    onUpdateChartData({
      userShapes: withChartUserShapeGroupChildAdded(
        shapes,
        path,
        createDefaultChartUserShapeGroupChild(transform)
      )
    });
  };
  return /* @__PURE__ */ jsxs("div", { className: CARD2, children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("div", { className: HEADING2, children: t("pptx.chart.userShapes") }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: BTN2,
          onClick: () => onUpdateChartData({
            userShapes: withChartUserShapeAdded(shapes, createDefaultChartUserShape())
          }),
          children: t("pptx.chart.userShapeAddTextBox")
        }
      )
    ] }),
    rows.length === 0 ? /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: t("pptx.chart.userShapesEmpty") }) : /* @__PURE__ */ jsx("div", { className: "space-y-2", children: rows.map((row) => /* @__PURE__ */ jsxs(
      "div",
      {
        "data-chart-user-shape-path": PATH_KEY(row.path),
        style: { marginLeft: row.depth * 12 },
        className: "space-y-1 rounded border border-border p-1.5",
        children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
            /* @__PURE__ */ jsxs("span", { className: "flex-1 truncate", children: [
              kindLabel(row.kind),
              row.text ? ` - ${row.text}` : ""
            ] }),
            row.isGroup ? /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: BTN2,
                onClick: () => addIntoGroup(row.path),
                children: t("pptx.chart.userShapeAddIntoGroup")
              }
            ) : null,
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                "aria-label": t("pptx.chart.userShapeDelete"),
                className: BTN2,
                onClick: () => onUpdateChartData({
                  userShapes: withChartUserShapeRowRemoved(shapes, row.path)
                }),
                children: "\u2715"
              }
            )
          ] }),
          row.editableVisuals ? /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 text-[11px]", children: [
            /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeText") }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                "aria-label": t("pptx.chart.userShapeText"),
                disabled: !canEdit,
                className: INPUT2,
                value: row.text ?? "",
                onChange: (e) => onUpdateChartData({
                  userShapes: withChartUserShapeRowTextUpdated(
                    shapes,
                    row.path,
                    e.target.value
                  )
                })
              }
            )
          ] }) : null,
          row.editableVisuals ? /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 text-[11px]", children: [
            /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeFill") }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "color",
                  "aria-label": t("pptx.chart.userShapeFill"),
                  disabled: !canEdit,
                  value: row.fill ?? "#ffffff",
                  onChange: (e) => update(row.path, { fill: e.target.value })
                }
              )
            ] }),
            /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeStroke") }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "color",
                  "aria-label": t("pptx.chart.userShapeStroke"),
                  disabled: !canEdit,
                  value: row.stroke ?? "#000000",
                  onChange: (e) => update(row.path, { stroke: e.target.value })
                }
              )
            ] })
          ] }) : null,
          row.editableAltText ? /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 text-[11px]", children: [
            /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeAltText") }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                "aria-label": t("pptx.chart.userShapeAltText"),
                disabled: !canEdit,
                className: INPUT2,
                value: row.altText ?? "",
                onChange: (e) => update(row.path, { altText: e.target.value })
              }
            )
          ] }) : null,
          /* @__PURE__ */ jsx(
            ChartUserShapePositionFields,
            {
              row,
              userShapes: shapes,
              canEdit,
              onPatch: update,
              onBoxPatch: updateBox,
              onRotationPatch: updateRotation,
              onFlipPatch: updateFlip
            }
          )
        ]
      },
      PATH_KEY(row.path)
    )) })
  ] });
}
function ChartDataPanel({ selectedElement, canEdit, onUpdateElement }) {
  const chartData = selectedElement.chartData;
  const followDataPoint = useViewerOptionsContext().advanced.chartPropertiesFollowDataPoint;
  const { selection: partSelection } = useChartPartSelection();
  const canvasPart = partSelection?.elementId === selectedElement.id ? partSelection.part : null;
  const title = chartData?.title;
  const chartType = chartData?.chartType;
  const categories = chartData?.categories;
  const series = chartData?.series;
  const style = chartData?.style;
  const grouping = chartData?.grouping;
  const replaceChartData = useCallback(
    (newData) => {
      onUpdateElement({
        chartData: newData
      });
    },
    [onUpdateElement]
  );
  const updateChartData = useCallback(
    (patch) => {
      if (!chartData) {
        return;
      }
      replaceChartData(patchChartData(chartData, patch));
    },
    [chartData, replaceChartData]
  );
  const updateTitle = useCallback(
    (text) => {
      if (!chartData) {
        return;
      }
      updateChartData(collapseChartTitleRunsForEdit(chartData, text));
    },
    [chartData, updateChartData]
  );
  const updateStyle = useCallback(
    (patch) => {
      if (!chartData) {
        return;
      }
      onUpdateElement({
        chartData: {
          ...chartData,
          style: { ...style, ...patch }
        }
      });
    },
    [chartData, style, onUpdateElement]
  );
  const hasGridlines = chartData ? chartGridlinesState(chartData) : false;
  const toggleGridlines = useCallback(
    (show) => {
      if (!chartData) {
        return;
      }
      updateChartData(chartGridlinesPatch(chartData, show));
    },
    [chartData, updateChartData]
  );
  const toggleSecondaryAxis = useCallback(
    (seriesIndex, useSecondary) => {
      if (!chartData) {
        return;
      }
      updateChartData(seriesSecondaryAxisPatch(chartData, seriesIndex, useSecondary));
    },
    [chartData, updateChartData]
  );
  const updateAxis = useCallback(
    (axisType, patch) => {
      if (!chartData) {
        return;
      }
      const axes = chartData.axes ? [...chartData.axes] : [];
      const index = axes.findIndex((a) => a.axisType === axisType);
      if (index === -1) {
        axes.push({ axisType, ...patch });
      } else {
        axes[index] = { ...axes[index], ...patch };
      }
      updateChartData({ axes });
    },
    [chartData, updateChartData]
  );
  const updateSeries = useCallback(
    (index, patch) => {
      if (!series) {
        return;
      }
      const updated = series.map((s, i) => i === index ? { ...s, ...patch } : s);
      updateChartData({ series: updated });
    },
    [series, updateChartData]
  );
  const setSeriesTrendline = useCallback(
    (index, trendline) => {
      if (!series) {
        return;
      }
      const updated = series.map(
        (s, i) => i === index ? { ...s, trendlines: trendline ? [trendline] : [] } : s
      );
      updateChartData({ series: updated });
    },
    [series, updateChartData]
  );
  const setSeriesErrorBars = useCallback(
    (index, errBars) => {
      if (!series) {
        return;
      }
      const updated = series.map(
        (s, i) => i === index ? { ...s, errBars: errBars ? [errBars] : [] } : s
      );
      updateChartData({ series: updated });
    },
    [series, updateChartData]
  );
  const setSeriesColor = useCallback(
    (index, color) => {
      updateSeries(index, { color: color ?? void 0 });
    },
    [updateSeries]
  );
  const updateCategoryLabel = useCallback(
    (catIndex, value) => {
      const next = chartData && setChartCategoryLabel(chartData, catIndex, value);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, replaceChartData]
  );
  const updateValue = useCallback(
    (seriesIndex, catIndex, raw) => {
      const next = chartData && setChartCellValue(chartData, seriesIndex, catIndex, raw);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, replaceChartData]
  );
  const addCategory = useCallback(() => {
    if (chartData) {
      replaceChartData(addChartCategory(chartData));
    }
  }, [chartData, replaceChartData]);
  const removeCategory = useCallback(
    (catIndex) => {
      const next = chartData && removeChartCategory(chartData, catIndex, followDataPoint);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, followDataPoint, replaceChartData]
  );
  const addSeries = useCallback(() => {
    if (chartData) {
      replaceChartData(addChartSeries(chartData));
    }
  }, [chartData, replaceChartData]);
  const removeSeries = useCallback(
    (seriesIndex) => {
      const next = chartData && removeChartSeries(chartData, seriesIndex);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, replaceChartData]
  );
  const applyChartOp = useCallback(
    (mutate) => {
      if (!chartData) {
        return;
      }
      const clone = {
        ...selectedElement,
        chartData: structuredClone(chartData)
      };
      mutate(clone);
      replaceChartData(clone.chartData);
    },
    [chartData, selectedElement, replaceChartData]
  );
  const setAxisLogScale = useCallback(
    (axisType, opts) => applyChartOp((el) => setChartAxisLogScale(el, axisType, opts)),
    [applyChartOp]
  );
  const setAxisTitleStyle = useCallback(
    (axisType, edit) => applyChartOp((el) => setChartAxisTitleStyle(el, axisType, edit)),
    [applyChartOp]
  );
  const setGridlineStyle = useCallback(
    (axisType, which, edit) => applyChartOp((el) => setChartAxisGridlineStyle(el, axisType, which, edit)),
    [applyChartOp]
  );
  const setSeriesMarker = useCallback(
    (index, marker) => applyChartOp((el) => setChartSeriesMarker(el, index, marker)),
    [applyChartOp]
  );
  const setSeriesType = useCallback(
    (index, seriesType) => applyChartOp((el) => setChartSeriesChartType(el, index, seriesType)),
    [applyChartOp]
  );
  const setPointFill = useCallback(
    (seriesIndex, pointIndex, color) => applyChartOp((el) => setChartDataPointFill(el, seriesIndex, pointIndex, color)),
    [applyChartOp]
  );
  const setPointExplosion = useCallback(
    (seriesIndex, pointIndex, explosion) => applyChartOp((el) => setChartDataPointExplosion(el, seriesIndex, pointIndex, explosion)),
    [applyChartOp]
  );
  const setPointMarker = useCallback(
    (seriesIndex, pointIndex, marker) => applyChartOp((el) => setChartDataPointMarker(el, seriesIndex, pointIndex, marker)),
    [applyChartOp]
  );
  const setPointLabel = useCallback(
    (seriesIndex, pointIndex, text) => applyChartOp(
      (el) => setChartDataPointLabel(el, seriesIndex, pointIndex, text !== null ? { text } : null)
    ),
    [applyChartOp]
  );
  if (!chartData || !categories || !series) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      ChartTypeSelector,
      {
        title,
        chartType,
        chartData,
        grouping,
        seriesCount: series.length,
        categoryCount: categories.length,
        canEdit,
        onUpdateChartData: updateChartData,
        onTitleChange: updateTitle
      }
    ),
    /* @__PURE__ */ jsx(
      ChartDisplayOptions,
      {
        style,
        canEdit,
        onUpdateStyle: updateStyle,
        hasGridlines,
        onToggleGridlines: toggleGridlines
      }
    ),
    /* @__PURE__ */ jsx(
      ChartSubtypeOptions,
      {
        chartData,
        canEdit,
        onUpdateChartData: updateChartData
      }
    ),
    /* @__PURE__ */ jsx(ChartDataLabelOptions, { style, canEdit, onUpdateStyle: updateStyle }),
    /* @__PURE__ */ jsx(ChartAxisOptions, { axes: chartData.axes, canEdit, onUpdateAxis: updateAxis }),
    /* @__PURE__ */ jsx(
      ChartAxisStyleOptions,
      {
        axes: chartData.axes,
        canEdit,
        onSetLogScale: setAxisLogScale,
        onSetTitleStyle: setAxisTitleStyle,
        onSetGridlineStyle: setGridlineStyle
      }
    ),
    /* @__PURE__ */ jsx(
      ChartMarkerOptions,
      {
        chartType,
        series,
        canEdit,
        onSetMarker: setSeriesMarker
      }
    ),
    /* @__PURE__ */ jsx(
      ChartComboTypeOptions,
      {
        chartType,
        series,
        canEdit,
        onSetSeriesType: setSeriesType
      }
    ),
    /* @__PURE__ */ jsx(
      ChartDataPointOptions,
      {
        chartType,
        categories,
        series,
        canEdit,
        onSetPointFill: setPointFill,
        onSetPointExplosion: setPointExplosion,
        onSetPointLabel: setPointLabel
      }
    ),
    /* @__PURE__ */ jsx(
      ChartDataPointMarkerOptions,
      {
        chartType,
        categories,
        series,
        canEdit,
        onSetPointMarker: setPointMarker
      }
    ),
    /* @__PURE__ */ jsx(
      ChartTrendlineOptions,
      {
        chartType,
        series,
        canEdit,
        onSetTrendline: setSeriesTrendline
      }
    ),
    /* @__PURE__ */ jsx(
      ChartErrorBarOptions,
      {
        chartType,
        series,
        canEdit,
        onSetErrorBars: setSeriesErrorBars
      }
    ),
    /* @__PURE__ */ jsx(
      ChartSeriesColorOptions,
      {
        chartData,
        canEdit,
        onSetColor: setSeriesColor,
        onToggleSecondaryAxis: toggleSecondaryAxis
      }
    ),
    /* @__PURE__ */ jsx(
      ChartUserShapeOptions,
      {
        chartData,
        canEdit,
        onUpdateChartData: updateChartData
      }
    ),
    /* @__PURE__ */ jsx(
      ChartDataGrid,
      {
        categories,
        series,
        canEdit,
        highlightCell: canvasPart ? { seriesIndex: canvasPart.seriesIndex, pointIndex: canvasPart.pointIndex } : null,
        onUpdateSeries: updateSeries,
        onUpdateCategoryLabel: updateCategoryLabel,
        onUpdateValue: updateValue,
        onAddCategory: addCategory,
        onRemoveCategory: removeCategory,
        onAddSeries: addSeries,
        onRemoveSeries: removeSeries
      }
    )
  ] });
}

// src/viewer/components/inspector/element-properties-constants.ts
var SELECT_CLS2 = "bg-muted border border-border rounded px-2 py-1";
var BTN_CLS = "inline-flex items-center justify-center gap-1 rounded bg-muted hover:bg-accent px-2 py-1";
var END_LABEL_KEYS = {
  Start: {
    arrow: "pptx.connectorArrows.startArrow",
    width: "pptx.connectorArrows.startWidth",
    length: "pptx.connectorArrows.startLength"
  },
  End: {
    arrow: "pptx.connectorArrows.endArrow",
    width: "pptx.connectorArrows.endWidth",
    length: "pptx.connectorArrows.endLength"
  }
};
function ConnectorArrowsSection({
  selectedShapeStyle,
  canEdit = true,
  onUpdateShapeStyle,
  markDirty
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
    ["Start", "End"].map((end) => {
      const key = end === "Start" ? "connectorStartArrow" : "connectorEndArrow";
      return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t(END_LABEL_KEYS[end].arrow) }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t(END_LABEL_KEYS[end].arrow),
            value: selectedShapeStyle?.[key] || "none",
            onChange: (e) => {
              onUpdateShapeStyle({
                [key]: e.target.value
              });
              markDirty?.();
            },
            disabled: !canEdit,
            className: SELECT_CLS2,
            children: CONNECTOR_ARROW_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.i18nKey) }, o.value))
          }
        )
      ] }, end);
    }),
    ["Start", "End"].map((end) => {
      const widthKey = end === "Start" ? "connectorStartArrowWidth" : "connectorEndArrowWidth";
      const lengthKey = end === "Start" ? "connectorStartArrowLength" : "connectorEndArrowLength";
      return /* @__PURE__ */ jsxs(React20.Fragment, { children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t(END_LABEL_KEYS[end].width) }),
          /* @__PURE__ */ jsx(
            "select",
            {
              "aria-label": t(END_LABEL_KEYS[end].width),
              value: selectedShapeStyle?.[widthKey] || "med",
              onChange: (e) => {
                onUpdateShapeStyle({
                  [widthKey]: e.target.value
                });
                markDirty?.();
              },
              disabled: !canEdit,
              className: SELECT_CLS2,
              children: ARROW_SIZE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.i18nKey) }, o.value))
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t(END_LABEL_KEYS[end].length) }),
          /* @__PURE__ */ jsx(
            "select",
            {
              "aria-label": t(END_LABEL_KEYS[end].length),
              value: selectedShapeStyle?.[lengthKey] || "med",
              onChange: (e) => {
                onUpdateShapeStyle({
                  [lengthKey]: e.target.value
                });
                markDirty?.();
              },
              disabled: !canEdit,
              className: SELECT_CLS2,
              children: ARROW_SIZE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.i18nKey) }, o.value))
            }
          )
        ] })
      ] }, `${end}-size`);
    })
  ] });
}
function ConnectorPanel({
  selectedElement,
  canEdit,
  onUpdateElementStyle
}) {
  if (selectedElement.type !== "connector") {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: "Connector" }),
    /* @__PURE__ */ jsx(
      ConnectorArrowsSection,
      {
        selectedShapeStyle: selectedElement.shapeStyle,
        canEdit: canEdit && canInteractWithElement(selectedElement, "changeArrowheads"),
        onUpdateShapeStyle: onUpdateElementStyle
      }
    )
  ] });
}
function GroupInfoPanel({
  selectedElement
}) {
  const { t } = useTranslation();
  if (selectedElement.type !== "group") {
    return null;
  }
  const group = selectedElement;
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.elementType.group") }),
    /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: Array.isArray(group.children) ? t("pptx.group.childCount", { count: group.children.length }) : t("pptx.group.groupedElement") })
  ] });
}
function OlePropertiesPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  if (selectedElement.type !== "ole") {
    return null;
  }
  const ole = selectedElement;
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.ole.title") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 text-[11px]", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.ole.objectName") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            disabled: !canEdit,
            className: INPUT,
            value: ole.oleName ?? "",
            placeholder: t("pptx.ole.objectNamePlaceholder"),
            onChange: (e) => onUpdateElement(buildOleObjectNamePatch(e.target.value))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.ole.type") }),
        /* @__PURE__ */ jsx("span", { className: "text-foreground truncate", children: getOleObjectTypeLabel(ole.oleObjectType) })
      ] }),
      ole.fileName && /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.ole.fileName") }),
        /* @__PURE__ */ jsx("span", { className: "text-foreground truncate", title: ole.fileName, children: ole.fileName })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.ole.linkStatus") }),
        /* @__PURE__ */ jsx(
          "span",
          {
            className: cn(
              "inline-flex items-center rounded-full px-2 py-0.5 text-[10px] font-medium",
              ole.isLinked ? "bg-blue-500/20 text-blue-400" : "bg-green-500/20 text-green-400"
            ),
            children: ole.isLinked ? t("pptx.ole.linked") : t("pptx.ole.embedded")
          }
        )
      ] })
    ] })
  ] });
}
function TransformPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: "Transform" }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Rotation (\xB0)" }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: INPUT,
            value: Math.round(selectedElement.rotation || 0),
            onChange: (e) => onUpdateElement({ rotation: Number(e.target.value) })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Opacity" }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            disabled: !canEdit,
            min: 0,
            max: 100,
            value: Math.round((selectedElement.opacity ?? 1) * 100),
            className: "accent-primary",
            onChange: (e) => onUpdateElement({ opacity: Number(e.target.value) / 100 })
          }
        )
      ] })
    ] })
  ] });
}
function LayerOrderButtons({
  canEdit,
  onMoveLayer
}) {
  return /* @__PURE__ */ jsxs("div", { className: "flex gap-1", children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: cn("flex-1", BTN),
        disabled: !canEdit,
        onClick: () => onMoveLayer("forward"),
        children: "\u2191 Forward"
      }
    ),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: cn("flex-1", BTN),
        disabled: !canEdit,
        onClick: () => onMoveLayer("backward"),
        children: "\u2193 Backward"
      }
    )
  ] });
}
function ArtisticEffectsGallery({
  imgSrc: imgSrc2,
  fx,
  canEdit,
  updateEffects
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1 text-[11px]", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.artisticEffects") }),
    /* @__PURE__ */ jsx("div", { className: "grid grid-cols-4 gap-1", children: ARTISTIC_EFFECTS.map(([effectName, tKey, cssFilter]) => {
      const isActive = effectName === "none" ? !fx?.artisticEffect : fx?.artisticEffect === effectName;
      return /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "flex flex-col items-center gap-0.5 rounded border p-0.5 hover:bg-accent/50",
            isActive ? "border-primary bg-primary/10" : "border-border"
          ),
          title: t(tKey),
          onClick: () => updateEffects({
            artisticEffect: effectName === "none" ? void 0 : effectName,
            ...effectName === "grayscale" ? { grayscale: void 0 } : {}
          }),
          children: [
            /* @__PURE__ */ jsx(
              "div",
              {
                className: "w-10 h-7 rounded overflow-hidden bg-muted",
                style: {
                  backgroundImage: imgSrc2 ? `url(${imgSrc2})` : void 0,
                  backgroundSize: "cover",
                  backgroundPosition: "center",
                  filter: cssFilter || void 0
                }
              }
            ),
            /* @__PURE__ */ jsx("span", { className: "text-[8px] text-muted-foreground truncate w-full text-center", children: t(tKey) })
          ]
        },
        effectName
      );
    }) })
  ] });
}
function ColorChangeSection({
  fx,
  canEdit,
  updateEffects
}) {
  const { t } = useTranslation();
  const cc = fx?.clrChange;
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1 text-[11px]", children: [
    /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.colorChange") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: Boolean(cc),
          onChange: (e) => updateEffects({
            clrChange: e.target.checked ? {
              clrFrom: "#ffffff",
              clrTo: "#000000",
              clrToTransparent: false
            } : void 0
          })
        }
      )
    ] }),
    cc && /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.colorChangeFrom") }),
        /* @__PURE__ */ jsx(
          DebouncedColorInput,
          {
            disabled: !canEdit,
            value: normalizeHexColor(cc.clrFrom, "#ffffff"),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => updateEffects({
              clrChange: {
                ...cc,
                clrFrom: hex,
                clrTo: cc.clrTo ?? "#000000"
              }
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.colorChangeTo") }),
        /* @__PURE__ */ jsx(
          DebouncedColorInput,
          {
            disabled: !canEdit || Boolean(cc.clrToTransparent),
            value: normalizeHexColor(cc.clrTo, "#000000"),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => updateEffects({
              clrChange: {
                ...cc,
                clrFrom: cc.clrFrom ?? "#ffffff",
                clrTo: hex
              }
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2 col-span-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.colorChangeTransparent") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(cc.clrToTransparent),
            onChange: (e) => updateEffects({
              clrChange: {
                clrFrom: cc.clrFrom ?? "#ffffff",
                clrTo: cc.clrTo ?? "#000000",
                clrToTransparent: e.target.checked
              }
            })
          }
        )
      ] })
    ] })
  ] });
}
function ColorWashSection({
  fx,
  canEdit,
  updateEffects
}) {
  const { t } = useTranslation();
  const wash = fx?.colorWash;
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1 text-[11px]", children: [
    /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.colorWash") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: Boolean(wash),
          onChange: (e) => updateEffects({
            colorWash: e.target.checked ? { color: "#0066cc", opacity: 40 } : void 0
          })
        }
      )
    ] }),
    wash && /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.washColor") }),
        /* @__PURE__ */ jsx(
          DebouncedColorInput,
          {
            disabled: !canEdit,
            value: normalizeHexColor(wash.color, "#0066cc"),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => updateEffects({
              colorWash: { color: hex, opacity: wash.opacity ?? 40 }
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.washOpacity") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            min: 0,
            max: 100,
            disabled: !canEdit,
            className: "accent-primary",
            value: wash.opacity,
            onChange: (e) => updateEffects({
              colorWash: {
                color: wash.color ?? "#0066cc",
                opacity: Number(e.target.value)
              }
            })
          }
        )
      ] })
    ] })
  ] });
}
function DebouncedColorInput2({
  value,
  disabled,
  className,
  onCommit
}) {
  const [local, setLocal] = useState(value);
  const commitRef = useRef(onCommit);
  commitRef.current = onCommit;
  const { pushColor } = useRecentColors();
  useEffect(() => {
    setLocal(value);
  }, [value]);
  const handleChange = useCallback((e) => {
    setLocal(e.target.value);
  }, []);
  const handleBlur = useCallback(() => {
    commitRef.current(local);
    pushColor(local);
  }, [local, pushColor]);
  const inputRef = useRef(null);
  useEffect(() => {
    const el = inputRef.current;
    if (!el) {
      return;
    }
    const handler = () => {
      commitRef.current(el.value);
      pushColor(el.value);
    };
    el.addEventListener("change", handler);
    return () => el.removeEventListener("change", handler);
  }, [pushColor]);
  return /* @__PURE__ */ jsx(
    "input",
    {
      ref: inputRef,
      type: "color",
      disabled,
      className,
      value: local,
      onChange: handleChange,
      onBlur: handleBlur
    }
  );
}
function DuotonePanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const effects = "imageEffects" in selectedElement ? selectedElement.imageEffects : void 0;
  const commitDuotone = useCallback(
    (color1, color2) => {
      onUpdateElement({
        imageEffects: {
          ...effects,
          duotone: { color1, color2 }
        }
      });
    },
    [effects, onUpdateElement]
  );
  const clearDuotone = useCallback(() => {
    onUpdateElement({
      imageEffects: {
        ...effects,
        duotone: void 0
      }
    });
  }, [effects, onUpdateElement]);
  const shadowColor = normalizeHexColor(effects?.duotone?.color1, "#000000");
  const highlightColor = normalizeHexColor(effects?.duotone?.color2, "#ffffff");
  return /* @__PURE__ */ jsxs("div", { className: "space-y-2 text-[11px]", children: [
    /* @__PURE__ */ jsx("div", { className: "text-muted-foreground font-medium", children: t("pptx.image.duotone") }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1.5", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.duotoneShadows") }),
        /* @__PURE__ */ jsx(
          DebouncedColorInput2,
          {
            disabled: !canEdit,
            value: shadowColor,
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => commitDuotone(hex, highlightColor)
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1.5", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.duotoneHighlights") }),
        /* @__PURE__ */ jsx(
          DebouncedColorInput2,
          {
            disabled: !canEdit,
            value: highlightColor,
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => commitDuotone(shadowColor, hex)
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
      /* @__PURE__ */ jsx("div", { className: "text-muted-foreground text-[10px]", children: t("pptx.image.duotonePresets") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-4 gap-1", children: DUOTONE_PRESETS.map((preset) => /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: "flex flex-col items-center gap-0.5 rounded border border-border hover:bg-accent p-1 transition-colors",
          title: t(preset.labelKey),
          onClick: () => commitDuotone(preset.shadow, preset.highlight),
          children: [
            /* @__PURE__ */ jsxs("div", { className: "flex h-4 w-full rounded overflow-hidden", children: [
              /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: preset.shadow } }),
              /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: preset.highlight } })
            ] }),
            /* @__PURE__ */ jsx("span", { className: "text-[8px] text-muted-foreground truncate w-full text-center", children: t(preset.labelKey) })
          ]
        },
        preset.labelKey
      )) })
    ] }),
    effects?.duotone && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: "w-full rounded bg-muted hover:bg-accent border border-border px-2 py-1 text-foreground transition-colors",
        onClick: clearDuotone,
        children: t("pptx.image.duotoneClear")
      }
    )
  ] });
}
function ImageAdjustmentsPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const effects = useMemo(
    () => "imageEffects" in selectedElement ? selectedElement.imageEffects : void 0,
    [selectedElement]
  );
  const alphaModFix = effects?.alphaModFix ?? 100;
  const transparency = 100 - alphaModFix;
  const biLevelThreshold = effects?.biLevel ?? 0;
  const hasValues = effects?.alphaModFix !== void 0 || effects?.biLevel !== void 0;
  const commitEffects = useCallback(
    (patch) => {
      onUpdateElement({
        imageEffects: {
          ...effects,
          ...patch
        }
      });
    },
    [effects, onUpdateElement]
  );
  const handleTransparencyChange = useCallback(
    (e) => {
      const val = Number(e.target.value);
      commitEffects({ alphaModFix: 100 - val });
    },
    [commitEffects]
  );
  const handleBiLevelChange = useCallback(
    (e) => {
      commitEffects({ biLevel: Number(e.target.value) });
    },
    [commitEffects]
  );
  const handleReset = useCallback(() => {
    commitEffects({ alphaModFix: void 0, biLevel: void 0 });
  }, [commitEffects]);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-3 text-[11px]", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground font-medium", children: t("pptx.imageAdjustments.title") }),
      hasValues && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: "rounded bg-muted hover:bg-accent border border-border px-1.5 py-0.5 text-[10px] text-foreground disabled:opacity-50",
          onClick: handleReset,
          children: t("pptx.common.reset")
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.imageAdjustments.transparency") }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            min: 0,
            max: 100,
            step: 1,
            disabled: !canEdit,
            className: "accent-primary flex-1",
            value: transparency,
            onChange: handleTransparencyChange
          }
        ),
        /* @__PURE__ */ jsxs("span", { className: "w-8 text-right tabular-nums text-muted-foreground", children: [
          transparency,
          "%"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.imageAdjustments.biLevelThreshold") }),
      /* @__PURE__ */ jsx("p", { className: "text-[10px] text-muted-foreground/70", children: t("pptx.imageAdjustments.biLevelDescription") }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            min: 0,
            max: 100,
            step: 1,
            disabled: !canEdit,
            className: "accent-primary flex-1",
            value: biLevelThreshold,
            onChange: handleBiLevelChange
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "w-8 text-right tabular-nums text-muted-foreground", children: biLevelThreshold })
      ] })
    ] })
  ] });
}
var CROP_SIDES = ["Left", "Top", "Right", "Bottom"];
var MAX_CROP_PERCENT = 80;
function ImageCropSection({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  if (!isImageLikeElement(selectedElement)) {
    return null;
  }
  const croppable = canEdit && canInteractWithElement(selectedElement, "crop");
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", "data-pptx-image-crop": true, children: [
    CROP_SIDES.map((side) => {
      const key = `crop${side}`;
      return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 text-[11px]", children: [
        /* @__PURE__ */ jsxs("span", { className: "text-muted-foreground", children: [
          "Crop ",
          side
        ] }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            min: 0,
            max: MAX_CROP_PERCENT,
            disabled: !croppable,
            className: "accent-primary",
            value: Math.round(clampCropValue(selectedElement[key]) * 100),
            onChange: (e) => onUpdateElement({
              [key]: Number(e.target.value) / 100
            })
          }
        )
      ] }, side);
    }),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !croppable,
        className: `${BTN_CLS} w-full`,
        onClick: () => onUpdateElement({
          cropLeft: 0,
          cropTop: 0,
          cropRight: 0,
          cropBottom: 0
        }),
        children: t("pptx.image.resetCrop")
      }
    )
  ] });
}
function RangeSlider({
  label,
  disabled,
  value,
  onChange
}) {
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: label }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "range",
        min: -100,
        max: 100,
        disabled,
        className: "accent-primary",
        value,
        onChange: (e) => onChange(Number(e.target.value))
      }
    )
  ] });
}
function ImagePropertiesPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const fx = selectedElement.imageEffects;
  const updateEffects = (patch) => {
    onUpdateElement({
      imageEffects: { ...fx, ...patch }
    });
  };
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.image.properties") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.image.altText") }),
        /* @__PURE__ */ jsx(
          "textarea",
          {
            rows: 2,
            disabled: !canEdit,
            className: cn(INPUT, "resize-none text-[11px]"),
            value: selectedElement.altText ?? "",
            onChange: (e) => onUpdateElement({
              altText: e.target.value
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
        /* @__PURE__ */ jsx(
          RangeSlider,
          {
            label: t("pptx.imageAdjustments.brightness"),
            disabled: !canEdit,
            value: fx?.brightness ?? 0,
            onChange: (v) => updateEffects({ brightness: v })
          }
        ),
        /* @__PURE__ */ jsx(
          RangeSlider,
          {
            label: t("pptx.imageAdjustments.contrast"),
            disabled: !canEdit,
            value: fx?.contrast ?? 0,
            onChange: (v) => updateEffects({ contrast: v })
          }
        ),
        /* @__PURE__ */ jsx(
          RangeSlider,
          {
            label: t("pptx.image.saturation"),
            disabled: !canEdit,
            value: fx?.saturation ?? 0,
            onChange: (v) => updateEffects({ saturation: v })
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        ImageCropSection,
        {
          selectedElement,
          canEdit,
          onUpdateElement
        }
      ),
      /* @__PURE__ */ jsx(
        ImageAdjustmentsPanel,
        {
          selectedElement,
          canEdit,
          onUpdateElement
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Grayscale" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: Boolean(fx?.grayscale),
              onChange: (e) => updateEffects({ grayscale: e.target.checked })
            }
          )
        ] }),
        /* @__PURE__ */ jsx(
          DuotonePanel,
          {
            selectedElement,
            canEdit,
            onUpdateElement
          }
        )
      ] }),
      /* @__PURE__ */ jsx(ColorWashSection, { fx, canEdit, updateEffects }),
      /* @__PURE__ */ jsx(ColorChangeSection, { fx, canEdit, updateEffects }),
      /* @__PURE__ */ jsx(
        ArtisticEffectsGallery,
        {
          imgSrc: selectedElement.imageData,
          fx,
          canEdit,
          updateEffects
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: "w-full text-[11px] rounded bg-muted hover:bg-accent border border-border px-2 py-1 text-foreground",
          onClick: () => onUpdateElement({
            imageEffects: {
              brightness: 0,
              contrast: 0,
              saturation: 0,
              grayscale: false,
              artisticEffect: void 0,
              colorWash: void 0,
              alphaModFix: void 0,
              biLevel: void 0,
              duotone: void 0
            },
            cropShape: "none"
          }),
          children: t("pptx.image.resetImage")
        }
      )
    ] })
  ] });
}
var HEADING3 = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD3 = "rounded border border-border bg-card p-2 space-y-2";
var INPUT3 = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN3 = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var LABEL_CLS = "flex items-center justify-between gap-2";
var LABEL_TEXT = "text-muted-foreground";
function msToMmSs(ms) {
  if (!Number.isFinite(ms) || ms < 0) {
    return "00:00";
  }
  const totalSeconds = Math.round(ms / 1e3);
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`;
}
function mmSsToMs(value) {
  const trimmed = value.trim();
  if (trimmed.length === 0) {
    return void 0;
  }
  const colonIdx = trimmed.indexOf(":");
  if (colonIdx >= 0) {
    const minPart = trimmed.slice(0, colonIdx);
    const secPart = trimmed.slice(colonIdx + 1);
    const minutes = parseInt(minPart, 10);
    const seconds2 = parseInt(secPart, 10);
    if (!Number.isFinite(minutes) || !Number.isFinite(seconds2)) {
      return void 0;
    }
    if (minutes < 0 || seconds2 < 0 || seconds2 >= 60) {
      return void 0;
    }
    return (minutes * 60 + seconds2) * 1e3;
  }
  const seconds = parseInt(trimmed, 10);
  if (!Number.isFinite(seconds) || seconds < 0) {
    return void 0;
  }
  return seconds * 1e3;
}
var TRIM_ERROR_KEYS = {
  negative: "pptx.media.trimErrorNegative",
  startAfterEnd: "pptx.media.trimErrorStartAfterEnd",
  beyondDuration: "pptx.media.trimErrorBeyondDuration"
};
function validateTrimRange(trimStartMs, trimEndMs, durationMs) {
  const error = validateMediaTrimRange(trimStartMs, trimEndMs, durationMs);
  return error ? TRIM_ERROR_KEYS[error] : null;
}
function TrimTimeInput({
  label,
  valueMs,
  maxMs,
  disabled,
  onChange
}) {
  const [editValue, setEditValue] = useState(null);
  const [hasError, setHasError] = useState(false);
  const displayValue = editValue ?? msToMmSs(valueMs);
  const handleFocus = useCallback(() => {
    setEditValue(msToMmSs(valueMs));
    setHasError(false);
  }, [valueMs]);
  const handleBlur = useCallback(() => {
    if (editValue !== null) {
      const parsed = mmSsToMs(editValue);
      if (parsed !== void 0 && parsed >= 0 && (maxMs <= 0 || parsed <= maxMs)) {
        onChange(parsed);
        setHasError(false);
      } else {
        setHasError(false);
      }
    }
    setEditValue(null);
  }, [editValue, maxMs, onChange]);
  const handleChange = useCallback((e) => {
    const val = e.target.value;
    setEditValue(val);
    const parsed = mmSsToMs(val);
    setHasError(parsed === void 0);
  }, []);
  const handleKeyDown = useCallback((e) => {
    if (e.key === "Enter") {
      e.currentTarget.blur();
    } else if (e.key === "Escape") {
      setEditValue(null);
      setHasError(false);
    }
  }, []);
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
    /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT}`, children: label }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "text",
        disabled,
        className: `${INPUT3} text-[11px] tabular-nums ${hasError ? "border-red-400" : ""}`,
        value: displayValue,
        placeholder: "00:00",
        onFocus: handleFocus,
        onBlur: handleBlur,
        onChange: handleChange,
        onKeyDown: handleKeyDown
      }
    )
  ] });
}
function MediaInspector({
  element,
  canEdit,
  durationSeconds,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const trimStartMs = element.trimStartMs ?? 0;
  const trimEndMs = element.trimEndMs ?? 0;
  const durationMs = durationSeconds * 1e3;
  const trimmedDuration = useMemo(
    () => msToMmSs(trimmedMediaDurationMs(trimStartMs, trimEndMs, durationMs)),
    [trimStartMs, trimEndMs, durationMs]
  );
  const trimEndAbsoluteMs = mediaTrimEndAbsoluteMs(durationMs, trimEndMs);
  const validationError = useMemo(
    () => validateTrimRange(trimStartMs, trimEndMs, durationMs),
    [trimStartMs, trimEndMs, durationMs]
  );
  const hasTrim = trimStartMs > 0 || trimEndMs > 0;
  const handleTrimStartChange = useCallback(
    (ms) => {
      onUpdateElement({
        trimStartMs: ms
      });
    },
    [onUpdateElement]
  );
  const handleTrimEndChange = useCallback(
    (ms) => {
      onUpdateElement({
        trimEndMs: durationMs > 0 ? mediaTrimEndMsFromAbsoluteMs(durationMs, ms) : ms
      });
    },
    [durationMs, onUpdateElement]
  );
  const handleResetTrim = useCallback(() => {
    onUpdateElement({
      trimStartMs: 0,
      trimEndMs: 0
    });
  }, [onUpdateElement]);
  return /* @__PURE__ */ jsxs("div", { className: CARD3, children: [
    /* @__PURE__ */ jsxs("div", { className: HEADING3, children: [
      /* @__PURE__ */ jsx(LuScissors, { className: "inline w-3 h-3 mr-1" }),
      t("pptx.media.trim")
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
      /* @__PURE__ */ jsx(
        TrimTimeInput,
        {
          label: t("pptx.media.trimStartTime"),
          valueMs: trimStartMs,
          maxMs: durationMs,
          disabled: !canEdit,
          onChange: handleTrimStartChange
        }
      ),
      /* @__PURE__ */ jsx(
        TrimTimeInput,
        {
          label: t("pptx.media.trimEndTime"),
          valueMs: trimEndAbsoluteMs,
          maxMs: durationMs,
          disabled: !canEdit,
          onChange: handleTrimEndChange
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: LABEL_CLS, children: [
      /* @__PURE__ */ jsxs("span", { className: `text-[11px] ${LABEL_TEXT}`, children: [
        /* @__PURE__ */ jsx(LuClock, { className: "inline w-3 h-3 mr-1" }),
        t("pptx.media.trimmedDuration")
      ] }),
      /* @__PURE__ */ jsx("span", { className: "text-[11px] tabular-nums font-medium", children: trimmedDuration })
    ] }),
    validationError && /* @__PURE__ */ jsx("div", { className: "text-[10px] text-red-400", children: t(validationError) }),
    canEdit && hasTrim && /* @__PURE__ */ jsx("button", { type: "button", className: `${BTN3} w-full text-center`, onClick: handleResetTrim, children: t("pptx.media.resetTrim") })
  ] });
}
function formatTime2(seconds) {
  const m = Math.floor(seconds / 60);
  const s = Math.floor(seconds % 60);
  const ms = Math.round(seconds % 1 * 10);
  return `${m}:${String(s).padStart(2, "0")}.${ms}`;
}
function generateBookmarkId() {
  return `bmk-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`;
}
var HEADING4 = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD4 = "rounded border border-border bg-card p-2 space-y-2";
var INPUT4 = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN4 = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var LABEL_CLS2 = "flex items-center justify-between gap-2";
var LABEL_TEXT2 = "text-muted-foreground";
function TrimTimeline({
  duration,
  trimStartMs,
  trimEndMs,
  currentTime,
  bookmarks,
  canEdit,
  onTrimChange,
  onSeek
}) {
  const barRef = useRef(null);
  const [dragging, setDragging] = useState(null);
  const trimStartSec = trimStartMs / 1e3;
  const trimEndSec = mediaTrimEndSeconds(duration, trimEndMs);
  const safeDuration = duration > 0 ? duration : 1;
  const startPct = trimStartSec / safeDuration * 100;
  const endPct = trimEndSec / safeDuration * 100;
  const playheadPct = currentTime / safeDuration * 100;
  const getTimeFromPointer = useCallback(
    (clientX) => {
      const bar = barRef.current;
      if (!bar) {
        return 0;
      }
      const rect = bar.getBoundingClientRect();
      const ratio = Math.max(0, Math.min(1, (clientX - rect.left) / rect.width));
      return ratio * safeDuration;
    },
    [safeDuration]
  );
  useEffect(() => {
    if (!dragging) {
      return;
    }
    const handlePointerMove = (e) => {
      const t = getTimeFromPointer(e.clientX);
      if (dragging === "start") {
        const newStart = Math.min(t, trimEndSec - 0.1);
        onTrimChange(Math.max(0, newStart) * 1e3, trimEndMs);
      } else {
        const newEnd = Math.max(t, trimStartSec + 0.1);
        onTrimChange(trimStartMs, mediaTrimEndMsFromSeconds(duration, newEnd));
      }
    };
    const handlePointerUp = () => {
      setDragging(null);
    };
    window.addEventListener("pointermove", handlePointerMove);
    window.addEventListener("pointerup", handlePointerUp);
    return () => {
      window.removeEventListener("pointermove", handlePointerMove);
      window.removeEventListener("pointerup", handlePointerUp);
    };
  }, [
    dragging,
    duration,
    getTimeFromPointer,
    onTrimChange,
    trimEndMs,
    trimEndSec,
    trimStartMs,
    trimStartSec
  ]);
  const handleBarClick = (e) => {
    const t = getTimeFromPointer(e.clientX);
    onSeek(t);
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between text-[10px] text-muted-foreground", children: [
      /* @__PURE__ */ jsx("span", { children: formatTime2(trimStartSec) }),
      /* @__PURE__ */ jsx("span", { children: formatTime2(trimEndSec) })
    ] }),
    /* @__PURE__ */ jsxs(
      "div",
      {
        ref: barRef,
        className: "relative h-5 rounded bg-muted cursor-pointer select-none",
        onClick: handleBarClick,
        children: [
          /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute top-0 bottom-0 bg-primary/30 rounded",
              style: { left: `${startPct}%`, width: `${endPct - startPct}%` }
            }
          ),
          /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute top-0 bottom-0 w-0.5 bg-white z-10",
              style: { left: `${Math.min(playheadPct, 100)}%` }
            }
          ),
          canEdit && /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute top-0 bottom-0 w-2 bg-primary rounded-l cursor-ew-resize z-20 hover:bg-primary/80",
              style: { left: `calc(${startPct}% - 4px)` },
              onPointerDown: (e) => {
                e.stopPropagation();
                setDragging("start");
              }
            }
          ),
          canEdit && /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute top-0 bottom-0 w-2 bg-primary rounded-r cursor-ew-resize z-20 hover:bg-primary/80",
              style: { left: `calc(${endPct}% - 4px)` },
              onPointerDown: (e) => {
                e.stopPropagation();
                setDragging("end");
              }
            }
          ),
          bookmarks.map((bmk) => {
            const pct = bmk.time / safeDuration * 100;
            return /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute top-0 bottom-0 w-1 bg-yellow-400/70 z-10 cursor-pointer",
                style: { left: `${pct}%` },
                title: bmk.label,
                onClick: (e) => {
                  e.stopPropagation();
                  onSeek(bmk.time);
                }
              },
              bmk.id
            );
          })
        ]
      }
    )
  ] });
}
function MediaPlaybackBookmarks({
  element,
  canEdit,
  currentTime,
  bookmarks,
  onUpdateElement,
  onSeekTo
}) {
  const { t } = useTranslation();
  const [newBookmarkLabel, setNewBookmarkLabel] = useState("");
  const volumePercent = Math.round((element.volume ?? 1) * 100);
  const handleAddBookmark = useCallback(() => {
    const label = newBookmarkLabel.trim() || `${t("pptx.media.bookmark")} ${bookmarks.length + 1}`;
    const newBmk = {
      id: generateBookmarkId(),
      time: currentTime,
      label
    };
    onUpdateElement({
      bookmarks: [...bookmarks, newBmk]
    });
    setNewBookmarkLabel("");
  }, [bookmarks, currentTime, newBookmarkLabel, onUpdateElement, t]);
  const handleRemoveBookmark = useCallback(
    (bmkId) => {
      onUpdateElement({
        bookmarks: bookmarks.filter((b) => b.id !== bmkId)
      });
    },
    [bookmarks, onUpdateElement]
  );
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: CARD4, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING4, children: t("pptx.media.playback") }),
      /* @__PURE__ */ jsxs("label", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.volume") }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 flex-1 max-w-[140px]", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "range",
              min: 0,
              max: 100,
              step: 1,
              disabled: !canEdit,
              className: "flex-1",
              value: volumePercent,
              onChange: (e) => onUpdateElement({
                volume: Number(e.target.value) / 100
              })
            }
          ),
          /* @__PURE__ */ jsxs("span", { className: "text-[10px] text-muted-foreground w-7 text-right tabular-nums", children: [
            volumePercent,
            "%"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ jsxs("label", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.speed") }),
        /* @__PURE__ */ jsxs(
          "select",
          {
            "aria-label": t("pptx.media.speed"),
            disabled: !canEdit,
            className: `${INPUT4} text-[11px] max-w-[100px]`,
            value: element.playbackSpeed ?? 1,
            onChange: (e) => onUpdateElement({
              playbackSpeed: Number(e.target.value)
            }),
            children: [
              /* @__PURE__ */ jsx("option", { value: 0.25, children: "0.25x" }),
              /* @__PURE__ */ jsx("option", { value: 0.5, children: "0.5x" }),
              /* @__PURE__ */ jsx("option", { value: 0.75, children: "0.75x" }),
              /* @__PURE__ */ jsx("option", { value: 1, children: "1x" }),
              /* @__PURE__ */ jsx("option", { value: 1.25, children: "1.25x" }),
              /* @__PURE__ */ jsx("option", { value: 1.5, children: "1.5x" }),
              /* @__PURE__ */ jsx("option", { value: 2, children: "2x" }),
              /* @__PURE__ */ jsx("option", { value: 3, children: "3x" }),
              /* @__PURE__ */ jsx("option", { value: 4, children: "4x" })
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
          /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.fadeIn") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              step: 0.1,
              disabled: !canEdit,
              className: `${INPUT4} text-[11px]`,
              value: element.fadeInDuration ?? 0,
              onChange: (e) => onUpdateElement({
                fadeInDuration: Number(e.target.value) || void 0
              })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
          /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.fadeOut") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              step: 0.1,
              disabled: !canEdit,
              className: `${INPUT4} text-[11px]`,
              value: element.fadeOutDuration ?? 0,
              onChange: (e) => onUpdateElement({
                fadeOutDuration: Number(e.target.value) || void 0
              })
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ jsxs("label", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.loop") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.loop),
            onChange: (e) => onUpdateElement({
              loop: e.target.checked || void 0
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.startTrigger") }),
        /* @__PURE__ */ jsxs(
          "select",
          {
            "aria-label": t("pptx.media.startTrigger"),
            disabled: !canEdit,
            className: "text-[11px] bg-transparent border border-border rounded px-1 py-0.5",
            value: element.autoPlay ? "auto" : "onClick",
            onChange: (e) => onUpdateElement({
              autoPlay: e.target.value === "auto" || void 0
            }),
            children: [
              /* @__PURE__ */ jsx("option", { value: "onClick", children: t("pptx.media.startOnClick") }),
              /* @__PURE__ */ jsx("option", { value: "auto", children: t("pptx.media.startAutomatically") })
            ]
          }
        )
      ] }),
      element.mediaType === "audio" && /* @__PURE__ */ jsxs("label", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.playAcrossSlides") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.playAcrossSlides),
            onChange: (e) => onUpdateElement({
              playAcrossSlides: e.target.checked || void 0,
              ...e.target.checked ? { autoPlay: true } : {}
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.fullScreen") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.fullScreen),
            onChange: (e) => onUpdateElement({
              fullScreen: e.target.checked || void 0
            })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.hideWhenNotPlaying") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.hideWhenNotPlaying),
            onChange: (e) => onUpdateElement({
              hideWhenNotPlaying: e.target.checked || void 0
            })
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: CARD4, children: [
      /* @__PURE__ */ jsxs("div", { className: HEADING4, children: [
        /* @__PURE__ */ jsx(LuBookmark, { className: "inline w-3 h-3 mr-1" }),
        t("pptx.media.bookmarks")
      ] }),
      bookmarks.length > 0 && /* @__PURE__ */ jsx("div", { className: "space-y-1 max-h-32 overflow-y-auto", children: bookmarks.slice().sort((a, b) => a.time - b.time).map((bmk) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 text-[11px] group", children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "text-primary hover:text-primary/80 truncate flex-1 text-left",
            onClick: () => onSeekTo(bmk.time),
            title: t("pptx.media.seekToBookmark"),
            children: bmk.label
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground tabular-nums text-[10px]", children: formatTime2(bmk.time) }),
        canEdit && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "opacity-0 group-hover:opacity-100 text-red-400 hover:text-red-300 transition-opacity",
            onClick: () => handleRemoveBookmark(bmk.id),
            title: t("pptx.common.remove"),
            children: /* @__PURE__ */ jsx(LuTrash2, { className: "w-3 h-3" })
          }
        )
      ] }, bmk.id)) }),
      canEdit && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            className: `${INPUT4} text-[11px]`,
            placeholder: t("pptx.media.bookmarkLabel"),
            value: newBookmarkLabel,
            onChange: (e) => setNewBookmarkLabel(e.target.value),
            onKeyDown: (e) => {
              if (e.key === "Enter") {
                e.preventDefault();
                handleAddBookmark();
              }
            }
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN4,
            onClick: handleAddBookmark,
            title: t("pptx.media.addBookmark"),
            children: /* @__PURE__ */ jsx(LuPlus, { className: "w-3 h-3" })
          }
        )
      ] })
    ] })
  ] });
}
function MediaPropertiesPanel({
  element,
  mediaDataUrls,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const mediaRef = useRef(null);
  const [isPlaying, setIsPlaying] = useState(false);
  const [currentTime, setCurrentTime] = useState(0);
  const [duration, setDuration] = useState(0);
  const dataUrl = element.mediaData ?? (element.mediaPath ? mediaDataUrls.get(element.mediaPath) : void 0);
  const bookmarks = element.bookmarks ?? [];
  const isVideo = element.mediaType === "video";
  useEffect(() => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    const onTimeUpdate = () => setCurrentTime(el.currentTime);
    const onDurationChange = () => {
      if (Number.isFinite(el.duration)) {
        setDuration(el.duration);
      }
    };
    const onPlay = () => setIsPlaying(true);
    const onPause = () => setIsPlaying(false);
    const onEnded = () => setIsPlaying(false);
    el.addEventListener("timeupdate", onTimeUpdate);
    el.addEventListener("durationchange", onDurationChange);
    el.addEventListener("loadedmetadata", onDurationChange);
    el.addEventListener("play", onPlay);
    el.addEventListener("pause", onPause);
    el.addEventListener("ended", onEnded);
    return () => {
      el.removeEventListener("timeupdate", onTimeUpdate);
      el.removeEventListener("durationchange", onDurationChange);
      el.removeEventListener("loadedmetadata", onDurationChange);
      el.removeEventListener("play", onPlay);
      el.removeEventListener("pause", onPause);
      el.removeEventListener("ended", onEnded);
    };
  }, [dataUrl]);
  const togglePlay = useCallback(() => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    if (el.paused) {
      void el.play();
    } else {
      el.pause();
    }
  }, []);
  const seekTo = useCallback((time) => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    el.currentTime = time;
    setCurrentTime(time);
  }, []);
  const handleTrimChange = useCallback(
    (trimStartMs, trimEndMs) => {
      onUpdateElement({ trimStartMs, trimEndMs });
    },
    [onUpdateElement]
  );
  const [extractedMetadata, setExtractedMetadata] = useState(
    element.metadata
  );
  useEffect(() => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    if (element.metadata?.duration !== void 0) {
      setExtractedMetadata(element.metadata);
      return;
    }
    const extract = () => {
      if (!Number.isFinite(el.duration) || el.duration === 0) {
        return;
      }
      const meta = { duration: el.duration };
      if (el instanceof HTMLVideoElement) {
        if (el.videoWidth > 0) {
          meta.videoWidth = el.videoWidth;
        }
        if (el.videoHeight > 0) {
          meta.videoHeight = el.videoHeight;
        }
      }
      const sourceEl = el.querySelector("source");
      if (sourceEl?.type) {
        meta.codecInfo = sourceEl.type;
      }
      element.metadata = meta;
      setExtractedMetadata(meta);
    };
    el.addEventListener("loadedmetadata", extract);
    if (el.readyState >= 1) {
      extract();
    }
    return () => {
      el.removeEventListener("loadedmetadata", extract);
    };
  }, [dataUrl, element]);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-3", children: [
    /* @__PURE__ */ jsxs("div", { className: CARD4, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING4, children: t("pptx.media.title") }),
      /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: isVideo ? t("pptx.media.videoClip") : t("pptx.media.audioClip") }),
      dataUrl && /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        isVideo ? /* @__PURE__ */ jsx(
          "video",
          {
            ref: mediaRef,
            className: "w-full rounded bg-black max-h-32 object-contain",
            src: dataUrl,
            preload: "metadata"
          }
        ) : /* @__PURE__ */ jsx(
          "audio",
          {
            ref: mediaRef,
            className: "w-full",
            src: dataUrl,
            preload: "metadata"
          }
        ),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: BTN4,
              onClick: togglePlay,
              title: isPlaying ? t("pptx.media.pause") : t("pptx.media.play"),
              children: isPlaying ? /* @__PURE__ */ jsx(LuPause, { className: "w-3 h-3" }) : /* @__PURE__ */ jsx(LuPlay, { className: "w-3 h-3" })
            }
          ),
          /* @__PURE__ */ jsxs("span", { className: "text-[10px] text-muted-foreground tabular-nums", children: [
            formatTime2(currentTime),
            " / ",
            formatTime2(duration)
          ] })
        ] })
      ] }),
      duration > 0 && /* @__PURE__ */ jsx(
        TrimTimeline,
        {
          duration,
          trimStartMs: element.trimStartMs ?? 0,
          trimEndMs: element.trimEndMs ?? 0,
          currentTime,
          bookmarks,
          canEdit,
          onTrimChange: handleTrimChange,
          onSeek: seekTo
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      MediaInspector,
      {
        element,
        canEdit,
        durationSeconds: duration,
        onUpdateElement
      }
    ),
    (extractedMetadata || element.posterFrameData) && /* @__PURE__ */ jsxs("div", { className: CARD4, children: [
      /* @__PURE__ */ jsxs("div", { className: HEADING4, children: [
        /* @__PURE__ */ jsx(LuInfo, { className: "inline w-3 h-3 mr-1" }),
        t("pptx.media.info")
      ] }),
      element.posterFrameData && /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsx("div", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.posterFrame") }),
        /* @__PURE__ */ jsx(
          "img",
          {
            src: element.posterFrameData,
            alt: t("pptx.media.posterFrame"),
            className: "w-full max-h-20 object-contain rounded bg-black/20"
          }
        )
      ] }),
      extractedMetadata?.duration !== void 0 && /* @__PURE__ */ jsxs("div", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.duration") }),
        /* @__PURE__ */ jsx("span", { className: "text-[11px] tabular-nums", children: formatTime2(extractedMetadata.duration) })
      ] }),
      extractedMetadata?.videoWidth !== void 0 && extractedMetadata?.videoHeight !== void 0 && /* @__PURE__ */ jsxs("div", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.resolution") }),
        /* @__PURE__ */ jsxs("span", { className: "text-[11px] tabular-nums", children: [
          extractedMetadata.videoWidth,
          " x ",
          extractedMetadata.videoHeight
        ] })
      ] }),
      extractedMetadata?.codecInfo && /* @__PURE__ */ jsxs("div", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.format") }),
        /* @__PURE__ */ jsx(
          "span",
          {
            className: "text-[11px] truncate max-w-[140px]",
            title: extractedMetadata.codecInfo,
            children: extractedMetadata.codecInfo
          }
        )
      ] }),
      element.mediaPath && /* @__PURE__ */ jsxs("div", { className: LABEL_CLS2, children: [
        /* @__PURE__ */ jsx("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.filePath") }),
        /* @__PURE__ */ jsx(
          "span",
          {
            className: "text-[10px] truncate max-w-[140px] text-muted-foreground",
            title: element.mediaPath,
            children: element.mediaPath.split("/").pop()
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx(
      MediaPlaybackBookmarks,
      {
        element,
        canEdit,
        currentTime,
        bookmarks,
        onUpdateElement,
        onSeekTo: seekTo
      }
    )
  ] });
}
function AccessibilityTextSection({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const fields = getNonVisualDescriptionFields(selectedElement);
  if (!fields.showAltText && !fields.showTitle) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD, "data-pptx-accessibility-text": true, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.accessibility.heading", "Accessibility") }),
    fields.showAltText && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.elementAccessibility.altText") }),
      /* @__PURE__ */ jsx(
        "textarea",
        {
          rows: 2,
          disabled: !canEdit,
          placeholder: t("pptx.elementAccessibility.altTextPlaceholder"),
          className: cn(INPUT, "resize-none text-[11px]"),
          value: fields.altText,
          onChange: (e) => onUpdateElement({ altText: e.target.value })
        }
      )
    ] }),
    fields.showTitle && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.elementAccessibility.title") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          disabled: !canEdit,
          placeholder: t("pptx.elementAccessibility.titlePlaceholder"),
          className: cn(INPUT, "text-[11px]"),
          value: fields.title,
          onChange: (e) => onUpdateElement({ title: e.target.value })
        }
      )
    ] })
  ] });
}

// src/viewer/components/inspector/fill-stroke-options.ts
var COMPOUND_LINE_OPTIONS2 = COMPOUND_LINE_OPTIONS;
var LINE_JOIN_OPTIONS2 = LINE_JOIN_OPTIONS;
var LINE_CAP_OPTIONS2 = LINE_CAP_OPTIONS;
var PATTERN_PRESET_OPTIONS2 = PATTERN_PRESET_OPTIONS;
var FILL_MODE_OPTIONS2 = [
  { value: "solid", label: "Solid", i18nKey: "pptx.fill.solid" },
  { value: "gradient", label: "Gradient", i18nKey: "pptx.fill.gradient" },
  { value: "pattern", label: "Pattern", i18nKey: "pptx.table.fillPattern" },
  { value: "image", label: "Image", i18nKey: "pptx.inspector.image" },
  { value: "none", label: "None", i18nKey: "pptx.fill.none" }
];
var GRADIENT_TYPE_OPTIONS2 = [
  { value: "linear", label: "Linear", i18nKey: "pptx.gradient.linear" },
  { value: "radial", label: "Radial", i18nKey: "pptx.gradient.radial" }
];
var IMAGE_MODE_OPTIONS = [
  { value: "stretch", label: "Stretch", i18nKey: "pptx.image.stretch" },
  { value: "tile", label: "Tile", i18nKey: "pptx.image.tile" }
];
function getCompoundLinePreviewStyle(type) {
  const baseColor = "#6b7280";
  switch (type) {
    case "sng":
      return {
        borderTop: `2px solid ${baseColor}`,
        width: "100%"
      };
    case "dbl": {
      const lineWidth = 2;
      const gap = 2;
      return {
        position: "relative",
        height: `${lineWidth * 2 + gap}px`,
        width: "100%",
        boxShadow: `inset 0 ${lineWidth + gap}px 0 ${-lineWidth}px ${baseColor}, inset 0 ${ -4}px 0 ${-lineWidth}px ${baseColor}`
      };
    }
    case "thickThin": {
      const thickWidth = 3;
      const thinWidth = 1;
      const gap = 1;
      return {
        position: "relative",
        height: `${thickWidth + thinWidth + gap}px`,
        width: "100%",
        boxShadow: `inset 0 ${thickWidth / 2 + gap}px 0 ${-thickWidth}px ${baseColor}, inset 0 ${ -3.5}px 0 ${-thinWidth}px ${baseColor}`
      };
    }
    case "thinThick": {
      const thinWidth = 1;
      const thickWidth = 3;
      const gap = 1;
      return {
        position: "relative",
        height: `${thinWidth + thickWidth + gap}px`,
        width: "100%",
        boxShadow: `inset 0 ${thickWidth / 2 + gap}px 0 ${-thinWidth}px ${baseColor}, inset 0 ${ -3.5}px 0 ${-thickWidth}px ${baseColor}`
      };
    }
    case "tri": {
      const lineWidth = 1;
      const gap = 1;
      const offset1 = lineWidth + gap;
      return {
        position: "relative",
        height: `${lineWidth * 3 + gap * 2}px`,
        width: "100%",
        boxShadow: `inset 0 0 0 ${-lineWidth}px ${baseColor}, inset 0 ${offset1}px 0 ${-lineWidth}px ${baseColor}, inset 0 ${ -4}px 0 ${-lineWidth}px ${baseColor}`
      };
    }
    default:
      return {};
  }
}

// src/viewer/components/inspector/bevel-type-options.ts
var BEVEL_TYPE_OPTIONS = [
  { value: "circle", label: "Circle", i18nKey: "pptx.bevelTypes.circle" },
  { value: "relaxedInset", label: "Relaxed Inset", i18nKey: "pptx.bevelTypes.relaxedInset" },
  { value: "cross", label: "Cross", i18nKey: "pptx.bevelTypes.cross" },
  { value: "slope", label: "Slope", i18nKey: "pptx.bevelTypes.slope" },
  { value: "convex", label: "Convex", i18nKey: "pptx.bevelTypes.convex" },
  { value: "coolSlant", label: "Cool Slant", i18nKey: "pptx.bevelTypes.coolSlant" },
  { value: "angle", label: "Angle", i18nKey: "pptx.bevelTypes.angle" },
  { value: "softRound", label: "Soft Round", i18nKey: "pptx.bevelTypes.softRound" },
  { value: "riblet", label: "Riblet", i18nKey: "pptx.bevelTypes.riblet" },
  { value: "hardEdge", label: "Hard Edge", i18nKey: "pptx.bevelTypes.hardEdge" },
  { value: "artDeco", label: "Art Deco", i18nKey: "pptx.bevelTypes.artDeco" },
  { value: "divot", label: "Divot", i18nKey: "pptx.bevelTypes.divot" }
];

// src/viewer/components/inspector/fill-stroke-effect-configs.ts
var clamp = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
var SHADOW_EFFECT_CONFIGS = [
  /* Shadow */
  {
    label: "Shadow",
    isOn: (s) => Boolean(s?.shadowColor) && s?.shadowColor !== "transparent",
    onEnable: (s) => {
      let angle = s?.shadowAngle ?? 315;
      let distance = s?.shadowDistance ?? 5.66;
      const offsetX = s?.shadowOffsetX ?? 4;
      const offsetY = s?.shadowOffsetY ?? 4;
      if (s?.shadowAngle === null && s?.shadowDistance === null && (offsetX !== 0 || offsetY !== 0)) {
        distance = Math.sqrt(offsetX * offsetX + offsetY * offsetY);
        angle = (Math.atan2(offsetY, offsetX) * 180 / Math.PI + 360) % 360;
      }
      return {
        shadowColor: normalizeHexColor(s?.shadowColor, "#000000"),
        shadowOpacity: s?.shadowOpacity ?? 0.35,
        shadowBlur: s?.shadowBlur ?? 6,
        shadowAngle: angle,
        shadowDistance: distance,
        shadowOffsetX: offsetX,
        shadowOffsetY: offsetY,
        shadowRotateWithShape: s?.shadowRotateWithShape ?? true
      };
    },
    onDisable: () => ({ shadowColor: "transparent" }),
    fields: [
      {
        key: "sc",
        label: "Shadow Color",
        type: "color",
        read: (s) => normalizeHexColor(s?.shadowColor, "#000000"),
        write: (v) => ({ shadowColor: String(v) })
      },
      {
        key: "so",
        label: "Shadow Opacity",
        type: "range",
        min: 0,
        max: 100,
        isPercent: true,
        read: (s) => Math.round((s?.shadowOpacity ?? 0.35) * 100),
        write: (v) => ({ shadowOpacity: Number(v) / 100 })
      },
      {
        key: "sb",
        label: "Shadow Blur",
        type: "number",
        min: 0,
        max: 96,
        read: (s) => Math.round(s?.shadowBlur ?? 6),
        write: (v) => ({ shadowBlur: clamp(Number(v), 0, 96) })
      },
      {
        key: "sa",
        label: "Shadow Angle",
        type: "number",
        min: 0,
        max: 359,
        read: (s) => {
          if (typeof s?.shadowAngle === "number") {
            return Math.round(s.shadowAngle) % 360;
          }
          const x = s?.shadowOffsetX ?? 4;
          const y = s?.shadowOffsetY ?? 4;
          return Math.round((Math.atan2(y, x) * 180 / Math.PI + 360) % 360);
        },
        write: (v) => {
          const angle = Number(v) % 360;
          return (s) => {
            const dist = typeof s?.shadowDistance === "number" ? s.shadowDistance : 5.66;
            const angleRad = angle * Math.PI / 180;
            return {
              shadowAngle: angle,
              shadowDistance: dist,
              shadowOffsetX: Math.cos(angleRad) * dist,
              shadowOffsetY: Math.sin(angleRad) * dist
            };
          };
        }
      },
      {
        key: "sd",
        label: "Shadow Distance",
        type: "number",
        min: 0,
        max: 100,
        read: (s) => {
          if (typeof s?.shadowDistance === "number") {
            return Math.round(s.shadowDistance * 10) / 10;
          }
          const x = s?.shadowOffsetX ?? 4;
          const y = s?.shadowOffsetY ?? 4;
          return Math.round(Math.sqrt(x * x + y * y) * 10) / 10;
        },
        write: (v) => {
          return (s) => {
            const distance = clamp(Number(v), 0, 100);
            const angle = typeof s?.shadowAngle === "number" ? s.shadowAngle : 315;
            const angleRad = angle * Math.PI / 180;
            return {
              shadowAngle: angle,
              shadowDistance: distance,
              shadowOffsetX: Math.cos(angleRad) * distance,
              shadowOffsetY: Math.sin(angleRad) * distance
            };
          };
        }
      },
      {
        key: "sr",
        label: "Rotate with Shape",
        i18nKey: "pptx.effects.rotateWithShape",
        type: "checkbox",
        read: (s) => s?.shadowRotateWithShape ?? true,
        write: (v) => ({ shadowRotateWithShape: Boolean(v) })
      },
      {
        key: "sx",
        label: "Shadow X",
        type: "number",
        min: -96,
        max: 96,
        read: (s) => Math.round(s?.shadowOffsetX ?? 4),
        write: (v) => {
          return (s) => {
            const offsetX = clamp(Number(v), -96, 96);
            const offsetY = s?.shadowOffsetY ?? 4;
            const distance = Math.sqrt(offsetX * offsetX + offsetY * offsetY);
            const angle = (Math.atan2(offsetY, offsetX) * 180 / Math.PI + 360) % 360;
            return {
              shadowOffsetX: offsetX,
              shadowAngle: angle,
              shadowDistance: distance
            };
          };
        }
      },
      {
        key: "sy",
        label: "Shadow Y",
        type: "number",
        min: -96,
        max: 96,
        span2: true,
        read: (s) => Math.round(s?.shadowOffsetY ?? 4),
        write: (v) => {
          return (s) => {
            const offsetY = clamp(Number(v), -96, 96);
            const offsetX = s?.shadowOffsetX ?? 4;
            const distance = Math.sqrt(offsetX * offsetX + offsetY * offsetY);
            const angle = (Math.atan2(offsetY, offsetX) * 180 / Math.PI + 360) % 360;
            return {
              shadowOffsetY: offsetY,
              shadowAngle: angle,
              shadowDistance: distance
            };
          };
        }
      }
    ]
  },
  /* Inner Shadow */
  {
    label: "Inner Shadow",
    isOn: (s) => Boolean(s?.innerShadowColor) && s?.innerShadowColor !== "transparent",
    onEnable: (s) => ({
      innerShadowColor: normalizeHexColor(s?.innerShadowColor, "#000000"),
      innerShadowOpacity: s?.innerShadowOpacity ?? 0.5,
      innerShadowBlur: s?.innerShadowBlur ?? 5,
      innerShadowOffsetX: s?.innerShadowOffsetX ?? 0,
      innerShadowOffsetY: s?.innerShadowOffsetY ?? 0
    }),
    onDisable: () => ({ innerShadowColor: "transparent" }),
    fields: [
      {
        key: "isc",
        label: "Color",
        type: "color",
        read: (s) => normalizeHexColor(s?.innerShadowColor, "#000000"),
        write: (v) => ({ innerShadowColor: String(v) })
      },
      {
        key: "iso",
        label: "Opacity",
        type: "range",
        min: 0,
        max: 100,
        isPercent: true,
        read: (s) => Math.round((s?.innerShadowOpacity ?? 0.5) * 100),
        write: (v) => ({ innerShadowOpacity: Number(v) / 100 })
      },
      {
        key: "isb",
        label: "Blur",
        type: "number",
        min: 0,
        max: 96,
        read: (s) => Math.round(s?.innerShadowBlur ?? 5),
        write: (v) => ({ innerShadowBlur: clamp(Number(v), 0, 96) })
      },
      {
        key: "isx",
        label: "Offset X",
        type: "number",
        min: -96,
        max: 96,
        read: (s) => Math.round(s?.innerShadowOffsetX ?? 0),
        write: (v) => ({
          innerShadowOffsetX: clamp(Number(v), -96, 96)
        })
      },
      {
        key: "isy",
        label: "Offset Y",
        type: "number",
        min: -96,
        max: 96,
        span2: true,
        read: (s) => Math.round(s?.innerShadowOffsetY ?? 0),
        write: (v) => ({
          innerShadowOffsetY: clamp(Number(v), -96, 96)
        })
      }
    ]
  }
];

// src/viewer/components/inspector/fill-stroke-visual-configs.ts
var clamp2 = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
var VISUAL_EFFECT_CONFIGS = [
  /* Glow */
  {
    label: "Glow",
    isOn: (s) => Boolean(s?.glowColor) && s?.glowColor !== "transparent",
    onEnable: (s) => ({
      glowColor: s?.glowColor && s.glowColor !== "transparent" ? s.glowColor : "#ffff00",
      glowOpacity: s?.glowOpacity ?? 0.75,
      glowRadius: s?.glowRadius ?? 6
    }),
    onDisable: () => ({ glowColor: "transparent", glowRadius: 0 }),
    fields: [
      {
        key: "gc",
        label: "Glow Color",
        type: "color",
        read: (s) => s?.glowColor && s.glowColor !== "transparent" ? s.glowColor : "#ffff00",
        write: (v) => ({ glowColor: String(v) })
      },
      {
        key: "go",
        label: "Glow Opacity",
        type: "range",
        min: 0,
        max: 100,
        isPercent: true,
        read: (s) => Math.round((s?.glowOpacity ?? 0.75) * 100),
        write: (v) => ({ glowOpacity: Number(v) / 100 })
      },
      {
        key: "gr",
        label: "Glow Radius",
        type: "number",
        min: 0,
        max: 96,
        span2: true,
        read: (s) => Math.round(s?.glowRadius ?? 6),
        write: (v) => ({ glowRadius: clamp2(Number(v), 0, 96) })
      }
    ]
  },
  /* Soft Edge */
  {
    label: "Soft Edge",
    i18nKey: "pptx.inspector.softEdge",
    isOn: (s) => typeof s?.softEdgeRadius === "number" && s.softEdgeRadius > 0,
    onEnable: () => ({ softEdgeRadius: 6 }),
    onDisable: () => ({ softEdgeRadius: 0 }),
    fields: [
      {
        key: "se",
        label: "Soft Edge Radius",
        i18nKey: "pptx.inspector.softEdgeRadius",
        type: "number",
        min: 0,
        max: 96,
        span2: true,
        read: (s) => Math.round(s?.softEdgeRadius ?? 6),
        write: (v) => ({ softEdgeRadius: clamp2(Number(v), 0, 96) })
      }
    ]
  },
  /* Reflection */
  {
    label: "Reflection",
    isOn: (s) => (s?.reflectionBlurRadius ?? 0) > 0 || (s?.reflectionStartOpacity ?? 0) > 0,
    onEnable: () => ({
      reflectionBlurRadius: 3,
      reflectionStartOpacity: 50,
      reflectionEndOpacity: 0,
      reflectionEndPosition: 50,
      reflectionDirection: 90,
      reflectionDistance: 0
    }),
    onDisable: () => ({
      reflectionBlurRadius: 0,
      reflectionStartOpacity: 0,
      reflectionEndOpacity: 0,
      reflectionEndPosition: 0,
      reflectionDirection: 0,
      reflectionDistance: 0
    }),
    fields: [
      {
        key: "rb",
        label: "Blur",
        type: "number",
        min: 0,
        max: 20,
        step: 0.5,
        read: (s) => s?.reflectionBlurRadius ?? 3,
        write: (v) => ({ reflectionBlurRadius: clamp2(Number(v), 0, 20) })
      },
      {
        key: "rs",
        label: "Start Opacity %",
        type: "number",
        min: 0,
        max: 100,
        read: (s) => s?.reflectionStartOpacity ?? 50,
        write: (v) => ({ reflectionStartOpacity: clamp2(Number(v), 0, 100) })
      },
      {
        key: "re",
        label: "End Opacity %",
        type: "number",
        min: 0,
        max: 100,
        read: (s) => s?.reflectionEndOpacity ?? 0,
        write: (v) => ({ reflectionEndOpacity: clamp2(Number(v), 0, 100) })
      },
      {
        key: "rd",
        label: "Distance",
        type: "number",
        min: 0,
        max: 50,
        read: (s) => s?.reflectionDistance ?? 0,
        write: (v) => ({ reflectionDistance: clamp2(Number(v), 0, 50) })
      },
      {
        key: "rdir",
        label: "Direction",
        type: "number",
        min: 0,
        max: 360,
        read: (s) => s?.reflectionDirection ?? 90,
        write: (v) => ({ reflectionDirection: clamp2(Number(v), 0, 360) })
      },
      {
        key: "rrot",
        label: "Rotation",
        type: "number",
        min: 0,
        max: 360,
        span2: true,
        read: (s) => s?.reflectionRotation ?? 0,
        write: (v) => ({ reflectionRotation: clamp2(Number(v), 0, 360) })
      }
    ]
  },
  /* Blur */
  {
    label: "Blur",
    isOn: (s) => (s?.blurRadius ?? 0) > 0,
    onEnable: () => ({ blurRadius: 4 }),
    onDisable: () => ({ blurRadius: 0 }),
    fields: [
      {
        key: "bl",
        label: "Blur Radius",
        type: "number",
        min: 0,
        max: 50,
        span2: true,
        read: (s) => Math.round(s?.blurRadius ?? 4),
        write: (v) => ({ blurRadius: clamp2(Number(v), 0, 50) })
      }
    ]
  },
  /* Bevel / 3D */
  {
    label: "Bevel / 3D",
    isOn: (s) => Boolean(s?.shape3d?.bevelTopType) && s?.shape3d?.bevelTopType !== "none",
    onEnable: (s) => ({
      shape3d: {
        ...s?.shape3d,
        bevelTopType: s?.shape3d?.bevelTopType || "circle",
        bevelTopWidth: s?.shape3d?.bevelTopWidth ?? 76200,
        bevelTopHeight: s?.shape3d?.bevelTopHeight ?? 76200
      }
    }),
    onDisable: (s) => ({
      shape3d: {
        ...s?.shape3d,
        bevelTopType: void 0,
        bevelTopWidth: void 0,
        bevelTopHeight: void 0
      }
    }),
    fields: [
      {
        key: "btype",
        label: "Bevel Type",
        type: "select",
        span2: true,
        options: BEVEL_TYPE_OPTIONS,
        read: (s) => s?.shape3d?.bevelTopType ?? "circle",
        write: (v, s) => ({
          shape3d: { ...s?.shape3d, bevelTopType: String(v) }
        })
      },
      {
        key: "bw",
        label: "Bevel Width (EMU)",
        type: "number",
        min: 0,
        max: 5e5,
        step: 12700,
        read: (s) => s?.shape3d?.bevelTopWidth ?? 76200,
        write: (v, s) => ({
          shape3d: {
            ...s?.shape3d,
            bevelTopWidth: clamp2(Number(v), 0, 5e5)
          }
        })
      },
      {
        key: "bh",
        label: "Bevel Height (EMU)",
        type: "number",
        min: 0,
        max: 5e5,
        step: 12700,
        span2: true,
        read: (s) => s?.shape3d?.bevelTopHeight ?? 76200,
        write: (v, s) => ({
          shape3d: {
            ...s?.shape3d,
            bevelTopHeight: clamp2(Number(v), 0, 5e5)
          }
        })
      }
    ]
  }
];

// src/viewer/components/inspector/FillStrokeHelpers.tsx
var EFFECT_CONFIGS = [...SHADOW_EFFECT_CONFIGS, ...VISUAL_EFFECT_CONFIGS];
var SEL2 = "bg-muted border border-border rounded px-2 py-1";
var NUM2 = SEL2;
var RNG = "accent-primary";
var SWATCH = "h-4 w-4 rounded border border-border";
var DIS = "disabled:opacity-40 disabled:cursor-not-allowed";
var LBL2 = "text-muted-foreground";
var COL2 = "col-span-2";
var isLineish = (el, st) => el.type === "connector" || st === "line";
var safeNum = (raw, fallback) => {
  const n = Number(raw);
  return Number.isFinite(n) ? n : fallback;
};
function EffectField({
  field,
  style,
  onUpdate
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const fieldLabel = field.i18nKey ? t(field.i18nKey) : field.label;
  const val = field.read(style);
  const cls = `flex flex-col gap-1 ${field.span2 ? COL2 : ""}`;
  if (field.type === "select" && field.options) {
    return /* @__PURE__ */ jsxs("label", { className: cls, children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: fieldLabel }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": fieldLabel,
          value: String(val),
          onChange: (e) => {
            const result = field.write(e.target.value, style);
            onUpdate(typeof result === "function" ? result(style) : result);
          },
          className: SEL2,
          children: field.options.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: o.label }, o.value))
        }
      )
    ] });
  }
  if (field.type === "color") {
    return /* @__PURE__ */ jsxs("label", { className: cls, children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: fieldLabel }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          value: String(val),
          onChange: (e) => {
            const result = field.write(e.target.value, style);
            onUpdate(typeof result === "function" ? result(style) : result);
            pushColor(e.target.value);
          },
          className: `h-8 ${SEL2} px-1`
        }
      )
    ] });
  }
  if (field.type === "checkbox") {
    return /* @__PURE__ */ jsxs("label", { className: `flex items-center gap-2 ${field.span2 ? COL2 : ""}`, children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: Boolean(val),
          onChange: (e) => {
            const result = field.write(e.target.checked, style);
            onUpdate(typeof result === "function" ? result(style) : result);
          },
          className: "h-4 w-4"
        }
      ),
      /* @__PURE__ */ jsx("span", { className: LBL2, children: fieldLabel })
    ] });
  }
  if (field.type === "range") {
    return /* @__PURE__ */ jsxs("label", { className: cls, children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: fieldLabel }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "range",
          min: field.min ?? 0,
          max: field.max ?? 100,
          value: Number(val),
          onChange: (e) => {
            const result = field.write(Number(e.target.value), style);
            onUpdate(typeof result === "function" ? result(style) : result);
          },
          className: RNG
        }
      )
    ] });
  }
  return /* @__PURE__ */ jsxs("label", { className: cls, children: [
    /* @__PURE__ */ jsx("span", { className: LBL2, children: fieldLabel }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "number",
        min: field.min,
        max: field.max,
        step: field.step,
        value: Number(val),
        onChange: (e) => {
          const n = safeNum(e.target.value, Number(val));
          const result = field.write(n, style);
          onUpdate(typeof result === "function" ? result(style) : result);
        },
        className: NUM2
      }
    )
  ] });
}
function SelectRow({
  label,
  value,
  span2,
  options,
  onChange
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("label", { className: `flex flex-col gap-1 ${span2 ? COL2 : ""}`, children: [
    /* @__PURE__ */ jsx("span", { className: LBL2, children: label }),
    /* @__PURE__ */ jsx(
      "select",
      {
        "aria-label": label,
        value,
        onChange: (e) => onChange(e.target.value),
        className: SEL2,
        children: options.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: o.i18nKey ? t(o.i18nKey) : o.label }, o.value))
      }
    )
  ] });
}
function ColorPickerRow({
  label,
  value,
  selectedRef,
  disabled,
  prefix,
  onChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const commit = (color, ref) => {
    onChange(color, ref);
    pushColor(color);
  };
  const handleEyedropper = async () => {
    const color = await openNativeEyeDropper();
    if (color) {
      commit(color);
    }
  };
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: LBL2, children: label }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          value,
          disabled,
          onChange: (e) => commit(e.target.value),
          className: `h-8 flex-1 ${SEL2} px-1 ${DIS}`
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled,
          className: "h-8 w-8 flex items-center justify-center rounded border border-border bg-muted hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed transition-colors",
          title: t("pptx.fillStroke.eyedropperTooltip"),
          onClick: (e) => {
            e.preventDefault();
            void handleEyedropper();
          },
          children: /* @__PURE__ */ jsx(LuPipette, { className: "w-3.5 h-3.5" })
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      ThemeColorSwatchGrid,
      {
        prefix,
        disabled,
        selectedRef,
        selectedHex: value,
        onPick: (c) => commit(c.hex, c.ref)
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "mt-1", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.colorPicker.standardColors") }),
      /* @__PURE__ */ jsx("div", { className: "flex flex-wrap gap-1", children: OFFICE_COLOR_SWATCHES.map((c) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: `${SWATCH} ${DIS}`,
          style: { backgroundColor: c.hex },
          title: `${label} ${c.label}`,
          "aria-label": `${label} ${c.label}`,
          "data-pptx-compact": true,
          disabled,
          onClick: () => commit(c.hex)
        },
        `${prefix}-standard-${c.hex}`
      )) })
    ] }),
    /* @__PURE__ */ jsx(RecentColorsRow, { prefix, disabled, onCommit: commit })
  ] });
}
function GradientStopRow({
  stop,
  index,
  total,
  onUpdate,
  allStops
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const patchStop = (patch) => {
    const next = allStops.map((s, i) => i === index ? { ...s, ...patch } : s);
    onUpdate(next);
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[auto,1fr,auto] items-center gap-2", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          value: normalizeHexColor(stop.color, "#3b82f6"),
          onChange: (e) => {
            const hex = normalizeHexColor(e.target.value, "#3b82f6");
            patchStop({ color: hex, colorRef: void 0 });
            pushColor(hex);
          },
          className: "h-7 w-10 rounded border border-border bg-muted"
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          value: Math.round(stop.position),
          onChange: (e) => patchStop({ position: Number(e.target.value) }),
          className: RNG
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: total <= 2,
          className: "rounded bg-muted px-2 py-1 text-[11px] hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed",
          onClick: () => onUpdate(allStops.filter((_, i) => i !== index)),
          children: t("pptx.comments.remove")
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      ThemeColorSwatchGrid,
      {
        prefix: `gradient-stop-${index}`,
        selectedRef: stop.colorRef,
        selectedHex: stop.color,
        onPick: (c) => {
          patchStop({ color: c.hex, colorRef: c.ref });
          pushColor(c.hex);
        }
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[auto,1fr,auto] items-center gap-2 pl-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground w-10 text-center", children: "Opacity" }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          value: Math.round((stop.opacity ?? 1) * 100),
          onChange: (e) => patchStop({ opacity: Number(e.target.value) / 100 }),
          className: RNG
        }
      ),
      /* @__PURE__ */ jsxs("span", { className: "text-[10px] text-muted-foreground w-[52px] text-right", children: [
        Math.round((stop.opacity ?? 1) * 100),
        "%"
      ] })
    ] })
  ] });
}
function FillAdvancedControls({
  style,
  gradientStops,
  showGradient,
  gradType,
  isLine,
  onUpdateShapeStyle,
  onUpdateGradientStops,
  onSetFillColor: _onSetFillColor
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    showGradient && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(
        SelectRow,
        {
          label: t("pptx.fillAdvanced.gradientType"),
          span2: true,
          value: gradType,
          options: GRADIENT_TYPE_OPTIONS2,
          onChange: (v) => onUpdateShapeStyle({
            fillMode: "gradient",
            fillGradientType: v
          })
        }
      ),
      gradType === "linear" && /* @__PURE__ */ jsxs("label", { className: `flex flex-col gap-1 ${COL2}`, children: [
        /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.fillAdvanced.gradientAngle") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            min: 0,
            max: 360,
            value: Math.round(style?.fillGradientAngle ?? 90),
            onChange: (e) => onUpdateShapeStyle({
              fillMode: "gradient",
              fillGradientAngle: Number(e.target.value)
            }),
            className: RNG
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: `${COL2} space-y-1.5 rounded border border-border p-2`, children: [
        /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.fillAdvanced.gradientStops") }),
        gradientStops.map((stop, i) => /* @__PURE__ */ jsx(
          GradientStopRow,
          {
            stop,
            index: i,
            total: gradientStops.length,
            allStops: gradientStops,
            onUpdate: onUpdateGradientStops
          },
          `gradient-stop-${i}`
        )),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "inline-flex items-center justify-center rounded bg-muted px-2 py-1 text-[11px] hover:bg-accent",
            onClick: () => onUpdateGradientStops([
              ...gradientStops,
              {
                color: normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR),
                position: 50,
                opacity: 1
              }
            ]),
            children: t("pptx.gradient.addStop")
          }
        )
      ] })
    ] }),
    style?.fillMode === "pattern" && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsxs("div", { className: `${COL2} flex flex-col gap-2`, children: [
        /* @__PURE__ */ jsx("span", { className: LBL2, children: "Pattern" }),
        /* @__PURE__ */ jsx("div", { className: "grid grid-cols-8 gap-1 max-h-48 overflow-y-auto rounded border border-border p-2", children: PATTERN_PRESET_OPTIONS2.map((opt) => {
          const fgColor = normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR);
          const bgColor = normalizeHexColor(style?.fillPatternBackgroundColor, "#ffffff");
          const svgPattern = getPatternSvg(opt.value, fgColor, bgColor);
          const isSelected = (style.fillPatternPreset || "pct20") === opt.value;
          return /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              title: t(opt.labelKey),
              className: `h-8 w-8 rounded border transition-all ${isSelected ? "border-primary border-2 ring-2 ring-primary/20" : "border-border hover:border-primary/50"}`,
              onClick: () => onUpdateShapeStyle({
                fillMode: "pattern",
                fillPatternPreset: opt.value
              }),
              children: svgPattern && /* @__PURE__ */ jsx(
                "div",
                {
                  className: "w-full h-full rounded-sm",
                  style: {
                    backgroundImage: `url('data:image/svg+xml;utf8,${encodeURIComponent(svgPattern)}')`,
                    backgroundRepeat: "repeat",
                    backgroundSize: "8px 8px"
                  }
                }
              )
            },
            opt.value
          );
        }) })
      ] }),
      /* @__PURE__ */ jsx(
        ColorPickerRow,
        {
          label: t("pptx.fillAdvanced.foregroundColor"),
          prefix: "pattern-fg",
          value: normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR),
          disabled: isLine,
          onChange: (color) => onUpdateShapeStyle({ fillMode: "pattern", fillColor: color })
        }
      ),
      /* @__PURE__ */ jsx(
        ColorPickerRow,
        {
          label: t("pptx.fillAdvanced.backgroundColor"),
          prefix: "pattern-bg",
          value: normalizeHexColor(style?.fillPatternBackgroundColor, "#ffffff"),
          disabled: isLine,
          onChange: (color) => onUpdateShapeStyle({
            fillMode: "pattern",
            fillPatternBackgroundColor: color
          })
        }
      )
    ] }),
    style?.fillMode === "image" && /* @__PURE__ */ jsxs("div", { className: `${COL2} space-y-2`, children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.fillAdvanced.imageUrl") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            placeholder: "https://example.com/image.png",
            value: style?.fillImageUrl || "",
            onChange: (e) => onUpdateShapeStyle({ fillImageUrl: e.target.value }),
            className: `${SEL2} text-xs`
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.fillAdvanced.orChooseFile") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "file",
            accept: "image/*",
            onChange: (e) => {
              const file = e.target.files?.[0];
              if (!file) {
                return;
              }
              const reader = new FileReader();
              reader.onload = () => onUpdateShapeStyle({ fillImageUrl: reader.result });
              reader.readAsDataURL(file);
            },
            className: "text-xs text-foreground file:bg-accent file:border-0 file:rounded file:px-2 file:py-1 file:text-xs file:text-foreground file:mr-2"
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        SelectRow,
        {
          label: t("pptx.fillAdvanced.imageMode"),
          value: style?.fillImageMode || "stretch",
          options: IMAGE_MODE_OPTIONS,
          onChange: (v) => onUpdateShapeStyle({ fillImageMode: v })
        }
      ),
      style?.fillImageUrl && /* @__PURE__ */ jsx("div", { className: "mt-1 rounded border border-border overflow-hidden h-16", children: /* @__PURE__ */ jsx(
        "img",
        {
          src: style.fillImageUrl,
          alt: t("pptx.fillAdvanced.fillPreviewAlt"),
          className: "w-full h-full object-cover"
        }
      ) })
    ] })
  ] });
}
function QuickStylesGallery({
  onUpdateShapeStyle
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.shape.quickStyles") }),
    /* @__PURE__ */ jsx("div", { className: "grid grid-cols-6 gap-1", children: SHAPE_QUICK_STYLES.map((qs, idx) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        title: qs.name,
        "aria-label": qs.name,
        className: "h-7 w-full rounded border border-border hover:border-primary transition-colors",
        style: quickStyleSwatchCss(qs),
        onClick: () => onUpdateShapeStyle(qs.style)
      },
      idx
    )) })
  ] });
}
function StrokeEffectsSection({
  style,
  isLine,
  onUpdateShapeStyle,
  onSetStrokeColor
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      ColorPickerRow,
      {
        label: t("pptx.inspector.stroke"),
        prefix: "stroke",
        value: normalizeHexColor(style?.strokeColor, DEFAULT_STROKE_COLOR),
        selectedRef: style?.strokeColorRef,
        onChange: onSetStrokeColor
      }
    ),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.strokeEffects.strokeWidth") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "number",
          min: 0,
          value: Math.round(style?.strokeWidth || 0),
          onChange: (e) => {
            const v = safeNum(e.target.value, 0);
            onUpdateShapeStyle({ strokeWidth: Math.max(0, v) });
          },
          className: NUM2
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.strokeEffects.fillOpacity") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          disabled: isLine,
          value: Math.round((style?.fillOpacity ?? 1) * 100),
          onChange: (e) => onUpdateShapeStyle({ fillOpacity: Number(e.target.value) / 100 }),
          className: `${RNG} ${DIS}`
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.strokeEffects.strokeOpacity") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          value: Math.round((style?.strokeOpacity ?? 1) * 100),
          onChange: (e) => onUpdateShapeStyle({
            strokeOpacity: Number(e.target.value) / 100
          }),
          className: RNG
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: `flex flex-col gap-1 ${COL2}`, children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.strokeEffects.strokeDash") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.strokeEffects.strokeDash"),
          value: style?.strokeDash || "solid",
          onChange: (e) => onUpdateShapeStyle({
            strokeDash: e.target.value
          }),
          className: "bg-muted border border-border rounded px-2 py-1",
          children: STROKE_DASH_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.i18nKey) }, o.value))
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: `flex flex-col gap-1 ${COL2}`, children: [
      /* @__PURE__ */ jsx("span", { className: LBL2, children: t("pptx.strokeEffects.compoundLine") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-5 gap-1", children: COMPOUND_LINE_OPTIONS2.map((opt) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          title: t(opt.i18nKey),
          "aria-label": t(opt.i18nKey),
          className: `h-10 px-2 rounded border transition-all ${(style?.compoundLine || "sng") === opt.value ? "border-primary bg-accent" : "border-border hover:border-primary/50"}`,
          onClick: () => onUpdateShapeStyle({
            compoundLine: opt.value
          }),
          children: /* @__PURE__ */ jsx(
            "div",
            {
              className: "w-full h-full flex items-center justify-center",
              style: getCompoundLinePreviewStyle(opt.value)
            }
          )
        },
        opt.value
      )) })
    ] }),
    EFFECT_CONFIGS.map((cfg) => {
      const on = cfg.isOn(style);
      return /* @__PURE__ */ jsxs(React20.Fragment, { children: [
        /* @__PURE__ */ jsxs("label", { className: `inline-flex items-center gap-2 text-foreground ${COL2}`, children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "checkbox",
              checked: on,
              onChange: (e) => {
                onUpdateShapeStyle(e.target.checked ? cfg.onEnable(style) : cfg.onDisable(style));
              }
            }
          ),
          cfg.i18nKey ? t(cfg.i18nKey) : cfg.label
        ] }),
        on && cfg.fields.map((f) => /* @__PURE__ */ jsx(EffectField, { field: f, style, onUpdate: onUpdateShapeStyle }, f.key))
      ] }, cfg.label);
    }),
    /* @__PURE__ */ jsx(
      SelectRow,
      {
        label: t("pptx.strokeEffects.lineJoin"),
        value: style?.lineJoin || "round",
        options: LINE_JOIN_OPTIONS2,
        onChange: (v) => onUpdateShapeStyle({ lineJoin: v })
      }
    ),
    /* @__PURE__ */ jsx(
      SelectRow,
      {
        label: t("pptx.strokeEffects.lineCap"),
        value: style?.lineCap || "flat",
        options: LINE_CAP_OPTIONS2,
        onChange: (v) => onUpdateShapeStyle({ lineCap: v })
      }
    )
  ] });
}
function FillStrokeProperties({
  selectedElement,
  selectedShapeStyle,
  selectedShapeType,
  selectedGradientStops,
  onUpdateShapeStyle,
  onSetFillColor,
  onSetStrokeColor
}) {
  const { t } = useTranslation();
  const line = isLineish(selectedElement, selectedShapeType);
  const style = selectedShapeStyle;
  const handleFillModeChange = (nextMode) => {
    if (nextMode === "none") {
      onUpdateShapeStyle({ fillMode: "none", fillColor: "transparent" });
      return;
    }
    if (nextMode === "image") {
      onUpdateShapeStyle({
        fillMode: "image",
        fillImageMode: style?.fillImageMode || "stretch"
      });
      return;
    }
    if (nextMode === "gradient") {
      const existing = sanitizeGradientStops(style?.fillGradientStops);
      const stops = existing.length >= 2 ? existing : [
        {
          color: normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR),
          position: 0,
          opacity: 1
        },
        { color: "#ffffff", position: 100, opacity: 1 }
      ];
      onUpdateShapeStyle({
        fillMode: "gradient",
        fillGradientType: style?.fillGradientType || "linear",
        fillGradientAngle: style?.fillGradientAngle ?? 90,
        fillGradientStops: stops
      });
      return;
    }
    if (nextMode === "pattern") {
      onUpdateShapeStyle({
        fillMode: "pattern",
        fillPatternPreset: style?.fillPatternPreset || "pct20",
        fillPatternBackgroundColor: style?.fillPatternBackgroundColor || "#ffffff"
      });
      return;
    }
    onUpdateShapeStyle({ fillMode: "solid" });
  };
  const gradientStops = selectedGradientStops.length > 0 ? selectedGradientStops : [
    { color: DEFAULT_FILL_COLOR, position: 0 },
    { color: "#ffffff", position: 100 }
  ];
  const showGradient = style?.fillMode === "gradient" || selectedGradientStops.length > 0;
  const gradType = style?.fillGradientType || "linear";
  const updateGradientStops = (stops) => {
    onUpdateShapeStyle({ fillMode: "gradient", fillGradientStops: stops });
  };
  const showQuickStyles = selectedElement.type === "shape" || selectedElement.type === "text";
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    showQuickStyles && /* @__PURE__ */ jsx(QuickStylesGallery, { onUpdateShapeStyle }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
      /* @__PURE__ */ jsx(
        SelectRow,
        {
          label: t("pptx.table.fillMode"),
          span2: true,
          value: style?.fillMode || (style?.fillColor === "transparent" ? "none" : "solid"),
          options: FILL_MODE_OPTIONS2,
          onChange: handleFillModeChange
        }
      ),
      /* @__PURE__ */ jsx(
        ColorPickerRow,
        {
          label: t("pptx.fill.fill"),
          prefix: "fill",
          value: normalizeHexColor(style?.fillColor, DEFAULT_FILL_COLOR),
          selectedRef: style?.fillColorRef,
          disabled: line,
          onChange: onSetFillColor
        }
      ),
      /* @__PURE__ */ jsx(
        FillAdvancedControls,
        {
          style,
          gradientStops,
          showGradient,
          gradType,
          isLine: line,
          onUpdateShapeStyle,
          onUpdateGradientStops: updateGradientStops,
          onSetFillColor
        }
      ),
      /* @__PURE__ */ jsx(
        StrokeEffectsSection,
        {
          style,
          isLine: line,
          onUpdateShapeStyle,
          onSetStrokeColor
        }
      )
    ] })
  ] });
}
var INPUT_CLS2 = "bg-muted border border-border rounded px-2 py-1";
var COLOR_CLS = "h-8 bg-muted border border-border rounded px-1";
function Text3DProperties({
  ts,
  onUpdateTextStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const t3d = ts?.text3d;
  const hasExtrusion = Boolean(t3d?.extrusionHeight && t3d.extrusionHeight > 0);
  const update3d = (partial) => {
    const merged = { ...ts?.text3d, ...partial };
    onUpdateTextStyle({ text3d: merged });
  };
  const toggleExtrusion = (checked) => {
    onUpdateTextStyle({ text3d: toggleText3dExtrusion(ts?.text3d, checked) });
  };
  return /* @__PURE__ */ jsxs("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", "data-pptx-text-3d": true, children: [
    /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.text3d.title") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            checked: hasExtrusion,
            onChange: (e) => toggleExtrusion(e.target.checked)
          }
        ),
        t("pptx.text3d.extrusion")
      ] }),
      hasExtrusion && /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text3d.extrusionDepth") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              max: 100,
              step: 1,
              value: text3dEmuToPt(t3d?.extrusionHeight),
              onChange: (e) => {
                const v = Number(e.target.value);
                if (Number.isFinite(v)) {
                  update3d({
                    extrusionHeight: text3dPtToEmu(Math.max(0, Math.min(100, v)))
                  });
                }
              },
              className: INPUT_CLS2
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text3d.extrusionColor") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              value: normalizeHexColor(t3d?.extrusionColor, "#888888"),
              onChange: (e) => {
                update3d({ extrusionColor: e.target.value });
                pushColor(e.target.value);
              },
              className: COLOR_CLS
            }
          )
        ] })
      ] })
    ] }),
    hasExtrusion && /* @__PURE__ */ jsx(
      BevelSection,
      {
        label: t("pptx.text3d.bevelTop"),
        bevelType: t3d?.bevelTopType,
        bevelWidth: t3d?.bevelTopWidth,
        bevelHeight: t3d?.bevelTopHeight,
        onTypeChange: (v) => update3d({
          bevelTopType: v || void 0
        }),
        onWidthChange: (v) => update3d({ bevelTopWidth: text3dPtToEmu(v) }),
        onHeightChange: (v) => update3d({ bevelTopHeight: text3dPtToEmu(v) }),
        t
      }
    ),
    hasExtrusion && /* @__PURE__ */ jsx(
      BevelSection,
      {
        label: t("pptx.text3d.bevelBottom"),
        bevelType: t3d?.bevelBottomType,
        bevelWidth: t3d?.bevelBottomWidth,
        bevelHeight: t3d?.bevelBottomHeight,
        onTypeChange: (v) => update3d({
          bevelBottomType: v || void 0
        }),
        onWidthChange: (v) => update3d({ bevelBottomWidth: text3dPtToEmu(v) }),
        onHeightChange: (v) => update3d({ bevelBottomHeight: text3dPtToEmu(v) }),
        t
      }
    ),
    hasExtrusion && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 pl-4", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text3d.material") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.text3d.material"),
          value: t3d?.presetMaterial ?? "",
          onChange: (e) => {
            const v = e.target.value;
            update3d({
              presetMaterial: v ? v : void 0
            });
          },
          className: INPUT_CLS2,
          children: MATERIAL_PRESETS.map(({ value, label }) => /* @__PURE__ */ jsx("option", { value, children: label }, value))
        }
      )
    ] })
  ] });
}
function BevelSection({
  label,
  bevelType,
  bevelWidth,
  bevelHeight,
  onTypeChange,
  onWidthChange,
  onHeightChange,
  t
}) {
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1 pl-4", children: [
    /* @__PURE__ */ jsx("span", { className: "text-[11px] text-muted-foreground", children: label }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-3 gap-2", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text3d.bevelType") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.text3d.bevelType"),
            value: bevelType ?? "none",
            onChange: (e) => onTypeChange(e.target.value),
            className: INPUT_CLS2,
            children: BEVEL_PRESETS.map(({ value, label: l }) => /* @__PURE__ */ jsx("option", { value, children: l }, value))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text3d.bevelWidth") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 50,
            step: 1,
            value: text3dEmuToPt(bevelWidth),
            onChange: (e) => {
              const v = Number(e.target.value);
              if (Number.isFinite(v)) {
                onWidthChange(Math.max(0, Math.min(50, v)));
              }
            },
            className: INPUT_CLS2
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text3d.bevelHeight") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 50,
            step: 1,
            value: text3dEmuToPt(bevelHeight),
            onChange: (e) => {
              const v = Number(e.target.value);
              if (Number.isFinite(v)) {
                onHeightChange(Math.max(0, Math.min(50, v)));
              }
            },
            className: INPUT_CLS2
          }
        )
      ] })
    ] })
  ] });
}
var INPUT_CLS3 = "bg-muted border border-border rounded px-2 py-1";
var COLOR_CLS2 = "h-8 bg-muted border border-border rounded px-1";
var UNDERLINE_STYLES = [
  ["sng", "Single"],
  ["dbl", "Double"],
  ["heavy", "Heavy"],
  ["dotted", "Dotted"],
  ["dottedHeavy", "Dotted Heavy"],
  ["dash", "Dash"],
  ["dashHeavy", "Dash Heavy"],
  ["dashLong", "Long Dash"],
  ["dashLongHeavy", "Long Dash Heavy"],
  ["dotDash", "Dot Dash"],
  ["dotDashHeavy", "Dot Dash Heavy"],
  ["dotDotDash", "Dot Dot Dash"],
  ["dotDotDashHeavy", "Dot Dot Dash Heavy"],
  ["wavy", "Wavy"],
  ["wavyHeavy", "Wavy Heavy"],
  ["wavyDbl", "Wavy Double"],
  ["none", "None"]
];
var BASELINE_TOGGLES = [
  ["Superscript", 3e4],
  ["Subscript", -25e3]
];
function createNumericChangeHandler(onUpdateTextStyle) {
  return (fn) => (e) => {
    const v = Number(e.target.value);
    if (Number.isFinite(v)) {
      onUpdateTextStyle(fn(v));
    }
  };
}
function AdvancedTextFormatting({
  ts,
  canEdit,
  onUpdateTextStyle,
  numChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  return /* @__PURE__ */ jsxs("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", children: [
    /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.textProperties.advancedTextHeading") }),
    /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-2", children: BASELINE_TOGGLES.map(([label, baseline]) => /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: ts?.baseline === baseline,
          onChange: () => onUpdateTextStyle({
            baseline: ts?.baseline === baseline ? 0 : baseline
          })
        }
      ),
      label
    ] }, label)) }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Highlight" }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          value: normalizeHexColor(ts?.highlightColor, "#ffff00"),
          onChange: (e) => {
            onUpdateTextStyle({ highlightColor: e.target.value });
            pushColor(e.target.value);
          },
          disabled: !canEdit,
          className: COLOR_CLS2
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textProperties.underlineStyle") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.textProperties.underlineStyle"),
            value: ts?.underlineStyle || "sng",
            disabled: !canEdit,
            onChange: (e) => onUpdateTextStyle({
              underline: true,
              underlineStyle: e.target.value
            }),
            className: INPUT_CLS3,
            children: UNDERLINE_STYLES.map(([v, l]) => /* @__PURE__ */ jsx("option", { value: v, children: l }, v))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textProperties.underlineColour") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            value: ts?.underlineColor || ts?.color || "#000000",
            onChange: (e) => {
              onUpdateTextStyle({ underlineColor: e.target.value });
              pushColor(e.target.value);
            },
            className: COLOR_CLS2
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textProperties.outlineWidth") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 10,
            step: 0.5,
            value: ts?.textOutlineWidth ?? 0,
            onChange: (e) => onUpdateTextStyle({
              textOutlineWidth: parseFloat(e.target.value) || 0
            }),
            className: INPUT_CLS3
          }
        )
      ] }),
      (ts?.textOutlineWidth ?? 0) > 0 && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textProperties.outlineColour") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            value: ts?.textOutlineColor || "#000000",
            onChange: (e) => {
              onUpdateTextStyle({ textOutlineColor: e.target.value });
              pushColor(e.target.value);
            },
            className: COLOR_CLS2
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.text.characterSpacing") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "number",
          min: -1e3,
          max: 5e3,
          step: 50,
          value: Math.round(ts?.characterSpacing ?? 0),
          disabled: !canEdit,
          onChange: numChange((v) => ({ characterSpacing: v })),
          className: INPUT_CLS3
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textProperties.leftMargin") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 500,
            step: 4,
            value: Math.round(ts?.paragraphMarginLeft ?? 0),
            disabled: !canEdit,
            onChange: numChange((v) => ({
              paragraphMarginLeft: Math.max(0, Math.min(500, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textProperties.firstIndent") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: -500,
            max: 500,
            step: 4,
            value: Math.round(ts?.paragraphIndent ?? 0),
            disabled: !canEdit,
            onChange: numChange((v) => ({
              paragraphIndent: Math.max(-500, Math.min(500, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: Boolean(ts?.autoFit),
          disabled: !canEdit,
          onChange: (e) => onUpdateTextStyle({ autoFit: e.target.checked })
        }
      ),
      "Auto-fit text to shape"
    ] })
  ] });
}
function TextReflectionSection({
  ts,
  onUpdateTextStyle,
  numChange
}) {
  const { t } = useTranslation();
  const hasReflection = Boolean(ts?.textReflection);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
    /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: hasReflection,
          onChange: (e) => {
            if (e.target.checked) {
              onUpdateTextStyle({
                textReflection: true,
                textReflectionBlur: 1,
                textReflectionStartOpacity: 0.5,
                textReflectionEndOpacity: 0,
                textReflectionOffset: 3
              });
            } else {
              onUpdateTextStyle({
                textReflection: void 0,
                textReflectionBlur: void 0,
                textReflectionStartOpacity: void 0,
                textReflectionEndOpacity: void 0,
                textReflectionOffset: void 0
              });
            }
          }
        }
      ),
      t("pptx.textEffects.reflection")
    ] }),
    hasReflection && /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Blur" }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 20,
            step: 0.5,
            value: Number(ts?.textReflectionBlur ?? 1).toFixed(1),
            onChange: numChange((v) => ({
              textReflectionBlur: Math.max(0, Math.min(20, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Offset" }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 20,
            step: 1,
            value: Math.round(ts?.textReflectionOffset ?? 3),
            onChange: numChange((v) => ({
              textReflectionOffset: Math.max(0, Math.min(20, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textEffects.startOpacity") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 1,
            step: 0.05,
            value: Number(ts?.textReflectionStartOpacity ?? 0.5).toFixed(2),
            onChange: numChange((v) => ({
              textReflectionStartOpacity: Math.max(0, Math.min(1, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textEffects.endOpacity") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 0,
            max: 1,
            step: 0.05,
            value: Number(ts?.textReflectionEndOpacity ?? 0).toFixed(2),
            onChange: numChange((v) => ({
              textReflectionEndOpacity: Math.max(0, Math.min(1, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] })
    ] })
  ] });
}
function TextEffectsPanel({
  ts,
  onUpdateTextStyle,
  numChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const hasShadow = Boolean(
    ts?.textShadowColor || typeof ts?.textShadowBlur === "number" && ts.textShadowBlur > 0
  );
  const hasGlow = Boolean(
    ts?.textGlowColor || typeof ts?.textGlowRadius === "number" && ts.textGlowRadius > 0
  );
  return /* @__PURE__ */ jsxs("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", children: [
    /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.textEffects.title") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            checked: hasShadow,
            onChange: (e) => {
              if (e.target.checked) {
                onUpdateTextStyle({
                  textShadowColor: "#000000",
                  textShadowBlur: 4,
                  textShadowOffsetX: 2,
                  textShadowOffsetY: 2,
                  textShadowOpacity: 0.5
                });
              } else {
                onUpdateTextStyle({
                  textShadowColor: void 0,
                  textShadowBlur: void 0,
                  textShadowOffsetX: void 0,
                  textShadowOffsetY: void 0,
                  textShadowOpacity: void 0
                });
              }
            }
          }
        ),
        t("pptx.textEffects.shadow")
      ] }),
      hasShadow && /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Color" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              value: normalizeHexColor(ts?.textShadowColor, "#000000"),
              onChange: (e) => {
                onUpdateTextStyle({ textShadowColor: e.target.value });
                pushColor(e.target.value);
              },
              className: COLOR_CLS2
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Opacity" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              max: 1,
              step: 0.05,
              value: Number(ts?.textShadowOpacity ?? 0.5).toFixed(2),
              onChange: numChange((v) => ({
                textShadowOpacity: Math.max(0, Math.min(1, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Blur" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              max: 50,
              step: 1,
              value: Math.round(ts?.textShadowBlur ?? 4),
              onChange: numChange((v) => ({
                textShadowBlur: Math.max(0, Math.min(50, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textEffects.offsetX") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: -50,
              max: 50,
              step: 1,
              value: Math.round(ts?.textShadowOffsetX ?? 2),
              onChange: numChange((v) => ({ textShadowOffsetX: v })),
              className: INPUT_CLS3
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.textEffects.offsetY") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: -50,
              max: 50,
              step: 1,
              value: Math.round(ts?.textShadowOffsetY ?? 2),
              onChange: numChange((v) => ({ textShadowOffsetY: v })),
              className: INPUT_CLS3
            }
          )
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            checked: hasGlow,
            onChange: (e) => {
              if (e.target.checked) {
                onUpdateTextStyle({
                  textGlowColor: "#ffff00",
                  textGlowRadius: 6,
                  textGlowOpacity: 0.6
                });
              } else {
                onUpdateTextStyle({
                  textGlowColor: void 0,
                  textGlowRadius: void 0,
                  textGlowOpacity: void 0
                });
              }
            }
          }
        ),
        t("pptx.textEffects.glow")
      ] }),
      hasGlow && /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Color" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              value: normalizeHexColor(ts?.textGlowColor, "#ffff00"),
              onChange: (e) => {
                onUpdateTextStyle({ textGlowColor: e.target.value });
                pushColor(e.target.value);
              },
              className: COLOR_CLS2
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Opacity" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              max: 1,
              step: 0.05,
              value: Number(ts?.textGlowOpacity ?? 0.6).toFixed(2),
              onChange: numChange((v) => ({
                textGlowOpacity: Math.max(0, Math.min(1, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Size" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 1,
              max: 50,
              step: 1,
              value: Math.round(ts?.textGlowRadius ?? 6),
              onChange: numChange((v) => ({
                textGlowRadius: Math.max(1, Math.min(50, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx(TextReflectionSection, { ts, onUpdateTextStyle, numChange })
  ] });
}
function TextWarpGallery({
  ts,
  onUpdateTextStyle
}) {
  const { t } = useTranslation();
  const [expanded, setExpanded] = React20.useState(false);
  const currentPreset = ts?.textWarpPreset || "textNoShape";
  return /* @__PURE__ */ jsxs("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "flex w-full items-center justify-between text-[11px] uppercase tracking-wide text-muted-foreground",
        onClick: () => setExpanded(!expanded),
        children: [
          /* @__PURE__ */ jsx("span", { children: t("pptx.textWarp.title") }),
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: expanded ? "\u2212" : "+" })
        ]
      }
    ),
    !expanded && /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: TEXT_WARP_PRESETS.find((p) => p.value === currentPreset)?.label || currentPreset }),
    expanded && /* @__PURE__ */ jsx("div", { className: "grid grid-cols-5 gap-1", children: TEXT_WARP_PRESETS.map(({ value, label }) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        title: label,
        "aria-label": label,
        className: `flex items-center justify-center rounded p-1 ${currentPreset === value ? "bg-primary ring-1 ring-primary" : "bg-muted hover:bg-accent"}`,
        onClick: () => onUpdateTextStyle({
          textWarpPreset: value === "textNoShape" ? void 0 : value
        }),
        children: /* @__PURE__ */ jsx("svg", { width: 40, height: 20, viewBox: "0 0 40 20", children: /* @__PURE__ */ jsx(
          "path",
          {
            d: warpPreviewPath(value),
            stroke: "currentColor",
            strokeWidth: 1.5,
            fill: "none",
            className: currentPreset === value ? "text-white" : "text-muted-foreground"
          }
        ) })
      },
      value
    )) })
  ] });
}
function TextAdvancedSections({
  selectedElement,
  canEdit,
  onUpdateTextStyle
}) {
  if (!hasTextProperties(selectedElement)) {
    return null;
  }
  const ts = selectedElement.textStyle;
  const numChange = createNumericChangeHandler(onUpdateTextStyle);
  return /* @__PURE__ */ jsxs("div", { "data-pptx-text-advanced": true, children: [
    /* @__PURE__ */ jsx(
      AdvancedTextFormatting,
      {
        ts,
        canEdit,
        onUpdateTextStyle,
        numChange
      }
    ),
    /* @__PURE__ */ jsx(TextWarpGallery, { ts, onUpdateTextStyle }),
    /* @__PURE__ */ jsx(TextEffectsPanel, { ts, onUpdateTextStyle, numChange }),
    /* @__PURE__ */ jsx(Text3DProperties, { ts, onUpdateTextStyle })
  ] });
}
function ShapeTextPanels({
  selectedElement,
  canEdit,
  onUpdateElement,
  onUpdateElementStyle,
  onUpdateTextStyle
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    hasShapeProperties(selectedElement) && /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.shape.type", "Shape Type") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          value: selectedElement.shapeType || "rect",
          disabled: !canEdit,
          className: cn(INPUT, "w-full"),
          onChange: (e) => onUpdateElement({
            shapeType: e.target.value
          }),
          children: SHAPE_PRESETS.filter((p) => p.type !== "connector").map((p) => /* @__PURE__ */ jsx("option", { value: p.type, children: t(p.i18nKey) }, p.type))
        }
      )
    ] }),
    hasShapeProperties(selectedElement) && /* @__PURE__ */ jsxs("div", { className: CARD, "data-pptx-fill-stroke": true, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.shape.fillStroke", "Fill & Stroke") }),
      /* @__PURE__ */ jsx(
        FillStrokeProperties,
        {
          selectedElement,
          selectedShapeStyle: selectedElement.shapeStyle,
          selectedShapeType: selectedElement.shapeType,
          selectedGradientStops: sanitizeGradientStops(
            selectedElement.shapeStyle?.fillGradientStops
          ),
          canEdit,
          onUpdateShapeStyle: onUpdateElementStyle,
          onSetFillColor: (hex, ref) => onUpdateElementStyle({ fillColor: hex, fillMode: "solid", fillColorRef: ref }),
          onSetStrokeColor: (hex, ref) => onUpdateElementStyle({ strokeColor: hex, strokeColorRef: ref })
        }
      )
    ] }),
    hasTextProperties(selectedElement) && /* @__PURE__ */ jsxs("div", { className: CARD, "data-pptx-text-card": true, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.text.title", "Text") }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Size" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              disabled: !canEdit,
              className: INPUT,
              min: 6,
              max: 200,
              step: "any",
              value: selectedElement.textStyle?.fontSize !== void 0 ? textFontSizePxToPt(selectedElement.textStyle.fontSize) : 18,
              onChange: (e) => onUpdateTextStyle({ fontSize: textFontSizePtToPx(Number(e.target.value)) })
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "Color" }),
          /* @__PURE__ */ jsx(
            DebouncedColorInput,
            {
              disabled: !canEdit,
              ariaLabel: "Text Color",
              value: normalizeHexColor(selectedElement.textStyle?.color, "#000000"),
              className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
              onCommit: (hex) => onUpdateTextStyle({ color: hex, colorRef: void 0 })
            }
          )
        ] }),
        /* @__PURE__ */ jsx("div", { className: "col-span-2", children: /* @__PURE__ */ jsx(
          ThemeColorSwatchGrid,
          {
            prefix: "text-color",
            disabled: !canEdit,
            selectedRef: selectedElement.textStyle?.colorRef,
            selectedHex: selectedElement.textStyle?.color,
            onPick: (c) => onUpdateTextStyle({ color: c.hex, colorRef: c.ref })
          }
        ) }),
        /* @__PURE__ */ jsx("div", { className: "col-span-2", children: /* @__PURE__ */ jsx(
          RecentColorsRow,
          {
            prefix: "text-color",
            disabled: !canEdit,
            onCommit: (hex) => onUpdateTextStyle({ color: hex, colorRef: void 0 })
          }
        ) }),
        /* @__PURE__ */ jsxs("div", { className: "flex gap-1 col-span-2", children: [
          /* @__PURE__ */ jsx(
            TextFormatToggle,
            {
              label: "B",
              active: Boolean(selectedElement.textStyle?.bold),
              disabled: !canEdit,
              onClick: () => onUpdateTextStyle({ bold: !selectedElement.textStyle?.bold })
            }
          ),
          /* @__PURE__ */ jsx(
            TextFormatToggle,
            {
              label: "I",
              active: Boolean(selectedElement.textStyle?.italic),
              disabled: !canEdit,
              italic: true,
              onClick: () => onUpdateTextStyle({
                italic: !selectedElement.textStyle?.italic
              })
            }
          ),
          /* @__PURE__ */ jsx(
            TextFormatToggle,
            {
              label: "U",
              active: Boolean(selectedElement.textStyle?.underline),
              disabled: !canEdit,
              underline: true,
              onClick: () => onUpdateTextStyle({
                underline: !selectedElement.textStyle?.underline
              })
            }
          )
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx(
      TextAdvancedSections,
      {
        selectedElement,
        canEdit,
        onUpdateTextStyle
      }
    ),
    shouldShowAccessibilitySection(selectedElement) && /* @__PURE__ */ jsx(
      AccessibilityTextSection,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    )
  ] });
}
function TextFormatToggle({
  label,
  active,
  disabled,
  italic,
  underline,
  onClick
}) {
  return /* @__PURE__ */ jsx(
    "button",
    {
      type: "button",
      disabled,
      className: cn(
        "px-2 py-1 rounded text-[11px] transition-colors",
        italic && "italic",
        underline && "underline",
        active ? "bg-primary text-white" : "bg-muted hover:bg-accent"
      ),
      onClick,
      children: label
    }
  );
}
function ProcessThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("rect", { x: "2", y: "10", width: "10", height: "12", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("path", { d: "M14 16 L17 16", stroke: "currentColor", strokeWidth: "1.5", opacity: 0.5 }),
    /* @__PURE__ */ jsx("rect", { x: "19", y: "10", width: "10", height: "12", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("path", { d: "M31 16 L34 16", stroke: "currentColor", strokeWidth: "1.5", opacity: 0.5 }),
    /* @__PURE__ */ jsx("rect", { x: "36", y: "10", width: "10", height: "12", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function HierarchyThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("rect", { x: "17", y: "2", width: "14", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("path", { d: "M24 10 L12 18 M24 10 L36 18", stroke: "currentColor", strokeWidth: "1", opacity: 0.5 }),
    /* @__PURE__ */ jsx("rect", { x: "4", y: "18", width: "14", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "30", y: "18", width: "14", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function CycleThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("circle", { cx: "24", cy: "5", r: "4", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("circle", { cx: "37", cy: "20", r: "4", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("circle", { cx: "11", cy: "20", r: "4", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx(
      "path",
      {
        d: "M28 6 L34 17 M33 23 L14 23 M14 18 L20 7",
        stroke: "currentColor",
        strokeWidth: "1",
        opacity: 0.4,
        fill: "none"
      }
    )
  ] });
}
function MatrixThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("rect", { x: "4", y: "3", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "26", y: "3", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "4", y: "18", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "26", y: "18", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function PyramidThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("rect", { x: "16", y: "2", width: "16", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "10", y: "12", width: "28", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "4", y: "22", width: "40", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function ListThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("rect", { x: "4", y: "3", width: "40", height: "7", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "4", y: "13", width: "40", height: "7", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "4", y: "23", width: "40", height: "7", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function FunnelThumb() {
  return /* @__PURE__ */ jsx("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: /* @__PURE__ */ jsx("path", { d: "M4 3 L44 3 L28 16 L28 29 L20 29 L20 16 Z", fill: "currentColor", opacity: 0.7 }) });
}
function TargetThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("circle", { cx: "24", cy: "16", r: "14", fill: "currentColor", opacity: 0.3 }),
    /* @__PURE__ */ jsx("circle", { cx: "24", cy: "16", r: "9", fill: "currentColor", opacity: 0.5 }),
    /* @__PURE__ */ jsx("circle", { cx: "24", cy: "16", r: "4", fill: "currentColor", opacity: 0.8 })
  ] });
}
function GearThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("circle", { cx: "16", cy: "16", r: "9", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("circle", { cx: "16", cy: "16", r: "3", fill: "white", opacity: 0.9 }),
    /* @__PURE__ */ jsx("circle", { cx: "33", cy: "16", r: "6", fill: "currentColor", opacity: 0.5 }),
    /* @__PURE__ */ jsx("circle", { cx: "33", cy: "16", r: "2", fill: "white", opacity: 0.9 })
  ] });
}
function VennThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("circle", { cx: "19", cy: "16", r: "11", fill: "currentColor", opacity: 0.45 }),
    /* @__PURE__ */ jsx("circle", { cx: "29", cy: "16", r: "11", fill: "currentColor", opacity: 0.45 })
  ] });
}
function TimelineThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("path", { d: "M4 16 L44 16", stroke: "currentColor", strokeWidth: "1.5", opacity: 0.5 }),
    /* @__PURE__ */ jsx("circle", { cx: "10", cy: "16", r: "3", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("circle", { cx: "24", cy: "16", r: "3", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("circle", { cx: "38", cy: "16", r: "3", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("rect", { x: "6", y: "4", width: "8", height: "6", rx: "1", fill: "currentColor", opacity: 0.5 }),
    /* @__PURE__ */ jsx("rect", { x: "34", y: "4", width: "8", height: "6", rx: "1", fill: "currentColor", opacity: 0.5 })
  ] });
}
function RelationshipThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("circle", { cx: "14", cy: "16", r: "10", fill: "currentColor", opacity: 0.5 }),
    /* @__PURE__ */ jsx("circle", { cx: "34", cy: "16", r: "10", fill: "currentColor", opacity: 0.5 }),
    /* @__PURE__ */ jsx("path", { d: "M14 6 L34 26 M34 6 L14 26", stroke: "currentColor", strokeWidth: "1", opacity: 0.3 })
  ] });
}
function ChevronThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("path", { d: "M2 8 L14 8 L20 16 L14 24 L2 24 L8 16 Z", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("path", { d: "M17 8 L29 8 L35 16 L29 24 L17 24 L23 16 Z", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx("path", { d: "M32 8 L44 8 L44 24 L32 24 L38 16 Z", fill: "currentColor", opacity: 0.7 })
  ] });
}
function BendingThumb() {
  return /* @__PURE__ */ jsxs("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    /* @__PURE__ */ jsx("rect", { x: "2", y: "3", width: "11", height: "9", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx(
      "path",
      {
        d: "M13 7 L20 7 L20 16",
        stroke: "currentColor",
        strokeWidth: "1.5",
        fill: "none",
        opacity: 0.5
      }
    ),
    /* @__PURE__ */ jsx("rect", { x: "19", y: "12", width: "11", height: "9", rx: "2", fill: "currentColor", opacity: 0.7 }),
    /* @__PURE__ */ jsx(
      "path",
      {
        d: "M30 16 L37 16 L37 25",
        stroke: "currentColor",
        strokeWidth: "1.5",
        fill: "none",
        opacity: 0.5
      }
    ),
    /* @__PURE__ */ jsx("rect", { x: "35", y: "21", width: "11", height: "9", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
var THUMB_COMPONENTS = {
  process: ProcessThumb,
  hierarchy: HierarchyThumb,
  cycle: CycleThumb,
  matrix: MatrixThumb,
  pyramid: PyramidThumb,
  list: ListThumb,
  funnel: FunnelThumb,
  target: TargetThumb,
  gear: GearThumb,
  venn: VennThumb,
  timeline: TimelineThumb,
  relationship: RelationshipThumb,
  chevron: ChevronThumb,
  bending: BendingThumb
};
var LAYOUT_DESCRIPTORS = SWITCHABLE_LAYOUT_TYPES.map((type) => ({
  type,
  i18nKey: `pptx.smartart.category.${type}`
}));
function SmartArtLayoutSwitcher({
  smartArtData,
  canEdit,
  onUpdateSmartArt
}) {
  const { t } = useTranslation();
  const currentLayout = smartArtData.resolvedLayoutType ?? "list";
  const handleLayoutSwitch = (newLayout) => {
    if (!canEdit || newLayout === currentLayout) {
      return;
    }
    const updated = switchSmartArtLayout(smartArtData, newLayout);
    onUpdateSmartArt({
      layoutType: updated.layoutType,
      resolvedLayoutType: updated.resolvedLayoutType,
      layout: updated.layout,
      layoutDirty: updated.layoutDirty,
      drawingDirty: updated.drawingDirty,
      drawingShapes: updated.drawingShapes
    });
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
    /* @__PURE__ */ jsx("span", { className: "text-[11px] text-muted-foreground", children: t("pptx.smartart.switchLayout") }),
    /* @__PURE__ */ jsx("div", { className: "grid grid-cols-3 gap-1.5", children: LAYOUT_DESCRIPTORS.map(({ type, i18nKey }) => {
      const isActive = currentLayout === type;
      const ThumbComponent = THUMB_COMPONENTS[type];
      return /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          "data-testid": `smartart-layout-${type}`,
          disabled: !canEdit,
          className: cn(
            "flex flex-col items-center gap-0.5 rounded border p-1.5 transition-colors",
            "text-[9px] leading-tight",
            isActive ? "border-primary bg-primary/15 text-primary" : "border-border text-muted-foreground hover:bg-muted hover:text-foreground",
            !canEdit && "opacity-50 cursor-not-allowed"
          ),
          onClick: () => handleLayoutSwitch(type),
          title: t(i18nKey),
          children: [
            /* @__PURE__ */ jsx("div", { className: "w-10 h-7", children: ThumbComponent ? /* @__PURE__ */ jsx(ThumbComponent, {}) : null }),
            /* @__PURE__ */ jsx("span", { className: "truncate w-full text-center", children: t(i18nKey) })
          ]
        },
        type
      );
    }) })
  ] });
}
var DEFAULT_FILL = "#3b82f6";
var DEFAULT_FONT = "#ffffff";
var EMPHASIS_BTN = "px-1.5 py-0.5 text-[10px] rounded border transition-colors disabled:opacity-40 disabled:cursor-not-allowed";
function SmartArtNodeStyleControls({
  nodeId,
  style,
  label,
  canEdit,
  onChangeStyle
}) {
  const { pushColor } = useRecentColors();
  const bold = Boolean(style?.bold);
  const italic = Boolean(style?.italic);
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "flex items-center gap-1.5 mt-1 pl-4",
      role: "group",
      "aria-label": `Style for ${label}`,
      children: [
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-0.5 text-[9px] text-muted-foreground", children: [
          /* @__PURE__ */ jsx("span", { children: "Fill" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              "aria-label": `Fill colour for ${label}`,
              className: "h-4 w-5 cursor-pointer rounded border border-border bg-transparent p-0 disabled:cursor-not-allowed",
              value: style?.fillColor ?? DEFAULT_FILL,
              onChange: (e) => {
                onChangeStyle(nodeId, { fillColor: e.target.value });
                pushColor(e.target.value);
              }
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-0.5 text-[9px] text-muted-foreground", children: [
          /* @__PURE__ */ jsx("span", { children: "Text" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              "aria-label": `Font colour for ${label}`,
              className: "h-4 w-5 cursor-pointer rounded border border-border bg-transparent p-0 disabled:cursor-not-allowed",
              value: style?.fontColor ?? DEFAULT_FONT,
              onChange: (e) => {
                onChangeStyle(nodeId, { fontColor: e.target.value });
                pushColor(e.target.value);
              }
            }
          )
        ] }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            "aria-label": `Bold ${label}`,
            "aria-pressed": bold,
            className: cn(
              EMPHASIS_BTN,
              "font-bold",
              bold ? "border-primary bg-primary/20 text-primary" : "border-border text-muted-foreground"
            ),
            onClick: () => onChangeStyle(nodeId, { bold: !bold }),
            children: "B"
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            "aria-label": `Italic ${label}`,
            "aria-pressed": italic,
            className: cn(
              EMPHASIS_BTN,
              "italic",
              italic ? "border-primary bg-primary/20 text-primary" : "border-border text-muted-foreground"
            ),
            onClick: () => onChangeStyle(nodeId, { italic: !italic }),
            children: "I"
          }
        )
      ]
    }
  );
}
var MINI_BTN = "text-[9px] text-muted-foreground hover:text-primary px-1 disabled:opacity-40 disabled:cursor-not-allowed";
function SmartArtNodeRow({
  nodeId,
  text,
  displayIndex,
  isChild,
  canEdit,
  removeDisabled,
  moveUpDisabled,
  moveDownDisabled,
  inputRef,
  style,
  onChangeText,
  onKeyDown,
  onAddSubItem,
  onMoveUp,
  onMoveDown,
  onRemove,
  onChangeStyle
}) {
  const { t } = useTranslation();
  const label = isChild ? `Sub-item: ${text || "empty"}` : `Item ${displayIndex}: ${text || "empty"}`;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      role: "listitem",
      className: cn(
        "rounded border bg-background/60 p-1.5",
        isChild ? "border-border/60 ml-4" : "border-border"
      ),
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground w-3 shrink-0", "aria-hidden": "true", children: isChild ? "\u2022" : `${displayIndex}` }),
          /* @__PURE__ */ jsx(
            "input",
            {
              ref: inputRef,
              type: "text",
              "data-testid": "smartart-node-text",
              disabled: !canEdit,
              "aria-label": label,
              className: cn(INPUT, "flex-1 text-[11px] py-0.5"),
              value: text,
              onChange: (e) => onChangeText(nodeId, e.target.value),
              onKeyDown: (e) => onKeyDown(e, nodeId),
              placeholder: t("pptx.smartArt.nodePlaceholder")
            }
          ),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-0.5 shrink-0", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit || moveUpDisabled,
                className: MINI_BTN,
                onClick: () => onMoveUp(nodeId),
                "aria-label": `Move ${label} up`,
                title: t("pptx.smartArt.moveUp"),
                children: "\u2191"
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit || moveDownDisabled,
                className: MINI_BTN,
                onClick: () => onMoveDown(nodeId),
                "aria-label": `Move ${label} down`,
                title: t("pptx.smartArt.moveDown"),
                children: "\u2193"
              }
            ),
            !isChild && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: MINI_BTN,
                onClick: () => onAddSubItem(nodeId),
                "aria-label": `Add sub-item under ${label}`,
                title: t("pptx.smartArt.addSubItem"),
                children: "+Sub"
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit || removeDisabled,
                className: cn(MINI_BTN, "hover:text-red-400"),
                onClick: () => onRemove(nodeId),
                "aria-label": `Remove ${label}`,
                title: removeDisabled ? t("pptx.smartArt.layoutMinimum") : t("pptx.smartArt.remove"),
                children: "x"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsx(
          SmartArtNodeStyleControls,
          {
            nodeId,
            style,
            label,
            canEdit,
            onChangeStyle
          }
        )
      ]
    }
  );
}
function SmartArtStyleControls({
  colorSchemes,
  styleOptions,
  colorScheme,
  style,
  canEdit,
  onChangeColorScheme,
  onChangeStyle
}) {
  const { t } = useTranslation();
  const translate = React20.useCallback((key) => t(key), [t]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.smartart.colorScheme") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          disabled: !canEdit,
          "data-testid": "smartart-color-scheme",
          "aria-label": t("pptx.smartart.colorScheme"),
          className: cn(INPUT, "w-full"),
          value: colorScheme,
          onChange: (e) => onChangeColorScheme(e.target.value),
          children: colorSchemes.map((cs) => /* @__PURE__ */ jsx("option", { value: cs, children: schemaLabel(SMARTART_COLOR_SCHEME_LABEL_KEYS, cs, translate) }, cs))
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.smartart.style") }),
      /* @__PURE__ */ jsx("div", { className: "flex gap-1", role: "group", "aria-label": t("pptx.smartart.style"), children: styleOptions.map((s) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          "aria-pressed": style === s,
          className: cn(
            "flex-1 px-2 py-1 text-[10px] rounded border transition-colors",
            style === s ? "border-primary bg-primary/20 text-primary" : "border-border text-muted-foreground hover:bg-muted"
          ),
          onClick: () => onChangeStyle(s),
          children: schemaLabel(SMARTART_STYLE_LABEL_KEYS, s, translate)
        },
        s
      )) })
    ] })
  ] });
}
var COLOR_SCHEMES = [
  "colorful1",
  "colorful2",
  "colorful3",
  "monochromatic1",
  "monochromatic2"
];
var STYLE_OPTIONS = ["flat", "moderate", "intense"];
function SmartArtPropertiesPanel({
  smartArtData,
  canEdit,
  onUpdateElement,
  box
}) {
  const { t } = useTranslation();
  const nodes = smartArtData.nodes ?? [];
  const layout = smartArtData.resolvedLayoutType;
  const topLevelCount = nodes.filter((n) => !n.parentId).length;
  const inputRefs = React20.useRef(/* @__PURE__ */ new Map());
  const pendingFocusId = React20.useRef(null);
  React20.useEffect(() => {
    const id = pendingFocusId.current;
    if (id) {
      inputRefs.current.get(id)?.focus();
      pendingFocusId.current = null;
    }
  });
  const applySmartArtData = (newData, focusId) => {
    if (focusId) {
      pendingFocusId.current = focusId;
    }
    const reflowed = box ? rebuildDrawingShapesIfCleared(
      newData,
      newData.layout,
      resolveSmartArtDataPalette(newData),
      newData.style ?? "flat",
      "inspector",
      box
    ) : newData;
    onUpdateElement({ smartArtData: reflowed });
  };
  const updateSmartArt = (patch) => {
    applySmartArtData({ ...smartArtData, ...patch });
  };
  const handleUpdateNodeText = (nodeId, text) => {
    applySmartArtData(updateSmartArtNodeText(smartArtData, nodeId, text));
  };
  const handleChangeNodeStyle = (nodeId, patch) => {
    const next = setSmartArtNodeStyle(smartArtData, nodeId, patch);
    if (next !== smartArtData) {
      applySmartArtData(next);
    }
  };
  const addNode = () => {
    if (!canAddTopLevelNode(layout, topLevelCount)) {
      return;
    }
    applySmartArtData(addSmartArtNodeAsChild(smartArtData));
  };
  const addSubItem = (parentId) => {
    applySmartArtData(addSmartArtNodeAsChild(smartArtData, parentId, "Sub-item"));
  };
  const removeNode = (nodeId) => {
    const isTopLevel = !nodes.find((n) => n.id === nodeId)?.parentId;
    if (isTopLevel && !canRemoveTopLevelNode(layout, topLevelCount)) {
      return;
    }
    applySmartArtData(removeSmartArtNode(smartArtData, nodeId));
  };
  const moveNode = (nodeId, direction) => {
    const next = reorder(smartArtData, nodeId, direction);
    if (next) {
      applySmartArtData(next, nodeId);
    }
  };
  const handleKeyDown = (e, nodeId) => {
    const node = nodes.find((n) => n.id === nodeId);
    const isEmpty = !node?.text;
    if (e.key === "Enter") {
      e.preventDefault();
      const result = addSiblingAfter(smartArtData, nodeId);
      if (result) {
        applySmartArtData(result.data, result.focusNodeId);
      }
    } else if ((e.key === "Backspace" || e.key === "Delete") && isEmpty) {
      const isTop = !node?.parentId;
      if (isTop && !canRemoveTopLevelNode(layout, topLevelCount)) {
        return;
      }
      e.preventDefault();
      const result = removeEmptyNode(smartArtData, nodeId);
      if (result) {
        applySmartArtData(result.data, result.focusNodeId);
      }
    } else if (e.key === "Tab" && !e.shiftKey) {
      e.preventDefault();
      const next = demote(smartArtData, nodeId);
      if (next) {
        applySmartArtData(next, nodeId);
      }
    } else if (e.key === "Tab" && e.shiftKey) {
      e.preventDefault();
      const next = promote(smartArtData, nodeId);
      if (next) {
        applySmartArtData(next, nodeId);
      }
    }
  };
  const setInputRef = (nodeId) => (el) => {
    if (el) {
      inputRefs.current.set(nodeId, el);
    } else {
      inputRefs.current.delete(nodeId);
    }
  };
  const boundsHint = describeSmartArtBounds(layout);
  const addDisabled = !canEdit || !canAddTopLevelNode(layout, topLevelCount);
  const extraConnections = classifyExtraConnections(smartArtData);
  const extraConnectionSummary = Array.from(
    new Set(extraConnections.map((c) => c.type ?? "unknown"))
  ).join(", ");
  let topDisplayIndex = 0;
  return /* @__PURE__ */ jsxs("div", { className: CARD, role: "group", "aria-label": t("pptx.smartart.title"), children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.smartart.title") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
      /* @__PURE__ */ jsx(
        SmartArtLayoutSwitcher,
        {
          smartArtData,
          canEdit,
          onUpdateSmartArt: updateSmartArt
        }
      ),
      /* @__PURE__ */ jsx(
        SmartArtStyleControls,
        {
          colorSchemes: COLOR_SCHEMES,
          styleOptions: STYLE_OPTIONS,
          colorScheme: smartArtData.colorScheme ?? "colorful1",
          style: smartArtData.style ?? "flat",
          canEdit,
          onChangeColorScheme: (colorScheme) => updateSmartArt({ colorScheme }),
          onChangeStyle: (style) => updateSmartArt({ style })
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
        /* @__PURE__ */ jsxs("span", { className: "text-[11px] text-muted-foreground", children: [
          t("pptx.smartart.textPane"),
          " (",
          nodes.length,
          ")"
        ] }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: addDisabled,
            className: BTN,
            onClick: addNode,
            title: addDisabled ? boundsHint : void 0,
            children: t("pptx.smartart.addItem")
          }
        )
      ] }),
      boundsHint && /* @__PURE__ */ jsx("div", { className: "text-[9px] text-muted-foreground", role: "note", children: boundsHint }),
      /* @__PURE__ */ jsx("div", { className: "max-h-52 overflow-y-auto space-y-1 pr-1", role: "list", children: nodes.map((node) => {
        const isChild = Boolean(node.parentId);
        if (!isChild) {
          topDisplayIndex += 1;
        }
        const sIdx = siblingIndex(smartArtData, node.id);
        const sCount = siblingCount(smartArtData, node.id);
        const removeDisabled = nodes.length <= 1 || !isChild && !canRemoveTopLevelNode(layout, topLevelCount);
        return /* @__PURE__ */ jsx(
          SmartArtNodeRow,
          {
            nodeId: node.id,
            text: node.text,
            displayIndex: topDisplayIndex,
            isChild,
            canEdit,
            removeDisabled,
            moveUpDisabled: sIdx <= 0,
            moveDownDisabled: sIdx < 0 || sIdx >= sCount - 1,
            inputRef: setInputRef(node.id),
            style: node.style,
            onChangeText: handleUpdateNodeText,
            onKeyDown: handleKeyDown,
            onAddSubItem: addSubItem,
            onMoveUp: (id) => moveNode(id, -1),
            onMoveDown: (id) => moveNode(id, 1),
            onRemove: removeNode,
            onChangeStyle: handleChangeNodeStyle
          },
          node.id
        );
      }) }),
      extraConnections.length > 0 && /* @__PURE__ */ jsxs(
        "div",
        {
          className: "text-[9px] text-muted-foreground",
          role: "note",
          "aria-label": `${extraConnections.length} non-tree relationship connection(s): ${extraConnectionSummary}`,
          children: [
            t("pptx.smartart.extraConnections", { count: extraConnections.length }),
            /* @__PURE__ */ jsx("span", { className: "block opacity-80", children: extraConnectionSummary })
          ]
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "text-[9px] text-muted-foreground mt-1", children: t("pptx.smartart.tabHint") })
    ] })
  ] });
}
var HEADER_CELL = "flex items-center justify-center gap-0.5 bg-muted text-muted-foreground border border-border -m-px px-1 py-0.5 whitespace-nowrap";
var CELL_INPUT2 = "w-full box-border bg-muted px-1 py-0.5 text-[11px] border-0 outline-none focus:bg-accent disabled:opacity-60";
var REMOVE_BTN = "px-0.5 leading-none text-destructive hover:opacity-80";
function TableDataGrid({
  tableElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const grid = buildTableDataGrid(tableElement);
  if (grid.rowCount === 0 || grid.colCount === 0) {
    return null;
  }
  const commit = (next) => {
    if (next === tableElement) {
      return;
    }
    onUpdateElement({ tableData: next.tableData, rawXml: next.rawXml });
  };
  return /* @__PURE__ */ jsx("div", { className: CARD, children: /* @__PURE__ */ jsxs("section", { "aria-label": t("pptx.tableDataEditor.ariaLabel"), children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-1 mb-1.5", children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.inspector.tableData") }),
      canEdit && /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap gap-0.5", children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            title: t("pptx.tableDataEditor.addRowTitle"),
            onClick: () => commit(appendTableElementRow(tableElement)),
            children: t("pptx.tableDataEditor.addRowLabel")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !grid.canRemoveRow,
            title: t("pptx.tableDataEditor.removeRowTitle"),
            onClick: () => commit(removeLastTableElementRow(tableElement)),
            children: t("pptx.tableDataEditor.removeRowLabel")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            title: t("pptx.tableDataEditor.addColumnTitle"),
            onClick: () => commit(appendTableElementColumn(tableElement)),
            children: t("pptx.tableDataEditor.addColumnLabel")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !grid.canRemoveColumn,
            title: t("pptx.tableDataEditor.removeColumnTitle"),
            onClick: () => commit(removeLastTableElementColumn(tableElement)),
            children: t("pptx.tableDataEditor.removeColumnLabel")
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx("div", { className: "overflow-x-auto", children: /* @__PURE__ */ jsxs("div", { className: "flex flex-col text-[11px] w-max min-w-full", role: "grid", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex", role: "row", children: [
        /* @__PURE__ */ jsx("div", { className: `${HEADER_CELL} flex-none w-10`, "aria-hidden": "true" }),
        grid.colIndices.map((colIndex) => /* @__PURE__ */ jsxs(
          "div",
          {
            className: `${HEADER_CELL} flex-1 basis-16`,
            role: "columnheader",
            children: [
              /* @__PURE__ */ jsx("span", { children: colIndex + 1 }),
              canEdit && grid.canRemoveColumn && /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  className: REMOVE_BTN,
                  "aria-label": t("pptx.tableDataEditor.removeColumnN", {
                    number: colIndex + 1
                  }),
                  title: t("pptx.tableDataEditor.removeColumnN", { number: colIndex + 1 }),
                  onClick: () => commit(removeTableElementColumn(tableElement, colIndex)),
                  children: "\xD7"
                }
              )
            ]
          },
          colIndex
        ))
      ] }),
      grid.rows.map((row) => /* @__PURE__ */ jsxs("div", { className: "flex", role: "row", children: [
        /* @__PURE__ */ jsxs("div", { className: `${HEADER_CELL} flex-none w-10`, role: "rowheader", children: [
          /* @__PURE__ */ jsx("span", { children: row.rowIndex + 1 }),
          canEdit && grid.canRemoveRow && /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: REMOVE_BTN,
              "aria-label": t("pptx.tableDataEditor.removeRowN", {
                number: row.rowIndex + 1
              }),
              title: t("pptx.tableDataEditor.removeRowN", { number: row.rowIndex + 1 }),
              onClick: () => commit(removeTableElementRow(tableElement, row.rowIndex)),
              children: "\xD7"
            }
          )
        ] }),
        row.cells.map((cell) => /* @__PURE__ */ jsx(
          "div",
          {
            className: "flex flex-1 basis-16 p-px border border-border -m-px",
            role: "gridcell",
            children: /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                className: CELL_INPUT2,
                disabled: !canEdit,
                "aria-label": t("pptx.tableDataEditor.cellAriaLabel", {
                  row: cell.rowIndex + 1,
                  column: cell.colIndex + 1
                }),
                value: cell.text,
                onChange: (event) => commit(
                  setTableElementCellText(
                    tableElement,
                    cell.rowIndex,
                    cell.colIndex,
                    event.target.value
                  )
                )
              }
            )
          },
          cell.colIndex
        ))
      ] }, row.rowIndex))
    ] }) })
  ] }) });
}
function TableCellPatternFill({
  cellStyle,
  canEdit,
  onUpdateCellStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const translate = (key) => t(key);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
      /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.patternPreset") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.table.patternPreset"),
          disabled: !canEdit,
          className: SEL,
          value: cellStyle.patternFillPreset ?? "ltDnDiag",
          onChange: (e) => onUpdateCellStyle({ patternFillPreset: e.target.value }),
          children: PATTERN_OPTIONS.map((p) => /* @__PURE__ */ jsx("option", { value: p, children: schemaLabel(FILL_PATTERN_LABEL_KEYS, p, translate) }, p))
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.patternForeground") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
            value: cellStyle.patternFillForeground ?? "#000000",
            onChange: (e) => {
              onUpdateCellStyle({ patternFillForeground: e.target.value });
              pushColor(e.target.value);
            }
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.patternBackground") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
            value: cellStyle.patternFillBackground ?? "#FFFFFF",
            onChange: (e) => {
              onUpdateCellStyle({ patternFillBackground: e.target.value });
              pushColor(e.target.value);
            }
          }
        )
      ] })
    ] })
  ] });
}
function TableCellAdvancedFill({
  cellStyle,
  canEdit,
  onUpdateCellStyle
}) {
  const { t } = useTranslation();
  const fillMode = cellStyle.fillMode ?? "solid";
  const handleFillModeChange = (mode) => {
    const next = mode;
    if (next === "gradient") {
      onUpdateCellStyle({
        fillMode: "gradient",
        gradientFillType: cellStyle.gradientFillType ?? "linear",
        gradientFillAngle: cellStyle.gradientFillAngle ?? 90,
        gradientFillStops: cellStyle.gradientFillStops ?? [
          { color: "#FF0000", position: 0 },
          { color: "#0000FF", position: 100 }
        ]
      });
    } else if (next === "pattern") {
      onUpdateCellStyle({
        fillMode: "pattern",
        patternFillPreset: cellStyle.patternFillPreset ?? "ltDnDiag",
        patternFillForeground: cellStyle.patternFillForeground ?? "#000000",
        patternFillBackground: cellStyle.patternFillBackground ?? "#FFFFFF"
      });
    } else {
      onUpdateCellStyle({ fillMode: next });
    }
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
    /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
      /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.fillMode") }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": t("pptx.table.fillMode"),
          disabled: !canEdit,
          className: SEL,
          value: fillMode,
          onChange: (e) => handleFillModeChange(e.target.value),
          children: FILL_MODE_OPTIONS.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.value ?? "", children: t(opt.i18nKey) }, opt.value))
        }
      )
    ] }),
    fillMode === "gradient" && /* @__PURE__ */ jsx(
      GradientControls,
      {
        cellStyle,
        canEdit,
        onUpdateCellStyle
      }
    ),
    fillMode === "pattern" && /* @__PURE__ */ jsx(
      TableCellPatternFill,
      {
        cellStyle,
        canEdit,
        onUpdateCellStyle
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
      /* @__PURE__ */ jsx("span", { className: SECTION_HEADING, children: t("pptx.table.margins") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-1.5", children: [
        ["marginTop", "pptx.table.marginTop"],
        ["marginBottom", "pptx.table.marginBottom"],
        ["marginLeft", "pptx.table.marginLeft"],
        ["marginRight", "pptx.table.marginRight"]
      ].map(([key, i18nKey]) => /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: LBL, children: t(i18nKey) }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: NUM,
            min: 0,
            max: 200,
            value: cellStyle[key] ?? 0,
            onChange: (e) => onUpdateCellStyle({
              [key]: Number(e.target.value)
            })
          }
        )
      ] }, key)) })
    ] })
  ] });
}
function GradientControls({
  cellStyle,
  canEdit,
  onUpdateCellStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const stops = cellStyle.gradientFillStops ?? [];
  const gradType = cellStyle.gradientFillType ?? "linear";
  const updateStop = (index, patch) => {
    const next = stops.map((s, i) => i === index ? { ...s, ...patch } : s);
    onUpdateCellStyle({ gradientFillStops: next });
  };
  const addStop = () => {
    onUpdateCellStyle({
      gradientFillStops: [...stops, { color: "#888888", position: 50 }]
    });
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.gradientType") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.table.gradientType"),
            disabled: !canEdit,
            className: SEL,
            value: gradType,
            onChange: (e) => onUpdateCellStyle({
              gradientFillType: e.target.value
            }),
            children: GRADIENT_TYPE_OPTIONS.map((o) => /* @__PURE__ */ jsx("option", { value: o.value, children: t(o.i18nKey) }, o.value))
          }
        )
      ] }),
      gradType === "linear" && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
        /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.gradientAngle") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: NUM,
            min: 0,
            max: 360,
            value: cellStyle.gradientFillAngle ?? 90,
            onChange: (e) => onUpdateCellStyle({
              gradientFillAngle: Number(e.target.value)
            })
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx("span", { className: LBL, children: t("pptx.table.gradientStops") }),
    stops.map((stop, idx) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "color",
          disabled: !canEdit,
          className: "h-6 w-6 rounded border border-border cursor-pointer",
          value: stop.color,
          onChange: (e) => {
            updateStop(idx, { color: e.target.value });
            pushColor(e.target.value);
          }
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "number",
          disabled: !canEdit,
          className: NUM,
          min: 0,
          max: 100,
          value: Math.round(stop.position),
          onChange: (e) => updateStop(idx, {
            position: Number(e.target.value)
          })
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: "%" })
    ] }, idx)),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: "text-[10px] text-primary hover:underline",
        onClick: addStop,
        children: t("pptx.table.gradientAddStop")
      }
    )
  ] });
}
function TableCellColorField({
  label,
  prefix,
  value,
  fallback,
  selectedRef,
  disabled,
  onCommit
}) {
  const hex = normalizeHexColor(value, fallback);
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: label }),
    /* @__PURE__ */ jsx(
      DebouncedColorInput,
      {
        disabled,
        value: hex,
        className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
        onCommit: (next) => onCommit(next, void 0)
      }
    ),
    /* @__PURE__ */ jsx(
      ThemeColorSwatchGrid,
      {
        prefix,
        disabled,
        selectedRef,
        selectedHex: hex,
        onPick: (c) => onCommit(c.hex, c.ref)
      }
    )
  ] });
}
function TableCellFormattingPanel({
  tableData: td,
  tableEditorState,
  canEdit,
  onUpdateTableData
}) {
  const { t } = useTranslation();
  const { rowIndex, columnIndex } = tableEditorState;
  const cell = td.rows[rowIndex]?.cells[columnIndex];
  if (!cell) {
    return null;
  }
  const cs = cell.style ?? {};
  const updateCellStyle = (updates) => {
    const newRows = td.rows.map((row, ri) => {
      if (ri !== rowIndex) {
        return row;
      }
      return {
        ...row,
        cells: row.cells.map((c, ci) => {
          if (ci !== columnIndex) {
            return c;
          }
          return { ...c, style: { ...cs, ...updates } };
        })
      };
    });
    onUpdateTableData({ rows: newRows });
  };
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.table.cell", { row: rowIndex + 1, col: columnIndex + 1 }) }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-10 text-muted-foreground", children: t("pptx.table.fontSize") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: INPUT,
            min: 6,
            max: 200,
            value: cs.fontSize ?? 14,
            onChange: (e) => updateCellStyle({ fontSize: Number(e.target.value) })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
        /* @__PURE__ */ jsx(
          TableCellColorField,
          {
            label: t("pptx.table.color"),
            prefix: "table-cell-text",
            value: cs.color,
            fallback: "#000000",
            selectedRef: cs.colorRef,
            disabled: !canEdit,
            onCommit: (hex, ref) => updateCellStyle({ color: hex, colorRef: ref })
          }
        ),
        /* @__PURE__ */ jsx(
          TableCellColorField,
          {
            label: t("pptx.table.background"),
            prefix: "table-cell-bg",
            value: cs.backgroundColor,
            fallback: "#ffffff",
            selectedRef: cs.backgroundColorRef,
            disabled: !canEdit,
            onCommit: (hex, ref) => updateCellStyle({ backgroundColor: hex, backgroundColorRef: ref })
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        TableCellAdvancedFill,
        {
          cellStyle: cs,
          canEdit,
          onUpdateCellStyle: updateCellStyle
        }
      ),
      /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: [
        ["bold", "B", "font-bold"],
        ["italic", "I", "italic"],
        ["underline", "U", "underline"]
      ].map(([key, lbl, cls]) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "px-2 py-1 rounded text-[11px] transition-colors",
            cls,
            cs[key] ? "bg-primary text-white" : "bg-muted hover:bg-accent"
          ),
          onClick: () => updateCellStyle({ [key]: !cs[key] }),
          children: lbl
        },
        key
      )) }),
      /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: [
        ["left", "L"],
        ["center", "C"],
        ["right", "R"]
      ].map(([val, lbl]) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "px-2 py-1 rounded text-[11px] transition-colors",
            cs.align === val ? "bg-primary text-white" : "bg-muted hover:bg-accent"
          ),
          onClick: () => updateCellStyle({ align: val }),
          children: lbl
        },
        val
      )) }),
      /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: [
        ["top", "T"],
        ["middle", "M"],
        ["bottom", "B"]
      ].map(([val, lbl]) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "px-2 py-1 rounded text-[11px] transition-colors",
            cs.vAlign === val ? "bg-primary text-white" : "bg-muted hover:bg-accent"
          ),
          onClick: () => updateCellStyle({ vAlign: val }),
          children: lbl
        },
        val
      )) }),
      /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.table.cellBorders") }),
        /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-1.5", children: [
          ["Top", "borderTopColor", "borderTopWidth"],
          ["Bottom", "borderBottomColor", "borderBottomWidth"],
          ["Left", "borderLeftColor", "borderLeftWidth"],
          ["Right", "borderRightColor", "borderRightWidth"]
        ].map(([edge, colorKey, widthKey]) => /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-0.5", children: [
          /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t(`pptx.table.border${edge}`) }),
          /* @__PURE__ */ jsxs("div", { className: "flex gap-1 items-center", children: [
            /* @__PURE__ */ jsx(
              DebouncedColorInput,
              {
                disabled: !canEdit,
                value: normalizeHexColor(cs[colorKey], "#374151"),
                className: "w-7 h-6 rounded border border-border bg-transparent cursor-pointer shrink-0",
                onCommit: (hex) => updateCellStyle({ [colorKey]: hex })
              }
            ),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "number",
                disabled: !canEdit,
                className: cn(INPUT, "w-14"),
                min: 0,
                max: 10,
                value: cs[widthKey] ?? 1,
                onChange: (e) => updateCellStyle({ [widthKey]: Number(e.target.value) })
              }
            )
          ] })
        ] }, edge)) })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-3 gap-1", children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: cn(BTN, "text-center"),
            onClick: () => {
              const newRows = computeMergeCellRight(td, rowIndex, columnIndex);
              if (newRows) {
                onUpdateTableData({ rows: newRows });
              }
            },
            children: t("pptx.table.mergeRight")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: cn(BTN, "text-center"),
            onClick: () => {
              const newRows = computeMergeCellDown(td, rowIndex, columnIndex);
              if (newRows) {
                onUpdateTableData({ rows: newRows });
              }
            },
            children: t("pptx.table.mergeDown")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: cn(BTN, "text-center"),
            onClick: () => {
              const newRows = computeSplitCell(td, rowIndex, columnIndex);
              if (newRows) {
                onUpdateTableData({ rows: newRows });
              }
            },
            children: t("pptx.table.split")
          }
        )
      ] })
    ] })
  ] });
}
function TableStyleEditorFields({
  descriptor,
  canEdit,
  onEdit
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
    /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.tableStyleEditor.fillSection") }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            value: descriptor.fill.color.hex,
            onChange: (e) => onEdit({ kind: "fillColor", hex: e.target.value, ref: void 0 }),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer"
          }
        ),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: descriptor.fill.noFill,
              onChange: (e) => onEdit({ kind: "fillNone", noFill: e.target.checked })
            }
          ),
          t("pptx.tableStyleEditor.noFill")
        ] })
      ] }),
      /* @__PURE__ */ jsx(
        ThemeColorSwatchGrid,
        {
          prefix: "table-style-fill",
          disabled: !canEdit,
          selectedRef: descriptor.fill.color.ref,
          selectedHex: descriptor.fill.color.hex,
          onPick: (c) => onEdit({ kind: "fillColor", hex: c.hex, ref: c.ref })
        }
      )
    ] }),
    descriptor.hasTextAndBorders && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.tableStyleEditor.textSection") }),
        /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: ["bold", "italic", "underline"].map((flag) => /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: `${BTN} ${descriptor.text[flag] ? "bg-accent" : ""}`,
            onClick: () => onEdit(textFlagEdit(flag, !descriptor.text[flag])),
            children: t(`pptx.format.${flag}`)
          },
          flag
        )) }),
        /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { children: t("pptx.tableStyleEditor.textColor") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              value: descriptor.text.color.hex,
              onChange: (e) => onEdit({ kind: "textColor", hex: e.target.value, ref: void 0 }),
              className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer"
            }
          )
        ] }),
        /* @__PURE__ */ jsx(
          ThemeColorSwatchGrid,
          {
            prefix: "table-style-text",
            disabled: !canEdit,
            selectedRef: descriptor.text.color.ref,
            selectedHex: descriptor.text.color.hex,
            onPick: (c) => onEdit({ kind: "textColor", hex: c.hex, ref: c.ref })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.tableStyleEditor.bordersSection") }),
        TABLE_STYLE_BORDER_SIDES.map((side) => /* @__PURE__ */ jsx(
          BorderSideRow,
          {
            side,
            state: descriptor.borders[side],
            canEdit,
            onEdit
          },
          side
        ))
      ] })
    ] })
  ] });
}
function textFlagEdit(flag, value) {
  if (flag === "bold") {
    return { kind: "textBold", value };
  }
  if (flag === "italic") {
    return { kind: "textItalic", value };
  }
  return { kind: "textUnderline", value };
}
function BorderSideRow({
  side,
  state,
  canEdit,
  onEdit
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1.5 text-[11px]", children: [
    /* @__PURE__ */ jsx("span", { className: "w-28 shrink-0 text-muted-foreground", children: t(TABLE_STYLE_BORDER_SIDE_LABEL_KEYS[side]) }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "color",
        disabled: !canEdit,
        value: state.color.hex,
        onChange: (e) => onEdit({ kind: "borderColor", side, hex: e.target.value, ref: void 0 }),
        className: "h-6 w-7 rounded border border-border bg-transparent cursor-pointer"
      }
    ),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "number",
        min: 0,
        max: 20,
        disabled: !canEdit,
        value: state.width,
        onChange: (e) => onEdit({ kind: "borderWidth", side, width: Number(e.target.value) }),
        className: `${INPUT} w-12`
      }
    ),
    /* @__PURE__ */ jsx(
      "select",
      {
        disabled: !canEdit,
        value: state.dash,
        onChange: (e) => onEdit({ kind: "borderDash", side, dash: e.target.value }),
        className: INPUT,
        children: TABLE_STYLE_DASH_PRESETS.map((dash) => /* @__PURE__ */ jsx("option", { value: dash, children: dash }, dash))
      }
    ),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1 shrink-0", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: state.noFill,
          onChange: (e) => onEdit({ kind: "borderNone", side, noFill: e.target.checked })
        }
      ),
      t("pptx.tableStyleEditor.noBorder")
    ] })
  ] });
}
function TableStyleEditor({
  styleMap,
  styleId,
  canEdit,
  onStyleMapChange,
  onDeleteStyle,
  onAssignStyle,
  onClose
}) {
  const { t } = useTranslation();
  const themeColorMap = useThemeColorMap();
  const [activeStyleId, setActiveStyleId] = useState(
    styleId ? normalizeTableStyleGuid(styleId) : ""
  );
  const [selectedPart, setSelectedPart] = useState("wholeTbl");
  const entry = activeStyleId ? styleMap?.[activeStyleId] : void 0;
  const descriptor = describeTableStyleEditor(entry, selectedPart, themeColorMap);
  function edit(fieldEdit) {
    if (!entry || !styleMap) {
      return;
    }
    const { entry: nextEntry } = applyTableStyleFieldEdit(entry, selectedPart, fieldEdit);
    onStyleMapChange({ ...styleMap, [nextEntry.styleId]: nextEntry });
  }
  function createFromCurrent() {
    const name = window.prompt(
      t("pptx.tableStyleEditor.newStyleNamePrompt"),
      entry ? `${entry.styleName ?? ""} Copy`.trim() : ""
    );
    if (!name) {
      return;
    }
    const nextMap = { ...styleMap ?? {} };
    const created = createTableStyleEntry(nextMap, { styleName: name, basedOn: entry });
    addTableStyleToMap(nextMap, created);
    onStyleMapChange(nextMap);
    setActiveStyleId(created.styleId);
    onAssignStyle?.(created.styleId);
  }
  function handleDelete() {
    if (!entry || !styleMap || !window.confirm(t("pptx.tableStyleEditor.deleteConfirm"))) {
      return;
    }
    const nextMap = { ...styleMap };
    deleteTableStyleFromMap(nextMap, entry.styleId);
    onStyleMapChange(nextMap);
    onDeleteStyle(entry.styleId);
    onClose();
  }
  return /* @__PURE__ */ jsxs("div", { className: CARD, "data-testid": "table-style-editor", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.tableStyleEditor.title") }),
      /* @__PURE__ */ jsx("button", { type: "button", className: BTN, onClick: onClose, children: t("pptx.tableStyleEditor.close") })
    ] }),
    !entry && /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: t("pptx.tableStyleEditor.noStyleSelected") }),
    entry && /* @__PURE__ */ jsx("div", { className: "flex flex-wrap gap-1", children: TABLE_STYLE_EDITOR_PARTS.map((part) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: `${BTN} ${selectedPart === part.id ? "bg-accent" : ""}`,
        onClick: () => setSelectedPart(part.id),
        children: t(part.labelKey)
      },
      part.id
    )) }),
    descriptor && /* @__PURE__ */ jsx(TableStyleEditorFields, { descriptor, canEdit, onEdit: edit }),
    /* @__PURE__ */ jsxs("div", { className: "flex gap-1.5 pt-1 border-t border-border", children: [
      /* @__PURE__ */ jsx("button", { type: "button", className: BTN, disabled: !canEdit, onClick: createFromCurrent, children: entry ? t("pptx.tableStyleEditor.newFromCurrent") : t("pptx.tableStyleEditor.newStyle") }),
      entry && /* @__PURE__ */ jsx("button", { type: "button", className: BTN, disabled: !canEdit, onClick: handleDelete, children: t("pptx.tableStyleEditor.deleteStyle") })
    ] })
  ] });
}
function TablePropertiesPanel({
  tableElement,
  canEdit,
  onUpdateElement,
  tableEditorState,
  tableStyleMap,
  onTableStyleMapChange,
  onDeleteTableStyle
}) {
  const { t } = useTranslation();
  const [showStyleEditor, setShowStyleEditor] = useState(false);
  const td = tableElement.tableData;
  if (!td) {
    return null;
  }
  const rowCount = td.rows.length;
  const colCount = td.columnWidths.length;
  const updateTableData = (patch) => {
    onUpdateElement({
      tableData: { ...td, ...patch }
    });
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.table.title") }),
      /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground mb-2", children: t("pptx.table.rowsColumns", { rows: rowCount, cols: colCount }) }),
      /* @__PURE__ */ jsx("div", { className: "space-y-1", children: [
        ["bandedRows", "pptx.table.bandedRows"],
        ["firstRowHeader", "pptx.table.headerRow"],
        ["bandedColumns", "pptx.table.bandedColumns"],
        ["firstCol", "pptx.table.firstColumn"],
        ["lastCol", "pptx.table.lastColumn"],
        ["lastRow", "pptx.table.lastRow"]
      ].map(([key, i18nKey]) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(td[key]),
            onChange: (e) => updateTableData({ [key]: e.target.checked }),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[11px]", children: t(i18nKey) })
      ] }, key)) }),
      td.bandedRows && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 mt-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-[11px] text-muted-foreground", children: t("pptx.table.bandRowCycle") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 1,
            max: 99,
            disabled: !canEdit,
            value: td.bandRowCycle ?? 1,
            onChange: (e) => updateTableData({
              bandRowCycle: Math.max(1, parseInt(e.target.value, 10) || 1)
            }),
            className: "w-14 rounded border border-border bg-background px-1 py-0.5 text-[11px]"
          }
        )
      ] }),
      td.bandedColumns && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 mt-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-[11px] text-muted-foreground", children: t("pptx.table.bandColCycle") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 1,
            max: 99,
            disabled: !canEdit,
            value: td.bandColCycle ?? 1,
            onChange: (e) => updateTableData({
              bandColCycle: Math.max(1, parseInt(e.target.value, 10) || 1)
            }),
            className: "w-14 rounded border border-border bg-background px-1 py-0.5 text-[11px]"
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.table.stylePresets") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-3 gap-1.5", children: TABLE_STYLE_PRESETS.map((preset) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          title: preset.label,
          "aria-label": preset.label,
          className: "rounded border border-border hover:border-primary overflow-hidden h-10 transition-colors",
          onClick: () => {
            updateTableData({ rows: applyTableStylePreset(td, preset) });
          },
          children: /* @__PURE__ */ jsxs("div", { className: "flex flex-col h-full", children: [
            /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: preset.headerBg } }),
            /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: preset.bandBg } }),
            /* @__PURE__ */ jsx("div", { className: "flex-1 border-t", style: { borderColor: preset.borderColor } })
          ] })
        },
        preset.id
      )) }),
      onTableStyleMapChange && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: `${BTN} mt-1.5`,
          disabled: !canEdit,
          onClick: () => setShowStyleEditor((v) => !v),
          children: t("pptx.tableStyleEditor.editButton")
        }
      )
    ] }),
    showStyleEditor && onTableStyleMapChange && /* @__PURE__ */ jsx(
      TableStyleEditor,
      {
        styleMap: tableStyleMap,
        styleId: td.tableStyleId,
        canEdit,
        onStyleMapChange: onTableStyleMapChange,
        onDeleteStyle: (styleId) => onDeleteTableStyle?.(styleId),
        onAssignStyle: (styleId) => updateTableData(tableStyleAssignmentUpdate(styleId)),
        onClose: () => setShowStyleEditor(false)
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between mb-1", children: [
        /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.table.columnWidths") }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit,
            onClick: () => {
              updateTableData({ columnWidths: evenColumnWidths(colCount) });
            },
            children: t("pptx.table.even")
          }
        )
      ] }),
      /* @__PURE__ */ jsx("div", { className: "space-y-1", children: td.columnWidths.map((w, ci) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-6 text-muted-foreground shrink-0", children: ci + 1 }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "range",
            disabled: !canEdit,
            min: 5,
            max: 80,
            value: Math.round(w * 100),
            className: "flex-1 accent-primary",
            onChange: (e) => {
              const newPct = Number(e.target.value) / 100;
              updateTableData({
                columnWidths: redistributeColumnWidth(td.columnWidths, ci, newPct)
              });
            }
          }
        ),
        /* @__PURE__ */ jsxs("span", { className: "w-10 text-right text-muted-foreground", children: [
          Math.round(w * 100),
          "%"
        ] })
      ] }, ci)) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between mb-1", children: [
        /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.table.rowHeights") }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit,
            onClick: () => {
              updateTableData({ rows: evenRowHeights(td.rows) });
            },
            children: t("pptx.table.even")
          }
        )
      ] }),
      /* @__PURE__ */ jsx("div", { className: "space-y-1", children: td.rows.map((row, ri) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-6 text-muted-foreground shrink-0", children: ri + 1 }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: INPUT,
            min: 16,
            max: 500,
            value: row.height ?? 32,
            onChange: (e) => {
              const newRows = td.rows.map(
                (r, i) => i === ri ? { ...r, height: Number(e.target.value) } : r
              );
              updateTableData({ rows: newRows });
            }
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "w-6 text-muted-foreground", children: "px" })
      ] }, ri)) })
    ] }),
    tableEditorState && /* @__PURE__ */ jsx(
      TableCellFormattingPanel,
      {
        tableData: td,
        tableEditorState,
        canEdit,
        onUpdateTableData: updateTableData
      }
    )
  ] });
}
function ElementInspectorBody({
  selectedElement,
  canEdit,
  slides,
  customShows,
  tableEditorState,
  mediaDataUrls,
  tableStyleMap,
  onTableStyleMapChange,
  onDeleteTableStyle,
  onUpdateElement,
  onUpdateElementStyle,
  onUpdateTextStyle,
  onMoveLayer
}) {
  const { t } = useTranslation();
  const isLocked = isElementLocked(selectedElement);
  const handleToggleLock = () => {
    if (!canEdit) {
      return;
    }
    onUpdateElement({ locks: elementLockTogglePatch(!isLocked) });
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
        /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.inspector.element") }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: handleToggleLock,
            disabled: !canEdit,
            title: isLocked ? t("pptx.inspector.unlock") : t("pptx.inspector.lock"),
            className: "p-1 rounded hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed transition-colors",
            "aria-pressed": isLocked,
            children: isLocked ? /* @__PURE__ */ jsx(LuLock, { className: "w-3.5 h-3.5 text-amber-400" }) : /* @__PURE__ */ jsx(LuLockOpen, { className: "w-3.5 h-3.5 text-muted-foreground" })
          }
        )
      ] }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: POS_FIELDS.map(([label, key]) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "w-4 text-muted-foreground", children: label }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            className: INPUT,
            disabled: !canEdit,
            value: Math.round(selectedElement[key] ?? 0),
            onChange: (e) => onUpdateElement({
              [key]: Number(e.target.value)
            })
          }
        )
      ] }, key)) })
    ] }),
    selectedElement.type === "table" && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(
        TableDataGrid,
        {
          tableElement: selectedElement,
          canEdit,
          onUpdateElement
        }
      ),
      /* @__PURE__ */ jsx(
        TablePropertiesPanel,
        {
          tableElement: selectedElement,
          canEdit,
          onUpdateElement,
          tableEditorState,
          tableStyleMap,
          onTableStyleMapChange,
          onDeleteTableStyle
        }
      )
    ] }),
    selectedElement.type === "chart" && /* @__PURE__ */ jsx(
      ChartDataPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    selectedElement.type === "smartArt" && selectedElement.smartArtData && /* @__PURE__ */ jsx(
      SmartArtPropertiesPanel,
      {
        smartArtData: selectedElement.smartArtData,
        canEdit,
        onUpdateElement,
        box: { width: selectedElement.width, height: selectedElement.height }
      }
    ),
    isImageLikeElement(selectedElement) && /* @__PURE__ */ jsx(
      ImagePropertiesPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    selectedElement.type === "media" && /* @__PURE__ */ jsx(
      MediaPropertiesPanel,
      {
        element: selectedElement,
        mediaDataUrls: mediaDataUrls ?? /* @__PURE__ */ new Map(),
        canEdit,
        onUpdateElement
      }
    ),
    /* @__PURE__ */ jsx(
      ConnectorPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElementStyle
      }
    ),
    /* @__PURE__ */ jsx(GroupInfoPanel, { selectedElement }),
    /* @__PURE__ */ jsx(
      OlePropertiesPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    /* @__PURE__ */ jsx(
      ShapeTextPanels,
      {
        selectedElement,
        canEdit,
        onUpdateElement,
        onUpdateElementStyle,
        onUpdateTextStyle
      }
    ),
    /* @__PURE__ */ jsx(
      ActionSettingsPanel,
      {
        selectedElement,
        slides,
        canEdit,
        customShows,
        onUpdateElement
      }
    ),
    /* @__PURE__ */ jsx(
      TransformPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    /* @__PURE__ */ jsx(LayerOrderButtons, { canEdit, onMoveLayer })
  ] });
}
function CommentMentionTextarea({
  value,
  mentions,
  authors,
  onChange,
  onSubmitShortcut,
  placeholder,
  rows = 2,
  className,
  ariaLabel
}) {
  const { t } = useTranslation();
  const textareaRef = useRef(null);
  const [caret, setCaret] = useState(value.length);
  const [highlighted, setHighlighted] = useState(0);
  const [dismissedAt, setDismissedAt] = useState(null);
  const query = useMemo(() => commentMentionQuery(value, caret), [value, caret]);
  const activeQuery = query && query.start !== dismissedAt ? query : null;
  const matches = useMemo(
    () => activeQuery ? matchCommentMentionAuthors(authors, activeQuery.query) : [],
    [activeQuery, authors]
  );
  const safeHighlighted = matches.length > 0 ? highlighted % matches.length : 0;
  const syncCaret = (target) => {
    setCaret(target.selectionStart ?? target.value.length);
    setHighlighted(0);
  };
  const acceptAuthor = (author) => {
    const result = insertCommentMention(value, mentions, caret, author);
    onChange(result.text, result.mentions);
    setCaret(result.caret);
    setDismissedAt(null);
    requestAnimationFrame(() => {
      textareaRef.current?.setSelectionRange(result.caret, result.caret);
      textareaRef.current?.focus();
    });
  };
  const handleChange = (event) => {
    onChange(event.target.value, mentions);
    syncCaret(event.target);
  };
  const handleKeyDown = (event) => {
    if (matches.length > 0) {
      if (event.key === "ArrowDown") {
        event.preventDefault();
        setHighlighted((index) => (index + 1) % matches.length);
        return;
      }
      if (event.key === "ArrowUp") {
        event.preventDefault();
        setHighlighted((index) => (index - 1 + matches.length) % matches.length);
        return;
      }
      if (event.key === "Enter" || event.key === "Tab") {
        event.preventDefault();
        acceptAuthor(matches[safeHighlighted]);
        return;
      }
      if (event.key === "Escape") {
        event.preventDefault();
        setDismissedAt(activeQuery?.start ?? null);
        return;
      }
    }
    if (onSubmitShortcut && event.key === "Enter" && (event.metaKey || event.ctrlKey)) {
      event.preventDefault();
      onSubmitShortcut();
    }
  };
  return /* @__PURE__ */ jsxs("div", { className: "relative", children: [
    /* @__PURE__ */ jsx(
      "textarea",
      {
        ref: textareaRef,
        "aria-label": ariaLabel,
        rows,
        placeholder: placeholder ?? t("pptx.comments.mentionPlaceholder"),
        value,
        className,
        onChange: handleChange,
        onSelect: (event) => syncCaret(event.currentTarget),
        onClick: (event) => syncCaret(event.currentTarget),
        onKeyDown: handleKeyDown,
        onKeyUp: (event) => syncCaret(event.currentTarget)
      }
    ),
    activeQuery && matches.length > 0 && /* @__PURE__ */ jsx(
      "div",
      {
        "data-testid": "pptx-comment-mention-suggestions",
        role: "listbox",
        "aria-label": t("pptx.comments.mentionSuggestions"),
        className: "absolute z-10 mt-0.5 max-h-40 w-full overflow-y-auto rounded border border-border bg-popover shadow-lg",
        children: matches.map((author, index) => /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-testid": "pptx-comment-mention-option",
            role: "option",
            "aria-selected": index === safeHighlighted,
            className: cn(
              "block w-full px-2 py-1 text-left text-[11px] hover:bg-accent",
              index === safeHighlighted && "bg-accent"
            ),
            onMouseDown: (event) => {
              event.preventDefault();
              acceptAuthor(author);
            },
            children: author.name
          },
          author.id
        ))
      }
    )
  ] });
}
function CommentBody({ text, mentions, className }) {
  const segments = commentTextSegments(text, mentions);
  if (!segments.some((segment) => segment.kind === "mention")) {
    return /* @__PURE__ */ jsx("div", { className, children: text });
  }
  return /* @__PURE__ */ jsx("div", { className, children: segments.map(
    (segment, index) => segment.kind === "mention" ? /* @__PURE__ */ jsx(
      "span",
      {
        className: `${COMMENT_MENTION_CLASS} rounded bg-primary/15 px-0.5 font-semibold text-primary`,
        ...{ [COMMENT_MENTION_ATTRIBUTE]: segment.personId || "" },
        title: segment.authorName,
        children: segment.text
      },
      index
    ) : (
      // eslint-disable-next-line react/no-array-index-key -- segments are positional
      /* @__PURE__ */ jsx(React20.Fragment, { children: segment.text }, index)
    )
  ) });
}
function InspectorCommentReplyForm({
  commentId,
  authorName,
  draft,
  mentions,
  authors,
  onDraftChange,
  onSubmit,
  onCancel
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "mt-2 space-y-1.5 pl-3 border-l-2 border-l-primary/40", children: [
    /* @__PURE__ */ jsx(
      CommentMentionTextarea,
      {
        value: draft,
        mentions,
        authors,
        rows: 2,
        placeholder: t("pptx.comments.replyPlaceholder", { author: authorName || "Author" }),
        className: "w-full rounded border border-border bg-background px-2 py-1.5 text-xs text-foreground outline-none focus:border-primary resize-y",
        onChange: (text, next) => onDraftChange(commentId, text, next),
        onSubmitShortcut: () => onSubmit(commentId)
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1.5", children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          className: "inline-flex items-center gap-1 rounded bg-primary px-2 py-1 text-[11px] text-white hover:bg-primary/80 disabled:opacity-40 disabled:cursor-not-allowed",
          onClick: () => onSubmit(commentId),
          disabled: draft.trim().length === 0,
          children: [
            /* @__PURE__ */ jsx(LuReply, { className: "h-3 w-3" }),
            t("pptx.comments.addReply")
          ]
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "inline-flex items-center gap-1 rounded bg-muted px-2 py-1 text-[11px] text-foreground hover:bg-accent",
          onClick: onCancel,
          children: t("pptx.comments.cancel")
        }
      )
    ] })
  ] });
}
var EMPTY_REPLY_MENTIONS = {};
var EMPTY_AUTHORS = [];
function InspectorCommentRow({
  comment,
  canEdit,
  activeSlide,
  editingCommentId,
  commentEditDraft,
  replyingToCommentId,
  replyDraftByCommentId,
  replyDraftMentionsByCommentId = EMPTY_REPLY_MENTIONS,
  commentAuthors = EMPTY_AUTHORS,
  onStartEditComment,
  onSaveEditComment,
  onCancelEditComment,
  onSetCommentEditDraft,
  onDeleteComment,
  onToggleCommentResolved,
  onStartReply,
  onCancelReply,
  onReplyDraftChange,
  onSubmitReply,
  onSelectElement,
  depth = 0
}) {
  const { t } = useTranslation();
  const isEditing = editingCommentId === comment.id;
  const isReplying = replyingToCommentId === comment.id;
  const replyDraft = replyDraftByCommentId[comment.id] ?? "";
  const [showReplies, setShowReplies] = useState(true);
  const replies = comment.replies ?? [];
  const targetElement = comment.elementId ? activeSlide?.elements?.find((el) => el.id === comment.elementId) ?? null : null;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "rounded border border-border bg-card p-2",
        depth > 0 && "ml-3 border-l-2 border-l-primary/40"
      ),
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1.5 min-w-0", children: [
            /* @__PURE__ */ jsx("span", { className: "text-[11px] font-medium text-foreground truncate", children: comment.author || "Author" }),
            comment.resolved && /* @__PURE__ */ jsxs("span", { className: "inline-flex items-center gap-0.5 rounded-full bg-green-900/40 px-1.5 py-0.5 text-[9px] font-medium text-green-300 flex-shrink-0", children: [
              /* @__PURE__ */ jsx(LuCheck, { className: "h-2.5 w-2.5" }),
              t("pptx.comments.resolved")
            ] })
          ] }),
          /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground flex-shrink-0", children: formatCommentTimestamp(comment.createdAt) })
        ] }),
        comment.elementId && (targetElement ? /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            className: "mt-1 inline-flex items-center gap-1 rounded bg-primary/20 px-1.5 py-0.5 text-[10px] text-primary hover:bg-primary/30 transition-colors",
            title: t("pptx.comments.commentingOn", {
              element: getElementLabel(targetElement)
            }),
            onClick: () => onSelectElement(targetElement.id),
            children: [
              /* @__PURE__ */ jsx(LuType, { className: "h-2.5 w-2.5" }),
              getElementLabel(targetElement)
            ]
          }
        ) : /* @__PURE__ */ jsx("span", { className: "mt-1 inline-flex items-center gap-1 rounded bg-muted/60 px-1.5 py-0.5 text-[10px] text-muted-foreground", children: t("pptx.comments.deletedElement") })),
        isEditing ? /* @__PURE__ */ jsxs("div", { className: "mt-1.5 space-y-1.5", children: [
          /* @__PURE__ */ jsx(
            "textarea",
            {
              value: commentEditDraft,
              onChange: (e) => onSetCommentEditDraft(e.target.value),
              rows: 3,
              className: "w-full rounded border border-border bg-background px-2 py-1.5 text-xs text-foreground outline-none focus:border-primary resize-y"
            }
          ),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1.5", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-primary px-2 py-1 text-[11px] text-white hover:bg-primary/80 disabled:opacity-40 disabled:cursor-not-allowed",
                onClick: () => onSaveEditComment(comment.id),
                disabled: String(commentEditDraft).trim().length === 0,
                children: t("pptx.comments.save")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-muted px-2 py-1 text-[11px] text-foreground hover:bg-accent",
                onClick: onCancelEditComment,
                children: t("pptx.comments.cancel")
              }
            )
          ] })
        ] }) : /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx(
            CommentBody,
            {
              text: comment.text,
              mentions: comment.mentions,
              className: "mt-1 whitespace-pre-wrap break-words text-[11px] text-foreground"
            }
          ),
          canEdit && /* @__PURE__ */ jsxs("div", { className: "mt-1.5 inline-flex items-center rounded bg-muted overflow-hidden", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "inline-flex items-center justify-center p-1.5 hover:bg-accent text-foreground hover:text-foreground transition-colors",
                onClick: () => onStartEditComment(comment.id),
                title: t("pptx.comments.edit"),
                children: /* @__PURE__ */ jsx(LuPencil, { className: "h-3 w-3" })
              }
            ),
            onStartReply && depth === 0 && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "inline-flex items-center justify-center p-1.5 border-l border-border hover:bg-accent text-foreground hover:text-foreground transition-colors",
                onClick: () => onStartReply(comment.id),
                title: t("pptx.comments.reply"),
                children: /* @__PURE__ */ jsx(LuReply, { className: "h-3 w-3" })
              }
            ),
            onToggleCommentResolved && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: cn(
                  "inline-flex items-center justify-center p-1.5 border-l border-border transition-colors",
                  comment.resolved ? "bg-green-900/40 text-green-300 hover:bg-green-900/60" : "hover:bg-accent text-foreground hover:text-foreground"
                ),
                onClick: () => onToggleCommentResolved(comment.id),
                title: comment.resolved ? t("pptx.comments.unresolve") : t("pptx.comments.resolve"),
                children: /* @__PURE__ */ jsx(LuCheck, { className: "h-3 w-3" })
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: "inline-flex items-center justify-center p-1.5 border-l border-border hover:bg-red-900/40 text-muted-foreground hover:text-red-300 transition-colors",
                onClick: () => onDeleteComment(comment.id),
                title: t("pptx.comments.delete"),
                children: /* @__PURE__ */ jsx(LuTrash2, { className: "h-3 w-3" })
              }
            )
          ] })
        ] }),
        replies.length > 0 && /* @__PURE__ */ jsxs("div", { className: "mt-2", children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex items-center gap-1 text-[10px] text-muted-foreground hover:text-foreground transition-colors",
              onClick: () => setShowReplies((p) => !p),
              children: [
                showReplies ? /* @__PURE__ */ jsx(LuChevronDown, { className: "h-3 w-3" }) : /* @__PURE__ */ jsx(LuChevronRight, { className: "h-3 w-3" }),
                t("pptx.comments.repliesCount", { count: replies.length })
              ]
            }
          ),
          showReplies && /* @__PURE__ */ jsx("div", { className: "mt-1.5 space-y-1.5", children: replies.map((reply, ri) => /* @__PURE__ */ jsx(
            InspectorCommentRow,
            {
              comment: reply,
              canEdit,
              activeSlide,
              editingCommentId,
              commentEditDraft,
              replyingToCommentId,
              replyDraftByCommentId,
              replyDraftMentionsByCommentId,
              commentAuthors,
              onStartEditComment,
              onSaveEditComment,
              onCancelEditComment,
              onSetCommentEditDraft,
              onDeleteComment,
              onToggleCommentResolved,
              onStartReply,
              onCancelReply,
              onReplyDraftChange,
              onSubmitReply,
              onSelectElement,
              depth: depth + 1
            },
            `${reply.id}-${ri}`
          )) })
        ] }),
        isReplying && onCancelReply && onReplyDraftChange && onSubmitReply && /* @__PURE__ */ jsx(
          InspectorCommentReplyForm,
          {
            commentId: comment.id,
            authorName: comment.author ?? "",
            draft: replyDraft,
            mentions: replyDraftMentionsByCommentId[comment.id] ?? [],
            authors: commentAuthors,
            onDraftChange: onReplyDraftChange,
            onSubmit: onSubmitReply,
            onCancel: onCancelReply
          }
        )
      ]
    }
  );
}
function InspectorCommentsSection({
  comments,
  canEdit,
  activeSlide,
  selectedElement,
  editingCommentId,
  commentEditDraft,
  commentDraft,
  commentDraftMentions,
  commentAuthors,
  replyingToCommentId,
  replyDraftByCommentId,
  replyDraftMentionsByCommentId,
  onSetCommentDraft,
  onAddComment,
  onDeleteComment,
  onStartEditComment,
  onSaveEditComment,
  onCancelEditComment,
  onSetCommentEditDraft,
  onToggleCommentResolved,
  onStartReply,
  onCancelReply,
  onReplyDraftChange,
  onSubmitReply,
  onSelectElement
}) {
  const { t } = useTranslation();
  const HEADING_CLS = "text-[11px] uppercase tracking-wide text-muted-foreground";
  return /* @__PURE__ */ jsxs("div", { className: "space-y-3", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("div", { className: HEADING_CLS, children: t("pptx.comments.slideComments") }),
      /* @__PURE__ */ jsx("div", { className: "text-[11px] text-muted-foreground", children: comments.length })
    ] }),
    comments.length === 0 ? /* @__PURE__ */ jsx("div", { className: "text-xs text-muted-foreground", children: t("pptx.comments.noComments") }) : /* @__PURE__ */ jsx("div", { className: "space-y-2 max-h-[42vh] overflow-y-auto pr-1", children: comments.map((c, idx) => /* @__PURE__ */ jsx(
      InspectorCommentRow,
      {
        comment: c,
        canEdit,
        activeSlide,
        editingCommentId,
        commentEditDraft,
        replyingToCommentId,
        replyDraftByCommentId,
        replyDraftMentionsByCommentId,
        commentAuthors,
        onStartEditComment,
        onSaveEditComment,
        onCancelEditComment,
        onSetCommentEditDraft,
        onDeleteComment,
        onToggleCommentResolved,
        onStartReply,
        onCancelReply,
        onReplyDraftChange,
        onSubmitReply,
        onSelectElement
      },
      `${c.id}-${idx}`
    )) }),
    canEdit && /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 pt-1 border-t border-border", children: [
      selectedElement && /* @__PURE__ */ jsxs("div", { className: "inline-flex items-center gap-1 rounded bg-primary/20 px-1.5 py-0.5 text-[10px] text-primary", children: [
        /* @__PURE__ */ jsx(LuType, { className: "h-2.5 w-2.5" }),
        t("pptx.comments.commentingOn", {
          element: getElementLabel(selectedElement)
        })
      ] }),
      /* @__PURE__ */ jsx(
        CommentMentionTextarea,
        {
          value: commentDraft,
          mentions: commentDraftMentions,
          authors: commentAuthors,
          rows: 2,
          placeholder: selectedElement ? t("pptx.comments.addOnElement", {
            element: getElementLabel(selectedElement)
          }) : t("pptx.comments.addPlaceholder"),
          className: "w-full bg-muted border border-border rounded px-2 py-1 text-xs resize-y outline-none focus:border-primary",
          onChange: onSetCommentDraft,
          onSubmitShortcut: onAddComment
        }
      ),
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          onClick: onAddComment,
          disabled: !commentDraft.trim(),
          className: cn(
            "w-full inline-flex items-center justify-center gap-1 rounded px-2 py-1 text-[11px] font-medium transition-colors",
            commentDraft.trim() ? "bg-primary hover:bg-primary/80 text-white" : "bg-muted text-muted-foreground cursor-not-allowed"
          ),
          children: [
            /* @__PURE__ */ jsx(LuMessageSquare, { className: "w-3.5 h-3.5" }),
            t("pptx.comments.addComment")
          ]
        }
      )
    ] })
  ] });
}
function elementLabel(element) {
  const text = hasTextProperties(element) ? (element.text || "").slice(0, 24) : void 0;
  return text || element.type;
}
function InspectorElementsTab({
  activeSlide,
  selectedElementId,
  selectedElementIds,
  canEdit,
  onSelectElement,
  onUpdateSlide
}) {
  const { t } = useTranslation();
  const toggleHidden = (elementId) => {
    const elements2 = activeSlide?.elements;
    if (!canEdit || !elements2) {
      return;
    }
    const index = elements2.findIndex((el) => el.id === elementId);
    if (index === -1) {
      return;
    }
    const next = [...elements2];
    next[index] = { ...next[index], hidden: !next[index].hidden };
    onUpdateSlide({ elements: next });
  };
  if (!activeSlide) {
    return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
      /* @__PURE__ */ jsx("div", { className: cn(HEADING, "mb-2"), children: t("pptx.inspector.layerOrder") }),
      /* @__PURE__ */ jsx("div", { className: "text-muted-foreground italic", children: t("pptx.inspector.noSlideSelected") })
    ] });
  }
  const elements = activeSlide.elements || [];
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1", "data-pptx-elements-tab": true, children: [
    /* @__PURE__ */ jsx("div", { className: cn(HEADING, "mb-2"), children: t("pptx.inspector.layerOrder") }),
    [...elements].reverse().map((el, reversedIndex) => {
      const index = elements.length - 1 - reversedIndex;
      const selected = selectedElementId === el.id || selectedElementIds.includes(el.id);
      return /* @__PURE__ */ jsxs(
        "div",
        {
          title: `${el.type} - ${el.id}`,
          className: cn(
            "flex items-center gap-2 px-2 py-1 rounded cursor-pointer transition-colors",
            selected ? "bg-primary/30 text-primary" : "hover:bg-muted text-foreground"
          ),
          onClick: () => onSelectElement(el.id),
          children: [
            /* @__PURE__ */ jsx("span", { className: "text-muted-foreground w-4 text-right", children: index + 1 }),
            /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", children: elementLabel(el) }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                "data-pptx-element-visibility": el.id,
                "aria-pressed": Boolean(el.hidden),
                className: "text-muted-foreground hover:text-foreground disabled:opacity-40 disabled:cursor-not-allowed",
                title: el.hidden ? t("pptx.selectionPane.showElement") : t("pptx.selectionPane.hideElement"),
                onClick: (event) => {
                  event.stopPropagation();
                  toggleHidden(el.id);
                },
                children: el.hidden ? /* @__PURE__ */ jsx(LuEyeOff, { className: "w-3.5 h-3.5" }) : /* @__PURE__ */ jsx(LuEye, { className: "w-3.5 h-3.5 opacity-50" })
              }
            )
          ]
        },
        el.id
      );
    })
  ] });
}
function InspectorPaneHeader({
  activeTab,
  onSetActiveTab,
  onClose
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2 px-3 py-2 border-b border-border", children: [
    /* @__PURE__ */ jsx("div", { className: "flex items-center gap-1 rounded bg-muted p-0.5", children: INSPECTOR_TABS.map(({ key, label, icon: Icon }) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        title: label,
        className: cn(
          "flex items-center gap-1 px-2 py-1 rounded text-[11px] transition-colors",
          activeTab === key ? "bg-primary text-white" : "text-muted-foreground hover:text-foreground hover:bg-accent"
        ),
        onClick: () => onSetActiveTab(key),
        children: [
          /* @__PURE__ */ jsx(Icon, { className: "w-3.5 h-3.5" }),
          /* @__PURE__ */ jsx("span", { className: "hidden sm:inline", children: label })
        ]
      },
      key
    )) }),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onClose,
        title: t("pptx.common.close"),
        className: "p-1 rounded text-muted-foreground hover:text-foreground hover:bg-muted transition-colors",
        children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
      }
    )
  ] });
}
function InfoRow({ label, value }) {
  return /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2", children: [
    /* @__PURE__ */ jsx("span", { children: label }),
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: value })
  ] });
}
function TextFieldRow({
  label,
  disabled,
  value,
  onChange
}) {
  return /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: label }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "text",
        className: INPUT,
        disabled,
        value,
        onChange: (e) => onChange(e.target.value)
      }
    )
  ] });
}
function CustomPropertiesBlock({
  customProperties,
  canEdit,
  onUpdate
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.documentProperties.custom.heading") }),
      canEdit && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: BTN,
          onClick: () => onUpdate([
            ...customProperties,
            {
              name: `Property ${customProperties.length + 1}`,
              value: "",
              type: "lpwstr"
            }
          ]),
          children: t("pptx.documentProperties.custom.add")
        }
      )
    ] }),
    customProperties.length === 0 ? /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.documentProperties.custom.empty") }) : customProperties.map((entry, index) => /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[1fr,1fr,auto] gap-1", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          className: INPUT,
          disabled: !canEdit,
          value: entry.name,
          onChange: (e) => onUpdate(
            customProperties.map(
              (c, i) => i === index ? { ...c, name: e.target.value } : c
            )
          )
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          className: INPUT,
          disabled: !canEdit,
          value: entry.value,
          onChange: (e) => onUpdate(
            customProperties.map(
              (c, i) => i === index ? { ...c, value: e.target.value } : c
            )
          )
        }
      ),
      canEdit && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: cn(BTN, "px-1.5 text-red-400 hover:text-red-300"),
          onClick: () => onUpdate(customProperties.filter((_, i) => i !== index)),
          children: "\xD7"
        }
      )
    ] }, `${entry.name}-${index}`))
  ] });
}
function NotesHandoutCard({
  notesCanvasSize,
  notesMaster,
  handoutMaster
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.documentProperties.notesHandoutHeading") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1 text-[11px] text-muted-foreground", children: [
      /* @__PURE__ */ jsx(
        InfoRow,
        {
          label: t("pptx.documentProperties.notesSize"),
          value: notesCanvasSize ? `${notesCanvasSize.width} \xD7 ${notesCanvasSize.height}px` : t("pptx.digitalSignatures.notAvailable")
        }
      ),
      /* @__PURE__ */ jsx(
        InfoRow,
        {
          label: t("pptx.master.notesMasterTitle"),
          value: notesMaster ? `${notesMaster.placeholders?.length ?? 0} placeholders` : t("pptx.digitalSignatures.notAvailable")
        }
      ),
      /* @__PURE__ */ jsx(
        InfoRow,
        {
          label: t("pptx.master.handoutMasterTitle"),
          value: handoutMaster ? `${handoutMaster.placeholders?.length ?? 0} placeholders` : t("pptx.digitalSignatures.notAvailable")
        }
      )
    ] })
  ] });
}
function DocumentPropertiesCard({
  coreProperties,
  appProperties,
  customProperties,
  canEdit,
  onUpdateCoreProperties,
  onUpdateAppProperties,
  onUpdateCustomProperties
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.documentProperties.documentHeading") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-2 text-[11px] text-muted-foreground", children: [
      /* @__PURE__ */ jsx(
        TextFieldRow,
        {
          label: t("pptx.properties.titleLabel"),
          disabled: !canEdit,
          value: coreProperties?.title ?? "",
          onChange: (v) => onUpdateCoreProperties({ title: v })
        }
      ),
      /* @__PURE__ */ jsx(
        TextFieldRow,
        {
          label: t("pptx.properties.author"),
          disabled: !canEdit,
          value: coreProperties?.creator ?? "",
          onChange: (v) => onUpdateCoreProperties({ creator: v })
        }
      ),
      /* @__PURE__ */ jsx(
        TextFieldRow,
        {
          label: t("pptx.documentProperties.summary.company"),
          disabled: !canEdit,
          value: appProperties?.company ?? "",
          onChange: (v) => onUpdateAppProperties({ company: v })
        }
      ),
      /* @__PURE__ */ jsx(
        TextFieldRow,
        {
          label: t("pptx.documentProperties.statistics.application"),
          disabled: !canEdit,
          value: appProperties?.application ?? "",
          onChange: (v) => onUpdateAppProperties({ application: v })
        }
      ),
      /* @__PURE__ */ jsx(
        CustomPropertiesBlock,
        {
          customProperties,
          canEdit,
          onUpdate: onUpdateCustomProperties
        }
      )
    ] })
  ] });
}
function SlideSizeRescalePrompt({
  onChoose
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 rounded border border-border bg-muted/40 p-2 text-[11px]", children: [
    /* @__PURE__ */ jsx("div", { className: "font-medium", children: t("pptx.slideSize.rescaleTitle") }),
    /* @__PURE__ */ jsx("div", { className: "text-muted-foreground", children: t("pptx.slideSize.rescaleDescription") }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5 pt-1", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          "data-testid": "pptx-slide-size-rescale-maximize",
          title: t("pptx.slideSize.rescaleMaximizeHint"),
          className: "rounded bg-primary px-2 py-1 text-primary-foreground transition-colors hover:opacity-90",
          onClick: () => onChoose("maximize"),
          children: t("pptx.slideSize.rescaleMaximize")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          "data-testid": "pptx-slide-size-rescale-ensure-fit",
          title: t("pptx.slideSize.rescaleEnsureFitHint"),
          className: "rounded bg-muted px-2 py-1 transition-colors hover:bg-accent",
          onClick: () => onChoose("ensureFit"),
          children: t("pptx.slideSize.rescaleEnsureFit")
        }
      )
    ] })
  ] });
}
var CUSTOM_PRESET_VALUE = "";
function sizesDiffer(a, b) {
  return a.widthEmu !== b.widthEmu || a.heightEmu !== b.heightEmu;
}
function SlideSizeCard({
  canvasSize,
  slideSizeEmu,
  canEdit,
  onUpdate,
  onUpdateSlideSize,
  hasContent
}) {
  const { t } = useTranslation();
  const selection = resolveSlideSizeSelection({ current: slideSizeEmu, canvas: canvasSize });
  const presetDisabled = !canEdit || !onUpdateSlideSize;
  const [pendingSize, setPendingSize] = useState(null);
  const applyOrPromptSize = (nextSize) => {
    if (hasContent && sizesDiffer(selection.size, nextSize)) {
      setPendingSize(nextSize);
      return;
    }
    onUpdateSlideSize?.(nextSize);
  };
  const handlePreset = (labelKey) => {
    const preset = SLIDE_SIZE_PRESETS.find((candidate) => candidate.labelKey === labelKey);
    if (!preset) {
      return;
    }
    applyOrPromptSize(slideSizeFromPreset(preset, selection.orientation));
  };
  const handleOrientation = (orientation) => {
    applyOrPromptSize(withSlideSizeOrientation(selection.size, orientation));
  };
  const handleRescale = (mode) => {
    if (!pendingSize) {
      return;
    }
    onUpdateSlideSize?.(pendingSize, mode);
    setPendingSize(null);
  };
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.slideSize.title") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 text-[11px]", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.slideSize.presets") }),
        /* @__PURE__ */ jsxs(
          "select",
          {
            "aria-label": t("pptx.slideSize.presets"),
            "data-pptx-slide-size-preset": true,
            disabled: presetDisabled,
            className: INPUT,
            value: selection.preset?.labelKey ?? CUSTOM_PRESET_VALUE,
            onChange: (e) => handlePreset(e.target.value),
            children: [
              selection.preset === void 0 && /* @__PURE__ */ jsx("option", { value: CUSTOM_PRESET_VALUE, children: t("pptx.slideSize.customSize") }),
              SLIDE_SIZE_PRESETS.map((preset) => /* @__PURE__ */ jsx("option", { value: preset.labelKey, children: t(`pptx.slideSize.preset.${preset.labelKey}`) }, preset.labelKey))
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.slideSize.orientation") }),
        /* @__PURE__ */ jsxs(
          "select",
          {
            "aria-label": t("pptx.slideSize.orientation"),
            "data-pptx-slide-size-orientation": true,
            disabled: presetDisabled,
            className: INPUT,
            value: selection.orientation,
            onChange: (e) => handleOrientation(e.target.value),
            children: [
              /* @__PURE__ */ jsx("option", { value: "landscape", children: t("pptx.slideSize.landscape") }),
              /* @__PURE__ */ jsx("option", { value: "portrait", children: t("pptx.slideSize.portrait") })
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-1.5", children: [
        ["W", "width"],
        ["H", "height"]
      ].map(([label, key]) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: label }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            className: INPUT,
            disabled: !canEdit,
            value: canvasSize[key],
            onChange: (e) => onUpdate({ ...canvasSize, [key]: Number(e.target.value) })
          }
        )
      ] }, key)) }),
      pendingSize && /* @__PURE__ */ jsx(SlideSizeRescalePrompt, { onChoose: handleRescale })
    ] })
  ] });
}
function CheckboxRow({
  label,
  disabled,
  checked,
  onChange
}) {
  return /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
    /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: label }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "checkbox",
        disabled,
        checked,
        onChange: (e) => onChange(e.target.checked)
      }
    )
  ] });
}
function PresentationSettingsCard({
  presentationProperties,
  canEdit,
  onUpdate
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.slideInspector.presentation") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5 text-[11px]", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.presentationSettings.showType") }),
        /* @__PURE__ */ jsxs(
          "select",
          {
            "aria-label": t("pptx.presentationSettings.showType"),
            disabled: !canEdit,
            className: cn(INPUT, "w-28"),
            value: presentationProperties.showType ?? "presented",
            onChange: (e) => onUpdate({
              showType: e.target.value
            }),
            children: [
              /* @__PURE__ */ jsx("option", { value: "presented", children: t("pptx.presentationSettings.showTypePresented") }),
              /* @__PURE__ */ jsx("option", { value: "browsed", children: t("pptx.presentationSettings.showTypeBrowsed") }),
              /* @__PURE__ */ jsx("option", { value: "kiosk", children: t("pptx.presentationSettings.showTypeKiosk") })
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.loopContinuously"),
          disabled: !canEdit,
          checked: Boolean(presentationProperties.loopContinuously),
          onChange: (v) => onUpdate({ loopContinuously: v })
        }
      ),
      /* @__PURE__ */ jsx(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.showNarration"),
          disabled: !canEdit,
          checked: presentationProperties.showWithNarration !== false,
          onChange: (v) => onUpdate({ showWithNarration: v })
        }
      ),
      /* @__PURE__ */ jsx(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.showAnimation"),
          disabled: !canEdit,
          checked: presentationProperties.showWithAnimation !== false,
          onChange: (v) => onUpdate({ showWithAnimation: v })
        }
      ),
      /* @__PURE__ */ jsx(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.frameSlides"),
          disabled: !canEdit,
          checked: printPropertiesFrameSlides(presentationProperties.printProperties),
          onChange: (v) => onUpdate({
            printProperties: withFrameSlides(presentationProperties.printProperties, v)
          })
        }
      ),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.presentationSettings.slidesPerPage") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 1,
            max: 16,
            disabled: !canEdit,
            className: cn(INPUT, "w-20"),
            value: printPropertiesSlidesPerPage(presentationProperties.printProperties),
            onChange: (e) => onUpdate({
              printProperties: withSlidesPerPage(
                presentationProperties.printProperties,
                Number(e.target.value)
              )
            })
          }
        )
      ] })
    ] })
  ] });
}
function ThemeSelectorCard({
  themeOptions,
  selectedThemePath,
  setSelectedThemePath,
  canEdit,
  onApplyTheme
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.documentProperties.themeHeading") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-2 text-[11px]", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.documentProperties.themeHeading") }),
        /* @__PURE__ */ jsx(
          "select",
          {
            "aria-label": t("pptx.documentProperties.themeHeading"),
            disabled: themeOptions.length === 0,
            className: INPUT,
            value: selectedThemePath,
            onChange: (e) => setSelectedThemePath(e.target.value),
            children: themeOptions.length === 0 ? /* @__PURE__ */ jsx("option", { value: "", children: t("pptx.documentProperties.noThemesOption") }) : themeOptions.map((opt) => /* @__PURE__ */ jsx("option", { value: opt.path, children: opt.name || opt.path.split("/").pop() }, opt.path))
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-1.5", children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit || !selectedThemePath,
            onClick: () => onApplyTheme(selectedThemePath, false),
            children: t("pptx.documentProperties.applyFirstMaster")
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit || !selectedThemePath,
            onClick: () => onApplyTheme(selectedThemePath, true),
            children: t("pptx.documentProperties.applyAllMasters")
          }
        )
      ] })
    ] })
  ] });
}
var ALIAS_LABELS = {
  bg1: "Background 1",
  tx1: "Text 1",
  bg2: "Background 2",
  tx2: "Text 2",
  accent1: "Accent 1",
  accent2: "Accent 2",
  accent3: "Accent 3",
  accent4: "Accent 4",
  accent5: "Accent 5",
  accent6: "Accent 6",
  hlink: "Hyperlink",
  folHlink: "Followed Hyperlink"
};
function SlideThemeOverridePanel({
  activeSlide,
  theme,
  canEdit,
  onUpdateSlide
}) {
  const { t } = useTranslation();
  const translate = useCallback((key) => t(key), [t]);
  const override = activeSlide?.clrMapOverride;
  const isOverrideActive = override !== void 0;
  const targetSlotOptions = useMemo(() => [...THEME_COLOR_SCHEME_KEYS], []);
  const commitOverride = useCallback(
    (nextOverride) => {
      if (!activeSlide || !theme?.colorScheme) {
        onUpdateSlide({ clrMapOverride: nextOverride });
        return;
      }
      const nextSlide = applyThemeOverrideToSlide(activeSlide, theme.colorScheme, nextOverride);
      onUpdateSlide({
        clrMapOverride: nextSlide.clrMapOverride,
        backgroundColor: nextSlide.backgroundColor,
        elements: nextSlide.elements
      });
    },
    [activeSlide, onUpdateSlide, theme?.colorScheme]
  );
  const handleToggle = useCallback(
    (enabled) => {
      if (enabled) {
        const defaultMap = {};
        for (const key of COLOR_MAP_ALIAS_KEYS) {
          defaultMap[key] = DEFAULT_COLOR_MAP[key];
        }
        commitOverride(defaultMap);
      } else {
        commitOverride(void 0);
      }
    },
    [commitOverride]
  );
  const handleAliasChange = useCallback(
    (alias, targetSlot) => {
      const current = override ?? {};
      const next = { ...current, [alias]: targetSlot };
      for (const key of COLOR_MAP_ALIAS_KEYS) {
        if (!next[key]) {
          next[key] = DEFAULT_COLOR_MAP[key];
        }
      }
      commitOverride(next);
    },
    [commitOverride, override]
  );
  const resolveSlotColor = useCallback(
    (slot) => {
      if (!theme?.colorScheme) {
        return void 0;
      }
      const cs = theme.colorScheme;
      return cs[slot];
    },
    [theme]
  );
  if (!activeSlide) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
    /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-xs", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: isOverrideActive,
          onChange: (e) => handleToggle(e.target.checked)
        }
      ),
      t("pptx.themeOverride.enableOverride")
    ] }),
    isOverrideActive && override && /* @__PURE__ */ jsx("div", { className: "space-y-1.5", children: COLOR_MAP_ALIAS_KEYS.map((alias) => {
      const currentTarget = override[alias] ?? DEFAULT_COLOR_MAP[alias];
      const resolvedHex = resolveSlotColor(currentTarget);
      return /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "w-24 truncate text-muted-foreground", title: ALIAS_LABELS[alias], children: ALIAS_LABELS[alias] }),
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "h-4 w-4 rounded-sm border border-border shrink-0",
            style: {
              backgroundColor: resolvedHex ? `#${resolvedHex.replace(/^#/, "")}` : void 0
            }
          }
        ),
        /* @__PURE__ */ jsx(
          "select",
          {
            disabled: !canEdit,
            className: "flex-1 bg-muted border border-border rounded px-1 py-0.5 text-[11px]",
            value: currentTarget,
            onChange: (e) => handleAliasChange(alias, e.target.value),
            children: targetSlotOptions.map((slot) => /* @__PURE__ */ jsx("option", { value: slot, children: schemaLabel(THEME_COLOR_SLOT_LABEL_KEYS, slot, translate) }, slot))
          }
        )
      ] }, alias);
    }) })
  ] });
}
var DIR_ARROWS = TRANSITION_DIR_ARROWS;
function DirectionPicker({
  directions,
  value,
  disabled,
  onChange
}) {
  const { t } = useTranslation();
  if (directions.length <= 3) {
    return /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: directions.map((dir) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        disabled,
        onClick: () => onChange(dir),
        className: `px-2 py-1 rounded text-xs border ${value === dir ? "bg-primary text-white border-primary" : "bg-muted border-border hover:bg-accent"}`,
        title: t(`pptx.transition.dir.${dir}`, dir),
        children: DIR_ARROWS[dir] ?? dir
      },
      dir
    )) });
  }
  const cells = buildDirectionGrid(directions);
  return /* @__PURE__ */ jsx("div", { className: "inline-grid grid-cols-3 gap-0.5", children: cells.flatMap(
    (row, ri) => row.map((cell, ci) => {
      if (!cell) {
        return /* @__PURE__ */ jsx("div", { className: "w-6 h-6" }, `${ri}-${ci}`);
      }
      return /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled,
          onClick: () => onChange(cell),
          className: `w-6 h-6 rounded text-xs flex items-center justify-center border ${value === cell ? "bg-primary text-white border-primary" : "bg-muted border-border hover:bg-accent"}`,
          title: t(`pptx.transition.dir.${cell}`, cell),
          children: DIR_ARROWS[cell] ?? cell
        },
        cell
      );
    })
  ) });
}
function TransitionPreview({
  transition
}) {
  const previewRef = useRef(null);
  const [playing, setPlaying] = useState(false);
  const [animKey, setAnimKey] = useState(0);
  const { t } = useTranslation();
  const durationMs = transition.durationMs ?? 500;
  const animations = useMemo(
    () => getSlideTransitionAnimations(
      transition.type,
      durationMs,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern
    ),
    [
      transition.type,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern,
      durationMs
    ]
  );
  const handlePlay = useCallback(() => {
    setPlaying(true);
    setAnimKey((k) => k + 1);
  }, []);
  useEffect(() => {
    if (!playing) {
      return;
    }
    const timer = window.setTimeout(() => setPlaying(false), durationMs + 100);
    return () => {
      window.clearTimeout(timer);
    };
  }, [playing, durationMs, animKey]);
  if (transition.type === "none" || transition.type === "cut") {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
    /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.transition.preview") }),
    /* @__PURE__ */ jsxs(
      "div",
      {
        ref: previewRef,
        className: "relative w-full h-16 rounded border border-border overflow-hidden bg-muted cursor-pointer",
        onClick: handlePlay,
        title: t("pptx.transition.preview"),
        children: [
          /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute inset-0 bg-primary/20 flex items-center justify-center text-[9px] text-muted-foreground",
              style: {
                animation: playing && animations.incoming !== "none" ? animations.incoming : void 0
              },
              children: "B"
            },
            `in-${animKey}`
          ),
          /* @__PURE__ */ jsx(
            "div",
            {
              className: "absolute inset-0 bg-card flex items-center justify-center text-[9px] text-muted-foreground",
              style: {
                zIndex: animations.outgoingOnTop ? 2 : 0,
                animation: playing && animations.outgoing !== "none" ? animations.outgoing : !playing ? void 0 : `pptx-tr-fade-out ${durationMs}ms ease-in-out forwards`
              },
              children: "A"
            },
            `out-${animKey}`
          )
        ]
      }
    ),
    /* @__PURE__ */ jsx("style", { children: SLIDE_TRANSITION_KEYFRAMES })
  ] });
}
var DEFAULT_DURATION_MS = 320;
var MAX_DURATION_MS = 1e4;
var MIN_SPOKES = 1;
var MAX_SPOKES = 8;
function SlideTransitionSection({
  activeSlide,
  canEdit = true,
  onTransitionChange
}) {
  const { t } = useTranslation();
  if (!activeSlide) {
    return null;
  }
  const transitionType = activeSlide.transition?.type ?? "none";
  const validDirections = TRANSITION_VALID_DIRECTIONS[transitionType];
  const hasDirections = validDirections !== void 0 && validDirections.length > 0;
  const usesOrientation = TRANSITION_ORIENTATION_TYPES.has(transitionType);
  const isWheel = transitionType === "wheel";
  const isMorph = transitionType === "morph";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "mb-3 rounded border border-border bg-card p-2 space-y-2",
      "data-pptx-slide-transition": true,
      children: [
        /* @__PURE__ */ jsx("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.slideInspector.slideTransition") }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.type") }),
          /* @__PURE__ */ jsx(
            "select",
            {
              value: activeSlide.transition?.type || "none",
              disabled: !canEdit,
              "aria-label": t("pptx.transition.type"),
              onChange: (e) => onTransitionChange({
                type: e.target.value
              }),
              className: "bg-muted border border-border rounded px-2 py-1",
              children: SLIDE_TRANSITION_OPTIONS.map((option) => /* @__PURE__ */ jsx("option", { value: option.value, children: t(option.i18nKey) }, option.value))
            }
          )
        ] }),
        hasDirections && !usesOrientation && /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.direction") }),
          /* @__PURE__ */ jsx(
            DirectionPicker,
            {
              directions: validDirections,
              value: activeSlide.transition?.direction,
              disabled: !canEdit,
              onChange: (dir) => onTransitionChange({ direction: dir })
            }
          )
        ] }),
        usesOrientation && /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.orientation") }),
          /* @__PURE__ */ jsx("div", { className: "flex gap-1", children: ["horz", "vert"].map((o) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              disabled: !canEdit,
              onClick: () => onTransitionChange({ orient: o }),
              className: `px-2 py-1 rounded text-xs border ${(activeSlide.transition?.orient ?? "horz") === o ? "bg-primary text-white border-primary" : "bg-muted border-border hover:bg-accent"}`,
              children: t(
                o === "horz" ? "pptx.slideInspector.horizontal" : "pptx.slideInspector.vertical"
              )
            },
            o
          )) })
        ] }),
        isWheel && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.spokes") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: MIN_SPOKES,
              max: MAX_SPOKES,
              disabled: !canEdit,
              value: activeSlide.transition?.spokes ?? 4,
              onChange: (e) => {
                const spokes = clampTransitionNumber(Number(e.target.value), MIN_SPOKES, MAX_SPOKES);
                if (spokes !== null) {
                  onTransitionChange({ spokes });
                }
              },
              className: "bg-muted border border-border rounded px-2 py-1 text-xs w-16"
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.duration") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 0,
              max: MAX_DURATION_MS,
              disabled: !canEdit,
              "aria-label": t("pptx.transition.duration"),
              value: Math.round(activeSlide.transition?.durationMs || DEFAULT_DURATION_MS),
              onChange: (e) => {
                const durationMs = clampTransitionNumber(Number(e.target.value), 0, MAX_DURATION_MS);
                if (durationMs !== null) {
                  onTransitionChange({ durationMs });
                }
              },
              className: "bg-muted border border-border rounded px-2 py-1"
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.speed") }),
          /* @__PURE__ */ jsx(
            "select",
            {
              value: activeSlide.transition?.speed ?? "fast",
              disabled: !canEdit,
              "aria-label": t("pptx.transition.speed"),
              onChange: (e) => onTransitionChange({
                speed: e.target.value
              }),
              className: "bg-muted border border-border rounded px-2 py-1",
              children: TRANSITION_SPEED_OPTIONS.map((option) => /* @__PURE__ */ jsx("option", { value: option.value, children: t(option.i18nKey) }, option.value))
            }
          )
        ] }),
        isMorph && /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.morphOption") }),
          /* @__PURE__ */ jsx(
            "select",
            {
              value: activeSlide.transition?.morphOption ?? "byObject",
              disabled: !canEdit,
              "aria-label": t("pptx.transition.morphOption"),
              onChange: (e) => onTransitionChange({
                morphOption: e.target.value
              }),
              className: "bg-muted border border-border rounded px-2 py-1",
              children: TRANSITION_MORPH_OPTIONS.map((option) => /* @__PURE__ */ jsx("option", { value: option.value, children: t(option.i18nKey) }, option.value))
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "inline-flex items-center gap-2 text-foreground text-xs", children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: activeSlide.transition?.advanceOnClick !== false,
              onChange: (e) => onTransitionChange({ advanceOnClick: e.target.checked })
            }
          ),
          t("pptx.transition.advanceOnClick")
        ] }),
        activeSlide.transition?.soundFileName && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1 text-xs text-muted-foreground", children: [
          /* @__PURE__ */ jsxs("span", { className: "text-muted-foreground", children: [
            t("pptx.transition.sound"),
            ":"
          ] }),
          /* @__PURE__ */ jsx("span", { className: "text-foreground truncate", title: activeSlide.transition.soundFileName, children: activeSlide.transition.soundFileName })
        ] }),
        activeSlide.transition && /* @__PURE__ */ jsx(TransitionPreview, { transition: activeSlide.transition })
      ]
    }
  );
}
function TagsSection({
  tagCollections,
  onUpdateTagCollections,
  canEdit
}) {
  const { t } = useTranslation();
  const [collapsed, setCollapsed] = useState(true);
  const allTags = flattenTagCollections(tagCollections);
  const updateTag = (colIdx, tagIdx, field, newValue) => {
    onUpdateTagCollections(updateTagInCollections(tagCollections, colIdx, tagIdx, field, newValue));
  };
  const deleteTag = (colIdx, tagIdx) => {
    onUpdateTagCollections(deleteTagFromCollections(tagCollections, colIdx, tagIdx));
  };
  const addTag = () => {
    onUpdateTagCollections(addTagToCollections(tagCollections));
  };
  return /* @__PURE__ */ jsxs("div", { className: CARD, children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        className: "flex items-center gap-1 w-full",
        onClick: () => setCollapsed(!collapsed),
        children: [
          collapsed ? /* @__PURE__ */ jsx(LuChevronRight, { className: "w-3 h-3 text-muted-foreground" }) : /* @__PURE__ */ jsx(LuChevronDown, { className: "w-3 h-3 text-muted-foreground" }),
          /* @__PURE__ */ jsx("span", { className: HEADING, children: t("pptx.tags.title") }),
          /* @__PURE__ */ jsx("span", { className: "ml-auto text-[10px] text-muted-foreground", children: allTags.length })
        ]
      }
    ),
    !collapsed && /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      allTags.length === 0 ? /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.tags.noTags") }) : allTags.map((tag, idx) => /* @__PURE__ */ jsxs(
        "div",
        {
          className: "grid grid-cols-[1fr,1fr,auto] gap-1 text-[11px]",
          children: [
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                className: INPUT,
                disabled: !canEdit,
                placeholder: t("pptx.tags.name"),
                value: tag.name,
                onChange: (e) => updateTag(tag.colIdx, tag.tagIdx, "name", e.target.value)
              }
            ),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                className: INPUT,
                disabled: !canEdit,
                placeholder: t("pptx.tags.value"),
                value: tag.value,
                onChange: (e) => updateTag(tag.colIdx, tag.tagIdx, "value", e.target.value)
              }
            ),
            canEdit && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                className: cn(BTN, "px-1.5 text-red-400 hover:text-red-300"),
                title: t("pptx.tags.deleteTag"),
                onClick: () => deleteTag(tag.colIdx, tag.tagIdx),
                children: /* @__PURE__ */ jsx(LuTrash2, { className: "w-3 h-3" })
              }
            )
          ]
        },
        `${tag.colIdx}-${tag.tagIdx}-${idx}`
      )),
      canEdit && /* @__PURE__ */ jsx("button", { type: "button", className: BTN, onClick: addTag, children: t("pptx.tags.addTag") })
    ] })
  ] });
}
function PresentationPropertiesPanel({
  presentationProperties,
  canEdit,
  onUpdatePresentationProperties,
  themeOptions,
  selectedThemePath,
  setSelectedThemePath,
  onApplyTheme,
  activeSlide,
  theme,
  onUpdateSlide,
  canvasSize,
  onUpdateCanvasSize,
  slideSizeEmu,
  onUpdateSlideSize,
  hasContent,
  notesCanvasSize,
  notesMaster,
  handoutMaster,
  coreProperties,
  onUpdateCoreProperties,
  appProperties,
  onUpdateAppProperties,
  customProperties,
  onUpdateCustomProperties,
  tagCollections,
  onUpdateTagCollections
}) {
  const { t } = useTranslation();
  const handleTransitionChange = React20.useCallback(
    (updates) => {
      onUpdateSlide({
        transition: mergeSlideTransition(activeSlide?.transition, updates)
      });
    },
    [activeSlide?.transition, onUpdateSlide]
  );
  return /* @__PURE__ */ jsxs("div", { className: "space-y-3", children: [
    /* @__PURE__ */ jsx(
      PresentationSettingsCard,
      {
        presentationProperties,
        canEdit,
        onUpdate: onUpdatePresentationProperties
      }
    ),
    /* @__PURE__ */ jsx(
      ThemeSelectorCard,
      {
        themeOptions,
        selectedThemePath,
        setSelectedThemePath,
        canEdit,
        onApplyTheme
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: CARD, children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.themeOverride.heading") }),
      /* @__PURE__ */ jsx(
        SlideThemeOverridePanel,
        {
          activeSlide,
          theme,
          canEdit,
          onUpdateSlide
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      SlideSizeCard,
      {
        canvasSize,
        slideSizeEmu,
        canEdit,
        onUpdate: onUpdateCanvasSize,
        onUpdateSlideSize,
        hasContent
      }
    ),
    /* @__PURE__ */ jsx(
      SlideTransitionSection,
      {
        activeSlide: activeSlide ?? null,
        canEdit,
        onTransitionChange: handleTransitionChange
      }
    ),
    /* @__PURE__ */ jsx(
      NotesHandoutCard,
      {
        notesCanvasSize,
        notesMaster,
        handoutMaster
      }
    ),
    /* @__PURE__ */ jsx(
      DocumentPropertiesCard,
      {
        coreProperties,
        appProperties,
        customProperties,
        canEdit,
        onUpdateCoreProperties,
        onUpdateAppProperties,
        onUpdateCustomProperties
      }
    ),
    tagCollections && onUpdateTagCollections && /* @__PURE__ */ jsx(
      TagsSection,
      {
        tagCollections,
        onUpdateTagCollections,
        canEdit
      }
    ),
    activeSlide && /* @__PURE__ */ jsxs("div", { className: cn(CARD, "space-y-1"), children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: "Slide" }),
      /* @__PURE__ */ jsxs("div", { className: "text-[11px] text-muted-foreground", children: [
        activeSlide.elements?.length ?? 0,
        " elements"
      ] })
    ] })
  ] });
}
function SlideBackgroundPanel({
  activeSlide,
  canEdit,
  onUpdateSlide,
  editTemplateMode,
  slideMasters,
  onSetTemplateBackground,
  onGetTemplateBackgroundColor
}) {
  const { t } = useTranslation();
  const bgImageInputRef = useRef(null);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: cn(CARD, "space-y-2"), children: [
      /* @__PURE__ */ jsx("div", { className: HEADING, children: "Background" }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground w-10 shrink-0", children: "Colour" }),
        /* @__PURE__ */ jsx(
          DebouncedColorInput,
          {
            value: normalizeHexColor(activeSlide.backgroundColor, "#ffffff"),
            disabled: !canEdit,
            className: "h-6 w-8 rounded border border-border bg-muted cursor-pointer",
            onCommit: (hex) => onUpdateSlide({ backgroundColor: hex })
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[10px] truncate", children: activeSlide.backgroundColor || "none" })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "space-y-1", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-[11px]", children: [
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground w-10 shrink-0", children: "Image" }),
          /* @__PURE__ */ jsx(
            "input",
            {
              ref: bgImageInputRef,
              type: "file",
              accept: "image/png,image/jpeg,image/gif,image/webp,image/svg+xml",
              className: "hidden",
              disabled: !canEdit,
              onChange: (e) => {
                const file = e.target.files?.[0];
                if (!file) {
                  return;
                }
                const reader = new FileReader();
                reader.onload = () => {
                  if (typeof reader.result === "string") {
                    onUpdateSlide({ backgroundImage: reader.result });
                  }
                };
                reader.readAsDataURL(file);
                e.target.value = "";
              }
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: cn(BTN, "flex-1 text-center"),
              disabled: !canEdit,
              onClick: () => bgImageInputRef.current?.click(),
              children: activeSlide.backgroundImage ? t("pptx.slideBackground.replaceImage") : t("pptx.slideBackground.chooseImage")
            }
          )
        ] }),
        activeSlide.backgroundImage && /* @__PURE__ */ jsxs("div", { className: "relative mt-1", children: [
          /* @__PURE__ */ jsx(
            "img",
            {
              src: activeSlide.backgroundImage,
              alt: t("pptx.slideBackground.backgroundPreview"),
              className: "w-full h-16 object-cover rounded border border-border"
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              className: "absolute top-0.5 right-0.5 rounded bg-background/80 hover:bg-red-700 p-0.5 text-[10px] transition-colors",
              disabled: !canEdit,
              title: t("pptx.slideBackground.removeBackgroundImage"),
              onClick: () => onUpdateSlide({ backgroundImage: void 0 }),
              children: /* @__PURE__ */ jsx(LuX, { className: "w-3 h-3" })
            }
          )
        ] })
      ] }),
      (activeSlide.backgroundColor || activeSlide.backgroundImage || activeSlide.backgroundGradient) && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: cn(BTN, "w-full text-center text-red-400 hover:text-red-300"),
          disabled: !canEdit,
          onClick: () => onUpdateSlide({
            backgroundColor: void 0,
            backgroundImage: void 0,
            backgroundGradient: void 0
          }),
          children: t("pptx.slideBackground.clearBackground")
        }
      ),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "checkbox",
            checked: activeSlide.showMasterShapes === false,
            disabled: !canEdit,
            onChange: (e) => onUpdateSlide({ showMasterShapes: !e.target.checked })
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.slideBackground.hideBackgroundGraphics") })
      ] })
    ] }),
    editTemplateMode && onSetTemplateBackground && onGetTemplateBackgroundColor && /* @__PURE__ */ jsx(
      TemplateBackgroundCard,
      {
        activeSlide,
        slideMasters,
        canEdit,
        onSetTemplateBackground,
        onGetTemplateBackgroundColor
      }
    )
  ] });
}
function TemplateBackgroundCard({
  activeSlide,
  slideMasters,
  canEdit,
  onSetTemplateBackground,
  onGetTemplateBackgroundColor
}) {
  const { t } = useTranslation();
  const rows = resolveTemplateBackgroundRows(
    activeSlide,
    slideMasters,
    t("pptx.master.layout"),
    t("pptx.master.master")
  );
  return /* @__PURE__ */ jsxs("div", { className: cn(CARD, "space-y-2"), children: [
    /* @__PURE__ */ jsx("div", { className: HEADING, children: t("pptx.slideBackground.templateBackgroundsHeading") }),
    rows.layout && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground w-14 shrink-0 truncate", title: rows.layout.title, children: t("pptx.master.layout") }),
      /* @__PURE__ */ jsx(
        DebouncedColorInput,
        {
          value: normalizeHexColor(onGetTemplateBackgroundColor(rows.layout.path), "#ffffff"),
          disabled: !canEdit,
          className: "h-6 w-8 rounded border border-border bg-muted cursor-pointer",
          onCommit: (hex) => onSetTemplateBackground(rows.layout.path, hex)
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[10px] truncate", children: rows.layout.label })
    ] }),
    rows.master && /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-[11px]", children: [
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground w-14 shrink-0 truncate", title: rows.master.title, children: t("pptx.master.master") }),
      /* @__PURE__ */ jsx(
        DebouncedColorInput,
        {
          value: normalizeHexColor(onGetTemplateBackgroundColor(rows.master.path), "#ffffff"),
          disabled: !canEdit,
          className: "h-6 w-8 rounded border border-border bg-muted cursor-pointer",
          onCommit: (hex) => onSetTemplateBackground(rows.master.path, hex)
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "text-muted-foreground text-[10px] truncate", children: rows.master.label })
    ] })
  ] });
}
var ANIMATION_PANEL_MIN_HEIGHT = 80;
var ANIMATION_PANEL_MAX_HEIGHT = 500;
var ANIMATION_PANEL_DEFAULT_HEIGHT = 220;
function useInspectorPaneState(themeOptions, slideMasters, theme) {
  const activeThemePath = slideMasters?.[0]?.themePath;
  const effectiveThemeOptions = useMemo(() => {
    if (themeOptions.length > 0 || !activeThemePath) {
      return themeOptions;
    }
    return [{ path: activeThemePath, name: theme?.name }];
  }, [activeThemePath, theme?.name, themeOptions]);
  const [selectedThemePath, setSelectedThemePath] = useState("");
  useEffect(() => {
    setSelectedThemePath(activeThemePath ?? effectiveThemeOptions[0]?.path ?? "");
  }, [activeThemePath, effectiveThemeOptions]);
  const [animationPanelHeight, setAnimationPanelHeight] = useState(ANIMATION_PANEL_DEFAULT_HEIGHT);
  const onResizeAnimationPanel = useCallback((delta) => {
    setAnimationPanelHeight(
      (height) => Math.min(ANIMATION_PANEL_MAX_HEIGHT, Math.max(ANIMATION_PANEL_MIN_HEIGHT, height - delta))
    );
  }, []);
  return {
    animationPanelHeight,
    effectiveThemeOptions,
    onResizeAnimationPanel,
    selectedThemePath,
    setSelectedThemePath
  };
}
function InspectorPane(props) {
  const {
    isOpen,
    canEdit,
    activeSlide,
    slides,
    customShows,
    canvasSize,
    selectedElement,
    selectedElementIds,
    tableEditorState,
    activeTab,
    onSetActiveTab,
    onClose,
    onUpdateElement,
    onSelectElement,
    onMoveLayer,
    presentationProperties,
    onUpdatePresentationProperties,
    notesMaster,
    handoutMaster,
    notesCanvasSize,
    coreProperties,
    appProperties,
    customProperties,
    themeOptions,
    onUpdateCoreProperties,
    onUpdateAppProperties,
    onUpdateCustomProperties,
    tagCollections,
    onUpdateTagCollections,
    onApplyTheme,
    comments,
    commentDraft,
    commentDraftMentions,
    commentAuthors,
    editingCommentId,
    commentEditDraft,
    onSetCommentDraft,
    onAddComment,
    onDeleteComment,
    onStartEditComment,
    onSaveEditComment,
    onCancelEditComment,
    onSetCommentEditDraft,
    onToggleCommentResolved,
    onStartReply,
    onCancelReply,
    onReplyDraftChange,
    onSubmitReply,
    replyingToCommentId,
    replyDraftByCommentId,
    replyDraftMentionsByCommentId,
    onUpdateCanvasSize,
    slideSizeEmu,
    onUpdateSlideSize,
    onUpdateElementStyle,
    onUpdateTextStyle,
    onUpdateSlide,
    editTemplateMode,
    slideMasters,
    onSetTemplateBackground,
    onGetTemplateBackgroundColor,
    mediaDataUrls,
    theme,
    tableStyleMap,
    onTableStyleMapChange,
    onDeleteTableStyle,
    panelWidth
  } = props;
  const hasSelection = selectedElement !== null;
  const { t } = useTranslation();
  const { dragY, handlers: dragHandlers } = useSheetDismissDrag(onClose);
  const {
    animationPanelHeight,
    effectiveThemeOptions,
    onResizeAnimationPanel,
    selectedThemePath,
    setSelectedThemePath
  } = useInspectorPaneState(themeOptions, slideMasters, theme);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    isOpen && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.common.close"),
        onClick: onClose,
        className: "md:hidden fixed inset-0 z-20 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150"
      }
    ),
    /* @__PURE__ */ jsxs(
      "div",
      {
        className: cn(
          // Shared styles
          "bg-background flex flex-col text-xs text-foreground shadow-xl",
          // Mobile: absolute bottom sheet overlay sized via dvh so it
          // adapts to the on-screen keyboard / dynamic browser chrome.
          "max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[75dvh] max-md:rounded-t-2xl max-md:border-t max-md:border-border max-md:z-30 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
          "max-md:transition-transform max-md:duration-200 max-md:ease-in-out",
          isOpen ? "max-md:translate-y-0" : "max-md:translate-y-full",
          // Desktop: flow-based flex child (takes space from canvas)
          "md:h-full md:border-l md:border-border",
          !panelWidth && "md:w-72"
        ),
        style: {
          ...panelWidth ? { width: panelWidth } : {},
          ...dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : {}
        },
        children: [
          /* @__PURE__ */ jsx(
            "div",
            {
              className: "md:hidden flex items-center justify-center pt-3 pb-2 cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: dragHandlers.onPointerDown,
              onPointerMove: dragHandlers.onPointerMove,
              onPointerUp: dragHandlers.onPointerUp,
              onPointerCancel: dragHandlers.onPointerCancel,
              children: /* @__PURE__ */ jsx("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" })
            }
          ),
          /* @__PURE__ */ jsx(
            InspectorPaneHeader,
            {
              activeTab,
              onSetActiveTab,
              onClose
            }
          ),
          /* @__PURE__ */ jsxs("div", { className: "flex-1 overflow-y-auto p-3 space-y-3", children: [
            activeTab === "elements" && /* @__PURE__ */ jsx(
              InspectorElementsTab,
              {
                activeSlide,
                selectedElementId: selectedElement?.id ?? null,
                selectedElementIds,
                canEdit,
                onSelectElement,
                onUpdateSlide
              }
            ),
            activeTab === "properties" && /* @__PURE__ */ jsx("div", { className: "space-y-3", children: hasSelection && selectedElement ? /* @__PURE__ */ jsx(
              ElementInspectorBody,
              {
                selectedElement,
                canEdit,
                slides,
                customShows,
                tableEditorState,
                mediaDataUrls,
                tableStyleMap,
                onTableStyleMapChange,
                onDeleteTableStyle,
                onUpdateElement,
                onUpdateElementStyle,
                onUpdateTextStyle,
                onMoveLayer
              }
            ) : /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(
                PresentationPropertiesPanel,
                {
                  canEdit,
                  canvasSize,
                  presentationProperties,
                  onUpdatePresentationProperties,
                  notesMaster,
                  handoutMaster,
                  notesCanvasSize,
                  coreProperties,
                  appProperties,
                  customProperties,
                  themeOptions: effectiveThemeOptions,
                  selectedThemePath,
                  setSelectedThemePath,
                  onApplyTheme,
                  onUpdateCoreProperties,
                  onUpdateAppProperties,
                  onUpdateCustomProperties,
                  tagCollections,
                  onUpdateTagCollections,
                  onUpdateCanvasSize,
                  slideSizeEmu,
                  onUpdateSlideSize,
                  hasContent: slides.some((slide) => slide.elements.length > 0),
                  activeSlide,
                  theme,
                  onUpdateSlide
                }
              ),
              activeSlide && /* @__PURE__ */ jsx(
                SlideBackgroundPanel,
                {
                  activeSlide,
                  canEdit,
                  onUpdateSlide,
                  editTemplateMode,
                  slideMasters,
                  onSetTemplateBackground,
                  onGetTemplateBackgroundColor
                }
              )
            ] }) }),
            activeTab === "comments" && /* @__PURE__ */ jsx(
              InspectorCommentsSection,
              {
                comments,
                canEdit,
                activeSlide,
                selectedElement,
                editingCommentId,
                commentEditDraft,
                commentDraft,
                commentDraftMentions: commentDraftMentions ?? [],
                commentAuthors: commentAuthors ?? [],
                replyingToCommentId: replyingToCommentId ?? null,
                replyDraftByCommentId: replyDraftByCommentId ?? {},
                replyDraftMentionsByCommentId: replyDraftMentionsByCommentId ?? {},
                onSetCommentDraft,
                onAddComment,
                onDeleteComment,
                onStartEditComment,
                onSaveEditComment,
                onCancelEditComment,
                onSetCommentEditDraft,
                onToggleCommentResolved,
                onStartReply,
                onCancelReply,
                onReplyDraftChange,
                onSubmitReply,
                onSelectElement
              }
            )
          ] }),
          hasSelection && selectedElement && activeSlide && /* @__PURE__ */ jsxs(Fragment, { children: [
            /* @__PURE__ */ jsx(ResizeHandle, { direction: "vertical", onResize: onResizeAnimationPanel }),
            /* @__PURE__ */ jsx(
              "div",
              {
                className: "border-t border-border p-3 overflow-y-auto flex-shrink-0",
                style: { height: animationPanelHeight },
                children: /* @__PURE__ */ jsx(
                  AnimationPanel,
                  {
                    selectedElement,
                    activeSlide,
                    canEdit,
                    onUpdateSlide
                  }
                )
              }
            )
          ] })
        ]
      }
    )
  ] });
}
function PresentationAnnotationOverlay({
  canvasSize,
  editorScale,
  presentationTool,
  blackout,
  annotationStrokes,
  currentStroke,
  laserPosition,
  onPointerDown,
  onPointerMove,
  onPointerUp,
  onLaserMove,
  onLaserLeave,
  onEraseAtPoint
}) {
  const svgRef = useRef(null);
  const isErasingRef = useRef(false);
  const toSlideCoords = useCallback(
    (clientX, clientY) => {
      const svg = svgRef.current;
      if (!svg) {
        return null;
      }
      const rect = svg.getBoundingClientRect();
      const x = (clientX - rect.left) / editorScale;
      const y = (clientY - rect.top) / editorScale;
      return { x, y };
    },
    [editorScale]
  );
  const handlePointerDown = useCallback(
    (e) => {
      if (presentationTool === "none") {
        return;
      }
      e.preventDefault();
      e.stopPropagation();
      const coords = toSlideCoords(e.clientX, e.clientY);
      if (!coords) {
        return;
      }
      if (presentationTool === "eraser") {
        isErasingRef.current = true;
        onEraseAtPoint(coords.x, coords.y);
        return;
      }
      if (presentationTool === "pen" || presentationTool === "highlighter") {
        onPointerDown(coords.x, coords.y);
      }
    },
    [presentationTool, toSlideCoords, onPointerDown, onEraseAtPoint]
  );
  const handlePointerMove = useCallback(
    (e) => {
      if (presentationTool === "none") {
        return;
      }
      const coords = toSlideCoords(e.clientX, e.clientY);
      if (!coords) {
        return;
      }
      if (presentationTool === "laser") {
        onLaserMove(coords.x, coords.y);
        return;
      }
      if (presentationTool === "eraser" && isErasingRef.current) {
        onEraseAtPoint(coords.x, coords.y);
        return;
      }
      if (presentationTool === "pen" || presentationTool === "highlighter") {
        onPointerMove(coords.x, coords.y);
      }
    },
    [presentationTool, toSlideCoords, onPointerMove, onLaserMove, onEraseAtPoint]
  );
  const handlePointerUp = useCallback(
    (e) => {
      if (presentationTool === "none") {
        return;
      }
      e.preventDefault();
      if (presentationTool === "eraser") {
        isErasingRef.current = false;
        return;
      }
      onPointerUp();
    },
    [presentationTool, onPointerUp]
  );
  const handlePointerLeave = useCallback(() => {
    if (presentationTool === "laser") {
      onLaserLeave();
    }
    if (presentationTool === "eraser") {
      isErasingRef.current = false;
    }
    onPointerUp();
  }, [presentationTool, onLaserLeave, onPointerUp]);
  const allStrokes = currentStroke ? [...annotationStrokes, currentStroke] : annotationStrokes;
  const isCapturing = presentationTool !== "none";
  if (!isCapturing && allStrokes.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-pptx-annotation-overlay": true,
      className: "absolute inset-0",
      style: {
        zIndex: annotationOverlayZIndex(blackout),
        cursor: cursorForTool(presentationTool),
        pointerEvents: isCapturing ? "auto" : "none"
      },
      children: [
        /* @__PURE__ */ jsx(
          "svg",
          {
            ref: svgRef,
            className: "absolute",
            style: {
              width: canvasSize.width,
              height: canvasSize.height,
              transformOrigin: "top left",
              transform: `scale(${editorScale})`
            },
            viewBox: `0 0 ${canvasSize.width} ${canvasSize.height}`,
            onPointerDown: handlePointerDown,
            onPointerMove: handlePointerMove,
            onPointerUp: handlePointerUp,
            onPointerLeave: handlePointerLeave,
            children: allStrokes.map((stroke) => /* @__PURE__ */ jsx(
              "path",
              {
                d: buildStrokePathD(stroke.points),
                fill: "none",
                stroke: stroke.color,
                strokeWidth: stroke.width,
                strokeLinecap: "round",
                strokeLinejoin: "round",
                opacity: stroke.opacity
              },
              stroke.id
            ))
          }
        ),
        presentationTool === "laser" && laserPosition && /* @__PURE__ */ jsx(
          "div",
          {
            className: "absolute rounded-full pointer-events-none",
            style: {
              width: 24,
              height: 24,
              left: laserPosition.x * editorScale - 12,
              top: laserPosition.y * editorScale - 12,
              backgroundColor: "rgba(255, 0, 0, 0.85)",
              boxShadow: "0 0 12px 6px rgba(255, 0, 0, 0.5), 0 0 24px 12px rgba(255, 0, 0, 0.25)",
              filter: "drop-shadow(0 0 8px rgba(255, 0, 0, 0.7))",
              zIndex: 70
            }
          }
        )
      ]
    }
  );
}
function PresentationToolbar({
  presentationTool,
  penColor,
  highlighterColor,
  hasAnnotations,
  onSetTool,
  onSetPenColor,
  onSetHighlighterColor,
  onClearAnnotations,
  blackout,
  onToggleBlackboard,
  currentSlideIndex,
  totalSlides,
  onMovePresentationSlide,
  presentationStartTime,
  onEndPresentation,
  onTogglePresenterView,
  presenterMode
}) {
  const { t } = useTranslation();
  const [showPenColors, setShowPenColors] = useState(false);
  const [showHighlighterColors, setShowHighlighterColors] = useState(false);
  const toolbarRef = useRef(null);
  const [now, setNow] = useState(Date.now());
  useEffect(() => {
    if (!presentationStartTime) {
      return;
    }
    const interval = window.setInterval(() => setNow(Date.now()), 1e3);
    return () => window.clearInterval(interval);
  }, [presentationStartTime]);
  const elapsed = presentationStartTime ? now - presentationStartTime : 0;
  useEffect(() => {
    const handler = (e) => {
      if (toolbarRef.current && !toolbarRef.current.contains(e.target)) {
        setShowPenColors(false);
        setShowHighlighterColors(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, []);
  const handleToolClick = useCallback(
    (tool) => {
      onSetTool(tool);
      setShowPenColors(false);
      setShowHighlighterColors(false);
    },
    [onSetTool]
  );
  const handlePenRightClick = useCallback((e) => {
    e.preventDefault();
    setShowPenColors((prev) => !prev);
    setShowHighlighterColors(false);
  }, []);
  const handleHighlighterRightClick = useCallback((e) => {
    e.preventDefault();
    setShowHighlighterColors((prev) => !prev);
    setShowPenColors(false);
  }, []);
  const toolBtnClass = (tool) => presentationTool === tool ? PRESENT_TOOLBAR_CLASSES.toggleActive : PRESENT_TOOLBAR_CLASSES.toggle;
  const navBtnClass = PRESENT_TOOLBAR_CLASSES.button;
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ref: toolbarRef,
      "data-pptx-present-toolbar": true,
      role: "toolbar",
      tabIndex: -1,
      "aria-label": t("pptx.toolbar.presentationToolbarAria"),
      className: PRESENT_TOOLBAR_CLASSES.container,
      onClick: (e) => e.stopPropagation(),
      children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "previous",
            className: navBtnClass,
            onClick: () => onMovePresentationSlide(-1),
            disabled: currentSlideIndex === 0,
            title: t("pptx.presenter.previousSlide"),
            "aria-label": t("pptx.presenter.previousSlide"),
            children: /* @__PURE__ */ jsx(LuChevronLeft, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsx("span", { "data-pptx-present-control": "counter", className: PRESENT_TOOLBAR_CLASSES.counter, children: formatSlideCounter(currentSlideIndex, totalSlides) }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "next",
            className: navBtnClass,
            onClick: () => onMovePresentationSlide(1),
            disabled: currentSlideIndex >= totalSlides - 1,
            title: t("pptx.presenter.nextSlide"),
            "aria-label": t("pptx.presenter.nextSlide"),
            children: /* @__PURE__ */ jsx(LuChevronRight, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsx(
          "div",
          {
            "data-pptx-present-control": "divider-navigation",
            className: PRESENT_TOOLBAR_CLASSES.divider
          }
        ),
        /* @__PURE__ */ jsxs(
          "div",
          {
            "data-pptx-present-control": "timer",
            className: PRESENT_TOOLBAR_CLASSES.timer,
            title: t("pptx.presenter.elapsed"),
            "aria-label": t("pptx.presenter.elapsed"),
            children: [
              /* @__PURE__ */ jsx(LuTimer, { size: 14 }),
              /* @__PURE__ */ jsx("span", { children: formatElapsed(elapsed) })
            ]
          }
        ),
        /* @__PURE__ */ jsx("div", { "data-pptx-present-control": "divider-timer", className: PRESENT_TOOLBAR_CLASSES.divider }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "laser",
            className: toolBtnClass("laser"),
            onClick: () => handleToolClick("laser"),
            title: t("pptx.presentation.laserPointer"),
            "aria-label": t("pptx.presentation.laserPointer"),
            children: /* @__PURE__ */ jsx(LuMousePointer2, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsxs("div", { className: "relative flex items-center", children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "pen",
              className: toolBtnClass("pen"),
              onClick: () => handleToolClick("pen"),
              onContextMenu: handlePenRightClick,
              title: t("pptx.presentation.pen"),
              "aria-label": t("pptx.presentation.pen"),
              children: [
                /* @__PURE__ */ jsx(LuPenTool, { size: 18 }),
                /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: PRESENT_TOOLBAR_CLASSES.swatchBar,
                    style: { backgroundColor: penColor }
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "pen-color",
              className: PRESENT_TOOLBAR_CLASSES.caret,
              onClick: () => {
                setShowPenColors((prev) => !prev);
                setShowHighlighterColors(false);
              },
              title: t("pptx.presentationToolbar.penColor"),
              "aria-label": t("pptx.presentationToolbar.penColor"),
              children: /* @__PURE__ */ jsx(LuChevronDown, { size: 12 })
            }
          ),
          showPenColors && /* @__PURE__ */ jsx("div", { className: PRESENT_TOOLBAR_CLASSES.palette, children: PEN_COLORS.map((color) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "aria-label": t("pptx.presentationToolbar.penColorValue", { color }),
              className: `${PRESENT_TOOLBAR_CLASSES.swatch} ${penColor === color ? "border-white" : "border-white/20"}`,
              style: { backgroundColor: color },
              onClick: () => {
                onSetPenColor(color);
                setShowPenColors(false);
                if (presentationTool !== "pen") {
                  onSetTool("pen");
                }
              }
            },
            color
          )) })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "relative flex items-center", children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "highlighter",
              className: toolBtnClass("highlighter"),
              onClick: () => handleToolClick("highlighter"),
              onContextMenu: handleHighlighterRightClick,
              title: t("pptx.presentation.highlighter"),
              "aria-label": t("pptx.presentation.highlighter"),
              children: [
                /* @__PURE__ */ jsx(LuHighlighter, { size: 18 }),
                /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: PRESENT_TOOLBAR_CLASSES.swatchBar,
                    style: { backgroundColor: highlighterColor }
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "highlighter-color",
              className: PRESENT_TOOLBAR_CLASSES.caret,
              onClick: () => {
                setShowHighlighterColors((prev) => !prev);
                setShowPenColors(false);
              },
              title: t("pptx.presentationToolbar.highlighterColor"),
              "aria-label": t("pptx.presentationToolbar.highlighterColor"),
              children: /* @__PURE__ */ jsx(LuChevronDown, { size: 12 })
            }
          ),
          showHighlighterColors && /* @__PURE__ */ jsx("div", { className: PRESENT_TOOLBAR_CLASSES.palette, children: HIGHLIGHTER_COLORS.map((color) => /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "aria-label": t("pptx.presentationToolbar.highlighterColorValue", { color }),
              className: `${PRESENT_TOOLBAR_CLASSES.swatch} ${highlighterColor === color ? "border-white" : "border-white/20"}`,
              style: { backgroundColor: color },
              onClick: () => {
                onSetHighlighterColor(color);
                setShowHighlighterColors(false);
                if (presentationTool !== "highlighter") {
                  onSetTool("highlighter");
                }
              }
            },
            color
          )) })
        ] }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "eraser",
            className: toolBtnClass("eraser"),
            onClick: () => handleToolClick("eraser"),
            title: t("pptx.presentation.eraser"),
            "aria-label": t("pptx.presentation.eraser"),
            children: /* @__PURE__ */ jsx(LuEraser, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "blackboard",
            className: isBlackboardActive(blackout, presentationTool) ? PRESENT_TOOLBAR_CLASSES.toggleActive : PRESENT_TOOLBAR_CLASSES.toggle,
            onClick: onToggleBlackboard,
            title: t("pptx.presentation.blackboard"),
            "aria-label": t("pptx.presentation.blackboard"),
            children: /* @__PURE__ */ jsx(LuPresentation, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "clear",
            className: `${PRESENT_TOOLBAR_CLASSES.button} ${hasAnnotations ? "hover:text-red-400" : ""}`,
            onClick: hasAnnotations ? onClearAnnotations : void 0,
            title: t("pptx.presentation.clearAnnotations"),
            "aria-label": t("pptx.presentation.clearAnnotations"),
            disabled: !hasAnnotations,
            children: /* @__PURE__ */ jsx(LuTrash2, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsx("div", { "data-pptx-present-control": "divider-tools", className: PRESENT_TOOLBAR_CLASSES.divider }),
        onTogglePresenterView && /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "presenter-view",
            className: presenterMode ? PRESENT_TOOLBAR_CLASSES.toggleActive : PRESENT_TOOLBAR_CLASSES.toggle,
            onClick: onTogglePresenterView,
            title: t("pptx.presenter.presenterView"),
            "aria-label": t("pptx.presenter.presenterView"),
            children: /* @__PURE__ */ jsx(LuPanelRight, { size: 18 })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "end",
            className: `${PRESENT_TOOLBAR_CLASSES.button} hover:text-red-400`,
            onClick: onEndPresentation,
            title: t("pptx.presenter.endPresentation"),
            "aria-label": t("pptx.presenter.endPresentation"),
            children: /* @__PURE__ */ jsx(LuX, { size: 18 })
          }
        )
      ]
    }
  );
}
function PresentationTouchControls({
  currentSlideIndex,
  totalSlides,
  onMovePresentationSlide,
  onEndPresentation,
  hideNavigation = false
}) {
  const { t } = useTranslation();
  const { isTouchDevice } = useIsMobile();
  if (!isTouchDevice) {
    return null;
  }
  const btnClass = "pointer-events-auto flex items-center justify-center w-11 h-11 min-w-11 min-h-11 rounded-full bg-black/55 text-white shadow-lg active:bg-black/75";
  const stop = (e) => {
    e.stopPropagation();
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: `fixed z-[90] ${btnClass}`,
        style: {
          top: "calc(env(safe-area-inset-top, 0px) + 0.5rem)",
          right: "calc(env(safe-area-inset-right, 0px) + 0.5rem)",
          touchAction: "manipulation"
        },
        onClick: (e) => {
          stop(e);
          onEndPresentation();
        },
        "aria-label": t("pptx.presenter.endPresentation"),
        children: /* @__PURE__ */ jsx(LuX, { size: 22 })
      }
    ),
    !hideNavigation && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: `fixed z-[90] top-1/2 -translate-y-1/2 ${btnClass} disabled:opacity-30`,
          style: {
            left: "calc(env(safe-area-inset-left, 0px) + 0.5rem)",
            touchAction: "manipulation"
          },
          disabled: currentSlideIndex <= 0,
          onClick: (e) => {
            stop(e);
            onMovePresentationSlide(-1);
          },
          "aria-label": t("pptx.presenter.previousSlide"),
          children: /* @__PURE__ */ jsx(LuChevronLeft, { size: 26 })
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: `fixed z-[90] top-1/2 -translate-y-1/2 ${btnClass} disabled:opacity-30`,
          style: {
            right: "calc(env(safe-area-inset-right, 0px) + 0.5rem)",
            touchAction: "manipulation"
          },
          disabled: currentSlideIndex >= totalSlides - 1,
          onClick: (e) => {
            stop(e);
            onMovePresentationSlide(1);
          },
          "aria-label": t("pptx.presenter.nextSlide"),
          children: /* @__PURE__ */ jsx(LuChevronRight, { size: 26 })
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      "span",
      {
        className: "fixed z-[90] left-1/2 -translate-x-1/2 px-3 py-1 rounded-full bg-black/55 text-white text-xs font-mono tabular-nums pointer-events-none",
        style: { bottom: "calc(env(safe-area-inset-bottom, 0px) + 0.5rem)" },
        children: totalSlides === 0 ? "0 / 0" : `${currentSlideIndex + 1} / ${totalSlides}`
      }
    )
  ] });
}
function SlideLayer({ slide, templateElements, canvasSize }) {
  const safeWidth = Math.max(canvasSize.width, 1);
  const safeHeight = Math.max(canvasSize.height, 1);
  const elements = [...visibleTemplateElements(slide, templateElements), ...slide.elements];
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: "relative overflow-hidden",
      style: {
        width: safeWidth,
        height: safeHeight,
        backgroundColor: slide.backgroundColor ? normalizeHexColor(slide.backgroundColor, "#ffffff") : "#ffffff",
        backgroundImage: slide.backgroundImage ? `url(${slide.backgroundImage})` : slide.backgroundGradient ? slide.backgroundGradient : void 0,
        backgroundSize: slide.backgroundImage ? "cover" : void 0,
        backgroundPosition: slide.backgroundImage ? "center" : void 0
      },
      children: [
        /* @__PURE__ */ jsx(SlideBackgroundImageLayer, { slide }),
        elements.map((element, index) => /* @__PURE__ */ jsx(
          StaticElementRenderer,
          {
            element,
            activeSlide: slide,
            allSlides: [slide],
            zIndex: index
          },
          element.id
        ))
      ]
    }
  );
}
function PresentationTransitionOverlay({
  outgoingSlide,
  templateElements,
  canvasSize,
  transition,
  durationMs,
  scale: stageScale,
  morphPlan,
  incomingSlide,
  onComplete
}) {
  const containerRef = useRef(null);
  const [containerSize, setContainerSize] = useState(null);
  const resolvedStageScale = typeof stageScale === "number" && Number.isFinite(stageScale) && stageScale > 0 ? stageScale : null;
  useEffect(() => {
    if (resolvedStageScale !== null) {
      return;
    }
    const el = containerRef.current;
    if (!el) {
      return;
    }
    const rect = el.getBoundingClientRect();
    setContainerSize({ width: rect.width, height: rect.height });
  }, [resolvedStageScale]);
  useEffect(() => {
    const timer = window.setTimeout(onComplete, durationMs + 50);
    return () => {
      window.clearTimeout(timer);
    };
  }, [durationMs, onComplete]);
  const animations = useMemo(
    () => getSlideTransitionAnimations(
      transition.type,
      durationMs,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern
    ),
    [
      transition.type,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern,
      durationMs
    ]
  );
  const scale = useMemo(() => {
    if (resolvedStageScale !== null) {
      return resolvedStageScale;
    }
    if (!containerSize) {
      return 1;
    }
    const scaleX = containerSize.width / Math.max(canvasSize.width, 1);
    const scaleY = containerSize.height / Math.max(canvasSize.height, 1);
    return Math.min(scaleX, scaleY);
  }, [resolvedStageScale, containerSize, canvasSize]);
  const outgoingZIndex = animations.outgoingOnTop ? 40 : 20;
  if (morphPlan) {
    return /* @__PURE__ */ jsxs(
      "div",
      {
        ref: containerRef,
        "data-pptx-transition-overlay": true,
        "data-pptx-transition-morph": "true",
        className: "pptx-react-transition-overlay absolute inset-0 pointer-events-none overflow-hidden",
        style: { zIndex: 40 },
        children: [
          /* @__PURE__ */ jsx("style", { children: morphPlan.keyframesCss }),
          /* @__PURE__ */ jsx("div", { className: "absolute inset-0 flex items-center justify-center", children: /* @__PURE__ */ jsxs(
            "div",
            {
              style: {
                width: canvasSize.width,
                height: canvasSize.height,
                // The slide box is a flex ITEM, so it shrinks to the container
                // unless told not to. A show surface narrower than the deck's own
                // canvas (a windowed show, or a display scaled past 100%) squeezed
                // the unscaled 1280px box down to the container width and only
                // then scaled it, landing the whole outgoing slide up to ~77px to
                // the right of the live one for the length of the morph (issue
                // #161). The stage below positions its slide box absolutely and is
                // unaffected, so the two layers disagreed.
                flexShrink: 0,
                transform: `scale(${scale})`,
                transformOrigin: "center",
                position: "relative"
              },
              children: [
                morphPlan.outgoingElements.map((element, index) => /* @__PURE__ */ jsx(
                  "div",
                  {
                    "data-pptx-morph-outgoing": element.id,
                    style: { position: "absolute", inset: 0 },
                    children: /* @__PURE__ */ jsx(
                      StaticElementRenderer,
                      {
                        element,
                        activeSlide: outgoingSlide,
                        allSlides: [outgoingSlide],
                        zIndex: index,
                        animation: morphPlan.outgoingAnimations.get(element.id),
                        imageAnimation: morphPlan.outgoingImageAnimations.get(element.id)
                      }
                    )
                  },
                  element.id
                )),
                incomingSlide && morphPlan.overlayIncomingElements.map((element, index) => /* @__PURE__ */ jsx(
                  "div",
                  {
                    "data-pptx-morph-lifted": element.id,
                    style: { position: "absolute", inset: 0 },
                    children: /* @__PURE__ */ jsx(
                      StaticElementRenderer,
                      {
                        element,
                        activeSlide: incomingSlide,
                        allSlides: [incomingSlide],
                        zIndex: morphPlan.outgoingElements.length + index,
                        animation: morphPlan.overlayIncomingAnimations.get(element.id)
                      }
                    )
                  },
                  element.id
                )),
                incomingSlide && morphPlan.crossfadeGroups.map((group, index) => /* @__PURE__ */ jsxs(
                  "div",
                  {
                    "data-pptx-morph-crossfade": group.incoming.id,
                    style: {
                      ...MORPH_CROSSFADE_GROUP_STYLE,
                      zIndex: morphPlan.outgoingElements.length + morphPlan.overlayIncomingElements.length + index
                    },
                    children: [
                      /* @__PURE__ */ jsx(
                        "div",
                        {
                          "data-pptx-morph-outgoing": group.outgoing.id,
                          style: { ...MORPH_CROSSFADE_HALF_STYLE, animation: group.outgoingAnimation },
                          children: /* @__PURE__ */ jsx(
                            StaticElementRenderer,
                            {
                              element: group.outgoing,
                              activeSlide: outgoingSlide,
                              allSlides: [outgoingSlide],
                              zIndex: 0,
                              animation: morphPlan.outgoingAnimations.get(group.outgoing.id),
                              imageAnimation: morphPlan.outgoingImageAnimations.get(group.outgoing.id)
                            }
                          )
                        }
                      ),
                      /* @__PURE__ */ jsx(
                        "div",
                        {
                          "data-pptx-morph-lifted": group.incoming.id,
                          style: { ...MORPH_CROSSFADE_HALF_STYLE, animation: group.incomingAnimation },
                          children: /* @__PURE__ */ jsx(
                            StaticElementRenderer,
                            {
                              element: group.incoming,
                              activeSlide: incomingSlide,
                              allSlides: [incomingSlide],
                              zIndex: 0,
                              animation: morphPlan.overlayIncomingAnimations.get(group.incoming.id)
                            }
                          )
                        }
                      )
                    ]
                  },
                  group.incoming.id
                ))
              ]
            }
          ) })
        ]
      }
    );
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ref: containerRef,
      "data-pptx-transition-overlay": true,
      className: "pptx-react-transition-overlay absolute inset-0 pointer-events-none overflow-hidden",
      style: { zIndex: outgoingZIndex },
      children: [
        /* @__PURE__ */ jsx("style", { children: SLIDE_TRANSITION_KEYFRAMES }),
        !morphPlan && animations.incoming !== "none" && incomingSlide ? /* @__PURE__ */ jsx(
          "div",
          {
            "data-pptx-transition-layer": "incoming",
            className: "pptx-react-transition-layer absolute inset-0 flex items-center justify-center",
            style: {
              animation: animations.incoming,
              zIndex: animations.outgoingOnTop ? 0 : 10
            },
            children: /* @__PURE__ */ jsx(
              "div",
              {
                style: {
                  width: canvasSize.width,
                  height: canvasSize.height,
                  flexShrink: 0,
                  transform: `scale(${scale})`,
                  transformOrigin: "center"
                },
                children: /* @__PURE__ */ jsx(
                  SlideLayer,
                  {
                    slide: incomingSlide,
                    templateElements,
                    canvasSize
                  }
                )
              }
            )
          }
        ) : null,
        /* @__PURE__ */ jsx(
          "div",
          {
            "data-pptx-transition-layer": "outgoing",
            className: "pptx-react-transition-layer absolute inset-0 flex items-center justify-center",
            style: {
              animation: animations.outgoing !== "none" ? animations.outgoing : void 0,
              zIndex: animations.outgoingOnTop ? 10 : 0
            },
            children: /* @__PURE__ */ jsx(
              "div",
              {
                style: {
                  width: canvasSize.width,
                  height: canvasSize.height,
                  // See the morph layer above: a flex item shrinks to its container,
                  // which offsets the whole outgoing slide on a show surface narrower
                  // than the deck's canvas.
                  flexShrink: 0,
                  transform: `scale(${scale})`,
                  transformOrigin: "center"
                },
                children: /* @__PURE__ */ jsx(
                  SlideLayer,
                  {
                    slide: outgoingSlide,
                    templateElements,
                    canvasSize
                  }
                )
              }
            )
          }
        )
      ]
    }
  );
}
function KeepAnnotationsDialog({
  isOpen,
  annotationCount,
  slideCount,
  onKeep,
  onDiscard
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/50",
      children: /* @__PURE__ */ jsxs("div", { className: "bg-background border border-border rounded-lg shadow-xl w-[420px] max-w-[90vw] p-6 animate-in fade-in zoom-in-95 duration-200", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 mb-4", children: [
          /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center w-10 h-10 rounded-full bg-primary/10", children: /* @__PURE__ */ jsx(LuPenTool, { className: "w-5 h-5 text-primary" }) }),
          /* @__PURE__ */ jsxs("div", { children: [
            /* @__PURE__ */ jsx("h2", { className: "text-base font-semibold text-foreground", children: t("pptx.presentation.keepAnnotationsTitle") }),
            /* @__PURE__ */ jsx("p", { className: "text-sm text-muted-foreground", children: t("pptx.presentation.keepAnnotationsDescription", {
              count: annotationCount,
              slides: slideCount
            }) })
          ] })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 mt-6", children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md border border-border bg-background text-foreground hover:bg-accent transition-colors",
              onClick: onDiscard,
              children: [
                /* @__PURE__ */ jsx(LuTrash2, { className: "w-4 h-4" }),
                t("pptx.presentation.discardAnnotations")
              ]
            }
          ),
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md bg-primary text-white hover:bg-primary/90 transition-colors",
              onClick: onKeep,
              children: [
                /* @__PURE__ */ jsx(LuPenTool, { className: "w-4 h-4" }),
                t("pptx.presentation.keepAnnotations")
              ]
            }
          )
        ] })
      ] })
    }
  );
}
var PREVIEW_SCALE2 = 0.9;
var NOTE_LINE_COUNT = 5;
function NotesPagePreview({
  slideIndices,
  slides,
  frameSlides,
  maxPages = 3
}) {
  const { t } = useTranslation();
  const pages = useMemo(() => computeAllNotesPages(slideIndices), [slideIndices]);
  const printable = useMemo(() => getNotesPrintableArea(), []);
  const visiblePages = pages.slice(0, maxPages);
  const scaledW = printable.width * PREVIEW_SCALE2;
  const scaledH = printable.height * PREVIEW_SCALE2;
  if (slideIndices.length === 0) {
    return /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-full text-xs text-muted-foreground", children: t("pptx.print.preview.noSlides") });
  }
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-3 py-2", children: [
    /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wide", children: t("pptx.print.preview.title") }),
    visiblePages.map((page) => {
      const slide = slides[page.slideIndex];
      const hasNotes = Boolean(slide?.notes);
      const borderCls = frameSlides ? "border-2 border-gray-800" : "border border-gray-300";
      return /* @__PURE__ */ jsxs(
        "div",
        {
          className: "relative bg-white border border-gray-200 shadow-sm rounded",
          style: {
            width: scaledW,
            height: scaledH,
            overflow: "hidden"
          },
          children: [
            /* @__PURE__ */ jsx(
              "div",
              {
                className: `absolute flex items-center justify-center rounded-sm bg-gray-50 ${borderCls}`,
                style: {
                  left: page.slideArea.x * PREVIEW_SCALE2,
                  top: page.slideArea.y * PREVIEW_SCALE2,
                  width: page.slideArea.width * PREVIEW_SCALE2,
                  height: page.slideArea.height * PREVIEW_SCALE2
                },
                children: /* @__PURE__ */ jsx("span", { className: "text-[9px] font-medium text-gray-400", children: page.slideIndex + 1 })
              }
            ),
            /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute left-1 right-1 border-b border-gray-200",
                style: {
                  top: page.textArea.y * PREVIEW_SCALE2 - 1
                }
              }
            ),
            /* @__PURE__ */ jsx(
              "div",
              {
                className: "absolute",
                style: {
                  left: page.textArea.x * PREVIEW_SCALE2 + 4,
                  top: page.textArea.y * PREVIEW_SCALE2,
                  width: page.textArea.width * PREVIEW_SCALE2 - 8,
                  height: page.textArea.height * PREVIEW_SCALE2
                },
                children: Array.from({ length: NOTE_LINE_COUNT }, (_, li) => {
                  const lineSpacing = page.textArea.height * PREVIEW_SCALE2 / (NOTE_LINE_COUNT + 1);
                  return /* @__PURE__ */ jsx(
                    "div",
                    {
                      className: `absolute left-0 right-0 border-b ${hasNotes ? "border-gray-300" : "border-gray-200"}`,
                      style: { top: lineSpacing * (li + 1) }
                    },
                    li
                  );
                })
              }
            )
          ]
        },
        page.pageIndex
      );
    }),
    pages.length > maxPages && /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.print.preview.morePages", {
      count: pages.length - maxPages
    }) })
  ] });
}
var PREVIEW_SCALE3 = 0.9;
function PrintPreview({
  slideIndices,
  slidesPerPage,
  orientation,
  frameSlides,
  maxPages = 3
}) {
  const { t } = useTranslation();
  const pages = useMemo(
    () => computeHandoutLayout(slideIndices, slidesPerPage, orientation),
    [slideIndices, slidesPerPage, orientation]
  );
  const printable = useMemo(() => getPrintableArea(orientation), [orientation]);
  const visiblePages = pages.slice(0, maxPages);
  const isThreePerPage = slidesPerPage === 3;
  const noteLineCount = generateNoteLineCount();
  const scaledW = printable.width * PREVIEW_SCALE3;
  const scaledH = printable.height * PREVIEW_SCALE3;
  const noteAreaFraction = 0.5;
  if (slideIndices.length === 0) {
    return /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center h-full text-xs text-muted-foreground", children: t("pptx.print.preview.noSlides") });
  }
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-3 py-2", children: [
    /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wide", children: t("pptx.print.preview.title") }),
    visiblePages.map((page) => /* @__PURE__ */ jsx(
      "div",
      {
        className: "relative bg-white border border-gray-200 shadow-sm rounded",
        style: {
          width: scaledW,
          height: scaledH,
          overflow: "hidden"
        },
        children: page.cells.map((cell, idx) => {
          const isEmpty = cell.slideIndex < 0;
          const cx = cell.x * PREVIEW_SCALE3;
          const cy = cell.y * PREVIEW_SCALE3;
          const cw = cell.width * PREVIEW_SCALE3;
          const ch = cell.height * PREVIEW_SCALE3;
          const borderCls = frameSlides ? "border-2 border-gray-800" : "border border-gray-300";
          return /* @__PURE__ */ jsxs(
            "div",
            {
              className: "absolute flex items-start",
              style: {
                left: cx,
                top: cy,
                gap: isThreePerPage ? 2 : 0
              },
              children: [
                /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: `flex items-center justify-center rounded-sm bg-gray-50 ${isEmpty ? "opacity-30" : ""} ${borderCls}`,
                    style: { width: cw, height: ch },
                    children: !isEmpty && /* @__PURE__ */ jsx("span", { className: "text-[9px] font-medium text-gray-400", children: cell.slideIndex + 1 })
                  }
                ),
                isThreePerPage && !isEmpty && /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: "relative border-l border-gray-200 pl-1",
                    style: {
                      width: scaledW * noteAreaFraction,
                      height: ch
                    },
                    children: Array.from({ length: noteLineCount }, (_, li) => {
                      const lineY = ch / (noteLineCount + 1) * (li + 1);
                      return /* @__PURE__ */ jsx(
                        "div",
                        {
                          className: "absolute left-1 right-0 border-b border-gray-200",
                          style: { top: lineY }
                        },
                        li
                      );
                    })
                  }
                )
              ]
            },
            idx
          );
        })
      },
      page.pageIndex
    )),
    pages.length > maxPages && /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.print.preview.morePages", {
      count: pages.length - maxPages
    }) })
  ] });
}

// src/viewer/components/print-dialog-types.ts
var radioClass = (active) => `flex items-center gap-2 px-3 py-2 rounded-lg border text-sm cursor-pointer transition-colors ${active ? "border-primary bg-primary/10 text-foreground" : "border-border bg-background text-muted-foreground hover:border-primary/40"}`;
function PrintSettingsPanel({
  printWhat,
  onPrintWhatChange,
  orientation,
  onOrientationChange,
  colorMode,
  onColorModeChange,
  frameSlides,
  onFrameSlidesChange,
  slidesPerPage,
  onSlidesPerPageChange,
  slideRange,
  onSlideRangeChange,
  customFrom,
  onCustomFromChange,
  customTo,
  onCustomToChange,
  totalSlides,
  activeSlideIndex
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "flex-1 space-y-5 min-w-0", children: [
    /* @__PURE__ */ jsxs("fieldset", { children: [
      /* @__PURE__ */ jsx("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.printWhat") }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
        /* @__PURE__ */ jsxs("label", { className: radioClass(printWhat === "slides"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "slides",
              onChange: () => onPrintWhatChange("slides")
            }
          ),
          /* @__PURE__ */ jsx(LuFileText, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.fullPageSlides")
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(printWhat === "handouts"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "handouts",
              onChange: () => onPrintWhatChange("handouts")
            }
          ),
          /* @__PURE__ */ jsx(LuGrid2X2, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.handouts")
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(printWhat === "notes"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "notes",
              onChange: () => onPrintWhatChange("notes")
            }
          ),
          /* @__PURE__ */ jsx(LuStickyNote, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.notesPages")
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(printWhat === "outline"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "outline",
              onChange: () => onPrintWhatChange("outline")
            }
          ),
          /* @__PURE__ */ jsx(LuList, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.outline")
        ] })
      ] })
    ] }),
    printWhat === "handouts" && /* @__PURE__ */ jsxs("fieldset", { children: [
      /* @__PURE__ */ jsx("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.slidesPerPage") }),
      /* @__PURE__ */ jsx("div", { className: "flex gap-1.5 flex-wrap", children: HANDOUT_OPTIONS.map((n) => /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: () => onSlidesPerPageChange(n),
          className: `px-3 py-1.5 rounded-md text-sm font-medium border transition-colors ${slidesPerPage === n ? "border-primary bg-primary/10 text-foreground" : "border-border bg-background text-muted-foreground hover:border-primary/40"}`,
          children: n
        },
        n
      )) })
    ] }),
    /* @__PURE__ */ jsxs("fieldset", { children: [
      /* @__PURE__ */ jsx("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.slideRange") }),
      /* @__PURE__ */ jsxs("div", { className: "space-y-2", children: [
        /* @__PURE__ */ jsxs("label", { className: radioClass(slideRange === "all"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "slideRange",
              className: "sr-only",
              checked: slideRange === "all",
              onChange: () => onSlideRangeChange("all")
            }
          ),
          t("pptx.print.allSlides"),
          " (",
          totalSlides,
          ")"
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(slideRange === "current"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "slideRange",
              className: "sr-only",
              checked: slideRange === "current",
              onChange: () => onSlideRangeChange("current")
            }
          ),
          t("pptx.print.currentSlide"),
          " (",
          activeSlideIndex + 1,
          ")"
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(slideRange === "custom"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "slideRange",
              className: "sr-only",
              checked: slideRange === "custom",
              onChange: () => onSlideRangeChange("custom")
            }
          ),
          t("pptx.print.customRange")
        ] }),
        slideRange === "custom" && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 pl-6", children: [
          /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: t("pptx.print.from") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 1,
              max: totalSlides,
              value: customFrom,
              onChange: (e) => onCustomFromChange(Math.max(1, parseInt(e.target.value, 10) || 1)),
              className: "w-16 px-2 py-1 text-sm border border-border rounded bg-background text-foreground"
            }
          ),
          /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: t("pptx.print.to") }),
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "number",
              min: 1,
              max: totalSlides,
              value: customTo,
              onChange: (e) => onCustomToChange(Math.max(1, parseInt(e.target.value, 10) || 1)),
              className: "w-16 px-2 py-1 text-sm border border-border rounded bg-background text-foreground"
            }
          )
        ] })
      ] })
    ] }),
    printWhat === "slides" && /* @__PURE__ */ jsxs("fieldset", { children: [
      /* @__PURE__ */ jsx("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.orientation") }),
      /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ jsxs("label", { className: radioClass(orientation === "landscape"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "orientation",
              className: "sr-only",
              checked: orientation === "landscape",
              onChange: () => onOrientationChange("landscape")
            }
          ),
          t("pptx.print.landscape")
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(orientation === "portrait"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "orientation",
              className: "sr-only",
              checked: orientation === "portrait",
              onChange: () => onOrientationChange("portrait")
            }
          ),
          t("pptx.print.portrait")
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("fieldset", { children: [
      /* @__PURE__ */ jsx("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.colorMode") }),
      /* @__PURE__ */ jsxs("div", { className: "flex gap-2 flex-wrap", children: [
        /* @__PURE__ */ jsxs("label", { className: radioClass(colorMode === "color"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "colorMode",
              className: "sr-only",
              checked: colorMode === "color",
              onChange: () => onColorModeChange("color")
            }
          ),
          t("pptx.print.color")
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(colorMode === "grayscale"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "colorMode",
              className: "sr-only",
              checked: colorMode === "grayscale",
              onChange: () => onColorModeChange("grayscale")
            }
          ),
          t("pptx.print.grayscale")
        ] }),
        /* @__PURE__ */ jsxs("label", { className: radioClass(colorMode === "blackAndWhite"), children: [
          /* @__PURE__ */ jsx(
            "input",
            {
              type: "radio",
              name: "colorMode",
              className: "sr-only",
              checked: colorMode === "blackAndWhite",
              onChange: () => onColorModeChange("blackAndWhite")
            }
          ),
          t("pptx.print.blackAndWhite")
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: frameSlides,
          onChange: (e) => onFrameSlidesChange(e.target.checked),
          className: "rounded border-border"
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "text-sm text-foreground", children: t("pptx.print.frameSlides") })
    ] })
  ] });
}
function PrintDialog({
  open,
  onClose,
  onPrint,
  slides,
  activeSlideIndex,
  defaultSlidesPerPage,
  defaultFrameSlides
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const printOptionDefaults = resolveDefaultPrintSettings(useViewerOptionsContext());
  const [printWhat, setPrintWhat] = useState(printOptionDefaults?.printWhat ?? "slides");
  const [orientation, setOrientation] = useState("landscape");
  const [colorMode, setColorMode] = useState(
    printOptionDefaults?.colorMode ?? "color"
  );
  const [frameSlides, setFrameSlides] = useState(
    printOptionDefaults?.frameSlides ?? defaultFrameSlides ?? false
  );
  const [slidesPerPage, setSlidesPerPage] = useState(
    HANDOUT_OPTIONS.includes(defaultSlidesPerPage) ? defaultSlidesPerPage : 6
  );
  const [slideRange, setSlideRange] = useState("all");
  const [customFrom, setCustomFrom] = useState(1);
  const [customTo, setCustomTo] = useState(slides.length);
  const effectiveOrientation = useMemo(() => {
    if (printWhat === "notes" || printWhat === "outline") {
      return "portrait";
    }
    if (printWhat === "handouts") {
      return "portrait";
    }
    return orientation;
  }, [printWhat, orientation]);
  const slideCount = useMemo(() => {
    if (slideRange === "all") {
      return slides.length;
    }
    if (slideRange === "current") {
      return 1;
    }
    const from = Math.max(1, Math.min(customFrom, slides.length));
    const to = Math.max(from, Math.min(customTo, slides.length));
    return to - from + 1;
  }, [slideRange, slides.length, customFrom, customTo]);
  const pageCount = useMemo(() => {
    if (printWhat === "slides") {
      return slideCount;
    }
    if (printWhat === "notes") {
      return slideCount;
    }
    if (printWhat === "outline") {
      return 1;
    }
    return Math.ceil(slideCount / slidesPerPage);
  }, [printWhat, slideCount, slidesPerPage]);
  const previewSlideIndices = useMemo(() => {
    if (slideRange === "current") {
      return [activeSlideIndex];
    }
    if (slideRange === "custom") {
      const from = Math.max(0, customFrom - 1);
      const to = Math.min(slides.length - 1, customTo - 1);
      return Array.from({ length: Math.max(0, to - from + 1) }, (_, i) => from + i);
    }
    return Array.from({ length: slides.length }, (_, i) => i);
  }, [slideRange, activeSlideIndex, customFrom, customTo, slides.length]);
  const handlePrint = useCallback(() => {
    onPrint({
      printWhat,
      orientation: effectiveOrientation,
      colorMode,
      frameSlides,
      slidesPerPage,
      slideRange,
      customRangeFrom: Math.max(1, Math.min(customFrom, slides.length)),
      customRangeTo: Math.max(1, Math.min(customTo, slides.length)),
      // Options > Advanced > "Print scale to fit"; no dialog control of its
      // own (PowerPoint keeps this an Options default, not a per-job choice).
      scaleToFit: printOptionDefaults?.scaleToFit ?? true
    });
  }, [
    printWhat,
    effectiveOrientation,
    colorMode,
    frameSlides,
    slidesPerPage,
    slideRange,
    customFrom,
    customTo,
    slides.length,
    printOptionDefaults?.scaleToFit,
    onPrint
  ]);
  if (!open) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/60 backdrop-blur-sm",
      children: /* @__PURE__ */ jsxs(
        "div",
        {
          style: panelStyle,
          className: "w-[780px] max-h-[90vh] rounded-xl border border-border bg-background shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
          children: [
            /* @__PURE__ */ jsxs(
              "div",
              {
                ...dragHandlers,
                className: "flex items-center justify-between px-5 py-4 border-b border-border touch-none",
                children: [
                  /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
                    /* @__PURE__ */ jsx(LuPrinter, { className: "w-4 h-4 text-muted-foreground" }),
                    /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.print.title") })
                  ] }),
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      onClick: onClose,
                      className: "p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors",
                      children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
                    }
                  )
                ]
              }
            ),
            /* @__PURE__ */ jsxs("div", { className: "flex-1 overflow-y-auto px-5 py-4 flex gap-5", children: [
              /* @__PURE__ */ jsx(
                PrintSettingsPanel,
                {
                  printWhat,
                  onPrintWhatChange: setPrintWhat,
                  orientation,
                  onOrientationChange: setOrientation,
                  colorMode,
                  onColorModeChange: setColorMode,
                  frameSlides,
                  onFrameSlidesChange: setFrameSlides,
                  slidesPerPage,
                  onSlidesPerPageChange: setSlidesPerPage,
                  slideRange,
                  onSlideRangeChange: setSlideRange,
                  customFrom,
                  onCustomFromChange: setCustomFrom,
                  customTo,
                  onCustomToChange: setCustomTo,
                  totalSlides: slides.length,
                  activeSlideIndex
                }
              ),
              printWhat === "handouts" && /* @__PURE__ */ jsx("div", { className: "w-[230px] shrink-0 border-l border-border pl-4 overflow-y-auto", children: /* @__PURE__ */ jsx(
                PrintPreview,
                {
                  slideIndices: previewSlideIndices,
                  slidesPerPage,
                  orientation: effectiveOrientation,
                  frameSlides
                }
              ) }),
              printWhat === "notes" && /* @__PURE__ */ jsx("div", { className: "w-[230px] shrink-0 border-l border-border pl-4 overflow-y-auto", children: /* @__PURE__ */ jsx(
                NotesPagePreview,
                {
                  slideIndices: previewSlideIndices,
                  slides,
                  frameSlides
                }
              ) })
            ] }),
            /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-3 border-t border-border", children: [
              /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: t("pptx.print.pageEstimate", {
                pages: pageCount,
                slides: slideCount
              }) }),
              /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-4 py-2 text-sm rounded-lg border border-border text-muted-foreground hover:text-foreground hover:bg-accent transition-colors",
                    children: t("pptx.common.cancel")
                  }
                ),
                /* @__PURE__ */ jsxs(
                  "button",
                  {
                    type: "button",
                    onClick: handlePrint,
                    className: "px-4 py-2 text-sm rounded-lg bg-primary text-white hover:bg-primary/90 transition-colors flex items-center gap-1.5",
                    children: [
                      /* @__PURE__ */ jsx(LuPrinter, { className: "w-3.5 h-3.5" }),
                      t("pptx.print.printButton")
                    ]
                  }
                )
              ] })
            ] })
          ]
        }
      )
    }
  );
}
function ShowOptionsFieldset({
  draft,
  update
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("fieldset", { className: "space-y-1.5", children: [
    /* @__PURE__ */ jsx("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.showOptions") }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: Boolean(draft.loopContinuously),
          onChange: (e) => update({
            loopContinuously: e.target.checked
          }),
          className: "accent-primary"
        }
      ),
      /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.loopContinuously") })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: draft.showWithNarration === false,
          onChange: (e) => update({
            showWithNarration: !e.target.checked
          }),
          className: "accent-primary"
        }
      ),
      /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.showWithoutNarration") })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: draft.showWithAnimation === false,
          onChange: (e) => update({
            showWithAnimation: !e.target.checked
          }),
          className: "accent-primary"
        }
      ),
      /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.showWithoutAnimation") })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: Boolean(draft.showSubtitles),
          onChange: (e) => update({ showSubtitles: e.target.checked }),
          className: "accent-primary"
        }
      ),
      /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.showSubtitles") })
    ] })
  ] });
}
function ShowSlidesFieldset({
  draft,
  update,
  showSlidesMode,
  slideCount,
  customShows
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("fieldset", { className: "space-y-1.5", children: [
    /* @__PURE__ */ jsx("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.showSlides") }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "radio",
          name: "showSlides",
          value: "all",
          checked: showSlidesMode === "all",
          onChange: () => update({ showSlidesMode: "all" }),
          className: "accent-primary"
        }
      ),
      /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.allSlides") })
    ] }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "radio",
          name: "showSlides",
          value: "range",
          checked: showSlidesMode === "range",
          onChange: () => update({
            showSlidesMode: "range",
            showSlidesFrom: draft.showSlidesFrom ?? 1,
            showSlidesTo: draft.showSlidesTo ?? slideCount
          }),
          className: "accent-primary"
        }
      ),
      /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.fromTo") })
    ] }),
    showSlidesMode === "range" && /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 ml-6", children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.slideShow.from") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 1,
            max: slideCount,
            value: draft.showSlidesFrom ?? 1,
            onChange: (e) => update({
              showSlidesFrom: Math.max(1, Number.parseInt(e.target.value, 10) || 1)
            }),
            className: "w-14 px-1.5 py-0.5 rounded bg-muted border border-border text-foreground text-[11px]"
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: t("pptx.slideShow.to") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            min: 1,
            max: slideCount,
            value: draft.showSlidesTo ?? slideCount,
            onChange: (e) => update({
              showSlidesTo: Math.min(
                slideCount,
                Number.parseInt(e.target.value, 10) || slideCount
              )
            }),
            className: "w-14 px-1.5 py-0.5 rounded bg-muted border border-border text-foreground text-[11px]"
          }
        )
      ] })
    ] }),
    customShows.length > 0 && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "radio",
            name: "showSlides",
            value: "customShow",
            checked: showSlidesMode === "customShow",
            onChange: () => update({
              showSlidesMode: "customShow",
              showSlidesCustomShowId: draft.showSlidesCustomShowId ?? customShows[0]?.id
            }),
            className: "accent-primary"
          }
        ),
        /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.customShow") })
      ] }),
      showSlidesMode === "customShow" && /* @__PURE__ */ jsx("div", { className: "ml-6", children: /* @__PURE__ */ jsx(
        "select",
        {
          value: draft.showSlidesCustomShowId ?? customShows[0]?.id ?? "",
          onChange: (e) => update({
            showSlidesCustomShowId: e.target.value
          }),
          className: "w-full px-2 py-1 rounded bg-muted border border-border text-foreground text-[11px]",
          children: customShows.map((cs) => /* @__PURE__ */ jsx("option", { value: cs.id, children: cs.name }, cs.id))
        }
      ) })
    ] })
  ] });
}
function SetUpSlideShowDialog({
  open,
  onClose,
  properties,
  onSave,
  customShows,
  slideCount
}) {
  const { t } = useTranslation();
  const [draft, setDraft] = useState({
    ...properties
  });
  const update = useCallback((patch) => {
    setDraft((prev) => ({ ...prev, ...patch }));
  }, []);
  const handleSave = useCallback(() => {
    onSave(draft);
    onClose();
  }, [draft, onSave, onClose]);
  if (!open) {
    return null;
  }
  const showType = draft.showType ?? "presented";
  const showSlidesMode = draft.showSlidesMode ?? "all";
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        "aria-label": t("pptx.share.closeDialog"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs("div", { className: "pointer-events-auto flex flex-col w-[440px] max-h-[90vh] rounded-xl border border-border bg-background shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-5 py-3 border-b border-border shrink-0", children: [
            /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.slideShow.setUpTitle") }),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "text-muted-foreground hover:text-foreground text-lg leading-none",
                "aria-label": t("pptx.common.close"),
                children: "\xD7"
              }
            )
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex-1 px-5 py-4 space-y-5 text-[12px] text-foreground overflow-y-auto", children: [
            /* @__PURE__ */ jsxs("fieldset", { className: "space-y-1.5", children: [
              /* @__PURE__ */ jsx("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.showType") }),
              [
                ["presented", t("pptx.slideShow.presentedBySpeaker")],
                ["browsed", t("pptx.slideShow.browsedByIndividual")],
                ["kiosk", t("pptx.slideShow.browsedAtKiosk")]
              ].map(([value, label]) => /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
                /* @__PURE__ */ jsx(
                  "input",
                  {
                    type: "radio",
                    name: "showType",
                    value,
                    checked: showType === value,
                    onChange: () => update({
                      showType: value,
                      ...value === "kiosk" ? { loopContinuously: true } : {}
                    }),
                    className: "accent-primary"
                  }
                ),
                /* @__PURE__ */ jsx("span", { children: label })
              ] }, value))
            ] }),
            /* @__PURE__ */ jsx(
              ShowSlidesFieldset,
              {
                draft,
                update,
                showSlidesMode,
                slideCount,
                customShows
              }
            ),
            /* @__PURE__ */ jsxs("fieldset", { className: "space-y-1.5", children: [
              /* @__PURE__ */ jsx("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.advanceSlides") }),
              /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
                /* @__PURE__ */ jsx(
                  "input",
                  {
                    type: "radio",
                    name: "advanceMode",
                    value: "manual",
                    checked: draft.advanceMode === "manual",
                    onChange: () => update({ advanceMode: "manual" }),
                    className: "accent-primary"
                  }
                ),
                /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.manually") })
              ] }),
              /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 cursor-pointer", children: [
                /* @__PURE__ */ jsx(
                  "input",
                  {
                    type: "radio",
                    name: "advanceMode",
                    value: "useTimings",
                    checked: (draft.advanceMode ?? "useTimings") === "useTimings",
                    onChange: () => update({ advanceMode: "useTimings" }),
                    className: "accent-primary"
                  }
                ),
                /* @__PURE__ */ jsx("span", { children: t("pptx.slideShow.useTimings") })
              ] })
            ] }),
            /* @__PURE__ */ jsx(ShowOptionsFieldset, { draft, update })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 px-5 py-3 border-t border-border shrink-0", children: [
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "px-3 py-1.5 rounded bg-muted hover:bg-accent text-[12px] text-foreground transition-colors",
                children: t("pptx.common.cancel")
              }
            ),
            /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: handleSave,
                className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/80 text-[12px] text-white transition-colors",
                children: t("pptx.common.ok")
              }
            )
          ] })
        ] })
      }
    )
  ] });
}
function BroadcastDialog({
  open,
  onClose,
  onStartBroadcast,
  onStopBroadcast,
  onStartPresenting,
  defaultRoomId,
  defaultUserName,
  defaultServerUrl
}) {
  const { t } = useTranslation();
  const collab = useCollaboration();
  const isBroadcasting = collab !== null && collab.status !== "disconnected" && collab.status !== "error" && collab.config.role === "owner";
  const [roomId, setRoomId] = useState("");
  const [userName, setUserName] = useState("");
  const [serverUrl, setServerUrl] = useState("");
  const [copied, setCopied] = useState(false);
  const dialogRef = useRef(null);
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  useModalFocus(open, dialogRef, onClose);
  useEffect(() => {
    if (open && !isBroadcasting) {
      const broadcastRoom = defaultRoomId ? `broadcast-${defaultRoomId}` : generateBroadcastRoomId();
      setRoomId(broadcastRoom);
      setUserName(defaultUserName ?? "");
      setServerUrl(defaultServerUrl ?? DEFAULT_BROADCAST_SERVER_URL);
    }
  }, [open, isBroadcasting, defaultRoomId, defaultUserName, defaultServerUrl]);
  const activeRoomId = isBroadcasting ? collab.config.roomId : roomId;
  const activeServerUrl = isBroadcasting ? collab.config.serverUrl : serverUrl;
  const broadcastUrl = typeof window !== "undefined" ? buildBroadcastViewerUrl(activeRoomId, activeServerUrl, window.location) : activeRoomId;
  const handleCopyUrl = useCallback(() => {
    void navigator.clipboard.writeText(broadcastUrl).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 2e3);
      return void 0;
    });
  }, [broadcastUrl]);
  const handleStartBroadcast = useCallback(() => {
    if (!roomId.trim() || !userName.trim()) {
      return;
    }
    const trimmedServer = serverUrl.trim();
    onStartBroadcast?.({
      roomId: roomId.trim(),
      serverUrl: trimmedServer,
      userName: userName.trim(),
      role: "owner",
      // A blank server broadcasts over the serverless peer-to-peer transport.
      transport: resolveTransportForServerUrl(trimmedServer)
    });
    setTimeout(() => {
      onStartPresenting?.();
    }, 100);
    onClose();
  }, [roomId, userName, serverUrl, onStartBroadcast, onStartPresenting, onClose]);
  const handleStopBroadcast = useCallback(() => {
    onStopBroadcast?.();
    onClose();
  }, [onStopBroadcast, onClose]);
  const canStart = roomId.trim().length > 0 && userName.trim().length > 0;
  if (!open) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        "aria-label": t("pptx.share.closeDialog"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            ref: dialogRef,
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.broadcast.title"),
            tabIndex: -1,
            style: panelStyle,
            className: "pointer-events-auto w-full max-w-md rounded-xl border border-border bg-popover text-foreground shadow-2xl outline-none max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:max-w-none max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-5 py-3 border-b border-border touch-none",
                  children: [
                    /* @__PURE__ */ jsxs("h2", { className: "text-sm font-semibold text-foreground flex items-center gap-2", children: [
                      /* @__PURE__ */ jsx(LuCast, { className: "w-4 h-4" }),
                      isBroadcasting ? t("pptx.broadcast.broadcasting") : t("pptx.broadcast.title")
                    ] }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "text-muted-foreground hover:text-foreground text-lg leading-none",
                        "aria-label": t("pptx.common.close"),
                        children: "\xD7"
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsx("div", { className: "px-5 py-4", children: isBroadcasting ? /* @__PURE__ */ jsx(
                ActiveBroadcastView,
                {
                  collab,
                  broadcastUrl,
                  copied,
                  onCopyUrl: handleCopyUrl,
                  onStopBroadcast: handleStopBroadcast
                }
              ) : /* @__PURE__ */ jsx(
                StartBroadcastForm,
                {
                  roomId,
                  userName,
                  serverUrl,
                  onRoomIdChange: setRoomId,
                  onUserNameChange: setUserName,
                  onServerUrlChange: setServerUrl
                }
              ) }),
              !isBroadcasting && /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 px-5 py-3 border-t border-border", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 rounded bg-muted hover:bg-accent text-[12px] text-foreground transition-colors",
                    children: t("pptx.common.close")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    disabled: !canStart,
                    onClick: handleStartBroadcast,
                    className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/90 text-[12px] text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
                    children: t("pptx.broadcast.startBroadcast")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function StartBroadcastForm({
  roomId,
  userName,
  serverUrl,
  onRoomIdChange,
  onUserNameChange,
  onServerUrlChange
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsx("p", { className: "text-[13px] text-muted-foreground leading-relaxed", children: t("pptx.broadcast.description") }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx(
        "label",
        {
          htmlFor: "broadcast-room-id",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.broadcast.sessionName")
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "broadcast-room-id",
          type: "text",
          value: roomId,
          onChange: (e) => onRoomIdChange(e.target.value),
          placeholder: "broadcast-abc123",
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx(
        "label",
        {
          htmlFor: "broadcast-user-name",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.broadcast.displayName")
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "broadcast-user-name",
          type: "text",
          value: userName,
          onChange: (e) => onUserNameChange(e.target.value),
          placeholder: t("pptx.broadcast.presenterPlaceholder"),
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx(
        "label",
        {
          htmlFor: "broadcast-server-url",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.broadcast.serverLabel")
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "broadcast-server-url",
          type: "text",
          value: serverUrl,
          onChange: (e) => onServerUrlChange(e.target.value),
          placeholder: "ws://localhost:1234",
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      )
    ] }),
    /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground/70 leading-relaxed", children: serverUrl.trim().length === 0 ? t("pptx.broadcast.p2pHint") : t("pptx.broadcast.hint") })
  ] });
}
function ActiveBroadcastView({
  collab,
  broadcastUrl,
  copied,
  onCopyUrl,
  onStopBroadcast
}) {
  const { t } = useTranslation();
  const statusColor = collab.status === "connected" ? "text-green-400" : collab.status === "connecting" ? "text-yellow-400" : "text-red-400";
  const statusIcon = collab.status === "connected" || collab.status === "connecting" ? /* @__PURE__ */ jsx(LuWifi, { className: "w-4 h-4" }) : /* @__PURE__ */ jsx(LuWifiOff, { className: "w-4 h-4" });
  const viewerCount = collab.remoteUsers.filter((u) => u.role === "viewer").length;
  return /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
      /* @__PURE__ */ jsx("span", { className: statusColor, children: statusIcon }),
      /* @__PURE__ */ jsx("span", { className: "text-[13px] font-medium text-foreground capitalize", children: collab.status }),
      /* @__PURE__ */ jsxs("span", { className: "text-[12px] text-muted-foreground ml-auto flex items-center gap-1", children: [
        /* @__PURE__ */ jsx(LuUsers, { className: "w-3.5 h-3.5" }),
        t("pptx.broadcast.viewerCount", { count: viewerCount })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.broadcast.viewerLink") }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx("div", { className: "flex-1 px-3 py-1.5 rounded border border-border bg-background text-[11px] text-foreground select-all font-mono truncate", children: broadcastUrl }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onCopyUrl,
            className: "flex items-center gap-1 px-2.5 py-1.5 rounded border border-border bg-muted hover:bg-accent text-[12px] text-foreground transition-colors shrink-0",
            title: t("pptx.broadcast.copyLink"),
            children: copied ? /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(LuCheck, { className: "w-3.5 h-3.5 text-green-400" }),
              /* @__PURE__ */ jsx("span", { children: t("pptx.share.copied") })
            ] }) : /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(LuCopy, { className: "w-3.5 h-3.5" }),
              /* @__PURE__ */ jsx("span", { children: t("pptx.share.copyUrl") })
            ] })
          }
        )
      ] }),
      /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.broadcast.shareHint") })
    ] }),
    collab.remoteUsers.length > 0 && /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.broadcast.viewers") }),
      /* @__PURE__ */ jsx("div", { className: "rounded border border-border bg-background divide-y divide-border max-h-[120px] overflow-y-auto", children: collab.remoteUsers.map((user) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 px-3 py-2", children: [
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "w-5 h-5 rounded-full flex items-center justify-center text-[8px] font-semibold text-white shrink-0",
            style: { backgroundColor: user.userColor },
            children: user.userName.slice(0, 2).toUpperCase()
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[12px] text-foreground truncate", children: user.userName }),
        /* @__PURE__ */ jsxs("span", { className: "text-[10px] text-muted-foreground ml-auto", children: [
          "Slide ",
          user.activeSlideIndex + 1
        ] })
      ] }, user.clientId)) })
    ] }),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onStopBroadcast,
        className: "w-full px-3 py-2 rounded border border-red-500/30 bg-red-500/10 hover:bg-red-500/20 text-[12px] text-red-400 font-medium transition-colors",
        children: t("pptx.broadcast.stopBroadcast")
      }
    )
  ] });
}
function isP2P(config) {
  return config.transport === "webrtc" || !config.serverUrl?.trim();
}
function shareUrl(config) {
  return buildCollaborationShareUrl(
    config,
    typeof window === "undefined" ? void 0 : { origin: window.location.origin, pathname: window.location.pathname }
  );
}
function ActiveSessionView({
  collab,
  activeCollaboration,
  copied,
  onCopyRoomId,
  onStopCollaboration
}) {
  const { t } = useTranslation(), viewerOptions = useViewerOptionsContext(), statusColor = collab.status === "connected" ? "text-green-400" : collab.status === "connecting" ? "text-yellow-400" : "text-red-400", statusIcon = collab.status === "connected" || collab.status === "connecting" ? /* @__PURE__ */ jsx(LuWifi, { className: "w-4 h-4" }) : /* @__PURE__ */ jsx(LuWifiOff, { className: "w-4 h-4" }), sessionUsers = buildActiveSessionUsers({
    localUserName: activeCollaboration?.userName ?? collab.config.userName,
    localUserInitials: viewerOptions.general.userInitials,
    localUserColor: collab.config.userColor,
    remoteUsers: collab.remoteUsers
  });
  return /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
      /* @__PURE__ */ jsx("span", { className: statusColor, children: statusIcon }),
      /* @__PURE__ */ jsx("span", { className: "text-[13px] font-medium text-foreground capitalize", children: collab.status }),
      /* @__PURE__ */ jsxs("span", { className: "text-[12px] text-muted-foreground ml-auto flex items-center gap-1", children: [
        /* @__PURE__ */ jsx(LuUsers, { className: "w-3.5 h-3.5" }),
        t("pptx.collaboration.userCount", { count: collab.connectedCount })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.shareLink") }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx("div", { className: "flex-1 px-3 py-1.5 rounded border border-border bg-background text-[11px] text-foreground select-all font-mono truncate", children: shareUrl(collab.config) }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onCopyRoomId,
            className: "flex items-center gap-1 px-2.5 py-1.5 rounded border border-border bg-muted hover:bg-accent text-[12px] text-foreground transition-colors shrink-0",
            title: t("pptx.share.copyLink"),
            children: copied ? /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(LuCheck, { className: "w-3.5 h-3.5 text-green-400" }),
              /* @__PURE__ */ jsx("span", { children: t("pptx.share.copied") })
            ] }) : /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(LuCopy, { className: "w-3.5 h-3.5" }),
              /* @__PURE__ */ jsx("span", { children: t("pptx.share.copyUrl") })
            ] })
          }
        )
      ] }),
      /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.share.shareHint") })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 text-[11px] text-muted-foreground", children: [
      /* @__PURE__ */ jsxs("span", { children: [
        t("pptx.share.room"),
        " ",
        /* @__PURE__ */ jsx("code", { className: "font-mono text-foreground", children: collab.config.roomId })
      ] }),
      /* @__PURE__ */ jsxs("span", { children: [
        t("pptx.share.server"),
        " ",
        /* @__PURE__ */ jsx("code", { className: "font-mono text-foreground", children: isP2P(collab.config) ? t("pptx.share.p2pServerValue") : collab.config.serverUrl })
      ] })
    ] }),
    collab.remoteUsers.length > 0 && /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.connectedUsers") }),
      /* @__PURE__ */ jsx("div", { className: "rounded border border-border bg-background divide-y divide-border max-h-[140px] overflow-y-auto", children: sessionUsers.map((user) => /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 px-3 py-2", children: [
        /* @__PURE__ */ jsx(
          "div",
          {
            className: "w-6 h-6 rounded-full flex items-center justify-center text-[9px] font-semibold text-white shrink-0",
            style: { backgroundColor: user.color },
            children: user.avatarUrl ? /* @__PURE__ */ jsx(
              "img",
              {
                src: user.avatarUrl,
                alt: "",
                className: "w-full h-full rounded-full object-cover"
              }
            ) : user.initials
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[12px] text-foreground truncate", children: user.name }),
        /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground ml-auto", children: user.isLocal ? t("pptx.share.you") : t("pptx.notes.slideN", { n: user.slideNumber }) })
      ] }, user.id)) })
    ] }),
    onStopCollaboration && /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onStopCollaboration,
        className: "w-full px-3 py-2 rounded border border-red-500/30 bg-red-500/10 hover:bg-red-500/20 text-[12px] text-red-400 font-medium transition-colors",
        children: t("pptx.share.stopSharing")
      }
    )
  ] });
}
function StartSessionForm({
  mode,
  invitation,
  roomId,
  userName,
  serverUrl,
  onRoomIdChange,
  onUserNameChange,
  onServerUrlChange,
  onModeChange,
  onInvitationChange,
  preconfigured,
  connectionFailed,
  onRetry
}) {
  const { t } = useTranslation();
  const inputReadOnlyClass = preconfigured ? " opacity-70 cursor-not-allowed" : "";
  return /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-1 rounded-lg bg-muted p-1", role: "tablist", children: ["create", "join"].map((candidate) => /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        role: "tab",
        "aria-selected": mode === candidate,
        onClick: () => onModeChange(candidate),
        className: `rounded-md px-3 py-1.5 text-[12px] font-medium transition-colors ${mode === candidate ? "bg-background text-foreground shadow-sm" : "text-muted-foreground hover:text-foreground"}`,
        children: t(candidate === "create" ? "pptx.share.createSession" : "pptx.share.joinSession")
      },
      candidate
    )) }),
    /* @__PURE__ */ jsx("p", { className: "text-[13px] text-muted-foreground leading-relaxed", children: mode === "join" ? t("pptx.share.joinDescription") : preconfigured ? t("pptx.share.preconfiguredDescription") : t("pptx.share.description") }),
    connectionFailed && /* @__PURE__ */ jsxs(
      "div",
      {
        role: "alert",
        className: "flex items-start gap-2 rounded border border-red-500/30 bg-red-500/10 px-3 py-2 text-[12px] text-red-400",
        children: [
          /* @__PURE__ */ jsx(LuWifiOff, { className: "w-4 h-4 mt-0.5 shrink-0" }),
          /* @__PURE__ */ jsxs("div", { className: "flex-1 space-y-1", children: [
            /* @__PURE__ */ jsx("p", { className: "font-medium", children: t("pptx.share.connectionError") }),
            onRetry && /* @__PURE__ */ jsx(
              "button",
              {
                type: "button",
                onClick: onRetry,
                className: "underline underline-offset-2 hover:no-underline",
                children: t("pptx.collaboration.retry")
              }
            )
          ] })
        ]
      }
    ),
    mode === "join" ? /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx(
        "label",
        {
          htmlFor: "share-invitation",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.share.invitationLabel")
        }
      ),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "share-invitation",
          type: "text",
          value: invitation,
          onChange: (event) => onInvitationChange(event.target.value),
          placeholder: t("pptx.share.invitationPlaceholder"),
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      ),
      /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.share.invitationHint") })
    ] }) : /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { htmlFor: "share-room-id", className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.sessionName") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "share-room-id",
          type: "text",
          "aria-label": t("pptx.share.sessionName"),
          value: roomId,
          onChange: (e) => onRoomIdChange(e.target.value),
          readOnly: preconfigured,
          placeholder: t("pptx.share.sessionPlaceholder"),
          className: `w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary${inputReadOnlyClass}`
        }
      ),
      /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.share.sessionHint") })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { htmlFor: "share-user-name", className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.displayName") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "share-user-name",
          type: "text",
          "aria-label": t("pptx.share.displayName"),
          value: userName,
          onChange: (e) => onUserNameChange(e.target.value),
          readOnly: preconfigured,
          placeholder: t("pptx.share.namePlaceholder"),
          className: `w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary${inputReadOnlyClass}`
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
      /* @__PURE__ */ jsx("label", { htmlFor: "share-server-url", className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.serverLabel") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          id: "share-server-url",
          type: "text",
          "aria-label": t("pptx.share.serverLabel"),
          value: serverUrl,
          onChange: (e) => onServerUrlChange(e.target.value),
          readOnly: preconfigured,
          placeholder: t("pptx.share.serverPlaceholder"),
          className: `w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary${inputReadOnlyClass}`
        }
      )
    ] }),
    /* @__PURE__ */ jsx("p", { className: "text-[11px] text-muted-foreground", children: serverUrl.trim().length === 0 ? t("pptx.share.p2pHint") : t("pptx.share.serverHint") })
  ] });
}
function shareUrl2(config) {
  return buildCollaborationShareUrl(
    config,
    typeof window === "undefined" ? void 0 : { origin: window.location.origin, pathname: window.location.pathname }
  );
}
function ShareDialog({
  open,
  onClose,
  activeCollaboration,
  onStartCollaboration,
  onStopCollaboration,
  preconfigured,
  defaultRoomId,
  defaultUserName,
  defaultServerUrl
}) {
  const collab = useCollaboration();
  const isActive = collab !== null && collab.status !== "disconnected" && collab.status !== "error";
  const { t } = useTranslation();
  const [roomId, setRoomId] = useState(defaultRoomId ?? "");
  const [userName, setUserName] = useState(defaultUserName ?? "");
  const [serverUrl, setServerUrl] = useState(defaultServerUrl ?? "");
  const [mode, setMode] = useState("create");
  const [invitation, setInvitation] = useState("");
  const [copied, setCopied] = useState(false);
  const dialogRef = useRef(null);
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  useModalFocus(open, dialogRef, onClose);
  useEffect(() => {
    if (activeCollaboration) {
      setRoomId(activeCollaboration.roomId);
      setUserName(activeCollaboration.userName);
      setServerUrl(activeCollaboration.serverUrl);
    }
  }, [activeCollaboration]);
  const handleCopyRoomId = useCallback(() => {
    const config = activeCollaboration ?? { roomId, serverUrl };
    void navigator.clipboard.writeText(shareUrl2(config)).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 2e3);
      return void 0;
    });
  }, [activeCollaboration, roomId, serverUrl]);
  const handleStartSharing = useCallback(() => {
    const config = mode === "join" ? buildJoinCollaborationConfig({ invitation, userName, serverUrl }) : buildCreateCollaborationConfig({ roomId, userName, serverUrl });
    if (config) {
      onStartCollaboration?.(config);
    }
  }, [invitation, mode, roomId, userName, serverUrl, onStartCollaboration]);
  const canStart = (mode === "join" ? buildJoinCollaborationConfig({ invitation, userName, serverUrl }) : buildCreateCollaborationConfig({ roomId, userName, serverUrl })) !== null;
  if (!open) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        "aria-label": t("pptx.share.closeDialog"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            ref: dialogRef,
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.share.title"),
            tabIndex: -1,
            style: panelStyle,
            className: "pointer-events-auto w-full max-w-md rounded-xl border border-border bg-popover text-foreground shadow-2xl outline-none max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:max-w-none max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-5 py-3 border-b border-border touch-none",
                  children: [
                    /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: isActive ? t("pptx.share.collaborationActive") : t("pptx.share.title") }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "text-muted-foreground hover:text-foreground text-lg leading-none",
                        "aria-label": t("pptx.share.close"),
                        children: "\xD7"
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsx("div", { className: "px-5 py-4", children: isActive ? /* @__PURE__ */ jsx(
                ActiveSessionView,
                {
                  collab,
                  activeCollaboration,
                  copied,
                  onCopyRoomId: handleCopyRoomId,
                  onStopCollaboration
                }
              ) : /* @__PURE__ */ jsx(
                StartSessionForm,
                {
                  mode,
                  invitation,
                  roomId,
                  userName,
                  serverUrl,
                  onRoomIdChange: setRoomId,
                  onUserNameChange: setUserName,
                  onServerUrlChange: setServerUrl,
                  onModeChange: setMode,
                  onInvitationChange: setInvitation,
                  preconfigured,
                  connectionFailed: collab?.status === "error",
                  onRetry: collab?.retry
                }
              ) }),
              /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 px-5 py-3 border-t border-border", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 rounded bg-muted hover:bg-accent text-[12px] text-foreground transition-colors",
                    children: isActive ? t("pptx.share.close") : t("pptx.share.cancel")
                  }
                ),
                !isActive && /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    disabled: !canStart,
                    onClick: handleStartSharing,
                    className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/90 text-[12px] text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
                    children: t(mode === "join" ? "pptx.share.joinSession" : "pptx.share.startSharing")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function HyperlinkTabContent({
  targetType,
  url,
  setUrl,
  tooltip,
  setTooltip,
  emailAddress,
  setEmailAddress,
  emailSubject,
  setEmailSubject,
  slideNumber,
  setSlideNumber,
  slideCount,
  filePath,
  setFilePath,
  actionVerb,
  setActionVerb,
  inputRef,
  inputCls,
  onEnterConfirm
}) {
  const { t } = useTranslation();
  const handleEnterKey = (e) => {
    if (e.key === "Enter") {
      onEnterConfirm();
    }
  };
  return /* @__PURE__ */ jsxs("div", { className: "px-4 py-3 space-y-3", children: [
    targetType === "url" && /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.urlLabel") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          ref: inputRef,
          type: "url",
          className: inputCls,
          placeholder: "https://example.com",
          value: url,
          onChange: (e) => setUrl(e.target.value),
          onKeyDown: handleEnterKey
        }
      )
    ] }),
    targetType === "email" && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.emailLabel") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            ref: inputRef,
            type: "email",
            className: inputCls,
            placeholder: "user@example.com",
            value: emailAddress,
            onChange: (e) => setEmailAddress(e.target.value),
            onKeyDown: handleEnterKey
          }
        )
      ] }),
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.subjectLabel") }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            className: inputCls,
            value: emailSubject,
            onChange: (e) => setEmailSubject(e.target.value)
          }
        )
      ] })
    ] }),
    targetType === "slide" && /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.slideLabel") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          ref: inputRef,
          type: "number",
          min: 1,
          max: slideCount,
          className: inputCls,
          value: slideNumber,
          onChange: (e) => setSlideNumber(parseInt(e.target.value, 10) || 1),
          onKeyDown: handleEnterKey
        }
      ),
      /* @__PURE__ */ jsx("p", { className: "text-[10px] text-muted-foreground mt-1", children: t("pptx.hyperlink.slideRange", { max: slideCount }) })
    ] }),
    targetType === "file" && /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.fileLabel") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          ref: inputRef,
          type: "text",
          className: inputCls,
          placeholder: "../documents/file.docx",
          value: filePath,
          onChange: (e) => setFilePath(e.target.value),
          onKeyDown: handleEnterKey
        }
      )
    ] }),
    targetType === "action" && /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.actionLabel") }),
      /* @__PURE__ */ jsxs(
        "select",
        {
          className: inputCls,
          value: actionVerb,
          onChange: (e) => setActionVerb(e.target.value),
          children: [
            /* @__PURE__ */ jsx("option", { value: "nextSlide", children: t("pptx.hyperlink.actionNextSlide") }),
            /* @__PURE__ */ jsx("option", { value: "prevSlide", children: t("pptx.hyperlink.actionPrevSlide") }),
            /* @__PURE__ */ jsx("option", { value: "firstSlide", children: t("pptx.hyperlink.actionFirstSlide") }),
            /* @__PURE__ */ jsx("option", { value: "lastSlide", children: t("pptx.hyperlink.actionLastSlide") }),
            /* @__PURE__ */ jsx("option", { value: "endShow", children: t("pptx.hyperlink.actionEndShow") })
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { children: [
      /* @__PURE__ */ jsx("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.tooltipLabel") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          className: inputCls,
          value: tooltip,
          onChange: (e) => setTooltip(e.target.value)
        }
      )
    ] })
  ] });
}
function HyperlinkEditDialog({
  open,
  initialUrl,
  initialTooltip,
  initialAction,
  slideCount,
  onConfirm,
  onCancel
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onCancel);
  const inputRef = useRef(null);
  const detectedType = detectTargetType(initialUrl, initialAction);
  const emailParts = parseEmailUrl(initialUrl || "");
  const detectedSlide = parseSlideFromUrl(initialUrl, initialAction);
  const detectedVerb = initialAction ? ACTION_VERB_MAP[initialAction.toLowerCase()] || "nextSlide" : "nextSlide";
  const [targetType, setTargetType] = useState(detectedType);
  const [url, setUrl] = useState(
    detectedType === "email" || detectedType === "slide" || detectedType === "action" ? "" : initialUrl || ""
  );
  const [tooltip, setTooltip] = useState(initialTooltip || "");
  const [emailAddress, setEmailAddress] = useState(emailParts.address);
  const [emailSubject, setEmailSubject] = useState(emailParts.subject);
  const [slideNumber, setSlideNumber] = useState(detectedSlide);
  const [filePath, setFilePath] = useState(detectedType === "file" ? initialUrl || "" : "");
  const [actionVerb, setActionVerb] = useState(detectedVerb);
  useEffect(() => {
    if (open) {
      setTimeout(() => inputRef.current?.focus(), 50);
    }
  }, [open]);
  if (!open) {
    return null;
  }
  const handleConfirm = () => {
    onConfirm({
      targetType,
      url,
      tooltip,
      emailAddress,
      emailSubject,
      slideNumber,
      filePath,
      actionVerb
    });
  };
  const tabCls = (active) => cn(
    "px-3 py-1.5 text-xs rounded-t border-b-2 transition-colors",
    active ? "border-primary text-primary font-medium" : "border-transparent text-muted-foreground hover:text-foreground hover:border-border"
  );
  const inputCls = "w-full rounded border border-border bg-background px-2.5 py-1.5 text-xs text-foreground outline-none focus:ring-1 focus:ring-primary";
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("div", { style: { zIndex: 1200 }, className: "fixed inset-0 bg-black/40", onClick: onCancel }),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            style: panelStyle,
            className: "pointer-events-auto w-[440px] rounded-lg border border-border bg-popover shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            onClick: (e) => e.stopPropagation(),
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-4 py-3 border-b border-border touch-none",
                  children: [
                    /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.hyperlink.editTitle") }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        className: "text-muted-foreground hover:text-foreground",
                        onClick: onCancel,
                        children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsxs("div", { className: "flex gap-1 px-4 pt-2 border-b border-border", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "url"),
                    onClick: () => setTargetType("url"),
                    children: t("pptx.hyperlink.tabUrl")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "email"),
                    onClick: () => setTargetType("email"),
                    children: t("pptx.hyperlink.tabEmail")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "slide"),
                    onClick: () => setTargetType("slide"),
                    children: t("pptx.hyperlink.tabSlide")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "file"),
                    onClick: () => setTargetType("file"),
                    children: t("pptx.hyperlink.tabFile")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "action"),
                    onClick: () => setTargetType("action"),
                    children: t("pptx.hyperlink.tabAction")
                  }
                )
              ] }),
              /* @__PURE__ */ jsx(
                HyperlinkTabContent,
                {
                  targetType,
                  url,
                  setUrl,
                  tooltip,
                  setTooltip,
                  emailAddress,
                  setEmailAddress,
                  emailSubject,
                  setEmailSubject,
                  slideNumber,
                  setSlideNumber,
                  slideCount,
                  filePath,
                  setFilePath,
                  actionVerb,
                  setActionVerb,
                  inputRef,
                  inputCls,
                  onEnterConfirm: handleConfirm
                }
              ),
              /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 px-4 py-3 border-t border-border", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: "px-3 py-1.5 text-xs rounded border border-border text-foreground hover:bg-muted",
                    onClick: onCancel,
                    children: t("pptx.common.cancel")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    className: "px-3 py-1.5 text-xs rounded bg-primary text-white hover:bg-primary/90",
                    onClick: handleConfirm,
                    children: t("pptx.common.apply")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function ViewerOverlays({
  isShortcutHelpOpen,
  isAccessibilityPanelOpen,
  showSlideSorter,
  showReadingView,
  showOutlineView,
  templateElements,
  accessibilityIssues,
  slides,
  activeSlideIndex,
  canvasSize,
  canEdit,
  sectionGroups,
  onCloseShortcuts,
  onCloseAccessibility,
  onSelectSlide,
  onMoveSlide,
  onDeleteSlides,
  onDuplicateSlides,
  onToggleHideSlides,
  onCloseSorter,
  onCloseReadingView,
  onCloseOutlineView,
  setSlides,
  setActiveSlideIndex,
  bumpHistory,
  reducedMotion,
  onToggleReducedMotion
}) {
  const hasOverlay = isShortcutHelpOpen || isAccessibilityPanelOpen || showSlideSorter || showReadingView || showOutlineView;
  if (!hasOverlay) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    isShortcutHelpOpen && /* @__PURE__ */ jsx(ShortcutPanel, { isOpen: isShortcutHelpOpen, onClose: onCloseShortcuts }),
    isAccessibilityPanelOpen && /* @__PURE__ */ jsx(
      AccessibilityPanel,
      {
        isOpen: isAccessibilityPanelOpen,
        issues: accessibilityIssues,
        onClose: onCloseAccessibility,
        reducedMotion,
        onToggleReducedMotion
      }
    ),
    showSlideSorter && /* @__PURE__ */ jsx(
      SlideSorterOverlay,
      {
        slides,
        activeSlideIndex,
        canvasSize,
        canEdit,
        sectionGroups,
        onSelectSlide,
        onMoveSlide,
        onDeleteSlides,
        onDuplicateSlides,
        onToggleHideSlides,
        onClose: onCloseSorter
      }
    ),
    showReadingView && /* @__PURE__ */ jsx(
      ReadingViewOverlay,
      {
        slides,
        templateElements,
        canvasSize,
        activeSlideIndex,
        onExit: onCloseReadingView
      }
    ),
    showOutlineView && /* @__PURE__ */ jsx(
      OutlineViewOverlay,
      {
        slides,
        canvasSize,
        canEdit,
        setSlides,
        setActiveSlideIndex,
        bumpHistory,
        onClose: onCloseOutlineView
      }
    )
  ] });
}
function ViewerBottomPanels({
  activeSlide,
  allSlides,
  isSlideNotesCollapsed,
  canEdit,
  slideCount,
  activeSlideIndex,
  isDirty,
  autosaveStatus,
  onToggleNotes,
  onUpdateNotes,
  collaborationSlot,
  notesPanelHeight,
  onResizeBottom,
  scale,
  onZoomIn,
  onZoomOut,
  onZoomToFit,
  mode,
  onSetMode,
  onToggleSlideSorter,
  hideStatusBar = false,
  hiddenActions,
  notesStyle
}) {
  const { isHidden } = useToolbarVisibility(hiddenActions);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    onResizeBottom && !isSlideNotesCollapsed && /* @__PURE__ */ jsx(ResizeHandle, { direction: "vertical", onResize: onResizeBottom }),
    /* @__PURE__ */ jsx(
      SlideNotesPanel,
      {
        activeSlide,
        allSlides,
        isExpanded: !isSlideNotesCollapsed,
        canEdit,
        onToggle: onToggleNotes,
        onUpdateNotes,
        panelHeight: notesPanelHeight,
        notesStyle
      }
    ),
    !hideStatusBar && /* @__PURE__ */ jsx(
      StatusBar,
      {
        slideCount,
        activeSlideIndex,
        isDirty,
        autosaveStatus,
        scale,
        onZoomIn,
        onZoomOut,
        onZoomToFit,
        isNotesExpanded: !isSlideNotesCollapsed,
        onToggleNotes,
        mode,
        onSetMode,
        onToggleSlideSorter,
        collaborationSlot,
        hideZoomControls: isHidden("zoom"),
        hideNotesToggle: isHidden("notes"),
        hideFullscreenToggle: isHidden("fullscreen")
      }
    )
  ] });
}
function ViewerInspector({
  isOpen,
  canEdit,
  mode,
  activeSlide,
  slides,
  customShows,
  canvasSize,
  selectedElement,
  effectiveSelectedIds,
  tableEditorState,
  sidebarPanelMode,
  activeSlideIndex,
  comments,
  commentAuthors,
  onSetSidebarPanelMode,
  onClose,
  onUpdateElementStyle,
  onUpdateTextStyle,
  onUpdateElement,
  onApplySelection,
  onSetCanvasSize,
  slideSizeEmu,
  onSetSlideSize,
  onMoveLayer,
  onMoveLayerToEdge,
  onDeleteElement,
  onUpdateSlide,
  presentationProperties,
  onUpdatePresentationProperties,
  notesMaster,
  handoutMaster,
  notesCanvasSize,
  coreProperties,
  appProperties,
  customProperties,
  themeOptions,
  onUpdateCoreProperties,
  onUpdateAppProperties,
  onUpdateCustomProperties,
  tagCollections,
  onUpdateTagCollections,
  onApplyTheme,
  editTemplateMode,
  slideMasters,
  onSetTemplateBackground,
  onGetTemplateBackgroundColor,
  mediaDataUrls,
  theme,
  tableStyleMap,
  onTableStyleMapChange,
  onDeleteTableStyle,
  panelWidth
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  const slideId = activeSlide?.id ?? "";
  const commentDraft = comments.commentDraftBySlideId[slideId] ?? "";
  const editingCommentId = comments.editingCommentIdBySlideId[slideId] ?? null;
  return /* @__PURE__ */ jsx(
    "div",
    {
      "data-pptx-inspector": true,
      role: "complementary",
      "aria-label": t("pptx.inspector.properties"),
      className: "h-full flex-shrink-0 max-md:absolute max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:z-30",
      style: panelWidth ? { width: panelWidth } : void 0,
      children: /* @__PURE__ */ jsx(
        InspectorPane,
        {
          isOpen,
          canEdit,
          mode,
          activeSlide,
          slides,
          customShows,
          canvasSize,
          selectedElement,
          selectedElementIds: effectiveSelectedIds,
          tableEditorState,
          activeTab: sidebarPanelMode,
          onSetActiveTab: (nextTab) => onSetSidebarPanelMode(nextTab),
          onClose,
          onUpdateElementStyle,
          onUpdateTextStyle,
          onUpdateElement,
          onUpdateSlide,
          onSelectElement: (id) => onApplySelection(id),
          onMoveLayer,
          onMoveLayerToEdge,
          onDeleteElement,
          comments: activeSlide?.comments ?? [],
          presentationProperties,
          onUpdatePresentationProperties,
          notesMaster,
          handoutMaster,
          notesCanvasSize,
          coreProperties,
          appProperties,
          customProperties,
          themeOptions,
          onUpdateCoreProperties,
          onUpdateAppProperties,
          onUpdateCustomProperties,
          tagCollections,
          onUpdateTagCollections,
          onApplyTheme,
          commentDraft,
          commentDraftMentions: comments.commentDraftMentionsBySlideId?.[slideId] ?? [],
          commentAuthors: commentAuthors ?? [],
          editingCommentId,
          commentEditDraft: editingCommentId ? comments.commentEditDraftByCommentId[editingCommentId] ?? "" : "",
          onSetCommentDraft: (draft, mentions) => comments.handleCommentDraftChange(slideId, draft, mentions),
          onAddComment: () => comments.handleAddSlideComment(activeSlideIndex),
          onDeleteComment: (id) => comments.handleDeleteSlideComment(activeSlideIndex, id),
          onStartEditComment: (id) => comments.handleStartCommentEdit(slideId, id),
          onSaveEditComment: (id) => comments.handleSaveCommentEdit(activeSlideIndex, id),
          onCancelEditComment: () => comments.handleCancelCommentEdit(slideId),
          onSetCommentEditDraft: (draft) => comments.handleSetCommentEditDraft(
            comments.editingCommentIdBySlideId[slideId] ?? "",
            draft
          ),
          onToggleCommentResolved: (id) => comments.handleToggleCommentResolved(activeSlideIndex, id),
          onStartReply: (id) => comments.handleStartReply(activeSlideIndex, id),
          onCancelReply: comments.handleCancelReply,
          onReplyDraftChange: (commentId, draft, mentions) => comments.handleReplyDraftChange(commentId, draft, mentions),
          onSubmitReply: (commentId) => comments.handleSubmitReply(activeSlideIndex, commentId),
          replyingToCommentId: comments.replyingToCommentId,
          replyDraftByCommentId: comments.replyDraftByCommentId,
          replyDraftMentionsByCommentId: comments.replyDraftMentionsByCommentId,
          onUpdateCanvasSize: onSetCanvasSize,
          slideSizeEmu,
          onUpdateSlideSize: onSetSlideSize,
          editTemplateMode,
          slideMasters,
          onSetTemplateBackground,
          onGetTemplateBackgroundColor,
          mediaDataUrls,
          theme,
          tableStyleMap,
          onTableStyleMapChange,
          onDeleteTableStyle,
          panelWidth
        }
      )
    }
  );
}
function TitleBar(p) {
  const { t } = useTranslation();
  const editing = (p.mode === "edit" || p.mode === "master") && p.canEdit;
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  const quickAccess = useViewerOptionsContext().quickAccess;
  const [searchQuery, setSearchQuery] = useState("");
  const [searchFocused, setSearchFocused] = useState(false);
  const searchRef = useRef(null);
  const commandResults = filterCommands(searchQuery, t);
  const handleCommandSelect = useCallback(
    (entry) => {
      p.onCommandSearch?.(entry.command);
      setSearchQuery("");
      setSearchFocused(false);
    },
    [p]
  );
  const handleSearchKeyDown = useCallback(
    (e) => {
      if (e.key === "Enter" && searchQuery.trim()) {
        if (commandResults.length > 0) {
          handleCommandSelect(commandResults[0]);
        } else {
          p.onToggleFindReplace();
          setSearchFocused(false);
        }
      } else if (e.key === "Escape") {
        setSearchQuery("");
        setSearchFocused(false);
      }
    },
    [searchQuery, commandResults, handleCommandSelect, p]
  );
  useEffect(() => {
    const handler = (e) => {
      if (searchRef.current && !searchRef.current.contains(e.target)) {
        setSearchFocused(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, []);
  const statusKey = resolveTitleBarStatusKey({
    autosaveState: p.autosaveStatus?.state ?? "idle",
    isDirty: p.isDirty,
    autosaveEnabled: p.autosaveEnabled,
    disabledReason: p.autosaveStatus?.state === "disabled" ? p.autosaveStatus.reason : void 0
  });
  return /* @__PURE__ */ jsxs("div", { className: TITLE_BAR_CLASSES.container, "data-pptx-title-bar": "", children: [
    /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.logo, "aria-hidden": "true", children: "P" }),
    editing && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsxs("span", { className: TITLE_BAR_CLASSES.autosaveGroup, children: [
        /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.autosaveLabel, children: t("pptx.titleBar.autoSave") }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            role: "switch",
            "aria-checked": p.autosaveEnabled,
            onClick: p.onToggleAutosave,
            className: cn(
              TITLE_BAR_CLASSES.toggleTrack,
              p.autosaveEnabled ? TITLE_BAR_CLASSES.toggleTrackOn : TITLE_BAR_CLASSES.toggleTrackOff
            ),
            title: t("pptx.titleBar.toggleAutoSave"),
            "aria-label": t("pptx.titleBar.toggleAutoSave"),
            children: /* @__PURE__ */ jsx(
              "span",
              {
                className: cn(
                  TITLE_BAR_CLASSES.toggleKnob,
                  p.autosaveEnabled ? TITLE_BAR_CLASSES.toggleKnobOn : TITLE_BAR_CLASSES.toggleKnobOff
                )
              }
            )
          }
        ),
        /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.autosaveLabel, children: t(p.autosaveEnabled ? "pptx.titleBar.autoSaveOn" : "pptx.titleBar.autoSaveOff") })
      ] }),
      /* @__PURE__ */ jsx("div", { className: TITLE_BAR_CLASSES.separator }),
      p.onSave && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: p.onSave,
          className: TITLE_BAR_CLASSES.quickButton,
          title: t("pptx.titleBar.save"),
          "aria-label": t("pptx.titleBar.save"),
          children: /* @__PURE__ */ jsx(LuSave, { className: "w-3.5 h-3.5" })
        }
      ),
      !isHidden("undo") && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: p.onUndo,
          disabled: !p.canUndo,
          className: TITLE_BAR_CLASSES.quickButton,
          title: p.undoLabel ? t("pptx.toolbar.undoAction", { action: p.undoLabel }) : t("pptx.toolbar.undo"),
          "aria-label": t("pptx.toolbar.undo"),
          children: /* @__PURE__ */ jsx(LuUndo, { className: "w-3.5 h-3.5" })
        }
      ),
      !isHidden("redo") && /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: p.onRedo,
          disabled: !p.canRedo,
          className: TITLE_BAR_CLASSES.quickButton,
          title: p.redoLabel ? t("pptx.toolbar.redoAction", { action: p.redoLabel }) : t("pptx.toolbar.redo"),
          "aria-label": t("pptx.toolbar.redo"),
          children: /* @__PURE__ */ jsx(LuRedo, { className: "w-3.5 h-3.5" })
        }
      ),
      quickAccess.position !== "below" && /* @__PURE__ */ jsx(TitleBarQuickExtras, { quickAccess, onCommand: p.onQuickCommand }),
      /* @__PURE__ */ jsx("div", { className: TITLE_BAR_CLASSES.separator })
    ] }),
    /* @__PURE__ */ jsxs("span", { className: TITLE_BAR_CLASSES.fileGroup, children: [
      /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.fileName, children: p.fileName || t(TITLE_BAR_DEFAULT_FILE_KEY) }),
      editing && /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.statusDot, "aria-hidden": "true", children: "\u2022" }),
        /* @__PURE__ */ jsx(
          "span",
          {
            className: cn(
              TITLE_BAR_CLASSES.statusText,
              p.autosaveStatus?.state === "error" && p.autosaveEnabled && TITLE_BAR_CLASSES.statusError,
              p.autosaveStatus?.state === "saving" && p.autosaveEnabled && TITLE_BAR_CLASSES.statusSaving
            ),
            children: t(statusKey)
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.searchWrap, children: (p.mode === "edit" || p.mode === "master") && /* @__PURE__ */ jsxs("div", { ref: searchRef, className: "relative w-full max-w-md", children: [
      /* @__PURE__ */ jsxs(
        "div",
        {
          className: cn(
            TITLE_BAR_CLASSES.searchBox,
            (searchFocused || p.findReplaceOpen) && "text-foreground bg-background"
          ),
          children: [
            /* @__PURE__ */ jsx(LuSearch, { className: TITLE_BAR_CLASSES.searchIcon }),
            /* @__PURE__ */ jsx(
              "input",
              {
                type: "text",
                value: searchQuery,
                onChange: (e) => setSearchQuery(e.target.value),
                onFocus: () => setSearchFocused(true),
                onKeyDown: handleSearchKeyDown,
                placeholder: t("pptx.titleBar.searchPlaceholder"),
                className: "flex-1 bg-transparent text-[11px] outline-none placeholder:text-muted-foreground/60",
                "aria-label": t("pptx.titleBar.search")
              }
            )
          ]
        }
      ),
      searchFocused && searchQuery.trim() && /* @__PURE__ */ jsxs("div", { className: "absolute left-0 right-0 top-full z-50 mt-1 rounded-lg border border-border bg-popover shadow-xl max-h-64 overflow-y-auto", children: [
        commandResults.length > 0 ? /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx("div", { className: "px-3 py-1.5 text-[10px] font-semibold text-muted-foreground uppercase tracking-wider", children: t("pptx.titleBar.searchCommands") }),
          commandResults.slice(0, 8).map((entry) => /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-xs text-foreground hover:bg-accent transition-colors",
              onMouseDown: () => handleCommandSelect(entry),
              children: [
                /* @__PURE__ */ jsx("span", { className: "truncate", children: t(entry.labelKey) }),
                /* @__PURE__ */ jsx("span", { className: "ml-auto text-[10px] text-muted-foreground capitalize", children: entry.category })
              ]
            },
            entry.command
          ))
        ] }) : /* @__PURE__ */ jsx("div", { className: "px-3 py-2 text-xs text-muted-foreground", children: t("pptx.titleBar.searchNoResults") }),
        /* @__PURE__ */ jsx("div", { className: "border-t border-border/60", children: /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            className: "flex w-full items-center gap-2 px-3 py-1.5 text-xs text-foreground hover:bg-accent transition-colors",
            onMouseDown: () => {
              p.onToggleFindReplace();
              setSearchFocused(false);
              setSearchQuery("");
            },
            children: [
              /* @__PURE__ */ jsx(LuSearch, { className: "w-3 h-3 shrink-0" }),
              /* @__PURE__ */ jsxs("span", { children: [
                t("pptx.titleBar.searchContent"),
                " \u201C",
                searchQuery,
                "\u201D"
              ] })
            ]
          }
        ) })
      ] })
    ] }) }),
    /* @__PURE__ */ jsx("span", { className: TITLE_BAR_CLASSES.rightSpacer })
  ] });
}
function ViewerToolbarSection(props) {
  const {
    mode,
    canEdit,
    state: s,
    selectedElement,
    activeSlide,
    activeSlideIndex,
    zoom,
    history,
    findReplace,
    manipulation,
    insertHandlers,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    dialogs,
    slideOps,
    sectionOps,
    onApplyLayout,
    loadLayoutPreviews,
    customFontFamilies,
    ops,
    onSetMode,
    onPresentFromBeginning,
    onEnterPresenterView,
    onEnterRehearsalMode,
    onOpenSettings,
    onOpenHeaderFooter,
    onOpenShareDialog,
    onOpenFile,
    onOpenRecentFile,
    onToggleFormatPainter: onToggleFormatPainterProp,
    fileName,
    autosaveStatus,
    autosaveEnabled = true,
    onToggleAutosave,
    hiddenActions,
    recentPresentationsCount,
    aiEnabled,
    isAiPanelOpen,
    onToggleAiPanel,
    isProtectedView,
    onEnableEditing,
    onSetSnapToGrid,
    onSetSnapToShape,
    onSetShowGuides
  } = props;
  const { t } = useTranslation();
  const handleAddAnimation = useCallback(
    (preset, group) => {
      if (!selectedElement || !activeSlide) {
        return;
      }
      const current = activeSlide.animations ?? [];
      if (group === "motionPath") {
        propertyHandlers.handleUpdateSlide({
          animations: applyMotionPathPreset(current, selectedElement.id, preset)
        });
        return;
      }
      const existing = current.find((a) => a.elementId === selectedElement.id);
      const presetValue = preset;
      if (existing) {
        const updated = current.map(
          (a) => a.elementId === selectedElement.id ? { ...a, [group]: presetValue } : a
        );
        propertyHandlers.handleUpdateSlide({ animations: updated });
      } else {
        const newAnim = {
          elementId: selectedElement.id,
          [group]: presetValue,
          durationMs: 500,
          order: current.length,
          trigger: "onClick"
        };
        propertyHandlers.handleUpdateSlide({ animations: [...current, newAnim] });
      }
    },
    [selectedElement, activeSlide, propertyHandlers]
  );
  const handleRemoveAnimation = useCallback(() => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    const current = activeSlide.animations ?? [];
    const filtered = current.filter((a) => a.elementId !== selectedElement.id);
    propertyHandlers.handleUpdateSlide({ animations: filtered });
  }, [selectedElement, activeSlide, propertyHandlers]);
  const handleTransitionChange = useCallback(
    (updates) => {
      if (!activeSlide) {
        return;
      }
      const current = activeSlide.transition ?? { type: "none" };
      propertyHandlers.handleUpdateSlide({ transition: { ...current, ...updates } });
    },
    [activeSlide, propertyHandlers]
  );
  const scopedLayoutOptions = React20.useMemo(
    () => scopeLayoutOptionsToActiveSlide(s.layoutOptions, activeSlide),
    [s.layoutOptions, activeSlide]
  );
  const handleResetSlide = useCallback(() => {
    const path = resetSlideLayoutPath(activeSlide);
    if (path) {
      onApplyLayout?.(path);
    }
  }, [activeSlide, onApplyLayout]);
  const handleAddSection = useCallback(() => {
    sectionOps?.addSection(t("pptx.sections.defaultName"), activeSlideIndex);
  }, [sectionOps, activeSlideIndex, t]);
  const handleOpenSlideSize = useCallback(() => {
    s.setSelectedElementId(null);
    s.setSelectedElementIds([]);
    s.setSidebarPanelMode("properties");
    s.setIsInspectorPaneOpen(true);
  }, [s]);
  const handleSelectAll = useCallback(() => {
    const allIds = activeSlide?.elements.map((element) => element.id) ?? [];
    if (allIds.length > 0) {
      ops.applySelection(allIds[0], allIds);
    }
  }, [activeSlide, ops]);
  const handlePresentationPropertiesChange = useCallback(
    (updates) => {
      s.setPresentationProperties((prev) => ({ ...prev, ...updates }));
      history.markDirty();
    },
    [s, history]
  );
  const handleApplyTransitionToAll = useCallback(() => {
    const transition = activeSlide?.transition;
    if (!transition) {
      return;
    }
    ops.updateSlides((prev) => prev.map((sl) => ({ ...sl, transition })));
    history.markDirty();
  }, [activeSlide, ops, history]);
  const handleCommandSearch = useCallback(
    (command) => {
      const [category, action] = command.split(".");
      switch (category) {
        case "format":
          switch (action) {
            case "bold":
              ops.updateSelectedTextStyle({ bold: true });
              break;
            case "italic":
              ops.updateSelectedTextStyle({ italic: true });
              break;
            case "underline":
              ops.updateSelectedTextStyle({ underline: true });
              break;
            case "alignLeft":
              ops.updateSelectedTextStyle({ align: "left" });
              break;
            case "alignCenter":
              ops.updateSelectedTextStyle({ align: "center" });
              break;
            case "alignRight":
              ops.updateSelectedTextStyle({ align: "right" });
              break;
            case "clear":
              ops.updateSelectedTextStyle({
                bold: false,
                italic: false,
                underline: false,
                strikethrough: false
              });
              break;
          }
          break;
        case "insert":
          switch (action) {
            case "textBox":
              insertHandlers.handleAddTextBox();
              break;
            case "shape":
              insertHandlers.handleAddShape();
              break;
            case "image":
              s.imageInputRef.current?.click();
              break;
            case "media":
              s.mediaInputRef.current?.click();
              break;
            case "table":
              insertHandlers.handleAddTable();
              break;
            case "chart":
              insertHandlers.handleAddChart(DEFAULT_INSERT_CHART_KIND);
              break;
            case "smartArt":
              dialogs.setIsSmartArtDialogOpen(true);
              break;
            case "equation":
              dialogs.setIsEquationDialogOpen(true);
              break;
            case "link":
              dialogs.setIsHyperlinkDialogOpen(true);
              break;
          }
          break;
        case "view":
          switch (action) {
            case "toggleGrid":
              s.setShowGrid((p) => !p);
              break;
            case "toggleRulers":
              s.setShowRulers((p) => !p);
              break;
            case "slideSorter":
              s.setShowSlideSorter((p) => !p);
              break;
            case "zoomToFit":
              zoom.handleZoomToFit();
              break;
          }
          break;
        case "slideShow":
          switch (action) {
            case "fromBeginning":
              (onPresentFromBeginning ?? (() => onSetMode("present")))();
              break;
            case "presenterView":
              onEnterPresenterView();
              break;
          }
          break;
        case "design":
          switch (action) {
            case "browseThemes":
              s.setIsThemeGalleryOpen((p) => !p);
              break;
            case "slideSize":
              handleOpenSlideSize();
              break;
          }
          break;
        case "arrange":
          switch (action) {
            case "bringToFront":
              manipulation.handleMoveLayerToEdge("front");
              break;
            case "sendToBack":
              manipulation.handleMoveLayerToEdge("back");
              break;
            case "duplicate":
              manipulation.handleDuplicate();
              break;
          }
          break;
        case "review":
          switch (action) {
            case "spelling":
              s.setSpellCheckEnabled((p) => !p);
              break;
            case "accessibility":
              dialogs.handleRunAccessibilityCheck();
              break;
          }
          break;
      }
    },
    [
      ops,
      insertHandlers,
      s,
      dialogs,
      zoom,
      onSetMode,
      onPresentFromBeginning,
      onEnterPresenterView,
      manipulation,
      handleOpenSlideSize
    ]
  );
  const handleQuickAccessCommand = useCallback(
    (id) => {
      const handlers = {
        presentFromStart: onPresentFromBeginning ?? (() => onSetMode("present")),
        print: printHandlers.handlePrint,
        exportPdf: exportHandlers.handleExportPdf,
        newSlide: slideOps.handleAddSlide,
        spellCheck: () => s.setSpellCheckEnabled((p) => !p),
        zoomIn: zoom.handleZoomIn,
        zoomOut: zoom.handleZoomOut
      };
      handlers[id]?.();
    },
    [onSetMode, onPresentFromBeginning, printHandlers, exportHandlers, slideOps, s, zoom]
  );
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    !dialogs.isNarrowViewport && /* @__PURE__ */ jsx(
      TitleBar,
      {
        mode,
        canEdit,
        fileName,
        isDirty: s.isDirty,
        autosaveStatus,
        autosaveEnabled,
        onToggleAutosave: onToggleAutosave ?? (() => {
        }),
        canUndo: history.canUndo,
        canRedo: history.canRedo,
        undoLabel: history.undoLabel,
        redoLabel: history.redoLabel,
        onUndo: history.handleUndo,
        onRedo: history.handleRedo,
        onSave: exportHandlers.handleSaveAsPptx,
        findReplaceOpen: findReplace.findReplaceOpen,
        onToggleFindReplace: () => findReplace.setFindReplaceOpen(!findReplace.findReplaceOpen),
        onCommandSearch: handleCommandSearch,
        onQuickCommand: handleQuickAccessCommand,
        hiddenActions
      }
    ),
    /* @__PURE__ */ jsx(
      Toolbar,
      {
        fileName,
        mode,
        canEdit,
        isProtectedView,
        onEnableEditing,
        onQuickCommand: handleQuickAccessCommand,
        hiddenActions,
        recentPresentationsCount,
        isNarrowViewport: dialogs.isNarrowViewport,
        isSidebarCollapsed: !s.isSlidesPaneOpen,
        isInspectorPaneOpen: s.isInspectorPaneOpen,
        isCompactToolbarOpen: s.isCompactToolbarOpen,
        toolbarSection: s.toolbarSection,
        scale: zoom.scale,
        canUndo: history.canUndo,
        canRedo: history.canRedo,
        undoLabel: history.undoLabel,
        redoLabel: history.redoLabel,
        findReplaceOpen: findReplace.findReplaceOpen,
        selectedElement,
        tableEditorState: s.tableEditorState,
        editTemplateMode: s.editTemplateMode,
        newShapeType: s.newShapeType,
        activeTool: s.activeTool,
        drawingColor: s.drawingColor,
        drawingWidth: s.drawingWidth,
        clipboardPayload: s.clipboardPayload,
        onSetMode,
        onPresentFromBeginning,
        onToggleSidebar: () => s.setIsSlidesPaneOpen((p) => !p),
        onToggleInspector: () => s.setIsInspectorPaneOpen((p) => !p),
        onOpenAnimationPanel: () => {
          s.setIsInspectorPaneOpen(true);
          s.setSidebarPanelMode("properties");
        },
        onAddAnimation: handleAddAnimation,
        onRemoveAnimation: handleRemoveAnimation,
        onToggleCompactToolbar: () => s.setIsCompactToolbarOpen((p) => !p),
        onSetToolbarSection: s.setToolbarSection,
        onZoomIn: zoom.handleZoomIn,
        onZoomOut: zoom.handleZoomOut,
        onZoomToFit: zoom.handleZoomToFit,
        onUndo: history.handleUndo,
        onRedo: history.handleRedo,
        onToggleFindReplace: () => findReplace.setFindReplaceOpen(!findReplace.findReplaceOpen),
        onSetNewShapeType: s.setNewShapeType,
        onAddTextBox: insertHandlers.handleAddTextBox,
        onAddShape: insertHandlers.handleAddShape,
        onAddTable: insertHandlers.handleAddTable,
        onAddChart: insertHandlers.handleAddChart,
        onAddSmartArt: () => dialogs.setIsSmartArtDialogOpen(true),
        onAddEquation: () => {
          dialogs.setEditingEquationOmml(null);
          dialogs.setIsEquationDialogOpen(true);
        },
        onAddActionButton: insertHandlers.handleAddActionButton,
        onInsertField: insertHandlers.handleInsertField,
        onOpenHeaderFooter,
        onOpenImagePicker: () => s.imageInputRef.current?.click(),
        onOpenMediaPicker: () => s.mediaInputRef.current?.click(),
        onSetActiveTool: s.setActiveTool,
        onSetDrawingColor: s.setDrawingColor,
        onSetDrawingWidth: s.setDrawingWidth,
        onSetEditTemplateMode: s.setEditTemplateMode,
        spellCheckEnabled: s.spellCheckEnabled,
        showGrid: s.showGrid,
        showRulers: s.showRulers,
        showGuides: s.showGuides,
        snapToGrid: s.snapToGrid,
        snapToShape: s.snapToShape,
        onSetSpellCheckEnabled: s.setSpellCheckEnabled,
        onSetShowGrid: s.setShowGrid,
        onSetShowRulers: s.setShowRulers,
        onSetShowGuides: onSetShowGuides ?? s.setShowGuides,
        onSetSnapToGrid: onSetSnapToGrid ?? s.setSnapToGrid,
        onSetSnapToShape: onSetSnapToShape ?? s.setSnapToShape,
        onAddGuide: dialogs.handleAddGuide,
        onAlignElements: manipulation.handleAlignElements,
        onDistributeElements: manipulation.handleDistributeElements,
        canDistribute: manipulation.canDistribute,
        onCopy: manipulation.handleCopy,
        onCut: manipulation.handleCut,
        onPaste: manipulation.handlePaste,
        onFlip: manipulation.handleFlip,
        onMoveLayer: manipulation.handleMoveLayer,
        onMoveLayerToEdge: manipulation.handleMoveLayerToEdge,
        onGroupElements: manipulation.handleGroupElements,
        onUngroupElement: manipulation.handleUngroupElement,
        onUpdateElementStyle: ops.updateSelectedShapeStyle,
        selectedCount: s.selectedElementIds.length > 0 ? s.selectedElementIds.length : selectedElement ? 1 : 0,
        selectionGroupable: manipulation.selectionGroupable,
        onOpenHyperlinkDialog: () => dialogs.setIsHyperlinkDialogOpen(true),
        onDuplicate: manipulation.handleDuplicate,
        onDelete: manipulation.handleDelete,
        onExportPng: exportHandlers.handleExportPng,
        onExportPdf: exportHandlers.handleExportPdf,
        onExportVideo: exportHandlers.handleExportVideo,
        onExportGif: exportHandlers.handleExportGif,
        onExportJson: exportHandlers.handleExportJson,
        onOpenFile,
        onOpenRecentFile,
        onCreatePresentation: (templateId) => {
          s.setSlides(createBackstagePresentation(templateId));
          s.setActiveSlideIndex(0);
          s.setSelectedElementId(null);
          s.setSelectedElementIds([]);
          s.setTemplateElementsBySlideId({});
          s.setIsDirty(true);
        },
        onOpenShareDialog,
        onSaveAsPptx: exportHandlers.handleSaveAsPptx,
        onSaveAsPpsx: exportHandlers.handleSaveAsPpsx,
        onSaveAsPptm: exportHandlers.handleSaveAsPptm,
        hasMacros: s.hasMacros,
        onCopySlideAsImage: exportHandlers.handleCopySlideAsImage,
        onPrint: printHandlers.handlePrint,
        onToggleShortcuts: () => s.setIsShortcutHelpOpen((p) => !p),
        onOpenSettings,
        onRunAccessibilityCheck: dialogs.handleRunAccessibilityCheck,
        onToggleSlideSorter: () => s.setShowSlideSorter((p) => !p),
        onOpenReadingView: () => s.setShowReadingView(true),
        onOpenOutlineView: () => s.setShowOutlineView(true),
        onUpdateTextStyle: ops.updateSelectedTextStyle,
        onTransformTextCase: ops.updateSelectedTextCase,
        isOverflowMenuOpen: s.isOverflowMenuOpen,
        onSetOverflowMenuOpen: s.setIsOverflowMenuOpen,
        layoutOptions: scopedLayoutOptions,
        currentLayoutPath: activeSlide?.layoutPath,
        loadLayoutPreviews,
        themeFonts: {
          heading: s.theme?.fontScheme?.majorFont?.latin,
          body: s.theme?.fontScheme?.minorFont?.latin
        },
        embeddedFontFamilies: s.embeddedFonts.map((font) => font.name),
        customFontFamilies,
        onInsertSlideFromLayout: slideOps.handleInsertSlideFromLayout,
        onApplyLayout,
        onInsertSlideFromTemplate: slideOps.handleInsertSlideFromTemplate,
        templateScheme: templateSchemeFromTheme(s.theme?.colorScheme),
        customShows: s.customShows,
        activeCustomShowId: s.activeCustomShowId,
        onSetActiveCustomShowId: s.setActiveCustomShowId,
        onCreateCustomShow: dialogs.handleCreateCustomShow,
        onRenameActiveCustomShow: dialogs.handleRenameActiveCustomShow,
        onDeleteActiveCustomShow: dialogs.handleDeleteActiveCustomShow,
        onToggleCurrentSlideInActiveShow: dialogs.handleToggleCurrentSlideInActiveShow,
        isCurrentSlideInActiveShow: dialogs.isCurrentSlideInActiveShow,
        onEnterMasterView: dialogs.handleEnterMasterView,
        onCloseMasterView: dialogs.handleCloseMasterView,
        onToggleVersionHistory: () => propertyHandlers.setIsVersionHistoryOpen((p) => !p),
        onOpenPasswordProtection: () => dialogs.setIsPasswordDialogOpen(true),
        onOpenDocumentProperties: () => dialogs.setIsDocPropsDialogOpen(true),
        onOpenSlideSize: handleOpenSlideSize,
        onOpenFontEmbedding: () => dialogs.setIsFontEmbeddingOpen(true),
        onOpenDigitalSignatures: () => dialogs.setIsDigitalSigDialogOpen(true),
        onEnterPresenterView,
        onEnterRehearsalMode,
        onToggleThemeEditor: () => s.setIsThemeEditorOpen((p) => !p),
        isThemeEditorOpen: s.isThemeEditorOpen,
        onToggleThemeGallery: () => s.setIsThemeGalleryOpen((p) => !p),
        isThemeGalleryOpen: s.isThemeGalleryOpen,
        onCompare: propertyHandlers.handleCompare,
        onToggleComments: () => {
          s.setSidebarPanelMode("comments");
          if (!s.isInspectorPaneOpen) {
            s.setIsInspectorPaneOpen(true);
          }
        },
        isCommentsPanelOpen: s.isInspectorPaneOpen,
        slideCommentCount: activeSlide?.comments?.length ?? 0,
        formatPainterActive: s.formatPainterActive,
        canActivateFormatPainter: hasCopyableFormat(selectedElement),
        onToggleFormatPainter: onToggleFormatPainterProp ?? (() => s.setFormatPainterActive((p) => !p)),
        isSelectionPaneOpen: s.isSelectionPaneOpen,
        onToggleSelectionPane: () => s.setIsSelectionPaneOpen((p) => !p),
        eyedropperActive: s.eyedropperActive,
        onToggleEyedropper: () => s.setEyedropperActive((p) => !p),
        onOpenSetUpSlideShow: () => dialogs.setIsSetUpSlideShowOpen(true),
        onToggleHideSlide: () => slideOps.handleToggleHideSlides([activeSlideIndex]),
        activeSlideHidden: Boolean(activeSlide?.hidden),
        onOpenBroadcastDialog: () => dialogs.setIsBroadcastDialogOpen(true),
        onToggleSubtitles: dialogs.handleToggleSubtitles,
        showSubtitles: Boolean(s.presentationProperties.showSubtitles),
        activeSlide,
        onTransitionChange: handleTransitionChange,
        onApplyTransitionToAll: handleApplyTransitionToAll,
        onResetSlide: handleResetSlide,
        onAddSection: sectionOps ? handleAddSection : void 0,
        onSelectAll: handleSelectAll,
        presentationProperties: s.presentationProperties,
        onPresentationPropertiesChange: handlePresentationPropertiesChange,
        aiEnabled,
        isAiPanelOpen,
        onToggleAiPanel
      }
    ),
    s.hasDigitalSignatures && /* @__PURE__ */ jsx("div", { className: "flex items-center px-3 py-1 z-10", children: /* @__PURE__ */ jsx(
      SignatureStatusBadge,
      {
        hasSignatures: s.hasDigitalSignatures,
        signatureCount: s.digitalSignatureCount,
        onClick: () => dialogs.setIsDigitalSigDialogOpen(true)
      }
    ) }),
    /* @__PURE__ */ jsx(
      "input",
      {
        ref: s.imageInputRef,
        type: "file",
        name: "image-upload",
        accept: "image/*",
        className: "hidden",
        onChange: insertHandlers.handleImageFileChange
      }
    ),
    /* @__PURE__ */ jsx(
      "input",
      {
        ref: s.mediaInputRef,
        type: "file",
        name: "media-upload",
        accept: "video/*,audio/*",
        className: "hidden",
        onChange: insertHandlers.handleMediaFileChange
      }
    )
  ] });
}
var TEMPLATES = EQUATION_TEMPLATES;
var TEMPLATE_MATHML = compileEquationTemplateMathMl();
function MathMlPreview({ mathml }) {
  const containerRef = useRef(null);
  useEffect(() => {
    if (!containerRef.current) {
      return;
    }
    if (mathml) {
      containerRef.current.innerHTML = sanitizeMathMl(mathml);
    } else {
      containerRef.current.innerHTML = "";
    }
  }, [mathml]);
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref: containerRef,
      className: "flex items-center justify-center min-h-[80px] text-2xl text-foreground",
      style: { fontFamily: '"Cambria Math", "STIX Two Math", serif' }
    }
  );
}
function EquationEditorDialog({
  isOpen,
  onClose,
  onInsert,
  existingOmml
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const initialLatex = useMemo(() => {
    if (!existingOmml) {
      return "";
    }
    return convertOmmlToLatex(existingOmml);
  }, [existingOmml]);
  const [latex, setLatex] = useState(initialLatex);
  const deferredLatex = useDeferredValue(latex);
  const textareaRef = useRef(null);
  const dialogRef = useRef(null);
  useModalFocus(isOpen, dialogRef, onClose, textareaRef);
  useEffect(() => {
    if (isOpen) {
      setLatex(initialLatex);
    }
  }, [isOpen, initialLatex]);
  const { mathml, omml } = useMemo(() => compileLatexEquation(deferredLatex), [deferredLatex]);
  const handleInsert = useCallback(() => {
    if (!latex.trim()) {
      return;
    }
    let insertOmml = omml;
    try {
      const fresh = convertLatexToOmml(latex);
      if (fresh && Object.keys(fresh).length > 0) {
        insertOmml = fresh;
      }
    } catch {
    }
    onInsert(insertOmml);
    onClose();
  }, [latex, omml, onInsert, onClose]);
  const handleTemplateClick = useCallback((templateLatex) => {
    setLatex(templateLatex);
  }, []);
  const handleKeyDown = useCallback(
    (e) => {
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        handleInsert();
      }
      if (e.key === "Escape") {
        e.preventDefault();
        onClose();
      }
    },
    [handleInsert, onClose]
  );
  if (!isOpen) {
    return null;
  }
  const isEditing = Boolean(existingOmml);
  const hasContent = latex.trim().length > 0 && Object.keys(omml).length > 0;
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 backdrop-blur-sm",
      onClick: (e) => {
        if (e.target === e.currentTarget) {
          onClose();
        }
      },
      onKeyDown: handleKeyDown,
      children: /* @__PURE__ */ jsxs(
        "div",
        {
          ref: dialogRef,
          style: panelStyle,
          className: "bg-background border border-border rounded-xl shadow-2xl w-[640px] max-h-[85vh] flex flex-col overflow-hidden max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
          onClick: (e) => e.stopPropagation(),
          role: "dialog",
          "aria-modal": "true",
          tabIndex: -1,
          "aria-label": isEditing ? t("pptx.equation.editTitle", "Edit Equation") : t("pptx.equation.insertTitle", "Insert Equation"),
          children: [
            /* @__PURE__ */ jsxs(
              "div",
              {
                ...dragHandlers,
                className: "flex items-center justify-between px-5 py-3 border-b border-border touch-none",
                children: [
                  /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: isEditing ? t("pptx.equation.editTitle", "Edit Equation") : t("pptx.equation.insertTitle", "Insert Equation") }),
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      onClick: onClose,
                      className: "p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors",
                      "aria-label": t("pptx.settings.close"),
                      children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
                    }
                  )
                ]
              }
            ),
            /* @__PURE__ */ jsxs("div", { className: "flex-1 overflow-y-auto px-5 py-4 space-y-4", children: [
              /* @__PURE__ */ jsx("div", { className: "rounded-lg border border-border bg-muted/60 p-4 min-h-[80px] flex items-center justify-center", children: hasContent ? /* @__PURE__ */ jsx(MathMlPreview, { mathml }) : /* @__PURE__ */ jsx("span", { className: "text-sm text-muted-foreground italic", children: t("pptx.equation.previewPlaceholder", "Equation preview will appear here") }) }),
              /* @__PURE__ */ jsxs("div", { children: [
                /* @__PURE__ */ jsx("label", { className: "block text-xs font-medium text-muted-foreground mb-1.5", children: t("pptx.equation.latexInput", "LaTeX Input") }),
                /* @__PURE__ */ jsx(
                  "textarea",
                  {
                    ref: textareaRef,
                    value: latex,
                    onChange: (e) => setLatex(e.target.value),
                    placeholder: "\\\\frac{a}{b} + \\\\sqrt{c}",
                    className: "w-full h-24 px-3 py-2 rounded-lg border border-border bg-muted text-foreground text-sm font-mono resize-none focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary placeholder:text-muted-foreground",
                    spellCheck: false
                  }
                ),
                /* @__PURE__ */ jsx("p", { className: "mt-1 text-[11px] text-muted-foreground", children: t("pptx.equation.latexHint", "Use LaTeX syntax. Ctrl+Enter to insert.") })
              ] }),
              /* @__PURE__ */ jsxs("div", { children: [
                /* @__PURE__ */ jsx("h3", { className: "text-xs font-medium text-muted-foreground mb-2", children: t("pptx.equation.templates", "Common Templates") }),
                /* @__PURE__ */ jsx("div", { className: "grid grid-cols-4 gap-1.5", children: TEMPLATES.map((tmpl, idx) => {
                  const tmplMathml = TEMPLATE_MATHML[idx];
                  return /* @__PURE__ */ jsxs(
                    "button",
                    {
                      type: "button",
                      onClick: () => handleTemplateClick(tmpl.latex),
                      className: cn(
                        "flex flex-col items-center gap-1 p-2 rounded-lg border transition-colors",
                        latex === tmpl.latex ? "border-primary bg-primary/10" : "border-border bg-muted/40 hover:bg-accent/60 hover:border-border"
                      ),
                      title: t(tmpl.i18nKey, tmpl.label),
                      children: [
                        /* @__PURE__ */ jsx(
                          "div",
                          {
                            className: "text-sm text-foreground h-8 flex items-center justify-center overflow-hidden",
                            style: {
                              fontFamily: '"Cambria Math", "STIX Two Math", serif'
                            },
                            dangerouslySetInnerHTML: { __html: tmplMathml }
                          }
                        ),
                        /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground truncate w-full text-center", children: t(tmpl.i18nKey, tmpl.label) })
                      ]
                    },
                    tmpl.latex
                  );
                }) })
              ] })
            ] }),
            /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 px-5 py-3 border-t border-border", children: [
              /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  onClick: onClose,
                  className: "px-4 py-1.5 rounded-lg text-xs text-foreground hover:bg-accent transition-colors",
                  children: t("pptx.equation.cancel", "Cancel")
                }
              ),
              /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  onClick: handleInsert,
                  disabled: !hasContent,
                  className: "px-4 py-1.5 rounded-lg text-xs font-medium bg-primary hover:bg-primary/80 text-white disabled:opacity-40 disabled:cursor-not-allowed transition-colors",
                  children: isEditing ? t("pptx.equation.update", "Update") : t("pptx.equation.insert", "Insert")
                }
              )
            ] })
          ]
        }
      )
    }
  );
}
var PREVIEW_TILE_WIDTH2 = 64;
function getPreviewForLayout(layout) {
  const scale = PREVIEW_TILE_WIDTH2 / SMARTART_PREVIEW_ELEMENT_WIDTH;
  return /* @__PURE__ */ jsx(
    "div",
    {
      "aria-hidden": true,
      className: "overflow-hidden pointer-events-none",
      style: {
        width: PREVIEW_TILE_WIDTH2,
        height: Math.round(SMARTART_PREVIEW_ELEMENT_HEIGHT * scale)
      },
      children: /* @__PURE__ */ jsx(
        "div",
        {
          style: {
            width: SMARTART_PREVIEW_ELEMENT_WIDTH,
            height: SMARTART_PREVIEW_ELEMENT_HEIGHT,
            transform: `scale(${scale})`,
            transformOrigin: "top left"
          },
          children: /* @__PURE__ */ jsx(SmartArtRenderer, { element: buildSmartArtPreviewElement(layout) })
        }
      )
    }
  );
}
function InsertSmartArtDialog({
  isOpen,
  onClose,
  onInsert
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const [activeCategory, setActiveCategory] = useState("list");
  const [selectedLayout, setSelectedLayout] = useState(null);
  const dialogRef = useRef(null);
  useModalFocus(isOpen, dialogRef, onClose);
  const filteredPresets = PRESETS.filter((p) => p.category === activeCategory);
  const handleInsert = useCallback(() => {
    if (!selectedLayout) {
      return;
    }
    const preset = PRESETS.find((p) => p.layout === selectedLayout);
    if (!preset) {
      return;
    }
    onInsert(preset.layout, preset.defaultItems);
    onClose();
  }, [selectedLayout, onInsert, onClose]);
  if (!isOpen) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        onClick: onClose,
        "aria-label": t("pptx.smartart.close")
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            ref: dialogRef,
            style: panelStyle,
            className: "pointer-events-auto w-[600px] max-w-[90vw] max-h-[80vh] rounded-lg border border-border bg-background shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.smartart.insertTitle"),
            tabIndex: -1,
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-4 py-3 border-b border-border touch-none",
                  children: [
                    /* @__PURE__ */ jsx("h2", { className: "text-sm font-medium text-foreground", children: t("pptx.smartart.insertTitle") }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "p-1 rounded hover:bg-muted transition-colors",
                        "aria-label": t("pptx.smartart.close"),
                        children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsxs("div", { className: "flex flex-1 overflow-hidden", children: [
                /* @__PURE__ */ jsx(
                  "nav",
                  {
                    className: "w-40 border-r border-border py-2",
                    "aria-label": t("pptx.insertSmartArt.categories"),
                    children: CATEGORIES.map((cat) => /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: () => {
                          setActiveCategory(cat.id);
                          setSelectedLayout(null);
                        },
                        className: cn(
                          "w-full text-left px-3 py-1.5 text-xs transition-colors",
                          activeCategory === cat.id ? "bg-primary text-white" : "text-foreground hover:bg-muted"
                        ),
                        children: t(cat.label)
                      },
                      cat.id
                    ))
                  }
                ),
                /* @__PURE__ */ jsx("div", { className: "flex-1 p-3 overflow-y-auto", children: /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: "grid grid-cols-3 gap-2",
                    role: "listbox",
                    "aria-label": t("pptx.insertSmartArt.layouts"),
                    children: filteredPresets.map((preset) => /* @__PURE__ */ jsxs(
                      "button",
                      {
                        type: "button",
                        role: "option",
                        "aria-selected": selectedLayout === preset.layout,
                        onClick: () => setSelectedLayout(preset.layout),
                        onDoubleClick: () => {
                          setSelectedLayout(preset.layout);
                          onInsert(preset.layout, preset.defaultItems);
                          onClose();
                        },
                        className: cn(
                          "flex flex-col items-center gap-1 p-2 rounded border transition-colors",
                          selectedLayout === preset.layout ? "border-primary bg-primary/20" : "border-border hover:border-border hover:bg-muted/50"
                        ),
                        children: [
                          /* @__PURE__ */ jsx("div", { className: "w-16 h-12 flex items-center justify-center bg-muted rounded", children: getPreviewForLayout(preset.layout) }),
                          /* @__PURE__ */ jsx("span", { className: "text-[10px] text-foreground text-center leading-tight", children: t(preset.labelKey) })
                        ]
                      },
                      preset.layout
                    ))
                  }
                ) })
              ] }),
              /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 px-4 py-3 border-t border-border", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 text-xs rounded bg-muted hover:bg-accent text-foreground transition-colors",
                    children: t("pptx.smartart.cancel")
                  }
                ),
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: handleInsert,
                    disabled: !selectedLayout,
                    className: cn(
                      "px-3 py-1.5 text-xs rounded transition-colors",
                      selectedLayout ? "bg-primary hover:bg-primary/80 text-white" : "bg-muted text-muted-foreground cursor-not-allowed"
                    ),
                    children: t("pptx.smartart.insert")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function ViewerDialogGroup(props) {
  const {
    dialogs,
    insertHandlers,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    annotations,
    slides,
    activeSlideIndex,
    canvasSize,
    filePath,
    coreProperties,
    customProperties,
    appProperties,
    embeddedFonts,
    hasDigitalSignatures,
    digitalSignatureCount,
    presentationProperties,
    customShows,
    selectedElements,
    isEncryptedDialogOpen,
    setIsEncryptedDialogOpen,
    showKeepAnnotationsDialog,
    onKeepAnnotations,
    onDiscardAnnotations
  } = props;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      VersionHistoryPanel,
      {
        isOpen: propertyHandlers.isVersionHistoryOpen,
        filePath,
        onClose: () => propertyHandlers.setIsVersionHistoryOpen(false),
        onRestore: propertyHandlers.handleRestoreVersion
      }
    ),
    /* @__PURE__ */ jsx(
      ComparePanel,
      {
        isOpen: propertyHandlers.isComparePanelOpen,
        compareResult: propertyHandlers.compareResult,
        canvasSize,
        onClose: () => propertyHandlers.setIsComparePanelOpen(false),
        onAcceptSlide: propertyHandlers.handleAcceptSlide,
        onRejectSlide: propertyHandlers.handleRejectSlide,
        onAcceptAll: propertyHandlers.handleAcceptAllSlides
      }
    ),
    /* @__PURE__ */ jsx(
      PasswordProtectionDialog,
      {
        isOpen: dialogs.isPasswordDialogOpen,
        isCurrentlyProtected: dialogs.isPasswordProtected,
        onClose: () => dialogs.setIsPasswordDialogOpen(false),
        onSetPassword: dialogs.handleSetPassword,
        onRemovePassword: dialogs.handleRemovePassword
      }
    ),
    /* @__PURE__ */ jsx(
      DocumentPropertiesDialog,
      {
        isOpen: dialogs.isDocPropsDialogOpen,
        coreProperties: coreProperties ?? {},
        customProperties,
        appProperties,
        onClose: () => dialogs.setIsDocPropsDialogOpen(false),
        onSave: (core, custom, app) => {
          propertyHandlers.handleUpdateCoreProperties(core);
          propertyHandlers.handleUpdateCustomProperties(custom);
          if (app) {
            propertyHandlers.handleUpdateAppProperties(app);
          }
        }
      }
    ),
    /* @__PURE__ */ jsx(
      FontEmbeddingPanel,
      {
        isOpen: dialogs.isFontEmbeddingOpen,
        embedFontsEnabled: dialogs.embedFontsEnabled,
        usedFontFamilies: propertyHandlers.usedFontFamilies,
        embeddedFonts: embeddedFonts.map((f) => f.name),
        canEmbedFonts: dialogs.fontEmbedding.interactive,
        embedUnavailableKey: dialogs.fontEmbedding.disabledReasonKey,
        onClose: () => dialogs.setIsFontEmbeddingOpen(false),
        onToggleEmbedFonts: dialogs.setEmbedFontsEnabled
      }
    ),
    /* @__PURE__ */ jsx(
      DigitalSignaturesDialog,
      {
        isOpen: dialogs.isDigitalSigDialogOpen,
        onClose: () => dialogs.setIsDigitalSigDialogOpen(false),
        hasSignatures: hasDigitalSignatures,
        signatureCount: digitalSignatureCount
      }
    ),
    /* @__PURE__ */ jsx(
      SignatureStrippedDialog,
      {
        isOpen: dialogs.isSignatureStrippedDialogOpen,
        signatureCount: digitalSignatureCount,
        onConfirm: () => dialogs.setIsSignatureStrippedDialogOpen(false),
        onCancel: () => dialogs.setIsSignatureStrippedDialogOpen(false)
      }
    ),
    /* @__PURE__ */ jsx(
      EncryptedFileDialog,
      {
        isOpen: isEncryptedDialogOpen,
        onClose: () => setIsEncryptedDialogOpen(false)
      }
    ),
    /* @__PURE__ */ jsx(
      InsertSmartArtDialog,
      {
        isOpen: dialogs.isSmartArtDialogOpen,
        onClose: () => dialogs.setIsSmartArtDialogOpen(false),
        onInsert: insertHandlers.handleInsertSmartArt
      }
    ),
    /* @__PURE__ */ jsx(
      EquationEditorDialog,
      {
        isOpen: dialogs.isEquationDialogOpen,
        onClose: () => dialogs.setIsEquationDialogOpen(false),
        onInsert: dialogs.editingEquationOmml ? insertHandlers.handleUpdateEquation : insertHandlers.handleInsertEquation,
        existingOmml: dialogs.editingEquationOmml
      }
    ),
    /* @__PURE__ */ jsx(
      HyperlinkEditDialog,
      {
        open: dialogs.isHyperlinkDialogOpen,
        initialUrl: selectedElements[0]?.actionClick?.url,
        initialTooltip: selectedElements[0]?.actionClick?.tooltip,
        initialAction: selectedElements[0]?.actionClick?.action,
        slideCount: slides.length,
        onConfirm: insertHandlers.handleHyperlinkConfirm,
        onCancel: () => dialogs.setIsHyperlinkDialogOpen(false)
      }
    ),
    /* @__PURE__ */ jsx(
      ExportProgressModal,
      {
        isOpen: exportHandlers.exportModalOpen,
        title: exportHandlers.exportModalTitle,
        progress: exportHandlers.exportProgress,
        statusMessage: exportHandlers.exportStatusMessage,
        onCancel: exportHandlers.handleCancelExport
      }
    ),
    /* @__PURE__ */ jsx(
      SetUpSlideShowDialog,
      {
        open: dialogs.isSetUpSlideShowOpen,
        onClose: () => dialogs.setIsSetUpSlideShowOpen(false),
        properties: presentationProperties,
        onSave: dialogs.handleSaveSlideShowSettings,
        customShows,
        slideCount: slides.length
      }
    ),
    /* @__PURE__ */ jsx(
      PrintDialog,
      {
        open: printHandlers.isPrintDialogOpen,
        onClose: () => printHandlers.setIsPrintDialogOpen(false),
        onPrint: printHandlers.handlePrintSvg,
        slides,
        activeSlideIndex,
        defaultSlidesPerPage: printPropertiesSlidesPerPage(presentationProperties.printProperties),
        defaultFrameSlides: printPropertiesFrameSlides(presentationProperties.printProperties)
      }
    ),
    /* @__PURE__ */ jsx(
      KeepAnnotationsDialog,
      {
        isOpen: showKeepAnnotationsDialog,
        annotationCount: Array.from(annotations.allSlideAnnotations.values()).reduce(
          (sum, strokes) => sum + strokes.length,
          0
        ),
        slideCount: annotations.allSlideAnnotations.size,
        onKeep: onKeepAnnotations,
        onDiscard: onDiscardAnnotations
      }
    )
  ] });
}
function ViewerPresentationLayer(props) {
  const {
    mode,
    slides,
    canvasSize,
    templateElements,
    presentation,
    activeCustomShow,
    authoredRange,
    onExitPresentation,
    isMobile,
    onUpdateNotes
  } = props;
  const presenterActive = mode === "present" && presentation.presenterMode;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    presenterActive && (isMobile ? /* @__PURE__ */ jsx(
      MobilePresenterView,
      {
        slides,
        currentSlideIndex: presentation.presentationSlideIndex,
        canvasSize,
        templateElements,
        activeCustomShow,
        authoredRange,
        presentationStartTime: presentation.presentationStartTime,
        onMovePresentationSlide: presentation.movePresentationSlide,
        onExit: onExitPresentation
      }
    ) : /* @__PURE__ */ jsx(
      PresenterView,
      {
        slides,
        currentSlideIndex: presentation.presentationSlideIndex,
        canvasSize,
        templateElements,
        activeCustomShow,
        authoredRange,
        presentationStartTime: presentation.presentationStartTime,
        onMovePresentationSlide: presentation.movePresentationSlide,
        onExit: onExitPresentation,
        onOpenAudienceWindow: presentation.openAudienceWindow,
        onCloseAudienceWindow: presentation.closeAudienceWindow,
        isAudienceWindowOpen: presentation.isAudienceWindowOpen(),
        endOfShowVisible: presentation.endOfShowVisible,
        snapshot: presentation.presenterSnapshot,
        onNavigateToSlide: presentation.navigateToSlide,
        onToggleTimer: presentation.togglePresenterTimer,
        onResetTimer: presentation.resetPresenterTimer,
        onStepZoom: presentation.stepPresenterZoom,
        onResetZoom: presentation.resetPresenterZoom,
        onSetBlackout: presentation.setPresenterBlackout,
        onUpdateSnapshot: presentation.updatePresenterSnapshot,
        onToggleSubtitles: () => presentation.setPresenterSubtitlesVisible(
          !presentation.presenterSnapshot.subtitlesVisible
        ),
        onSwapDisplays: () => void presentation.swapPresenterDisplays(),
        onUpdateNotes
      }
    )),
    mode === "present" && presentation.rehearsing && /* @__PURE__ */ jsx(
      RehearseTimingsHud,
      {
        presentationStartTime: presentation.presentationStartTime,
        slideStartTime: presentation.slideStartTime,
        paused: presentation.rehearsalPaused,
        onTogglePause: presentation.toggleRehearsalPause
      }
    ),
    presentation.showRehearsalSummary && /* @__PURE__ */ jsx(
      RehearseTimingsSummary,
      {
        slides,
        canvasSize,
        recordedTimings: presentation.recordedTimings,
        onSave: presentation.saveRehearsalTimings,
        onDiscard: presentation.dismissRehearsalSummary
      }
    )
  ] });
}
function PresentationStage({
  activeSlide,
  templateElements,
  canvasSize,
  mediaDataUrls,
  presentationElementStates,
  presentationKeyframesCss,
  onActionClick,
  onHyperlinkClick,
  allSlides,
  onZoomClick,
  sourceSlideIndex,
  fieldContext,
  tableStyleContext,
  onStageClick,
  children,
  screenOverlay
}) {
  const containerRef = useRef(null);
  const [box, setBox] = useState(null);
  useEffect(() => {
    const el = containerRef.current;
    if (!el) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      const rect = entries[0]?.contentRect;
      if (rect && rect.width > 0 && rect.height > 0) {
        setBox({ width: rect.width, height: rect.height });
      }
    });
    observer.observe(el);
    const initial = el.getBoundingClientRect();
    if (initial.width > 0 && initial.height > 0) {
      setBox({ width: initial.width, height: initial.height });
    }
    return () => {
      observer.disconnect();
    };
  }, []);
  const safeWidth = Math.max(canvasSize.width, 1);
  const safeHeight = Math.max(canvasSize.height, 1);
  const scale = useMemo(() => {
    if (!box) {
      return 1;
    }
    const fit = Math.min(box.width / safeWidth, box.height / safeHeight);
    return fit > 0 ? fit : 1;
  }, [box, safeWidth, safeHeight]);
  const scaledWidth = safeWidth * scale;
  const scaledHeight = safeHeight * scale;
  return (
    // oxlint-disable-next-line jsx-a11y/no-static-element-interactions, jsx-a11y/click-events-have-key-events -- the show surface is not a control; it carries PowerPoint's click-to-advance, whose keyboard equivalent (Space / arrows / PageDown) is bound globally by usePresentationKeyboard
    /* @__PURE__ */ jsxs(
      "div",
      {
        ref: containerRef,
        "data-pptx-presentation-stage": true,
        className: "relative flex-1 min-h-0 overflow-hidden bg-black select-none",
        onClick: onStageClick,
        children: [
          /* @__PURE__ */ jsxs(
            "div",
            {
              className: "absolute",
              style: {
                width: scaledWidth,
                height: scaledHeight,
                // Centre the letterboxed slide without flexbox so the box keeps a
                // definite size for the overlays parented to it.
                left: box ? Math.max(0, (box.width - scaledWidth) / 2) : 0,
                top: box ? Math.max(0, (box.height - scaledHeight) / 2) : 0
              },
              children: [
                /* @__PURE__ */ jsxs(
                  "div",
                  {
                    role: "region",
                    "aria-roledescription": "slide",
                    "aria-label": `Slide ${(sourceSlideIndex ?? 0) + 1}`,
                    ...{ [PRESENTATION_STAGE_ATTRIBUTE]: "true" },
                    className: "absolute top-0 left-0 overflow-hidden",
                    style: {
                      width: safeWidth,
                      height: safeHeight,
                      transform: `scale(${scale})`,
                      transformOrigin: "top left",
                      // Motion-path keyframes translate by a fraction of the SLIDE, so
                      // the stage publishes its own size for those calc() offsets.
                      "--pptx-slide-w": `${safeWidth}px`,
                      "--pptx-slide-h": `${safeHeight}px`,
                      ...getReactSlideBackgroundStyle(activeSlide, {
                        widthPx: canvasSize.width,
                        heightPx: canvasSize.height
                      })
                    },
                    children: [
                      /* @__PURE__ */ jsx(SlideBackgroundImageLayer, { slide: activeSlide }),
                      /* @__PURE__ */ jsx("style", { children: PRESENTATION_HIT_TEST_CSS }),
                      presentationKeyframesCss && /* @__PURE__ */ jsx("style", { children: presentationKeyframesCss }),
                      templateElements.map((element, index) => /* @__PURE__ */ jsx(
                        ElementRenderer,
                        {
                          element,
                          activeSlide,
                          isSelected: false,
                          isInlineEditing: false,
                          inlineEditingText: "",
                          canInteract: false,
                          presenting: true,
                          spellCheckEnabled: false,
                          mediaDataUrls,
                          selectionColorClass: "blue-400",
                          showHoverBorder: false,
                          zIndex: index,
                          imageAltText: "Template element",
                          showResizeHandles: false,
                          renderInk: false,
                          renderGroups: true,
                          adjustmentHandles: [],
                          onResizePointerDown: noop2,
                          onAdjustmentPointerDown: noop2,
                          onInlineEditChange: noop2,
                          onInlineEditCommit: noop2,
                          onInlineEditCancel: noop2,
                          onActionClick,
                          onHyperlinkClick,
                          animationState: presentationElementStates?.get(element.id),
                          presentationElementStates,
                          allSlides,
                          onZoomClick,
                          sourceSlideIndex,
                          fieldContext,
                          tableStyleContext
                        },
                        `tpl-${element.id}`
                      )),
                      activeSlide?.elements.map((element, index) => /* @__PURE__ */ jsx(
                        ElementRenderer,
                        {
                          element,
                          activeSlide,
                          isSelected: false,
                          isInlineEditing: false,
                          inlineEditingText: "",
                          canInteract: false,
                          presenting: true,
                          spellCheckEnabled: false,
                          mediaDataUrls,
                          selectionColorClass: "blue-500",
                          showHoverBorder: false,
                          zIndex: templateElements.length + index,
                          imageAltText: "Slide element",
                          showResizeHandles: false,
                          renderInk: true,
                          renderGroups: true,
                          adjustmentHandles: [],
                          onResizePointerDown: noop2,
                          onAdjustmentPointerDown: noop2,
                          onInlineEditChange: noop2,
                          onInlineEditCommit: noop2,
                          onInlineEditCancel: noop2,
                          onActionClick,
                          onHyperlinkClick,
                          animationState: presentationElementStates?.get(element.id),
                          presentationElementStates,
                          allSlides,
                          onZoomClick,
                          sourceSlideIndex,
                          fieldContext,
                          tableStyleContext
                        },
                        element.id
                      ))
                    ]
                  }
                ),
                children?.(scale)
              ]
            }
          ),
          screenOverlay
        ]
      }
    )
  );
}
function noop2() {
}
var itemClass = "block w-full whitespace-nowrap px-3 py-1.5 text-left text-xs text-foreground transition-colors hover:bg-accent";
var headingClass = "px-3 pb-0.5 pt-1.5 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground";
function PresentationContextMenu(p) {
  const { t } = useTranslation();
  const run = (action) => {
    action();
    p.onClose();
  };
  const item = (label, action) => action ? /* @__PURE__ */ jsx("button", { type: "button", role: "menuitem", className: itemClass, onClick: () => run(action), children: label }) : null;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "div",
      {
        className: "fixed inset-0 z-[1299]",
        onClick: p.onClose,
        onContextMenu: (e) => {
          e.preventDefault();
          p.onClose();
        }
      }
    ),
    /* @__PURE__ */ jsxs(
      "div",
      {
        "data-pptx-presentation-menu": "",
        role: "menu",
        tabIndex: -1,
        className: "fixed z-[1300] min-w-[180px] rounded-md border border-border bg-popover py-1 shadow-xl",
        style: { left: p.state.x, top: p.state.y },
        onContextMenu: (e) => e.preventDefault(),
        children: [
          item(t("pptx.presenter.nextSlide"), p.onNext),
          item(t("pptx.presenter.previousSlide"), p.onPrevious),
          item(t("pptx.presenter.seeAllSlides"), p.onSeeAllSlides),
          item(t("pptx.presenter.presenterView"), p.onShowPresenterView),
          (p.onPointerTool || p.onEraseInk) && /* @__PURE__ */ jsxs(Fragment, { children: [
            /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border/60" }),
            /* @__PURE__ */ jsx("div", { className: headingClass, children: t("pptx.presentation.pointerTools") }),
            item(
              t("pptx.presenter.pointerArrow"),
              p.onPointerTool && (() => p.onPointerTool?.("none"))
            ),
            item(
              t("pptx.presenter.pointerPen"),
              p.onPointerTool && (() => p.onPointerTool?.("pen"))
            ),
            item(
              t("pptx.presenter.pointerHighlighter"),
              p.onPointerTool && (() => p.onPointerTool?.("highlighter"))
            ),
            item(
              t("pptx.presentation.laserPointer"),
              p.onPointerTool && (() => p.onPointerTool?.("laser"))
            ),
            item(t("pptx.presenter.eraseAllInk"), p.onEraseInk)
          ] }),
          p.onBlank && /* @__PURE__ */ jsxs(Fragment, { children: [
            /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border/60" }),
            /* @__PURE__ */ jsx("div", { className: headingClass, children: t("pptx.presenter.screen") }),
            item(t("pptx.presenter.blackScreen"), () => p.onBlank?.("black")),
            item(t("pptx.presenter.whiteScreen"), () => p.onBlank?.("white"))
          ] }),
          /* @__PURE__ */ jsx("div", { className: "my-1 border-t border-border/60" }),
          item(t("pptx.presenter.endPresentation"), p.onEndShow)
        ]
      }
    )
  ] });
}
function ViewerCanvasArea(props) {
  const {
    mode,
    canEdit,
    slides,
    activeSlide,
    masterPseudoSlide,
    templateElements,
    canvasSize,
    activeSlideIndex,
    gridSpacingPx,
    zoom,
    state: s,
    selectedElement,
    canvasHandlers,
    insertHandlers,
    tableOps,
    annotations,
    presentation,
    onEndPresentation,
    findReplace,
    hiddenActions,
    aiPickMode = false,
    onAiPickElement,
    aiHighlightOverlay,
    aiCanvasActive = false
  } = props;
  const { t } = useTranslation();
  const { isHidden } = useToolbarVisibility(hiddenActions);
  const viewerOptions = useViewerOptionsContext();
  const effectiveSlide = mode === "master" ? masterPseudoSlide : activeSlide;
  const effectiveTemplateElements = mode === "master" ? [] : templateElements;
  const fieldContext = useMemo(
    () => buildFieldSubstitutionContext({
      headerFooter: s.headerFooter,
      customProperties: s.customProperties,
      slide: activeSlide
    }),
    [s.headerFooter, s.customProperties, activeSlide]
  );
  const tableStyleContext = useMemo(() => {
    if (!s.theme && !s.tableStyleMap) {
      return void 0;
    }
    return { theme: s.theme, tableStyleMap: s.tableStyleMap };
  }, [s.theme, s.tableStyleMap]);
  const openExternalUrl = useCallback(
    (url) => {
      if (shouldConfirmExternalHyperlink(viewerOptions, url) && !window.confirm(`${t("pptx.options.trust.confirmHyperlinks")}

${url}`)) {
        return;
      }
      safeOpenUrl(url);
    },
    [viewerOptions, t]
  );
  const handleActionClick = useCallback(
    (elementId, action) => {
      if (mode === "present") {
        const resolved = resolvePresentationAction(action, { slideCount: slides.length });
        if (resolved.intent.kind === "openUrl") {
          openExternalUrl(resolved.intent.url);
          return;
        }
        presentation.handlePresentationAction(action, elementId);
      } else if (action.url) {
        openExternalUrl(action.url);
      }
    },
    [mode, presentation, openExternalUrl, slides.length]
  );
  const handleHyperlinkClick = useCallback(
    (url) => {
      if (isPpactionUrl(url)) {
        if (mode === "present") {
          const parsed = parsePpactionUrl(url);
          if (parsed) {
            const action = {
              action: parsed.action,
              targetSlideIndex: parsed.targetSlideIndex
            };
            presentation.handlePresentationAction(action);
          }
        }
        return;
      }
      openExternalUrl(url);
    },
    [mode, presentation, openExternalUrl]
  );
  const [presentationMenu, setPresentationMenu] = useState(
    null
  );
  const handlePresentationContextMenu = useCallback(
    (e) => {
      if (mode !== "present") {
        return;
      }
      e.preventDefault();
      if (!viewerOptions.advanced.slideShowShowMenuOnRightClick) {
        return;
      }
      setPresentationMenu({ x: e.clientX, y: e.clientY });
    },
    [mode, viewerOptions.advanced.slideShowShowMenuOnRightClick]
  );
  const swipeHandledAtRef = useRef(0);
  const handleStageClick = useCallback(
    (e) => {
      if (mode !== "present") {
        return;
      }
      if (Date.now() - swipeHandledAtRef.current < 700) {
        return;
      }
      if (annotations.presentationTool !== "none") {
        return;
      }
      if (!isPresentationAdvanceClick(e.target)) {
        return;
      }
      presentation.movePresentationSlide(1, "click");
    },
    [annotations.presentationTool, mode, presentation]
  );
  const handleToggleBlackboard = useCallback(() => {
    const next = toggleBlackboard(
      presentation.presenterSnapshot.blackout,
      annotations.presentationTool
    );
    presentation.setPresenterBlackout(next.blackout);
    if (next.tool !== annotations.presentationTool) {
      annotations.setPresentationTool(next.tool);
    }
  }, [annotations, presentation]);
  const toolbarHoveringRef = useRef(false);
  const handleToolbarMouseEnter = useCallback(() => {
    toolbarHoveringRef.current = true;
    annotations.setToolbarVisible(true);
  }, [annotations]);
  const handleToolbarMouseLeave = useCallback(() => {
    toolbarHoveringRef.current = false;
  }, []);
  const swipeEnabled = mode === "preview" || mode === "present";
  const handleSwipeNext = useCallback(() => {
    if (mode === "present") {
      swipeHandledAtRef.current = Date.now();
      presentation.movePresentationSlide(1, "click");
    } else {
      s.setActiveSlideIndex((i) => Math.min(slides.length - 1, i + 1));
    }
  }, [mode, presentation, s, slides.length]);
  const handleSwipePrev = useCallback(() => {
    if (mode === "present") {
      swipeHandledAtRef.current = Date.now();
      presentation.movePresentationSlide(-1);
    } else {
      s.setActiveSlideIndex((i) => Math.max(0, i - 1));
    }
  }, [mode, presentation, s]);
  const swipe = useSwipeNavigation({
    enabled: swipeEnabled,
    onNext: handleSwipeNext,
    onPrev: handleSwipePrev
  });
  const pickActive = aiPickMode && Boolean(onAiPickElement) && mode === "edit";
  const handleElementClick = pickActive ? (elementId, e) => {
    e.stopPropagation();
    onAiPickElement?.(activeSlideIndex, elementId);
  } : canvasHandlers.handleElementClick;
  const handleElementMouseDown = pickActive ? (_elementId, e) => {
    e.stopPropagation();
  } : canvasHandlers.handleElementMouseDown;
  return (
    // eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions -- the canvas area hosts swipe navigation and the presentation-mode right-click menu; it has no interactive semantics of its own
    /* @__PURE__ */ jsxs(
      "main",
      {
        "aria-label": t("pptx.viewer.slideEditorAria"),
        "data-ai-pick-mode": pickActive ? "true" : void 0,
        className: `flex-1 min-w-0 relative flex flex-col bg-background${pickActive ? " cursor-crosshair" : ""}`,
        onTouchStart: swipe.onTouchStart,
        onTouchEnd: swipe.onTouchEnd,
        onContextMenu: mode === "present" ? handlePresentationContextMenu : void 0,
        children: [
          findReplace.findReplaceOpen && /* @__PURE__ */ jsx(
            FindReplacePanel,
            {
              findQuery: findReplace.findQuery,
              replaceQuery: findReplace.replaceQuery,
              findMatchCase: findReplace.findMatchCase,
              findResults: findReplace.findResults,
              findResultIndex: findReplace.findResultIndex,
              onSetFindQuery: findReplace.setFindQuery,
              onSetReplaceQuery: findReplace.setReplaceQuery,
              onSetFindMatchCase: findReplace.setFindMatchCase,
              onPerformFind: findReplace.performFind,
              onNavigateResult: findReplace.navigateFindResult,
              onReplace: findReplace.handleReplace,
              onReplaceAll: findReplace.handleReplaceAll,
              onClose: () => findReplace.setFindReplaceOpen(false)
            }
          ),
          mode === "present" ? /* @__PURE__ */ jsx(
            PresentationStage,
            {
              activeSlide,
              templateElements,
              canvasSize,
              mediaDataUrls: s.mediaDataUrls,
              presentationElementStates: presentation.presentationElementStates,
              presentationKeyframesCss: presentation.presentationKeyframesCss,
              onActionClick: handleActionClick,
              onHyperlinkClick: handleHyperlinkClick,
              allSlides: slides,
              onZoomClick: presentation.handleZoomClick,
              sourceSlideIndex: activeSlideIndex,
              fieldContext,
              tableStyleContext,
              onStageClick: handleStageClick,
              screenOverlay: (
                // Blackout/whiteout, audience ink, laser and captions. Rendered on
                // the stage rather than beside the viewer, because the fullscreen
                // element is the viewer's inner container: anything mounted outside
                // it simply is not painted while a real fullscreen show is running.
                /* @__PURE__ */ jsx(PresentationAudienceEffects, { snapshot: presentation.presenterSnapshot })
              ),
              children: (stageScale) => /* @__PURE__ */ jsxs(Fragment, { children: [
                presentation.transitionOverlay && slides[presentation.transitionOverlay.outgoingSlideIndex] && /* @__PURE__ */ jsx(
                  PresentationTransitionOverlay,
                  {
                    outgoingSlide: slides[presentation.transitionOverlay.outgoingSlideIndex],
                    templateElements,
                    canvasSize,
                    transition: presentation.transitionOverlay.transition,
                    durationMs: presentation.transitionOverlay.durationMs,
                    scale: stageScale,
                    morphPlan: presentation.morphPlan,
                    incomingSlide: slides[presentation.transitionOverlay.incomingSlideIndex],
                    onComplete: presentation.handleTransitionOverlayComplete
                  },
                  `${presentation.transitionOverlay.outgoingSlideIndex}-${presentation.transitionOverlay.incomingSlideIndex}`
                ),
                annotations.inkMarkupVisible && /* @__PURE__ */ jsx(
                  PresentationAnnotationOverlay,
                  {
                    canvasSize,
                    editorScale: stageScale,
                    presentationTool: annotations.presentationTool,
                    blackout: presentation.presenterSnapshot.blackout,
                    annotationStrokes: annotations.annotationStrokes,
                    currentStroke: annotations.currentStroke,
                    laserPosition: annotations.laserPosition,
                    onPointerDown: annotations.handlePointerDown,
                    onPointerMove: annotations.handlePointerMove,
                    onPointerUp: annotations.handlePointerUp,
                    onLaserMove: annotations.handleLaserMove,
                    onLaserLeave: annotations.handleLaserLeave,
                    onEraseAtPoint: annotations.eraseAtPoint
                  }
                )
              ] })
            }
          ) : mode === "master" && s.masterViewTab === "notes" ? /* @__PURE__ */ jsx(
            NotesMasterCanvas,
            {
              notesMaster: s.notesMaster,
              canvasSize,
              notesCanvasSize: s.notesCanvasSize
            }
          ) : mode === "master" && s.masterViewTab === "handout" ? /* @__PURE__ */ jsx(
            HandoutMasterCanvas,
            {
              handoutMaster: s.handoutMaster,
              canvasSize,
              slidesPerPage: s.handoutMaster?.slidesPerPage ?? s.handoutSlidesPerPage
            }
          ) : /* @__PURE__ */ jsx(
            SlideCanvas,
            {
              onUpdateSlideAnimations: props.onUpdateSlideAnimations,
              activeSlide: effectiveSlide,
              templateElements: effectiveTemplateElements,
              canvasSize,
              zoom,
              mode,
              canEdit,
              editTemplateMode: mode === "master" || s.editTemplateMode,
              selectedElementIdSet: s.selectedElementIdSet,
              selectedElement,
              inlineEditingElementId: s.inlineEditingElementId,
              inlineEditingText: s.inlineEditingText,
              spellCheckEnabled: s.spellCheckEnabled,
              mediaDataUrls: s.mediaDataUrls,
              tableEditorState: s.tableEditorState,
              marqueeSelectionState: s.marqueeSelectionState,
              snapLines: s.snapLines,
              showGrid: s.showGrid,
              gridSpacingPx,
              showRulers: s.showRulers,
              guides: s.guides,
              onClick: handleElementClick,
              onDoubleClick: canvasHandlers.handleElementDoubleClick,
              onMouseDown: handleElementMouseDown,
              onContextMenu: canvasHandlers.handleElementContextMenu,
              onCanvasMouseDown: canvasHandlers.handleCanvasMouseDown,
              onResizePointerDown: canvasHandlers.handleResizePointerDown,
              onAdjustmentPointerDown: canvasHandlers.handleAdjustmentPointerDown,
              onRotate: canvasHandlers.handleRotate,
              onInlineEditChange: s.setInlineEditingText,
              onInlineEditCommit: canvasHandlers.handleInlineEditCommit,
              onInlineEditCancel: () => s.setInlineEditingElementId(null),
              onTableCellSelect: (cell, elementId) => s.setTableEditorState(cell ? { ...cell, elementId } : null),
              onCommitCellEdit: tableOps.handleCommitCellEdit,
              onUpdateSmartArtElement: canvasHandlers.handleUpdateSmartArtElement,
              onFormatText: canvasHandlers.handleFormatText,
              onResizeTableColumns: tableOps.handleResizeTableColumns,
              onResizeTableRow: tableOps.handleResizeTableRow,
              findResults: findReplace.findResults,
              findResultIndex: findReplace.findResultIndex,
              activeSlideIndex,
              activeTool: s.activeTool,
              drawingColor: s.drawingColor,
              drawingWidth: s.drawingWidth,
              isDrawingRef: s.isDrawingRef,
              onAddInkElement: insertHandlers.handleAddInkElement,
              onAddFreeformShape: insertHandlers.handleAddFreeformShape,
              onEraseInkElement: insertHandlers.handleEraseInkElement,
              onActionClick: handleActionClick,
              onHyperlinkClick: handleHyperlinkClick,
              fieldContext,
              tableStyleContext,
              aiActive: aiCanvasActive,
              collaborationOverlay: /* @__PURE__ */ jsxs(Fragment, { children: [
                /* @__PURE__ */ jsx(
                  RemoteSelectionOverlay,
                  {
                    elements: effectiveSlide?.elements ?? [],
                    activeSlideIndex
                  }
                ),
                /* @__PURE__ */ jsx(
                  CollaborationCursorOverlay,
                  {
                    activeSlideIndex,
                    canvasWidth: canvasSize.width,
                    canvasHeight: canvasSize.height,
                    selectedElementId: s.selectedElementId
                  }
                ),
                aiHighlightOverlay
              ] }),
              comments: activeSlide?.comments,
              showCommentMarkers: s.sidebarPanelMode === "comments",
              onCommentMarkerClick: () => s.setSidebarPanelMode("comments"),
              onMoveGuide: (guideId, position) => {
                s.setGuides(
                  (prev) => prev.map(
                    (guide) => guide.id === guideId ? {
                      ...guide,
                      position: guide.axis === "h" ? Math.max(0, Math.min(canvasSize.height, position)) : Math.max(0, Math.min(canvasSize.width, position))
                    } : guide
                  )
                );
              },
              onDeleteGuide: (guideId) => {
                s.setGuides((prev) => prev.filter((guide) => guide.id !== guideId));
              },
              onCreateGuideFromRuler: (axis, positionPx) => {
                s.setGuides((prev) => [
                  ...prev,
                  {
                    id: `guide-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
                    axis,
                    position: positionPx
                  }
                ]);
              }
            }
          ),
          mode === "present" && presentation.allSlidesOpen && /* @__PURE__ */ jsx(
            PresenterSlideNavigator,
            {
              slides,
              current: presentation.presentationSlideIndex,
              canvasSize,
              templateElements,
              onSelect: (index) => {
                presentation.navigateToSlide(index);
                presentation.closeAllSlides();
              },
              onClose: presentation.closeAllSlides
            }
          ),
          mode === "present" && presentation.endOfShowVisible && /* @__PURE__ */ jsx(PresentationEndOverlay, { onExit: () => presentation.movePresentationSlide(1) }),
          mode === "present" && presentationMenu && /* @__PURE__ */ jsx(
            PresentationContextMenu,
            {
              state: presentationMenu,
              onNext: () => presentation.movePresentationSlide(1),
              onPrevious: () => presentation.movePresentationSlide(-1),
              onEndShow: onEndPresentation ?? (() => {
              }),
              onClose: () => setPresentationMenu(null),
              onSeeAllSlides: presentation.openAllSlides,
              onShowPresenterView: presentation.togglePresenterView,
              onBlank: (value) => presentation.setPresenterBlackout(
                presentation.presenterSnapshot.blackout === value ? "none" : value
              ),
              onPointerTool: (tool) => annotations.setPresentationTool(tool === "none" ? "none" : tool),
              onEraseInk: annotations.clearAnnotations
            }
          ),
          mode === "present" && /* @__PURE__ */ jsx(PresentationSubtitleBar, { visible: Boolean(s.presentationProperties.showSubtitles) }),
          mode === "present" && /* @__PURE__ */ jsx(
            PresentationTouchControls,
            {
              currentSlideIndex: presentation.presentationSlideIndex,
              totalSlides: slides.length,
              onMovePresentationSlide: presentation.movePresentationSlide,
              onEndPresentation: onEndPresentation ?? (() => {
              }),
              hideNavigation: isHidden("navigation")
            }
          ),
          mode === "present" && /* @__PURE__ */ jsx(
            "div",
            {
              className: PRESENT_TOOLBAR_CLASSES.wrapper,
              style: {
                opacity: annotations.toolbarVisible ? 1 : 0,
                pointerEvents: annotations.toolbarVisible ? "auto" : "none"
              },
              onMouseEnter: handleToolbarMouseEnter,
              onMouseLeave: handleToolbarMouseLeave,
              children: /* @__PURE__ */ jsx(
                PresentationToolbar,
                {
                  presentationTool: annotations.presentationTool,
                  penColor: annotations.penColor,
                  highlighterColor: annotations.highlighterColor,
                  hasAnnotations: annotations.annotationStrokes.length > 0,
                  onSetTool: annotations.setPresentationTool,
                  onSetPenColor: annotations.setPenColor,
                  onSetHighlighterColor: annotations.setHighlighterColor,
                  onClearAnnotations: annotations.clearAnnotations,
                  blackout: presentation.presenterSnapshot.blackout,
                  onToggleBlackboard: handleToggleBlackboard,
                  currentSlideIndex: presentation.presentationSlideIndex,
                  totalSlides: slides.length,
                  onMovePresentationSlide: presentation.movePresentationSlide,
                  presentationStartTime: presentation.presentationStartTime,
                  onEndPresentation: onEndPresentation ?? (() => {
                  }),
                  onTogglePresenterView: presentation.togglePresenterView,
                  presenterMode: presentation.presenterMode
                }
              )
            }
          )
        ]
      }
    )
  );
}

// src/viewer/hooks/useTableStyleMapHandlers.ts
function useTableStyleMapHandlers(input) {
  const { tableStyleMap, setTableStyleMap, tableStylesToDelete, setTableStylesToDelete } = input;
  const handleTableStyleMapChange = (nextMap) => {
    const result = applyTableStyleMapChange({ tableStyleMap, tableStylesToDelete }, nextMap);
    setTableStyleMap(result.tableStyleMap);
    setTableStylesToDelete(result.tableStylesToDelete);
  };
  const handleDeleteTableStyle = (styleId) => {
    const result = applyTableStyleDelete({ tableStyleMap, tableStylesToDelete }, styleId);
    setTableStyleMap(result.tableStyleMap);
    setTableStylesToDelete(result.tableStylesToDelete);
  };
  return { handleTableStyleMapChange, handleDeleteTableStyle };
}
var AiChatPanel = lazy(async () => {
  const [{ useChat }, { default: Panel }] = await Promise.all([
    import('@ai-sdk/react'),
    import('./AiChatPanel-II6VOOAL.mjs')
  ]);
  function BoundAiChatPanel(props) {
    return /* @__PURE__ */ jsx(Panel, { ...props, useChat });
  }
  return { default: BoundAiChatPanel };
});
function AiChatPanelLazy(props) {
  return /* @__PURE__ */ jsx(
    Suspense,
    {
      fallback: /* @__PURE__ */ jsx(
        "div",
        {
          className: "absolute right-0 top-0 z-30 flex h-full w-80 items-center justify-center border-l border-border bg-card text-muted-foreground max-md:inset-x-0 max-md:top-auto max-md:bottom-0 max-md:h-[75dvh] max-md:w-full max-md:rounded-t-2xl max-md:border-l-0 max-md:border-t max-md:shadow-2xl",
          style: props.panelWidth ? { width: props.panelWidth } : void 0,
          children: /* @__PURE__ */ jsx(LuLoaderCircle, { className: "w-5 h-5 animate-spin" })
        }
      ),
      children: /* @__PURE__ */ jsx(AiChatPanel, { ...props })
    }
  );
}
var OVERLAY_CSS = `
@keyframes pptx-ai-ring-pulse {
	0% { box-shadow: 0 0 0 0 rgba(59,130,246,0.55), 0 0 0 0 rgba(59,130,246,0.35); }
	70% { box-shadow: 0 0 0 6px rgba(59,130,246,0), 0 0 14px 4px rgba(59,130,246,0.28); }
	100% { box-shadow: 0 0 0 0 rgba(59,130,246,0), 0 0 10px 2px rgba(59,130,246,0.22); }
}
@keyframes pptx-ai-ring-in {
	0% { opacity: 0; transform: scale(1.04); }
	100% { opacity: 1; transform: scale(1); }
}
/* While the AI is active, tween colour changes on slide elements so an edit
   fades from its old value to the new one instead of snapping. */
[data-pptx-ai-active='true'] [data-element-id],
[data-pptx-ai-active='true'] [data-element-id] * {
	transition: color 0.5s ease, fill 0.5s ease, stroke 0.5s ease, background-color 0.5s ease,
		border-color 0.5s ease;
}
`;
function AiFocusHighlightOverlay({
  highlights,
  elements,
  activeSlideIndex
}) {
  if (highlights.length === 0) {
    return null;
  }
  const byId = new Map(elements.map((el) => [el.id, el]));
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("style", { children: OVERLAY_CSS }),
    highlights.map((hl) => {
      if (hl.slideIndex !== activeSlideIndex) {
        return null;
      }
      const el = byId.get(hl.elementId);
      if (!el) {
        return null;
      }
      const active = hl.variant === "active";
      return /* @__PURE__ */ jsx(
        "div",
        {
          "data-testid": `ai-focus-highlight-${hl.elementId}`,
          "data-ai-highlight": hl.variant,
          "data-export-ignore": "true",
          className: "absolute pointer-events-none rounded-[3px]",
          style: {
            left: el.x - 3,
            top: el.y - 3,
            width: el.width + 6,
            height: el.height + 6,
            zIndex: 9998,
            border: active ? "2px solid rgba(59,130,246,0.9)" : "2px solid rgba(59,130,246,0.55)",
            animation: active ? "pptx-ai-ring-in 0.18s ease-out, pptx-ai-ring-pulse 1s ease-out infinite" : "pptx-ai-ring-in 0.9s ease-out",
            boxShadow: active ? void 0 : "0 0 10px 2px rgba(59,130,246,0.18)"
          }
        },
        `ai-hl-${hl.variant}-${hl.elementId}`
      );
    })
  ] });
}
function AiChangeOverlay({ batch, activeSlideIndex }) {
  const [phase, setPhase] = useState("start");
  const nonce = batch?.nonce ?? 0;
  useEffect(() => {
    if (!batch) {
      return;
    }
    setPhase("start");
    let inner = 0;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setPhase("end"));
    });
    return () => {
      cancelAnimationFrame(outer);
      cancelAnimationFrame(inner);
    };
  }, [nonce, batch]);
  if (!batch) {
    return null;
  }
  const changes = batch.changes.filter((c) => c.slideIndex === activeSlideIndex);
  if (changes.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("style", { children: aiChangeAnimationCss(batch.config) }),
    changes.map((change) => /* @__PURE__ */ jsx(
      "div",
      {
        "data-testid": `ai-change-${change.elementId}`,
        "data-ai-change": change.kind,
        "data-export-ignore": "true",
        style: changeGhostStyle(change, phase, batch.config)
      },
      `ai-change-${change.elementId}-${nonce}`
    ))
  ] });
}

// src/viewer/components/inspector/theme-editor-constants.ts
var THEME_EDITOR_CARD = "rounded bg-muted/50 p-2.5 space-y-2";
var THEME_EDITOR_HEADING = "text-[11px] font-medium uppercase tracking-wide text-muted-foreground mb-1.5";
var THEME_EDITOR_INPUT = "w-full rounded border border-border bg-background px-2 py-1 text-xs text-foreground focus:border-primary focus:outline-none";
var THEME_EDITOR_BTN = "rounded bg-primary px-3 py-1.5 text-xs font-medium text-white hover:bg-primary/80 disabled:opacity-40 disabled:cursor-not-allowed transition-colors";
var THEME_EDITOR_BTN_SECONDARY = "rounded bg-accent px-3 py-1.5 text-xs font-medium text-foreground hover:bg-accent/80 disabled:opacity-40 disabled:cursor-not-allowed transition-colors";
function ThemeColorSchemeEditor({
  editColors,
  canEdit,
  activePickerKey,
  majorFont,
  minorFont,
  onSetActivePickerKey,
  onColorChange,
  onMajorFontChange,
  onMinorFontChange
}) {
  const { t } = useTranslation();
  const slotLabel = useCallback((key) => themeColorLabel(key, t), [t]);
  const previewGrid = useMemo(() => buildThemeColorGrid(editColors, t), [editColors, t]);
  const handleColorText = useCallback(
    (val) => {
      if (activePickerKey && /^#[0-9a-fA-F]{6}$/.test(val)) {
        onColorChange(activePickerKey, val);
      }
    },
    [activePickerKey, onColorChange]
  );
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs("div", { className: THEME_EDITOR_CARD, children: [
      /* @__PURE__ */ jsx("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.colorScheme") }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-4 gap-1.5", children: THEME_COLOR_SCHEME_KEYS.map((key) => /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: `h-6 w-full rounded-sm border transition-colors ${activePickerKey === key ? "border-primary ring-1 ring-primary" : "border-border hover:border-muted-foreground"} disabled:opacity-40 disabled:cursor-not-allowed`,
            style: { backgroundColor: editColors[key] },
            title: `${slotLabel(key)}: ${editColors[key]}`,
            "aria-label": `${slotLabel(key)}: ${editColors[key]}`,
            onClick: () => onSetActivePickerKey(activePickerKey === key ? null : key)
          }
        ),
        /* @__PURE__ */ jsx("span", { className: "text-[9px] text-muted-foreground truncate w-full text-center", children: slotLabel(key) })
      ] }, key)) }),
      activePickerKey && /* @__PURE__ */ jsxs("div", { className: "mt-2 flex items-center gap-2 rounded bg-background p-2", children: [
        /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground min-w-[60px]", children: slotLabel(activePickerKey) }),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            className: "h-6 w-8 cursor-pointer rounded border-0 bg-transparent p-0",
            value: editColors[activePickerKey],
            onChange: (e) => onColorChange(activePickerKey, e.target.value)
          }
        ),
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            disabled: !canEdit,
            className: "flex-1 rounded border border-border bg-muted px-1.5 py-0.5 text-[10px] text-foreground font-mono",
            value: editColors[activePickerKey],
            onChange: (e) => handleColorText(e.target.value)
          }
        )
      ] })
    ] }),
    previewGrid && /* @__PURE__ */ jsxs("div", { className: THEME_EDITOR_CARD, children: [
      /* @__PURE__ */ jsx("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.preview") }),
      /* @__PURE__ */ jsx("div", { className: "flex flex-col gap-px", children: previewGrid.map((row, rowIdx) => /* @__PURE__ */ jsx("div", { className: "grid grid-cols-12 gap-px", children: row.map((cell) => /* @__PURE__ */ jsx(
        "div",
        {
          className: "h-3.5 rounded-sm",
          style: { backgroundColor: cell.hex },
          title: `${cell.colLabel} -- ${cell.rowLabel} (${cell.hex})`
        },
        `${cell.schemeKey}-${rowIdx}`
      )) }, rowIdx)) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: THEME_EDITOR_CARD, children: [
      /* @__PURE__ */ jsx("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.fonts") }),
      /* @__PURE__ */ jsxs("div", { className: "space-y-1.5", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
          /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.themeEditor.headingFont") }),
          /* @__PURE__ */ jsxs(
            "select",
            {
              "aria-label": t("pptx.themeEditor.headingFont"),
              className: THEME_EDITOR_INPUT,
              disabled: !canEdit,
              value: COMMON_FONTS.includes(majorFont) ? majorFont : "__custom__",
              onChange: (e) => {
                if (e.target.value !== "__custom__") {
                  onMajorFontChange(e.target.value);
                }
              },
              children: [
                COMMON_FONTS.map((f) => /* @__PURE__ */ jsx("option", { value: f, children: f }, f)),
                !COMMON_FONTS.includes(majorFont) && /* @__PURE__ */ jsx("option", { value: "__custom__", children: majorFont })
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("label", { className: "flex flex-col gap-0.5", children: [
          /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.themeEditor.bodyFont") }),
          /* @__PURE__ */ jsxs(
            "select",
            {
              "aria-label": t("pptx.themeEditor.bodyFont"),
              className: THEME_EDITOR_INPUT,
              disabled: !canEdit,
              value: COMMON_FONTS.includes(minorFont) ? minorFont : "__custom__",
              onChange: (e) => {
                if (e.target.value !== "__custom__") {
                  onMinorFontChange(e.target.value);
                }
              },
              children: [
                COMMON_FONTS.map((f) => /* @__PURE__ */ jsx("option", { value: f, children: f }, f)),
                !COMMON_FONTS.includes(minorFont) && /* @__PURE__ */ jsx("option", { value: "__custom__", children: minorFont })
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 pt-1 text-[10px] text-muted-foreground", children: [
          /* @__PURE__ */ jsx("span", { style: { fontFamily: majorFont }, className: "text-foreground", children: t("pptx.themeEditor.headingSample") }),
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "|" }),
          /* @__PURE__ */ jsx("span", { style: { fontFamily: minorFont }, className: "text-foreground", children: t("pptx.themeEditor.bodySample") })
        ] })
      ] })
    ] })
  ] });
}
var ACCENT_KEYS = ["accent1", "accent2", "accent3", "accent4", "accent5", "accent6"];
function ThemePresetGallery({
  canEdit,
  currentThemeName,
  onSelectPreset
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: THEME_EDITOR_CARD, children: [
    /* @__PURE__ */ jsx("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.presetThemes") }),
    /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-1.5", children: PRESET_THEMES.map((preset) => /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: `flex flex-col items-start gap-1 rounded border p-1.5 text-left transition-colors ${currentThemeName === preset.name ? "border-primary bg-primary/10" : "border-border hover:border-muted-foreground hover:bg-muted"} disabled:opacity-40 disabled:cursor-not-allowed`,
        onClick: () => onSelectPreset(preset),
        title: preset.name,
        children: [
          /* @__PURE__ */ jsx("div", { className: "flex gap-px w-full", children: ACCENT_KEYS.map((key) => /* @__PURE__ */ jsx(
            "div",
            {
              className: "h-3 flex-1 first:rounded-l-sm last:rounded-r-sm",
              style: { backgroundColor: preset.colorScheme[key] }
            },
            key
          )) }),
          /* @__PURE__ */ jsx("span", { className: "text-[10px] text-muted-foreground truncate w-full", children: preset.name })
        ]
      },
      preset.name
    )) })
  ] });
}
function ThemeEditorPanel({
  theme,
  canEdit,
  onUpdateColorScheme,
  onUpdateFontScheme,
  onUpdateThemeName,
  onApplyToPresentation,
  onClose
}) {
  const { t } = useTranslation();
  const [editColors, setEditColors] = useState(
    () => theme?.colorScheme ?? PRESET_THEMES[0].colorScheme
  );
  const [majorFont, setMajorFont] = useState(
    () => theme?.fontScheme?.majorFont?.latin ?? "Calibri Light"
  );
  const [minorFont, setMinorFont] = useState(
    () => theme?.fontScheme?.minorFont?.latin ?? "Calibri"
  );
  const [themeName, setThemeName] = useState(() => theme?.name ?? "Custom Theme");
  const [activePickerKey, setActivePickerKey] = useState(null);
  const handleColorChange = useCallback(
    (key, hex) => {
      const next = { ...editColors, [key]: hex };
      setEditColors(next);
      onUpdateColorScheme(next);
    },
    [editColors, onUpdateColorScheme]
  );
  const handleSelectPreset = useCallback(
    (preset) => {
      setEditColors(preset.colorScheme);
      setMajorFont(preset.majorFont);
      setMinorFont(preset.minorFont);
      setThemeName(preset.name);
      onUpdateColorScheme(preset.colorScheme);
      onUpdateFontScheme({
        majorFont: { latin: preset.majorFont },
        minorFont: { latin: preset.minorFont }
      });
      onUpdateThemeName(preset.name);
    },
    [onUpdateColorScheme, onUpdateFontScheme, onUpdateThemeName]
  );
  const handleReset = useCallback(() => {
    if (!theme?.colorScheme) {
      return;
    }
    setEditColors(theme.colorScheme);
    setMajorFont(theme.fontScheme?.majorFont?.latin ?? "Calibri Light");
    setMinorFont(theme.fontScheme?.minorFont?.latin ?? "Calibri");
    setThemeName(theme.name ?? "Custom Theme");
    onUpdateColorScheme(theme.colorScheme);
    onUpdateFontScheme({
      majorFont: {
        latin: theme.fontScheme?.majorFont?.latin ?? "Calibri Light"
      },
      minorFont: { latin: theme.fontScheme?.minorFont?.latin ?? "Calibri" }
    });
    onUpdateThemeName(theme.name ?? "Custom Theme");
  }, [theme, onUpdateColorScheme, onUpdateFontScheme, onUpdateThemeName]);
  const handleMajorFontChange = useCallback(
    (font) => {
      setMajorFont(font);
      onUpdateFontScheme({
        majorFont: { latin: font },
        minorFont: { latin: minorFont }
      });
    },
    [minorFont, onUpdateFontScheme]
  );
  const handleMinorFontChange = useCallback(
    (font) => {
      setMinorFont(font);
      onUpdateFontScheme({
        majorFont: { latin: majorFont },
        minorFont: { latin: font }
      });
    },
    [majorFont, onUpdateFontScheme]
  );
  const handleNameChange = useCallback(
    (name) => {
      setThemeName(name);
      onUpdateThemeName(name);
    },
    [onUpdateThemeName]
  );
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-2 overflow-y-auto max-h-[calc(100vh-120px)]", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("h3", { className: "text-sm font-semibold text-foreground", children: t("pptx.themeEditor.title") }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "rounded p-1 text-muted-foreground hover:bg-accent hover:text-foreground",
          title: t("pptx.themeEditor.close"),
          "aria-label": t("pptx.themeEditor.close"),
          children: /* @__PURE__ */ jsx(
            "svg",
            {
              className: "h-4 w-4",
              fill: "none",
              viewBox: "0 0 24 24",
              stroke: "currentColor",
              strokeWidth: 2,
              children: /* @__PURE__ */ jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M6 18L18 6M6 6l12 12" })
            }
          )
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: THEME_EDITOR_CARD, children: [
      /* @__PURE__ */ jsx("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.themeName") }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          className: THEME_EDITOR_INPUT,
          disabled: !canEdit,
          value: themeName,
          onChange: (e) => handleNameChange(e.target.value),
          placeholder: t("pptx.themeEditor.themeNamePlaceholder")
        }
      )
    ] }),
    /* @__PURE__ */ jsx(
      ThemePresetGallery,
      {
        canEdit,
        currentThemeName: themeName,
        onSelectPreset: handleSelectPreset
      }
    ),
    /* @__PURE__ */ jsx(
      ThemeColorSchemeEditor,
      {
        editColors,
        canEdit,
        activePickerKey,
        majorFont,
        minorFont,
        onSetActivePickerKey: setActivePickerKey,
        onColorChange: handleColorChange,
        onMajorFontChange: handleMajorFontChange,
        onMinorFontChange: handleMinorFontChange
      }
    ),
    /* @__PURE__ */ jsxs("div", { className: "flex gap-1.5 pt-1", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: THEME_EDITOR_BTN,
          onClick: onApplyToPresentation,
          children: t("pptx.themeEditor.applyToPresentation")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: THEME_EDITOR_BTN_SECONDARY,
          onClick: handleReset,
          children: t("pptx.themeEditor.reset")
        }
      )
    ] })
  ] });
}
function MobileDismissSheet({
  onClose,
  className,
  children
}) {
  const { dragY, handlers } = useSheetDismissDrag(onClose);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        "aria-label": "Close",
        onClick: onClose,
        className: "md:hidden fixed inset-0 z-20 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150"
      }
    ),
    /* @__PURE__ */ jsxs(
      "div",
      {
        className,
        style: dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : void 0,
        children: [
          /* @__PURE__ */ jsx(
            "div",
            {
              className: "md:hidden flex items-center justify-center pt-2 pb-1 cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: handlers.onPointerDown,
              onPointerMove: handlers.onPointerMove,
              onPointerUp: handlers.onPointerUp,
              onPointerCancel: handlers.onPointerCancel,
              children: /* @__PURE__ */ jsx("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" })
            }
          ),
          children
        ]
      }
    )
  ] });
}

// src/viewer/components/toolbar/theme-gallery-data.ts
var BUILT_IN_THEMES = [
  {
    id: "office",
    name: "Office",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#44546A",
      lt2: "#E7E6E6",
      accent1: "#4472C4",
      accent2: "#ED7D31",
      accent3: "#A5A5A5",
      accent4: "#FFC000",
      accent5: "#5B9BD5",
      accent6: "#70AD47",
      hlink: "#0563C1",
      folHlink: "#954F72"
    },
    fontScheme: {
      majorFont: "Calibri Light",
      minorFont: "Calibri"
    }
  },
  {
    id: "facet",
    name: "Facet",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#2C3C43",
      lt2: "#CFD7D9",
      accent1: "#90C226",
      accent2: "#54A021",
      accent3: "#E6B91E",
      accent4: "#E76618",
      accent5: "#C42F1A",
      accent6: "#918655",
      hlink: "#99CA3C",
      folHlink: "#B9D181"
    },
    fontScheme: {
      majorFont: "Trebuchet MS",
      minorFont: "Trebuchet MS"
    }
  },
  {
    id: "integral",
    name: "Integral",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#335B74",
      lt2: "#DFE3E5",
      accent1: "#1CADE4",
      accent2: "#2683C6",
      accent3: "#27CED7",
      accent4: "#42BA97",
      accent5: "#3E8853",
      accent6: "#62A39F",
      hlink: "#6BA5D1",
      folHlink: "#9DCCE0"
    },
    fontScheme: {
      majorFont: "Tw Cen MT Condensed",
      minorFont: "Tw Cen MT"
    }
  },
  {
    id: "ion",
    name: "Ion",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#262626",
      lt2: "#E1E1E1",
      accent1: "#B01513",
      accent2: "#EA6312",
      accent3: "#E6B729",
      accent4: "#6AAC90",
      accent5: "#5F9C9D",
      accent6: "#9B6BF2",
      hlink: "#F09415",
      folHlink: "#D14D0F"
    },
    fontScheme: {
      majorFont: "Century Gothic",
      minorFont: "Century Gothic"
    }
  },
  {
    id: "retrospect",
    name: "Retrospect",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#637052",
      lt2: "#CCDDEA",
      accent1: "#E48312",
      accent2: "#BD582C",
      accent3: "#865640",
      accent4: "#9B8357",
      accent5: "#C2BC80",
      accent6: "#94A088",
      hlink: "#3A7CCB",
      folHlink: "#8C8C8C"
    },
    fontScheme: {
      majorFont: "Calibri Light",
      minorFont: "Calibri"
    }
  },
  {
    id: "organic",
    name: "Organic",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#283138",
      lt2: "#E3DED1",
      accent1: "#83992A",
      accent2: "#3C9770",
      accent3: "#44709D",
      accent4: "#A23C33",
      accent5: "#D97828",
      accent6: "#DEB340",
      hlink: "#A8B97F",
      folHlink: "#89C2A3"
    },
    fontScheme: {
      majorFont: "Garamond",
      minorFont: "Garamond"
    }
  },
  {
    id: "wisp",
    name: "Wisp",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#46393B",
      lt2: "#E8DCD8",
      accent1: "#A53010",
      accent2: "#DE7E18",
      accent3: "#9F8351",
      accent4: "#728653",
      accent5: "#92AA4C",
      accent6: "#6AAC91",
      hlink: "#FB4F14",
      folHlink: "#E25839"
    },
    fontScheme: {
      majorFont: "Century Gothic",
      minorFont: "Century Gothic"
    }
  },
  {
    id: "berlin",
    name: "Berlin",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#3E5C61",
      lt2: "#D5E0D0",
      accent1: "#F09415",
      accent2: "#C1B56B",
      accent3: "#89C2A3",
      accent4: "#729BBE",
      accent5: "#9A90C2",
      accent6: "#BE8DBE",
      hlink: "#25A0DA",
      folHlink: "#FF7F00"
    },
    fontScheme: {
      majorFont: "Trebuchet MS",
      minorFont: "Trebuchet MS"
    }
  },
  {
    id: "slice",
    name: "Slice",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#2E3B4C",
      lt2: "#C7CDD3",
      accent1: "#E2751D",
      accent2: "#C53B23",
      accent3: "#A13D69",
      accent4: "#58599E",
      accent5: "#36749D",
      accent6: "#4EA39A",
      hlink: "#FD6B32",
      folHlink: "#D14D0F"
    },
    fontScheme: {
      majorFont: "Century Gothic",
      minorFont: "Century Gothic"
    }
  },
  {
    id: "dividend",
    name: "Dividend",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#3D3D3D",
      lt2: "#E1E1E1",
      accent1: "#4D1434",
      accent2: "#903163",
      accent3: "#B2324B",
      accent4: "#D34817",
      accent5: "#E8971C",
      accent6: "#FDCB00",
      hlink: "#A03068",
      folHlink: "#C34882"
    },
    fontScheme: {
      majorFont: "Gill Sans MT",
      minorFont: "Gill Sans MT"
    }
  }
];
function ThemeThumbnail({
  theme,
  selected,
  onClick
}) {
  const { colorScheme } = theme;
  return /* @__PURE__ */ jsxs(
    "button",
    {
      type: "button",
      onClick,
      className: cn(
        "group relative flex flex-col rounded-lg border-2 transition-all overflow-hidden",
        selected ? "border-primary shadow-lg scale-[1.02]" : "border-border hover:border-primary/50 hover:shadow-md"
      ),
      title: theme.name,
      children: [
        /* @__PURE__ */ jsxs("div", { className: "h-24 flex flex-col", children: [
          /* @__PURE__ */ jsxs("div", { className: "h-10 flex", children: [
            /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: colorScheme.accent1 } }),
            /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: colorScheme.accent2 } }),
            /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: colorScheme.accent3 } })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex-1 flex", children: [
            /* @__PURE__ */ jsx("div", { className: "w-1/3", style: { backgroundColor: colorScheme.dk2 } }),
            /* @__PURE__ */ jsx("div", { className: "flex-1", style: { backgroundColor: colorScheme.lt2 } })
          ] })
        ] }),
        /* @__PURE__ */ jsx("div", { className: "bg-background border-t border-border px-2 py-1.5", children: /* @__PURE__ */ jsx("p", { className: "text-xs font-medium text-foreground text-center", children: theme.name }) }),
        selected && /* @__PURE__ */ jsx("div", { className: "absolute top-1 right-1 bg-primary text-white rounded-full p-1", children: /* @__PURE__ */ jsx(LuCheck, { className: "w-3 h-3" }) })
      ]
    }
  );
}
function ThemeGallery({
  open,
  currentTheme,
  canEdit,
  onClose,
  onApplyTheme,
  onImportTheme
}) {
  const { t } = useTranslation();
  const [selectedTheme, setSelectedTheme] = useState(currentTheme ?? null);
  const fileInputRef = useRef(null);
  useEffect(() => {
    if (open) {
      setSelectedTheme(currentTheme ?? null);
    }
  }, [currentTheme, open]);
  if (!open) {
    return null;
  }
  const handleApply = () => {
    if (selectedTheme) {
      onApplyTheme(selectedTheme);
      onClose();
    }
  };
  const handleImportClick = () => {
    fileInputRef.current?.click();
  };
  const handleFileChange = (event) => {
    const file = event.target.files?.[0];
    if (file && onImportTheme) {
      onImportTheme(file);
    }
    event.target.value = "";
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        className: "fixed inset-0 z-[100] bg-black/60 backdrop-blur-sm",
        onClick: onClose,
        "aria-label": t("pptx.common.close")
      }
    ),
    /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-[101] flex items-center justify-center p-4", children: /* @__PURE__ */ jsxs("div", { className: "bg-background border border-border rounded-xl shadow-2xl max-w-5xl w-full max-h-[90vh] flex flex-col", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-6 py-4 border-b border-border", children: [
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("h2", { className: "text-lg font-semibold text-foreground", children: t("pptx.themes.gallery.title") }),
          /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: t("pptx.themes.gallery.description") })
        ] }),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            onClick: onClose,
            className: "p-2 rounded hover:bg-accent transition-colors",
            "aria-label": t("pptx.common.close"),
            children: /* @__PURE__ */ jsx(LuX, { className: "w-5 h-5" })
          }
        )
      ] }),
      /* @__PURE__ */ jsx("div", { className: "flex-1 overflow-auto p-6", children: /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4", children: BUILT_IN_THEMES.map((theme) => /* @__PURE__ */ jsx(
        ThemeThumbnail,
        {
          theme,
          selected: selectedTheme?.id === theme.id,
          onClick: () => setSelectedTheme(theme)
        },
        theme.id
      )) }) }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-6 py-4 border-t border-border", children: [
        /* @__PURE__ */ jsx("div", { className: "flex items-center gap-2", children: onImportTheme && /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsxs(
            "button",
            {
              type: "button",
              onClick: handleImportClick,
              disabled: !canEdit,
              className: "inline-flex items-center gap-2 px-3 py-1.5 rounded bg-accent hover:bg-accent/80 text-xs font-medium text-foreground transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
              children: [
                /* @__PURE__ */ jsx(LuUpload, { className: "w-3.5 h-3.5" }),
                t("pptx.themes.gallery.importTheme")
              ]
            }
          ),
          /* @__PURE__ */ jsx(
            "input",
            {
              ref: fileInputRef,
              type: "file",
              accept: ".thmx,.pptx",
              className: "hidden",
              onChange: handleFileChange
            }
          )
        ] }) }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 rounded bg-accent hover:bg-accent/80 text-xs font-medium text-foreground transition-colors",
              children: t("pptx.common.cancel")
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              onClick: handleApply,
              disabled: !canEdit || !selectedTheme,
              className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/80 text-xs font-medium text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
              children: t("pptx.common.apply")
            }
          )
        ] })
      ] })
    ] }) })
  ] });
}
function ViewerSidePanels(props) {
  const {
    mode,
    canEdit,
    activeSlide,
    masterPseudoSlide,
    slides,
    canvasSize,
    activeSlideIndex,
    selectedElement,
    state: s,
    comments,
    ops,
    manipulation,
    propertyHandlers,
    themeHandlers,
    history,
    panelWidth,
    onResizeRight,
    aiConfig,
    aiBridge,
    aiPanel
  } = props;
  const handleSetSlideSize = (size, rescaleMode) => {
    if (rescaleMode && s.slideSizeEmu) {
      s.setSlides(scaleSlidesForSizeChange(slides, s.slideSizeEmu, size, rescaleMode));
    }
    s.setSlideSizeEmu(size);
    s.setCanvasSize(slideSizeToCanvasPx(size));
    history.markDirty();
  };
  const { handleTableStyleMapChange, handleDeleteTableStyle } = useTableStyleMapHandlers({
    tableStyleMap: s.tableStyleMap,
    setTableStyleMap: s.setTableStyleMap,
    tableStylesToDelete: s.tableStylesToDelete,
    setTableStylesToDelete: s.setTableStylesToDelete
  });
  const effectiveSlide = mode === "master" ? masterPseudoSlide : activeSlide;
  const currentBuiltInTheme = BUILT_IN_THEMES.find(
    (candidate) => themeColorSchemesEqual(s.theme?.colorScheme, candidate.colorScheme)
  ) ?? null;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    (mode === "edit" || mode === "master") && s.isInspectorPaneOpen && onResizeRight && /* @__PURE__ */ jsx(ResizeHandle, { direction: "horizontal", onResize: onResizeRight }),
    /* @__PURE__ */ jsx(
      ViewerInspector,
      {
        isOpen: (mode === "edit" || mode === "master") && s.isInspectorPaneOpen,
        canEdit,
        mode,
        activeSlide: effectiveSlide,
        slides,
        customShows: s.customShows,
        canvasSize,
        selectedElement,
        effectiveSelectedIds: s.effectiveSelectedIds,
        tableEditorState: s.tableEditorState,
        sidebarPanelMode: s.sidebarPanelMode,
        activeSlideIndex,
        comments,
        commentAuthors: s.modernCommentAuthors,
        onSetSidebarPanelMode: s.setSidebarPanelMode,
        onClose: () => s.setIsInspectorPaneOpen(false),
        onUpdateElementStyle: ops.updateSelectedShapeStyle,
        onUpdateTextStyle: ops.updateSelectedTextStyle,
        onUpdateElement: ops.updateSelectedElement,
        onApplySelection: ops.applySelection,
        onSetCanvasSize: s.setCanvasSize,
        slideSizeEmu: s.slideSizeEmu,
        onSetSlideSize: handleSetSlideSize,
        onMoveLayer: manipulation.handleMoveLayer,
        onMoveLayerToEdge: manipulation.handleMoveLayerToEdge,
        onDeleteElement: manipulation.handleDelete,
        onUpdateSlide: propertyHandlers.handleUpdateSlide,
        presentationProperties: s.presentationProperties,
        onUpdatePresentationProperties: propertyHandlers.handleUpdatePresentationProperties,
        editTemplateMode: s.editTemplateMode,
        slideMasters: s.slideMasters,
        themeOptions: s.themeOptions,
        notesMaster: s.notesMaster,
        handoutMaster: s.handoutMaster,
        notesCanvasSize: s.notesCanvasSize,
        coreProperties: s.coreProperties,
        appProperties: s.appProperties,
        customProperties: s.customProperties,
        tagCollections: s.tagCollections,
        onUpdateTagCollections: s.setTagCollections,
        onUpdateCoreProperties: propertyHandlers.handleUpdateCoreProperties,
        onUpdateAppProperties: propertyHandlers.handleUpdateAppProperties,
        onUpdateCustomProperties: propertyHandlers.handleUpdateCustomProperties,
        onApplyTheme: themeHandlers.handleApplyTheme,
        onSetTemplateBackground: themeHandlers.handleSetTemplateBackground,
        onGetTemplateBackgroundColor: themeHandlers.handleGetTemplateBackgroundColor,
        mediaDataUrls: s.mediaDataUrls,
        theme: s.theme,
        tableStyleMap: s.tableStyleMap,
        onTableStyleMapChange: handleTableStyleMapChange,
        onDeleteTableStyle: handleDeleteTableStyle,
        panelWidth
      }
    ),
    s.isSelectionPaneOpen && (mode === "edit" || mode === "master") && /* @__PURE__ */ jsx(
      MobileDismissSheet,
      {
        onClose: () => s.setIsSelectionPaneOpen(false),
        className: "absolute right-0 top-0 z-30 h-full max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:h-auto max-md:max-h-[50vh] max-md:rounded-t-xl max-md:border-t max-md:border-border max-md:shadow-2xl max-md:bg-background",
        children: /* @__PURE__ */ jsx(
          SelectionPane,
          {
            slides,
            activeSlideIndex,
            selectedElementId: s.selectedElementId,
            selectedElementIds: s.selectedElementIds,
            canEdit,
            setSelectedElementId: s.setSelectedElementId,
            setSelectedElementIds: s.setSelectedElementIds,
            setSlides: s.setSlides,
            markDirty: history.markDirty,
            onClose: () => s.setIsSelectionPaneOpen(false)
          }
        )
      }
    ),
    s.isThemeEditorOpen && mode === "edit" && /* @__PURE__ */ jsx(
      MobileDismissSheet,
      {
        onClose: () => s.setIsThemeEditorOpen(false),
        className: "absolute right-0 top-0 z-30 h-full w-72 overflow-y-auto border-l border-border bg-card p-2.5 shadow-xl max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:h-auto max-md:max-h-[60vh] max-md:rounded-t-xl max-md:border-t max-md:border-l-0",
        children: /* @__PURE__ */ jsx(
          ThemeEditorPanel,
          {
            theme: s.theme,
            canEdit,
            onUpdateColorScheme: themeHandlers.handleUpdateThemeColorScheme,
            onUpdateFontScheme: themeHandlers.handleUpdateThemeFontScheme,
            onUpdateThemeName: themeHandlers.handleUpdateThemeName,
            onApplyToPresentation: themeHandlers.handleApplyThemeToPresentation,
            onClose: () => s.setIsThemeEditorOpen(false)
          }
        )
      }
    ),
    aiPanel?.isOpen && aiConfig && aiBridge && (mode === "edit" || mode === "master") && /* @__PURE__ */ jsx(
      AiChatPanelLazy,
      {
        bridge: aiBridge,
        config: aiConfig,
        aiPanel,
        onClose: aiPanel.close,
        panelWidth
      }
    ),
    /* @__PURE__ */ jsx(
      ThemeGallery,
      {
        open: s.isThemeGalleryOpen,
        currentTheme: currentBuiltInTheme,
        canEdit,
        onClose: () => s.setIsThemeGalleryOpen(false),
        onApplyTheme: (theme) => {
          themeHandlers.handleApplyThemeData(
            theme.colorScheme,
            {
              majorFont: {
                latin: theme.fontScheme.majorFont,
                eastAsia: theme.fontScheme.majorFont,
                complexScript: theme.fontScheme.majorFont
              },
              minorFont: {
                latin: theme.fontScheme.minorFont,
                eastAsia: theme.fontScheme.minorFont,
                complexScript: theme.fontScheme.minorFont
              }
            },
            theme.name
          );
        }
      }
    )
  ] });
}
function ViewerMainContent(props) {
  const {
    mode,
    canEdit,
    slides,
    activeSlide,
    masterPseudoSlide,
    activeSlideIndex,
    canvasSize,
    gridSpacingPx,
    slideSectionGroups,
    showSlidesPane,
    showMasterPane,
    selectedElement,
    state,
    editorOps,
    dialogs,
    presentation,
    annotations,
    propertyHandlers,
    themeHandlers,
    history,
    comments,
    masterViewCrud,
    zoom,
    isMobile: _isMobile = false,
    isTouchDevice: _isTouchDevice = false,
    onEndPresentation,
    leftPanelWidth,
    onResizeLeft,
    rightPanelWidth,
    onResizeRight,
    hiddenActions,
    aiConfig,
    aiBridge,
    aiPanel
  } = props;
  const {
    ops,
    sectionOps,
    canvasHandlers,
    insertHandlers,
    manipulation,
    slideOps,
    tableOps,
    findReplace
  } = editorOps;
  const hf = state.headerFooter;
  const thumbnailFieldContext = useMemo(
    () => ({
      dateTimeText: hf.dateTimeText,
      dateFormat: hf.dateFormat,
      footerText: hf.footerText,
      headerText: hf.headerText,
      customProperties: state.customProperties.map((p) => ({ name: p.name, value: p.value }))
    }),
    [hf.dateTimeText, hf.dateFormat, hf.footerText, hf.headerText, state.customProperties]
  );
  const thumbnailTableStyleContext = useMemo(
    () => state.theme || state.tableStyleMap ? { theme: state.theme, tableStyleMap: state.tableStyleMap } : void 0,
    [state.theme, state.tableStyleMap]
  );
  return /* @__PURE__ */ jsx(ChartPartSelectionProvider, { children: /* @__PURE__ */ jsxs("div", { className: "relative z-10 flex flex-1 min-h-0", children: [
    showSlidesPane && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(
        SlidesPaneSidebar,
        {
          slides,
          templateElementsBySlideId: state.templateElementsBySlideId,
          activeSlideIndex,
          canvasSize,
          sectionGroups: slideSectionGroups,
          isOpen: state.isSlidesPaneOpen,
          canEdit,
          onSelectSlide: state.setActiveSlideIndex,
          onSlideContextMenu: slideOps.handleSlideContextMenu,
          onMoveSlide: slideOps.handleMoveSlide,
          onAddSlide: slideOps.handleAddSlide,
          onCollapse: () => state.setIsSlidesPaneOpen(false),
          onAddSection: sectionOps.addSection,
          onRenameSection: sectionOps.renameSection,
          onDeleteSection: sectionOps.deleteSection,
          onMoveSectionUp: sectionOps.moveSectionUp,
          onMoveSectionDown: sectionOps.moveSectionDown,
          onToggleSectionCollapse: sectionOps.toggleSectionCollapse,
          rehearsalTimings: Object.keys(presentation.recordedTimings).length > 0 ? presentation.recordedTimings : void 0,
          panelWidth: leftPanelWidth,
          fieldContext: thumbnailFieldContext,
          tableStyleContext: thumbnailTableStyleContext
        }
      ),
      onResizeLeft && /* @__PURE__ */ jsx(ResizeHandle, { direction: "horizontal", onResize: onResizeLeft })
    ] }),
    showMasterPane && /* @__PURE__ */ jsx(
      MasterViewSidebar,
      {
        slideMasters: state.slideMasters,
        activeMasterIndex: state.activeMasterIndex,
        activeLayoutIndex: state.activeLayoutIndex,
        canvasSize,
        masterViewTab: state.masterViewTab,
        notesMaster: state.notesMaster,
        handoutMaster: state.handoutMaster,
        handoutSlidesPerPage: state.handoutMaster?.slidesPerPage ?? state.handoutSlidesPerPage,
        onSelectMaster: dialogs.handleSelectMaster,
        onSelectLayout: dialogs.handleSelectLayout,
        onCollapse: dialogs.handleCloseMasterView,
        onTabChange: state.setMasterViewTab,
        crudActions: masterViewCrud.crudActions,
        onCrudAction: masterViewCrud.handleCrudAction,
        onHandoutSlidesPerPageChange: (count) => {
          state.setHandoutSlidesPerPage(count);
          state.setHandoutMaster(
            (master) => master ? { ...master, slidesPerPage: count } : master
          );
          state.setIsDirty(true);
        },
        onNotesMasterBackgroundChange: (backgroundColor) => {
          state.setNotesMaster((master) => master ? { ...master, backgroundColor } : master);
          state.setIsDirty(true);
        },
        onHandoutMasterBackgroundChange: (backgroundColor) => {
          state.setHandoutMaster(
            (master) => master ? { ...master, backgroundColor } : master
          );
          state.setIsDirty(true);
        },
        canEdit,
        onSlidesBackgroundChange: (backgroundColor) => {
          const write = setMasterViewBackgroundColor(
            { slideMasters: state.slideMasters },
            {
              tab: "slides",
              masterIndex: state.activeMasterIndex,
              layoutIndex: state.activeLayoutIndex
            },
            backgroundColor
          );
          if (write?.slideMasters) {
            state.setSlideMasters(write.slideMasters);
            state.setIsDirty(true);
          }
        }
      }
    ),
    /* @__PURE__ */ jsx(
      ViewerCanvasArea,
      {
        onUpdateSlideAnimations: (animations) => propertyHandlers.handleUpdateSlide({ animations }),
        mode,
        canEdit,
        slides,
        activeSlide,
        masterPseudoSlide,
        templateElements: state.templateElements,
        canvasSize,
        activeSlideIndex,
        gridSpacingPx,
        zoom,
        state,
        selectedElement,
        canvasHandlers,
        insertHandlers,
        tableOps,
        annotations,
        presentation,
        onEndPresentation,
        findReplace,
        hiddenActions,
        aiPickMode: aiPanel?.pickMode,
        onAiPickElement: aiPanel ? aiPanel.addPick : void 0,
        aiCanvasActive: aiPanel?.canvasAnimating,
        aiHighlightOverlay: aiPanel ? /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx(
            AiFocusHighlightOverlay,
            {
              highlights: aiPanel.canvasHighlights,
              elements: activeSlide?.elements ?? [],
              activeSlideIndex
            }
          ),
          /* @__PURE__ */ jsx(AiChangeOverlay, { batch: aiPanel.changeBatch, activeSlideIndex })
        ] }) : void 0
      }
    ),
    state.contextMenuState && /* @__PURE__ */ jsx(
      ContextMenu,
      {
        contextMenuState: state.contextMenuState,
        mode,
        selectedElement,
        tableEditorState: state.tableEditorState,
        hasMultiSelection: state.effectiveSelectedIds.length > 1,
        selectionGroupable: manipulation.selectionGroupable,
        onAction: manipulation.handleContextMenuAction,
        onInsertTableRow: tableOps.handleInsertTableRow,
        onDeleteTableRow: tableOps.handleDeleteTableRow,
        onInsertTableColumn: tableOps.handleInsertTableColumn,
        onDeleteTableColumn: tableOps.handleDeleteTableColumn,
        onMergeCellRight: tableOps.handleMergeCellRight,
        onMergeCellDown: tableOps.handleMergeCellDown,
        onMergeSelectedCells: tableOps.handleMergeSelectedCells,
        onSplitCell: tableOps.handleSplitCell,
        onAskAi: aiPanel && selectedElement ? () => {
          aiPanel.askAboutSelection();
          state.setContextMenuState(null);
        } : void 0,
        onFixAi: aiPanel && selectedElement ? () => {
          aiPanel.fixSelection();
          state.setContextMenuState(null);
        } : void 0,
        onClose: () => state.setContextMenuState(null)
      }
    ),
    /* @__PURE__ */ jsx(
      ViewerSidePanels,
      {
        mode,
        canEdit,
        activeSlide,
        masterPseudoSlide,
        slides,
        canvasSize,
        activeSlideIndex,
        selectedElement,
        state,
        comments,
        ops,
        manipulation,
        propertyHandlers,
        themeHandlers,
        history,
        panelWidth: rightPanelWidth,
        onResizeRight,
        aiConfig,
        aiBridge,
        aiPanel
      }
    )
  ] }) });
}
function AutosaveRecoveryDialog({
  prompt,
  onRestore,
  onDiscard
}) {
  const { t } = useTranslation();
  if (!prompt) {
    return null;
  }
  const title = t(prompt.titleKey);
  const when = t(prompt.ageKey, prompt.ageParams);
  return /* @__PURE__ */ jsx(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/50",
      "data-pptx-autosave-recovery": "true",
      children: /* @__PURE__ */ jsxs(
        "div",
        {
          role: "dialog",
          "aria-modal": "true",
          "aria-label": title,
          className: "bg-background border border-border rounded-lg shadow-xl w-[440px] max-w-[90vw] p-6",
          children: [
            /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 mb-3", children: [
              /* @__PURE__ */ jsx("div", { className: "flex items-center justify-center w-10 h-10 rounded-full bg-primary/10", children: /* @__PURE__ */ jsx(LuHistory, { className: "w-5 h-5 text-primary" }) }),
              /* @__PURE__ */ jsx("h2", { className: "text-base font-semibold text-foreground", children: title })
            ] }),
            /* @__PURE__ */ jsx("p", { className: "text-sm text-muted-foreground", children: t(prompt.messageKey, prompt.messageParams) }),
            /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground mt-2", children: t("pptx.autosave.recovery.savedLabel", { when }) }),
            /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2 mt-6", children: [
              /* @__PURE__ */ jsxs(
                "button",
                {
                  type: "button",
                  className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md border border-border bg-background text-foreground hover:bg-accent transition-colors",
                  onClick: onDiscard,
                  children: [
                    /* @__PURE__ */ jsx(LuTrash2, { className: "w-4 h-4" }),
                    t(prompt.discardKey)
                  ]
                }
              ),
              /* @__PURE__ */ jsxs(
                "button",
                {
                  type: "button",
                  className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md bg-primary text-white hover:bg-primary/90 transition-colors",
                  onClick: onRestore,
                  children: [
                    /* @__PURE__ */ jsx(LuHistory, { className: "w-4 h-4" }),
                    t(prompt.restoreKey)
                  ]
                }
              )
            ] })
          ]
        }
      )
    }
  );
}
function CompatibilityToasts({ toasts, onDismiss, onDismissAll }) {
  const { t } = useTranslation();
  if (toasts.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "pptx-compat-toasts",
      className: "max-h-[60%] overflow-y-auto",
      style: compatToastStackStyle(),
      children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-1", style: { pointerEvents: "auto" }, children: [
          /* @__PURE__ */ jsx("span", { className: "text-[11px] font-medium uppercase tracking-wide text-muted-foreground", children: t("pptx.compatibility.toastTitle") }),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "data-testid": "pptx-compat-toasts-dismiss-all",
              onClick: onDismissAll,
              className: "text-[11px] text-muted-foreground underline-offset-2 hover:underline",
              children: t("pptx.compatibility.dismissAll")
            }
          )
        ] }),
        toasts.map((toast) => /* @__PURE__ */ jsxs(
          "div",
          {
            "data-testid": "pptx-compat-toast",
            "data-code": toast.code,
            "data-severity": toast.severity,
            className: "flex items-start gap-2 rounded border border-border bg-card p-2 text-[11px] shadow-md",
            style: { pointerEvents: "auto" },
            children: [
              /* @__PURE__ */ jsx("span", { className: "flex-1 min-w-0", children: t(toast.messageKey, toast.params) }),
              /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  "data-testid": "pptx-compat-toast-dismiss",
                  onClick: () => onDismiss(toast.id),
                  "aria-label": t("pptx.compatibility.dismiss"),
                  className: "shrink-0 text-muted-foreground hover:text-foreground",
                  children: "\xD7"
                }
              )
            ]
          },
          toast.id
        ))
      ]
    }
  );
}
function ToggleRow({ checked, onChange, icon, label, testId }) {
  return /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2.5 cursor-pointer group select-none", children: [
    /* @__PURE__ */ jsxs("span", { className: "relative flex items-center justify-center w-4 h-4", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked,
          onChange: (e) => onChange(e.target.checked),
          "data-testid": testId,
          className: "peer sr-only"
        }
      ),
      /* @__PURE__ */ jsx("span", { className: "absolute inset-0 rounded border border-border bg-muted transition-colors peer-checked:border-primary peer-checked:bg-primary peer-focus-visible:ring-2 peer-focus-visible:ring-primary/50" }),
      checked && /* @__PURE__ */ jsx(LuCheck, { className: "relative z-10 w-3 h-3 text-white" })
    ] }),
    /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1.5 text-xs text-foreground group-hover:text-foreground transition-colors", children: [
      icon,
      label
    ] })
  ] });
}
function HeaderFooterPanel({
  headerFooter,
  onUpdate,
  onApplyToAll,
  onApplyToCurrent,
  onClose
}) {
  const { t } = useTranslation();
  const showDateTime = headerFooter.hasDateTime ?? false;
  const dateTimeAuto = headerFooter.dateTimeAuto ?? false;
  const showDateText = isHeaderFooterDateTextVisible(headerFooter);
  const showSlideNumber = headerFooter.hasSlideNumber ?? false;
  const showHeader = headerFooter.hasHeader ?? false;
  const showHeaderText = isHeaderFooterHeaderTextVisible(headerFooter);
  const showFooter = headerFooter.hasFooter ?? false;
  const showFooterText = isHeaderFooterFooterTextVisible(headerFooter);
  const handleDateTextChange = useCallback(
    (e) => onUpdate({ dateTimeText: e.target.value }),
    [onUpdate]
  );
  const handleHeaderTextChange = useCallback(
    (e) => onUpdate({ headerText: e.target.value }),
    [onUpdate]
  );
  const handleFooterTextChange = useCallback(
    (e) => onUpdate({ footerText: e.target.value }),
    [onUpdate]
  );
  return /* @__PURE__ */ jsx("div", { className: "absolute inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm", children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-sm rounded-lg border border-border bg-background shadow-2xl", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between border-b border-border px-4 py-3", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.headerFooter.title") }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "rounded p-1 text-muted-foreground hover:bg-muted hover:text-foreground transition-colors",
          "aria-label": t("pptx.headerFooter.close"),
          "data-testid": "header-footer-close",
          children: /* @__PURE__ */ jsx(LuX, { className: "w-4 h-4" })
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-4 px-4 py-4", children: [
      /* @__PURE__ */ jsx(
        ToggleRow,
        {
          checked: showDateTime,
          onChange: (hasDateTime) => onUpdate({ hasDateTime }),
          icon: /* @__PURE__ */ jsx(LuCalendarDays, { className: "w-3.5 h-3.5" }),
          label: t("pptx.headerFooter.dateAndTime"),
          testId: "hf-date-time"
        }
      ),
      showDateTime && /* @__PURE__ */ jsxs("div", { className: "pl-6 space-y-2", children: [
        /* @__PURE__ */ jsx(
          ToggleRow,
          {
            checked: dateTimeAuto,
            onChange: (auto) => onUpdate({ dateTimeAuto: auto }),
            icon: /* @__PURE__ */ jsx(LuClock, { className: "w-3.5 h-3.5" }),
            label: t("pptx.headerFooter.updateAutomatically"),
            testId: "hf-date-auto"
          }
        ),
        showDateText && /* @__PURE__ */ jsx(
          "input",
          {
            type: "text",
            value: headerFooter.dateTimeText ?? "",
            onChange: handleDateTextChange,
            placeholder: t("pptx.headerFooter.fixedDate"),
            "data-testid": "hf-date-text",
            className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground placeholder-muted-foreground outline-none transition-colors focus:border-primary focus:ring-1 focus:ring-primary/30"
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        ToggleRow,
        {
          checked: showSlideNumber,
          onChange: (hasSlideNumber) => onUpdate({ hasSlideNumber }),
          icon: /* @__PURE__ */ jsx(LuHash, { className: "w-3.5 h-3.5" }),
          label: t("pptx.headerFooter.slideNumber"),
          testId: "hf-slide-number"
        }
      ),
      /* @__PURE__ */ jsx(
        ToggleRow,
        {
          checked: showHeader,
          onChange: (hasHeader) => onUpdate({ hasHeader }),
          icon: /* @__PURE__ */ jsx(LuFileText, { className: "w-3.5 h-3.5" }),
          label: t("pptx.field.header"),
          testId: "hf-header"
        }
      ),
      showHeaderText && /* @__PURE__ */ jsx("div", { className: "pl-6", children: /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          value: headerFooter.headerText ?? "",
          onChange: handleHeaderTextChange,
          placeholder: t("pptx.headerFooter.headerText"),
          "data-testid": "hf-header-text",
          className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground placeholder-muted-foreground outline-none transition-colors focus:border-primary focus:ring-1 focus:ring-primary/30"
        }
      ) }),
      /* @__PURE__ */ jsx(
        ToggleRow,
        {
          checked: showFooter,
          onChange: (hasFooter) => onUpdate({ hasFooter }),
          icon: /* @__PURE__ */ jsx(LuText, { className: "w-3.5 h-3.5" }),
          label: t("pptx.headerFooter.footer"),
          testId: "hf-footer"
        }
      ),
      showFooterText && /* @__PURE__ */ jsx("div", { className: "pl-6", children: /* @__PURE__ */ jsx(
        "input",
        {
          type: "text",
          value: headerFooter.footerText ?? "",
          onChange: handleFooterTextChange,
          placeholder: t("pptx.headerFooter.footerPlaceholder"),
          "data-testid": "hf-footer-text",
          className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground placeholder-muted-foreground outline-none transition-colors focus:border-primary focus:ring-1 focus:ring-primary/30"
        }
      ) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-2 border-t border-border px-4 py-3", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onApplyToAll,
          "data-testid": "hf-apply-all",
          className: "rounded bg-accent px-3 py-1.5 text-xs font-medium text-foreground hover:bg-accent/80 transition-colors",
          children: t("pptx.headerFooter.applyToAll")
        }
      ),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onApplyToCurrent,
          "data-testid": "hf-apply-current",
          className: "rounded bg-primary px-3 py-1.5 text-xs font-medium text-white hover:bg-primary/80 transition-colors",
          children: t("pptx.headerFooter.applyToCurrent")
        }
      )
    ] })
  ] }) });
}
function isActionKey(key) {
  return key === "slides" || key === "insert" || key === "inspector" || key === "comments" || key === "notes";
}
var LABEL_BY_KEY = {
  slides: "pptx.sections.slides",
  insert: "pptx.mobileBar.insert",
  inspector: "pptx.field.format",
  comments: "pptx.toolbar.comments",
  notes: "pptx.notes.title"
};
var ICON_BY_KEY = {
  slides: LuLayers,
  insert: LuPlus,
  inspector: LuSettings2,
  comments: LuMessageSquare,
  notes: LuStickyNote
};
function MobileBottomBar({
  slideCount,
  onOpenSlides,
  onOpenInsert,
  onOpenInspector,
  onOpenComments,
  onToggleNotes,
  activeSheet,
  commentCount
}) {
  const { t } = useTranslation(), onClickByKey = {
    slides: onOpenSlides,
    insert: onOpenInsert,
    inspector: onOpenInspector,
    comments: onOpenComments,
    notes: onToggleNotes
  }, actions = buildBarActions({ slideCount }).filter((descriptor) => isActionKey(descriptor.key)).map((descriptor) => {
    const key = descriptor.key;
    return {
      key,
      label: LABEL_BY_KEY[key],
      icon: ICON_BY_KEY[key],
      onClick: onClickByKey[key],
      disabled: descriptor.disabled,
      badge: key === "comments" ? commentCount : void 0
    };
  });
  return /* @__PURE__ */ jsx(
    "nav",
    {
      "aria-label": t("pptx.mobileBar.ariaLabel"),
      className: "flex items-stretch justify-around border-t border-border bg-secondary/80 backdrop-blur supports-[backdrop-filter]:bg-secondary/60 pb-[max(env(safe-area-inset-bottom),0px)]",
      children: actions.map(({ key, label, icon: Icon, onClick, disabled, badge }) => {
        const active = activeSheet === key;
        return /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            onClick,
            disabled,
            className: cn(
              "relative flex flex-col items-center justify-center gap-0.5 flex-1 min-h-[56px] py-1.5 text-[10px] font-medium transition-colors active:scale-95 disabled:opacity-40 disabled:pointer-events-none",
              active ? "text-primary" : "text-muted-foreground hover:text-foreground"
            ),
            "aria-pressed": active,
            "aria-label": key === "notes" ? t("pptx.statusBar.toggleNotes") : void 0,
            children: [
              /* @__PURE__ */ jsx(Icon, { className: "w-5 h-5" }),
              /* @__PURE__ */ jsx("span", { children: t(label) }),
              badge !== void 0 && badge > 0 && /* @__PURE__ */ jsx("span", { className: "absolute top-1 right-1/4 flex items-center justify-center min-w-[16px] h-4 px-1 rounded-full bg-primary text-[9px] font-semibold text-white", children: badge > 99 ? "99+" : badge }),
              active && /* @__PURE__ */ jsx("span", { className: "absolute top-0 left-1/2 -translate-x-1/2 w-8 h-0.5 rounded-full bg-primary" })
            ]
          },
          key
        );
      })
    }
  );
}
function MobileSlidesSheet({
  open,
  onClose,
  ...sidebar
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(
    MobileSheet,
    {
      open,
      onClose,
      heightFraction: 0.7,
      title: t("pptx.sections.slides"),
      children: /* @__PURE__ */ jsx("div", { className: "h-full", children: /* @__PURE__ */ jsx(SlidesPaneSidebar, { ...sidebar }) })
    }
  );
}
function nextMobileSheet(current, tapped) {
  const next = toggleSheet(current, tapped);
  return next === tapped ? tapped : null;
}
function MobileChromeOverlay(props) {
  const {
    state: s,
    editorOps,
    presentation,
    slides,
    activeSlideIndex,
    canvasSize,
    slideSectionGroups,
    canEdit,
    commentCount
  } = props, { keyboardInset, isKeyboardOpen } = useKeyboardInsets(), activeSheet = s.isSlidesPaneOpen ? "slides" : s.isInspectorPaneOpen ? s.sidebarPanelMode === "comments" ? "comments" : "inspector" : !s.isSlideNotesCollapsed ? "notes" : null, closeAllSheets = () => {
    s.setIsSlidesPaneOpen(false);
    s.setIsInspectorPaneOpen(false);
    s.setIsSlideNotesCollapsed(true);
  }, openSheet = (which) => {
    closeAllSheets();
    switch (which) {
      case "slides":
        s.setIsSlidesPaneOpen(true);
        break;
      case "inspector":
        s.setSidebarPanelMode("properties");
        s.setIsInspectorPaneOpen(true);
        break;
      case "comments":
        s.setSidebarPanelMode("comments");
        s.setIsInspectorPaneOpen(true);
        break;
      case "notes":
        s.setIsSlideNotesCollapsed(false);
        break;
    }
  }, applySheetTap = (tapped) => {
    const next = nextMobileSheet(activeSheet, tapped);
    closeAllSheets();
    if (next) {
      openSheet(next);
    }
  };
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      MobileSlidesSheet,
      {
        open: s.isSlidesPaneOpen,
        onClose: () => s.setIsSlidesPaneOpen(false),
        slides,
        templateElementsBySlideId: s.templateElementsBySlideId,
        activeSlideIndex,
        canvasSize,
        sectionGroups: slideSectionGroups,
        isOpen: true,
        canEdit,
        onSelectSlide: (index) => {
          s.setActiveSlideIndex(index);
          s.setIsSlidesPaneOpen(false);
        },
        onSlideContextMenu: editorOps.slideOps.handleSlideContextMenu,
        onMoveSlide: editorOps.slideOps.handleMoveSlide,
        onAddSlide: editorOps.slideOps.handleAddSlide,
        onCollapse: () => s.setIsSlidesPaneOpen(false),
        onAddSection: editorOps.sectionOps.addSection,
        onRenameSection: editorOps.sectionOps.renameSection,
        onDeleteSection: editorOps.sectionOps.deleteSection,
        onMoveSectionUp: editorOps.sectionOps.moveSectionUp,
        onMoveSectionDown: editorOps.sectionOps.moveSectionDown,
        onToggleSectionCollapse: editorOps.sectionOps.toggleSectionCollapse,
        rehearsalTimings: Object.keys(presentation.recordedTimings).length > 0 ? presentation.recordedTimings : void 0
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        className: "contents",
        style: keyboardInset > 0 ? {
          display: "block",
          transform: `translateY(-${keyboardInset}px)`,
          transition: "transform 150ms ease-out",
          willChange: "transform"
        } : void 0,
        "data-keyboard-open": isKeyboardOpen ? "true" : void 0,
        children: /* @__PURE__ */ jsx(
          MobileBottomBar,
          {
            slideCount: slides.length,
            activeSheet,
            commentCount,
            onOpenSlides: () => applySheetTap("slides"),
            onOpenInsert: () => {
              editorOps.insertHandlers.handleAddTextBox();
            },
            onOpenInspector: () => applySheetTap("inspector"),
            onOpenComments: () => applySheetTap("comments"),
            onToggleNotes: () => applySheetTap("notes")
          }
        )
      }
    )
  ] });
}
function ReadOnlyBanner({
  recommendation,
  onEditAnyway,
  onDismiss,
  passwordPromptOpen = false,
  passwordError = null,
  checkingPassword = false,
  onSubmitPassword,
  onCancelPassword
}) {
  const { t } = useTranslation();
  const [password, setPassword] = useState("");
  const inputId = useId();
  const errorId = useId();
  const handleSubmit = (event) => {
    event.preventDefault();
    onSubmitPassword?.(password);
  };
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-testid": "pptx-readonly-banner",
      "data-kind": recommendation.kind ?? void 0,
      className: "flex items-center gap-3 px-3 py-1.5 bg-amber-600/10 border-b border-amber-600/30 text-[12px] text-amber-900 dark:text-amber-200",
      children: [
        /* @__PURE__ */ jsx("span", { className: "flex-1 min-w-0", children: t(recommendation.messageKey) }),
        passwordPromptOpen ? /* @__PURE__ */ jsxs(
          "form",
          {
            "data-testid": "pptx-readonly-password-form",
            onSubmit: handleSubmit,
            className: "flex shrink-0 items-center gap-2",
            children: [
              /* @__PURE__ */ jsx("label", { htmlFor: inputId, className: "sr-only", children: t("pptx.readOnly.passwordLabel") }),
              /* @__PURE__ */ jsx(
                "input",
                {
                  id: inputId,
                  "data-testid": "pptx-readonly-password-input",
                  type: "password",
                  autoFocus: true,
                  value: password,
                  disabled: checkingPassword,
                  onChange: (event) => setPassword(event.target.value),
                  placeholder: t("pptx.readOnly.passwordPlaceholder"),
                  "aria-invalid": passwordError !== null,
                  "aria-describedby": passwordError !== null ? errorId : void 0,
                  className: "rounded-sm border border-amber-600/40 bg-white/80 px-1.5 py-0.5 text-[11px] text-amber-950 dark:bg-black/20 dark:text-amber-100"
                }
              ),
              /* @__PURE__ */ jsx(
                "button",
                {
                  type: "submit",
                  "data-testid": "pptx-readonly-unlock",
                  disabled: checkingPassword,
                  className: "shrink-0 rounded-sm bg-amber-600/90 px-2 py-0.5 text-[11px] text-amber-50 transition-colors hover:bg-amber-600 disabled:opacity-60",
                  children: t("pptx.readOnly.unlock")
                }
              ),
              /* @__PURE__ */ jsx(
                "button",
                {
                  type: "button",
                  "data-testid": "pptx-readonly-password-cancel",
                  onClick: onCancelPassword,
                  className: "shrink-0 rounded-sm px-1.5 py-0.5 text-[11px] text-amber-900/80 transition-colors hover:bg-amber-600/20 dark:text-amber-200/80",
                  children: t("pptx.common.cancel")
                }
              ),
              passwordError !== null && /* @__PURE__ */ jsx(
                "span",
                {
                  id: errorId,
                  role: "alert",
                  "data-testid": "pptx-readonly-password-error",
                  className: "shrink-0 text-[11px] text-red-700 dark:text-red-300",
                  children: t(
                    passwordError === "wrong-password" ? "pptx.readOnly.wrongPassword" : "pptx.readOnly.unsupportedAlgorithm"
                  )
                }
              )
            ]
          }
        ) : /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "data-testid": "pptx-readonly-edit-anyway",
              onClick: onEditAnyway,
              className: "shrink-0 rounded-sm bg-amber-600/90 px-2 py-0.5 text-[11px] text-amber-50 transition-colors hover:bg-amber-600",
              children: t("pptx.readOnly.editAnyway")
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              "data-testid": "pptx-readonly-dismiss",
              onClick: onDismiss,
              "aria-label": t("pptx.readOnly.dismiss"),
              className: "shrink-0 rounded-sm px-1.5 py-0.5 text-[11px] text-amber-900/80 transition-colors hover:bg-amber-600/20 dark:text-amber-200/80",
              children: t("pptx.readOnly.dismiss")
            }
          )
        ] })
      ]
    }
  );
}
function AddinTable({
  title,
  rows,
  selectedId,
  onSelect
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("section", { children: [
    /* @__PURE__ */ jsx("h4", { className: "mb-1 text-xs font-semibold text-foreground", children: title }),
    rows.length === 0 ? /* @__PURE__ */ jsx("p", { className: "px-2 py-1 text-xs italic text-muted-foreground", children: t("pptx.options.addIns.description") }) : /* @__PURE__ */ jsx("table", { className: "w-full border-collapse text-left", children: /* @__PURE__ */ jsx("tbody", { children: rows.map((row) => /* @__PURE__ */ jsxs(
      "tr",
      {
        onClick: () => onSelect(row.id),
        className: cn(
          "cursor-pointer border-b border-border/40 transition-colors",
          selectedId === row.id ? "bg-primary/10" : "hover:bg-accent"
        ),
        children: [
          /* @__PURE__ */ jsx("td", { className: "px-2 py-1.5 text-xs text-foreground", children: t(row.nameKey) }),
          /* @__PURE__ */ jsx("td", { className: "px-2 py-1.5 font-mono text-[11px] text-muted-foreground", children: row.location }),
          /* @__PURE__ */ jsx("td", { className: "px-2 py-1.5 text-xs text-muted-foreground", children: t(`pptx.options.addInType.${row.type}`) })
        ]
      },
      row.id
    )) }) })
  ] });
}
function OptionsAddInsPane({ addinStatus }) {
  const { t } = useTranslation();
  const [selectedId, setSelectedId] = useState(null);
  const rows = resolveViewerAddinRows(addinStatus);
  const active = rows.filter((row) => row.active);
  const inactive = rows.filter((row) => !row.active);
  const selected = rows.find((row) => row.id === selectedId);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-[1fr_auto_auto] gap-x-2 border-b border-border pb-1 pl-2 text-[11px] font-semibold uppercase tracking-wide text-muted-foreground", children: [
      /* @__PURE__ */ jsx("span", { children: t("pptx.options.addIns.name") }),
      /* @__PURE__ */ jsx("span", { children: t("pptx.options.addIns.location") }),
      /* @__PURE__ */ jsx("span", { children: t("pptx.options.addIns.type") })
    ] }),
    /* @__PURE__ */ jsx(
      AddinTable,
      {
        title: t("pptx.options.addIns.active"),
        rows: active,
        selectedId,
        onSelect: setSelectedId
      }
    ),
    /* @__PURE__ */ jsx(
      AddinTable,
      {
        title: t("pptx.options.addIns.inactive"),
        rows: inactive,
        selectedId,
        onSelect: setSelectedId
      }
    ),
    selected && /* @__PURE__ */ jsxs("div", { className: "rounded border border-border/60 bg-muted/40 p-3", children: [
      /* @__PURE__ */ jsx("p", { className: "text-xs font-semibold text-foreground", children: t(selected.nameKey) }),
      /* @__PURE__ */ jsx("p", { className: "mt-1 text-xs text-muted-foreground", children: t(selected.descriptionKey) }),
      /* @__PURE__ */ jsx("p", { className: "mt-1 font-mono text-[11px] text-muted-foreground", children: selected.location })
    ] })
  ] });
}
function readValue(options, control) {
  const group = options[control.group];
  const value = group[control.key];
  return typeof value === "boolean" || typeof value === "number" || typeof value === "string" ? value : void 0;
}
function InfoTip({ text }) {
  return /* @__PURE__ */ jsx("span", { title: text, className: "inline-flex cursor-help align-middle", children: /* @__PURE__ */ jsx(LuInfo, { className: "ml-1 h-3.5 w-3.5 text-primary/70", "aria-label": text }) });
}
function ControlRow({
  control,
  options,
  onOptionChange
}) {
  const { t } = useTranslation();
  const value = readValue(options, control);
  const label = t(control.labelKey);
  const info = control.infoKey ? /* @__PURE__ */ jsx(InfoTip, { text: t(control.infoKey) }) : null;
  const rowClass = cn("flex items-center justify-between gap-3 py-1.5", control.indent && "pl-6");
  if (control.kind === "toggle") {
    return /* @__PURE__ */ jsxs("label", { className: cn(rowClass, "cursor-pointer select-none"), children: [
      /* @__PURE__ */ jsxs("span", { className: "text-sm text-foreground", children: [
        label,
        info
      ] }),
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          className: "h-4 w-4 shrink-0 accent-[var(--pptx-primary,#6366f1)]",
          checked: value === true,
          onChange: (event) => onOptionChange(control.group, control.key, event.target.checked)
        }
      )
    ] });
  }
  if (control.kind === "select") {
    return /* @__PURE__ */ jsxs("div", { className: rowClass, children: [
      /* @__PURE__ */ jsxs("span", { className: "text-sm text-foreground", children: [
        label,
        info
      ] }),
      /* @__PURE__ */ jsx(
        "select",
        {
          "aria-label": label,
          className: "max-w-[55%] rounded border border-border bg-background px-2 py-1 text-xs text-foreground",
          value: typeof value === "string" ? value : "",
          onChange: (event) => onOptionChange(control.group, control.key, event.target.value),
          children: control.choices.map((choice) => /* @__PURE__ */ jsx("option", { value: choice.value, children: t(choice.labelKey) }, choice.value))
        }
      )
    ] });
  }
  if (control.kind === "number") {
    return /* @__PURE__ */ jsxs("div", { className: rowClass, children: [
      /* @__PURE__ */ jsxs("span", { className: "text-sm text-foreground", children: [
        label,
        info
      ] }),
      /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1.5", children: [
        /* @__PURE__ */ jsx(
          "input",
          {
            type: "number",
            "aria-label": label,
            className: "w-20 rounded border border-border bg-background px-2 py-1 text-right text-xs text-foreground",
            min: control.min,
            max: control.max,
            step: control.step ?? 1,
            value: typeof value === "number" ? value : control.min,
            onChange: (event) => {
              const clamped = clampOptionNumber(event.target.value, control.min, control.max);
              if (clamped !== void 0) {
                onOptionChange(control.group, control.key, clamped);
              }
            }
          }
        ),
        control.unitKey && /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: t(control.unitKey) })
      ] })
    ] });
  }
  return /* @__PURE__ */ jsxs("div", { className: rowClass, children: [
    /* @__PURE__ */ jsxs("span", { className: "text-sm text-foreground", children: [
      label,
      info
    ] }),
    /* @__PURE__ */ jsx(
      "input",
      {
        type: "text",
        "aria-label": label,
        className: "w-48 max-w-[55%] rounded border border-border bg-background px-2 py-1 text-xs text-foreground",
        maxLength: control.maxLength,
        value: typeof value === "string" ? value : "",
        onChange: (event) => onOptionChange(control.group, control.key, event.target.value)
      }
    )
  ] });
}
function OptionsPane({
  tab,
  options,
  onOptionChange,
  renderSpecial,
  children
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "space-y-5", children: [
    /* @__PURE__ */ jsx("p", { className: "text-sm font-medium text-foreground", children: t(tab.descriptionKey) }),
    tab.sections.map((section) => /* @__PURE__ */ jsxs("section", { children: [
      /* @__PURE__ */ jsx("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t(section.titleKey) }),
      section.descriptionKey && /* @__PURE__ */ jsx("p", { className: "mb-2 text-xs text-muted-foreground", children: t(section.descriptionKey) }),
      /* @__PURE__ */ jsx("div", { className: "space-y-0.5", children: section.controls.map((control) => /* @__PURE__ */ jsx(
        ControlRow,
        {
          control,
          options,
          onOptionChange
        },
        `${control.group}.${control.key}.${section.id}`
      )) }),
      section.special && renderSpecial?.(section)
    ] }, section.id)),
    children
  ] });
}
function CommandList({
  title,
  commandIds,
  selectedId,
  onSelect
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs("div", { className: "flex-1", children: [
    /* @__PURE__ */ jsx("p", { className: "mb-1 text-xs font-medium text-muted-foreground", children: title }),
    /* @__PURE__ */ jsx(
      "div",
      {
        role: "listbox",
        "aria-label": title,
        className: "h-48 space-y-0.5 overflow-y-auto rounded border border-border/60 p-1",
        children: commandIds.map((id) => {
          const command = QUICK_ACCESS_COMMAND_CATALOG.find((entry) => entry.id === id);
          if (!command) {
            return null;
          }
          return /* @__PURE__ */ jsx(
            "button",
            {
              type: "button",
              role: "option",
              "aria-selected": selectedId === id,
              onClick: () => onSelect(id),
              className: cn(
                "flex w-full items-center rounded px-2 py-1.5 text-left text-sm transition-colors",
                selectedId === id ? "bg-primary/15 text-primary" : "text-foreground hover:bg-accent"
              ),
              children: t(command.labelKey)
            },
            id
          );
        })
      }
    )
  ] });
}
function OptionsQuickAccessPane({
  options,
  onQuickAccessCommandsChange,
  onResetQuickAccess
}) {
  const { t } = useTranslation();
  const [selectedAvailable, setSelectedAvailable] = useState(null);
  const [selectedCurrent, setSelectedCurrent] = useState(null);
  const current = options.quickAccess.commandIds;
  const available = availableQuickAccessCommands(current).map((entry) => entry.id);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-3", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-stretch gap-3", children: [
      /* @__PURE__ */ jsx(
        CommandList,
        {
          title: t("pptx.options.quickAccess.chooseCommands"),
          commandIds: available,
          selectedId: selectedAvailable,
          onSelect: setSelectedAvailable
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: "flex flex-col justify-center gap-2", children: [
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            disabled: !selectedAvailable,
            onClick: () => {
              if (selectedAvailable) {
                onQuickAccessCommandsChange(addQuickAccessCommand(current, selectedAvailable));
                setSelectedAvailable(null);
              }
            },
            className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: [
              t("pptx.options.quickAccess.add"),
              " >>"
            ]
          }
        ),
        /* @__PURE__ */ jsxs(
          "button",
          {
            type: "button",
            disabled: !selectedCurrent,
            onClick: () => {
              if (selectedCurrent) {
                onQuickAccessCommandsChange(removeQuickAccessCommand(current, selectedCurrent));
                setSelectedCurrent(null);
              }
            },
            className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: [
              "<< ",
              t("pptx.options.quickAccess.remove")
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        CommandList,
        {
          title: t("pptx.options.quickAccess.currentCommands"),
          commandIds: current,
          selectedId: selectedCurrent,
          onSelect: setSelectedCurrent
        }
      ),
      /* @__PURE__ */ jsxs("div", { className: "flex flex-col justify-center gap-2", children: [
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "aria-label": t("pptx.options.quickAccess.moveUp"),
            disabled: !selectedCurrent,
            onClick: () => {
              if (selectedCurrent) {
                onQuickAccessCommandsChange(moveQuickAccessCommand(current, selectedCurrent, "up"));
              }
            },
            className: "rounded border border-border p-1.5 text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: /* @__PURE__ */ jsx(LuChevronUp, { className: "h-4 w-4" })
          }
        ),
        /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            "aria-label": t("pptx.options.quickAccess.moveDown"),
            disabled: !selectedCurrent,
            onClick: () => {
              if (selectedCurrent) {
                onQuickAccessCommandsChange(
                  moveQuickAccessCommand(current, selectedCurrent, "down")
                );
              }
            },
            className: "rounded border border-border p-1.5 text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: /* @__PURE__ */ jsx(LuChevronDown, { className: "h-4 w-4" })
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        onClick: onResetQuickAccess,
        className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
        children: t("pptx.options.quickAccess.reset")
      }
    )
  ] });
}
function OptionsRibbonPane({
  options,
  onRibbonTabHiddenChange,
  onResetRibbon
}) {
  const { t } = useTranslation();
  const hidden = new Set(options.ribbon.hiddenTabIds);
  return /* @__PURE__ */ jsxs("div", { className: "space-y-5", children: [
    /* @__PURE__ */ jsxs("section", { children: [
      /* @__PURE__ */ jsx("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.options.ribbon.tabsTitle") }),
      /* @__PURE__ */ jsx("p", { className: "mb-2 text-xs text-muted-foreground", children: t("pptx.options.ribbon.tabsDescription") }),
      /* @__PURE__ */ jsx("div", { className: "space-y-0.5 rounded border border-border/60 p-2", children: TOOLBAR_TABS.map((tab) => {
        const isFile = tab.id === "file";
        const isVisible = isFile || !hidden.has(tab.id);
        return /* @__PURE__ */ jsxs(
          "label",
          {
            className: cn(
              "flex items-center gap-2 rounded px-2 py-1.5",
              isFile ? "cursor-not-allowed opacity-60" : "cursor-pointer hover:bg-accent"
            ),
            children: [
              /* @__PURE__ */ jsx(
                "input",
                {
                  type: "checkbox",
                  className: "h-4 w-4 accent-[var(--pptx-primary,#6366f1)]",
                  checked: isVisible,
                  disabled: isFile,
                  onChange: (event) => onRibbonTabHiddenChange(tab.id, !event.target.checked)
                }
              ),
              /* @__PURE__ */ jsx("span", { className: "text-sm text-foreground", children: t(tab.labelKey) })
            ]
          },
          tab.id
        );
      }) }),
      /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          onClick: onResetRibbon,
          className: "mt-2 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
          children: t("pptx.options.ribbon.reset")
        }
      )
    ] }),
    /* @__PURE__ */ jsxs("section", { children: [
      /* @__PURE__ */ jsx("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.settings.keyboardShortcuts") }),
      /* @__PURE__ */ jsx("div", { className: "space-y-0.5", children: SHORTCUT_REFERENCE_ITEMS.map((shortcut, i) => /* @__PURE__ */ jsxs(
        "div",
        {
          className: cn(
            "flex items-center justify-between gap-3 rounded px-3 py-2",
            i % 2 === 0 ? "bg-muted/60" : ""
          ),
          children: [
            /* @__PURE__ */ jsx("span", { className: "text-xs text-foreground", children: t(shortcut.actionKey) }),
            /* @__PURE__ */ jsx("span", { className: "whitespace-nowrap font-mono text-[11px] text-muted-foreground", children: shortcut.shortcut })
          ]
        },
        shortcut.actionKey
      )) })
    ] })
  ] });
}

// src/viewer/utils/ai-log-export.ts
async function exportAiChatLogs2(options) {
  const store = options?.store ?? createChatHistoryStore();
  const chats = await collectStoredChats(store);
  return exportAiChatLogs(
    chats,
    { format: options?.format, detailed: options?.detailed, now: options?.now },
    (filename, content, mime) => {
      downloadBlob(new Blob([content], { type: mime }), filename);
    }
  );
}
function SettingsAiTab({ store }) {
  const { t } = useTranslation();
  const activeStore = useMemo(() => store ?? createChatHistoryStore(), [store]);
  const [chatCount, setChatCount] = useState(null);
  const [detailed, setDetailed] = useState(true);
  const [status, setStatus] = useState({ kind: "idle" });
  useEffect(() => {
    let cancelled = false;
    const load = async () => {
      try {
        const chats = await activeStore.listChats();
        if (!cancelled) {
          setChatCount(chats.length);
        }
      } catch {
        if (!cancelled) {
          setChatCount(0);
        }
      }
    };
    void load();
    return () => {
      cancelled = true;
    };
  }, [activeStore]);
  const handleExport = useCallback(
    async (format) => {
      setStatus({ kind: "busy" });
      try {
        const count = await exportAiChatLogs2({ store: activeStore, format, detailed });
        setStatus({ kind: "done", count });
      } catch {
        setStatus({ kind: "done", count: 0 });
      }
    },
    [activeStore, detailed]
  );
  return /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-start gap-2 rounded-lg border border-border/60 bg-muted/30 p-3", children: [
      /* @__PURE__ */ jsx(LuBug, { className: "mt-0.5 h-4 w-4 shrink-0 text-primary" }),
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("p", { className: "text-sm font-medium text-foreground", children: t("pptx.ai.settingsSectionTitle") }),
        /* @__PURE__ */ jsx("p", { className: "mt-1 text-xs text-muted-foreground", children: t("pptx.ai.exportLogsHint") })
      ] })
    ] }),
    /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", children: chatCount === null ? t("pptx.ai.exportLogsCounting") : t("pptx.ai.exportLogsStoredCount", { count: chatCount }) }),
    /* @__PURE__ */ jsxs("label", { className: "flex items-center gap-2 text-xs text-foreground", children: [
      /* @__PURE__ */ jsx(
        "input",
        {
          type: "checkbox",
          checked: detailed,
          onChange: (e) => setDetailed(e.target.checked),
          className: "h-3.5 w-3.5 rounded border-border"
        }
      ),
      t("pptx.ai.exportLogsDetailed")
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-center gap-2", children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          onClick: () => void handleExport("json"),
          disabled: status.kind === "busy",
          className: "inline-flex items-center gap-1.5 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:opacity-50",
          children: [
            /* @__PURE__ */ jsx(LuDownload, { className: "h-3.5 w-3.5" }),
            t("pptx.ai.exportLogsJson")
          ]
        }
      ),
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          onClick: () => void handleExport("markdown"),
          disabled: status.kind === "busy",
          className: "inline-flex items-center gap-1.5 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:opacity-50",
          children: [
            /* @__PURE__ */ jsx(LuDownload, { className: "h-3.5 w-3.5" }),
            t("pptx.ai.exportLogsMarkdown")
          ]
        }
      )
    ] }),
    status.kind === "done" && /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", role: "status", children: status.count > 0 ? t("pptx.ai.exportLogsDone", { count: status.count }) : t("pptx.ai.noChatsToExport") })
  ] });
}
function SettingsAppearanceTab({
  activeThemeKey,
  themes,
  onSelectTheme
}) {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 gap-3", children: themes.map((entry) => {
    const isActive = entry.key === activeThemeKey;
    const previewBackground = entry.theme?.colors?.background ?? "#0b0f19";
    const previewPrimary = entry.theme?.colors?.primary ?? "#6366f1";
    return /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: () => onSelectTheme(entry.key),
        "aria-pressed": isActive,
        className: cn(
          "flex items-center gap-3 rounded-lg border px-3 py-2.5 text-left transition-colors",
          isActive ? "border-primary bg-primary/10" : "border-border hover:border-primary/50 hover:bg-accent"
        ),
        children: [
          /* @__PURE__ */ jsx(
            "span",
            {
              className: "h-8 w-8 shrink-0 rounded-full border border-border/60 shadow-inner",
              style: {
                background: `linear-gradient(135deg, ${previewBackground} 50%, ${previewPrimary} 50%)`
              }
            }
          ),
          /* @__PURE__ */ jsx(
            "span",
            {
              className: cn("text-xs font-medium", isActive ? "text-primary" : "text-foreground"),
              children: t(entry.labelKey)
            }
          )
        ]
      },
      entry.key
    );
  }) });
}
function SettingsCustomFontsSection({
  enabled,
  families,
  onRegistered
}) {
  const { t } = useTranslation();
  const inputRef = useRef(null);
  const [error, setError] = useState(false);
  const handleFile = async (file) => {
    setError(false);
    try {
      const registration = await registerCustomFont(file);
      if (registration) {
        onRegistered(registration.family);
      } else {
        setError(true);
      }
    } catch {
      setError(true);
    }
  };
  return /* @__PURE__ */ jsxs("div", { className: "mt-2", children: [
    /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        disabled: !enabled,
        onClick: () => inputRef.current?.click(),
        className: "inline-flex items-center gap-2 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
        children: [
          /* @__PURE__ */ jsx(LuUpload, { className: "h-3.5 w-3.5" }),
          t("pptx.options.general.addFontFile")
        ]
      }
    ),
    /* @__PURE__ */ jsx(
      "input",
      {
        ref: inputRef,
        type: "file",
        accept: CUSTOM_FONT_ACCEPT,
        className: "hidden",
        onChange: (event) => {
          const file = event.currentTarget.files?.[0];
          event.currentTarget.value = "";
          if (file) {
            void handleFile(file);
          }
        }
      }
    ),
    !enabled && /* @__PURE__ */ jsx("p", { className: "mt-2 text-xs text-muted-foreground", children: t("pptx.options.general.customFontsDisabled") }),
    error && /* @__PURE__ */ jsx("p", { role: "alert", className: "mt-2 text-xs text-destructive", children: t("pptx.options.general.customFontError") }),
    /* @__PURE__ */ jsx("p", { className: "mt-3 text-[11px] font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.options.general.customFontsAdded") }),
    families.length === 0 ? /* @__PURE__ */ jsx("p", { className: "mt-1 text-xs text-muted-foreground", children: t("pptx.options.general.customFontsEmpty") }) : /* @__PURE__ */ jsx("ul", { className: "mt-1 flex flex-col gap-0.5", children: families.map((family) => /* @__PURE__ */ jsx("li", { className: "text-xs text-foreground", style: { fontFamily: family }, children: family }, family)) })
  ] });
}
function SettingsLanguageTab({
  activeLocale,
  locales,
  onSelectLocale
}) {
  return /* @__PURE__ */ jsx("div", { className: "space-y-0.5", children: locales.map((locale) => {
    const isActive = locale.code === activeLocale;
    return /* @__PURE__ */ jsxs(
      "button",
      {
        type: "button",
        onClick: () => onSelectLocale(locale.code),
        "aria-pressed": isActive,
        className: cn(
          "flex w-full items-center justify-between rounded px-3 py-2.5 text-left transition-colors",
          isActive ? "bg-primary/10 text-primary" : "text-foreground hover:bg-accent"
        ),
        children: [
          /* @__PURE__ */ jsx("span", { className: "text-sm", children: locale.nativeLabel }),
          locale.nativeLabel !== locale.label && /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: locale.label })
        ]
      },
      locale.code
    );
  }) });
}
var AI_TAB_ID = "ai";
var NO_CUSTOM_FONT_FAMILIES = [];
function SettingsDialog({
  isOpen,
  onClose,
  options,
  onOptionChange,
  onRestoreOptions,
  onRibbonTabHiddenChange,
  onQuickAccessCommandsChange,
  onResetOptions,
  onClearCache,
  addinStatus,
  themeKey,
  availableThemes,
  onSelectTheme,
  localeCode,
  availableLocales,
  onSelectLocale,
  aiEnabled,
  chatStore,
  customFontFamilies = NO_CUSTOM_FONT_FAMILIES,
  onCustomFontRegistered
}) {
  const [activeTabId, setActiveTabId] = useState("general");
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const snapshotRef = useRef(null);
  const wasOpenRef = useRef(false);
  useEffect(() => {
    if (isOpen && !wasOpenRef.current) {
      snapshotRef.current = options;
    }
    wasOpenRef.current = isOpen;
  }, [isOpen, options]);
  const handleCancel = useCallback(() => {
    if (snapshotRef.current) {
      onRestoreOptions(snapshotRef.current);
    }
    onClose();
  }, [onClose, onRestoreOptions]);
  const handleKeyDown = useCallback(
    (e) => {
      if (e.key === "Escape") {
        onClose();
      }
    },
    [onClose]
  );
  useEffect(() => {
    if (isOpen) {
      document.addEventListener("keydown", handleKeyDown);
      return () => document.removeEventListener("keydown", handleKeyDown);
    }
  }, [isOpen, handleKeyDown]);
  if (!isOpen) {
    return null;
  }
  const activeTab = VIEWER_OPTIONS_TABS.find((tab) => tab.id === activeTabId) ?? VIEWER_OPTIONS_TABS[0];
  if (!activeTab) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.settings.closeSettings"),
        onClick: onClose
      }
    ),
    /* @__PURE__ */ jsx(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: /* @__PURE__ */ jsxs(
          "div",
          {
            style: panelStyle,
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.options.title"),
            className: "pointer-events-auto flex max-h-[85vh] w-[min(56rem,calc(100%-2rem))] flex-col rounded-xl border border-border bg-popover shadow-2xl backdrop-blur-xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:max-h-[88dvh] max-md:w-full max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              /* @__PURE__ */ jsxs(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between border-b border-border/60 px-5 py-4 touch-none",
                  children: [
                    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
                      /* @__PURE__ */ jsx(LuSettings, { className: "h-5 w-5 text-primary" }),
                      /* @__PURE__ */ jsx("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.options.title") })
                    ] }),
                    /* @__PURE__ */ jsx(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "rounded p-1 transition-colors hover:bg-accent",
                        "aria-label": t("pptx.settings.close"),
                        children: /* @__PURE__ */ jsx(LuX, { className: "h-4 w-4 text-muted-foreground" })
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsxs("div", { className: "flex min-h-0 flex-1 max-md:flex-col", children: [
                /* @__PURE__ */ jsxs(
                  "nav",
                  {
                    "aria-label": t("pptx.options.title"),
                    className: "w-44 shrink-0 space-y-0.5 overflow-y-auto border-r border-border/60 p-2 max-md:flex max-md:w-full max-md:space-y-0 max-md:gap-1 max-md:overflow-x-auto max-md:border-b max-md:border-r-0",
                    children: [
                      VIEWER_OPTIONS_TABS.map((tab) => /* @__PURE__ */ jsx(
                        "button",
                        {
                          type: "button",
                          onClick: () => setActiveTabId(tab.id),
                          "aria-current": activeTabId === tab.id,
                          className: cn(
                            "block w-full whitespace-nowrap rounded px-3 py-2 text-left text-sm transition-colors max-md:w-auto",
                            activeTabId === tab.id ? "bg-primary/10 font-medium text-primary" : "text-foreground hover:bg-accent"
                          ),
                          children: t(tab.labelKey)
                        },
                        tab.id
                      )),
                      aiEnabled && /* @__PURE__ */ jsx(
                        "button",
                        {
                          type: "button",
                          onClick: () => setActiveTabId(AI_TAB_ID),
                          "aria-current": activeTabId === AI_TAB_ID,
                          className: cn(
                            "block w-full whitespace-nowrap rounded px-3 py-2 text-left text-sm transition-colors max-md:w-auto",
                            activeTabId === AI_TAB_ID ? "bg-primary/10 font-medium text-primary" : "text-foreground hover:bg-accent"
                          ),
                          children: t("pptx.ai.settingsSectionTitle")
                        }
                      )
                    ]
                  }
                ),
                /* @__PURE__ */ jsx("div", { className: "min-h-0 flex-1 overflow-y-auto px-5 py-4", children: activeTabId === AI_TAB_ID ? /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
                  /* @__PURE__ */ jsx("p", { className: "text-sm font-medium text-foreground", children: t("pptx.ai.settingsSectionTitle") }),
                  /* @__PURE__ */ jsx(SettingsAiTab, { store: chatStore })
                ] }) : activeTab.custom === "language" ? /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
                  /* @__PURE__ */ jsx("p", { className: "text-sm font-medium text-foreground", children: t(activeTab.descriptionKey) }),
                  /* @__PURE__ */ jsxs("section", { children: [
                    /* @__PURE__ */ jsx("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.options.language.displayLanguage") }),
                    /* @__PURE__ */ jsx("p", { className: "mb-2 text-xs text-muted-foreground", children: t("pptx.options.language.displayLanguageDescription") }),
                    /* @__PURE__ */ jsx(
                      SettingsLanguageTab,
                      {
                        activeLocale: localeCode,
                        locales: availableLocales,
                        onSelectLocale
                      }
                    )
                  ] })
                ] }) : activeTab.custom === "ribbon" ? /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
                  /* @__PURE__ */ jsx("p", { className: "text-sm font-medium text-foreground", children: t(activeTab.descriptionKey) }),
                  /* @__PURE__ */ jsx(
                    OptionsRibbonPane,
                    {
                      options,
                      onRibbonTabHiddenChange,
                      onResetRibbon: () => onResetOptions("ribbon")
                    }
                  )
                ] }) : activeTab.custom === "addIns" ? /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
                  /* @__PURE__ */ jsx("p", { className: "text-sm font-medium text-foreground", children: t(activeTab.descriptionKey) }),
                  /* @__PURE__ */ jsx(OptionsAddInsPane, { addinStatus })
                ] }) : /* @__PURE__ */ jsx(
                  OptionsPane,
                  {
                    tab: activeTab,
                    options,
                    onOptionChange,
                    renderSpecial: (section) => {
                      if (section.special === "themePicker") {
                        return /* @__PURE__ */ jsx("div", { className: "mt-2", children: /* @__PURE__ */ jsx(
                          SettingsAppearanceTab,
                          {
                            activeThemeKey: themeKey,
                            themes: availableThemes,
                            onSelectTheme
                          }
                        ) });
                      }
                      if (section.special === "customFonts") {
                        return /* @__PURE__ */ jsx(
                          SettingsCustomFontsSection,
                          {
                            enabled: options.general.enableCustomFontUpload,
                            families: customFontFamilies,
                            onRegistered: (family) => onCustomFontRegistered?.(family)
                          }
                        );
                      }
                      if (section.special === "clearCache") {
                        return /* @__PURE__ */ jsxs("div", { className: "mt-2", children: [
                          /* @__PURE__ */ jsx("p", { className: "mb-2 text-xs text-muted-foreground", children: t("pptx.options.save.clearCacheDescription") }),
                          /* @__PURE__ */ jsx(
                            "button",
                            {
                              type: "button",
                              onClick: onClearCache,
                              className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
                              children: t("pptx.options.save.clearCacheNow")
                            }
                          )
                        ] });
                      }
                      return null;
                    },
                    children: activeTab.custom === "quickAccess" && /* @__PURE__ */ jsx(
                      OptionsQuickAccessPane,
                      {
                        options,
                        onQuickAccessCommandsChange,
                        onResetQuickAccess: () => onQuickAccessCommandsChange([...DEFAULT_QUICK_ACCESS_COMMAND_IDS])
                      }
                    )
                  }
                ) })
              ] }),
              /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-2 border-t border-border/60 px-5 py-3", children: [
                /* @__PURE__ */ jsx(
                  "button",
                  {
                    type: "button",
                    onClick: () => onResetOptions(),
                    className: "rounded border border-border px-3 py-1.5 text-xs text-muted-foreground transition-colors hover:bg-accent hover:text-foreground",
                    children: t("pptx.options.resetAll")
                  }
                ),
                /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      onClick: handleCancel,
                      className: "rounded border border-border px-4 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
                      children: t("pptx.common.cancel")
                    }
                  ),
                  /* @__PURE__ */ jsx(
                    "button",
                    {
                      type: "button",
                      onClick: onClose,
                      className: "rounded bg-primary px-4 py-1.5 text-xs font-medium text-primary-foreground transition-colors hover:bg-primary/90",
                      children: t("pptx.common.ok")
                    }
                  )
                ] })
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function useAiBridge(input) {
  const ref = useRef(input);
  ref.current = input;
  return useMemo(() => {
    const applySlidesUpdate = (updater, _label) => {
      const live = ref.current;
      live.setSlides((prev) => updater(structuredClone(prev)));
      live.bumpHistory();
      live.markDirty();
    };
    const readDeckData = (live) => ({
      slides: live.slides,
      width: live.canvasSize.width,
      height: live.canvasSize.height,
      theme: live.theme,
      sections: live.sections,
      presentationProperties: live.presentationProperties,
      customProperties: live.customProperties,
      coreProperties: live.coreProperties,
      appProperties: live.appProperties,
      viewProperties: live.viewProperties,
      tableStyleMap: live.tableStyleMap,
      tableStylesDefaultId: live.tableStylesDefaultId,
      tags: live.tagCollections
    });
    const differs = deckDataFieldChanged;
    return {
      getDeckMeta() {
        const live = ref.current;
        const firstTitle = live.slides[0]?.elements.find(
          (el) => "text" in el && typeof el.text === "string" && el.text.trim().length > 0
        );
        return {
          slideCount: live.slides.length,
          activeSlideIndex: live.activeSlideIndex,
          title: live.fileName ?? (firstTitle && "text" in firstTitle ? String(firstTitle.text) : void 0),
          width: live.canvasSize.width,
          height: live.canvasSize.height
        };
      },
      getSlides: () => ref.current.slides,
      getActiveSlideIndex: () => ref.current.activeSlideIndex,
      getTheme: () => ref.current.theme,
      getHandler: () => ref.current.handlerRef.current ?? void 0,
      goToSlide(index) {
        ref.current.setActiveSlideIndex(index);
      },
      selectElements(slideIndex, elementIds) {
        const live = ref.current;
        if (slideIndex !== live.activeSlideIndex) {
          live.setActiveSlideIndex(slideIndex);
        }
        live.applySelection(elementIds[0] ?? null, elementIds);
      },
      applySlidesUpdate,
      updateElement(slideIndex, elementId, updates) {
        applySlidesUpdate((slides) => {
          const el = slides[slideIndex]?.elements.find((e) => e.id === elementId);
          if (el) {
            applyElementUpdate(el, updates);
          }
          return slides;
        });
      },
      applyTheme(updates) {
        ref.current.applyThemeUpdates(updates);
      },
      getDeckData() {
        return readDeckData(ref.current);
      },
      applyDeckData(updater, _label) {
        const live = ref.current;
        const before = readDeckData(live);
        const after = updater(structuredClone(before));
        if (differs(before.slides, after.slides)) {
          live.setSlides(() => after.slides);
          live.bumpHistory();
        }
        if (before.width !== after.width || before.height !== after.height) {
          live.setCanvasSize({ width: after.width, height: after.height });
        }
        const nextSections = after.sections ?? before.sections ?? [];
        if (differs(before.sections, nextSections)) {
          live.setSections(nextSections);
        }
        const nextPresProps = after.presentationProperties ?? live.presentationProperties;
        if (differs(before.presentationProperties, nextPresProps)) {
          live.setPresentationProperties(nextPresProps);
        }
        const nextCustomProps = after.customProperties ?? before.customProperties ?? [];
        if (differs(before.customProperties, nextCustomProps)) {
          live.setCustomProperties(nextCustomProps);
        }
        if (differs(before.coreProperties, after.coreProperties)) {
          live.setCoreProperties(after.coreProperties);
        }
        if (differs(before.appProperties, after.appProperties)) {
          live.setAppProperties(after.appProperties);
        }
        if (differs(before.viewProperties, after.viewProperties)) {
          live.setViewProperties(after.viewProperties);
        }
        if (differs(before.tableStyleMap, after.tableStyleMap)) {
          live.setTableStyleMap(after.tableStyleMap);
        }
        if (differs(before.tableStylesDefaultId, after.tableStylesDefaultId)) {
          live.setTableStylesDefaultId(after.tableStylesDefaultId);
        }
        const nextTags = after.tags ?? before.tags ?? [];
        if (differs(before.tags, nextTags)) {
          live.setTagCollections(nextTags);
        }
        live.markDirty();
      },
      getFocusedTargets() {
        const live = ref.current;
        if (live.pickedFocus && live.pickedFocus.length > 0) {
          return live.pickedFocus;
        }
        if (live.pinnedFocus && live.pinnedFocus.length > 0) {
          return live.pinnedFocus;
        }
        return computeFocusTargets({
          activeSlideIndex: live.activeSlideIndex,
          selectedElementIds: live.selectedElementIds,
          selectedElementId: live.selectedElementId
        });
      },
      notify(message, level) {
        ref.current.notify?.(message, level);
      }
    };
  }, []);
}
var TOOL_FLASH_MS = 2600;
function fixDirective(element, slideIndex) {
  return `Review and fix any issues with this ${element.type} (id=${element.id}) on slide ${slideIndex + 1}.`;
}
function useAiPanelController(input) {
  const { activeSlideIndex, selectedElementId, selectedElementIds, selectedElement } = input;
  const [isOpen, setIsOpen] = useState(false);
  const [pinnedFocus, setPinnedFocus] = useState(null);
  const [prefill, setPrefill] = useState({ text: "", nonce: 0 });
  const [pickMode, setPickMode] = useState(false);
  const [pickTargets, setPickTargets] = useState([]);
  const [toolFocus, setToolFocus] = useState(
    null
  );
  const [flashTick, setFlashTick] = useState(0);
  const flashTimer = useRef(null);
  const [changeBatch, setChangeBatch] = useState(null);
  const showChangeBatch = useCallback((batch) => setChangeBatch(batch), []);
  const liveFocusTargets = useMemo(
    () => computeFocusTargets({ activeSlideIndex, selectedElementIds, selectedElementId }),
    [activeSlideIndex, selectedElementIds, selectedElementId]
  );
  const open = useCallback(() => setIsOpen(true), []);
  const close = useCallback(() => setIsOpen(false), []);
  const toggle = useCallback(() => setIsOpen((p) => !p), []);
  const pinFocus = useCallback(() => setPinnedFocus(liveFocusTargets), [liveFocusTargets]);
  const clearPinnedFocus = useCallback(() => setPinnedFocus(null), []);
  const askAboutSelection = useCallback(() => {
    setPinnedFocus(liveFocusTargets);
    setPrefill((p) => ({ text: "", nonce: p.nonce + 1 }));
    setIsOpen(true);
  }, [liveFocusTargets]);
  const fixSelection = useCallback(() => {
    setPinnedFocus(liveFocusTargets);
    const text = selectedElement ? fixDirective(selectedElement, activeSlideIndex) : "";
    setPrefill((p) => ({ text, nonce: p.nonce + 1 }));
    setIsOpen(true);
  }, [liveFocusTargets, selectedElement, activeSlideIndex]);
  const startPicking = useCallback(() => {
    setPickMode(true);
    setIsOpen(true);
  }, []);
  const stopPicking = useCallback(() => setPickMode(false), []);
  const addPick = useCallback((slideIndex, elementId) => {
    setPickTargets((prev) => {
      if (prev.some((t) => t.kind === "element" && t.elementId === elementId)) {
        return prev;
      }
      return [...prev, { kind: "element", slideIndex, elementId }];
    });
  }, []);
  const clearPicks = useCallback(() => {
    setPickTargets([]);
    setPickMode(false);
  }, []);
  const flashToolTarget = useCallback((target) => {
    if (target && target.slideIndex !== void 0 && target.elementIds.length > 0) {
      setToolFocus({ slideIndex: target.slideIndex, elementIds: target.elementIds });
    } else {
      setToolFocus(null);
    }
    setFlashTick((n) => n + 1);
    if (flashTimer.current) {
      clearTimeout(flashTimer.current);
    }
    flashTimer.current = setTimeout(() => {
      setToolFocus(null);
      setFlashTick(0);
    }, TOOL_FLASH_MS);
  }, []);
  useEffect(
    () => () => {
      if (flashTimer.current) {
        clearTimeout(flashTimer.current);
      }
    },
    []
  );
  const canvasHighlights = useMemo(() => {
    const out = [];
    for (const t of pickTargets) {
      if (t.kind === "element") {
        out.push({ slideIndex: t.slideIndex, elementId: t.elementId, variant: "pick" });
      }
    }
    if (toolFocus) {
      for (const elementId of toolFocus.elementIds) {
        out.push({ slideIndex: toolFocus.slideIndex, elementId, variant: "active" });
      }
    }
    return out;
  }, [pickTargets, toolFocus]);
  const canvasAnimating = canvasHighlights.length > 0 || flashTick > 0;
  return {
    isOpen,
    open,
    close,
    toggle,
    liveFocusTargets,
    pinnedFocus,
    pinFocus,
    clearPinnedFocus,
    prefill,
    askAboutSelection,
    fixSelection,
    pickMode,
    startPicking,
    stopPicking,
    pickTargets,
    addPick,
    clearPicks,
    canvasHighlights,
    canvasAnimating,
    flashToolTarget,
    changeBatch,
    showChangeBatch
  };
}
function useMasterViewCrud(input) {
  const {
    handlerRef,
    slides,
    slideMasters,
    target,
    setSlides,
    setSlideMasters,
    setActiveMasterIndex,
    setActiveLayoutIndex,
    markDirty,
    pushToast
  } = input;
  const { t } = useTranslation();
  const data = useMemo(
    () => ({ slides, slideMasters }),
    [slides, slideMasters]
  );
  const crudActions = useMemo(() => masterViewCrudActions(data, target), [data, target]);
  const handleCrudAction = useCallback(
    async (id) => {
      const handler = handlerRef.current;
      if (!handler || !target) {
        return;
      }
      const action = crudActions.find((candidate) => candidate.id === id);
      if (!action?.enabled) {
        return;
      }
      let name;
      if (id === "renameLayout" || id === "renameMaster") {
        const master = slideMasters[target.masterIndex];
        const current = id === "renameLayout" ? master?.layouts?.[target.layoutIndex ?? -1]?.name ?? "" : master?.name ?? "";
        const next = safePrompt(t("pptx.masterView.renamePrompt"), current)?.trim();
        if (!next) {
          return;
        }
        name = next;
      }
      const result = await applyMasterViewCrudAction(
        handler,
        data,
        id,
        target,
        name !== void 0 ? { name } : void 0
      );
      if (!result.ok) {
        pushToast({
          id: `master-crud-${Date.now()}`,
          code: `masterViewCrud:${result.reason}`,
          severity: "warning",
          messageKey: masterViewCrudFailureKey(id, result.reason)
        });
        return;
      }
      handlerRef.current = result.handler;
      setSlides(result.data.slides);
      setSlideMasters(result.data.slideMasters ?? []);
      setActiveMasterIndex(result.target.masterIndex);
      setActiveLayoutIndex(result.target.layoutIndex);
      markDirty();
    },
    [
      handlerRef,
      target,
      crudActions,
      slideMasters,
      t,
      data,
      pushToast,
      setSlides,
      setSlideMasters,
      setActiveMasterIndex,
      setActiveLayoutIndex,
      markDirty
    ]
  );
  return { crudActions, handleCrudAction };
}
function applyRecentColorPick(recent, hex) {
  const next = pushRecentColor(recent, hex);
  if (next === recent) {
    return null;
  }
  return { recentColors: next, patch: mruColorsPatch(next) };
}
function useRecentColorsSync(input) {
  const { setRecentColors, setPresentationProperties } = input;
  const pushColor = useCallback(
    (hex) => {
      setRecentColors((prev) => {
        const applied = applyRecentColorPick(prev, hex);
        if (!applied) {
          return prev;
        }
        setPresentationProperties((properties) => ({ ...properties, ...applied.patch }));
        return applied.recentColors;
      });
    },
    [setRecentColors, setPresentationProperties]
  );
  return { pushColor };
}
var PowerPointViewer = forwardRef(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the forwardRef component its displayName
  function PowerPointViewer2(props, ref) {
    const {
      content: incomingContent,
      fonts = [],
      filePath,
      fileName,
      canEdit: hostCanEdit = false,
      autosave: hostAutosave,
      autosaveIntervalMs: hostAutosaveIntervalMs,
      onContentChange,
      onDirtyChange,
      onActiveSlideChange,
      onModeChange,
      onZoomChange,
      onSelectionChange,
      onSlideCountChange,
      onOpenFile: hostOpenFile,
      theme,
      defaultThemeKey,
      availableThemes,
      onThemeChange,
      defaultLocale,
      availableLocales,
      onLocaleChange,
      accountAuth,
      authorName,
      collaboration,
      onStartCollaboration,
      onStopCollaboration,
      shareDefaults,
      smartArt3D = false,
      surfaceChart3D = false,
      barChart3D = false,
      lineChart3D = false,
      areaChart3D = false,
      pieChart3D = false,
      hiddenActions,
      ai
    } = props;
    useEffect(() => {
      const css = buildUserFontFaceStyles(fonts);
      if (!css) {
        return;
      }
      const style = document.createElement("style");
      style.dataset.pptxUserFonts = "true";
      style.textContent = css;
      document.head.appendChild(style);
      return () => style.remove();
    }, [fonts]);
    const [themeKey, setThemeKey] = useState(
      () => defaultThemeKey ?? readStoredViewerPrefs().themeKey ?? "default"
    );
    const themeCatalog = availableThemes ?? THEME_CATALOG;
    const effectiveTheme = theme ?? resolveThemeCatalogEntry(themeKey, themeCatalog);
    const handleThemeChange = useCallback(
      (key) => {
        setThemeKey(key);
        if (onThemeChange) {
          onThemeChange(key);
        } else {
          writeStoredViewerPrefs({ themeKey: key });
        }
      },
      [onThemeChange]
    );
    const themeStyle = useThemeStyle(effectiveTheme);
    const [customFontFamilies, setCustomFontFamilies] = useState([]);
    const handleCustomFontRegistered = useCallback((family) => {
      setCustomFontFamilies(
        (current) => current.includes(family) ? current : [...current, family]
      );
    }, []);
    const { i18n } = useTranslation();
    const [localeCode, setLocaleCode] = useState(
      () => defaultLocale ?? readStoredViewerPrefs().localeCode ?? "en"
    );
    useEffect(() => {
      if (localeCode !== "en" && !onLocaleChange) {
        void i18n.changeLanguage(localeCode);
      }
    }, []);
    const resolvedLocales = useMemo(() => {
      if (availableLocales) {
        return availableLocales;
      }
      const registeredCodes = i18n.options.resources ? Object.keys(i18n.options.resources) : i18n.languages ?? ["en"];
      return registeredCodes.map(
        (code) => LOCALE_CATALOG.find((entry) => entry.code === code) ?? {
          code,
          label: code,
          nativeLabel: code
        }
      );
    }, [availableLocales, i18n]);
    const handleLocaleChange = useCallback(
      (code) => {
        setLocaleCode(code);
        if (onLocaleChange) {
          onLocaleChange(code);
        } else {
          void i18n.changeLanguage(code);
          writeStoredViewerPrefs({ localeCode: code });
        }
      },
      [onLocaleChange, i18n]
    );
    const [content, setContent] = useState(incomingContent);
    const [loadOrigin, setLoadOrigin] = useState("bootstrap");
    useEffect(() => {
      setContent(incomingContent);
      setLoadOrigin("bootstrap");
    }, [incomingContent]);
    const handleOpenFile = useCallback(() => {
      if (hostOpenFile) {
        hostOpenFile();
        return;
      }
      void (async () => {
        const picked = await openPptxFile();
        if (picked) {
          setLoadOrigin("user");
          setContent(picked.buffer);
        }
      })();
    }, [hostOpenFile]);
    const handleOpenRecentFile = useCallback((key) => {
      void (async () => {
        const bytes = await readBackstageRecentFile(key);
        if (bytes) {
          setLoadOrigin("user");
          setContent(bytes);
        }
      })();
    }, []);
    const { optionsStore, options: viewerOptions } = useViewerOptions();
    const [protectedViewOverridden, setProtectedViewOverridden] = useState(false);
    useEffect(() => {
      setProtectedViewOverridden(false);
    }, [content]);
    const isProtectedView = viewerOptions.trust.openInProtectedView && !protectedViewOverridden;
    const handleEnableEditing = useCallback(() => {
      setProtectedViewOverridden(true);
    }, []);
    const readOnlyRec = useReadOnlyRecommendationState(content);
    const compatToastsState = useCompatibilityToastsState();
    const canEdit = hostCanEdit && !isProtectedView && !readOnlyRec.locked;
    const [isSettingsOpen, setIsSettingsOpen] = useState(false);
    const [isHeaderFooterOpen, setIsHeaderFooterOpen] = useState(false);
    const [autosaveEnabled, setAutosaveEnabled] = useState(true);
    const autosaveActivation = resolveAutosaveActivation({
      hostAutosave,
      userEnabled: autosaveEnabled,
      canEdit,
      filePath
    });
    const [isShareDialogOpen, setIsShareDialogOpen] = useState(false);
    const { reducedMotion, toggleReducedMotion } = useReducedMotion();
    const state = useViewerState({ content, canEdit });
    const {
      containerRef,
      mode,
      slides,
      canvasSize,
      loading,
      error,
      activeSlideIndex,
      selectedElementId,
      selectedElementIds,
      templateElementsBySlideId,
      activeSlide,
      selectedElement
    } = state;
    const { pushColor } = useRecentColorsSync({
      setRecentColors: state.setRecentColors,
      setPresentationProperties: state.setPresentationProperties
    });
    const recentColorsValue = useMemo(
      () => ({ recentColors: state.recentColors, pushColor }),
      [state.recentColors, pushColor]
    );
    const themeColorMapValue = useMemo(
      () => state.theme?.colorScheme ? buildThemeColorMap(state.theme.colorScheme) : void 0,
      [state.theme?.colorScheme]
    );
    const settings = useMemo(
      () => ({
        autoSave: autosaveEnabled,
        spellCheck: state.spellCheckEnabled,
        showGrid: state.showGrid,
        showRulers: state.showRulers,
        snapToGrid: state.snapToGrid,
        reducedMotion
      }),
      [
        autosaveEnabled,
        state.spellCheckEnabled,
        state.showGrid,
        state.showRulers,
        state.snapToGrid,
        reducedMotion
      ]
    );
    const handleSettingsChange = useCallback(
      (key, value) => {
        switch (key) {
          case "autoSave":
            setAutosaveEnabled(value);
            break;
          case "spellCheck":
            state.setSpellCheckEnabled(value);
            break;
          case "showGrid":
            state.setShowGrid(value);
            break;
          case "showRulers":
            state.setShowRulers(value);
            break;
          case "snapToGrid":
            state.setSnapToGrid(value);
            break;
          case "reducedMotion":
            if (value !== reducedMotion) {
              toggleReducedMotion();
            }
            break;
        }
      },
      [state, reducedMotion, toggleReducedMotion]
    );
    const settingsRef = useRef(settings);
    settingsRef.current = settings;
    const handleSettingsChangeRef = useRef(handleSettingsChange);
    handleSettingsChangeRef.current = handleSettingsChange;
    const syncingFromOptionsRef = useRef(false);
    useEffect(() => {
      const mapped = viewerOptionsToPreferences(viewerOptions);
      let changed = false;
      for (const key of Object.keys(mapped)) {
        if (mapped[key] !== settingsRef.current[key]) {
          changed = true;
          handleSettingsChangeRef.current(key, mapped[key]);
        }
      }
      if (changed) {
        syncingFromOptionsRef.current = true;
      }
    }, [viewerOptions]);
    useEffect(() => {
      const current = optionsStore.getOptions();
      const mapped = viewerOptionsToPreferences(current);
      const keys = Object.keys(mapped);
      if (syncingFromOptionsRef.current) {
        if (keys.every((key) => mapped[key] === settings[key])) {
          syncingFromOptionsRef.current = false;
        }
        return;
      }
      let next = current;
      for (const key of keys) {
        if (mapped[key] !== settings[key]) {
          next = applyPreferenceToOptions(next, key, settings[key]);
        }
      }
      if (next !== current) {
        optionsStore.setOptions(next);
      }
    }, [settings, optionsStore]);
    const handleClearOptionsCache = useCallback(() => {
      void (async () => {
        const snapshots = await listAutosaveSnapshots();
        await Promise.all(snapshots.map((entry) => deleteAutosaveSnapshot(entry.key)));
      })();
    }, []);
    useEffect(() => {
      void (async () => {
        try {
          const snapshots = await listAutosaveSnapshots();
          const expired = resolveExpiredAutosaveSnapshots(snapshots, viewerOptions);
          await Promise.all(expired.map((key) => deleteAutosaveSnapshot(key)));
        } catch {
        }
      })();
    }, []);
    useEffect(() => {
      const clearIfRequested = () => {
        if (shouldClearAutosaveCacheOnClose(viewerOptions)) {
          handleClearOptionsCache();
        }
      };
      window.addEventListener("beforeunload", clearIfRequested);
      return () => {
        window.removeEventListener("beforeunload", clearIfRequested);
        clearIfRequested();
      };
    }, [viewerOptions, handleClearOptionsCache]);
    const mobile = useIsMobile();
    const { isMobile, isTouchDevice, isVirtualKeyboardOpen } = mobile;
    const resizablePanels = useResizablePanels();
    const {
      gridSpacingPx,
      visibleSlideIndexes,
      slideSectionGroups,
      masterPseudoSlide,
      authoredRange
    } = useDerivedSlideState({
      slides,
      sections: state.sections,
      customShows: state.customShows,
      activeCustomShowId: state.activeCustomShowId,
      presentationProperties: state.presentationProperties,
      mode,
      activeLayout: state.activeLayout,
      activeMaster: state.activeMaster,
      documentGridSpacing: state.viewProperties?.gridSpacing
    });
    const activeCustomShow = state.customShows.find((show) => show.id === state.activeCustomShowId) ?? null;
    const hasActivePointerInteraction = useCallback(
      () => Boolean(
        state.dragStateRef.current || state.resizeStateRef.current || state.marqueeStateRef.current || state.shapeAdjustmentDragStateRef.current || state.isDrawingRef.current
      ),
      [
        state.dragStateRef,
        state.resizeStateRef,
        state.marqueeStateRef,
        state.shapeAdjustmentDragStateRef,
        state.isDrawingRef
      ]
    );
    const zoom = useZoomViewport({
      canvasSize,
      selectedElements: state.selectedElements
    });
    const { setIsDirty } = state;
    const markDocumentDirty = useCallback(() => {
      setIsDirty(true);
    }, [setIsDirty]);
    const history = useEditorHistory({
      slides,
      canvasSize,
      activeSlideIndex,
      templateElementsBySlideId,
      selectedElementId,
      selectedElementIds,
      editTemplateMode: state.editTemplateMode,
      headerFooter: state.headerFooter,
      loading,
      error,
      // File > Options > Advanced > "Maximum number of undos", re-read every
      // render so a mid-session change reaches the undo stack.
      maxHistoryEntries: resolveHistoryDepth(viewerOptions),
      hasActivePointerInteraction,
      pointerCommitNonce: state.pointerCommitNonce,
      onDirty: markDocumentDirty,
      setSlides: state.setSlides,
      setCanvasSize: state.setCanvasSize,
      setActiveSlideIndex: state.setActiveSlideIndex,
      setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
      setSelectedElementId: state.setSelectedElementId,
      setSelectedElementIds: state.setSelectedElementIds,
      setEditTemplateMode: state.setEditTemplateMode,
      setHeaderFooter: state.setHeaderFooter
    });
    const { presentation, annotations, actionSoundHandlerRef, setExitModeHandler } = usePresentationSetup({
      mode,
      slides,
      visibleSlideIndexes,
      // File > Options > Advanced > "End with black slide". Off means the show
      // exits straight to the editor instead of raising the black end screen.
      endWithBlackSlide: viewerOptions.advanced.slideShowEndWithBlackSlide,
      // File > Options > Advanced > "Prompt to keep ink annotations when
      // exiting". Off skips the keep/discard dialog entirely.
      promptKeepInkAnnotations: viewerOptions.advanced.slideShowPromptKeepInkAnnotations,
      // File > Options > Advanced > "Show popup toolbar" while presenting.
      popupToolbarEnabled: viewerOptions.advanced.slideShowShowPopupToolbar,
      activeSlideIndex,
      containerRef,
      content,
      mediaDataUrls: state.mediaDataUrls,
      presentationProperties: state.presentationProperties,
      setMode: state.setMode,
      setActiveSlideIndex: state.setActiveSlideIndex,
      setSlides: state.setSlides,
      history,
      // PowerPoint's bare `J` during a show toggles live captions. It resolves
      // in the shared slide-show keymap; this is the state it has to reach.
      onToggleSubtitles: () => state.setPresentationProperties((prev) => ({
        ...prev,
        showSubtitles: !prev.showSubtitles
      })),
      customShows: state.customShows,
      activeCustomShowId: state.activeCustomShowId,
      onSetActiveCustomShowId: state.setActiveCustomShowId
    });
    useTouchGestures({
      targetRef: zoom.canvasViewportRef,
      currentScale: zoom.scale,
      callbacks: {
        onPinchZoom: (newScale) => zoom.setScale(newScale),
        onSwipe: mode === "present" ? (direction) => presentation.movePresentationSlide(direction === 1 ? -1 : 1) : void 0,
        onLongPress: (clientX, clientY) => {
          if (mode !== "edit" || !canEdit) {
            return;
          }
          if (!state.selectedElementId) {
            return;
          }
          state.setContextMenuState({
            x: clientX,
            y: clientY,
            elementId: state.selectedElementId
          });
        }
      },
      enabled: isTouchDevice
    });
    const dialogs = useViewerDialogs({
      mode,
      slides,
      activeSlide,
      activeSlideIndex,
      canvasSize,
      containerRef,
      customShows: state.customShows,
      activeCustomShowId: state.activeCustomShowId,
      setCustomShows: state.setCustomShows,
      setActiveCustomShowId: state.setActiveCustomShowId,
      setGuides: state.setGuides,
      setPresentationProperties: state.setPresentationProperties,
      setAccessibilityIssues: state.setAccessibilityIssues,
      setIsAccessibilityPanelOpen: state.setIsAccessibilityPanelOpen,
      setMode: state.setMode,
      setPreMasterMode: state.setPreMasterMode,
      setActiveMasterIndex: state.setActiveMasterIndex,
      setActiveLayoutIndex: state.setActiveLayoutIndex,
      setSelectedElementId: state.setSelectedElementId,
      setSelectedElementIds: state.setSelectedElementIds,
      preMasterMode: state.preMasterMode,
      hasDigitalSignatures: state.hasDigitalSignatures,
      isDirty: state.isDirty,
      history,
      embeddedFontFamilies: state.embeddedFonts.map((font) => font.name)
    });
    useEffect(() => {
      state.setSelectedElementId(null);
      state.setSelectedElementIds([]);
      state.setInlineEditingElementId(null);
    }, [activeSlideIndex]);
    useEffect(() => {
      if (mode !== "present") {
        return;
      }
      state.setSelectedElementId(null);
      state.setSelectedElementIds([]);
      state.setInlineEditingElementId(null);
      zoom.handleZoomToFit();
      zoom.canvasViewportRef.current?.scrollTo({ left: 0, top: 0 });
    }, [mode]);
    const editorOps = useEditorOperations({
      state,
      history,
      zoom,
      mode,
      canEdit,
      slides,
      activeSlide,
      activeSlideIndex,
      selectedElement,
      selectedElementId,
      selectedElementIds,
      canvasSize,
      dialogs,
      presentation,
      // Comment/reply authorship: an explicit host `authorName` or an active
      // collaboration session's identity wins; otherwise fall back to the
      // user's own Options > General > "User name" before the generic "You".
      userName: authorName ?? collaboration?.userName ?? (viewerOptions.general.userName || void 0),
      handlerRef: actionSoundHandlerRef,
      // Options > Proofing > AutoCorrect, applied to committed inline-edit text.
      transformCommittedText: (text) => applyAutoCorrect(text, viewerOptions.proofing)
    });
    const {
      exportHandlers: rawExportHandlers,
      printHandlers,
      themeHandlers,
      propertyHandlers,
      showKeepAnnotationsDialog,
      handleSetMode,
      handleKeepAnnotations,
      handleDiscardAnnotations,
      handleEnterPresenterView,
      handleEnterRehearsalMode,
      autosaveStatus,
      recovery,
      isEncryptedDialogOpen,
      setIsEncryptedDialogOpen,
      handlerRef,
      loadVersion
    } = useViewerIntegration({
      state,
      zoom,
      history,
      presentation,
      annotations,
      actionSoundHandlerRef,
      editorOps,
      dialogs,
      gridSpacingPx,
      content,
      filePath,
      autosaveEnabled: autosaveActivation.active,
      autosaveAllowed: hostAutosave !== false,
      // Host prop first (an explicit policy), else File > Options > Save >
      // "Save AutoRecover information every N minutes", else the shared 120s.
      autosaveIntervalMs: resolveAutosaveIntervalMs({
        hostIntervalMs: hostAutosaveIntervalMs,
        optionsIntervalSeconds: resolveAutosaveIntervalSeconds(viewerOptions)
      }),
      // Trust Center > "Allow external content": core defaults this to false
      // (SSRF/privacy-safe), so only an explicit `true` here lets remote
      // http(s) image sources actually load.
      allowExternalImages: viewerOptions.trust.allowExternalContent,
      setReadOnlyRecommendation: readOnlyRec.setRecommendation,
      setModifyVerifier: readOnlyRec.setModifyVerifier,
      setCompatToasts: compatToastsState.setToasts,
      canEdit,
      promptKeepInkAnnotations: viewerOptions.advanced.slideShowPromptKeepInkAnnotations,
      // File > Options > Advanced > "Image Size and Quality" (do-not-compress /
      // default resolution), resolved to a raster-scale multiplier for
      // PNG/PDF export and copy-slide-as-image. Multiplied against the
      // pre-existing 2x baseline (not used outright) so the default "High
      // fidelity" preset (raw multiplier 1) keeps today's export quality
      // instead of silently downgrading it; the explicit ppi presets still
      // scale proportionally from that baseline. Mirrors the other bindings.
      imageExportScale: 2 * resolveImageResolutionScale(viewerOptions),
      mode,
      slides,
      activeSlide,
      activeSlideIndex,
      canvasSize,
      loading,
      error,
      ref,
      setContent,
      onContentChange,
      onDirtyChange,
      onActiveSlideChange,
      onModeChange,
      onZoomChange,
      onSelectionChange,
      onSlideCountChange
    });
    const viewPreferencesSync = useViewPreferencesSync({
      loadVersion,
      viewProperties: state.viewProperties,
      setViewProperties: state.setViewProperties,
      snapToGrid: state.snapToGrid,
      setSnapToGrid: state.setSnapToGrid,
      snapToShape: state.snapToShape,
      setSnapToShape: state.setSnapToShape,
      showGuides: state.showGuides,
      setShowGuides: state.setShowGuides
    });
    const exportHandlers = useMemo(() => {
      const afterSuccessfulSave = (format) => {
        playFeedbackSound(viewerOptions);
        if (format === "pptx" && filePath && shouldDiscardAutosaveOnSuccessfulSave(viewerOptions)) {
          void deleteAutosaveSnapshot(filePath);
        }
      };
      const handleSaveAsFormat = async (format) => {
        await rawExportHandlers.handleSaveAsFormat(format);
        afterSuccessfulSave(format);
      };
      return {
        ...rawExportHandlers,
        handleSaveAsFormat,
        handleSaveAsPptx: () => void handleSaveAsFormat("pptx"),
        handleSaveAsPpsx: () => void handleSaveAsFormat("ppsx"),
        handleSaveAsPptm: () => void handleSaveAsFormat("pptm")
      };
    }, [rawExportHandlers, viewerOptions, filePath]);
    setExitModeHandler(handleSetMode);
    const handleTemplateElementsChanged = useCallback(
      (slideId, elements) => {
        state.setTemplateElementsBySlideId((prev) => ({ ...prev, [slideId]: elements }));
      },
      [state]
    );
    const layoutSwitching = useLayoutSwitching({
      handler: handlerRef.current,
      slides,
      activeSlideIndex,
      ops: editorOps.ops,
      history,
      onTemplateElementsChanged: handleTemplateElementsChanged
    });
    const masterViewCrud = useMasterViewCrud({
      handlerRef,
      slides,
      slideMasters: state.slideMasters,
      target: {
        tab: state.masterViewTab,
        masterIndex: state.activeMasterIndex,
        layoutIndex: state.activeLayoutIndex
      },
      setSlides: state.setSlides,
      setSlideMasters: state.setSlideMasters,
      setActiveMasterIndex: state.setActiveMasterIndex,
      setActiveLayoutIndex: state.setActiveLayoutIndex,
      markDirty: history.markDirty,
      pushToast: (toast) => compatToastsState.setToasts((prev) => [...prev, toast])
    });
    const applyAiTheme = useCallback(
      (updates) => {
        if (updates.colorScheme) {
          void themeHandlers.handleUpdateThemeColorScheme(updates.colorScheme);
        }
        if (updates.fontScheme) {
          void themeHandlers.handleUpdateThemeFontScheme(updates.fontScheme);
        }
      },
      [themeHandlers]
    );
    const bumpAiHistory = useCallback(() => {
      state.setPointerCommitNonce((n) => n + 1);
    }, [state]);
    const aiPanel = useAiPanelController({
      activeSlideIndex,
      selectedElementId,
      selectedElementIds,
      selectedElement
    });
    const aiBridge = useAiBridge({
      slides,
      activeSlideIndex,
      canvasSize,
      theme: state.theme,
      fileName,
      selectedElementId,
      selectedElementIds,
      pinnedFocus: aiPanel.pinnedFocus,
      pickedFocus: aiPanel.pickTargets.length > 0 ? aiPanel.pickTargets : null,
      handlerRef,
      sections: state.sections,
      presentationProperties: state.presentationProperties,
      customProperties: state.customProperties,
      coreProperties: state.coreProperties,
      appProperties: state.appProperties,
      viewProperties: state.viewProperties,
      tableStyleMap: state.tableStyleMap,
      tableStylesDefaultId: state.tableStylesDefaultId,
      tagCollections: state.tagCollections,
      setCanvasSize: state.setCanvasSize,
      setSections: state.setSections,
      setPresentationProperties: state.setPresentationProperties,
      setCustomProperties: state.setCustomProperties,
      setCoreProperties: state.setCoreProperties,
      setAppProperties: state.setAppProperties,
      setViewProperties: state.setViewProperties,
      setTableStyleMap: state.setTableStyleMap,
      setTableStylesDefaultId: state.setTableStylesDefaultId,
      setTagCollections: state.setTagCollections,
      setSlides: state.setSlides,
      setActiveSlideIndex: state.setActiveSlideIndex,
      applySelection: editorOps.ops.applySelection,
      bumpHistory: bumpAiHistory,
      markDirty: history.markDirty,
      applyThemeUpdates: applyAiTheme
    });
    const showSlidesPane = mode === "edit" && !isMobile && !dialogs.isNarrowViewport && state.isSlidesPaneOpen;
    const showMasterPane = mode === "master" && !isMobile && state.isSlidesPaneOpen;
    const addinStatus = {
      smartArt3d: smartArt3D && !viewerOptions.advanced.disable3DRendering,
      collaboration: Boolean(collaboration),
      locales: resolvedLocales.length > 1
    };
    const viewerContent = /* @__PURE__ */ jsxs(
      "div",
      {
        style: themeStyle,
        "data-pptx-viewer": "",
        "aria-busy": loading,
        className: cn(
          "h-full w-full bg-background text-foreground relative",
          ...resolveOptionRootClasses(viewerOptions, "pptx")
        ),
        children: [
          /* @__PURE__ */ jsx(
            "div",
            {
              ref: containerRef,
              tabIndex: 0,
              className: "h-full w-full flex flex-col overflow-hidden outline-none",
              children: loading ? /* @__PURE__ */ jsx(LoadingState, {}) : error ? /* @__PURE__ */ jsx(ErrorState, { error }) : /* @__PURE__ */ jsx(ThemeColorMapProvider, { value: themeColorMapValue, children: /* @__PURE__ */ jsxs(RecentColorsProvider, { value: recentColorsValue, children: [
                mode !== "present" && /* @__PURE__ */ jsx(
                  ViewerToolbarSection,
                  {
                    mode,
                    canEdit,
                    state,
                    selectedElement,
                    activeSlide,
                    activeSlideIndex,
                    zoom,
                    history,
                    findReplace: editorOps.findReplace,
                    manipulation: editorOps.manipulation,
                    insertHandlers: editorOps.insertHandlers,
                    exportHandlers,
                    printHandlers,
                    propertyHandlers,
                    dialogs,
                    slideOps: editorOps.slideOps,
                    sectionOps: editorOps.sectionOps,
                    onApplyLayout: (path) => void layoutSwitching.applyLayout(path),
                    loadLayoutPreviews: layoutSwitching.loadLayoutPreviews,
                    customFontFamilies,
                    ops: editorOps.ops,
                    onSetMode: handleSetMode,
                    onPresentFromBeginning: presentation.enterPresentModeFromBeginning,
                    onEnterPresenterView: handleEnterPresenterView,
                    onEnterRehearsalMode: handleEnterRehearsalMode,
                    onOpenSettings: () => setIsSettingsOpen(true),
                    onOpenHeaderFooter: () => setIsHeaderFooterOpen(true),
                    onOpenShareDialog: () => setIsShareDialogOpen(true),
                    onOpenFile: handleOpenFile,
                    onOpenRecentFile: handleOpenRecentFile,
                    fileName,
                    autosaveStatus,
                    autosaveEnabled: autosaveActivation.active,
                    onToggleAutosave: () => {
                      if (autosaveActivation.toggleAvailable) {
                        setAutosaveEnabled((p) => !p);
                      }
                    },
                    hiddenActions,
                    recentPresentationsCount: viewerOptions.advanced.recentPresentationsCount,
                    aiEnabled: Boolean(ai),
                    isAiPanelOpen: aiPanel.isOpen,
                    onToggleAiPanel: aiPanel.toggle,
                    isProtectedView,
                    onEnableEditing: hostCanEdit ? handleEnableEditing : void 0,
                    onSetSnapToGrid: viewPreferencesSync.handleSetSnapToGrid,
                    onSetSnapToShape: viewPreferencesSync.handleSetSnapToShape,
                    onSetShowGuides: viewPreferencesSync.handleSetShowGuides
                  }
                ),
                mode !== "present" && readOnlyRec.bannerVisible && /* @__PURE__ */ jsx(
                  ReadOnlyBanner,
                  {
                    recommendation: readOnlyRec.recommendation,
                    onEditAnyway: readOnlyRec.editAnyway,
                    onDismiss: readOnlyRec.dismiss,
                    passwordPromptOpen: readOnlyRec.passwordPromptOpen,
                    passwordError: readOnlyRec.passwordError,
                    checkingPassword: readOnlyRec.checkingPassword,
                    onSubmitPassword: readOnlyRec.submitPassword,
                    onCancelPassword: readOnlyRec.cancelPasswordPrompt
                  }
                ),
                /* @__PURE__ */ jsx(
                  ViewerMainContent,
                  {
                    mode,
                    canEdit,
                    slides,
                    activeSlide,
                    masterPseudoSlide,
                    activeSlideIndex,
                    canvasSize,
                    gridSpacingPx,
                    slideSectionGroups,
                    showSlidesPane,
                    showMasterPane,
                    selectedElement,
                    state,
                    editorOps,
                    dialogs,
                    presentation,
                    annotations,
                    propertyHandlers,
                    themeHandlers,
                    history,
                    comments: editorOps.comments,
                    masterViewCrud,
                    zoom,
                    isMobile,
                    isTouchDevice,
                    onEndPresentation: () => handleSetMode("edit"),
                    leftPanelWidth: isMobile ? void 0 : resizablePanels.leftWidth,
                    onResizeLeft: isMobile ? void 0 : resizablePanels.onResizeLeft,
                    rightPanelWidth: isMobile ? void 0 : resizablePanels.rightWidth,
                    onResizeRight: isMobile ? void 0 : resizablePanels.onResizeRight,
                    hiddenActions,
                    aiConfig: ai,
                    aiBridge: ai ? aiBridge : void 0,
                    aiPanel: ai ? aiPanel : void 0
                  }
                ),
                mode !== "present" && (!isVirtualKeyboardOpen || !state.isSlideNotesCollapsed) && /* @__PURE__ */ jsx(
                  ViewerBottomPanels,
                  {
                    activeSlide,
                    allSlides: slides,
                    isSlideNotesCollapsed: state.isSlideNotesCollapsed,
                    canEdit,
                    slideCount: slides.length,
                    activeSlideIndex,
                    isDirty: state.isDirty,
                    autosaveStatus,
                    onToggleNotes: () => state.setIsSlideNotesCollapsed((p) => !p),
                    onUpdateNotes: propertyHandlers.handleUpdateNotes,
                    collaborationSlot: collaboration ? /* @__PURE__ */ jsx(CollaborationStatusStrip, {}) : void 0,
                    notesPanelHeight: isMobile ? void 0 : resizablePanels.bottomHeight,
                    onResizeBottom: isMobile ? void 0 : resizablePanels.onResizeBottom,
                    scale: zoom.scale,
                    onZoomIn: zoom.handleZoomIn,
                    onZoomOut: zoom.handleZoomOut,
                    onZoomToFit: zoom.handleZoomToFit,
                    mode,
                    onSetMode: handleSetMode,
                    onToggleSlideSorter: () => state.setShowSlideSorter((p) => !p),
                    hideStatusBar: isMobile,
                    hiddenActions,
                    notesStyle: state.notesMaster?.notesStyle
                  }
                ),
                mode !== "present" && isMobile && /* @__PURE__ */ jsx(
                  MobileChromeOverlay,
                  {
                    state,
                    editorOps,
                    presentation,
                    slides,
                    activeSlideIndex,
                    canvasSize,
                    slideSectionGroups,
                    canEdit,
                    commentCount: activeSlide?.comments?.length ?? 0
                  }
                )
              ] }) })
            }
          ),
          mode !== "present" && /* @__PURE__ */ jsx(
            CompatibilityToasts,
            {
              toasts: compatToastsState.toasts,
              onDismiss: compatToastsState.dismiss,
              onDismissAll: compatToastsState.dismissAll
            }
          ),
          /* @__PURE__ */ jsx(
            ViewerDialogGroup,
            {
              dialogs,
              insertHandlers: editorOps.insertHandlers,
              exportHandlers,
              printHandlers,
              propertyHandlers,
              annotations,
              slides,
              activeSlideIndex,
              canvasSize,
              filePath,
              coreProperties: state.coreProperties,
              customProperties: state.customProperties,
              appProperties: state.appProperties,
              embeddedFonts: state.embeddedFonts,
              hasDigitalSignatures: state.hasDigitalSignatures,
              digitalSignatureCount: state.digitalSignatureCount,
              presentationProperties: state.presentationProperties,
              customShows: state.customShows,
              selectedElements: state.selectedElements,
              isEncryptedDialogOpen,
              setIsEncryptedDialogOpen,
              showKeepAnnotationsDialog,
              onKeepAnnotations: handleKeepAnnotations,
              onDiscardAnnotations: handleDiscardAnnotations
            }
          ),
          /* @__PURE__ */ jsx(
            AutosaveRecoveryDialog,
            {
              prompt: shouldShowAutosaveRecoveryPrompt({
                prompt: recovery.prompt,
                presenting: mode === "present"
              }) ? recovery.prompt : null,
              onRestore: recovery.restore,
              onDiscard: recovery.discard
            }
          ),
          /* @__PURE__ */ jsx(
            SettingsDialog,
            {
              isOpen: isSettingsOpen,
              onClose: () => setIsSettingsOpen(false),
              options: viewerOptions,
              onOptionChange: (group, key, value) => optionsStore.setValue(group, key, value),
              onRestoreOptions: (snapshot) => optionsStore.setOptions(snapshot),
              onRibbonTabHiddenChange: (tabId, hidden) => optionsStore.setRibbonTabHidden(tabId, hidden),
              onQuickAccessCommandsChange: (commandIds) => optionsStore.setQuickAccessCommands(commandIds),
              onResetOptions: (group) => optionsStore.reset(group),
              onClearCache: handleClearOptionsCache,
              addinStatus,
              themeKey,
              availableThemes: themeCatalog,
              onSelectTheme: handleThemeChange,
              localeCode,
              availableLocales: resolvedLocales,
              onSelectLocale: handleLocaleChange,
              aiEnabled: Boolean(ai),
              customFontFamilies,
              onCustomFontRegistered: handleCustomFontRegistered
            }
          ),
          isHeaderFooterOpen && /* @__PURE__ */ jsx(
            HeaderFooterPanel,
            {
              headerFooter: state.headerFooter,
              onUpdate: (patch) => state.setHeaderFooter((current) => ({ ...current, ...patch })),
              onApplyToAll: () => {
                history.markDirty();
                setIsHeaderFooterOpen(false);
              },
              onApplyToCurrent: () => {
                history.markDirty();
                setIsHeaderFooterOpen(false);
              },
              onClose: () => setIsHeaderFooterOpen(false)
            }
          ),
          /* @__PURE__ */ jsx(
            ShareDialog,
            {
              open: isShareDialogOpen,
              onClose: () => setIsShareDialogOpen(false),
              activeCollaboration: collaboration,
              onStartCollaboration,
              onStopCollaboration,
              preconfigured: Boolean(collaboration),
              defaultRoomId: shareDefaults?.roomId,
              defaultUserName: shareDefaults?.userName,
              defaultServerUrl: shareDefaults?.serverUrl
            }
          ),
          /* @__PURE__ */ jsx(
            BroadcastDialog,
            {
              open: dialogs.isBroadcastDialogOpen,
              onClose: () => dialogs.setIsBroadcastDialogOpen(false),
              onStartBroadcast: onStartCollaboration,
              onStopBroadcast: onStopCollaboration,
              onStartPresenting: () => handleSetMode("present"),
              defaultRoomId: shareDefaults?.roomId,
              defaultUserName: shareDefaults?.userName,
              defaultServerUrl: shareDefaults?.serverUrl
            }
          ),
          /* @__PURE__ */ jsx(
            ViewerOverlays,
            {
              isShortcutHelpOpen: state.isShortcutHelpOpen,
              isAccessibilityPanelOpen: state.isAccessibilityPanelOpen,
              showSlideSorter: state.showSlideSorter,
              showReadingView: state.showReadingView,
              showOutlineView: state.showOutlineView,
              templateElements: state.templateElements,
              accessibilityIssues: state.accessibilityIssues,
              slides,
              activeSlideIndex,
              canvasSize,
              canEdit,
              sectionGroups: slideSectionGroups,
              onCloseShortcuts: () => state.setIsShortcutHelpOpen(false),
              onCloseAccessibility: () => state.setIsAccessibilityPanelOpen(false),
              onSelectSlide: (i) => {
                state.setActiveSlideIndex(i);
                state.setShowSlideSorter(false);
              },
              onMoveSlide: editorOps.slideOps.handleMoveSlide,
              onDeleteSlides: editorOps.slideOps.handleDeleteSlides,
              onDuplicateSlides: editorOps.slideOps.handleDuplicateSlides,
              onToggleHideSlides: editorOps.slideOps.handleToggleHideSlides,
              onCloseSorter: () => state.setShowSlideSorter(false),
              onCloseReadingView: (slideIndex) => {
                state.setShowReadingView(false);
                state.setActiveSlideIndex(slideIndex);
              },
              onCloseOutlineView: () => state.setShowOutlineView(false),
              setSlides: state.setSlides,
              setActiveSlideIndex: state.setActiveSlideIndex,
              bumpHistory: () => state.setPointerCommitNonce((n) => n + 1),
              reducedMotion,
              onToggleReducedMotion: toggleReducedMotion
            }
          ),
          /* @__PURE__ */ jsx(
            ViewerPresentationLayer,
            {
              mode,
              slides,
              canvasSize,
              templateElements: state.templateElements,
              presentation,
              activeCustomShow,
              authoredRange,
              onExitPresentation: () => handleSetMode("edit"),
              onUpdateNotes: propertyHandlers.handleUpdateNotes,
              isMobile
            }
          )
        ]
      }
    );
    const effective3D = resolve3DRenderingFlags(
      { smartArt3D, surfaceChart3D, barChart3D, lineChart3D, areaChart3D, pieChart3D },
      viewerOptions
    );
    return (
      // `ViewerOptionsContext` is how every deeply-nested chrome piece (the
      // toolbar, title bar, Quick Access strip, print dialog, canvas
      // hyperlink gate, share dialog) reads the live File > Options
      // snapshot without threading it through each intermediate prop list.
      // Nothing previously provided it, so every one of those consumers was
      // silently reading `DEFAULT_VIEWER_OPTIONS` forever, no matter what
      // the user changed in the Options dialog.
      /* @__PURE__ */ jsx(ViewerOptionsContext.Provider, { value: viewerOptions, children: /* @__PURE__ */ jsx(AccountAuthContext.Provider, { value: accountAuth, children: /* @__PURE__ */ jsx(SmartArt3DContext.Provider, { value: effective3D.smartArt3D, children: /* @__PURE__ */ jsx(SurfaceChart3DContext.Provider, { value: effective3D.surfaceChart3D, children: /* @__PURE__ */ jsx(BarChart3DContext.Provider, { value: effective3D.barChart3D, children: /* @__PURE__ */ jsx(LineChart3DContext.Provider, { value: effective3D.lineChart3D, children: /* @__PURE__ */ jsx(AreaChart3DContext.Provider, { value: effective3D.areaChart3D, children: /* @__PURE__ */ jsx(PieChart3DContext.Provider, { value: effective3D.pieChart3D, children: /* @__PURE__ */ jsx(ViewerThemeProvider, { theme: effectiveTheme, children: /* @__PURE__ */ jsxs(
        CollaborationProvider,
        {
          config: collaboration,
          canvasWidth: canvasSize.width,
          canvasHeight: canvasSize.height,
          children: [
            /* @__PURE__ */ jsx(
              CollaborationDocumentSync,
              {
                slides,
                templateElementsBySlideId,
                setSlides: state.setSlides,
                config: collaboration,
                content,
                loadVersion,
                loadOrigin,
                livePatcher: state.livePatcher,
                deckSaveState: {
                  headerFooter: state.headerFooter,
                  presentationProperties: state.presentationProperties,
                  viewProperties: state.viewProperties,
                  customShows: state.customShows,
                  sections: state.sections,
                  coreProperties: state.coreProperties,
                  appProperties: state.appProperties,
                  customProperties: state.customProperties,
                  tagCollections: state.tagCollections,
                  slideMasters: state.slideMasters,
                  notesMaster: state.notesMaster,
                  handoutMaster: state.handoutMaster,
                  slideSize: resolveSlideSizeSelection({
                    current: state.slideSizeEmu,
                    canvas: canvasSize
                  }).size,
                  tableStyleMap: state.tableStyleMap,
                  tableStylesDefaultId: state.tableStylesDefaultId,
                  tableStylesToDelete: state.tableStylesToDelete,
                  embedFonts: dialogs.embedFontsEnabled
                }
              }
            ),
            /* @__PURE__ */ jsx(
              CollaborationFollowLayer,
              {
                activeSlideIndex,
                setActiveSlideIndex: state.setActiveSlideIndex,
                slideCount: slides.length
              }
            ),
            viewerContent
          ]
        }
      ) }) }) }) }) }) }) }) }) })
    );
  }
);
PowerPointViewer.displayName = "PowerPointViewer";
function CollaborationStatusStrip() {
  const collab = useCollaboration();
  if (!collab) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    CollaborationStatusIndicator,
    {
      status: collab.status,
      connectedCount: collab.connectedCount,
      onRetry: collab.retry
    }
  );
}
function CollaborationDocumentSync({
  slides,
  templateElementsBySlideId,
  setSlides,
  config,
  content,
  loadVersion,
  loadOrigin,
  livePatcher,
  deckSaveState
}) {
  const collab = useCollaboration();
  const contentRef = useRef(content);
  contentRef.current = content;
  const getSourceBytes = useCallback(() => {
    const bytes = contentRef.current;
    if (!bytes) {
      return null;
    }
    return bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes);
  }, []);
  const deckSaveStateRef = useRef(deckSaveState);
  deckSaveStateRef.current = deckSaveState;
  const getSaveOptions = useCallback(() => buildDeckSaveOptions(deckSaveStateRef.current), []);
  useYjsDocumentSync({
    doc: collab?.doc ?? null,
    slides,
    templateElementsBySlideId,
    setSlides,
    isConnected: collab?.status === "connected",
    isSynced: collab?.synced ?? true,
    config,
    getSourceBytes,
    getSaveOptions,
    loadVersion,
    loadOrigin
  });
  useCollaborationLivePatch({
    patcher: livePatcher,
    doc: collab?.doc ?? null,
    isConnected: collab?.status === "connected",
    isSynced: collab?.synced ?? true
  });
  return null;
}
function CollaborationFollowLayer({
  activeSlideIndex,
  setActiveSlideIndex,
  slideCount
}) {
  const collab = useCollaboration();
  const { followedClientId, followUser } = useFollowMode({
    collab,
    activeSlideIndex,
    setActiveSlideIndex,
    slideCount
  });
  useBroadcastFollower({
    collab,
    activeSlideIndex,
    setActiveSlideIndex,
    slideCount,
    paused: followedClientId !== null
  });
  if (!collab) {
    return null;
  }
  return /* @__PURE__ */ jsx("div", { className: "pointer-events-none fixed inset-x-0 top-2 z-[1100] flex justify-center px-2", children: /* @__PURE__ */ jsx("div", { className: "pointer-events-auto", children: /* @__PURE__ */ jsx(
    FollowModeBar,
    {
      presences: collab.remoteUsers,
      followedClientId,
      onFollow: followUser
    }
  ) }) });
}

export { CollaborationProvider, CollaborationStatusIndicator, PowerPointViewer, RemoteUserCursors, SlideCanvas, SlideTemplateGalleryDialog, SlideTemplatePreview, Toolbar, UserAvatarBar, ViewerThemeProvider, useViewerTheme };
