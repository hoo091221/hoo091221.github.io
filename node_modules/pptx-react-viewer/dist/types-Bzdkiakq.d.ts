import { n as LocaleCatalogEntry } from "./index-CGKutKk1.js";
import { ConnectorArrowType, PptxAnimationPreset, PptxData, PptxElement, PptxElementAnimation, PptxHandler, PptxSlide, PptxSlideTransition, PptxTheme, ShapeStyle, StrokeDashType, XmlObject } from "pptx-viewer-core";
import { AccountAuthConfig, CollaborationConfig as CollaborationConfig$1, CollaborationRole, ElementClipboardPayload, PowerPointViewerAPI, ShapeAdjustmentDragState, ShapeAdjustmentHandleDescriptor as ShapeAdjustmentHandleDescriptor$1, THEME_CATALOG, ThemeCatalogEntry, ThemeCatalogEntry as ThemeCatalogEntry$1, ToolbarActionId, ViewerFontSource, ViewerMode as ViewerMode$1, ViewerTheme, ViewerThemeColors, defaultCssVars, defaultRadius, defaultThemeColors, resolveThemeCatalogEntry, themeToCssVars, vermilionDarkColors, vermilionDarkTheme, vermilionLightColors, vermilionLightTheme, vermilionRadius } from "pptx-viewer-shared";
import { ChatTransport, LanguageModel, ToolSet, UIMessage } from "ai";
import "zod";
import "pptx-viewer-mcp";
import React$1 from "react";
import { Doc } from "yjs";
//#region src/viewer/types-core.d.ts
/**
 * Union of all shape preset types that the viewer can insert or render.
 * Maps to OOXML `a:prstGeom` preset values (e.g. "rect", "ellipse").
 * Includes basic shapes, arrows, connectors, and freeform paths.
 */
type SupportedShapeType = 'rect' | 'roundRect' | 'ellipse' | 'triangle' | 'rtTriangle' | 'diamond' | 'cylinder' | 'parallelogram' | 'trapezoid' | 'pentagon' | 'hexagon' | 'octagon' | 'chevron' | 'star5' | 'star6' | 'star8' | 'plus' | 'heart' | 'cloud' | 'sun' | 'moon' | 'pie' | 'plaque' | 'teardrop' | 'line' | 'rtArrow' | 'leftArrow' | 'upArrow' | 'downArrow' | 'connector' | 'freeform';
/**
 * Connector geometry presets corresponding to OOXML `a:prstGeom` connector types.
 * Determines the routing style (straight, bent with N segments, or curved).
 */
type ConnectorGeometryType = 'straightConnector1' | 'bentConnector2' | 'bentConnector3' | 'bentConnector4' | 'bentConnector5' | 'curvedConnector2' | 'curvedConnector3' | 'curvedConnector4' | 'curvedConnector5';
/** The pixel dimensions of the slide canvas (presentation slide size). */
interface CanvasSize {
  /** Width of the canvas in CSS pixels. */
  width: number;
  /** Height of the canvas in CSS pixels. */
  height: number;
}
/**
 * Tracks the state of an ongoing element drag operation.
 * Created on pointer-down and destroyed on pointer-up.
 * During the drag, position changes are applied directly to DOM elements
 * for performance (bypassing React state) and committed to state on completion.
 */
interface DragState {
  /** ID of the primary element being dragged. */
  elementId: string;
  /** Client X coordinate at the start of the drag. */
  startClientX: number;
  /** Client Y coordinate at the start of the drag. */
  startClientY: number;
  /** Original positions of all selected elements, keyed by element ID. */
  startPositionsById: Record<string, {
    x: number;
    y: number;
  }>;
  /** DOM elements cached at drag-start to avoid per-frame querySelector calls. */
  domEls: Map<string, HTMLElement>;
  /** Whether the pointer has actually moved since the drag started. */
  moved: boolean;
  /** Accumulated delta, stored during DOM-only drag, committed on pointerup. */
  lastDx: number;
  lastDy: number;
}
/** Handle used as the resize anchor point (corners + edge midpoints). */
type ResizeHandle = 'nw' | 'ne' | 'sw' | 'se' | 'n' | 's' | 'e' | 'w';
/**
 * Tracks the state of an ongoing element resize operation.
 * Uses direct DOM manipulation during the resize for performance,
 * then commits the final geometry to React state on pointer-up.
 */
interface ResizeState {
  /** ID of the element being resized. */
  elementId: string;
  /** Client X coordinate at the start of the resize. */
  startClientX: number;
  /** Client Y coordinate at the start of the resize. */
  startClientY: number;
  /** Original X position of the element. */
  startX: number;
  /** Original Y position of the element. */
  startY: number;
  /** Original width of the element. */
  startWidth: number;
  /** Original height of the element. */
  startHeight: number;
  /** Which handle initiated the resize. */
  handle: ResizeHandle;
  /** Whether the pointer has actually moved since the resize started. */
  moved: boolean;
  /** DOM element cached at resize-start to avoid per-frame querySelector calls. */
  domEl: HTMLElement | null;
  /** Accumulated final geometry, committed on pointerup. */
  lastX: number;
  lastY: number;
  lastWidth: number;
  lastHeight: number;
}
/**
 * A snapshot of editor state captured for undo/redo history.
 * Contains the full slide deck state at a point in time so that
 * any editing operation can be reverted or replayed.
 */
interface EditorHistorySnapshot {
  width: number;
  height: number;
  activeSlideIndex: number;
  slides: PptxSlide[];
  templateElementsBySlideId: Record<string, PptxElement[]>;
  actionLabel?: string;
}
/** Axis-aligned bounding box for an element, used for hit-testing and alignment. */
interface ElementBounds {
  minX: number;
  minY: number;
  maxX: number;
  maxY: number;
}
/**
 * Geometry data for rendering a connector line as an SVG path.
 * Includes start/end coordinates, the SVG path data string,
 * and optional marker (arrowhead) identifiers.
 */
interface ConnectorPathGeometry {
  startX: number;
  startY: number;
  endX: number;
  endY: number;
  pathData: string;
  d?: string;
  viewBox?: string;
  startMarkerId?: string;
  endMarkerId?: string;
}
/** A named preset shape style used in the Quick Styles gallery. */
interface ShapeQuickStyle {
  name: string;
  style: Partial<ShapeStyle>;
}
//#endregion
//#region src/theme/context.d.ts
interface ViewerThemeProviderProps {
  theme?: ViewerTheme;
  children: React.ReactNode;
}
/**
 * Provides a `ViewerTheme` to all descendant viewer components.
 *
 * Typically you do **not** need to use this directly; passing a `theme`
 * prop to `<PowerPointViewer>` is sufficient. This provider is exposed
 * for advanced use-cases where you want to wrap multiple viewers or
 * share a theme across a wider subtree.
 */
declare function ViewerThemeProvider({ theme, children }: ViewerThemeProviderProps): import("react").JSX.Element;
/**
 * Returns the active `ViewerTheme` (if any) from the nearest
 * `ViewerThemeProvider`.
 */
declare function useViewerTheme(): ViewerTheme | undefined;
//#endregion
//#region ../shared/dist/ai/index.d.ts
//#endregion
//#region src/ai/change-animator.d.ts
/** Host-tunable options for how AI edits are animated on the canvas. */
interface AiChangeAnimationConfig {
  /** Master switch. Default true. */
  enabled?: boolean;
  /** How long the motion + glow plays, in ms. Default 900. */
  durationMs?: number;
  /** Draw the pulsing glow highlight on changed elements. Default true. */
  glow?: boolean;
  /** Glide old->new bounds and cross-fade colours. Default true. */
  tween?: boolean;
  /** Accent colour (any CSS colour) for the glow/ghosts. Default a blue. */
  color?: string;
}
//#endregion
//#region src/ai/config.d.ts
/** The UI message shape exchanged with the assistant. Alias of the SDK type. */
type PptxAiUIMessage = UIMessage;
/**
 * Canonical name of every tool the assistant can call. Document tools mirror the
 * `pptx-viewer-mcp` server exactly (they ARE the same functions, run against the
 * live deck); the viewer-only tools (navigation, deck outline, element/notes
 * readers, table merge) have no MCP counterpart.
 */
type PptxAiToolName = 'get_deck_overview' | 'get_slide' | 'get_element' | 'get_speaker_notes' | 'find_text' | 'get_theme' | 'go_to_slide' | 'select_elements' | 'merge_tables' | 'get_metadata' | 'get_layouts' | 'find_placeholders' | 'get_presentation_properties' | 'run_accessibility_check' | 'convert_to_markdown' | 'export_to_json' | 'add_element' | 'update_element' | 'delete_elements' | 'arrange_elements' | 'clone_element' | 'set_element_animation' | 'group_elements' | 'ungroup_elements' | 'batch_update_elements' | 'update_element_style' | 'replace_geometry' | 'set_element_lock' | 'manage_hyperlinks' | 'replace_text' | 'manage_comments' | 'update_table_cells' | 'manage_table_structure' | 'create_chart' | 'update_chart' | 'add_chart_series' | 'remove_chart_series' | 'update_chart_series_data' | 'manage_smart_art' | 'apply_template' | 'add_slide' | 'duplicate_slide' | 'delete_slides' | 'reorder_slides' | 'update_slide_properties' | 'set_slide_transition' | 'apply_theme_preset' | 'update_theme_colors' | 'update_theme_fonts' | 'set_canvas_size' | 'update_metadata' | 'manage_sections' | 'update_presentation_properties' | 'import_from_json' | 'apply_layout';
type Resolvable<T> = T | (() => T | Promise<T>);
/** How the assistant reaches a language model. */
type PptxAiConnection =
/**
 * Post messages to a host backend route (recommended for production so the
 * provider API key stays server-side). Maps to `DefaultChatTransport`.
 */
{
  kind: 'endpoint';
  api: string;
  headers?: Resolvable<Record<string, string>>;
  body?: Resolvable<Record<string, unknown>>;
  credentials?: RequestCredentials;
  fetch?: typeof globalThis.fetch;
} |
/**
 * Run a language model in-process in the browser (bring-your-own key /
 * local model). Maps to a `ToolLoopAgent` behind a `DirectChatTransport`.
 */
{
  kind: 'model';
  model: LanguageModel;
  system?: string;
  maxSteps?: number;
} |
/** Provide a fully-constructed transport (advanced / testing escape hatch). */
{
  kind: 'transport';
  transport: ChatTransport<PptxAiUIMessage>;
};
/** How writes proposed by the assistant reach the document. */
type PptxAiWritePolicy = 'stage' | 'approve' | 'auto';
/** Which deck context is fed to the model with each turn. */
type PptxAiContextStrategy = 'outline' | 'current-slide' | 'none';
/** Optional per-session history persistence hooks. */
interface PptxAiHistoryHooks {
  load?(id: string): Promise<PptxAiUIMessage[]>;
  save?(id: string, messages: PptxAiUIMessage[]): Promise<void>;
}
/** Complete host configuration for an AI chat session. */
interface PptxAiConfig {
  connection: PptxAiConnection;
  /** Extra host instructions appended to the base system prompt. */
  systemPromptExtras?: string;
  tools?: {
    /** Allowlist. When set, only these tools are exposed. */
    enabled?: PptxAiToolName[];
    /** Denylist, applied after `enabled`. */
    disabled?: PptxAiToolName[];
    /** Additional host-defined tools merged into the tool set. */
    extra?: ToolSet;
  };
  /** Default `'stage'`. */
  writePolicy?: PptxAiWritePolicy;
  /** Default `'outline'`. */
  contextStrategy?: PptxAiContextStrategy;
  history?: PptxAiHistoryHooks;
  /**
   * How AI edits are animated on the canvas so the user can watch them land
   * (glide old->new, fade/scale in-out, glow-pulse). Omit for the defaults;
   * set `{ enabled: false }` to turn it off.
   */
  changeAnimation?: AiChangeAnimationConfig;
  onError?(error: Error): void;
}
//#endregion
//#region src/ai/bridge.d.ts
/** Lightweight, model-friendly summary of the whole deck. */
interface PptxAiDeckMeta {
  /** Total number of slides. */
  slideCount: number;
  /** Zero-based index of the currently active slide. */
  activeSlideIndex: number;
  /** Deck title, when known (first slide title / core properties). */
  title?: string;
  /** Slide canvas width in CSS pixels. */
  width: number;
  /** Slide canvas height in CSS pixels. */
  height: number;
}
/** Severity hint for {@link PptxAiBridge.notify}. */
type PptxAiNotifyLevel = 'info' | 'success' | 'warning' | 'error';
/**
 * A target the user has scoped the assistant to: either a whole slide or a
 * single element on a slide. Returned by {@link PptxAiBridge.getFocusedTargets}
 * so the context builder can tell the model exactly what to focus on.
 */
type PptxAiFocusedTarget = {
  kind: 'slide';
  slideIndex: number;
} | {
  kind: 'element';
  slideIndex: number;
  elementId: string;
};
/**
 * A pure updater over the deck's slides. It receives a deep clone of the
 * current slides (mutation-safe) and returns the next slides array. The bridge
 * commits the returned array as ONE history entry.
 */
type PptxAiSlidesUpdater = (slides: PptxSlide[]) => PptxSlide[];
/**
 * A pure updater over the whole parsed deck ({@link PptxData}). Mirrors the
 * `pptx-viewer-mcp` tool model (data in, mutated data out) so presentation-level
 * MCP tools (metadata, sections, canvas size, presentation properties, layouts)
 * can be committed as ONE undoable history entry through {@link
 * PptxAiBridge.applyDeckData}. Optional: bindings that only track slide/theme
 * state can omit it, in which case those presentation-level tools report that
 * they are unavailable in this viewer while every slide/theme tool still works.
 */
type PptxAiDataUpdater = (data: PptxData) => PptxData;
/** Field-level updates for a single element, mirroring the MCP update vocab. */
interface PptxAiElementUpdate {
  x?: number;
  y?: number;
  width?: number;
  height?: number;
  rotation?: number;
  opacity?: number;
  hidden?: boolean;
  flipHorizontal?: boolean;
  flipVertical?: boolean;
  text?: string;
  fontSize?: number;
  fontFamily?: string;
  fontColor?: string;
  bold?: boolean;
  italic?: boolean;
  underline?: boolean;
  align?: 'left' | 'center' | 'right' | 'justify';
  fillColor?: string;
  strokeColor?: string;
  strokeWidth?: number;
}
/**
 * Implemented by each binding to expose its live editor to the AI core.
 *
 * Read methods must be cheap and synchronous. Write methods must route through
 * the binding's editor-history layer so AI edits are undoable like manual ones.
 */
interface PptxAiBridge {
  /** Return a summary of the whole deck. */
  getDeckMeta(): PptxAiDeckMeta;
  /** Return the deck's slides. Callers must not mutate the returned array. */
  getSlides(): PptxSlide[];
  /** Return the zero-based index of the active slide. */
  getActiveSlideIndex(): number;
  /** Return the resolved presentation theme, when available. */
  getTheme(): PptxTheme | undefined;
  /** Return the underlying core handler, when the binding exposes one. */
  getHandler(): PptxHandler | undefined;
  /** Navigate the viewer to a slide by zero-based index. */
  goToSlide(index: number): void;
  /** Select the given elements on a slide (empty array clears selection). */
  selectElements(slideIndex: number, elementIds: string[]): void;
  /**
   * Apply a slides updater as a single, atomic, undoable history entry. The
   * binding is responsible for cloning current slides before calling
   * `updater` and for installing the result.
   */
  applySlidesUpdate(updater: PptxAiSlidesUpdater, label: string): void;
  /** Apply field updates to one element as a single history entry. */
  updateElement(slideIndex: number, elementId: string, updates: PptxAiElementUpdate): void;
  /** Apply partial theme updates as a single history entry. */
  applyTheme(updates: Partial<PptxTheme>): void;
  /**
   * Return the full parsed {@link PptxData} for the open deck, with the live
   * (edited) slides and theme overlaid. Enables `pptx-viewer-mcp` tools that
   * read presentation-level state (metadata, sections, layouts, presentation
   * properties). Optional: when absent, the AI core synthesises a minimal
   * PptxData from slides + dimensions, which is enough for every slide/theme
   * tool but not for presentation-level reads.
   */
  getDeckData?(): PptxData | undefined;
  /**
   * Commit a whole-deck {@link PptxData} mutation as one undoable history entry.
   * Used to apply presentation-level MCP tool results (metadata, sections,
   * canvas size, presentation properties, layouts). Optional: when absent,
   * those tools report they are not supported in this viewer; slide/theme tools
   * are unaffected (they route through {@link PptxAiBridge.applySlidesUpdate}
   * and {@link PptxAiBridge.applyTheme}).
   */
  applyDeckData?(updater: PptxAiDataUpdater, label: string): void;
  /**
   * Return the slides / elements the user has scoped the assistant to, if any.
   * When present and non-empty, the context builder tells the model to focus on
   * exactly these targets. Optional so existing bridges satisfy the contract
   * without change; a bridge that does not implement it behaves as before (no
   * focus scoping).
   */
  getFocusedTargets?(): PptxAiFocusedTarget[];
  /** Surface a transient message in the host UI (toast / status line). */
  notify?(message: string, level?: PptxAiNotifyLevel): void;
}
//#endregion
//#region src/viewer/hooks/collaboration/types.d.ts
/** Connection lifecycle states for the Yjs WebSocket provider. */
type ConnectionStatus = 'disconnected' | 'connecting' | 'connected' | 'error';
/**
 * Presence data broadcast to other participants.
 * Cursor position is relative to the slide canvas (0..canvasWidth, 0..canvasHeight).
 */
interface UserPresence {
  /** Unique client ID (assigned by Yjs awareness). */
  clientId: number;
  /** Sanitised display name. */
  userName: string;
  /** Optional avatar URL (validated). */
  userAvatar?: string;
  /** Hex colour for the user's cursor ring. */
  userColor: string;
  /** Slide index the user is currently viewing. */
  activeSlideIndex: number;
  /** Cursor X on the canvas (clamped to slide bounds). */
  cursorX: number;
  /** Cursor Y on the canvas (clamped to slide bounds). */
  cursorY: number;
  /** ISO timestamp of last update (for stale-presence cleanup). */
  lastUpdated: string;
  /** Optional currently selected element ID. */
  selectedElementId?: string;
  /** Role in the session (owner, viewer, or collaborator). */
  role?: CollaborationRole;
}
/** Value exposed by `CollaborationContext`. */
interface CollaborationContextValue {
  /** Current WebSocket connection status. */
  status: ConnectionStatus;
  /** Presence data for all remote users (excludes the local user). */
  remoteUsers: UserPresence[];
  /** Broadcast the local user's presence state. */
  broadcastPresence: (update: Partial<Omit<UserPresence, 'clientId'>>) => void;
  /** Total number of connected users (including local). */
  connectedCount: number;
  /** The collaboration config that was provided. */
  config: CollaborationConfig$1;
  /** The Yjs document (for document sync). */
  doc: Doc | null;
  /**
   * Whether the provider completed its initial document sync (or the
   * first-write grace period elapsed). Local doc writes are gated on this.
   */
  synced: boolean;
  /** Manually retry the WebSocket connection after a timeout or error. */
  retry: () => void;
}
//#endregion
//#region src/viewer/types-ui.d.ts
/**
 * Base handle interface for file viewer components.
 * Defined locally to avoid dependency on external file-viewer plugin packages.
 * Provides a standard `getContent` method used by the host application to
 * retrieve the current file content (e.g. for saving).
 */
interface FileViewerHandle {
  /** Get the current content of the file (for saving) */
  getContent: () => Promise<string | Uint8Array>;
}
/** A shape preset entry used in the toolbar shape insertion palette. */
interface ShapePreset {
  type: SupportedShapeType;
  label: string;
  icon: React$1.ReactNode;
}
/** Tracks the position and target element of an open context menu. */
interface ElementContextMenuState {
  x: number;
  y: number;
  elementId: string;
}
/** Identifies an action triggered from the element right-click context menu. */
type ElementContextMenuAction = 'copy' | 'cut' | 'paste' | 'duplicate' | 'delete' | 'bring-forward' | 'send-backward' | 'bring-front' | 'send-back' | 'bringForward' | 'sendBackward' | 'bringToFront' | 'sendToBack' | 'comment' | 'addComment' | 'group' | 'ungroup' | 'editPoints' | 'editHyperlink';
/**
 * State of an active marquee (rubber-band) selection rectangle.
 * Created when the user clicks and drags on the canvas background,
 * and used to compute which elements fall within the selection area.
 */
interface MarqueeSelectionState {
  startX: number;
  startY: number;
  currentX: number;
  currentY: number;
  additive: boolean;
  baseSelectionIds?: string[];
}
/** Tracks which table cell is selected and/or actively being edited. */
interface TableCellEditorState {
  rowIndex: number;
  columnIndex: number;
  /** When true the cell has an active text input. */
  isEditing?: boolean;
  /** Optional multi-cell selection (Shift+Click range). Each entry is {row, col}. */
  selectedCells?: Array<{
    row: number;
    col: number;
  }>;
}
/** A single parsed table cell with its position, text content, and computed CSS style. */
interface ParsedTableCell {
  rowIndex: number;
  columnIndex: number;
  text: string;
  style: React$1.CSSProperties;
  rawCell: XmlObject;
}
/** Complete parsed table data including row/column structure and all cells. */
interface ParsedTableData {
  rowCount: number;
  columnCount: number;
  rows: XmlObject[];
  columnPercentages: number[];
  cells: ParsedTableCell[];
}
/** Runtime state for a single element's animation during presentation mode. */
interface PresentationAnimationRuntime {
  elementId: string;
  state: 'hidden' | 'entering' | 'visible';
  animation: PptxElementAnimation;
}
/**
 * Groups slides into named sections for the slides pane sidebar.
 * Corresponds to OOXML `p15:section` elements in `presentation.xml`.
 */
interface SlideSectionGroup {
  id: string;
  label: string;
  slideIndexes: number[];
  /** Section highlight color from p15:sectionPr. */
  color?: string;
  /** Whether the section should start collapsed (from p15:sectionPr). */
  defaultCollapsed?: boolean;
}
/** Alignment direction for distributing/aligning multiple selected elements on the slide. */
type SlideAlignment = 'left' | 'center' | 'right' | 'top' | 'middle' | 'bottom';
/** Identifies one of the ribbon-style toolbar tabs (home, insert, text, etc.). */
type ToolbarSection = 'file' | 'home' | 'insert' | 'text' | 'arrange' | 'draw' | 'design' | 'transitions' | 'animations' | 'slideShow' | 'record' | 'review' | 'view' | 'help';
/** The active drawing/inking tool selected in the Draw toolbar tab. */
type DrawingTool = 'select' | 'pen' | 'highlighter' | 'eraser' | 'freeform';
/** A single entry in the keyboard shortcuts help panel. */
interface ShortcutReferenceItem {
  actionKey: string;
  shortcut: string;
}
/** An accessibility audit finding (missing alt text, reading order issues, etc.). */
interface AccessibilityIssue {
  slideIndex: number;
  elementId?: string;
  severity: 'error' | 'warning' | 'info';
  message: string;
}
/** Dropdown option for selecting a connector geometry type. */
interface ConnectorGeometryOption {
  value: ConnectorGeometryType;
  label: string;
}
/** Dropdown option for selecting a connector arrowhead style. */
interface ConnectorArrowOption {
  value: ConnectorArrowType;
  label: string;
}
/** Dropdown option for selecting a stroke dash pattern. */
interface StrokeDashOption {
  value: StrokeDashType;
  label: string;
}
/** Dropdown option for selecting a slide transition type. */
interface SlideTransitionOption {
  value: NonNullable<PptxSlideTransition['type']>;
  label: string;
}
/** Dropdown option for selecting an animation effect preset. */
interface AnimationPresetOption {
  value: Exclude<PptxAnimationPreset, 'none'>;
  label: string;
}
interface PowerPointViewerProps {
  /** PowerPoint content as Uint8Array */
  content: Uint8Array;
  /** Licensed fonts supplied by the host application. No fonts are bundled. */
  fonts?: ViewerFontSource[];
  /** Original file path, used for autosave recovery */
  filePath?: string;
  /**
   * Display name of the open document, shown in the PowerPoint-style title
   * bar (e.g. "quarterly-review.pptx"). Falls back to a generic label.
   */
  fileName?: string;
  /** Callback when content has unsaved changes */
  onDirtyChange?: (isDirty: boolean) => void;
  onContentChange?: (content: Uint8Array) => void;
  /** Callback when active slide changes */
  onActiveSlideChange?: (slideIndex: number) => void;
  /** Callback when the viewer mode changes (e.g. edit to present). */
  onModeChange?: (mode: import('pptx-viewer-shared').ViewerMode) => void;
  /** Callback when the zoom level changes. */
  onZoomChange?: (zoom: number) => void;
  /** Callback when element selection changes. */
  onSelectionChange?: (elementIds: string[]) => void;
  /** Callback when the total slide count changes (slide added/deleted). */
  onSlideCountChange?: (count: number) => void;
  /**
   * Host override for the File ▸ Open action. When provided, the built-in
   * native file picker is bypassed and this is invoked instead; the host is
   * then responsible for supplying a new `content` buffer. When omitted, the
   * viewer opens its own picker and loads the chosen presentation in place.
   */
  onOpenFile?: () => void;
  /** Whether editing actions are enabled */
  canEdit?: boolean;
  /**
   * Recovery autosave: after an edit the deck is re-serialised (always as a
   * plain, unencrypted package, because recovery has no password) and stashed
   * in the shared IndexedDB store keyed by {@link filePath}. It is a crash-
   * safety net and never replaces the user's real Save: the document stays
   * dirty. On load, a newer snapshot is offered back through a recovery prompt.
   *
   * **The prop is a policy ceiling; the title-bar AutoSave toggle is the user's
   * preference inside it.** `false` turns autosave off and makes the toggle
   * inert (a user cannot switch on what the application forbade). `true` or
   * omitted permits it, and the toggle decides, defaulting to on. Identical in
   * all five bindings; see `resolveAutosaveActivation` in `pptx-viewer-shared`.
   *
   * @default true
   */
  autosave?: boolean;
  /**
   * Recovery cadence in milliseconds. An explicit value is a host policy and is
   * honoured as given; omit it to follow the user's File > Options > Save >
   * "Save AutoRecover information every N minutes" (two minutes by default).
   */
  autosaveIntervalMs?: number;
  /** Optional class name */
  className?: string;
  /**
   * Display name used as the author for comments and annotations.
   * Falls back to `collaboration.userName` when collaborating, or `'You'`.
   */
  authorName?: string;
  /**
   * Theme configuration for customising the viewer's appearance.
   *
   * Accepts partial color overrides, a custom border-radius, and
   * arbitrary CSS custom properties. Unset values fall back to the
   * built-in dark theme.
   *
   * @example
   * ```tsx
   * <PowerPointViewer
   *   content={bytes}
   *   theme={{
   *     colors: { primary: "#6366f1", background: "#0f172a" },
   *     radius: "0.75rem",
   *   }}
   * />
   * ```
   *
   * @see {@link ViewerTheme} for the full type definition.
   */
  theme?: ViewerTheme;
  /**
   * Initial key into `availableThemes` (or the built-in `THEME_CATALOG`) for
   * File > Options > Appearance. Ignored once the user picks a different
   * entry in that session, and always overridden by the `theme` prop when
   * both are supplied. Falls back to a persisted `localStorage` choice, then
   * `'default'`.
   */
  defaultThemeKey?: string;
  /**
   * Theme choices offered by File > Options > Appearance. Defaults to the
   * shared `THEME_CATALOG` (default/light/vermilionLight/vermilionDark).
   */
  availableThemes?: ThemeCatalogEntry[];
  /**
   * Called when the user picks a theme from File > Options > Appearance.
   * When provided, the host owns persisting the choice (the viewer will not
   * also write it to `localStorage`); otherwise the viewer persists it via
   * the shared `writeStoredViewerPrefs` helper.
   */
  onThemeChange?: (key: string) => void;
  /**
   * Initial locale code for File > Options > Language. Falls back to a
   * persisted `localStorage` choice, then `'en'`.
   */
  defaultLocale?: string;
  /**
   * Locale choices offered by File > Options > Language. Defaults to the
   * codes actually registered on the host's `react-i18next` instance
   * (mapped through the shared `LOCALE_CATALOG` for display labels).
   */
  availableLocales?: LocaleCatalogEntry[];
  /**
   * Called when the user picks a language from File > Options > Language.
   * When provided, the host owns applying and persisting the choice (the
   * viewer will not call `i18n.changeLanguage` or touch `localStorage`
   * itself); otherwise the viewer applies and persists it directly.
   */
  onLocaleChange?: (code: string) => void;
  /**
   * Optional hook point for File > Account's sign-in section. Disabled by
   * default (renders nothing extra); pass `{ enabled: true, onSignIn }` to
   * surface a real sign-in flow.
   */
  accountAuth?: AccountAuthConfig;
  /**
   * Optional real-time collaboration configuration.
   *
   * When provided, the viewer enables collaborative editing with live
   * cursors, user presence indicators, and CRDT-based state sync via Yjs.
   * Requires `yjs` and `y-websocket` peer dependencies.
   *
   * @example
   * ```tsx
   * <PowerPointViewer
   *   content={bytes}
   *   collaboration={{
   *     roomId: "my-room-123",
   *     serverUrl: "wss://collab.example.com",
   *     userName: "Alice",
   *     userColor: "#6366f1",
   *   }}
   * />
   * ```
   */
  collaboration?: CollaborationConfig$1;
  /**
   * Callback invoked when the user starts a collaboration session from the
   * Share dialog. The host app should use this to set the `collaboration`
   * prop with the returned config.
   */
  onStartCollaboration?: (config: CollaborationConfig$1) => void;
  /**
   * Callback invoked when the user stops a collaboration session from the
   * Share dialog. The host app should clear the `collaboration` prop.
   */
  onStopCollaboration?: () => void;
  /**
   * Default values for the Share dialog fields. The host app should provide
   * these to control the session name, user display name, and server URL.
   * If omitted, the Share dialog fields will be empty and require user input.
   *
   * @example
   * ```tsx
   * <PowerPointViewer
   *   shareDefaults={{
   *     roomId: "session-abc123",
   *     userName: "Alice",
   *     serverUrl: "ws://localhost:1234",
   *   }}
   * />
   * ```
   */
  shareDefaults?: {
    roomId?: string;
    userName?: string;
    serverUrl?: string;
  };
  /**
   * Opt in to the Three.js SmartArt renderer. When `true`, SmartArt diagrams
   * render as extruded 3D blocks on a WebGL canvas instead of flat SVG.
   * Requires the optional `three` peer dependency; when it is not installed
   * (or the diagram has no geometry), the viewer transparently falls back to
   * the SVG `SmartArtRenderer`. Default `false`.
   */
  smartArt3D?: boolean;
  /**
   * Opt in to the interactive Three.js surface-chart renderer. When `true`,
   * `surface`/`surface3D` charts render as a camera-orbitable WebGL mesh
   * (drag to rotate, scroll to zoom) instead of the static SVG isometric
   * projection. Chart marks are not selectable/draggable in this mode.
   * Requires the optional `three` peer dependency; when it is not installed
   * (or the chart has no plottable grid), the viewer transparently falls back
   * to the SVG surface renderer. Default `false`.
   */
  surfaceChart3D?: boolean;
  /**
   * Opt in to the interactive Three.js bar3D-chart renderer. When `true`,
   * `bar3D` charts render as camera-orbitable real box meshes (drag to
   * rotate, scroll to zoom) instead of the flat SVG oblique-projection
   * illusion. Chart marks are not selectable/draggable in this mode.
   * Requires the optional `three` peer dependency; when it is not installed
   * (or the chart has no plottable grid, or it is a horizontal 3-D Bar), the
   * viewer transparently falls back to the flat SVG bar3D renderer. Default
   * `false`.
   */
  barChart3D?: boolean;
  /**
   * Opt in to the interactive Three.js line3D-chart renderer. When `true`,
   * `line3D` charts render as a camera-orbitable real tube-path mesh per
   * series, one per depth ("series") plane (drag to rotate, scroll to zoom),
   * instead of the flat SVG oblique-projection illusion. Chart marks are not
   * selectable/draggable in this mode. Requires the optional `three` peer
   * dependency; when it is not installed (or the chart has no plottable
   * grid), the viewer transparently falls back to the flat SVG line3D
   * renderer. Default `false`.
   */
  lineChart3D?: boolean;
  /**
   * Opt in to the interactive Three.js area3D-chart renderer. When `true`,
   * `area3D` charts render as a camera-orbitable real tube path + filled
   * ribbon mesh per series, one per depth ("series") plane (drag to rotate,
   * scroll to zoom), instead of the flat SVG oblique-projection illusion.
   * Chart marks are not selectable/draggable in this mode. Requires the
   * optional `three` peer dependency; when it is not installed (or the chart
   * has no plottable grid), the viewer transparently falls back to the flat
   * SVG area3D renderer. Default `false`.
   */
  areaChart3D?: boolean;
  /**
   * Opt in to the interactive Three.js pie3D-chart renderer. When `true`,
   * `pie3D` charts render as camera-orbitable real wedge meshes (drag to
   * rotate, scroll to zoom) instead of the flat SVG oblique-projection
   * illusion. Chart marks are not selectable/draggable in this mode.
   * Requires the optional `three` peer dependency; when it is not installed
   * (or the chart has no plottable series), the viewer transparently falls
   * back to the flat SVG pie3D renderer. Default `false`.
   */
  pieChart3D?: boolean;
  /**
   * Hide individual toolbar buttons and/or ribbon tabs instead of the whole
   * toolbar. Accepts any mix of button ids (`share`, `broadcast`, `export`,
   * `undo`, `redo`, `record`, `notes`, `fullscreen`, `zoom`, `navigation`)
   * and ribbon-tab ids (`file`, `home`, `insert`, `draw`, `design`,
   * `transitions`, `animations`, `slideShow`, `record`, `review`, `view`,
   * `help`). `zoom` and `navigation` each hide their whole control cluster
   * (zoom in/out/fit; previous/next slide), not each sub-button. `record`
   * is shared between the quick-access Record button and the Record ribbon
   * tab: hiding it hides both. Omitted or empty hides nothing (default,
   * fully backward compatible).
   *
   * @example
   * ```tsx
   * <PowerPointViewer content={bytes} hiddenActions={['share', 'broadcast', 'record']} />
   * ```
   */
  hiddenActions?: ToolbarActionId[];
  /**
   * Opt in to the built-in AI assistant. When provided, a Sparkles toggle
   * appears in the toolbar and opens a chat panel wired to this config; when
   * omitted, no AI icon renders and no AI code is loaded (the panel and its
   * `@ai-sdk/react` dependency are `React.lazy`-imported only on first open).
   *
   * The host supplies the model connection (a backend `endpoint`, an in-browser
   * `model`, or a custom `transport`) plus optional tool allow/deny lists and a
   * write policy. Requires the optional `ai` and `@ai-sdk/react` peers.
   *
   * @example
   * ```tsx
   * <PowerPointViewer content={bytes} ai={{ connection: { kind: 'model', model } }} />
   * ```
   *
   * @see {@link PptxAiConfig}
   */
  ai?: PptxAiConfig;
}
interface PowerPointViewerHandle extends FileViewerHandle, PowerPointViewerAPI {
  getContent: () => Promise<Uint8Array>;
}
//#endregion
export { DragState as $, PptxAiConnection as A, themeToCssVars as B, CollaborationConfig$1 as C, UserPresence as D, ConnectionStatus as E, useViewerTheme as F, vermilionRadius as G, vermilionDarkTheme as H, THEME_CATALOG as I, ViewerTheme as J, defaultRadius as K, ThemeCatalogEntry$1 as L, PptxAiToolName as M, PptxAiWritePolicy as N, PptxAiBridge as O, ViewerThemeProvider as P, ConnectorPathGeometry as Q, resolveThemeCatalogEntry as R, ToolbarSection as S, CollaborationRole as T, vermilionLightColors as U, vermilionDarkColors as V, vermilionLightTheme as W, CanvasSize as X, ViewerThemeColors as Y, ConnectorGeometryType as Z, SlideAlignment as _, DrawingTool as a, ShapeAdjustmentDragState as at, StrokeDashOption as b, FileViewerHandle as c, SupportedShapeType as ct, ParsedTableData as d, EditorHistorySnapshot as et, PowerPointViewerHandle as f, ShortcutReferenceItem as g, ShapePreset as h, ConnectorGeometryOption as i, ResizeState as it, PptxAiContextStrategy as j, PptxAiConfig as k, MarqueeSelectionState as l, ViewerMode$1 as lt, PresentationAnimationRuntime as m, AnimationPresetOption as n, ElementClipboardPayload as nt, ElementContextMenuAction as o, ShapeAdjustmentHandleDescriptor$1 as ot, PowerPointViewerProps as p, defaultThemeColors as q, ConnectorArrowOption as r, ResizeHandle as rt, ElementContextMenuState as s, ShapeQuickStyle as st, AccessibilityIssue as t, ElementBounds as tt, ParsedTableCell as u, SlideSectionGroup as v, CollaborationContextValue as w, TableCellEditorState as x, SlideTransitionOption as y, defaultCssVars as z };
//# sourceMappingURL=types-Bzdkiakq.d.ts.map