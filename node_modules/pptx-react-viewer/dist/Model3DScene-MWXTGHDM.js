'use strict';

var chunkG3JPCRKN_js = require('./chunk-G3JPCRKN.js');
require('./chunk-4VNS5WPM.js');
var react = require('react');
var jsxRuntime = require('react/jsx-runtime');

function Model3DScene({ modelUrl, interactive, width, height }) {
  const containerRef = react.useRef(null);
  const handleRef = react.useRef(null);
  react.useEffect(() => {
    const container = containerRef.current;
    if (!container) {
      return;
    }
    let disposed = false;
    void chunkG3JPCRKN_js.mountModel3D(container, modelUrl, { width, height, interactive }).then((handle) => {
      if (disposed) {
        handle.dispose();
      } else {
        handleRef.current = handle;
      }
      return void 0;
    });
    return () => {
      disposed = true;
      handleRef.current?.dispose();
      handleRef.current = null;
    };
  }, [modelUrl]);
  react.useEffect(() => {
    handleRef.current?.setInteractive(interactive);
  }, [interactive]);
  react.useEffect(() => {
    handleRef.current?.resize(width, height);
  }, [width, height]);
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      ref: containerRef,
      style: {
        width,
        height,
        willChange: "transform"
      }
    }
  );
}

module.exports = Model3DScene;
