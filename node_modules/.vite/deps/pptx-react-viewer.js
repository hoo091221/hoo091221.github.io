import {
  ANIMATION_PRESET_OPTIONS,
  ARROW_SIZE_OPTIONS,
  CONNECTOR_ARROW_OPTIONS,
  DEFAULT_CANVAS_HEIGHT,
  DEFAULT_CANVAS_WIDTH,
  DEFAULT_FILL_COLOR,
  DEFAULT_FONT_FAMILY2,
  DEFAULT_SECTION_GROUP_ID,
  DEFAULT_STROKE_COLOR,
  DEFAULT_TABLE_COLUMNS,
  DEFAULT_TABLE_ROWS,
  DEFAULT_TEXT_COLOR,
  DEFAULT_TEXT_FONT_SIZE,
  GRID_SIZE,
  HYPERLINK_COLOR,
  LOCALE_CATALOG,
  LuAArrowDown,
  LuAArrowUp,
  LuAlignCenter,
  LuAlignHorizontalSpaceAround,
  LuAlignJustify,
  LuAlignLeft,
  LuAlignRight,
  LuAlignVerticalSpaceAround,
  LuArrowDown,
  LuArrowLeft,
  LuArrowLeftRight,
  LuArrowRight,
  LuArrowUp,
  LuBold,
  LuBookOpen,
  LuBookmark,
  LuBox,
  LuBug,
  LuCalendarDays,
  LuCamera,
  LuCaptions,
  LuCaseSensitive,
  LuCast,
  LuCheck,
  LuChevronDown,
  LuChevronLeft,
  LuChevronRight,
  LuChevronUp,
  LuCircleHelp,
  LuCirclePause,
  LuCirclePlay,
  LuClipboardCopy,
  LuClipboardPaste,
  LuClock,
  LuClock3,
  LuCode,
  LuColumns2,
  LuColumns3,
  LuCopy,
  LuCopyPlus,
  LuDatabase,
  LuDownload,
  LuEllipsis,
  LuEraser,
  LuEye,
  LuEyeOff,
  LuFile,
  LuFileDown,
  LuFileImage,
  LuFileJson,
  LuFilePlus2,
  LuFileText,
  LuFolderOpen,
  LuFolderPlus,
  LuGitCompare,
  LuGlobe,
  LuGrid2X2,
  LuGrid3X3,
  LuGripVertical,
  LuGroup,
  LuHardDrive,
  LuHash,
  LuHighlighter,
  LuHistory,
  LuHouse,
  LuImage,
  LuIndentDecrease,
  LuIndentIncrease,
  LuInfo,
  LuItalic,
  LuLanguages,
  LuLayers,
  LuLayoutGrid,
  LuLayoutTemplate,
  LuLink,
  LuList,
  LuListOrdered,
  LuListVideo,
  LuLoader,
  LuLoaderCircle,
  LuLock,
  LuLockKeyhole,
  LuLockOpen,
  LuLogIn,
  LuMaximize2,
  LuMenu,
  LuMessageSquare,
  LuMessageSquarePlus,
  LuMinus,
  LuMonitor,
  LuMonitorOff,
  LuMonitorPlay,
  LuMousePointer2,
  LuMousePointerClick,
  LuMove,
  LuMoveRight,
  LuPaintBucket,
  LuPaintbrush,
  LuPalette,
  LuPanelLeft,
  LuPanelLeftClose,
  LuPanelRight,
  LuPanelTop,
  LuPause,
  LuPenLine,
  LuPenTool,
  LuPencil,
  LuPipette,
  LuPlay,
  LuPlus,
  LuPresentation,
  LuPrinter,
  LuRadio,
  LuRedo,
  LuRemoveFormatting,
  LuReplace,
  LuReply,
  LuRotateCcw,
  LuRotateCw,
  LuRuler,
  LuSave,
  LuScan,
  LuScissors,
  LuSearch,
  LuSettings,
  LuSettings2,
  LuShapes,
  LuShare2,
  LuShieldAlert,
  LuShieldCheck,
  LuSignature,
  LuSparkles,
  LuSpellCheck,
  LuSpline,
  LuSquare,
  LuStar,
  LuStickyNote,
  LuStrikethrough,
  LuText,
  LuTextCursorInput,
  LuTimer,
  LuTrash2,
  LuTriangleAlert,
  LuType,
  LuUnderline,
  LuUndo,
  LuUngroup,
  LuUpload,
  LuUser,
  LuUserRound,
  LuUsers,
  LuVideo,
  LuWand,
  LuWifi,
  LuWifiOff,
  LuX,
  LuZoomIn,
  LuZoomOut,
  MIN_ELEMENT_SIZE,
  SHAPE_PRESETS,
  SHORTCUT_REFERENCE_ITEMS as SHORTCUT_REFERENCE_ITEMS2,
  SLIDE_NAV_THUMBNAIL_WIDTH,
  STROKE_DASH_OPTIONS2,
  TOOLBAR_SECTIONS,
  UNGROUPED_SECTION_ID,
  ZOOM_TO_SELECTION_PADDING,
  aiChangeAnimationCss,
  applyElementUpdate,
  build3DExtrusionData2,
  buildLineGlowFilter,
  buildLineShadowCss,
  buildNotesPdf,
  buildPdfFromImageData,
  buildReactChartViewModel,
  buildTextBody3DSceneStyle2,
  changeGhostStyle,
  cloneHistorySnapshot2,
  cn,
  collectStoredChats,
  computeFocusTargets,
  createChatHistoryStore,
  deckDataFieldChanged,
  exportAiChatLogs,
  getAnimationInitialStyle2,
  getConnectorPathGeometry,
  getCropShapeClipPath2,
  getDuotoneColors,
  getImageRenderStyle,
  getImageSurfaceMaskStyle,
  getImageSurfaceStyle,
  getImageTilingStyle2,
  getShapeVisualStyle,
  getTextLayoutStyle,
  getTextStyleForElement,
  getTextWarpStyle,
  hasDagDuotoneEffect,
  isConnectorOrLineElement,
  renderChartElement,
  renderConnectorMarker,
  renderDagDuotoneSvgFilter,
  renderMediaElement,
  renderTableElement,
  renderTextSegments,
  renderVectorShape,
  shouldRenderFallbackLabel,
  startMotionPathPreview,
  startPreviewAnimation,
  stopPreviewAnimation,
  translationsEn
} from "./chunk-PTYSKNLV.js";
import {
  ACTION_BUTTON_PRESETS,
  ACTION_INDICATOR_CLASS,
  ACTION_INDICATOR_ICON_PATH,
  ARTISTIC_EFFECTS,
  AVATAR_COLOR_SWATCHES,
  BACKSTAGE_NAV,
  BACKSTAGE_TEMPLATES,
  BAR3D_SHAPE_OPTIONS,
  BEVEL_PRESETS,
  BROADCAST_THROTTLE_MS,
  CATEGORIES,
  CHARACTER_SPACING_OPTIONS,
  CHART_TYPE_OPTIONS,
  COLOR_MAP_ALIAS_KEYS,
  COMMENT_MARKER_SIZE,
  COMMENT_MENTION_ATTRIBUTE,
  COMMENT_MENTION_CLASS,
  COMMON_FONTS,
  COMMON_FONT_SIZES,
  COMPOUND_LINE_OPTIONS,
  CONNECTION_TIMEOUT_MS,
  CONNECTOR_SITE_SNAP_PX,
  CUSTOM_FONT_ACCEPT,
  DEBOUNCE_MS,
  DEFAULT_BROADCAST_SERVER_URL,
  DEFAULT_COLOR_CHANGE_TOLERANCE,
  DEFAULT_COLOR_MAP,
  DEFAULT_INSERT_CHART_KIND,
  DEFAULT_MOTION_PATH_PRESET_ID,
  DEFAULT_PALETTE2,
  DEFAULT_QUICK_ACCESS_COMMAND_IDS,
  DEFAULT_VIEWER_OPTIONS,
  DEFAULT_VIEWER_PREFERENCES,
  DEFAULT_VIEWER_PROFILE,
  DEFAULT_VIRTUAL_OVERSCAN,
  DIRECTIONAL_PRESETS,
  DUOTONE_PRESETS,
  ELEMENT_ACTION_TYPE_OPTIONS,
  EMPHASIS_PRESET_VALUES,
  ENTRANCE_PRESET_VALUES,
  EQUATION_TEMPLATES,
  EXIT_PRESET_VALUES,
  EXPANDED_MAX_HEIGHT,
  EXPORT_ASSEMBLING_PERCENT,
  EXPORT_DONE_PERCENT,
  EncryptedFileError,
  FILL_MODE_OPTIONS,
  FILL_PATTERN_LABEL_KEYS,
  GRADIENT_TYPE_OPTIONS,
  HANDOUT_OPTIONS,
  HIDDEN_SLIDE_LABEL_KEY,
  HIDDEN_SLIDE_SLASH_GRADIENT,
  HIGHLIGHTER_COLORS,
  INDENT_PX,
  INK_REPLAY_KEYFRAMES,
  INSERT_CHART_TYPES,
  LBL,
  LINE_CAP_OPTIONS,
  LINE_JOIN_OPTIONS,
  LINK_TOOLTIP_CLASS,
  LINK_TOOLTIP_HINT_CLASS,
  LINK_TOOLTIP_HOST_CLASS,
  LINK_TOOLTIP_LABEL_CLASS,
  LINK_TOOLTIP_PANEL_CLASS,
  LOCAL_SYNC_ORIGIN,
  MATERIAL_PRESETS,
  MAX_INDENT_LEVEL,
  MAX_ZOOM_SCALE,
  MIN_ZOOM_SCALE,
  MORPH_CROSSFADE_GROUP_STYLE,
  MORPH_CROSSFADE_HALF_STYLE,
  MOTION_PATH_FAMILIES,
  NOTES_FONT_SIZE_DEFAULT,
  NOTES_FONT_SIZE_MAX,
  NOTES_FONT_SIZE_MIN,
  NOTES_FONT_SIZE_STEP,
  NUM,
  OFFICE_COLOR_SWATCHES,
  OUTLINE_LEVEL_ATTR,
  OUTLINE_ROW_ATTR,
  OUTLINE_SLIDE_ATTR,
  OUTLINE_VIEW_ATTR,
  PATTERN_OPTIONS,
  PATTERN_PRESET_OPTIONS,
  PEN_COLORS,
  PPTX_OPEN_ACCEPT,
  PRESENTATION_CHANNEL_NAME,
  PRESENTATION_HASH,
  PRESENTATION_HIT_TEST_CSS,
  PRESENTATION_MESSAGE_ORIGIN,
  PRESENTATION_NONCE_KEY,
  PRESENTATION_OPEN_EXTENSIONS,
  PRESENTATION_STAGE_ATTRIBUTE,
  PRESENTER_CONSOLE_CLASSES,
  PRESENTER_CONSOLE_CONTROLS,
  PRESENTER_NAVIGATOR_LABEL_KEYS,
  PRESENT_TOOLBAR_CLASSES,
  PRESETS,
  PRESET_THEMES,
  PptxHandler,
  PresentationAnimationController,
  QUICK_ACCESS_COMMAND_CATALOG,
  RADAR_STYLE_OPTIONS,
  READING_VIEW_ATTR,
  READING_VIEW_COUNTER_ATTR,
  READING_VIEW_STAGE_ATTR,
  RIBBON_SHAPE_SWATCHES,
  RIBBON_TRANSITION_PRESETS,
  RULER_FONT_SIZE,
  RULER_THICKNESS,
  SECTION_HEADING,
  SEL,
  SHAPE_QUICK_STYLES,
  SHORTCUT_REFERENCE_ITEMS,
  SLIDE_SHOW_OPTIONS,
  SLIDE_SIZE_PRESETS,
  SLIDE_TEMPLATES,
  SLIDE_TRANSITION_KEYFRAMES,
  SLIDE_TRANSITION_OPTIONS,
  SLIDE_VIRTUALIZATION_THRESHOLD,
  SMARTART_COLOR_SCHEME_LABEL_KEYS,
  SMARTART_PREVIEW_ELEMENT_HEIGHT,
  SMARTART_PREVIEW_ELEMENT_WIDTH,
  SMARTART_STYLE_LABEL_KEYS,
  SORTER_MAX_ZOOM,
  SORTER_MIN_ZOOM,
  SORTER_ZOOM_STEP,
  STATUS_BAR_CLASSES,
  SURFACE_WIREFRAME_OPTIONS,
  SWITCHABLE_LAYOUT_TYPES,
  SvgExporter,
  TABLE_STYLE_BORDER_SIDES,
  TABLE_STYLE_BORDER_SIDE_LABEL_KEYS,
  TABLE_STYLE_DASH_PRESETS,
  TABLE_STYLE_EDITOR_PARTS,
  TABLE_STYLE_PRESETS,
  TAB_ROW_ACTION_CLASSES,
  TEXT_WARP_PRESETS,
  THEME_CATALOG,
  THEME_COLOR_SCHEME_KEYS,
  THEME_COLOR_SLOT_LABEL_KEYS,
  TITLE_BAR_CLASSES,
  TITLE_BAR_DEFAULT_FILE_KEY,
  TOOLBAR_TABS,
  TRANSITION_DIR_ARROWS,
  TRANSITION_MORPH_OPTIONS,
  TRANSITION_ORIENTATION_TYPES,
  TRANSITION_SOUND_NONE_VALUE,
  TRANSITION_SOUND_OTHER_VALUE,
  TRANSITION_SPEED_OPTIONS,
  TRANSITION_VALID_DIRECTIONS,
  VIEWER_OPTIONS_TABS,
  VIEWER_PREFS_STORAGE_KEY,
  WEBM_MIME_CANDIDATES,
  acceptAutosaveRecovery,
  acceptsPresentationInput,
  actionAffordanceLabels,
  activateModalFocus,
  addChartCategory,
  addChartSeries,
  addCommentToList,
  addQuickAccessCommand,
  addSection,
  addSiblingAfter,
  addSmartArtNodeAsChild,
  addTableStyleToMap,
  addTagToCollections,
  advanceChartMarkDrag,
  advanceChartValueDrag,
  advanceMainSequence,
  alignElements,
  animationEffectLabel,
  annotationOverlayZIndex,
  appendPresentationInkPoint,
  appendTableElementColumn,
  appendTableElementRow,
  applyAcceptAllSlides,
  applyAcceptSlide,
  applyAnimationTimelineOrder,
  applyAutoCorrect,
  applyCaseTransformToSegments,
  applyChartPartHighlight,
  applyColorChange,
  applyDuotone,
  applyFindReplacements,
  applyFormatToElement,
  applyHighlightClickStyle,
  applyImagePathPatches,
  applyMasterViewCrudAction,
  applyMotionPathPreset,
  applyOutlineEdit,
  applyPreferenceToOptions,
  applyReadingViewCommand,
  applyRehearsalTimings,
  applyReroutedConnectors,
  applyResize,
  applyStyleToSelectedSegments,
  applyTableStyleDelete,
  applyTableStyleFieldEdit,
  applyTableStyleMapChange,
  applyTableStylePreset,
  applyThemeOverrideToSlide,
  applyTransitionSoundFile,
  attachPresentationVisibilityPause,
  authorConnectorBetweenSites,
  autosaveSnapshotMark,
  availableQuickAccessCommands,
  backstageCardsFor,
  bar3DShapePatch,
  beginChartMarkDrag,
  beginChartValueDrag,
  beginShapeAdjustment,
  bringForward,
  bringToFront,
  buildActiveSessionUsers,
  buildAnimationTimelineBars,
  buildAnimationTimelineRows,
  buildAreaChart3DDataForElement,
  buildBarActions,
  buildBarChart3DDataForElement,
  buildBroadcastViewerUrl,
  buildCacheKey,
  buildChartMarkDragGeometry,
  buildChromeStyle,
  buildCollaborationShareUrl,
  buildCommentMarkers,
  buildContentPartStrokes,
  buildContextMenuEntries,
  buildCreateCollaborationConfig,
  buildCssGradientFromShapeStyle,
  buildDeckSaveOptions,
  buildDirectionGrid,
  buildDuotoneCacheKey,
  buildElementClipboardPayload,
  buildFieldSubstitutionContext,
  buildFontCatalog,
  buildGlyphEnvelope,
  buildHandoutsHtml,
  buildHollowHitOutline,
  buildInitialGuides,
  buildInkGroupStrokes,
  buildJoinCollaborationConfig,
  buildLayoutPreviewGeometry,
  buildLineChart3DDataForElement,
  buildLiveInkStrokeView,
  buildMorphAnimationRules,
  buildMorphTransitionPlan,
  buildNotesHtml,
  buildNotesPrintHtml,
  buildOleObjectNamePatch,
  buildOutline,
  buildOutlineHtml,
  buildPieChart3DDataForElement,
  buildPresentationAudienceUrl,
  buildPreviewElements,
  buildPrintDocument,
  buildPrintHtmlDocument,
  buildSaveSlides,
  buildSlideTemplateContent,
  buildSlideTemplateSlide,
  buildSlidesHtml,
  buildSmartArt3DModel,
  buildSmartArtA11y,
  buildSmartArtPresetData,
  buildSmartArtPreviewElement,
  buildStrokeOutline,
  buildStrokePathD,
  buildSubpathFillOverlay,
  buildSummaryZoomView,
  buildSurfaceChart3DDataForElement,
  buildTableDataGrid,
  buildTextStyleOverrideCss,
  buildThemeColorGrid,
  buildThemeColorMap,
  buildThemeColorSwatchGrid,
  buildUserFontFaceStyles,
  canAddTopLevelNode,
  canCommitActionType,
  canDrillDown,
  canGoNext,
  canGoPrevious,
  canGroupSelection,
  canInteractWithElement,
  canRemoveTopLevelNode,
  canSetStrokeWidth,
  canUngroupSelection,
  cancelBuildReveal,
  canvasToJpegData,
  centeredSvgTextLines,
  chartGridlinesPatch,
  chartGridlinesState,
  checkModifyPassword,
  clampCropValue,
  clampNotesFontSize,
  clampOptionNumber,
  clampPercent2,
  clampSorterZoom,
  clampTransitionNumber,
  clampZoomScale,
  classifyExtraConnections,
  classifyMediaType,
  clearAllLocalViewerData,
  clearAudienceContent,
  clearLocalAwareness,
  clearMotionPath,
  clearPlaybackTimers,
  clearStoredViewerPrefs,
  clearTransitionSound,
  cloneElementForPaste,
  cloneSlide,
  cloneTemplateElementsBySlideId,
  collapseChartTitleRunsForEdit,
  collectCoherent3DOffNodeIds,
  collectConnectorSiteCandidates,
  collectImagePaths,
  collectMediaElements,
  collectReferencedFontFamilies,
  collectUsedFonts,
  colorWithOpacity,
  commentMentionQuery,
  commentTextSegments,
  comparePresentation,
  compatToastStackStyle,
  compatibilityWarningToasts,
  compileEquationTemplateMathMl,
  compileLatexEquation,
  computeAllNotesPages,
  computeAnchoredPopupPosition,
  computeColorFilter,
  computeDrawingViewBox,
  computeEntranceAnimationDelay,
  computeGridSpacingPx,
  computeHandoutLayout,
  computeInlineEditorRect,
  computeKeyboardInset,
  computeMarqueeHitIds,
  computeMergeCellDown,
  computeMergeCellRight,
  computeScrollDelta,
  computeSlideIndices,
  computeSmartArtElementLayout,
  computeSnapToShape,
  computeSplitCell,
  computeVirtualRange,
  connectorWrapperTransform,
  convertLatexToOmml,
  convertOmmlToLatex,
  copyFormatFromElement,
  createActiveAnimationGroup,
  createBackstagePresentation,
  createBlankSlide,
  createCollaborationLivePatcher,
  createDefaultChartElement,
  createDefaultChartUserShape,
  createDefaultChartUserShapeGroupChild,
  createDepartureChannel,
  createInitialPresentationSnapshot,
  createPlainNotesSegments,
  createPresenceProjector,
  createPresentationKeyBuffer,
  createPresentationSessionId,
  createPresenterShowGuard,
  createPresenterTimer,
  createSyncGate,
  createTableStyleEntry,
  createTouchGestureRecognizer,
  createViewerOptionsStore,
  createViewerZoomStore,
  createWheelStepBuffer,
  cursorForTool,
  defaultCssVars,
  defaultRadius,
  defaultThemeColors,
  deleteAutosaveSnapshot,
  deleteSection,
  deleteTableColumn,
  deleteTableRow,
  deleteTableStyleFromMap,
  deleteTagFromCollections,
  demote,
  deriveSlideFieldContext,
  deriveViewportBreakpoints,
  describeFontEmbedding,
  describeSmartArtBounds,
  describeTableStyleEditor,
  detectOrientation,
  detectTouchDevice,
  discardAutosaveRecovery,
  distributeElements,
  downloadBlob,
  downloadDataUrl,
  editCommentInList,
  elementActionToPptxAction,
  elementIdSelector,
  elementLockTogglePatch,
  encodeGif,
  endAudienceDisplay,
  ensureActionAffordanceStyles,
  ensureChartInteractionStyles,
  erasePresentationInkAt,
  evenColumnWidths,
  evenRowHeights,
  exportDeckJson,
  extraQuickAccessCommands,
  filterCommands,
  filterHiddenSlideIndices,
  filterInteractableIds,
  filterRenderedElements,
  findChartPartTarget,
  findConnectorSiteNear,
  findEraserHitElementId,
  findInSlides,
  findSelectedThemeSwatch,
  findSmartArtNodeText,
  finishPrintWindow,
  firstShowSlideIndex,
  flattenNodes,
  flattenTagCollections,
  formatAxisValue,
  formatBackstageDate,
  formatBackstageSize,
  formatBytes,
  formatCommentTimestamp,
  formatElapsed,
  formatMobileElapsed,
  formatRelativeTime,
  formatSlideCounter,
  formatTime,
  formatVersionTimestamp,
  fpsToFrameIntervalMs,
  generateBroadcastRoomId,
  generateElementId,
  generateNoteLineCount,
  generateTicks,
  getActiveXControlOverlayView,
  getAriaLabel,
  getAriaRole,
  getAriaRoleDescription,
  getBarFacePicturePixelSampleVersion,
  getCachedResult,
  getChartUserShapeGroupTransform,
  getChartUserShapeRowChartBox,
  getCompoundLineOffsets,
  getCompoundLineWidths,
  getComputedEffectStyle,
  getConnectorEndpointHandles,
  getContentPartReplayStyles,
  getCurrentParagraphIndex,
  getDraggedShapeAdjustments,
  getDuotoneCachedResult,
  getEffectSoundState,
  getEffectStyleSource,
  getElementLabel,
  getElementTransform,
  getElementTransformWithoutRotation,
  getGroupChildParentFill,
  getImageColorWashStyle,
  getImageEffectsFilter,
  getImageEffectsOpacity,
  getImageSvgFilters,
  getInkReplayStyles,
  getInlineEditorSelection,
  getLocalStorageUsageSummary,
  getNonVisualDescriptionFields,
  getNotesPrintableArea,
  getOleAriaLabel,
  getOleBadgeLabel,
  getOleDisplayName,
  getOleObjectTypeLabel,
  getOleTypeColor,
  getOleTypeLabel,
  getOverflowSegments,
  getPatternSvg,
  getPendingSelectionRestore,
  getPresetShapeClipPath,
  getPrintableArea,
  getShapeAdjustmentHandleDescriptors,
  getShapeConnectionSites,
  getSlideBackgroundStyle,
  getSlideTransitionAnimations,
  getSoftEdgeSvgFilter,
  getSubstituteFontFamily,
  getSvgStrokeDasharray,
  getTextBodyRotationTransform,
  getTextCompensationTransform,
  getWarpPath,
  groupElements,
  groupIntoParagraphs,
  groupSlidesBySection,
  guidePxToEmu,
  handleReadingViewKey,
  hasCopyableFormat,
  hasGlyphEnvelope,
  hasShapeProperties,
  hasShowSlideAfter,
  hasTextProperties,
  hasTiltData,
  hiddenSlideCue,
  inlineElementPointerEvents,
  insertCommentMention,
  insertTableColumn,
  insertTableRow,
  isActionHidden,
  isBlackboardActive,
  isBrowserOpenableMime,
  isCurrentLayout,
  isEditableMotionPath,
  isEditableTextElement,
  isEditorTextInputTarget,
  isElementActionable,
  isElementLocked,
  isElementRendered,
  isExportAbortError,
  isHeaderFooterDateTextVisible,
  isHeaderFooterFooterTextVisible,
  isHeaderFooterHeaderTextVisible,
  isHollowShapeElement,
  isImageLikeElement,
  isImageTiled,
  isInkElement,
  isKeyboardOpen,
  isLegacyBinaryPresentation,
  isLinkedTextBox,
  isMixedContentBlocked,
  isMobileViewport,
  isPpactionUrl,
  isPresentationAdvanceClick,
  isPresentationSessionMessage,
  isSeriesUsingSecondaryAxis,
  isSupportedPresentationFile,
  isTemplateElementId3,
  isUrlSafe,
  isWedgeCalloutPresetShape,
  lastShowSlideIndex,
  listAutosaveSnapshots,
  listBackstageRecentFiles,
  listChartUserShapeRows,
  lockResizeAspect,
  makeCloneId,
  makeSlideId,
  mapEditingWheel,
  mapEditorKey,
  mapOutlineKey,
  mapPresentationKey,
  mapPresentationWheel,
  mapSlideShowStartKey,
  mapSlideSorterKey,
  masterViewBackgroundColor,
  masterViewCrudActions,
  masterViewCrudFailureKey,
  masterViewElements,
  masterViewPseudoSlide,
  matchCommentMentionAuthors,
  mayLeaveSlideShow,
  mediaTransportVisible,
  mediaTrimEndAbsoluteMs,
  mediaTrimEndMsFromAbsoluteMs,
  mediaTrimEndMsFromSeconds,
  mediaTrimEndSeconds,
  mergeAdditiveSelection,
  mergeCells,
  mergePresentationSnapshot,
  mergeShapes,
  mergeSlideTransition,
  mobileElapsedSince,
  mobileSlideCounter,
  modelDataToBlobUrl,
  morphOptionToMode,
  motionPathEndPixel,
  motionPathFamilyLabelKey,
  motionPathFor,
  motionPathPresetIdForPath,
  motionPathPresetLabelKey,
  motionPathPresetsByFamily,
  motionPathToSvgD,
  movePresenterPointer,
  moveQuickAccessCommand,
  moveSectionDown,
  moveSectionUp,
  moveSlidesToSection,
  mruColorsPatch,
  newTableElement,
  nextPresentedSlide,
  nextShowSlideIndex,
  normalizeColorsForCapture,
  normalizeHexColor2,
  normalizeStrokeDashType,
  normalizeTableStyleGuid,
  notesSegmentsToSpans,
  observeYDocSlides,
  openNativeEyeDropper,
  openPendingPrintWindow,
  openPptxFile,
  openReadingView,
  openUrlInNewTab,
  paintedStrokeWidth,
  paragraphsToSegments,
  parsePpactionUrl,
  parseSegmentsFromRichEditor,
  partitionTemplateElements,
  patchChartData,
  pickSupportedMimeType,
  placeAudienceWindow,
  placeCaretAtEnd,
  placeholderPromptDescriptor,
  playFeedbackSound,
  playGroup,
  playSlideTransitionPreview,
  polygonsToSvgPath,
  pptxActionToElementAction,
  preprocessCssForCapture,
  presentationBaseName,
  presentationEntrySlideIndex,
  presentationInkPath,
  presenterElapsed,
  presenterNextDisabled,
  presenterPaneAdvancesOnClick,
  presenterPrevDisabled,
  presenterTimerProgress,
  previousShowSlideIndex,
  printPropertiesFrameSlides,
  printPropertiesSlidesPerPage,
  probeAutosaveRecovery,
  projectDrawingShapes,
  promote,
  publishLiveGeometry,
  publishLiveInlineText,
  pushRecentColor,
  quickStyleSwatchCss,
  radarStylePatch,
  reResolveElementColors,
  reResolveSlideColors,
  readBackstageRecentFile,
  readEditableText,
  readOnlyRecommendation,
  readRibbonTransitionDraft,
  readSlideShowOption,
  readSlidesFromYDoc,
  readSoundFileAsDataUrl,
  readStoredViewerPrefs,
  readViewportMetrics,
  readingViewFitScale,
  rebuildDrawingShapesIfCleared,
  rebuildTableStructureInRawXml,
  reconcileSlidesInYDoc,
  recordProgressPercent,
  redistributeColumnWidth,
  registerCollaborationTeardown,
  registerCustomFont,
  remapTextToSegments,
  removeChartCategory,
  removeChartSeries,
  removeCommentFromList,
  removeEmptyNode,
  removeLastTableElementColumn,
  removeLastTableElementRow,
  removeQuickAccessCommand,
  removeSmartArtNode,
  removeTableElementColumn,
  removeTableElementRow,
  renameSection,
  reorder2,
  reorderAnimationTimelineRows,
  replaceMasterViewElements,
  replyToCommentInList,
  rerouteConnectorsForMovedElements,
  resetPresenterTimer,
  resetSlideLayoutPath,
  resolve3DRenderingFlags,
  resolveActionType,
  resolveAudienceScreenPlacement,
  resolveAuthoredCustomShowId,
  resolveAuthoredSlideRange,
  resolveAutoAdvanceDelayMs,
  resolveAutosaveActivation,
  resolveAutosaveIntervalMs,
  resolveAutosaveIntervalSeconds,
  resolveChartKind,
  resolveConnectorEndpointUpdate,
  resolveDefaultFontFamily,
  resolveDefaultPrintSettings,
  resolveDisplayedChartType,
  resolveElementActionAffordance,
  resolveElementAriaAttributes,
  resolveElementHighlightClick,
  resolveElementInteractivity,
  resolveExpiredAutosaveSnapshots,
  resolveGoogleWebfontHref,
  resolveGroupChildFill,
  resolveHistoryDepth,
  resolveImageResolutionScale,
  resolveInlineEditAutoFitHeight,
  resolveInlineEditNormAutofitShrink,
  resolveMediaElementSource,
  resolveMediaTimeNodeElementIds,
  resolveNotesSchematicBodyFontSizePx,
  resolveNotesSegments,
  resolveOleType,
  resolveOleVerbTarget,
  resolveOptionRootClasses,
  resolvePalette,
  resolvePresentationAction,
  resolveProfileInitial,
  resolveRevealedChartData,
  resolveRevealedDrawingShapeNodeIds,
  resolveRevealedDrawingShapes,
  resolveRevealedSmartArtNodes,
  resolveScreenTip,
  resolveSelectionPaneRename,
  resolveShowSlideIndexes,
  resolveSlideSizeSelection,
  resolveTableCellImageUrls,
  resolveTableStyleImageUrls,
  resolveTemplateBackgroundRows,
  resolveThemeCatalogEntry,
  resolveTitleBarStatusKey,
  resolveTransitionDurationMs,
  resolveTransitionSoundAction,
  resolveTransportForServerUrl,
  resolveViewerAddinRows,
  restoreSegmentSelection,
  ribbonTransitionUpdates,
  rulerDragToGuidePosition,
  runPresentationAction,
  safeOpenUrl,
  sanitizeColor,
  sanitizeGradientStops,
  sanitizeMathMl,
  saveAutosaveSnapshot,
  saveDeckWithPassword,
  saveViewerProfile,
  savedPresentationFileName,
  scaleSlidesForSizeChange,
  scanAvailableFontFamilies,
  scheduleAutoAdvanceChain,
  schemaLabel,
  scopeLayoutOptionsToSlide,
  seedRecentColors,
  segmentFrameCount,
  segmentsToEditorHtml,
  segmentsToParagraphs,
  segmentsToPlainText,
  sendBackward,
  sendToBack,
  seriesSecondaryAxisPatch,
  setAfterAnimation,
  setAfterAnimationColor,
  setCachedResult,
  setCellText,
  setChartAxisGridlineStyle,
  setChartAxisLogScale,
  setChartAxisTitleStyle,
  setChartCategoryLabel,
  setChartCellValue,
  setChartDataPointExplosion,
  setChartDataPointFill,
  setChartDataPointLabel,
  setChartDataPointMarker,
  setChartSeriesChartType,
  setChartSeriesMarker,
  setDuotoneCachedResult,
  setEffectSound,
  setMasterViewBackgroundColor,
  setMotionPath,
  setMotionPathEnd,
  setPendingSelectionRestore,
  setSmartArtNodeStyle,
  setTableElementCellText,
  shapeFillChange,
  shapeOutlineChange,
  shouldAutoFollowBroadcaster,
  shouldClearAutosaveCacheOnClose,
  shouldCommitSmartArtNodeText,
  shouldConfirmExternalHyperlink,
  shouldDiscardAutosaveOnSuccessfulSave,
  shouldLoopContinuously,
  shouldProbeAutosaveRecovery,
  shouldRoomSlidesReplaceLoad,
  shouldShowAccessibilitySection,
  shouldShowAutosaveRecoveryPrompt,
  shouldShowElementHandles,
  shouldUseSvgWarp,
  shouldWriteAutosaveSnapshot,
  siblingCount,
  siblingIndex,
  slideProgressPercent,
  slideShowOptionChange,
  slideSizeFromPreset,
  slideSizeToCanvasPx,
  slideStatusLabel,
  snapBoxToGrid,
  splitCell,
  stepPresenterZoom,
  stopAllPersistentAudio,
  storeAudienceContent,
  strokeOutlineViewBox,
  strokeWidthOf,
  styleShadowFilter,
  subscribeBarFacePicturePixelSamples,
  substituteFieldText,
  surfaceWireframePatch,
  svgLineCap,
  svgPathToPolygons,
  swapPresentationWindows,
  switchSmartArtLayout,
  tableStyleAssignmentUpdate,
  templateSchemeFromTheme,
  text3dEmuToPt,
  text3dPtToEmu,
  textFontSizePtToPx,
  textFontSizePxToPt,
  themeColorLabel,
  themeColorSchemesEqual,
  themeColorSwatchRows,
  themeSwatchCommit,
  themeToCssVars,
  toSlideIndex,
  toggleBlackboard,
  toggleCommentResolvedInList,
  togglePresenterTimer,
  toggleSheet,
  toggleStageElementMedia,
  toggleText3dExtrusion,
  transformTextCase,
  transitionSoundOptions,
  transitionSoundSelectedValue,
  trimmedMediaDurationMs,
  ungroupElements,
  updateCellTextInRawXml,
  updateCellTextStyleInRawXml,
  updateElement,
  updateMasterViewElement,
  updateMergeAttrsInRawXml,
  updateSmartArtNodeText,
  updateTagInCollections,
  validateMediaTrimRange,
  validateRoomId,
  vermilionDarkColors,
  vermilionDarkTheme,
  vermilionLightColors,
  vermilionLightTheme,
  vermilionRadius,
  viewPropertiesPatchFromPreferences,
  viewerOptionsToPreferences,
  viewerPreferencesFromViewProperties,
  visibleTemplateElements,
  warpPreviewPath,
  withChartPointValue,
  withChartTitle,
  withChartUserShapeAdded,
  withChartUserShapeGroupChildAdded,
  withChartUserShapeRowChartBoxUpdated,
  withChartUserShapeRowFlipUpdated,
  withChartUserShapeRowRemoved,
  withChartUserShapeRowRotationUpdated,
  withChartUserShapeRowTextUpdated,
  withChartUserShapeRowUpdated,
  withConnectorEndpointUpdate,
  withFrameSlides,
  withSlideSizeOrientation,
  withSlidesPerPage,
  writeStoredViewerPrefs
} from "./chunk-DWD53VEA.js";
import "./chunk-3CXS76P3.js";
import {
  useTranslation
} from "./chunk-B42DHXME.js";
import "./chunk-BQ5CSGUK.js";
import {
  require_jsx_runtime
} from "./chunk-YG5AX2YU.js";
import {
  require_react
} from "./chunk-73THXJN7.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/pptx-react-viewer/dist/chunk-PCCM7NVY.mjs
var import_react = __toESM(require_react(), 1);
function useVirtualizedSlides({
  totalItems,
  itemHeight,
  overscan = DEFAULT_VIRTUAL_OVERSCAN
}) {
  const scrollContainerRef = (0, import_react.useRef)(null);
  const [scrollTop, setScrollTop] = (0, import_react.useState)(0);
  const [viewportHeight, setViewportHeight] = (0, import_react.useState)(0);
  (0, import_react.useEffect)(() => {
    const container = scrollContainerRef.current;
    if (!container) {
      return;
    }
    const handleScroll = () => {
      setScrollTop(container.scrollTop);
    };
    setViewportHeight(container.clientHeight);
    setScrollTop(container.scrollTop);
    container.addEventListener("scroll", handleScroll, { passive: true });
    let resizeObserver;
    if (typeof ResizeObserver !== "undefined") {
      resizeObserver = new ResizeObserver((entries) => {
        for (const entry of entries) {
          setViewportHeight(entry.contentRect.height);
        }
      });
      resizeObserver.observe(container);
    }
    return () => {
      container.removeEventListener("scroll", handleScroll);
      resizeObserver == null ? void 0 : resizeObserver.disconnect();
    };
  }, []);
  const range = computeVirtualRange(totalItems, itemHeight, scrollTop, viewportHeight, overscan);
  const safeItemHeight = Math.max(itemHeight, 1);
  const scrollToIndex = (0, import_react.useCallback)(
    (index, behavior = "smooth") => {
      const container = scrollContainerRef.current;
      if (!container) {
        return;
      }
      const targetTop = index * safeItemHeight;
      const targetBottom = targetTop + safeItemHeight;
      const containerTop = container.scrollTop;
      const containerBottom = containerTop + container.clientHeight;
      if (targetTop < containerTop) {
        container.scrollTo({ top: targetTop, behavior });
      } else if (targetBottom > containerBottom) {
        container.scrollTo({
          top: targetBottom - container.clientHeight,
          behavior
        });
      }
    },
    [safeItemHeight]
  );
  return {
    ...range,
    scrollContainerRef,
    scrollToIndex
  };
}
function usePresenceTracking({
  awareness,
  localClientId,
  userName,
  userColor,
  userAvatar,
  role,
  canvasWidth,
  canvasHeight
}) {
  const [remoteUsers, setRemoteUsers] = (0, import_react.useState)([]);
  const projectorRef = (0, import_react.useRef)(null);
  projectorRef.current ?? (projectorRef.current = createPresenceProjector());
  const projector = projectorRef.current;
  const lastBroadcastRef = (0, import_react.useRef)(0);
  const pendingBroadcastRef = (0, import_react.useRef)(null);
  const latestLocalState = (0, import_react.useRef)({});
  const broadcastPresence = (0, import_react.useCallback)(
    (update) => {
      if (!awareness) {
        return;
      }
      Object.assign(latestLocalState.current, update);
      const now = Date.now();
      const elapsed = now - lastBroadcastRef.current;
      const flush = () => {
        const state = {
          ...latestLocalState.current,
          userName,
          userColor,
          userAvatar,
          role,
          lastUpdated: (/* @__PURE__ */ new Date()).toISOString()
        };
        awareness.setLocalStateField("presence", state);
        lastBroadcastRef.current = Date.now();
      };
      if (elapsed >= BROADCAST_THROTTLE_MS) {
        if (pendingBroadcastRef.current) {
          clearTimeout(pendingBroadcastRef.current);
          pendingBroadcastRef.current = null;
        }
        flush();
      } else if (!pendingBroadcastRef.current) {
        pendingBroadcastRef.current = setTimeout(() => {
          pendingBroadcastRef.current = null;
          flush();
        }, BROADCAST_THROTTLE_MS - elapsed);
      }
    },
    [awareness, userName, userColor, userAvatar, role]
  );
  (0, import_react.useEffect)(() => {
    if (!awareness) {
      return;
    }
    awareness.setLocalStateField("presence", {
      userName,
      userColor,
      userAvatar,
      role,
      activeSlideIndex: 0,
      cursorX: 0,
      cursorY: 0,
      lastUpdated: (/* @__PURE__ */ new Date()).toISOString()
    });
  }, [awareness, userName, userColor, userAvatar, role]);
  (0, import_react.useEffect)(() => {
    if (!awareness || localClientId === null) {
      return;
    }
    const handleChange = () => {
      const { list, changed } = projector.project(
        awareness.getStates(),
        localClientId,
        canvasWidth,
        canvasHeight
      );
      if (!changed) {
        return;
      }
      setRemoteUsers(list);
    };
    awareness.on("change", handleChange);
    awareness.on("update", handleChange);
    handleChange();
    return () => {
      awareness.off("change", handleChange);
      awareness.off("update", handleChange);
    };
  }, [awareness, localClientId, canvasWidth, canvasHeight, projector]);
  (0, import_react.useEffect)(() => {
    if (!awareness) {
      return;
    }
    const interval = setInterval(() => {
      awareness.setLocalStateField("presence", {
        ...latestLocalState.current,
        userName,
        userColor,
        userAvatar,
        role,
        lastUpdated: (/* @__PURE__ */ new Date()).toISOString()
      });
    }, 1e4);
    return () => clearInterval(interval);
  }, [awareness, userName, userColor, userAvatar, role]);
  (0, import_react.useEffect)(() => {
    return () => {
      if (pendingBroadcastRef.current) {
        clearTimeout(pendingBroadcastRef.current);
      }
    };
  }, []);
  return { remoteUsers, broadcastPresence };
}
function useYjsProvider({ config }) {
  const [status, setStatus] = (0, import_react.useState)("disconnected");
  const [awareness, setAwareness] = (0, import_react.useState)(null);
  const [doc, setDoc] = (0, import_react.useState)(null);
  const [clientId, setClientId] = (0, import_react.useState)(null);
  const [synced, setSynced] = (0, import_react.useState)(false);
  const cleanupRef = (0, import_react.useRef)(null);
  const timeoutRef = (0, import_react.useRef)(null);
  const departureRef = (0, import_react.useRef)(null);
  const gateRef = (0, import_react.useRef)(null);
  if (!gateRef.current) {
    gateRef.current = createSyncGate(() => setSynced(true));
  }
  const teardown = (0, import_react.useCallback)(() => {
    var _a, _b2;
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
      timeoutRef.current = null;
    }
    (_a = gateRef.current) == null ? void 0 : _a.reset();
    setSynced(false);
    (_b2 = cleanupRef.current) == null ? void 0 : _b2.call(cleanupRef);
    cleanupRef.current = null;
  }, []);
  const announceDeparture = (0, import_react.useCallback)(() => {
    var _a, _b2;
    (_a = departureRef.current) == null ? void 0 : _a.announce();
    (_b2 = departureRef.current) == null ? void 0 : _b2.dispose();
    departureRef.current = null;
  }, []);
  const buildCleanup = (0, import_react.useCallback)(
    (provider, yDoc) => () => {
      announceDeparture();
      clearLocalAwareness(provider.awareness);
      provider.destroy();
      yDoc.destroy();
      setDoc(null);
      setAwareness(null);
      setClientId(null);
      setStatus("disconnected");
    },
    [announceDeparture]
  );
  const initWebrtc = (0, import_react.useCallback)(async () => {
    var _a;
    if (!config) {
      return;
    }
    setStatus("connecting");
    try {
      const [Y, { WebrtcProvider }] = await Promise.all([import("./yjs-4ED7E5CZ-X3LPTJYE.js"), import("./y-webrtc-7DLWG3LJ-BQOOTX24.js")]);
      const yDoc = new Y.Doc();
      const opts = {};
      if (config.signaling && config.signaling.length > 0) {
        opts.signaling = config.signaling;
      }
      if (config.authToken) {
        opts.password = config.authToken;
      }
      const roomId = validateRoomId(config.roomId);
      const provider = new WebrtcProvider(roomId, yDoc, opts);
      departureRef.current = createDepartureChannel(roomId, provider.awareness);
      setDoc(yDoc);
      setAwareness(provider.awareness);
      setClientId(provider.awareness.clientID);
      setStatus("connected");
      const handleSynced = (event) => {
        var _a2;
        if ((event == null ? void 0 : event.synced) !== false) {
          (_a2 = gateRef.current) == null ? void 0 : _a2.open();
        }
      };
      provider.on("synced", handleSynced);
      (_a = gateRef.current) == null ? void 0 : _a.arm();
      const handleStatus = (event) => {
        var _a2, _b2;
        if (event.connected === false) {
          setStatus("disconnected");
          (_a2 = gateRef.current) == null ? void 0 : _a2.reset();
          setSynced(false);
          (_b2 = gateRef.current) == null ? void 0 : _b2.arm();
        } else if (event.connected) {
          setStatus("connected");
        }
      };
      provider.on("status", handleStatus);
      const baseCleanup = buildCleanup(provider, yDoc);
      cleanupRef.current = () => {
        provider.off("synced", handleSynced);
        provider.off("status", handleStatus);
        baseCleanup();
      };
    } catch (err) {
      console.warn(
        "[pptx-viewer] WebRTC collaboration packages not available:",
        err instanceof Error ? err.message : err
      );
      setStatus("error");
    }
  }, [config == null ? void 0 : config.roomId, config == null ? void 0 : config.authToken, config == null ? void 0 : config.signaling, buildCleanup]);
  const init = (0, import_react.useCallback)(async () => {
    var _a, _b2;
    teardown();
    if (!config) {
      return;
    }
    const roomId = validateRoomId(config.roomId);
    const transport = config.transport ?? resolveTransportForServerUrl(config.serverUrl);
    if (transport === "webrtc") {
      await initWebrtc();
      return;
    }
    if (isMixedContentBlocked(config.serverUrl)) {
      console.warn(
        `[pptx-viewer] Refusing to connect: insecure ws:// server "${config.serverUrl}" is blocked from a secure (https) page. Use a wss:// URL.`
      );
      setStatus("error");
      return;
    }
    setStatus("connecting");
    try {
      const [Y, { WebsocketProvider }] = await Promise.all([import("./yjs-4ED7E5CZ-X3LPTJYE.js"), import("./y-websocket-HUEA7LH7-TYYGXE66.js")]);
      const yDoc = new Y.Doc();
      const provider = new WebsocketProvider(config.serverUrl, roomId, yDoc, {
        params: config.authToken ? { token: config.authToken } : void 0
      });
      departureRef.current = createDepartureChannel(roomId, provider.awareness);
      let connected = false;
      const handleStatus = (event) => {
        var _a2, _b3, _c;
        if (event.status === "connected") {
          connected = true;
          if (timeoutRef.current) {
            clearTimeout(timeoutRef.current);
            timeoutRef.current = null;
          }
          setStatus("connected");
          (_a2 = gateRef.current) == null ? void 0 : _a2.arm();
        } else if (event.status === "disconnected") {
          setStatus("disconnected");
          (_b3 = gateRef.current) == null ? void 0 : _b3.reset();
          setSynced(false);
          (_c = gateRef.current) == null ? void 0 : _c.arm();
        }
      };
      const handleSynced = (isSynced) => {
        var _a2;
        if (isSynced) {
          (_a2 = gateRef.current) == null ? void 0 : _a2.open();
        }
      };
      provider.on("status", handleStatus);
      provider.on("sync", handleSynced);
      if (provider.wsconnected) {
        connected = true;
        setStatus("connected");
        (_a = gateRef.current) == null ? void 0 : _a.arm();
      }
      if (provider.synced) {
        (_b2 = gateRef.current) == null ? void 0 : _b2.open();
      }
      if (!connected) {
        timeoutRef.current = setTimeout(() => {
          timeoutRef.current = null;
          if (!connected) {
            provider.off("status", handleStatus);
            provider.off("sync", handleSynced);
            announceDeparture();
            clearLocalAwareness(provider.awareness);
            provider.destroy();
            yDoc.destroy();
            setDoc(null);
            setAwareness(null);
            setClientId(null);
            cleanupRef.current = null;
            setStatus("error");
          }
        }, CONNECTION_TIMEOUT_MS);
      }
      setDoc(yDoc);
      setAwareness(provider.awareness);
      setClientId(provider.awareness.clientID);
      cleanupRef.current = () => {
        provider.off("status", handleStatus);
        provider.off("sync", handleSynced);
        announceDeparture();
        clearLocalAwareness(provider.awareness);
        provider.destroy();
        yDoc.destroy();
        setDoc(null);
        setAwareness(null);
        setClientId(null);
        setStatus("disconnected");
      };
    } catch (err) {
      console.warn(
        "[pptx-viewer] Collaboration packages not available:",
        err instanceof Error ? err.message : err
      );
      setStatus("error");
    }
  }, [
    config == null ? void 0 : config.roomId,
    config == null ? void 0 : config.serverUrl,
    config == null ? void 0 : config.authToken,
    config == null ? void 0 : config.transport,
    announceDeparture,
    initWebrtc,
    teardown
  ]);
  (0, import_react.useEffect)(() => {
    init();
    const disposeTeardown = registerCollaborationTeardown({
      leave: teardown,
      rejoin: init
    });
    return () => {
      disposeTeardown();
      teardown();
    };
  }, [init, teardown]);
  const retry = (0, import_react.useCallback)(() => {
    init();
  }, [init]);
  return { status, awareness, doc, clientId, synced, retry };
}
function useCollaborativeState({
  config,
  canvasWidth,
  canvasHeight
}) {
  const userColor = sanitizeColor(config == null ? void 0 : config.userColor, "#6366f1");
  const { status, awareness, doc, clientId, synced, retry } = useYjsProvider({ config });
  const { remoteUsers, broadcastPresence } = usePresenceTracking({
    awareness,
    localClientId: clientId,
    userName: (config == null ? void 0 : config.userName) ?? "",
    userColor,
    userAvatar: config == null ? void 0 : config.userAvatar,
    role: config == null ? void 0 : config.role,
    canvasWidth,
    canvasHeight
  });
  const connectedCount = status === "connected" ? remoteUsers.length + 1 : remoteUsers.length;
  if (!config) {
    return null;
  }
  return {
    status,
    remoteUsers,
    broadcastPresence,
    connectedCount,
    config,
    doc,
    synced,
    retry
  };
}
function useSheetDismissDrag(onClose, threshold = 120) {
  const [dragY, setDragY] = (0, import_react.useState)(0);
  const [dragging, setDragging] = (0, import_react.useState)(false);
  const startRef = (0, import_react.useRef)(null);
  const onPointerDown = (0, import_react.useCallback)((e) => {
    var _a, _b2;
    startRef.current = e.clientY;
    setDragging(true);
    (_b2 = (_a = e.target).setPointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
  }, []);
  const onPointerMove = (0, import_react.useCallback)((e) => {
    if (startRef.current === null) {
      return;
    }
    setDragY(Math.max(0, e.clientY - startRef.current));
  }, []);
  const onPointerUp = (0, import_react.useCallback)(
    (e) => {
      var _a, _b2;
      if (startRef.current === null) {
        return;
      }
      const delta = e.clientY - startRef.current;
      startRef.current = null;
      setDragging(false);
      (_b2 = (_a = e.target).releasePointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
      if (delta > threshold) {
        onClose();
      }
      setDragY(0);
    },
    [onClose, threshold]
  );
  return {
    dragY,
    dragging,
    handlers: { onPointerDown, onPointerMove, onPointerUp, onPointerCancel: onPointerUp }
  };
}
var DEFAULT_MAX_HISTORY_ENTRIES = 120;
function serializeDocument(snapshot) {
  return JSON.stringify({
    width: snapshot.width,
    height: snapshot.height,
    slides: snapshot.slides,
    templateElementsBySlideId: snapshot.templateElementsBySlideId
  });
}
function useEditorHistory(input) {
  const {
    slides,
    canvasSize,
    activeSlideIndex,
    templateElementsBySlideId,
    loading,
    error,
    maxHistoryEntries = DEFAULT_MAX_HISTORY_ENTRIES,
    hasActivePointerInteraction,
    pointerCommitNonce,
    onDirty,
    setSlides,
    setCanvasSize,
    setActiveSlideIndex,
    setTemplateElementsBySlideId,
    setSelectedElementId,
    setSelectedElementIds
  } = input;
  const historyPastRef = (0, import_react.useRef)([]);
  const historyFutureRef = (0, import_react.useRef)([]);
  const lastHistorySnapshotRef = (0, import_react.useRef)(null);
  const lastHistorySerializedRef = (0, import_react.useRef)("");
  const lastCheapHashRef = (0, import_react.useRef)("");
  const isApplyingHistoryRef = (0, import_react.useRef)(false);
  const unlockHistoryTimerRef = (0, import_react.useRef)(null);
  const [canUndo, setCanUndo] = (0, import_react.useState)(false);
  const [canRedo, setCanRedo] = (0, import_react.useState)(false);
  const [undoLabel, setUndoLabel] = (0, import_react.useState)(void 0);
  const [redoLabel, setRedoLabel] = (0, import_react.useState)(void 0);
  const [editCommitNonce, setEditCommitNonce] = (0, import_react.useState)(0);
  const updateHistoryAvailability = (0, import_react.useCallback)(() => {
    setCanUndo(historyPastRef.current.length > 0);
    setCanRedo(historyFutureRef.current.length > 0);
    const pastTop = historyPastRef.current[historyPastRef.current.length - 1];
    setUndoLabel(pastTop == null ? void 0 : pastTop.actionLabel);
    const futureTop = historyFutureRef.current[historyFutureRef.current.length - 1];
    setRedoLabel(futureTop == null ? void 0 : futureTop.actionLabel);
  }, []);
  const buildHistorySnapshot = (0, import_react.useCallback)(
    (actionLabel) => {
      return {
        width: canvasSize.width,
        height: canvasSize.height,
        activeSlideIndex,
        slides: slides.map(cloneSlide),
        templateElementsBySlideId: cloneTemplateElementsBySlideId(templateElementsBySlideId),
        ...actionLabel ? { actionLabel } : {}
      };
    },
    [activeSlideIndex, canvasSize, slides, templateElementsBySlideId]
  );
  const applyHistorySnapshot = (0, import_react.useCallback)(
    (snapshot) => {
      const maxSlideIndex = Math.max(snapshot.slides.length - 1, 0);
      setCanvasSize({
        width: snapshot.width,
        height: snapshot.height
      });
      setSlides(snapshot.slides.map(cloneSlide));
      setTemplateElementsBySlideId(
        cloneTemplateElementsBySlideId(snapshot.templateElementsBySlideId)
      );
      setActiveSlideIndex(Math.min(snapshot.activeSlideIndex, maxSlideIndex));
      setSelectedElementIds([]);
      setSelectedElementId(null);
    },
    [
      setActiveSlideIndex,
      setCanvasSize,
      setSelectedElementId,
      setSelectedElementIds,
      setSlides,
      setTemplateElementsBySlideId
    ]
  );
  const unlockHistoryTracking = (0, import_react.useCallback)(() => {
    if (unlockHistoryTimerRef.current) {
      clearTimeout(unlockHistoryTimerRef.current);
    }
    unlockHistoryTimerRef.current = setTimeout(() => {
      isApplyingHistoryRef.current = false;
    }, 0);
  }, []);
  const onDirtyRef = (0, import_react.useRef)(onDirty);
  (0, import_react.useEffect)(() => {
    onDirtyRef.current = onDirty;
  }, [onDirty]);
  const markDirty = (0, import_react.useCallback)(() => {
    var _a;
    setEditCommitNonce((previous) => previous + 1);
    (_a = onDirtyRef.current) == null ? void 0 : _a.call(onDirtyRef);
  }, []);
  const handleUndo = (0, import_react.useCallback)(() => {
    const previousSnapshot = historyPastRef.current.pop();
    if (!previousSnapshot) {
      return;
    }
    const currentSnapshot = buildHistorySnapshot();
    historyFutureRef.current.push(currentSnapshot);
    isApplyingHistoryRef.current = true;
    const nextSnapshot = cloneHistorySnapshot2(previousSnapshot);
    lastHistorySnapshotRef.current = cloneHistorySnapshot2(nextSnapshot);
    lastHistorySerializedRef.current = serializeDocument(nextSnapshot);
    applyHistorySnapshot(nextSnapshot);
    updateHistoryAvailability();
    unlockHistoryTracking();
    markDirty();
  }, [
    applyHistorySnapshot,
    buildHistorySnapshot,
    markDirty,
    unlockHistoryTracking,
    updateHistoryAvailability
  ]);
  const handleRedo = (0, import_react.useCallback)(() => {
    const nextSnapshot = historyFutureRef.current.pop();
    if (!nextSnapshot) {
      return;
    }
    const currentSnapshot = buildHistorySnapshot();
    historyPastRef.current.push(currentSnapshot);
    isApplyingHistoryRef.current = true;
    const targetSnapshot = cloneHistorySnapshot2(nextSnapshot);
    lastHistorySnapshotRef.current = cloneHistorySnapshot2(targetSnapshot);
    lastHistorySerializedRef.current = serializeDocument(targetSnapshot);
    applyHistorySnapshot(targetSnapshot);
    updateHistoryAvailability();
    unlockHistoryTracking();
    markDirty();
  }, [
    applyHistorySnapshot,
    buildHistorySnapshot,
    markDirty,
    unlockHistoryTracking,
    updateHistoryAvailability
  ]);
  const resetHistory = (0, import_react.useCallback)(
    (initialSnapshot) => {
      historyPastRef.current = [];
      historyFutureRef.current = [];
      if (initialSnapshot) {
        const clonedInitial = cloneHistorySnapshot2(initialSnapshot);
        lastHistorySnapshotRef.current = clonedInitial;
        lastHistorySerializedRef.current = serializeDocument(clonedInitial);
      } else {
        lastHistorySnapshotRef.current = null;
        lastHistorySerializedRef.current = "";
      }
      updateHistoryAvailability();
    },
    [updateHistoryAvailability]
  );
  (0, import_react.useEffect)(() => {
    return () => {
      if (unlockHistoryTimerRef.current) {
        clearTimeout(unlockHistoryTimerRef.current);
        unlockHistoryTimerRef.current = null;
      }
    };
  }, []);
  (0, import_react.useEffect)(() => {
    var _a;
    if (loading || error) {
      return;
    }
    if (isApplyingHistoryRef.current) {
      return;
    }
    if (hasActivePointerInteraction()) {
      return;
    }
    const cheapHash = `${pointerCommitNonce}|${editCommitNonce}|${slides.length}|${activeSlideIndex}|${canvasSize.width}x${canvasSize.height}|${slides.map((s) => `${s.id}:${s.elements.length}`).join("/")}`;
    if (cheapHash === lastCheapHashRef.current) {
      return;
    }
    const snapshot = buildHistorySnapshot();
    const serialized = serializeDocument(snapshot);
    if (serialized === lastHistorySerializedRef.current) {
      const previous = lastHistorySnapshotRef.current;
      if (previous && previous.activeSlideIndex !== snapshot.activeSlideIndex) {
        lastHistorySnapshotRef.current = {
          ...previous,
          activeSlideIndex: snapshot.activeSlideIndex
        };
      }
      lastCheapHashRef.current = cheapHash;
      return;
    }
    const previousSnapshot = lastHistorySnapshotRef.current;
    if (!previousSnapshot) {
      lastHistorySnapshotRef.current = cloneHistorySnapshot2(snapshot);
      lastHistorySerializedRef.current = serialized;
      lastCheapHashRef.current = cheapHash;
      updateHistoryAvailability();
      return;
    }
    (_a = onDirtyRef.current) == null ? void 0 : _a.call(onDirtyRef);
    historyPastRef.current.push(cloneHistorySnapshot2(previousSnapshot));
    while (historyPastRef.current.length > Math.max(1, maxHistoryEntries)) {
      historyPastRef.current.shift();
    }
    historyFutureRef.current = [];
    lastHistorySnapshotRef.current = cloneHistorySnapshot2(snapshot);
    lastHistorySerializedRef.current = serialized;
    lastCheapHashRef.current = cheapHash;
    updateHistoryAvailability();
  }, [
    activeSlideIndex,
    buildHistorySnapshot,
    canvasSize.height,
    canvasSize.width,
    editCommitNonce,
    error,
    hasActivePointerInteraction,
    loading,
    maxHistoryEntries,
    pointerCommitNonce,
    slides,
    updateHistoryAvailability
  ]);
  return {
    canUndo,
    canRedo,
    undoLabel,
    redoLabel,
    handleUndo,
    handleRedo,
    resetHistory,
    markDirty,
    buildHistorySnapshot
  };
}
function useViewerStore(store, selector, isEqual = Object.is) {
  const cache = (0, import_react.useRef)(null);
  const getSnapshot2 = (0, import_react.useCallback)(() => {
    const state = store.getState();
    const last = cache.current;
    if (last && Object.is(last.state, state)) {
      return last.value;
    }
    const next = selector(state);
    const value = last && isEqual(last.value, next) ? last.value : next;
    cache.current = { state, value };
    return value;
  }, [store, selector, isEqual]);
  const subscribe2 = (0, import_react.useCallback)(
    (onStoreChange) => store.subscribe(onStoreChange),
    [store]
  );
  return (0, import_react.useSyncExternalStore)(subscribe2, getSnapshot2, getSnapshot2);
}
var selectZoom = (state) => state.zoom;
function useZoomViewport({
  canvasSize,
  selectedElements
}) {
  const editWrapperRef = (0, import_react.useRef)(null);
  const canvasStageRef = (0, import_react.useRef)(null);
  const canvasViewportRef = (0, import_react.useRef)(null);
  const renderScaleRef = (0, import_react.useRef)(1);
  const wheelBufferRef = (0, import_react.useRef)(createWheelStepBuffer());
  const storeRef = (0, import_react.useRef)(null);
  storeRef.current ?? (storeRef.current = createViewerZoomStore());
  const store = storeRef.current;
  const scale = useViewerStore(store, selectZoom);
  const setScale = (0, import_react.useCallback)(
    (next) => store.dispatch({ type: "set-zoom", zoom: next }),
    [store]
  );
  const [editorDimensions, setEditorDimensions] = (0, import_react.useState)(null);
  const effectiveEditorDimensions = editorDimensions || {
    width: canvasSize.width,
    height: canvasSize.height
  };
  const fitScale = (0, import_react.useMemo)(() => {
    if (!effectiveEditorDimensions.width || !effectiveEditorDimensions.height) {
      return 1;
    }
    const widthScale = effectiveEditorDimensions.width / canvasSize.width;
    const heightScale = effectiveEditorDimensions.height / canvasSize.height;
    return Math.min(widthScale, heightScale, 1);
  }, [canvasSize, effectiveEditorDimensions.height, effectiveEditorDimensions.width]);
  const editorScale = fitScale * scale;
  (0, import_react.useEffect)(() => {
    renderScaleRef.current = editorScale;
  }, [editorScale]);
  (0, import_react.useEffect)(() => {
    store.dispatch({ type: "set-fit-scale", fitScale });
  }, [store, fitScale]);
  (0, import_react.useEffect)(() => {
    let observer = null;
    let raf = 0;
    const VERTICAL_MARGIN = 32;
    const measure = (el) => {
      const width = Math.max(0, el.clientWidth - 8);
      const height = Math.max(0, el.clientHeight - VERTICAL_MARGIN);
      if (width > 0 && height > 0) {
        setEditorDimensions({ width, height });
      }
    };
    const attach = () => {
      const el = canvasViewportRef.current;
      if (!el) {
        raf = requestAnimationFrame(attach);
        return;
      }
      observer = new ResizeObserver(() => measure(el));
      observer.observe(el);
      measure(el);
    };
    attach();
    return () => {
      cancelAnimationFrame(raf);
      observer == null ? void 0 : observer.disconnect();
    };
  }, []);
  const centerBoundsInViewport = (0, import_react.useCallback)(
    (bounds, nextScale) => {
      const wrapper = editWrapperRef.current;
      const canvasViewport = canvasViewportRef.current;
      if (!wrapper || !canvasViewport) {
        return;
      }
      const boundedScale = clampZoomScale(nextScale);
      const nextEditorScale = fitScale * boundedScale;
      const centerX = (bounds.minX + bounds.maxX) / 2;
      const centerY = (bounds.minY + bounds.maxY) / 2;
      const targetScrollLeft = canvasViewport.offsetLeft + centerX * nextEditorScale - wrapper.clientWidth / 2;
      const targetScrollTop = canvasViewport.offsetTop + centerY * nextEditorScale - wrapper.clientHeight / 2;
      wrapper.scrollTo({
        left: Math.max(targetScrollLeft, 0),
        top: Math.max(targetScrollTop, 0),
        behavior: "smooth"
      });
    },
    [fitScale]
  );
  const handleZoomIn = (0, import_react.useCallback)(() => {
    store.dispatch({ type: "zoom-in" });
  }, [store]);
  const handleZoomOut = (0, import_react.useCallback)(() => {
    store.dispatch({ type: "zoom-out" });
  }, [store]);
  const handleResetZoom = (0, import_react.useCallback)(() => {
    store.dispatch({ type: "zoom-to-fit" });
  }, [store]);
  const handleZoomToFit = (0, import_react.useCallback)(() => {
    store.dispatch({ type: "zoom-to-fit" });
  }, [store]);
  const handleZoomToSelection = (0, import_react.useCallback)(() => {
    if (selectedElements.length === 0) {
      return;
    }
    let minX = Number.POSITIVE_INFINITY;
    let minY = Number.POSITIVE_INFINITY;
    let maxX = Number.NEGATIVE_INFINITY;
    let maxY = Number.NEGATIVE_INFINITY;
    selectedElements.forEach((element) => {
      minX = Math.min(minX, element.x);
      minY = Math.min(minY, element.y);
      maxX = Math.max(maxX, element.x + Math.max(element.width, MIN_ELEMENT_SIZE));
      maxY = Math.max(maxY, element.y + Math.max(element.height, MIN_ELEMENT_SIZE));
    });
    if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
      return;
    }
    const selectionBounds = { minX, minY, maxX, maxY };
    const boundsWidth = Math.max(selectionBounds.maxX - selectionBounds.minX, MIN_ELEMENT_SIZE);
    const boundsHeight = Math.max(selectionBounds.maxY - selectionBounds.minY, MIN_ELEMENT_SIZE);
    const availableWidth = Math.max(
      effectiveEditorDimensions.width - ZOOM_TO_SELECTION_PADDING,
      MIN_ELEMENT_SIZE
    );
    const availableHeight = Math.max(
      effectiveEditorDimensions.height - ZOOM_TO_SELECTION_PADDING,
      MIN_ELEMENT_SIZE
    );
    const targetEditorScale = Math.min(
      availableWidth / boundsWidth,
      availableHeight / boundsHeight
    );
    const safeFitScale = fitScale > Number.EPSILON ? fitScale : Number.EPSILON;
    const nextScale = clampZoomScale(targetEditorScale / safeFitScale);
    setScale(nextScale);
    window.requestAnimationFrame(() => {
      window.requestAnimationFrame(() => {
        centerBoundsInViewport(selectionBounds, nextScale);
      });
    });
  }, [
    centerBoundsInViewport,
    effectiveEditorDimensions.height,
    effectiveEditorDimensions.width,
    fitScale,
    selectedElements,
    setScale
  ]);
  const handleWheel = (0, import_react.useCallback)(
    (event) => {
      const mapped = mapEditingWheel(event, wheelBufferRef.current, true);
      if (mapped.intent !== "zoom") {
        return;
      }
      event.preventDefault();
      store.dispatch({
        type: "set-zoom",
        zoom: clampZoomScale(store.getState().zoom + mapped.deltaScale)
      });
    },
    [store]
  );
  (0, import_react.useEffect)(() => {
    let raf = 0;
    let attached = null;
    const attach = () => {
      const viewport = canvasViewportRef.current;
      if (!viewport) {
        raf = requestAnimationFrame(attach);
        return;
      }
      attached = viewport;
      viewport.addEventListener("wheel", handleWheel, { passive: false });
    };
    attach();
    return () => {
      cancelAnimationFrame(raf);
      attached == null ? void 0 : attached.removeEventListener("wheel", handleWheel);
    };
  }, [handleWheel]);
  const getCanvasPointFromClient = (0, import_react.useCallback)(
    (clientX, clientY) => {
      const canvasStage = canvasStageRef.current;
      if (!canvasStage) {
        return null;
      }
      const rect = canvasStage.getBoundingClientRect();
      if (rect.width <= 0 || rect.height <= 0) {
        return null;
      }
      const x = (clientX - rect.left) / editorScale;
      const y = (clientY - rect.top) / editorScale;
      return {
        x: Math.max(0, Math.min(canvasSize.width, x)),
        y: Math.max(0, Math.min(canvasSize.height, y))
      };
    },
    [canvasSize.height, canvasSize.width, editorScale]
  );
  return {
    editWrapperRef,
    canvasStageRef,
    canvasViewportRef,
    renderScaleRef,
    scale,
    setScale,
    editorDimensions,
    setEditorDimensions,
    fitScale,
    editorScale,
    handleZoomIn,
    handleZoomOut,
    handleResetZoom,
    handleZoomToFit,
    handleZoomToSelection,
    handleWheel,
    centerBoundsInViewport,
    getCanvasPointFromClient
  };
}
var activeAudio = null;
function playAnimationSound(soundUrl, loop = false) {
  stopAnimationSound();
  activeAudio = new Audio(soundUrl);
  activeAudio.loop = loop;
  activeAudio.play().catch(() => {
  });
}
function stopAnimationSound() {
  if (activeAudio) {
    activeAudio.pause();
    activeAudio.currentTime = 0;
    activeAudio = null;
  }
}
function scheduleOpeningAutoPlayGroup(controller, ctx) {
  if (!controller.hasMoreSteps()) {
    return;
  }
  const firstGroup = controller.peekNext();
  if (!(firstGroup == null ? void 0 : firstGroup.autoAdvance)) {
    return;
  }
  const timer = window.setTimeout(() => {
    const group = controller.advance();
    if (!group) {
      return;
    }
    playGroup(controller, group, ctx);
    scheduleAutoAdvanceChain(controller, ctx);
  }, firstGroup.autoAdvanceDelayMs ?? 0);
  ctx.timers.push(timer);
}
function scheduleEntranceAnimationTimers(slide, setPresentationAnimations, presentationTimersRef) {
  const entranceAnimations = [...slide.animations || []].filter((animation) => Boolean(animation.entrance)).sort(
    (left, right) => (left.order || Number.MAX_SAFE_INTEGER) - (right.order || Number.MAX_SAFE_INTEGER)
  );
  if (entranceAnimations.length === 0) {
    return;
  }
  setPresentationAnimations(
    entranceAnimations.map((animation) => ({
      elementId: animation.elementId,
      state: "hidden",
      animation
    }))
  );
  entranceAnimations.forEach((animation, animationIndex) => {
    const delay = computeEntranceAnimationDelay(animation.delayMs, animationIndex);
    const timer = window.setTimeout(() => {
      setPresentationAnimations(
        (previousAnimations) => previousAnimations.map(
          (entry) => entry.elementId === animation.elementId ? { ...entry, state: "visible" } : entry
        )
      );
    }, delay);
    presentationTimersRef.current.push(timer);
  });
}
function useAnimationPlayback(input) {
  const { slides, onPlayActionSound, showWithAnimation, canvasSize, themeColorMap } = input;
  const animationsEnabled = showWithAnimation !== false;
  const [presentationAnimations, setPresentationAnimations] = (0, import_react.useState)([]);
  const [presentationElementStates, setPresentationElementStates] = (0, import_react.useState)(/* @__PURE__ */ new Map());
  const [presentationKeyframesCss, setPresentationKeyframesCss] = (0, import_react.useState)("");
  const [interactiveTriggerShapeIds, setInteractiveTriggerShapeIds] = (0, import_react.useState)(
    /* @__PURE__ */ new Set()
  );
  const [hoverTriggerShapeIds, setHoverTriggerShapeIds] = (0, import_react.useState)(/* @__PURE__ */ new Set());
  const presentationTimersRef = (0, import_react.useRef)([]);
  const controllerRef = (0, import_react.useRef)(null);
  const buildRafRef = (0, import_react.useRef)(null);
  const activeAnimationGroupRef = (0, import_react.useRef)(createActiveAnimationGroup());
  const seededCompletedRef = (0, import_react.useRef)(false);
  const mediaTimeNodeElementIdsRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
  const lastSeedCompletedRef = (0, import_react.useRef)(false);
  const buildPlaybackContext = (0, import_react.useCallback)(() => {
    return {
      setStates: setPresentationElementStates,
      timers: presentationTimersRef.current,
      buildHandle: buildRafRef,
      onPlayActionSound,
      playSound: playAnimationSound,
      stopSound: stopAnimationSound,
      mediaTimeNodeElementIds: mediaTimeNodeElementIdsRef.current
    };
  }, [onPlayActionSound]);
  (0, import_react.useEffect)(() => {
    return () => {
      cancelBuildReveal(buildRafRef);
    };
  }, []);
  const clearPresentationTimers = (0, import_react.useCallback)(() => {
    clearPlaybackTimers(buildPlaybackContext(), activeAnimationGroupRef.current);
  }, [buildPlaybackContext]);
  const resetSlideTimeline = (0, import_react.useCallback)(
    (slideIndex) => {
      cancelBuildReveal(buildRafRef);
      const slide = slides[slideIndex];
      if (!slide) {
        controllerRef.current = null;
        mediaTimeNodeElementIdsRef.current = /* @__PURE__ */ new Map();
        setPresentationElementStates(/* @__PURE__ */ new Map());
        setPresentationKeyframesCss("");
        setInteractiveTriggerShapeIds(/* @__PURE__ */ new Set());
        setHoverTriggerShapeIds(/* @__PURE__ */ new Set());
        return;
      }
      const controller = PresentationAnimationController.fromSlide(slide, {
        slideHeightPx: canvasSize == null ? void 0 : canvasSize.height,
        slideWidthPx: canvasSize == null ? void 0 : canvasSize.width,
        themeColorMap
      });
      controllerRef.current = controller;
      mediaTimeNodeElementIdsRef.current = resolveMediaTimeNodeElementIds(
        slide.nativeAnimations ?? []
      );
      setPresentationKeyframesCss(controller.keyframesCss);
      setInteractiveTriggerShapeIds(controller.interactiveTriggerShapeIds);
      setHoverTriggerShapeIds(controller.hoverTriggerShapeIds);
      setPresentationElementStates(controller.computeStates());
    },
    [slides, canvasSize, themeColorMap]
  );
  const playNextAnimationGroup = (0, import_react.useCallback)(() => {
    if (!animationsEnabled) {
      return false;
    }
    return advanceMainSequence(
      controllerRef.current,
      buildPlaybackContext(),
      activeAnimationGroupRef.current
    );
  }, [animationsEnabled, buildPlaybackContext]);
  const handleInteractiveShapeClick = (0, import_react.useCallback)(
    (shapeId) => {
      const controller = controllerRef.current;
      if (!controller || !controller.hasInteractiveSequence(shapeId)) {
        return false;
      }
      const group = controller.advanceInteractive(shapeId);
      if (!group) {
        return false;
      }
      playGroup(controller, group, buildPlaybackContext());
      return true;
    },
    [buildPlaybackContext]
  );
  const handleHoverStart = (0, import_react.useCallback)(
    (shapeId) => {
      if (!animationsEnabled) {
        return false;
      }
      const controller = controllerRef.current;
      if (!controller || !controller.hasHoverSequence(shapeId)) {
        return false;
      }
      controller.resetHover(shapeId);
      const group = controller.advanceHover(shapeId);
      if (!group) {
        return false;
      }
      playGroup(controller, group, buildPlaybackContext());
      return true;
    },
    [animationsEnabled, buildPlaybackContext]
  );
  const handleHoverEnd = (0, import_react.useCallback)((shapeId) => {
    const controller = controllerRef.current;
    if (!controller || !controller.hasHoverSequence(shapeId)) {
      return;
    }
    controller.resetHover(shapeId);
  }, []);
  const seedSlideAnimations = (0, import_react.useCallback)(
    (slideIndex, options) => {
      clearPresentationTimers();
      setPresentationAnimations([]);
      if (!animationsEnabled) {
        controllerRef.current = null;
        setPresentationElementStates(/* @__PURE__ */ new Map());
        setPresentationKeyframesCss("");
        setInteractiveTriggerShapeIds(/* @__PURE__ */ new Set());
        setHoverTriggerShapeIds(/* @__PURE__ */ new Set());
        return;
      }
      resetSlideTimeline(slideIndex);
      seededCompletedRef.current = false;
      lastSeedCompletedRef.current = (options == null ? void 0 : options.completed) === true;
      if (options == null ? void 0 : options.completed) {
        const seeded = controllerRef.current;
        if (seeded) {
          seededCompletedRef.current = seeded.hasMoreSteps();
          seeded.completeAll();
          setPresentationElementStates(seeded.computeStates());
        }
      }
    },
    [animationsEnabled, clearPresentationTimers, resetSlideTimeline]
  );
  const startSlideAnimations = (0, import_react.useCallback)(
    (slideIndex) => {
      if (!animationsEnabled || lastSeedCompletedRef.current) {
        return;
      }
      const slide = slides[slideIndex];
      if (!slide) {
        return;
      }
      const controller = controllerRef.current;
      if (controller) {
        const ctx = buildPlaybackContext();
        scheduleOpeningAutoPlayGroup(controller, ctx);
      }
      scheduleEntranceAnimationTimers(slide, setPresentationAnimations, presentationTimersRef);
    },
    [animationsEnabled, slides, buildPlaybackContext]
  );
  const runPresentationEntranceAnimations = (0, import_react.useCallback)(
    (slideIndex, options) => {
      seedSlideAnimations(slideIndex, options);
      startSlideAnimations(slideIndex);
    },
    [seedSlideAnimations, startSlideAnimations]
  );
  return {
    presentationAnimations,
    presentationElementStates,
    presentationKeyframesCss,
    interactiveTriggerShapeIds,
    hoverTriggerShapeIds,
    clearPresentationTimers,
    playNextAnimationGroup,
    handleInteractiveShapeClick,
    handleHoverStart,
    handleHoverEnd,
    runPresentationEntranceAnimations,
    seedSlideAnimations,
    startSlideAnimations,
    isSeededCompleted: () => seededCompletedRef.current,
    presentationTimersRef
  };
}
var PRESENTER_CHANNEL_NAME = PRESENTATION_CHANNEL_NAME;
var AUDIENCE_HASH = PRESENTATION_HASH;
var PRESENTER_MSG_ORIGIN = PRESENTATION_MESSAGE_ORIGIN;
var AUDIENCE_NONCE_KEY = PRESENTATION_NONCE_KEY;
function generateSessionId() {
  return createPresentationSessionId();
}
function parseAudienceNonce() {
  const hash = window.location.hash;
  if (!hash.startsWith(AUDIENCE_HASH)) {
    return null;
  }
  const trailing = hash.slice(AUDIENCE_HASH.length);
  if (!trailing) {
    return null;
  }
  const params = new URLSearchParams(trailing.replace(/^[&;?]/u, ""));
  return params.get(AUDIENCE_NONCE_KEY);
}
function isAudienceTab() {
  return window.location.hash.startsWith(AUDIENCE_HASH);
}
function usePresenterWindow(input) {
  const { isPresenterMode, content, snapshot } = input;
  const audienceWindowRef = (0, import_react.useRef)(null);
  const channelRef = (0, import_react.useRef)(null);
  const pollTimerRef = (0, import_react.useRef)(null);
  const sessionIdRef = (0, import_react.useRef)("");
  const snapshotRef = (0, import_react.useRef)(snapshot);
  snapshotRef.current = snapshot;
  const getChannel = (0, import_react.useCallback)(() => {
    if (!channelRef.current) {
      channelRef.current = new BroadcastChannel(PRESENTER_CHANNEL_NAME);
    }
    return channelRef.current;
  }, []);
  const isAudienceWindowOpen = (0, import_react.useCallback)(() => {
    return audienceWindowRef.current !== null && !audienceWindowRef.current.closed;
  }, []);
  const swapDisplays = (0, import_react.useCallback)(async () => {
    const audience = audienceWindowRef.current;
    return audience && !audience.closed ? swapPresentationWindows(window, audience) : false;
  }, []);
  const syncSlideToAudience = (0, import_react.useCallback)(
    (slideIndex) => {
      if (!sessionIdRef.current) {
        return;
      }
      const msg = {
        origin: PRESENTER_MSG_ORIGIN,
        type: "presenter-slide-change",
        slideIndex,
        sessionId: sessionIdRef.current
      };
      try {
        getChannel().postMessage(msg);
      } catch {
      }
    },
    [getChannel]
  );
  const syncStateToAudience = (0, import_react.useCallback)(
    (nextSnapshot) => {
      if (!sessionIdRef.current) {
        return;
      }
      try {
        getChannel().postMessage({
          origin: PRESENTER_MSG_ORIGIN,
          type: "presenter-state",
          sessionId: sessionIdRef.current,
          snapshot: nextSnapshot
        });
      } catch {
      }
    },
    [getChannel]
  );
  const closeAudienceWindow = (0, import_react.useCallback)(() => {
    const closingSessionId = sessionIdRef.current;
    if (sessionIdRef.current) {
      try {
        const exitMsg = {
          origin: PRESENTER_MSG_ORIGIN,
          type: "presenter-exit",
          sessionId: sessionIdRef.current
        };
        getChannel().postMessage(exitMsg);
      } catch {
      }
    }
    const win = audienceWindowRef.current;
    if (win && !win.closed) {
      try {
        win.close();
      } catch {
      }
    }
    audienceWindowRef.current = null;
    sessionIdRef.current = "";
    if (pollTimerRef.current !== null) {
      clearInterval(pollTimerRef.current);
      pollTimerRef.current = null;
    }
    if (closingSessionId) {
      void clearAudienceContent(closingSessionId);
    }
  }, [getChannel]);
  const openAudienceWindow = (0, import_react.useCallback)(() => {
    if (isAudienceWindowOpen()) {
      closeAudienceWindow();
    }
    const blankWin = window.open(
      "about:blank",
      "pptx-viewer-audience",
      "popup=yes,width=1280,height=720"
    );
    if (!blankWin) {
      return false;
    }
    audienceWindowRef.current = blankWin;
    const sessionId = generateSessionId();
    sessionIdRef.current = sessionId;
    const audienceUrl = buildPresentationAudienceUrl(window.location.href, sessionId);
    void resolveAudienceScreenPlacement(window).then((placement) => {
      if (placement && audienceWindowRef.current === blankWin && !blankWin.closed) {
        placeAudienceWindow(blankWin, placement);
      }
      return void 0;
    });
    const navigateOrClose = (ok) => {
      const win = audienceWindowRef.current;
      if (!win || win.closed) {
        return;
      }
      if (!ok) {
        try {
          win.close();
        } catch {
        }
        audienceWindowRef.current = null;
        sessionIdRef.current = "";
        return;
      }
      try {
        win.location.replace(audienceUrl);
      } catch {
        try {
          win.close();
        } catch {
        }
        audienceWindowRef.current = null;
        sessionIdRef.current = "";
      }
    };
    if (content) {
      void storeAudienceContent(content, sessionId).then(() => navigateOrClose(true)).catch(() => navigateOrClose(false));
    } else {
      navigateOrClose(true);
    }
    pollTimerRef.current = setInterval(() => {
      const win = audienceWindowRef.current;
      if (!win || win.closed) {
        audienceWindowRef.current = null;
        sessionIdRef.current = "";
        if (pollTimerRef.current !== null) {
          clearInterval(pollTimerRef.current);
          pollTimerRef.current = null;
        }
      }
    }, 1e3);
    return true;
  }, [isAudienceWindowOpen, closeAudienceWindow, content]);
  (0, import_react.useEffect)(() => {
    let channel;
    try {
      channel = getChannel();
    } catch {
      return;
    }
    const handleMessage = (event) => {
      const message = event.data;
      if (isPresentationSessionMessage(message) && message.type === "audience-ready" && message.sessionId === sessionIdRef.current) {
        syncStateToAudience(snapshotRef.current);
      }
    };
    channel.addEventListener("message", handleMessage);
    return () => channel.removeEventListener("message", handleMessage);
  }, [getChannel, syncStateToAudience]);
  (0, import_react.useEffect)(() => {
    if (isPresenterMode && isAudienceWindowOpen()) {
      syncStateToAudience(snapshot);
    }
  }, [snapshot, isPresenterMode, isAudienceWindowOpen, syncStateToAudience]);
  (0, import_react.useEffect)(() => {
    return () => {
      var _a;
      closeAudienceWindow();
      try {
        (_a = channelRef.current) == null ? void 0 : _a.close();
      } catch {
      }
    };
  }, [closeAudienceWindow]);
  (0, import_react.useEffect)(() => {
    if (!isPresenterMode) {
      closeAudienceWindow();
    }
  }, [isPresenterMode, closeAudienceWindow]);
  (0, import_react.useEffect)(() => {
    const handleBeforeUnload = () => {
      if (sessionIdRef.current) {
        void clearAudienceContent(sessionIdRef.current);
      }
    };
    window.addEventListener("beforeunload", handleBeforeUnload);
    return () => window.removeEventListener("beforeunload", handleBeforeUnload);
  }, []);
  return {
    openAudienceWindow,
    closeAudienceWindow,
    isAudienceWindowOpen,
    syncSlideToAudience,
    syncStateToAudience,
    swapDisplays
  };
}
function useAudienceMode(input) {
  const {
    mode: _mode,
    onSetMode,
    onSetActiveSlideIndex,
    containerRef,
    onSnapshot,
    onPresenterEnded
  } = input;
  const initialised = (0, import_react.useRef)(false);
  (0, import_react.useEffect)(() => {
    if (!isAudienceTab()) {
      return;
    }
    if (initialised.current) {
      return;
    }
    initialised.current = true;
    onSetMode("present");
    const requestAudienceFullscreen = () => {
      const wrapper = containerRef.current;
      if (!document.fullscreenElement && (wrapper == null ? void 0 : wrapper.requestFullscreen)) {
        void wrapper.requestFullscreen().catch(() => void 0);
      }
    };
    document.addEventListener("pointerdown", requestAudienceFullscreen, { once: true });
    document.addEventListener("keydown", requestAudienceFullscreen, { once: true });
    return () => {
      document.removeEventListener("pointerdown", requestAudienceFullscreen);
      document.removeEventListener("keydown", requestAudienceFullscreen);
    };
  }, [containerRef, onSetMode]);
  const handlersRef = (0, import_react.useRef)({ onSetActiveSlideIndex, onSnapshot, onPresenterEnded });
  handlersRef.current = { onSetActiveSlideIndex, onSnapshot, onPresenterEnded };
  (0, import_react.useEffect)(() => {
    if (!isAudienceTab()) {
      return;
    }
    const expectedSessionId = parseAudienceNonce();
    let channel;
    try {
      channel = new BroadcastChannel(PRESENTER_CHANNEL_NAME);
    } catch {
      return;
    }
    channel.onmessage = (event) => {
      var _a, _b2;
      const data = event.data;
      if (!isPresentationSessionMessage(data) || data.origin !== PRESENTER_MSG_ORIGIN) {
        return;
      }
      if (expectedSessionId && data.sessionId !== expectedSessionId) {
        return;
      }
      const handlers = handlersRef.current;
      if (data.type === "presenter-slide-change") {
        handlers.onSetActiveSlideIndex(data.slideIndex);
      }
      if (data.type === "presenter-state") {
        handlers.onSetActiveSlideIndex(data.snapshot.slideIndex);
        (_a = handlers.onSnapshot) == null ? void 0 : _a.call(handlers, data.snapshot);
      }
      if (data.type === "presenter-exit") {
        if (expectedSessionId) {
          void clearAudienceContent(expectedSessionId);
        }
        if (endAudienceDisplay(window)) {
          (_b2 = handlers.onPresenterEnded) == null ? void 0 : _b2.call(handlers);
        }
      }
    };
    if (expectedSessionId) {
      channel.postMessage({
        origin: PRESENTATION_MESSAGE_ORIGIN,
        type: "audience-ready",
        sessionId: expectedSessionId
      });
    }
    return () => {
      try {
        channel.close();
      } catch {
      }
    };
  }, []);
}
function usePresentationKeyboard(input) {
  const wheelBufferRef = (0, import_react.useRef)(createWheelStepBuffer());
  const {
    mode,
    movePresentationSlide,
    navigateToSlide,
    slideCount,
    showSlideIndexes,
    onSetMode,
    onSetPointerTool,
    onEraseAnnotations,
    onToggleInkMarkup,
    onToggleToolbar,
    onShowAllSlides,
    onToggleSubtitles,
    onToggleBlackScreen,
    onToggleWhiteScreen,
    rehearsing,
    recordCurrentSlideTime,
    presentationSlideIndex,
    setShowRehearsalSummary
  } = input;
  const keyBufferRef = (0, import_react.useRef)(createPresentationKeyBuffer());
  (0, import_react.useEffect)(() => {
    if (mode !== "present" || !acceptsPresentationInput()) {
      keyBufferRef.current = createPresentationKeyBuffer();
      return;
    }
    const handleKeyDown = (event) => {
      const mapped = mapPresentationKey(event, keyBufferRef.current);
      if (mapped.action === "none") {
        return;
      }
      event.preventDefault();
      switch (mapped.action) {
        case "end":
          if (rehearsing) {
            recordCurrentSlideTime(presentationSlideIndex);
            setShowRehearsalSummary(true);
          }
          onSetMode("edit");
          return;
        case "next":
          movePresentationSlide(1);
          return;
        case "previous":
          movePresentationSlide(-1);
          return;
        case "first": {
          const first = firstShowSlideIndex(showSlideIndexes);
          navigateToSlide(first ?? 0);
          return;
        }
        case "last": {
          const last = lastShowSlideIndex(showSlideIndexes);
          navigateToSlide(last ?? Math.max(0, slideCount - 1));
          return;
        }
        case "goto": {
          const index = mapped.slideNumber - 1;
          if (index >= 0 && index < slideCount) {
            navigateToSlide(index);
          }
          return;
        }
        case "pointerTool":
          onSetPointerTool == null ? void 0 : onSetPointerTool(mapped.tool);
          return;
        case "eraseAnnotations":
          onEraseAnnotations == null ? void 0 : onEraseAnnotations();
          return;
        case "toggleInkMarkup":
          onToggleInkMarkup == null ? void 0 : onToggleInkMarkup();
          return;
        case "toggleChrome":
          onToggleToolbar == null ? void 0 : onToggleToolbar();
          return;
        case "showAllSlides":
          onShowAllSlides == null ? void 0 : onShowAllSlides();
          return;
        case "toggleSubtitles":
          onToggleSubtitles == null ? void 0 : onToggleSubtitles();
          return;
        case "toggleBlackScreen":
          onToggleBlackScreen == null ? void 0 : onToggleBlackScreen();
          return;
        case "toggleWhiteScreen":
          onToggleWhiteScreen == null ? void 0 : onToggleWhiteScreen();
          break;
      }
    };
    const handleWheel = (event) => {
      if (mode !== "present" || !acceptsPresentationInput()) {
        return;
      }
      const mapped = mapPresentationWheel(event, wheelBufferRef.current);
      if (mapped.intent === "next-slide") {
        event.preventDefault();
        movePresentationSlide(1);
      } else if (mapped.intent === "previous-slide") {
        event.preventDefault();
        movePresentationSlide(-1);
      }
    };
    window.addEventListener("keydown", handleKeyDown);
    window.addEventListener("wheel", handleWheel, { passive: false });
    return () => {
      window.removeEventListener("keydown", handleKeyDown);
      window.removeEventListener("wheel", handleWheel);
    };
  }, [
    mode,
    movePresentationSlide,
    navigateToSlide,
    slideCount,
    showSlideIndexes,
    onSetMode,
    onSetPointerTool,
    onEraseAnnotations,
    onToggleInkMarkup,
    onToggleToolbar,
    onShowAllSlides,
    onToggleSubtitles,
    onToggleBlackScreen,
    onToggleWhiteScreen,
    rehearsing,
    recordCurrentSlideTime,
    presentationSlideIndex,
    setShowRehearsalSummary
  ]);
}
function useRehearsalTimings(input) {
  const {
    containerRef,
    onSetMode,
    onSaveRehearsalTimings,
    setPresentationStartTime,
    setPresenterMode
  } = input;
  const [rehearsing, setRehearsing] = (0, import_react.useState)(false);
  const [recordedTimings, setRecordedTimings] = (0, import_react.useState)({});
  const [slideStartTime, setSlideStartTime] = (0, import_react.useState)(null);
  const [showRehearsalSummary, setShowRehearsalSummary] = (0, import_react.useState)(false);
  const [rehearsalPaused, setRehearsalPaused] = (0, import_react.useState)(false);
  const pauseAccumulatedRef = (0, import_react.useRef)(0);
  const pauseStartRef = (0, import_react.useRef)(null);
  const recordCurrentSlideTime = (0, import_react.useCallback)(
    (slideIdx) => {
      if (slideStartTime === null) {
        return;
      }
      let pausedMs = pauseAccumulatedRef.current;
      if (pauseStartRef.current !== null) {
        pausedMs += Date.now() - pauseStartRef.current;
      }
      const elapsed = Date.now() - slideStartTime - pausedMs;
      setRecordedTimings((prev) => ({
        ...prev,
        [slideIdx]: Math.max(0, elapsed)
      }));
      pauseAccumulatedRef.current = 0;
      pauseStartRef.current = rehearsalPaused ? Date.now() : null;
      setSlideStartTime(Date.now());
    },
    [slideStartTime, rehearsalPaused]
  );
  const dismissRehearsalSummary = (0, import_react.useCallback)(() => {
    setShowRehearsalSummary(false);
    setRehearsing(false);
    setRecordedTimings({});
  }, []);
  const saveRehearsalTimings = (0, import_react.useCallback)(() => {
    onSaveRehearsalTimings == null ? void 0 : onSaveRehearsalTimings(recordedTimings);
    setShowRehearsalSummary(false);
    setRehearsing(false);
  }, [onSaveRehearsalTimings, recordedTimings]);
  const enterRehearsalMode = (0, import_react.useCallback)(() => {
    setPresenterMode(false);
    setRehearsing(true);
    setRecordedTimings({});
    setRehearsalPaused(false);
    setShowRehearsalSummary(false);
    pauseAccumulatedRef.current = 0;
    pauseStartRef.current = null;
    const now = Date.now();
    setPresentationStartTime(now);
    setSlideStartTime(now);
    try {
      const wrapper = containerRef.current;
      if (wrapper && typeof wrapper.requestFullscreen === "function") {
        void wrapper.requestFullscreen().catch(() => {
        });
      }
    } catch {
    }
    onSetMode("present");
  }, [containerRef, onSetMode, setPresentationStartTime, setPresenterMode]);
  const toggleRehearsalPause = (0, import_react.useCallback)(() => {
    setRehearsalPaused((prev) => {
      if (prev) {
        if (pauseStartRef.current !== null) {
          pauseAccumulatedRef.current += Date.now() - pauseStartRef.current;
          pauseStartRef.current = null;
        }
      } else {
        pauseStartRef.current = Date.now();
      }
      return !prev;
    });
  }, []);
  return {
    rehearsing,
    setRehearsing,
    recordedTimings,
    slideStartTime,
    showRehearsalSummary,
    setShowRehearsalSummary,
    rehearsalPaused,
    recordCurrentSlideTime,
    dismissRehearsalSummary,
    saveRehearsalTimings,
    enterRehearsalMode,
    toggleRehearsalPause
  };
}
function handlePresentationActionImpl(action, deps) {
  runPresentationAction(
    action,
    { slideCount: deps.slidesLength, ...deps.elementId ? { elementId: deps.elementId } : {} },
    {
      goToSlide: (slideIndex) => {
        deps.navigateToSlide(slideIndex);
      },
      move: (direction) => {
        deps.movePresentationSlide(direction);
      },
      endShow: () => {
        deps.onSetMode("edit");
      },
      playSound: deps.onPlayActionSound ? (soundPath) => {
        var _a;
        return (_a = deps.onPlayActionSound) == null ? void 0 : _a.call(deps, soundPath);
      } : void 0,
      lastViewed: deps.onLastViewed,
      customShow: deps.onCustomShow,
      openFile: deps.onOpenFile,
      openPresentation: deps.onOpenPresentation,
      playMedia: deps.onPlayMedia,
      oleVerb: deps.onOleVerb
    }
  );
}
function executeSlideTransition(nextSlideIndex, deps) {
  var _a, _b2;
  const incomingSlide = deps.slides[nextSlideIndex];
  const transition = deps.reverse ? (_a = deps.slides[deps.currentSlideIndex]) == null ? void 0 : _a.transition : incomingSlide == null ? void 0 : incomingSlide.transition;
  const durationMs = deps.playTransition ? resolveTransitionDurationMs(transition) : 0;
  deps.clearPresentationTimers();
  deps.setPresentationSlideIndex(nextSlideIndex);
  deps.onSetActiveSlideIndex(nextSlideIndex);
  deps.setPresentationSlideVisible(true);
  deps.seedSlideAnimations(nextSlideIndex, { completed: deps.seedCompleted });
  const soundAction = resolveTransitionSoundAction(transition);
  if (soundAction.kind === "play" && deps.onPlayActionSound) {
    deps.onPlayActionSound(soundAction.soundPath, { loop: soundAction.loop });
  } else if (soundAction.kind === "stop" && deps.onStopActionSound) {
    deps.onStopActionSound();
  }
  if (durationMs > 0 && transition) {
    deps.setTransitionOverlay({
      outgoingSlideIndex: deps.currentSlideIndex,
      incomingSlideIndex: nextSlideIndex,
      transition,
      durationMs
    });
    const timer = window.setTimeout(() => {
      var _a2;
      deps.startSlideAnimations(nextSlideIndex);
      (_a2 = deps.scheduleAutoAdvanceForSlide) == null ? void 0 : _a2.call(deps, nextSlideIndex);
    }, durationMs);
    deps.presentationTimersRef.current.push(timer);
    return;
  }
  deps.setTransitionOverlay(null);
  deps.startSlideAnimations(nextSlideIndex);
  (_b2 = deps.scheduleAutoAdvanceForSlide) == null ? void 0 : _b2.call(deps, nextSlideIndex);
}
function isClickAdvanceBlocked(slide, direction, trigger) {
  var _a;
  return trigger === "click" && direction === 1 && ((_a = slide == null ? void 0 : slide.transition) == null ? void 0 : _a.advanceOnClick) === false;
}
function useSlideNavigation(input) {
  const {
    slides,
    visibleSlideIndexes,
    presentationSlideIndex,
    setPresentationSlideIndex,
    setPresentationSlideVisible,
    setTransitionOverlay,
    onSetMode,
    onSetActiveSlideIndex,
    onPlayActionSound,
    onStopActionSound,
    loopContinuously,
    useTimings,
    onAdvancePastLastSlide,
    playNextAnimationGroup,
    clearPresentationTimers,
    seedSlideAnimations,
    startSlideAnimations,
    presentationTimersRef,
    rehearsing,
    recordCurrentSlideTime,
    setShowRehearsalSummary,
    onLastViewed,
    onCustomShow,
    onOpenFile,
    onOpenPresentation,
    onPlayMedia,
    onOleVerb
  } = input;
  const movePresentationSlideRef = (0, import_react.useRef)(() => {
  });
  const scheduleAutoAdvanceForSlide = (0, import_react.useCallback)(
    (slideIndex) => {
      const delayMs = resolveAutoAdvanceDelayMs(slides[slideIndex], { useTimings });
      if (delayMs === void 0) {
        return;
      }
      const timer = window.setTimeout(() => {
        movePresentationSlideRef.current(1);
      }, delayMs);
      presentationTimersRef.current.push(timer);
    },
    [slides, presentationTimersRef, useTimings]
  );
  const movePresentationSlide = (0, import_react.useCallback)(
    (direction, trigger = "explicit") => {
      if (direction === 1 && playNextAnimationGroup()) {
        return;
      }
      if (isClickAdvanceBlocked(slides[presentationSlideIndex], direction, trigger)) {
        return;
      }
      const availableSlideIndexes = visibleSlideIndexes.length > 0 ? visibleSlideIndexes : slides.map((_slide, slideIndex) => slideIndex);
      if (availableSlideIndexes.length === 0) {
        return;
      }
      const pastLastSlide = direction === 1 && !hasShowSlideAfter(presentationSlideIndex, availableSlideIndexes);
      if (rehearsing && pastLastSlide) {
        recordCurrentSlideTime(presentationSlideIndex);
        try {
          if (document.fullscreenElement) {
            void document.exitFullscreen().catch(() => {
            });
          }
        } catch {
        }
        onSetMode("edit");
        setShowRehearsalSummary(true);
        return;
      }
      if (!loopContinuously && !rehearsing && pastLastSlide && onAdvancePastLastSlide) {
        onAdvancePastLastSlide();
        return;
      }
      const nextSlideIndex = direction === 1 ? nextShowSlideIndex(presentationSlideIndex, availableSlideIndexes, {
        loop: Boolean(loopContinuously) && !rehearsing
      }) : previousShowSlideIndex(presentationSlideIndex, availableSlideIndexes);
      if (nextSlideIndex === void 0 || nextSlideIndex === presentationSlideIndex) {
        return;
      }
      if (rehearsing && direction === 1) {
        recordCurrentSlideTime(presentationSlideIndex);
      }
      executeSlideTransition(nextSlideIndex, {
        slides,
        currentSlideIndex: presentationSlideIndex,
        onPlayActionSound,
        onStopActionSound,
        setPresentationSlideVisible,
        clearPresentationTimers,
        setPresentationSlideIndex,
        onSetActiveSlideIndex,
        seedSlideAnimations,
        startSlideAnimations,
        scheduleAutoAdvanceForSlide: rehearsing ? void 0 : scheduleAutoAdvanceForSlide,
        presentationTimersRef,
        setTransitionOverlay,
        // PowerPoint plays a slide's transition only when advancing into it,
        // and replays the LEAVING slide's transition in reverse when stepping
        // back (a morph glides its shapes back to where they came from).
        playTransition: true,
        reverse: direction === -1,
        // Stepping back onto a slide shows it with its builds already played.
        seedCompleted: direction === -1
      });
    },
    [
      clearPresentationTimers,
      loopContinuously,
      onAdvancePastLastSlide,
      onPlayActionSound,
      onStopActionSound,
      onSetActiveSlideIndex,
      onSetMode,
      playNextAnimationGroup,
      presentationSlideIndex,
      presentationTimersRef,
      recordCurrentSlideTime,
      rehearsing,
      seedSlideAnimations,
      startSlideAnimations,
      scheduleAutoAdvanceForSlide,
      setShowRehearsalSummary,
      slides,
      visibleSlideIndexes,
      setPresentationSlideVisible,
      setPresentationSlideIndex,
      setTransitionOverlay
    ]
  );
  movePresentationSlideRef.current = movePresentationSlide;
  const navigateToSlide = (0, import_react.useCallback)(
    (targetIndex) => {
      if (targetIndex < 0 || targetIndex >= slides.length) {
        return;
      }
      if (targetIndex === presentationSlideIndex) {
        return;
      }
      executeSlideTransition(targetIndex, {
        slides,
        currentSlideIndex: presentationSlideIndex,
        onPlayActionSound,
        onStopActionSound,
        setPresentationSlideVisible,
        clearPresentationTimers,
        setPresentationSlideIndex,
        onSetActiveSlideIndex,
        seedSlideAnimations,
        startSlideAnimations,
        scheduleAutoAdvanceForSlide,
        presentationTimersRef,
        setTransitionOverlay,
        // A jump ENTERS the target slide, so PowerPoint plays that slide's
        // transition exactly as it does for a forward advance - hyperlinks,
        // action buttons, Home/End and "type a number + Enter" all animate.
        // Suppressing it here is why a deck navigated by clicking its own
        // on-slide links (this is how the issue #131 reporter drives their
        // wheel menu) appeared to have no morph at all, while the same
        // transition played fine on PageDown.
        playTransition: true
      });
    },
    [
      clearPresentationTimers,
      onPlayActionSound,
      onStopActionSound,
      onSetActiveSlideIndex,
      presentationSlideIndex,
      presentationTimersRef,
      seedSlideAnimations,
      startSlideAnimations,
      scheduleAutoAdvanceForSlide,
      slides,
      setPresentationSlideIndex,
      setPresentationSlideVisible,
      setTransitionOverlay
    ]
  );
  const handlePresentationAction = (0, import_react.useCallback)(
    (action, elementId) => {
      handlePresentationActionImpl(action, {
        elementId,
        movePresentationSlide,
        navigateToSlide,
        onPlayActionSound,
        onSetMode,
        slidesLength: slides.length,
        onLastViewed,
        onCustomShow,
        onOpenFile,
        onOpenPresentation,
        onPlayMedia,
        onOleVerb
      });
    },
    [
      movePresentationSlide,
      navigateToSlide,
      onPlayActionSound,
      onSetMode,
      slides.length,
      onLastViewed,
      onCustomShow,
      onOpenFile,
      onOpenPresentation,
      onPlayMedia,
      onOleVerb
    ]
  );
  return {
    movePresentationSlide,
    navigateToSlide,
    handlePresentationAction,
    scheduleAutoAdvanceForSlide
  };
}
function useZoomNavigation(input) {
  const { navigateToSlide } = input;
  const zoomReturnSlideIndex = (0, import_react.useRef)(null);
  const handleZoomClick = (0, import_react.useCallback)(
    (targetSlideIndex, returnSlideIndex) => {
      zoomReturnSlideIndex.current = returnSlideIndex;
      navigateToSlide(targetSlideIndex);
    },
    [navigateToSlide]
  );
  const returnToZoomSlide = (0, import_react.useCallback)(() => {
    const returnIndex = zoomReturnSlideIndex.current;
    if (returnIndex === null) {
      return false;
    }
    zoomReturnSlideIndex.current = null;
    navigateToSlide(returnIndex);
    return true;
  }, [navigateToSlide]);
  const clearZoomReturn = (0, import_react.useCallback)(() => {
    zoomReturnSlideIndex.current = null;
  }, []);
  return {
    handleZoomClick,
    zoomReturnSlideIndex,
    returnToZoomSlide,
    clearZoomReturn
  };
}
function createCustomShowRunner(deps) {
  let origin = null;
  return {
    runCustomShow(customShowId, returnAfter) {
      const target = deps.getCustomShows().find((show) => show.id === customShowId);
      if (!target) {
        return;
      }
      origin = returnAfter ? { showId: deps.getActiveCustomShowId(), slideIndex: deps.getPresentationSlideIndex() } : null;
      const first = firstShowSlideIndex(resolveShowSlideIndexes(deps.getSlides(), target));
      deps.setActiveCustomShowId(customShowId);
      if (first !== void 0) {
        deps.navigateToSlide(first);
      }
    },
    tryReturnFromCustomShow() {
      if (!origin) {
        return false;
      }
      const { showId, slideIndex } = origin;
      origin = null;
      deps.setActiveCustomShowId(showId);
      deps.navigateToSlide(slideIndex);
      return true;
    }
  };
}
function useCustomShowRunner(deps) {
  const depsRef = (0, import_react.useRef)(deps);
  depsRef.current = deps;
  const runnerRef = (0, import_react.useRef)(null);
  runnerRef.current ?? (runnerRef.current = createCustomShowRunner({
    getSlides: () => depsRef.current.getSlides(),
    getCustomShows: () => depsRef.current.getCustomShows(),
    getActiveCustomShowId: () => depsRef.current.getActiveCustomShowId(),
    setActiveCustomShowId: (id) => depsRef.current.setActiveCustomShowId(id),
    navigateToSlide: (index) => depsRef.current.navigateToSlide(index),
    getPresentationSlideIndex: () => depsRef.current.getPresentationSlideIndex()
  }));
  return runnerRef.current;
}
function usePresentationActionExtensions(input) {
  const {
    slides,
    customShows,
    activeCustomShowId,
    onSetActiveCustomShowId,
    presentationSlideIndex,
    containerRef,
    endWithBlackSlide,
    onSetMode,
    setEndOfShowVisible
  } = input;
  const navigateToSlideRef = (0, import_react.useRef)(null);
  const navigateToSlideViaRef = (0, import_react.useCallback)((index) => {
    var _a;
    (_a = navigateToSlideRef.current) == null ? void 0 : _a.call(navigateToSlideRef, index);
  }, []);
  const customShowRunner = useCustomShowRunner({
    getSlides: () => slides,
    getCustomShows: () => customShows,
    getActiveCustomShowId: () => activeCustomShowId,
    setActiveCustomShowId: (id) => onSetActiveCustomShowId == null ? void 0 : onSetActiveCustomShowId(id),
    navigateToSlide: navigateToSlideViaRef,
    getPresentationSlideIndex: () => presentationSlideIndex
  });
  const previousSlideRef = (0, import_react.useRef)(presentationSlideIndex);
  const lastViewedSlideRef = (0, import_react.useRef)(null);
  (0, import_react.useEffect)(() => {
    lastViewedSlideRef.current = previousSlideRef.current;
    previousSlideRef.current = presentationSlideIndex;
  }, [presentationSlideIndex]);
  const onCustomShow = (0, import_react.useCallback)(
    (customShowId, returnAfter) => {
      customShowRunner.runCustomShow(customShowId, returnAfter);
    },
    [customShowRunner]
  );
  const onLastViewed = (0, import_react.useCallback)(() => {
    const previous = lastViewedSlideRef.current;
    if (previous !== null) {
      navigateToSlideViaRef(previous);
    }
  }, [navigateToSlideViaRef]);
  const onOpenFile = (0, import_react.useCallback)((target) => {
    safeOpenUrl(target);
  }, []);
  const onOpenPresentation = (0, import_react.useCallback)((target) => {
    safeOpenUrl(target);
  }, []);
  const onPlayMedia = (0, import_react.useCallback)(
    (elementId) => {
      toggleStageElementMedia(containerRef.current, elementId);
    },
    [containerRef]
  );
  const onOleVerb = (0, import_react.useCallback)(
    (verb, elementId) => {
      const target = resolveOleVerbTarget(slides[presentationSlideIndex], elementId, verb);
      if (target) {
        openUrlInNewTab(target.url);
      }
    },
    [slides, presentationSlideIndex]
  );
  const handleAdvancePastLastSlide = (0, import_react.useCallback)(() => {
    if (customShowRunner.tryReturnFromCustomShow()) {
      return;
    }
    if (endWithBlackSlide) {
      setEndOfShowVisible(true);
    } else {
      onSetMode("edit");
    }
  }, [customShowRunner, endWithBlackSlide, onSetMode, setEndOfShowVisible]);
  const bindNavigateToSlide = (0, import_react.useCallback)((navigateToSlide) => {
    navigateToSlideRef.current = navigateToSlide;
  }, []);
  return {
    onLastViewed,
    onCustomShow,
    onOpenFile,
    onOpenPresentation,
    onPlayMedia,
    onOleVerb,
    handleAdvancePastLastSlide,
    bindNavigateToSlide
  };
}
function usePresenterConsole(slideIndex, active) {
  const timerRef = (0, import_react.useRef)(createPresenterTimer());
  const [snapshot, setSnapshot] = (0, import_react.useState)(() => createInitialPresentationSnapshot(slideIndex));
  const patch = (0, import_react.useCallback)((value) => {
    setSnapshot((current) => mergePresentationSnapshot(current, value));
  }, []);
  (0, import_react.useEffect)(() => patch({ slideIndex }), [patch, slideIndex]);
  (0, import_react.useEffect)(() => {
    if (!active) {
      return;
    }
    timerRef.current = resetPresenterTimer();
    patch({ paused: false, elapsedMs: 0 });
    const timer = window.setInterval(() => {
      patch({
        paused: timerRef.current.paused,
        elapsedMs: presenterElapsed(timerRef.current)
      });
    }, 1e3);
    return () => window.clearInterval(timer);
  }, [active, patch]);
  return {
    snapshot,
    applyAudienceSnapshot: setSnapshot,
    setBlackout: (blackout) => patch({ blackout }),
    toggleTimer: () => {
      timerRef.current = togglePresenterTimer(timerRef.current);
      patch({
        paused: timerRef.current.paused,
        elapsedMs: presenterElapsed(timerRef.current)
      });
    },
    resetTimer: () => {
      timerRef.current = resetPresenterTimer();
      patch({ paused: false, elapsedMs: 0 });
    },
    stepZoom: (direction) => setSnapshot(
      (current) => mergePresentationSnapshot(current, {
        zoom: stepPresenterZoom(
          current.zoom ?? { scale: 1, originX: 0.5, originY: 0.5 },
          direction
        )
      })
    ),
    resetZoom: () => patch({ zoom: { scale: 1, originX: 0.5, originY: 0.5 } }),
    setCaption: (caption) => patch({ caption }),
    setSubtitlesVisible: (subtitlesVisible) => patch({ subtitlesVisible }),
    updateSnapshot: patch
  };
}
function usePresentationMode(input) {
  const {
    mode,
    slides,
    templateElementsBySlideId,
    visibleSlideIndexes,
    canvasSize,
    themeColorMap,
    activeSlideIndex,
    containerRef,
    content,
    onSetMode,
    onSetActiveSlideIndex,
    onPlayActionSound,
    onStopActionSound,
    onSetPointerTool,
    onEraseAnnotations,
    onToggleInkMarkup,
    onToggleToolbar,
    onToggleSubtitles,
    onSaveRehearsalTimings,
    loopContinuously,
    showWithAnimation,
    useTimings,
    endWithBlackSlide = true,
    customShows = [],
    activeCustomShowId = null,
    onSetActiveCustomShowId
  } = input;
  const [presentationSlideIndex, setPresentationSlideIndex] = (0, import_react.useState)(0);
  const [presentationSlideVisible, setPresentationSlideVisible] = (0, import_react.useState)(true);
  const [transitionOverlay, setTransitionOverlay] = (0, import_react.useState)(null);
  const handleTransitionOverlayComplete = (0, import_react.useCallback)(() => {
    setTransitionOverlay(null);
  }, []);
  const [presenterMode, setPresenterMode] = (0, import_react.useState)(false);
  const [allSlidesOpen, setAllSlidesOpen] = (0, import_react.useState)(false);
  const [endOfShowVisible, setEndOfShowVisible] = (0, import_react.useState)(false);
  const [presentationStartTime, setPresentationStartTime] = (0, import_react.useState)(null);
  const showGuard = (0, import_react.useMemo)(() => createPresenterShowGuard(), []);
  const {
    presentationAnimations,
    presentationElementStates,
    presentationKeyframesCss,
    interactiveTriggerShapeIds,
    hoverTriggerShapeIds,
    clearPresentationTimers,
    playNextAnimationGroup,
    handleInteractiveShapeClick,
    handleHoverStart,
    handleHoverEnd,
    runPresentationEntranceAnimations,
    seedSlideAnimations,
    startSlideAnimations,
    isSeededCompleted,
    presentationTimersRef
  } = useAnimationPlayback({
    slides,
    onPlayActionSound,
    showWithAnimation,
    canvasSize,
    themeColorMap
  });
  const {
    rehearsing,
    setRehearsing,
    recordedTimings,
    slideStartTime,
    showRehearsalSummary,
    setShowRehearsalSummary,
    rehearsalPaused,
    recordCurrentSlideTime,
    dismissRehearsalSummary,
    saveRehearsalTimings,
    enterRehearsalMode,
    toggleRehearsalPause
  } = useRehearsalTimings({
    containerRef,
    onSetMode,
    onSaveRehearsalTimings,
    setPresentationStartTime,
    setPresenterMode
  });
  (0, import_react.useEffect)(() => {
    if (mode !== "present") {
      setPresenterMode(false);
    }
  }, [mode]);
  const actionExtensions = usePresentationActionExtensions({
    slides,
    customShows,
    activeCustomShowId,
    onSetActiveCustomShowId,
    presentationSlideIndex,
    containerRef,
    endWithBlackSlide,
    onSetMode,
    setEndOfShowVisible
  });
  const {
    movePresentationSlide: moveNavigationSlide,
    navigateToSlide,
    handlePresentationAction,
    scheduleAutoAdvanceForSlide
  } = useSlideNavigation({
    slides,
    visibleSlideIndexes,
    presentationSlideIndex,
    setPresentationSlideIndex,
    setPresentationSlideVisible,
    setTransitionOverlay,
    onSetMode,
    onSetActiveSlideIndex,
    onPlayActionSound,
    onStopActionSound,
    loopContinuously,
    useTimings,
    onAdvancePastLastSlide: actionExtensions.handleAdvancePastLastSlide,
    playNextAnimationGroup,
    clearPresentationTimers,
    seedSlideAnimations,
    startSlideAnimations,
    presentationTimersRef,
    rehearsing,
    recordCurrentSlideTime,
    setShowRehearsalSummary,
    onLastViewed: actionExtensions.onLastViewed,
    onCustomShow: actionExtensions.onCustomShow,
    onOpenFile: actionExtensions.onOpenFile,
    onOpenPresentation: actionExtensions.onOpenPresentation,
    onPlayMedia: actionExtensions.onPlayMedia,
    onOleVerb: actionExtensions.onOleVerb
  });
  actionExtensions.bindNavigateToSlide(navigateToSlide);
  const movePresentationSlide = (0, import_react.useCallback)(
    (direction, trigger = "explicit") => {
      if (!acceptsPresentationInput()) {
        return;
      }
      if (endOfShowVisible) {
        setEndOfShowVisible(false);
        if (direction === 1) {
          onSetMode("edit");
        }
        return;
      }
      if (direction === -1 && isSeededCompleted()) {
        runPresentationEntranceAnimations(presentationSlideIndex);
        return;
      }
      moveNavigationSlide(direction, trigger);
    },
    [
      endOfShowVisible,
      isSeededCompleted,
      moveNavigationSlide,
      onSetMode,
      presentationSlideIndex,
      runPresentationEntranceAnimations
    ]
  );
  const { handleZoomClick, zoomReturnSlideIndex, returnToZoomSlide, clearZoomReturn } = useZoomNavigation({ navigateToSlide });
  const openAllSlides = (0, import_react.useCallback)(() => setAllSlidesOpen(true), []);
  const closeAllSlides = (0, import_react.useCallback)(() => setAllSlidesOpen(false), []);
  const presenterConsole = usePresenterConsole(presentationSlideIndex, mode === "present");
  const {
    openAudienceWindow,
    closeAudienceWindow,
    isAudienceWindowOpen,
    syncSlideToAudience,
    syncStateToAudience,
    swapDisplays
  } = usePresenterWindow({
    isPresenterMode: presenterMode,
    content,
    snapshot: presenterConsole.snapshot
  });
  useAudienceMode({
    mode,
    onSetMode,
    onSetActiveSlideIndex: (index) => {
      onSetActiveSlideIndex(index);
      setPresentationSlideIndex(index);
    },
    containerRef,
    onSnapshot: presenterConsole.applyAudienceSnapshot,
    // The presenter ended the session but this tab could not close itself:
    // show the black end-of-slide-show screen rather than the editor.
    onPresenterEnded: () => setEndOfShowVisible(true)
  });
  const enterPresentMode = (0, import_react.useCallback)(() => {
    setPresenterMode(false);
    setRehearsing(false);
    setPresentationStartTime(Date.now());
    try {
      const wrapper = containerRef.current;
      if (wrapper && typeof wrapper.requestFullscreen === "function") {
        void wrapper.requestFullscreen().catch(() => {
        });
      }
    } catch {
    }
    onSetMode("present");
  }, [containerRef, onSetMode, setRehearsing]);
  const enterPresentModeFromBeginning = (0, import_react.useCallback)(() => {
    const first = firstShowSlideIndex(visibleSlideIndexes);
    if (first !== void 0) {
      onSetActiveSlideIndex(first);
    }
    enterPresentMode();
  }, [visibleSlideIndexes, onSetActiveSlideIndex, enterPresentMode]);
  const enterPresenterView = (0, import_react.useCallback)(() => {
    setPresenterMode(true);
    setRehearsing(false);
    setPresentationStartTime(Date.now());
    onSetMode("present");
  }, [onSetMode, setRehearsing]);
  const togglePresenterView = (0, import_react.useCallback)(() => {
    if (!presenterMode) {
      showGuard.expectAudienceBounce();
      try {
        if (document.fullscreenElement) {
          void document.exitFullscreen().catch(() => {
          });
        }
      } catch {
      }
      setPresenterMode(true);
      if (!presentationStartTime) {
        setPresentationStartTime(Date.now());
      }
    } else {
      setPresenterMode(false);
      try {
        const wrapper = containerRef.current;
        if (wrapper && typeof wrapper.requestFullscreen === "function") {
          void wrapper.requestFullscreen().catch(() => {
          });
        }
      } catch {
      }
    }
  }, [presenterMode, presentationStartTime, containerRef, showGuard]);
  usePresentationKeyboard({
    mode,
    movePresentationSlide,
    navigateToSlide,
    slideCount: slides.length,
    showSlideIndexes: visibleSlideIndexes,
    onSetMode,
    onSetPointerTool,
    onEraseAnnotations,
    onToggleInkMarkup,
    onToggleToolbar,
    onShowAllSlides: openAllSlides,
    onToggleSubtitles,
    onToggleBlackScreen: () => presenterConsole.setBlackout(
      presenterConsole.snapshot.blackout === "black" ? "none" : "black"
    ),
    onToggleWhiteScreen: () => presenterConsole.setBlackout(
      presenterConsole.snapshot.blackout === "white" ? "none" : "white"
    ),
    rehearsing,
    recordCurrentSlideTime,
    presentationSlideIndex,
    setShowRehearsalSummary
  });
  const runEntranceAnimationsRef = (0, import_react.useRef)(runPresentationEntranceAnimations);
  runEntranceAnimationsRef.current = runPresentationEntranceAnimations;
  const scheduleAutoAdvanceRef = (0, import_react.useRef)(scheduleAutoAdvanceForSlide);
  scheduleAutoAdvanceRef.current = scheduleAutoAdvanceForSlide;
  const activeSlideIndexRef = (0, import_react.useRef)(activeSlideIndex);
  activeSlideIndexRef.current = activeSlideIndex;
  const visibleSlideIndexesRef = (0, import_react.useRef)(visibleSlideIndexes);
  visibleSlideIndexesRef.current = visibleSlideIndexes;
  const onSetActiveSlideIndexRef = (0, import_react.useRef)(onSetActiveSlideIndex);
  onSetActiveSlideIndexRef.current = onSetActiveSlideIndex;
  const presentationSlideIndexRef = (0, import_react.useRef)(presentationSlideIndex);
  presentationSlideIndexRef.current = presentationSlideIndex;
  const clearPresentationTimersRef = (0, import_react.useRef)(clearPresentationTimers);
  clearPresentationTimersRef.current = clearPresentationTimers;
  (0, import_react.useEffect)(() => {
    if (mode !== "present") {
      return;
    }
    return attachPresentationVisibilityPause({
      root: containerRef.current ?? void 0,
      onHidden: () => clearPresentationTimersRef.current(),
      onVisible: () => scheduleAutoAdvanceRef.current(presentationSlideIndexRef.current)
    });
  }, [mode, containerRef]);
  (0, import_react.useLayoutEffect)(() => {
    if (mode === "present") {
      const idx = presentationEntrySlideIndex(
        activeSlideIndexRef.current,
        visibleSlideIndexesRef.current
      );
      setPresentationSlideIndex(idx);
      if (idx !== activeSlideIndexRef.current) {
        onSetActiveSlideIndexRef.current(idx);
      }
      runEntranceAnimationsRef.current(idx);
      scheduleAutoAdvanceRef.current(idx);
    } else {
      setTransitionOverlay(null);
    }
    setEndOfShowVisible(false);
  }, [mode]);
  (0, import_react.useEffect)(() => {
    if (mode !== "present") {
      return;
    }
    return () => {
      try {
        if (document.fullscreenElement) {
          void document.exitFullscreen().catch(() => {
          });
        }
      } catch {
      }
    };
  }, [mode]);
  (0, import_react.useEffect)(() => {
    if (mode !== "present") {
      return;
    }
    if (presenterMode) {
      return;
    }
    let wasFullscreen = Boolean(document.fullscreenElement);
    const handleFullscreenChange = () => {
      if (showGuard.classifyFullscreenExit() === "restore-show") {
        wasFullscreen = Boolean(document.fullscreenElement);
        return;
      }
      const isFullscreen = Boolean(document.fullscreenElement);
      if (wasFullscreen && !isFullscreen) {
        if (rehearsing) {
          recordCurrentSlideTime(presentationSlideIndex);
          setShowRehearsalSummary(true);
        }
        onSetMode("edit");
      }
      wasFullscreen = isFullscreen;
    };
    document.addEventListener("fullscreenchange", handleFullscreenChange);
    return () => {
      document.removeEventListener("fullscreenchange", handleFullscreenChange);
    };
  }, [
    mode,
    onSetMode,
    presenterMode,
    rehearsing,
    recordCurrentSlideTime,
    presentationSlideIndex,
    setShowRehearsalSummary,
    showGuard
  ]);
  (0, import_react.useEffect)(() => {
    const timersRef = presentationTimersRef;
    return () => {
      timersRef.current.forEach((timer) => {
        window.clearTimeout(timer);
      });
    };
  }, [presentationTimersRef]);
  const morphPlan = (0, import_react.useMemo)(() => {
    var _a;
    if (!transitionOverlay || transitionOverlay.transition.type !== "morph") {
      return void 0;
    }
    const incoming = slides[transitionOverlay.incomingSlideIndex];
    return buildMorphTransitionPlan(
      slides[transitionOverlay.outgoingSlideIndex],
      incoming,
      transitionOverlay.durationMs,
      morphOptionToMode(transitionOverlay.transition.morphOption),
      // `PresentationStage` stacks the incoming slide's elements above its
      // template shapes (`zIndex = templateElements.length + index`), so a
      // z-index journey has to start counting where the stage does.
      { zIndexBase: incoming ? ((_a = templateElementsBySlideId == null ? void 0 : templateElementsBySlideId[incoming.id]) == null ? void 0 : _a.length) ?? 0 : 0 }
    );
  }, [transitionOverlay, slides, templateElementsBySlideId]);
  const morphedElementStates = (0, import_react.useMemo)(() => {
    if (!morphPlan || morphPlan.incomingAnimations.size === 0) {
      return presentationElementStates;
    }
    const merged = new Map(presentationElementStates);
    for (const [elementId, animation] of morphPlan.incomingAnimations) {
      const existing = merged.get(elementId);
      if (existing == null ? void 0 : existing.cssAnimation) {
        continue;
      }
      merged.set(elementId, { ...existing, visible: true, cssAnimation: animation });
    }
    return merged;
  }, [morphPlan, presentationElementStates]);
  const morphedKeyframesCss = (0, import_react.useMemo)(() => {
    if (!morphPlan) {
      return presentationKeyframesCss;
    }
    const imageRules = buildMorphAnimationRules(morphPlan, "", "incoming", "image");
    return `${presentationKeyframesCss ?? ""}
${morphPlan.keyframesCss}
${imageRules}`;
  }, [morphPlan, presentationKeyframesCss]);
  return {
    presentationSlideIndex,
    setPresentationSlideIndex,
    presentationSlideVisible,
    transitionOverlay,
    handleTransitionOverlayComplete,
    morphPlan,
    presentationAnimations,
    presentationElementStates: morphedElementStates,
    presentationKeyframesCss: morphedKeyframesCss,
    clearPresentationTimers,
    runPresentationEntranceAnimations,
    endOfShowVisible,
    allSlidesOpen,
    openAllSlides,
    closeAllSlides,
    movePresentationSlide,
    navigateToSlide,
    handlePresentationAction,
    handleInteractiveShapeClick,
    interactiveTriggerShapeIds,
    hoverTriggerShapeIds,
    handleHoverStart,
    handleHoverEnd,
    enterPresentMode,
    enterPresentModeFromBeginning,
    presenterMode,
    enterPresenterView,
    togglePresenterView,
    presentationStartTime,
    rehearsing,
    enterRehearsalMode,
    recordedTimings,
    slideStartTime,
    showRehearsalSummary,
    dismissRehearsalSummary,
    saveRehearsalTimings,
    rehearsalPaused,
    toggleRehearsalPause,
    handleZoomClick,
    zoomReturnSlideIndex,
    returnToZoomSlide,
    clearZoomReturn,
    openAudienceWindow,
    closeAudienceWindow,
    isAudienceWindowOpen,
    syncSlideToAudience,
    syncStateToAudience,
    swapPresenterDisplays: swapDisplays,
    presenterSnapshot: presenterConsole.snapshot,
    setPresenterBlackout: presenterConsole.setBlackout,
    togglePresenterTimer: presenterConsole.toggleTimer,
    resetPresenterTimer: presenterConsole.resetTimer,
    stepPresenterZoom: presenterConsole.stepZoom,
    resetPresenterZoom: presenterConsole.resetZoom,
    setPresenterCaption: presenterConsole.setCaption,
    setPresenterSubtitlesVisible: presenterConsole.setSubtitlesVisible,
    updatePresenterSnapshot: presenterConsole.updateSnapshot
  };
}
var PEN_WIDTH = 2.5;
var HIGHLIGHTER_WIDTH = 14;
var HIGHLIGHTER_OPACITY = 0.4;
var ERASER_RADIUS = 16;
var strokeIdCounter = 0;
function nextStrokeId() {
  strokeIdCounter += 1;
  return `stroke-${strokeIdCounter}`;
}
function usePresentationAnnotations(input) {
  const { isActive, activeSlideIndex, popupToolbarEnabled = true } = input;
  const [presentationTool, setPresentationToolState] = (0, import_react.useState)("none");
  const [penColor, setPenColor] = (0, import_react.useState)("#ff0000");
  const [highlighterColor, setHighlighterColor] = (0, import_react.useState)("#ffff00");
  const [currentStroke, setCurrentStroke] = (0, import_react.useState)(null);
  const [laserPosition, setLaserPosition] = (0, import_react.useState)(null);
  const [toolbarVisible, setToolbarVisible] = (0, import_react.useState)(false);
  const [inkMarkupVisible, setInkMarkupVisible] = (0, import_react.useState)(true);
  const slideAnnotationsRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
  const [annotationStrokes, setAnnotationStrokes] = (0, import_react.useState)([]);
  const [annotationVersion, setAnnotationVersion] = (0, import_react.useState)(0);
  const isDrawingRef = (0, import_react.useRef)(false);
  const activeSlideIndexRef = (0, import_react.useRef)(activeSlideIndex);
  activeSlideIndexRef.current = activeSlideIndex;
  const prevSlideIndexRef = (0, import_react.useRef)(activeSlideIndex);
  (0, import_react.useEffect)(() => {
    if (!isActive) {
      return;
    }
    if (prevSlideIndexRef.current === activeSlideIndex) {
      return;
    }
    setAnnotationStrokes((currentStrokes) => {
      if (currentStrokes.length > 0) {
        slideAnnotationsRef.current.set(prevSlideIndexRef.current, currentStrokes);
      } else {
        slideAnnotationsRef.current.delete(prevSlideIndexRef.current);
      }
      setAnnotationVersion((v) => v + 1);
      return slideAnnotationsRef.current.get(activeSlideIndex) ?? [];
    });
    prevSlideIndexRef.current = activeSlideIndex;
  }, [isActive, activeSlideIndex]);
  const setPresentationTool = (0, import_react.useCallback)(
    (tool) => {
      if (!isActive) {
        return;
      }
      setPresentationToolState((prev) => prev === tool ? "none" : tool);
    },
    [isActive]
  );
  const handlePointerDown = (0, import_react.useCallback)(
    (x, y) => {
      if (!isActive) {
        return;
      }
      if (presentationTool === "pen" || presentationTool === "highlighter") {
        isDrawingRef.current = true;
        const isPen = presentationTool === "pen";
        setCurrentStroke({
          id: nextStrokeId(),
          points: [{ x, y }],
          color: isPen ? penColor : highlighterColor,
          width: isPen ? PEN_WIDTH : HIGHLIGHTER_WIDTH,
          opacity: isPen ? 1 : HIGHLIGHTER_OPACITY
        });
      }
    },
    [isActive, presentationTool, penColor, highlighterColor]
  );
  const handlePointerMove = (0, import_react.useCallback)(
    (x, y) => {
      if (!isActive || !isDrawingRef.current) {
        return;
      }
      setCurrentStroke((prev) => {
        if (!prev) {
          return null;
        }
        return { ...prev, points: [...prev.points, { x, y }] };
      });
    },
    [isActive]
  );
  const handlePointerUp = (0, import_react.useCallback)(() => {
    if (!isDrawingRef.current) {
      return;
    }
    isDrawingRef.current = false;
    setCurrentStroke((prev) => {
      if (prev && prev.points.length > 1) {
        setAnnotationStrokes((strokes) => {
          if (strokes.some((s) => s.id === prev.id)) {
            return strokes;
          }
          const updated = [...strokes, prev];
          slideAnnotationsRef.current.set(activeSlideIndexRef.current, updated);
          setAnnotationVersion((v) => v + 1);
          return updated;
        });
      }
      return null;
    });
  }, []);
  const handleLaserMove = (0, import_react.useCallback)(
    (x, y) => {
      if (!isActive || presentationTool !== "laser") {
        return;
      }
      setLaserPosition({ x, y });
    },
    [isActive, presentationTool]
  );
  const handleLaserLeave = (0, import_react.useCallback)(() => {
    setLaserPosition(null);
  }, []);
  const eraseAtPoint = (0, import_react.useCallback)(
    (x, y) => {
      if (!isActive || presentationTool !== "eraser") {
        return;
      }
      setAnnotationStrokes((strokes) => {
        const filtered = strokes.filter((stroke) => {
          return !stroke.points.some((pt) => {
            const dx = pt.x - x;
            const dy = pt.y - y;
            return dx * dx + dy * dy < ERASER_RADIUS * ERASER_RADIUS;
          });
        });
        if (filtered.length > 0) {
          slideAnnotationsRef.current.set(activeSlideIndexRef.current, filtered);
        } else {
          slideAnnotationsRef.current.delete(activeSlideIndexRef.current);
        }
        setAnnotationVersion((v) => v + 1);
        return filtered;
      });
    },
    [isActive, presentationTool]
  );
  const clearAnnotations = (0, import_react.useCallback)(() => {
    setAnnotationStrokes([]);
    setCurrentStroke(null);
    isDrawingRef.current = false;
    slideAnnotationsRef.current.delete(activeSlideIndexRef.current);
    setAnnotationVersion((v) => v + 1);
  }, []);
  const clearAllAnnotations = (0, import_react.useCallback)(() => {
    setAnnotationStrokes([]);
    setCurrentStroke(null);
    isDrawingRef.current = false;
    slideAnnotationsRef.current.clear();
    setAnnotationVersion((v) => v + 1);
  }, []);
  const toolbarTimerRef = (0, import_react.useRef)(0);
  (0, import_react.useEffect)(() => {
    if (!isActive) {
      setToolbarVisible(false);
      setPresentationToolState("none");
      setCurrentStroke(null);
      setLaserPosition(null);
      return;
    }
    if (!popupToolbarEnabled) {
      return;
    }
    const handleMouseMove = () => {
      setToolbarVisible(true);
      window.clearTimeout(toolbarTimerRef.current);
      toolbarTimerRef.current = window.setTimeout(() => {
        setToolbarVisible(false);
      }, 3e3);
    };
    window.addEventListener("mousemove", handleMouseMove);
    return () => {
      window.removeEventListener("mousemove", handleMouseMove);
      window.clearTimeout(toolbarTimerRef.current);
    };
  }, [isActive, popupToolbarEnabled]);
  const getAllSlideAnnotations = (0, import_react.useCallback)(() => {
    const map = new Map(slideAnnotationsRef.current);
    if (annotationStrokes.length > 0) {
      map.set(activeSlideIndex, annotationStrokes);
    }
    return map;
  }, [annotationStrokes, activeSlideIndex]);
  const hasAnyAnnotations = (
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    annotationVersion >= 0 && (annotationStrokes.length > 0 || slideAnnotationsRef.current.size > 0)
  );
  return {
    presentationTool,
    setPresentationTool,
    penColor,
    setPenColor,
    highlighterColor,
    setHighlighterColor,
    annotationStrokes,
    currentStroke,
    laserPosition,
    toolbarVisible,
    setToolbarVisible,
    inkMarkupVisible,
    setInkMarkupVisible,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp,
    handleLaserMove,
    handleLaserLeave,
    clearAnnotations,
    eraseAtPoint,
    allSlideAnnotations: getAllSlideAnnotations(),
    hasAnyAnnotations,
    clearAllAnnotations
  };
}
function useFindReplace({
  slides,
  mode,
  onSetActiveSlideIndex,
  onSetSelectedElementId,
  onUpdateSlides,
  onMarkDirty
}) {
  const [findReplaceOpen, setFindReplaceOpen] = (0, import_react.useState)(false);
  const [findQuery, setFindQuery] = (0, import_react.useState)("");
  const [replaceQuery, setReplaceQuery] = (0, import_react.useState)("");
  const [findMatchCase, setFindMatchCase] = (0, import_react.useState)(false);
  const [findResults, setFindResults] = (0, import_react.useState)([]);
  const [findResultIndex, setFindResultIndex] = (0, import_react.useState)(-1);
  const performFind = (0, import_react.useCallback)(() => {
    const results = findInSlides(slides, findQuery, { matchCase: findMatchCase });
    if (!findQuery) {
      setFindResults([]);
      setFindResultIndex(-1);
      return;
    }
    setFindResults(results);
    setFindResultIndex(results.length > 0 ? 0 : -1);
    if (results.length > 0) {
      onSetActiveSlideIndex(results[0].slideIndex);
      onSetSelectedElementId(results[0].elementId);
    }
  }, [slides, findQuery, findMatchCase, onSetActiveSlideIndex, onSetSelectedElementId]);
  const navigateFindResult = (0, import_react.useCallback)(
    (direction) => {
      if (findResults.length === 0) {
        return;
      }
      const nextIdx = (findResultIndex + direction + findResults.length) % findResults.length;
      setFindResultIndex(nextIdx);
      const match = findResults[nextIdx];
      if (match) {
        onSetActiveSlideIndex(match.slideIndex);
        onSetSelectedElementId(match.elementId);
      }
    },
    [findResults, findResultIndex, onSetActiveSlideIndex, onSetSelectedElementId]
  );
  const applyReplacements = (0, import_react.useCallback)(
    (toReplace) => {
      if (toReplace.length === 0) {
        return;
      }
      onUpdateSlides(
        (prevSlides) => applyFindReplacements(prevSlides, toReplace, replaceQuery).slides
      );
      onMarkDirty();
    },
    [replaceQuery, onUpdateSlides, onMarkDirty]
  );
  const handleReplace = (0, import_react.useCallback)(() => {
    if (findResults.length === 0 || findResultIndex < 0) {
      return;
    }
    applyReplacements([findResults[findResultIndex]]);
    setTimeout(performFind, 0);
  }, [findResults, findResultIndex, applyReplacements, performFind]);
  const handleReplaceAll = (0, import_react.useCallback)(() => {
    if (findResults.length === 0) {
      return;
    }
    applyReplacements(findResults);
    setTimeout(performFind, 0);
  }, [findResults, applyReplacements, performFind]);
  (0, import_react.useEffect)(() => {
    const handler = (event) => {
      const { action } = mapEditorKey(event, {
        canEdit: mode === "edit",
        isTextInputTarget: isEditorTextInputTarget(event.target)
      });
      if (action !== "find") {
        return;
      }
      event.preventDefault();
      setFindReplaceOpen((prev) => !prev);
    };
    window.addEventListener("keydown", handler);
    return () => window.removeEventListener("keydown", handler);
  }, [mode]);
  return {
    findReplaceOpen,
    setFindReplaceOpen,
    findQuery,
    setFindQuery,
    replaceQuery,
    setReplaceQuery,
    findMatchCase,
    setFindMatchCase,
    findResults,
    findResultIndex,
    performFind,
    navigateFindResult,
    handleReplace,
    handleReplaceAll
  };
}
function useModalDismissDrag(onClose, threshold = 120) {
  const [dragY, setDragY] = (0, import_react.useState)(0);
  const startRef = (0, import_react.useRef)(null);
  const onPointerDown = (0, import_react.useCallback)((e) => {
    var _a, _b2;
    if (e.pointerType === "mouse") {
      return;
    }
    if (e.target.closest('button, a, input, select, textarea, [role="button"]')) {
      return;
    }
    startRef.current = e.clientY;
    (_b2 = (_a = e.target).setPointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
  }, []);
  const onPointerMove = (0, import_react.useCallback)((e) => {
    if (startRef.current === null) {
      return;
    }
    setDragY(Math.max(0, e.clientY - startRef.current));
  }, []);
  const onPointerUp = (0, import_react.useCallback)(
    (e) => {
      var _a, _b2;
      if (startRef.current === null) {
        return;
      }
      const delta = e.clientY - startRef.current;
      startRef.current = null;
      (_b2 = (_a = e.target).releasePointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
      if (delta > threshold) {
        onClose();
      }
      setDragY(0);
    },
    [onClose, threshold]
  );
  return {
    panelStyle: dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : { transition: "transform 150ms ease-out" },
    handlers: { onPointerDown, onPointerMove, onPointerUp, onPointerCancel: onPointerUp }
  };
}
function withoutKey(map, key) {
  if (!(key in map)) {
    return map;
  }
  const next = { ...map };
  delete next[key];
  return next;
}
function useCommentMentionDrafts() {
  const [commentDraftMentionsBySlideId, setCommentDraftMentionsBySlideId] = (0, import_react.useState)({});
  const [replyDraftMentionsByCommentId, setReplyDraftMentionsByCommentId] = (0, import_react.useState)({});
  const setCommentDraftMentions = (0, import_react.useCallback)((slideId, mentions) => {
    setCommentDraftMentionsBySlideId((prev) => ({ ...prev, [slideId]: mentions }));
  }, []);
  const setReplyDraftMentions = (0, import_react.useCallback)((commentId, mentions) => {
    setReplyDraftMentionsByCommentId((prev) => ({ ...prev, [commentId]: mentions }));
  }, []);
  const clearCommentDraftMentions = (0, import_react.useCallback)((slideId) => {
    setCommentDraftMentionsBySlideId((prev) => withoutKey(prev, slideId));
  }, []);
  const clearReplyDraftMentions = (0, import_react.useCallback)((commentId) => {
    setReplyDraftMentionsByCommentId((prev) => withoutKey(prev, commentId));
  }, []);
  return {
    commentDraftMentionsBySlideId,
    replyDraftMentionsByCommentId,
    setCommentDraftMentions,
    setReplyDraftMentions,
    clearCommentDraftMentions,
    clearReplyDraftMentions
  };
}
function replaceCommentsOnSlide(slides, slideIndex, next) {
  return slides.map(
    (entry, index) => index === slideIndex ? { ...entry, comments: next } : entry
  );
}
function addCommentToSlide(slides, slideIndex, text, authorName, elementId, mentions) {
  const slide = slides[slideIndex];
  if (!slide) {
    return { slides, didAdd: false };
  }
  const next = addCommentToList(
    slide.comments || [],
    text,
    authorName,
    void 0,
    void 0,
    elementId
  );
  if (!next) {
    return { slides, didAdd: false };
  }
  const withMentions = mentions && mentions.length > 0 ? next.map(
    (comment, index) => index === next.length - 1 ? { ...comment, mentions } : comment
  ) : next;
  return { slides: replaceCommentsOnSlide(slides, slideIndex, withMentions), didAdd: true };
}
function removeCommentFromSlide(slides, slideIndex, commentId) {
  const slide = slides[slideIndex];
  if (!slide) {
    return { slides, didDelete: false };
  }
  const next = removeCommentFromList(slide.comments || [], commentId);
  if (!next) {
    return { slides, didDelete: false };
  }
  return { slides: replaceCommentsOnSlide(slides, slideIndex, next), didDelete: true };
}
function editCommentInSlide(slides, slideIndex, commentId, newText) {
  const slide = slides[slideIndex];
  if (!slide) {
    return { slides, didUpdate: false };
  }
  const next = editCommentInList(slide.comments || [], commentId, newText);
  if (!next) {
    return { slides, didUpdate: false };
  }
  return { slides: replaceCommentsOnSlide(slides, slideIndex, next), didUpdate: true };
}
function toggleResolvedInSlide(slides, slideIndex, commentId) {
  const slide = slides[slideIndex];
  if (!slide) {
    return { slides, didUpdate: false };
  }
  const next = toggleCommentResolvedInList(slide.comments || [], commentId);
  if (!next) {
    return { slides, didUpdate: false };
  }
  return { slides: replaceCommentsOnSlide(slides, slideIndex, next), didUpdate: true };
}
function addReplyToSlide(slides, slideIndex, parentId, text, authorName, mentions) {
  const slide = slides[slideIndex];
  if (!slide) {
    return { slides, didAdd: false };
  }
  const next = replyToCommentInList(slide.comments || [], parentId, text, authorName);
  if (!next) {
    return { slides, didAdd: false };
  }
  const withMentions = mentions && mentions.length > 0 ? next.map(
    (comment) => comment.id === parentId && comment.replies && comment.replies.length > 0 ? {
      ...comment,
      replies: comment.replies.map(
        (reply, index) => index === comment.replies.length - 1 ? { ...reply, mentions } : reply
      )
    } : comment
  ) : next;
  return { slides: replaceCommentsOnSlide(slides, slideIndex, withMentions), didAdd: true };
}
function pruneSlideDrafts(drafts, slideIds) {
  const next = {};
  let changed = false;
  for (const [id, draft] of Object.entries(drafts)) {
    if (!slideIds.has(id)) {
      changed = true;
      continue;
    }
    next[id] = draft;
  }
  return changed ? next : null;
}
function useCommentReplyHandlers(input) {
  const {
    slides,
    canEdit,
    userName,
    onUpdateSlides,
    onMarkDirty,
    replyDraftMentionsByCommentId,
    setReplyDraftMentions,
    clearReplyDraftMentions
  } = input;
  const [replyDraftByCommentId, setReplyDraftByCommentId] = (0, import_react.useState)({});
  const [replyingToCommentId, setReplyingToCommentId] = (0, import_react.useState)(null);
  const handleStartReply = (0, import_react.useCallback)((_slideIndex, commentId) => {
    setReplyingToCommentId(commentId);
    setReplyDraftByCommentId((prev) => ({ ...prev, [commentId]: "" }));
  }, []);
  const handleCancelReply = (0, import_react.useCallback)(() => {
    setReplyingToCommentId(null);
  }, []);
  const handleReplyDraftChange = (0, import_react.useCallback)(
    (commentId, draft, mentions) => {
      setReplyDraftByCommentId((prev) => ({ ...prev, [commentId]: draft }));
      if (mentions) {
        setReplyDraftMentions(commentId, mentions);
      }
    },
    [setReplyDraftMentions]
  );
  const handleSubmitReply = (0, import_react.useCallback)(
    (slideIndex, commentId) => {
      if (!canEdit) {
        return;
      }
      const slide = slides[slideIndex];
      if (!slide) {
        return;
      }
      const replyText = String(replyDraftByCommentId[commentId] || "").trim();
      if (replyText.length === 0) {
        return;
      }
      let didAdd = false;
      onUpdateSlides((prev) => {
        const result = addReplyToSlide(
          prev,
          slideIndex,
          commentId,
          replyText,
          userName,
          replyDraftMentionsByCommentId[commentId]
        );
        didAdd = result.didAdd;
        return result.slides;
      });
      if (!didAdd) {
        return;
      }
      setReplyingToCommentId(null);
      setReplyDraftByCommentId((prev) => {
        const next = { ...prev };
        delete next[commentId];
        return next;
      });
      clearReplyDraftMentions(commentId);
      onMarkDirty();
    },
    [
      canEdit,
      onMarkDirty,
      onUpdateSlides,
      replyDraftByCommentId,
      replyDraftMentionsByCommentId,
      clearReplyDraftMentions,
      slides,
      userName
    ]
  );
  return {
    replyingToCommentId,
    replyDraftByCommentId,
    handleStartReply,
    handleCancelReply,
    handleReplyDraftChange,
    handleSubmitReply
  };
}
function useComments({
  slides,
  canEdit,
  userName = "You",
  selectedElementId,
  onUpdateSlides,
  onMarkDirty
}) {
  const [commentDraftBySlideId, setCommentDraftBySlideId] = (0, import_react.useState)({});
  const [editingCommentIdBySlideId, setEditingCommentIdBySlideId] = (0, import_react.useState)({});
  const [commentEditDraftByCommentId, setCommentEditDraftByCommentId] = (0, import_react.useState)({});
  const {
    commentDraftMentionsBySlideId,
    replyDraftMentionsByCommentId,
    setCommentDraftMentions,
    setReplyDraftMentions,
    clearCommentDraftMentions,
    clearReplyDraftMentions
  } = useCommentMentionDrafts();
  const reply = useCommentReplyHandlers({
    slides,
    canEdit,
    userName,
    onUpdateSlides,
    onMarkDirty,
    replyDraftMentionsByCommentId,
    setReplyDraftMentions,
    clearReplyDraftMentions
  });
  const handleCommentDraftChange = (0, import_react.useCallback)(
    (slideId, draft, mentions) => {
      setCommentDraftBySlideId((prev) => ({ ...prev, [slideId]: draft }));
      if (mentions) {
        setCommentDraftMentions(slideId, mentions);
      }
    },
    [setCommentDraftMentions]
  );
  const handleSetCommentEditDraft = (0, import_react.useCallback)((commentId, draft) => {
    setCommentEditDraftByCommentId((prev) => ({
      ...prev,
      [commentId]: draft
    }));
  }, []);
  const handleAddSlideComment = (0, import_react.useCallback)(
    (slideIndex) => {
      if (!canEdit) {
        return;
      }
      const slide = slides[slideIndex];
      if (!slide) {
        return;
      }
      const draft = String(commentDraftBySlideId[slide.id] || "").trim();
      if (draft.length === 0) {
        return;
      }
      let didAdd = false;
      onUpdateSlides((prev) => {
        const result = addCommentToSlide(
          prev,
          slideIndex,
          draft,
          userName,
          selectedElementId ?? void 0,
          commentDraftMentionsBySlideId[slide.id]
        );
        didAdd = result.didAdd;
        return result.slides;
      });
      if (!didAdd) {
        return;
      }
      setCommentDraftBySlideId((prev) => ({
        ...prev,
        [slide.id]: ""
      }));
      clearCommentDraftMentions(slide.id);
      onMarkDirty();
    },
    [
      canEdit,
      commentDraftBySlideId,
      commentDraftMentionsBySlideId,
      clearCommentDraftMentions,
      onMarkDirty,
      onUpdateSlides,
      selectedElementId,
      slides,
      userName
    ]
  );
  const handleDeleteSlideComment = (0, import_react.useCallback)(
    (slideIndex, commentId) => {
      if (!canEdit) {
        return;
      }
      const slide = slides[slideIndex];
      if (!slide) {
        return;
      }
      let didDelete = false;
      onUpdateSlides((prev) => {
        const result = removeCommentFromSlide(prev, slideIndex, commentId);
        didDelete = result.didDelete;
        return result.slides;
      });
      if (!didDelete) {
        return;
      }
      setEditingCommentIdBySlideId((prev) => ({
        ...prev,
        [slide.id]: null
      }));
      onMarkDirty();
    },
    [canEdit, onMarkDirty, onUpdateSlides, slides]
  );
  const handleStartCommentEdit = (0, import_react.useCallback)(
    (slideId, commentId) => {
      const slide = slides.find((s) => s.id === slideId);
      if (!slide) {
        return;
      }
      const comment = (slide.comments || []).find((c) => c.id === commentId);
      if (!comment) {
        return;
      }
      setEditingCommentIdBySlideId((prev) => ({
        ...prev,
        [slideId]: commentId
      }));
      setCommentEditDraftByCommentId((prev) => ({
        ...prev,
        [commentId]: comment.text || ""
      }));
    },
    [slides]
  );
  const handleCancelCommentEdit = (0, import_react.useCallback)((slideId) => {
    setEditingCommentIdBySlideId((prev) => ({
      ...prev,
      [slideId]: null
    }));
  }, []);
  const handleSaveCommentEdit = (0, import_react.useCallback)(
    (slideIndex, commentId) => {
      if (!canEdit) {
        return;
      }
      const slide = slides[slideIndex];
      if (!slide) {
        return;
      }
      const draft = String(commentEditDraftByCommentId[commentId] || "").trim();
      if (draft.length === 0) {
        return;
      }
      let didUpdate = false;
      onUpdateSlides((prev) => {
        const result = editCommentInSlide(prev, slideIndex, commentId, draft);
        didUpdate = result.didUpdate;
        return result.slides;
      });
      if (!didUpdate) {
        return;
      }
      setEditingCommentIdBySlideId((prev) => ({
        ...prev,
        [slide.id]: null
      }));
      onMarkDirty();
    },
    [canEdit, commentEditDraftByCommentId, onMarkDirty, onUpdateSlides, slides]
  );
  const handleToggleCommentResolved = (0, import_react.useCallback)(
    (slideIndex, commentId) => {
      if (!canEdit) {
        return;
      }
      const slide = slides[slideIndex];
      if (!slide) {
        return;
      }
      let didUpdate = false;
      onUpdateSlides((prev) => {
        const result = toggleResolvedInSlide(prev, slideIndex, commentId);
        didUpdate = result.didUpdate;
        return result.slides;
      });
      if (didUpdate) {
        onMarkDirty();
      }
    },
    [canEdit, onMarkDirty, onUpdateSlides, slides]
  );
  (0, import_react.useEffect)(() => {
    const slideIds = new Set(slides.map((slide) => slide.id));
    setCommentDraftBySlideId((prev) => {
      const pruned = pruneSlideDrafts(prev, slideIds);
      return pruned ?? prev;
    });
  }, [slides]);
  return {
    commentDraftBySlideId,
    editingCommentIdBySlideId,
    commentEditDraftByCommentId,
    commentDraftMentionsBySlideId,
    replyDraftMentionsByCommentId,
    handleCommentDraftChange,
    handleAddSlideComment,
    handleDeleteSlideComment,
    handleStartCommentEdit,
    handleCancelCommentEdit,
    handleSaveCommentEdit,
    handleSetCommentEditDraft,
    handleToggleCommentResolved,
    ...reply
  };
}
function buildElementLookup(templateElements, slideElements) {
  const map = /* @__PURE__ */ new Map();
  for (const el of templateElements) {
    map.set(el.id, el);
  }
  for (const el of slideElements) {
    map.set(el.id, el);
  }
  return map;
}
function computeEffectiveSelectedIds(selectedElementId, selectedElementIds) {
  if (selectedElementIds.length > 0) {
    return selectedElementIds;
  }
  return selectedElementId ? [selectedElementId] : [];
}
function resolveActiveLayout(activeMaster, activeLayoutIndex) {
  if (activeLayoutIndex === null || !(activeMaster == null ? void 0 : activeMaster.layouts)) {
    return void 0;
  }
  return activeMaster.layouts[activeLayoutIndex];
}
function computeMasterViewElements(activeMaster, activeLayout) {
  if (activeLayout) {
    return activeLayout.elements ?? [];
  }
  if (activeMaster) {
    return activeMaster.elements ?? [];
  }
  return [];
}
function useDerivedElementState(input) {
  const {
    slides,
    activeSlideIndex,
    templateElementsBySlideId,
    selectedElementId,
    selectedElementIds,
    slideMasters,
    activeMasterIndex,
    activeLayoutIndex,
    notesMaster,
    handoutMaster
  } = input;
  const activeSlide = slides[activeSlideIndex];
  const templateElements = (0, import_react.useMemo)(() => {
    if (!activeSlide) {
      return [];
    }
    return [
      ...visibleTemplateElements(activeSlide, templateElementsBySlideId[activeSlide.id] ?? [])
    ];
  }, [activeSlide, templateElementsBySlideId]);
  const activeMaster = slideMasters[activeMasterIndex];
  const activeLayout = (0, import_react.useMemo)(
    () => resolveActiveLayout(activeMaster, activeLayoutIndex),
    [activeMaster, activeLayoutIndex]
  );
  const masterViewElements2 = (0, import_react.useMemo)(
    () => computeMasterViewElements(activeMaster, activeLayout),
    [activeMaster, activeLayout]
  );
  const notesMasterElements = (0, import_react.useMemo)(() => (notesMaster == null ? void 0 : notesMaster.elements) ?? [], [notesMaster]);
  const handoutMasterElements = (0, import_react.useMemo)(() => (handoutMaster == null ? void 0 : handoutMaster.elements) ?? [], [handoutMaster]);
  const elementLookup = (0, import_react.useMemo)(
    () => buildElementLookup(
      [
        ...templateElements,
        ...masterViewElements2,
        ...notesMasterElements,
        ...handoutMasterElements
      ],
      (activeSlide == null ? void 0 : activeSlide.elements) ?? []
    ),
    [activeSlide, templateElements, masterViewElements2, notesMasterElements, handoutMasterElements]
  );
  const selectedElement = (0, import_react.useMemo)(() => {
    if (!selectedElementId) {
      return null;
    }
    return elementLookup.get(selectedElementId) ?? null;
  }, [elementLookup, selectedElementId]);
  const effectiveSelectedIds = (0, import_react.useMemo)(
    () => computeEffectiveSelectedIds(selectedElementId, selectedElementIds),
    [selectedElementId, selectedElementIds]
  );
  const selectedElementIdSet = (0, import_react.useMemo)(() => new Set(effectiveSelectedIds), [effectiveSelectedIds]);
  const selectedElements = (0, import_react.useMemo)(
    () => effectiveSelectedIds.map((id) => elementLookup.get(id)).filter(Boolean),
    [effectiveSelectedIds, elementLookup]
  );
  return {
    activeSlide,
    templateElements,
    elementLookup,
    selectedElement,
    effectiveSelectedIds,
    selectedElementIdSet,
    selectedElements,
    activeMaster,
    activeLayout,
    masterViewElements: masterViewElements2,
    notesMasterElements,
    handoutMasterElements
  };
}
function useViewerCoreState(_input) {
  const containerRef = (0, import_react.useRef)(null);
  const imageInputRef = (0, import_react.useRef)(null);
  const mediaInputRef = (0, import_react.useRef)(null);
  const activeSlideIndexRef = (0, import_react.useRef)(0);
  const dragStateRef = (0, import_react.useRef)(null);
  const resizeStateRef = (0, import_react.useRef)(null);
  const shapeAdjustmentDragStateRef = (0, import_react.useRef)(null);
  const marqueeStateRef = (0, import_react.useRef)(null);
  const isDrawingRef = (0, import_react.useRef)(false);
  const justInteractedRef = (0, import_react.useRef)(false);
  const livePatcher = (0, import_react.useMemo)(() => createCollaborationLivePatcher(), []);
  (0, import_react.useEffect)(() => () => livePatcher.dispose(), [livePatcher]);
  const slidesRef = (0, import_react.useRef)([]);
  const [mode, setMode] = (0, import_react.useState)("edit");
  const [loading, setLoading] = (0, import_react.useState)(true);
  const [error, setError] = (0, import_react.useState)(null);
  const [slides, setSlides] = (0, import_react.useState)([]);
  const [templateElementsBySlideId, setTemplateElementsBySlideId] = (0, import_react.useState)({});
  const [canvasSize, setCanvasSize] = (0, import_react.useState)({
    width: DEFAULT_CANVAS_WIDTH,
    height: DEFAULT_CANVAS_HEIGHT
  });
  const [slideSizeEmu, setSlideSizeEmu] = (0, import_react.useState)();
  const [activeSlideIndex, setActiveSlideIndex] = (0, import_react.useState)(0);
  const [selectedElementId, setSelectedElementId] = (0, import_react.useState)(null);
  const [selectedElementIds, setSelectedElementIds] = (0, import_react.useState)([]);
  const [isDirty, setIsDirty] = (0, import_react.useState)(false);
  const inlineEditingElementIdRef = (0, import_react.useRef)(null);
  const inlineEditingTextRef = (0, import_react.useRef)("");
  const [inlineEditingElementId, setInlineEditingElementIdBase] = (0, import_react.useState)(null);
  const [inlineEditingText, setInlineEditingTextBase] = (0, import_react.useState)("");
  const setInlineEditingElementId = (0, import_react.useCallback)((value) => {
    const resolved = typeof value === "function" ? value(inlineEditingElementIdRef.current) : value;
    inlineEditingElementIdRef.current = resolved;
    setInlineEditingElementIdBase(value);
  }, []);
  const setInlineEditingText = (0, import_react.useCallback)(
    (value) => {
      const resolved = typeof value === "function" ? value(inlineEditingTextRef.current) : value;
      inlineEditingTextRef.current = resolved;
      setInlineEditingTextBase(value);
      publishLiveInlineText(
        livePatcher,
        slidesRef.current[activeSlideIndexRef.current],
        inlineEditingElementIdRef.current,
        resolved
      );
    },
    [livePatcher]
  );
  const [editTemplateMode, setEditTemplateMode] = (0, import_react.useState)(false);
  const [newShapeType, setNewShapeType] = (0, import_react.useState)("rect");
  const [clipboardPayload, setClipboardPayload] = (0, import_react.useState)(null);
  const [pointerCommitNonce, setPointerCommitNonce] = (0, import_react.useState)(0);
  const [headerFooter, setHeaderFooter] = (0, import_react.useState)({});
  const [layoutOptions, setLayoutOptions] = (0, import_react.useState)([]);
  const [slideMasters, setSlideMasters] = (0, import_react.useState)([]);
  const [modernCommentAuthors, setModernCommentAuthors] = (0, import_react.useState)([]);
  const [recentColors, setRecentColors] = (0, import_react.useState)([]);
  const [theme, setTheme] = (0, import_react.useState)();
  const [tableStyleMap, setTableStyleMap] = (0, import_react.useState)();
  const [tableStylesDefaultId, setTableStylesDefaultId] = (0, import_react.useState)();
  const [tableStylesToDelete, setTableStylesToDelete] = (0, import_react.useState)([]);
  const [themeOptions, setThemeOptions] = (0, import_react.useState)([]);
  const [customShows, setCustomShows] = (0, import_react.useState)([]);
  const [activeCustomShowId, setActiveCustomShowId] = (0, import_react.useState)(null);
  const [sections, setSections] = (0, import_react.useState)([]);
  const [presentationProperties, setPresentationProperties] = (0, import_react.useState)(
    {}
  );
  const [viewProperties, setViewProperties] = (0, import_react.useState)(void 0);
  const [notesMaster, setNotesMaster] = (0, import_react.useState)();
  const [handoutMaster, setHandoutMaster] = (0, import_react.useState)();
  const [notesCanvasSize, setNotesCanvasSize] = (0, import_react.useState)();
  const [customProperties, setCustomProperties] = (0, import_react.useState)([]);
  const [tagCollections, setTagCollections] = (0, import_react.useState)([]);
  const [coreProperties, setCoreProperties] = (0, import_react.useState)();
  const [appProperties, setAppProperties] = (0, import_react.useState)();
  const [embeddedFonts, setEmbeddedFonts] = (0, import_react.useState)([]);
  const [hasMacros, setHasMacros] = (0, import_react.useState)(false);
  const [hasDigitalSignatures, setHasDigitalSignatures] = (0, import_react.useState)(false);
  const [digitalSignatureCount, setDigitalSignatureCount] = (0, import_react.useState)(0);
  const [activeMasterIndex, setActiveMasterIndex] = (0, import_react.useState)(0);
  const [activeLayoutIndex, setActiveLayoutIndex] = (0, import_react.useState)(null);
  const [preMasterMode, setPreMasterMode] = (0, import_react.useState)("edit");
  const [masterViewTab, setMasterViewTab] = (0, import_react.useState)("slides");
  const [handoutSlidesPerPage, setHandoutSlidesPerPage] = (0, import_react.useState)(4);
  slidesRef.current = slides;
  const derived = useDerivedElementState({
    slides,
    activeSlideIndex,
    templateElementsBySlideId,
    selectedElementId,
    selectedElementIds,
    slideMasters,
    activeMasterIndex,
    activeLayoutIndex,
    notesMaster,
    handoutMaster
  });
  return {
    containerRef,
    imageInputRef,
    mediaInputRef,
    activeSlideIndexRef,
    inlineEditingElementIdRef,
    inlineEditingTextRef,
    dragStateRef,
    resizeStateRef,
    shapeAdjustmentDragStateRef,
    marqueeStateRef,
    isDrawingRef,
    justInteractedRef,
    livePatcher,
    mode,
    setMode,
    loading,
    setLoading,
    error,
    setError,
    slides,
    setSlides,
    templateElementsBySlideId,
    setTemplateElementsBySlideId,
    canvasSize,
    setCanvasSize,
    slideSizeEmu,
    setSlideSizeEmu,
    activeSlideIndex,
    setActiveSlideIndex,
    selectedElementId,
    setSelectedElementId,
    selectedElementIds,
    setSelectedElementIds,
    isDirty,
    setIsDirty,
    inlineEditingElementId,
    setInlineEditingElementId,
    inlineEditingText,
    setInlineEditingText,
    editTemplateMode,
    setEditTemplateMode,
    newShapeType,
    setNewShapeType,
    clipboardPayload,
    setClipboardPayload,
    pointerCommitNonce,
    setPointerCommitNonce,
    headerFooter,
    setHeaderFooter,
    layoutOptions,
    setLayoutOptions,
    slideMasters,
    setSlideMasters,
    modernCommentAuthors,
    setModernCommentAuthors,
    recentColors,
    setRecentColors,
    theme,
    setTheme,
    tableStyleMap,
    setTableStyleMap,
    tableStylesDefaultId,
    setTableStylesDefaultId,
    tableStylesToDelete,
    setTableStylesToDelete,
    themeOptions,
    setThemeOptions,
    customShows,
    setCustomShows,
    activeCustomShowId,
    setActiveCustomShowId,
    sections,
    setSections,
    presentationProperties,
    setPresentationProperties,
    viewProperties,
    setViewProperties,
    notesMaster,
    setNotesMaster,
    handoutMaster,
    setHandoutMaster,
    notesCanvasSize,
    setNotesCanvasSize,
    customProperties,
    setCustomProperties,
    tagCollections,
    setTagCollections,
    coreProperties,
    setCoreProperties,
    appProperties,
    setAppProperties,
    embeddedFonts,
    setEmbeddedFonts,
    hasMacros,
    setHasMacros,
    hasDigitalSignatures,
    setHasDigitalSignatures,
    digitalSignatureCount,
    setDigitalSignatureCount,
    activeMasterIndex,
    setActiveMasterIndex,
    activeLayoutIndex,
    setActiveLayoutIndex,
    preMasterMode,
    setPreMasterMode,
    masterViewTab,
    setMasterViewTab,
    handoutSlidesPerPage,
    setHandoutSlidesPerPage,
    ...derived
  };
}
function useViewerUIState() {
  const [isCompactToolbarOpen, setIsCompactToolbarOpen] = (0, import_react.useState)(false);
  const [toolbarSection, setToolbarSection] = (0, import_react.useState)("home");
  const [isSlidesPaneOpen, setIsSlidesPaneOpen] = (0, import_react.useState)(
    () => typeof window === "undefined" ? true : window.innerWidth >= 768
  );
  const [isInspectorPaneOpen, setIsInspectorPaneOpen] = (0, import_react.useState)(
    () => typeof window === "undefined" ? true : window.innerWidth >= 768
  );
  const [isSlideNotesCollapsed, setIsSlideNotesCollapsed] = (0, import_react.useState)(true);
  const [isOverflowMenuOpen, setIsOverflowMenuOpen] = (0, import_react.useState)(false);
  const [isSidebarCollapsed, setIsSidebarCollapsed] = (0, import_react.useState)(false);
  const [sidebarPanelMode, setSidebarPanelMode] = (0, import_react.useState)("properties");
  const [showSlideSorter, setShowSlideSorter] = (0, import_react.useState)(false);
  const [showReadingView, setShowReadingView] = (0, import_react.useState)(false);
  const [showOutlineView, setShowOutlineView] = (0, import_react.useState)(false);
  const [isShortcutHelpOpen, setIsShortcutHelpOpen] = (0, import_react.useState)(false);
  const [isAccessibilityPanelOpen, setIsAccessibilityPanelOpen] = (0, import_react.useState)(false);
  const [accessibilityIssues, setAccessibilityIssues] = (0, import_react.useState)([]);
  const [contextMenuState, setContextMenuState] = (0, import_react.useState)(null);
  const [tableEditorState, setTableEditorState] = (0, import_react.useState)(null);
  const [spellCheckEnabled, setSpellCheckEnabled] = (0, import_react.useState)(true);
  const [showGrid, setShowGrid] = (0, import_react.useState)(false);
  const [showRulers, setShowRulers] = (0, import_react.useState)(false);
  const [showGuides, setShowGuides] = (0, import_react.useState)(true);
  const [snapToGrid, setSnapToGrid] = (0, import_react.useState)(true);
  const [snapToShape, setSnapToShape] = (0, import_react.useState)(true);
  const [guides, setGuides] = (0, import_react.useState)(
    []
  );
  const [marqueeSelectionState, setMarqueeSelectionState] = (0, import_react.useState)(
    null
  );
  const [snapLines, setSnapLines] = (0, import_react.useState)([]);
  const [mediaDataUrls, setMediaDataUrls] = (0, import_react.useState)(/* @__PURE__ */ new Map());
  const [activeTool, setActiveTool] = (0, import_react.useState)("select");
  const [drawingColor, setDrawingColor] = (0, import_react.useState)("#000000");
  const [drawingWidth, setDrawingWidth] = (0, import_react.useState)(3);
  const [isThemeEditorOpen, setIsThemeEditorOpen] = (0, import_react.useState)(false);
  const [isThemeGalleryOpen, setIsThemeGalleryOpen] = (0, import_react.useState)(false);
  const [formatPainterActive, setFormatPainterActive] = (0, import_react.useState)(false);
  const [isSelectionPaneOpen, setIsSelectionPaneOpen] = (0, import_react.useState)(false);
  const [eyedropperActive, setEyedropperActive] = (0, import_react.useState)(false);
  return {
    isCompactToolbarOpen,
    setIsCompactToolbarOpen,
    toolbarSection,
    setToolbarSection,
    isSlidesPaneOpen,
    setIsSlidesPaneOpen,
    isInspectorPaneOpen,
    setIsInspectorPaneOpen,
    isSlideNotesCollapsed,
    setIsSlideNotesCollapsed,
    isOverflowMenuOpen,
    setIsOverflowMenuOpen,
    isSidebarCollapsed,
    setIsSidebarCollapsed,
    sidebarPanelMode,
    setSidebarPanelMode,
    showSlideSorter,
    setShowSlideSorter,
    showReadingView,
    setShowReadingView,
    showOutlineView,
    setShowOutlineView,
    isShortcutHelpOpen,
    setIsShortcutHelpOpen,
    isAccessibilityPanelOpen,
    setIsAccessibilityPanelOpen,
    accessibilityIssues,
    setAccessibilityIssues,
    contextMenuState,
    setContextMenuState,
    tableEditorState,
    setTableEditorState,
    spellCheckEnabled,
    setSpellCheckEnabled,
    showGrid,
    setShowGrid,
    showRulers,
    setShowRulers,
    showGuides,
    setShowGuides,
    snapToGrid,
    setSnapToGrid,
    snapToShape,
    setSnapToShape,
    guides,
    setGuides,
    marqueeSelectionState,
    setMarqueeSelectionState,
    snapLines,
    setSnapLines,
    mediaDataUrls,
    setMediaDataUrls,
    activeTool,
    setActiveTool,
    drawingColor,
    setDrawingColor,
    drawingWidth,
    setDrawingWidth,
    isThemeEditorOpen,
    setIsThemeEditorOpen,
    isThemeGalleryOpen,
    setIsThemeGalleryOpen,
    formatPainterActive,
    setFormatPainterActive,
    isSelectionPaneOpen,
    setIsSelectionPaneOpen,
    eyedropperActive,
    setEyedropperActive
  };
}
function useViewerState(input) {
  const core = useViewerCoreState();
  const ui = useViewerUIState();
  return { ...core, ...ui };
}
function useLoadContent({
  content,
  clearSelection,
  history,
  setSlides,
  setTemplateElementsBySlideId,
  mediaDataUrls,
  setCanvasSize,
  setSlideSizeEmu,
  setHeaderFooter,
  setLayoutOptions,
  setSlideMasters,
  setModernCommentAuthors,
  setRecentColors,
  setTheme,
  setTableStyleMap,
  setTableStylesDefaultId,
  setTableStylesToDelete,
  setThemeOptions,
  setCustomShows,
  setActiveCustomShowId,
  setSections,
  setPresentationProperties,
  setViewProperties,
  setNotesMaster,
  setHandoutMaster,
  setNotesCanvasSize,
  setCustomProperties,
  setTagCollections,
  setCoreProperties,
  setAppProperties,
  setEmbeddedFonts,
  setActiveSlideIndex,
  setHasMacros,
  setHasDigitalSignatures,
  setDigitalSignatureCount,
  setGuides,
  setReadOnlyRecommendation,
  setModifyVerifier,
  setCompatToasts,
  setLoading,
  setError,
  setIsDirty,
  setIsEncrypted,
  onContentApplied,
  allowExternalImages
}) {
  const handlerRef = (0, import_react.useRef)(null);
  const originalBufferRef = (0, import_react.useRef)(null);
  const renderTokenRef = (0, import_react.useRef)(0);
  (0, import_react.useEffect)(() => {
    if (!content) {
      return;
    }
    let cancelled = false;
    const token = ++renderTokenRef.current;
    const loadBlobUrls = [];
    (async () => {
      var _a;
      try {
        setLoading(true);
        setError(null);
        const buffer = content instanceof Uint8Array ? content.buffer.slice(content.byteOffset, content.byteOffset + content.byteLength) : content;
        originalBufferRef.current = buffer instanceof ArrayBuffer ? buffer : null;
        const fileSizeMB = buffer instanceof ArrayBuffer ? buffer.byteLength / (1024 * 1024) : 0;
        if (fileSizeMB > 50) {
          console.warn(
            `[pptx] Large file detected (${fileSizeMB.toFixed(1)} MB). Loading may use significant memory.`
          );
        }
        const previousHandler = handlerRef.current;
        const handler = new PptxHandler();
        const parsed = await handler.load(buffer, { allowExternalImages });
        if (cancelled || token !== renderTokenRef.current) {
          handler.dispose();
          return;
        }
        if (previousHandler) {
          previousHandler.dispose();
        }
        handlerRef.current = null;
        const mediaElements = [];
        for (const slide of parsed.slides) {
          collectMediaElements(slide.elements, mediaElements);
        }
        for (const url of mediaDataUrls.values()) {
          if (url.startsWith("blob:")) {
            URL.revokeObjectURL(url);
          }
        }
        mediaDataUrls.clear();
        await Promise.all(
          mediaElements.map(async (mediaElement) => {
            const resolved = await resolveMediaElementSource(mediaElement, handler);
            if (resolved.missing || !resolved.mediaPath || !resolved.url) {
              mediaElement.mediaMissing = true;
              return;
            }
            mediaDataUrls.set(resolved.mediaPath, resolved.url);
            if (resolved.isBlobUrl) {
              loadBlobUrls.push(resolved.url);
            }
          })
        );
        const { paths: imagePaths, refs: imageRefs } = collectImagePaths(parsed.slides);
        let nextSlides = parsed.slides;
        if (imagePaths.size > 0) {
          const resolvedMap = /* @__PURE__ */ new Map();
          await Promise.all(
            Array.from(imagePaths).map(async (path) => {
              try {
                const url = await handler.getImageData(path);
                if (url) {
                  resolvedMap.set(path, url);
                }
              } catch {
              }
            })
          );
          nextSlides = parsed.slides.map((s) => {
            const newElements = applyImagePathPatches(s.elements, resolvedMap, imageRefs);
            return newElements === s.elements ? s : { ...s, elements: newElements };
          });
        }
        nextSlides = await resolveTableCellImageUrls(
          nextSlides,
          (path) => handler.getImageData(path)
        );
        const nextTableStyleMap = await resolveTableStyleImageUrls(
          parsed.tableStyleMap,
          (path) => handler.getImageData(path)
        );
        handlerRef.current = handler;
        const partition = partitionTemplateElements(nextSlides);
        setSlides(partition.slides);
        setTemplateElementsBySlideId(partition.templateElementsBySlideId);
        setCanvasSize({
          width: parsed.width ?? DEFAULT_CANVAS_WIDTH,
          height: parsed.height ?? DEFAULT_CANVAS_HEIGHT
        });
        setSlideSizeEmu(
          typeof parsed.widthEmu === "number" && typeof parsed.heightEmu === "number" && parsed.widthEmu > 0 && parsed.heightEmu > 0 ? {
            widthEmu: parsed.widthEmu,
            heightEmu: parsed.heightEmu,
            type: parsed.slideSizeType ?? ""
          } : void 0
        );
        setHeaderFooter(parsed.headerFooter ?? {});
        setLayoutOptions(parsed.layoutOptions ?? []);
        setSlideMasters(parsed.slideMasters ?? []);
        setModernCommentAuthors(parsed.modernCommentAuthors ?? []);
        setRecentColors(seedRecentColors({ mruColors: parsed.mruColors }));
        setTheme(parsed.theme);
        setTableStyleMap(nextTableStyleMap);
        setTableStylesDefaultId(parsed.tableStylesDefaultId);
        setTableStylesToDelete([]);
        setThemeOptions(parsed.themeOptions ?? []);
        setCustomShows(parsed.customShows ?? []);
        setActiveCustomShowId(
          resolveAuthoredCustomShowId(parsed.presentationProperties, parsed.customShows) ?? null
        );
        setSections(parsed.sections ?? []);
        setPresentationProperties(parsed.presentationProperties ?? {});
        setViewProperties(parsed.viewProperties);
        setNotesMaster(parsed.notesMaster);
        setHandoutMaster(parsed.handoutMaster);
        if (typeof parsed.notesWidthEmu === "number" && typeof parsed.notesHeightEmu === "number" && parsed.notesWidthEmu > 0 && parsed.notesHeightEmu > 0) {
          setNotesCanvasSize({
            width: Math.round(parsed.notesWidthEmu / 9525),
            height: Math.round(parsed.notesHeightEmu / 9525)
          });
        } else {
          setNotesCanvasSize(void 0);
        }
        setCustomProperties(parsed.customProperties ?? []);
        setTagCollections(parsed.tags ?? []);
        setCoreProperties(parsed.coreProperties);
        setAppProperties(parsed.appProperties);
        setEmbeddedFonts(parsed.embeddedFonts ?? []);
        setHasMacros(parsed.hasMacros === true);
        setHasDigitalSignatures(parsed.hasDigitalSignatures === true);
        setDigitalSignatureCount(parsed.digitalSignatureCount ?? 0);
        setGuides(buildInitialGuides(parsed.presentationGuides, (_a = parsed.slides[0]) == null ? void 0 : _a.guides));
        setReadOnlyRecommendation(readOnlyRecommendation(parsed));
        setModifyVerifier(parsed.modifyVerifier ?? void 0);
        setCompatToasts(
          compatibilityWarningToasts([
            ...parsed.warnings ?? [],
            ...parsed.slides.flatMap((slide) => slide.warnings ?? [])
          ])
        );
        setActiveSlideIndex(0);
        clearSelection();
        setIsDirty(false);
        history.resetHistory();
        onContentApplied == null ? void 0 : onContentApplied();
      } catch (err) {
        if (!cancelled && token === renderTokenRef.current) {
          if (err instanceof EncryptedFileError) {
            setIsEncrypted(true);
          } else {
            console.error("[pptx] Failed to load presentation content:", err);
            setError(err instanceof Error ? err.message : String(err));
          }
        }
      } finally {
        if (!cancelled && token === renderTokenRef.current) {
          setLoading(false);
        }
      }
    })();
    return () => {
      cancelled = true;
      for (const url of loadBlobUrls) {
        URL.revokeObjectURL(url);
      }
      if (handlerRef.current) {
        handlerRef.current.dispose();
        handlerRef.current = null;
      }
    };
  }, [content]);
  return { handlerRef };
}
function useAutosave(input) {
  const { isDirty, filePath, serializeSlides, enabled = true, getChangeSources } = input;
  const intervalMs = resolveAutosaveIntervalMs({
    hostIntervalMs: input.intervalMs,
    optionsIntervalSeconds: input.intervalSeconds
  });
  const [autosaveStatus, setAutosaveStatus] = (0, import_react.useState)({
    state: "idle"
  });
  const isDirtyRef = (0, import_react.useRef)(isDirty);
  const filePathRef = (0, import_react.useRef)(filePath);
  const serializeRef = (0, import_react.useRef)(serializeSlides);
  const isSavingRef = (0, import_react.useRef)(false);
  const changeSourcesRef = (0, import_react.useRef)(getChangeSources);
  const lastSnapshotRef = (0, import_react.useRef)(void 0);
  (0, import_react.useEffect)(() => {
    isDirtyRef.current = isDirty;
  }, [isDirty]);
  (0, import_react.useEffect)(() => {
    filePathRef.current = filePath;
  }, [filePath]);
  (0, import_react.useEffect)(() => {
    serializeRef.current = serializeSlides;
  }, [serializeSlides]);
  (0, import_react.useEffect)(() => {
    changeSourcesRef.current = getChangeSources;
  }, [getChangeSources]);
  const doAutosave = (0, import_react.useCallback)(async (options) => {
    var _a;
    const path = filePathRef.current;
    if (!path) {
      return;
    }
    const sources = ((_a = changeSourcesRef.current) == null ? void 0 : _a.call(changeSourcesRef)) ?? [];
    if (!shouldWriteAutosaveSnapshot({
      filePath: path,
      isDirty: isDirtyRef.current,
      saving: isSavingRef.current,
      sources: (options == null ? void 0 : options.polled) === true ? sources : [],
      lastSnapshot: lastSnapshotRef.current
    })) {
      return;
    }
    isSavingRef.current = true;
    setAutosaveStatus({ state: "saving" });
    try {
      const data = await serializeRef.current();
      if (!data) {
        setAutosaveStatus({ state: "idle" });
        isSavingRef.current = false;
        return;
      }
      await saveAutosaveSnapshot(path, data);
      lastSnapshotRef.current = autosaveSnapshotMark(path, sources);
      setAutosaveStatus({ state: "saved", timestamp: Date.now() });
    } catch (err) {
      setAutosaveStatus({
        state: "error",
        message: err instanceof Error ? err.message : "Autosave failed"
      });
    } finally {
      isSavingRef.current = false;
    }
  }, []);
  (0, import_react.useEffect)(() => {
    if (!enabled) {
      setAutosaveStatus({ state: "disabled", reason: "autosave_toggle_off" });
      return;
    }
    if (!filePath) {
      setAutosaveStatus({
        state: "disabled",
        reason: "no_file_path"
      });
      return;
    }
    setAutosaveStatus((prev) => prev.state === "disabled" ? { state: "idle" } : prev);
    const id = setInterval(() => {
      void doAutosave({ polled: true });
    }, intervalMs);
    return () => clearInterval(id);
  }, [enabled, filePath, intervalMs, doAutosave]);
  return {
    autosaveStatus,
    triggerAutosave: () => doAutosave()
  };
}
function useElementOperations(input) {
  const {
    activeSlide,
    activeSlideIndex,
    selectedElement,
    selectedElementId,
    editTemplateMode,
    templateElements,
    masterView,
    history,
    setSlides,
    setTemplateElementsBySlideId,
    setSelectedElementId,
    setSelectedElementIds,
    setInlineEditingElementId,
    setContextMenuState,
    inlineEditingElementId,
    inlineEditingText
  } = input;
  const liveTextSegments = (0, import_react.useCallback)(() => {
    if (!selectedElement || !hasTextProperties(selectedElement)) {
      return void 0;
    }
    if (selectedElement.id !== inlineEditingElementId) {
      return selectedElement.textSegments;
    }
    return remapTextToSegments(
      inlineEditingText,
      selectedElement.textSegments,
      selectedElement.textStyle
    );
  }, [selectedElement, inlineEditingElementId, inlineEditingText]);
  const applySelection = (0, import_react.useCallback)(
    (primaryId, ids = []) => {
      setSelectedElementId(primaryId);
      setSelectedElementIds(ids);
    },
    [setSelectedElementId, setSelectedElementIds]
  );
  const clearSelection = (0, import_react.useCallback)(() => {
    applySelection(null, []);
    setInlineEditingElementId(null);
    setContextMenuState(null);
  }, [applySelection, setInlineEditingElementId, setContextMenuState]);
  const applyMasterViewWrite = (0, import_react.useCallback)(
    (write) => {
      if (!write || !masterView) {
        return false;
      }
      if (write.slideMasters) {
        masterView.setSlideMasters(write.slideMasters);
      }
      if (write.notesMaster) {
        masterView.setNotesMaster(write.notesMaster);
      }
      if (write.handoutMaster) {
        masterView.setHandoutMaster(write.handoutMaster);
      }
      history.markDirty();
      return true;
    },
    [history, masterView]
  );
  const updateElementById = (0, import_react.useCallback)(
    (elementId, updates) => {
      if (masterView == null ? void 0 : masterView.target) {
        applyMasterViewWrite(
          updateMasterViewElement(masterView, masterView.target, elementId, updates)
        );
        return;
      }
      if (isTemplateElementId3(elementId)) {
        const slideId = activeSlide == null ? void 0 : activeSlide.id;
        if (slideId) {
          setTemplateElementsBySlideId((prev) => ({
            ...prev,
            [slideId]: (prev[slideId] ?? []).map(
              (el) => el.id === elementId ? { ...el, ...updates } : el
            )
          }));
        }
      } else {
        setSlides((prev) => updateElement(prev, activeSlideIndex, elementId, updates));
      }
      history.markDirty();
    },
    [
      activeSlide == null ? void 0 : activeSlide.id,
      activeSlideIndex,
      applyMasterViewWrite,
      history,
      masterView,
      setSlides,
      setTemplateElementsBySlideId
    ]
  );
  const updateSelectedElement = (0, import_react.useCallback)(
    (updates) => {
      if (!selectedElementId) {
        return;
      }
      updateElementById(selectedElementId, updates);
    },
    [selectedElementId, updateElementById]
  );
  const updateSelectedShapeStyle = (0, import_react.useCallback)(
    (updates) => {
      if (!selectedElement || !hasShapeProperties(selectedElement)) {
        return;
      }
      updateSelectedElement({
        shapeStyle: { ...selectedElement.shapeStyle, ...updates }
      });
    },
    [selectedElement, updateSelectedElement]
  );
  const updateSelectedTextStyle = (0, import_react.useCallback)(
    (updates) => {
      if (!selectedElement || !hasTextProperties(selectedElement)) {
        return;
      }
      const isLiveEditing = selectedElement.id === inlineEditingElementId;
      const currentSegments = liveTextSegments();
      const inlineSel = getInlineEditorSelection(currentSegments);
      if (inlineSel && currentSegments) {
        const { newSegments: newSegments2, newSelection } = applyStyleToSelectedSegments(
          currentSegments,
          inlineSel,
          updates
        );
        setPendingSelectionRestore(newSelection);
        updateSelectedElement({
          textSegments: newSegments2,
          ...isLiveEditing ? { text: inlineEditingText } : {}
        });
        return;
      }
      const newTextStyle = { ...selectedElement.textStyle, ...updates };
      const newSegments = currentSegments == null ? void 0 : currentSegments.map((seg) => ({
        ...seg,
        style: { ...seg.style, ...updates }
      }));
      updateSelectedElement({
        textStyle: newTextStyle,
        textSegments: newSegments,
        ...isLiveEditing ? { text: inlineEditingText } : {}
      });
    },
    [
      selectedElement,
      updateSelectedElement,
      inlineEditingElementId,
      inlineEditingText,
      liveTextSegments
    ]
  );
  const updateSelectedTextCase = (0, import_react.useCallback)(
    (mode) => {
      if (!selectedElement || !hasTextProperties(selectedElement)) {
        return;
      }
      const isLiveEditing = selectedElement.id === inlineEditingElementId;
      const currentSegments = liveTextSegments();
      const inlineSel = getInlineEditorSelection(currentSegments);
      if (inlineSel && currentSegments) {
        const newSegments = applyCaseTransformToSegments(currentSegments, inlineSel, mode);
        updateSelectedElement({
          textSegments: newSegments,
          ...isLiveEditing ? { text: inlineEditingText } : {}
        });
        return;
      }
      const updates = {};
      if (currentSegments && currentSegments.length > 0) {
        updates.textSegments = applyCaseTransformToSegments(
          currentSegments,
          null,
          mode
        );
      }
      const baseText = isLiveEditing ? inlineEditingText : selectedElement.text;
      if (typeof baseText === "string") {
        updates.text = transformTextCase(baseText, mode);
      }
      updateSelectedElement(updates);
    },
    [
      selectedElement,
      updateSelectedElement,
      inlineEditingElementId,
      inlineEditingText,
      liveTextSegments
    ]
  );
  const updateSlides = (0, import_react.useCallback)(
    (updater) => {
      setSlides((prev) => updater(prev));
    },
    [setSlides]
  );
  const masterViewTarget = (masterView == null ? void 0 : masterView.target) ?? null;
  const activeElements = masterViewTarget ? masterViewElements(masterView, masterViewTarget) : editTemplateMode ? templateElements : (activeSlide == null ? void 0 : activeSlide.elements) ?? [];
  const updateActiveElements = (0, import_react.useCallback)(
    (updater) => {
      if (masterView == null ? void 0 : masterView.target) {
        applyMasterViewWrite(
          replaceMasterViewElements(
            masterView,
            masterView.target,
            updater(masterViewElements(masterView, masterView.target))
          )
        );
        return;
      }
      if (editTemplateMode) {
        const slideId = activeSlide == null ? void 0 : activeSlide.id;
        if (!slideId) {
          return;
        }
        setTemplateElementsBySlideId((prev) => ({
          ...prev,
          [slideId]: updater(prev[slideId] ?? [])
        }));
      } else {
        setSlides(
          (prev) => prev.map(
            (s, i) => i === activeSlideIndex ? { ...s, elements: updater(s.elements) } : s
          )
        );
      }
    },
    [
      applyMasterViewWrite,
      editTemplateMode,
      masterView,
      activeSlide == null ? void 0 : activeSlide.id,
      activeSlideIndex,
      setSlides,
      setTemplateElementsBySlideId
    ]
  );
  const serializeSlides = (0, import_react.useCallback)(async () => {
    return null;
  }, []);
  return {
    applySelection,
    clearSelection,
    updateElementById,
    updateSelectedElement,
    updateSelectedShapeStyle,
    updateSelectedTextStyle,
    updateSelectedTextCase,
    updateSlides,
    activeElements,
    updateActiveElements,
    serializeSlides
  };
}
function useSectionOperations(input) {
  const { sections, setSections, slides, setSlides, markDirty } = input;
  const addSection2 = (0, import_react.useCallback)(
    (name, afterSlideIndex) => {
      const result = addSection(sections, slides, name, afterSlideIndex);
      setSlides(result.slides);
      setSections(result.sections);
      markDirty();
    },
    [sections, slides, setSlides, setSections, markDirty]
  );
  const renameSection2 = (0, import_react.useCallback)(
    (sectionId, newName) => {
      const result = renameSection(sections, slides, sectionId, newName);
      setSections(result.sections);
      setSlides(result.slides);
      markDirty();
    },
    [sections, slides, setSections, setSlides, markDirty]
  );
  const deleteSection2 = (0, import_react.useCallback)(
    (sectionId) => {
      const result = deleteSection(sections, slides, sectionId);
      setSections(result.sections);
      setSlides(result.slides);
      markDirty();
    },
    [sections, slides, setSections, setSlides, markDirty]
  );
  const moveSectionUp2 = (0, import_react.useCallback)(
    (sectionId) => {
      setSections(moveSectionUp(sections, sectionId));
      markDirty();
    },
    [sections, setSections, markDirty]
  );
  const moveSectionDown2 = (0, import_react.useCallback)(
    (sectionId) => {
      setSections(moveSectionDown(sections, sectionId));
      markDirty();
    },
    [sections, setSections, markDirty]
  );
  const moveSlidesToSection2 = (0, import_react.useCallback)(
    (slideIndexes, targetSectionId) => {
      const result = moveSlidesToSection(sections, slides, slideIndexes, targetSectionId);
      setSlides(result.slides);
      setSections(result.sections);
      markDirty();
    },
    [sections, slides, setSlides, setSections, markDirty]
  );
  const toggleSectionCollapse = (0, import_react.useCallback)(
    (sectionId) => {
      setSections(
        (prev) => prev.map((sec) => sec.id === sectionId ? { ...sec, collapsed: !sec.collapsed } : sec)
      );
      markDirty();
    },
    [setSections, markDirty]
  );
  return {
    addSection: addSection2,
    renameSection: renameSection2,
    deleteSection: deleteSection2,
    moveSectionUp: moveSectionUp2,
    moveSectionDown: moveSectionDown2,
    moveSlidesToSection: moveSlidesToSection2,
    toggleSectionCollapse
  };
}
var VIEWPORT_SELECTOR = "[data-pptx-viewport]";
function findSelectionHandleHost(elementNode, elementId) {
  const scope = elementNode.closest(VIEWPORT_SELECTOR) ?? elementNode.parentElement;
  if (!scope) {
    return null;
  }
  for (const candidate of scope.querySelectorAll("[data-pptx-handle-for]")) {
    if (candidate.getAttribute("data-pptx-handle-for") === elementId) {
      return candidate;
    }
  }
  return null;
}
function syncSelectionHandleOverlay(elementNode, elementId, geometry) {
  const host = findSelectionHandleHost(elementNode, elementId);
  if (!host) {
    return;
  }
  if (geometry.x !== void 0) {
    host.style.left = `${geometry.x}px`;
  }
  if (geometry.y !== void 0) {
    host.style.top = `${geometry.y}px`;
  }
  if (geometry.width !== void 0) {
    host.style.width = `${geometry.width}px`;
  }
  if (geometry.height !== void 0) {
    host.style.height = `${geometry.height}px`;
  }
  if (geometry.transform !== void 0) {
    host.style.transform = geometry.transform;
  }
}
function computeResizeGeometry(handle, startX, startY, startWidth, startHeight, dx, dy, snapToGrid, gridSpacingPx, lockAspect = false) {
  const startBox = { x: startX, y: startY, width: startWidth, height: startHeight };
  let resized = applyResize(startBox, handle, dx, dy, 1, { minSize: MIN_ELEMENT_SIZE });
  if (lockAspect) {
    resized = lockResizeAspect(resized, startBox, handle, MIN_ELEMENT_SIZE);
  }
  const box = { x: resized.x, y: resized.y, width: resized.width, height: resized.height };
  if (!snapToGrid) {
    return box;
  }
  return snapBoxToGrid(box, handle, gridSpacingPx, MIN_ELEMENT_SIZE);
}
function resolveLiveTarget(input) {
  const { livePatcher, editTemplateMode, activeSlide } = input;
  if (!livePatcher || editTemplateMode || !livePatcher.isActive()) {
    return void 0;
  }
  return { patcher: livePatcher, slideId: activeSlide == null ? void 0 : activeSlide.id };
}
function processPointerMove(e, input, tracker) {
  const {
    editorScale,
    canvasStageRef,
    canvasSize,
    snapToGrid,
    snapToShape,
    gridSpacingPx,
    editTemplateMode,
    templateElements,
    activeSlide,
    guides,
    elementLookup,
    marqueeStateRef,
    dragStateRef,
    resizeStateRef,
    shapeAdjustmentDragStateRef,
    setMarqueeSelectionState,
    setSnapLines,
    updateElementById
  } = input;
  const marquee = marqueeStateRef.current;
  if (marquee) {
    processMarqueeMove(
      e,
      marquee,
      canvasStageRef,
      canvasSize,
      editorScale,
      setMarqueeSelectionState
    );
    return;
  }
  const drag = dragStateRef.current;
  if (drag) {
    processDragMove(
      e,
      drag,
      editorScale,
      snapToGrid,
      snapToShape,
      gridSpacingPx,
      editTemplateMode,
      templateElements,
      activeSlide,
      guides,
      elementLookup,
      tracker,
      setSnapLines,
      resolveLiveTarget(input)
    );
    return;
  }
  const rs = resizeStateRef.current;
  if (rs) {
    processResizeMove(e, rs, editorScale, snapToGrid, gridSpacingPx, resolveLiveTarget(input));
    return;
  }
  const adj = shapeAdjustmentDragStateRef.current;
  if (adj) {
    processAdjustmentMove(e, adj, editorScale, updateElementById);
  }
}
function processMarqueeMove(e, marquee, canvasStageRef, canvasSize, editorScale, setMarqueeSelectionState) {
  const stage = canvasStageRef.current;
  if (!stage) {
    return;
  }
  const rect = stage.getBoundingClientRect();
  marquee.currentX = Math.max(0, Math.min(canvasSize.width, (e.clientX - rect.left) / editorScale));
  marquee.currentY = Math.max(0, Math.min(canvasSize.height, (e.clientY - rect.top) / editorScale));
  setMarqueeSelectionState({ ...marquee });
}
function processDragMove(e, drag, editorScale, snapToGrid, snapToShape, gridSpacingPx, editTemplateMode, templateElements, activeSlide, guides, elementLookup, tracker, setSnapLines, live) {
  const dx = (e.clientX - drag.startClientX) / editorScale;
  const dy = (e.clientY - drag.startClientY) / editorScale;
  if (!drag.moved && (Math.abs(dx) > 2 || Math.abs(dy) > 2)) {
    drag.moved = true;
  }
  if (!drag.moved) {
    return;
  }
  let appliedDx = dx;
  let appliedDy = dy;
  const draggedIds = Object.keys(drag.startPositionsById);
  const primaryId = draggedIds[0];
  const primaryStart = primaryId ? drag.startPositionsById[primaryId] : void 0;
  const primaryEl = primaryId ? elementLookup.get(primaryId) : void 0;
  if (primaryStart && primaryEl) {
    let targetX = primaryStart.x + appliedDx;
    let targetY = primaryStart.y + appliedDy;
    if (snapToGrid) {
      targetX = Math.round(targetX / gridSpacingPx) * gridSpacingPx;
      targetY = Math.round(targetY / gridSpacingPx) * gridSpacingPx;
    }
    if (snapToShape) {
      const siblingSource = editTemplateMode ? templateElements : (activeSlide == null ? void 0 : activeSlide.elements) ?? [];
      const siblings = siblingSource.map((el) => ({
        id: el.id,
        x: el.x,
        y: el.y,
        width: el.width,
        height: el.height
      }));
      const snapResult = computeSnapToShape(
        targetX,
        targetY,
        primaryEl.width,
        primaryEl.height,
        siblings,
        new Set(draggedIds),
        guides.map((g) => ({ axis: g.axis, position: g.position }))
      );
      targetX = snapResult.x;
      targetY = snapResult.y;
      const newSnapLines = snapResult.lines.map((line) => ({
        axis: line.axis === "v" ? "x" : "y",
        position: line.position
      }));
      const newKey = JSON.stringify(newSnapLines);
      if (newKey !== tracker.lastSnapLinesKey) {
        tracker.lastSnapLinesKey = newKey;
        setSnapLines(newSnapLines);
      }
    } else if (tracker.lastSnapLinesKey !== "[]") {
      tracker.lastSnapLinesKey = "[]";
      setSnapLines([]);
    }
    appliedDx = targetX - primaryStart.x;
    appliedDy = targetY - primaryStart.y;
  }
  drag.lastDx = appliedDx;
  drag.lastDy = appliedDy;
  for (const [id, domEl] of drag.domEls) {
    const start = drag.startPositionsById[id];
    if (start) {
      const x = start.x + appliedDx;
      const y = start.y + appliedDy;
      domEl.style.left = `${x}px`;
      domEl.style.top = `${y}px`;
      syncSelectionHandleOverlay(domEl, id, { x, y });
    }
  }
  if (live) {
    for (const id of Object.keys(drag.startPositionsById)) {
      const start = drag.startPositionsById[id];
      publishLiveGeometry(live.patcher, live.slideId, id, {
        x: start.x + appliedDx,
        y: start.y + appliedDy
      });
    }
  }
}
function processResizeMove(e, rs, editorScale, snapToGrid, gridSpacingPx, live) {
  const dx = (e.clientX - rs.startClientX) / editorScale;
  const dy = (e.clientY - rs.startClientY) / editorScale;
  if (!rs.moved && (Math.abs(dx) > 2 || Math.abs(dy) > 2)) {
    rs.moved = true;
  }
  if (!rs.moved) {
    return;
  }
  const geo = computeResizeGeometry(
    rs.handle,
    rs.startX,
    rs.startY,
    rs.startWidth,
    rs.startHeight,
    dx,
    dy,
    snapToGrid,
    gridSpacingPx,
    e.shiftKey
  );
  rs.lastX = geo.x;
  rs.lastY = geo.y;
  rs.lastWidth = geo.width;
  rs.lastHeight = geo.height;
  const width = Math.max(geo.width, MIN_ELEMENT_SIZE);
  const height = Math.max(geo.height, MIN_ELEMENT_SIZE);
  if (rs.domEl) {
    rs.domEl.style.left = `${geo.x}px`;
    rs.domEl.style.top = `${geo.y}px`;
    rs.domEl.style.width = `${width}px`;
    rs.domEl.style.height = `${height}px`;
    syncSelectionHandleOverlay(rs.domEl, rs.elementId, { x: geo.x, y: geo.y, width, height });
  }
  if (live) {
    publishLiveGeometry(live.patcher, live.slideId, rs.elementId, {
      x: geo.x,
      y: geo.y,
      width,
      height
    });
  }
}
function processAdjustmentMove(e, adj, editorScale, updateElementById) {
  const dx = (e.clientX - adj.startClientX) / editorScale;
  const dy = (e.clientY - adj.startClientY) / editorScale;
  const adjustments = getDraggedShapeAdjustments(adj, dx, dy);
  if (!adj.moved && Math.hypot(dx, dy) > 2) {
    adj.moved = true;
  }
  if (adj.moved) {
    updateElementById(adj.elementId, {
      shapeAdjustments: adjustments
    });
  }
}
function processPointerUp(input) {
  const {
    editTemplateMode,
    templateElements,
    activeSlide,
    activeSlideIndex,
    marqueeStateRef,
    dragStateRef,
    resizeStateRef,
    shapeAdjustmentDragStateRef,
    justInteractedRef,
    setMarqueeSelectionState,
    setSnapLines,
    setTemplateElementsBySlideId,
    setPointerCommitNonce,
    applySelection,
    clearSelection,
    updateSlides,
    updateElementById,
    markDirty,
    livePatcher
  } = input;
  const marquee = marqueeStateRef.current;
  const drag = dragStateRef.current;
  const rs = resizeStateRef.current;
  const adj = shapeAdjustmentDragStateRef.current;
  if (marquee) {
    commitMarquee(
      marquee,
      editTemplateMode,
      templateElements,
      activeSlide,
      applySelection,
      clearSelection
    );
    marqueeStateRef.current = null;
    setMarqueeSelectionState(null);
  }
  if (drag == null ? void 0 : drag.moved) {
    commitDrag(
      drag,
      editTemplateMode,
      activeSlide,
      activeSlideIndex,
      setTemplateElementsBySlideId,
      updateSlides
    );
  }
  if (rs == null ? void 0 : rs.moved) {
    const resizedId = rs.elementId;
    const resizeUpdates = {
      x: rs.lastX,
      y: rs.lastY,
      width: rs.lastWidth,
      height: rs.lastHeight
    };
    updateElementById(resizedId, resizeUpdates);
    const movedIds = /* @__PURE__ */ new Set([resizedId]);
    updateSlides(
      (prev) => prev.map((s, i) => {
        if (i !== activeSlideIndex) {
          return s;
        }
        const rerouted = rerouteConnectorsForMovedElements(s.elements, movedIds);
        if (rerouted.length === 0) {
          return s;
        }
        return {
          ...s,
          elements: applyReroutedConnectors(s.elements, rerouted)
        };
      })
    );
  }
  const wasMoved = (drag == null ? void 0 : drag.moved) || (rs == null ? void 0 : rs.moved) || (adj == null ? void 0 : adj.moved);
  if (wasMoved) {
    justInteractedRef.current = true;
  }
  if (wasMoved) {
    livePatcher == null ? void 0 : livePatcher.flush();
  }
  marqueeStateRef.current = null;
  dragStateRef.current = null;
  resizeStateRef.current = null;
  shapeAdjustmentDragStateRef.current = null;
  setMarqueeSelectionState(null);
  setSnapLines([]);
  if (wasMoved) {
    markDirty();
    setPointerCommitNonce((n) => n + 1);
  }
}
function commitMarquee(marquee, editTemplateMode, templateElements, activeSlide, applySelection, clearSelection) {
  const sourceElements = editTemplateMode ? templateElements : (activeSlide == null ? void 0 : activeSlide.elements) ?? [];
  const hitIds = computeMarqueeHitIds(marquee, sourceElements);
  if (marquee.additive) {
    const merged = mergeAdditiveSelection(marquee.baseSelectionIds, hitIds);
    if (merged.length > 0) {
      applySelection(merged[0], merged);
    } else {
      clearSelection();
    }
  } else if (hitIds.length > 0) {
    applySelection(hitIds[0], hitIds);
  } else {
    clearSelection();
  }
}
function commitDrag(drag, editTemplateMode, activeSlide, activeSlideIndex, setTemplateElementsBySlideId, updateSlides) {
  const dx = drag.lastDx, dy = drag.lastDy;
  const movedIds = new Set(Object.keys(drag.startPositionsById));
  const moveElements = (elements) => {
    const movedElements = elements.map((el) => {
      const start = drag.startPositionsById[el.id];
      if (!start) {
        return el;
      }
      return { ...el, x: start.x + dx, y: start.y + dy };
    });
    const rerouted = rerouteConnectorsForMovedElements(movedElements, movedIds);
    return applyReroutedConnectors(movedElements, rerouted);
  };
  if (editTemplateMode) {
    const slideId = activeSlide == null ? void 0 : activeSlide.id;
    if (!slideId) {
      return;
    }
    setTemplateElementsBySlideId((prev) => ({
      ...prev,
      [slideId]: moveElements(prev[slideId] ?? [])
    }));
    return;
  }
  updateSlides(
    (prev) => prev.map((s, i) => i !== activeSlideIndex ? s : { ...s, elements: moveElements(s.elements) })
  );
}
function usePointerHandlers(input) {
  const {
    editorScale,
    canvasStageRef,
    canvasSize,
    activeSlide,
    activeSlideIndex,
    gridSpacingPx,
    dragStateRef,
    resizeStateRef,
    shapeAdjustmentDragStateRef,
    marqueeStateRef,
    editTemplateMode,
    snapToGrid,
    snapToShape,
    guides,
    templateElements,
    elementLookup,
    setMarqueeSelectionState,
    setSnapLines,
    setTemplateElementsBySlideId,
    setPointerCommitNonce,
    applySelection,
    clearSelection,
    updateSlides,
    updateElementById,
    markDirty,
    livePatcher
  } = input;
  (0, import_react.useEffect)(() => {
    const tracker = {
      rafId: 0,
      pendingMoveEvent: null,
      lastSnapLinesKey: ""
    };
    const handlePointerMove = (e) => {
      tracker.pendingMoveEvent = e;
      if (tracker.rafId === 0) {
        tracker.rafId = requestAnimationFrame(() => {
          tracker.rafId = 0;
          if (tracker.pendingMoveEvent) {
            processPointerMove(tracker.pendingMoveEvent, input, tracker);
            tracker.pendingMoveEvent = null;
          }
        });
      }
    };
    const handlePointerUp = () => {
      if (tracker.rafId !== 0) {
        cancelAnimationFrame(tracker.rafId);
        tracker.rafId = 0;
        tracker.pendingMoveEvent = null;
      }
      processPointerUp(input);
    };
    document.addEventListener("pointermove", handlePointerMove);
    document.addEventListener("pointerup", handlePointerUp);
    return () => {
      document.removeEventListener("pointermove", handlePointerMove);
      document.removeEventListener("pointerup", handlePointerUp);
      if (tracker.rafId !== 0) {
        cancelAnimationFrame(tracker.rafId);
      }
    };
  }, [
    editorScale,
    canvasStageRef,
    dragStateRef,
    resizeStateRef,
    shapeAdjustmentDragStateRef,
    marqueeStateRef,
    editTemplateMode,
    snapToGrid,
    snapToShape,
    guides,
    templateElements,
    setMarqueeSelectionState,
    setSnapLines,
    elementLookup,
    setTemplateElementsBySlideId,
    setPointerCommitNonce,
    activeSlide,
    activeSlideIndex,
    canvasSize.width,
    canvasSize.height,
    gridSpacingPx,
    applySelection,
    clearSelection,
    updateSlides,
    updateElementById,
    markDirty,
    livePatcher
  ]);
}
function mouseDownStartsSelectionDrag(event) {
  return !event.shiftKey && !event.metaKey;
}
function useCanvasInteractions(input) {
  const {
    mode,
    canEdit,
    canvasSize,
    selectedElementId,
    selectedElementIds,
    selectedElementIdSet,
    inlineEditingElementId,
    effectiveSelectedIds,
    elementLookup,
    activeTool,
    editorScale,
    canvasStageRef,
    dragStateRef,
    resizeStateRef,
    shapeAdjustmentDragStateRef,
    marqueeStateRef,
    justInteractedRef,
    setInlineEditingElementId,
    setInlineEditingText,
    setContextMenuState,
    setMarqueeSelectionState,
    setSnapLines,
    inlineEditingText,
    ops,
    history,
    presentationHandleAction,
    setEditingEquationOmml,
    setIsEquationDialogOpen,
    setPointerCommitNonce,
    transformCommittedText
  } = input;
  const justSelectedRef = (0, import_react.useRef)(false);
  const handleInlineEditCommit = () => {
    const editId = inlineEditingElementId;
    if (!editId) {
      return;
    }
    const el = elementLookup.get(editId);
    if (el && hasTextProperties(el)) {
      const committedText = transformCommittedText ? transformCommittedText(inlineEditingText) : inlineEditingText;
      const newSegments = remapTextToSegments(committedText, el.textSegments, el.textStyle);
      const editorEl = document.querySelector("[data-inline-editor]");
      const newHeight = resolveInlineEditAutoFitHeight(el.textStyle, el.height, editorEl);
      const shrink = resolveInlineEditNormAutofitShrink(el.textStyle, el.height, editorEl);
      ops.updateElementById(editId, {
        text: committedText,
        textSegments: newSegments,
        ...newHeight !== void 0 ? { height: newHeight } : {},
        ...shrink !== "unchanged" ? {
          textStyle: {
            ...el.textStyle,
            autoFitFontScale: shrink.fontScale,
            autoFitLineSpacingReduction: shrink.lnSpcReduction
          }
        } : {}
      });
      history.markDirty();
    }
    setInlineEditingElementId(null);
    setInlineEditingText("");
  };
  const openEquationEditorForElement = (el) => {
    var _a;
    if (!hasTextProperties(el)) {
      return false;
    }
    const eqSeg = (_a = el.textSegments) == null ? void 0 : _a.find((seg) => seg.equationXml);
    if (!(eqSeg == null ? void 0 : eqSeg.equationXml)) {
      return false;
    }
    setEditingEquationOmml(eqSeg.equationXml);
    setIsEquationDialogOpen(true);
    return true;
  };
  const handleElementClick = (elementId, e) => {
    e.stopPropagation();
    if (mode === "present") {
      const el = elementLookup.get(elementId);
      if (el == null ? void 0 : el.actionClick) {
        presentationHandleAction(el.actionClick);
      }
      return;
    }
    if (e.shiftKey || e.metaKey) {
      const ids = selectedElementIds.length ? selectedElementIds : selectedElementId ? [selectedElementId] : [];
      const newIds = ids.includes(elementId) ? ids.filter((id) => id !== elementId) : [...ids, elementId];
      ops.applySelection(newIds[0] ?? null, newIds);
    } else if (selectedElementIdSet.has(elementId) && !inlineEditingElementId) {
      if (justSelectedRef.current || justInteractedRef.current) {
        justSelectedRef.current = false;
        justInteractedRef.current = false;
      } else {
        const el = elementLookup.get(elementId);
        if (el && hasTextProperties(el) && canInteractWithElement(el, "textEdit")) {
          if (!openEquationEditorForElement(el)) {
            setInlineEditingElementId(elementId);
            setInlineEditingText(el.text ?? "");
          }
        }
      }
    } else {
      ops.applySelection(elementId);
    }
  };
  const handleElementDoubleClick = (elementId, _e) => {
    const el = elementLookup.get(elementId);
    if (!el) {
      return;
    }
    if (openEquationEditorForElement(el)) {
      return;
    }
    if (hasTextProperties(el) && canInteractWithElement(el, "textEdit")) {
      setInlineEditingElementId(elementId);
      setInlineEditingText(el.text ?? "");
    }
  };
  const handleElementMouseDown = (elementId, e) => {
    if (e.button !== 0) {
      return;
    }
    if (inlineEditingElementId && inlineEditingElementId !== elementId) {
      handleInlineEditCommit();
    }
    if (!mouseDownStartsSelectionDrag(e)) {
      return;
    }
    const wasSelected = selectedElementIdSet.has(elementId);
    if (!wasSelected) {
      ops.applySelection(elementId);
      justSelectedRef.current = true;
    } else {
      justSelectedRef.current = false;
    }
    const ids = !wasSelected ? [elementId] : effectiveSelectedIds.length ? effectiveSelectedIds : [elementId];
    const movableIds = filterInteractableIds(ids, (id) => elementLookup.get(id), "move");
    if (movableIds.length === 0) {
      return;
    }
    const startPositions = {};
    const domEls = /* @__PURE__ */ new Map();
    for (const id of movableIds) {
      const el = elementLookup.get(id);
      if (el) {
        startPositions[id] = { x: el.x, y: el.y };
      }
      const domEl = document.querySelector(`[data-element-id="${id}"]`);
      if (domEl) {
        domEls.set(id, domEl);
      }
    }
    dragStateRef.current = {
      elementId,
      startClientX: e.clientX,
      startClientY: e.clientY,
      startPositionsById: startPositions,
      domEls,
      moved: false,
      lastDx: 0,
      lastDy: 0
    };
    setSnapLines([]);
  };
  const handleElementContextMenu = (elementId, e) => {
    if (mode === "present") {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    if (!selectedElementIdSet.has(elementId)) {
      ops.applySelection(elementId);
    }
    setContextMenuState({ x: e.clientX, y: e.clientY, elementId });
  };
  const handleCanvasMouseDown = (e) => {
    if (mode !== "edit" || !canEdit || e.button !== 0 || activeTool !== "select") {
      return;
    }
    if (inlineEditingElementId) {
      handleInlineEditCommit();
    }
    const stage = canvasStageRef.current;
    if (!stage) {
      return;
    }
    const rect = stage.getBoundingClientRect();
    const scale = editorScale || 1;
    const startX = Math.max(0, Math.min(canvasSize.width, (e.clientX - rect.left) / scale));
    const startY = Math.max(0, Math.min(canvasSize.height, (e.clientY - rect.top) / scale));
    const additive = e.shiftKey || e.metaKey;
    const nextMarquee = {
      startX,
      startY,
      currentX: startX,
      currentY: startY,
      additive,
      baseSelectionIds: additive ? effectiveSelectedIds : []
    };
    marqueeStateRef.current = nextMarquee;
    setMarqueeSelectionState(nextMarquee);
    setContextMenuState(null);
  };
  const handleResizePointerDown = (elementId, e, handle) => {
    e.stopPropagation();
    const el = elementLookup.get(elementId);
    if (!el || !canInteractWithElement(el, "resize")) {
      return;
    }
    resizeStateRef.current = {
      elementId,
      startClientX: e.clientX,
      startClientY: e.clientY,
      startX: el.x,
      startY: el.y,
      startWidth: el.width,
      startHeight: el.height,
      handle,
      moved: false,
      domEl: document.querySelector(`[data-element-id="${elementId}"]`),
      lastX: el.x,
      lastY: el.y,
      lastWidth: el.width,
      lastHeight: el.height
    };
  };
  const handleRotate = (elementId, rotationDeg) => {
    const el = elementLookup.get(elementId);
    if (!el || !canInteractWithElement(el, "rotate")) {
      return;
    }
    ops.updateElementById(elementId, { rotation: rotationDeg });
    history.markDirty();
    setPointerCommitNonce == null ? void 0 : setPointerCommitNonce((n) => n + 1);
  };
  const handleUpdateSmartArtElement = (elementId, updates) => {
    if (!elementLookup.has(elementId)) {
      return;
    }
    ops.updateElementById(elementId, updates);
    setPointerCommitNonce == null ? void 0 : setPointerCommitNonce((n) => n + 1);
  };
  const handleFormatText = (updates) => {
    ops.updateSelectedTextStyle(updates);
  };
  const handleAdjustmentPointerDown = (elementId, e, descriptor) => {
    e.stopPropagation();
    const el = elementLookup.get(elementId);
    if (!el || !canInteractWithElement(el, "adjustHandle")) {
      return;
    }
    shapeAdjustmentDragStateRef.current = beginShapeAdjustment(
      el,
      descriptor,
      e.clientX,
      e.clientY
    );
  };
  return {
    handleElementClick,
    handleElementDoubleClick,
    handleElementMouseDown,
    handleElementContextMenu,
    handleCanvasMouseDown,
    handleResizePointerDown,
    handleAdjustmentPointerDown,
    handleRotate,
    handleUpdateSmartArtElement,
    handleFormatText,
    handleInlineEditCommit
  };
}
function createFileHandlers(deps) {
  const { activeSlide, canvasSize, addElement } = deps;
  const handleImageFileChange = (e) => {
    var _a;
    const file = (_a = e.target.files) == null ? void 0 : _a[0];
    if (!file || !activeSlide) {
      return;
    }
    e.target.value = "";
    const reader = new FileReader();
    reader.onload = () => {
      const dataUrl = reader.result;
      if (!dataUrl) {
        return;
      }
      const img = new Image();
      img.onload = () => {
        const MAX_W = 500, MAX_H = 400;
        let w = img.naturalWidth || 400, h = img.naturalHeight || 300;
        if (w > MAX_W || h > MAX_H) {
          const scale = Math.min(MAX_W / w, MAX_H / h);
          w = Math.round(w * scale);
          h = Math.round(h * scale);
        }
        addElement({
          id: generateElementId(),
          type: "image",
          x: Math.round((canvasSize.width - w) / 2),
          y: Math.round((canvasSize.height - h) / 2),
          width: w,
          height: h,
          imageData: dataUrl
        });
      };
      img.onerror = () => {
        addElement({
          id: generateElementId(),
          type: "image",
          x: 100,
          y: 100,
          width: 400,
          height: 300,
          imageData: dataUrl
        });
      };
      img.src = dataUrl;
    };
    reader.readAsDataURL(file);
  };
  const handleMediaFileChange = (e) => {
    var _a;
    const file = (_a = e.target.files) == null ? void 0 : _a[0];
    if (!file || !activeSlide) {
      return;
    }
    e.target.value = "";
    const mediaType = classifyMediaType(file.type);
    if (!mediaType) {
      return;
    }
    const elFilePath = file.path;
    const insertMediaElement = (mediaSourceUrl, width, height) => {
      addElement({
        id: generateElementId(),
        type: "media",
        mediaType,
        mediaMimeType: file.type || void 0,
        mediaData: mediaSourceUrl,
        x: Math.round((canvasSize.width - width) / 2),
        y: Math.round((canvasSize.height - height) / 2),
        width,
        height
      });
    };
    if (elFilePath) {
      let normalized = elFilePath.replace(/\\/gu, "/");
      if (!normalized.startsWith("/")) {
        normalized = `/${normalized}`;
      }
      const mediaUrl = `pptx-resource://media${encodeURI(normalized)}`;
      if (mediaType === "audio") {
        insertMediaElement(mediaUrl, 420, 64);
        return;
      }
      const probeUrl = URL.createObjectURL(file);
      const probeVideo = document.createElement("video");
      probeVideo.preload = "metadata";
      probeVideo.onloadedmetadata = () => {
        URL.revokeObjectURL(probeUrl);
        const mW = 640, mH = 360;
        let w = probeVideo.videoWidth || mW, h = probeVideo.videoHeight || mH;
        if (w > mW || h > mH) {
          const sc = Math.min(mW / w, mH / h);
          w = Math.round(w * sc);
          h = Math.round(h * sc);
        }
        insertMediaElement(mediaUrl, w, h);
      };
      probeVideo.onerror = () => {
        URL.revokeObjectURL(probeUrl);
        insertMediaElement(mediaUrl, 640, 360);
      };
      probeVideo.src = probeUrl;
      return;
    }
    const reader = new FileReader();
    reader.onload = () => {
      const dataUrl = reader.result;
      if (!dataUrl) {
        return;
      }
      if (mediaType === "audio") {
        insertMediaElement(dataUrl, 420, 64);
        return;
      }
      const probeVideo = document.createElement("video");
      probeVideo.preload = "metadata";
      probeVideo.onloadedmetadata = () => {
        const mW = 640, mH = 360;
        let w = probeVideo.videoWidth || mW, h = probeVideo.videoHeight || mH;
        if (w > mW || h > mH) {
          const sc = Math.min(mW / w, mH / h);
          w = Math.round(w * sc);
          h = Math.round(h * sc);
        }
        insertMediaElement(dataUrl, w, h);
      };
      probeVideo.onerror = () => {
        insertMediaElement(dataUrl, 640, 360);
      };
      probeVideo.src = dataUrl;
    };
    reader.readAsDataURL(file);
  };
  return { handleImageFileChange, handleMediaFileChange };
}
var ACTION_VERB_MAP = {
  "ppaction://hlinkshowjump?jump=nextslide": "nextSlide",
  "ppaction://hlinkshowjump?jump=previousslide": "prevSlide",
  "ppaction://hlinkshowjump?jump=firstslide": "firstSlide",
  "ppaction://hlinkshowjump?jump=lastslide": "lastSlide",
  "ppaction://hlinkshowjump?jump=endshow": "endShow"
};
var ACTION_VERB_TO_PPACTION = {
  nextSlide: "ppaction://hlinkshowjump?jump=nextslide",
  prevSlide: "ppaction://hlinkshowjump?jump=previousslide",
  firstSlide: "ppaction://hlinkshowjump?jump=firstslide",
  lastSlide: "ppaction://hlinkshowjump?jump=lastslide",
  endShow: "ppaction://hlinkshowjump?jump=endshow"
};
function detectTargetType(url, action) {
  if (action && action.startsWith("ppaction://")) {
    if (action.includes("hlinksldjump")) {
      return "slide";
    }
    if (ACTION_VERB_MAP[action.toLowerCase()]) {
      return "action";
    }
    return "action";
  }
  if (!url) {
    return "url";
  }
  if (url.startsWith("mailto:")) {
    return "email";
  }
  if (/^[a-zA-Z]:\\|^\.\.?[/\\]|^file:/i.test(url)) {
    return "file";
  }
  return "url";
}
function parseEmailUrl(url) {
  if (!url.startsWith("mailto:")) {
    return { address: url, subject: "" };
  }
  const withoutScheme = url.slice(7);
  const qIdx = withoutScheme.indexOf("?");
  if (qIdx < 0) {
    return { address: withoutScheme, subject: "" };
  }
  const address = withoutScheme.slice(0, qIdx);
  const params = new URLSearchParams(withoutScheme.slice(qIdx + 1));
  return { address, subject: params.get("subject") || "" };
}
function parseSlideFromUrl(url, action) {
  if (action === "ppaction://hlinksldjump" && url) {
    const match = url.match(/slide(\d+)\.xml$/i);
    if (match) {
      return parseInt(match[1], 10);
    }
  }
  if (url) {
    const hashMatch = url.match(/#\s*Slide\s+(\d+)/i);
    if (hashMatch) {
      return parseInt(hashMatch[1], 10);
    }
  }
  return 1;
}
function resolveHyperlinkEditResult(data) {
  const tooltip = data.tooltip.trim() || void 0;
  switch (data.targetType) {
    case "email": {
      const mailto = data.emailSubject ? `mailto:${data.emailAddress}?subject=${encodeURIComponent(data.emailSubject)}` : `mailto:${data.emailAddress}`;
      return { url: mailto, tooltip };
    }
    case "slide":
      return {
        url: `slide${data.slideNumber}.xml`,
        action: "ppaction://hlinksldjump",
        tooltip
      };
    case "file":
      return { url: data.filePath, tooltip };
    case "action":
      return {
        url: "",
        action: ACTION_VERB_TO_PPACTION[data.actionVerb],
        tooltip
      };
    case "url":
    default:
      return { url: data.url, tooltip };
  }
}
function createStructuredElementHandlers(deps) {
  const { activeSlide, activeSlideIndex, selectedElements, ops, history, addElement } = deps;
  const handleInsertSmartArt = (layout, defaultItems) => {
    if (!activeSlide) {
      return;
    }
    addElement({
      id: generateElementId(),
      type: "smartArt",
      x: 100,
      y: 120,
      width: 600,
      height: 340,
      smartArtData: buildSmartArtPresetData(
        layout,
        defaultItems,
        (i) => `node-${Date.now()}-${i}-${Math.random().toString(36).slice(2, 8)}`
      )
    });
  };
  const handleInsertEquation = (omml) => {
    if (!activeSlide) {
      return;
    }
    addElement({
      id: generateElementId(),
      type: "shape",
      x: 120,
      y: 200,
      width: 400,
      height: 80,
      text: "[Equation]",
      textStyle: { fontSize: 18, fontFamily: "Cambria Math" },
      textSegments: [
        {
          text: "[Equation]",
          style: { fontSize: 18, fontFamily: "Cambria Math" },
          equationXml: omml
        }
      ]
    });
  };
  const handleUpdateEquation = (omml) => {
    const sel = selectedElements[0];
    if (!sel) {
      return;
    }
    const updatedSegments = [
      {
        text: "[Equation]",
        style: { fontSize: 18, fontFamily: "Cambria Math" },
        equationXml: omml
      }
    ];
    ops.updateElementById(sel.id, { textSegments: updatedSegments });
    history.markDirty();
  };
  const handleHyperlinkConfirm = (data) => {
    const sel = selectedElements[0];
    if (!sel) {
      return;
    }
    const resolved = resolveHyperlinkEditResult(data);
    if (resolved.url && !isUrlSafe(resolved.url)) {
      return;
    }
    const actionClick = {
      url: resolved.url || void 0,
      action: resolved.action,
      tooltip: resolved.tooltip
    };
    ops.updateElementById(sel.id, { actionClick });
    history.markDirty();
  };
  const handleInsertField = (fieldType, value) => {
    if (!activeSlide) {
      return;
    }
    const fieldTexts = {
      slidenum: String(activeSlideIndex + 1),
      datetime: (/* @__PURE__ */ new Date()).toLocaleDateString(),
      header: "Header",
      footer: "Footer"
    };
    const displayText = value || fieldTexts[fieldType] || fieldType;
    const fieldGuid = `{${crypto.randomUUID().toUpperCase()}}`;
    addElement({
      id: generateElementId(),
      type: "shape",
      x: 120,
      y: 200,
      width: 200,
      height: 40,
      text: displayText,
      textStyle: { fontSize: 14 },
      textSegments: [
        {
          text: displayText,
          style: { fontSize: 14 },
          fieldType,
          fieldGuid
        }
      ]
    });
  };
  const handleAddActionButton = (shapeType) => {
    if (!activeSlide) {
      return;
    }
    const preset = ACTION_BUTTON_PRESETS.find((p) => p.shapeType === shapeType);
    if (!preset) {
      return;
    }
    const defaultPptxAction = elementActionToPptxAction({
      trigger: "click",
      type: preset.defaultAction
    });
    if (defaultPptxAction) {
      defaultPptxAction.tooltip = preset.label;
      defaultPptxAction.highlightClick = true;
    }
    addElement({
      id: generateElementId(),
      type: "shape",
      x: 150,
      y: 150,
      width: 120,
      height: 50,
      shapeType: preset.shapeType,
      text: preset.label,
      textStyle: {
        fontSize: 11,
        fontColor: "#FFFFFF",
        align: "center",
        verticalAlign: "middle"
      },
      textSegments: [
        {
          text: preset.label,
          style: {
            fontSize: 11,
            fontColor: "#FFFFFF",
            bold: true
          }
        }
      ],
      shapeStyle: {
        fillColor: "#4472C4",
        strokeColor: "#2F5597",
        strokeWidth: 1
      },
      actionClick: defaultPptxAction
    });
  };
  return {
    handleInsertSmartArt,
    handleInsertEquation,
    handleUpdateEquation,
    handleHyperlinkConfirm,
    handleInsertField,
    handleAddActionButton
  };
}
function useInsertElements(input) {
  const {
    activeSlide,
    activeSlideIndex,
    canvasSize,
    newShapeType,
    selectedElements,
    ops,
    history
  } = input;
  const addElement = (element) => {
    ops.updateSlides(
      (prev) => prev.map(
        (s, i) => i === activeSlideIndex ? { ...s, elements: [...s.elements, element] } : s
      )
    );
    ops.applySelection(element.id);
    history.markDirty();
  };
  const handleAddTextBox = () => {
    if (!activeSlide) {
      return;
    }
    addElement({
      id: generateElementId(),
      type: "text",
      x: 100,
      y: 100,
      width: 300,
      height: 60,
      text: "",
      textStyle: { fontSize: DEFAULT_TEXT_FONT_SIZE }
    });
  };
  const handleAddShape = () => {
    if (!activeSlide) {
      return;
    }
    addElement({
      id: generateElementId(),
      type: "shape",
      x: 150,
      y: 150,
      width: 200,
      height: 150,
      shapeType: newShapeType,
      shapeStyle: {
        fillColor: "#3b82f6",
        strokeColor: "#1f2937",
        strokeWidth: 2
      }
    });
  };
  const handleAddTable = () => {
    if (!activeSlide) {
      return;
    }
    addElement({
      ...newTableElement(DEFAULT_TABLE_ROWS, DEFAULT_TABLE_COLUMNS, 100, 200),
      id: generateElementId()
    });
  };
  const handleAddChart = (chartKind) => {
    if (!activeSlide) {
      return;
    }
    addElement(createDefaultChartElement(chartKind));
  };
  const structured = createStructuredElementHandlers({
    activeSlide,
    activeSlideIndex,
    selectedElements,
    ops,
    history,
    addElement
  });
  const fileHandlers = createFileHandlers({
    activeSlide,
    canvasSize,
    addElement
  });
  const handleAddInkElement = (ink) => {
    if (!activeSlide) {
      return;
    }
    ops.updateSlides(
      (prev) => prev.map((s, i) => i === activeSlideIndex ? { ...s, elements: [...s.elements, ink] } : s)
    );
    history.markDirty();
  };
  const handleAddFreeformShape = (shape) => {
    if (!activeSlide) {
      return;
    }
    addElement(shape);
  };
  const handleEraseInkElement = (elementId) => {
    if (!activeSlide) {
      return;
    }
    ops.updateSlides(
      (prev) => prev.map(
        (s, i) => i === activeSlideIndex ? { ...s, elements: s.elements.filter((el) => el.id !== elementId) } : s
      )
    );
    history.markDirty();
  };
  return {
    handleAddTextBox,
    handleAddShape,
    handleAddTable,
    handleAddChart,
    ...structured,
    handleAddInkElement,
    handleAddFreeformShape,
    handleEraseInkElement,
    ...fileHandlers
  };
}
function useClipboardHandlers(input) {
  const {
    activeSlide,
    selectedElement,
    effectiveSelectedIds,
    editTemplateMode,
    clipboardPayload,
    setClipboardPayload,
    ops,
    history
  } = input;
  const handleCopy = () => {
    if (!selectedElement) {
      return;
    }
    setClipboardPayload(buildElementClipboardPayload(selectedElement, editTemplateMode));
  };
  const handleDelete = () => {
    const idsToDelete = effectiveSelectedIds;
    if (!idsToDelete.length || !activeSlide) {
      return;
    }
    const idSet = new Set(idsToDelete);
    ops.updateActiveElements((els) => els.filter((el) => !idSet.has(el.id)));
    ops.clearSelection();
    history.markDirty();
  };
  const handleCut = () => {
    handleCopy();
    handleDelete();
  };
  const insertClone = (source) => {
    const clone = cloneElementForPaste(source, { intoTemplate: editTemplateMode });
    ops.updateActiveElements((els) => [...els, clone]);
    ops.applySelection(clone.id);
    history.markDirty();
  };
  const handlePaste = () => {
    if (!clipboardPayload || !activeSlide) {
      return;
    }
    insertClone(clipboardPayload.element);
  };
  const handleDuplicate = () => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    insertClone(selectedElement);
  };
  return {
    handleCopy,
    handleCut,
    handlePaste,
    handleDuplicate,
    handleDelete
  };
}
function isShapeLike(el) {
  return el.type === "shape" || el.type === "image" || el.type === "picture";
}
function resolveShapePath(el) {
  if ("pathData" in el && typeof el.pathData === "string" && el.pathData) {
    return el.pathData;
  }
  if ("shapeType" in el && typeof el.shapeType === "string") {
    const clipPath = getPresetShapeClipPath(el.shapeType);
    if (clipPath) {
      const svgPath = clipPathToSvgPath(clipPath, el.width, el.height);
      if (svgPath) {
        return svgPath;
      }
    }
  }
  return `M 0 0 L ${el.width} 0 L ${el.width} ${el.height} L 0 ${el.height} Z`;
}
function clipPathToSvgPath(clipPath, width, height) {
  const match = clipPath.match(/polygon\((?<points>[^)]+)\)/iu);
  if (!(match == null ? void 0 : match.groups)) {
    return null;
  }
  const pairs = match.groups.points.split(",").map((s) => s.trim());
  const points = [];
  for (const pair of pairs) {
    const parts2 = pair.split(/\s+/u);
    if (parts2.length < 2) {
      continue;
    }
    const xStr = parts2[0];
    const yStr = parts2[1];
    let x;
    let y;
    if (xStr.endsWith("%")) {
      x = parseFloat(xStr) / 100 * width;
    } else {
      x = parseFloat(xStr);
    }
    if (yStr.endsWith("%")) {
      y = parseFloat(yStr) / 100 * height;
    } else {
      y = parseFloat(yStr);
    }
    if (!isNaN(x) && !isNaN(y)) {
      points.push({ x, y });
    }
  }
  if (points.length < 3) {
    return null;
  }
  const parts = [`M ${points[0].x} ${points[0].y}`];
  for (let i = 1; i < points.length; i++) {
    parts.push(`L ${points[i].x} ${points[i].y}`);
  }
  parts.push("Z");
  return parts.join(" ");
}
function transformPathToGlobal(pathData, el) {
  const polys = svgPathToPolygons(pathData);
  if (polys.length === 0) {
    return pathData;
  }
  let pathW = el.width;
  let pathH = el.height;
  if ("pathWidth" in el && typeof el.pathWidth === "number") {
    pathW = el.pathWidth;
  }
  if ("pathHeight" in el && typeof el.pathHeight === "number") {
    pathH = el.pathHeight;
  }
  const scaleX = pathW > 0 ? el.width / pathW : 1;
  const scaleY = pathH > 0 ? el.height / pathH : 1;
  const transformed = polys.map(
    (poly) => poly.map((pt) => ({
      x: pt.x * scaleX + el.x,
      y: pt.y * scaleY + el.y
    }))
  );
  return polygonsToSvgPath(transformed);
}
function transformPathToLocal(pathData, x, y, _width, _height) {
  const polys = svgPathToPolygons(pathData);
  if (polys.length === 0) {
    return pathData;
  }
  const transformed = polys.map(
    (poly) => poly.map((pt) => ({
      x: pt.x - x,
      y: pt.y - y
    }))
  );
  return polygonsToSvgPath(transformed);
}
function pathBounds(pathData) {
  const polys = svgPathToPolygons(pathData);
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const poly of polys) {
    for (const pt of poly) {
      if (pt.x < minX) {
        minX = pt.x;
      }
      if (pt.y < minY) {
        minY = pt.y;
      }
      if (pt.x > maxX) {
        maxX = pt.x;
      }
      if (pt.y > maxY) {
        maxY = pt.y;
      }
    }
  }
  if (!isFinite(minX)) {
    return { x: 0, y: 0, width: 0, height: 0 };
  }
  return {
    x: minX,
    y: minY,
    width: maxX - minX,
    height: maxY - minY
  };
}
function useMergeShapesHandler(input) {
  const {
    activeSlide,
    activeSlideIndex,
    selectedElements,
    effectiveSelectedIds,
    setSelectedElementIds,
    ops,
    history
  } = input;
  const shapeLikeElements = selectedElements.filter(isShapeLike);
  const canMergeShapes = shapeLikeElements.length >= 2;
  const handleMergeShapes = (operation) => {
    if (!canMergeShapes || !activeSlide) {
      return;
    }
    const targets = shapeLikeElements;
    if (targets.length < 2) {
      return;
    }
    const globalPaths = targets.map((el) => {
      const localPath = resolveShapePath(el);
      return transformPathToGlobal(localPath, el);
    });
    if (operation === "fragment") {
      let allFragments = [];
      let remaining = globalPaths[0];
      for (let i = 1; i < globalPaths.length; i++) {
        const frags = mergeShapes("fragment", remaining, globalPaths[i]);
        if (Array.isArray(frags)) {
          allFragments = frags;
        }
        if (i < globalPaths.length - 1 && allFragments.length > 0) {
          remaining = allFragments[0];
        }
      }
      if (allFragments.length === 0) {
        return;
      }
      const idSet = new Set(effectiveSelectedIds);
      const newElements = [];
      for (const frag of allFragments) {
        if (!frag) {
          continue;
        }
        const bounds = pathBounds(frag);
        if (bounds.width <= 0 || bounds.height <= 0) {
          continue;
        }
        const localPath = transformPathToLocal(
          frag,
          bounds.x,
          bounds.y,
          bounds.width,
          bounds.height
        );
        const newEl = {
          id: generateElementId(),
          type: "shape",
          x: bounds.x,
          y: bounds.y,
          width: bounds.width,
          height: bounds.height,
          rotation: 0,
          flipHorizontal: false,
          flipVertical: false,
          hidden: false,
          opacity: targets[0].opacity ?? 1,
          rawXml: {},
          shapeStyle: {
            ...targets[0].shapeStyle
          },
          shapeType: "custGeom",
          pathData: localPath,
          pathWidth: bounds.width,
          pathHeight: bounds.height
        };
        newElements.push(newEl);
      }
      if (newElements.length === 0) {
        return;
      }
      ops.updateSlides(
        (prev) => prev.map(
          (s, i) => i === activeSlideIndex ? {
            ...s,
            elements: [...s.elements.filter((el) => !idSet.has(el.id)), ...newElements]
          } : s
        )
      );
      setSelectedElementIds(newElements.map((el) => el.id));
      history.markDirty();
    } else {
      let resultPath = globalPaths[0];
      for (let i = 1; i < globalPaths.length; i++) {
        const result = mergeShapes(operation, resultPath, globalPaths[i]);
        if (typeof result === "string") {
          resultPath = result;
        }
      }
      if (!resultPath) {
        return;
      }
      const bounds = pathBounds(resultPath);
      if (bounds.width <= 0 || bounds.height <= 0) {
        return;
      }
      const localPath = transformPathToLocal(
        resultPath,
        bounds.x,
        bounds.y,
        bounds.width,
        bounds.height
      );
      const primary = targets[0];
      const newEl = {
        id: generateElementId(),
        type: "shape",
        x: bounds.x,
        y: bounds.y,
        width: bounds.width,
        height: bounds.height,
        rotation: 0,
        flipHorizontal: false,
        flipVertical: false,
        hidden: false,
        opacity: primary.opacity ?? 1,
        rawXml: {},
        shapeStyle: { ...primary.shapeStyle },
        shapeType: "custGeom",
        pathData: localPath,
        pathWidth: bounds.width,
        pathHeight: bounds.height
      };
      const idSet = new Set(effectiveSelectedIds);
      ops.updateSlides(
        (prev) => prev.map(
          (s, i) => i === activeSlideIndex ? {
            ...s,
            elements: [...s.elements.filter((el) => !idSet.has(el.id)), newEl]
          } : s
        )
      );
      ops.applySelection(newEl.id);
      history.markDirty();
    }
  };
  return {
    handleMergeShapes,
    canMergeShapes
  };
}
var ALIGN_EDGE_BY_KEY = {
  left: "left",
  center: "centerH",
  right: "right",
  top: "top",
  middle: "middle",
  bottom: "bottom"
};
function useGroupAlignLayerHandlers(input) {
  const {
    activeSlide,
    activeSlideIndex,
    selectedElement,
    effectiveSelectedIds,
    selectedElements,
    elementLookup,
    setSelectedElementIds,
    ops,
    history
  } = input;
  const handleGroupElements = () => {
    const ids = effectiveSelectedIds;
    if (ids.length < 2 || !activeSlide) {
      return;
    }
    if (!selectedElements.every((el) => canInteractWithElement(el, "group"))) {
      return;
    }
    const { elements, groupId } = groupElements(ops.activeElements, ids, generateElementId());
    if (groupId === null) {
      return;
    }
    ops.updateActiveElements(() => elements);
    ops.applySelection(groupId);
    history.markDirty();
  };
  const handleUngroupElement = () => {
    if (!selectedElement || selectedElement.type !== "group" || !activeSlide) {
      return;
    }
    if (!canInteractWithElement(selectedElement, "group")) {
      return;
    }
    const group = selectedElement;
    const intoTemplate = isTemplateElementId3(group.id);
    const childIds = group.children.map(
      (child) => intoTemplate ? makeCloneId(true, child.id || group.id) : child.id || generateElementId()
    );
    const { elements, childIds: usedIds } = ungroupElements(
      ops.activeElements,
      group.id,
      childIds,
      { intoTemplate }
    );
    if (usedIds.length === 0) {
      return;
    }
    ops.updateActiveElements(() => elements);
    setSelectedElementIds(usedIds);
    history.markDirty();
  };
  const handleFlip = (direction) => {
    if (!selectedElement) {
      return;
    }
    const update = direction === "horizontal" ? { flipHorizontal: !selectedElement.flipHorizontal } : { flipVertical: !selectedElement.flipVertical };
    ops.updateSelectedElement(update);
    history.markDirty();
  };
  const handleAlignElements = (align) => {
    if (selectedElements.length < 2) {
      return;
    }
    const edge = ALIGN_EDGE_BY_KEY[align];
    if (!edge) {
      return;
    }
    const positions = alignElements(selectedElements, edge);
    for (const [id, pos] of positions) {
      const el = elementLookup.get(id);
      if (!el) {
        continue;
      }
      ops.updateElementById(id, { x: pos.x ?? el.x, y: pos.y ?? el.y });
    }
    history.markDirty();
  };
  const handleDistributeElements = (axis) => {
    if (selectedElements.length < 3) {
      return;
    }
    const distAxis = axis === "horizontal" || axis === "vertical" ? axis : null;
    if (!distAxis) {
      return;
    }
    const positions = distributeElements(selectedElements, distAxis);
    for (const [id, pos] of positions) {
      const el = elementLookup.get(id);
      if (!el) {
        continue;
      }
      ops.updateElementById(id, { x: pos.x ?? el.x, y: pos.y ?? el.y });
    }
    history.markDirty();
  };
  const canDistribute = selectedElements.length >= 3;
  const selectionGroupable = selectedElements.every((el) => canInteractWithElement(el, "group"));
  const handleMoveLayer = (direction) => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    const id = selectedElement.id;
    ops.updateActiveElements(
      (els) => direction === "forward" ? bringForward(els, id) : direction === "backward" ? sendBackward(els, id) : els
    );
    history.markDirty();
  };
  const handleMoveLayerToEdge = (direction) => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    const id = selectedElement.id;
    ops.updateActiveElements(
      (els) => direction === "front" ? bringToFront(els, id) : sendToBack(els, id)
    );
    history.markDirty();
  };
  const { handleMergeShapes, canMergeShapes } = useMergeShapesHandler({
    activeSlide,
    activeSlideIndex,
    selectedElements,
    effectiveSelectedIds,
    setSelectedElementIds,
    ops,
    history
  });
  return {
    handleGroupElements,
    handleUngroupElement,
    handleFlip,
    handleAlignElements,
    handleDistributeElements,
    canDistribute,
    selectionGroupable,
    handleMoveLayer,
    handleMoveLayerToEdge,
    handleMergeShapes,
    canMergeShapes
  };
}
function useElementManipulation(input) {
  const { setIsInspectorPaneOpen, setSidebarPanelMode, onOpenHyperlinkDialog } = input;
  const { handleCopy, handleCut, handlePaste, handleDuplicate, handleDelete } = useClipboardHandlers(input);
  const {
    handleGroupElements,
    handleUngroupElement,
    handleFlip,
    handleAlignElements,
    handleDistributeElements,
    canDistribute,
    selectionGroupable,
    handleMoveLayer,
    handleMoveLayerToEdge,
    handleMergeShapes,
    canMergeShapes
  } = useGroupAlignLayerHandlers(input);
  const handleContextMenuAction = (action) => {
    switch (action) {
      case "copy":
        handleCopy();
        break;
      case "cut":
        handleCut();
        break;
      case "paste":
        handlePaste();
        break;
      case "duplicate":
        handleDuplicate();
        break;
      case "delete":
        handleDelete();
        break;
      case "bring-forward":
      case "bringForward":
        handleMoveLayer("forward");
        break;
      case "send-backward":
      case "sendBackward":
        handleMoveLayer("backward");
        break;
      case "bring-front":
      case "bringToFront":
        handleMoveLayerToEdge("front");
        break;
      case "send-back":
      case "sendToBack":
        handleMoveLayerToEdge("back");
        break;
      case "comment":
      case "addComment":
        setIsInspectorPaneOpen(true);
        setSidebarPanelMode("comments");
        break;
      case "group":
        handleGroupElements();
        break;
      case "ungroup":
        handleUngroupElement();
        break;
      case "editHyperlink":
        onOpenHyperlinkDialog();
        break;
    }
  };
  return {
    handleCopy,
    handleCut,
    handlePaste,
    handleDuplicate,
    handleGroupElements,
    handleUngroupElement,
    handleDelete,
    handleFlip,
    handleAlignElements,
    handleDistributeElements,
    canDistribute,
    selectionGroupable,
    handleMoveLayer,
    handleMoveLayerToEdge,
    handleMergeShapes,
    canMergeShapes,
    handleContextMenuAction
  };
}
function insertSlideFromLayoutUpdater(slides, activeIndex, draft) {
  const next = [...slides];
  const insertAt = Math.max(0, Math.min(activeIndex + 1, next.length));
  next.splice(insertAt, 0, draft);
  return next;
}
function useSlideManagement(input) {
  const {
    slides,
    activeSlideIndex,
    setActiveSlideIndex,
    ops,
    history,
    handlerRef,
    canvasSize,
    theme
  } = input;
  const handleAddSlide = () => {
    const newSlide = createBlankSlide(slides.length + 1);
    ops.updateSlides((prev) => {
      const next = [...prev];
      next.splice(activeSlideIndex + 1, 0, newSlide);
      return next;
    });
    setActiveSlideIndex(activeSlideIndex + 1);
    history.markDirty();
  };
  const handleMoveSlide = (fromIndex, toIndex) => {
    if (fromIndex === toIndex) {
      return;
    }
    ops.updateSlides((prev) => {
      const next = [...prev];
      const [moved] = next.splice(fromIndex, 1);
      next.splice(toIndex, 0, moved);
      return next;
    });
    setActiveSlideIndex(toIndex);
    history.markDirty();
  };
  const handleSlideContextMenu = (_e, _index) => {
  };
  const handleDeleteSlides = (indexes) => {
    if (indexes.length === 0 || slides.length <= 1) {
      return;
    }
    const sorted = [...indexes].sort((a, b) => b - a);
    ops.updateSlides((prev) => {
      const next = [...prev];
      for (const i of sorted) {
        if (next.length > 1) {
          next.splice(i, 1);
        }
      }
      return next;
    });
    const minIdx = Math.min(...indexes);
    setActiveSlideIndex(
      Math.min(
        minIdx,
        slides.length - indexes.length - 1,
        Math.max(slides.length - indexes.length - 1, 0)
      )
    );
    history.markDirty();
  };
  const handleDuplicateSlides = (indexes) => {
    if (indexes.length === 0) {
      return;
    }
    const sorted = [...indexes].sort((a, b) => a - b);
    ops.updateSlides((prev) => {
      const next = [...prev];
      let offset = 0;
      for (const i of sorted) {
        const src = next[i + offset];
        if (!src) {
          continue;
        }
        const clone = {
          ...src,
          id: makeSlideId(),
          elements: src.elements.map((el) => ({
            ...el,
            id: `${el.id}-dup-${Math.random().toString(36).slice(2, 6)}`
          }))
        };
        next.splice(i + offset + 1, 0, clone);
        offset++;
      }
      return next;
    });
    history.markDirty();
  };
  const handleToggleHideSlides = (indexes) => {
    if (indexes.length === 0) {
      return;
    }
    ops.updateSlides((prev) => {
      const next = [...prev];
      for (const i of indexes) {
        const slide = next[i];
        if (slide) {
          next[i] = { ...slide, hidden: !slide.hidden };
        }
      }
      return next;
    });
    history.markDirty();
  };
  const handleInsertSlideFromLayout = (layoutPath, layoutName) => {
    const insertAt = activeSlideIndex + 1;
    const draft = {
      ...createBlankSlide(slides.length + 1),
      layoutPath,
      ...layoutName ? { layoutName } : {}
    };
    let inserted = [];
    ops.updateSlides((prev) => {
      inserted = insertSlideFromLayoutUpdater(prev, activeSlideIndex, draft);
      return inserted;
    });
    setActiveSlideIndex(insertAt);
    history.markDirty();
    const handler = handlerRef == null ? void 0 : handlerRef.current;
    if (handler) {
      void handler.applyLayoutToSlide(insertAt, layoutPath, inserted).then(
        (updated) => {
          ops.updateSlides((prev) => {
            var _a;
            if (((_a = prev[insertAt]) == null ? void 0 : _a.id) !== draft.id) {
              return prev;
            }
            const next = [...prev];
            next[insertAt] = updated;
            return next;
          });
          return void 0;
        },
        () => {
          return void 0;
        }
      );
    }
  };
  const handleInsertSlideFromTemplate = (templateId) => {
    const insertAt = activeSlideIndex + 1;
    const draft = buildSlideTemplateSlide(templateId, makeSlideId(), slides.length + 1, {
      ...canvasSize ? { slideWidth: canvasSize.width, slideHeight: canvasSize.height } : {},
      scheme: templateSchemeFromTheme(theme == null ? void 0 : theme.colorScheme)
    });
    ops.updateSlides((prev) => insertSlideFromLayoutUpdater(prev, activeSlideIndex, draft));
    setActiveSlideIndex(insertAt);
    history.markDirty();
  };
  return {
    handleAddSlide,
    handleMoveSlide,
    handleSlideContextMenu,
    handleDeleteSlides,
    handleDuplicateSlides,
    handleToggleHideSlides,
    handleInsertSlideFromLayout,
    handleInsertSlideFromTemplate
  };
}
function createTableMergeHandlers(input) {
  const { selectedElement, tableEditorState: ts, setTableEditorState, ops, history } = input;
  const handleMergeCellRight = () => {
    var _a, _b2;
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData || !ts) {
      return;
    }
    const { rowIndex, columnIndex } = ts;
    const td = selectedElement.tableData;
    const cellAtCursor = (_a = td.rows[rowIndex]) == null ? void 0 : _a.cells[columnIndex];
    if (!cellAtCursor) {
      return;
    }
    const currentSpan = Math.max(1, cellAtCursor.gridSpan ?? 1);
    const nextColIdx = columnIndex + currentSpan;
    const nextCell = (_b2 = td.rows[rowIndex]) == null ? void 0 : _b2.cells[nextColIdx];
    if (!nextCell || nextCell.hMerge || nextCell.vMerge) {
      return;
    }
    const newTableData = mergeCells(
      [
        { row: rowIndex, col: columnIndex },
        { row: rowIndex, col: nextColIdx }
      ],
      td
    );
    const updates = { tableData: newTableData };
    const newRawXml = updateMergeAttrsInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  const handleMergeCellDown = () => {
    var _a, _b2;
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData || !ts) {
      return;
    }
    const { rowIndex, columnIndex } = ts;
    const td = selectedElement.tableData;
    const cellAtCursor = (_a = td.rows[rowIndex]) == null ? void 0 : _a.cells[columnIndex];
    if (!cellAtCursor) {
      return;
    }
    const currentRowSpan = Math.max(1, cellAtCursor.rowSpan ?? 1);
    const targetNextRowIdx = rowIndex + currentRowSpan;
    if (targetNextRowIdx >= td.rows.length) {
      return;
    }
    const targetNextCell = (_b2 = td.rows[targetNextRowIdx]) == null ? void 0 : _b2.cells[columnIndex];
    if (!targetNextCell || targetNextCell.hMerge || targetNextCell.vMerge) {
      return;
    }
    const newTableData = mergeCells(
      [
        { row: rowIndex, col: columnIndex },
        { row: targetNextRowIdx, col: columnIndex }
      ],
      td
    );
    const updates = { tableData: newTableData };
    const newRawXml = updateMergeAttrsInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  const handleMergeSelectedCells = () => {
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData) {
      return;
    }
    if (!ts || !ts.selectedCells || ts.selectedCells.length < 2) {
      return;
    }
    const td = selectedElement.tableData;
    const newTableData = mergeCells(ts.selectedCells, td);
    const updates = { tableData: newTableData };
    const newRawXml = updateMergeAttrsInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    setTableEditorState({
      rowIndex: ts.rowIndex,
      columnIndex: ts.columnIndex
    });
    history.markDirty();
  };
  const handleSplitCell = () => {
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData || !ts) {
      return;
    }
    const { rowIndex, columnIndex } = ts;
    const td = selectedElement.tableData;
    const newTableData = splitCell(rowIndex, columnIndex, td);
    const updates = { tableData: newTableData };
    const newRawXml = updateMergeAttrsInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  return {
    handleMergeCellRight,
    handleMergeCellDown,
    handleMergeSelectedCells,
    handleSplitCell
  };
}
function createTableStructHandlers(input) {
  const {
    selectedElement,
    tableEditorState: ts,
    elementLookup,
    setTableEditorState,
    ops,
    history,
    transformCommittedText
  } = input;
  const handleCommitCellEdit = (elementId, rowIndex, colIndex, rawText) => {
    const el = elementLookup.get(elementId);
    if (!el || el.type !== "table") {
      return;
    }
    const text = transformCommittedText ? transformCommittedText(rawText) : rawText;
    const updates = {};
    if (el.tableData) {
      updates.tableData = setCellText(el, rowIndex, colIndex, text).tableData;
    }
    if (el.rawXml) {
      const newRawXml = updateCellTextInRawXml(el, rowIndex, colIndex, text);
      if (newRawXml) {
        updates.rawXml = newRawXml;
      }
    }
    if (Object.keys(updates).length === 0) {
      return;
    }
    ops.updateElementById(elementId, updates);
    history.markDirty();
    setTableEditorState({
      rowIndex,
      columnIndex: colIndex,
      elementId
    });
  };
  const handleUpdateCellTextStyle = (styleUpdates) => {
    if (!selectedElement || selectedElement.type !== "table" || !ts) {
      return;
    }
    const elementId = selectedElement.id;
    const { rowIndex, columnIndex: colIndex } = ts;
    const updates = {};
    if (selectedElement.rawXml) {
      const newRawXml = updateCellTextStyleInRawXml(
        selectedElement,
        rowIndex,
        colIndex,
        styleUpdates
      );
      if (newRawXml) {
        updates.rawXml = newRawXml;
      }
    }
    if (selectedElement.tableData) {
      const newRows = selectedElement.tableData.rows.map((row, ri) => {
        if (ri !== rowIndex) {
          return row;
        }
        return {
          ...row,
          cells: row.cells.map((cell, ci) => {
            if (ci !== colIndex) {
              return cell;
            }
            return {
              ...cell,
              style: { ...cell.style, ...styleUpdates }
            };
          })
        };
      });
      updates.tableData = { ...selectedElement.tableData, rows: newRows };
    }
    if (Object.keys(updates).length === 0) {
      return;
    }
    ops.updateElementById(elementId, updates);
    history.markDirty();
  };
  const handleResizeTableColumns = (elementId, newWidths) => {
    const el = elementLookup.get(elementId);
    if (!el || el.type !== "table" || !el.tableData) {
      return;
    }
    ops.updateElementById(elementId, {
      tableData: { ...el.tableData, columnWidths: newWidths }
    });
    history.markDirty();
  };
  const handleResizeTableRow = (elementId, rowIndex, newHeight) => {
    const el = elementLookup.get(elementId);
    if (!el || el.type !== "table" || !el.tableData) {
      return;
    }
    const newRows = el.tableData.rows.map(
      (row, i) => i === rowIndex ? { ...row, height: newHeight } : row
    );
    ops.updateElementById(elementId, {
      tableData: { ...el.tableData, rows: newRows }
    });
    history.markDirty();
  };
  const handleInsertTableRow = (position) => {
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData) {
      return;
    }
    const rowIdx = (ts == null ? void 0 : ts.rowIndex) ?? 0;
    const newTableData = insertTableRow(selectedElement.tableData, rowIdx, position);
    const updates = { tableData: newTableData };
    const newRawXml = rebuildTableStructureInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  const handleDeleteTableRow = () => {
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData) {
      return;
    }
    const td = selectedElement.tableData;
    const rowIdx = (ts == null ? void 0 : ts.rowIndex) ?? 0;
    const newTableData = deleteTableRow(td, rowIdx);
    if (newTableData === td) {
      return;
    }
    const updates = { tableData: newTableData };
    const newRawXml = rebuildTableStructureInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  const handleInsertTableColumn = (position) => {
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData) {
      return;
    }
    const colIdx = (ts == null ? void 0 : ts.columnIndex) ?? 0;
    const newTableData = insertTableColumn(selectedElement.tableData, colIdx, position);
    const updates = { tableData: newTableData };
    const newRawXml = rebuildTableStructureInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  const handleDeleteTableColumn = () => {
    if (!selectedElement || selectedElement.type !== "table" || !selectedElement.tableData) {
      return;
    }
    const td = selectedElement.tableData;
    const colIdx = (ts == null ? void 0 : ts.columnIndex) ?? 0;
    const newTableData = deleteTableColumn(td, colIdx);
    if (newTableData === td) {
      return;
    }
    const updates = { tableData: newTableData };
    const newRawXml = rebuildTableStructureInRawXml(selectedElement, newTableData);
    if (newRawXml) {
      updates.rawXml = newRawXml;
    }
    ops.updateSelectedElement(updates);
    history.markDirty();
  };
  return {
    handleCommitCellEdit,
    handleUpdateCellTextStyle,
    handleResizeTableColumns,
    handleResizeTableRow,
    handleInsertTableRow,
    handleDeleteTableRow,
    handleInsertTableColumn,
    handleDeleteTableColumn
  };
}
function useTableOperations(input) {
  const structHandlers = createTableStructHandlers(input);
  const mergeHandlers = createTableMergeHandlers(input);
  return {
    ...structHandlers,
    ...mergeHandlers
  };
}
async function renderToCanvas(element, options = {}) {
  const userOnClone = options.onclone;
  const { default: html2canvasPro } = await import("./html2canvas-pro.esm-7UJ6T6UA.js");
  return html2canvasPro(element, {
    ...options,
    onclone: async (doc, clonedEl) => {
      await normalizeColorsForCapture(doc, clonedEl);
      preprocessCssForCapture(clonedEl);
      if (typeof userOnClone === "function") {
        userOnClone(doc, clonedEl);
      }
    }
  });
}
function useExportSaveAs(input) {
  const {
    slides,
    templateElementsBySlideId,
    filePath,
    serializeSlides,
    headerFooter,
    presentationProperties,
    customShows,
    sections,
    coreProperties,
    appProperties,
    customProperties,
    tagCollections,
    notesMaster,
    handoutMaster,
    theme,
    canvasSize,
    slideSizeEmu
  } = input;
  const handleSaveAsFormat = async (format) => {
    const downloadName = savedPresentationFileName(filePath, format);
    try {
      const data = await serializeSlides(format);
      if (!data) {
        return;
      }
      const blob = new Blob([data], {
        type: "application/vnd.openxmlformats-officedocument.presentationml.presentation"
      });
      downloadBlob(blob, downloadName);
    } catch (err) {
      console.error(`[PowerPointViewer] Save as .${format} failed:`, err);
    }
  };
  const handleExportJson = () => {
    const sourceName = filePath ? filePath.replace(/\\/gu, "/").split("/").pop() : void 0;
    const slideSize = resolveSlideSizeSelection({ current: slideSizeEmu, canvas: canvasSize }).size;
    const data = {
      slides: buildSaveSlides(slides, templateElementsBySlideId),
      width: canvasSize.width,
      height: canvasSize.height,
      widthEmu: slideSize.widthEmu,
      heightEmu: slideSize.heightEmu,
      slideSizeType: slideSize.type === "" ? void 0 : slideSize.type,
      theme,
      headerFooter,
      presentationProperties,
      customShows: customShows.length > 0 ? customShows : void 0,
      sections: sections.length > 0 ? sections : void 0,
      coreProperties,
      appProperties,
      customProperties: customProperties.length > 0 ? customProperties : void 0,
      tags: tagCollections.length > 0 ? tagCollections : void 0,
      notesMaster,
      handoutMaster
    };
    try {
      exportDeckJson(data, sourceName);
    } catch (err) {
      console.error("[PowerPointViewer] JSON export failed:", err);
    }
  };
  const handleSaveAsPptx = () => {
    void handleSaveAsFormat("pptx");
  };
  const handleSaveAsPpsx = () => {
    void handleSaveAsFormat("ppsx");
  };
  const handleSaveAsPptm = () => {
    void handleSaveAsFormat("pptm");
  };
  return {
    handleExportJson,
    handleSaveAsFormat,
    handleSaveAsPptx,
    handleSaveAsPpsx,
    handleSaveAsPptm
  };
}
function safePrompt(message, defaultValue) {
  try {
    return window.prompt(message, defaultValue);
  } catch {
    return null;
  }
}
function safeConfirm(message) {
  try {
    return window.confirm(message);
  } catch {
    return false;
  }
}
async function renderElementToCanvas(element, scale = 2, backgroundColor) {
  const canvas = await renderToCanvas(element, {
    scale,
    useCORS: true,
    allowTaint: true,
    backgroundColor: backgroundColor ?? null,
    logging: false,
    // Ignore elements that interfere with export (selection overlays, snap lines, etc.)
    ignoreElements: (el) => {
      var _a, _b2;
      const htmlEl = el;
      if (((_a = htmlEl.dataset) == null ? void 0 : _a.exportIgnore) === "true") {
        return true;
      }
      if (((_b2 = htmlEl.classList) == null ? void 0 : _b2.contains("pointer-events-none")) && (htmlEl.classList.contains("z-50") || htmlEl.classList.contains("z-[60]"))) {
        return true;
      }
      return false;
    }
  });
  return canvas;
}
function waitForRender(ms = 100) {
  return new Promise((resolve) => {
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        setTimeout(resolve, ms);
      });
    });
  });
}
async function exportSlideToPngBlob(slideElement, options = {}) {
  const { scale = 2, backgroundColor } = options;
  const canvas = await renderElementToCanvas(slideElement, scale, backgroundColor);
  return new Promise((resolve, reject) => {
    canvas.toBlob((blob) => {
      if (blob) {
        resolve(blob);
      } else {
        reject(new Error("Canvas toBlob returned null"));
      }
    }, "image/png");
  });
}
async function exportSlideAsPng(slideElement, slideIndex, options = {}) {
  const blob = await exportSlideToPngBlob(slideElement, options);
  downloadBlob(blob, `slide-${slideIndex + 1}.png`);
}
async function copySlideToClipboard(slideElement, options = {}) {
  const blob = await exportSlideToPngBlob(slideElement, options);
  await navigator.clipboard.write([new ClipboardItem({ "image/png": blob })]);
}
async function exportAllSlidesAsPdf(slideStageRef, totalSlides, setActiveSlide, currentSlideIndex, filename = "presentation.pdf", options = {}) {
  const { scale = 2, onProgress, signal } = options;
  const images = [];
  for (let i = 0; i < totalSlides; i++) {
    if (signal == null ? void 0 : signal.aborted) {
      throw new DOMException("Export cancelled", "AbortError");
    }
    onProgress == null ? void 0 : onProgress(i, totalSlides);
    setActiveSlide(i);
    await waitForRender(150);
    const stageEl = slideStageRef.current;
    if (!stageEl) {
      console.warn(`[export] Could not find slide stage element for slide ${i}`);
      continue;
    }
    const canvas = await renderElementToCanvas(stageEl, scale);
    images.push(canvasToJpegData(canvas));
  }
  onProgress == null ? void 0 : onProgress(totalSlides, totalSlides);
  setActiveSlide(currentSlideIndex);
  if (images.length === 0) {
    throw new Error(translationsEn["pptx.export.errorNoSlidesPdf"]);
  }
  const pdfBlobUrl = buildPdfFromImageData(images);
  downloadDataUrl(pdfBlobUrl, filename);
}
async function captureAllSlidesAsPngDataUrls(slideStageRef, totalSlides, setActiveSlide, currentSlideIndex, options = {}) {
  const { scale = 2, onProgress } = options;
  const dataUrls = [];
  for (let i = 0; i < totalSlides; i++) {
    onProgress == null ? void 0 : onProgress(i, totalSlides);
    setActiveSlide(i);
    await waitForRender(150);
    const stageEl = slideStageRef.current;
    if (!stageEl) {
      console.warn(`[export] Could not find slide stage element for slide ${i}`);
      continue;
    }
    const canvas = await renderElementToCanvas(stageEl, scale);
    dataUrls.push(canvas.toDataURL("image/png"));
  }
  onProgress == null ? void 0 : onProgress(totalSlides, totalSlides);
  setActiveSlide(currentSlideIndex);
  return dataUrls;
}
async function exportAllSlidesAsNotesPdf(slideStageRef, totalSlides, setActiveSlide, currentSlideIndex, slideNotes, filename = "presentation-notes.pdf", options = {}) {
  const { scale = 2, onProgress, signal } = options;
  const pages = [];
  for (let i = 0; i < totalSlides; i++) {
    if (signal == null ? void 0 : signal.aborted) {
      throw new DOMException("Export cancelled", "AbortError");
    }
    onProgress == null ? void 0 : onProgress(i, totalSlides);
    setActiveSlide(i);
    await waitForRender(150);
    const stageEl = slideStageRef.current;
    if (!stageEl) {
      console.warn(`[export] Could not find slide stage element for slide ${i}`);
      continue;
    }
    const canvas = await renderElementToCanvas(stageEl, scale);
    pages.push({
      canvas,
      notes: slideNotes[i],
      slideNumber: i + 1
    });
  }
  onProgress == null ? void 0 : onProgress(totalSlides, totalSlides);
  setActiveSlide(currentSlideIndex);
  if (pages.length === 0) {
    throw new Error(translationsEn["pptx.export.errorNoSlidesNotesPdf"]);
  }
  const pdfDataUrl = buildNotesPdf(pages);
  downloadDataUrl(pdfDataUrl, filename);
}
async function exportAllSlidesAsVideo(slideStageRef, totalSlides, setActiveSlide, currentSlideIndex, options = {}) {
  const {
    scale = 1,
    slideDurationMs = 3e3,
    slideTimingsMs,
    onProgress,
    onRecordProgress,
    signal
  } = options;
  const slideCanvases = [];
  for (let i = 0; i < totalSlides; i++) {
    if (signal == null ? void 0 : signal.aborted) {
      throw new DOMException("Export cancelled", "AbortError");
    }
    onProgress == null ? void 0 : onProgress(i, totalSlides);
    setActiveSlide(i);
    await waitForRender(150);
    const stageEl = slideStageRef.current;
    if (!stageEl) {
      console.warn(`[export] Could not find slide stage element for slide ${i}`);
      continue;
    }
    const canvas = await renderElementToCanvas(stageEl, scale);
    slideCanvases.push(canvas);
  }
  setActiveSlide(currentSlideIndex);
  if (slideCanvases.length === 0) {
    throw new Error(translationsEn["pptx.export.errorNoSlidesVideo"]);
  }
  const firstCanvas = slideCanvases[0];
  const recordingCanvas = document.createElement("canvas");
  recordingCanvas.width = firstCanvas.width;
  recordingCanvas.height = firstCanvas.height;
  const ctx = recordingCanvas.getContext("2d");
  if (!ctx) {
    throw new Error(translationsEn["pptx.export.errorNo2dContext"]);
  }
  const fps = 30;
  const stream = recordingCanvas.captureStream(fps);
  const recorder = new MediaRecorder(stream, {
    mimeType: pickSupportedMimeType([...WEBM_MIME_CANDIDATES]),
    videoBitsPerSecond: 5e6
  });
  const chunks = [];
  recorder.ondataavailable = (e) => {
    if (e.data.size > 0) {
      chunks.push(e.data);
    }
  };
  const recorderDone = new Promise((resolve, reject) => {
    recorder.onstop = () => resolve();
    recorder.onerror = (e) => reject(e);
  });
  recorder.start();
  for (let i = 0; i < slideCanvases.length; i++) {
    if (signal == null ? void 0 : signal.aborted) {
      recorder.stop();
      throw new DOMException("Export cancelled", "AbortError");
    }
    onRecordProgress == null ? void 0 : onRecordProgress(i, slideCanvases.length);
    const duration = (slideTimingsMs == null ? void 0 : slideTimingsMs[i]) ?? slideDurationMs;
    ctx.clearRect(0, 0, recordingCanvas.width, recordingCanvas.height);
    ctx.drawImage(slideCanvases[i], 0, 0);
    const frameInterval = fpsToFrameIntervalMs(fps);
    const framesNeeded = segmentFrameCount(duration, fps);
    for (let f = 0; f < framesNeeded; f++) {
      if (signal == null ? void 0 : signal.aborted) {
        recorder.stop();
        throw new DOMException("Export cancelled", "AbortError");
      }
      ctx.drawImage(slideCanvases[i], 0, 0);
      await new Promise((r) => {
        setTimeout(r, frameInterval);
      });
    }
  }
  recorder.stop();
  await recorderDone;
  onProgress == null ? void 0 : onProgress(totalSlides, totalSlides);
  return new Blob(chunks, { type: "video/webm" });
}
async function exportAllSlidesAsGif(slideStageRef, totalSlides, setActiveSlide, currentSlideIndex, options = {}) {
  const { scale = 0.5, slideDurationMs = 2e3, onProgress, signal } = options;
  const frames = [];
  for (let i = 0; i < totalSlides; i++) {
    if (signal == null ? void 0 : signal.aborted) {
      throw new DOMException("Export cancelled", "AbortError");
    }
    onProgress == null ? void 0 : onProgress(i, totalSlides);
    setActiveSlide(i);
    await waitForRender(150);
    const stageEl = slideStageRef.current;
    if (!stageEl) {
      continue;
    }
    const canvas = await renderElementToCanvas(stageEl, scale);
    const ctx = canvas.getContext("2d");
    if (!ctx) {
      continue;
    }
    const imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
    frames.push({
      imageData,
      width: canvas.width,
      height: canvas.height
    });
  }
  setActiveSlide(currentSlideIndex);
  if (frames.length === 0) {
    throw new Error(translationsEn["pptx.export.errorNoSlidesGif"]);
  }
  const gifBytes = encodeGif(frames, Math.round(slideDurationMs / 10));
  onProgress == null ? void 0 : onProgress(totalSlides, totalSlides);
  const buf = new ArrayBuffer(gifBytes.length);
  new Uint8Array(buf).set(gifBytes);
  return new Blob([buf], { type: "image/gif" });
}
function injectFontFaces(svg, fontFaces) {
  if (!fontFaces.length) {
    return svg;
  }
  const safeFontFaces = fontFaces.filter((f) => {
    if (f.css.toLowerCase().includes("</style")) {
      console.warn(
        `[export-svg] Dropping @font-face entry for "${f.family}" containing "</style" - would break out of the <style> block.`
      );
      return false;
    }
    return true;
  });
  if (!safeFontFaces.length) {
    return svg;
  }
  const styleBlock = `<style type="text/css">${safeFontFaces.map((f) => f.css).join("\n")}</style>`;
  if (svg.includes("<defs>")) {
    return svg.replace("<defs>", `<defs>${styleBlock}`);
  }
  const closingBracket = svg.indexOf(">");
  if (closingBracket === -1) {
    return svg;
  }
  return `${svg.slice(0, closingBracket + 1)}<defs>${styleBlock}</defs>${svg.slice(closingBracket + 1)}`;
}
function exportAllSlidesToSvg(data, options = {}) {
  const { onProgress, embedFonts, fontFaces } = options;
  const svgOptions = {
    includeHidden: options.includeHidden,
    slideIndices: options.slideIndices,
    defaultFontFamily: options.defaultFontFamily,
    defaultFontSize: options.defaultFontSize
  };
  const results = [];
  const includeHidden = svgOptions.includeHidden ?? false;
  let processedCount = 0;
  for (let i = 0; i < data.slides.length; i++) {
    if (svgOptions.slideIndices && !svgOptions.slideIndices.includes(i)) {
      continue;
    }
    const slide = data.slides[i];
    if (slide.hidden && !includeHidden) {
      continue;
    }
    onProgress == null ? void 0 : onProgress(processedCount, data.slides.length);
    let svg = SvgExporter.exportSlide(slide, data.width, data.height, svgOptions);
    if (embedFonts && (fontFaces == null ? void 0 : fontFaces.length)) {
      svg = injectFontFaces(svg, fontFaces);
    }
    results.push(svg);
    processedCount++;
  }
  onProgress == null ? void 0 : onProgress(processedCount, data.slides.length);
  return results;
}
function useExportHandlers(input) {
  const {
    slides,
    templateElementsBySlideId,
    activeSlide,
    activeSlideIndex,
    filePath,
    canvasStageRef,
    setActiveSlideIndex,
    serializeSlides,
    headerFooter,
    presentationProperties,
    customShows,
    sections,
    coreProperties,
    appProperties,
    customProperties,
    tagCollections,
    notesMaster,
    handoutMaster,
    theme,
    canvasSize,
    slideSizeEmu,
    imageExportScale
  } = input;
  const [exportModalOpen, setExportModalOpen] = (0, import_react.useState)(false);
  const [exportModalTitle, setExportModalTitle] = (0, import_react.useState)("");
  const [exportProgress, setExportProgress] = (0, import_react.useState)(0);
  const [exportStatusMessage, setExportStatusMessage] = (0, import_react.useState)("");
  const exportAbortRef = (0, import_react.useRef)(null);
  const {
    handleExportJson,
    handleSaveAsFormat,
    handleSaveAsPptx,
    handleSaveAsPpsx,
    handleSaveAsPptm
  } = useExportSaveAs({
    slides,
    templateElementsBySlideId,
    filePath,
    serializeSlides,
    headerFooter,
    presentationProperties,
    customShows,
    sections,
    coreProperties,
    appProperties,
    customProperties,
    tagCollections,
    notesMaster,
    handoutMaster,
    theme,
    canvasSize,
    slideSizeEmu
  });
  const handleExportPng = (0, import_react.useCallback)(async () => {
    const stageEl = canvasStageRef.current;
    if (!stageEl) {
      return;
    }
    try {
      await exportSlideAsPng(stageEl, activeSlideIndex, {
        backgroundColor: activeSlide == null ? void 0 : activeSlide.backgroundColor,
        scale: imageExportScale
      });
    } catch (err) {
      console.error("[PowerPointViewer] PNG export failed:", err);
    }
  }, [canvasStageRef, activeSlideIndex, activeSlide == null ? void 0 : activeSlide.backgroundColor, imageExportScale]);
  const handleExportPdf = (0, import_react.useCallback)(async () => {
    if (!canvasStageRef.current) {
      return;
    }
    const abortCtrl = new AbortController();
    exportAbortRef.current = abortCtrl;
    setExportModalTitle("Export as PDF");
    setExportStatusMessage("Capturing slides...");
    setExportProgress(0);
    setExportModalOpen(true);
    try {
      await exportAllSlidesAsPdf(
        canvasStageRef,
        slides.length,
        setActiveSlideIndex,
        activeSlideIndex,
        "presentation.pdf",
        {
          scale: imageExportScale,
          onProgress: (current, total) => {
            setExportProgress(slideProgressPercent(current, total));
            setExportStatusMessage(slideStatusLabel("Rendering", current, total));
          },
          signal: abortCtrl.signal
        }
      );
      setExportProgress(EXPORT_ASSEMBLING_PERCENT);
      setExportStatusMessage("Building PDF...");
      await new Promise((r) => {
        setTimeout(r, 100);
      });
      setExportProgress(EXPORT_DONE_PERCENT);
      setExportStatusMessage("Done!");
    } catch (err) {
      if (!isExportAbortError(err)) {
        console.error("[PowerPointViewer] PDF export failed:", err);
      }
    } finally {
      exportAbortRef.current = null;
      setExportModalOpen(false);
    }
  }, [canvasStageRef, slides.length, setActiveSlideIndex, activeSlideIndex, imageExportScale]);
  const handleExportNotesPdf = (0, import_react.useCallback)(async () => {
    if (!canvasStageRef.current) {
      return;
    }
    const abortCtrl = new AbortController();
    exportAbortRef.current = abortCtrl;
    setExportModalTitle("Export as PDF (Notes)");
    setExportStatusMessage("Capturing slides...");
    setExportProgress(0);
    setExportModalOpen(true);
    try {
      const slideNotes = slides.map((s) => s.notes);
      await exportAllSlidesAsNotesPdf(
        canvasStageRef,
        slides.length,
        setActiveSlideIndex,
        activeSlideIndex,
        slideNotes,
        "presentation-notes.pdf",
        {
          scale: imageExportScale,
          onProgress: (current, total) => {
            setExportProgress(slideProgressPercent(current, total));
            setExportStatusMessage(slideStatusLabel("Rendering", current, total));
          },
          signal: abortCtrl.signal
        }
      );
      setExportProgress(EXPORT_ASSEMBLING_PERCENT);
      setExportStatusMessage("Building PDF...");
      await new Promise((r) => {
        setTimeout(r, 100);
      });
      setExportProgress(EXPORT_DONE_PERCENT);
      setExportStatusMessage("Done!");
    } catch (err) {
      if (!isExportAbortError(err)) {
        console.error("[PowerPointViewer] Notes PDF export failed:", err);
      }
    } finally {
      exportAbortRef.current = null;
      setExportModalOpen(false);
    }
  }, [canvasStageRef, slides, setActiveSlideIndex, activeSlideIndex, imageExportScale]);
  const handleCopySlideAsImage = (0, import_react.useCallback)(async () => {
    const stageEl = canvasStageRef.current;
    if (!stageEl) {
      return;
    }
    try {
      await copySlideToClipboard(stageEl, {
        backgroundColor: activeSlide == null ? void 0 : activeSlide.backgroundColor,
        scale: imageExportScale
      });
    } catch (err) {
      console.error("[PowerPointViewer] Copy slide as image failed:", err);
    }
  }, [canvasStageRef, activeSlide == null ? void 0 : activeSlide.backgroundColor, imageExportScale]);
  const handleExportVideo = (0, import_react.useCallback)(async () => {
    if (!canvasStageRef.current) {
      return;
    }
    const abortCtrl = new AbortController();
    exportAbortRef.current = abortCtrl;
    setExportModalTitle("Export as Video");
    setExportStatusMessage("Capturing slides...");
    setExportProgress(0);
    setExportModalOpen(true);
    try {
      const blob = await exportAllSlidesAsVideo(
        canvasStageRef,
        slides.length,
        setActiveSlideIndex,
        activeSlideIndex,
        {
          scale: 1,
          slideDurationMs: 3e3,
          onProgress: (current, total) => {
            setExportProgress(slideProgressPercent(current, total, 45));
            setExportStatusMessage(slideStatusLabel("Capturing", current, total));
          },
          onRecordProgress: (current, total) => {
            setExportProgress(recordProgressPercent(current, total));
            setExportStatusMessage(slideStatusLabel("Recording", current, total));
          },
          signal: abortCtrl.signal
        }
      );
      setExportProgress(EXPORT_ASSEMBLING_PERCENT);
      setExportStatusMessage("Saving file...");
      downloadBlob(blob, "presentation.webm");
      setExportProgress(EXPORT_DONE_PERCENT);
    } catch (err) {
      if (!isExportAbortError(err)) {
        console.error("[PowerPointViewer] Video export failed:", err);
      }
    } finally {
      exportAbortRef.current = null;
      setExportModalOpen(false);
    }
  }, [canvasStageRef, slides.length, setActiveSlideIndex, activeSlideIndex]);
  const handleExportGif = (0, import_react.useCallback)(async () => {
    if (!canvasStageRef.current) {
      return;
    }
    const abortCtrl = new AbortController();
    exportAbortRef.current = abortCtrl;
    setExportModalTitle("Export as GIF");
    setExportStatusMessage("Capturing slides...");
    setExportProgress(0);
    setExportModalOpen(true);
    try {
      const blob = await exportAllSlidesAsGif(
        canvasStageRef,
        slides.length,
        setActiveSlideIndex,
        activeSlideIndex,
        {
          scale: 0.5,
          slideDurationMs: 2e3,
          onProgress: (current, total) => {
            setExportProgress(slideProgressPercent(current, total));
            setExportStatusMessage(slideStatusLabel("Encoding", current, total));
          },
          signal: abortCtrl.signal
        }
      );
      setExportProgress(EXPORT_ASSEMBLING_PERCENT);
      setExportStatusMessage("Saving file...");
      downloadBlob(blob, "presentation.gif");
      setExportProgress(EXPORT_DONE_PERCENT);
    } catch (err) {
      if (!isExportAbortError(err)) {
        console.error("[PowerPointViewer] GIF export failed:", err);
      }
    } finally {
      exportAbortRef.current = null;
      setExportModalOpen(false);
    }
  }, [canvasStageRef, slides.length, setActiveSlideIndex, activeSlideIndex]);
  const handleCancelExport = (0, import_react.useCallback)(() => {
    var _a;
    (_a = exportAbortRef.current) == null ? void 0 : _a.abort();
    exportAbortRef.current = null;
    setExportModalOpen(false);
    setExportProgress(0);
  }, []);
  return {
    handleExportPng,
    handleExportPdf,
    handleExportNotesPdf,
    handleCopySlideAsImage,
    handleExportVideo,
    handleExportGif,
    handleExportJson,
    handleSaveAsFormat,
    handleSaveAsPptx,
    handleSaveAsPpsx,
    handleSaveAsPptm,
    handleCancelExport,
    exportModalOpen,
    exportModalTitle,
    exportProgress,
    exportStatusMessage
  };
}
var ViewerOptionsContext = (0, import_react.createContext)(DEFAULT_VIEWER_OPTIONS);
function useViewerOptionsContext() {
  return (0, import_react.useContext)(ViewerOptionsContext);
}
function usePrintHandlers(input) {
  const { slides, activeSlideIndex, canvasStageRef, setActiveSlideIndex, pptxData } = input;
  const [isPrintDialogOpen, setIsPrintDialogOpen] = (0, import_react.useState)(false);
  const { t } = useTranslation();
  const viewerOptions = useViewerOptionsContext();
  const includeHiddenSlides = viewerOptions.advanced.printHiddenSlides;
  const printRasterScale = (viewerOptions.advanced.printHighQuality ? 4 : 3) * resolveImageResolutionScale(viewerOptions);
  const handlePrint = () => {
    setIsPrintDialogOpen(true);
  };
  const handlePrintSvg = async (settings) => {
    setIsPrintDialogOpen(false);
    const printWindow = openPendingPrintWindow(t("pptx.print.preparingToPrint"));
    if (!printWindow) {
      console.warn(
        "[PowerPointViewer] Print window was blocked by the browser. Allow popups for this site to print."
      );
      return;
    }
    if (!pptxData || settings.printWhat !== "slides") {
      return runRasterPrint(printWindow, settings);
    }
    const colorFilter = computeColorFilter(settings.colorMode);
    const slideIndices = filterHiddenSlideIndices(
      computeSlideIndices(
        settings.slideRange,
        activeSlideIndex,
        slides.length,
        settings.customRangeFrom,
        settings.customRangeTo
      ),
      slides,
      includeHiddenSlides
    );
    try {
      const svgs = exportAllSlidesToSvg(pptxData, {
        slideIndices
      });
      if (svgs.length === 0) {
        printWindow.close();
        return;
      }
      const printDoc = buildPrintDocument(svgs, pptxData.width, pptxData.height, {
        title: "Slides (Vector)",
        orientation: settings.orientation,
        colorFilter,
        scaleToFit: settings.scaleToFit
      });
      finishPrintWindow(printWindow, printDoc);
    } catch (err) {
      console.warn("[PowerPointViewer] SVG print path failed, falling back to raster:", err);
      return runRasterPrint(printWindow, settings);
    }
  };
  const runRasterPrint = async (printWindow, settings) => {
    const colorFilter = computeColorFilter(settings.colorMode);
    const slideIndices = filterHiddenSlideIndices(
      computeSlideIndices(
        settings.slideRange,
        activeSlideIndex,
        slides.length,
        settings.customRangeFrom,
        settings.customRangeTo
      ),
      slides,
      includeHiddenSlides
    );
    if (settings.printWhat === "outline") {
      finishPrintWindow(
        printWindow,
        buildPrintHtmlDocument({
          title: "Outline",
          bodyHtml: `<div class="outline-page">${buildOutlineHtml(slideIndices, slides)}</div>`,
          orientation: settings.orientation,
          colorFilter,
          frameSlides: settings.frameSlides,
          scaleToFit: settings.scaleToFit
        })
      );
      return;
    }
    try {
      if (!canvasStageRef.current) {
        printWindow.close();
        return;
      }
      const allImages = await captureAllSlidesAsPngDataUrls(
        canvasStageRef,
        slides.length,
        setActiveSlideIndex,
        activeSlideIndex,
        { scale: printRasterScale }
      );
      if (allImages.length === 0) {
        printWindow.close();
        return;
      }
      const slideImages = slideIndices.map((idx) => allImages[idx]).filter(Boolean);
      if (settings.printWhat === "slides") {
        finishPrintWindow(
          printWindow,
          buildPrintHtmlDocument({
            title: "Slides",
            bodyHtml: buildSlidesHtml(slideImages, slideIndices),
            orientation: settings.orientation,
            colorFilter,
            frameSlides: settings.frameSlides,
            scaleToFit: settings.scaleToFit
          })
        );
        return;
      }
      if (settings.printWhat === "notes") {
        finishPrintWindow(
          printWindow,
          buildPrintHtmlDocument({
            title: "Notes Pages",
            bodyHtml: buildNotesHtml(slideImages, slideIndices, slides),
            orientation: "portrait",
            colorFilter,
            frameSlides: settings.frameSlides,
            scaleToFit: settings.scaleToFit
          })
        );
        return;
      }
      if (settings.printWhat === "handouts") {
        const spp = settings.slidesPerPage;
        finishPrintWindow(
          printWindow,
          buildPrintHtmlDocument({
            title: `Handout ${spp} per page`,
            bodyHtml: buildHandoutsHtml(slideImages, slideIndices, spp, pptxData == null ? void 0 : pptxData.handoutMaster),
            orientation: "portrait",
            colorFilter,
            frameSlides: settings.frameSlides,
            scaleToFit: settings.scaleToFit
          })
        );
      }
    } catch (err) {
      console.error("[PowerPointViewer] Print layout failed:", err);
    }
  };
  const handlePrintWithSettings = async (settings) => {
    setIsPrintDialogOpen(false);
    const printWindow = openPendingPrintWindow(t("pptx.print.preparingToPrint"));
    if (!printWindow) {
      console.warn(
        "[PowerPointViewer] Print window was blocked by the browser. Allow popups for this site to print."
      );
      return;
    }
    return runRasterPrint(printWindow, settings);
  };
  return {
    handlePrint,
    handlePrintWithSettings,
    handlePrintSvg,
    isPrintDialogOpen,
    setIsPrintDialogOpen
  };
}
function useThemeHandlers(input) {
  const {
    handlerRef,
    serializeSlides,
    setContent,
    onContentChange,
    setTheme,
    setSlideMasters,
    slideMasters,
    history,
    setSlides,
    templateElementsBySlideId,
    setTemplateElementsBySlideId,
    theme,
    bumpHistory
  } = input;
  const refreshContentAfterThemeChange = async () => {
    const updated = await serializeSlides();
    if (!updated) {
      return;
    }
    setContent(updated);
    if (onContentChange) {
      onContentChange(updated);
    }
    history.markDirty();
  };
  const handleApplyTheme = async (themePath, applyToAllMasters) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    await handler.setPresentationTheme(themePath, applyToAllMasters);
    setSlideMasters(
      (prev) => prev.map(
        (master, index) => applyToAllMasters || index === 0 ? { ...master, themePath } : master
      )
    );
    await refreshContentAfterThemeChange();
  };
  const handleUpdateThemeColorScheme = async (colorScheme) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    await handler.updateThemeColorScheme(colorScheme);
    const previousMap = (theme == null ? void 0 : theme.colorScheme) ? buildThemeColorMap(theme.colorScheme) : {};
    setSlides((prev) => reResolveSlideColors(prev, previousMap, colorScheme));
    if (Object.keys(templateElementsBySlideId).length > 0) {
      setTemplateElementsBySlideId((prev) => {
        const next = {};
        for (const [slideId, elements] of Object.entries(prev)) {
          next[slideId] = reResolveElementColors(elements, previousMap, colorScheme);
        }
        return next;
      });
    }
    setTheme((prev) => prev ? { ...prev, colorScheme } : { colorScheme });
    bumpHistory();
    history.markDirty();
  };
  const handleUpdateThemeFontScheme = async (fontScheme) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    await handler.updateThemeFontScheme(fontScheme);
    setTheme((prev) => prev ? { ...prev, fontScheme } : { fontScheme });
    await refreshContentAfterThemeChange();
  };
  const handleUpdateThemeName = async (name) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    await handler.updateThemeName(name);
    setTheme((prev) => prev ? { ...prev, name } : { name });
    history.markDirty();
  };
  const handleApplyThemeToPresentation = async () => {
    var _a;
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    const primaryThemePath = (_a = slideMasters[0]) == null ? void 0 : _a.themePath;
    if (primaryThemePath) {
      await handler.setPresentationTheme(primaryThemePath, true);
    }
    await refreshContentAfterThemeChange();
  };
  const handleApplyThemeData = async (colorScheme, fontScheme, themeName) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    await handler.applyTheme(colorScheme, fontScheme, themeName);
    setTheme((prev) => ({
      ...prev,
      colorScheme,
      fontScheme,
      ...themeName && { name: themeName }
    }));
    await refreshContentAfterThemeChange();
  };
  const handleSetTemplateBackground = (path, backgroundColor) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    handler.setTemplateBackground(path, backgroundColor);
    setSlideMasters(
      (prev) => prev.map((m) => m.path === path ? { ...m, backgroundColor } : m)
    );
    history.markDirty();
  };
  const handleGetTemplateBackgroundColor = (path) => {
    const handler = handlerRef.current;
    if (!handler) {
      return void 0;
    }
    return handler.getTemplateBackgroundColor(path);
  };
  const handleGetAvailableThemes = async () => {
    const handler = handlerRef.current;
    if (!handler) {
      return [];
    }
    return handler.getAvailableThemes();
  };
  const handleSwitchTheme = async (themePath) => {
    const handler = handlerRef.current;
    if (!handler) {
      return;
    }
    await handler.setPresentationTheme(themePath, true);
    setSlideMasters((prev) => prev.map((master) => ({ ...master, themePath })));
    await refreshContentAfterThemeChange();
  };
  return {
    handleApplyTheme,
    handleUpdateThemeColorScheme,
    handleUpdateThemeFontScheme,
    handleUpdateThemeName,
    handleApplyThemeToPresentation,
    handleApplyThemeData,
    handleSetTemplateBackground,
    handleGetTemplateBackgroundColor,
    handleGetAvailableThemes,
    handleSwitchTheme
  };
}
function usePropertyHandlers(input) {
  const {
    slides,
    activeSlideIndex,
    canvasSize,
    setContent,
    setPresentationProperties,
    setCoreProperties,
    setAppProperties,
    setCustomProperties,
    setSlides,
    setIsDirty,
    ops,
    history
  } = input;
  const [isVersionHistoryOpen, setIsVersionHistoryOpen] = (0, import_react.useState)(false);
  const [isComparePanelOpen, setIsComparePanelOpen] = (0, import_react.useState)(false);
  const [compareResult, setCompareResult] = (0, import_react.useState)(null);
  const handleUpdateNotes = (text, segments) => {
    ops.updateSlides(
      (prev) => prev.map(
        (s, i) => i === activeSlideIndex ? {
          ...s,
          notes: text,
          notesSegments: segments && segments.length > 0 ? segments : s.notesSegments
        } : s
      )
    );
    history.markDirty();
  };
  const handleUpdateSlide = (updates) => {
    ops.updateSlides(
      (prev) => prev.map((s, i) => i === activeSlideIndex ? { ...s, ...updates } : s)
    );
    history.markDirty();
  };
  const handleUpdatePresentationProperties = (updates) => {
    setPresentationProperties((prev) => ({ ...prev, ...updates }));
    history.markDirty();
  };
  const handleUpdateCoreProperties = (updates) => {
    setCoreProperties((prev) => ({ ...prev, ...updates }));
    history.markDirty();
  };
  const handleUpdateAppProperties = (updates) => {
    setAppProperties((prev) => ({ ...prev, ...updates }));
    history.markDirty();
  };
  const handleUpdateCustomProperties = (next) => {
    setCustomProperties(next);
    history.markDirty();
  };
  const handleRestoreVersion = (0, import_react.useCallback)(
    (versionData) => {
      setContent(versionData);
    },
    [setContent]
  );
  const handleCompare = (0, import_react.useCallback)(async () => {
    const fileInput = document.createElement("input");
    fileInput.type = "file";
    fileInput.accept = ".pptx";
    fileInput.onchange = async () => {
      var _a;
      const file = (_a = fileInput.files) == null ? void 0 : _a[0];
      if (!file) {
        return;
      }
      try {
        const ab = await file.arrayBuffer();
        const h = new PptxHandler();
        const other = await h.load(ab);
        if (!other) {
          return;
        }
        const cur = {
          slides,
          width: canvasSize.width,
          height: canvasSize.height
        };
        setCompareResult(comparePresentation(cur, other));
        setIsComparePanelOpen(true);
      } catch (err) {
        console.warn("Compare failed:", err);
      }
    };
    fileInput.click();
  }, [slides, canvasSize.width, canvasSize.height]);
  const handleAcceptSlide = (0, import_react.useCallback)(
    (di) => {
      if (!compareResult) {
        return;
      }
      const diff = compareResult.diffs[di];
      if (!diff || diff.status === "unchanged") {
        return;
      }
      setSlides((prev) => applyAcceptSlide(prev, diff));
      setIsDirty(true);
    },
    [compareResult, setSlides, setIsDirty]
  );
  const handleRejectSlide = (0, import_react.useCallback)((_di) => {
  }, []);
  const handleAcceptAllSlides = (0, import_react.useCallback)(() => {
    if (!compareResult) {
      return;
    }
    setSlides((prev) => applyAcceptAllSlides(prev, compareResult));
    setIsDirty(true);
  }, [compareResult, setSlides, setIsDirty]);
  const usedFontFamilies = (0, import_react.useMemo)(() => collectUsedFonts(slides), [slides]);
  return {
    handleUpdateNotes,
    handleUpdateSlide,
    handleUpdatePresentationProperties,
    handleUpdateCoreProperties,
    handleUpdateAppProperties,
    handleUpdateCustomProperties,
    handleRestoreVersion,
    handleCompare,
    handleAcceptSlide,
    handleRejectSlide,
    handleAcceptAllSlides,
    isVersionHistoryOpen,
    setIsVersionHistoryOpen,
    isComparePanelOpen,
    setIsComparePanelOpen,
    compareResult,
    usedFontFamilies
  };
}
function useDialogCustomShows(input) {
  const {
    activeSlide,
    customShows,
    activeCustomShowId,
    setCustomShows,
    setActiveCustomShowId,
    history
  } = input;
  const handleCreateCustomShow = () => {
    const name = safePrompt("Custom show name", "Custom Show");
    const safeName = (name == null ? void 0 : name.trim()) || `Custom Show ${customShows.length + 1}`;
    const id = `custShow-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
    const initialSlideRIds = (activeSlide == null ? void 0 : activeSlide.rId) && activeSlide.rId.length > 0 ? [activeSlide.rId] : [];
    setCustomShows((prev) => [...prev, { id, name: safeName, slideRIds: initialSlideRIds }]);
    setActiveCustomShowId(id);
    history.markDirty();
  };
  const handleRenameActiveCustomShow = () => {
    var _a;
    if (!activeCustomShowId) {
      return;
    }
    const show = customShows.find((s) => s.id === activeCustomShowId);
    if (!show) {
      return;
    }
    const nextName = (_a = safePrompt("Rename custom show", show.name)) == null ? void 0 : _a.trim();
    if (!nextName) {
      return;
    }
    setCustomShows((prev) => prev.map((s) => s.id === show.id ? { ...s, name: nextName } : s));
    history.markDirty();
  };
  const handleDeleteActiveCustomShow = () => {
    const showId = activeCustomShowId;
    if (!showId) {
      return;
    }
    const show = customShows.find((s) => s.id === showId);
    if (!show) {
      return;
    }
    if (!safeConfirm(`Delete custom show "${show.name}"?`)) {
      return;
    }
    setCustomShows((prev) => prev.filter((s) => s.id !== showId));
    setActiveCustomShowId(null);
    history.markDirty();
  };
  const handleToggleCurrentSlideInActiveShow = () => {
    const showId = activeCustomShowId;
    const slideRId = activeSlide == null ? void 0 : activeSlide.rId;
    if (!showId || !slideRId) {
      return;
    }
    setCustomShows(
      (prev) => prev.map((s) => {
        if (s.id !== showId) {
          return s;
        }
        const hasSlide = s.slideRIds.includes(slideRId);
        return {
          ...s,
          slideRIds: hasSlide ? s.slideRIds.filter((rid) => rid !== slideRId) : [...s.slideRIds, slideRId]
        };
      })
    );
    history.markDirty();
  };
  const isCurrentSlideInActiveShow = (0, import_react.useMemo)(() => {
    if (!activeCustomShowId || !(activeSlide == null ? void 0 : activeSlide.rId)) {
      return false;
    }
    const show = customShows.find((s) => s.id === activeCustomShowId);
    return show ? show.slideRIds.includes(activeSlide.rId) : false;
  }, [activeCustomShowId, activeSlide == null ? void 0 : activeSlide.rId, customShows]);
  return {
    handleCreateCustomShow,
    handleRenameActiveCustomShow,
    handleDeleteActiveCustomShow,
    handleToggleCurrentSlideInActiveShow,
    isCurrentSlideInActiveShow
  };
}
function viewportIsTouch() {
  if (typeof window === "undefined") {
    return false;
  }
  return "ontouchstart" in window || navigator.maxTouchPoints > 0;
}
function useViewerDialogs(input) {
  const {
    mode,
    slides,
    activeSlide,
    canvasSize,
    customShows,
    activeCustomShowId,
    setCustomShows,
    setActiveCustomShowId,
    setGuides,
    setPresentationProperties,
    setAccessibilityIssues,
    setIsAccessibilityPanelOpen,
    setMode,
    setPreMasterMode,
    setActiveMasterIndex,
    setActiveLayoutIndex,
    setSelectedElementId,
    setSelectedElementIds,
    preMasterMode,
    hasDigitalSignatures,
    isDirty,
    history,
    embeddedFontFamilies
  } = input;
  const [isSmartArtDialogOpen, setIsSmartArtDialogOpen] = (0, import_react.useState)(false);
  const [isEquationDialogOpen, setIsEquationDialogOpen] = (0, import_react.useState)(false);
  const [isHyperlinkDialogOpen, setIsHyperlinkDialogOpen] = (0, import_react.useState)(false);
  const [isPasswordDialogOpen, setIsPasswordDialogOpen] = (0, import_react.useState)(false);
  const [isDocPropsDialogOpen, setIsDocPropsDialogOpen] = (0, import_react.useState)(false);
  const [isFontEmbeddingOpen, setIsFontEmbeddingOpen] = (0, import_react.useState)(false);
  const [isDigitalSigDialogOpen, setIsDigitalSigDialogOpen] = (0, import_react.useState)(false);
  const [isSignatureStrippedDialogOpen, setIsSignatureStrippedDialogOpen] = (0, import_react.useState)(false);
  const signatureStripAcknowledgedRef = (0, import_react.useRef)(false);
  const [isSetUpSlideShowOpen, setIsSetUpSlideShowOpen] = (0, import_react.useState)(false);
  const [isBroadcastDialogOpen, setIsBroadcastDialogOpen] = (0, import_react.useState)(false);
  const [isPasswordProtected, setIsPasswordProtected] = (0, import_react.useState)(false);
  const [presentationPassword, setPresentationPassword] = (0, import_react.useState)(null);
  const [editingEquationOmml, setEditingEquationOmml] = (0, import_react.useState)(
    null
  );
  const embeddedFontKey = (embeddedFontFamilies ?? []).join("\0");
  const fontEmbedding = (0, import_react.useMemo)(
    () => describeFontEmbedding(embeddedFontKey ? embeddedFontKey.split("\0") : []),
    [embeddedFontKey]
  );
  const [embedFontsEnabled, setEmbedFontsEnabled] = (0, import_react.useState)(fontEmbedding.initialEnabled);
  const [embedFontsKey, setEmbedFontsKey] = (0, import_react.useState)(embeddedFontKey);
  if (embeddedFontKey !== embedFontsKey) {
    setEmbedFontsKey(embeddedFontKey);
    setEmbedFontsEnabled(fontEmbedding.initialEnabled);
  }
  const [isNarrowViewport, setIsNarrowViewport] = (0, import_react.useState)(
    () => typeof window !== "undefined" ? isMobileViewport(window.innerWidth, window.innerHeight, viewportIsTouch()) : false
  );
  (0, import_react.useEffect)(() => {
    const handleWindow = () => setIsNarrowViewport(
      isMobileViewport(window.innerWidth, window.innerHeight, viewportIsTouch())
    );
    window.addEventListener("resize", handleWindow);
    return () => window.removeEventListener("resize", handleWindow);
  }, []);
  (0, import_react.useEffect)(() => {
    if (isDirty && hasDigitalSignatures && !signatureStripAcknowledgedRef.current) {
      setIsSignatureStrippedDialogOpen(true);
    }
  }, [isDirty, hasDigitalSignatures]);
  const handleEnterMasterView = (0, import_react.useCallback)(() => {
    setPreMasterMode(mode === "master" ? "edit" : mode);
    setMode("master");
    setActiveMasterIndex(0);
    setActiveLayoutIndex(null);
    setSelectedElementId(null);
    setSelectedElementIds([]);
  }, [
    mode,
    setPreMasterMode,
    setMode,
    setActiveMasterIndex,
    setActiveLayoutIndex,
    setSelectedElementId,
    setSelectedElementIds
  ]);
  const handleCloseMasterView = (0, import_react.useCallback)(() => {
    setMode(preMasterMode);
    setSelectedElementId(null);
    setSelectedElementIds([]);
  }, [preMasterMode, setMode, setSelectedElementId, setSelectedElementIds]);
  const handleSelectMaster = (0, import_react.useCallback)(
    (index) => {
      setActiveMasterIndex(index);
      setActiveLayoutIndex(null);
      setSelectedElementId(null);
      setSelectedElementIds([]);
    },
    [setActiveMasterIndex, setActiveLayoutIndex, setSelectedElementId, setSelectedElementIds]
  );
  const handleSelectLayout = (0, import_react.useCallback)(
    (masterIndex, layoutIndex) => {
      setActiveMasterIndex(masterIndex);
      setActiveLayoutIndex(layoutIndex);
      setSelectedElementId(null);
      setSelectedElementIds([]);
    },
    [setActiveMasterIndex, setActiveLayoutIndex, setSelectedElementId, setSelectedElementIds]
  );
  const handleAddGuide = (axis) => {
    const position = axis === "h" ? canvasSize.height / 2 : canvasSize.width / 2;
    setGuides((prev) => [
      ...prev,
      {
        id: `guide-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
        axis,
        position
      }
    ]);
  };
  const customShowHandlers = useDialogCustomShows({
    activeSlide,
    customShows,
    activeCustomShowId,
    setCustomShows,
    setActiveCustomShowId,
    history
  });
  const handleSaveSlideShowSettings = (0, import_react.useCallback)(
    (props) => {
      setPresentationProperties(props);
      history.markDirty();
    },
    [setPresentationProperties, history]
  );
  const handleToggleSubtitles = (0, import_react.useCallback)(() => {
    setPresentationProperties((prev) => ({
      ...prev,
      showSubtitles: !prev.showSubtitles
    }));
    history.markDirty();
  }, [setPresentationProperties, history]);
  const handleSetPassword = (0, import_react.useCallback)((password) => {
    setPresentationPassword(password);
    setIsPasswordProtected(true);
  }, []);
  const handleRemovePassword = (0, import_react.useCallback)(() => {
    setPresentationPassword(null);
    setIsPasswordProtected(false);
  }, []);
  const handleRunAccessibilityCheck = () => {
    const issues = [];
    slides.forEach((slide, si) => {
      for (const el of slide.elements) {
        if ((el.type === "image" || el.type === "picture") && !("altText" in el && el.altText)) {
          issues.push({
            slideIndex: si,
            elementId: el.id,
            severity: "warning",
            message: "Image missing alt text"
          });
        }
      }
    });
    if (issues.length > 0) {
      console.info(`[Accessibility] Found ${issues.length} issue(s):`, issues);
    }
    setAccessibilityIssues(issues);
    setIsAccessibilityPanelOpen(true);
  };
  return {
    isSmartArtDialogOpen,
    setIsSmartArtDialogOpen,
    isEquationDialogOpen,
    setIsEquationDialogOpen,
    isHyperlinkDialogOpen,
    setIsHyperlinkDialogOpen,
    isPasswordDialogOpen,
    setIsPasswordDialogOpen,
    isDocPropsDialogOpen,
    setIsDocPropsDialogOpen,
    isFontEmbeddingOpen,
    setIsFontEmbeddingOpen,
    isDigitalSigDialogOpen,
    setIsDigitalSigDialogOpen,
    isSignatureStrippedDialogOpen,
    setIsSignatureStrippedDialogOpen,
    isSetUpSlideShowOpen,
    setIsSetUpSlideShowOpen,
    isBroadcastDialogOpen,
    setIsBroadcastDialogOpen,
    isPasswordProtected,
    presentationPassword,
    editingEquationOmml,
    setEditingEquationOmml,
    embedFontsEnabled,
    setEmbedFontsEnabled,
    fontEmbedding,
    isNarrowViewport,
    handleEnterMasterView,
    handleCloseMasterView,
    handleSelectMaster,
    handleSelectLayout,
    handleAddGuide,
    ...customShowHandlers,
    handleSaveSlideShowSettings,
    handleToggleSubtitles,
    handleSetPassword,
    handleRemovePassword,
    handleRunAccessibilityCheck
  };
}
function computeGridSpacingPx2(documentGridSpacing) {
  return computeGridSpacingPx(documentGridSpacing, GRID_SIZE);
}
function computeVisibleSlideIndexes(slides, activeCustomShowId, customShows, presentationProperties) {
  const activeShow = activeCustomShowId ? customShows.find((show) => show.id === activeCustomShowId) : void 0;
  const authoredRange = resolveAuthoredSlideRange(presentationProperties, slides.length);
  return resolveShowSlideIndexes(slides, activeShow, authoredRange);
}
function computeSlideSectionGroups(slides, sections) {
  return groupSlidesBySection(sections, slides).map((group) => {
    var _a, _b2, _c, _d;
    return {
      id: ((_a = group.section) == null ? void 0 : _a.id) ?? (sections.length > 0 ? UNGROUPED_SECTION_ID : DEFAULT_SECTION_GROUP_ID),
      label: ((_b2 = group.section) == null ? void 0 : _b2.name) ?? (sections.length > 0 ? "Ungrouped Slides" : "Slides"),
      slideIndexes: group.slideIndexes,
      ...((_c = group.section) == null ? void 0 : _c.color) !== void 0 ? { color: group.section.color } : {},
      ...((_d = group.section) == null ? void 0 : _d.collapsed) !== void 0 ? { defaultCollapsed: group.section.collapsed } : {}
    };
  });
}
function computeMasterPseudoSlide(mode, activeLayout, activeMaster) {
  if (mode !== "master" || !activeMaster) {
    return void 0;
  }
  const document2 = activeLayout ? { slideMasters: [{ ...activeMaster, layouts: [activeLayout] }] } : { slideMasters: [activeMaster] };
  return masterViewPseudoSlide(document2, {
    tab: "slides",
    masterIndex: 0,
    layoutIndex: activeLayout ? 0 : null
  });
}
function useDerivedSlideState(input) {
  const { t } = useTranslation();
  const {
    slides,
    sections,
    customShows,
    activeCustomShowId,
    presentationProperties,
    mode,
    activeLayout,
    activeMaster,
    documentGridSpacing
  } = input;
  const gridSpacingPx = (0, import_react.useMemo)(
    () => computeGridSpacingPx2(documentGridSpacing),
    [documentGridSpacing]
  );
  const visibleSlideIndexes = (0, import_react.useMemo)(
    () => computeVisibleSlideIndexes(slides, activeCustomShowId, customShows, presentationProperties),
    [slides, activeCustomShowId, customShows, presentationProperties]
  );
  const authoredRange = (0, import_react.useMemo)(
    () => resolveAuthoredSlideRange(presentationProperties, slides.length),
    [presentationProperties, slides.length]
  );
  const slideSectionGroups = (0, import_react.useMemo)(
    () => computeSlideSectionGroups(slides, sections).map((group) => {
      if (group.id === UNGROUPED_SECTION_ID && group.label === "Ungrouped Slides") {
        return { ...group, label: t("pptx.slides.ungroupedSlides") };
      }
      if (group.id === DEFAULT_SECTION_GROUP_ID && group.label === "Slides") {
        return { ...group, label: t("pptx.sections.slides") };
      }
      return group;
    }),
    [slides, sections, t]
  );
  const masterPseudoSlide = (0, import_react.useMemo)(
    () => computeMasterPseudoSlide(mode, activeLayout, activeMaster),
    [mode, activeLayout, activeMaster]
  );
  return {
    gridSpacingPx,
    visibleSlideIndexes,
    slideSectionGroups,
    masterPseudoSlide,
    authoredRange
  };
}
function useAnnotationHandlers(input) {
  const {
    mode,
    presentation,
    annotations,
    history,
    setMode,
    setSlides,
    promptKeepInkAnnotations = true
  } = input;
  const [showKeepAnnotationsDialog, setShowKeepAnnotationsDialog] = (0, import_react.useState)(false);
  const pendingModeAfterAnnotationsRef = (0, import_react.useRef)("edit");
  const handleKeepAnnotations = (0, import_react.useCallback)(() => {
    const annotationMap = annotations.allSlideAnnotations;
    if (annotationMap.size === 0) {
      setShowKeepAnnotationsDialog(false);
      setMode(pendingModeAfterAnnotationsRef.current);
      annotations.clearAllAnnotations();
      return;
    }
    setSlides(
      (prev) => prev.map((slide, slideIndex) => {
        const strokes = annotationMap.get(slideIndex);
        if (!strokes || strokes.length === 0) {
          return slide;
        }
        const inkElements = strokes.map((stroke) => {
          let minX = Infinity;
          let minY = Infinity;
          let maxX = -Infinity;
          let maxY = -Infinity;
          for (const pt of stroke.points) {
            if (pt.x < minX) {
              minX = pt.x;
            }
            if (pt.y < minY) {
              minY = pt.y;
            }
            if (pt.x > maxX) {
              maxX = pt.x;
            }
            if (pt.y > maxY) {
              maxY = pt.y;
            }
          }
          const bboxWidth = Math.max(maxX - minX, 1);
          const bboxHeight = Math.max(maxY - minY, 1);
          const pathParts = [];
          for (let i = 0; i < stroke.points.length; i++) {
            const pt = stroke.points[i];
            const rx = pt.x - minX;
            const ry = pt.y - minY;
            pathParts.push(i === 0 ? `M ${rx} ${ry}` : `L ${rx} ${ry}`);
          }
          const inkTool = stroke.opacity < 1 ? "highlighter" : "pen";
          return {
            id: `ink-annotation-${stroke.id}`,
            type: "ink",
            x: minX,
            y: minY,
            width: bboxWidth,
            height: bboxHeight,
            inkPaths: [pathParts.join(" ")],
            inkColors: [stroke.color],
            inkWidths: [stroke.width],
            inkOpacities: [stroke.opacity],
            inkTool
          };
        });
        return {
          ...slide,
          elements: [...slide.elements, ...inkElements]
        };
      })
    );
    history.markDirty();
    annotations.clearAllAnnotations();
    setShowKeepAnnotationsDialog(false);
    setMode(pendingModeAfterAnnotationsRef.current);
  }, [annotations, setMode, setSlides, history]);
  const handleSetMode = (0, import_react.useCallback)(
    (nextMode) => {
      if (mode === "present" && nextMode !== "present" && !mayLeaveSlideShow()) {
        return;
      }
      if (nextMode === "present") {
        presentation.enterPresentMode();
      } else if (mode === "present" && annotations.hasAnyAnnotations) {
        stopAllPersistentAudio();
        if (promptKeepInkAnnotations) {
          pendingModeAfterAnnotationsRef.current = nextMode;
          setShowKeepAnnotationsDialog(true);
        } else {
          annotations.clearAllAnnotations();
          setMode(nextMode);
        }
      } else {
        if (mode === "present") {
          stopAllPersistentAudio();
        }
        setMode(nextMode);
      }
    },
    [presentation, mode, annotations, promptKeepInkAnnotations, setMode]
  );
  const handleDiscardAnnotations = (0, import_react.useCallback)(() => {
    annotations.clearAllAnnotations();
    setShowKeepAnnotationsDialog(false);
    setMode(pendingModeAfterAnnotationsRef.current);
  }, [annotations, setMode]);
  const handleEnterPresenterView = (0, import_react.useCallback)(() => {
    presentation.enterPresenterView();
  }, [presentation]);
  const handleEnterRehearsalMode = (0, import_react.useCallback)(() => {
    presentation.enterRehearsalMode();
  }, [presentation]);
  return {
    showKeepAnnotationsDialog,
    handleSetMode,
    handleKeepAnnotations,
    handleDiscardAnnotations,
    handleEnterPresenterView,
    handleEnterRehearsalMode
  };
}
function useSerialize(input) {
  const {
    slides,
    templateElementsBySlideId,
    activeSlideIndex,
    canvasSize,
    slideSizeEmu,
    guides,
    headerFooter,
    presentationProperties,
    viewProperties,
    tableStyleMap,
    tableStylesDefaultId,
    tableStylesToDelete = [],
    customShows,
    sections,
    coreProperties,
    appProperties,
    customProperties,
    tagCollections,
    slideMasters,
    notesMaster,
    handoutMaster,
    handlerRef,
    inlineEditingElementIdRef,
    inlineEditingTextRef,
    password,
    embedFonts = true,
    purpose
  } = input;
  return (0, import_react.useCallback)(
    async (format) => {
      const handler = handlerRef.current;
      if (!handler) {
        return null;
      }
      const pendingEditId = inlineEditingElementIdRef.current;
      const pendingEditText = inlineEditingTextRef.current;
      const slidesWithGuides = slides.map((slide, idx) => {
        let processedSlide = slide;
        if (pendingEditId) {
          const updatedElements = slide.elements.map((el) => {
            if (el.id !== pendingEditId || !hasTextProperties(el)) {
              return el;
            }
            return {
              ...el,
              text: pendingEditText,
              textSegments: remapTextToSegments(pendingEditText, el.textSegments, el.textStyle)
            };
          });
          if (updatedElements.some((el, i) => el !== slide.elements[i])) {
            processedSlide = { ...slide, elements: updatedElements };
          }
        }
        if (idx !== activeSlideIndex) {
          return processedSlide;
        }
        const pptxGuides = guides.map((g) => ({
          id: g.id,
          orientation: g.axis === "h" ? "horz" : "vert",
          positionEmu: guidePxToEmu(g.position)
        }));
        return {
          ...processedSlide,
          guides: pptxGuides.length > 0 ? pptxGuides : void 0
        };
      });
      const slidesToSave = buildSaveSlides(slidesWithGuides, templateElementsBySlideId);
      const saveOptions = buildDeckSaveOptions({
        // Without this the Slide Size card edited a viewer-only pixel value and
        // the saved `p:sldSz` never moved: passing `slideSize` is the only way a
        // slide-size edit reaches the file.
        slideSize: resolveSlideSizeSelection({ current: slideSizeEmu, canvas: canvasSize }).size,
        headerFooter,
        presentationProperties,
        viewProperties,
        customShows,
        sections,
        coreProperties,
        appProperties,
        customProperties,
        tagCollections,
        slideMasters,
        notesMaster,
        handoutMaster,
        tableStyleMap,
        tableStylesDefaultId,
        tableStylesToDelete,
        embedFonts,
        outputFormat: format
      });
      return saveDeckWithPassword(handler, slidesToSave, saveOptions, { password, purpose });
    },
    [
      slides,
      templateElementsBySlideId,
      canvasSize,
      slideSizeEmu,
      headerFooter,
      presentationProperties,
      viewProperties,
      tableStyleMap,
      tableStylesDefaultId,
      tableStylesToDelete,
      customShows,
      sections,
      coreProperties,
      appProperties,
      customProperties,
      tagCollections,
      slideMasters,
      notesMaster,
      handoutMaster,
      guides,
      activeSlideIndex,
      handlerRef,
      inlineEditingElementIdRef,
      inlineEditingTextRef,
      password,
      embedFonts,
      purpose
    ]
  );
}
function useRecoveryDetection(input) {
  const { filePath, loading, error, slideCount, autosaveAllowed = true } = input;
  const recoveryCheckedRef = (0, import_react.useRef)(false);
  const recordRef = (0, import_react.useRef)(null);
  const [prompt, setPrompt] = (0, import_react.useState)(null);
  const onRestore = input.onRestore;
  const openVersionHistory = input.openVersionHistory;
  (0, import_react.useEffect)(() => {
    if (!shouldProbeAutosaveRecovery({
      alreadyChecked: recoveryCheckedRef.current,
      filePath,
      loading,
      error,
      slideCount,
      autosaveAllowed
    })) {
      return;
    }
    recoveryCheckedRef.current = true;
    void (async () => {
      const offer = await probeAutosaveRecovery(filePath);
      if (!offer) {
        return;
      }
      recordRef.current = offer.record;
      setPrompt(offer.prompt);
      openVersionHistory == null ? void 0 : openVersionHistory();
    })();
  }, [filePath, loading, error, slideCount, autosaveAllowed, openVersionHistory]);
  const restore = (0, import_react.useCallback)(() => {
    const record = recordRef.current;
    setPrompt(null);
    if (record && onRestore) {
      onRestore(acceptAutosaveRecovery(record));
    }
    recordRef.current = null;
  }, [onRestore]);
  const discard = (0, import_react.useCallback)(() => {
    const record = recordRef.current;
    setPrompt(null);
    recordRef.current = null;
    if (record) {
      void discardAutosaveRecovery(record);
    }
  }, []);
  return { prompt, restore, discard };
}
function usePresentationSetup(input) {
  const {
    mode,
    slides,
    templateElementsBySlideId,
    visibleSlideIndexes,
    canvasSize,
    themeColorMap,
    activeSlideIndex,
    containerRef,
    content,
    mediaDataUrls,
    presentationProperties,
    setMode,
    setActiveSlideIndex,
    setSlides,
    history,
    endWithBlackSlide = true,
    promptKeepInkAnnotations = true,
    popupToolbarEnabled = true,
    onToggleSubtitles,
    customShows = [],
    activeCustomShowId = null,
    onSetActiveCustomShowId
  } = input;
  const actionSoundHandlerRef = (0, import_react.useRef)(null);
  const exitModeHandlerRef = (0, import_react.useRef)(null);
  const annotations = usePresentationAnnotations({
    isActive: mode === "present",
    activeSlideIndex,
    popupToolbarEnabled
  });
  const presentation = usePresentationMode({
    mode,
    slides,
    templateElementsBySlideId,
    visibleSlideIndexes,
    canvasSize,
    themeColorMap,
    activeSlideIndex,
    containerRef,
    content,
    onSetMode: (nextMode) => {
      if (mode === "present" && nextMode !== "present" && !mayLeaveSlideShow()) {
        return;
      }
      if (mode === "present" && nextMode !== "present") {
        stopAllPersistentAudio();
        stopAnimationSound();
        const exitHandler = exitModeHandlerRef.current;
        if (exitHandler) {
          exitHandler(nextMode);
          return;
        }
        if (annotations.hasAnyAnnotations && !promptKeepInkAnnotations) {
          annotations.clearAllAnnotations();
        }
      }
      setMode(nextMode);
    },
    onSetActiveSlideIndex: setActiveSlideIndex,
    // `p:sndAc/p:endSnd` ("Stop Previous Sound"): silences whatever transition
    // sound is currently looping. A non-looping transition sound plays through
    // its own ad hoc `Audio` below rather than this singleton (it finishes on
    // its own in a few seconds either way), so only the loop case is stoppable
    // here - matching the common "Loop Until Next Sound... then Stop" pairing.
    onStopActionSound: () => stopAnimationSound(),
    onPlayActionSound: (soundPath, options) => {
      void (async () => {
        if (!soundPath) {
          return;
        }
        const loop = (options == null ? void 0 : options.loop) === true;
        const cachedSound = mediaDataUrls.get(soundPath);
        if (cachedSound) {
          try {
            if (loop) {
              playAnimationSound(cachedSound, true);
            } else {
              const audio = new Audio(cachedSound);
              void audio.play().catch(() => {
              });
            }
          } catch {
          }
          return;
        }
        const sharedHandler = actionSoundHandlerRef.current;
        if (!sharedHandler) {
          return;
        }
        try {
          const dataUrl = await sharedHandler.getImageData(soundPath);
          if (!dataUrl) {
            return;
          }
          mediaDataUrls.set(soundPath, dataUrl);
          if (loop) {
            playAnimationSound(dataUrl, true);
          } else {
            const audio = new Audio(dataUrl);
            void audio.play().catch(() => {
            });
          }
        } catch {
        }
      })();
    },
    // PowerPoint's Ctrl+A "arrow" is the plain pointer, i.e. no active tool.
    onSetPointerTool: (tool) => annotations.setPresentationTool(tool === "arrow" ? "none" : tool),
    onEraseAnnotations: () => annotations.clearAnnotations(),
    onToggleInkMarkup: () => annotations.setInkMarkupVisible(!annotations.inkMarkupVisible),
    onToggleToolbar: () => annotations.setToolbarVisible(!annotations.toolbarVisible),
    onToggleSubtitles,
    onSaveRehearsalTimings: (timings) => {
      setSlides((prev) => applyRehearsalTimings(prev, timings));
      history.markDirty();
    },
    loopContinuously: shouldLoopContinuously(presentationProperties),
    showWithAnimation: presentationProperties.showWithAnimation,
    useTimings: presentationProperties.advanceMode !== "manual",
    endWithBlackSlide,
    customShows,
    activeCustomShowId,
    onSetActiveCustomShowId
  });
  return {
    presentation,
    annotations,
    actionSoundHandlerRef,
    setExitModeHandler: (handler) => {
      exitModeHandlerRef.current = handler;
    }
  };
}
function useEditorOperations(input) {
  const {
    state,
    history,
    zoom,
    mode,
    canEdit,
    slides,
    activeSlide,
    activeSlideIndex,
    selectedElement,
    selectedElementId,
    selectedElementIds,
    canvasSize,
    dialogs,
    presentation,
    userName,
    handlerRef,
    transformCommittedText
  } = input;
  const masterView = (0, import_react.useMemo)(
    () => ({
      target: mode === "master" ? {
        tab: state.masterViewTab,
        masterIndex: state.activeMasterIndex,
        layoutIndex: state.activeLayoutIndex
      } : null,
      slideMasters: state.slideMasters,
      notesMaster: state.notesMaster,
      handoutMaster: state.handoutMaster,
      setSlideMasters: state.setSlideMasters,
      setNotesMaster: state.setNotesMaster,
      setHandoutMaster: state.setHandoutMaster
    }),
    [
      mode,
      state.masterViewTab,
      state.activeMasterIndex,
      state.activeLayoutIndex,
      state.slideMasters,
      state.notesMaster,
      state.handoutMaster,
      state.setSlideMasters,
      state.setNotesMaster,
      state.setHandoutMaster
    ]
  );
  const ops = useElementOperations({
    activeSlide,
    activeSlideIndex,
    selectedElement,
    selectedElementId,
    editTemplateMode: state.editTemplateMode,
    templateElements: state.templateElements,
    masterView,
    history,
    setSlides: state.setSlides,
    setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
    setSelectedElementId: state.setSelectedElementId,
    setSelectedElementIds: state.setSelectedElementIds,
    setInlineEditingElementId: state.setInlineEditingElementId,
    setContextMenuState: state.setContextMenuState,
    inlineEditingElementId: state.inlineEditingElementId,
    inlineEditingText: state.inlineEditingText
  });
  const sectionOps = useSectionOperations({
    sections: state.sections,
    setSections: state.setSections,
    slides,
    setSlides: state.setSlides,
    markDirty: history.markDirty
  });
  const findReplace = useFindReplace({
    slides,
    mode,
    onSetActiveSlideIndex: state.setActiveSlideIndex,
    onSetSelectedElementId: state.setSelectedElementId,
    onUpdateSlides: ops.updateSlides,
    onMarkDirty: history.markDirty
  });
  const comments = useComments({
    slides,
    canEdit,
    userName,
    selectedElementId: state.selectedElementId,
    onUpdateSlides: ops.updateSlides,
    onMarkDirty: history.markDirty
  });
  const canvasHandlers = useCanvasInteractions({
    mode,
    canEdit,
    canvasSize,
    selectedElementId,
    selectedElementIds,
    selectedElementIdSet: state.selectedElementIdSet,
    inlineEditingElementId: state.inlineEditingElementId,
    effectiveSelectedIds: state.effectiveSelectedIds,
    elementLookup: state.elementLookup,
    activeTool: state.activeTool,
    editTemplateMode: state.editTemplateMode,
    editorScale: zoom.editorScale,
    canvasStageRef: zoom.canvasStageRef,
    dragStateRef: state.dragStateRef,
    resizeStateRef: state.resizeStateRef,
    shapeAdjustmentDragStateRef: state.shapeAdjustmentDragStateRef,
    marqueeStateRef: state.marqueeStateRef,
    justInteractedRef: state.justInteractedRef,
    setInlineEditingElementId: state.setInlineEditingElementId,
    setInlineEditingText: state.setInlineEditingText,
    setContextMenuState: state.setContextMenuState,
    setMarqueeSelectionState: state.setMarqueeSelectionState,
    setSnapLines: state.setSnapLines,
    inlineEditingText: state.inlineEditingText,
    ops,
    history,
    presentationHandleAction: presentation.handlePresentationAction,
    setEditingEquationOmml: dialogs.setEditingEquationOmml,
    setIsEquationDialogOpen: dialogs.setIsEquationDialogOpen,
    setPointerCommitNonce: state.setPointerCommitNonce,
    transformCommittedText
  });
  const insertHandlers = useInsertElements({
    activeSlide,
    activeSlideIndex,
    canvasSize,
    newShapeType: state.newShapeType,
    selectedElements: state.selectedElements,
    ops,
    history
  });
  const manipulation = useElementManipulation({
    activeSlide,
    activeSlideIndex,
    selectedElement,
    effectiveSelectedIds: state.effectiveSelectedIds,
    selectedElements: state.selectedElements,
    selectedElementIdSet: state.selectedElementIdSet,
    elementLookup: state.elementLookup,
    editTemplateMode: state.editTemplateMode,
    clipboardPayload: state.clipboardPayload,
    setClipboardPayload: state.setClipboardPayload,
    setSelectedElementIds: state.setSelectedElementIds,
    setIsInspectorPaneOpen: state.setIsInspectorPaneOpen,
    setSidebarPanelMode: state.setSidebarPanelMode,
    ops,
    history,
    onOpenHyperlinkDialog: () => dialogs.setIsHyperlinkDialogOpen(true)
  });
  const slideOps = useSlideManagement({
    slides,
    activeSlideIndex,
    setActiveSlideIndex: state.setActiveSlideIndex,
    ops,
    history,
    handlerRef,
    canvasSize,
    theme: state.theme
  });
  const tableOps = useTableOperations({
    selectedElement,
    elementLookup: state.elementLookup,
    tableEditorState: state.tableEditorState,
    setTableEditorState: state.setTableEditorState,
    ops,
    history,
    transformCommittedText
  });
  const combinedUpdateTextStyle = (0, import_react.useCallback)(
    (updates) => {
      if ((selectedElement == null ? void 0 : selectedElement.type) === "table" && state.tableEditorState) {
        tableOps.handleUpdateCellTextStyle(updates);
        return;
      }
      ops.updateSelectedTextStyle(updates);
    },
    [selectedElement, state.tableEditorState, tableOps, ops]
  );
  const combinedOps = (0, import_react.useMemo)(
    () => ({
      ...ops,
      updateSelectedTextStyle: combinedUpdateTextStyle
    }),
    [ops, combinedUpdateTextStyle]
  );
  const copiedFormatRef = (0, import_react.useRef)(null);
  const prevFormatPainterRef = (0, import_react.useRef)(false);
  const { formatPainterActive, setFormatPainterActive, elementLookup } = state;
  (0, import_react.useEffect)(() => {
    if (formatPainterActive && !prevFormatPainterRef.current && selectedElement) {
      copiedFormatRef.current = copyFormatFromElement(selectedElement);
    } else if (!formatPainterActive) {
      copiedFormatRef.current = null;
    }
    prevFormatPainterRef.current = formatPainterActive;
  }, [formatPainterActive, selectedElement]);
  (0, import_react.useEffect)(() => {
    if (!formatPainterActive) {
      return;
    }
    const onKey = (e) => {
      if (e.key === "Escape") {
        setFormatPainterActive(false);
      }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [formatPainterActive, setFormatPainterActive]);
  const formatPainterCanvasHandlers = (0, import_react.useMemo)(
    () => ({
      ...canvasHandlers,
      handleElementClick: (elementId, e) => {
        if (formatPainterActive && copiedFormatRef.current) {
          e.stopPropagation();
          const element = elementLookup.get(elementId);
          if (element) {
            const updated = applyFormatToElement(element, copiedFormatRef.current);
            const updates = {};
            if (hasShapeProperties(updated)) {
              updates.shapeStyle = updated.shapeStyle;
            }
            if (hasTextProperties(updated)) {
              updates.textStyle = updated.textStyle;
            }
            ops.updateElementById(elementId, updates);
          }
          copiedFormatRef.current = null;
          setFormatPainterActive(false);
          ops.applySelection(elementId);
          return;
        }
        canvasHandlers.handleElementClick(elementId, e);
      },
      handleCanvasMouseDown: (e) => {
        if (formatPainterActive) {
          setFormatPainterActive(false);
          return;
        }
        canvasHandlers.handleCanvasMouseDown(e);
      }
    }),
    [canvasHandlers, ops, formatPainterActive, setFormatPainterActive, elementLookup]
  );
  return {
    ops: combinedOps,
    sectionOps,
    findReplace,
    comments,
    canvasHandlers: formatPainterCanvasHandlers,
    insertHandlers,
    manipulation,
    slideOps,
    tableOps
  };
}
function useIOHandlers(input) {
  const {
    state,
    slides,
    activeSlideIndex,
    canvasSize,
    filePath,
    history,
    ops,
    zoom,
    handlerRef,
    serializeSlides,
    serializeForRecovery,
    setContent,
    onContentChange,
    imageExportScale
  } = input;
  const exportHandlers = useExportHandlers({
    slides,
    templateElementsBySlideId: state.templateElementsBySlideId,
    activeSlide: slides[activeSlideIndex],
    activeSlideIndex,
    filePath,
    canvasStageRef: zoom.canvasStageRef,
    setActiveSlideIndex: state.setActiveSlideIndex,
    serializeSlides,
    headerFooter: state.headerFooter,
    presentationProperties: state.presentationProperties,
    customShows: state.customShows,
    sections: state.sections,
    coreProperties: state.coreProperties,
    appProperties: state.appProperties,
    customProperties: state.customProperties,
    tagCollections: state.tagCollections,
    notesMaster: state.notesMaster,
    handoutMaster: state.handoutMaster,
    theme: state.theme,
    canvasSize,
    slideSizeEmu: state.slideSizeEmu,
    imageExportScale
  });
  const printHandlers = usePrintHandlers({
    slides,
    activeSlideIndex,
    canvasStageRef: zoom.canvasStageRef,
    setActiveSlideIndex: state.setActiveSlideIndex
  });
  const themeHandlers = useThemeHandlers({
    handlerRef,
    // Theme apply re-serialises and re-parses through `setContent`, so these
    // bytes go back into our own loader: plaintext, never the protected save.
    serializeSlides: serializeForRecovery,
    setContent,
    onContentChange,
    setTheme: state.setTheme,
    setSlideMasters: state.setSlideMasters,
    slideMasters: state.slideMasters,
    history,
    setSlides: state.setSlides,
    templateElementsBySlideId: state.templateElementsBySlideId,
    setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
    theme: state.theme,
    bumpHistory: () => state.setPointerCommitNonce((n) => n + 1)
  });
  const propertyHandlers = usePropertyHandlers({
    slides,
    activeSlideIndex,
    canvasSize,
    setContent,
    setPresentationProperties: state.setPresentationProperties,
    setCoreProperties: state.setCoreProperties,
    setAppProperties: state.setAppProperties,
    setCustomProperties: state.setCustomProperties,
    setSlides: state.setSlides,
    setIsDirty: state.setIsDirty,
    ops,
    history
  });
  return { exportHandlers, printHandlers, themeHandlers, propertyHandlers };
}
var EMBEDDED_FONTS_STYLE_ID = "pptx-embedded-fonts";
var GOOGLE_FONTS_LINK_ID = "pptx-google-fonts";
var SYMBOL_FONTS_STYLE_ID = "pptx-symbol-font-fallback";
var SYMBOL_FONT_FAMILIES = [
  "Wingdings",
  "Wingdings 2",
  "Wingdings 3",
  "Symbol",
  "Webdings"
];
var FONT_NAME_UNSAFE_CHARS = /["\\\n\r;}<>]/u;
var FONT_FORMAT_ALLOWED = /* @__PURE__ */ new Set([
  "truetype",
  "opentype",
  "woff",
  "woff2",
  "svg",
  "embedded-opentype"
]);
var FONT_DATA_URL_PATTERN = /^data:font\/[a-z0-9+.-]+(?:;charset=[a-z0-9-]+)?;base64,[A-Za-z0-9+/=]+$/iu;
function isFontDataUrlSafe(url) {
  if (typeof url !== "string" || url.length === 0) {
    return false;
  }
  if (url.startsWith("blob:")) {
    return true;
  }
  return FONT_DATA_URL_PATTERN.test(url);
}
function useFontInjection({ embeddedFonts, slides }) {
  (0, import_react.useEffect)(() => {
    if (!embeddedFonts.length) {
      return;
    }
    const styleEl = document.createElement("style");
    styleEl.id = EMBEDDED_FONTS_STYLE_ID;
    const cssRules = embeddedFonts.flatMap((font) => {
      if (typeof font.name !== "string" || font.name.length === 0 || FONT_NAME_UNSAFE_CHARS.test(font.name)) {
        return [];
      }
      if (!isFontDataUrlSafe(font.dataUrl)) {
        return [];
      }
      const fontFormat = font.format ?? "truetype";
      if (!FONT_FORMAT_ALLOWED.has(fontFormat)) {
        return [];
      }
      const fontWeight = font.bold ? "700" : "400";
      const fontStyleCss = font.italic ? "italic" : "normal";
      return [
        `@font-face {
	font-family: "${font.name}";
	src: url("${font.dataUrl}") format("${fontFormat}");
	font-weight: ${fontWeight};
	font-style: ${fontStyleCss};
	font-display: swap;
}`
      ];
    }).join("\n");
    styleEl.textContent = cssRules;
    document.head.appendChild(styleEl);
    return () => {
      const existing = document.getElementById(EMBEDDED_FONTS_STYLE_ID);
      if (existing) {
        document.head.removeChild(existing);
      }
    };
  }, [embeddedFonts]);
  const referencedFamilies = (0, import_react.useMemo)(() => collectReferencedFontFamilies(slides), [slides]);
  (0, import_react.useEffect)(() => {
    let cancelled = false;
    void resolveGoogleWebfontHref(slides, embeddedFonts).then((href) => {
      if (cancelled) {
        return null;
      }
      if (!href || document.getElementById(GOOGLE_FONTS_LINK_ID)) {
        return null;
      }
      const linkEl = document.createElement("link");
      linkEl.id = GOOGLE_FONTS_LINK_ID;
      linkEl.rel = "stylesheet";
      linkEl.href = href;
      document.head.appendChild(linkEl);
      return href;
    });
    return () => {
      cancelled = true;
      const existing = document.getElementById(GOOGLE_FONTS_LINK_ID);
      if (existing) {
        document.head.removeChild(existing);
      }
    };
  }, [embeddedFonts, slides]);
  (0, import_react.useEffect)(() => {
    const neededSymbolFonts = SYMBOL_FONT_FAMILIES.filter((f) => referencedFamilies.has(f));
    if (neededSymbolFonts.length === 0) {
      return;
    }
    const styleEl = document.createElement("style");
    styleEl.id = SYMBOL_FONTS_STYLE_ID;
    const rules = neededSymbolFonts.flatMap((font) => {
      if (typeof font !== "string" || FONT_NAME_UNSAFE_CHARS.test(font)) {
        return [];
      }
      return [
        `@font-face {
	font-family: "${font}";
	src: local("${font}"), local("${font} Regular");
	font-display: swap;
}`
      ];
    }).join("\n");
    styleEl.textContent = rules;
    document.head.appendChild(styleEl);
    return () => {
      const existing = document.getElementById(SYMBOL_FONTS_STYLE_ID);
      if (existing) {
        document.head.removeChild(existing);
      }
    };
  }, [referencedFamilies]);
}
function useContentLifecycle(input) {
  const {
    content,
    filePath,
    autosaveEnabled = true,
    autosaveIntervalMs,
    embedFonts = true,
    slides,
    state,
    history,
    ops,
    actionSoundHandlerRef,
    setIsEncryptedDialogOpen,
    password,
    onContentApplied,
    allowExternalImages,
    setReadOnlyRecommendation,
    setModifyVerifier,
    setCompatToasts
  } = input;
  const { handlerRef } = useLoadContent({
    content,
    clearSelection: ops.clearSelection,
    history,
    setSlides: state.setSlides,
    setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
    mediaDataUrls: state.mediaDataUrls,
    setCanvasSize: state.setCanvasSize,
    setSlideSizeEmu: state.setSlideSizeEmu,
    setHeaderFooter: state.setHeaderFooter,
    setLayoutOptions: state.setLayoutOptions,
    setSlideMasters: state.setSlideMasters,
    setModernCommentAuthors: state.setModernCommentAuthors,
    setRecentColors: state.setRecentColors,
    setTheme: state.setTheme,
    setTableStyleMap: state.setTableStyleMap,
    setTableStylesDefaultId: state.setTableStylesDefaultId,
    setTableStylesToDelete: state.setTableStylesToDelete,
    setThemeOptions: state.setThemeOptions,
    setCustomShows: state.setCustomShows,
    setActiveCustomShowId: state.setActiveCustomShowId,
    setSections: state.setSections,
    setPresentationProperties: state.setPresentationProperties,
    setViewProperties: state.setViewProperties,
    setNotesMaster: state.setNotesMaster,
    setHandoutMaster: state.setHandoutMaster,
    setNotesCanvasSize: state.setNotesCanvasSize,
    setCustomProperties: state.setCustomProperties,
    setTagCollections: state.setTagCollections,
    setCoreProperties: state.setCoreProperties,
    setAppProperties: state.setAppProperties,
    setEmbeddedFonts: state.setEmbeddedFonts,
    setActiveSlideIndex: state.setActiveSlideIndex,
    setHasMacros: state.setHasMacros,
    setHasDigitalSignatures: state.setHasDigitalSignatures,
    setDigitalSignatureCount: state.setDigitalSignatureCount,
    setGuides: state.setGuides,
    setReadOnlyRecommendation,
    setModifyVerifier,
    setCompatToasts,
    setLoading: state.setLoading,
    setError: state.setError,
    setIsDirty: state.setIsDirty,
    setIsEncrypted: setIsEncryptedDialogOpen,
    onContentApplied,
    allowExternalImages
  });
  (0, import_react.useEffect)(() => {
    actionSoundHandlerRef.current = handlerRef.current;
  }, [handlerRef, actionSoundHandlerRef, state.loading]);
  useFontInjection({ embeddedFonts: state.embeddedFonts, slides });
  const serializeInput = {
    slides,
    templateElementsBySlideId: state.templateElementsBySlideId,
    activeSlideIndex: state.activeSlideIndex,
    canvasSize: state.canvasSize,
    slideSizeEmu: state.slideSizeEmu,
    guides: state.guides,
    headerFooter: state.headerFooter,
    presentationProperties: state.presentationProperties,
    viewProperties: state.viewProperties,
    tableStyleMap: state.tableStyleMap,
    tableStylesDefaultId: state.tableStylesDefaultId,
    tableStylesToDelete: state.tableStylesToDelete,
    customShows: state.customShows,
    sections: state.sections,
    coreProperties: state.coreProperties,
    appProperties: state.appProperties,
    customProperties: state.customProperties,
    tagCollections: state.tagCollections,
    slideMasters: state.slideMasters,
    notesMaster: state.notesMaster,
    handoutMaster: state.handoutMaster,
    handlerRef,
    inlineEditingElementIdRef: state.inlineEditingElementIdRef,
    inlineEditingTextRef: state.inlineEditingTextRef,
    password,
    embedFonts
  };
  const serializeSlides = useSerialize(serializeInput);
  const serializeForRecovery = useSerialize({ ...serializeInput, purpose: "recovery-snapshot" });
  const { autosaveStatus } = useAutosave({
    isDirty: state.isDirty,
    filePath,
    serializeSlides: serializeForRecovery,
    enabled: autosaveEnabled,
    ...autosaveIntervalMs === void 0 ? {} : { intervalMs: autosaveIntervalMs },
    // Everything `serializeForRecovery` reads that changes by REASSIGNMENT,
    // so a poll that finds all of them unchanged can skip re-serializing a
    // deck it has already snapshotted. The two refs are read by `.current`
    // because an inline edit in progress lives there and nowhere else yet.
    getChangeSources: () => [
      slides,
      state.templateElementsBySlideId,
      state.guides,
      state.headerFooter,
      state.presentationProperties,
      state.customShows,
      state.sections,
      state.coreProperties,
      state.appProperties,
      state.customProperties,
      state.tagCollections,
      state.notesMaster,
      state.handoutMaster,
      state.embeddedFonts,
      embedFonts,
      password,
      state.inlineEditingElementIdRef.current,
      state.inlineEditingTextRef.current
    ]
  });
  return { handlerRef, serializeSlides, serializeForRecovery, autosaveStatus };
}
function useKeyboardShortcuts(input) {
  const inputRef = (0, import_react.useRef)(input);
  inputRef.current = input;
  const handleKeyDown = (0, import_react.useCallback)((e) => {
    var _a, _b2, _c, _d, _e, _f, _g;
    const current = inputRef.current;
    const startAction = mapSlideShowStartKey(e, { isPresenting: current.mode === "present" });
    if (startAction !== null) {
      e.preventDefault();
      if (startAction === "fromBeginning") {
        current.onStartShowFromBeginning();
      } else {
        current.onStartShowFromCurrent();
      }
      return;
    }
    const { action, dx, dy } = mapEditorKey(e, {
      canEdit: current.canEdit,
      // The Slide Master view is an editing surface, not a viewing one.
      // Gating on `mode !== 'edit'` alone made `mapEditorKey` return
      // NO_ACTION there, so Delete, the arrow-key nudges and the clipboard
      // keys were all inert over a master shape even though the write path
      // behind them routes to the owning part correctly. Svelte and
      // vanilla, which render the master into their ordinary editable
      // stage, never had the gap.
      isPresenting: current.mode !== "edit" && current.mode !== "master",
      hasSelection: current.hasSelection,
      isEditingText: Boolean(current.inlineEditingElementId || ((_a = current.tableEditorState) == null ? void 0 : _a.isEditing)),
      isDrawing: current.activeTool !== "select",
      isTextInputTarget: isEditorTextInputTarget(e.target)
    });
    if (action === null) {
      return;
    }
    e.preventDefault();
    switch (action) {
      case "escape":
        current.onEscape();
        break;
      case "delete":
        current.onDelete();
        break;
      case "undo":
        current.onUndo();
        break;
      case "redo":
        current.onRedo();
        break;
      case "copy":
        current.onCopy();
        break;
      case "cut":
        current.onCut();
        break;
      case "paste":
        current.onPaste();
        break;
      case "duplicate":
        current.onDuplicate();
        break;
      case "selectAll":
        current.onSelectAll();
        break;
      case "group":
        (_b2 = current.onGroup) == null ? void 0 : _b2.call(current);
        break;
      case "ungroup":
        (_c = current.onUngroup) == null ? void 0 : _c.call(current);
        break;
      case "toggleShortcuts":
        (_d = current.onToggleShortcuts) == null ? void 0 : _d.call(current);
        break;
      case "find":
        (_e = current.onFind) == null ? void 0 : _e.call(current);
        break;
      case "nudge":
        current.onNudge(dx ?? 0, dy ?? 0);
        break;
      case "prevSlide":
        (_f = current.onPrevSlide) == null ? void 0 : _f.call(current);
        break;
      case "nextSlide":
        (_g = current.onNextSlide) == null ? void 0 : _g.call(current);
        break;
    }
  }, []);
  (0, import_react.useEffect)(() => {
    window.addEventListener("keydown", handleKeyDown);
    return () => {
      window.removeEventListener("keydown", handleKeyDown);
    };
  }, [handleKeyDown]);
}
function useKeyboardShortcutWiring(input) {
  const {
    state,
    mode,
    canEdit,
    slides,
    activeSlide,
    ops,
    manipulation,
    history,
    onEnterPresentModeFromBeginning,
    onSetMode
  } = input;
  useKeyboardShortcuts({
    containerRef: state.containerRef,
    mode,
    canEdit,
    inlineEditingElementId: state.inlineEditingElementId,
    tableEditorState: state.tableEditorState,
    activeTool: state.activeTool,
    hasSelection: state.effectiveSelectedIds.length > 0,
    effectiveSelectedIds: state.effectiveSelectedIds,
    onDelete: manipulation.handleDelete,
    onCopy: manipulation.handleCopy,
    onCut: manipulation.handleCut,
    onPaste: manipulation.handlePaste,
    onDuplicate: manipulation.handleDuplicate,
    onUndo: history.handleUndo,
    onRedo: history.handleRedo,
    onSelectAll: () => {
      if (!activeSlide) {
        return;
      }
      const allIds = activeSlide.elements.map((el) => el.id);
      if (allIds.length > 0) {
        ops.applySelection(allIds[0], allIds);
      }
    },
    onGroup: manipulation.handleGroupElements,
    onUngroup: manipulation.handleUngroupElement,
    onToggleShortcuts: () => state.setIsShortcutHelpOpen((prev) => !prev),
    onEscape: () => {
      if (state.isShortcutHelpOpen) {
        state.setIsShortcutHelpOpen(false);
      } else if (state.inlineEditingElementId) {
        state.setInlineEditingElementId(null);
        state.setInlineEditingText("");
      } else if (state.contextMenuState) {
        state.setContextMenuState(null);
      } else if (state.tableEditorState) {
        state.setTableEditorState(null);
      } else {
        ops.clearSelection();
      }
    },
    onNudge: (dx, dy) => {
      const ids = state.effectiveSelectedIds;
      if (!ids.length) {
        return;
      }
      for (const id of ids) {
        const el = state.elementLookup.get(id);
        if (el) {
          ops.updateElementById(id, {
            x: el.x + dx,
            y: el.y + dy
          });
        }
      }
      history.markDirty();
    },
    onPrevSlide: () => {
      if (slides.length === 0) {
        return;
      }
      state.setActiveSlideIndex((prev) => Math.max(0, prev - 1));
    },
    onNextSlide: () => {
      if (slides.length === 0) {
        return;
      }
      state.setActiveSlideIndex((prev) => Math.min(slides.length - 1, prev + 1));
    },
    onStartShowFromBeginning: onEnterPresentModeFromBeginning,
    onStartShowFromCurrent: () => onSetMode("present")
  });
}
function useViewerIntegration(input) {
  const {
    state,
    zoom,
    history,
    presentation,
    annotations,
    actionSoundHandlerRef,
    editorOps,
    dialogs,
    gridSpacingPx,
    content,
    filePath,
    autosaveEnabled,
    autosaveAllowed = true,
    autosaveIntervalMs,
    allowExternalImages,
    setReadOnlyRecommendation,
    setModifyVerifier,
    setCompatToasts,
    canEdit,
    promptKeepInkAnnotations,
    imageExportScale,
    mode,
    slides,
    activeSlide,
    activeSlideIndex,
    canvasSize,
    loading,
    error,
    ref,
    setContent,
    onContentChange,
    onDirtyChange,
    onActiveSlideChange,
    onModeChange,
    onZoomChange,
    onSelectionChange,
    onSlideCountChange
  } = input;
  usePointerHandlers({
    editorScale: zoom.editorScale,
    canvasStageRef: zoom.canvasStageRef,
    canvasSize,
    activeSlide,
    activeSlideIndex,
    gridSpacingPx,
    dragStateRef: state.dragStateRef,
    resizeStateRef: state.resizeStateRef,
    shapeAdjustmentDragStateRef: state.shapeAdjustmentDragStateRef,
    marqueeStateRef: state.marqueeStateRef,
    justInteractedRef: state.justInteractedRef,
    editTemplateMode: state.editTemplateMode,
    snapToGrid: state.snapToGrid,
    snapToShape: state.snapToShape,
    guides: state.guides,
    templateElements: state.templateElements,
    elementLookup: state.elementLookup,
    setMarqueeSelectionState: state.setMarqueeSelectionState,
    setSnapLines: state.setSnapLines,
    setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
    setPointerCommitNonce: state.setPointerCommitNonce,
    effectiveSelectedIds: state.effectiveSelectedIds,
    applySelection: editorOps.ops.applySelection,
    clearSelection: editorOps.ops.clearSelection,
    updateSlides: editorOps.ops.updateSlides,
    updateElementById: editorOps.ops.updateElementById,
    markDirty: history.markDirty,
    livePatcher: state.livePatcher
  });
  const [isEncryptedDialogOpen, setIsEncryptedDialogOpen] = (0, import_react.useState)(false);
  const [loadVersion, setLoadVersion] = (0, import_react.useState)(0);
  const { handlerRef, serializeSlides, serializeForRecovery, autosaveStatus } = useContentLifecycle(
    {
      content,
      filePath,
      autosaveEnabled,
      autosaveIntervalMs,
      // The File > Fonts toggle, finally reaching a save call.
      embedFonts: dialogs.embedFontsEnabled,
      slides,
      state,
      history,
      ops: editorOps.ops,
      actionSoundHandlerRef,
      setIsEncryptedDialogOpen,
      password: dialogs.presentationPassword ?? void 0,
      onContentApplied: () => setLoadVersion((v) => v + 1),
      allowExternalImages,
      setReadOnlyRecommendation,
      setModifyVerifier,
      setCompatToasts
    }
  );
  const { exportHandlers, printHandlers, themeHandlers, propertyHandlers } = useIOHandlers({
    state,
    slides,
    activeSlideIndex,
    canvasSize,
    filePath,
    history,
    ops: editorOps.ops,
    zoom,
    handlerRef,
    serializeSlides,
    serializeForRecovery,
    setContent,
    onContentChange,
    imageExportScale
  });
  const {
    showKeepAnnotationsDialog,
    handleSetMode,
    handleKeepAnnotations,
    handleDiscardAnnotations,
    handleEnterPresenterView,
    handleEnterRehearsalMode
  } = useAnnotationHandlers({
    mode,
    presentation,
    annotations,
    history,
    setMode: state.setMode,
    setSlides: state.setSlides,
    promptKeepInkAnnotations
  });
  const recovery = useRecoveryDetection({
    filePath,
    loading,
    error,
    slideCount: slides.length,
    autosaveAllowed,
    onRestore: setContent
  });
  (0, import_react.useImperativeHandle)(
    ref,
    () => ({
      async getContent() {
        const data = await serializeSlides();
        if (data && onContentChange) {
          onContentChange(data);
        }
        return data ?? new Uint8Array(0);
      },
      goTo(index) {
        if (index >= 0 && index < slides.length) {
          state.setActiveSlideIndex(index);
        }
      },
      goPrev() {
        const next = activeSlideIndex - 1;
        if (next >= 0) {
          state.setActiveSlideIndex(next);
        }
      },
      goNext() {
        const next = activeSlideIndex + 1;
        if (next < slides.length) {
          state.setActiveSlideIndex(next);
        }
      },
      undo() {
        history.handleUndo();
      },
      redo() {
        history.handleRedo();
      },
      canUndo() {
        return history.canUndo;
      },
      canRedo() {
        return history.canRedo;
      },
      getZoom() {
        return zoom.scale;
      },
      setZoom(level) {
        zoom.setScale(clampZoomScale(level));
      },
      zoomIn() {
        zoom.handleZoomIn();
      },
      zoomOut() {
        zoom.handleZoomOut();
      },
      zoomReset() {
        zoom.handleResetZoom();
      },
      getMode() {
        return mode;
      },
      setMode(newMode) {
        state.setMode(newMode);
      },
      getActiveSlideIndex() {
        return activeSlideIndex;
      },
      getSlideCount() {
        return slides.length;
      },
      isDirty() {
        return state.isDirty;
      },
      getSelectedElementIds() {
        return state.selectedElementIds;
      },
      selectElements(ids) {
        state.setSelectedElementIds(ids);
        state.setSelectedElementId(ids[0] ?? null);
      },
      clearSelection() {
        state.setSelectedElementIds([]);
        state.setSelectedElementId(null);
      },
      // -- Active slide (alias) --
      setActiveSlideIndex(index) {
        if (index >= 0 && index < slides.length) {
          state.setActiveSlideIndex(index);
        }
      },
      // -- Slide access --
      getSlides() {
        return slides;
      },
      getSlide(index) {
        return slides[index];
      },
      getActiveSlide() {
        return slides[activeSlideIndex];
      },
      // -- Slide manipulation --
      addSlide(_afterIndex) {
        editorOps.slideOps.handleAddSlide();
      },
      deleteSlides(indexes) {
        editorOps.slideOps.handleDeleteSlides(indexes);
      },
      duplicateSlides(indexes) {
        editorOps.slideOps.handleDuplicateSlides(indexes);
      },
      moveSlide(fromIndex, toIndex) {
        editorOps.slideOps.handleMoveSlide(fromIndex, toIndex);
      },
      toggleHideSlides(indexes) {
        editorOps.slideOps.handleToggleHideSlides(indexes);
      },
      // -- Element access --
      getElements(slideIndex) {
        const idx = slideIndex ?? activeSlideIndex;
        const s = slides[idx];
        return (s == null ? void 0 : s.elements) ?? [];
      },
      getElementById(elementId, slideIndex) {
        const idx = slideIndex ?? activeSlideIndex;
        const s = slides[idx];
        return s == null ? void 0 : s.elements.find((e) => e.id === elementId);
      },
      // -- Element manipulation --
      updateElement(elementId, updates) {
        editorOps.ops.updateElementById(elementId, updates);
      },
      deleteElements(elementIds) {
        state.setSelectedElementIds(elementIds);
        state.setSelectedElementId(elementIds[0] ?? null);
        editorOps.manipulation.handleDelete();
      },
      duplicateElement(elementId) {
        state.setSelectedElementIds([elementId]);
        state.setSelectedElementId(elementId);
        editorOps.manipulation.handleDuplicate();
        return state.selectedElementIds[0];
      }
    }),
    [
      serializeSlides,
      onContentChange,
      slides,
      activeSlideIndex,
      state,
      history,
      zoom,
      mode,
      editorOps
    ]
  );
  (0, import_react.useEffect)(() => {
    if (onDirtyChange) {
      onDirtyChange(state.isDirty);
    }
  }, [state.isDirty, onDirtyChange]);
  (0, import_react.useEffect)(() => {
    if (onActiveSlideChange) {
      onActiveSlideChange(activeSlideIndex);
    }
  }, [activeSlideIndex, onActiveSlideChange]);
  (0, import_react.useEffect)(() => {
    state.activeSlideIndexRef.current = activeSlideIndex;
  }, [activeSlideIndex, state.activeSlideIndexRef]);
  (0, import_react.useEffect)(() => {
    if (onModeChange) {
      onModeChange(mode);
    }
  }, [mode, onModeChange]);
  (0, import_react.useEffect)(() => {
    if (onZoomChange) {
      onZoomChange(zoom.scale);
    }
  }, [zoom.scale, onZoomChange]);
  (0, import_react.useEffect)(() => {
    if (onSelectionChange) {
      onSelectionChange(state.selectedElementIds);
    }
  }, [state.selectedElementIds, onSelectionChange]);
  (0, import_react.useEffect)(() => {
    if (onSlideCountChange) {
      onSlideCountChange(slides.length);
    }
  }, [slides.length, onSlideCountChange]);
  useKeyboardShortcutWiring({
    state,
    mode,
    canEdit,
    slides,
    activeSlide,
    ops: editorOps.ops,
    manipulation: editorOps.manipulation,
    history,
    onEnterPresentModeFromBeginning: presentation.enterPresentModeFromBeginning,
    onSetMode: handleSetMode
  });
  return {
    exportHandlers,
    printHandlers,
    themeHandlers,
    propertyHandlers,
    showKeepAnnotationsDialog,
    handleSetMode,
    handleKeepAnnotations,
    handleDiscardAnnotations,
    handleEnterPresenterView,
    handleEnterRehearsalMode,
    autosaveStatus,
    recovery,
    isEncryptedDialogOpen,
    setIsEncryptedDialogOpen,
    handlerRef,
    loadVersion
  };
}
function getSnapshot() {
  if (typeof window === "undefined") {
    return false;
  }
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
}
function getServerSnapshot() {
  return false;
}
function subscribe(callback) {
  if (typeof window === "undefined") {
    return () => {
    };
  }
  const mql = window.matchMedia("(prefers-reduced-motion: reduce)");
  mql.addEventListener("change", callback);
  return () => mql.removeEventListener("change", callback);
}
function useReducedMotion() {
  const osReducedMotion = (0, import_react.useSyncExternalStore)(subscribe, getSnapshot, getServerSnapshot);
  const [manualReducedMotion, setManualReducedMotion] = (0, import_react.useState)(false);
  const reducedMotion = osReducedMotion || manualReducedMotion;
  const toggleReducedMotion = (0, import_react.useCallback)(() => {
    setManualReducedMotion((prev) => !prev);
  }, []);
  (0, import_react.useEffect)(() => {
    const root = document.documentElement;
    if (reducedMotion) {
      root.classList.add("pptx-reduced-motion");
    } else {
      root.classList.remove("pptx-reduced-motion");
    }
    return () => {
      root.classList.remove("pptx-reduced-motion");
    };
  }, [reducedMotion]);
  return {
    osReducedMotion,
    manualReducedMotion,
    reducedMotion,
    toggleReducedMotion,
    setManualReducedMotion
  };
}
function useViewerOptions() {
  const storeRef = (0, import_react.useRef)(null);
  storeRef.current ?? (storeRef.current = createViewerOptionsStore());
  const store = storeRef.current;
  const options = (0, import_react.useSyncExternalStore)(
    (onStoreChange) => store.subscribe(onStoreChange),
    () => store.getOptions(),
    () => store.getOptions()
  );
  return { optionsStore: store, options };
}
var KNOWN_TAB_IDS = new Set(TOOLBAR_TABS.map((tab) => tab.id));
function computeToolbarVisibility(hiddenActions, hiddenTabIds = []) {
  const isHidden = (id) => isActionHidden(id, hiddenActions);
  const userHidden = new Set(hiddenTabIds);
  userHidden.delete("file");
  const isTabVisible = (id) => !KNOWN_TAB_IDS.has(id) || !isHidden(id) && !userHidden.has(id);
  return { isHidden, isTabVisible };
}
function useToolbarVisibility(hiddenActions) {
  const hiddenTabIds = useViewerOptionsContext().ribbon.hiddenTabIds;
  return (0, import_react.useMemo)(
    () => computeToolbarVisibility(hiddenActions, hiddenTabIds),
    [hiddenActions, hiddenTabIds]
  );
}
var WRITE_BACK_DEBOUNCE_DEFAULT_MS = 5e3;
function useYjsDocumentSync({
  doc,
  slides,
  templateElementsBySlideId,
  setSlides,
  isConnected,
  isSynced = true,
  config,
  getSourceBytes,
  getSaveOptions,
  loadVersion = 0,
  loadOrigin = "user"
}) {
  const isApplyingRemoteRef = (0, import_react.useRef)(false);
  const lastSyncedRef = (0, import_react.useRef)("");
  const hasInitializedRef = (0, import_react.useRef)(false);
  const writeBackTimerRef = (0, import_react.useRef)(null);
  const factoriesRef = (0, import_react.useRef)(null);
  const lastLoadVersionRef = (0, import_react.useRef)(loadVersion);
  const getFactories = (0, import_react.useCallback)(async () => {
    if (factoriesRef.current) {
      return factoriesRef.current;
    }
    const Y = await import("./yjs-4ED7E5CZ-X3LPTJYE.js");
    const factories = {
      createMap: () => new Y.Map(),
      createArray: () => new Y.Array(),
      createText: () => new Y.Text()
    };
    factoriesRef.current = factories;
    return factories;
  }, []);
  const scheduleWriteBack = (0, import_react.useCallback)(() => {
    if (!(config == null ? void 0 : config.onWriteBack) || config.role !== "owner" || !doc) {
      return;
    }
    if (writeBackTimerRef.current !== null) {
      clearTimeout(writeBackTimerRef.current);
    }
    const debounceMs = config.writeBackDebounceMs ?? WRITE_BACK_DEBOUNCE_DEFAULT_MS;
    writeBackTimerRef.current = setTimeout(async () => {
      writeBackTimerRef.current = null;
      if (!doc || !config.onWriteBack) {
        return;
      }
      const sourceBytes = getSourceBytes == null ? void 0 : getSourceBytes();
      if (!sourceBytes) {
        return;
      }
      try {
        const handler = new PptxHandler();
        await handler.load(sourceBytes.buffer);
        const currentSlides = readSlidesFromYDoc(
          doc
        );
        const slidesToSave = buildSaveSlides(currentSlides, templateElementsBySlideId);
        const bytes = await handler.save(slidesToSave, getSaveOptions == null ? void 0 : getSaveOptions());
        config.onWriteBack(bytes);
      } catch {
      }
    }, debounceMs);
  }, [doc, config, getSourceBytes, getSaveOptions, templateElementsBySlideId]);
  (0, import_react.useEffect)(() => {
    if (loadVersion === lastLoadVersionRef.current) {
      return;
    }
    lastLoadVersionRef.current = loadVersion;
    if (!doc || !isConnected) {
      return;
    }
    const docSlides = readSlidesFromYDoc(
      doc
    );
    if (!shouldRoomSlidesReplaceLoad(loadOrigin, docSlides.length)) {
      return;
    }
    lastSyncedRef.current = JSON.stringify(docSlides);
    isApplyingRemoteRef.current = true;
    setSlides(docSlides);
    isApplyingRemoteRef.current = false;
  }, [loadVersion, loadOrigin, doc, isConnected, setSlides]);
  (0, import_react.useEffect)(() => {
    if (!isConnected || !isSynced || !doc || isApplyingRemoteRef.current || slides.length === 0) {
      return;
    }
    const serialized = JSON.stringify(slides);
    if (serialized === lastSyncedRef.current) {
      return;
    }
    lastSyncedRef.current = serialized;
    void (async () => {
      const factories = await getFactories();
      reconcileSlidesInYDoc(
        slides,
        doc,
        factories,
        LOCAL_SYNC_ORIGIN
      );
      scheduleWriteBack();
    })();
  }, [doc, slides, isConnected, isSynced, getFactories, scheduleWriteBack]);
  (0, import_react.useEffect)(() => {
    hasInitializedRef.current = false;
    lastSyncedRef.current = "";
  }, [doc]);
  (0, import_react.useEffect)(() => {
    if (!isConnected || !doc) {
      return;
    }
    const handleChange = (_events, transaction) => {
      if ((transaction == null ? void 0 : transaction.origin) === LOCAL_SYNC_ORIGIN) {
        return;
      }
      const remoteSlides = readSlidesFromYDoc(
        doc
      );
      if (remoteSlides.length === 0) {
        return;
      }
      const serialized = JSON.stringify(remoteSlides);
      if (serialized === lastSyncedRef.current) {
        return;
      }
      lastSyncedRef.current = serialized;
      isApplyingRemoteRef.current = true;
      setSlides(remoteSlides);
      isApplyingRemoteRef.current = false;
      scheduleWriteBack();
    };
    const unobserve = observeYDocSlides(
      doc,
      handleChange
    );
    if (!hasInitializedRef.current) {
      hasInitializedRef.current = true;
      const arr = doc.getArray(
        "pptx:slides"
      );
      if (arr.length > 0) {
        handleChange();
      }
    }
    return () => {
      unobserve();
      if (writeBackTimerRef.current !== null) {
        clearTimeout(writeBackTimerRef.current);
        writeBackTimerRef.current = null;
      }
    };
  }, [doc, isConnected, setSlides, scheduleWriteBack]);
}
function useBroadcastFollower({
  collab,
  activeSlideIndex,
  setActiveSlideIndex,
  slideCount,
  paused = false
}) {
  const lastBroadcasterSlide = (0, import_react.useRef)(-1);
  (0, import_react.useEffect)(() => {
    if (!collab || paused) {
      return;
    }
    const broadcaster = collab.remoteUsers.find((u) => u.role === "owner");
    if (!broadcaster) {
      return;
    }
    if (!shouldAutoFollowBroadcaster({
      localRole: collab.config.role,
      broadcasterRole: broadcaster.role
    })) {
      return;
    }
    const targetSlide = broadcaster.activeSlideIndex;
    if (targetSlide < 0 || targetSlide >= slideCount) {
      return;
    }
    if (targetSlide === lastBroadcasterSlide.current) {
      return;
    }
    lastBroadcasterSlide.current = targetSlide;
    if (targetSlide !== activeSlideIndex) {
      setActiveSlideIndex(targetSlide);
    }
  }, [collab, activeSlideIndex, setActiveSlideIndex, slideCount, paused]);
}
function useFollowMode({
  collab,
  activeSlideIndex,
  setActiveSlideIndex,
  slideCount
}) {
  const [followedClientId, setFollowedClientId] = (0, import_react.useState)(null);
  const lastAppliedRef = (0, import_react.useRef)(-1);
  const followUser = (0, import_react.useCallback)((clientId) => {
    lastAppliedRef.current = -1;
    setFollowedClientId(clientId);
  }, []);
  const followedPeer = followedClientId === null ? null : (collab == null ? void 0 : collab.remoteUsers.find((u) => u.clientId === followedClientId)) ?? null;
  (0, import_react.useEffect)(() => {
    if (followedClientId !== null && collab && followedPeer === null) {
      setFollowedClientId(null);
    }
  }, [collab, followedClientId, followedPeer]);
  const targetSlide = (followedPeer == null ? void 0 : followedPeer.activeSlideIndex) ?? null;
  (0, import_react.useEffect)(() => {
    if (targetSlide === null || targetSlide < 0 || targetSlide >= slideCount) {
      return;
    }
    if (targetSlide === activeSlideIndex) {
      lastAppliedRef.current = targetSlide;
      return;
    }
    lastAppliedRef.current = targetSlide;
    setActiveSlideIndex(targetSlide);
  }, [targetSlide, slideCount, setActiveSlideIndex]);
  (0, import_react.useEffect)(() => {
    if (followedClientId === null) {
      return;
    }
    if (lastAppliedRef.current !== -1 && activeSlideIndex !== lastAppliedRef.current) {
      setFollowedClientId(null);
    }
  }, [activeSlideIndex, followedClientId]);
  return { followedClientId, followUser };
}
function useCollaborationLivePatch({
  patcher,
  doc,
  isConnected,
  isSynced = true
}) {
  (0, import_react.useEffect)(() => {
    if (!doc || !isConnected || !isSynced) {
      patcher.configure(null, null);
      return;
    }
    let cancelled = false;
    void (async () => {
      const Y = await import("./yjs-4ED7E5CZ-X3LPTJYE.js");
      if (cancelled) {
        return;
      }
      const factories = {
        createMap: () => new Y.Map(),
        createArray: () => new Y.Array(),
        createText: () => new Y.Text()
      };
      patcher.configure(doc, factories);
    })();
    return () => {
      cancelled = true;
      patcher.configure(null, null);
    };
  }, [patcher, doc, isConnected, isSynced]);
}
function useLayoutSwitching(input) {
  var _a;
  const { handler, slides, activeSlideIndex, ops, history, onTemplateElementsChanged } = input;
  const [availableLayouts, setAvailableLayouts] = (0, import_react.useState)([]);
  const [isLoading, setIsLoading] = (0, import_react.useState)(false);
  const slidesRef = (0, import_react.useRef)(slides);
  slidesRef.current = slides;
  const currentLayoutPath = (_a = slides[activeSlideIndex]) == null ? void 0 : _a.layoutPath;
  const loadAvailableLayouts = (0, import_react.useCallback)(async () => {
    if (!handler) {
      return;
    }
    setIsLoading(true);
    try {
      const layouts = await handler.getAvailableLayoutsForSlide(
        activeSlideIndex,
        slidesRef.current
      );
      setAvailableLayouts(layouts);
    } finally {
      setIsLoading(false);
    }
  }, [handler, activeSlideIndex]);
  const applyLayout = (0, import_react.useCallback)(
    async (layoutPath) => {
      if (!handler) {
        return;
      }
      setIsLoading(true);
      try {
        const updated = await handler.applyLayoutToSlide(
          activeSlideIndex,
          layoutPath,
          slidesRef.current
        );
        ops.updateSlides((prev) => {
          const next = [...prev];
          next[activeSlideIndex] = updated;
          return next;
        });
        if (onTemplateElementsChanged) {
          onTemplateElementsChanged(
            updated.id,
            await handler.getTemplateElementsForSlide(updated.id)
          );
        }
        history.markDirty();
      } finally {
        setIsLoading(false);
      }
    },
    // `activeSlideIndex` IS read above (inside the `try`), but the analyzer
    // doesn't see through an `await` call wrapped in try/finally and flags it
    // as unused; verified as a false positive with a minimal repro.
    // oxlint-disable-next-line react/memo-dependencies -- see comment above
    [handler, activeSlideIndex, ops, history, onTemplateElementsChanged]
  );
  const loadLayoutPreviews = (0, import_react.useCallback)(
    async () => handler ? handler.getLayoutPreviews() : [],
    [handler]
  );
  return {
    availableLayouts,
    isLoading,
    loadAvailableLayouts,
    applyLayout,
    loadLayoutPreviews,
    currentLayoutPath
  };
}
function subscribeTouchCapability(callback) {
  if (typeof window === "undefined") {
    return () => {
    };
  }
  const handler = () => callback();
  window.addEventListener("pointerdown", handler, { once: true });
  return () => window.removeEventListener("pointerdown", handler);
}
function useIsMobile() {
  const isTouchDevice = (0, import_react.useSyncExternalStore)(
    subscribeTouchCapability,
    detectTouchDevice,
    () => false
    // server snapshot
  );
  const [viewportWidth, setViewportWidth] = (0, import_react.useState)(
    () => typeof window === "undefined" ? 1024 : window.innerWidth
  );
  const [viewportHeight, setViewportHeight] = (0, import_react.useState)(
    () => typeof window === "undefined" ? 768 : window.innerHeight
  );
  const [orientation, setOrientation] = (0, import_react.useState)(detectOrientation);
  const [isVirtualKeyboardOpen, setIsVirtualKeyboardOpen] = (0, import_react.useState)(false);
  const [initialViewportHeight] = (0, import_react.useState)(
    () => typeof window !== "undefined" ? window.innerHeight : 800
  );
  (0, import_react.useEffect)(() => {
    if (typeof window === "undefined") {
      return;
    }
    const handleViewportResize = () => {
      setViewportWidth(window.innerWidth);
      setViewportHeight(window.innerHeight);
    };
    window.addEventListener("resize", handleViewportResize);
    return () => window.removeEventListener("resize", handleViewportResize);
  }, []);
  (0, import_react.useEffect)(() => {
    if (typeof window === "undefined") {
      return;
    }
    const handleOrientationChange = () => {
      setOrientation(detectOrientation());
    };
    if (screen.orientation) {
      screen.orientation.addEventListener("change", handleOrientationChange);
    }
    window.addEventListener("resize", handleOrientationChange);
    return () => {
      if (screen.orientation) {
        screen.orientation.removeEventListener("change", handleOrientationChange);
      }
      window.removeEventListener("resize", handleOrientationChange);
    };
  }, []);
  (0, import_react.useEffect)(() => {
    if (!isTouchDevice || typeof window === "undefined") {
      return;
    }
    const handleResize = () => {
      var _a;
      const currentHeight = ((_a = window.visualViewport) == null ? void 0 : _a.height) ?? window.innerHeight;
      const shrinkRatio = currentHeight / initialViewportHeight;
      setIsVirtualKeyboardOpen(shrinkRatio < 0.7);
    };
    const vv = window.visualViewport;
    if (vv) {
      vv.addEventListener("resize", handleResize);
      return () => vv.removeEventListener("resize", handleResize);
    }
    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize);
  }, [isTouchDevice, initialViewportHeight]);
  const { isMobile, isTablet, isDesktop } = deriveViewportBreakpoints(
    viewportWidth,
    viewportHeight,
    isTouchDevice
  );
  return {
    isMobile,
    isTablet,
    isDesktop,
    isTouchDevice,
    orientation,
    isVirtualKeyboardOpen
  };
}
function useTouchGestures(input) {
  const { targetRef, currentScale, callbacks, enabled = true } = input;
  const callbacksRef = (0, import_react.useRef)(callbacks);
  callbacksRef.current = callbacks;
  const scaleRef = (0, import_react.useRef)(currentScale);
  scaleRef.current = currentScale;
  const [targetVersion, setTargetVersion] = (0, import_react.useState)(0);
  const lastTargetRef = (0, import_react.useRef)(null);
  if (targetRef.current !== lastTargetRef.current) {
    lastTargetRef.current = targetRef.current;
    queueMicrotask(() => setTargetVersion((v) => v + 1));
  }
  (0, import_react.useEffect)(() => {
    const el = targetRef.current;
    if (!el || !enabled) {
      return;
    }
    const recognizer = createTouchGestureRecognizer({
      getScale: () => scaleRef.current,
      minScale: MIN_ZOOM_SCALE,
      maxScale: MAX_ZOOM_SCALE,
      callbacks: {
        onPinchZoom: (newScale) => {
          var _a, _b2;
          return (_b2 = (_a = callbacksRef.current).onPinchZoom) == null ? void 0 : _b2.call(_a, newScale);
        },
        onSwipe: (direction) => {
          var _a, _b2;
          return (_b2 = (_a = callbacksRef.current).onSwipe) == null ? void 0 : _b2.call(_a, direction);
        },
        onLongPress: (x, y) => {
          var _a, _b2;
          return (_b2 = (_a = callbacksRef.current).onLongPress) == null ? void 0 : _b2.call(_a, x, y);
        }
      }
    });
    const handleTouchStart = (e) => recognizer.onTouchStart(e);
    const handleTouchMove = (e) => recognizer.onTouchMove(e);
    const handleTouchEnd = (e) => recognizer.onTouchEnd(e);
    const handleTouchCancel = () => recognizer.onTouchCancel();
    el.addEventListener("touchstart", handleTouchStart, { passive: false });
    el.addEventListener("touchmove", handleTouchMove, { passive: false });
    el.addEventListener("touchend", handleTouchEnd, { passive: true });
    el.addEventListener("touchcancel", handleTouchCancel, { passive: true });
    return () => {
      el.removeEventListener("touchstart", handleTouchStart);
      el.removeEventListener("touchmove", handleTouchMove);
      el.removeEventListener("touchend", handleTouchEnd);
      el.removeEventListener("touchcancel", handleTouchCancel);
      recognizer.cancel();
    };
  }, [targetRef, enabled, targetVersion]);
}
function isEditable(node) {
  if (!(node instanceof HTMLElement)) {
    return false;
  }
  const tag = node.tagName;
  return tag === "INPUT" || tag === "TEXTAREA" || node.isContentEditable;
}
function scrollFocusedIntoView(keyboardInset) {
  if (keyboardInset <= 0 || typeof document === "undefined") {
    return;
  }
  const active = document.activeElement;
  if (!isEditable(active)) {
    return;
  }
  const rect = active.getBoundingClientRect();
  const delta = computeScrollDelta(
    { top: rect.top, bottom: rect.bottom },
    window.innerHeight,
    keyboardInset
  );
  if (delta !== 0) {
    window.scrollBy({ top: delta, behavior: "smooth" });
  }
}
function useKeyboardInsets(enabled = true) {
  const [keyboardInset, setKeyboardInset] = (0, import_react.useState)(0);
  (0, import_react.useEffect)(() => {
    if (!enabled || typeof window === "undefined") {
      return;
    }
    const vv = window.visualViewport;
    if (!vv) {
      return;
    }
    const update = () => {
      const metrics = readViewportMetrics(window);
      const inset = metrics ? computeKeyboardInset(metrics) : 0;
      setKeyboardInset(inset);
      if (inset > 0) {
        window.requestAnimationFrame(() => scrollFocusedIntoView(inset));
      }
    };
    update();
    vv.addEventListener("resize", update);
    vv.addEventListener("scroll", update);
    const onFocusIn = () => {
      window.requestAnimationFrame(() => {
        const metrics = readViewportMetrics(window);
        const inset = metrics ? computeKeyboardInset(metrics) : 0;
        if (inset > 0) {
          scrollFocusedIntoView(inset);
        }
      });
    };
    document.addEventListener("focusin", onFocusIn);
    return () => {
      vv.removeEventListener("resize", update);
      vv.removeEventListener("scroll", update);
      document.removeEventListener("focusin", onFocusIn);
    };
  }, [enabled]);
  return { keyboardInset, isKeyboardOpen: isKeyboardOpen(keyboardInset) };
}
function buildCanvasProps(input) {
  var _a;
  const {
    mode,
    canEdit,
    slides,
    activeSlide,
    masterPseudoSlide,
    templateElements,
    canvasSize,
    activeSlideIndex,
    gridSpacingPx,
    zoom,
    state: s,
    selectedElement,
    canvasHandlers,
    insertHandlers,
    tableOps,
    presentation,
    findResults,
    findResultIndex,
    viewerOptions,
    buildHyperlinkConfirmMessage
  } = input;
  const effectiveSlide = mode === "master" ? masterPseudoSlide : activeSlide;
  const effectiveTemplateElements = mode === "master" ? s.activeLayout ? ((_a = s.activeMaster) == null ? void 0 : _a.elements) ?? [] : [] : templateElements;
  const fieldContext = buildFieldSubstitutionContext({
    headerFooter: s.headerFooter,
    customProperties: s.customProperties,
    slide: activeSlide
  });
  const tableStyleContext = s.theme || s.tableStyleMap ? { theme: s.theme, tableStyleMap: s.tableStyleMap } : void 0;
  const openExternalUrl = (url, target) => {
    if (shouldConfirmExternalHyperlink(viewerOptions, url) && !window.confirm(buildHyperlinkConfirmMessage(url))) {
      return;
    }
    safeOpenUrl(url, target);
  };
  const handleActionClick = (elementId, action) => {
    if (mode === "present") {
      const resolved = resolvePresentationAction(action, { slideCount: slides.length });
      if (resolved.intent.kind === "openUrl") {
        openExternalUrl(resolved.intent.url);
        return;
      }
      presentation.handlePresentationAction(action, elementId);
    } else if (action.url) {
      openExternalUrl(action.url);
    }
  };
  const handleHyperlinkClick = (url, target) => {
    if (isPpactionUrl(url)) {
      if (mode === "present") {
        const parsed = parsePpactionUrl(url);
        if (parsed) {
          presentation.handlePresentationAction({
            action: parsed.action,
            targetSlideIndex: parsed.targetSlideIndex
          });
        }
      }
      return;
    }
    openExternalUrl(url, target);
  };
  return {
    activeSlide: effectiveSlide,
    templateElements: effectiveTemplateElements,
    canvasSize,
    zoom,
    mode,
    canEdit,
    editTemplateMode: mode === "master" || s.editTemplateMode,
    selectedElementIdSet: s.selectedElementIdSet,
    selectedElement,
    inlineEditingElementId: s.inlineEditingElementId,
    inlineEditingText: s.inlineEditingText,
    spellCheckEnabled: s.spellCheckEnabled,
    mediaDataUrls: s.mediaDataUrls,
    tableEditorState: s.tableEditorState,
    marqueeSelectionState: s.marqueeSelectionState,
    snapLines: s.snapLines,
    showGrid: s.showGrid,
    gridSpacingPx,
    showRulers: s.showRulers,
    // View ▸ Guides hides the overlay without discarding the guides: they are
    // still snapped to while dragging and still round-trip on save.
    guides: s.showGuides ? s.guides : [],
    presentationElementStates: mode === "present" ? presentation.presentationElementStates : void 0,
    presentationKeyframesCss: mode === "present" ? presentation.presentationKeyframesCss : void 0,
    onClick: canvasHandlers.handleElementClick,
    onDoubleClick: canvasHandlers.handleElementDoubleClick,
    onMouseDown: canvasHandlers.handleElementMouseDown,
    onContextMenu: canvasHandlers.handleElementContextMenu,
    onCanvasMouseDown: canvasHandlers.handleCanvasMouseDown,
    onResizePointerDown: canvasHandlers.handleResizePointerDown,
    onAdjustmentPointerDown: canvasHandlers.handleAdjustmentPointerDown,
    onRotate: canvasHandlers.handleRotate,
    onInlineEditChange: s.setInlineEditingText,
    onInlineEditCommit: canvasHandlers.handleInlineEditCommit,
    onInlineEditCancel: () => s.setInlineEditingElementId(null),
    onTableCellSelect: (cell, elementId) => s.setTableEditorState(cell ? { ...cell, elementId } : null),
    onCommitCellEdit: tableOps.handleCommitCellEdit,
    onUpdateSmartArtElement: canvasHandlers.handleUpdateSmartArtElement,
    onFormatText: canvasHandlers.handleFormatText,
    onResizeTableColumns: tableOps.handleResizeTableColumns,
    onResizeTableRow: tableOps.handleResizeTableRow,
    findResults,
    findResultIndex,
    activeSlideIndex,
    activeTool: s.activeTool,
    drawingColor: s.drawingColor,
    drawingWidth: s.drawingWidth,
    isDrawingRef: s.isDrawingRef,
    onAddInkElement: insertHandlers.handleAddInkElement,
    onAddFreeformShape: insertHandlers.handleAddFreeformShape,
    onEraseInkElement: insertHandlers.handleEraseInkElement,
    onActionClick: handleActionClick,
    onHyperlinkClick: handleHyperlinkClick,
    allSlides: mode === "present" ? slides : void 0,
    onZoomClick: mode === "present" ? presentation.handleZoomClick : void 0,
    sourceSlideIndex: mode === "present" ? activeSlideIndex : void 0,
    fieldContext,
    tableStyleContext,
    // Collaboration cursor/selection overlays require a `CollaborationProvider`
    // ancestor, which these building blocks intentionally don't render (hosts
    // that need collaboration should use `PowerPointViewer` directly).
    collaborationOverlay: void 0,
    comments: activeSlide == null ? void 0 : activeSlide.comments,
    showCommentMarkers: s.sidebarPanelMode === "comments",
    onCommentMarkerClick: () => s.setSidebarPanelMode("comments"),
    onMoveGuide: (guideId, position) => {
      s.setGuides(
        (prev) => prev.map(
          (guide) => guide.id === guideId ? {
            ...guide,
            position: guide.axis === "h" ? Math.max(0, Math.min(canvasSize.height, position)) : Math.max(0, Math.min(canvasSize.width, position))
          } : guide
        )
      );
    },
    onDeleteGuide: (guideId) => {
      s.setGuides((prev) => prev.filter((guide) => guide.id !== guideId));
    },
    onCreateGuideFromRuler: (axis, positionPx) => {
      s.setGuides((prev) => [
        ...prev,
        {
          id: `guide-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
          axis,
          position: positionPx
        }
      ]);
    }
  };
}
function scopeLayoutOptionsToActiveSlide(options, activeSlide) {
  return scopeLayoutOptionsToSlide(options, activeSlide == null ? void 0 : activeSlide.layoutPath);
}
function buildToolbarAnimationHandlers(input) {
  const { selectedElement, activeSlide, propertyHandlers, ops, history } = input;
  const handleAddAnimation = (preset, group) => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    const current = activeSlide.animations ?? [];
    if (group === "motionPath") {
      propertyHandlers.handleUpdateSlide({
        animations: applyMotionPathPreset(current, selectedElement.id, preset)
      });
      return;
    }
    const existing = current.find((a) => a.elementId === selectedElement.id);
    const presetValue = preset;
    if (existing) {
      const updated = current.map(
        (a) => a.elementId === selectedElement.id ? { ...a, [group]: presetValue } : a
      );
      propertyHandlers.handleUpdateSlide({ animations: updated });
    } else {
      const newAnim = {
        elementId: selectedElement.id,
        [group]: presetValue,
        durationMs: 500,
        order: current.length,
        trigger: "onClick"
      };
      propertyHandlers.handleUpdateSlide({ animations: [...current, newAnim] });
    }
  };
  const handleRemoveAnimation = () => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    const current = activeSlide.animations ?? [];
    const filtered = current.filter((a) => a.elementId !== selectedElement.id);
    propertyHandlers.handleUpdateSlide({ animations: filtered });
  };
  const handleTransitionChange = (updates) => {
    if (!activeSlide) {
      return;
    }
    const current = activeSlide.transition ?? { type: "none" };
    propertyHandlers.handleUpdateSlide({ transition: { ...current, ...updates } });
  };
  const handleApplyTransitionToAll = () => {
    const transition = activeSlide == null ? void 0 : activeSlide.transition;
    if (!transition) {
      return;
    }
    ops.updateSlides((prev) => prev.map((sl) => ({ ...sl, transition })));
    history.markDirty();
  };
  return {
    handleAddAnimation,
    handleRemoveAnimation,
    handleTransitionChange,
    handleApplyTransitionToAll
  };
}
function buildToolbarProps(input) {
  var _a, _b2;
  const {
    mode,
    canEdit,
    state: s,
    selectedElement,
    activeSlide,
    activeSlideIndex,
    zoom,
    history,
    findReplace,
    manipulation,
    insertHandlers,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    dialogs,
    slideOps,
    ops,
    onSetMode,
    onEnterPresenterView,
    onEnterRehearsalMode,
    onOpenSettings,
    onOpenHeaderFooter,
    onOpenShareDialog,
    onOpenFile,
    onOpenRecentFile,
    fileName,
    hiddenActions,
    viewPreferencesSync
  } = input;
  const {
    handleAddAnimation,
    handleRemoveAnimation,
    handleTransitionChange,
    handleApplyTransitionToAll
  } = buildToolbarAnimationHandlers({
    selectedElement,
    activeSlide,
    propertyHandlers,
    ops,
    history
  });
  const scopedLayoutOptions = scopeLayoutOptionsToActiveSlide(s.layoutOptions, activeSlide);
  const selectionSize = selectedElement === null ? 0 : 1;
  return {
    fileName,
    mode,
    canEdit,
    isNarrowViewport: dialogs.isNarrowViewport,
    isSidebarCollapsed: !s.isSlidesPaneOpen,
    isInspectorPaneOpen: s.isInspectorPaneOpen,
    isCompactToolbarOpen: s.isCompactToolbarOpen,
    toolbarSection: s.toolbarSection,
    scale: zoom.scale,
    canUndo: history.canUndo,
    canRedo: history.canRedo,
    undoLabel: history.undoLabel,
    redoLabel: history.redoLabel,
    findReplaceOpen: findReplace.findReplaceOpen,
    selectedElement,
    tableEditorState: s.tableEditorState,
    editTemplateMode: s.editTemplateMode,
    newShapeType: s.newShapeType,
    activeTool: s.activeTool,
    drawingColor: s.drawingColor,
    drawingWidth: s.drawingWidth,
    clipboardPayload: s.clipboardPayload,
    onSetMode,
    onToggleSidebar: () => s.setIsSlidesPaneOpen((p) => !p),
    onToggleInspector: () => s.setIsInspectorPaneOpen((p) => !p),
    onOpenAnimationPanel: () => {
      s.setIsInspectorPaneOpen(true);
      s.setSidebarPanelMode("properties");
    },
    onAddAnimation: handleAddAnimation,
    onRemoveAnimation: handleRemoveAnimation,
    onToggleCompactToolbar: () => s.setIsCompactToolbarOpen((p) => !p),
    onSetToolbarSection: s.setToolbarSection,
    onZoomIn: zoom.handleZoomIn,
    onZoomOut: zoom.handleZoomOut,
    onZoomToFit: zoom.handleZoomToFit,
    onUndo: history.handleUndo,
    onRedo: history.handleRedo,
    onToggleFindReplace: () => findReplace.setFindReplaceOpen(!findReplace.findReplaceOpen),
    onSetNewShapeType: s.setNewShapeType,
    onAddTextBox: insertHandlers.handleAddTextBox,
    onAddShape: insertHandlers.handleAddShape,
    onAddTable: insertHandlers.handleAddTable,
    onAddChart: insertHandlers.handleAddChart,
    onAddSmartArt: () => dialogs.setIsSmartArtDialogOpen(true),
    onAddEquation: () => {
      dialogs.setEditingEquationOmml(null);
      dialogs.setIsEquationDialogOpen(true);
    },
    onAddActionButton: insertHandlers.handleAddActionButton,
    onInsertField: insertHandlers.handleInsertField,
    onOpenHeaderFooter,
    onOpenImagePicker: () => {
      var _a2;
      return (_a2 = s.imageInputRef.current) == null ? void 0 : _a2.click();
    },
    onOpenMediaPicker: () => {
      var _a2;
      return (_a2 = s.mediaInputRef.current) == null ? void 0 : _a2.click();
    },
    onSetActiveTool: s.setActiveTool,
    onSetDrawingColor: s.setDrawingColor,
    onSetDrawingWidth: s.setDrawingWidth,
    onSetEditTemplateMode: s.setEditTemplateMode,
    spellCheckEnabled: s.spellCheckEnabled,
    showGrid: s.showGrid,
    showRulers: s.showRulers,
    showGuides: s.showGuides,
    snapToGrid: s.snapToGrid,
    snapToShape: s.snapToShape,
    onSetSpellCheckEnabled: s.setSpellCheckEnabled,
    onSetShowGrid: s.setShowGrid,
    onSetShowRulers: s.setShowRulers,
    onSetShowGuides: (viewPreferencesSync == null ? void 0 : viewPreferencesSync.handleSetShowGuides) ?? s.setShowGuides,
    onSetSnapToGrid: (viewPreferencesSync == null ? void 0 : viewPreferencesSync.handleSetSnapToGrid) ?? s.setSnapToGrid,
    onSetSnapToShape: (viewPreferencesSync == null ? void 0 : viewPreferencesSync.handleSetSnapToShape) ?? s.setSnapToShape,
    onAddGuide: dialogs.handleAddGuide,
    onAlignElements: manipulation.handleAlignElements,
    onDistributeElements: manipulation.handleDistributeElements,
    canDistribute: manipulation.canDistribute,
    onCopy: manipulation.handleCopy,
    onCut: manipulation.handleCut,
    onPaste: manipulation.handlePaste,
    onFlip: manipulation.handleFlip,
    onMoveLayer: manipulation.handleMoveLayer,
    onMoveLayerToEdge: manipulation.handleMoveLayerToEdge,
    onGroupElements: manipulation.handleGroupElements,
    onUngroupElement: manipulation.handleUngroupElement,
    onUpdateElementStyle: ops.updateSelectedShapeStyle,
    selectedCount: s.selectedElementIds.length > 0 ? s.selectedElementIds.length : selectionSize,
    selectionGroupable: manipulation.selectionGroupable,
    onOpenHyperlinkDialog: () => dialogs.setIsHyperlinkDialogOpen(true),
    onDuplicate: manipulation.handleDuplicate,
    onDelete: manipulation.handleDelete,
    onExportPng: exportHandlers.handleExportPng,
    onExportPdf: exportHandlers.handleExportPdf,
    onExportVideo: exportHandlers.handleExportVideo,
    onExportGif: exportHandlers.handleExportGif,
    onExportJson: exportHandlers.handleExportJson,
    onOpenFile,
    onOpenRecentFile,
    onCreatePresentation: (templateId) => {
      s.setSlides(createBackstagePresentation(templateId));
      s.setActiveSlideIndex(0);
      s.setSelectedElementId(null);
      s.setSelectedElementIds([]);
      s.setTemplateElementsBySlideId({});
      s.setIsDirty(true);
    },
    onOpenShareDialog,
    onSaveAsPptx: exportHandlers.handleSaveAsPptx,
    onSaveAsPpsx: exportHandlers.handleSaveAsPpsx,
    onSaveAsPptm: exportHandlers.handleSaveAsPptm,
    hasMacros: s.hasMacros,
    onCopySlideAsImage: exportHandlers.handleCopySlideAsImage,
    onPrint: printHandlers.handlePrint,
    onToggleShortcuts: () => s.setIsShortcutHelpOpen((p) => !p),
    onOpenSettings,
    onRunAccessibilityCheck: dialogs.handleRunAccessibilityCheck,
    onToggleSlideSorter: () => s.setShowSlideSorter((p) => !p),
    onOpenReadingView: () => s.setShowReadingView(true),
    onOpenOutlineView: () => s.setShowOutlineView(true),
    onUpdateTextStyle: ops.updateSelectedTextStyle,
    onTransformTextCase: ops.updateSelectedTextCase,
    isOverflowMenuOpen: s.isOverflowMenuOpen,
    onSetOverflowMenuOpen: s.setIsOverflowMenuOpen,
    layoutOptions: scopedLayoutOptions,
    currentLayoutPath: activeSlide == null ? void 0 : activeSlide.layoutPath,
    loadLayoutPreviews: input.loadLayoutPreviews,
    onInsertSlideFromLayout: slideOps.handleInsertSlideFromLayout,
    onInsertSlideFromTemplate: slideOps.handleInsertSlideFromTemplate,
    templateScheme: templateSchemeFromTheme((_a = s.theme) == null ? void 0 : _a.colorScheme),
    customShows: s.customShows,
    activeCustomShowId: s.activeCustomShowId,
    onSetActiveCustomShowId: s.setActiveCustomShowId,
    onCreateCustomShow: dialogs.handleCreateCustomShow,
    onRenameActiveCustomShow: dialogs.handleRenameActiveCustomShow,
    onDeleteActiveCustomShow: dialogs.handleDeleteActiveCustomShow,
    onToggleCurrentSlideInActiveShow: dialogs.handleToggleCurrentSlideInActiveShow,
    isCurrentSlideInActiveShow: dialogs.isCurrentSlideInActiveShow,
    onEnterMasterView: dialogs.handleEnterMasterView,
    onCloseMasterView: dialogs.handleCloseMasterView,
    onToggleVersionHistory: () => propertyHandlers.setIsVersionHistoryOpen((p) => !p),
    onOpenPasswordProtection: () => dialogs.setIsPasswordDialogOpen(true),
    onOpenDocumentProperties: () => dialogs.setIsDocPropsDialogOpen(true),
    // Design > Slide Size: the size control is the inspector's SLIDE SIZE
    // card, which the deck (no-selection) panel renders. Drop the selection so
    // that panel is what the pane shows, then open it.
    onOpenSlideSize: () => {
      s.setSelectedElementId(null);
      s.setSelectedElementIds([]);
      s.setSidebarPanelMode("properties");
      s.setIsInspectorPaneOpen(true);
    },
    onOpenFontEmbedding: () => dialogs.setIsFontEmbeddingOpen(true),
    onOpenDigitalSignatures: () => dialogs.setIsDigitalSigDialogOpen(true),
    onEnterPresenterView,
    onEnterRehearsalMode,
    onToggleThemeEditor: () => s.setIsThemeEditorOpen((p) => !p),
    isThemeEditorOpen: s.isThemeEditorOpen,
    onToggleThemeGallery: () => s.setIsThemeGalleryOpen((p) => !p),
    isThemeGalleryOpen: s.isThemeGalleryOpen,
    onCompare: propertyHandlers.handleCompare,
    onToggleComments: () => {
      s.setSidebarPanelMode("comments");
      if (!s.isInspectorPaneOpen) {
        s.setIsInspectorPaneOpen(true);
      }
    },
    isCommentsPanelOpen: s.isInspectorPaneOpen,
    slideCommentCount: ((_b2 = activeSlide == null ? void 0 : activeSlide.comments) == null ? void 0 : _b2.length) ?? 0,
    formatPainterActive: s.formatPainterActive,
    canActivateFormatPainter: hasCopyableFormat(selectedElement),
    onToggleFormatPainter: () => s.setFormatPainterActive((p) => !p),
    isSelectionPaneOpen: s.isSelectionPaneOpen,
    onToggleSelectionPane: () => s.setIsSelectionPaneOpen((p) => !p),
    eyedropperActive: s.eyedropperActive,
    onToggleEyedropper: () => s.setEyedropperActive((p) => !p),
    onOpenSetUpSlideShow: () => dialogs.setIsSetUpSlideShowOpen(true),
    // PowerPoint's Hide Slide: skip the active slide during the show while
    // leaving it in the deck, the rail and the sorter.
    onToggleHideSlide: () => slideOps.handleToggleHideSlides([activeSlideIndex]),
    activeSlideHidden: Boolean(activeSlide == null ? void 0 : activeSlide.hidden),
    onOpenBroadcastDialog: () => dialogs.setIsBroadcastDialogOpen(true),
    onToggleSubtitles: dialogs.handleToggleSubtitles,
    showSubtitles: Boolean(s.presentationProperties.showSubtitles),
    activeSlide,
    onTransitionChange: handleTransitionChange,
    onApplyTransitionToAll: handleApplyTransitionToAll,
    // No `onResetSlide` here: this building-blocks surface supplies no
    // `onApplyLayout` either, so there is no layout to reset to. The mounted
    // viewer (`ViewerToolbarSection`) wires both.
    onSelectAll: () => {
      const allIds = (activeSlide == null ? void 0 : activeSlide.elements.map((element) => element.id)) ?? [];
      if (allIds.length > 0) {
        ops.applySelection(allIds[0], allIds);
      }
    },
    presentationProperties: s.presentationProperties,
    onPresentationPropertiesChange: (updates) => {
      s.setPresentationProperties((prev) => ({ ...prev, ...updates }));
      history.markDirty();
    },
    hiddenActions
  };
}
function useViewerBuildingBlocksCore(input) {
  var _a, _b2;
  const { content } = input;
  const state = useViewerState();
  const {
    containerRef,
    mode,
    slides,
    canvasSize,
    loading,
    error,
    activeSlideIndex,
    selectedElementId,
    selectedElementIds,
    templateElementsBySlideId,
    activeSlide,
    selectedElement
  } = state;
  const hasActivePointerInteraction = (0, import_react.useCallback)(
    () => Boolean(
      state.dragStateRef.current || state.resizeStateRef.current || state.marqueeStateRef.current || state.shapeAdjustmentDragStateRef.current || state.isDrawingRef.current
    ),
    [
      state.dragStateRef,
      state.resizeStateRef,
      state.marqueeStateRef,
      state.shapeAdjustmentDragStateRef,
      state.isDrawingRef
    ]
  );
  const zoom = useZoomViewport({ canvasSize, selectedElements: state.selectedElements });
  const { options: viewerOptions } = useViewerOptions();
  const { setIsDirty } = state;
  const markDocumentDirty = (0, import_react.useCallback)(() => {
    setIsDirty(true);
  }, [setIsDirty]);
  const history = useEditorHistory({
    slides,
    canvasSize,
    activeSlideIndex,
    templateElementsBySlideId,
    editTemplateMode: state.editTemplateMode,
    headerFooter: state.headerFooter,
    loading,
    error,
    maxHistoryEntries: resolveHistoryDepth(viewerOptions),
    hasActivePointerInteraction,
    pointerCommitNonce: state.pointerCommitNonce,
    onDirty: markDocumentDirty,
    setSlides: state.setSlides,
    setCanvasSize: state.setCanvasSize,
    setActiveSlideIndex: state.setActiveSlideIndex,
    setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
    setSelectedElementId: state.setSelectedElementId,
    setSelectedElementIds: state.setSelectedElementIds,
    setEditTemplateMode: state.setEditTemplateMode,
    setHeaderFooter: state.setHeaderFooter
  });
  const { gridSpacingPx, visibleSlideIndexes, masterPseudoSlide } = useDerivedSlideState({
    slides,
    sections: state.sections,
    customShows: state.customShows,
    activeCustomShowId: state.activeCustomShowId,
    presentationProperties: state.presentationProperties,
    mode,
    activeLayout: state.activeLayout,
    activeMaster: state.activeMaster,
    documentGridSpacing: (_a = state.viewProperties) == null ? void 0 : _a.gridSpacing
  });
  const themeColorMap = (0, import_react.useMemo)(
    () => {
      var _a2;
      return ((_a2 = state.theme) == null ? void 0 : _a2.colorScheme) ? buildThemeColorMap(state.theme.colorScheme) : void 0;
    },
    [(_b2 = state.theme) == null ? void 0 : _b2.colorScheme]
  );
  const { presentation, annotations, actionSoundHandlerRef, setExitModeHandler } = usePresentationSetup({
    mode,
    slides,
    templateElementsBySlideId,
    visibleSlideIndexes,
    canvasSize,
    themeColorMap,
    endWithBlackSlide: viewerOptions.advanced.slideShowEndWithBlackSlide,
    // File > Options > Advanced > "Prompt to keep ink annotations when
    // exiting" / "Show popup toolbar" while presenting.
    promptKeepInkAnnotations: viewerOptions.advanced.slideShowPromptKeepInkAnnotations,
    popupToolbarEnabled: viewerOptions.advanced.slideShowShowPopupToolbar,
    activeSlideIndex,
    containerRef,
    content,
    mediaDataUrls: state.mediaDataUrls,
    presentationProperties: state.presentationProperties,
    setMode: state.setMode,
    setActiveSlideIndex: state.setActiveSlideIndex,
    setSlides: state.setSlides,
    history,
    // PowerPoint's bare `J` during a show toggles live captions.
    onToggleSubtitles: () => state.setPresentationProperties((prev) => ({
      ...prev,
      showSubtitles: !prev.showSubtitles
    }))
  });
  return {
    state,
    mode,
    slides,
    loading,
    error,
    activeSlideIndex,
    activeSlide,
    selectedElement,
    zoom,
    history,
    presentation,
    annotations,
    actionSoundHandlerRef,
    setExitModeHandler,
    masterPseudoSlide,
    gridSpacingPx,
    // Multiplied against the pre-existing 2x baseline; see the matching
    // comment in `PowerPointViewer.tsx`.
    imageExportScale: 2 * resolveImageResolutionScale(viewerOptions),
    viewerOptions
  };
}
function useCompatibilityToastsState() {
  const [toasts, setToasts] = (0, import_react.useState)([]);
  const dismiss = (0, import_react.useCallback)((id) => {
    setToasts((prev) => prev.filter((toast) => toast.id !== id));
  }, []);
  const dismissAll = (0, import_react.useCallback)(() => {
    setToasts([]);
  }, []);
  return { toasts, setToasts, dismiss, dismissAll };
}
function useReadOnlyRecommendationState(content) {
  const [recommendation, setRecommendation] = (0, import_react.useState)(
    () => readOnlyRecommendation(void 0)
  );
  const [modifyVerifier, setModifyVerifier] = (0, import_react.useState)(void 0);
  const [bannerHidden, setBannerHidden] = (0, import_react.useState)(false);
  const [editAnywayActive, setEditAnywayActive] = (0, import_react.useState)(false);
  const [passwordPromptOpen, setPasswordPromptOpen] = (0, import_react.useState)(false);
  const [passwordError, setPasswordError] = (0, import_react.useState)(null);
  const [checkingPassword, setCheckingPassword] = (0, import_react.useState)(false);
  (0, import_react.useEffect)(() => {
    setBannerHidden(false);
    setEditAnywayActive(false);
    setPasswordPromptOpen(false);
    setPasswordError(null);
    setCheckingPassword(false);
  }, [content]);
  const unlock = (0, import_react.useCallback)(() => {
    setEditAnywayActive(true);
    setBannerHidden(true);
    setPasswordPromptOpen(false);
    setPasswordError(null);
  }, []);
  const editAnyway = (0, import_react.useCallback)(() => {
    if (recommendation.requiresPassword) {
      setPasswordPromptOpen(true);
      setPasswordError(null);
      return;
    }
    unlock();
  }, [recommendation.requiresPassword, unlock]);
  const dismiss = (0, import_react.useCallback)(() => {
    setBannerHidden(true);
  }, []);
  const cancelPasswordPrompt = (0, import_react.useCallback)(() => {
    setPasswordPromptOpen(false);
    setPasswordError(null);
  }, []);
  const submitPassword = (0, import_react.useCallback)(
    async (password) => {
      setCheckingPassword(true);
      try {
        const result = await checkModifyPassword(modifyVerifier, password);
        if (result.ok) {
          unlock();
        } else {
          setPasswordError(result.reason);
        }
      } finally {
        setCheckingPassword(false);
      }
    },
    // oxlint-disable-next-line react/memo-dependencies -- see comment above
    [modifyVerifier, unlock]
  );
  return {
    recommendation,
    setRecommendation,
    setModifyVerifier,
    /** Whether the viewer's `canEdit` should be forced off. */
    locked: recommendation.defaultReadOnly && !editAnywayActive,
    bannerVisible: recommendation.kind !== null && !bannerHidden,
    editAnyway,
    dismiss,
    /** Whether the inline password prompt should render in place of the banner's buttons. */
    passwordPromptOpen,
    /** Reason the last password attempt failed, or null before any attempt / after success. */
    passwordError,
    /** True while `submitPassword`'s check is in flight (disables the form). */
    checkingPassword,
    submitPassword,
    cancelPasswordPrompt
  };
}
function useViewPreferencesSync(input) {
  const {
    loadVersion,
    viewProperties,
    setViewProperties,
    snapToGrid,
    setSnapToGrid,
    snapToShape,
    setSnapToShape,
    showGuides,
    setShowGuides
  } = input;
  (0, import_react.useEffect)(() => {
    const seeded = viewerPreferencesFromViewProperties(
      { viewProperties },
      { ...DEFAULT_VIEWER_PREFERENCES, snapToGrid, snapToObjects: snapToShape, showGuides }
    );
    setSnapToGrid(seeded.snapToGrid);
    setSnapToShape(seeded.snapToObjects ?? snapToShape);
    setShowGuides(seeded.showGuides ?? showGuides);
  }, [loadVersion]);
  const patchViewProperties = (0, import_react.useCallback)(
    (prefsOverride) => {
      setViewProperties((prev) => {
        var _a, _b2;
        const merged = {
          ...DEFAULT_VIEWER_PREFERENCES,
          snapToGrid,
          snapToObjects: snapToShape,
          showGuides,
          gridSpacingCx: (_a = prev == null ? void 0 : prev.gridSpacing) == null ? void 0 : _a.cx,
          gridSpacingCy: (_b2 = prev == null ? void 0 : prev.gridSpacing) == null ? void 0 : _b2.cy,
          ...prefsOverride
        };
        const patch = viewPropertiesPatchFromPreferences(merged);
        return {
          ...prev,
          slideViewPr: { ...prev == null ? void 0 : prev.slideViewPr, ...patch.slideViewPr },
          ...patch.gridSpacing ? { gridSpacing: patch.gridSpacing } : {}
        };
      });
    },
    [setViewProperties, snapToGrid, snapToShape, showGuides]
  );
  const handleSetSnapToGrid = (0, import_react.useCallback)(
    (value) => {
      setSnapToGrid(value);
      patchViewProperties({ snapToGrid: value });
    },
    [setSnapToGrid, patchViewProperties]
  );
  const handleSetSnapToShape = (0, import_react.useCallback)(
    (value) => {
      setSnapToShape(value);
      patchViewProperties({ snapToObjects: value });
    },
    [setSnapToShape, patchViewProperties]
  );
  const handleSetShowGuides = (0, import_react.useCallback)(
    (value) => {
      setShowGuides(value);
      patchViewProperties({ showGuides: value });
    },
    [setShowGuides, patchViewProperties]
  );
  return { handleSetSnapToGrid, handleSetSnapToShape, handleSetShowGuides };
}
function useViewerBuildingBlocksState(input) {
  const { core, content, canEdit, filePath, autosaveEnabled, userName, handle, setContent } = input;
  const {
    onContentChange,
    onDirtyChange,
    onActiveSlideChange,
    onModeChange,
    onZoomChange,
    onSelectionChange,
    onSlideCountChange
  } = input;
  const {
    state,
    mode,
    slides,
    loading,
    error,
    activeSlideIndex,
    activeSlide,
    selectedElement,
    zoom,
    history,
    presentation,
    annotations,
    actionSoundHandlerRef,
    gridSpacingPx,
    imageExportScale,
    setExitModeHandler,
    viewerOptions
  } = core;
  const dialogs = useViewerDialogs({
    mode,
    slides,
    activeSlide,
    canvasSize: state.canvasSize,
    containerRef: state.containerRef,
    customShows: state.customShows,
    activeCustomShowId: state.activeCustomShowId,
    setCustomShows: state.setCustomShows,
    setActiveCustomShowId: state.setActiveCustomShowId,
    setGuides: state.setGuides,
    setPresentationProperties: state.setPresentationProperties,
    setAccessibilityIssues: state.setAccessibilityIssues,
    setIsAccessibilityPanelOpen: state.setIsAccessibilityPanelOpen,
    setMode: state.setMode,
    setPreMasterMode: state.setPreMasterMode,
    setActiveMasterIndex: state.setActiveMasterIndex,
    setActiveLayoutIndex: state.setActiveLayoutIndex,
    setSelectedElementId: state.setSelectedElementId,
    setSelectedElementIds: state.setSelectedElementIds,
    preMasterMode: state.preMasterMode,
    hasDigitalSignatures: state.hasDigitalSignatures,
    isDirty: state.isDirty,
    history
  });
  const readOnlyRec = useReadOnlyRecommendationState(content);
  const compatToastsState = useCompatibilityToastsState();
  (0, import_react.useEffect)(() => {
    state.setSelectedElementId(null);
    state.setSelectedElementIds([]);
    state.setInlineEditingElementId(null);
  }, [activeSlideIndex]);
  const editorOps = useEditorOperations({
    state,
    history,
    zoom,
    mode,
    canEdit,
    slides,
    activeSlide,
    activeSlideIndex,
    selectedElement,
    selectedElementId: state.selectedElementId,
    selectedElementIds: state.selectedElementIds,
    canvasSize: state.canvasSize,
    dialogs,
    presentation,
    userName,
    handlerRef: actionSoundHandlerRef
  });
  const {
    exportHandlers,
    printHandlers,
    propertyHandlers,
    handleSetMode,
    handleEnterPresenterView,
    handleEnterRehearsalMode,
    autosaveStatus,
    loadVersion
  } = useViewerIntegration({
    state,
    zoom,
    history,
    presentation,
    annotations,
    actionSoundHandlerRef,
    editorOps,
    dialogs,
    gridSpacingPx,
    content,
    filePath,
    autosaveEnabled,
    // Trust Center > "Allow external content": same gate `PowerPointViewer`
    // applies, so the headless building-blocks API loads http(s) images
    // under the same default-off, core-side SSRF/privacy guard.
    allowExternalImages: viewerOptions.trust.allowExternalContent,
    setReadOnlyRecommendation: readOnlyRec.setRecommendation,
    setModifyVerifier: readOnlyRec.setModifyVerifier,
    setCompatToasts: compatToastsState.setToasts,
    canEdit,
    promptKeepInkAnnotations: viewerOptions.advanced.slideShowPromptKeepInkAnnotations,
    imageExportScale,
    mode,
    slides,
    activeSlide,
    activeSlideIndex,
    canvasSize: state.canvasSize,
    loading,
    error,
    ref: handle ?? null,
    setContent,
    onContentChange,
    onDirtyChange,
    onActiveSlideChange,
    onModeChange,
    onZoomChange,
    onSelectionChange,
    onSlideCountChange
  });
  setExitModeHandler(handleSetMode);
  const viewPreferencesSync = useViewPreferencesSync({
    loadVersion,
    viewProperties: state.viewProperties,
    setViewProperties: state.setViewProperties,
    snapToGrid: state.snapToGrid,
    setSnapToGrid: state.setSnapToGrid,
    snapToShape: state.snapToShape,
    setSnapToShape: state.setSnapToShape,
    showGuides: state.showGuides,
    setShowGuides: state.setShowGuides
  });
  return {
    dialogs,
    editorOps,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    handleSetMode,
    handleEnterPresenterView,
    handleEnterRehearsalMode,
    autosaveStatus,
    viewPreferencesSync
  };
}
function useViewerBuildingBlocks(input) {
  const {
    content: incomingContent,
    canEdit = false,
    filePath,
    fileName,
    autosaveEnabled = true,
    userName,
    hiddenActions,
    handle,
    onContentChange,
    onDirtyChange,
    onActiveSlideChange,
    onModeChange,
    onZoomChange,
    onSelectionChange,
    onSlideCountChange,
    onOpenSettings,
    onOpenHeaderFooter,
    onOpenShareDialog
  } = input;
  const { t } = useTranslation();
  const [content, setContent] = (0, import_react.useState)(incomingContent ?? null);
  (0, import_react.useEffect)(() => {
    setContent(incomingContent ?? null);
  }, [incomingContent]);
  const onOpenFile = (0, import_react.useCallback)(() => {
    void (async () => {
      const picked = await openPptxFile();
      if (picked) {
        setContent(picked.buffer);
      }
    })();
  }, []);
  const onOpenRecentFile = (0, import_react.useCallback)((key) => {
    void (async () => {
      const bytes = await readBackstageRecentFile(key);
      if (bytes) {
        setContent(bytes);
      }
    })();
  }, []);
  const core = useViewerBuildingBlocksCore({ content });
  const {
    state,
    mode,
    slides,
    loading,
    error,
    activeSlideIndex,
    activeSlide,
    selectedElement,
    zoom,
    history,
    presentation,
    masterPseudoSlide,
    gridSpacingPx,
    viewerOptions
  } = core;
  const {
    dialogs,
    editorOps,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    handleSetMode,
    handleEnterPresenterView,
    handleEnterRehearsalMode,
    autosaveStatus,
    viewPreferencesSync
  } = useViewerBuildingBlocksState({
    core,
    content,
    canEdit,
    filePath,
    autosaveEnabled,
    userName,
    handle,
    setContent,
    onContentChange,
    onDirtyChange,
    onActiveSlideChange,
    onModeChange,
    onZoomChange,
    onSelectionChange,
    onSlideCountChange
  });
  const toolbarProps = buildToolbarProps({
    mode,
    canEdit,
    state,
    selectedElement,
    activeSlide,
    zoom,
    history,
    findReplace: editorOps.findReplace,
    manipulation: editorOps.manipulation,
    insertHandlers: editorOps.insertHandlers,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    dialogs,
    activeSlideIndex,
    slideOps: editorOps.slideOps,
    ops: editorOps.ops,
    onSetMode: handleSetMode,
    onEnterPresenterView: handleEnterPresenterView,
    onEnterRehearsalMode: handleEnterRehearsalMode,
    onOpenSettings,
    onOpenHeaderFooter,
    onOpenShareDialog,
    onOpenFile,
    onOpenRecentFile,
    fileName,
    hiddenActions,
    viewPreferencesSync
  });
  const canvasProps = buildCanvasProps({
    mode,
    canEdit,
    slides,
    activeSlide,
    masterPseudoSlide,
    templateElements: state.templateElements,
    canvasSize: state.canvasSize,
    activeSlideIndex,
    gridSpacingPx,
    zoom,
    state,
    selectedElement,
    canvasHandlers: editorOps.canvasHandlers,
    insertHandlers: editorOps.insertHandlers,
    tableOps: editorOps.tableOps,
    presentation,
    findResults: editorOps.findReplace.findResults,
    findResultIndex: editorOps.findReplace.findResultIndex,
    viewerOptions,
    buildHyperlinkConfirmMessage: (url) => `${t("pptx.options.trust.confirmHyperlinks")}

${url}`
  });
  return { toolbarProps, canvasProps, mode, loading, error, autosaveStatus };
}
var SWIPE_THRESHOLD = 50;
function useSwipeNavigation(input) {
  const { enabled, onNext, onPrev } = input;
  const startRef = (0, import_react.useRef)(null);
  const onTouchStart = (0, import_react.useCallback)(
    (e) => {
      if (!enabled || e.changedTouches.length !== 1) {
        startRef.current = null;
        return;
      }
      const touch = e.changedTouches[0];
      startRef.current = { x: touch.clientX, y: touch.clientY };
    },
    [enabled]
  );
  const onTouchEnd = (0, import_react.useCallback)(
    (e) => {
      const start = startRef.current;
      startRef.current = null;
      if (!enabled || start === null || e.changedTouches.length !== 1) {
        return;
      }
      const touch = e.changedTouches[0];
      const dx = touch.clientX - start.x;
      const dy = touch.clientY - start.y;
      if (Math.abs(dx) < SWIPE_THRESHOLD || Math.abs(dx) <= Math.abs(dy)) {
        return;
      }
      if (dx < 0) {
        onNext();
      } else {
        onPrev();
      }
    },
    [enabled, onNext, onPrev]
  );
  return { onTouchStart, onTouchEnd };
}
var LEFT_MIN = 120;
var LEFT_MAX = 400;
var LEFT_DEFAULT = 180;
var RIGHT_MIN = 220;
var RIGHT_MAX = 480;
var RIGHT_DEFAULT = 288;
var BOTTOM_MIN = 60;
var BOTTOM_MAX = 400;
var BOTTOM_DEFAULT = 120;
function useResizablePanels() {
  const [leftWidth, setLeftWidth] = (0, import_react.useState)(LEFT_DEFAULT);
  const [rightWidth, setRightWidth] = (0, import_react.useState)(RIGHT_DEFAULT);
  const [bottomHeight, setBottomHeight] = (0, import_react.useState)(BOTTOM_DEFAULT);
  const onResizeLeft = (0, import_react.useCallback)((delta) => {
    setLeftWidth((w) => Math.min(LEFT_MAX, Math.max(LEFT_MIN, w + delta)));
  }, []);
  const onResizeRight = (0, import_react.useCallback)((delta) => {
    setRightWidth((w) => Math.min(RIGHT_MAX, Math.max(RIGHT_MIN, w - delta)));
  }, []);
  const onResizeBottom = (0, import_react.useCallback)((delta) => {
    setBottomHeight((h) => Math.min(BOTTOM_MAX, Math.max(BOTTOM_MIN, h - delta)));
  }, []);
  return {
    leftWidth,
    rightWidth,
    bottomHeight,
    onResizeLeft,
    onResizeRight,
    onResizeBottom
  };
}

// node_modules/pptx-react-viewer/dist/chunk-76Z3X6JX.mjs
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var ViewerThemeContext = (0, import_react2.createContext)(void 0);
function ViewerThemeProvider({ theme, children }) {
  return (0, import_jsx_runtime.jsx)(ViewerThemeContext.Provider, { value: theme, children });
}
function useViewerTheme() {
  return (0, import_react2.useContext)(ViewerThemeContext);
}
function useThemeStyle(theme) {
  return (0, import_react2.useMemo)(() => {
    if (!theme) {
      return void 0;
    }
    const vars = themeToCssVars(theme);
    if (Object.keys(vars).length === 0) {
      return void 0;
    }
    return vars;
  }, [theme]);
}
var CollaborationContext = (0, import_react2.createContext)(null);
function useCollaboration() {
  return (0, import_react2.useContext)(CollaborationContext);
}
function CollaborationProvider({
  config,
  canvasWidth,
  canvasHeight,
  children
}) {
  const value = useCollaborativeState({
    config,
    canvasWidth,
    canvasHeight
  });
  return (0, import_jsx_runtime.jsx)(CollaborationContext.Provider, { value, children });
}
function RemoteUserCursors({
  remoteUsers,
  activeSlideIndex,
  canvasWidth,
  canvasHeight
}) {
  const visibleUsers = remoteUsers.filter((u) => u.activeSlideIndex === activeSlideIndex);
  if (visibleUsers.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      "data-testid": "remote-user-cursors",
      "data-export-ignore": "true",
      className: "absolute inset-0 pointer-events-none",
      style: { zIndex: 9999 },
      width: canvasWidth,
      height: canvasHeight,
      viewBox: `0 0 ${canvasWidth} ${canvasHeight}`,
      "aria-hidden": "true",
      children: visibleUsers.map((user) => (0, import_jsx_runtime.jsxs)(
        "g",
        {
          style: {
            transform: `translate(${user.cursorX}px, ${user.cursorY}px)`,
            transition: "transform 90ms linear",
            willChange: "transform"
          },
          "data-testid": `remote-cursor-${user.clientId}`,
          "data-pptx-remote-cursor": user.clientId,
          children: [
            (0, import_jsx_runtime.jsx)(
              "path",
              {
                d: "M0 0 L0 16 L4.5 12.5 L8 20 L10.5 19 L7 11.5 L12 11 Z",
                fill: user.userColor,
                stroke: "#fff",
                strokeWidth: 1,
                opacity: 0.9
              }
            ),
            (0, import_jsx_runtime.jsxs)("g", { transform: "translate(14, 18)", children: [
              (0, import_jsx_runtime.jsx)(
                "rect",
                {
                  rx: 3,
                  ry: 3,
                  x: -2,
                  y: -10,
                  width: Math.min(user.userName.length * 7 + 8, 150),
                  height: 16,
                  fill: user.userColor,
                  opacity: 0.85
                }
              ),
              (0, import_jsx_runtime.jsx)(
                "text",
                {
                  fill: "#fff",
                  fontSize: 10,
                  fontFamily: "system-ui, sans-serif",
                  fontWeight: 500,
                  dominantBaseline: "central",
                  y: -2,
                  x: 2,
                  children: user.userName.length > 20 ? `${user.userName.slice(0, 18)}...` : user.userName
                }
              )
            ] })
          ]
        },
        user.clientId
      ))
    }
  );
}
function getInitials(name) {
  const parts = name.trim().split(/\s+/u);
  if (parts.length >= 2) {
    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
  }
  return name.slice(0, 2).toUpperCase();
}
function AvatarCircle({
  name,
  color,
  avatar,
  isLocal
}) {
  const { t } = useTranslation();
  const initials = getInitials(name);
  const title = isLocal ? t("pptx.collaboration.youLabel", { name }) : name;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "relative w-7 h-7 rounded-full flex items-center justify-center text-[10px] font-semibold text-white border-2 -ml-1 first:ml-0",
      style: {
        backgroundColor: color,
        borderColor: isLocal ? "#fff" : color
      },
      title,
      "aria-label": title,
      children: avatar ? (0, import_jsx_runtime.jsx)(
        "img",
        {
          src: avatar,
          alt: "",
          className: "w-full h-full rounded-full object-cover",
          onError: (e) => {
            e.target.style.display = "none";
          }
        }
      ) : initials
    }
  );
}
function UserAvatarBar({
  remoteUsers,
  localUserName,
  localUserColor,
  localUserAvatar,
  status,
  maxVisible = 5
}) {
  const { t } = useTranslation();
  if (status === "disconnected" || status === "error") {
    return null;
  }
  const allUsers = [
    { name: localUserName, color: localUserColor, avatar: localUserAvatar, isLocal: true },
    ...remoteUsers.map((u) => ({
      name: u.userName,
      color: u.userColor,
      avatar: u.userAvatar,
      isLocal: false
    }))
  ];
  const visible = allUsers.slice(0, maxVisible);
  const overflow = allUsers.length - maxVisible;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "user-avatar-bar",
      className: "flex items-center px-2",
      "aria-label": t("pptx.collaboration.usersConnected", { count: allUsers.length }),
      children: [
        visible.map((user, i) => (0, import_jsx_runtime.jsx)(
          AvatarCircle,
          {
            name: user.name,
            color: user.color,
            avatar: user.avatar,
            isLocal: user.isLocal
          },
          user.isLocal ? "local" : `remote-${i}`
        )),
        overflow > 0 && (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: "w-7 h-7 rounded-full flex items-center justify-center text-[10px] font-semibold text-gray-300 bg-gray-700 border-2 border-gray-600 -ml-1",
            title: t("pptx.collaboration.moreUsers", { count: overflow }),
            children: [
              "+",
              overflow
            ]
          }
        )
      ]
    }
  );
}
var STATUS_STYLES = {
  connected: {
    dot: "bg-green-400",
    text: "text-green-400",
    label: "pptx.collaboration.status.connected"
  },
  connecting: {
    dot: "bg-yellow-400 animate-pulse",
    text: "text-yellow-400",
    label: "pptx.collaboration.status.connecting"
  },
  disconnected: {
    dot: "bg-gray-500",
    text: "text-gray-500",
    label: "pptx.collaboration.status.disconnected"
  },
  error: {
    dot: "bg-red-400",
    text: "text-red-400",
    label: "pptx.collaboration.status.error"
  }
};
function CollaborationStatusIndicator({
  status,
  connectedCount,
  onRetry
}) {
  const { t } = useTranslation();
  const style = STATUS_STYLES[status];
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      role: "status",
      "data-testid": "collaboration-status",
      className: "flex items-center gap-1.5",
      "aria-label": t("pptx.collaboration.statusAriaLabel", {
        status: t(`pptx.collaboration.status.${status}`),
        count: connectedCount
      }),
      children: [
        (0, import_jsx_runtime.jsx)("span", { className: `inline-block w-2 h-2 rounded-full ${style.dot}`, "aria-hidden": "true" }),
        (0, import_jsx_runtime.jsx)("span", { className: `text-[10px] ${style.text}`, children: status === "connected" ? t("pptx.collaboration.userCount", { count: connectedCount }) : t(`pptx.collaboration.status.${status}`) }),
        status === "error" && onRetry && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onRetry,
            className: "text-[10px] text-blue-400 hover:text-blue-300 underline underline-offset-2 transition-colors",
            "aria-label": t("pptx.collaboration.retry"),
            children: t("pptx.collaboration.retry")
          }
        )
      ]
    }
  );
}
function getInitials2(name) {
  const parts = name.trim().split(/\s+/u);
  if (parts.length >= 2 && parts[0] && parts[parts.length - 1]) {
    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
  }
  return name.slice(0, 2).toUpperCase() || "?";
}
function FollowModeBar({
  presences,
  followedClientId,
  onFollow
}) {
  const { t } = useTranslation();
  if (presences.length === 0) {
    return null;
  }
  const followedPeer = followedClientId === null ? null : presences.find((p) => p.clientId === followedClientId) ?? null;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "follow-mode-bar",
      "data-export-ignore": "true",
      className: "flex flex-wrap items-center gap-3 rounded-lg border border-border bg-card/95 px-2.5 py-1.5 text-xs text-foreground shadow-lg",
      children: [
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            "data-testid": "follow-status",
            className: "inline-flex items-center gap-1.5 whitespace-nowrap text-muted-foreground",
            children: followedPeer ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              t("pptx.followMode.following"),
              (0, import_jsx_runtime.jsx)("strong", { className: "text-foreground", children: followedPeer.userName }),
              (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  "data-testid": "follow-stop",
                  onClick: () => onFollow(null),
                  title: t("pptx.followMode.stopFollowing"),
                  className: "cursor-pointer rounded-md border border-border bg-transparent px-2 py-0.5 text-[11px] text-foreground hover:bg-muted",
                  children: t("pptx.followMode.stop")
                }
              )
            ] }) : t("pptx.followMode.followCollaborator")
          }
        ),
        (0, import_jsx_runtime.jsx)("ul", { className: "m-0 flex list-none items-center gap-1.5 p-0", children: presences.map((peer) => {
          const isFollowing = peer.clientId === followedClientId;
          return (0, import_jsx_runtime.jsx)("li", { children: (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              "data-testid": "follow-peer",
              "data-client-id": peer.clientId,
              "aria-pressed": isFollowing,
              title: isFollowing ? t("pptx.followMode.stopFollowingUser", { name: peer.userName }) : t("pptx.followMode.followUser", { name: peer.userName }),
              onClick: () => onFollow(isFollowing ? null : peer.clientId),
              className: `inline-flex cursor-pointer items-center gap-1.5 rounded-full border py-0.5 pl-0.5 pr-2 text-foreground hover:bg-muted ${isFollowing ? "border-primary bg-primary/30" : "border-transparent bg-muted/60"}`,
              children: [
                (0, import_jsx_runtime.jsx)(
                  "span",
                  {
                    "data-testid": "follow-avatar",
                    className: "inline-flex h-[22px] w-[22px] items-center justify-center rounded-full text-[10px] font-semibold leading-none text-white",
                    style: { backgroundColor: peer.userColor },
                    children: getInitials2(peer.userName)
                  }
                ),
                (0, import_jsx_runtime.jsx)("span", { className: "max-w-[120px] overflow-hidden text-ellipsis whitespace-nowrap", children: peer.userName })
              ]
            }
          ) }, peer.clientId);
        }) })
      ]
    }
  );
}
function CollaborationCursorOverlay({
  activeSlideIndex,
  canvasWidth,
  canvasHeight,
  selectedElementId
}) {
  const collab = useCollaboration();
  const containerRef = (0, import_react2.useRef)(null);
  const prevSelectionRef = (0, import_react2.useRef)(selectedElementId);
  (0, import_react2.useEffect)(() => {
    if (!collab || selectedElementId === prevSelectionRef.current) {
      return;
    }
    prevSelectionRef.current = selectedElementId;
    collab.broadcastPresence({
      selectedElementId: selectedElementId ?? void 0,
      activeSlideIndex
    });
  }, [collab, selectedElementId, activeSlideIndex]);
  (0, import_react2.useEffect)(() => {
    var _a;
    if (!collab) {
      return;
    }
    const parent = (_a = containerRef.current) == null ? void 0 : _a.parentElement;
    if (!parent) {
      return;
    }
    const handler = (e) => {
      const rect = parent.getBoundingClientRect();
      const x = (e.clientX - rect.left) / rect.width * canvasWidth;
      const y = (e.clientY - rect.top) / rect.height * canvasHeight;
      collab.broadcastPresence({
        cursorX: x,
        cursorY: y,
        activeSlideIndex
      });
    };
    parent.addEventListener("pointermove", handler);
    return () => parent.removeEventListener("pointermove", handler);
  }, [collab, canvasWidth, canvasHeight, activeSlideIndex]);
  if (!collab) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      ref: containerRef,
      "data-testid": "collab-pointer-tracker",
      "data-export-ignore": "true",
      style: { display: "contents" },
      children: (0, import_jsx_runtime.jsx)(
        RemoteUserCursors,
        {
          remoteUsers: collab.remoteUsers,
          activeSlideIndex,
          canvasWidth,
          canvasHeight
        }
      )
    }
  );
}
function RemoteSelectionOverlay({
  elements,
  activeSlideIndex
}) {
  const collab = useCollaboration();
  if (!collab) {
    return null;
  }
  const elementMap = /* @__PURE__ */ new Map();
  for (const el of elements) {
    elementMap.set(el.id, el);
  }
  const selections = [];
  for (const user of collab.remoteUsers) {
    if (user.activeSlideIndex === activeSlideIndex && user.selectedElementId) {
      const el = elementMap.get(user.selectedElementId);
      if (el) {
        selections.push({
          userName: user.userName,
          userColor: user.userColor,
          element: el
        });
      }
    }
  }
  if (selections.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: selections.map((sel) => (0, import_jsx_runtime.jsx)(
    "div",
    {
      "data-testid": `remote-selection-${sel.element.id}`,
      "data-pptx-remote-selection": sel.element.id,
      "data-export-ignore": "true",
      className: "absolute pointer-events-none",
      style: {
        left: sel.element.x,
        top: sel.element.y,
        width: sel.element.width,
        height: sel.element.height,
        zIndex: 9997,
        border: `2px solid ${sel.userColor}`,
        borderRadius: 2
      },
      children: (0, import_jsx_runtime.jsx)(
        "span",
        {
          className: "absolute -top-5 left-0 px-1 py-0.5 text-[9px] font-medium text-white rounded-sm whitespace-nowrap leading-none",
          style: { backgroundColor: sel.userColor },
          children: sel.userName
        }
      )
    },
    `remote-sel-${sel.element.id}`
  )) });
}
var ChartPartSelectionContext = (0, import_react2.createContext)({
  selection: null,
  setSelection: () => {
  }
});
function ChartPartSelectionProvider({
  children
}) {
  const [selection, setSelection] = (0, import_react2.useState)(null);
  const value = (0, import_react2.useMemo)(() => ({ selection, setSelection }), [selection]);
  return (0, import_jsx_runtime.jsx)(ChartPartSelectionContext.Provider, { value, children });
}
function useChartPartSelection() {
  return (0, import_react2.useContext)(ChartPartSelectionContext);
}
var AreaChart3DContext = (0, import_react2.createContext)(false);
function LoadingState({ className }) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      role: "status",
      "aria-live": "polite",
      "aria-atomic": "true",
      className: cn(
        "h-full w-full flex items-center justify-center text-muted-foreground",
        className
      ),
      children: (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "animate-spin rounded-full h-6 w-6 border-b-2 border-primary",
            "aria-hidden": "true"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { children: t("pptx.viewer.loading") })
      ] })
    }
  );
}
function FailedToLoad() {
  return null;
}
var LazyArea3DChartScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./Area3DChartScene-JM3CQ3WQ-RTMLLYJF.js");
    } catch {
      return { default: FailedToLoad };
    }
  }
);
var SceneErrorBoundary = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function Area3DChartRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = (0, import_react2.useMemo)(
    () => buildAreaChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return (0, import_jsx_runtime.jsx)(LoadingState, {});
  }
  return (0, import_jsx_runtime.jsx)(SceneErrorBoundary, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: (0, import_jsx_runtime.jsx)(LoadingState, {}), children: (0, import_jsx_runtime.jsx)(
    LazyArea3DChartScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
var BarChart3DContext = (0, import_react2.createContext)(false);
function FailedToLoad2() {
  return null;
}
var LazyBar3DChartScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./Bar3DChartScene-VNFWI2QG-SDYOZCWA.js");
    } catch {
      return { default: FailedToLoad2 };
    }
  }
);
var SceneErrorBoundary2 = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function Bar3DChartRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = (0, import_react2.useMemo)(
    () => buildBarChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return (0, import_jsx_runtime.jsx)(LoadingState, {});
  }
  return (0, import_jsx_runtime.jsx)(SceneErrorBoundary2, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: (0, import_jsx_runtime.jsx)(LoadingState, {}), children: (0, import_jsx_runtime.jsx)(
    LazyBar3DChartScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
function buildChart3DPartInteraction({
  element,
  canEdit,
  onUpdateElement,
  selection,
  setSelection,
  setDragValue
}) {
  if (!canEdit) {
    return void 0;
  }
  const onSelect = (part) => {
    if (part) {
      setSelection({ elementId: element.id, part });
    } else if ((selection == null ? void 0 : selection.elementId) === element.id) {
      setSelection(null);
    }
  };
  const onValueDragPreview = (_part, value) => {
    setDragValue(value);
  };
  const onValueDragCommit = (part, value) => {
    setDragValue(null);
    if (!onUpdateElement || !element.chartData || part.pointIndex === void 0) {
      return;
    }
    onUpdateElement({
      chartData: withChartPointValue(element.chartData, part.seriesIndex, part.pointIndex, value)
    });
  };
  return { onSelect, onValueDragPreview, onValueDragCommit };
}
var LineChart3DContext = (0, import_react2.createContext)(false);
function FailedToLoad3() {
  return null;
}
var LazyLine3DChartScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./Line3DChartScene-GXOWGIFJ-UY4SIT2K.js");
    } catch {
      return { default: FailedToLoad3 };
    }
  }
);
var SceneErrorBoundary3 = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function Line3DChartRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = (0, import_react2.useMemo)(
    () => buildLineChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return (0, import_jsx_runtime.jsx)(LoadingState, {});
  }
  return (0, import_jsx_runtime.jsx)(SceneErrorBoundary3, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: (0, import_jsx_runtime.jsx)(LoadingState, {}), children: (0, import_jsx_runtime.jsx)(
    LazyLine3DChartScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
var PieChart3DContext = (0, import_react2.createContext)(false);
function FailedToLoad4() {
  return null;
}
var LazyPieChart3DScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./PieChart3DScene-WXJEDYMK-XOT3OANH.js");
    } catch {
      return { default: FailedToLoad4 };
    }
  }
);
var SceneErrorBoundary4 = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function PieChart3DRenderer({
  element,
  interaction,
  selectedPart
}) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = (0, import_react2.useMemo)(
    () => buildPieChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return (0, import_jsx_runtime.jsx)(LoadingState, {});
  }
  return (0, import_jsx_runtime.jsx)(SceneErrorBoundary4, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: (0, import_jsx_runtime.jsx)(LoadingState, {}), children: (0, import_jsx_runtime.jsx)(
    LazyPieChart3DScene,
    {
      options,
      interaction,
      selectedPart
    }
  ) }) });
}
var SurfaceChart3DContext = (0, import_react2.createContext)(false);
function FailedToLoad5() {
  return null;
}
var LazySurfaceChart3DScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./SurfaceChart3DScene-IKPE2SVE-NYCIZNWH.js");
    } catch {
      return { default: FailedToLoad5 };
    }
  }
);
var SceneErrorBoundary5 = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function SurfaceChart3DRenderer({
  element,
  interaction,
  selectedPart,
  textStyle
}) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const options = (0, import_react2.useMemo)(
    () => buildSurfaceChart3DDataForElement(element, { width: element.width, height: element.height }),
    [element]
  );
  const svgFallback = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: renderChartElement(element) });
  if (!options || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return (0, import_jsx_runtime.jsx)(LoadingState, {});
  }
  return (0, import_jsx_runtime.jsx)(SceneErrorBoundary5, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: (0, import_jsx_runtime.jsx)(LoadingState, {}), children: (0, import_jsx_runtime.jsx)(
    LazySurfaceChart3DScene,
    {
      options,
      interaction,
      selectedPart,
      textStyle
    }
  ) }) });
}
function useChartMarkInteraction({
  element,
  canEdit,
  onUpdateElement,
  viewModel,
  wrapperRef,
  setSelection
}) {
  const dragRef = (0, import_react2.useRef)(null);
  const markDragRef = (0, import_react2.useRef)(null);
  const [previewData, setPreviewData] = (0, import_react2.useState)(null);
  const [dragValue, setDragValue] = (0, import_react2.useState)(null);
  const endDrag = (commit) => {
    const active = dragRef.current;
    const markActive = markDragRef.current;
    dragRef.current = null;
    markDragRef.current = null;
    setPreviewData(null);
    setDragValue(null);
    if (!commit || !onUpdateElement) {
      return;
    }
    if ((active == null ? void 0 : active.moved) && active.lastData) {
      onUpdateElement({ chartData: active.lastData });
    } else if ((markActive == null ? void 0 : markActive.moved) && markActive.lastData) {
      onUpdateElement({ chartData: markActive.lastData });
    }
  };
  (0, import_react2.useEffect)(() => {
    if (dragValue === null) {
      return;
    }
    const onKeyDown = (e) => {
      if (e.key === "Escape") {
        endDrag(false);
      }
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  });
  const handlePointerDown = (e) => {
    var _a, _b2;
    if (!canEdit) {
      return;
    }
    const part = findChartPartTarget(e.target);
    if (!part) {
      return;
    }
    e.stopPropagation();
    setSelection({ elementId: element.id, part });
    if (!viewModel || !element.chartData) {
      return;
    }
    let captured = false;
    const chartKind = resolveChartKind(element.chartData.chartType ?? "bar");
    if (part.pointIndex !== void 0 && chartKind !== "unsupported") {
      const markGeometry = buildChartMarkDragGeometry({
        kind: chartKind,
        element,
        chartData: element.chartData,
        categoryLabels: element.chartData.categories,
        seriesIndex: part.seriesIndex,
        pointIndex: part.pointIndex
      });
      const startedMark = beginChartMarkDrag({
        part,
        geometry: markGeometry,
        chartData: element.chartData,
        svgWidth: viewModel.svgWidth,
        svgHeight: viewModel.svgHeight,
        clientX: e.clientX,
        clientY: e.clientY
      });
      if (startedMark) {
        markDragRef.current = startedMark;
        captured = true;
      }
    }
    if (!captured) {
      const started = beginChartValueDrag({
        part,
        viewModel,
        chartData: element.chartData,
        clientY: e.clientY
      });
      if (started) {
        dragRef.current = started;
        captured = true;
      }
    }
    if (!captured) {
      return;
    }
    e.preventDefault();
    try {
      (_b2 = (_a = e.currentTarget).setPointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
    } catch {
    }
  };
  const handlePointerMove = (e) => {
    var _a, _b2, _c, _d;
    const markActive = markDragRef.current;
    if (markActive) {
      const rect = (_b2 = (_a = wrapperRef.current) == null ? void 0 : _a.querySelector("svg")) == null ? void 0 : _b2.getBoundingClientRect();
      if (!rect) {
        return;
      }
      const step2 = advanceChartMarkDrag(markActive, e.clientX, e.clientY, rect);
      if (!step2) {
        return;
      }
      setPreviewData(step2.chartData);
      setDragValue(step2.value);
      return;
    }
    const active = dragRef.current;
    if (!active) {
      return;
    }
    const height = ((_d = (_c = wrapperRef.current) == null ? void 0 : _c.querySelector("svg")) == null ? void 0 : _d.getBoundingClientRect().height) ?? 0;
    const step = advanceChartValueDrag(active, e.clientY, height);
    if (!step) {
      return;
    }
    setPreviewData(step.chartData);
    setDragValue(step.value);
  };
  const handlePointerUp = () => {
    if (dragRef.current || markDragRef.current) {
      endDrag(true);
    }
  };
  return {
    previewData,
    dragValue,
    setDragValue,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp
  };
}
function ChartElementView({
  element,
  editable,
  onUpdateElement,
  animationState
}) {
  var _a, _b2, _c, _d, _e;
  const wrapperRef = (0, import_react2.useRef)(null);
  const { selection, setSelection } = useChartPartSelection();
  const [titleDraft, setTitleDraft] = (0, import_react2.useState)(null);
  const selectedPart = (selection == null ? void 0 : selection.elementId) === element.id ? selection.part : null;
  const canEdit = editable && Boolean(onUpdateElement);
  const use3D = (0, import_react2.useContext)(SurfaceChart3DContext);
  const isSurfaceKind = resolveChartKind(((_a = element.chartData) == null ? void 0 : _a.chartType) ?? "bar") === "surface";
  const use3DBar = (0, import_react2.useContext)(BarChart3DContext);
  const isBar3DKind = ((_b2 = element.chartData) == null ? void 0 : _b2.chartType) === "bar3D";
  const use3DLine = (0, import_react2.useContext)(LineChart3DContext);
  const isLine3DKind = ((_c = element.chartData) == null ? void 0 : _c.chartType) === "line3D";
  const use3DArea = (0, import_react2.useContext)(AreaChart3DContext);
  const isArea3DKind = ((_d = element.chartData) == null ? void 0 : _d.chartType) === "area3D";
  const use3DPie = (0, import_react2.useContext)(PieChart3DContext);
  const isPie3DKind = ((_e = element.chartData) == null ? void 0 : _e.chartType) === "pie3D";
  const barFacePictureSampleVersion = (0, import_react2.useSyncExternalStore)(
    subscribeBarFacePicturePixelSamples,
    getBarFacePicturePixelSampleVersion,
    getBarFacePicturePixelSampleVersion
  );
  const viewModel = (0, import_react2.useMemo)(() => {
    return canEdit ? buildReactChartViewModel(element) : null;
  }, [canEdit, element, barFacePictureSampleVersion]);
  (0, import_react2.useEffect)(ensureChartInteractionStyles, []);
  (0, import_react2.useEffect)(() => {
    if (!onUpdateElement) {
      return;
    }
    if (!canEdit && (selection == null ? void 0 : selection.elementId) === element.id) {
      setSelection(null);
    }
  }, [canEdit, selection, element.id, setSelection, onUpdateElement]);
  (0, import_react2.useEffect)(() => {
    applyChartPartHighlight(wrapperRef.current, selectedPart);
  });
  const {
    previewData,
    dragValue,
    setDragValue,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp
  } = useChartMarkInteraction({
    element,
    canEdit,
    onUpdateElement,
    viewModel,
    wrapperRef,
    setSelection
  });
  const chart3DInteraction = buildChart3DPartInteraction({
    element,
    canEdit,
    onUpdateElement,
    selection,
    setSelection,
    setDragValue
  });
  const handleDoubleClick = (e) => {
    var _a2;
    if (!canEdit || !canDrillDown(element)) {
      return;
    }
    const target = e.target;
    if (typeof target.closest !== "function") {
      return;
    }
    if (target.closest("[data-chart-part='title']")) {
      e.stopPropagation();
      setTitleDraft(((_a2 = element.chartData) == null ? void 0 : _a2.title) ?? "");
      return;
    }
    if (findChartPartTarget(e.target)) {
      e.stopPropagation();
    }
  };
  const commitTitle = () => {
    if (titleDraft !== null && element.chartData && onUpdateElement) {
      onUpdateElement({
        chartData: withChartTitle(element.chartData, titleDraft)
      });
    }
    setTitleDraft(null);
  };
  const baseChartData = previewData ?? element.chartData;
  const revealedChartData = baseChartData ? resolveRevealedChartData(baseChartData, animationState) : baseChartData;
  const renderedElement = revealedChartData === element.chartData ? element : { ...element, chartData: revealedChartData };
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: wrapperRef,
      className: `relative w-full h-full ${canEdit ? "pptx-chart-interactive" : ""}`,
      onPointerDown: handlePointerDown,
      onPointerMove: handlePointerMove,
      onPointerUp: handlePointerUp,
      onDoubleClick: handleDoubleClick,
      children: [
        use3D && isSurfaceKind ? (0, import_jsx_runtime.jsx)(
          SurfaceChart3DRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState == null ? void 0 : animationState.textStyle
          }
        ) : use3DBar && isBar3DKind ? (0, import_jsx_runtime.jsx)(
          Bar3DChartRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState == null ? void 0 : animationState.textStyle
          }
        ) : use3DLine && isLine3DKind ? (0, import_jsx_runtime.jsx)(
          Line3DChartRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState == null ? void 0 : animationState.textStyle
          }
        ) : use3DArea && isArea3DKind ? (0, import_jsx_runtime.jsx)(
          Area3DChartRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart,
            textStyle: animationState == null ? void 0 : animationState.textStyle
          }
        ) : use3DPie && isPie3DKind ? (0, import_jsx_runtime.jsx)(
          PieChart3DRenderer,
          {
            element: renderedElement,
            interaction: chart3DInteraction,
            selectedPart
          }
        ) : renderChartElement(renderedElement),
        dragValue !== null && (0, import_jsx_runtime.jsx)("div", { className: "absolute top-1 right-1 z-10 rounded bg-primary/90 px-1.5 py-0.5 text-[10px] font-medium text-white pointer-events-none", children: formatAxisValue(dragValue) }),
        titleDraft !== null && (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            autoFocus: true,
            value: titleDraft,
            className: "absolute left-1/2 top-0.5 z-10 w-3/5 -translate-x-1/2 rounded border border-border bg-background px-1 py-0.5 text-center text-[11px] text-foreground shadow",
            onChange: (e) => setTitleDraft(e.target.value),
            onPointerDown: (e) => e.stopPropagation(),
            onDoubleClick: (e) => e.stopPropagation(),
            onKeyDown: (e) => {
              if (e.key === "Enter") {
                commitTitle();
              } else if (e.key === "Escape") {
                setTitleDraft(null);
              }
              e.stopPropagation();
            },
            onBlur: commitTitle
          }
        )
      ]
    }
  );
}
function ColorChangedImage({
  src,
  clrChange,
  tolerancePct,
  style,
  className,
  alt
}) {
  const tolerance = tolerancePct ?? DEFAULT_COLOR_CHANGE_TOLERANCE;
  const cacheKey = buildCacheKey(
    src,
    clrChange.clrFrom,
    clrChange.clrTo,
    tolerance,
    Boolean(clrChange.clrToTransparent)
  );
  const cached = getCachedResult(cacheKey);
  const [processedSrc, setProcessedSrc] = (0, import_react2.useState)(cached ?? null);
  (0, import_react2.useEffect)(() => {
    if (getCachedResult(cacheKey)) {
      setProcessedSrc(getCachedResult(cacheKey) ?? null);
      return;
    }
    let cancelled = false;
    applyColorChange(
      src,
      clrChange.clrFrom,
      clrChange.clrTo,
      tolerance,
      Boolean(clrChange.clrToTransparent)
    ).then((result) => {
      if (!cancelled) {
        setCachedResult(cacheKey, result.dataUrl);
        setProcessedSrc(result.dataUrl);
      }
      return void 0;
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [src, cacheKey, clrChange.clrFrom, clrChange.clrTo, tolerance, clrChange.clrToTransparent]);
  return (0, import_jsx_runtime.jsx)(
    "img",
    {
      src: processedSrc ?? src,
      alt,
      className,
      style,
      draggable: false,
      onError: (e) => {
        e.currentTarget.style.display = "none";
      }
    }
  );
}
function DuotoneImage({
  src,
  duotone,
  style,
  className,
  alt
}) {
  const cacheKey = buildDuotoneCacheKey(src, duotone.color1, duotone.color2);
  const cached = getDuotoneCachedResult(cacheKey);
  const [processedSrc, setProcessedSrc] = (0, import_react2.useState)(cached ?? null);
  (0, import_react2.useEffect)(() => {
    const cachedResult = getDuotoneCachedResult(cacheKey);
    if (cachedResult) {
      setProcessedSrc(cachedResult);
      return;
    }
    let cancelled = false;
    applyDuotone(src, duotone.color1, duotone.color2).then((result) => {
      if (!cancelled) {
        setDuotoneCachedResult(cacheKey, result.dataUrl);
        setProcessedSrc(result.dataUrl);
      }
      return void 0;
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [src, cacheKey, duotone.color1, duotone.color2]);
  return (0, import_jsx_runtime.jsx)(
    "img",
    {
      src: processedSrc ?? src,
      alt,
      className,
      style,
      draggable: false,
      onError: (e) => {
        e.currentTarget.style.display = "none";
      }
    }
  );
}
function renderImageEffectDefinitions(element, excludeDuotone = false) {
  return getImageSvgFilters(element).filter((definition) => !excludeDuotone || definition.id !== `duotone-${element.id}`).map((definition) => (0, import_jsx_runtime.jsx)(
    "svg",
    {
      width: 0,
      height: 0,
      style: { position: "absolute", overflow: "hidden" },
      "aria-hidden": "true",
      children: (0, import_jsx_runtime.jsx)("defs", { children: (0, import_jsx_runtime.jsx)(
        "filter",
        {
          id: definition.id,
          colorInterpolationFilters: "sRGB",
          dangerouslySetInnerHTML: { __html: definition.markup }
        }
      ) })
    },
    definition.id
  ));
}
function imgSrc(el) {
  if ("svgData" in el && el.svgData) {
    return el.svgData;
  }
  return "imageData" in el ? el.imageData : void 0;
}
function renderImg(el, style, filter, alt, opacity) {
  var _a, _b2, _c;
  const src = imgSrc(el);
  if (!src) {
    const imagePath = "imagePath" in el ? el.imagePath : void 0;
    if (imagePath) {
      const ext = (_a = imagePath.split(".").pop()) == null ? void 0 : _a.toLowerCase();
      if (ext === "emf" || ext === "wmf") {
        return (0, import_jsx_runtime.jsxs)("div", { className: "w-full h-full flex flex-col items-center justify-center text-[11px] text-gray-400 bg-gray-50 border border-dashed border-gray-200 rounded", children: [
          (0, import_jsx_runtime.jsxs)(
            "svg",
            {
              xmlns: "http://www.w3.org/2000/svg",
              width: "24",
              height: "24",
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "1.5",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              className: "mb-1 text-gray-300",
              children: [
                (0, import_jsx_runtime.jsx)("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2", ry: "2" }),
                (0, import_jsx_runtime.jsx)("circle", { cx: "8.5", cy: "8.5", r: "1.5" }),
                (0, import_jsx_runtime.jsx)("polyline", { points: "21 15 16 10 5 21" })
              ]
            }
          ),
          (0, import_jsx_runtime.jsxs)("span", { children: [
            ext.toUpperCase(),
            " metafile"
          ] })
        ] });
      }
    }
    return (0, import_jsx_runtime.jsx)("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-gray-500 bg-gray-100", children: translationsEn["pptx.elementType.image"] });
  }
  const effectStyles = {
    ...filter ? { filter } : {},
    ...opacity !== void 0 ? { opacity } : {}
  };
  if (isImageTiled(el)) {
    const tileSrc = imgSrc(el);
    if (!tileSrc) {
      return (0, import_jsx_runtime.jsx)("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-gray-500 bg-gray-100", children: "Image" });
    }
    return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      renderImageEffectDefinitions(el),
      (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: "pointer-events-none select-none w-full h-full",
          style: { ...getImageTilingStyle2(el), ...effectStyles }
        }
      )
    ] });
  }
  const colorWash = getImageColorWashStyle(
    isImageLikeElement(el) ? (_b2 = el.imageEffects) == null ? void 0 : _b2.colorWash : void 0
  );
  const clrChange = "imageEffects" in el ? (_c = el.imageEffects) == null ? void 0 : _c.clrChange : void 0;
  const duotoneColors = getDuotoneColors(el);
  const useDuotoneCanvas = Boolean(duotoneColors && !clrChange);
  const filterForImg = useDuotoneCanvas ? getImageEffectsFilter(el, { excludeDuotone: true }) : filter;
  const canvasEffectStyles = {
    ...filterForImg ? { filter: filterForImg } : {},
    ...opacity !== void 0 ? { opacity } : {}
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    renderImageEffectDefinitions(el, useDuotoneCanvas),
    useDuotoneCanvas && duotoneColors ? (0, import_jsx_runtime.jsx)(
      DuotoneImage,
      {
        src,
        duotone: duotoneColors,
        alt,
        className: "pointer-events-none select-none",
        style: { ...style, ...canvasEffectStyles }
      }
    ) : clrChange ? (0, import_jsx_runtime.jsx)(
      ColorChangedImage,
      {
        src,
        clrChange,
        alt,
        className: "pointer-events-none select-none",
        style: { ...style, ...effectStyles }
      }
    ) : (0, import_jsx_runtime.jsx)(
      "img",
      {
        src,
        alt,
        className: "pointer-events-none select-none",
        style: { ...style, ...effectStyles },
        draggable: false,
        onError: (e) => {
          e.currentTarget.style.display = "none";
        }
      }
    ),
    colorWash && (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "pointer-events-none absolute inset-0",
        style: {
          backgroundColor: colorWash.backgroundColor,
          opacity: colorWash.opacity
        }
      }
    )
  ] });
}
function useActionAffordance(element, canInteract) {
  const { t } = useTranslation();
  ensureActionAffordanceStyles();
  return (0, import_react2.useMemo)(
    () => resolveElementActionAffordance(element, {
      canInteract,
      // Wrapped rather than passed by reference: i18next's `TFunction` is
      // heavily overloaded and does not assign to a plain key -> string.
      labels: actionAffordanceLabels((key) => t(key))
    }),
    [element, canInteract, t]
  );
}
function ActionIndicator({ title }) {
  return (0, import_jsx_runtime.jsx)("div", { className: ACTION_INDICATOR_CLASS, title, children: (0, import_jsx_runtime.jsx)("svg", { viewBox: "0 0 24 24", fill: "currentColor", "aria-hidden": "true", children: (0, import_jsx_runtime.jsx)("path", { d: ACTION_INDICATOR_ICON_PATH }) }) });
}
function LinkTooltip({ label, hint }) {
  return (0, import_jsx_runtime.jsx)("div", { className: LINK_TOOLTIP_CLASS, children: (0, import_jsx_runtime.jsxs)("div", { className: LINK_TOOLTIP_PANEL_CLASS, children: [
    (0, import_jsx_runtime.jsx)("div", { className: LINK_TOOLTIP_LABEL_CLASS, children: label }),
    (0, import_jsx_runtime.jsx)("div", { className: LINK_TOOLTIP_HINT_CLASS, children: hint })
  ] }) });
}
function ActionAffordances({
  affordance
}) {
  if (!affordance.showIndicator && !affordance.showLinkTooltip) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    affordance.showIndicator && (0, import_jsx_runtime.jsx)(ActionIndicator, { title: affordance.indicatorTitle }),
    affordance.showLinkTooltip && (0, import_jsx_runtime.jsx)(LinkTooltip, { label: affordance.linkTooltipLabel, hint: affordance.linkTooltipHint })
  ] });
}
function ConnectorTextOverlay({
  connectorText,
  connectorTextSegments,
  connectorTextStyle
}) {
  if (!connectorText || !connectorTextSegments || connectorTextSegments.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "absolute inset-0 flex items-center justify-center overflow-hidden",
      style: {
        pointerEvents: "none",
        textAlign: ((connectorTextStyle == null ? void 0 : connectorTextStyle.align) === "justLow" || (connectorTextStyle == null ? void 0 : connectorTextStyle.align) === "dist" || (connectorTextStyle == null ? void 0 : connectorTextStyle.align) === "thaiDist" ? "justify" : connectorTextStyle == null ? void 0 : connectorTextStyle.align) ?? "center"
      },
      children: (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: "px-1",
          style: {
            fontFamily: (connectorTextStyle == null ? void 0 : connectorTextStyle.fontFamily) ?? "inherit",
            fontSize: (connectorTextStyle == null ? void 0 : connectorTextStyle.fontSize) ? `${connectorTextStyle.fontSize}px` : "10px",
            color: (connectorTextStyle == null ? void 0 : connectorTextStyle.color) ?? "#000000",
            fontWeight: (connectorTextStyle == null ? void 0 : connectorTextStyle.bold) ? "bold" : "normal",
            fontStyle: (connectorTextStyle == null ? void 0 : connectorTextStyle.italic) ? "italic" : "normal",
            textDecoration: (connectorTextStyle == null ? void 0 : connectorTextStyle.underline) ? "underline" : "none",
            whiteSpace: "pre-wrap",
            lineHeight: 1.2,
            maxWidth: "100%"
          },
          children: connectorTextSegments.map((seg, idx) => {
            var _a, _b2, _c, _d, _e, _f;
            return (0, import_jsx_runtime.jsx)(
              "span",
              {
                style: {
                  fontFamily: ((_a = seg.style) == null ? void 0 : _a.fontFamily) ?? (connectorTextStyle == null ? void 0 : connectorTextStyle.fontFamily) ?? "inherit",
                  fontSize: ((_b2 = seg.style) == null ? void 0 : _b2.fontSize) ? `${seg.style.fontSize}px` : void 0,
                  color: ((_c = seg.style) == null ? void 0 : _c.color) ?? (connectorTextStyle == null ? void 0 : connectorTextStyle.color) ?? "#000000",
                  fontWeight: ((_d = seg.style) == null ? void 0 : _d.bold) ? "bold" : (connectorTextStyle == null ? void 0 : connectorTextStyle.bold) ? "bold" : "normal",
                  fontStyle: ((_e = seg.style) == null ? void 0 : _e.italic) ? "italic" : (connectorTextStyle == null ? void 0 : connectorTextStyle.italic) ? "italic" : "normal",
                  textDecoration: ((_f = seg.style) == null ? void 0 : _f.underline) ? "underline" : "none"
                },
                children: seg.text
              },
              idx
            );
          })
        }
      )
    }
  );
}
var HANDLE_TOUCH_ACTION = { touchAction: "none" };
var CORNER_HANDLES = [
  {
    handle: "nw",
    posClass: "-left-1.5 -top-1.5 max-md:-left-2.5 max-md:-top-2.5",
    cursor: "cursor-nwse-resize"
  },
  {
    handle: "ne",
    posClass: "-right-1.5 -top-1.5 max-md:-right-2.5 max-md:-top-2.5",
    cursor: "cursor-nesw-resize"
  },
  {
    handle: "sw",
    posClass: "-left-1.5 -bottom-1.5 max-md:-left-2.5 max-md:-bottom-2.5",
    cursor: "cursor-nesw-resize"
  },
  {
    handle: "se",
    posClass: "-right-1.5 -bottom-1.5 max-md:-right-2.5 max-md:-bottom-2.5",
    cursor: "cursor-nwse-resize"
  }
];
var EDGE_HANDLES = [
  {
    handle: "n",
    posClass: "top-0 left-1/2 -translate-x-1/2 -translate-y-1/2",
    cursor: "cursor-ns-resize",
    sizeClass: "w-5 h-2 max-md:w-8 max-md:h-3 rounded-sm"
  },
  {
    handle: "s",
    posClass: "bottom-0 left-1/2 -translate-x-1/2 translate-y-1/2",
    cursor: "cursor-ns-resize",
    sizeClass: "w-5 h-2 max-md:w-8 max-md:h-3 rounded-sm"
  },
  {
    handle: "e",
    posClass: "right-0 top-1/2 translate-x-1/2 -translate-y-1/2",
    cursor: "cursor-ew-resize",
    sizeClass: "w-2 h-5 max-md:w-3 max-md:h-8 rounded-sm"
  },
  {
    handle: "w",
    posClass: "left-0 top-1/2 -translate-x-1/2 -translate-y-1/2",
    cursor: "cursor-ew-resize",
    sizeClass: "w-2 h-5 max-md:w-3 max-md:h-8 rounded-sm"
  }
];
function ResizeHandles({
  elementId,
  adjustmentHandles,
  onResizePointerDown,
  onAdjustmentPointerDown,
  forcePointerEvents,
  rotation,
  nonRotationTransform,
  onRotate
}) {
  const { t } = useTranslation();
  const peStyle = forcePointerEvents ? { ...HANDLE_TOUCH_ACTION, pointerEvents: "auto" } : HANDLE_TOUCH_ACTION;
  const handleResizePointer = (e, handle) => {
    var _a, _b2;
    if (e.pointerType === "mouse") {
      return;
    }
    e.stopPropagation();
    (_b2 = (_a = e.currentTarget).setPointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
    onResizePointerDown(elementId, e, handle);
  };
  const startRotate = (btn, pointerId) => {
    var _a;
    const scope = btn.closest("[data-pptx-viewport]") ?? document;
    const wrapper = scope.querySelector(elementIdSelector(elementId));
    if (!wrapper) {
      return;
    }
    const rect = wrapper.getBoundingClientRect();
    const cx = rect.left + rect.width / 2;
    const cy = rect.top + rect.height / 2;
    const base = nonRotationTransform ? ` ${nonRotationTransform}` : "";
    const startDeg = rotation ?? 0;
    let last = startDeg;
    if (pointerId !== void 0) {
      (_a = btn.setPointerCapture) == null ? void 0 : _a.call(btn, pointerId);
    }
    const apply = (clientX, clientY, shift) => {
      let deg = Math.atan2(clientY - cy, clientX - cx) * 180 / Math.PI + 90;
      if (shift) {
        deg = Math.round(deg / 15) * 15;
      }
      deg = Math.round((deg % 360 + 360) % 360);
      last = deg;
      const transform = `rotate(${deg}deg)${base}`;
      wrapper.style.transform = transform;
      syncSelectionHandleOverlay(wrapper, elementId, { transform });
    };
    const onPointerMove = (ev) => apply(ev.clientX, ev.clientY, ev.shiftKey);
    const end = () => {
      window.removeEventListener("pointermove", onPointerMove);
      window.removeEventListener("pointerup", end);
      window.removeEventListener("pointercancel", end);
      if (last !== startDeg) {
        onRotate == null ? void 0 : onRotate(elementId, last);
      }
    };
    window.addEventListener("pointermove", onPointerMove);
    window.addEventListener("pointerup", end);
    window.addEventListener("pointercancel", end);
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    CORNER_HANDLES.map(({ handle, posClass, cursor }) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.selectionOverlay.resize", { handle }),
        className: cn("absolute z-10 group", posClass, cursor),
        style: peStyle,
        onPointerDown: (e) => handleResizePointer(e, handle),
        onMouseDown: (e) => {
          e.stopPropagation();
          onResizePointerDown(elementId, e, handle);
        },
        children: [
          (0, import_jsx_runtime.jsx)("div", { className: "w-3 h-3 max-md:w-5.5 max-md:h-5.5 rounded-full border border-white bg-primary shadow" }),
          (0, import_jsx_runtime.jsx)("div", { className: "absolute -inset-1.5 max-md:-inset-1" })
        ]
      },
      handle
    )),
    EDGE_HANDLES.map(({ handle, posClass, cursor, sizeClass }) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.selectionOverlay.resize", { handle }),
        className: cn("absolute z-10", posClass, cursor),
        style: peStyle,
        onPointerDown: (e) => handleResizePointer(e, handle),
        onMouseDown: (e) => {
          e.stopPropagation();
          onResizePointerDown(elementId, e, handle);
        },
        children: [
          (0, import_jsx_runtime.jsx)("div", { className: cn(sizeClass, "border border-white bg-primary shadow") }),
          (0, import_jsx_runtime.jsx)("div", { className: "absolute -inset-2 max-md:-inset-1" })
        ]
      },
      handle
    )),
    onRotate ? (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.selectionOverlay.rotate"),
        "data-pptx-compact": true,
        className: "absolute left-1/2 top-0 -translate-x-1/2 -translate-y-1/2 z-20 flex items-center justify-center w-5 h-5 max-md:w-7 max-md:h-7 rounded-full border border-white bg-primary text-white shadow cursor-grab active:cursor-grabbing",
        style: peStyle,
        onPointerDown: (e) => {
          if (e.pointerType === "mouse") {
            return;
          }
          e.stopPropagation();
          startRotate(e.currentTarget, e.pointerId);
        },
        onMouseDown: (e) => {
          e.stopPropagation();
          startRotate(e.currentTarget);
        },
        children: [
          (0, import_jsx_runtime.jsx)(LuRotateCw, { className: "w-3 h-3 max-md:w-4 max-md:h-4" }),
          (0, import_jsx_runtime.jsx)("span", { className: "absolute -inset-2 max-md:-inset-1", "aria-hidden": "true" })
        ]
      }
    ) : null,
    adjustmentHandles.map((adjH) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.canvas.adjustShape"),
        "data-pptx-adjust-key": adjH.key,
        "data-pptx-compact": true,
        className: "absolute h-2.5 w-2.5 max-md:h-4 max-md:w-4 rotate-45 border border-amber-700 bg-amber-300 shadow z-10",
        style: {
          left: adjH.left - 5,
          top: adjH.top - 5,
          cursor: adjH.cursor,
          ...HANDLE_TOUCH_ACTION,
          ...forcePointerEvents ? { pointerEvents: "auto" } : {}
        },
        onPointerDown: (e) => {
          var _a, _b2;
          if (e.pointerType === "mouse") {
            return;
          }
          e.stopPropagation();
          (_b2 = (_a = e.currentTarget).setPointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
          onAdjustmentPointerDown(elementId, e, adjH);
        },
        onMouseDown: (e) => {
          e.stopPropagation();
          onAdjustmentPointerDown(elementId, e, adjH);
        }
      },
      adjH.key
    ))
  ] });
}
var ConnectorElementRenderer = import_react2.default.memo(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the memo component its displayName
  function ConnectorElementRendererInner({
    el,
    isSelected,
    canInteract,
    showResizeHandles,
    showHoverBorder,
    selectionColorClass: selClr,
    opacity,
    zIndex,
    adjustmentHandles: adjH,
    onResizePointerDown,
    onAdjustmentPointerDown,
    animationState,
    textStyleOverrideCss
  }) {
    var _a;
    const shapeEl = hasShapeProperties(el) ? el : void 0;
    const boxWidth = Math.max(el.width, MIN_ELEMENT_SIZE);
    const boxHeight = Math.max(el.height, MIN_ELEMENT_SIZE);
    const viewWidth = Math.max(el.width, 0);
    const viewHeight = Math.max(el.height, 0);
    const ss = shapeEl == null ? void 0 : shapeEl.shapeStyle;
    const strokeWidth = Math.max(0, (ss == null ? void 0 : ss.strokeWidth) ?? 2);
    const strokeColor = normalizeHexColor2(ss == null ? void 0 : ss.strokeColor, DEFAULT_STROKE_COLOR);
    const strokePaint = colorWithOpacity(strokeColor, ss == null ? void 0 : ss.strokeOpacity);
    const dashType = normalizeStrokeDashType(ss == null ? void 0 : ss.strokeDash);
    const dashArray = getSvgStrokeDasharray(
      dashType,
      Math.max(strokeWidth, 1),
      ss == null ? void 0 : ss.customDashSegments
    );
    const startArrow = ss == null ? void 0 : ss.connectorStartArrow;
    const endArrow = ss == null ? void 0 : ss.connectorEndArrow;
    const compoundLine = ss == null ? void 0 : ss.compoundLine;
    const markerSeed = el.id.replace(/[^a-zA-Z0-9_-]/gu, "_");
    const startMarkerId = `${markerSeed}-sel-start`;
    const endMarkerId = `${markerSeed}-sel-end`;
    const compoundOffsets = getCompoundLineOffsets(compoundLine, strokeWidth);
    const compoundWidths = getCompoundLineWidths(compoundLine, strokeWidth);
    const strokeCap = svgLineCap(ss == null ? void 0 : ss.lineCap);
    const textEl = hasTextProperties(el) ? el : void 0;
    const connectorText = ((_a = textEl == null ? void 0 : textEl.text) == null ? void 0 : _a.trim()) ?? "";
    const connectorTextSegments = textEl == null ? void 0 : textEl.textSegments;
    const connectorTextStyle = textEl == null ? void 0 : textEl.textStyle;
    const pathGeometry = shapeEl ? getConnectorPathGeometry(shapeEl) : {
      pathData: `M 0 0 L ${viewWidth} ${viewHeight}`,
      startX: 0,
      startY: 0,
      endX: viewWidth,
      endY: viewHeight
    };
    const hitTargetWidth = Math.max(strokeWidth * 3, 14);
    const selColor = selClr === "blue-400" ? "#60a5fa" : "#3b82f6";
    const lineShadow = buildLineShadowCss(el);
    const lineGlow = buildLineGlowFilter(el);
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        "data-pptx-element": "true",
        "data-element-id": el.id,
        role: getAriaRole(el, { actionable: false }),
        "aria-label": getAriaLabel(el),
        "aria-roledescription": getAriaRoleDescription(el),
        "aria-selected": isSelected ? true : void 0,
        className: "absolute",
        style: {
          left: el.x,
          top: el.y,
          width: boxWidth,
          height: boxHeight,
          transform: connectorWrapperTransform(el),
          transformOrigin: "center",
          background: "transparent",
          border: "none",
          pointerEvents: "none",
          opacity,
          zIndex,
          visibility: (animationState == null ? void 0 : animationState.visible) === false ? "hidden" : "visible",
          animation: animationState == null ? void 0 : animationState.cssAnimation,
          ...lineGlow ? { filter: lineGlow } : {}
        },
        children: [
          textStyleOverrideCss && (0, import_jsx_runtime.jsx)("style", { children: textStyleOverrideCss }),
          (0, import_jsx_runtime.jsxs)(
            "svg",
            {
              viewBox: `0 0 ${boxWidth} ${boxHeight}`,
              className: "w-full h-full",
              preserveAspectRatio: "none",
              style: { overflow: "visible", pointerEvents: "none" },
              children: [
                (0, import_jsx_runtime.jsxs)("defs", { children: [
                  renderConnectorMarker(
                    startMarkerId,
                    startArrow,
                    strokePaint,
                    ss == null ? void 0 : ss.connectorStartArrowWidth,
                    ss == null ? void 0 : ss.connectorStartArrowLength
                  ),
                  renderConnectorMarker(
                    endMarkerId,
                    endArrow,
                    strokePaint,
                    ss == null ? void 0 : ss.connectorEndArrowWidth,
                    ss == null ? void 0 : ss.connectorEndArrowLength
                  ),
                  lineShadow && (0, import_jsx_runtime.jsx)("filter", { id: `${markerSeed}-line-shadow`, x: "-50%", y: "-50%", width: "200%", height: "200%", children: (0, import_jsx_runtime.jsx)(
                    "feDropShadow",
                    {
                      dx: (ss == null ? void 0 : ss.lineShadowOffsetX) ?? 2,
                      dy: (ss == null ? void 0 : ss.lineShadowOffsetY) ?? 2,
                      stdDeviation: Math.max(0, ((ss == null ? void 0 : ss.lineShadowBlur) ?? 4) / 2),
                      floodColor: (ss == null ? void 0 : ss.lineShadowColor) ?? "#000000",
                      floodOpacity: (ss == null ? void 0 : ss.lineShadowOpacity) ?? 0.35
                    }
                  ) })
                ] }),
                isSelected && (0, import_jsx_runtime.jsx)(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: selColor,
                    strokeWidth: Math.max(strokeWidth, 2) + 6,
                    strokeOpacity: 0.35,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    vectorEffect: "non-scaling-stroke",
                    style: { pointerEvents: "none" }
                  }
                ),
                !isSelected && showHoverBorder && (0, import_jsx_runtime.jsx)(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: "#93c5fd",
                    strokeWidth: Math.max(strokeWidth, 2) + 4,
                    strokeOpacity: 0,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    vectorEffect: "non-scaling-stroke",
                    className: "transition-[stroke-opacity] duration-150 group-hover:stroke-opacity-40",
                    style: { pointerEvents: "none" }
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: "transparent",
                    strokeWidth: hitTargetWidth,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    style: {
                      pointerEvents: "stroke",
                      cursor: canInteract ? "move" : "default"
                    }
                  }
                ),
                compoundOffsets.map((offset, idx) => (0, import_jsx_runtime.jsx)(
                  "path",
                  {
                    d: pathGeometry.pathData,
                    fill: "none",
                    stroke: strokePaint,
                    strokeWidth: Math.max(compoundWidths[idx] ?? strokeWidth, 1),
                    strokeDasharray: dashArray,
                    strokeLinecap: strokeCap,
                    strokeLinejoin: "round",
                    markerStart: idx === 0 && startArrow && startArrow !== "none" ? `url(#${startMarkerId})` : void 0,
                    markerEnd: idx === compoundOffsets.length - 1 && endArrow && endArrow !== "none" ? `url(#${endMarkerId})` : void 0,
                    vectorEffect: "non-scaling-stroke",
                    filter: idx === 0 && lineShadow ? `url(#${markerSeed}-line-shadow)` : void 0,
                    style: {
                      pointerEvents: "none",
                      ...offset !== 0 ? {
                        transform: `translate(0, ${offset}px)`
                      } : {}
                    }
                  },
                  idx
                )),
                isSelected && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                  (0, import_jsx_runtime.jsx)(
                    "circle",
                    {
                      cx: pathGeometry.startX,
                      cy: pathGeometry.startY,
                      r: 4,
                      fill: selColor,
                      stroke: "white",
                      strokeWidth: 1.5,
                      style: { pointerEvents: "none" }
                    }
                  ),
                  (0, import_jsx_runtime.jsx)(
                    "circle",
                    {
                      cx: pathGeometry.endX,
                      cy: pathGeometry.endY,
                      r: 4,
                      fill: selColor,
                      stroke: "white",
                      strokeWidth: 1.5,
                      style: { pointerEvents: "none" }
                    }
                  )
                ] })
              ]
            }
          ),
          connectorTextSegments && (0, import_jsx_runtime.jsx)(
            ConnectorTextOverlay,
            {
              connectorText,
              connectorTextSegments,
              connectorTextStyle
            }
          ),
          showResizeHandles && (0, import_jsx_runtime.jsx)(
            ResizeHandles,
            {
              elementId: el.id,
              adjustmentHandles: adjH,
              onResizePointerDown,
              onAdjustmentPointerDown,
              forcePointerEvents: true
            }
          )
        ]
      }
    );
  }
);
ConnectorElementRenderer.displayName = "ConnectorElementRenderer";
function getElementInteractionProps({
  element,
  isEditableText,
  canInteract,
  isInlineEditing,
  isActionable,
  isPresentationPassive,
  onInlineEditCancel,
  onActionClick
}) {
  var _a, _b2, _c;
  const hasHoverAction = Boolean(element.actionHover);
  const highlight = resolveElementHighlightClick(element.actionClick, element.actionHover);
  const hoverLeaveStyle = (_a = highlight.hover) == null ? void 0 : _a.leaveStyle;
  return {
    onKeyDown: (event) => {
      if (event.key === "Enter" && isEditableText && canInteract && !isInlineEditing) {
        event.preventDefault();
        event.stopPropagation();
        event.currentTarget.dispatchEvent(new MouseEvent("dblclick", { bubbles: true }));
      } else if (event.key === "Escape" && isInlineEditing) {
        event.preventDefault();
        event.stopPropagation();
        onInlineEditCancel();
      } else if ((event.key === "Enter" || event.key === " ") && isActionable) {
        event.preventDefault();
        event.stopPropagation();
        event.currentTarget.click();
      }
    },
    onClick: (event) => {
      if (!element.actionClick || !onActionClick) {
        return;
      }
      const shouldTrigger = !canInteract || event.ctrlKey || event.metaKey;
      if (!shouldTrigger) {
        return;
      }
      event.stopPropagation();
      event.preventDefault();
      if (highlight.click) {
        const target = event.currentTarget;
        const { style, clearStyle, durationMs } = highlight.click;
        applyHighlightClickStyle(target, style);
        window.setTimeout(() => {
          applyHighlightClickStyle(target, clearStyle);
        }, durationMs);
      }
      onActionClick(element.id, element.actionClick);
    },
    onMouseEnter: (event) => {
      if (highlight.hover) {
        applyHighlightClickStyle(event.currentTarget, highlight.hover.enterStyle);
      }
      if (isPresentationPassive && element.actionHover && onActionClick && (element.actionHover.url || element.actionHover.targetSlideIndex !== void 0)) {
        onActionClick(element.id, element.actionHover);
      }
    },
    onMouseLeave: hasHoverAction && hoverLeaveStyle ? (event) => {
      applyHighlightClickStyle(event.currentTarget, hoverLeaveStyle);
    } : void 0,
    title: canInteract && element.actionClick ? void 0 : ((_b2 = element.actionClick) == null ? void 0 : _b2.tooltip) || ((_c = element.actionHover) == null ? void 0 : _c.tooltip) || void 0
  };
}
function renderDagDuotoneFilterForElement(el) {
  var _a;
  if (!hasDagDuotoneEffect(el)) {
    return null;
  }
  const duotone = (_a = el.shapeStyle) == null ? void 0 : _a.dagDuotone;
  if (!duotone) {
    return null;
  }
  return renderDagDuotoneSvgFilter(el.id, duotone.color1, duotone.color2);
}
function getContainerStyle({
  el,
  isFullscreenMedia,
  isImg,
  zIndex,
  opacity,
  animationState,
  shapeVisualStyle,
  has3DExtrusion,
  templateEditing
}) {
  const ss = hasShapeProperties(el) ? el.shapeStyle : void 0;
  const isPicture = el.type === "picture" || el.type === "image";
  const blurGrowVisible = Boolean(
    (ss == null ? void 0 : ss.blurGrow) && typeof ss.blurRadius === "number" && ss.blurRadius > 0
  );
  const overflowValue = has3DExtrusion || blurGrowVisible || isPicture ? "visible" : isImg || el.type === "media" ? "hidden" : void 0;
  return {
    left: isFullscreenMedia ? 0 : el.x,
    top: isFullscreenMedia ? 0 : el.y,
    width: isFullscreenMedia ? "100%" : Math.max(el.width, MIN_ELEMENT_SIZE),
    height: isFullscreenMedia ? "100%" : Math.max(el.height, MIN_ELEMENT_SIZE),
    transform: isFullscreenMedia ? "none" : getElementTransform(el),
    transformOrigin: "center",
    overflow: overflowValue,
    clipPath: isPicture ? void 0 : isImg && !has3DExtrusion ? getCropShapeClipPath2(el) : void 0,
    zIndex: isFullscreenMedia ? 20 : zIndex,
    visibility: (animationState == null ? void 0 : animationState.visible) === false ? "hidden" : "visible",
    animation: animationState == null ? void 0 : animationState.cssAnimation,
    background: isFullscreenMedia ? "#000" : void 0,
    transition: isFullscreenMedia ? "left 0.3s ease, top 0.3s ease, width 0.3s ease, height 0.3s ease" : void 0,
    borderColor: isFullscreenMedia ? "transparent" : void 0,
    ...shapeVisualStyle,
    ...isPicture ? {
      backgroundColor: "transparent",
      backgroundImage: void 0,
      backgroundRepeat: void 0,
      backgroundSize: void 0,
      backgroundPosition: void 0,
      borderRadius: void 0,
      clipPath: void 0,
      overflow: "visible"
    } : {},
    // COMPOSE the effect alpha (`a:alphaModFix` on the effect DAG) with the
    // element opacity instead of letting the spread clobber it. The other four
    // bindings multiply the two; React spread the shape style over its own
    // `opacity` key, so a half-transparent element carrying a DAG alpha
    // rendered at the DAG's alpha alone.
    opacity: typeof shapeVisualStyle.opacity === "number" ? (opacity ?? 1) * shapeVisualStyle.opacity : opacity,
    // Editable-template affordance: a distinct amber dashed ring + slight
    // transparency so inherited master/layout shapes read as "template" while
    // edit-template mode is on. Applied after the shape style so it wins; never
    // set for normal slide content or while the mode is off.
    ...templateEditing ? { outline: "2px dashed rgb(217, 119, 6)", outlineOffset: "1px", opacity: opacity ?? 0.95 } : {}
  };
}
function shapeParams(element) {
  const style = hasShapeProperties(element) ? element.shapeStyle : void 0;
  const strokeWidth = paintedStrokeWidth(style);
  const strokeColor = normalizeHexColor2(style == null ? void 0 : style.strokeColor, DEFAULT_STROKE_COLOR);
  const fillColor = normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR);
  const hasFill = (style == null ? void 0 : style.fillColor) !== void 0 && style.fillColor !== "transparent" || Boolean(buildCssGradientFromShapeStyle(style) || (style == null ? void 0 : style.fillGradient)) || (style == null ? void 0 : style.fillMode) === "pattern" && Boolean(style.fillPatternPreset);
  return { hf: hasFill, fc: fillColor, sw: strokeWidth, sc: strokeColor };
}
var Extrusion3DOverlay = import_react2.default.memo(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the memo component its displayName
  function Extrusion3DOverlayInner({ data }) {
    if (!data.hasExtrusion || data.panels.length === 0) {
      return null;
    }
    return (0, import_jsx_runtime.jsxs)("div", { className: "extrusion-3d-wrapper", style: data.wrapperStyle, "aria-hidden": "true", children: [
      data.panels.map((panel) => (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: `extrusion-3d-panel extrusion-3d-panel--${panel.side}`,
          style: panel.style
        },
        panel.side
      )),
      data.materialOverlay && (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: "extrusion-3d-material-overlay",
          style: {
            position: "absolute",
            inset: 0,
            backgroundImage: data.materialOverlay,
            pointerEvents: "none",
            borderRadius: "inherit",
            transform: data.frontFaceStyle.transform,
            transformStyle: "preserve-3d",
            backfaceVisibility: "hidden",
            mixBlendMode: "normal"
          }
        }
      )
    ] });
  }
);
Extrusion3DOverlay.displayName = "Extrusion3DOverlay";
function getScopedElementHandlers(elementId, options) {
  const cellSelectHandler = options.onTableCellSelect ? (cell) => {
    var _a;
    return (_a = options.onTableCellSelect) == null ? void 0 : _a.call(options, cell, elementId);
  } : void 0;
  const cellCommitHandler = options.onCommitCellEdit ? (rowIndex, colIndex, text) => {
    var _a;
    return (_a = options.onCommitCellEdit) == null ? void 0 : _a.call(options, elementId, rowIndex, colIndex, text);
  } : void 0;
  const colResizeHandler = options.onResizeTableColumns ? (newWidths) => {
    var _a;
    return (_a = options.onResizeTableColumns) == null ? void 0 : _a.call(options, elementId, newWidths);
  } : void 0;
  const rowResizeHandler = options.onResizeTableRow ? (rowIndex, newHeight) => {
    var _a;
    return (_a = options.onResizeTableRow) == null ? void 0 : _a.call(options, elementId, rowIndex, newHeight);
  } : void 0;
  const smartArtUpdateHandler = options.onUpdateSmartArtElement ? (updates) => {
    var _a;
    return (_a = options.onUpdateSmartArtElement) == null ? void 0 : _a.call(options, elementId, updates);
  } : void 0;
  return {
    cellSelectHandler,
    cellCommitHandler,
    colResizeHandler,
    rowResizeHandler,
    smartArtUpdateHandler
  };
}
function renderOutlinePaint(paint) {
  if (paint.kind === "pattern") {
    return (0, import_jsx_runtime.jsx)(
      "pattern",
      {
        id: paint.id,
        width: paint.width,
        height: paint.height,
        patternUnits: "userSpaceOnUse",
        children: (0, import_jsx_runtime.jsx)("image", { href: paint.href, width: paint.width, height: paint.height })
      }
    );
  }
  if (paint.kind === "rectPath") {
    return (0, import_jsx_runtime.jsx)("pattern", { id: paint.id, patternUnits: "objectBoundingBox", width: 1, height: 1, children: (0, import_jsx_runtime.jsx)("image", { href: paint.href, x: 0, y: 0, width: 1, height: 1, preserveAspectRatio: "none" }) });
  }
  const stops = paint.stops.map((stop, idx) => (0, import_jsx_runtime.jsx)(
    "stop",
    {
      offset: stop.offset,
      stopColor: stop.color,
      stopOpacity: typeof stop.opacity === "number" ? stop.opacity : void 0
    },
    idx
  ));
  if (paint.kind === "radial") {
    return (0, import_jsx_runtime.jsx)("radialGradient", { id: paint.id, cx: paint.cx, cy: paint.cy, r: paint.r, children: stops });
  }
  return (0, import_jsx_runtime.jsx)("linearGradient", { id: paint.id, x1: paint.x1, y1: paint.y1, x2: paint.x2, y2: paint.y2, children: stops });
}
function ShapeEffectOverlay({
  element,
  animatesFill = false,
  animatesStroke = false,
  suppressReflection = false
}) {
  const fx = getComputedEffectStyle(element);
  const overlay = fx.fillOverlay;
  const reflection = suppressReflection ? void 0 : fx.reflection;
  const softEdge = getSoftEdgeSvgFilter(getEffectStyleSource(element), element.id);
  const strokeOutline = buildStrokeOutline(element);
  const hollowHit = buildHollowHitOutline(element);
  const subpathFill = buildSubpathFillOverlay(element);
  if (!overlay && !softEdge && !strokeOutline && !hollowHit && !subpathFill && !reflection) {
    return null;
  }
  const fillOverlayStyle = overlay ? {
    position: "absolute",
    inset: 0,
    ...isImageLikeElement(element) ? getImageSurfaceMaskStyle(element) : {},
    background: overlay.color,
    mixBlendMode: overlay.blendMode,
    pointerEvents: "none"
  } : void 0;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    subpathFill ? (0, import_jsx_runtime.jsx)(
      "svg",
      {
        className: "pptx-react-subpath-fill",
        "aria-hidden": "true",
        viewBox: `0 0 ${subpathFill.viewBoxWidth} ${subpathFill.viewBoxHeight}`,
        preserveAspectRatio: "none",
        style: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%"
        },
        children: subpathFill.paints.map((paint, idx) => (0, import_jsx_runtime.jsx)(
          "path",
          {
            d: paint.d,
            fill: animatesFill ? "inherit" : paint.fill,
            stroke: "none"
          },
          idx
        ))
      }
    ) : null,
    softEdge ? (0, import_jsx_runtime.jsx)(
      "svg",
      {
        width: 0,
        height: 0,
        "aria-hidden": "true",
        style: {
          position: "absolute",
          width: 0,
          height: 0,
          overflow: "hidden"
        },
        children: (0, import_jsx_runtime.jsx)("defs", { dangerouslySetInnerHTML: { __html: softEdge.filterMarkup } })
      }
    ) : null,
    fillOverlayStyle ? (0, import_jsx_runtime.jsx)("div", { className: "pptx-react-fill-overlay", "aria-hidden": "true", style: fillOverlayStyle }) : null,
    strokeOutline ? (0, import_jsx_runtime.jsxs)(
      "svg",
      {
        className: "pptx-react-gradient-outline",
        "aria-hidden": "true",
        viewBox: strokeOutlineViewBox(element),
        preserveAspectRatio: "none",
        style: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%",
          overflow: "visible",
          pointerEvents: "none"
        },
        children: [
          strokeOutline.paint ? (0, import_jsx_runtime.jsx)("defs", { children: renderOutlinePaint(strokeOutline.paint) }) : null,
          strokeOutline.strands.map((strand, idx) => (0, import_jsx_runtime.jsx)(
            "path",
            {
              d: strokeOutline.d,
              fill: "none",
              stroke: animatesStroke ? "inherit" : strokeOutline.stroke,
              strokeWidth: strand.strokeWidth,
              strokeDasharray: strokeOutline.dashArray,
              strokeLinecap: strokeOutline.lineCap,
              strokeLinejoin: strokeOutline.lineJoin,
              style: strand.offset !== 0 ? { transform: `translate(0, ${strand.offset}px)` } : void 0
            },
            idx
          ))
        ]
      }
    ) : null,
    hollowHit ? (0, import_jsx_runtime.jsx)(
      "svg",
      {
        "aria-hidden": "true",
        viewBox: strokeOutlineViewBox(element),
        preserveAspectRatio: "none",
        style: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%",
          overflow: "visible",
          pointerEvents: "none"
        },
        children: (0, import_jsx_runtime.jsx)(
          "path",
          {
            d: hollowHit.d,
            fill: "none",
            stroke: "transparent",
            strokeWidth: hollowHit.strokeWidth,
            style: { pointerEvents: "stroke" }
          }
        )
      }
    ) : null,
    reflection ? (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "pptx-react-reflection",
        "aria-hidden": "true",
        style: reflection,
        children: (0, import_jsx_runtime.jsx)(StaticElementRenderer, { element, positioned: false, suppressReflection: true })
      }
    ) : null
  ] });
}
var ElementRenderer = import_react2.default.memo(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the memo component its displayName
  function ElementRendererInner({
    element: el,
    activeSlide,
    isSelected,
    isInlineEditing,
    inlineEditingText,
    canInteract,
    presenting,
    spellCheckEnabled,
    mediaDataUrls,
    tableEditorState,
    selectionColorClass: selClr,
    showHoverBorder,
    opacity,
    templateEditing,
    zIndex,
    imageAltText,
    showResizeHandles,
    renderInk: doInk,
    renderGroups: doGrp,
    adjustmentHandles: adjH,
    onResizePointerDown,
    onAdjustmentPointerDown,
    onInlineEditChange,
    onInlineEditCommit,
    onInlineEditCancel,
    onTableCellSelect,
    onCommitCellEdit,
    onUpdateSmartArtElement,
    onFormatText,
    onResizeTableColumns,
    onResizeTableRow,
    findHighlights,
    onActionClick,
    onHyperlinkClick,
    animationState,
    presentationElementStates,
    allSlides,
    onZoomClick,
    sourceSlideIndex,
    fieldContext,
    tableStyleContext
  }) {
    const {
      cellSelectHandler,
      cellCommitHandler,
      colResizeHandler,
      rowResizeHandler,
      smartArtUpdateHandler
    } = getScopedElementHandlers(el.id, {
      onTableCellSelect,
      onCommitCellEdit,
      onResizeTableColumns,
      onResizeTableRow,
      onUpdateSmartArtElement
    });
    const chartUpdateHandler = smartArtUpdateHandler;
    const { hf, fc, sw, sc } = shapeParams(el);
    const allow = resolveElementInteractivity(el);
    const backgroundAnimationState = presentationElementStates == null ? void 0 : presentationElementStates.get(`${el.id}::pptx-bg`);
    const visualAnimationState = backgroundAnimationState ?? animationState;
    const isTxt = isEditableTextElement(el) && allow.textEditable;
    const txtSE = hasTextProperties(el) ? el.textStyle : void 0;
    const textStyleOverrideCss = buildTextStyleOverrideCss(el.id, visualAnimationState == null ? void 0 : visualAnimationState.textStyle);
    const ss = getShapeVisualStyle(
      el,
      hf,
      fc,
      sw,
      sc,
      visualAnimationState == null ? void 0 : visualAnimationState.animatesFill,
      visualAnimationState == null ? void 0 : visualAnimationState.animatesStroke
    );
    const ts = getTextStyleForElement(el, DEFAULT_TEXT_COLOR);
    const isImg = el.type === "picture" || el.type === "image";
    const vs = renderVectorShape(
      el,
      isImg ? false : hf,
      fc,
      sw,
      sc,
      visualAnimationState == null ? void 0 : visualAnimationState.animatesFill,
      visualAnimationState == null ? void 0 : visualAnimationState.animatesStroke
    );
    const isModel3D = el.type === "model3d";
    const isConn = isConnectorOrLineElement(el);
    const shapeStyle3d = hasShapeProperties(el) ? el.shapeStyle : void 0;
    const extrusionData = (0, import_react2.useMemo)(
      () => build3DExtrusionData2(shapeStyle3d == null ? void 0 : shapeStyle3d.shape3d, shapeStyle3d == null ? void 0 : shapeStyle3d.scene3d, fc, el.width, el.height),
      [shapeStyle3d == null ? void 0 : shapeStyle3d.shape3d, shapeStyle3d == null ? void 0 : shapeStyle3d.scene3d, fc, el.width, el.height]
    );
    const actionAffordance = useActionAffordance(el, canInteract);
    const [isMediaPlaying, setIsMediaPlaying] = (0, import_react2.useState)(false);
    const handleMediaPlayStateChange = (0, import_react2.useCallback)((playing) => {
      setIsMediaPlaying(playing);
    }, []);
    if (!isElementRendered(el)) {
      return null;
    }
    if (isConn) {
      return (0, import_jsx_runtime.jsx)(
        ConnectorElementRenderer,
        {
          el,
          isSelected,
          canInteract,
          showResizeHandles: showResizeHandles && allow.resizable,
          showHoverBorder,
          selectionColorClass: selClr,
          opacity,
          zIndex,
          adjustmentHandles: adjH,
          onResizePointerDown,
          onAdjustmentPointerDown,
          animationState,
          textStyleOverrideCss
        }
      );
    }
    const effectiveCanInteract = canInteract && allow.selectable;
    const effectiveIsInlineEditing = isInlineEditing && allow.textEditable;
    const canEditSmartArt = effectiveCanInteract && allow.textEditable;
    const canEditChart = effectiveCanInteract;
    const hasAction = Boolean(el.actionClick && onActionClick);
    const isZoom = el.type === "zoom" && Boolean(onZoomClick);
    const aria = resolveElementAriaAttributes(el, {
      hasActionHandler: Boolean(onActionClick),
      hasHyperlinkHandler: Boolean(onHyperlinkClick),
      hasZoomHandler: Boolean(onZoomClick)
    });
    const isActionable = aria.actionable;
    const selB = isSelected ? `outline-1 -outline-offset-1 outline-${selClr} ring-2 ring-${selClr}/50` : showHoverBorder ? "outline-1 -outline-offset-1 outline-transparent hover:outline-primary/40" : "";
    const cur = effectiveIsInlineEditing ? "cursor-text" : effectiveCanInteract ? !allow.movable ? "cursor-default" : "cursor-move" : hasAction || isZoom ? "cursor-pointer" : "";
    const isPresentationPassive = presenting === true;
    const isFullscreenMedia = el.type === "media" && Boolean(el.fullScreen) && isPresentationPassive && isMediaPlaying;
    const isFocusable = effectiveCanInteract || isActionable;
    const interactionProps = getElementInteractionProps({
      element: el,
      isEditableText: isTxt,
      canInteract: effectiveCanInteract,
      isInlineEditing: effectiveIsInlineEditing,
      isActionable,
      isPresentationPassive,
      onInlineEditCancel,
      onActionClick
    });
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        "data-pptx-element": "true",
        "data-element-id": el.id,
        "data-pptx-action": isActionable ? "click" : void 0,
        role: aria.role,
        "aria-label": aria.label,
        "aria-roledescription": aria.roleDescription,
        "aria-hidden": aria.hidden ? true : void 0,
        "aria-selected": isSelected ? true : void 0,
        tabIndex: isFocusable ? 0 : -1,
        className: cn(
          "absolute",
          "focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-500",
          cur,
          // During a show this must stay EMPTY: `PRESENTATION_HIT_TEST_CSS`
          // (injected by `PresentationStage`) owns hit-testing there, because
          // it is the only form that can re-enable an action shape nested
          // inside inert scenery - an inline `pointer-events: none` on the
          // group could never do that, and React was the one binding that
          // wrote it. Off the show stage the inline rule still applies.
          inlineElementPointerEvents({
            interactive: effectiveCanInteract || isActionable,
            presenting: presenting === true
          }) === "none" ? "pointer-events-none" : "",
          // An unfilled, textless shape is a FRAME: PowerPoint hit-tests it on its
          // outline only, so its interior must not swallow clicks meant for what it
          // is drawn over. ShapeEffectOverlay paints a transparent
          // pointer-events:stroke band that opts the outline back in.
          isHollowShapeElement(el) ? "pointer-events-none" : "",
          isFullscreenMedia ? "pointer-events-auto" : "",
          selB,
          // Shared class the tooltip's `:hover` rule keys off (see
          // `ACTION_AFFORDANCE_CSS`), replacing React's Tailwind `group/link`
          // so the four non-Tailwind bindings reveal it the same way.
          actionAffordance.showLinkTooltip && LINK_TOOLTIP_HOST_CLASS
        ),
        style: getContainerStyle({
          el,
          isFullscreenMedia,
          isImg: isImg || isModel3D,
          zIndex,
          opacity,
          animationState,
          shapeVisualStyle: backgroundAnimationState ? {} : ss,
          has3DExtrusion: extrusionData.hasExtrusion,
          templateEditing
        }),
        ...interactionProps,
        children: [
          renderDagDuotoneFilterForElement(el),
          textStyleOverrideCss && (0, import_jsx_runtime.jsx)("style", { children: textStyleOverrideCss }),
          backgroundAnimationState ? (0, import_jsx_runtime.jsxs)(
            "div",
            {
              "data-pptx-animation-layer": "background",
              style: {
                ...ss,
                position: "absolute",
                inset: 0,
                transformOrigin: "center",
                visibility: backgroundAnimationState.visible === false ? "hidden" : "visible",
                animation: backgroundAnimationState.cssAnimation,
                pointerEvents: "none"
              },
              children: [
                (0, import_jsx_runtime.jsx)(
                  ShapeEffectOverlay,
                  {
                    element: el,
                    animatesFill: visualAnimationState == null ? void 0 : visualAnimationState.animatesFill,
                    animatesStroke: visualAnimationState == null ? void 0 : visualAnimationState.animatesStroke
                  }
                ),
                extrusionData.hasExtrusion && (0, import_jsx_runtime.jsx)(Extrusion3DOverlay, { data: extrusionData }),
                isImg ? null : vs
              ]
            }
          ) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            (0, import_jsx_runtime.jsx)(
              ShapeEffectOverlay,
              {
                element: el,
                animatesFill: visualAnimationState == null ? void 0 : visualAnimationState.animatesFill,
                animatesStroke: visualAnimationState == null ? void 0 : visualAnimationState.animatesStroke
              }
            ),
            extrusionData.hasExtrusion && (0, import_jsx_runtime.jsx)(Extrusion3DOverlay, { data: extrusionData })
          ] }),
          renderBody({
            el,
            isImg,
            isEditing: effectiveIsInlineEditing,
            editText: inlineEditingText,
            spellCheck: spellCheckEnabled,
            txtSE,
            txtS: ts,
            vecShape: backgroundAnimationState && !isImg ? null : vs,
            imgStyle: getImageRenderStyle(el),
            imgFilter: getImageEffectsFilter(el),
            imgOpacity: getImageEffectsOpacity(el),
            imgAlt: imageAltText,
            isTxtEl: isTxt,
            media: mediaDataUrls,
            tableSt: tableEditorState,
            isSel: isSelected,
            doInk,
            doGrp,
            renderGroupChild: (child, index) => {
              var _a;
              return (0, import_jsx_runtime.jsx)(
                StaticElementRenderer,
                {
                  element: child,
                  activeSlide,
                  allSlides,
                  mediaDataUrls,
                  sourceSlideIndex,
                  zIndex: index,
                  animation: (_a = presentationElementStates == null ? void 0 : presentationElementStates.get(child.id)) == null ? void 0 : _a.cssAnimation,
                  exposeElementId: true,
                  parentGroupFill: getGroupChildParentFill(el),
                  onActionClick: el.actionClick ? void 0 : onActionClick,
                  actionRequiresModifier: effectiveCanInteract
                },
                child.id
              );
            },
            onEditChange: onInlineEditChange,
            onCommit: onInlineEditCommit,
            onCancel: onInlineEditCancel,
            onCellSel: cellSelectHandler,
            onCellCommit: cellCommitHandler,
            onColResize: colResizeHandler,
            onRowResize: rowResizeHandler,
            findHl: findHighlights,
            onHyperlinkClick,
            isPresentationPassive,
            // Same rule as the other four bindings: the hint belongs to the
            // editable canvas, not to a read-only viewer or the show stage.
            placeholderPromptMode: canInteract && !isPresentationPassive ? "edit" : "present",
            handleMediaPlayStateChange,
            presentationElementStates,
            slideElements: activeSlide == null ? void 0 : activeSlide.elements,
            allSlides,
            onZoomClick,
            sourceSlideIndex,
            fieldContext,
            tableStyleContext,
            canEditSmartArt,
            onUpdateSmartArtElement: smartArtUpdateHandler,
            canEditChart,
            onUpdateChartElement: chartUpdateHandler,
            onFormatText
          }),
          (0, import_jsx_runtime.jsx)(ActionAffordances, { affordance: actionAffordance })
        ]
      }
    );
  }
);
function renderPressureStroke(circles, color, opacity, keyPrefix) {
  return (0, import_jsx_runtime.jsx)("g", { opacity, children: circles.map((c, j) => (0, import_jsx_runtime.jsx)("circle", { cx: c.cx, cy: c.cy, r: c.r, fill: color }, `${keyPrefix}-pc-${j}`)) });
}
function renderNibMarkStroke(marks, color, opacity, keyPrefix) {
  return (0, import_jsx_runtime.jsx)("g", { opacity, children: marks.map((m, j) => (0, import_jsx_runtime.jsx)(
    "ellipse",
    {
      cx: m.cx,
      cy: m.cy,
      rx: m.rPerp,
      ry: m.rTilt,
      transform: `rotate(${m.rotationDeg} ${m.cx} ${m.cy})`,
      fill: color
    },
    `${keyPrefix}-nib-${j}`
  )) });
}
function renderStrokeView(view, pressureSensitive, replayStyle, key) {
  if (view.nibMarks) {
    return (0, import_jsx_runtime.jsx)("g", { children: renderNibMarkStroke(view.nibMarks, view.color, view.opacity, key) }, key);
  }
  if (pressureSensitive && view.circles) {
    return (0, import_jsx_runtime.jsx)("g", { children: renderPressureStroke(view.circles, view.color, view.opacity, key) }, key);
  }
  return (0, import_jsx_runtime.jsx)(
    "path",
    {
      d: view.d,
      fill: "none",
      stroke: view.color,
      strokeWidth: view.width,
      strokeOpacity: view.opacity,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      vectorEffect: "non-scaling-stroke",
      ...replayStyle ? {
        strokeDasharray: replayStyle.strokeDasharray,
        strokeDashoffset: replayStyle.strokeDashoffset,
        style: {
          animation: replayStyle.animation,
          "--ink-path-length": replayStyle.pathLength
        }
      } : {}
    },
    key
  );
}
function renderInk(el, options) {
  const replay = (options == null ? void 0 : options.replay) ?? false;
  const strokes = buildInkGroupStrokes(el, { color: "#000", width: 3 });
  strokes.some((s) => s.circles || s.nibMarks);
  const pressureSensitive = options == null ? void 0 : options.pressureSensitive;
  const replayStyles = replay ? getInkReplayStyles(el, options == null ? void 0 : options.replayConfig) : null;
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      className: "w-full h-full pointer-events-none",
      viewBox: `0 0 ${Math.max(el.width, 1)} ${Math.max(el.height, 1)}`,
      preserveAspectRatio: "none",
      children: [
        replay && (0, import_jsx_runtime.jsx)("style", { children: INK_REPLAY_KEYFRAMES }),
        strokes.map(
          (s, i) => renderStrokeView(s, pressureSensitive, replayStyles == null ? void 0 : replayStyles[i], `${el.id}-ink-${i}`)
        )
      ]
    }
  );
}
function renderGroup(children, parentGroupFill) {
  return (0, import_jsx_runtime.jsx)("div", { className: "relative w-full h-full pointer-events-none", children: filterRenderedElements(children).map((c, childIndex) => {
    const { hf, fc, sw, sc } = shapeParams(c);
    const baseSs = getShapeVisualStyle(c, hf, fc, sw, sc);
    const inheritedFill = resolveGroupChildFill(c, parentGroupFill);
    const ss = inheritedFill ? {
      ...baseSs,
      backgroundColor: inheritedFill.backgroundColor,
      backgroundImage: inheritedFill.backgroundImage,
      backgroundRepeat: inheritedFill.backgroundRepeat,
      backgroundSize: inheritedFill.backgroundSize,
      backgroundPosition: inheritedFill.backgroundPosition
    } : baseSs;
    const isI = c.type === "picture" || c.type === "image";
    const vs = renderVectorShape(c, isI ? false : hf, fc, sw, sc);
    const ts = getTextStyleForElement(c, DEFAULT_TEXT_COLOR);
    const isTxt = isEditableTextElement(c);
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        "data-element-id": c.id,
        className: "absolute",
        style: {
          left: c.x,
          top: c.y,
          width: Math.max(c.width, MIN_ELEMENT_SIZE),
          height: Math.max(c.height, MIN_ELEMENT_SIZE),
          transform: getElementTransform(c),
          transformOrigin: "center",
          ...ss,
          ...isI ? {
            backgroundColor: "transparent",
            backgroundImage: void 0,
            backgroundRepeat: void 0,
            backgroundSize: void 0,
            backgroundPosition: void 0,
            borderRadius: void 0,
            clipPath: void 0,
            overflow: "visible"
          } : {},
          // Explicit z-index preserves document order stacking within the
          // group: later children in the array (= later in p:grpSp XML)
          // render on top, matching PowerPoint's painter's algorithm.
          // Placed after ...ss to ensure it is never overwritten.
          zIndex: childIndex
        },
        children: [
          (0, import_jsx_runtime.jsx)(ShapeEffectOverlay, { element: c }),
          c.type === "group" ? (
            // A nested group: recurse, chaining the inherited fill so a
            // `grpFill` shape under a fill-less sub-group still paints.
            renderGroup(c.children, getGroupChildParentFill(c, parentGroupFill))
          ) : isI && ("svgData" in c && c.svgData || "imageData" in c && c.imageData) ? (0, import_jsx_runtime.jsxs)("div", { className: "absolute inset-0 pointer-events-none", style: getImageSurfaceStyle(c), children: [
            isImageTiled(c) ? (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "pointer-events-none select-none w-full h-full",
                style: getImageTilingStyle2(c)
              }
            ) : (0, import_jsx_runtime.jsx)(
              "img",
              {
                src: "svgData" in c && c.svgData ? c.svgData : c.imageData,
                alt: translationsEn["pptx.ink.groupChildAlt"],
                className: "pointer-events-none select-none",
                style: getImageRenderStyle(c),
                draggable: false
              }
            ),
            vs ? (0, import_jsx_runtime.jsx)("div", { className: "pointer-events-none absolute inset-0", children: vs }) : null
          ] }) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            vs,
            isTxt ? (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "relative z-10 w-full h-full pointer-events-none whitespace-pre-wrap break-words leading-[1.3]",
                style: ts,
                children: renderTextSegments(c, DEFAULT_TEXT_COLOR)
              }
            ) : null
          ] })
        ]
      },
      c.id
    );
  }) });
}
function renderContentPart(el, options) {
  if (el.inkStrokes && el.inkStrokes.length > 0) {
    const replay = (options == null ? void 0 : options.replay) ?? false;
    const pressureSensitive = (options == null ? void 0 : options.pressureSensitive) ?? true;
    const strokes = buildContentPartStrokes(el);
    const replayStyles = replay ? getContentPartReplayStyles(el.inkStrokes, options == null ? void 0 : options.replayConfig) : null;
    return (0, import_jsx_runtime.jsxs)(
      "svg",
      {
        className: "w-full h-full pointer-events-none",
        viewBox: `0 0 ${Math.max(el.width, 1)} ${Math.max(el.height, 1)}`,
        preserveAspectRatio: "none",
        children: [
          replay && (0, import_jsx_runtime.jsx)("style", { children: INK_REPLAY_KEYFRAMES }),
          strokes.map(
            (s, i) => renderStrokeView(s, pressureSensitive, replayStyles == null ? void 0 : replayStyles[i], `${el.id}-cp-ink-${i}`)
          )
        ]
      }
    );
  }
  return (0, import_jsx_runtime.jsx)("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none", children: translationsEn["pptx.ink.contentPartFallback"] });
}
function ExcelIcon(color, size = 32) {
  return (0, import_jsx_runtime.jsxs)("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "3",
        y: "3",
        width: "18",
        height: "18",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    (0, import_jsx_runtime.jsx)("line", { x1: "3", y1: "9", x2: "21", y2: "9", stroke: color, strokeWidth: "1" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "3", y1: "15", x2: "21", y2: "15", stroke: color, strokeWidth: "1" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "9", y1: "3", x2: "9", y2: "21", stroke: color, strokeWidth: "1" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "15", y1: "3", x2: "15", y2: "21", stroke: color, strokeWidth: "1" })
  ] });
}
function WordIcon(color, size = 32) {
  return (0, import_jsx_runtime.jsxs)("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "4",
        y: "2",
        width: "16",
        height: "20",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    (0, import_jsx_runtime.jsx)("line", { x1: "7", y1: "7", x2: "17", y2: "7", stroke: color, strokeWidth: "1.5", strokeLinecap: "round" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "7", y1: "11", x2: "17", y2: "11", stroke: color, strokeWidth: "1.5", strokeLinecap: "round" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "7", y1: "15", x2: "13", y2: "15", stroke: color, strokeWidth: "1.5", strokeLinecap: "round" })
  ] });
}
function PdfIcon(color, size = 32) {
  return (0, import_jsx_runtime.jsxs)("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "4",
        y: "2",
        width: "16",
        height: "20",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    (0, import_jsx_runtime.jsx)("text", { x: "12", y: "14", textAnchor: "middle", fill: color, fontSize: "7", fontWeight: "bold", children: translationsEn["pptx.file.pdf"] })
  ] });
}
function VisioIcon(color, size = 32) {
  return (0, import_jsx_runtime.jsxs)("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "8", y: "2", width: "8", height: "5", rx: "1", stroke: color, strokeWidth: "1.5", fill: "none" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "12", y1: "7", x2: "12", y2: "10", stroke: color, strokeWidth: "1.5" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "6", y1: "10", x2: "18", y2: "10", stroke: color, strokeWidth: "1.5" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "6", y1: "10", x2: "6", y2: "13", stroke: color, strokeWidth: "1.5" }),
    (0, import_jsx_runtime.jsx)("line", { x1: "18", y1: "10", x2: "18", y2: "13", stroke: color, strokeWidth: "1.5" }),
    (0, import_jsx_runtime.jsx)("rect", { x: "2", y: "13", width: "8", height: "5", rx: "1", stroke: color, strokeWidth: "1.5", fill: "none" }),
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "14",
        y: "13",
        width: "8",
        height: "5",
        rx: "1",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    )
  ] });
}
function MathIcon(color, size = 32) {
  return (0, import_jsx_runtime.jsxs)("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "2",
        y: "4",
        width: "20",
        height: "16",
        rx: "2",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "text",
      {
        x: "12",
        y: "15",
        textAnchor: "middle",
        fill: color,
        fontSize: "9",
        fontStyle: "italic",
        fontWeight: "bold",
        children: "f(x)"
      }
    )
  ] });
}
function GenericOleIcon(color, size = 32) {
  return (0, import_jsx_runtime.jsxs)("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", children: [
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "2",
        y: "5",
        width: "9",
        height: "7",
        rx: "1.5",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "rect",
      {
        x: "13",
        y: "12",
        width: "9",
        height: "7",
        rx: "1.5",
        stroke: color,
        strokeWidth: "1.5",
        fill: "none"
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "line",
      {
        x1: "11",
        y1: "8.5",
        x2: "13",
        y2: "15.5",
        stroke: color,
        strokeWidth: "1.5",
        strokeLinecap: "round"
      }
    )
  ] });
}
function getOleIcon(type, color, size = 32) {
  switch (type) {
    case "excel":
      return ExcelIcon(color, size);
    case "word":
      return WordIcon(color, size);
    case "pdf":
      return PdfIcon(color, size);
    case "visio":
      return VisioIcon(color, size);
    case "mathtype":
      return MathIcon(color, size);
    case "unknown":
    default:
      return GenericOleIcon(color, size);
  }
}
function renderOleBadge(oleType) {
  const color = getOleTypeColor(oleType);
  const shortLabel = getOleBadgeLabel(oleType);
  return (0, import_jsx_runtime.jsxs)("svg", { width: "24", height: "24", viewBox: "0 0 24 24", className: "absolute bottom-1 right-1 z-10", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "2", y: "2", width: "20", height: "20", rx: "3", fill: color }),
    (0, import_jsx_runtime.jsx)(
      "text",
      {
        x: "12",
        y: "16",
        textAnchor: "middle",
        fill: "white",
        fontSize: shortLabel.length > 4 ? "6" : "10",
        fontWeight: "bold",
        children: shortLabel
      }
    )
  ] });
}
function InlineTextEditor({
  initialText,
  spellCheck,
  rtl,
  textDirection: _textDirection,
  textStyle,
  textStyleRaw,
  layoutStyle,
  element,
  onCommit,
  onCancel,
  onEditChange,
  onFormatText
}) {
  const editorRef = (0, import_react2.useRef)(null);
  const seedRef = (0, import_react2.useRef)(null);
  if (seedRef.current === null) {
    seedRef.current = {
      initialText,
      hasRichSegments: Boolean(
        hasTextProperties(element) && element.textSegments && element.textSegments.length > 0
      )
    };
  }
  const seed = seedRef.current;
  const extractText = (0, import_react2.useCallback)(() => {
    const el = editorRef.current;
    if (!el) {
      return seed.initialText;
    }
    return readEditableText(el);
  }, [seed]);
  const handleInput = (0, import_react2.useCallback)(() => {
    onEditChange(extractText());
  }, [extractText, onEditChange]);
  const trimTrailingSpaceBeforeCaret = (0, import_react2.useCallback)(() => {
    const selection = window.getSelection();
    if (!selection || !selection.isCollapsed || selection.rangeCount === 0) {
      return;
    }
    const range = selection.getRangeAt(0);
    const { startContainer, startOffset } = range;
    if (startContainer.nodeType !== Node.TEXT_NODE || startOffset === 0) {
      return;
    }
    const text = startContainer.textContent ?? "";
    if (text.charAt(startOffset - 1) !== " ") {
      return;
    }
    const trimRange = document.createRange();
    trimRange.setStart(startContainer, startOffset - 1);
    trimRange.setEnd(startContainer, startOffset);
    trimRange.deleteContents();
  }, []);
  (0, import_react2.useEffect)(() => {
    const el = editorRef.current;
    if (!el) {
      return;
    }
    el.focus();
    placeCaretAtEnd(el);
  }, []);
  const mountedRef = (0, import_react2.useRef)(false);
  (0, import_react2.useLayoutEffect)(() => {
    if (!mountedRef.current) {
      mountedRef.current = true;
      return;
    }
    const pending = getPendingSelectionRestore();
    if (!pending || !editorRef.current) {
      return;
    }
    restoreSegmentSelection(
      editorRef.current,
      pending.startSegIdx,
      pending.startOffset,
      pending.endSegIdx,
      pending.endOffset
    );
  });
  const warpStyle = getTextWarpStyle(textStyleRaw);
  const compensationTransform = getTextCompensationTransform(element);
  const warpTransform = warpStyle == null ? void 0 : warpStyle.transform;
  const mergedTransform = [compensationTransform, warpTransform].filter(Boolean).join(" ") || void 0;
  const wrapperStyle = {
    ...layoutStyle,
    ...textStyle,
    ...warpStyle,
    transform: mergedTransform,
    transformOrigin: (warpStyle == null ? void 0 : warpStyle.transformOrigin) || "center"
  };
  const nextInlineStyleValue = (property) => {
    var _a, _b2;
    if (!hasTextProperties(element)) {
      return true;
    }
    const selection = getInlineEditorSelection(element.textSegments);
    const segment = selection ? (_a = element.textSegments) == null ? void 0 : _a[selection.startSegIdx] : (_b2 = element.textSegments) == null ? void 0 : _b2[0];
    const style = (segment == null ? void 0 : segment.style) ?? element.textStyle;
    return !(style == null ? void 0 : style[property]);
  };
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      ref: editorRef,
      contentEditable: true,
      suppressContentEditableWarning: true,
      "data-inline-editor": true,
      spellCheck,
      dir: rtl ? "rtl" : "ltr",
      className: "relative z-10 w-full h-full whitespace-pre-wrap break-words leading-[1.3] outline-none",
      style: {
        ...wrapperStyle,
        cursor: "text",
        minHeight: "1em"
      },
      onPointerDown: (e) => e.stopPropagation(),
      onMouseDown: (e) => e.stopPropagation(),
      onClick: (e) => e.stopPropagation(),
      onInput: handleInput,
      onBlur: () => {
        onEditChange(extractText());
        onCommit();
      },
      onKeyDown: (e) => {
        if ((e.ctrlKey || e.metaKey) && !e.shiftKey && onFormatText) {
          const key = e.key.toLowerCase();
          if (key === "b" || key === "i" || key === "u") {
            e.preventDefault();
            e.stopPropagation();
            switch (key) {
              case "b":
                onFormatText({ bold: nextInlineStyleValue("bold") });
                break;
              case "i":
                onFormatText({ italic: nextInlineStyleValue("italic") });
                break;
              case "u":
                onFormatText({ underline: nextInlineStyleValue("underline") });
                break;
            }
            return;
          }
        }
        if (e.key === "Escape") {
          e.preventDefault();
          onCancel();
          return;
        }
        if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
          e.preventDefault();
          onEditChange(extractText());
          onCommit();
          return;
        }
        if (e.key === "Enter") {
          trimTrailingSpaceBeforeCaret();
        }
      },
      onPaste: (e) => {
        e.preventDefault();
        const text = e.clipboardData.getData("text/plain");
        document.execCommand("insertText", false, text);
      },
      children: seed.hasRichSegments ? renderTextSegments(element, DEFAULT_TEXT_COLOR) : seed.initialText
    }
  );
}
function FailedToLoad6() {
  return null;
}
var LazyModel3DScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./Model3DScene-WJ3MMIZH-QP6XYWM7.js");
    } catch {
      return { default: FailedToLoad6 };
    }
  }
);
function dataUrlToBlobUrl(dataUrl, mimeType) {
  return modelDataToBlobUrl(dataUrl, mimeType);
}
function PosterFallback({
  element,
  width,
  height
}) {
  const src = element.posterImage ?? element.imageData;
  if (src) {
    return (0, import_jsx_runtime.jsx)(
      "img",
      {
        src,
        alt: "3D Model",
        className: "pointer-events-none select-none",
        style: {
          width,
          height,
          objectFit: "contain"
        },
        draggable: false
      }
    );
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "w-full h-full flex flex-col items-center justify-center text-[11px] text-gray-400 bg-gray-50 border border-dashed border-gray-200 rounded",
      style: { width, height },
      children: [
        (0, import_jsx_runtime.jsxs)(
          "svg",
          {
            xmlns: "http://www.w3.org/2000/svg",
            width: "24",
            height: "24",
            viewBox: "0 0 24 24",
            fill: "none",
            stroke: "currentColor",
            strokeWidth: "1.5",
            strokeLinecap: "round",
            strokeLinejoin: "round",
            className: "mb-1 text-gray-300",
            children: [
              (0, import_jsx_runtime.jsx)("path", { d: "M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" }),
              (0, import_jsx_runtime.jsx)("polyline", { points: "3.27 6.96 12 12.01 20.73 6.96" }),
              (0, import_jsx_runtime.jsx)("line", { x1: "12", y1: "22.08", x2: "12", y2: "12" })
            ]
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { children: "3D Model" })
      ]
    }
  );
}
var Model3DErrorBoundary = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    if (this.state.hasError) {
      return this.props.fallback;
    }
    return this.props.children;
  }
};
function Model3DRenderer({ element, width, height, interactive }) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const blobUrl = (0, import_react2.useMemo)(
    () => dataUrlToBlobUrl(element.modelData, element.modelMimeType),
    [element.modelData, element.modelMimeType]
  );
  (0, import_react2.useEffect)(() => {
    return () => {
      if (blobUrl) {
        URL.revokeObjectURL(blobUrl);
      }
    };
  }, [blobUrl]);
  const posterFallback = (0, import_jsx_runtime.jsx)(PosterFallback, { element, width, height });
  if (threeAvailable === null) {
    return posterFallback;
  }
  if (!threeAvailable || !blobUrl) {
    return posterFallback;
  }
  return (0, import_jsx_runtime.jsx)(Model3DErrorBoundary, { fallback: posterFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: posterFallback, children: (0, import_jsx_runtime.jsx)(
    LazyModel3DScene,
    {
      modelUrl: blobUrl,
      interactive,
      width,
      height
    }
  ) }) });
}
function buildEmbedInfo(el) {
  const canDownload = Boolean(el.oleEmbeddedData);
  return {
    fileName: el.oleEmbeddedFileName ?? el.fileName,
    displayName: getOleDisplayName(el),
    readableSize: formatBytes(el.oleEmbeddedByteSize),
    progId: el.oleProgId,
    typeLabel: getOleTypeLabel(resolveOleType(el)),
    canDownload,
    canOpenInBrowser: canDownload && isBrowserOpenableMime(el.oleEmbeddedMimeType)
  };
}
function buildTooltip(info) {
  const parts = [info.displayName, info.typeLabel];
  if (info.readableSize) {
    parts.push(info.readableSize);
  }
  if (info.progId) {
    parts.push(info.progId);
  }
  return Array.from(new Set(parts)).join(" · ");
}
function stopPointer(e) {
  e.stopPropagation();
}
function OleActionFooter({ el, info }) {
  const { t } = useTranslation();
  if (!info.canDownload || !el.oleEmbeddedData) {
    return null;
  }
  const downloadName = info.fileName ?? "embedded-object";
  const sizeSuffix = info.readableSize ? ` (${info.readableSize})` : "";
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "absolute inset-x-0 bottom-0 z-20 flex items-center justify-between gap-2 px-2 py-1 bg-black/70 text-white text-[10px] opacity-0 transition-opacity group-hover:opacity-100 focus-within:opacity-100",
      onPointerDown: stopPointer,
      onMouseDown: stopPointer,
      onClick: stopPointer,
      children: [
        (0, import_jsx_runtime.jsxs)("span", { className: "min-w-0 truncate", title: downloadName, children: [
          downloadName,
          sizeSuffix
        ] }),
        (0, import_jsx_runtime.jsxs)("span", { className: "flex shrink-0 items-center gap-1", children: [
          (0, import_jsx_runtime.jsx)(
            "a",
            {
              href: el.oleEmbeddedData,
              download: downloadName,
              className: "rounded px-1.5 py-0.5 bg-white/15 hover:bg-white/25 focus-visible:outline focus-visible:outline-1 focus-visible:outline-white",
              title: `Download ${downloadName}`,
              children: t("pptx.ole.download")
            }
          ),
          info.canOpenInBrowser && el.oleEmbeddedData && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: () => openUrlInNewTab(el.oleEmbeddedData),
              className: "rounded px-1.5 py-0.5 bg-white/15 hover:bg-white/25 focus-visible:outline focus-visible:outline-1 focus-visible:outline-white",
              title: `Open ${downloadName} in a new tab`,
              children: t("pptx.ole.open")
            }
          )
        ] })
      ]
    }
  );
}
function OleRenderer({ element: el }) {
  const oleType = resolveOleType(el);
  const ariaLabel = getOleAriaLabel(el);
  const info = buildEmbedInfo(el);
  const tooltip = buildTooltip(info);
  if (el.previewImageData) {
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: "group relative w-full h-full",
        role: "img",
        "aria-label": ariaLabel,
        title: tooltip,
        children: [
          (0, import_jsx_runtime.jsx)(
            "img",
            {
              src: el.previewImageData,
              alt: ariaLabel,
              className: "pointer-events-none select-none w-full h-full object-contain",
              draggable: false
            }
          ),
          renderOleBadge(oleType),
          (0, import_jsx_runtime.jsx)(OleActionFooter, { el, info })
        ]
      }
    );
  }
  const color = getOleTypeColor(oleType);
  const label = getOleTypeLabel(oleType);
  const displayName = info.displayName;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "group relative w-full h-full flex flex-col items-center justify-center",
      role: "img",
      "aria-label": ariaLabel,
      title: tooltip,
      style: {
        border: `2px solid ${color}33`,
        borderRadius: 6,
        backgroundColor: `${color}0D`
      },
      children: [
        getOleIcon(oleType, color, 36),
        (0, import_jsx_runtime.jsx)("span", { className: "mt-2 text-[12px] font-medium max-w-[90%] truncate", style: { color }, children: displayName }),
        (0, import_jsx_runtime.jsx)("span", { className: "mt-0.5 text-[10px] text-white/50 max-w-[90%] truncate", children: info.readableSize ? `${label} · ${info.readableSize}` : label }),
        (0, import_jsx_runtime.jsx)(OleActionFooter, { el, info })
      ]
    }
  );
}
var SmartArt3DContext = (0, import_react2.createContext)(false);
function resolvePalette2(el) {
  return el.type === "smartArt" ? resolvePalette(el.smartArtData) : DEFAULT_PALETTE2;
}
function resolveStyle(el) {
  if (el.type !== "smartArt" || !el.smartArtData) {
    return "flat";
  }
  return el.smartArtData.style ?? "flat";
}
function resolveSmartArtDataPalette(data) {
  return resolvePalette(data);
}
var LINE_HEIGHT_RATIO = 1.2;
var DEFAULT_FONT_SIZE = 12;
function SmartArtInlineNodeEditor({
  initialText,
  rect,
  fontSize = DEFAULT_FONT_SIZE,
  onCommit,
  onCancel
}) {
  const { t } = useTranslation();
  const ref = import_react2.default.useRef(null);
  const [value, setValue] = import_react2.default.useState(initialText);
  const settled = import_react2.default.useRef(false);
  import_react2.default.useEffect(() => {
    const el = ref.current;
    if (el) {
      el.focus();
      el.select();
    }
  }, []);
  const commit = () => {
    if (settled.current) {
      return;
    }
    settled.current = true;
    onCommit(value);
  };
  const cancel = () => {
    if (settled.current) {
      return;
    }
    settled.current = true;
    onCancel();
  };
  return (0, import_jsx_runtime.jsx)(
    "textarea",
    {
      ref,
      value,
      "aria-label": t("pptx.smartArt.editNodeText"),
      spellCheck: false,
      className: "absolute z-20 resize-none rounded-sm border border-primary bg-white/95 text-center text-black shadow outline-none",
      style: {
        left: rect.left,
        top: rect.top,
        width: rect.width,
        height: rect.height,
        fontSize,
        lineHeight: `${fontSize * LINE_HEIGHT_RATIO}px`
      },
      onChange: (e) => setValue(e.target.value),
      onMouseDown: (e) => e.stopPropagation(),
      onClick: (e) => e.stopPropagation(),
      onDoubleClick: (e) => e.stopPropagation(),
      onBlur: commit,
      onKeyDown: (e) => {
        if (e.key === "Enter" && !e.shiftKey) {
          e.preventDefault();
          commit();
        } else if (e.key === "Escape") {
          e.preventDefault();
          cancel();
        }
        e.stopPropagation();
      }
    }
  );
}
function SmartArtNodeStyleBar({
  palette,
  onPickFill
}) {
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "flex gap-1.5 px-2 py-1.5 bg-white/90 shadow-sm rounded-full border border-border",
      onMouseDown: (e) => e.stopPropagation(),
      onClick: (e) => e.stopPropagation(),
      children: palette.slice(0, 6).map((color) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          "aria-label": `Set fill to ${color}`,
          "data-pptx-compact": true,
          className: "w-5 h-5 rounded-full border border-black/10 hover:scale-125 transition-transform",
          style: { background: color },
          onClick: () => onPickFill(color)
        },
        color
      ))
    }
  );
}
var NODE_ID_ATTR = "data-smartart-node-id";
function findNodeIdFromEvent(target) {
  let el = target instanceof Element ? target : null;
  while (el) {
    if (el.hasAttribute(NODE_ID_ATTR)) {
      return el;
    }
    el = el.parentElement;
  }
  return null;
}
function useSmartArtHoverState(containerRef) {
  const [hoveredNodeId, setHoveredNodeId] = import_react2.default.useState(null);
  const [hoveredNodeRect, setHoveredNodeRect] = import_react2.default.useState(null);
  const hideTimeoutRef = import_react2.default.useRef(null);
  const cancelPendingHide = import_react2.default.useCallback(() => {
    if (hideTimeoutRef.current !== null) {
      window.clearTimeout(hideTimeoutRef.current);
      hideTimeoutRef.current = null;
    }
  }, []);
  const handleMouseMove = import_react2.default.useCallback(
    (e, ignoreRef) => {
      const nodeEl = findNodeIdFromEvent(e.target);
      const container = containerRef.current;
      if (nodeEl && container) {
        cancelPendingHide();
        setHoveredNodeId(nodeEl.getAttribute(NODE_ID_ATTR));
        setHoveredNodeRect(
          computeInlineEditorRect(
            nodeEl.getBoundingClientRect(),
            container.getBoundingClientRect()
          )
        );
        return;
      }
      if ((ignoreRef == null ? void 0 : ignoreRef.current) && e.target instanceof Node && ignoreRef.current.contains(e.target)) {
        cancelPendingHide();
        return;
      }
      cancelPendingHide();
      hideTimeoutRef.current = window.setTimeout(() => {
        setHoveredNodeId(null);
        setHoveredNodeRect(null);
        hideTimeoutRef.current = null;
      }, 150);
    },
    [containerRef, cancelPendingHide]
  );
  const clearHover = import_react2.default.useCallback(() => {
    cancelPendingHide();
    setHoveredNodeId(null);
    setHoveredNodeRect(null);
  }, [cancelPendingHide]);
  import_react2.default.useEffect(() => cancelPendingHide, [cancelPendingHide]);
  return { hoveredNodeId, hoveredNodeRect, handleMouseMove, clearHover };
}
var EDITOR_PADDING = 4;
var LINE_HEIGHT_RATIO2 = 1.2;
var STYLE_BAR_WIDTH = 168;
var STYLE_BAR_HEIGHT = 40;
function SmartArtEditableLayer({
  smartArtData,
  canEdit,
  onCommitNodeText,
  palette,
  onChangeNodeStyle,
  children
}) {
  const containerRef = import_react2.default.useRef(null);
  const styleBarRef = import_react2.default.useRef(null);
  const [edit, setEdit] = import_react2.default.useState(null);
  const { hoveredNodeId, hoveredNodeRect, handleMouseMove, clearHover } = useSmartArtHoverState(containerRef);
  const openEditor = import_react2.default.useCallback(
    (target) => {
      const nodeEl = findNodeIdFromEvent(target);
      const container = containerRef.current;
      if (!nodeEl || !container) {
        return;
      }
      const nodeId = nodeEl.getAttribute(NODE_ID_ATTR);
      if (!nodeId) {
        return;
      }
      const textEl = nodeEl.querySelector("text");
      const textRect = textEl == null ? void 0 : textEl.getBoundingClientRect();
      const hasTextRect = textRect !== void 0 && textRect.width > 0 && textRect.height > 0;
      const sourceRect = hasTextRect ? textRect : nodeEl.getBoundingClientRect();
      const rect = computeInlineEditorRect(sourceRect, container.getBoundingClientRect());
      const paddedRect = {
        left: rect.left - EDITOR_PADDING,
        top: rect.top - EDITOR_PADDING,
        width: rect.width + EDITOR_PADDING * 2,
        height: rect.height + EDITOR_PADDING * 2
      };
      const lineCount = Math.max(1, (textEl == null ? void 0 : textEl.querySelectorAll("tspan").length) ?? 1);
      const fontSize = hasTextRect ? Math.max(8, textRect.height / (lineCount * LINE_HEIGHT_RATIO2)) : void 0;
      clearHover();
      setEdit({ nodeId, rect: paddedRect, fontSize });
    },
    [clearHover]
  );
  if (!canEdit) {
    return children;
  }
  const initialText = edit ? findSmartArtNodeText(smartArtData, edit.nodeId) ?? "" : "";
  const showStyleBar = !edit && hoveredNodeId !== null && hoveredNodeRect !== null && palette !== void 0 && onChangeNodeStyle !== void 0;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: containerRef,
      className: "relative h-full w-full",
      style: { cursor: hoveredNodeId ? "text" : void 0 },
      onMouseMove: (e) => handleMouseMove(e, styleBarRef),
      onMouseLeave: clearHover,
      onDoubleClick: (e) => {
        const nodeEl = findNodeIdFromEvent(e.target);
        if (nodeEl) {
          e.stopPropagation();
          openEditor(e.target);
        }
      },
      children: [
        !edit && (0, import_jsx_runtime.jsx)("style", { children: `[data-smartart-node-id]:hover { outline: 2px solid rgba(96,165,250,0.6); outline-offset: 1px; }` }),
        children,
        showStyleBar && (() => {
          const container = containerRef.current;
          const maxLeft = Math.max(
            0,
            ((container == null ? void 0 : container.clientWidth) ?? STYLE_BAR_WIDTH) - STYLE_BAR_WIDTH
          );
          const maxTop = Math.max(
            0,
            ((container == null ? void 0 : container.clientHeight) ?? STYLE_BAR_HEIGHT) - STYLE_BAR_HEIGHT
          );
          return (0, import_jsx_runtime.jsx)(
            "div",
            {
              ref: styleBarRef,
              style: {
                position: "absolute",
                left: Math.min(
                  maxLeft,
                  Math.max(0, hoveredNodeRect.left + hoveredNodeRect.width - STYLE_BAR_WIDTH)
                ),
                top: Math.min(maxTop, Math.max(0, hoveredNodeRect.top - 22)),
                zIndex: 10
              },
              children: (0, import_jsx_runtime.jsx)(
                SmartArtNodeStyleBar,
                {
                  palette,
                  onPickFill: (color) => onChangeNodeStyle(hoveredNodeId, color)
                }
              )
            }
          );
        })(),
        edit && (0, import_jsx_runtime.jsx)(
          SmartArtInlineNodeEditor,
          {
            initialText,
            rect: edit.rect,
            fontSize: edit.fontSize,
            onCommit: (text) => {
              onCommitNodeText(edit.nodeId, text);
              setEdit(null);
            },
            onCancel: () => setEdit(null)
          },
          edit.nodeId
        )
      ]
    }
  );
}
var LABEL_WIDTH_FRACTION = 0.82;
function smartArtNodeGroupProps(nodeId, shadow, label) {
  const props = {
    "data-smartart-node-id": nodeId,
    style: { filter: shadow, pointerEvents: "auto" }
  };
  if (label) {
    props.role = "img";
    props["aria-label"] = label;
  }
  return props;
}
function SmartArtGradient({ gradient }) {
  const stops = gradient.stops.map((stop, i) => (0, import_jsx_runtime.jsx)(
    "stop",
    {
      offset: stop.offset,
      stopColor: stop.color,
      ...stop.opacity !== void 0 ? { stopOpacity: stop.opacity } : {}
    },
    `${gradient.id}-s${i}`
  ));
  return gradient.kind === "radial" ? (0, import_jsx_runtime.jsx)("radialGradient", { id: gradient.id, cx: gradient.cx, cy: gradient.cy, r: gradient.r, children: stops }) : (0, import_jsx_runtime.jsx)(
    "linearGradient",
    {
      id: gradient.id,
      x1: gradient.x1,
      y1: gradient.y1,
      x2: gradient.x2,
      y2: gradient.y2,
      children: stops
    }
  );
}
function SmartArtNodeText({
  text,
  lines: positionedLines,
  x,
  y,
  fill,
  fontSize,
  fontWeight,
  fontStyle,
  fontFamily,
  className,
  textAnchor = "middle",
  anchor = "middle",
  maxWidth
}) {
  if (positionedLines) {
    return (0, import_jsx_runtime.jsx)(
      "text",
      {
        x,
        textAnchor,
        dominantBaseline: "central",
        fill,
        fontSize,
        fontWeight,
        fontStyle,
        fontFamily,
        className,
        children: positionedLines.map((line, i) => (0, import_jsx_runtime.jsx)("tspan", { x, y: line.y, children: line.text }, i))
      }
    );
  }
  const source = text ?? "", lines = maxWidth !== void 0 ? centeredSvgTextLines(source, fontSize, { maxWidth: maxWidth * LABEL_WIDTH_FRACTION }).map(
    (line) => line.text
  ) : source.split("\n").filter((l) => l.length > 0), lineHeight = fontSize * 1.2;
  let startY, dominantBaseline;
  if (anchor === "bottom") {
    startY = lines.length > 0 ? y - (lines.length - 1) * lineHeight : y;
    dominantBaseline = "auto";
  } else if (anchor === "top") {
    startY = y;
    dominantBaseline = "hanging";
  } else {
    const totalHeight = lines.length * lineHeight;
    startY = lines.length > 0 ? y - totalHeight / 2 + lineHeight / 2 : y;
    dominantBaseline = "central";
  }
  return (0, import_jsx_runtime.jsx)(
    "text",
    {
      x,
      textAnchor,
      dominantBaseline,
      fill,
      fontSize,
      fontWeight,
      fontStyle,
      fontFamily,
      className,
      children: lines.map((line, i) => (0, import_jsx_runtime.jsx)("tspan", { x, y: startY + i * lineHeight, children: line }, i))
    }
  );
}
function chromeStyleToReactCss(style) {
  const react = {};
  for (const [key, value] of Object.entries(style)) {
    react[key.replace(/-([a-z])/gu, (_match, letter) => letter.toUpperCase())] = value;
  }
  return react;
}
function wrapChrome(chrome, content, className, a11y) {
  const wrapperStyle = chromeStyleToReactCss(buildChromeStyle(chrome));
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: `w-full h-full ${className}`,
      style: wrapperStyle,
      role: a11y == null ? void 0 : a11y.role,
      "aria-label": a11y == null ? void 0 : a11y.label,
      children: content
    }
  );
}
function shapeBody(shape) {
  switch (shape.kind) {
    case "image":
      return (0, import_jsx_runtime.jsx)(
        "image",
        {
          x: shape.x,
          y: shape.y,
          width: shape.width,
          height: shape.height,
          href: shape.imageUrl,
          preserveAspectRatio: "xMidYMid meet",
          transform: shape.transform
        }
      );
    case "ellipse":
      return (0, import_jsx_runtime.jsx)(
        "ellipse",
        {
          cx: shape.cx,
          cy: shape.cy,
          rx: shape.width / 2,
          ry: shape.height / 2,
          fill: shape.fill,
          stroke: shape.stroke,
          strokeWidth: shape.strokeWidth,
          transform: shape.transform
        }
      );
    case "path":
      return (0, import_jsx_runtime.jsx)(
        "path",
        {
          d: shape.pathData,
          fill: shape.fill,
          stroke: shape.stroke,
          strokeWidth: shape.strokeWidth,
          transform: shape.pathTransform
        }
      );
    default:
      return (0, import_jsx_runtime.jsx)(
        "rect",
        {
          x: shape.x,
          y: shape.y,
          width: shape.width,
          height: shape.height,
          rx: shape.rx,
          fill: shape.fill,
          stroke: shape.stroke,
          strokeWidth: shape.strokeWidth,
          transform: shape.transform
        }
      );
  }
}
function DrawingShapeRenderer({
  elementId,
  shapes,
  allShapes = shapes,
  style,
  palette,
  nodes,
  nodeLabels
}) {
  const viewBox = computeDrawingViewBox(allShapes);
  const rendered = projectDrawingShapes(elementId, shapes, viewBox, palette, style);
  const nodeIds = nodes ? resolveRevealedDrawingShapeNodeIds(allShapes, shapes, nodes) : void 0;
  const shadow = styleShadowFilter(style);
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      viewBox: `0 0 ${viewBox.width} ${viewBox.height}`,
      className: "w-full h-full pointer-events-none",
      preserveAspectRatio: "xMidYMid meet",
      "data-testid": "smartart-drawing-shapes",
      children: rendered.map((shape, i) => {
        const nodeId = nodeIds == null ? void 0 : nodeIds[i];
        const nodeLabel = nodeId ? nodeLabels == null ? void 0 : nodeLabels.get(nodeId) : void 0;
        const groupProps = nodeId ? smartArtNodeGroupProps(nodeId, shadow, nodeLabel) : { style: { filter: shadow } };
        return (0, import_jsx_runtime.jsxs)("g", { ...groupProps, children: [
          nodeLabel ? (0, import_jsx_runtime.jsx)("title", { children: nodeLabel }) : null,
          shape.gradient ? (0, import_jsx_runtime.jsx)("defs", { children: (0, import_jsx_runtime.jsx)(SmartArtGradient, { gradient: shape.gradient }) }) : null,
          shapeBody(shape),
          shape.textLines.length > 0 ? (0, import_jsx_runtime.jsx)(
            SmartArtNodeText,
            {
              x: shape.textX,
              y: shape.textY,
              lines: shape.textLines,
              fill: shape.fontColor,
              fontSize: shape.fontSize,
              fontFamily: shape.fontFamily,
              fontWeight: shape.fontWeight,
              fontStyle: shape.fontStyle,
              className: "pointer-events-none"
            }
          ) : null
        ] }, shape.key);
      })
    }
  );
}
var CONNECTOR_STROKE = "#94a3b8";
var CONNECTOR_WIDTH = 1.5;
var CONNECTOR_OPACITY = 0.5;
var DEFAULT_LABEL_COLOR = "white";
function ConnectorPath({ connector }) {
  return (0, import_jsx_runtime.jsx)(
    "path",
    {
      d: connector.d,
      fill: "none",
      stroke: connector.stroke ?? CONNECTOR_STROKE,
      strokeWidth: connector.strokeWidth ?? CONNECTOR_WIDTH,
      opacity: connector.opacity ?? CONNECTOR_OPACITY,
      strokeDasharray: connector.dash
    }
  );
}
function NodeShape({ node }) {
  if (node.kind === "circle") {
    return (0, import_jsx_runtime.jsx)(
      "circle",
      {
        cx: node.cx,
        cy: node.cy,
        r: node.r,
        fill: node.fill,
        stroke: node.stroke,
        strokeWidth: node.strokeWidth,
        opacity: node.opacity
      }
    );
  }
  if (node.kind === "polygon") {
    return (0, import_jsx_runtime.jsx)(
      "polygon",
      {
        points: node.points,
        fill: node.fill,
        stroke: node.stroke,
        strokeWidth: node.strokeWidth,
        opacity: node.opacity
      }
    );
  }
  return (0, import_jsx_runtime.jsx)(
    "rect",
    {
      x: node.x,
      y: node.y,
      width: node.width,
      height: node.height,
      rx: node.rx,
      fill: node.fill,
      stroke: node.stroke,
      strokeWidth: node.strokeWidth,
      opacity: node.opacity
    }
  );
}
function labelAnchor(node) {
  if (node.kind === "circle") {
    return {
      x: node.textX ?? node.cx,
      y: node.textY ?? node.cy,
      textAnchor: node.textAnchor ?? "middle",
      baseline: node.textBaseline ?? "middle"
    };
  }
  return { x: node.textX, y: node.textY, textAnchor: "middle", baseline: "middle" };
}
function SmartArtLayoutSvg({
  layout,
  nodeIds,
  nodeLabels
}) {
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      className: "w-full h-full pointer-events-none",
      viewBox: layout.viewBox,
      preserveAspectRatio: "xMidYMid meet",
      "data-testid": `smartart-${layout.family}`,
      "data-layout-family": layout.family,
      children: [
        layout.connectors.map((connector) => (0, import_jsx_runtime.jsx)(ConnectorPath, { connector }, connector.key)),
        layout.nodes.map((node, i) => {
          const nodeId = nodeIds[i];
          const label = nodeId ? nodeLabels == null ? void 0 : nodeLabels.get(nodeId) : void 0;
          const anchor = labelAnchor(node);
          return (0, import_jsx_runtime.jsxs)(
            "g",
            {
              ...smartArtNodeGroupProps(nodeId ?? node.key, layout.shadowFilter, label),
              children: [
                label ? (0, import_jsx_runtime.jsx)("title", { children: label }) : null,
                (0, import_jsx_runtime.jsx)(NodeShape, { node }),
                node.text.length > 0 ? (0, import_jsx_runtime.jsx)(
                  SmartArtNodeText,
                  {
                    x: anchor.x,
                    y: anchor.y,
                    text: node.text,
                    fill: node.fontColor ?? DEFAULT_LABEL_COLOR,
                    fontSize: node.fontSize,
                    fontWeight: node.fontWeight,
                    fontStyle: node.fontStyle,
                    textAnchor: anchor.textAnchor,
                    anchor: anchor.baseline,
                    className: "pointer-events-none"
                  }
                ) : null
              ]
            },
            node.key
          );
        })
      ]
    }
  );
}
function SmartArtRendererImpl({
  element,
  className = "",
  canEdit = false,
  onUpdateElement,
  animationState
}) {
  const { t } = useTranslation();
  if (element.type !== "smartArt" || !element.smartArtData) {
    return (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: `w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none ${className}`,
        children: t("pptx.smartArt.placeholder")
      }
    );
  }
  const smartArtData = element.smartArtData;
  const { nodes, drawingShapes, chrome } = smartArtData;
  const { nodes: revealedNodes } = resolveRevealedSmartArtNodes(
    nodes,
    animationState,
    smartArtData.presLayoutVars
  );
  const revealedShapes = drawingShapes && drawingShapes.length > 0 ? resolveRevealedDrawingShapes(drawingShapes, nodes, animationState) : drawingShapes;
  if (nodes.length === 0) {
    return (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: `w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none ${className}`,
        children: t("pptx.smartArt.placeholder")
      }
    );
  }
  const palette = resolvePalette2(element);
  const style = resolveStyle(element);
  const a11y = buildSmartArtA11y(smartArtData);
  const nodeLabels = new Map(a11y.nodes.map((n) => [n.id, n.label]));
  const editable = canEdit && Boolean(onUpdateElement) && canDrillDown(element);
  const handleCommitNodeText = (nodeId, text) => {
    if (!onUpdateElement || !shouldCommitSmartArtNodeText(smartArtData, nodeId, text)) {
      return;
    }
    const updated = updateSmartArtNodeText(smartArtData, nodeId, text);
    const box = { width: element.width, height: element.height };
    const reflowed = rebuildDrawingShapesIfCleared(
      updated,
      smartArtData.layout,
      resolveSmartArtDataPalette(updated),
      style,
      element.id,
      box
    );
    onUpdateElement({ smartArtData: reflowed });
  };
  const handleChangeNodeStyle = (nodeId, fill) => {
    if (!onUpdateElement) {
      return;
    }
    const next = setSmartArtNodeStyle(smartArtData, nodeId, { fillColor: fill });
    if (next !== smartArtData) {
      const box = { width: element.width, height: element.height };
      const reflowed = rebuildDrawingShapesIfCleared(
        next,
        smartArtData.layout,
        resolveSmartArtDataPalette(next),
        style,
        element.id,
        box
      );
      onUpdateElement({ smartArtData: reflowed });
    }
  };
  let content;
  if (revealedShapes && revealedShapes.length > 0) {
    content = (0, import_jsx_runtime.jsx)(
      DrawingShapeRenderer,
      {
        elementId: element.id,
        shapes: revealedShapes,
        allShapes: drawingShapes,
        style,
        palette,
        nodes,
        nodeLabels
      }
    );
  } else {
    const layout = computeSmartArtElementLayout(
      smartArtData,
      revealedNodes,
      { width: element.width, height: element.height },
      palette,
      style,
      element.id
    );
    const nodeIds = flattenNodes(revealedNodes).map((n) => n.id);
    content = (0, import_jsx_runtime.jsx)(SmartArtLayoutSvg, { layout, nodeIds, nodeLabels });
  }
  const body = editable ? (0, import_jsx_runtime.jsx)(
    SmartArtEditableLayer,
    {
      smartArtData,
      canEdit: editable,
      onCommitNodeText: handleCommitNodeText,
      palette,
      onChangeNodeStyle: handleChangeNodeStyle,
      children: content
    }
  ) : content;
  return wrapChrome(chrome, body, className, { role: a11y.role, label: a11y.label });
}
function arePropsEqual(prev, next) {
  var _a, _b2, _c, _d;
  if (prev.className !== next.className) {
    return false;
  }
  if (prev.canEdit !== next.canEdit) {
    return false;
  }
  if (prev.onUpdateElement !== next.onUpdateElement) {
    return false;
  }
  if (prev.element.id !== next.element.id) {
    return false;
  }
  if (prev.element.type !== next.element.type) {
    return false;
  }
  if (prev.element.width !== next.element.width || prev.element.height !== next.element.height) {
    return false;
  }
  if (prev.element.x !== next.element.x || prev.element.y !== next.element.y) {
    return false;
  }
  const prevData = prev.element.type === "smartArt" ? prev.element.smartArtData : void 0;
  const nextData = next.element.type === "smartArt" ? next.element.smartArtData : void 0;
  if (prevData !== nextData) {
    return false;
  }
  const prevBuild = (_a = prev.animationState) == null ? void 0 : _a.build;
  const nextBuild = (_b2 = next.animationState) == null ? void 0 : _b2.build;
  if ((prevBuild == null ? void 0 : prevBuild.kind) !== (nextBuild == null ? void 0 : nextBuild.kind) || (prevBuild == null ? void 0 : prevBuild.mode) !== (nextBuild == null ? void 0 : nextBuild.mode) || (prevBuild == null ? void 0 : prevBuild.progress) !== (nextBuild == null ? void 0 : nextBuild.progress)) {
    return false;
  }
  return sameDiagramReveal((_c = prev.animationState) == null ? void 0 : _c.diagramReveal, (_d = next.animationState) == null ? void 0 : _d.diagramReveal);
}
function sameDiagramReveal(prev, next) {
  if (prev === next) {
    return true;
  }
  if (!prev || !next) {
    return false;
  }
  if (prev.mode !== next.mode || prev.descriptor.background !== next.descriptor.background) {
    return false;
  }
  const prevIds = prev.descriptor.nodeIds;
  const nextIds = next.descriptor.nodeIds;
  if (prevIds.size !== nextIds.size) {
    return false;
  }
  for (const id of prevIds) {
    if (!nextIds.has(id)) {
      return false;
    }
  }
  return true;
}
var SmartArtRenderer = import_react2.default.memo(SmartArtRendererImpl, arePropsEqual);
function FailedToLoad7() {
  return null;
}
var LazySmartArt3DScene = import_react2.default.lazy(
  async () => {
    try {
      return await import("./SmartArt3DScene-ODKCGJBO-H2LVEL66.js");
    } catch {
      return { default: FailedToLoad7 };
    }
  }
);
var SceneErrorBoundary6 = class extends import_react2.default.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
};
function SmartArt3DRenderer({
  element,
  className,
  interactive = false,
  canEdit,
  onUpdateElement,
  animationState
}) {
  const [threeAvailable, setThreeAvailable] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    import("./three.module-AC7ESV4W-S2UPL2MH.js").then(() => {
      if (!cancelled) {
        setThreeAvailable(true);
      }
      return void 0;
    }).catch(() => {
      if (!cancelled) {
        setThreeAvailable(false);
      }
    });
    return () => {
      cancelled = true;
    };
  }, []);
  const model = (0, import_react2.useMemo)(() => {
    if (element.type !== "smartArt" || !element.smartArtData) {
      return null;
    }
    const { nodes, chrome } = element.smartArtData;
    if (nodes.length === 0) {
      return null;
    }
    const palette = resolvePalette2(element);
    const style = resolveStyle(element);
    const layoutResult = computeSmartArtElementLayout(
      element.smartArtData,
      nodes,
      { width: element.width, height: element.height },
      palette,
      style,
      element.id
    );
    return buildSmartArt3DModel(layoutResult, {
      background: chrome == null ? void 0 : chrome.backgroundColor,
      spatial: true,
      coherent3DOffNodeIds: collectCoherent3DOffNodeIds(nodes)
    });
  }, [element]);
  const svgFallback = (0, import_jsx_runtime.jsx)(SmartArtRenderer, { element, className });
  if (!model || model.meshes.length === 0 || threeAvailable === false) {
    return svgFallback;
  }
  if (threeAvailable === null) {
    return svgFallback;
  }
  const sceneNode = (0, import_jsx_runtime.jsx)(SceneErrorBoundary6, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(import_react2.Suspense, { fallback: svgFallback, children: (0, import_jsx_runtime.jsx)(
    LazySmartArt3DScene,
    {
      model,
      width: element.width,
      height: element.height,
      interactive,
      textStyle: animationState == null ? void 0 : animationState.textStyle
    }
  ) }) });
  const editEnabled = canEdit && Boolean(onUpdateElement);
  const smartArtData = element.type === "smartArt" ? element.smartArtData : void 0;
  if (!editEnabled || !smartArtData) {
    return sceneNode;
  }
  const handleCommitNodeText = (nodeId, text) => {
    if (!shouldCommitSmartArtNodeText(smartArtData, nodeId, text)) {
      return;
    }
    onUpdateElement({
      smartArtData: updateSmartArtNodeText(smartArtData, nodeId, text)
    });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", style: { width: element.width, height: element.height }, children: [
    sceneNode,
    (0, import_jsx_runtime.jsx)(
      SmartArtEditableLayer,
      {
        smartArtData,
        canEdit: true,
        onCommitNodeText: handleCommitNodeText,
        children: (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 opacity-0", children: (0, import_jsx_runtime.jsx)(SmartArtRenderer, { element, canEdit: false }) })
      }
    )
  ] });
}
function SmartArtElement({
  element,
  className,
  canEdit,
  onUpdateElement,
  animationState
}) {
  const use3D = (0, import_react2.useContext)(SmartArt3DContext);
  if (use3D) {
    return (0, import_jsx_runtime.jsx)(
      SmartArt3DRenderer,
      {
        element,
        className,
        canEdit,
        onUpdateElement,
        animationState
      }
    );
  }
  return (0, import_jsx_runtime.jsx)(
    SmartArtRenderer,
    {
      element,
      className,
      canEdit,
      onUpdateElement,
      animationState
    }
  );
}
function groupIntoParagraphs2(element, fieldContext) {
  return groupIntoParagraphs(
    element,
    fieldContext ? (seg) => {
      if (seg.fieldType) {
        const substituted = substituteFieldText(seg.text, seg.fieldType, fieldContext);
        if (substituted !== seg.text) {
          return { ...seg, text: substituted };
        }
      }
      return seg;
    } : void 0
  );
}
function getAlignmentProps(align) {
  switch (align) {
    case "center":
      return { startOffset: "50%", textAnchor: "middle" };
    case "right":
      return { startOffset: "100%", textAnchor: "end" };
    case "left":
    case "justify":
    default:
      return { startOffset: "0%", textAnchor: "start" };
  }
}
function getSegmentTspanProps(segment, element, fallbackColor) {
  var _a, _b2, _c, _d, _e;
  const s = segment.style || {};
  const decos = [];
  if (s.underline || s.hyperlink) {
    decos.push("underline");
  }
  if (s.strikethrough) {
    decos.push("line-through");
  }
  const fill = s.hyperlink ? normalizeHexColor2(s.color || ((_a = element.textStyle) == null ? void 0 : _a.color), HYPERLINK_COLOR) : normalizeHexColor2(s.color || ((_b2 = element.textStyle) == null ? void 0 : _b2.color), fallbackColor);
  return {
    fill,
    fontSize: s.fontSize ?? ((_c = element.textStyle) == null ? void 0 : _c.fontSize) ?? DEFAULT_TEXT_FONT_SIZE,
    fontWeight: s.bold ? 700 : 400,
    fontStyle: s.italic ? "italic" : void 0,
    fontFamily: s.fontFamily || ((_d = element.textStyle) == null ? void 0 : _d.fontFamily) ? getSubstituteFontFamily(s.fontFamily || ((_e = element.textStyle) == null ? void 0 : _e.fontFamily) || "") : DEFAULT_FONT_FAMILY2,
    textDecoration: decos.length > 0 ? decos.join(" ") : void 0
  };
}
function resolveSegmentFont(segment, element) {
  var _a, _b2, _c, _d;
  const s = segment.style || {};
  const family = s.fontFamily || ((_a = element.textStyle) == null ? void 0 : _a.fontFamily);
  return {
    fontFamily: family ? getSubstituteFontFamily(family) : DEFAULT_FONT_FAMILY2,
    fontSizePx: s.fontSize ?? ((_b2 = element.textStyle) == null ? void 0 : _b2.fontSize) ?? DEFAULT_TEXT_FONT_SIZE,
    bold: s.bold ?? ((_c = element.textStyle) == null ? void 0 : _c.bold),
    italic: s.italic ?? ((_d = element.textStyle) == null ? void 0 : _d.italic)
  };
}
function EnvelopeGlyph({
  glyphKey,
  glyph,
  tspanProps
}) {
  if (!glyph.slices || glyph.slices.length <= 1) {
    return (0, import_jsx_runtime.jsx)("text", { x: glyph.x, y: glyph.y, transform: glyph.transform, ...tspanProps, children: glyph.char });
  }
  return (0, import_jsx_runtime.jsx)("g", { "data-glyph-slices": glyph.slices.length, children: glyph.slices.map((slice, si) => {
    const clipId = `${glyphKey}-s${si}`;
    return (0, import_jsx_runtime.jsxs)(import_react2.default.Fragment, { children: [
      (0, import_jsx_runtime.jsx)("clipPath", { id: clipId, clipPathUnits: "userSpaceOnUse", children: (0, import_jsx_runtime.jsx)(
        "rect",
        {
          x: slice.clipX0,
          y: -1e5,
          width: Math.max(0, slice.clipX1 - slice.clipX0),
          height: 2e5
        }
      ) }),
      (0, import_jsx_runtime.jsx)(
        "text",
        {
          x: glyph.x,
          y: glyph.y,
          transform: slice.transform,
          clipPath: `url(#${clipId})`,
          ...tspanProps,
          children: glyph.char
        }
      )
    ] }, clipId);
  }) });
}
function EnvelopeLine({
  paraSegments,
  glyphs,
  element,
  fallbackColor,
  lineIdPrefix
}) {
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: glyphs.map((g, i) => (0, import_jsx_runtime.jsx)(
    EnvelopeGlyph,
    {
      glyphKey: `${lineIdPrefix}-g${i}`,
      glyph: g,
      tspanProps: getSegmentTspanProps(paraSegments[g.segmentIndex], element, fallbackColor)
    },
    i
  )) });
}
function WarpedText({
  element,
  width,
  height,
  fallbackColor,
  fieldContext
}) {
  var _a, _b2, _c, _d, _e, _f, _g;
  if (!hasTextProperties(element)) {
    return null;
  }
  const textEl = element;
  const preset = (_a = textEl.textStyle) == null ? void 0 : _a.textWarpPreset;
  if (!preset || !shouldUseSvgWarp(preset)) {
    return null;
  }
  const paragraphs = groupIntoParagraphs2(textEl, fieldContext);
  if (paragraphs.length === 0) {
    return null;
  }
  const lineCount = paragraphs.length;
  const pathIdPrefix = `warp-${element.id}`;
  const warpAdj = (_b2 = textEl.textStyle) == null ? void 0 : _b2.textWarpAdj;
  const warpAdj2 = (_c = textEl.textStyle) == null ? void 0 : _c.textWarpAdj2;
  const align = ((_d = textEl.textStyle) == null ? void 0 : _d.align) ?? "center";
  const { startOffset, textAnchor } = getAlignmentProps(align);
  const baseFontSize = ((_e = textEl.textStyle) == null ? void 0 : _e.fontSize) ?? DEFAULT_TEXT_FONT_SIZE;
  const baseFontFamily = ((_f = textEl.textStyle) == null ? void 0 : _f.fontFamily) ? getSubstituteFontFamily(textEl.textStyle.fontFamily) : DEFAULT_FONT_FAMILY2;
  const baseFill = normalizeHexColor2((_g = textEl.textStyle) == null ? void 0 : _g.color, fallbackColor);
  if (hasGlyphEnvelope(preset)) {
    return (0, import_jsx_runtime.jsx)(
      "svg",
      {
        width,
        height,
        viewBox: `0 0 ${width} ${height}`,
        xmlns: "http://www.w3.org/2000/svg",
        style: { overflow: "visible", position: "absolute", inset: 0, pointerEvents: "none" },
        "aria-hidden": "true",
        children: paragraphs.map((para, paraIdx) => {
          const segsInput = para.segments.map((seg, i) => ({
            text: seg.text,
            font: resolveSegmentFont(seg, textEl),
            segmentIndex: i
          }));
          const glyphs = buildGlyphEnvelope(
            preset,
            segsInput,
            width,
            height,
            align,
            warpAdj,
            warpAdj2,
            paraIdx,
            lineCount
          );
          return (0, import_jsx_runtime.jsx)(
            EnvelopeLine,
            {
              paraSegments: para.segments,
              glyphs,
              element: textEl,
              fallbackColor,
              lineIdPrefix: `warp-${element.id}-l${paraIdx}`
            },
            `envelope-line-${paraIdx}`
          );
        })
      }
    );
  }
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      width,
      height,
      viewBox: `0 0 ${width} ${height}`,
      xmlns: "http://www.w3.org/2000/svg",
      style: {
        overflow: "visible",
        position: "absolute",
        inset: 0,
        pointerEvents: "none"
      },
      "aria-hidden": "true",
      children: [
        (0, import_jsx_runtime.jsx)("defs", { children: paragraphs.map((_para, i) => (0, import_jsx_runtime.jsx)(
          "path",
          {
            id: `${pathIdPrefix}-${i}`,
            d: getWarpPath(preset, width, height, i, lineCount, warpAdj, warpAdj2),
            fill: "none"
          },
          `${pathIdPrefix}-def-${i}`
        )) }),
        paragraphs.map((para, paraIdx) => {
          var _a2, _b3;
          return (0, import_jsx_runtime.jsx)(
            "text",
            {
              fontSize: baseFontSize,
              fontFamily: baseFontFamily,
              fill: baseFill,
              fontWeight: ((_a2 = textEl.textStyle) == null ? void 0 : _a2.bold) ? 700 : 400,
              fontStyle: ((_b3 = textEl.textStyle) == null ? void 0 : _b3.italic) ? "italic" : "normal",
              children: (0, import_jsx_runtime.jsx)(
                "textPath",
                {
                  href: `#${pathIdPrefix}-${paraIdx}`,
                  startOffset,
                  textAnchor,
                  children: para.segments.map((seg, segIdx) => (0, import_jsx_runtime.jsx)(
                    "tspan",
                    {
                      ...getSegmentTspanProps(seg, textEl, fallbackColor),
                      children: seg.text
                    },
                    `${pathIdPrefix}-ts-${paraIdx}-${segIdx}`
                  ))
                }
              )
            },
            `${pathIdPrefix}-txt-${paraIdx}`
          );
        })
      ]
    }
  );
}
var GLYPH_BY_SHAPE = Object.fromEntries(
  ACTION_BUTTON_PRESETS.map((p) => [p.shapeType, p.iconPath])
);
GLYPH_BY_SHAPE["actionButtonForwardOrNext"] = GLYPH_BY_SHAPE["actionButtonForwardNext"];
GLYPH_BY_SHAPE["actionButtonBackOrPrevious"] = GLYPH_BY_SHAPE["actionButtonBackPrevious"];
function isActionButtonShape(shapeType) {
  return Boolean(shapeType && shapeType in GLYPH_BY_SHAPE);
}
function getActionButtonGlyphPath(shapeType) {
  if (!shapeType) {
    return void 0;
  }
  const path = GLYPH_BY_SHAPE[shapeType];
  return path && path.length > 0 ? path : void 0;
}
function ActionButtonGlyphOverlay({
  element,
  color
}) {
  var _a;
  const shapeType = "shapeType" in element ? element.shapeType : void 0;
  const path = getActionButtonGlyphPath(shapeType);
  if (!path) {
    return null;
  }
  const stroke = color ?? ("textStyle" in element && ((_a = element.textStyle) == null ? void 0 : _a.color) || "#ffffff");
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      viewBox: "0 0 24 24",
      width: "100%",
      height: "100%",
      preserveAspectRatio: "xMidYMid meet",
      style: {
        position: "absolute",
        inset: 0,
        pointerEvents: "none",
        padding: "20%"
      },
      "aria-hidden": "true",
      children: (0, import_jsx_runtime.jsx)(
        "path",
        {
          d: path,
          fill: "none",
          stroke,
          strokeWidth: 2,
          strokeLinecap: "round",
          strokeLinejoin: "round"
        }
      )
    }
  );
}
function renderTextElementBody(options) {
  var _a;
  const {
    el,
    vecShape,
    isTxtEl,
    txtS,
    txtSE,
    findHl,
    onHyperlinkClick,
    fieldContext,
    presentationElementStates,
    isPresentationPassive,
    placeholderPromptMode = "present",
    slideElements
  } = options;
  const isLinkedTextBox2 = isLinkedTextBox(el);
  const linkedSegments = getOverflowSegments(el, slideElements);
  const useSvgWarp = shouldUseSvgWarp(
    hasTextProperties(el) ? (_a = el.textStyle) == null ? void 0 : _a.textWarpPreset : void 0
  );
  const scene3dStyle = hasTextProperties(el) ? buildTextBody3DSceneStyle2(el.textStyle, { width: el.width, height: el.height }) : void 0;
  const rotationTransform = getTextBodyRotationTransform(el);
  const composedTransform = [rotationTransform, getTextCompensationTransform(el), scene3dStyle == null ? void 0 : scene3dStyle.transform].filter(Boolean).join(" ") || void 0;
  const transformStyle = {
    transform: composedTransform,
    // A COM-measured homography (`scene3dStyle.transformOrigin === '0 0'`)
    // MUST win over the default centred origin, or the matrix3d's baked-in
    // translation (relative to the element's own top-left) is applied from
    // the wrong pivot and the projection is wrong (see
    // `visual-3d-camera-homography`'s module doc comment). This used to be
    // hardcoded to `'center'` unconditionally, silently clobbering
    // `scene3dStyle.transformOrigin` the same way Svelte's `ElementRenderer`
    // once re-clobbered `pointerEvents` from its own interactive flag.
    transformOrigin: (scene3dStyle == null ? void 0 : scene3dStyle.transformOrigin) ?? "center",
    ...(scene3dStyle == null ? void 0 : scene3dStyle.perspective) ? { perspective: scene3dStyle.perspective } : {},
    // COM-calibrated off-axis correction for a handful of camera presets
    // (see `visual-3d-camera`'s module doc comment); only reachable when an
    // explicit `a:camera/a:rot`/`@fov`/`@zoom` override sits alongside one of
    // those presets, since the homography path above never sets it.
    ...(scene3dStyle == null ? void 0 : scene3dStyle.perspectiveOrigin) ? { perspectiveOrigin: scene3dStyle.perspectiveOrigin } : {},
    ...(scene3dStyle == null ? void 0 : scene3dStyle.transformStyle) ? { transformStyle: scene3dStyle.transformStyle } : {},
    ...isLinkedTextBox2 ? { overflow: "hidden" } : {}
  };
  const shapeType = "shapeType" in el ? el.shapeType : void 0;
  const placeholderPrompt = placeholderPromptDescriptor(el, placeholderPromptMode);
  const shouldRenderText = isTxtEl || placeholderPrompt !== null;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    vecShape,
    isActionButtonShape(shapeType) && (0, import_jsx_runtime.jsx)(ActionButtonGlyphOverlay, { element: el }),
    shouldRenderText && (useSvgWarp ? (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: cn(
          "relative z-10 w-full h-full",
          onHyperlinkClick ? "" : "pointer-events-none"
        ),
        style: { ...getTextLayoutStyle(el), ...transformStyle },
        children: (0, import_jsx_runtime.jsx)(
          WarpedText,
          {
            element: el,
            width: el.width,
            height: el.height,
            fallbackColor: DEFAULT_TEXT_COLOR,
            findHighlights: findHl,
            fieldContext
          }
        )
      }
    ) : (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: cn(
          "relative z-10 w-full h-full whitespace-pre-wrap break-words leading-[1.3]",
          onHyperlinkClick ? "" : "pointer-events-none"
        ),
        style: {
          ...getTextLayoutStyle(el),
          ...txtS,
          ...getTextWarpStyle(txtSE),
          ...transformStyle
        },
        children: renderTextSegments(
          el,
          DEFAULT_TEXT_COLOR,
          void 0,
          findHl,
          onHyperlinkClick,
          fieldContext,
          presentationElementStates,
          linkedSegments ?? void 0,
          !isPresentationPassive,
          placeholderPrompt
        )
      }
    ))
  ] });
}
function ZoomElementRenderer({
  element,
  slides,
  isPresentationMode,
  onZoomClick,
  sourceSlideIndex
}) {
  const { t } = useTranslation();
  const summaryView = buildSummaryZoomView(
    element,
    (index) => {
      const slide = slides == null ? void 0 : slides[index];
      return slide ? {
        slideNumber: slide.slideNumber,
        sectionName: slide.sectionName,
        backgroundColor: slide.backgroundColor
      } : void 0;
    },
    // The tile captions and aria-labels are built in shared; without this the
    // slide subtitle and the container's aria-label stay English.
    (key, params) => t(key, params ?? {})
  );
  if (summaryView) {
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: "zoom-element-container zoom-summary-container",
        style: summaryView.containerStyle,
        "data-zoom-type": "summary",
        "data-testid": "zoom-element",
        role: "group",
        "aria-label": summaryView.ariaLabel,
        children: [
          summaryView.tiles.map((tile) => (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "zoom-summary-tile",
              style: {
                ...tile.style,
                backgroundColor: tile.backgroundColor,
                overflow: "hidden",
                border: "1px solid rgba(0, 0, 0, 0.12)",
                cursor: isPresentationMode ? "pointer" : "default"
              },
              "data-zoom-target": tile.targetSlideIndex,
              "data-section-id": tile.sectionId,
              role: isPresentationMode ? "button" : void 0,
              tabIndex: isPresentationMode ? 0 : -1,
              "aria-label": tile.ariaLabel,
              onClick: (event) => {
                if (isPresentationMode && onZoomClick) {
                  event.stopPropagation();
                  onZoomClick(tile.targetSlideIndex, sourceSlideIndex ?? 0);
                }
              },
              onKeyDown: (event) => {
                if (isPresentationMode && onZoomClick && (event.key === "Enter" || event.key === " ")) {
                  event.preventDefault();
                  event.stopPropagation();
                  onZoomClick(tile.targetSlideIndex, sourceSlideIndex ?? 0);
                }
              },
              children: tile.imageSrc ? (0, import_jsx_runtime.jsx)(
                "img",
                {
                  src: tile.imageSrc,
                  alt: tile.ariaLabel,
                  style: { width: "100%", height: "100%", objectFit: "contain" }
                }
              ) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                (0, import_jsx_runtime.jsx)("div", { children: tile.label }),
                (0, import_jsx_runtime.jsx)("div", { children: tile.slideLabel })
              ] })
            },
            tile.key
          )),
          (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", right: 4, bottom: 4, fontSize: 9 }, children: t("pptx.zoom.summaryZoom") })
        ]
      }
    );
  }
  const targetSlide = slides == null ? void 0 : slides[element.targetSlideIndex];
  const handleClick = (e) => {
    if (!isPresentationMode || !onZoomClick) {
      return;
    }
    e.stopPropagation();
    onZoomClick(element.targetSlideIndex, sourceSlideIndex ?? 0);
  };
  const handleKeyDown = (e) => {
    if (!isPresentationMode || !onZoomClick) {
      return;
    }
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      e.stopPropagation();
      onZoomClick(element.targetSlideIndex, sourceSlideIndex ?? 0);
    }
  };
  const hasPreviewImage = Boolean(element.imageData);
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "zoom-element-container",
      style: {
        width: "100%",
        height: "100%",
        position: "relative",
        cursor: isPresentationMode ? "pointer" : "default",
        overflow: "hidden",
        borderRadius: "4px",
        boxShadow: "0 2px 8px rgba(0, 0, 0, 0.15)",
        transition: "box-shadow 0.2s ease, transform 0.15s ease"
      },
      "data-zoom-type": element.zoomType,
      "data-zoom-target": element.targetSlideIndex,
      "data-testid": "zoom-element",
      onClick: handleClick,
      onKeyDown: handleKeyDown,
      role: isPresentationMode ? "button" : void 0,
      tabIndex: isPresentationMode ? 0 : -1,
      "aria-label": `Zoom to slide ${element.targetSlideIndex + 1}${element.zoomType === "section" && element.targetSectionId ? ` (section: ${element.targetSectionId})` : ""}`,
      children: [
        hasPreviewImage ? renderImg(
          element,
          {
            width: "100%",
            height: "100%",
            objectFit: "contain"
          },
          void 0,
          `Preview of slide ${element.targetSlideIndex + 1}`
        ) : (0, import_jsx_runtime.jsx)(ZoomSlideThumbnail, { targetSlide, slideIndex: element.targetSlideIndex }),
        isPresentationMode && (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "zoom-hover-overlay",
            style: {
              position: "absolute",
              inset: 0,
              background: "transparent",
              transition: "background 0.2s ease",
              pointerEvents: "none"
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            style: {
              position: "absolute",
              bottom: 4,
              right: 4,
              fontSize: "9px",
              padding: "1px 4px",
              borderRadius: "2px",
              backgroundColor: "rgba(0, 0, 0, 0.5)",
              color: "#fff",
              pointerEvents: "none",
              lineHeight: "1.4"
            },
            children: element.zoomType === "section" ? t("pptx.zoom.sectionZoom") : t("pptx.zoom.slideZoom")
          }
        )
      ]
    }
  );
}
function ZoomSlideThumbnail({
  targetSlide,
  slideIndex
}) {
  const bgColor = (targetSlide == null ? void 0 : targetSlide.backgroundColor) || "#f0f0f0";
  const slideLabel = targetSlide ? `Slide ${targetSlide.slideNumber}` : `Slide ${slideIndex + 1}`;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "zoom-thumbnail",
      style: {
        width: "100%",
        height: "100%",
        backgroundColor: bgColor,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        border: "1px solid rgba(0, 0, 0, 0.1)"
      },
      children: [
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            style: {
              fontSize: "14px",
              fontWeight: 600,
              color: "rgba(0, 0, 0, 0.5)",
              marginBottom: "4px"
            },
            children: slideLabel
          }
        ),
        (targetSlide == null ? void 0 : targetSlide.sectionName) && (0, import_jsx_runtime.jsx)(
          "div",
          {
            style: {
              fontSize: "10px",
              color: "rgba(0, 0, 0, 0.4)"
            },
            children: targetSlide.sectionName
          }
        )
      ]
    }
  );
}
function renderBody(options) {
  const {
    el,
    isImg,
    isEditing,
    editText,
    spellCheck,
    txtSE,
    txtS,
    vecShape,
    imgStyle,
    imgFilter,
    imgOpacity,
    imgAlt,
    isTxtEl,
    media,
    tableSt,
    isSel,
    doInk,
    doGrp,
    renderGroupChild,
    onEditChange,
    onCommit,
    onCancel,
    onCellSel,
    onCellCommit,
    onColResize,
    onRowResize,
    isPresentationPassive,
    isStaticSurface,
    handleMediaPlayStateChange,
    allSlides,
    onZoomClick,
    sourceSlideIndex,
    tableStyleContext,
    onFormatText,
    canEditSmartArt,
    onUpdateSmartArtElement,
    canEditChart,
    onUpdateChartElement,
    presentationElementStates
  } = options;
  const animationState = presentationElementStates == null ? void 0 : presentationElementStates.get(el.id);
  if (el.type === "model3d") {
    return (0, import_jsx_runtime.jsx)(
      Model3DRenderer,
      {
        element: el,
        width: el.width,
        height: el.height,
        interactive: !isPresentationPassive
      }
    );
  }
  if (el.type === "zoom") {
    return (0, import_jsx_runtime.jsx)(
      ZoomElementRenderer,
      {
        element: el,
        slides: allSlides,
        isPresentationMode: isPresentationPassive,
        onZoomClick,
        sourceSlideIndex
      }
    );
  }
  if (isImg) {
    return (0, import_jsx_runtime.jsxs)("div", { className: "absolute inset-0 pointer-events-none", style: getImageSurfaceStyle(el), children: [
      renderImg(el, imgStyle, imgFilter, imgAlt, imgOpacity),
      vecShape ? (0, import_jsx_runtime.jsx)("div", { className: "pointer-events-none absolute inset-0", children: vecShape }) : null
    ] });
  }
  if (isEditing) {
    return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      vecShape,
      (0, import_jsx_runtime.jsx)(
        InlineTextEditor,
        {
          initialText: editText,
          spellCheck,
          rtl: txtSE == null ? void 0 : txtSE.rtl,
          textDirection: txtSE == null ? void 0 : txtSE.textDirection,
          textStyle: txtS,
          textStyleRaw: txtSE,
          layoutStyle: getTextLayoutStyle(el),
          element: el,
          onCommit,
          onCancel,
          onEditChange,
          onFormatText
        }
      )
    ] });
  }
  if (el.type === "table") {
    return renderTableElement(el, txtS, {
      editable: isSel,
      selectedCell: isSel ? tableSt : null,
      onSelectCell: onCellSel,
      onCommitCellEdit: onCellCommit,
      onResizeColumns: onColResize,
      onResizeRow: onRowResize,
      styleCtx: tableStyleContext
    });
  }
  if (el.type === "chart") {
    return (0, import_jsx_runtime.jsx)(
      ChartElementView,
      {
        element: el,
        editable: Boolean(isSel && canEditChart),
        onUpdateElement: onUpdateChartElement,
        animationState
      }
    );
  }
  if (el.type === "smartArt") {
    return (0, import_jsx_runtime.jsx)(
      SmartArtElement,
      {
        element: el,
        canEdit: canEditSmartArt,
        onUpdateElement: onUpdateSmartArtElement,
        animationState
      }
    );
  }
  if (el.type === "media") {
    return renderMediaElement(el, media, {
      // Present mode autoplays every media element on the active slide,
      // not only ones persisted with their own `autoPlay` flag (matching
      // the other four bindings' unconditional `presenting`-gated
      // `startMediaAutoplay` call): a clip inserted via Insert > Media
      // never gets that flag, so gating on `el.autoPlay` left it silently
      // paused the moment the show reached its slide.
      autoPlay: isPresentationPassive === true,
      fullScreen: isPresentationPassive && Boolean(el.fullScreen),
      isPresentationMode: isPresentationPassive,
      // A still of a slide never carries a transport, whatever the canvas
      // does. The rule is shared so the five bindings cannot drift on it.
      showTransport: mediaTransportVisible({
        presenting: isPresentationPassive === true,
        preview: isStaticSurface === true,
        canvasTransport: true
      }),
      // ...and never the play badge / placeholder box either: a still is
      // slide content, and the transition overlay paints one (issue #147).
      preview: isStaticSurface === true,
      onPlayStateChange: handleMediaPlayStateChange
    });
  }
  if (doInk && isInkElement(el)) {
    return renderInk(el, {
      replay: isPresentationPassive,
      pressureSensitive: true
    });
  }
  if (el.type === "contentPart") {
    return renderContentPart(el, {
      replay: isPresentationPassive
    });
  }
  if (el.type === "ole") {
    return (0, import_jsx_runtime.jsx)(OleRenderer, { element: el });
  }
  if (doGrp && el.type === "group" && el.children) {
    if (renderGroupChild) {
      return (0, import_jsx_runtime.jsx)("div", { className: "relative w-full h-full pointer-events-none", children: el.children.map(renderGroupChild) });
    }
    return renderGroup(el.children, getGroupChildParentFill(el));
  }
  if (shouldRenderFallbackLabel(el, isTxtEl)) {
    return (0, import_jsx_runtime.jsx)("div", { className: "w-full h-full flex items-center justify-center text-[11px] text-white/80 pointer-events-none", children: getElementLabel(el) });
  }
  return renderTextElementBody(options);
}
function defaultTextColorForFill(fillColor) {
  const hex = String(fillColor ?? "").replace(/^#/u, "");
  if (!/^[0-9a-f]{6}$/iu.test(hex)) {
    return DEFAULT_TEXT_COLOR;
  }
  const channels = [0, 2, 4].map((offset) => Number.parseInt(hex.slice(offset, offset + 2), 16) / 255).map((channel) => channel <= 0.04045 ? channel / 12.92 : ((channel + 0.055) / 1.055) ** 2.4);
  const luminance = channels[0] * 0.2126 + channels[1] * 0.7152 + channels[2] * 0.0722;
  return luminance > 0.179 ? DEFAULT_TEXT_COLOR : "#ffffff";
}
function getStaticElementVisualState(element, parentGroupFill) {
  var _a, _b2;
  const style = hasShapeProperties(element) ? element.shapeStyle : void 0;
  const hasFill = (style == null ? void 0 : style.fillColor) !== void 0 && style.fillColor !== "transparent" || Boolean(buildCssGradientFromShapeStyle(style) || (style == null ? void 0 : style.fillGradient)) || (style == null ? void 0 : style.fillMode) === "pattern" && Boolean(style.fillPatternPreset);
  const fill = normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR);
  const strokeWidth = paintedStrokeWidth(style);
  const stroke = normalizeHexColor2(style == null ? void 0 : style.strokeColor, DEFAULT_STROKE_COLOR);
  const baseVisualStyle = getShapeVisualStyle(element, hasFill, fill, strokeWidth, stroke);
  const inheritedFill = resolveGroupChildFill(element, parentGroupFill);
  const visualStyle = inheritedFill ? {
    ...baseVisualStyle,
    backgroundColor: inheritedFill.backgroundColor,
    backgroundImage: inheritedFill.backgroundImage,
    backgroundRepeat: inheritedFill.backgroundRepeat,
    backgroundSize: inheritedFill.backgroundSize,
    backgroundPosition: inheritedFill.backgroundPosition
  } : baseVisualStyle;
  const textStyle = getTextStyleForElement(
    element,
    element.type === "shape" && hasFill ? defaultTextColorForFill(fill) : DEFAULT_TEXT_COLOR
  );
  const isImage = element.type === "picture" || element.type === "image";
  const letsTextOverflow = hasTextProperties(element) && ((_a = element.textStyle) == null ? void 0 : _a.autoFitMode) === "shrink" && (element.type === "text" || ((_b2 = element.locks) == null ? void 0 : _b2.txBox) === true);
  const shapeType = hasShapeProperties(element) ? element.shapeType : void 0;
  const isCallout = isWedgeCalloutPresetShape(shapeType) || String(shapeType ?? "").toLowerCase().includes("callout");
  return {
    hasFill,
    fill,
    strokeWidth,
    stroke,
    visualStyle,
    textStyle,
    isImage,
    letsTextOverflow,
    isCallout
  };
}
function getStaticElementInteractionState(element, hasActionHandler, exposeElementId) {
  const action = element.actionClick;
  const isActionable = Boolean(action && hasActionHandler);
  const contractRole = exposeElementId ? getAriaRole(element, { actionable: isElementActionable(element) }) : isActionable ? "button" : void 0;
  return { action, isActionable, contractRole };
}
function getStaticElementWrapperClassName(element, visual, positioned, isActionable) {
  const overflowClass = visual.isImage || element.type === "group" || isConnectorOrLineElement(element) || visual.isCallout || visual.letsTextOverflow ? "" : "overflow-hidden";
  const pointerClass = isActionable ? "pointer-events-auto cursor-pointer" : "pointer-events-none";
  return `${positioned ? "absolute" : "relative"} ${overflowClass} ${pointerClass}`;
}
var noop = () => {
};
var EMPTY_MEDIA_DATA_URLS = /* @__PURE__ */ new Map();
function StaticElementRendererImpl({
  element,
  activeSlide,
  allSlides,
  mediaDataUrls = EMPTY_MEDIA_DATA_URLS,
  sourceSlideIndex,
  zIndex,
  positioned = true,
  fieldContext,
  tableStyleContext,
  parentGroupFill,
  onActionClick,
  actionRequiresModifier = false,
  animation,
  imageAnimation,
  exposeElementId = false,
  suppressReflection = false
}) {
  if (!isElementRendered(element)) {
    return null;
  }
  const {
    hasFill,
    fill,
    strokeWidth,
    stroke,
    visualStyle,
    textStyle,
    isImage,
    letsTextOverflow,
    isCallout
  } = getStaticElementVisualState(element, parentGroupFill);
  const { action, isActionable, contractRole } = getStaticElementInteractionState(
    element,
    Boolean(onActionClick),
    exposeElementId
  );
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-static-element-type": element.type,
      "data-element-id": exposeElementId ? element.id : void 0,
      "data-pptx-element": exposeElementId ? "true" : void 0,
      "data-pptx-action": isActionable ? "click" : void 0,
      className: getStaticElementWrapperClassName(
        element,
        { isImage, letsTextOverflow, isCallout },
        positioned,
        isActionable
      ),
      role: contractRole,
      "aria-label": exposeElementId ? getAriaLabel(element) : void 0,
      "aria-roledescription": exposeElementId ? getAriaRoleDescription(element) : void 0,
      tabIndex: isActionable ? 0 : void 0,
      title: isActionable ? (action == null ? void 0 : action.tooltip) || (action == null ? void 0 : action.url) || void 0 : void 0,
      onClick: isActionable ? (event) => {
        if (actionRequiresModifier && !event.ctrlKey && !event.metaKey) {
          return;
        }
        event.stopPropagation();
        event.preventDefault();
        onActionClick == null ? void 0 : onActionClick(element.id, action);
      } : void 0,
      onKeyDown: isActionable ? (event) => {
        if (event.key !== "Enter" && event.key !== " ") {
          return;
        }
        event.preventDefault();
        event.stopPropagation();
        onActionClick == null ? void 0 : onActionClick(element.id, action);
      } : void 0,
      style: {
        left: positioned ? element.x : void 0,
        top: positioned ? element.y : void 0,
        width: positioned ? Math.max(element.width, 1) : "100%",
        height: positioned ? Math.max(element.height, 1) : "100%",
        transform: positioned ? getElementTransform(element) : void 0,
        transformOrigin: "center",
        zIndex,
        ...visualStyle,
        ...isImage ? {
          backgroundColor: "transparent",
          backgroundImage: void 0,
          backgroundRepeat: void 0,
          backgroundSize: void 0,
          backgroundPosition: void 0,
          borderRadius: void 0,
          clipPath: void 0,
          overflow: "visible"
        } : {},
        animation
      },
      children: [
        (0, import_jsx_runtime.jsx)(ShapeEffectOverlay, { element, suppressReflection }),
        element.type === "group" ? (0, import_jsx_runtime.jsx)("div", { className: "relative w-full h-full", children: (element.children ?? []).map((child, index) => (0, import_jsx_runtime.jsx)(
          StaticElementRenderer,
          {
            element: child,
            activeSlide,
            allSlides,
            mediaDataUrls,
            sourceSlideIndex,
            zIndex: index,
            fieldContext,
            tableStyleContext,
            parentGroupFill: getGroupChildParentFill(element, parentGroupFill),
            onActionClick,
            actionRequiresModifier,
            exposeElementId
          },
          child.id
        )) }) : renderBody({
          el: element,
          isImg: isImage,
          isEditing: false,
          editText: "",
          spellCheck: false,
          txtSE: hasTextProperties(element) ? element.textStyle : void 0,
          txtS: textStyle,
          vecShape: renderVectorShape(
            element,
            isImage ? false : hasFill,
            fill,
            strokeWidth,
            stroke
          ),
          imgStyle: imageAnimation ? { ...getImageRenderStyle(element), animation: imageAnimation } : getImageRenderStyle(element),
          imgFilter: getImageEffectsFilter(element),
          imgOpacity: getImageEffectsOpacity(element),
          imgAlt: "",
          isTxtEl: isEditableTextElement(element),
          media: mediaDataUrls,
          tableSt: null,
          isSel: false,
          doInk: true,
          doGrp: false,
          onEditChange: noop,
          onCommit: noop,
          onCancel: noop,
          isPresentationPassive: false,
          // This renderer only ever paints a STILL of a slide (presenter
          // console panes, thumbnails, previews). It is not in presentation
          // mode, so without saying so a video here would carry Chrome's
          // scrubber over a slide nobody can play.
          isStaticSurface: true,
          placeholderPromptMode: "thumbnail",
          slideElements: activeSlide == null ? void 0 : activeSlide.elements,
          allSlides,
          sourceSlideIndex,
          fieldContext,
          tableStyleContext,
          canEditSmartArt: false,
          canEditChart: false
        })
      ]
    }
  );
}
var StaticElementRenderer = import_react2.default.memo(StaticElementRendererImpl);
StaticElementRenderer.displayName = "StaticElementRenderer";
var PREVIEW_CANVAS_WIDTH = 1280;
var PREVIEW_CANVAS_HEIGHT = 720;
var PREVIEW_TILE_WIDTH = 144;
var PREVIEW_SCALE = PREVIEW_TILE_WIDTH / PREVIEW_CANVAS_WIDTH;
var PREVIEW_TILE_HEIGHT = Math.round(PREVIEW_CANVAS_HEIGHT * PREVIEW_SCALE);
function SlideTemplatePreview({
  templateId,
  scheme
}) {
  const previewSlide = (0, import_react2.useMemo)(() => {
    const content = buildSlideTemplateContent(templateId, {
      slideWidth: PREVIEW_CANVAS_WIDTH,
      slideHeight: PREVIEW_CANVAS_HEIGHT,
      ...scheme ? { scheme } : {},
      idFor: (index) => `tpl-preview-${templateId}-${index}`
    });
    return {
      id: `tpl-preview-${templateId}`,
      rId: "",
      slideNumber: 1,
      elements: content.elements,
      ...content.backgroundColor ? { backgroundColor: content.backgroundColor } : {}
    };
  }, [templateId, scheme]);
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      "aria-hidden": "true",
      className: "overflow-hidden pointer-events-none rounded",
      style: {
        width: PREVIEW_TILE_WIDTH,
        height: PREVIEW_TILE_HEIGHT,
        backgroundColor: previewSlide.backgroundColor ?? "#FFFFFF"
      },
      children: (0, import_jsx_runtime.jsx)(
        "div",
        {
          style: {
            position: "relative",
            width: PREVIEW_CANVAS_WIDTH,
            height: PREVIEW_CANVAS_HEIGHT,
            transform: `scale(${PREVIEW_SCALE})`,
            transformOrigin: "top left"
          },
          children: previewSlide.elements.map((element, index) => (0, import_jsx_runtime.jsx)(
            StaticElementRenderer,
            {
              element,
              activeSlide: previewSlide,
              allSlides: [previewSlide],
              zIndex: index
            },
            element.id
          ))
        }
      )
    }
  );
}
function useModalFocus(open, panelRef, onClose, initialFocusRef) {
  const onCloseRef = (0, import_react2.useRef)(onClose);
  (0, import_react2.useEffect)(() => {
    onCloseRef.current = onClose;
  }, [onClose]);
  (0, import_react2.useEffect)(() => {
    const panel = panelRef.current;
    if (!open || !panel) {
      return;
    }
    return activateModalFocus(panel, {
      initialFocus: initialFocusRef == null ? void 0 : initialFocusRef.current,
      onEscape: () => onCloseRef.current()
    });
  }, [initialFocusRef, open, panelRef]);
}
function SlideTemplateGalleryDialog({
  isOpen,
  onClose,
  onInsert,
  scheme
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const [selected, setSelected] = (0, import_react2.useState)(null);
  const dialogRef = (0, import_react2.useRef)(null);
  useModalFocus(isOpen, dialogRef, onClose);
  const handleInsert = (0, import_react2.useCallback)(() => {
    if (!selected) {
      return;
    }
    onInsert(selected);
    onClose();
  }, [selected, onInsert, onClose]);
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        onClick: onClose,
        "aria-label": t("pptx.slideTemplates.close")
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            ref: dialogRef,
            style: panelStyle,
            className: "pointer-events-auto w-[640px] max-w-[92vw] max-h-[80vh] rounded-lg border border-border bg-background shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.slideTemplates.galleryTitle"),
            tabIndex: -1,
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-4 py-3 border-b border-border touch-none",
                  children: [
                    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-0.5", children: [
                      (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-medium text-foreground", children: t("pptx.slideTemplates.galleryTitle") }),
                      (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.slideTemplates.galleryDescription") })
                    ] }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "p-1 rounded hover:bg-muted transition-colors",
                        "aria-label": t("pptx.slideTemplates.close"),
                        children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsx)("div", { className: "flex-1 p-3 overflow-y-auto", children: (0, import_jsx_runtime.jsx)(
                "div",
                {
                  className: "grid grid-cols-3 gap-2",
                  role: "listbox",
                  "aria-label": t("pptx.slideTemplates.gallery"),
                  children: SLIDE_TEMPLATES.map((spec) => (0, import_jsx_runtime.jsxs)(
                    "button",
                    {
                      type: "button",
                      role: "option",
                      "aria-selected": selected === spec.id,
                      "aria-label": t(spec.nameKey),
                      title: t(spec.descriptionKey),
                      onClick: () => setSelected(spec.id),
                      onDoubleClick: () => {
                        onInsert(spec.id);
                        onClose();
                      },
                      className: cn(
                        "flex flex-col items-center gap-1 p-2 rounded border transition-colors",
                        selected === spec.id ? "border-primary bg-primary/20" : "border-border hover:border-border hover:bg-muted/50"
                      ),
                      children: [
                        (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center bg-muted rounded", children: (0, import_jsx_runtime.jsx)(SlideTemplatePreview, { templateId: spec.id, scheme }) }),
                        (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-foreground text-center leading-tight", children: t(spec.nameKey) })
                      ]
                    },
                    spec.id
                  ))
                }
              ) }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 px-4 py-3 border-t border-border", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 text-xs rounded bg-muted hover:bg-accent text-foreground transition-colors",
                    children: t("pptx.slideTemplates.cancel")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: handleInsert,
                    disabled: !selected,
                    className: cn(
                      "px-3 py-1.5 text-xs rounded transition-colors",
                      selected ? "bg-primary hover:bg-primary/80 text-white" : "bg-muted text-muted-foreground cursor-not-allowed"
                    ),
                    children: t("pptx.slideTemplates.insert")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
var CATEGORIES2 = [
  {
    group: "entrance",
    labelKey: "pptx.animation.entrance",
    tone: "text-emerald-500",
    presets: ENTRANCE_PRESET_VALUES
  },
  {
    group: "emphasis",
    labelKey: "pptx.animation.emphasis",
    tone: "text-amber-500",
    presets: EMPHASIS_PRESET_VALUES
  },
  {
    group: "exit",
    labelKey: "pptx.animation.exit",
    tone: "text-red-500",
    presets: EXIT_PRESET_VALUES
  }
];
function AnimationPresetGallery({
  disabled,
  onAddAnimation
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "flex max-h-[62px] items-start gap-2 overflow-y-auto rounded-sm border border-border/60 bg-muted/30 px-1.5 py-1",
      "aria-label": t("pptx.animations.galleryAria"),
      children: CATEGORIES2.map((category) => (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] font-semibold leading-3 text-muted-foreground", children: t(category.labelKey) }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex max-w-[150px] flex-wrap gap-0.5", children: category.presets.map((preset) => {
          const label = t(`pptx.animation.preset.${preset}`);
          return (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled,
              onClick: () => onAddAnimation == null ? void 0 : onAddAnimation(preset, category.group),
              title: label,
              className: "inline-flex items-center gap-0.5 rounded-sm px-1 py-0.5 text-[9px] leading-3 text-foreground transition-colors hover:bg-accent disabled:opacity-35",
              children: [
                (0, import_jsx_runtime.jsx)(
                  LuStar,
                  {
                    className: `h-2.5 w-2.5 fill-current ${category.tone}`,
                    "aria-hidden": "true"
                  }
                ),
                (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap", children: label })
              ]
            },
            preset
          );
        }) })
      ] }, category.group))
    }
  );
}
function MotionPathGallery({
  disabled,
  onApplyMotionPath
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "flex max-h-[62px] items-start gap-2 overflow-y-auto rounded-sm border border-border/60 bg-muted/30 px-1.5 py-1",
      "aria-label": t("pptx.animations.motionPathGalleryAria"),
      children: MOTION_PATH_FAMILIES.map((family) => (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] font-semibold leading-3 text-muted-foreground", children: t(motionPathFamilyLabelKey(family)) }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex max-w-[150px] flex-wrap gap-0.5", children: motionPathPresetsByFamily(family).map((preset) => {
          const label = t(motionPathPresetLabelKey(preset.id));
          return (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled,
              onClick: () => onApplyMotionPath == null ? void 0 : onApplyMotionPath(preset.id),
              title: label,
              className: "inline-flex items-center gap-0.5 rounded-sm px-1 py-0.5 text-[9px] leading-3 text-foreground transition-colors hover:bg-accent disabled:opacity-35",
              children: [
                (0, import_jsx_runtime.jsx)(LuMoveRight, { className: "h-2.5 w-2.5 text-sky-500", "aria-hidden": "true" }),
                (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap", children: label })
              ]
            },
            preset.id
          );
        }) })
      ] }, family))
    }
  );
}
function RibbonGroup({
  label,
  children,
  className
}) {
  return (0, import_jsx_runtime.jsxs)(
    "section",
    {
      className: cn(
        "relative flex min-h-[78px] shrink-0 items-start gap-1 border-r border-border/60 px-2 pb-4 pt-1 last:border-r-0",
        className
      ),
      "aria-label": label,
      children: [
        children,
        (0, import_jsx_runtime.jsx)("span", { className: "pointer-events-none absolute inset-x-1 bottom-0.5 truncate text-center text-[9px] leading-3 text-muted-foreground", children: label })
      ]
    }
  );
}
function RibbonCommand({
  label,
  icon,
  onClick,
  disabled,
  active,
  compact = false,
  title,
  pressed
}) {
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      onClick,
      disabled,
      title: title ?? label,
      "aria-pressed": pressed,
      className: cn(
        "inline-flex rounded-sm text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-35",
        active && "bg-primary/15 text-primary ring-1 ring-primary/35",
        compact ? "h-[26px] min-w-[88px] items-center gap-1.5 px-1.5 text-left text-[10px] leading-3" : "h-[58px] min-w-[54px] max-w-[72px] flex-col items-center justify-start gap-0.5 px-1 py-1 text-center text-[9px] leading-[11px]"
      ),
      children: [
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: cn(
              "grid shrink-0 place-items-center text-primary",
              compact ? "h-4 w-4 [&>svg]:h-4 [&>svg]:w-4" : "h-7 w-7 [&>svg]:h-6 [&>svg]:w-6"
            ),
            "aria-hidden": "true",
            children: icon
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { children: label })
      ]
    }
  );
}
function RibbonCommandStack({
  children
}) {
  return (0, import_jsx_runtime.jsx)("div", { className: "flex flex-col justify-start gap-0.5", children });
}
function RibbonToggle({
  label,
  checked,
  onChange,
  disabled,
  title
}) {
  return (0, import_jsx_runtime.jsxs)(
    "label",
    {
      title,
      className: cn(
        "flex h-[19px] items-center gap-1 whitespace-nowrap rounded-sm px-1 text-[10px] text-foreground",
        checked && "bg-primary/15 text-primary"
      ),
      children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            checked,
            disabled,
            onChange: (event) => onChange == null ? void 0 : onChange(event.target.checked),
            className: "h-3 w-3 accent-primary disabled:opacity-35"
          }
        ),
        label
      ]
    }
  );
}
function AnimationsSection(p) {
  const { t } = useTranslation();
  const [previewActive, setPreviewActive] = (0, import_react2.useState)(false);
  const disabled = !p.canEdit || p.selectedElement === null;
  const preview = () => {
    if (disabled) {
      return;
    }
    setPreviewActive(true);
    setTimeout(() => setPreviewActive(false), 1200);
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(RibbonGroup, { label: t("pptx.animations.preview"), children: (0, import_jsx_runtime.jsx)(
      RibbonCommand,
      {
        label: t("pptx.animations.preview"),
        icon: (0, import_jsx_runtime.jsx)(LuPlay, {}),
        onClick: preview,
        disabled,
        active: previewActive,
        title: t("pptx.animations.previewTooltip")
      }
    ) }),
    (0, import_jsx_runtime.jsx)(
      RibbonGroup,
      {
        label: t("pptx.animations.animation", { defaultValue: "Animation" }),
        className: "max-w-[500px] overflow-hidden",
        children: (0, import_jsx_runtime.jsx)(AnimationPresetGallery, { disabled, onAddAnimation: p.onAddAnimation })
      }
    ),
    (0, import_jsx_runtime.jsx)(RibbonGroup, { label: t("pptx.animation.motionPath"), className: "max-w-[420px] overflow-hidden", children: (0, import_jsx_runtime.jsx)(
      MotionPathGallery,
      {
        disabled,
        onApplyMotionPath: (presetId) => {
          var _a;
          return (_a = p.onAddAnimation) == null ? void 0 : _a.call(p, presetId, "motionPath");
        }
      }
    ) }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.animations.advanced", { defaultValue: "Advanced Animation" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.animations.exitEffects", { defaultValue: "Exit Effects" }),
          icon: (0, import_jsx_runtime.jsx)(LuStar, { className: "text-red-500" }),
          onClick: () => {
            var _a;
            return (_a = p.onAddAnimation) == null ? void 0 : _a.call(p, "fadeOut", "exit");
          },
          disabled
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.animations.pathAnimation", { defaultValue: "Path Animation" }),
          icon: (0, import_jsx_runtime.jsx)(LuMoveRight, {}),
          onClick: () => {
            var _a;
            return (_a = p.onAddAnimation) == null ? void 0 : _a.call(p, DEFAULT_MOTION_PATH_PRESET_ID, "motionPath");
          },
          disabled
        }
      ),
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.effectOptions", { defaultValue: "Effect Options" }),
            icon: (0, import_jsx_runtime.jsx)(LuSparkles, {}),
            onClick: p.onOpenAnimationPanel ?? p.onToggleInspector,
            disabled
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.animationPanel"),
            icon: (0, import_jsx_runtime.jsx)(LuPanelRight, {}),
            onClick: p.onOpenAnimationPanel ?? p.onToggleInspector,
            active: p.isInspectorPaneOpen,
            title: t("pptx.animations.openPanelTooltip")
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.trigger", { defaultValue: "Trigger" }),
            icon: (0, import_jsx_runtime.jsx)(LuMousePointerClick, {}),
            onClick: p.onOpenAnimationPanel ?? p.onToggleInspector,
            disabled
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.animations.painter", { defaultValue: "Animation Painter" }),
            icon: (0, import_jsx_runtime.jsx)(LuPaintbrush, {}),
            disabled: true
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.animations.remove"),
          icon: (0, import_jsx_runtime.jsx)(LuTrash2, {}),
          onClick: p.onRemoveAnimation,
          disabled,
          title: t("pptx.animations.removeTooltip")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(RibbonGroup, { label: t("pptx.animations.timing", { defaultValue: "Timing" }), children: (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[48px_82px] items-center gap-x-1 gap-y-1 text-[10px]", children: [
      (0, import_jsx_runtime.jsx)("label", { htmlFor: "pptx-animation-start", children: t("pptx.animations.start", { defaultValue: "Start" }) }),
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          id: "pptx-animation-start",
          disabled: true,
          className: "h-6 rounded-sm border border-border bg-muted px-1 text-[10px]",
          children: [
            (0, import_jsx_runtime.jsx)("option", { children: t("pptx.animations.onClick", { defaultValue: "On Click" }) }),
            (0, import_jsx_runtime.jsx)("option", { children: t("pptx.animations.withPrevious", { defaultValue: "With Previous" }) }),
            (0, import_jsx_runtime.jsx)("option", { children: t("pptx.animations.afterPrevious", { defaultValue: "After Previous" }) })
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)("span", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)(LuClock3, {}),
        " ",
        t("pptx.animations.duration", { defaultValue: "Duration" })
      ] }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "number",
          min: "0",
          step: "0.1",
          defaultValue: "0.5",
          disabled: true,
          "aria-label": t("pptx.animations.duration", { defaultValue: "Duration" }),
          className: "h-6 rounded-sm border border-border bg-muted px-1 text-[10px]"
        }
      )
    ] }) })
  ] });
}
var _b = "inline-flex items-center justify-center px-2.5 py-1.5 max-md:min-h-[44px] max-md:min-w-[44px] active:scale-95 active:opacity-80";
var gB = `${_b} border-r border-border hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed`;
var gL = `${_b} hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed`;
var grp = "inline-flex items-center rounded bg-muted text-xs overflow-hidden";
var pill = "inline-flex items-center gap-1.5 px-2.5 py-1.5 max-md:min-h-[44px] rounded bg-muted hover:bg-accent text-xs transition-colors active:scale-95 active:opacity-80 disabled:opacity-40 disabled:cursor-not-allowed";
var sep = (0, import_jsx_runtime.jsx)("div", { className: "w-px self-stretch bg-border/40 mx-1 max-md:hidden" });
var ic = "w-4 h-4";
var ics = "w-3.5 h-3.5";
var ALIGN_BTNS = [
  { k: "left", el: (0, import_jsx_runtime.jsx)(LuAlignLeft, { className: ic }) },
  { k: "center", el: (0, import_jsx_runtime.jsx)(LuAlignCenter, { className: ic }) },
  { k: "right", el: (0, import_jsx_runtime.jsx)(LuAlignRight, { className: ic }) },
  { k: "top", el: (0, import_jsx_runtime.jsx)(LuChevronUp, { className: ic }) },
  { k: "middle", el: (0, import_jsx_runtime.jsx)(LuAlignCenter, { className: `${ic} rotate-90` }) },
  { k: "bottom", el: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: ic }) }
];
var DISTRIBUTE_BTNS = [
  { k: "horizontal", el: (0, import_jsx_runtime.jsx)(LuAlignHorizontalSpaceAround, { className: ic }) },
  { k: "vertical", el: (0, import_jsx_runtime.jsx)(LuAlignVerticalSpaceAround, { className: ic }) }
];
var DRAW_TOOLS = [
  {
    id: "select",
    icon: (0, import_jsx_runtime.jsx)(LuMoveRight, { className: ic }),
    labelKey: "pptx.ribbon.tool.select",
    ac: "bg-primary text-white"
  },
  {
    id: "pen",
    icon: (0, import_jsx_runtime.jsx)(LuPencil, { className: ic }),
    labelKey: "pptx.ribbon.tool.pen",
    ac: "bg-primary text-white"
  },
  {
    id: "highlighter",
    icon: (0, import_jsx_runtime.jsx)(LuType, { className: ic }),
    labelKey: "pptx.ribbon.tool.highlighter",
    ac: "bg-yellow-600 text-white"
  },
  {
    id: "eraser",
    icon: (0, import_jsx_runtime.jsx)(LuMinus, { className: ic }),
    labelKey: "pptx.ribbon.tool.eraser",
    ac: "bg-red-600 text-white"
  },
  {
    id: "freeform",
    icon: (0, import_jsx_runtime.jsx)(LuSpline, { className: ic }),
    labelKey: "pptx.ribbon.tool.freeform",
    ac: "bg-primary text-white"
  }
];
var OV = [
  {
    k: "png",
    labelKey: "pptx.ribbon.exportPng",
    i: (0, import_jsx_runtime.jsx)(LuDownload, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "pdf",
    labelKey: "pptx.ribbon.exportPdf",
    i: (0, import_jsx_runtime.jsx)(LuFileText, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "video",
    labelKey: "pptx.ribbon.exportVideo",
    i: (0, import_jsx_runtime.jsx)(LuVideo, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "gif",
    labelKey: "pptx.ribbon.exportGif",
    i: (0, import_jsx_runtime.jsx)(LuImage, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "pptx",
    labelKey: "pptx.file.saveAsPptxTooltip",
    i: (0, import_jsx_runtime.jsx)(LuDownload, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "ppsx",
    labelKey: "pptx.file.saveAsPpsxTooltip",
    i: (0, import_jsx_runtime.jsx)(LuPlay, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "pptm",
    labelKey: "pptx.file.saveAsPptmTooltip",
    i: (0, import_jsx_runtime.jsx)(LuDatabase, { className: `${ics} text-muted-foreground` })
  },
  { k: "---0", labelKey: "", i: null },
  {
    k: "print",
    labelKey: "pptx.print.printButton",
    i: (0, import_jsx_runtime.jsx)(LuPrinter, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "copyImg",
    labelKey: "pptx.file.copyImageTooltip",
    i: (0, import_jsx_runtime.jsx)(LuCopy, { className: `${ics} text-muted-foreground` })
  },
  { k: "---", labelKey: "", i: null },
  {
    k: "a11y",
    labelKey: "pptx.ribbon.accessibilityCheck",
    i: (0, import_jsx_runtime.jsx)(LuCheck, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "shortcuts",
    labelKey: "pptx.settings.keyboardShortcuts",
    i: (0, import_jsx_runtime.jsx)(LuSearch, { className: `${ics} text-muted-foreground` })
  },
  { k: "---2", labelKey: "", i: null },
  {
    k: "versionHistory",
    labelKey: "pptx.ribbon.versionHistory",
    i: (0, import_jsx_runtime.jsx)(LuClock, { className: `${ics} text-muted-foreground` })
  },
  { k: "---3", labelKey: "", i: null },
  {
    k: "documentProperties",
    labelKey: "pptx.ribbon.documentProperties",
    i: (0, import_jsx_runtime.jsx)(LuInfo, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "passwordProtection",
    labelKey: "pptx.security.protectPresentation",
    i: (0, import_jsx_runtime.jsx)(LuLock, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "fontEmbedding",
    labelKey: "pptx.ribbon.embedFonts",
    i: (0, import_jsx_runtime.jsx)(LuType, { className: `${ics} text-muted-foreground` })
  },
  {
    k: "digitalSignatures",
    labelKey: "pptx.viewer.digitalSignatures",
    i: (0, import_jsx_runtime.jsx)(LuShieldAlert, { className: `${ics} text-muted-foreground` })
  }
];
var FMT = [
  { id: "bold", i: (0, import_jsx_runtime.jsx)(LuBold, { className: ic }), labelKey: "pptx.textPanel.bold" },
  { id: "italic", i: (0, import_jsx_runtime.jsx)(LuItalic, { className: ic }), labelKey: "pptx.textPanel.italic" },
  { id: "underline", i: (0, import_jsx_runtime.jsx)(LuUnderline, { className: ic }), labelKey: "pptx.textPanel.underline" },
  {
    id: "strikethrough",
    i: (0, import_jsx_runtime.jsx)(LuStrikethrough, { className: ic }),
    labelKey: "pptx.textPanel.strikethrough"
  }
];
var ATXT = [
  { id: "left", i: (0, import_jsx_runtime.jsx)(LuAlignLeft, { className: ic }), labelKey: "pptx.ribbon.alignLeft" },
  { id: "center", i: (0, import_jsx_runtime.jsx)(LuAlignCenter, { className: ic }), labelKey: "pptx.ribbon.alignCenter" },
  { id: "right", i: (0, import_jsx_runtime.jsx)(LuAlignRight, { className: ic }), labelKey: "pptx.ribbon.alignRight" },
  { id: "justify", i: (0, import_jsx_runtime.jsx)(LuAlignJustify, { className: ic }), labelKey: "pptx.ribbon.justify" }
];
function ShapeArrangeExtras(p) {
  const { t } = useTranslation();
  const canGroup = canGroupSelection(p.canEdit, p.selectedCount, p.selectionGroupable);
  const canUngroup = canUngroupSelection(p.canEdit, p.selectedElement);
  const canStrokeWidth = canSetStrokeWidth(p.canEdit, p.selectedElement);
  const strokeWidth = strokeWidthOf(p.selectedElement);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: p.onGroupElements,
          disabled: !canGroup,
          className: gB,
          title: t("pptx.contextMenu.group"),
          "aria-label": t("pptx.contextMenu.group"),
          children: (0, import_jsx_runtime.jsx)(LuGroup, { className: ic })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: p.onUngroupElement,
          disabled: !canUngroup,
          className: gL,
          title: t("pptx.contextMenu.ungroup"),
          "aria-label": t("pptx.contextMenu.ungroup"),
          children: (0, import_jsx_runtime.jsx)(LuUngroup, { className: ic })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "number",
        min: "0",
        max: "120",
        step: "0.5",
        disabled: !canStrokeWidth,
        "aria-label": t("pptx.ribbon.strokeWidth"),
        title: t("pptx.ribbon.strokeWidth"),
        value: strokeWidth,
        onChange: (event) => {
          const next = Number(event.target.value);
          if (Number.isFinite(next)) {
            p.onUpdateElementStyle({ strokeWidth: Math.max(0, next) });
          }
        },
        className: "h-[26px] w-[52px] rounded border border-border bg-muted px-1 text-center text-[11px] text-foreground disabled:opacity-40"
      }
    )
  ] });
}
function ArrangeSection(p) {
  const { t } = useTranslation();
  const hasSel = Boolean(p.selectedElement);
  const canMut = hasSel && p.canEdit;
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
      (0, import_jsx_runtime.jsx)("div", { className: grp, children: ALIGN_BTNS.map((a, i, arr) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: () => p.onAlignElements(a.k),
          disabled: !canMut,
          className: i < arr.length - 1 ? gB : gL,
          title: t("pptx.arrange.align", { direction: a.k }),
          children: a.el
        },
        a.k
      )) }),
      (0, import_jsx_runtime.jsx)("div", { className: grp, children: DISTRIBUTE_BTNS.map((d, i, arr) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: () => p.onDistributeElements(d.k),
          disabled: !p.canEdit || !p.canDistribute,
          className: i < arr.length - 1 ? gB : gL,
          title: t(`pptx.arrange.distribute${d.k.charAt(0).toUpperCase()}${d.k.slice(1)}`),
          children: d.el
        },
        d.k
      )) }),
      p.onToggleFormatPainter && (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          onClick: p.onToggleFormatPainter,
          disabled: !p.canEdit || p.canActivateFormatPainter === false && !p.formatPainterActive,
          "data-testid": "format-painter-toggle",
          "data-active": p.formatPainterActive ? "true" : "false",
          className: cn(
            pill,
            p.formatPainterActive ? "bg-amber-600 hover:bg-amber-500 text-amber-50" : ""
          ),
          title: t("pptx.arrange.formatPainter"),
          children: [
            (0, import_jsx_runtime.jsx)(LuPaintbrush, { className: ic }),
            t("pptx.arrange.format")
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: () => p.onFlip("horizontal"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.flipHorizontally"),
            children: t("pptx.arrange.flipH")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: () => p.onFlip("vertical"),
            disabled: !canMut,
            className: gL,
            title: t("pptx.arrange.flipVertically"),
            children: t("pptx.arrange.flipV")
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        ShapeArrangeExtras,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          selectedCount: p.selectedCount,
          selectionGroupable: p.selectionGroupable,
          onGroupElements: p.onGroupElements,
          onUngroupElement: p.onUngroupElement,
          onUpdateElementStyle: p.onUpdateElementStyle
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            onClick: () => p.onMoveLayer("backward"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.sendBackward"),
            children: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: ic })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            onClick: () => p.onMoveLayer("forward"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.bringForward"),
            children: (0, import_jsx_runtime.jsx)(LuChevronUp, { className: ic })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            onClick: () => p.onMoveLayerToEdge("back"),
            disabled: !canMut,
            className: gB,
            title: t("pptx.arrange.sendToBack"),
            children: t("pptx.arrange.back")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            onClick: () => p.onMoveLayerToEdge("front"),
            disabled: !canMut,
            className: gL,
            title: t("pptx.arrange.bringToFront"),
            children: t("pptx.arrange.front")
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          onClick: p.onDuplicate,
          disabled: !canMut,
          className: pill,
          title: t("pptx.arrange.duplicate"),
          children: [
            (0, import_jsx_runtime.jsx)(LuCopy, { className: ic }),
            t("pptx.arrange.duplicate")
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          onClick: p.onDelete,
          disabled: !canMut,
          className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded bg-red-700/80 hover:bg-red-600 disabled:opacity-40 disabled:cursor-not-allowed text-xs transition-colors",
          title: t("pptx.arrange.delete"),
          children: [
            (0, import_jsx_runtime.jsx)(LuTrash2, { className: ic }),
            t("pptx.arrange.delete")
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: t("pptx.arrange.groupLabel") })
  ] });
}
function TransitionsSection(p) {
  var _a, _b2;
  const { t } = useTranslation();
  const canEdit = p.canEdit !== false;
  const draft = import_react2.default.useMemo(() => readRibbonTransitionDraft(p.activeSlide), [p.activeSlide]);
  const [durationBuffer, setDurationBuffer] = import_react2.default.useState(null);
  const [advanceBuffer, setAdvanceBuffer] = import_react2.default.useState(null);
  const commit = import_react2.default.useCallback(
    (changes) => {
      p.onTransitionChange(ribbonTransitionUpdates({ ...draft, ...changes }));
    },
    [draft, p]
  );
  const soundFileInputRef = import_react2.default.useRef(null);
  const handleSoundFilePicked = import_react2.default.useCallback(
    (file) => {
      void readSoundFileAsDataUrl(file).then((dataUrl) => {
        if (dataUrl) {
          p.onTransitionChange(applyTransitionSoundFile({ name: file.name, dataUrl }));
        }
        return void 0;
      });
    },
    [p]
  );
  const handleSoundSelectChange = (event) => {
    var _a2, _b3;
    const value = event.target.value;
    if (value === TRANSITION_SOUND_OTHER_VALUE) {
      (_a2 = soundFileInputRef.current) == null ? void 0 : _a2.click();
      event.target.value = transitionSoundSelectedValue((_b3 = p.activeSlide) == null ? void 0 : _b3.transition);
      return;
    }
    if (value === TRANSITION_SOUND_NONE_VALUE) {
      p.onTransitionChange(clearTransitionSound());
    }
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: pill,
        title: t("pptx.ribbon.previewTransition"),
        onClick: () => {
          var _a2;
          return playSlideTransitionPreview((_a2 = p.activeSlide) == null ? void 0 : _a2.transition, document);
        },
        children: [
          (0, import_jsx_runtime.jsx)(LuPlay, { className: ics }),
          t("pptx.ribbon.preview")
        ]
      }
    ),
    sep,
    (0, import_jsx_runtime.jsx)("div", { className: "inline-flex items-center gap-0.5 overflow-x-auto max-w-[420px]", children: RIBBON_TRANSITION_PRESETS.map((preset) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        onClick: () => commit({ type: preset.type }),
        className: cn(
          "flex-shrink-0 px-2 py-1 max-md:min-h-[44px] rounded border text-[11px] leading-tight transition-colors",
          draft.type === preset.type ? "border-primary bg-primary/10 text-primary font-medium" : "border-border bg-muted hover:bg-accent text-foreground"
        ),
        title: t("pptx.ribbon.transitionTitle", { name: t(preset.labelKey) }),
        children: t(preset.labelKey)
      },
      preset.type
    )) }),
    sep,
    (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-1.5 text-xs text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.duration") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "number",
          min: 0,
          max: 20,
          step: 0.25,
          disabled: !canEdit,
          value: durationBuffer ?? String(draft.durationSec),
          onChange: (e) => {
            setDurationBuffer(e.target.value);
            const seconds = Number(e.target.value);
            if (Number.isFinite(seconds) && e.target.value !== "") {
              commit({ durationSec: seconds });
            }
          },
          onBlur: () => setDurationBuffer(null),
          className: "w-16 px-1.5 py-1 rounded border border-border bg-muted text-xs text-foreground text-center",
          title: t("pptx.ribbon.transitionDurationTitle")
        }
      )
    ] }),
    sep,
    (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-1.5 text-xs text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.sound") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.ribbon.sound"),
          className: "w-24 px-1.5 py-1 rounded border border-border bg-muted text-xs text-foreground disabled:opacity-50",
          disabled: !canEdit,
          value: transitionSoundSelectedValue((_a = p.activeSlide) == null ? void 0 : _a.transition),
          onChange: handleSoundSelectChange,
          children: transitionSoundOptions((_b2 = p.activeSlide) == null ? void 0 : _b2.transition).map((option) => (0, import_jsx_runtime.jsx)("option", { value: option.value, children: option.i18nKey ? t(option.i18nKey) : option.label }, option.value))
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          ref: soundFileInputRef,
          type: "file",
          accept: "audio/*",
          className: "hidden",
          onChange: (e) => {
            var _a2;
            const file = (_a2 = e.target.files) == null ? void 0 : _a2[0];
            if (file) {
              handleSoundFilePicked(file);
            }
            e.target.value = "";
          }
        }
      )
    ] }),
    sep,
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: pill,
        title: t("pptx.ribbon.applyTransitionToAll"),
        onClick: p.onApplyTransitionToAll,
        children: [
          (0, import_jsx_runtime.jsx)(LuCopy, { className: ics }),
          t("pptx.headerFooter.applyToAll")
        ]
      }
    ),
    sep,
    (0, import_jsx_runtime.jsxs)("div", { className: "inline-flex flex-col gap-1 text-xs text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] font-medium text-foreground", children: t("pptx.ribbon.advanceSlide") }),
      (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-1.5 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: draft.advanceOnClick,
            onChange: (e) => commit({ advanceOnClick: e.target.checked }),
            className: "accent-primary h-3 w-3"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.onMouseClick") })
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-1.5 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            "aria-label": t("pptx.ribbon.afterDuration"),
            disabled: !canEdit,
            checked: draft.advanceAfter,
            onChange: (e) => commit({ advanceAfter: e.target.checked }),
            className: "accent-primary h-3 w-3"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap", children: t("pptx.ribbon.afterDuration") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            "aria-label": t("pptx.ribbon.advanceAfterSeconds"),
            value: advanceBuffer ?? draft.advanceAfterText,
            onChange: (e) => {
              setAdvanceBuffer(e.target.value);
              commit({ advanceAfter: true, advanceAfterText: e.target.value });
            },
            onBlur: () => setAdvanceBuffer(null),
            disabled: !canEdit || !draft.advanceAfter,
            className: "w-16 px-1 py-0.5 rounded border border-border bg-muted text-xs text-foreground text-center disabled:opacity-50",
            title: t("pptx.ribbon.advanceAfterSeconds")
          }
        )
      ] })
    ] }),
    sep,
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: p.onToggleInspector,
        className: cn(
          pill,
          p.isInspectorPaneOpen ? "bg-primary hover:bg-primary/80 text-white" : ""
        ),
        title: t("pptx.ribbon.openInspectorTransitions"),
        children: [
          (0, import_jsx_runtime.jsx)(LuPanelRight, { className: ic }),
          t("pptx.ribbon.inspector")
        ]
      }
    )
  ] });
}
function DesignSection(p) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onToggleThemeGallery,
        disabled: !p.canEdit,
        className: cn(
          pill,
          p.isThemeGalleryOpen ? "bg-primary hover:bg-primary/80 text-white" : ""
        ),
        title: t("pptx.ribbon.browseThemesTitle"),
        children: [
          (0, import_jsx_runtime.jsx)(LuPalette, { className: ics }),
          t("pptx.ribbon.browseThemes")
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onToggleThemeEditor,
        disabled: !p.canEdit,
        className: cn(pill, p.isThemeEditorOpen ? "bg-primary hover:bg-primary/80 text-white" : ""),
        title: t("pptx.ribbon.editThemeTitle"),
        children: [
          (0, import_jsx_runtime.jsx)(LuPencil, { className: ics }),
          t("pptx.ribbon.editTheme")
        ]
      }
    ),
    sep,
    (p.onOpenSlideSize ?? p.onOpenDocumentProperties) && (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onOpenSlideSize ?? p.onOpenDocumentProperties,
        className: pill,
        title: t("pptx.ribbon.slideSizeTitle"),
        children: [
          (0, import_jsx_runtime.jsx)(LuMonitor, { className: ics }),
          t("pptx.ribbon.slideSize")
        ]
      }
    ),
    p.onToggleInspector && (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onToggleInspector,
        className: cn(
          pill,
          p.isInspectorPaneOpen ? "bg-primary hover:bg-primary/80 text-white" : ""
        ),
        title: t("pptx.ribbon.formatBackgroundTitle"),
        children: [
          (0, import_jsx_runtime.jsx)(LuPaintBucket, { className: ics }),
          t("pptx.ribbon.formatBackground")
        ]
      }
    )
  ] });
}
var DEFAULT_VALUE = {
  recentColors: [],
  pushColor: () => {
  }
};
var RecentColorsContext = (0, import_react2.createContext)(DEFAULT_VALUE);
var RecentColorsProvider = RecentColorsContext.Provider;
function useRecentColors() {
  return (0, import_react2.useContext)(RecentColorsContext);
}
function DrawSection(p) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const colorRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    const el = colorRef.current;
    if (!el) {
      return;
    }
    const handler = () => pushColor(el.value);
    el.addEventListener("change", handler);
    return () => el.removeEventListener("change", handler);
  }, [pushColor]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("div", { className: grp, children: DRAW_TOOLS.map((tool, i, a) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: () => p.onSetActiveTool(tool.id),
        className: cn(
          i < a.length - 1 ? gB : gL,
          p.activeTool === tool.id ? tool.ac ?? "bg-accent text-foreground" : ""
        ),
        title: t(tool.labelKey),
        children: tool.icon
      },
      tool.id
    )) }),
    (0, import_jsx_runtime.jsxs)("div", { className: "inline-flex items-center gap-2 text-xs", children: [
      (0, import_jsx_runtime.jsxs)(
        "label",
        {
          className: "inline-flex items-center gap-1 text-muted-foreground",
          title: t("pptx.ribbon.penColour"),
          children: [
            t("pptx.ribbon.colour"),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                ref: colorRef,
                type: "color",
                value: p.drawingColor,
                onChange: (e) => p.onSetDrawingColor(e.target.value),
                className: "w-6 h-6 rounded border border-border bg-transparent cursor-pointer"
              }
            )
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "label",
        {
          className: "inline-flex items-center gap-1 text-muted-foreground",
          title: t("pptx.ribbon.strokeWidth"),
          children: [
            t("pptx.ribbon.width"),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "range",
                min: 1,
                max: 12,
                value: p.drawingWidth,
                onChange: (e) => p.onSetDrawingWidth(Number(e.target.value)),
                className: "w-16 h-1 accent-primary"
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { className: "text-foreground w-4 text-right", children: p.drawingWidth })
          ]
        }
      )
    ] })
  ] });
}
var AccountAuthContext = (0, import_react2.createContext)(void 0);
var cardClass = "border border-border bg-card p-6 text-card-foreground";
function AccountPage() {
  const { t } = useTranslation();
  const accountAuth = (0, import_react2.useContext)(AccountAuthContext);
  const [profile, setProfile] = (0, import_react2.useState)(
    () => readStoredViewerPrefs().profile ?? DEFAULT_VIEWER_PROFILE
  );
  const updateProfile = (patch) => {
    const next = { ...profile, ...patch };
    setProfile(next);
    saveViewerProfile(next);
  };
  const [usage, setUsage] = (0, import_react2.useState)(null);
  const [cleared, setCleared] = (0, import_react2.useState)(false);
  const refreshUsage = () => {
    void getLocalStorageUsageSummary().then(setUsage);
  };
  (0, import_react2.useEffect)(refreshUsage, []);
  const handleClear = () => {
    if (!window.confirm(t("pptx.account.storage.clearConfirm"))) {
      return;
    }
    void clearAllLocalViewerData().then(() => {
      setCleared(true);
      refreshUsage();
      setTimeout(() => setCleared(false), 4e3);
      return void 0;
    });
  };
  const version = "3.8.0";
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-8 max-w-[700px] space-y-6", children: [
    (0, import_jsx_runtime.jsxs)("section", { className: cardClass, children: [
      (0, import_jsx_runtime.jsxs)("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        (0, import_jsx_runtime.jsx)(LuUser, { className: "text-primary" }),
        " ",
        t("pptx.account.profile.title")
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "mt-4 flex items-center gap-4", children: [
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: "grid size-14 shrink-0 place-items-center rounded-full text-xl font-semibold text-white",
            style: { background: profile.avatarColor },
            children: resolveProfileInitial(profile)
          }
        ),
        (0, import_jsx_runtime.jsxs)("div", { className: "min-w-0 flex-1", children: [
          (0, import_jsx_runtime.jsx)("label", { htmlFor: "pptx-account-name", className: "text-xs text-muted-foreground", children: t("pptx.account.profile.nameLabel") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              id: "pptx-account-name",
              type: "text",
              value: profile.displayName,
              onChange: (e) => updateProfile({ displayName: e.target.value }),
              placeholder: t("pptx.account.profile.namePlaceholder"),
              className: "mt-1 h-9 w-full border border-input bg-background px-3 text-sm outline-none focus:border-ring"
            }
          )
        ] })
      ] }),
      (0, import_jsx_runtime.jsx)("p", { className: "mt-4 text-xs text-muted-foreground", children: t("pptx.account.profile.avatarColorLabel") }),
      (0, import_jsx_runtime.jsx)("div", { className: "mt-2 flex gap-2", children: AVATAR_COLOR_SWATCHES.map((color) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          "aria-label": color,
          "aria-pressed": profile.avatarColor === color,
          onClick: () => updateProfile({ avatarColor: color }),
          className: cn(
            "size-7 rounded-full border-2 transition-transform",
            profile.avatarColor === color ? "border-foreground scale-110" : "border-transparent hover:scale-105"
          ),
          style: { background: color }
        },
        color
      )) })
    ] }),
    (0, import_jsx_runtime.jsxs)("section", { className: cardClass, children: [
      (0, import_jsx_runtime.jsxs)("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        (0, import_jsx_runtime.jsx)(LuHardDrive, { className: "text-primary" }),
        " ",
        t("pptx.account.storage.title")
      ] }),
      (0, import_jsx_runtime.jsx)("p", { className: "mt-3 text-sm text-muted-foreground", children: usage && usage.presentationCount > 0 ? t("pptx.account.storage.usage", {
        count: usage.presentationCount,
        size: formatBackstageSize(usage.totalBytes)
      }) : t("pptx.account.storage.usageEmpty") }),
      (0, import_jsx_runtime.jsx)("p", { className: "mt-3 text-xs leading-5 text-muted-foreground", children: t("pptx.account.storage.privacyBlurb") }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: handleClear,
          className: "mt-4 border border-destructive/40 px-4 py-2 text-xs font-semibold text-destructive hover:bg-destructive/10",
          children: t("pptx.account.storage.clear")
        }
      ),
      cleared && (0, import_jsx_runtime.jsx)("p", { className: "mt-2 text-xs text-primary", children: t("pptx.account.storage.clearedNotice") })
    ] }),
    (0, import_jsx_runtime.jsxs)("section", { className: cardClass, children: [
      (0, import_jsx_runtime.jsxs)("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        (0, import_jsx_runtime.jsx)(LuInfo, { className: "text-primary" }),
        " ",
        t("pptx.account.about.title")
      ] }),
      (0, import_jsx_runtime.jsxs)("p", { className: "mt-3 text-sm text-muted-foreground", children: [
        "pptx-react-viewer",
        ` · ${t("pptx.account.about.version", { version })}`
      ] })
    ] }),
    (accountAuth == null ? void 0 : accountAuth.enabled) && (0, import_jsx_runtime.jsxs)("section", { className: cardClass, children: [
      (0, import_jsx_runtime.jsxs)("h2", { className: "flex items-center gap-2 text-sm font-semibold", children: [
        (0, import_jsx_runtime.jsx)(LuLogIn, { className: "text-primary" }),
        " ",
        t("pptx.account.signIn.title")
      ] }),
      accountAuth.signedInUser ? (0, import_jsx_runtime.jsx)("p", { className: "mt-3 text-sm text-muted-foreground", children: t("pptx.account.signIn.signedInAs", { name: accountAuth.signedInUser.name }) }) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        (0, import_jsx_runtime.jsx)("p", { className: "mt-3 text-sm text-muted-foreground", children: t("pptx.account.signIn.description") }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: accountAuth.onSignIn,
            className: "mt-4 bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90",
            children: t("pptx.account.signIn.button")
          }
        )
      ] })
    ] })
  ] });
}
var actionClass = "group flex min-h-28 items-start gap-4 border border-border bg-card p-5 text-left text-card-foreground transition hover:border-primary hover:shadow-md";
function BackstageAction(props) {
  return (0, import_jsx_runtime.jsxs)("button", { type: "button", className: actionClass, onClick: props.onClick, children: [
    (0, import_jsx_runtime.jsx)("span", { className: "mt-0.5 grid size-10 shrink-0 place-items-center bg-accent text-xl text-primary", children: props.icon }),
    (0, import_jsx_runtime.jsxs)("span", { children: [
      (0, import_jsx_runtime.jsx)("strong", { className: "block text-[15px] font-semibold", children: props.title }),
      (0, import_jsx_runtime.jsx)("span", { className: "mt-1 block text-[12px] leading-5 text-muted-foreground", children: props.body })
    ] })
  ] });
}
function BackstageNewGallery(props) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("h2", { className: "mt-7 text-[17px] font-semibold", children: t("pptx.backstage.newHeading") }),
    (0, import_jsx_runtime.jsx)("div", { className: "mt-5 grid grid-cols-[repeat(auto-fill,minmax(150px,1fr))] gap-6", children: BACKSTAGE_TEMPLATES.map((template) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: "text-left",
        onClick: () => props.onCreate(template.id),
        children: [
          (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: "block aspect-[16/9] border border-border bg-card shadow-sm transition hover:-translate-y-0.5 hover:border-primary hover:shadow-lg",
              style: { background: template.preview }
            }
          ),
          (0, import_jsx_runtime.jsx)("strong", { className: "mt-2 block truncate text-[12px] font-medium", children: t(template.nameKey, { defaultValue: template.name }) }),
          (0, import_jsx_runtime.jsx)("span", { className: "block truncate text-[10px] text-muted-foreground", children: t(template.descriptionKey, { defaultValue: template.description }) })
        ]
      },
      template.id
    )) })
  ] });
}
function BackstageRecentList(props) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-5 border-t border-border", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[1fr_120px_90px] px-3 py-2 text-[11px] font-semibold text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.backstage.columnName") }),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.backstage.columnModified") }),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.backstage.columnSize") })
    ] }),
    props.files.map((file) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: "grid w-full grid-cols-[1fr_120px_90px] items-center border-t border-border px-3 py-3 text-left hover:bg-accent",
        onClick: () => {
          var _a;
          return (_a = props.onOpen) == null ? void 0 : _a.call(props, file.key);
        },
        children: [
          (0, import_jsx_runtime.jsxs)("span", { className: "flex min-w-0 items-center gap-3", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "grid size-8 shrink-0 place-items-center bg-primary font-bold text-primary-foreground", children: "P" }),
            (0, import_jsx_runtime.jsxs)("span", { className: "min-w-0", children: [
              (0, import_jsx_runtime.jsx)("strong", { className: "block truncate text-[13px] font-normal", children: file.name }),
              (0, import_jsx_runtime.jsx)("small", { className: "block truncate text-[11px] text-muted-foreground", children: file.location })
            ] })
          ] }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] text-muted-foreground", children: formatBackstageDate(file.timestamp, Date.now(), t) }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] text-muted-foreground", children: formatBackstageSize(file.size) })
        ]
      },
      file.key
    )),
    props.files.length === 0 && (0, import_jsx_runtime.jsx)("div", { className: "border-t border-border px-3 py-10 text-center text-sm text-muted-foreground", children: t("pptx.backstage.noRecent") })
  ] });
}
var ICONS = {
  protect: LuLock,
  inspect: LuInfo,
  embedFonts: LuType,
  signatures: LuShieldAlert,
  versionHistory: LuClock3,
  saveAsPptx: LuDownload,
  saveAsPpsx: LuPlay,
  saveAsPptm: LuFileText,
  pdf: LuFileText,
  png: LuFileImage,
  video: LuVideo,
  gif: LuBox,
  json: LuFileJson,
  copyImage: LuCopy,
  print: LuPrinter,
  share: LuShare2
};
function BackstageCards({
  page,
  props,
  run
}) {
  const { t } = useTranslation();
  const handlers = {
    protect: props.onOpenPasswordProtection,
    inspect: props.onOpenDocumentProperties,
    embedFonts: props.onOpenFontEmbedding,
    signatures: props.onOpenDigitalSignatures,
    versionHistory: props.onOpenVersionHistory,
    saveAsPptx: props.onSaveAsPptx,
    saveAsPpsx: props.onSaveAsPpsx,
    saveAsPptm: props.onSaveAsPptm,
    pdf: props.onExportPdf,
    png: props.onExportPng,
    video: props.onExportVideo,
    gif: props.onExportGif,
    json: props.onExportJson,
    copyImage: props.onCopySlideAsImage,
    print: props.onPrint,
    share: props.onOpenShareDialog
  };
  const cards = backstageCardsFor(page).filter(
    (card) => card.id !== "saveAsPptm" || props.hasMacros
  );
  if (cards.length === 0) {
    return null;
  }
  const single = cards.length === 1;
  return (0, import_jsx_runtime.jsx)("div", { className: `mt-8 grid gap-5 ${single ? "max-w-[700px]" : "max-w-[900px] grid-cols-2"}`, children: cards.map((card) => {
    const Icon = ICONS[card.id];
    return (0, import_jsx_runtime.jsx)(
      BackstageAction,
      {
        icon: (0, import_jsx_runtime.jsx)(Icon, {}),
        title: t(card.titleKey, { defaultValue: card.title }),
        body: t(card.bodyKey, { defaultValue: card.body }),
        onClick: () => run(handlers[card.id])
      },
      card.id
    );
  }) });
}
var ICONS2 = {
  home: LuHouse,
  new: LuFilePlus2,
  open: LuFolderOpen,
  info: LuInfo,
  save: LuSave,
  saveAs: LuDownload,
  print: LuPrinter,
  share: LuShare2,
  export: LuUpload,
  close: LuX,
  account: LuUserRound,
  options: LuSettings
};
function BackstageNavIcon({ page }) {
  const Icon = ICONS2[page];
  return Icon ? (0, import_jsx_runtime.jsx)(Icon, { className: "size-[17px] shrink-0", "aria-hidden": "true" }) : null;
}
function FileSection(p) {
  const { t } = useTranslation();
  const [page, setPage] = (0, import_react2.useState)("home");
  const [query, setQuery] = (0, import_react2.useState)("");
  const [recent, setRecent] = (0, import_react2.useState)([]);
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  const exportHidden = isHidden("export");
  (0, import_react2.useEffect)(() => {
    void listBackstageRecentFiles(t, p.recentPresentationsCount).then(setRecent);
  }, [t, p.recentPresentationsCount]);
  const visibleRecent = (0, import_react2.useMemo)(() => {
    const q = query.trim().toLowerCase();
    return q ? recent.filter((file) => `${file.name} ${file.location}`.toLowerCase().includes(q)) : recent;
  }, [query, recent]);
  const run = (action) => {
    action == null ? void 0 : action();
    if (action) {
      p.onClose();
    }
  };
  const current = BACKSTAGE_NAV.find((item) => item.id === page);
  const title = current ? t(current.labelKey, { defaultValue: current.label }) : t("pptx.backstage.nav.home");
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "fixed inset-0 z-[200] flex bg-background font-[Aptos,Segoe_UI,sans-serif] text-foreground max-md:flex-col",
      role: "dialog",
      "aria-modal": "true",
      "aria-label": t("pptx.backstage.title"),
      children: [
        (0, import_jsx_runtime.jsxs)("aside", { className: "flex w-[148px] shrink-0 flex-col border-r border-border bg-secondary max-md:w-full max-md:flex-row max-md:items-center max-md:overflow-x-auto max-md:border-r-0 max-md:border-b", children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "aria-label": t("pptx.backstage.back"),
              onClick: p.onClose,
              className: "grid h-47px min-h-[48px] place-items-center border-b border-border text-xl hover:bg-accent max-md:min-w-[48px] max-md:shrink-0 max-md:border-b-0 max-md:border-r",
              children: (0, import_jsx_runtime.jsx)(LuArrowLeft, {})
            }
          ),
          (0, import_jsx_runtime.jsxs)("nav", { className: "flex min-h-0 flex-1 flex-col py-2 max-md:flex-row max-md:items-center max-md:py-0", children: [
            BACKSTAGE_NAV.filter(
              (item) => !item.group && !(item.id === "export" && exportHidden)
            ).map((item) => (0, import_jsx_runtime.jsxs)(
              "button",
              {
                type: "button",
                onClick: () => item.id === "close" ? p.onClose() : item.id === "save" ? run(p.onSaveAsPptx) : setPage(item.id),
                className: `flex min-h-10 items-center gap-3 border-l-2 px-4 text-left text-[12px] max-md:shrink-0 max-md:whitespace-nowrap max-md:border-l-0 max-md:border-b-2 max-md:px-3 ${page === item.id ? "border-primary bg-card text-primary" : "border-transparent hover:bg-accent"}`,
                children: [
                  (0, import_jsx_runtime.jsx)(BackstageNavIcon, { page: item.id }),
                  t(item.labelKey, { defaultValue: item.label })
                ]
              },
              item.id
            )),
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1 max-md:hidden" }),
            BACKSTAGE_NAV.filter((item) => item.group).map((item) => (0, import_jsx_runtime.jsxs)(
              "button",
              {
                type: "button",
                onClick: () => item.id === "options" && p.onOpenSettings ? run(p.onOpenSettings) : setPage(item.id),
                className: `flex min-h-10 items-center gap-3 border-l-2 px-4 text-left text-[12px] max-md:shrink-0 max-md:whitespace-nowrap max-md:border-l-0 max-md:border-b-2 max-md:px-3 ${page === item.id ? "border-primary bg-card text-primary" : "border-transparent hover:bg-accent"}`,
                children: [
                  (0, import_jsx_runtime.jsx)(BackstageNavIcon, { page: item.id }),
                  t(item.labelKey, { defaultValue: item.label })
                ]
              },
              item.id
            ))
          ] })
        ] }),
        (0, import_jsx_runtime.jsxs)("main", { className: "min-w-0 flex-1 overflow-y-auto bg-background px-[clamp(32px,4vw,72px)] py-5 max-md:px-4 max-md:py-4", children: [
          (0, import_jsx_runtime.jsx)("h1", { className: "text-[24px] font-semibold", children: page === "home" ? t("pptx.backstage.greeting") : title }),
          (page === "home" || page === "new") && (0, import_jsx_runtime.jsx)(
            BackstageNewGallery,
            {
              onCreate: (templateId) => run(() => p.onCreatePresentation(templateId))
            }
          ),
          (page === "home" || page === "open") && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "mt-8 flex max-w-[540px] items-center border border-input bg-card px-3 focus-within:border-ring", children: [
              (0, import_jsx_runtime.jsx)(LuSearch, { className: "text-muted-foreground" }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  value: query,
                  onChange: (event) => setQuery(event.target.value),
                  placeholder: t("pptx.backstage.searchPlaceholder"),
                  className: "h-10 min-w-0 flex-1 bg-transparent px-3 text-[13px] outline-none"
                }
              )
            ] }),
            page === "open" && (0, import_jsx_runtime.jsxs)(
              "button",
              {
                type: "button",
                className: "mt-4 inline-flex items-center gap-2 bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90",
                onClick: () => run(p.onOpenFile),
                children: [
                  (0, import_jsx_runtime.jsx)(LuFolderOpen, {}),
                  " ",
                  t("pptx.backstage.browseDevice")
                ]
              }
            ),
            (0, import_jsx_runtime.jsx)("h2", { className: "mt-6 text-[16px] font-semibold", children: t("pptx.backstage.recentHeading") }),
            (0, import_jsx_runtime.jsx)(
              BackstageRecentList,
              {
                files: visibleRecent,
                onOpen: (key) => run(() => {
                  var _a;
                  return (_a = p.onOpenRecentFile) == null ? void 0 : _a.call(p, key);
                })
              }
            )
          ] }),
          !(page === "export" && exportHidden) && (0, import_jsx_runtime.jsx)(BackstageCards, { page, props: p, run }),
          page === "account" && (0, import_jsx_runtime.jsx)(AccountPage, {}),
          page === "options" && (0, import_jsx_runtime.jsxs)("div", { className: "mt-8 max-w-[760px] border border-border bg-card p-7 text-card-foreground", children: [
            (0, import_jsx_runtime.jsx)(LuSettings, { className: "text-3xl text-primary" }),
            (0, import_jsx_runtime.jsx)("h2", { className: "mt-4 text-lg font-semibold", children: t("pptx.backstage.optionsTitle") }),
            (0, import_jsx_runtime.jsx)("p", { className: "mt-2 text-sm text-muted-foreground", children: t("pptx.backstage.optionsBody") }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "mt-6 bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90",
                onClick: () => run(p.onOpenSettings),
                children: t("pptx.backstage.openOptions")
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("p", { className: "mt-12 text-[11px] text-muted-foreground", children: [
            p.fileName || t("pptx.backstage.untitled"),
            " · ",
            t("pptx.backstage.savedToBrowser")
          ] })
        ] })
      ]
    }
  );
}
function RibbonMenu({
  anchorRef,
  className,
  children
}) {
  const [pos, setPos] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    const anchor = anchorRef.current;
    if (!anchor) {
      return;
    }
    const update = () => {
      const { left, top } = computeAnchoredPopupPosition(anchor.getBoundingClientRect());
      setPos(
        (prev) => prev && prev.left === left && prev.top === top ? prev : { left: left ?? 0, top }
      );
    };
    update();
    anchor.addEventListener("mouseenter", update);
    window.addEventListener("resize", update);
    document.addEventListener("scroll", update, true);
    return () => {
      anchor.removeEventListener("mouseenter", update);
      window.removeEventListener("resize", update);
      document.removeEventListener("scroll", update, true);
    };
  }, [anchorRef]);
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: cn("fixed z-50", className),
      style: pos ? { left: pos.left, top: pos.top } : { visibility: "hidden" },
      children
    }
  );
}
function FontFamilyMenu(p) {
  const { t } = useTranslation();
  const groups = buildFontCatalog({
    themeFonts: p.themeFonts,
    embeddedFonts: p.embeddedFonts,
    customFonts: p.customFonts
  });
  return (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: p.anchorRef, className: "flex flex-col w-64 pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 max-h-80 overflow-y-auto", children: groups.map((group, groupIndex) => (0, import_jsx_runtime.jsxs)(import_react2.default.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: `px-3 pb-1 pt-2 text-[10px] font-semibold uppercase tracking-wide text-muted-foreground${groupIndex > 0 ? " border-t border-border/60 mt-1" : ""}`,
        children: t(group.labelKey)
      }
    ),
    group.entries.map((entry) => (0, import_jsx_runtime.jsx)(FontRow, { entry, onSelect: p.onSelect }, `${group.id}-${entry.family}`))
  ] }, group.id)) }) });
}
function FontRow({
  entry,
  onSelect
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      className: "flex w-full items-center justify-between gap-3 px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
      style: { fontFamily: entry.family },
      onClick: () => onSelect(entry.family),
      children: [
        (0, import_jsx_runtime.jsx)("span", { className: "truncate", children: entry.family }),
        entry.themeRole && (0, import_jsx_runtime.jsx)("span", { className: "shrink-0 text-[10px] text-muted-foreground", children: t(`pptx.font.role.${entry.themeRole}`) })
      ]
    }
  );
}
function useLayoutPreviews(loadPreviews, enabled) {
  const [previews, setPreviews] = (0, import_react2.useState)(/* @__PURE__ */ new Map());
  (0, import_react2.useEffect)(() => {
    if (!enabled || !loadPreviews) {
      return;
    }
    let cancelled = false;
    void loadPreviews().then((loaded) => {
      if (!cancelled) {
        setPreviews(new Map(loaded.map((preview) => [preview.path, preview])));
      }
      return void 0;
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [enabled, loadPreviews]);
  return previews;
}
var THUMB_WIDTH = 128;
var THUMB_HEIGHT = 72;
var MAX_PREVIEW_ELEMENTS = 100;
function LayoutGalleryMenu(p) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: p.anchorRef, className: "flex flex-col w-[620px] pt-1", children: (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "layout-gallery-menu",
      className: "grid grid-cols-4 gap-2 rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-3 max-h-[520px] overflow-y-auto",
      children: [
        p.layoutOptions.length === 0 && (0, import_jsx_runtime.jsx)("p", { className: "col-span-4 px-2 py-3 text-xs text-muted-foreground", children: t("pptx.layoutGallery.empty") }),
        p.layoutOptions.map((layout) => {
          const isCurrent = isCurrentLayout(layout, p.currentLayoutPath);
          return (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              "aria-current": isCurrent ? "true" : void 0,
              title: isCurrent ? `${layout.name} (${t("pptx.layoutGallery.current")})` : layout.name,
              className: cn(
                "relative flex min-w-0 flex-col items-center gap-1 rounded border-2 p-1 text-xs text-foreground transition-colors hover:bg-muted",
                isCurrent ? "border-primary bg-primary/10" : "border-transparent"
              ),
              onClick: () => p.onSelect(layout),
              children: [
                (0, import_jsx_runtime.jsx)(LayoutThumbnail, { preview: p.previews.get(layout.path), name: layout.name }),
                (0, import_jsx_runtime.jsx)("span", { className: "w-full truncate text-center", children: layout.name })
              ]
            },
            layout.path
          );
        })
      ]
    }
  ) });
}
function LayoutThumbnail({ preview, name }) {
  const geometry = buildLayoutPreviewGeometry(preview, THUMB_WIDTH, THUMB_HEIGHT);
  const slide = {
    id: `layout-preview-${(preview == null ? void 0 : preview.path) ?? name}`,
    rId: "",
    slideNumber: 0,
    elements: (preview == null ? void 0 : preview.elements) ?? [],
    backgroundColor: geometry.backgroundColor
  };
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "relative shrink-0 overflow-hidden rounded-sm border border-border/70 shadow-sm",
      style: {
        width: geometry.boxWidth,
        height: geometry.boxHeight,
        backgroundColor: geometry.backgroundColor
      },
      children: (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "absolute left-0 top-0 origin-top-left overflow-hidden",
          style: {
            width: geometry.surfaceWidth,
            height: geometry.surfaceHeight,
            transform: `scale(${geometry.scale})`,
            backgroundColor: geometry.backgroundColor
          },
          children: [
            ((preview == null ? void 0 : preview.elements) ?? []).slice(0, MAX_PREVIEW_ELEMENTS).map((element, index) => (0, import_jsx_runtime.jsx)(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index
              },
              element.id
            )),
            geometry.frames.map((frame) => (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute border-dashed border-muted-foreground/70 bg-background/20",
                style: {
                  left: frame.left,
                  top: frame.top,
                  width: frame.width,
                  height: frame.height,
                  borderWidth: geometry.frameBorderWidth,
                  borderStyle: "dashed"
                }
              },
              frame.key
            ))
          ]
        }
      )
    }
  );
}
function SlidesGroup(p) {
  const { t } = useTranslation();
  const [newSlideMenuOpen, setNewSlideMenuOpen] = (0, import_react2.useState)(false);
  const [layoutMenuOpen, setLayoutMenuOpen] = (0, import_react2.useState)(false);
  const [templateGalleryOpen, setTemplateGalleryOpen] = (0, import_react2.useState)(false);
  const newSlideMenuRef = (0, import_react2.useRef)(null);
  const layoutMenuRef = (0, import_react2.useRef)(null);
  const previews = useLayoutPreviews(p.loadLayoutPreviews, newSlideMenuOpen || layoutMenuOpen);
  const handleNewSlide = (0, import_react2.useCallback)(() => {
    if (p.layoutOptions.length > 0) {
      const first = p.layoutOptions[0];
      p.onInsertSlideFromLayout(first.path, first.name);
    }
  }, [p]);
  (0, import_react2.useEffect)(() => {
    if (!newSlideMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (newSlideMenuRef.current && !newSlideMenuRef.current.contains(e.target)) {
        setNewSlideMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [newSlideMenuOpen]);
  (0, import_react2.useEffect)(() => {
    if (!layoutMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (layoutMenuRef.current && !layoutMenuRef.current.contains(e.target)) {
        setLayoutMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [layoutMenuOpen]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "relative inline-flex items-center", ref: newSlideMenuRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              onClick: handleNewSlide,
              disabled: !p.canEdit || p.layoutOptions.length === 0,
              className: cn(
                pill,
                "whitespace-nowrap",
                p.layoutOptions.length > 0 ? "rounded-r-none" : ""
              ),
              title: t("pptx.home.newSlide"),
              children: [
                (0, import_jsx_runtime.jsx)(LuPlus, { className: ic }),
                t("pptx.home.newSlide")
              ]
            }
          ),
          p.layoutOptions.length > 0 && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !p.canEdit,
              className: "inline-flex items-center justify-center self-stretch px-1 rounded-r bg-muted hover:bg-accent text-xs transition-colors border-l border-border/40 active:scale-95 active:opacity-80",
              title: t("pptx.home.chooseLayout"),
              onClick: () => setNewSlideMenuOpen((v) => !v),
              children: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3" })
            }
          ),
          newSlideMenuOpen && (0, import_jsx_runtime.jsx)(
            LayoutGalleryMenu,
            {
              anchorRef: newSlideMenuRef,
              layoutOptions: p.layoutOptions,
              previews,
              onSelect: (layout) => {
                p.onInsertSlideFromLayout(layout.path, layout.name);
                setNewSlideMenuOpen(false);
              }
            }
          )
        ] }),
        p.onInsertSlideFromTemplate && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            disabled: !p.canEdit,
            className: pill,
            title: t("pptx.home.slideTemplates"),
            onClick: () => setTemplateGalleryOpen(true),
            children: [
              (0, import_jsx_runtime.jsx)(LuLayoutTemplate, { className: ic }),
              t("pptx.home.slideTemplates")
            ]
          }
        ),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative inline-flex items-center", ref: layoutMenuRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled: !p.canEdit || p.layoutOptions.length === 0,
              className: pill,
              title: t("pptx.master.layout"),
              onClick: () => setLayoutMenuOpen((v) => !v),
              children: [
                (0, import_jsx_runtime.jsx)(LuLayoutGrid, { className: ic }),
                t("pptx.master.layout")
              ]
            }
          ),
          layoutMenuOpen && (0, import_jsx_runtime.jsx)(
            LayoutGalleryMenu,
            {
              anchorRef: layoutMenuRef,
              layoutOptions: p.layoutOptions,
              previews,
              currentLayoutPath: p.currentLayoutPath,
              onSelect: (layout) => {
                var _a;
                (_a = p.onApplyLayout) == null ? void 0 : _a.call(p, layout.path);
                setLayoutMenuOpen(false);
              }
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            disabled: !p.canEdit,
            className: pill,
            title: t("pptx.sections.resetSlideTitle"),
            onClick: p.onResetSlide,
            children: [
              (0, import_jsx_runtime.jsx)(LuRotateCcw, { className: ic }),
              t("pptx.animations.reset")
            ]
          }
        ),
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            disabled: !p.canEdit,
            className: pill,
            title: t("pptx.sections.addSection"),
            onClick: p.onAddSection,
            children: [
              (0, import_jsx_runtime.jsx)(LuFolderPlus, { className: ic }),
              t("pptx.sections.sectionButtonLabel")
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Slides" })
    ] }),
    sep,
    p.onInsertSlideFromTemplate && (0, import_jsx_runtime.jsx)(
      SlideTemplateGalleryDialog,
      {
        isOpen: templateGalleryOpen,
        onClose: () => setTemplateGalleryOpen(false),
        onInsert: (templateId) => {
          var _a;
          return (_a = p.onInsertSlideFromTemplate) == null ? void 0 : _a.call(p, templateId);
        },
        scheme: p.templateScheme
      }
    )
  ] });
}
function extractFontInfo(element, themeFonts) {
  var _a, _b2;
  const placeholderType = element == null ? void 0 : element.placeholderType;
  const defaults = {
    fontFamily: resolveDefaultFontFamily(placeholderType, themeFonts),
    fontSize: "24"
  };
  if (!element || !hasTextProperties(element)) {
    return defaults;
  }
  const segStyle = (_b2 = (_a = element.textSegments) == null ? void 0 : _a[0]) == null ? void 0 : _b2.style;
  const textStyle = element.textStyle;
  const fontFamily = (segStyle == null ? void 0 : segStyle.fontFamily) ?? (textStyle == null ? void 0 : textStyle.fontFamily) ?? defaults.fontFamily;
  const fontSize = (segStyle == null ? void 0 : segStyle.fontSize) ?? (textStyle == null ? void 0 : textStyle.fontSize);
  return {
    fontFamily,
    fontSize: fontSize !== void 0 && fontSize !== null ? String(textFontSizePxToPt(fontSize)) : defaults.fontSize
  };
}
function HomeSection(p) {
  const { t } = useTranslation();
  const [fontMenuOpen, setFontMenuOpen] = (0, import_react2.useState)(false);
  const [sizeMenuOpen, setSizeMenuOpen] = (0, import_react2.useState)(false);
  const [copiedFeedback, setCopiedFeedback] = (0, import_react2.useState)(false);
  const [cutFeedback, setCutFeedback] = (0, import_react2.useState)(false);
  const fontMenuRef = (0, import_react2.useRef)(null);
  const sizeMenuRef = (0, import_react2.useRef)(null);
  const { fontFamily, fontSize } = extractFontInfo(p.selectedElement, p.themeFonts);
  const hasSelection = Boolean(p.selectedElement);
  (0, import_react2.useEffect)(() => {
    if (!fontMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (fontMenuRef.current && !fontMenuRef.current.contains(e.target)) {
        setFontMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [fontMenuOpen]);
  (0, import_react2.useEffect)(() => {
    if (!sizeMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (sizeMenuRef.current && !sizeMenuRef.current.contains(e.target)) {
        setSizeMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [sizeMenuOpen]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: p.onPaste,
            disabled: !p.clipboardPayload || !p.canEdit,
            className: gB,
            title: t("pptx.arrange.paste"),
            children: (0, import_jsx_runtime.jsx)(LuClipboardPaste, { className: ic })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: () => {
              p.onCut();
              setCutFeedback(true);
              setTimeout(() => setCutFeedback(false), 600);
            },
            disabled: !p.canEdit || !hasSelection,
            className: cn(gB, cutFeedback && "bg-green-600/20 text-green-400"),
            title: t("pptx.arrange.cut"),
            children: (0, import_jsx_runtime.jsx)(LuScissors, { className: ic })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: () => {
              p.onCopy();
              setCopiedFeedback(true);
              setTimeout(() => setCopiedFeedback(false), 600);
            },
            disabled: !hasSelection,
            className: cn(gB, copiedFeedback && "bg-green-600/20 text-green-400"),
            title: t("pptx.arrange.copy"),
            children: (0, import_jsx_runtime.jsx)(LuCopy, { className: ic })
          }
        ),
        p.onToggleFormatPainter && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: p.onToggleFormatPainter,
            disabled: !p.canEdit || p.canActivateFormatPainter === false && !p.formatPainterActive,
            "data-testid": "format-painter-toggle",
            "data-active": p.formatPainterActive ? "true" : "false",
            className: cn(
              gL,
              p.formatPainterActive ? "bg-amber-600 hover:bg-amber-500 text-amber-50" : ""
            ),
            title: t("pptx.arrange.formatPainter"),
            children: (0, import_jsx_runtime.jsx)(LuPaintbrush, { className: ic })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: t("pptx.ribbon.clipboard") })
    ] }),
    sep,
    (0, import_jsx_runtime.jsx)(
      SlidesGroup,
      {
        canEdit: p.canEdit,
        layoutOptions: p.layoutOptions,
        currentLayoutPath: p.currentLayoutPath,
        loadLayoutPreviews: p.loadLayoutPreviews,
        onInsertSlideFromLayout: p.onInsertSlideFromLayout,
        onInsertSlideFromTemplate: p.onInsertSlideFromTemplate,
        templateScheme: p.templateScheme,
        onApplyLayout: p.onApplyLayout,
        onResetSlide: p.onResetSlide,
        onAddSection: p.onAddSection
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: fontMenuRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              onClick: () => setFontMenuOpen((v) => !v),
              "aria-label": t("pptx.ribbon.fontFamily"),
              className: "inline-flex items-center justify-between px-2 py-1 rounded-sm border border-border/60 bg-background/60 text-[11px] text-foreground min-w-[120px] truncate hover:bg-accent/40 transition-colors cursor-pointer",
              children: [
                (0, import_jsx_runtime.jsx)("span", { className: "truncate", children: fontFamily }),
                (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3 ml-1 shrink-0 text-muted-foreground" })
              ]
            }
          ),
          fontMenuOpen && (0, import_jsx_runtime.jsx)(
            FontFamilyMenu,
            {
              anchorRef: fontMenuRef,
              themeFonts: p.themeFonts,
              embeddedFonts: p.embeddedFontFamilies,
              customFonts: p.customFontFamilies,
              onSelect: (family) => {
                var _a;
                (_a = p.onUpdateTextStyle) == null ? void 0 : _a.call(p, { fontFamily: family });
                setFontMenuOpen(false);
              }
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: sizeMenuRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              onClick: () => setSizeMenuOpen((v) => !v),
              "aria-label": t("pptx.ribbon.fontSize"),
              className: "inline-flex items-center justify-between px-2 py-1 rounded-sm border border-border/60 bg-background/60 text-[11px] text-foreground min-w-[50px] text-center hover:bg-accent/40 transition-colors cursor-pointer",
              children: [
                (0, import_jsx_runtime.jsx)("span", { className: "truncate", children: fontSize }),
                (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3 ml-1 shrink-0 text-muted-foreground" })
              ]
            }
          ),
          sizeMenuOpen && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: sizeMenuRef, className: "flex flex-col w-48 pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 max-h-60 overflow-y-auto", children: COMMON_FONT_SIZES.map((s) => (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
              onClick: () => {
                var _a;
                (_a = p.onUpdateTextStyle) == null ? void 0 : _a.call(p, {
                  fontSize: p.selectedElement && hasTextProperties(p.selectedElement) ? textFontSizePtToPx(s) : s
                });
                setSizeMenuOpen(false);
              },
              children: s
            },
            s
          )) }) })
        ] })
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Font" })
    ] }),
    sep
  ] });
}
function InsertHyperlinkButton(p) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      onClick: p.onOpenHyperlinkDialog,
      disabled: !p.hasSelection,
      className: pill,
      title: t("pptx.hyperlinkDialog.title"),
      children: [
        (0, import_jsx_runtime.jsx)(LuLink, { className: ic }),
        t("pptx.hyperlinkDialog.title")
      ]
    }
  );
}
function InsertSection(p) {
  var _a;
  const { t } = useTranslation();
  const { canEdit } = p;
  const [datePickerOpen, setDatePickerOpen] = (0, import_react2.useState)(false);
  const [datePickerValue, setDatePickerValue] = (0, import_react2.useState)("");
  const [dateFormat, setDateFormat] = (0, import_react2.useState)("locale");
  const [newChartKind, setNewChartKind] = (0, import_react2.useState)(DEFAULT_INSERT_CHART_KIND);
  const datePickerRef = (0, import_react2.useRef)(null);
  const actionMenuRef = (0, import_react2.useRef)(null);
  const fieldMenuRef = (0, import_react2.useRef)(null);
  const openDatePicker = (0, import_react2.useCallback)(() => {
    const now = /* @__PURE__ */ new Date();
    const pad = (n) => String(n).padStart(2, "0");
    setDatePickerValue(
      `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}T${pad(now.getHours())}:${pad(now.getMinutes())}`
    );
    setDateFormat("locale");
    setDatePickerOpen(true);
  }, []);
  const confirmDatePicker = (0, import_react2.useCallback)(() => {
    if (!p.onInsertField) {
      return;
    }
    const d = new Date(datePickerValue);
    if (isNaN(d.getTime())) {
      return;
    }
    let formatted;
    switch (dateFormat) {
      case "iso":
        formatted = d.toISOString().slice(0, 10);
        break;
      case "long":
        formatted = d.toLocaleDateString(void 0, {
          weekday: "long",
          year: "numeric",
          month: "long",
          day: "numeric"
        });
        break;
      case "short":
        formatted = d.toLocaleDateString(void 0, {
          year: "numeric",
          month: "short",
          day: "numeric"
        });
        break;
      case "time":
        formatted = d.toLocaleString();
        break;
      default:
        formatted = d.toLocaleDateString();
        break;
    }
    p.onInsertField("datetime", formatted);
    setDatePickerOpen(false);
  }, [datePickerValue, dateFormat, p]);
  (0, import_react2.useEffect)(() => {
    if (!datePickerOpen) {
      return;
    }
    const handler = (e) => {
      if (datePickerRef.current && !datePickerRef.current.contains(e.target)) {
        setDatePickerOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [datePickerOpen]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onAddTextBox,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.addTextBox"),
        children: [
          (0, import_jsx_runtime.jsx)(LuType, { className: ic }),
          t("pptx.ribbon.textBox")
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          value: p.newShapeType,
          onChange: (e) => p.onSetNewShapeType(e.target.value),
          className: "bg-transparent py-1.5 pl-2 pr-1 outline-none text-xs",
          title: t("pptx.insert.shapeType"),
          children: SHAPE_PRESETS.map((sp) => (0, import_jsx_runtime.jsx)("option", { value: sp.type, className: "bg-background", children: t(sp.i18nKey) }, sp.type))
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          onClick: p.onAddShape,
          disabled: !canEdit,
          className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 border-l border-border hover:bg-accent transition-colors text-xs",
          title: t("pptx.insert.addShape"),
          children: [
            ((_a = SHAPE_PRESETS.find((sp) => sp.type === p.newShapeType)) == null ? void 0 : _a.icon) || (0, import_jsx_runtime.jsx)(LuSquare, { className: ic }),
            t("pptx.insert.shape")
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onOpenImagePicker,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.ribbon.insertImage"),
        children: [
          (0, import_jsx_runtime.jsx)(LuImage, { className: ic }),
          t("pptx.ribbon.image")
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onOpenMediaPicker,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.ribbon.insertMedia"),
        children: [
          (0, import_jsx_runtime.jsx)(LuVideo, { className: ic }),
          t("pptx.ribbon.media")
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onAddTable,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.insertTable"),
        children: [
          (0, import_jsx_runtime.jsx)(LuDatabase, { className: ic }),
          t("pptx.ribbon.table")
        ]
      }
    ),
    p.onAddChart && (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          value: newChartKind,
          onChange: (e) => {
            var _a2;
            const kind = (_a2 = INSERT_CHART_TYPES.find((ct) => ct.id === e.target.value)) == null ? void 0 : _a2.id;
            setNewChartKind(kind ?? DEFAULT_INSERT_CHART_KIND);
          },
          className: "bg-transparent py-1.5 pl-2 pr-1 outline-none text-xs",
          title: t("pptx.ribbon.chartType"),
          children: INSERT_CHART_TYPES.map((ct) => (0, import_jsx_runtime.jsx)("option", { value: ct.id, className: "bg-background", children: t(ct.labelKey) }, ct.id))
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          onClick: () => p.onAddChart(newChartKind),
          disabled: !canEdit,
          className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 border-l border-border hover:bg-accent transition-colors text-xs",
          title: t("pptx.ribbon.insertChart"),
          children: [
            (0, import_jsx_runtime.jsxs)(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: [
                  (0, import_jsx_runtime.jsx)("path", { d: "M3 3v18h18" }),
                  (0, import_jsx_runtime.jsx)("rect", { x: "7", y: "11", width: "3", height: "6" }),
                  (0, import_jsx_runtime.jsx)("rect", { x: "12", y: "7", width: "3", height: "10" }),
                  (0, import_jsx_runtime.jsx)("rect", { x: "17", y: "13", width: "3", height: "4" })
                ]
              }
            ),
            t("pptx.ribbon.chart")
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onAddSmartArt,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.insertSmartArt"),
        children: [
          (0, import_jsx_runtime.jsx)(LuLayers, { className: ic }),
          t("pptx.ribbon.smartArt")
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        onClick: p.onAddEquation,
        disabled: !canEdit,
        className: pill,
        title: t("pptx.insert.insertEquation"),
        children: [
          (0, import_jsx_runtime.jsx)(
            "svg",
            {
              className: ic,
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "2",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              children: (0, import_jsx_runtime.jsx)("path", { d: "M4 17h6M7 14v6M14 7l4.5 10M15.5 14h5" })
            }
          ),
          t("pptx.ribbon.equation")
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "relative group inline-flex items-center", ref: actionMenuRef, children: [
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: pill,
          title: t("pptx.ribbon.insertActionButton"),
          children: [
            (0, import_jsx_runtime.jsxs)(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: [
                  (0, import_jsx_runtime.jsx)("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2" }),
                  (0, import_jsx_runtime.jsx)("path", { d: "M13 7l4 5-4 5" })
                ]
              }
            ),
            t("pptx.ribbon.action"),
            (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3" })
          ]
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonMenu,
        {
          anchorRef: actionMenuRef,
          className: "hidden group-hover:flex flex-col w-40 pt-1",
          children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: ACTION_BUTTON_PRESETS.map((preset) => (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled: !canEdit,
              className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
              onClick: () => p.onAddActionButton(preset.shapeType),
              children: [
                (0, import_jsx_runtime.jsx)(
                  "svg",
                  {
                    className: "w-4 h-4 flex-shrink-0",
                    viewBox: "0 0 24 24",
                    fill: "none",
                    stroke: "currentColor",
                    strokeWidth: "2",
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    children: (0, import_jsx_runtime.jsx)("path", { d: preset.iconPath })
                  }
                ),
                preset.label
              ]
            },
            preset.shapeType
          )) })
        }
      )
    ] }),
    p.onInsertField && (0, import_jsx_runtime.jsxs)("div", { className: "relative group inline-flex items-center", ref: fieldMenuRef, children: [
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: pill,
          title: t("pptx.field.insertField"),
          children: [
            (0, import_jsx_runtime.jsxs)(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: [
                  (0, import_jsx_runtime.jsx)("path", { d: "M4 7h16M4 12h10M4 17h12" }),
                  (0, import_jsx_runtime.jsx)("circle", { cx: "19", cy: "15", r: "3" })
                ]
              }
            ),
            t("pptx.field.field"),
            (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3" })
          ]
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonMenu,
        {
          anchorRef: fieldMenuRef,
          className: "hidden group-hover:flex flex-col w-44 pt-1",
          children: (0, import_jsx_runtime.jsxs)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => p.onInsertField("slidenum"),
                children: t("pptx.field.slideNumber")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: openDatePicker,
                children: t("pptx.field.dateTime")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => p.onInsertField("header"),
                children: t("pptx.field.header")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => p.onInsertField("footer"),
                children: t("pptx.field.footer")
              }
            )
          ] })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      InsertHyperlinkButton,
      {
        hasSelection: p.hasSelection,
        onOpenHyperlinkDialog: p.onOpenHyperlinkDialog
      }
    ),
    p.onOpenHeaderFooter && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: cn(pill, "whitespace-nowrap"),
        onClick: p.onOpenHeaderFooter,
        children: t("pptx.headerFooter.title")
      }
    ),
    datePickerOpen && (0, import_jsx_runtime.jsx)(
      "div",
      {
        ref: datePickerRef,
        className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/30",
        onMouseDown: (e) => {
          if (e.target === e.currentTarget) {
            setDatePickerOpen(false);
          }
        },
        children: (0, import_jsx_runtime.jsxs)("div", { className: "rounded-lg border border-border bg-popover shadow-2xl p-4 w-72 space-y-3", children: [
          (0, import_jsx_runtime.jsx)("div", { className: "text-sm font-medium text-foreground", children: t("pptx.field.dateTime") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "datetime-local",
              className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              value: datePickerValue,
              onChange: (e) => setDatePickerValue(e.target.value)
            }
          ),
          (0, import_jsx_runtime.jsxs)("div", { children: [
            (0, import_jsx_runtime.jsx)("label", { className: "block text-[11px] text-muted-foreground mb-1", children: t("pptx.field.format", "Format") }),
            (0, import_jsx_runtime.jsxs)(
              "select",
              {
                className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                value: dateFormat,
                onChange: (e) => setDateFormat(e.target.value),
                children: [
                  (0, import_jsx_runtime.jsx)("option", { value: "locale", children: new Date(datePickerValue || Date.now()).toLocaleDateString() }),
                  (0, import_jsx_runtime.jsx)("option", { value: "long", children: new Date(datePickerValue || Date.now()).toLocaleDateString(void 0, {
                    weekday: "long",
                    year: "numeric",
                    month: "long",
                    day: "numeric"
                  }) }),
                  (0, import_jsx_runtime.jsx)("option", { value: "short", children: new Date(datePickerValue || Date.now()).toLocaleDateString(void 0, {
                    year: "numeric",
                    month: "short",
                    day: "numeric"
                  }) }),
                  (0, import_jsx_runtime.jsx)("option", { value: "iso", children: new Date(datePickerValue || Date.now()).toISOString().slice(0, 10) }),
                  (0, import_jsx_runtime.jsx)("option", { value: "time", children: new Date(datePickerValue || Date.now()).toLocaleString() })
                ]
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 pt-1", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "px-3 py-1.5 text-xs rounded border border-border text-foreground hover:bg-muted transition-colors",
                onClick: () => setDatePickerOpen(false),
                children: t("pptx.common.cancel", "Cancel")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "px-3 py-1.5 text-xs rounded bg-primary text-white hover:bg-primary/90 transition-colors",
                onClick: confirmDatePicker,
                children: t("pptx.common.insert", "Insert")
              }
            )
          ] })
        ] })
      }
    )
  ] });
}
function ReviewSection(p) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.review.proofing", { defaultValue: "Proofing" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.spelling"),
          icon: (0, import_jsx_runtime.jsx)(LuSpellCheck, {}),
          onClick: () => p.onSetSpellCheckEnabled(!p.spellCheckEnabled),
          active: p.spellCheckEnabled,
          title: t("pptx.review.toggleSpellCheck")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.thesaurus", { defaultValue: "Thesaurus" }),
          icon: (0, import_jsx_runtime.jsx)(LuBookOpen, {}),
          disabled: true
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(RibbonGroup, { label: t("pptx.review.accessibility", { defaultValue: "Accessibility" }), children: (0, import_jsx_runtime.jsx)(
      RibbonCommand,
      {
        label: t("pptx.review.accessibilityCheck"),
        icon: (0, import_jsx_runtime.jsx)(LuShieldCheck, {}),
        onClick: p.onOpenAccessibilityCheck
      }
    ) }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.review.language"), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.translate", { defaultValue: "Translate" }),
          icon: (0, import_jsx_runtime.jsx)(LuLanguages, {}),
          disabled: true
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.language"),
          icon: (0, import_jsx_runtime.jsx)(LuGlobe, {}),
          onClick: p.onSetLanguage
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.review.changes", { defaultValue: "Changes" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.markAllRead", { defaultValue: "Mark All as Read" }),
          icon: (0, import_jsx_runtime.jsx)(LuCopy, {}),
          disabled: true
        }
      ),
      p.onCompare && (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.ribbon.compare"),
          icon: (0, import_jsx_runtime.jsx)(LuGitCompare, {}),
          onClick: p.onCompare,
          disabled: !p.canEdit,
          title: t("pptx.ribbon.compareTitle")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.toolbar.comments"), children: [
      p.onToggleComments && (0, import_jsx_runtime.jsxs)("div", { className: "relative", children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            label: t("pptx.toolbar.comments"),
            icon: (0, import_jsx_runtime.jsx)(LuMessageSquarePlus, {}),
            onClick: p.onToggleComments,
            active: p.isCommentsPanelOpen,
            title: t("pptx.review.toggleComments")
          }
        ),
        Boolean(p.slideCommentCount) && (0, import_jsx_runtime.jsx)("span", { className: "absolute right-0 top-0 rounded-full bg-primary px-1 text-[9px] text-white", children: p.slideCommentCount })
      ] }),
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.common.delete", { defaultValue: "Delete" }),
            icon: (0, import_jsx_runtime.jsx)(LuTrash2, {}),
            disabled: true
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.common.previous", { defaultValue: "Previous" }),
            icon: (0, import_jsx_runtime.jsx)(LuChevronLeft, {}),
            disabled: true
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.common.next", { defaultValue: "Next" }),
            icon: (0, import_jsx_runtime.jsx)(LuChevronRight, {}),
            disabled: true
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.review.showComments", { defaultValue: "Show Comments" }),
            icon: (0, import_jsx_runtime.jsx)(LuMessageSquare, {}),
            onClick: p.onToggleComments
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.review.protect", { defaultValue: "Protect" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.readOnly", { defaultValue: "Always Open Read-only" }),
          icon: (0, import_jsx_runtime.jsx)(LuLockKeyhole, {}),
          disabled: true
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.restrictPermission", { defaultValue: "Restrict Permission" }),
          icon: (0, import_jsx_runtime.jsx)(LuShieldCheck, {}),
          disabled: true
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.review.hideInk", { defaultValue: "Hide Ink" }),
          icon: (0, import_jsx_runtime.jsx)(LuEyeOff, {}),
          disabled: true
        }
      )
    ] })
  ] });
}
function CustomShowsControls({
  customShows,
  activeCustomShowId,
  canEdit,
  isCurrentSlideInActiveShow,
  onSetActiveCustomShowId,
  onCreateCustomShow,
  onRenameActiveCustomShow,
  onDeleteActiveCustomShow,
  onToggleCurrentSlideInActiveShow
}) {
  const { t } = useTranslation();
  if (customShows.length > 0) {
    return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      sep,
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          value: activeCustomShowId ?? "",
          onChange: (e) => onSetActiveCustomShowId(e.target.value || null),
          className: "h-6 px-1.5 text-[11px] rounded bg-muted text-foreground border border-border hover:bg-accent transition-colors cursor-pointer",
          title: t("pptx.customShows.customShowTooltip"),
          "aria-label": t("pptx.customShows.selectCustomShow"),
          children: [
            (0, import_jsx_runtime.jsx)("option", { value: "", children: t("pptx.customShows.allSlides") }),
            customShows.map((cs) => (0, import_jsx_runtime.jsx)("option", { value: cs.id, children: cs.name }, cs.id))
          ]
        }
      ),
      canEdit && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onCreateCustomShow,
            className: "px-2 py-1 rounded bg-muted hover:bg-accent text-[11px] transition-colors",
            title: t("pptx.customShows.createTooltip"),
            children: "+ Show"
          }
        ),
        activeCustomShowId && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onRenameActiveCustomShow,
              className: "px-2 py-1 rounded bg-muted hover:bg-accent text-[11px] transition-colors",
              title: t("pptx.customShows.renameTooltip"),
              children: t("pptx.sections.rename")
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onDeleteActiveCustomShow,
              className: "px-2 py-1 rounded bg-red-700/80 hover:bg-red-600 text-[11px] transition-colors",
              title: t("pptx.customShows.deleteTooltip"),
              children: t("pptx.customShows.delete")
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onToggleCurrentSlideInActiveShow,
              className: cn(
                "px-2 py-1 rounded text-[11px] transition-colors",
                isCurrentSlideInActiveShow ? "bg-primary text-white" : "bg-muted hover:bg-accent"
              ),
              title: t("pptx.customShows.toggleSlideTooltip"),
              children: isCurrentSlideInActiveShow ? t("pptx.customShows.inShow") : t("pptx.customShows.addSlide")
            }
          )
        ] })
      ] })
    ] });
  }
  if (canEdit) {
    return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      sep,
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onCreateCustomShow,
          className: "px-2 py-1 rounded bg-muted hover:bg-accent text-[11px] transition-colors",
          title: t("pptx.customShows.createTooltip"),
          children: "+ Show"
        }
      )
    ] });
  }
  return null;
}
function SlideShowSection(p) {
  const { t } = useTranslation();
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  const [showsOpen, setShowsOpen] = (0, import_react2.useState)(false);
  const showsRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    if (!showsOpen) {
      return;
    }
    const handler = (event) => {
      if (showsRef.current && !showsRef.current.contains(event.target)) {
        setShowsOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [showsOpen]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.slideShow.start", { defaultValue: "Start Slide Show" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.fromBeginning"),
          icon: (0, import_jsx_runtime.jsx)(LuPlay, {}),
          onClick: p.onPresentFromBeginning ?? (() => p.onSetMode("present")),
          title: t("pptx.slideShow.fromBeginningTooltip")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.fromCurrent"),
          icon: (0, import_jsx_runtime.jsx)(LuMonitorPlay, {}),
          onClick: p.onPresent,
          title: t("pptx.slideShow.fromCurrentTooltip")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.slideShow.present", { defaultValue: "Present" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.presenterView"),
          icon: (0, import_jsx_runtime.jsx)(LuPresentation, {}),
          onClick: p.onEnterPresenterView,
          title: t("pptx.slideShow.presenterViewTooltip")
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: showsRef, children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            label: t("pptx.slideShow.customShow", { defaultValue: "Custom Show" }),
            icon: (0, import_jsx_runtime.jsx)(LuListVideo, {}),
            onClick: () => setShowsOpen((open) => !open),
            active: showsOpen,
            title: t("pptx.customShows.customShowTooltip")
          }
        ),
        showsOpen && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: showsRef, className: "pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "flex items-center gap-1 rounded-lg border border-border bg-popover p-2 shadow-2xl", children: (0, import_jsx_runtime.jsx)(CustomShowsControls, { ...p.customShowControls }) }) })
      ] }),
      !isHidden("broadcast") && (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.broadcast"),
          icon: (0, import_jsx_runtime.jsx)(LuCast, {}),
          onClick: p.onOpenBroadcastDialog,
          title: t("pptx.slideShow.broadcastTooltip")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.slideShow.setUpGroup", { defaultValue: "Set Up" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.rehearseCoach", { defaultValue: "Rehearse with Coach" }),
          icon: (0, import_jsx_runtime.jsx)(LuVideo, {}),
          disabled: true
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.setUp"),
          icon: (0, import_jsx_runtime.jsx)(LuSettings2, {}),
          onClick: p.onOpenSetUpSlideShow,
          title: t("pptx.slideShow.setUpTooltip")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.hideSlide", { defaultValue: "Hide Slide" }),
          icon: (0, import_jsx_runtime.jsx)(LuEyeOff, {}),
          onClick: p.onToggleHideSlide,
          active: p.activeSlideHidden,
          pressed: p.activeSlideHidden
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideShow.rehearseTimings"),
          icon: (0, import_jsx_runtime.jsx)(LuClock3, {}),
          onClick: p.onEnterRehearsalMode,
          title: t("pptx.slideShow.rehearseTimingsTooltip")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.titleBar.record"),
          icon: (0, import_jsx_runtime.jsx)(LuVideo, {}),
          onClick: p.onEnterRehearsalMode
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.slideShow.options", { defaultValue: "Options" }), children: [
      (0, import_jsx_runtime.jsx)(RibbonCommandStack, { children: SLIDE_SHOW_OPTIONS.slice(0, 3).map((option) => (0, import_jsx_runtime.jsx)(
        RibbonToggle,
        {
          label: t(option.labelKey),
          checked: readSlideShowOption(p.presentationProperties, option.id),
          disabled: option.unsupported,
          onChange: option.unsupported ? void 0 : (next) => {
            var _a;
            const change = slideShowOptionChange(option.id, next);
            if (change) {
              (_a = p.onPresentationPropertiesChange) == null ? void 0 : _a.call(p, change);
            }
          }
        },
        option.id
      )) }),
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonToggle,
          {
            label: t(SLIDE_SHOW_OPTIONS[3].labelKey),
            checked: readSlideShowOption(p.presentationProperties, SLIDE_SHOW_OPTIONS[3].id),
            disabled: SLIDE_SHOW_OPTIONS[3].unsupported
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonToggle,
          {
            label: t("pptx.slideShow.subtitles"),
            checked: p.showSubtitles,
            onChange: () => p.onToggleSubtitles(),
            title: t("pptx.slideShow.subtitlesTooltip")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.slideShow.subtitleSettings", { defaultValue: "Subtitle Settings" }),
            icon: (0, import_jsx_runtime.jsx)(LuCaptions, {}),
            onClick: p.onToggleSubtitles
          }
        )
      ] })
    ] })
  ] });
}
function RecentColorsRow({
  prefix,
  disabled,
  onCommit
}) {
  const { t } = useTranslation();
  const { recentColors, pushColor } = useRecentColors();
  if (recentColors.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "pptx-color-recent",
      "aria-label": t("pptx.colorPicker.recentColors"),
      className: "mt-1 flex flex-wrap items-center gap-1",
      children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground", children: t("pptx.colorPicker.recentColors") }),
        recentColors.map((c) => (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-compact": true,
            className: "h-4 w-4 rounded border border-primary",
            style: { backgroundColor: c },
            title: c,
            "aria-label": `Recent ${c}`,
            disabled,
            onClick: () => {
              onCommit(c);
              pushColor(c);
            }
          },
          `${prefix}-recent-${c}`
        ))
      ]
    }
  );
}
var ThemeColorMapContext = (0, import_react2.createContext)(void 0);
var ThemeColorMapProvider = ThemeColorMapContext.Provider;
function useThemeColorMap() {
  return (0, import_react2.useContext)(ThemeColorMapContext);
}
function ThemeColorSwatchGrid({
  prefix,
  disabled,
  selectedRef,
  selectedHex,
  onPick
}) {
  const { t } = useTranslation();
  const themeColorMap = useThemeColorMap();
  const columns = (0, import_react2.useMemo)(() => buildThemeColorSwatchGrid(themeColorMap), [themeColorMap]);
  const rows = (0, import_react2.useMemo)(() => themeColorSwatchRows(columns), [columns]);
  const selected = (0, import_react2.useMemo)(
    () => findSelectedThemeSwatch(columns, selectedRef, selectedHex),
    [columns, selectedRef, selectedHex]
  );
  if (columns.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-1", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.colorPicker.themeColors") }),
    (0, import_jsx_runtime.jsx)("div", { className: "flex flex-col gap-0.5", children: rows.map((row, rowIndex) => (
      // eslint-disable-next-line react/no-array-index-key
      (0, import_jsx_runtime.jsx)("div", { className: "flex gap-0.5", children: row.map(
        (swatch, colIndex) => swatch ? (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled,
            "data-pptx-compact": true,
            title: swatch.label,
            "aria-label": swatch.label,
            className: `h-4 w-4 rounded-sm border transition-transform hover:scale-110 disabled:opacity-40 disabled:cursor-not-allowed ${selected === swatch ? "border-primary ring-1 ring-primary" : "border-border"}`,
            style: { backgroundColor: swatch.hex },
            onClick: () => onPick(themeSwatchCommit(swatch))
          },
          `${prefix}-theme-${swatch.ref.scheme}-${rowIndex}`
        ) : (0, import_jsx_runtime.jsx)("div", { className: "h-4 w-4" }, `${prefix}-theme-empty-${rowIndex}-${colIndex}`)
      ) }, `${prefix}-theme-row-${rowIndex}`)
    )) })
  ] });
}
var LINE_SPACING_OPTIONS = [
  { label: "1.0", value: 1 },
  { label: "1.15", value: 1.15 },
  { label: "1.5", value: 1.5 },
  { label: "2.0", value: 2 },
  { label: "2.5", value: 2.5 },
  { label: "3.0", value: 3 }
];
var TEXT_DIRECTION_OPTIONS = [
  { label: "Horizontal", value: "horizontal" },
  { label: "Rotate 90°", value: "vertical" },
  { label: "Rotate 270°", value: "vertical270" },
  { label: "Stacked", value: "wordArtVert" }
];
var COLUMN_OPTIONS = [
  { label: "1 Column", value: 1 },
  { label: "2 Columns", value: 2 },
  { label: "3 Columns", value: 3 }
];
function useCloseOnClickOutside(ref, open, setOpen) {
  (0, import_react2.useEffect)(() => {
    if (!open) {
      return;
    }
    const handler = (e) => {
      if (ref.current && !ref.current.contains(e.target)) {
        setOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [open, ref, setOpen]);
}
function LineSpacingDropdown(p) {
  const { t } = useTranslation();
  const [open, setOpen] = (0, import_react2.useState)(false);
  const ref = (0, import_react2.useRef)(null);
  useCloseOnClickOutside(ref, open, setOpen);
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref, children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !p.canMut,
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => setOpen((v) => !v),
        className: pill,
        title: t("pptx.paragraph.lineSpacing"),
        "aria-label": t("pptx.paragraph.lineSpacing"),
        children: (0, import_jsx_runtime.jsxs)("svg", { className: ic, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", children: [
          (0, import_jsx_runtime.jsx)("line", { x1: "5", y1: "5", x2: "19", y2: "5" }),
          (0, import_jsx_runtime.jsx)("line", { x1: "5", y1: "12", x2: "19", y2: "12" }),
          (0, import_jsx_runtime.jsx)("line", { x1: "5", y1: "19", x2: "19", y2: "19" }),
          (0, import_jsx_runtime.jsx)("path", { d: "M2 8 L2 3 M2 3 L3.5 4.5 M2 3 L0.5 4.5" }),
          (0, import_jsx_runtime.jsx)("path", { d: "M2 16 L2 21 M2 21 L3.5 19.5 M2 21 L0.5 19.5" })
        ] })
      }
    ),
    open && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: ref, className: "pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-28", children: LINE_SPACING_OPTIONS.map((opt) => {
      var _a;
      return (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
          onMouseDown: (e) => e.preventDefault(),
          onClick: () => {
            if (!p.canFormat) {
              return;
            }
            p.onUpdateTextStyle({ lineSpacing: opt.value });
            setOpen(false);
          },
          children: [
            opt.label,
            ((_a = p.effectiveTs) == null ? void 0 : _a.lineSpacing) === opt.value && (0, import_jsx_runtime.jsx)("span", { className: "ml-auto text-primary", children: "•" })
          ]
        },
        opt.value
      );
    }) }) })
  ] });
}
function TextDirectionDropdown(p) {
  const { t } = useTranslation();
  const [open, setOpen] = (0, import_react2.useState)(false);
  const ref = (0, import_react2.useRef)(null);
  useCloseOnClickOutside(ref, open, setOpen);
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref, children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !p.canMut,
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => setOpen((v) => !v),
        className: pill,
        title: t("pptx.paragraph.textDirection"),
        "aria-label": t("pptx.paragraph.textDirection"),
        children: (0, import_jsx_runtime.jsxs)("svg", { className: ic, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", children: [
          (0, import_jsx_runtime.jsx)("text", { x: "3", y: "16", fontSize: "12", fill: "currentColor", stroke: "none", children: "A" }),
          (0, import_jsx_runtime.jsx)("path", { d: "M18 6 C21 6 21 10 18 10" }),
          (0, import_jsx_runtime.jsx)("path", { d: "M18 10 L19.5 8.5 M18 10 L16.5 8.5" })
        ] })
      }
    ),
    open && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: ref, className: "pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-36", children: TEXT_DIRECTION_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => {
          if (!p.canFormat) {
            return;
          }
          p.onUpdateTextStyle({ textDirection: opt.value });
          setOpen(false);
        },
        children: opt.label
      },
      opt.value
    )) }) })
  ] });
}
function ColumnsDropdown(p) {
  const { t } = useTranslation();
  const [open, setOpen] = (0, import_react2.useState)(false);
  const ref = (0, import_react2.useRef)(null);
  useCloseOnClickOutside(ref, open, setOpen);
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref, children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !p.canMut,
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => setOpen((v) => !v),
        className: pill,
        title: t("pptx.paragraph.columns"),
        children: (0, import_jsx_runtime.jsx)(LuColumns3, { className: ic })
      }
    ),
    open && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: ref, className: "pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-28", children: COLUMN_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
        onMouseDown: (e) => e.preventDefault(),
        onClick: () => {
          if (!p.canFormat) {
            return;
          }
          p.onUpdateTextStyle({ columnCount: opt.value });
          setOpen(false);
        },
        children: opt.label
      },
      opt.value
    )) }) })
  ] });
}
function getEffectiveTextStyle(element, tableEditorState) {
  var _a;
  if (!element) {
    return void 0;
  }
  if (hasTextProperties(element)) {
    return element.textStyle;
  }
  if (element.type === "table" && tableEditorState && element.tableData) {
    const cell = (_a = element.tableData.rows[tableEditorState.rowIndex]) == null ? void 0 : _a.cells[tableEditorState.columnIndex];
    return cell == null ? void 0 : cell.style;
  }
  return void 0;
}
var HIGHLIGHT_COLOR_PRESETS = [
  "#ffff00",
  "#00ff00",
  "#00ffff",
  "#ff00ff",
  "#0000ff",
  "#ff0000",
  "#000080",
  "#008080",
  "#008000",
  "#800080"
];
function TextSection(p) {
  var _a, _b2, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
  const { t } = useTranslation();
  const hasSel = Boolean(p.selectedElement);
  const canMut = hasSel && p.canEdit;
  const isTextEl = hasSel && p.selectedElement !== null && hasTextProperties(p.selectedElement);
  const isTable = hasSel && ((_a = p.selectedElement) == null ? void 0 : _a.type) === "table";
  const canFormat = isTextEl || isTable;
  const effectiveTs = getEffectiveTextStyle(p.selectedElement, p.tableEditorState);
  const currentColor = isTextEl && p.selectedElement && hasTextProperties(p.selectedElement) ? ((_d = (_c = (_b2 = p.selectedElement.textSegments) == null ? void 0 : _b2[0]) == null ? void 0 : _c.style) == null ? void 0 : _d.color) ?? ((_e = p.selectedElement.textStyle) == null ? void 0 : _e.color) ?? "#000000" : (effectiveTs == null ? void 0 : effectiveTs.color) ?? "#000000";
  const currentColorThemeRef = isTextEl && p.selectedElement && hasTextProperties(p.selectedElement) ? ((_h = (_g = (_f = p.selectedElement.textSegments) == null ? void 0 : _f[0]) == null ? void 0 : _g.style) == null ? void 0 : _h.colorRef) ?? ((_i = p.selectedElement.textStyle) == null ? void 0 : _i.colorRef) : void 0;
  const currentHighlight = isTextEl && p.selectedElement && hasTextProperties(p.selectedElement) ? ((_l = (_k = (_j = p.selectedElement.textSegments) == null ? void 0 : _j[0]) == null ? void 0 : _k.style) == null ? void 0 : _l.highlightColor) ?? ((_m = p.selectedElement.textStyle) == null ? void 0 : _m.highlightColor) ?? "#ffff00" : "#ffff00";
  const colorInputRef = (0, import_react2.useRef)(null);
  const highlightInputRef = (0, import_react2.useRef)(null);
  const charSpacingRef = (0, import_react2.useRef)(null);
  const changeCaseRef = (0, import_react2.useRef)(null);
  const fontColorRef = (0, import_react2.useRef)(null);
  const highlightMenuRef = (0, import_react2.useRef)(null);
  const { pushColor } = useRecentColors();
  const handleColorChange = (0, import_react2.useCallback)(
    (color, ref) => {
      if (!canFormat) {
        return;
      }
      p.onUpdateTextStyle({ color, colorRef: ref });
      pushColor(color);
    },
    [canFormat, p, pushColor]
  );
  const handleHighlightChange = (0, import_react2.useCallback)(
    (highlightColor) => {
      if (!canFormat) {
        return;
      }
      p.onUpdateTextStyle({ highlightColor });
      pushColor(highlightColor);
    },
    [canFormat, p, pushColor]
  );
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)("div", { className: grp, children: FMT.map((b, i, a) => {
          const handleClick = () => {
            if (!canFormat || !p.selectedElement) {
              return;
            }
            const ts = effectiveTs;
            switch (b.id) {
              case "bold":
                p.onUpdateTextStyle({ bold: !(ts == null ? void 0 : ts.bold) });
                break;
              case "italic":
                p.onUpdateTextStyle({ italic: !(ts == null ? void 0 : ts.italic) });
                break;
              case "underline":
                p.onUpdateTextStyle({
                  underline: !(ts == null ? void 0 : ts.underline)
                });
                break;
              case "strikethrough":
                p.onUpdateTextStyle({
                  strikethrough: !(ts == null ? void 0 : ts.strikethrough)
                });
                break;
            }
          };
          return (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: handleClick,
              className: i < a.length - 1 ? gB : gL,
              title: t(b.labelKey),
              children: b.i
            },
            b.id
          );
        }) }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canMut,
            onMouseDown: (e) => e.preventDefault(),
            onClick: () => {
              if (!canFormat) {
                return;
              }
              const hasShadow = Boolean(effectiveTs == null ? void 0 : effectiveTs.textShadowColor);
              p.onUpdateTextStyle(
                hasShadow ? {
                  textShadowColor: void 0,
                  textShadowBlur: void 0,
                  textShadowOffsetX: void 0,
                  textShadowOffsetY: void 0
                } : {
                  textShadowColor: "#000000",
                  textShadowBlur: 2,
                  textShadowOffsetX: 1,
                  textShadowOffsetY: 1,
                  textShadowOpacity: 0.5
                }
              );
            },
            className: pill,
            title: t("pptx.textEffects.shadow"),
            "aria-label": t("pptx.textEffects.shadow"),
            children: (0, import_jsx_runtime.jsxs)(
              "svg",
              {
                className: ic,
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                children: [
                  (0, import_jsx_runtime.jsx)("text", { x: "6", y: "17", fontSize: "16", fontWeight: "bold", fill: "currentColor", stroke: "none", children: "S" }),
                  (0, import_jsx_runtime.jsx)(
                    "text",
                    {
                      x: "7.5",
                      y: "18.5",
                      fontSize: "16",
                      fontWeight: "bold",
                      fill: "none",
                      stroke: "currentColor",
                      strokeWidth: "0.5",
                      opacity: "0.4",
                      children: "S"
                    }
                  )
                ]
              }
            )
          }
        ),
        (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = (effectiveTs == null ? void 0 : effectiveTs.fontSize) ?? (isTextEl ? textFontSizePtToPx(18) : 18);
                const delta = isTextEl ? textFontSizePtToPx(2) : 2;
                p.onUpdateTextStyle({ fontSize: current + delta });
              },
              className: gB,
              title: t("pptx.text.increaseFontSize"),
              children: (0, import_jsx_runtime.jsx)(LuAArrowUp, { className: ic })
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = (effectiveTs == null ? void 0 : effectiveTs.fontSize) ?? (isTextEl ? textFontSizePtToPx(18) : 18);
                const delta = isTextEl ? textFontSizePtToPx(2) : 2;
                const minimum = isTextEl ? textFontSizePtToPx(1) : 1;
                p.onUpdateTextStyle({ fontSize: Math.max(minimum, current - delta) });
              },
              className: gB,
              title: t("pptx.text.decreaseFontSize"),
              children: (0, import_jsx_runtime.jsx)(LuAArrowDown, { className: ic })
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat) {
                  return;
                }
                p.onUpdateTextStyle({
                  bold: false,
                  italic: false,
                  underline: false,
                  strikethrough: false,
                  highlightColor: void 0
                });
              },
              className: gL,
              title: t("pptx.text.clearFormatting"),
              children: (0, import_jsx_runtime.jsx)(LuRemoveFormatting, { className: ic })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative group", ref: charSpacingRef, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.characterSpacing"),
              "aria-label": t("pptx.text.characterSpacing"),
              children: (0, import_jsx_runtime.jsxs)(
                "svg",
                {
                  className: ic,
                  viewBox: "0 0 24 24",
                  fill: "none",
                  stroke: "currentColor",
                  strokeWidth: "1.5",
                  children: [
                    (0, import_jsx_runtime.jsx)(
                      "text",
                      {
                        x: "4",
                        y: "16",
                        fontSize: "11",
                        fontWeight: "bold",
                        fill: "currentColor",
                        stroke: "none",
                        children: "AV"
                      }
                    ),
                    (0, import_jsx_runtime.jsx)("path", { d: "M3 20 L1 20 M3 20 L5 20", strokeWidth: "1.5" }),
                    (0, import_jsx_runtime.jsx)("path", { d: "M21 20 L19 20 M21 20 L23 20", strokeWidth: "1.5" })
                  ]
                }
              )
            }
          ),
          (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: charSpacingRef, className: "hidden group-hover:block pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-32", children: CHARACTER_SPACING_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat) {
                  return;
                }
                p.onUpdateTextStyle({ characterSpacing: opt.value });
              },
              children: t(opt.i18nKey)
            },
            opt.i18nKey
          )) }) })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative group", ref: changeCaseRef, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.changeCase"),
              "aria-label": t("pptx.text.changeCase"),
              children: (0, import_jsx_runtime.jsx)(
                "svg",
                {
                  className: ic,
                  viewBox: "0 0 24 24",
                  fill: "none",
                  stroke: "currentColor",
                  strokeWidth: "1.5",
                  children: (0, import_jsx_runtime.jsx)(
                    "text",
                    {
                      x: "2",
                      y: "16",
                      fontSize: "13",
                      fontWeight: "bold",
                      fill: "currentColor",
                      stroke: "none",
                      children: "Aa"
                    }
                  )
                }
              )
            }
          ),
          (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: changeCaseRef, className: "hidden group-hover:block pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-44", children: [
            { label: t("pptx.text.changeCaseSentence"), value: "sentence" },
            { label: t("pptx.text.changeCaseLower"), value: "lower" },
            { label: t("pptx.text.changeCaseUpper"), value: "upper" },
            { label: t("pptx.text.changeCaseCapitalize"), value: "capitalize" },
            { label: t("pptx.text.changeCaseToggle"), value: "toggle" }
          ].map((opt) => (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat) {
                  return;
                }
                if (isTable) {
                  p.onUpdateTextStyle({
                    textCaps: opt.value === "upper" ? "all" : "none"
                  });
                  return;
                }
                p.onTransformTextCase(opt.value);
              },
              children: opt.label
            },
            opt.value
          )) }) })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative group", ref: fontColorRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.fontColor"),
              "aria-label": t("pptx.text.fontColor"),
              children: [
                (0, import_jsx_runtime.jsx)(
                  "svg",
                  {
                    className: ic,
                    viewBox: "0 0 24 24",
                    fill: "none",
                    stroke: "currentColor",
                    strokeWidth: "2",
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    children: (0, import_jsx_runtime.jsx)("path", { d: "M6 20h12M9.5 4h5L18 16H6L9.5 4z" })
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: "w-4 h-1 rounded-sm -mt-0.5",
                    style: { backgroundColor: currentColor }
                  }
                )
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: fontColorRef, className: "hidden group-hover:block pt-1", children: (0, import_jsx_runtime.jsxs)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-2 w-48", children: [
            (0, import_jsx_runtime.jsx)(
              ThemeColorSwatchGrid,
              {
                prefix: "font-color",
                disabled: !canMut,
                selectedRef: currentColorThemeRef,
                selectedHex: currentColor,
                onPick: (c) => handleColorChange(c.hex, c.ref)
              }
            ),
            (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mt-1 mb-1", children: t("pptx.colorPicker.standardColors") }),
            (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-5 gap-1.5 mb-2", children: OFFICE_COLOR_SWATCHES.map((c) => (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                "aria-label": c.label,
                title: c.label,
                "data-pptx-compact": true,
                className: `w-5 h-5 rounded-full border transition-transform hover:scale-125 ${(currentColor == null ? void 0 : currentColor.toLowerCase()) === c.hex ? "border-primary ring-1 ring-primary" : "border-border"}`,
                style: { backgroundColor: c.hex },
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => handleColorChange(c.hex)
              },
              c.hex
            )) }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "w-full text-[10px] text-muted-foreground hover:text-foreground py-1 transition-colors",
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => {
                  var _a2;
                  return (_a2 = colorInputRef.current) == null ? void 0 : _a2.click();
                },
                children: t("pptx.ribbon.customColour")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                ref: colorInputRef,
                type: "color",
                className: "sr-only",
                value: currentColor,
                onChange: (e) => handleColorChange(e.target.value)
              }
            ),
            (0, import_jsx_runtime.jsx)(
              RecentColorsRow,
              {
                prefix: "font-color",
                disabled: !canMut,
                onCommit: handleColorChange
              }
            )
          ] }) })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative group", ref: highlightMenuRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              className: pill,
              title: t("pptx.text.highlightColor"),
              "aria-label": t("pptx.text.highlightColor"),
              children: [
                (0, import_jsx_runtime.jsx)(LuHighlighter, { className: ic }),
                (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: "w-4 h-1 rounded-sm -mt-0.5",
                    style: { backgroundColor: currentHighlight }
                  }
                )
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: highlightMenuRef, className: "hidden group-hover:block pt-1", children: (0, import_jsx_runtime.jsxs)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-2 w-36", children: [
            (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-5 gap-1.5 mb-2", children: HIGHLIGHT_COLOR_PRESETS.map((c) => (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                "aria-label": c,
                "data-pptx-compact": true,
                className: `w-5 h-5 rounded-full border transition-transform hover:scale-125 ${(currentHighlight == null ? void 0 : currentHighlight.toLowerCase()) === c ? "border-primary ring-1 ring-primary" : "border-border"}`,
                style: { backgroundColor: c },
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => handleHighlightChange(c)
              },
              c
            )) }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "w-full text-[10px] text-muted-foreground hover:text-foreground py-1 transition-colors",
                onMouseDown: (e) => e.preventDefault(),
                onClick: () => {
                  var _a2;
                  return (_a2 = highlightInputRef.current) == null ? void 0 : _a2.click();
                },
                children: t("pptx.ribbon.customColour")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                ref: highlightInputRef,
                type: "color",
                className: "sr-only",
                value: currentHighlight,
                onChange: (e) => handleHighlightChange(e.target.value)
              }
            )
          ] }) })
        ] })
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Font" })
    ] }),
    sep,
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                p.onUpdateTextStyle({
                  listType: (effectiveTs == null ? void 0 : effectiveTs.listType) === "bullet" ? "none" : "bullet"
                });
              },
              className: gB,
              title: t("pptx.text.bulletList"),
              children: (0, import_jsx_runtime.jsx)(LuList, { className: ic })
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                p.onUpdateTextStyle({
                  listType: (effectiveTs == null ? void 0 : effectiveTs.listType) === "numbered" ? "none" : "numbered"
                });
              },
              className: gL,
              title: t("pptx.text.numberedList"),
              children: (0, import_jsx_runtime.jsx)(LuListOrdered, { className: ic })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = (effectiveTs == null ? void 0 : effectiveTs.paragraphMarginLeft) ?? 0;
                p.onUpdateTextStyle({
                  paragraphMarginLeft: Math.max(0, current - 24)
                });
              },
              className: gB,
              title: t("pptx.text.decreaseIndent"),
              children: (0, import_jsx_runtime.jsx)(LuIndentDecrease, { className: ic })
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                if (!canFormat || !p.selectedElement) {
                  return;
                }
                const current = (effectiveTs == null ? void 0 : effectiveTs.paragraphMarginLeft) ?? 0;
                p.onUpdateTextStyle({
                  paragraphMarginLeft: current + 24
                });
              },
              className: gL,
              title: t("pptx.text.increaseIndent"),
              children: (0, import_jsx_runtime.jsx)(LuIndentIncrease, { className: ic })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: grp, children: ATXT.map((b, i, a) => {
          const handleClick = () => {
            if (!canFormat) {
              return;
            }
            p.onUpdateTextStyle({ align: b.id });
          };
          return (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canMut,
              onMouseDown: (e) => e.preventDefault(),
              onClick: handleClick,
              className: i < a.length - 1 ? gB : gL,
              title: t(b.labelKey),
              children: b.i
            },
            b.id
          );
        }) }),
        (0, import_jsx_runtime.jsx)(
          LineSpacingDropdown,
          {
            canMut,
            canFormat,
            effectiveTs,
            onUpdateTextStyle: p.onUpdateTextStyle
          }
        ),
        (0, import_jsx_runtime.jsx)(
          TextDirectionDropdown,
          {
            canMut,
            canFormat,
            onUpdateTextStyle: p.onUpdateTextStyle
          }
        ),
        (0, import_jsx_runtime.jsx)(
          ColumnsDropdown,
          {
            canMut,
            canFormat,
            onUpdateTextStyle: p.onUpdateTextStyle
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Paragraph" })
    ] })
  ] });
}
function ViewSection(p) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.view.presentationViews"), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.view.normal"),
          icon: (0, import_jsx_runtime.jsx)(LuPanelTop, {}),
          title: t("pptx.statusBar.normalView")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.slideSorter.title"),
          icon: (0, import_jsx_runtime.jsx)(LuLayoutGrid, {}),
          onClick: p.onToggleSlideSorter,
          title: t("pptx.view.slideSorterTooltip")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.view.outlineView"),
          icon: (0, import_jsx_runtime.jsx)(LuIndentIncrease, {}),
          onClick: p.onOpenOutlineView,
          title: t("pptx.view.outlineViewTooltip")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.view.readingView"),
          icon: (0, import_jsx_runtime.jsx)(LuBookOpen, {}),
          onClick: p.onOpenReadingView,
          title: t("pptx.view.readingView")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.view.masterViews"), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.master.title"),
          icon: (0, import_jsx_runtime.jsx)(LuPresentation, {}),
          onClick: p.onEnterMasterView,
          disabled: !p.canEdit,
          title: t("pptx.view.slideMasterTooltip")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.master.handoutMasterTitle", { defaultValue: "Handout Master" }),
          icon: (0, import_jsx_runtime.jsx)(LuGrid3X3, {}),
          disabled: true
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.master.notesMasterTitle", { defaultValue: "Notes Master" }),
          icon: (0, import_jsx_runtime.jsx)(LuStickyNote, {}),
          disabled: true
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.view.show", { defaultValue: "Show" }), children: [
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonToggle,
          {
            label: t("pptx.ruler.rulers"),
            checked: p.showRulers,
            onChange: p.onSetShowRulers
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonToggle,
          {
            label: t("pptx.grid.grid"),
            checked: p.showGrid,
            onChange: p.onSetShowGrid,
            title: t("pptx.grid.toggleGrid")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonToggle,
          {
            label: t("pptx.view.guides", { defaultValue: "Guides" }),
            checked: p.showGuides,
            onChange: p.onSetShowGuides,
            title: t("pptx.ribbon.toggleGuides")
          }
        ),
        (0, import_jsx_runtime.jsx)(RibbonToggle, { label: "Snap to grid", checked: p.snapToGrid, onChange: p.onSetSnapToGrid })
      ] }),
      (0, import_jsx_runtime.jsxs)(RibbonCommandStack, { children: [
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.view.selection"),
            icon: (0, import_jsx_runtime.jsx)(LuList, {}),
            onClick: p.onToggleSelectionPane,
            active: p.isSelectionPaneOpen,
            title: t("pptx.selectionPane.title")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.ribbon.eyedropper"),
            icon: (0, import_jsx_runtime.jsx)(LuPipette, {}),
            onClick: p.onToggleEyedropper,
            active: p.eyedropperActive,
            disabled: !p.canEdit
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: t("pptx.view.snapToShape"),
            icon: (0, import_jsx_runtime.jsx)(LuGrid3X3, {}),
            onClick: () => p.onSetSnapToShape(!p.snapToShape),
            active: p.snapToShape,
            title: t("pptx.view.snapToShape")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: "H Guide",
            icon: (0, import_jsx_runtime.jsx)(LuRuler, {}),
            onClick: () => p.onAddGuide("h"),
            title: t("pptx.view.addHorizontalGuide")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RibbonCommand,
          {
            compact: true,
            label: "V Guide",
            icon: (0, import_jsx_runtime.jsx)(LuRuler, {}),
            onClick: () => p.onAddGuide("v"),
            title: t("pptx.view.addVerticalGuide")
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.slideSorter.zoom"), children: [
      (0, import_jsx_runtime.jsx)(RibbonCommand, { label: t("pptx.slideSorter.zoom"), icon: (0, import_jsx_runtime.jsx)(LuZoomIn, {}), disabled: true }),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.view.zoomToFit"),
          icon: (0, import_jsx_runtime.jsx)(LuMaximize2, {}),
          onClick: p.onZoomToFit
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: t("pptx.view.window", { defaultValue: "Window" }), children: [
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t(p.editTemplateMode ? "pptx.ribbon.templatesOn" : "pptx.ribbon.templatesOff"),
          icon: (0, import_jsx_runtime.jsx)(LuRuler, {}),
          onClick: () => p.onSetEditTemplateMode(!p.editTemplateMode),
          active: p.editTemplateMode,
          disabled: !p.canEdit,
          title: t("pptx.view.templateEditingTooltip", {
            defaultValue: "Toggle template/master element editing"
          })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        RibbonCommand,
        {
          label: t("pptx.view.macros", { defaultValue: "Macros" }),
          icon: (0, import_jsx_runtime.jsx)(LuCode, {}),
          disabled: true
        }
      )
    ] })
  ] });
}
function MobileSheet({
  open,
  onClose,
  title,
  children,
  heightFraction = 0.6,
  fullScreen = false,
  autoHeight = false,
  className,
  headerRight
}) {
  const sheetRef = (0, import_react2.useRef)(null);
  const { dragY, dragging, handlers } = useSheetDismissDrag(onClose);
  useModalFocus(open, sheetRef, onClose);
  if (!open) {
    return null;
  }
  const heightStyle = autoHeight ? { maxHeight: "calc(85dvh - env(safe-area-inset-top))" } : fullScreen ? { height: "calc(100dvh - env(safe-area-inset-top))" } : { height: `${Math.round(heightFraction * 100)}dvh` };
  return (0, import_jsx_runtime.jsxs)("div", { className: "fixed inset-0 z-50 flex flex-col justify-end", children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        "aria-label": "Close",
        className: "absolute inset-0 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150",
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        ref: sheetRef,
        role: "dialog",
        "aria-modal": "true",
        "aria-label": typeof title === "string" ? title : "Mobile panel",
        tabIndex: -1,
        className: cn(
          "relative bg-background border-t border-border rounded-t-2xl shadow-2xl flex flex-col overflow-hidden",
          "animate-in slide-in-from-bottom duration-200",
          className
        ),
        style: {
          ...heightStyle,
          transform: dragY > 0 ? `translateY(${dragY}px)` : void 0,
          transition: dragging ? "none" : "transform 150ms ease-out"
        },
        children: [
          (0, import_jsx_runtime.jsxs)(
            "div",
            {
              className: "cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: handlers.onPointerDown,
              onPointerMove: handlers.onPointerMove,
              onPointerUp: handlers.onPointerUp,
              onPointerCancel: handlers.onPointerCancel,
              children: [
                (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center pt-2 pb-1", children: (0, import_jsx_runtime.jsx)("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" }) }),
                (title || headerRight) && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2 px-4 pb-2 border-b border-border/60", children: [
                  (0, import_jsx_runtime.jsx)("div", { className: "text-sm font-semibold text-foreground truncate", children: title }),
                  headerRight,
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      "aria-label": "Close",
                      onPointerDown: (event) => event.stopPropagation(),
                      onClick: onClose,
                      className: "inline-flex h-8 w-8 items-center justify-center rounded text-xl text-muted-foreground hover:bg-accent hover:text-foreground",
                      children: "×"
                    }
                  )
                ] })
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-y-auto overscroll-contain", children })
        ]
      }
    )
  ] });
}
var MENU_ITEMS = [
  { key: "home", label: "pptx.ribbon.tab.home", icon: LuClipboardCopy },
  { key: "insert", label: "pptx.ribbon.tab.insert", icon: LuPlus },
  { key: "text", label: "pptx.ribbon.tab.text", icon: LuType },
  { key: "draw", label: "pptx.ribbon.tab.draw", icon: LuPaintbrush },
  { key: "arrange", label: "pptx.ribbon.tab.arrange", icon: LuShapes },
  { key: "design", label: "pptx.ribbon.tab.design", icon: LuLayoutGrid },
  { key: "transitions", label: "pptx.ribbon.tab.transitions", icon: LuSparkles },
  { key: "animations", label: "pptx.ribbon.tab.animations", icon: LuWand },
  { key: "slideShow", label: "pptx.ribbon.tab.slideShow", icon: LuPresentation },
  { key: "review", label: "pptx.ribbon.tab.review", icon: LuTextCursorInput },
  { key: "view", label: "pptx.ribbon.tab.view", icon: LuSettings },
  { key: "file", label: "pptx.ribbon.tab.file", icon: LuFile }
];
function MobileMenuSheet(props) {
  const { t } = useTranslation();
  const { open, onClose } = props;
  const [active, setActive] = (0, import_react2.useState)("home");
  const { isTabVisible } = useToolbarVisibility(props.hiddenActions);
  return (0, import_jsx_runtime.jsx)(MobileSheet, { open, onClose, autoHeight: true, title: t("pptx.mobileToolbar.menu"), children: (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "sticky top-0 z-10 bg-background border-b border-border", children: (0, import_jsx_runtime.jsx)("div", { className: "flex flex-wrap gap-1.5 px-3 py-2", children: MENU_ITEMS.filter(({ key }) => isTabVisible(key)).map(({ key, label, icon: Icon }) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: () => setActive(active === key ? null : key),
        className: cn(
          "inline-flex items-center gap-1.5 shrink-0 px-3 py-2 rounded-full text-[12px] font-medium border transition-colors min-h-[36px]",
          active === key ? "bg-primary text-white border-primary" : "border-border text-muted-foreground hover:text-foreground hover:bg-accent/40"
        ),
        children: [
          (0, import_jsx_runtime.jsx)(Icon, { className: "w-4 h-4" }),
          t(label)
        ]
      },
      key
    )) }) }),
    (0, import_jsx_runtime.jsx)("div", { className: "p-3", children: (0, import_jsx_runtime.jsx)(MobileSectionBody, { active, ...props }) })
  ] }) });
}
function MobileSectionBody({
  active,
  ...p
}) {
  const wrap = "flex flex-wrap items-center gap-2";
  switch (active) {
    case "home":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        HomeSection,
        {
          canEdit: p.canEdit,
          clipboardPayload: p.clipboardPayload,
          formatPainterActive: p.formatPainterActive,
          canActivateFormatPainter: p.canActivateFormatPainter,
          onCopy: p.onCopy,
          onCut: p.onCut,
          onPaste: p.onPaste,
          onToggleFormatPainter: p.onToggleFormatPainter,
          layoutOptions: p.layoutOptions,
          onInsertSlideFromLayout: p.onInsertSlideFromLayout,
          onInsertSlideFromTemplate: p.onInsertSlideFromTemplate,
          templateScheme: p.templateScheme,
          selectedElement: p.selectedElement,
          onUpdateTextStyle: p.onUpdateTextStyle
        }
      ) });
    case "insert":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        InsertSection,
        {
          canEdit: p.canEdit,
          newShapeType: p.newShapeType,
          onSetNewShapeType: p.onSetNewShapeType,
          onAddTextBox: p.onAddTextBox,
          onAddShape: p.onAddShape,
          onAddTable: p.onAddTable,
          onAddChart: p.onAddChart,
          onAddSmartArt: p.onAddSmartArt,
          onAddEquation: p.onAddEquation,
          onAddActionButton: p.onAddActionButton,
          onInsertField: p.onInsertField,
          onOpenImagePicker: p.onOpenImagePicker,
          onOpenMediaPicker: p.onOpenMediaPicker,
          hasSelection: Boolean(p.selectedElement),
          onOpenHyperlinkDialog: p.onOpenHyperlinkDialog
        }
      ) });
    case "text":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        TextSection,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          tableEditorState: p.tableEditorState,
          onUpdateTextStyle: p.onUpdateTextStyle,
          onTransformTextCase: p.onTransformTextCase
        }
      ) });
    case "draw":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        DrawSection,
        {
          activeTool: p.activeTool,
          drawingColor: p.drawingColor,
          drawingWidth: p.drawingWidth,
          onSetActiveTool: p.onSetActiveTool,
          onSetDrawingColor: p.onSetDrawingColor,
          onSetDrawingWidth: p.onSetDrawingWidth
        }
      ) });
    case "arrange":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        ArrangeSection,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          selectedCount: p.selectedCount,
          selectionGroupable: p.selectionGroupable,
          onAlignElements: p.onAlignElements,
          onDistributeElements: p.onDistributeElements,
          canDistribute: p.canDistribute,
          onFlip: p.onFlip,
          onMoveLayer: p.onMoveLayer,
          onMoveLayerToEdge: p.onMoveLayerToEdge,
          onGroupElements: p.onGroupElements,
          onUngroupElement: p.onUngroupElement,
          onUpdateElementStyle: p.onUpdateElementStyle,
          onDuplicate: p.onDuplicate,
          onDelete: p.onDelete,
          formatPainterActive: p.formatPainterActive,
          onToggleFormatPainter: p.onToggleFormatPainter,
          canActivateFormatPainter: p.canActivateFormatPainter
        }
      ) });
    case "design":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        DesignSection,
        {
          canEdit: p.canEdit,
          onToggleThemeGallery: p.onToggleThemeGallery,
          isThemeGalleryOpen: p.isThemeGalleryOpen,
          onToggleThemeEditor: p.onToggleThemeEditor,
          isThemeEditorOpen: p.isThemeEditorOpen,
          onOpenDocumentProperties: p.onOpenDocumentProperties,
          onOpenSlideSize: p.onOpenSlideSize,
          onToggleInspector: p.onToggleInspector,
          isInspectorPaneOpen: p.isInspectorPaneOpen
        }
      ) });
    case "transitions":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        TransitionsSection,
        {
          isInspectorPaneOpen: p.isInspectorPaneOpen,
          onToggleInspector: p.onToggleInspector,
          canEdit: p.canEdit,
          activeSlide: p.activeSlide,
          onTransitionChange: p.onTransitionChange,
          onApplyTransitionToAll: p.onApplyTransitionToAll
        }
      ) });
    case "animations":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        AnimationsSection,
        {
          canEdit: p.canEdit,
          selectedElement: p.selectedElement,
          isInspectorPaneOpen: p.isInspectorPaneOpen,
          onToggleInspector: p.onToggleInspector,
          onOpenAnimationPanel: p.onOpenAnimationPanel,
          onAddAnimation: p.onAddAnimation,
          onRemoveAnimation: p.onRemoveAnimation
        }
      ) });
    case "slideShow":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        SlideShowSection,
        {
          onPresent: () => p.onSetMode("present"),
          onPresentFromBeginning: p.onPresentFromBeginning,
          onEnterPresenterView: p.onEnterPresenterView ?? (() => {
          }),
          onEnterRehearsalMode: p.onEnterRehearsalMode ?? (() => {
          }),
          onOpenSetUpSlideShow: p.onOpenSetUpSlideShow ?? (() => {
          }),
          onToggleHideSlide: p.onToggleHideSlide ?? (() => {
          }),
          activeSlideHidden: p.activeSlideHidden ?? false,
          onOpenBroadcastDialog: p.onOpenBroadcastDialog ?? (() => {
          }),
          onToggleSubtitles: p.onToggleSubtitles ?? (() => {
          }),
          showSubtitles: p.showSubtitles ?? false,
          onSetMode: p.onSetMode,
          customShowControls: p,
          hiddenActions: p.hiddenActions
        }
      ) });
    case "review":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        ReviewSection,
        {
          canEdit: p.canEdit,
          spellCheckEnabled: p.spellCheckEnabled,
          onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
          onToggleComments: p.onToggleComments,
          isCommentsPanelOpen: p.isCommentsPanelOpen,
          slideCommentCount: p.slideCommentCount,
          onCompare: p.onCompare
        }
      ) });
    case "view":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        ViewSection,
        {
          canEdit: p.canEdit,
          editTemplateMode: p.editTemplateMode,
          onSetEditTemplateMode: p.onSetEditTemplateMode,
          spellCheckEnabled: p.spellCheckEnabled,
          onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
          showGrid: p.showGrid,
          showRulers: p.showRulers,
          showGuides: p.showGuides,
          onSetShowGuides: p.onSetShowGuides,
          snapToGrid: p.snapToGrid,
          snapToShape: p.snapToShape,
          onSetShowGrid: p.onSetShowGrid,
          onSetShowRulers: p.onSetShowRulers,
          onSetSnapToGrid: p.onSetSnapToGrid,
          onSetSnapToShape: p.onSetSnapToShape,
          onAddGuide: p.onAddGuide,
          onEnterMasterView: p.onEnterMasterView,
          isSelectionPaneOpen: p.isSelectionPaneOpen,
          onToggleSelectionPane: p.onToggleSelectionPane,
          eyedropperActive: p.eyedropperActive,
          onToggleEyedropper: p.onToggleEyedropper,
          onToggleSlideSorter: p.onToggleSlideSorter,
          onOpenReadingView: p.onOpenReadingView
        }
      ) });
    case "file":
      return (0, import_jsx_runtime.jsx)("div", { className: wrap, children: (0, import_jsx_runtime.jsx)(
        FileSection,
        {
          fileName: p.fileName,
          onClose: () => p.onSetToolbarSection("home"),
          onCreatePresentation: p.onCreatePresentation,
          onOpenFile: p.onOpenFile,
          onOpenRecentFile: p.onOpenRecentFile,
          onExportPng: p.onExportPng,
          onExportPdf: p.onExportPdf,
          onExportVideo: p.onExportVideo,
          onExportGif: p.onExportGif,
          onExportJson: p.onExportJson,
          onSaveAsPptx: p.onSaveAsPptx,
          onSaveAsPpsx: p.onSaveAsPpsx,
          onSaveAsPptm: p.onSaveAsPptm,
          hasMacros: p.hasMacros,
          onCopySlideAsImage: p.onCopySlideAsImage,
          onPrint: p.onPrint,
          onOpenSettings: p.onOpenSettings,
          onOpenShareDialog: p.onOpenShareDialog,
          onOpenDocumentProperties: p.onOpenDocumentProperties,
          onOpenPasswordProtection: p.onOpenPasswordProtection,
          onOpenFontEmbedding: p.onOpenFontEmbedding,
          onOpenDigitalSignatures: p.onOpenDigitalSignatures,
          onOpenVersionHistory: p.onToggleVersionHistory,
          hiddenActions: p.hiddenActions,
          recentPresentationsCount: p.recentPresentationsCount
        }
      ) });
    default:
      return (0, import_jsx_runtime.jsxs)("div", { className: "text-center text-sm text-muted-foreground py-8", children: [
        (0, import_jsx_runtime.jsx)(LuChevronRight, { className: "w-5 h-5 inline-block opacity-50" }),
        " Select a section above"
      ] });
  }
}
function MobileToolbar(props) {
  const { t } = useTranslation();
  const { mode, canUndo, canRedo, onUndo, onRedo, onSetMode, onSaveAsPptx } = props;
  const [menuOpen, setMenuOpen] = (0, import_react2.useState)(false);
  const { isHidden } = useToolbarVisibility(props.hiddenActions);
  const showEdit = mode === "edit" || mode === "master";
  const btn = "inline-flex items-center justify-center min-w-[44px] min-h-[44px] rounded-md text-foreground/80 hover:bg-accent/60 disabled:opacity-40 disabled:cursor-not-allowed active:scale-95 transition-transform";
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      role: "toolbar",
      "aria-label": t("pptx.mobileToolbar.toolbar"),
      className: "relative z-20 flex items-center gap-1 px-2 py-1 border-b border-border bg-secondary/50 min-h-[52px] pt-[max(env(safe-area-inset-top),0px)]",
      children: [
        showEdit && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: () => setMenuOpen(true),
            className: btn,
            title: t("pptx.mobileToolbar.menu"),
            "aria-label": t("pptx.mobileToolbar.menu"),
            children: (0, import_jsx_runtime.jsx)(LuMenu, { className: "w-5 h-5" })
          }
        ),
        showEdit && !isHidden("undo") && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onUndo,
            disabled: !canUndo,
            className: btn,
            title: t("pptx.toolbar.undo"),
            "aria-label": t("pptx.toolbar.undo"),
            children: (0, import_jsx_runtime.jsx)(LuUndo, { className: "w-5 h-5" })
          }
        ),
        showEdit && !isHidden("redo") && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onRedo,
            disabled: !canRedo,
            className: btn,
            title: t("pptx.toolbar.redo"),
            "aria-label": t("pptx.toolbar.redo"),
            children: (0, import_jsx_runtime.jsx)(LuRedo, { className: "w-5 h-5" })
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { className: "flex-1" }),
        showEdit && props.aiEnabled && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: props.onToggleAiPanel,
            className: cn(btn, props.isAiPanelOpen ? "text-primary" : void 0),
            title: t("pptx.toolbar.toggleAiAssistant"),
            "aria-label": t("pptx.toolbar.toggleAiAssistant"),
            children: (0, import_jsx_runtime.jsx)(LuSparkles, { className: "w-5 h-5" })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onSaveAsPptx,
            className: btn,
            title: t("pptx.toolbar.save", "Save"),
            "aria-label": t("pptx.toolbar.save", "Save"),
            children: (0, import_jsx_runtime.jsx)(LuDownload, { className: "w-5 h-5" })
          }
        ),
        !isHidden("fullscreen") && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: () => onSetMode("present"),
            className: cn(btn, "text-primary"),
            title: t("pptx.toolbar.present"),
            "aria-label": t("pptx.toolbar.present"),
            children: (0, import_jsx_runtime.jsx)(LuPresentation, { className: "w-5 h-5" })
          }
        ),
        showEdit && !isHidden("share") && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: props.onOpenShareDialog,
            className: cn(btn, "bg-primary text-white hover:bg-primary/90 px-3"),
            title: t("pptx.toolbar.share"),
            "aria-label": t("pptx.toolbar.share"),
            children: (0, import_jsx_runtime.jsx)(LuShare2, { className: "w-4 h-4" })
          }
        ),
        (0, import_jsx_runtime.jsx)(MobileMenuSheet, { open: menuOpen, onClose: () => setMenuOpen(false), ...props })
      ]
    }
  );
}
function ShapeColorPopover({
  icon,
  title,
  prefix,
  anchorRef,
  open,
  onToggle,
  disabled,
  swatchAriaLabel,
  selectedRef,
  selectedHex,
  onApply,
  onClose
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: anchorRef, children: [
    (0, import_jsx_runtime.jsx)("button", { type: "button", disabled, className: pill, title, onClick: onToggle, children: icon }),
    open && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef, className: "pt-1", children: (0, import_jsx_runtime.jsxs)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl p-2", children: [
      (0, import_jsx_runtime.jsx)(
        ThemeColorSwatchGrid,
        {
          prefix,
          disabled,
          selectedRef,
          selectedHex,
          onPick: (c) => {
            onApply(c.hex, c.ref);
            onClose();
          }
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "mt-1 text-[10px] text-muted-foreground mb-1", children: t("pptx.colorPicker.standardColors") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-6 gap-1", children: RIBBON_SHAPE_SWATCHES.map((c) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          "aria-label": `${swatchAriaLabel} ${c}`,
          "data-pptx-compact": true,
          className: "w-5 h-5 rounded border border-border/60 hover:scale-110 transition-transform",
          style: { backgroundColor: c },
          title: c,
          onClick: () => {
            onApply(c);
            onClose();
          }
        },
        c
      )) }),
      (0, import_jsx_runtime.jsx)(
        RecentColorsRow,
        {
          prefix,
          disabled,
          onCommit: (c) => {
            onApply(c);
            onClose();
          }
        }
      )
    ] }) })
  ] });
}
var TOP_SHAPES = SHAPE_PRESETS.slice(0, 12);
function DrawingGroup(p) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const selectedShapeStyle = p.selectedElement && hasShapeProperties(p.selectedElement) ? p.selectedElement.shapeStyle : void 0;
  const [shapesOpen, setShapesOpen] = (0, import_react2.useState)(false);
  const [arrangeOpen, setArrangeOpen] = (0, import_react2.useState)(false);
  const [fillOpen, setFillOpen] = (0, import_react2.useState)(false);
  const [outlineOpen, setOutlineOpen] = (0, import_react2.useState)(false);
  const shapesRef = (0, import_react2.useRef)(null);
  const arrangeRef = (0, import_react2.useRef)(null);
  const fillRef = (0, import_react2.useRef)(null);
  const outlineRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    if (!shapesOpen) {
      return;
    }
    const handler = (e) => {
      if (shapesRef.current && !shapesRef.current.contains(e.target)) {
        setShapesOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [shapesOpen]);
  (0, import_react2.useEffect)(() => {
    if (!arrangeOpen) {
      return;
    }
    const handler = (e) => {
      if (arrangeRef.current && !arrangeRef.current.contains(e.target)) {
        setArrangeOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [arrangeOpen]);
  (0, import_react2.useEffect)(() => {
    if (!fillOpen) {
      return;
    }
    const handler = (e) => {
      if (fillRef.current && !fillRef.current.contains(e.target)) {
        setFillOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [fillOpen]);
  (0, import_react2.useEffect)(() => {
    if (!outlineOpen) {
      return;
    }
    const handler = (e) => {
      if (outlineRef.current && !outlineRef.current.contains(e.target)) {
        setOutlineOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [outlineOpen]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: shapesRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled: !p.canEdit,
              className: pill,
              title: t("pptx.drawing.shapes"),
              onClick: () => setShapesOpen((v) => !v),
              children: [
                (0, import_jsx_runtime.jsx)(LuShapes, { className: ic }),
                t("pptx.drawing.shapes")
              ]
            }
          ),
          shapesOpen && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: shapesRef, className: "flex flex-col w-52 pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 max-h-60 overflow-y-auto", children: TOP_SHAPES.map((s) => (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: cn(
                "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                p.newShapeType === s.type && "bg-accent"
              ),
              onClick: () => {
                p.onSetNewShapeType(s.type);
                p.onAddShape();
                setShapesOpen(false);
              },
              children: [
                s.icon,
                t(s.i18nKey)
              ]
            },
            s.type
          )) }) })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: arrangeRef, children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              disabled: !p.canEdit || !p.selectedElement,
              className: pill,
              title: t("pptx.ribbon.arrange"),
              onClick: () => setArrangeOpen((v) => !v),
              children: [
                (0, import_jsx_runtime.jsx)(LuLayers, { className: ic }),
                t("pptx.ribbon.arrange")
              ]
            }
          ),
          arrangeOpen && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: arrangeRef, className: "flex flex-col w-44 pt-1", children: (0, import_jsx_runtime.jsxs)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayer("forward");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.bringForward")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayer("backward");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.sendBackward")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayerToEdge("front");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.bringToFront")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "flex items-center w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
                onClick: () => {
                  p.onMoveLayerToEdge("back");
                  setArrangeOpen(false);
                },
                children: t("pptx.contextMenu.sendToBack")
              }
            )
          ] }) })
        ] }),
        (0, import_jsx_runtime.jsx)(
          ShapeColorPopover,
          {
            icon: (0, import_jsx_runtime.jsx)(LuPaintBucket, { className: ic }),
            title: t("pptx.drawing.shapeFill"),
            prefix: "shape-fill",
            anchorRef: fillRef,
            open: fillOpen,
            onToggle: () => setFillOpen((v) => !v),
            disabled: !p.canEdit || !p.selectedElement,
            swatchAriaLabel: "Fill colour",
            selectedRef: selectedShapeStyle == null ? void 0 : selectedShapeStyle.fillColorRef,
            selectedHex: selectedShapeStyle == null ? void 0 : selectedShapeStyle.fillColor,
            onApply: (c, ref) => {
              var _a;
              (_a = p.onUpdateElementStyle) == null ? void 0 : _a.call(p, shapeFillChange(c, ref));
              pushColor(c);
            },
            onClose: () => setFillOpen(false)
          }
        ),
        (0, import_jsx_runtime.jsx)(
          ShapeColorPopover,
          {
            icon: (0, import_jsx_runtime.jsx)(LuPenLine, { className: ic }),
            title: t("pptx.drawing.shapeOutline"),
            prefix: "shape-outline",
            anchorRef: outlineRef,
            open: outlineOpen,
            onToggle: () => setOutlineOpen((v) => !v),
            disabled: !p.canEdit || !p.selectedElement,
            swatchAriaLabel: "Outline colour",
            selectedRef: selectedShapeStyle == null ? void 0 : selectedShapeStyle.strokeColorRef,
            selectedHex: selectedShapeStyle == null ? void 0 : selectedShapeStyle.strokeColor,
            onApply: (c, ref) => {
              var _a;
              (_a = p.onUpdateElementStyle) == null ? void 0 : _a.call(p, shapeOutlineChange(c, ref));
              pushColor(c);
            },
            onClose: () => setOutlineOpen(false)
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: true,
            className: pill,
            title: t("pptx.drawing.shapeEffectsUnavailable"),
            children: (0, import_jsx_runtime.jsx)(LuSparkles, { className: ic })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Drawing" })
    ] }),
    sep
  ] });
}
function EditingSection(p) {
  const { t } = useTranslation();
  const [selectMenuOpen, setSelectMenuOpen] = (0, import_react2.useState)(false);
  const menuRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    if (!selectMenuOpen) {
      return;
    }
    const handler = (e) => {
      if (menuRef.current && !menuRef.current.contains(e.target)) {
        setSelectMenuOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [selectMenuOpen]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    sep,
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: grp, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onMouseDown: (e) => e.preventDefault(),
              onClick: p.onToggleFindReplace,
              className: gB,
              title: t("pptx.editing.find"),
              children: (0, import_jsx_runtime.jsx)(LuSearch, { className: ic })
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onMouseDown: (e) => e.preventDefault(),
              onClick: p.onToggleFindReplace,
              className: gL,
              title: t("pptx.ribbon.replace"),
              children: (0, import_jsx_runtime.jsx)(LuReplace, { className: ic })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative", ref: menuRef, children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => setSelectMenuOpen((v) => !v),
              className: pill,
              title: t("pptx.ribbon.tool.select"),
              children: (0, import_jsx_runtime.jsx)(LuMousePointer2, { className: ic })
            }
          ),
          selectMenuOpen && (0, import_jsx_runtime.jsx)(RibbonMenu, { anchorRef: menuRef, className: "pt-1", children: (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1 w-32", children: (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "flex items-center w-full px-3 py-1.5 text-xs hover:bg-muted transition-colors",
              onMouseDown: (e) => e.preventDefault(),
              onClick: () => {
                var _a;
                (_a = p.onSelectAll) == null ? void 0 : _a.call(p);
                setSelectMenuOpen(false);
              },
              children: t("pptx.editing.selectAll")
            }
          ) }) })
        ] })
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground leading-none", children: "Editing" })
    ] })
  ] });
}
function HelpSection(p) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onOpenSettings ?? p.onToggleShortcuts,
        className: pill,
        title: t("pptx.settings.title"),
        children: t("pptx.settings.title")
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onToggleShortcuts,
        className: pill,
        title: t("pptx.settings.keyboardShortcuts"),
        children: t("pptx.settings.keyboardShortcuts")
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onRunAccessibilityCheck,
        className: pill,
        title: t("pptx.ribbon.accessibilityCheck"),
        children: t("pptx.ribbon.accessibilityCheck")
      }
    )
  ] });
}
function RecordSection({
  onRecordFromBeginning,
  onRecordFromCurrent
}) {
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(RibbonGroup, { label: "Camera", children: (0, import_jsx_runtime.jsx)(RibbonCommand, { icon: (0, import_jsx_runtime.jsx)(LuCamera, {}), label: "Cameo", disabled: true }) }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: "Record", children: [
      (0, import_jsx_runtime.jsx)(RibbonCommand, { icon: (0, import_jsx_runtime.jsx)(LuVideo, {}), label: "From Beginning", onClick: onRecordFromBeginning }),
      (0, import_jsx_runtime.jsx)(RibbonCommand, { icon: (0, import_jsx_runtime.jsx)(LuPlay, {}), label: "From Current Slide", onClick: onRecordFromCurrent })
    ] }),
    (0, import_jsx_runtime.jsxs)(RibbonGroup, { label: "Manage", children: [
      (0, import_jsx_runtime.jsx)(RibbonCommand, { icon: (0, import_jsx_runtime.jsx)(LuEraser, {}), label: "Clear", disabled: true }),
      (0, import_jsx_runtime.jsx)(RibbonCommand, { icon: (0, import_jsx_runtime.jsx)(LuRotateCcw, {}), label: "Reset to Cameo", disabled: true })
    ] }),
    (0, import_jsx_runtime.jsx)(RibbonGroup, { label: "Help", children: (0, import_jsx_runtime.jsx)(RibbonCommand, { icon: (0, import_jsx_runtime.jsx)(LuCircleHelp, {}), label: "Learn More", disabled: true }) })
  ] });
}
function TabRowActions(p) {
  const { t } = useTranslation();
  const collab = useCollaboration();
  const isCollaborating = collab && collab.status === "connected";
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 pr-1", children: [
    p.onEnterRehearsalMode && !isHidden("record") && (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: p.onEnterRehearsalMode,
        className: TAB_ROW_ACTION_CLASSES.record,
        title: t("pptx.titleBar.record"),
        "aria-label": t("pptx.titleBar.record"),
        children: [
          (0, import_jsx_runtime.jsx)("span", { className: TAB_ROW_ACTION_CLASSES.recordDot, "aria-hidden": "true" }),
          (0, import_jsx_runtime.jsx)("span", { children: t("pptx.titleBar.record") })
        ]
      }
    ),
    !isHidden("share") && (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: p.onOpenShareDialog,
        className: cn(
          "relative inline-flex items-center gap-1 px-2.5 py-1 rounded-sm text-[11px] font-medium transition-colors whitespace-nowrap",
          isCollaborating ? "bg-green-600 hover:bg-green-500 text-white" : "bg-primary hover:bg-primary/90 text-white"
        ),
        title: isCollaborating ? t("pptx.toolbar.sharingUsers", { count: collab.connectedCount }) : t("pptx.toolbar.share"),
        "aria-label": t("pptx.toolbar.share"),
        children: [
          (0, import_jsx_runtime.jsx)(LuShare2, { className: "w-3 h-3" }),
          (0, import_jsx_runtime.jsx)("span", { children: isCollaborating ? t("pptx.toolbar.sharingCount", { count: collab.connectedCount }) : t("pptx.toolbar.share") })
        ]
      }
    )
  ] });
}
var QUICK_ACCESS_ICONS = {
  save: LuSave,
  undo: LuUndo,
  redo: LuRedo,
  play: LuPlay,
  printer: LuPrinter,
  fileDown: LuFileDown,
  plus: LuPlus,
  spellCheck: LuSpellCheck,
  zoomIn: LuZoomIn,
  zoomOut: LuZoomOut
};
function TitleBarQuickExtras(p) {
  const { t } = useTranslation();
  if (!p.quickAccess.visible) {
    return null;
  }
  const commands = extraQuickAccessCommands(p.quickAccess.commandIds);
  if (commands.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: commands.map((command) => {
    const Icon = QUICK_ACCESS_ICONS[command.icon] ?? LuPlay;
    const label = t(command.labelKey);
    return (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: () => {
          var _a;
          return (_a = p.onCommand) == null ? void 0 : _a.call(p, command.id);
        },
        className: TITLE_BAR_CLASSES.quickButton,
        title: label,
        "aria-label": label,
        children: (0, import_jsx_runtime.jsx)(Icon, { className: "w-3.5 h-3.5" })
      },
      command.id
    );
  }) });
}
function PresentDropdown({
  isActive,
  onPresent,
  onPresenterView,
  onRehearse,
  onSetUpSlideShow,
  onBroadcast,
  onToggleSubtitles,
  showSubtitles
}) {
  const { t } = useTranslation();
  const [open, setOpen] = (0, import_react2.useState)(false);
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "inline-flex items-stretch rounded border border-border bg-muted text-xs overflow-hidden", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onPresent,
          className: cn(
            "px-2 py-1 transition-colors",
            isActive ? "bg-primary text-white" : "hover:bg-accent text-foreground"
          ),
          title: t("pptx.present.presentTooltip"),
          children: t("pptx.toolbar.present")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: () => setOpen((prev) => !prev),
          className: cn(
            "inline-flex items-center px-1 transition-colors border-l border-border",
            open ? "bg-primary text-white" : "hover:bg-accent text-foreground"
          ),
          title: t("pptx.present.optionsTooltip"),
          "aria-label": t("pptx.present.optionsTooltip"),
          children: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3" })
        }
      )
    ] }),
    open && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "fixed inset-0 z-40",
          "aria-label": t("pptx.overflow.closeMenu"),
          onClick: () => setOpen(false)
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: "absolute right-0 top-full mt-1 z-50 w-52 rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: [
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onPresent();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              (0, import_jsx_runtime.jsx)(LuPlay, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.toolbar.present")
            ]
          }
        ),
        onPresenterView && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onPresenterView();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              (0, import_jsx_runtime.jsx)(LuMonitor, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.slideShow.presenterView")
            ]
          }
        ),
        onRehearse && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onRehearse();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              (0, import_jsx_runtime.jsx)(LuClock, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.slideShow.rehearseTimings")
            ]
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border/60" }),
        onSetUpSlideShow && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onSetUpSlideShow();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              (0, import_jsx_runtime.jsx)(LuSettings, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.slideShow.setUp")
            ]
          }
        ),
        onBroadcast && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onBroadcast();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              (0, import_jsx_runtime.jsx)(LuRadio, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              t("pptx.present.presentOnline")
            ]
          }
        ),
        onToggleSubtitles && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => {
              setOpen(false);
              onToggleSubtitles();
            },
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              (0, import_jsx_runtime.jsx)(LuCaptions, { className: "w-3.5 h-3.5 text-muted-foreground" }),
              (0, import_jsx_runtime.jsx)("span", { className: "flex-1 text-left", children: "Subtitles" }),
              showSubtitles && (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3 h-3 text-primary" })
            ]
          }
        )
      ] })
    ] })
  ] });
}
function ModeSwitcher({
  mode,
  onSetMode,
  onCloseMasterView,
  onEnterPresenterView,
  onEnterRehearsalMode,
  onOpenSetUpSlideShow,
  onOpenBroadcastDialog,
  onToggleSubtitles,
  showSubtitles
}) {
  const { t } = useTranslation();
  if (mode === "master") {
    return (0, import_jsx_runtime.jsxs)("div", { className: "inline-flex items-center gap-1.5", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "inline-flex items-center px-2 py-0.5 rounded-sm bg-amber-600/90 text-[10px] text-amber-50", children: t("pptx.mode.masterView") }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onCloseMasterView,
          className: "px-2 py-0.5 rounded-sm hover:bg-accent text-[10px] text-foreground transition-colors",
          title: t("pptx.mode.closeMasterViewTooltip"),
          children: t("pptx.common.close")
        }
      )
    ] });
  }
  return (0, import_jsx_runtime.jsx)(
    PresentDropdown,
    {
      isActive: mode === "present",
      onPresent: () => onSetMode("present"),
      onPresenterView: onEnterPresenterView,
      onRehearse: onEnterRehearsalMode,
      onSetUpSlideShow: onOpenSetUpSlideShow,
      onBroadcast: onOpenBroadcastDialog,
      onToggleSubtitles,
      showSubtitles
    }
  );
}
function OverflowMenu(p) {
  const { t } = useTranslation();
  const ovAct = (k) => {
    var _a, _b2;
    p.onSetOverflowMenuOpen(false);
    (_b2 = (_a = {
      png: p.onExportPng,
      pdf: p.onExportPdf,
      video: p.onExportVideo,
      gif: p.onExportGif,
      pptx: p.onSaveAsPptx,
      ppsx: p.onSaveAsPpsx,
      pptm: p.onSaveAsPptm,
      print: p.onPrint,
      copyImg: p.onCopySlideAsImage,
      a11y: p.onRunAccessibilityCheck,
      shortcuts: p.onToggleShortcuts,
      versionHistory: p.onToggleVersionHistory,
      documentProperties: p.onOpenDocumentProperties,
      passwordProtection: p.onOpenPasswordProtection,
      fontEmbedding: p.onOpenFontEmbedding,
      digitalSignatures: p.onOpenDigitalSignatures
    })[k]) == null ? void 0 : _b2.call(_a);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: () => p.onSetOverflowMenuOpen(!p.isOverflowMenuOpen),
        className: cn(
          "p-1.5 rounded transition-colors",
          p.isOverflowMenuOpen ? "bg-primary/80 text-white" : "bg-muted hover:bg-accent"
        ),
        title: t("pptx.ribbon.moreActions"),
        "aria-label": t("pptx.ribbon.moreActions"),
        children: (0, import_jsx_runtime.jsx)(LuEllipsis, { className: ic })
      }
    ),
    p.isOverflowMenuOpen && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "fixed inset-0 z-40",
          "aria-label": t("pptx.overflow.closeMenu"),
          onClick: () => p.onSetOverflowMenuOpen(false)
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "absolute right-0 top-full mt-1 z-50 w-44 rounded-lg border border-border bg-popover backdrop-blur-lg shadow-2xl py-1", children: OV.filter((o) => o.k !== "pptm" || p.hasMacros).map(
        (o) => o.k.startsWith("---") ? (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border/60" }, o.k) : (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: () => ovAct(o.k),
            className: "flex items-center gap-2 w-full px-3 py-1.5 text-xs text-foreground hover:bg-muted transition-colors",
            children: [
              o.i,
              t(o.labelKey)
            ]
          },
          o.k
        )
      ) })
    ] })
  ] });
}
function ToolbarPrimaryRow(p) {
  const { t } = useTranslation();
  const {
    mode,
    canEdit,
    isSidebarCollapsed,
    isInspectorPaneOpen,
    onToggleSidebar,
    onToggleInspector
  } = p;
  const collab = useCollaboration();
  const qab = "p-1 max-md:p-2 max-md:min-h-[40px] max-md:min-w-[40px] rounded-sm transition-colors hover:bg-accent/60 disabled:opacity-40 disabled:cursor-not-allowed active:scale-90 active:opacity-70";
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-0.5 max-md:gap-0 px-1.5 py-0.5 max-md:px-1", children: [
    mode !== "present" && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onToggleSidebar,
        className: cn(qab, !isSidebarCollapsed ? "text-foreground" : "text-muted-foreground"),
        title: t("pptx.toolbar.toggleSlidesPanel"),
        "aria-label": t("pptx.toolbar.toggleSlidesPanel"),
        children: (0, import_jsx_runtime.jsx)(LuPanelLeft, { className: ic })
      }
    ),
    (0, import_jsx_runtime.jsx)("div", { className: "flex-1 min-w-2 max-md:min-w-1" }),
    (mode === "edit" || mode === "master") && (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: p.onToggleComments,
        className: cn(
          qab,
          "relative max-md:hidden",
          p.isCommentsPanelOpen ? "text-foreground" : "text-muted-foreground"
        ),
        title: t("pptx.toolbar.comments"),
        "aria-label": t("pptx.toolbar.comments"),
        children: [
          (0, import_jsx_runtime.jsx)(LuMessageSquare, { className: ics }),
          (p.slideCommentCount ?? 0) > 0 && (0, import_jsx_runtime.jsx)("span", { className: "absolute -top-0.5 -right-0.5 flex items-center justify-center w-3.5 h-3.5 rounded-full bg-primary text-[8px] text-white leading-none", children: p.slideCommentCount })
        ]
      }
    ),
    collab && (collab.status === "connected" || collab.status === "connecting") && collab.remoteUsers.length > 0 && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onOpenShareDialog,
        className: "mx-1 rounded-sm px-1 py-0.5 hover:bg-accent/60 transition-colors cursor-pointer",
        title: t("pptx.toolbar.sharingUsers", { count: collab.connectedCount }),
        children: (0, import_jsx_runtime.jsx)(
          UserAvatarBar,
          {
            remoteUsers: collab.remoteUsers,
            localUserName: collab.config.userName,
            localUserColor: collab.config.userColor ?? "#6366f1",
            localUserAvatar: collab.config.userAvatar,
            status: collab.status
          }
        )
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ModeSwitcher,
      {
        mode: p.mode,
        onSetMode: p.onSetMode,
        onCloseMasterView: p.onCloseMasterView,
        onToggleSlideSorter: p.onToggleSlideSorter,
        onEnterPresenterView: p.onEnterPresenterView,
        onEnterRehearsalMode: p.onEnterRehearsalMode,
        onOpenSetUpSlideShow: p.onOpenSetUpSlideShow,
        onOpenBroadcastDialog: p.onOpenBroadcastDialog,
        onToggleSubtitles: p.onToggleSubtitles,
        showSubtitles: p.showSubtitles
      }
    ),
    (0, import_jsx_runtime.jsx)(
      CustomShowsControls,
      {
        customShows: p.customShows,
        activeCustomShowId: p.activeCustomShowId,
        canEdit: p.canEdit,
        isCurrentSlideInActiveShow: p.isCurrentSlideInActiveShow,
        onSetActiveCustomShowId: p.onSetActiveCustomShowId,
        onCreateCustomShow: p.onCreateCustomShow,
        onRenameActiveCustomShow: p.onRenameActiveCustomShow,
        onDeleteActiveCustomShow: p.onDeleteActiveCustomShow,
        onToggleCurrentSlideInActiveShow: p.onToggleCurrentSlideInActiveShow
      }
    ),
    sep,
    (mode === "edit" || mode === "master") && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onToggleInspector,
        className: cn(qab, isInspectorPaneOpen ? "text-foreground" : "text-muted-foreground"),
        title: t("pptx.toolbar.toggleInspector"),
        "aria-label": t("pptx.toolbar.toggleInspector"),
        children: (0, import_jsx_runtime.jsx)(LuPanelRight, { className: ic })
      }
    ),
    p.aiEnabled && (mode === "edit" || mode === "master") && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onToggleAiPanel,
        className: cn(qab, p.isAiPanelOpen ? "text-primary" : "text-muted-foreground"),
        title: t("pptx.toolbar.toggleAiAssistant"),
        "aria-label": t("pptx.toolbar.toggleAiAssistant"),
        children: (0, import_jsx_runtime.jsx)(LuSparkles, { className: ic })
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onOpenSettings ?? p.onToggleShortcuts,
        className: cn(qab, "text-muted-foreground"),
        title: t("pptx.toolbar.settingsShortcuts"),
        "aria-label": t("pptx.toolbar.settings"),
        children: (0, import_jsx_runtime.jsx)(LuSettings, { className: ics })
      }
    ),
    !canEdit && p.isProtectedView && p.onEnableEditing ? (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: p.onEnableEditing,
        className: "inline-flex items-center px-2 py-0.5 rounded-sm bg-amber-600/90 text-[10px] text-amber-50 transition-colors hover:bg-amber-600",
        title: t("pptx.options.trust.protectedViewInfo"),
        children: t("pptx.security.enableEditing")
      }
    ) : !canEdit && (0, import_jsx_runtime.jsx)("span", { className: "inline-flex items-center px-2 py-0.5 rounded-sm bg-amber-600/90 text-[10px] text-amber-50", children: t("pptx.toolbar.readOnly") }),
    (0, import_jsx_runtime.jsx)(OverflowMenu, { ...p })
  ] });
}
function Toolbar(p) {
  const { mode, isNarrowViewport, isCompactToolbarOpen, toolbarSection, onSetToolbarSection } = p;
  const { t } = useTranslation();
  const { isTabVisible } = useToolbarVisibility(p.hiddenActions);
  const viewerOptions = useViewerOptionsContext();
  if (isNarrowViewport && mode !== "present") {
    return (0, import_jsx_runtime.jsx)(MobileToolbar, { ...p });
  }
  const sFil = toolbarSection === "file";
  const sHome = toolbarSection === "home";
  const sIns = toolbarSection === "insert";
  const sTxt = sHome || toolbarSection === "text";
  const sArr = sHome || toolbarSection === "arrange";
  const sDrw = toolbarSection === "draw";
  const sDes = toolbarSection === "design";
  const sTrn = toolbarSection === "transitions";
  const sAni = toolbarSection === "animations";
  const sSlw = toolbarSection === "slideShow";
  const sRec = toolbarSection === "record";
  const sRev = toolbarSection === "review";
  const sViw = toolbarSection === "view";
  const sHlp = toolbarSection === "help";
  const showRibbon = mode === "edit" || mode === "master";
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      role: "toolbar",
      "aria-label": t("pptx.toolbar.presentationToolbarAria"),
      className: "relative z-20 border-b border-border bg-secondary/50 overflow-visible",
      children: [
        (0, import_jsx_runtime.jsx)(ToolbarPrimaryRow, { ...p }),
        showRibbon && (0, import_jsx_runtime.jsxs)(
          "div",
          {
            role: "tablist",
            className: "flex items-center border-b border-border/60 px-1 max-md:overflow-x-auto max-md:scrollbar-none",
            children: [
              TOOLBAR_SECTIONS.filter((s) => isTabVisible(s.id)).map((s) => (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  role: "tab",
                  "aria-selected": toolbarSection === s.id,
                  title: resolveScreenTip(viewerOptions, t(s.labelKey)),
                  onClick: () => onSetToolbarSection(s.id),
                  className: cn(
                    "relative px-3.5 py-2 text-[12px] font-medium whitespace-nowrap transition-colors max-md:min-h-[36px] max-md:px-3",
                    toolbarSection === s.id ? s.id === "file" ? "text-white bg-primary/80 rounded-sm" : "text-foreground after:absolute after:-bottom-px after:left-0 after:right-0 after:h-[2.5px] after:bg-primary" : s.id === "file" ? "text-primary hover:bg-primary/15 rounded-sm" : "text-muted-foreground hover:text-foreground hover:bg-accent/30"
                  ),
                  children: t(s.labelKey)
                },
                s.id
              )),
              (0, import_jsx_runtime.jsx)("div", { className: "flex-1" }),
              (0, import_jsx_runtime.jsx)(
                TabRowActions,
                {
                  onEnterRehearsalMode: p.canEdit ? p.onEnterRehearsalMode : void 0,
                  onOpenShareDialog: p.onOpenShareDialog,
                  hiddenActions: p.hiddenActions
                }
              ),
              isNarrowViewport && (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  onClick: p.onToggleCompactToolbar,
                  className: cn(
                    "px-2 py-1 rounded text-[11px] transition-colors mr-1",
                    isCompactToolbarOpen ? "bg-primary/80 text-white" : "text-muted-foreground hover:text-foreground"
                  ),
                  title: t(
                    isCompactToolbarOpen ? "pptx.ribbon.collapseRibbon" : "pptx.ribbon.expandRibbon"
                  ),
                  children: t(isCompactToolbarOpen ? "pptx.ribbon.collapseRibbon" : "pptx.ribbon.expandRibbon")
                }
              )
            ]
          }
        ),
        showRibbon && viewerOptions.quickAccess.position === "below" && (0, import_jsx_runtime.jsx)("div", { className: "flex items-center gap-0.5 border-b border-border/60 px-2 py-1", children: (0, import_jsx_runtime.jsx)(
          TitleBarQuickExtras,
          {
            quickAccess: viewerOptions.quickAccess,
            onCommand: p.onQuickCommand
          }
        ) }),
        showRibbon && (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: cn(
              /*
               * `items-stretch` (not `items-center`): `RibbonGroup` pins its
               * caption to `bottom-0.5` of ITS OWN box (`position: relative`), so
               * a group left at its un-stretched intrinsic height sits centered in
               * the row with its caption floating well above the row's true bottom
               * edge whenever a taller sibling sets the row's height (e.g. View's
               * five-row "Show" group next to its two-row "Zoom" group). Stretching
               * every direct child to the row's full height lines up every group's
               * caption and right-hand divider at the same baseline. File's
               * backstage is unaffected: it renders `position: fixed`, outside this
               * row's flex layout entirely.
               */
              "flex min-h-[82px] items-stretch gap-0 overflow-x-auto px-1 py-0.5 max-md:min-h-0 max-md:px-1 max-md:py-0.5 flex-nowrap [&>*]:shrink-0",
              isNarrowViewport && !isCompactToolbarOpen && "hidden"
            ),
            children: [
              sFil && (0, import_jsx_runtime.jsx)(
                FileSection,
                {
                  fileName: p.fileName,
                  onClose: () => p.onSetToolbarSection("home"),
                  onCreatePresentation: p.onCreatePresentation,
                  onOpenFile: p.onOpenFile,
                  onOpenRecentFile: p.onOpenRecentFile,
                  onExportPng: p.onExportPng,
                  onExportPdf: p.onExportPdf,
                  onExportVideo: p.onExportVideo,
                  onExportGif: p.onExportGif,
                  onExportJson: p.onExportJson,
                  onSaveAsPptx: p.onSaveAsPptx,
                  onSaveAsPpsx: p.onSaveAsPpsx,
                  onSaveAsPptm: p.onSaveAsPptm,
                  hasMacros: p.hasMacros,
                  onCopySlideAsImage: p.onCopySlideAsImage,
                  onPrint: p.onPrint,
                  onOpenSettings: p.onOpenSettings,
                  onOpenShareDialog: p.onOpenShareDialog,
                  onOpenDocumentProperties: p.onOpenDocumentProperties,
                  onOpenPasswordProtection: p.onOpenPasswordProtection,
                  onOpenFontEmbedding: p.onOpenFontEmbedding,
                  onOpenDigitalSignatures: p.onOpenDigitalSignatures,
                  onOpenVersionHistory: p.onToggleVersionHistory,
                  hiddenActions: p.hiddenActions,
                  recentPresentationsCount: p.recentPresentationsCount
                }
              ),
              sHome && (0, import_jsx_runtime.jsx)(
                HomeSection,
                {
                  canEdit: p.canEdit,
                  clipboardPayload: p.clipboardPayload,
                  formatPainterActive: p.formatPainterActive,
                  canActivateFormatPainter: p.canActivateFormatPainter,
                  onCopy: p.onCopy,
                  onCut: p.onCut,
                  onPaste: p.onPaste,
                  onToggleFormatPainter: p.onToggleFormatPainter,
                  layoutOptions: p.layoutOptions,
                  currentLayoutPath: p.currentLayoutPath,
                  loadLayoutPreviews: p.loadLayoutPreviews,
                  themeFonts: p.themeFonts,
                  embeddedFontFamilies: p.embeddedFontFamilies,
                  customFontFamilies: p.customFontFamilies,
                  onInsertSlideFromLayout: p.onInsertSlideFromLayout,
                  onApplyLayout: p.onApplyLayout,
                  onInsertSlideFromTemplate: p.onInsertSlideFromTemplate,
                  templateScheme: p.templateScheme,
                  selectedElement: p.selectedElement,
                  onUpdateTextStyle: p.onUpdateTextStyle,
                  onResetSlide: p.onResetSlide,
                  onAddSection: p.onAddSection
                }
              ),
              sIns && (0, import_jsx_runtime.jsx)(
                InsertSection,
                {
                  canEdit: p.canEdit,
                  newShapeType: p.newShapeType,
                  onSetNewShapeType: p.onSetNewShapeType,
                  onAddTextBox: p.onAddTextBox,
                  onAddShape: p.onAddShape,
                  onAddTable: p.onAddTable,
                  onAddChart: p.onAddChart,
                  onAddSmartArt: p.onAddSmartArt,
                  onAddEquation: p.onAddEquation,
                  onAddActionButton: p.onAddActionButton,
                  onInsertField: p.onInsertField,
                  onOpenHeaderFooter: p.onOpenHeaderFooter,
                  onOpenImagePicker: p.onOpenImagePicker,
                  onOpenMediaPicker: p.onOpenMediaPicker,
                  hasSelection: Boolean(p.selectedElement),
                  onOpenHyperlinkDialog: p.onOpenHyperlinkDialog
                }
              ),
              sTxt && (0, import_jsx_runtime.jsx)(
                TextSection,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  tableEditorState: p.tableEditorState,
                  onUpdateTextStyle: p.onUpdateTextStyle,
                  onTransformTextCase: p.onTransformTextCase
                }
              ),
              sHome && (0, import_jsx_runtime.jsx)(
                EditingSection,
                {
                  onToggleFindReplace: p.onToggleFindReplace,
                  onSelectAll: p.onSelectAll
                }
              ),
              sHome && (0, import_jsx_runtime.jsx)(
                DrawingGroup,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  newShapeType: p.newShapeType,
                  onSetNewShapeType: p.onSetNewShapeType,
                  onAddShape: p.onAddShape,
                  onMoveLayer: p.onMoveLayer,
                  onMoveLayerToEdge: p.onMoveLayerToEdge,
                  onUpdateElementStyle: p.onUpdateElementStyle
                }
              ),
              sDrw && (0, import_jsx_runtime.jsx)(
                DrawSection,
                {
                  activeTool: p.activeTool,
                  drawingColor: p.drawingColor,
                  drawingWidth: p.drawingWidth,
                  onSetActiveTool: p.onSetActiveTool,
                  onSetDrawingColor: p.onSetDrawingColor,
                  onSetDrawingWidth: p.onSetDrawingWidth
                }
              ),
              sArr && (0, import_jsx_runtime.jsx)(
                ArrangeSection,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  selectedCount: p.selectedCount,
                  selectionGroupable: p.selectionGroupable,
                  onAlignElements: p.onAlignElements,
                  onDistributeElements: p.onDistributeElements,
                  canDistribute: p.canDistribute,
                  onFlip: p.onFlip,
                  onMoveLayer: p.onMoveLayer,
                  onMoveLayerToEdge: p.onMoveLayerToEdge,
                  onGroupElements: p.onGroupElements,
                  onUngroupElement: p.onUngroupElement,
                  onUpdateElementStyle: p.onUpdateElementStyle,
                  onDuplicate: p.onDuplicate,
                  onDelete: p.onDelete,
                  formatPainterActive: p.formatPainterActive,
                  onToggleFormatPainter: p.onToggleFormatPainter,
                  canActivateFormatPainter: p.canActivateFormatPainter
                }
              ),
              sDes && (0, import_jsx_runtime.jsx)(
                DesignSection,
                {
                  canEdit: p.canEdit,
                  onToggleThemeGallery: p.onToggleThemeGallery,
                  isThemeGalleryOpen: p.isThemeGalleryOpen,
                  onToggleThemeEditor: p.onToggleThemeEditor,
                  isThemeEditorOpen: p.isThemeEditorOpen,
                  onOpenDocumentProperties: p.onOpenDocumentProperties,
                  onOpenSlideSize: p.onOpenSlideSize,
                  onToggleInspector: p.onToggleInspector,
                  isInspectorPaneOpen: p.isInspectorPaneOpen
                }
              ),
              sTrn && (0, import_jsx_runtime.jsx)(
                TransitionsSection,
                {
                  isInspectorPaneOpen: p.isInspectorPaneOpen,
                  onToggleInspector: p.onToggleInspector,
                  canEdit: p.canEdit,
                  activeSlide: p.activeSlide,
                  onTransitionChange: p.onTransitionChange,
                  onApplyTransitionToAll: p.onApplyTransitionToAll
                }
              ),
              sAni && (0, import_jsx_runtime.jsx)(
                AnimationsSection,
                {
                  canEdit: p.canEdit,
                  selectedElement: p.selectedElement,
                  isInspectorPaneOpen: p.isInspectorPaneOpen,
                  onToggleInspector: p.onToggleInspector,
                  onOpenAnimationPanel: p.onOpenAnimationPanel,
                  onAddAnimation: p.onAddAnimation,
                  onRemoveAnimation: p.onRemoveAnimation
                }
              ),
              sSlw && (0, import_jsx_runtime.jsx)(
                SlideShowSection,
                {
                  onPresent: () => p.onSetMode("present"),
                  onPresentFromBeginning: p.onPresentFromBeginning,
                  onEnterPresenterView: p.onEnterPresenterView ?? (() => {
                  }),
                  onEnterRehearsalMode: p.onEnterRehearsalMode ?? (() => {
                  }),
                  onOpenSetUpSlideShow: p.onOpenSetUpSlideShow ?? (() => {
                  }),
                  onToggleHideSlide: p.onToggleHideSlide ?? (() => {
                  }),
                  activeSlideHidden: p.activeSlideHidden ?? false,
                  onOpenBroadcastDialog: p.onOpenBroadcastDialog ?? (() => {
                  }),
                  onToggleSubtitles: p.onToggleSubtitles ?? (() => {
                  }),
                  showSubtitles: p.showSubtitles ?? false,
                  onSetMode: p.onSetMode,
                  customShowControls: p,
                  hiddenActions: p.hiddenActions,
                  presentationProperties: p.presentationProperties,
                  onPresentationPropertiesChange: p.onPresentationPropertiesChange
                }
              ),
              sRec && (0, import_jsx_runtime.jsx)(
                RecordSection,
                {
                  onRecordFromBeginning: p.onEnterRehearsalMode ?? (() => {
                  }),
                  onRecordFromCurrent: p.onEnterRehearsalMode ?? (() => {
                  })
                }
              ),
              sRev && (0, import_jsx_runtime.jsx)(
                ReviewSection,
                {
                  canEdit: p.canEdit,
                  spellCheckEnabled: p.spellCheckEnabled,
                  onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
                  onToggleComments: p.onToggleComments,
                  isCommentsPanelOpen: p.isCommentsPanelOpen,
                  slideCommentCount: p.slideCommentCount,
                  onCompare: p.onCompare,
                  onOpenAccessibilityCheck: p.onRunAccessibilityCheck,
                  onSetLanguage: p.onOpenSettings
                }
              ),
              sViw && (0, import_jsx_runtime.jsx)(
                ViewSection,
                {
                  canEdit: p.canEdit,
                  editTemplateMode: p.editTemplateMode,
                  onSetEditTemplateMode: p.onSetEditTemplateMode,
                  spellCheckEnabled: p.spellCheckEnabled,
                  onSetSpellCheckEnabled: p.onSetSpellCheckEnabled,
                  showGrid: p.showGrid,
                  showRulers: p.showRulers,
                  showGuides: p.showGuides,
                  snapToGrid: p.snapToGrid,
                  snapToShape: p.snapToShape,
                  onSetShowGrid: p.onSetShowGrid,
                  onSetShowRulers: p.onSetShowRulers,
                  onSetShowGuides: p.onSetShowGuides,
                  onSetSnapToGrid: p.onSetSnapToGrid,
                  onSetSnapToShape: p.onSetSnapToShape,
                  onAddGuide: p.onAddGuide,
                  onEnterMasterView: p.onEnterMasterView,
                  isSelectionPaneOpen: p.isSelectionPaneOpen,
                  onToggleSelectionPane: p.onToggleSelectionPane,
                  eyedropperActive: p.eyedropperActive,
                  onToggleEyedropper: p.onToggleEyedropper,
                  onToggleSlideSorter: p.onToggleSlideSorter,
                  onOpenReadingView: p.onOpenReadingView,
                  onOpenOutlineView: p.onOpenOutlineView,
                  onZoomToFit: p.onZoomToFit
                }
              ),
              sHlp && (0, import_jsx_runtime.jsx)(
                HelpSection,
                {
                  onOpenSettings: p.onOpenSettings,
                  onToggleShortcuts: p.onToggleShortcuts,
                  onRunAccessibilityCheck: p.onRunAccessibilityCheck
                }
              )
            ]
          }
        )
      ]
    }
  );
}
function getReactSlideBackgroundStyle(slide, slideSize) {
  const style = getSlideBackgroundStyle(slide, slideSize);
  return {
    backgroundColor: style["background-color"],
    backgroundImage: (slide == null ? void 0 : slide.backgroundImage) ? void 0 : style["background-image"],
    backgroundSize: style["background-size"],
    backgroundRepeat: style["background-repeat"]
  };
}
function CanvasGuides({ guides, onDeleteGuide, onStartGuideDrag }) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: guides.map((guide) => (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "absolute z-[49] pointer-events-auto",
      style: guide.axis === "h" ? {
        left: 0,
        right: 0,
        top: guide.position,
        height: 1,
        backgroundColor: "rgba(250, 204, 21, 0.9)",
        cursor: "row-resize"
      } : {
        top: 0,
        bottom: 0,
        left: guide.position,
        width: 1,
        backgroundColor: "rgba(250, 204, 21, 0.9)",
        cursor: "col-resize"
      },
      onDoubleClick: (e) => {
        e.stopPropagation();
        onDeleteGuide == null ? void 0 : onDeleteGuide(guide.id);
      },
      onPointerDown: (e) => {
        e.stopPropagation();
        e.currentTarget.setPointerCapture(e.pointerId);
        onStartGuideDrag({
          id: guide.id,
          axis: guide.axis,
          pointerId: e.pointerId
        });
      },
      title: t("pptx.canvas.dragGuideTooltip")
    },
    guide.id
  )) });
}
function MarqueeOverlay({ marqueeSelectionState }) {
  if (!marqueeSelectionState) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "absolute border border-primary bg-primary/10 pointer-events-none z-50",
      style: {
        left: Math.min(marqueeSelectionState.startX, marqueeSelectionState.currentX),
        top: Math.min(marqueeSelectionState.startY, marqueeSelectionState.currentY),
        width: Math.abs(marqueeSelectionState.currentX - marqueeSelectionState.startX),
        height: Math.abs(marqueeSelectionState.currentY - marqueeSelectionState.startY)
      }
    }
  );
}
function SnapLinesOverlay({ snapLines }) {
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: snapLines.map((line, i) => (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "absolute bg-red-500 pointer-events-none z-50",
      style: line.axis === "x" ? {
        left: line.position,
        top: 0,
        width: 1,
        height: "100%"
      } : {
        left: 0,
        top: line.position,
        width: "100%",
        height: 1
      }
    },
    i
  )) });
}
function CommentMarkersOverlay({
  comments,
  canvasSize,
  onCommentMarkerClick
}) {
  const { t } = useTranslation(), half = COMMENT_MARKER_SIZE / 2, markers = buildCommentMarkers(
    comments,
    canvasSize.width,
    canvasSize.height,
    t("pptx.comments.unknownAuthor")
  );
  return (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 pointer-events-none z-[45]", children: markers.map((marker) => (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "absolute pointer-events-auto cursor-pointer",
      style: {
        left: marker.x - half,
        top: marker.y - half,
        width: COMMENT_MARKER_SIZE,
        height: COMMENT_MARKER_SIZE,
        borderRadius: "50%",
        backgroundColor: "rgba(255, 165, 0, 0.9)",
        border: "2px solid #fff",
        boxShadow: "0 1px 3px rgba(0,0,0,0.3)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 10,
        fontWeight: 700,
        color: "#fff",
        lineHeight: 1
      },
      title: marker.title,
      onClick: (e) => {
        e.stopPropagation();
        onCommentMarkerClick == null ? void 0 : onCommentMarkerClick(marker.commentId);
      },
      children: marker.label
    },
    marker.commentId
  )) });
}
function ConnectorEndpointOverlay({
  connector,
  elements,
  editorScale,
  canvasStageRef,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const [drag, setDrag] = (0, import_react2.useState)(null);
  const candidates = (0, import_react2.useMemo)(
    () => collectConnectorSiteCandidates(elements.filter((el) => el.id !== connector.id)),
    [elements, connector.id]
  );
  const handles = getConnectorEndpointHandles(connector);
  const snap = drag ? findConnectorSiteNear(candidates, drag.x, drag.y) : null;
  const toSlidePoint = (0, import_react2.useCallback)(
    (clientX, clientY) => {
      var _a;
      const rect = (_a = canvasStageRef.current) == null ? void 0 : _a.getBoundingClientRect();
      const scale = editorScale || 1;
      return {
        x: (clientX - ((rect == null ? void 0 : rect.left) ?? 0)) / scale,
        y: (clientY - ((rect == null ? void 0 : rect.top) ?? 0)) / scale
      };
    },
    [canvasStageRef, editorScale]
  );
  (0, import_react2.useEffect)(() => {
    if (!drag) {
      return;
    }
    const onMove = (event) => {
      const point2 = toSlidePoint(event.clientX, event.clientY);
      setDrag((prev) => prev ? { ...prev, ...point2 } : prev);
    };
    const onUp = (event) => {
      const point2 = toSlidePoint(event.clientX, event.clientY);
      const target = findConnectorSiteNear(candidates, point2.x, point2.y, CONNECTOR_SITE_SNAP_PX);
      const update = resolveConnectorEndpointUpdate(connector, elements, drag.kind, point2, target);
      const next = withConnectorEndpointUpdate(connector, update);
      onUpdateElement(connector.id, {
        x: next.x,
        y: next.y,
        width: next.width,
        height: next.height,
        flipHorizontal: next.flipHorizontal,
        flipVertical: next.flipVertical,
        // Sent whole: a DETACHED end has had its binding key deleted, so a
        // merge of only the surviving keys would leave the stale one behind.
        shapeStyle: next.shapeStyle
      });
      setDrag(null);
    };
    window.addEventListener("pointermove", onMove);
    window.addEventListener("pointerup", onUp);
    window.addEventListener("pointercancel", onUp);
    return () => {
      window.removeEventListener("pointermove", onMove);
      window.removeEventListener("pointerup", onUp);
      window.removeEventListener("pointercancel", onUp);
    };
  }, [drag, candidates, connector, elements, onUpdateElement, toSlidePoint]);
  return (0, import_jsx_runtime.jsxs)("div", { className: "absolute inset-0 z-[56] pointer-events-none", "data-pptx-connector-endpoints": true, children: [
    drag ? candidates.map((site) => (0, import_jsx_runtime.jsx)(
      "div",
      {
        "aria-hidden": "true",
        "data-pptx-connection-site": true,
        className: "absolute rounded-full border-2 border-blue-500",
        style: {
          left: site.x - 4,
          top: site.y - 4,
          width: 8,
          height: 8,
          background: (snap == null ? void 0 : snap.elementId) === site.elementId && snap.siteIndex === site.siteIndex ? "#3b82f6" : "rgba(96,165,250,0.6)"
        }
      },
      `${site.elementId}-${site.siteIndex}`
    )) : null,
    handles.map((handle) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        "data-pptx-connector-endpoint": handle.kind,
        "data-pptx-connector-attached": String(handle.attached),
        "aria-label": t(
          handle.kind === "start" ? "pptx.canvas.connectorEndpointStart" : "pptx.canvas.connectorEndpointEnd"
        ),
        className: "absolute rounded-full border-2 border-white shadow",
        style: {
          left: ((drag == null ? void 0 : drag.kind) === handle.kind ? drag.x : handle.x) - 5,
          top: ((drag == null ? void 0 : drag.kind) === handle.kind ? drag.y : handle.y) - 5,
          width: 10,
          height: 10,
          // A bound end is filled, a loose one hollow, so "is this
          // connector actually attached?" is answerable at a glance.
          background: handle.attached ? "#16a34a" : "#ffffff",
          boxShadow: "0 0 0 1px #16a34a",
          cursor: "crosshair",
          pointerEvents: "auto",
          touchAction: "none"
        },
        onPointerDown: (event) => {
          event.stopPropagation();
          event.preventDefault();
          const point2 = toSlidePoint(event.clientX, event.clientY);
          setDrag({ kind: handle.kind, ...point2 });
        }
      },
      handle.kind
    ))
  ] });
}
function ConnectorOverlay({
  activeSlide,
  canvasSize,
  zoom,
  connectorDragState,
  onConnectionSiteDown,
  onConnectorDragMove,
  onConnectionSiteDrop,
  onConnectorDragEnd
}) {
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "absolute inset-0 z-[55] pointer-events-none",
      onMouseMove: onConnectorDragMove,
      onMouseUp: onConnectorDragEnd,
      style: { pointerEvents: connectorDragState ? "auto" : "none" },
      children: [
        collectConnectorSiteCandidates(activeSlide.elements).map((site) => (0, import_jsx_runtime.jsx)(
          "div",
          {
            "data-pptx-connection-site": true,
            className: "absolute rounded-full border-2 border-blue-500 bg-blue-400/60 hover:bg-blue-500 hover:scale-125 transition-transform cursor-crosshair",
            style: {
              left: site.x - 5,
              top: site.y - 5,
              width: 10,
              height: 10,
              pointerEvents: "auto",
              zIndex: 56
            },
            onMouseDown: (e) => onConnectionSiteDown(site.elementId, site.siteIndex, e),
            onMouseUp: () => connectorDragState ? onConnectionSiteDrop(site.elementId, site.siteIndex) : void 0
          },
          `${site.elementId}-site-${site.siteIndex}`
        )),
        connectorDragState && (0, import_jsx_runtime.jsx)(
          ConnectorDragPreview,
          {
            activeSlide,
            canvasSize,
            zoom,
            connectorDragState
          }
        )
      ]
    }
  );
}
function ConnectorDragPreview({
  activeSlide,
  canvasSize,
  zoom,
  connectorDragState
}) {
  const startEl = activeSlide.elements.find((el) => el.id === connectorDragState.startElementId);
  if (!startEl) {
    return null;
  }
  const startSites = getShapeConnectionSites(startEl);
  const startSite = startSites[connectorDragState.startSiteIndex] ?? startSites[0];
  const sx = startEl.x + startSite.x;
  const sy = startEl.y + startSite.y;
  const stage = zoom.canvasStageRef.current;
  if (!stage) {
    return null;
  }
  const rect = stage.getBoundingClientRect();
  const scale = zoom.editorScale || 1;
  const ex = (connectorDragState.currentX - rect.left) / scale;
  const ey = (connectorDragState.currentY - rect.top) / scale;
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      className: "absolute inset-0",
      style: {
        width: canvasSize.width,
        height: canvasSize.height,
        pointerEvents: "none"
      },
      viewBox: `0 0 ${canvasSize.width} ${canvasSize.height}`,
      children: (0, import_jsx_runtime.jsx)(
        "line",
        {
          x1: sx,
          y1: sy,
          x2: ex,
          y2: ey,
          stroke: "#3b82f6",
          strokeWidth: 2,
          strokeDasharray: "6 4",
          strokeLinecap: "round"
        }
      )
    }
  );
}
function DrawingOverlaySvg({
  canvasSize,
  activeTool,
  drawingColor,
  drawingWidth,
  isStrokeActive,
  liveStrokeD,
  liveStrokeView,
  onPointerDown,
  onPointerMove,
  onPointerUp
}) {
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      className: "absolute inset-0 z-[60]",
      style: {
        width: canvasSize.width,
        height: canvasSize.height,
        cursor: "crosshair",
        touchAction: "none"
      },
      viewBox: `0 0 ${canvasSize.width} ${canvasSize.height}`,
      onPointerDown,
      onPointerMove,
      onPointerUp,
      children: isStrokeActive && liveStrokeView ? renderStrokeView(liveStrokeView, true, void 0, "ink-live-preview") : isStrokeActive && liveStrokeD && (0, import_jsx_runtime.jsx)(
        "path",
        {
          d: liveStrokeD,
          fill: "none",
          stroke: drawingColor,
          strokeWidth: drawingWidth,
          strokeOpacity: activeTool === "highlighter" ? 0.4 : 1,
          strokeLinecap: "round",
          strokeLinejoin: "round"
        }
      )
    }
  );
}
function GridOverlay({ canvasSize, gridSpacingPx = GRID_SIZE, visible }) {
  const spacing = Math.max(gridSpacingPx, 2);
  const patternId = (0, import_react2.useMemo)(() => `grid-dot-pattern-${Math.random().toString(36).slice(2, 8)}`, []);
  if (!visible) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      className: "absolute inset-0 pointer-events-none z-[2]",
      width: canvasSize.width,
      height: canvasSize.height,
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime.jsx)("defs", { children: (0, import_jsx_runtime.jsx)("pattern", { id: patternId, width: spacing, height: spacing, patternUnits: "userSpaceOnUse", children: (0, import_jsx_runtime.jsx)("circle", { cx: spacing / 2, cy: spacing / 2, r: 0.6, fill: "rgba(156, 163, 175, 0.55)" }) }) }),
        (0, import_jsx_runtime.jsx)("rect", { width: canvasSize.width, height: canvasSize.height, fill: `url(#${patternId})` })
      ]
    }
  );
}
function MotionPathOverlay({
  element,
  path,
  canvasSize,
  scale,
  canEdit,
  onChangePath
}) {
  const { t } = useTranslation();
  const dragRef = (0, import_react2.useRef)(null);
  const frame = {
    originX: element.x + element.width / 2,
    originY: element.y + element.height / 2,
    slideWidth: canvasSize.width,
    slideHeight: canvasSize.height
  };
  const d = motionPathToSvgD(path, frame);
  const end = motionPathEndPixel(path, frame);
  const editable = canEdit && Boolean(onChangePath) && isEditableMotionPath(path);
  const handlePointerDown = (0, import_react2.useCallback)(
    (event) => {
      if (!editable) {
        return;
      }
      event.stopPropagation();
      event.preventDefault();
      event.currentTarget.setPointerCapture(event.pointerId);
      dragRef.current = {
        pointerId: event.pointerId,
        startX: event.clientX,
        startY: event.clientY
      };
    },
    [editable]
  );
  const handlePointerMove = (0, import_react2.useCallback)(
    (event) => {
      const drag = dragRef.current;
      if (!drag || drag.pointerId !== event.pointerId || !onChangePath) {
        return;
      }
      event.stopPropagation();
      const dxPx = (event.clientX - drag.startX) / (scale || 1);
      const dyPx = (event.clientY - drag.startY) / (scale || 1);
      const nextX = (end.x + dxPx - frame.originX) / frame.slideWidth;
      const nextY = (end.y + dyPx - frame.originY) / frame.slideHeight;
      const next = setMotionPathEnd(path, nextX, nextY);
      if (next !== path) {
        dragRef.current = {
          pointerId: event.pointerId,
          startX: event.clientX,
          startY: event.clientY
        };
        onChangePath(next);
      }
    },
    [
      end.x,
      end.y,
      frame.originX,
      frame.originY,
      frame.slideWidth,
      frame.slideHeight,
      onChangePath,
      path,
      scale
    ]
  );
  const handlePointerUp = (0, import_react2.useCallback)((event) => {
    var _a;
    if (((_a = dragRef.current) == null ? void 0 : _a.pointerId) === event.pointerId) {
      event.currentTarget.releasePointerCapture(event.pointerId);
      dragRef.current = null;
    }
  }, []);
  if (!d) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      className: "pointer-events-none absolute left-0 top-0 z-[45]",
      width: canvasSize.width,
      height: canvasSize.height,
      role: "img",
      "aria-label": t("pptx.animation.motionPath.overlay"),
      "data-pptx-motion-path-overlay": "true",
      children: [
        (0, import_jsx_runtime.jsx)(
          "path",
          {
            d,
            fill: "none",
            stroke: "#0ea5e9",
            strokeWidth: 2,
            strokeDasharray: "6 4",
            vectorEffect: "non-scaling-stroke"
          }
        ),
        (0, import_jsx_runtime.jsx)("circle", { cx: frame.originX, cy: frame.originY, r: 5, fill: "#0ea5e9", opacity: 0.55 }),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: end.x,
            cy: end.y,
            r: 7,
            fill: "#ffffff",
            stroke: "#0ea5e9",
            strokeWidth: 2,
            className: editable ? "pointer-events-auto cursor-move" : "",
            "aria-label": t("pptx.animation.motionPath.endHandle"),
            "data-pptx-motion-path-handle": "end",
            onPointerDown: handlePointerDown,
            onPointerMove: handlePointerMove,
            onPointerUp: handlePointerUp,
            onPointerCancel: handlePointerUp
          }
        )
      ]
    }
  );
}
function HorizontalRulerView({ ticks, widthPx, highlight }) {
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      width: widthPx,
      height: RULER_THICKNESS,
      className: "block select-none",
      "aria-hidden": "true",
      "data-pptx-ruler": "h",
      children: [
        (0, import_jsx_runtime.jsx)("rect", { width: widthPx, height: RULER_THICKNESS, className: "fill-gray-100 dark:fill-gray-800" }),
        (0, import_jsx_runtime.jsx)(
          "line",
          {
            x1: 0,
            y1: RULER_THICKNESS - 0.5,
            x2: widthPx,
            y2: RULER_THICKNESS - 0.5,
            className: "stroke-border",
            strokeWidth: 1
          }
        ),
        highlight && (0, import_jsx_runtime.jsx)(
          "rect",
          {
            x: highlight.start,
            y: 0,
            width: Math.max(highlight.end - highlight.start, 1),
            height: RULER_THICKNESS,
            className: "fill-primary/20"
          }
        ),
        ticks.map((tick, i) => {
          const tickHeight = tick.isMajor ? RULER_THICKNESS * 0.6 : RULER_THICKNESS * 0.3;
          return (0, import_jsx_runtime.jsxs)("g", { children: [
            (0, import_jsx_runtime.jsx)(
              "line",
              {
                x1: tick.position,
                y1: RULER_THICKNESS,
                x2: tick.position,
                y2: RULER_THICKNESS - tickHeight,
                className: "stroke-gray-400 dark:stroke-gray-500",
                strokeWidth: tick.isMajor ? 1 : 0.5
              }
            ),
            tick.label && (0, import_jsx_runtime.jsx)(
              "text",
              {
                x: tick.position + 2,
                y: RULER_FONT_SIZE + 1,
                fontSize: RULER_FONT_SIZE,
                className: "fill-gray-500 dark:fill-gray-400",
                style: { fontFamily: "system-ui, sans-serif" },
                children: tick.label
              }
            )
          ] }, i);
        })
      ]
    }
  );
}
var HorizontalRuler = import_react2.default.memo(HorizontalRulerView);
HorizontalRuler.displayName = "HorizontalRuler";
function VerticalRulerView({ ticks, heightPx, highlight }) {
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      width: RULER_THICKNESS,
      height: heightPx,
      className: "block select-none",
      "aria-hidden": "true",
      "data-pptx-ruler": "v",
      children: [
        (0, import_jsx_runtime.jsx)(
          "rect",
          {
            width: RULER_THICKNESS,
            height: heightPx,
            className: "fill-gray-100 dark:fill-gray-800"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "line",
          {
            x1: RULER_THICKNESS - 0.5,
            y1: 0,
            x2: RULER_THICKNESS - 0.5,
            y2: heightPx,
            className: "stroke-border",
            strokeWidth: 1
          }
        ),
        highlight && (0, import_jsx_runtime.jsx)(
          "rect",
          {
            x: 0,
            y: highlight.start,
            width: RULER_THICKNESS,
            height: Math.max(highlight.end - highlight.start, 1),
            className: "fill-primary/20"
          }
        ),
        ticks.map((tick, i) => {
          const tickWidth = tick.isMajor ? RULER_THICKNESS * 0.6 : RULER_THICKNESS * 0.3;
          return (0, import_jsx_runtime.jsxs)("g", { children: [
            (0, import_jsx_runtime.jsx)(
              "line",
              {
                x1: RULER_THICKNESS,
                y1: tick.position,
                x2: RULER_THICKNESS - tickWidth,
                y2: tick.position,
                className: "stroke-gray-400 dark:stroke-gray-500",
                strokeWidth: tick.isMajor ? 1 : 0.5
              }
            ),
            tick.label && (0, import_jsx_runtime.jsx)(
              "text",
              {
                x: 2,
                y: tick.position + RULER_FONT_SIZE + 2,
                fontSize: RULER_FONT_SIZE,
                className: "fill-gray-500 dark:fill-gray-400",
                style: { fontFamily: "system-ui, sans-serif" },
                children: tick.label
              }
            )
          ] }, i);
        })
      ]
    }
  );
}
var VerticalRuler = import_react2.default.memo(VerticalRulerView);
VerticalRuler.displayName = "VerticalRuler";
function Ruler({
  canvasSize,
  editorScale,
  unit,
  visible,
  selectedBounds,
  onCreateGuideFromRuler
}) {
  const hTicks = (0, import_react2.useMemo)(
    () => generateTicks(canvasSize.width, editorScale, unit),
    [canvasSize.width, editorScale, unit]
  );
  const vTicks = (0, import_react2.useMemo)(
    () => generateTicks(canvasSize.height, editorScale, unit),
    [canvasSize.height, editorScale, unit]
  );
  const hHighlight = (0, import_react2.useMemo)(() => {
    if (!selectedBounds) {
      return null;
    }
    return {
      start: selectedBounds.x * editorScale,
      end: (selectedBounds.x + selectedBounds.width) * editorScale
    };
  }, [selectedBounds, editorScale]);
  const vHighlight = (0, import_react2.useMemo)(() => {
    if (!selectedBounds) {
      return null;
    }
    return {
      start: selectedBounds.y * editorScale,
      end: (selectedBounds.y + selectedBounds.height) * editorScale
    };
  }, [selectedBounds, editorScale]);
  const [rulerDrag, setRulerDrag] = (0, import_react2.useState)(null);
  const hRulerRef = (0, import_react2.useRef)(null);
  const vRulerRef = (0, import_react2.useRef)(null);
  const handleHRulerPointerDown = (0, import_react2.useCallback)(
    (e) => {
      if (!onCreateGuideFromRuler) {
        return;
      }
      e.preventDefault();
      e.currentTarget.setPointerCapture(e.pointerId);
      setRulerDrag({ axis: "h", pointerId: e.pointerId });
    },
    [onCreateGuideFromRuler]
  );
  const handleVRulerPointerDown = (0, import_react2.useCallback)(
    (e) => {
      if (!onCreateGuideFromRuler) {
        return;
      }
      e.preventDefault();
      e.currentTarget.setPointerCapture(e.pointerId);
      setRulerDrag({ axis: "v", pointerId: e.pointerId });
    },
    [onCreateGuideFromRuler]
  );
  const handleRulerPointerUp = (0, import_react2.useCallback)(
    (e) => {
      if (!rulerDrag || !onCreateGuideFromRuler) {
        return;
      }
      try {
        e.currentTarget.releasePointerCapture(rulerDrag.pointerId);
      } catch {
      }
      const horizontal = rulerDrag.axis === "h";
      const rulerEl = horizontal ? hRulerRef.current : vRulerRef.current;
      if (!rulerEl) {
        setRulerDrag(null);
        return;
      }
      const rect = rulerEl.getBoundingClientRect();
      const posScaled = horizontal ? e.clientY - rect.top : e.clientX - rect.left;
      const positionPx = rulerDragToGuidePosition(
        posScaled,
        editorScale,
        horizontal ? canvasSize.height : canvasSize.width
      );
      if (positionPx !== null) {
        onCreateGuideFromRuler(rulerDrag.axis, positionPx);
      }
      setRulerDrag(null);
    },
    [rulerDrag, onCreateGuideFromRuler, editorScale, canvasSize]
  );
  if (!visible) {
    return null;
  }
  const scaledWidth = canvasSize.width * editorScale;
  const scaledHeight = canvasSize.height * editorScale;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "absolute z-[51] bg-gray-100 dark:bg-gray-800 border-r border-b border-border",
        style: {
          top: 0,
          left: 0,
          width: RULER_THICKNESS,
          height: RULER_THICKNESS
        }
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        ref: hRulerRef,
        className: "absolute z-[50] overflow-visible",
        style: {
          top: 0,
          left: RULER_THICKNESS,
          width: scaledWidth,
          height: RULER_THICKNESS,
          cursor: onCreateGuideFromRuler ? "row-resize" : void 0
        },
        onPointerDown: handleHRulerPointerDown,
        onPointerUp: handleRulerPointerUp,
        children: (0, import_jsx_runtime.jsx)(HorizontalRuler, { ticks: hTicks, widthPx: scaledWidth, highlight: hHighlight })
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        ref: vRulerRef,
        className: "absolute z-[50] overflow-visible",
        style: {
          top: RULER_THICKNESS,
          left: 0,
          width: RULER_THICKNESS,
          height: scaledHeight,
          cursor: onCreateGuideFromRuler ? "col-resize" : void 0
        },
        onPointerDown: handleVRulerPointerDown,
        onPointerUp: handleRulerPointerUp,
        children: (0, import_jsx_runtime.jsx)(VerticalRuler, { ticks: vTicks, heightPx: scaledHeight, highlight: vHighlight })
      }
    )
  ] });
}
function SelectionHandleOverlay({
  element,
  adjustmentHandles,
  onResizePointerDown,
  onAdjustmentPointerDown,
  onRotate,
  onClick,
  onDoubleClick,
  onContextMenu
}) {
  const allow = resolveElementInteractivity(element);
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      "data-pptx-handle-for": element.id,
      "data-pptx-selection-handle-host": "true",
      style: {
        position: "absolute",
        left: element.x,
        top: element.y,
        width: Math.max(element.width, MIN_ELEMENT_SIZE),
        height: Math.max(element.height, MIN_ELEMENT_SIZE),
        // Every slide element carries an explicit numeric `zIndex` (see
        // `getContainerStyle`), so a sibling with no z-index at all paints
        // at the CSS "z-index: 0" level regardless of DOM order: any
        // element other than the very bottom one (index 0) then renders
        // ABOVE this host, hiding its own resize/rotate handles behind its
        // own fill (worst on a rotated shape, where the rotate knob
        // deliberately overlaps the box) and stealing their clicks. Pinned
        // above the stage's other interaction overlays
        // (marquee z-50, connector overlay z-55/56) to match, well clear of
        // any realistic per-slide element count.
        zIndex: 58,
        transform: getElementTransform(element),
        transformOrigin: "center",
        // Transparent everywhere a handle button is not, so a click that
        // misses every handle still reaches the shape (or whatever is
        // behind it) exactly as if this host did not exist. `ResizeHandles`
        // renders with `forcePointerEvents` so its own buttons opt back in.
        pointerEvents: "none"
      },
      onClick: (e) => onClick(element.id, e),
      onDoubleClick: (e) => onDoubleClick(element.id, e),
      onContextMenu: (e) => onContextMenu(element.id, e),
      children: (0, import_jsx_runtime.jsx)(
        ResizeHandles,
        {
          elementId: element.id,
          adjustmentHandles,
          onResizePointerDown,
          onAdjustmentPointerDown,
          rotation: element.rotation,
          nonRotationTransform: getElementTransformWithoutRotation(element),
          onRotate: allow.rotatable ? onRotate : void 0,
          forcePointerEvents: true
        }
      )
    }
  );
}
function getElementIdFromEvent(e) {
  var _a;
  let target = e.target;
  while (target) {
    if (((_a = target.dataset) == null ? void 0 : _a.pptxElement) === "true") {
      return target.dataset.elementId || null;
    }
    target = target.parentElement;
  }
  return null;
}
var DOUBLE_TAP_MS = 300;
function isViewportBackgroundMouseDownTarget(target, currentTarget) {
  return target === currentTarget;
}
function useCanvasEventHandlers({
  cbRef,
  onCanvasMouseDown,
  findResults,
  findResultIndex,
  activeSlideIndex,
  selectedElement,
  zoom,
  onMoveGuide
}) {
  const elementFindHighlightsMap = (0, import_react2.useMemo)(() => {
    const map = /* @__PURE__ */ new Map();
    if (!findResults || findResults.length === 0 || activeSlideIndex === null) {
      return map;
    }
    for (let i = 0; i < findResults.length; i++) {
      const r = findResults[i];
      if (r.slideIndex !== activeSlideIndex) {
        continue;
      }
      if (!map.has(r.elementId)) {
        map.set(r.elementId, /* @__PURE__ */ new Map());
      }
      const elMap = map.get(r.elementId);
      if (!elMap.has(r.segmentIndex)) {
        elMap.set(r.segmentIndex, []);
      }
      elMap.get(r.segmentIndex).push({
        startOffset: r.startOffset,
        length: r.length,
        isCurrent: i === (findResultIndex ?? -1)
      });
    }
    return map;
  }, [findResults, findResultIndex, activeSlideIndex]);
  const selectedBounds = (0, import_react2.useMemo)(() => {
    if (!selectedElement) {
      return null;
    }
    return {
      x: selectedElement.x,
      y: selectedElement.y,
      width: selectedElement.width,
      height: selectedElement.height
    };
  }, [selectedElement]);
  const handleStageClick = (0, import_react2.useCallback)(
    (e) => {
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onClick(id, e);
      }
    },
    [cbRef]
  );
  const handleStageDblClick = (0, import_react2.useCallback)(
    (e) => {
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onDoubleClick(id, e);
      }
    },
    [cbRef]
  );
  const suppressNextMouseDownRef = (0, import_react2.useRef)(false);
  const handleStageMouseDown = (0, import_react2.useCallback)(
    (e) => {
      if (suppressNextMouseDownRef.current) {
        suppressNextMouseDownRef.current = false;
        return;
      }
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onMouseDown(id, e);
        return;
      }
      onCanvasMouseDown == null ? void 0 : onCanvasMouseDown(e);
    },
    [cbRef, onCanvasMouseDown]
  );
  const handleViewportMouseDown = (0, import_react2.useCallback)(
    (e) => {
      if (!isViewportBackgroundMouseDownTarget(e.target, e.currentTarget)) {
        return;
      }
      onCanvasMouseDown == null ? void 0 : onCanvasMouseDown(e);
    },
    [onCanvasMouseDown]
  );
  const lastTapRef = (0, import_react2.useRef)(null);
  const handleStagePointerDown = (0, import_react2.useCallback)(
    (e) => {
      var _a, _b2;
      if (e.pointerType === "mouse") {
        return;
      }
      suppressNextMouseDownRef.current = true;
      (_b2 = (_a = e.currentTarget).setPointerCapture) == null ? void 0 : _b2.call(_a, e.pointerId);
      const id = getElementIdFromEvent(e);
      if (id) {
        const now = e.timeStamp || Date.now();
        const last = lastTapRef.current;
        if (last && last.id === id && now - last.time < DOUBLE_TAP_MS) {
          lastTapRef.current = null;
          cbRef.current.onDoubleClick(id, e);
          return;
        }
        lastTapRef.current = { id, time: now };
        cbRef.current.onMouseDown(id, e);
        return;
      }
      lastTapRef.current = null;
      onCanvasMouseDown == null ? void 0 : onCanvasMouseDown(e);
    },
    [cbRef, onCanvasMouseDown]
  );
  const handleStageContextMenu = (0, import_react2.useCallback)(
    (e) => {
      const id = getElementIdFromEvent(e);
      if (id) {
        cbRef.current.onContextMenu(id, e);
      }
    },
    [cbRef]
  );
  const [draggingGuide, setDraggingGuide] = (0, import_react2.useState)(null);
  const handleStagePointerMove = (0, import_react2.useCallback)(
    (e) => {
      if (!draggingGuide || !onMoveGuide) {
        return;
      }
      const stage = zoom.canvasStageRef.current;
      if (!stage) {
        return;
      }
      const rect = stage.getBoundingClientRect();
      const scale = zoom.editorScale || 1;
      const rawPosition = draggingGuide.axis === "h" ? (e.clientY - rect.top) / scale : (e.clientX - rect.left) / scale;
      onMoveGuide(draggingGuide.id, rawPosition);
    },
    [draggingGuide, onMoveGuide, zoom.canvasStageRef, zoom.editorScale]
  );
  const handleStagePointerUp = (0, import_react2.useCallback)(
    (e) => {
      if (!draggingGuide) {
        return;
      }
      try {
        e.currentTarget.releasePointerCapture(draggingGuide.pointerId);
      } catch {
      }
      setDraggingGuide(null);
    },
    [draggingGuide]
  );
  return {
    elementFindHighlightsMap,
    selectedBounds,
    handleStageClick,
    handleStageDblClick,
    handleStageMouseDown,
    handleViewportMouseDown,
    handleStagePointerDown,
    handleStageContextMenu,
    draggingGuide,
    setDraggingGuide,
    handleStagePointerMove,
    handleStagePointerUp
  };
}
function useConnectorCreation({
  activeSlide,
  zoom: _zoom,
  onCreateConnector
}) {
  const [connectorDragState, setConnectorDragState] = (0, import_react2.useState)(null);
  const handleConnectionSiteDown = (0, import_react2.useCallback)(
    (elementId, siteIndex, e) => {
      e.stopPropagation();
      e.preventDefault();
      setConnectorDragState({
        startElementId: elementId,
        startSiteIndex: siteIndex,
        currentX: e.clientX,
        currentY: e.clientY
      });
    },
    []
  );
  const handleConnectorDragMove = (0, import_react2.useCallback)(
    (e) => {
      if (!connectorDragState) {
        return;
      }
      setConnectorDragState(
        (prev) => prev ? { ...prev, currentX: e.clientX, currentY: e.clientY } : null
      );
    },
    [connectorDragState]
  );
  const handleConnectionSiteDrop = (0, import_react2.useCallback)(
    (targetElementId, targetSiteIndex) => {
      if (!connectorDragState || !onCreateConnector) {
        return;
      }
      const startEl = activeSlide == null ? void 0 : activeSlide.elements.find(
        (el) => el.id === connectorDragState.startElementId
      );
      const endEl = activeSlide == null ? void 0 : activeSlide.elements.find((el) => el.id === targetElementId);
      if (!startEl || !endEl) {
        setConnectorDragState(null);
        return;
      }
      const authored = authorConnectorBetweenSites(
        { element: startEl, siteIndex: connectorDragState.startSiteIndex },
        { element: endEl, siteIndex: targetSiteIndex }
      );
      if (!authored) {
        setConnectorDragState(null);
        return;
      }
      const newConnector = {
        id: `conn-new-${Date.now()}`,
        type: "connector",
        x: authored.x,
        y: authored.y,
        width: authored.width,
        height: authored.height,
        shapeType: authored.shapeType,
        shapeStyle: {
          strokeColor: "#4472C4",
          strokeWidth: 2,
          connectorStartConnection: authored.startConnection,
          connectorEndConnection: authored.endConnection
        }
      };
      onCreateConnector(newConnector);
      setConnectorDragState(null);
    },
    [connectorDragState, onCreateConnector, activeSlide == null ? void 0 : activeSlide.elements]
  );
  const handleConnectorDragEnd = (0, import_react2.useCallback)(() => {
    setConnectorDragState(null);
  }, []);
  return {
    connectorDragState,
    handleConnectionSiteDown,
    handleConnectorDragMove,
    handleConnectionSiteDrop,
    handleConnectorDragEnd
  };
}
function buildCanvasPathD(points) {
  if (points.length === 0) {
    return "";
  }
  const parts = [`M ${points[0].x} ${points[0].y}`];
  for (let i = 1; i < points.length; i++) {
    parts.push(`L ${points[i].x} ${points[i].y}`);
  }
  return parts.join(" ");
}
function finishDrawStroke(input) {
  const { tool, points, pressures, tiltX, tiltY, color, width } = input;
  if (points.length < 2) {
    return null;
  }
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const pt of points) {
    if (pt.x < minX) {
      minX = pt.x;
    }
    if (pt.y < minY) {
      minY = pt.y;
    }
    if (pt.x > maxX) {
      maxX = pt.x;
    }
    if (pt.y > maxY) {
      maxY = pt.y;
    }
  }
  const pad = width;
  minX -= pad;
  minY -= pad;
  maxX += pad;
  maxY += pad;
  const w = Math.max(maxX - minX, 1);
  const h = Math.max(maxY - minY, 1);
  const relPoints = points.map((pt) => ({ x: pt.x - minX, y: pt.y - minY }));
  if (tool === "freeform") {
    return {
      kind: "freeform",
      element: buildFreeformShape(relPoints, { x: minX, y: minY, w, h }, color, width)
    };
  }
  return {
    kind: "ink",
    element: buildInkElement(
      relPoints,
      { x: minX, y: minY, w, h },
      tool,
      { pressures, tiltX, tiltY },
      color,
      width
    )
  };
}
function buildFreeformShape(relPoints, box, color, width) {
  const COORD_SCALE = 100;
  const segments = [];
  for (let i = 0; i < relPoints.length; i++) {
    const scaledPt = {
      x: Math.round(relPoints[i].x * COORD_SCALE),
      y: Math.round(relPoints[i].y * COORD_SCALE)
    };
    segments.push(i === 0 ? { type: "moveTo", pt: scaledPt } : { type: "lineTo", pt: scaledPt });
  }
  if (segments.length > 2) {
    segments.push({ type: "close" });
  }
  return {
    id: `shape-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    type: "shape",
    x: box.x,
    y: box.y,
    width: box.w,
    height: box.h,
    shapeType: "custom",
    shapeStyle: { fillColor: "transparent", strokeColor: color, strokeWidth: width },
    customGeometryPaths: [
      { width: Math.round(box.w * COORD_SCALE), height: Math.round(box.h * COORD_SCALE), segments }
    ]
  };
}
function buildInkElement(relPoints, box, tool, channels, color, width) {
  const pathD = buildCanvasPathD(relPoints);
  const isHighlighter = tool === "highlighter";
  const { pressures, tiltX, tiltY } = channels;
  const hasPressure = pressures.length >= 2 && pressures.some((p) => Math.abs(p - pressures[0]) > 0.01);
  const hasTilt = hasTiltData(tiltX, tiltY);
  return {
    id: `ink-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    type: "ink",
    x: box.x,
    y: box.y,
    width: box.w,
    height: box.h,
    inkPaths: [pathD],
    inkColors: [color],
    inkWidths: [width],
    inkOpacities: [isHighlighter ? 0.4 : 1],
    inkTool: isHighlighter ? "highlighter" : "pen",
    ...hasPressure ? { inkPointPressures: [pressures] } : {},
    ...hasTilt ? { inkPointTiltX: [tiltX], inkPointTiltY: [tiltY] } : {}
  };
}
function useLiveInkPreview(isStrokeActive, activeTool, points, pressures, tiltX, tiltY, color, width) {
  const liveStrokeD = (0, import_react2.useMemo)(
    () => isStrokeActive ? buildCanvasPathD(points) : "",
    [isStrokeActive, points]
  );
  const liveStrokeView = (0, import_react2.useMemo)(() => {
    if (!isStrokeActive || activeTool === "select" || activeTool === "eraser") {
      return null;
    }
    const inkPoints = points.map((pt, i) => ({
      x: pt.x,
      y: pt.y,
      pressure: pressures[i],
      tiltX: tiltX[i],
      tiltY: tiltY[i]
    }));
    return buildLiveInkStrokeView({ points: inkPoints, color, width, tool: activeTool });
  }, [isStrokeActive, activeTool, points, pressures, tiltX, tiltY, color, width]);
  return { liveStrokeD, liveStrokeView };
}
function useDrawingOverlay({
  activeTool,
  activeSlide,
  zoom,
  drawingColor,
  drawingWidth,
  isDrawingRef,
  onAddInkElement,
  onAddFreeformShape,
  onEraseInkElement
}) {
  const isDrawing = activeTool !== "select";
  const [currentStrokePoints, setCurrentStrokePoints] = (0, import_react2.useState)(
    []
  );
  const [currentStrokePressures, setCurrentStrokePressures] = (0, import_react2.useState)([]);
  const [currentStrokeTiltX, setCurrentStrokeTiltX] = (0, import_react2.useState)([]);
  const [currentStrokeTiltY, setCurrentStrokeTiltY] = (0, import_react2.useState)([]);
  const [isStrokeActive, setIsStrokeActive] = (0, import_react2.useState)(false);
  const pointerToCanvasCoords = (0, import_react2.useCallback)(
    (e) => {
      const stage = zoom.canvasStageRef.current;
      if (!stage) {
        return null;
      }
      const rect = stage.getBoundingClientRect();
      const scale = zoom.editorScale || 1;
      return {
        x: (e.clientX - rect.left) / scale,
        y: (e.clientY - rect.top) / scale
      };
    },
    [zoom.canvasStageRef, zoom.editorScale]
  );
  const handleDrawPointerDown = (0, import_react2.useCallback)(
    (e) => {
      if (activeTool === "select") {
        return;
      }
      if (activeTool === "eraser" && activeSlide) {
        const pt2 = pointerToCanvasCoords(e);
        if (!pt2) {
          return;
        }
        const hitId = findEraserHitElementId(activeSlide.elements, pt2);
        if (hitId) {
          onEraseInkElement == null ? void 0 : onEraseInkElement(hitId);
        }
        return;
      }
      const pt = pointerToCanvasCoords(e);
      if (!pt) {
        return;
      }
      e.preventDefault();
      e.target.setPointerCapture(e.pointerId);
      setCurrentStrokePoints([pt]);
      setCurrentStrokePressures([e.pressure]);
      setCurrentStrokeTiltX([e.tiltX ?? 0]);
      setCurrentStrokeTiltY([e.tiltY ?? 0]);
      setIsStrokeActive(true);
      if (isDrawingRef) {
        isDrawingRef.current = true;
      }
    },
    [activeTool, activeSlide, pointerToCanvasCoords, isDrawingRef, onEraseInkElement]
  );
  const handleDrawPointerMove = (0, import_react2.useCallback)(
    (e) => {
      if (!isStrokeActive || activeTool === "select" || activeTool === "eraser") {
        return;
      }
      const pt = pointerToCanvasCoords(e);
      if (!pt) {
        return;
      }
      setCurrentStrokePoints((prev) => [...prev, pt]);
      setCurrentStrokePressures((prev) => [...prev, e.pressure]);
      setCurrentStrokeTiltX((prev) => [...prev, e.tiltX ?? 0]);
      setCurrentStrokeTiltY((prev) => [...prev, e.tiltY ?? 0]);
    },
    [isStrokeActive, activeTool, pointerToCanvasCoords]
  );
  const handleDrawPointerUp = (0, import_react2.useCallback)(
    (e) => {
      if (!isStrokeActive || activeTool === "select" || activeTool === "eraser") {
        return;
      }
      e.target.releasePointerCapture(e.pointerId);
      setIsStrokeActive(false);
      if (isDrawingRef) {
        isDrawingRef.current = false;
      }
      const finished = finishDrawStroke({
        tool: activeTool,
        points: currentStrokePoints,
        pressures: currentStrokePressures,
        tiltX: currentStrokeTiltX,
        tiltY: currentStrokeTiltY,
        color: drawingColor,
        width: drawingWidth
      });
      if ((finished == null ? void 0 : finished.kind) === "freeform") {
        onAddFreeformShape == null ? void 0 : onAddFreeformShape(finished.element);
      } else if ((finished == null ? void 0 : finished.kind) === "ink") {
        onAddInkElement == null ? void 0 : onAddInkElement(finished.element);
      }
      setCurrentStrokePoints([]);
      setCurrentStrokePressures([]);
      setCurrentStrokeTiltX([]);
      setCurrentStrokeTiltY([]);
    },
    [
      isStrokeActive,
      activeTool,
      currentStrokePoints,
      currentStrokeTiltX,
      currentStrokeTiltY,
      drawingColor,
      drawingWidth,
      isDrawingRef,
      onAddInkElement,
      onAddFreeformShape,
      currentStrokePressures
    ]
  );
  const { liveStrokeD, liveStrokeView } = useLiveInkPreview(
    isStrokeActive,
    activeTool,
    currentStrokePoints,
    currentStrokePressures,
    currentStrokeTiltX,
    currentStrokeTiltY,
    drawingColor,
    drawingWidth
  );
  return {
    isDrawing,
    isStrokeActive,
    liveStrokeD,
    liveStrokeView,
    currentStrokePoints,
    handleDrawPointerDown,
    handleDrawPointerMove,
    handleDrawPointerUp
  };
}
function useStableCallbacks(callbacks) {
  const cbRef = (0, import_react2.useRef)(callbacks);
  cbRef.current = callbacks;
  const stableResizePointerDown = (0, import_react2.useCallback)(
    (elementId, e, handle) => cbRef.current.onResizePointerDown(elementId, e, handle),
    []
  );
  const stableAdjustmentPointerDown = (0, import_react2.useCallback)(
    (elementId, e, descriptor) => cbRef.current.onAdjustmentPointerDown(elementId, e, descriptor),
    []
  );
  const stableRotate = (0, import_react2.useCallback)(
    (elementId, rotationDeg) => {
      var _a, _b2;
      return (_b2 = (_a = cbRef.current).onRotate) == null ? void 0 : _b2.call(_a, elementId, rotationDeg);
    },
    []
  );
  const stableInlineEditChange = (0, import_react2.useCallback)(
    (text) => cbRef.current.onInlineEditChange(text),
    []
  );
  const stableInlineEditCommit = (0, import_react2.useCallback)(() => cbRef.current.onInlineEditCommit(), []);
  const stableInlineEditCancel = (0, import_react2.useCallback)(() => cbRef.current.onInlineEditCancel(), []);
  const stableTableCellSelect = (0, import_react2.useCallback)(
    (cell, elementId) => cbRef.current.onTableCellSelect(cell, elementId),
    []
  );
  const stableCommitCellEdit = (0, import_react2.useCallback)(
    (elementId, rowIndex, colIndex, text) => {
      var _a, _b2;
      return (_b2 = (_a = cbRef.current).onCommitCellEdit) == null ? void 0 : _b2.call(_a, elementId, rowIndex, colIndex, text);
    },
    []
  );
  const stableUpdateSmartArtElement = (0, import_react2.useCallback)(
    (elementId, updates) => {
      var _a, _b2;
      return (_b2 = (_a = cbRef.current).onUpdateSmartArtElement) == null ? void 0 : _b2.call(_a, elementId, updates);
    },
    []
  );
  const stableFormatText = (0, import_react2.useCallback)(
    (updates) => {
      var _a, _b2;
      return (_b2 = (_a = cbRef.current).onFormatText) == null ? void 0 : _b2.call(_a, updates);
    },
    []
  );
  const stableResizeTableColumns = (0, import_react2.useCallback)(
    (elementId, newWidths) => {
      var _a, _b2;
      return (_b2 = (_a = cbRef.current).onResizeTableColumns) == null ? void 0 : _b2.call(_a, elementId, newWidths);
    },
    []
  );
  const stableResizeTableRow = (0, import_react2.useCallback)(
    (elementId, rowIndex, newHeight) => {
      var _a, _b2;
      return (_b2 = (_a = cbRef.current).onResizeTableRow) == null ? void 0 : _b2.call(_a, elementId, rowIndex, newHeight);
    },
    []
  );
  return {
    cbRef,
    stableResizePointerDown,
    stableAdjustmentPointerDown,
    stableRotate,
    stableInlineEditChange,
    stableInlineEditCommit,
    stableInlineEditCancel,
    stableTableCellSelect,
    stableCommitCellEdit,
    stableUpdateSmartArtElement,
    stableFormatText,
    stableResizeTableColumns,
    stableResizeTableRow
  };
}
function ActiveXControlOverlay({
  controls,
  canvasSize,
  resolveFallbackImage
}) {
  if (controls.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 pointer-events-none z-[40]", "data-testid": "pptx-activex-overlay", children: controls.map((control, idx) => {
    const view = getActiveXControlOverlayView(control, canvasSize, idx, resolveFallbackImage);
    if (view.className === "image" && view.imageUrl) {
      return (0, import_jsx_runtime.jsx)(
        "img",
        {
          src: view.imageUrl,
          alt: view.label,
          title: `ActiveX control: ${view.label}`,
          className: "absolute",
          style: { left: view.left, top: view.top, width: view.width, height: view.height }
        },
        `${control.relId}-${idx}`
      );
    }
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: "absolute",
        title: `ActiveX control: ${view.label} (interactive controls are not supported in the viewer)`,
        style: {
          left: view.left,
          top: view.top,
          width: view.width,
          height: view.height,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          gap: 6,
          padding: "2px 6px",
          boxSizing: "border-box",
          border: "1px dashed rgba(100, 116, 139, 0.8)",
          borderRadius: 4,
          background: "rgba(148, 163, 184, 0.14)",
          color: "rgb(51, 65, 85)",
          fontSize: 11,
          fontWeight: 600,
          lineHeight: 1.2,
          overflow: "hidden",
          textOverflow: "ellipsis",
          whiteSpace: "nowrap"
        },
        children: [
          (0, import_jsx_runtime.jsx)("span", { "aria-hidden": "true", children: "⚙" }),
          (0, import_jsx_runtime.jsx)("span", { style: { overflow: "hidden", textOverflow: "ellipsis" }, children: view.label })
        ]
      },
      `${control.relId}-${idx}`
    );
  }) });
}
function SlideBackgroundImageLayer({
  slide
}) {
  if (!(slide == null ? void 0 : slide.backgroundImage)) {
    return null;
  }
  const properties = slide.backgroundImageProperties ?? {};
  const originalEffects = properties.imageEffects;
  const renderEffects = originalEffects ? { ...originalEffects } : void 0;
  if (renderEffects) {
    delete renderEffects.alphaModFix;
    delete renderEffects.alphaModFixRawXml;
  }
  const idPart = String(slide.id || "slide").replace(/[^A-Za-z0-9_-]/gu, "-");
  const backgroundElement = {
    id: `slide-background-${idPart}`,
    type: "picture",
    x: 0,
    y: 0,
    width: 1,
    height: 1,
    ...properties,
    imageData: slide.backgroundImage,
    imageEffects: renderEffects
  };
  const opacity = typeof (originalEffects == null ? void 0 : originalEffects.alphaModFix) === "number" ? Math.max(0, Math.min(1, originalEffects.alphaModFix / 100)) : void 0;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      "aria-hidden": "true",
      className: "absolute inset-0 overflow-hidden pointer-events-none select-none",
      style: { zIndex: 0 },
      children: renderImg(
        backgroundElement,
        getImageRenderStyle(backgroundElement),
        getImageEffectsFilter(backgroundElement),
        "",
        opacity
      )
    }
  );
}
var EMPTY_ADJUSTMENT_HANDLES = [];
function SlideCanvas({
  activeSlide,
  templateElements,
  canvasSize,
  zoom,
  mode,
  canEdit,
  editTemplateMode,
  selectedElementIdSet,
  selectedElement,
  inlineEditingElementId,
  inlineEditingText,
  spellCheckEnabled,
  mediaDataUrls,
  tableEditorState,
  marqueeSelectionState,
  snapLines,
  showGrid,
  gridSpacingPx,
  showRulers,
  rulerUnit = "inches",
  guides,
  presentationElementStates,
  presentationKeyframesCss,
  onClick,
  onDoubleClick,
  onMouseDown,
  onContextMenu,
  onCanvasMouseDown,
  onResizePointerDown,
  onAdjustmentPointerDown,
  onRotate,
  onInlineEditChange,
  onInlineEditCommit,
  onInlineEditCancel,
  onTableCellSelect,
  onCommitCellEdit,
  onUpdateSmartArtElement,
  onFormatText,
  onResizeTableColumns,
  onResizeTableRow,
  findResults,
  findResultIndex,
  activeSlideIndex,
  activeTool = "select",
  drawingColor = "#000000",
  drawingWidth = 3,
  isDrawingRef,
  onAddInkElement,
  onAddFreeformShape,
  onEraseInkElement,
  onActionClick,
  onHyperlinkClick,
  comments,
  showCommentMarkers = false,
  onCommentMarkerClick,
  onMoveGuide,
  onDeleteGuide,
  onCreateGuideFromRuler,
  connectorCreationMode = false,
  onCreateConnector,
  onUpdateSlideAnimations,
  allSlides,
  onZoomClick,
  sourceSlideIndex,
  fieldContext,
  tableStyleContext,
  collaborationOverlay,
  aiActive = false
}) {
  const isEditableCanvas = (mode === "edit" || mode === "master") && canEdit;
  const {
    cbRef,
    stableResizePointerDown,
    stableAdjustmentPointerDown,
    stableRotate,
    stableInlineEditChange,
    stableInlineEditCommit,
    stableInlineEditCancel,
    stableTableCellSelect,
    stableCommitCellEdit,
    stableUpdateSmartArtElement,
    stableFormatText,
    stableResizeTableColumns,
    stableResizeTableRow
  } = useStableCallbacks({
    onClick,
    onDoubleClick,
    onMouseDown,
    onContextMenu,
    onResizePointerDown,
    onAdjustmentPointerDown,
    onRotate,
    onInlineEditChange,
    onInlineEditCommit,
    onInlineEditCancel,
    onTableCellSelect,
    onCommitCellEdit,
    onUpdateSmartArtElement,
    onFormatText,
    onResizeTableColumns,
    onResizeTableRow
  });
  const {
    elementFindHighlightsMap,
    selectedBounds,
    handleStageClick,
    handleStageDblClick,
    handleStageMouseDown,
    handleViewportMouseDown,
    handleStagePointerDown,
    handleStageContextMenu,
    setDraggingGuide,
    handleStagePointerMove,
    handleStagePointerUp
  } = useCanvasEventHandlers({
    cbRef,
    onCanvasMouseDown,
    findResults,
    findResultIndex,
    activeSlideIndex,
    selectedElement,
    zoom,
    onMoveGuide
  });
  const selectedMotionPath = selectedElement ? motionPathFor((activeSlide == null ? void 0 : activeSlide.animations) ?? [], selectedElement.id) : void 0;
  const handleMotionPathChange = (0, import_react2.useCallback)(
    (path) => {
      if (!selectedElement || !onUpdateSlideAnimations) {
        return;
      }
      onUpdateSlideAnimations(
        setMotionPath((activeSlide == null ? void 0 : activeSlide.animations) ?? [], selectedElement.id, path)
      );
    },
    [activeSlide == null ? void 0 : activeSlide.animations, onUpdateSlideAnimations, selectedElement]
  );
  const {
    connectorDragState,
    handleConnectionSiteDown,
    handleConnectorDragMove,
    handleConnectionSiteDrop,
    handleConnectorDragEnd
  } = useConnectorCreation({ activeSlide, zoom, onCreateConnector });
  const {
    isDrawing,
    isStrokeActive,
    liveStrokeD,
    liveStrokeView,
    handleDrawPointerDown,
    handleDrawPointerMove,
    handleDrawPointerUp
  } = useDrawingOverlay({
    activeTool,
    activeSlide,
    zoom,
    drawingColor,
    drawingWidth,
    isDrawingRef,
    onAddInkElement,
    onAddFreeformShape,
    onEraseInkElement
  });
  const rulerOffset = showRulers ? RULER_THICKNESS : 0;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      ref: zoom.canvasViewportRef,
      "data-pptx-viewport": true,
      className: "flex-1 flex overflow-auto relative",
      style: { touchAction: "pan-x pan-y" },
      onMouseDown: handleViewportMouseDown,
      children: (0, import_jsx_runtime.jsxs)(
        "div",
        {
          ref: zoom.editWrapperRef,
          className: "relative m-auto",
          style: {
            width: canvasSize.width * zoom.editorScale + rulerOffset,
            height: canvasSize.height * zoom.editorScale + rulerOffset
          },
          children: [
            (0, import_jsx_runtime.jsx)(
              Ruler,
              {
                canvasSize,
                editorScale: zoom.editorScale,
                unit: rulerUnit,
                visible: showRulers,
                selectedBounds,
                onCreateGuideFromRuler
              }
            ),
            (0, import_jsx_runtime.jsxs)(
              "div",
              {
                ref: zoom.canvasStageRef,
                role: "region",
                "aria-label": `Slide ${(activeSlideIndex ?? 0) + 1}`,
                "aria-roledescription": "slide",
                "data-pptx-ai-active": aiActive ? "true" : void 0,
                className: "relative shadow-2xl",
                style: {
                  width: canvasSize.width,
                  height: canvasSize.height,
                  transform: `scale(${zoom.editorScale})`,
                  transformOrigin: "top left",
                  // Motion-path keyframes translate by a fraction of the SLIDE, so
                  // the stage publishes its own size for those calc() offsets.
                  ["--pptx-slide-w"]: `${canvasSize.width}px`,
                  ["--pptx-slide-h"]: `${canvasSize.height}px`,
                  marginTop: rulerOffset,
                  marginLeft: rulerOffset,
                  // In edit/master mode the stage must own all touch gestures so
                  // drag/resize/marquee aren't stolen by the browser for panning or
                  // pinch-zoom. View/present mode keeps the default so the slide can
                  // still be scrolled and swipe-navigated.
                  touchAction: isEditableCanvas ? "none" : void 0,
                  ...getReactSlideBackgroundStyle(activeSlide, {
                    widthPx: canvasSize.width,
                    heightPx: canvasSize.height
                  })
                },
                onClick: handleStageClick,
                onDoubleClick: handleStageDblClick,
                onMouseDown: handleStageMouseDown,
                onPointerDown: isEditableCanvas ? handleStagePointerDown : void 0,
                onContextMenu: handleStageContextMenu,
                onPointerMove: handleStagePointerMove,
                onPointerUp: handleStagePointerUp,
                children: [
                  (0, import_jsx_runtime.jsx)(SlideBackgroundImageLayer, { slide: activeSlide }),
                  presentationKeyframesCss && (0, import_jsx_runtime.jsx)("style", { children: presentationKeyframesCss }),
                  (0, import_jsx_runtime.jsx)(GridOverlay, { canvasSize, gridSpacingPx, visible: showGrid }),
                  (0, import_jsx_runtime.jsx)(
                    CanvasGuides,
                    {
                      guides,
                      onDeleteGuide,
                      onStartGuideDrag: setDraggingGuide
                    }
                  ),
                  templateElements.map((element, index) => (0, import_jsx_runtime.jsx)(
                    ElementRenderer,
                    {
                      element,
                      activeSlide,
                      isSelected: selectedElementIdSet.has(element.id),
                      isInlineEditing: inlineEditingElementId === element.id,
                      inlineEditingText,
                      canInteract: (mode === "edit" || mode === "master") && canEdit && editTemplateMode,
                      spellCheckEnabled,
                      mediaDataUrls,
                      selectionColorClass: "blue-400",
                      showHoverBorder: false,
                      templateEditing: editTemplateMode,
                      zIndex: index,
                      imageAltText: "Template element",
                      showResizeHandles: shouldShowElementHandles(
                        isEditableCanvas,
                        selectedElementIdSet.has(element.id),
                        selectedElementIdSet.size
                      ),
                      renderInk: false,
                      renderGroups: true,
                      adjustmentHandles: isEditableCanvas && (selectedElement == null ? void 0 : selectedElement.id) === element.id ? getShapeAdjustmentHandleDescriptors(element) : EMPTY_ADJUSTMENT_HANDLES,
                      onResizePointerDown: stableResizePointerDown,
                      onAdjustmentPointerDown: stableAdjustmentPointerDown,
                      onRotate: stableRotate,
                      onInlineEditChange: stableInlineEditChange,
                      onInlineEditCommit: stableInlineEditCommit,
                      onInlineEditCancel: stableInlineEditCancel,
                      onUpdateSmartArtElement: stableUpdateSmartArtElement,
                      onFormatText: stableFormatText,
                      onActionClick,
                      onHyperlinkClick,
                      animationState: presentationElementStates == null ? void 0 : presentationElementStates.get(element.id),
                      presentationElementStates,
                      allSlides,
                      onZoomClick,
                      sourceSlideIndex,
                      fieldContext,
                      tableStyleContext
                    },
                    `tpl-${element.id}`
                  )),
                  activeSlide == null ? void 0 : activeSlide.elements.map((element, index) => (0, import_jsx_runtime.jsx)(
                    ElementRenderer,
                    {
                      element,
                      activeSlide,
                      isSelected: selectedElementIdSet.has(element.id),
                      isInlineEditing: inlineEditingElementId === element.id,
                      inlineEditingText,
                      canInteract: isEditableCanvas,
                      spellCheckEnabled,
                      mediaDataUrls,
                      tableEditorState,
                      selectionColorClass: "blue-500",
                      showHoverBorder: true,
                      zIndex: templateElements.length + index,
                      imageAltText: "Slide element",
                      showResizeHandles: shouldShowElementHandles(
                        isEditableCanvas,
                        selectedElementIdSet.has(element.id),
                        selectedElementIdSet.size
                      ),
                      renderInk: true,
                      renderGroups: true,
                      adjustmentHandles: isEditableCanvas && (selectedElement == null ? void 0 : selectedElement.id) === element.id ? getShapeAdjustmentHandleDescriptors(element) : EMPTY_ADJUSTMENT_HANDLES,
                      onResizePointerDown: stableResizePointerDown,
                      onAdjustmentPointerDown: stableAdjustmentPointerDown,
                      onRotate: stableRotate,
                      onInlineEditChange: stableInlineEditChange,
                      onInlineEditCommit: stableInlineEditCommit,
                      onInlineEditCancel: stableInlineEditCancel,
                      onUpdateSmartArtElement: stableUpdateSmartArtElement,
                      onFormatText: stableFormatText,
                      onTableCellSelect: stableTableCellSelect,
                      onCommitCellEdit: stableCommitCellEdit,
                      onResizeTableColumns: stableResizeTableColumns,
                      onResizeTableRow: stableResizeTableRow,
                      findHighlights: elementFindHighlightsMap.get(element.id),
                      onActionClick,
                      onHyperlinkClick,
                      animationState: presentationElementStates == null ? void 0 : presentationElementStates.get(element.id),
                      presentationElementStates,
                      allSlides,
                      onZoomClick,
                      sourceSlideIndex,
                      fieldContext,
                      tableStyleContext
                    },
                    element.id
                  )),
                  selectedElement && shouldShowElementHandles(isEditableCanvas, true, selectedElementIdSet.size) && !isConnectorOrLineElement(selectedElement) && (0, import_jsx_runtime.jsx)(
                    SelectionHandleOverlay,
                    {
                      element: selectedElement,
                      adjustmentHandles: getShapeAdjustmentHandleDescriptors(selectedElement),
                      onResizePointerDown: stableResizePointerDown,
                      onAdjustmentPointerDown: stableAdjustmentPointerDown,
                      onRotate: stableRotate,
                      onClick,
                      onDoubleClick,
                      onContextMenu
                    }
                  ),
                  (0, import_jsx_runtime.jsx)(MarqueeOverlay, { marqueeSelectionState }),
                  (activeSlide == null ? void 0 : activeSlide.activeXControls) && activeSlide.activeXControls.length > 0 && (0, import_jsx_runtime.jsx)(ActiveXControlOverlay, { controls: activeSlide.activeXControls, canvasSize }),
                  showCommentMarkers && comments && comments.length > 0 && (0, import_jsx_runtime.jsx)(
                    CommentMarkersOverlay,
                    {
                      comments,
                      canvasSize,
                      onCommentMarkerClick
                    }
                  ),
                  (0, import_jsx_runtime.jsx)(SnapLinesOverlay, { snapLines }),
                  isEditableCanvas && (selectedElement == null ? void 0 : selectedElement.type) === "connector" && activeSlide && (0, import_jsx_runtime.jsx)(
                    ConnectorEndpointOverlay,
                    {
                      connector: selectedElement,
                      elements: activeSlide.elements,
                      editorScale: zoom.editorScale,
                      canvasStageRef: zoom.canvasStageRef,
                      onUpdateElement: stableUpdateSmartArtElement
                    }
                  ),
                  connectorCreationMode && activeSlide && (0, import_jsx_runtime.jsx)(
                    ConnectorOverlay,
                    {
                      activeSlide,
                      canvasSize,
                      zoom,
                      connectorDragState,
                      onConnectionSiteDown: handleConnectionSiteDown,
                      onConnectorDragMove: handleConnectorDragMove,
                      onConnectionSiteDrop: handleConnectionSiteDrop,
                      onConnectorDragEnd: handleConnectorDragEnd
                    }
                  ),
                  isEditableCanvas && selectedElement && selectedMotionPath && (0, import_jsx_runtime.jsx)(
                    MotionPathOverlay,
                    {
                      element: selectedElement,
                      path: selectedMotionPath,
                      canvasSize,
                      scale: zoom.editorScale,
                      canEdit,
                      onChangePath: handleMotionPathChange
                    }
                  ),
                  isDrawing && (0, import_jsx_runtime.jsx)(
                    DrawingOverlaySvg,
                    {
                      canvasSize,
                      activeTool,
                      drawingColor,
                      drawingWidth,
                      isStrokeActive,
                      liveStrokeD,
                      liveStrokeView,
                      onPointerDown: handleDrawPointerDown,
                      onPointerMove: handleDrawPointerMove,
                      onPointerUp: handleDrawPointerUp
                    }
                  ),
                  collaborationOverlay
                ]
              }
            )
          ]
        }
      )
    }
  );
}
function ErrorState({ className, error }) {
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      role: "alert",
      "aria-live": "assertive",
      className: cn(
        "h-full w-full flex items-center justify-center p-6 text-center text-red-400",
        className
      ),
      children: error
    }
  );
}
function ShortcutPanel({ isOpen, onClose }) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-pptx-shortcuts-panel": "true",
      className: "absolute top-14 right-3 z-40 w-[min(24rem,calc(100%-1.5rem))] rounded border border-border bg-popover shadow-2xl",
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-xs uppercase tracking-wide text-foreground", children: t("pptx.shortcuts.title") }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "rounded px-2 py-1 text-[11px] text-foreground hover:bg-muted hover:text-foreground",
              children: t("pptx.shortcuts.close")
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: "max-h-64 overflow-y-auto p-2 space-y-1", children: SHORTCUT_REFERENCE_ITEMS2.map((shortcut) => (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: "flex items-center justify-between gap-3 rounded bg-muted/80 px-2 py-1.5",
            children: [
              (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-foreground", children: t(shortcut.actionKey) }),
              (0, import_jsx_runtime.jsx)("span", { className: "font-mono text-[11px] text-foreground whitespace-nowrap", children: shortcut.shortcut })
            ]
          },
          shortcut.actionKey
        )) })
      ]
    }
  );
}
function AccessibilityPanel({
  isOpen,
  issues,
  onClose,
  reducedMotion,
  onToggleReducedMotion
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      role: "dialog",
      "aria-label": t("pptx.accessibility.title"),
      className: "absolute top-14 right-3 z-40 w-[min(28rem,calc(100%-1.5rem))] rounded border border-border bg-popover shadow-2xl",
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-xs uppercase tracking-wide text-foreground", children: t("pptx.accessibility.title") }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.accessibility.issueCount", { count: issues.length }) }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onClose,
                "aria-label": t("pptx.accessibility.closePanel"),
                className: "rounded px-2 py-1 text-[11px] text-foreground hover:bg-muted hover:text-foreground",
                children: t("pptx.accessibility.close")
              }
            )
          ] })
        ] }),
        onToggleReducedMotion !== void 0 && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
          (0, import_jsx_runtime.jsx)("label", { htmlFor: "reduced-motion-toggle", className: "text-xs text-foreground", children: t("pptx.accessibility.reduceMotion") }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              id: "reduced-motion-toggle",
              type: "button",
              role: "switch",
              "aria-label": t("pptx.accessibility.reduceMotion"),
              "aria-checked": reducedMotion ?? false,
              onClick: onToggleReducedMotion,
              className: cn(
                "relative inline-flex h-5 w-9 items-center rounded-full transition-colors",
                reducedMotion ? "bg-primary" : "bg-muted-foreground/30"
              ),
              children: (0, import_jsx_runtime.jsx)(
                "span",
                {
                  className: cn(
                    "inline-block h-3.5 w-3.5 rounded-full bg-white transition-transform",
                    reducedMotion ? "translate-x-4" : "translate-x-0.5"
                  )
                }
              )
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            role: "list",
            "aria-label": t("pptx.accessibility.issuesList"),
            className: "max-h-72 overflow-y-auto p-2 space-y-1",
            children: issues.length === 0 ? (0, import_jsx_runtime.jsx)("div", { role: "listitem", className: "text-center text-xs text-muted-foreground py-4", children: t("pptx.accessibility.noIssues") }) : issues.map((issue, idx) => (0, import_jsx_runtime.jsxs)(
              "div",
              {
                role: "listitem",
                className: cn(
                  "flex items-start gap-2 rounded px-2 py-1.5 text-xs",
                  /* Severity-based background and text colouring */
                  issue.severity === "error" ? "bg-red-900/30 text-red-300" : issue.severity === "warning" ? "bg-amber-900/30 text-amber-300" : "bg-muted/50 text-muted-foreground"
                ),
                children: [
                  (0, import_jsx_runtime.jsx)("span", { className: "shrink-0 mt-0.5", "aria-hidden": "true", children: issue.severity === "error" ? "●" : issue.severity === "warning" ? "▲" : "ℹ" }),
                  (0, import_jsx_runtime.jsxs)("span", { children: [
                    issue.severity === "error" ? t("pptx.accessibility.error") : issue.severity === "warning" ? t("pptx.accessibility.warning") : t("pptx.accessibility.info"),
                    issue.message
                  ] })
                ]
              },
              idx
            ))
          }
        )
      ]
    }
  );
}
function ExportProgressModal({
  isOpen,
  title,
  progress,
  statusMessage,
  onCancel
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  const clampedProgress = clampPercent2(progress);
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/60 backdrop-blur-sm",
      children: (0, import_jsx_runtime.jsxs)("div", { className: "w-96 max-w-[calc(100%-2rem)] rounded-xl border border-border bg-background p-6 shadow-2xl", children: [
        (0, import_jsx_runtime.jsx)("h3", { className: "mb-4 text-sm font-semibold text-foreground", children: title }),
        (0, import_jsx_runtime.jsx)("div", { className: "mb-3 h-2.5 w-full overflow-hidden rounded-full bg-accent", children: (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "h-full rounded-full bg-primary transition-all duration-300 ease-out",
            style: { width: `${clampedProgress}%` }
          }
        ) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "mb-4 flex items-center justify-between text-xs text-muted-foreground", children: [
          (0, import_jsx_runtime.jsx)("span", { children: statusMessage ?? t("pptx.export.processing") }),
          (0, import_jsx_runtime.jsxs)("span", { className: "tabular-nums", children: [
            clampedProgress,
            "%"
          ] })
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex justify-end", children: (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onCancel,
            className: "rounded-md border border-border bg-muted px-4 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
            children: t("pptx.export.cancel")
          }
        ) })
      ] })
    }
  );
}
function formatAutosaveAge(timestamp, t) {
  const diff = Date.now() - timestamp;
  const minutes = Math.floor(diff / 6e4);
  if (minutes < 1) {
    return t("pptx.autosave.justNow");
  }
  if (minutes === 1) {
    return t("pptx.autosave.oneMinAgo");
  }
  return t("pptx.autosave.minutesAgo", { count: minutes });
}
function StatusBar({
  slideCount,
  activeSlideIndex,
  isDirty,
  autosaveStatus,
  scale,
  onZoomIn,
  onZoomOut,
  onZoomToFit,
  isNotesExpanded,
  onToggleNotes,
  mode,
  onSetMode,
  onToggleSlideSorter,
  collaborationSlot,
  hideZoomControls = false,
  hideNotesToggle = false,
  hideFullscreenToggle = false
}) {
  const { t } = useTranslation();
  let statusText;
  if ((autosaveStatus == null ? void 0 : autosaveStatus.state) === "saving") {
    statusText = t("pptx.autosave.saving");
  } else if ((autosaveStatus == null ? void 0 : autosaveStatus.state) === "saved") {
    statusText = t("pptx.autosave.saved", {
      time: formatAutosaveAge(autosaveStatus.timestamp, t)
    });
  } else if ((autosaveStatus == null ? void 0 : autosaveStatus.state) === "error") {
    statusText = t("pptx.autosave.error");
  } else if (isDirty) {
    statusText = t("pptx.statusBar.unsavedChanges");
  } else {
    statusText = t("pptx.statusBar.allSaved");
  }
  const vb = "p-1 rounded-sm transition-colors hover:bg-accent/60 text-muted-foreground active:scale-95 active:opacity-80";
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: cn(
        "w-full px-2 py-0.5 border-t border-border bg-secondary/50 text-[10px] text-muted-foreground flex items-center gap-1",
        // Pins the row height to the shared metric instead of letting it fall
        // out of the padding + button box, which is how the Vanilla and Svelte
        // ports ended up 2px shorter than this one.
        STATUS_BAR_CLASSES.container
      ),
      children: [
        (0, import_jsx_runtime.jsx)("span", { className: "shrink-0", children: slideCount > 0 ? t("pptx.statusBar.slideOf", {
          current: Math.min(activeSlideIndex + 1, slideCount),
          total: slideCount
        }) : t("pptx.statusBar.noSlides") }),
        (0, import_jsx_runtime.jsx)("div", { className: "w-px h-3 bg-border/40 mx-1 max-md:hidden" }),
        (0, import_jsx_runtime.jsx)("span", { className: "shrink-0 max-md:hidden text-[10px]", children: t("pptx.statusBar.language") }),
        (0, import_jsx_runtime.jsx)("div", { className: "w-px h-3 bg-border/60 mx-1 max-md:hidden" }),
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: cn(
              "shrink-0 max-md:hidden",
              (autosaveStatus == null ? void 0 : autosaveStatus.state) === "error" ? "text-red-400" : (autosaveStatus == null ? void 0 : autosaveStatus.state) === "saving" ? "text-yellow-400" : ""
            ),
            children: statusText
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { className: "flex-1" }),
        onToggleNotes && !hideNotesToggle && (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick: onToggleNotes,
            className: cn(
              vb,
              "flex items-center gap-1 text-[10px]",
              isNotesExpanded && "text-primary"
            ),
            title: t("pptx.statusBar.toggleNotes"),
            "aria-label": t("pptx.statusBar.toggleNotes"),
            children: [
              (0, import_jsx_runtime.jsx)(LuStickyNote, { className: "w-3 h-3" }),
              (0, import_jsx_runtime.jsx)("span", { className: "max-md:hidden", children: t("pptx.notes.title") })
            ]
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { className: "w-px h-3 bg-border/60 mx-0.5" }),
        onSetMode && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-0.5", children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: () => onSetMode("edit"),
              className: cn(vb, mode === "edit" && "text-primary"),
              title: t("pptx.statusBar.normalView"),
              "aria-label": t("pptx.statusBar.normalView"),
              children: (0, import_jsx_runtime.jsx)(LuMonitor, { className: "w-3.5 h-3.5" })
            }
          ),
          onToggleSlideSorter && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onToggleSlideSorter,
              className: vb,
              title: t("pptx.statusBar.slideSorter"),
              "aria-label": t("pptx.statusBar.slideSorter"),
              children: (0, import_jsx_runtime.jsx)(LuColumns2, { className: "w-3.5 h-3.5" })
            }
          ),
          !hideFullscreenToggle && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: () => onSetMode("present"),
              className: cn(vb, mode === "present" && "text-primary"),
              title: t("pptx.statusBar.slideShow"),
              "aria-label": t("pptx.statusBar.slideShow"),
              children: (0, import_jsx_runtime.jsx)(LuPresentation, { className: "w-3.5 h-3.5" })
            }
          )
        ] }),
        collaborationSlot && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)("div", { className: "w-px h-3 bg-border/40 mx-0.5" }),
          collaborationSlot
        ] }),
        scale !== void 0 && !hideZoomControls && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)("div", { className: "w-px h-3 bg-border/60 mx-0.5" }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-0.5", children: [
            onZoomOut && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onZoomOut,
                className: vb,
                title: t("pptx.statusBar.zoomOut"),
                "aria-label": t("pptx.statusBar.zoomOut"),
                children: (0, import_jsx_runtime.jsx)(LuMinus, { className: "w-3 h-3" })
              }
            ),
            (0, import_jsx_runtime.jsxs)(
              "button",
              {
                type: "button",
                onClick: onZoomToFit,
                className: "px-1.5 py-0.5 rounded-sm hover:bg-accent/60 text-[10px] text-muted-foreground tabular-nums min-w-[3rem] text-center transition-colors",
                title: t("pptx.statusBar.zoomToFit"),
                children: [
                  Math.round((scale ?? 1) * 100),
                  "%"
                ]
              }
            ),
            onZoomIn && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onZoomIn,
                className: vb,
                title: t("pptx.statusBar.zoomIn"),
                "aria-label": t("pptx.statusBar.zoomIn"),
                children: (0, import_jsx_runtime.jsx)(LuPlus, { className: "w-3 h-3" })
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function ResizeHandle({
  direction,
  onResize,
  onResizeEnd,
  className
}) {
  const startPos = (0, import_react2.useRef)(0);
  const handlePointerDown = (0, import_react2.useCallback)(
    (e) => {
      e.preventDefault();
      e.stopPropagation();
      const target = e.currentTarget;
      target.setPointerCapture(e.pointerId);
      startPos.current = direction === "horizontal" ? e.clientX : e.clientY;
      const handlePointerMove = (ev) => {
        const current = direction === "horizontal" ? ev.clientX : ev.clientY;
        const delta = current - startPos.current;
        startPos.current = current;
        onResize(delta);
      };
      const handlePointerUp = () => {
        target.removeEventListener("pointermove", handlePointerMove);
        target.removeEventListener("pointerup", handlePointerUp);
        target.removeEventListener("pointercancel", handlePointerUp);
        onResizeEnd == null ? void 0 : onResizeEnd();
      };
      target.addEventListener("pointermove", handlePointerMove);
      target.addEventListener("pointerup", handlePointerUp);
      target.addEventListener("pointercancel", handlePointerUp);
    },
    [direction, onResize, onResizeEnd]
  );
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      role: "separator",
      "aria-orientation": direction,
      className: cn(
        // Outer container: generous hit area for easy clicking.
        // The visible indicator is rendered via the inner pseudo-element (::after).
        "relative flex-shrink-0 z-20 group",
        direction === "horizontal" ? "w-3 cursor-col-resize -mx-1" : "h-3 cursor-row-resize -my-1",
        className
      ),
      onPointerDown: handlePointerDown,
      children: (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: cn(
            "absolute transition-colors",
            direction === "horizontal" ? "top-0 bottom-0 left-1/2 -translate-x-1/2 w-0.5 group-hover:w-1 group-hover:bg-primary/40 group-active:w-1 group-active:bg-primary/60" : "left-0 right-0 top-1/2 -translate-y-1/2 h-0.5 group-hover:h-1 group-hover:bg-primary/40 group-active:h-1 group-active:bg-primary/60"
          )
        }
      )
    }
  );
}
function SlideThumbnailImpl({
  slide,
  templateElements,
  canvasSize,
  fieldContext,
  tableStyleContext
}) {
  const safeCanvasWidth = Math.max(canvasSize.width, 1);
  const safeCanvasHeight = Math.max(canvasSize.height, 1);
  const scale = SLIDE_NAV_THUMBNAIL_WIDTH / safeCanvasWidth;
  const previewHeight = Math.max(56, Math.round(safeCanvasHeight * scale));
  const previewElements = buildPreviewElements(slide, templateElements);
  const slideFieldContext = deriveSlideFieldContext(fieldContext, slide);
  const { t } = useTranslation();
  const transitionLabel = slide.transition ? (() => {
    const opt = SLIDE_TRANSITION_OPTIONS.find((o) => {
      var _a;
      return o.value === ((_a = slide.transition) == null ? void 0 : _a.type);
    });
    return opt ? t(opt.i18nKey) : slide.transition.type;
  })() : void 0;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "relative w-full overflow-hidden rounded border border-border bg-white",
      style: {
        height: previewHeight,
        ...getReactSlideBackgroundStyle(slide, {
          widthPx: safeCanvasWidth,
          heightPx: safeCanvasHeight
        })
      },
      children: (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "absolute top-0 left-0 origin-top-left",
          style: {
            width: safeCanvasWidth,
            height: safeCanvasHeight,
            transform: `scale(${scale})`,
            transformOrigin: "top left"
          },
          children: [
            (0, import_jsx_runtime.jsx)(SlideBackgroundImageLayer, { slide }),
            slide.transition && slide.transition.type !== "none" && slide.transition.type !== "cut" && (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute top-0.5 right-0.5 z-10 px-1 py-px rounded bg-primary/80 text-[7px] text-white leading-tight pointer-events-none",
                title: t("pptx.slideThumbnail.transitionTooltip", { name: transitionLabel }),
                children: transitionLabel
              }
            ),
            previewElements.map((element, index) => (0, import_jsx_runtime.jsx)(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index,
                fieldContext: slideFieldContext,
                tableStyleContext
              },
              element.id
            ))
          ]
        }
      )
    }
  );
}
function arePropsEqual2(prev, next) {
  if (prev.slide.id !== next.slide.id) {
    return false;
  }
  if (prev.slide.isDirty !== next.slide.isDirty) {
    return false;
  }
  if (prev.slide.hidden !== next.slide.hidden) {
    return false;
  }
  if (prev.slide.elements !== next.slide.elements) {
    return false;
  }
  if (prev.slide.backgroundColor !== next.slide.backgroundColor) {
    return false;
  }
  if (prev.slide.backgroundImage !== next.slide.backgroundImage) {
    return false;
  }
  if (prev.slide.backgroundGradient !== next.slide.backgroundGradient) {
    return false;
  }
  if (prev.slide.backgroundPattern !== next.slide.backgroundPattern) {
    return false;
  }
  if (prev.templateElements !== next.templateElements) {
    return false;
  }
  if (prev.fieldContext !== next.fieldContext) {
    return false;
  }
  if (prev.tableStyleContext !== next.tableStyleContext) {
    return false;
  }
  if (prev.canvasSize.width !== next.canvasSize.width || prev.canvasSize.height !== next.canvasSize.height) {
    return false;
  }
  return true;
}
var SlideThumbnail = import_react2.default.memo(SlideThumbnailImpl, arePropsEqual2);
function tableCell(props) {
  var _a, _b2;
  const { selectedElement, tableEditorState } = props;
  if ((selectedElement == null ? void 0 : selectedElement.type) !== "table" || tableEditorState === null) {
    return null;
  }
  const rows = (_a = selectedElement.tableData) == null ? void 0 : _a.rows;
  const cell = (_b2 = rows == null ? void 0 : rows[tableEditorState.rowIndex]) == null ? void 0 : _b2.cells[tableEditorState.columnIndex];
  return {
    hasMultiCellSelection: Array.isArray(tableEditorState.selectedCells) && tableEditorState.selectedCells.length >= 2,
    isMergedCell: Boolean(cell && ((cell.gridSpan ?? 1) > 1 || (cell.rowSpan ?? 1) > 1))
  };
}
function contextMenuContext(props) {
  var _a;
  return {
    elementType: ((_a = props.selectedElement) == null ? void 0 : _a.type) ?? null,
    table: tableCell(props),
    hasMultiSelection: Boolean(props.hasMultiSelection),
    selectionGroupable: props.selectionGroupable,
    // Both AI entries appear together or not at all, so a viewer that wired
    // only one of them does not produce a menu the other bindings cannot match.
    aiEnabled: Boolean(props.onAskAi) && Boolean(props.onFixAi)
  };
}
function contextMenuHandlers(props) {
  const { onAction, onClose } = props;
  const andClose = (run) => run && (() => {
    run();
    onClose();
  });
  return {
    copy: andClose(() => onAction("copy")),
    cut: andClose(() => onAction("cut")),
    paste: andClose(() => onAction("paste")),
    duplicate: andClose(() => onAction("duplicate")),
    "bring-forward": andClose(() => onAction("bring-forward")),
    "send-backward": andClose(() => onAction("send-backward")),
    "bring-front": andClose(() => onAction("bring-front")),
    "send-back": andClose(() => onAction("send-back")),
    "ai-ask": andClose(props.onAskAi),
    "ai-fix": andClose(props.onFixAi),
    comment: andClose(() => onAction("comment")),
    hyperlink: andClose(() => onAction("editHyperlink")),
    "table-insert-row-above": andClose(() => props.onInsertTableRow("above")),
    "table-insert-row-below": andClose(() => props.onInsertTableRow("below")),
    "table-delete-row": andClose(props.onDeleteTableRow),
    "table-insert-col-left": andClose(() => props.onInsertTableColumn("left")),
    "table-insert-col-right": andClose(() => props.onInsertTableColumn("right")),
    "table-delete-col": andClose(props.onDeleteTableColumn),
    "table-merge-selected": andClose(props.onMergeSelectedCells),
    "table-merge-right": andClose(props.onMergeCellRight),
    "table-merge-down": andClose(props.onMergeCellDown),
    "table-split": andClose(props.onSplitCell),
    group: andClose(() => onAction("group")),
    ungroup: andClose(() => onAction("ungroup")),
    delete: andClose(() => onAction("delete"))
  };
}
var ITEM_CLASS = "w-full px-3 py-1.5 text-left hover:bg-muted";
var DANGER_ITEM_CLASS = "w-full px-3 py-1.5 text-left text-red-300 hover:bg-red-900/40";
var DISABLED_CLASS = " opacity-40 pointer-events-none";
function ContextMenuItem({
  onSelect,
  danger,
  disabled,
  children
}) {
  return (0, import_jsx_runtime.jsx)(
    "button",
    {
      type: "button",
      role: "menuitem",
      disabled,
      className: `${danger ? DANGER_ITEM_CLASS : ITEM_CLASS}${disabled ? DISABLED_CLASS : ""}`,
      onClick: onSelect,
      children
    }
  );
}
function ContextMenuSeparator() {
  return (0, import_jsx_runtime.jsx)("div", { role: "separator", "aria-orientation": "horizontal", className: "my-1 border-t border-border" });
}
function ContextMenu(props) {
  const { contextMenuState, mode, onClose } = props;
  const { t } = useTranslation();
  if (!contextMenuState || mode !== "edit") {
    return null;
  }
  const handlers = contextMenuHandlers(props);
  const entries = buildContextMenuEntries(contextMenuContext(props));
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "fixed inset-0 z-[119]",
        onClick: onClose,
        onContextMenu: (e) => {
          e.preventDefault();
          onClose();
        }
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        "data-pptx-context-menu": "true",
        role: "menu",
        "aria-label": t("pptx.contextMenu.ariaLabel"),
        className: "fixed z-[120] min-w-[180px] rounded border border-border bg-popover shadow-2xl py-1.5 text-xs text-foreground",
        style: {
          left: Math.max(contextMenuState.x, 8),
          top: Math.max(contextMenuState.y, 8)
        },
        children: entries.map((entry) => {
          const run = handlers[entry.id];
          return (0, import_jsx_runtime.jsxs)(import_react2.Fragment, { children: [
            entry.separatorBefore && (0, import_jsx_runtime.jsx)(ContextMenuSeparator, {}),
            (0, import_jsx_runtime.jsx)(
              ContextMenuItem,
              {
                danger: entry.danger,
                disabled: entry.disabled || !run,
                onSelect: () => run == null ? void 0 : run(),
                children: t(entry.labelKey)
              }
            )
          ] }, entry.id);
        })
      }
    )
  ] });
}
var ic2 = "w-3.5 h-3.5";
var btnBase = "inline-flex items-center justify-center rounded p-1 transition-colors disabled:opacity-40 disabled:cursor-not-allowed";
var btnGhost = `${btnBase} hover:bg-accent text-muted-foreground hover:text-foreground`;
var btnAction = `${btnBase} px-2 py-1 bg-muted hover:bg-accent text-foreground text-xs`;
function FindReplacePanel({
  findQuery,
  replaceQuery,
  findMatchCase,
  findResults,
  findResultIndex,
  onSetFindQuery,
  onSetReplaceQuery,
  onSetFindMatchCase,
  onPerformFind,
  onNavigateResult,
  onReplace,
  onReplaceAll,
  onClose
}) {
  const { t } = useTranslation();
  const searchInputRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    var _a;
    (_a = searchInputRef.current) == null ? void 0 : _a.focus();
  }, []);
  (0, import_react2.useEffect)(() => {
    onPerformFind();
  }, [findQuery, findMatchCase]);
  const handleSearchKeyDown = (0, import_react2.useCallback)(
    (event) => {
      if (event.key === "Escape") {
        onClose();
      } else if (event.key === "Enter") {
        if (event.shiftKey) {
          onNavigateResult(-1);
        } else {
          onNavigateResult(1);
        }
      }
    },
    [onClose, onNavigateResult]
  );
  const handleReplaceKeyDown = (0, import_react2.useCallback)(
    (event) => {
      if (event.key === "Escape") {
        onClose();
      } else if (event.key === "Enter") {
        onReplace();
      }
    },
    [onClose, onReplace]
  );
  const hasResults = findResults.length > 0;
  const matchCountLabel = hasResults ? t("pptx.findReplace.matchCount", { current: findResultIndex + 1, total: findResults.length }) : findQuery.length > 0 ? t("pptx.findReplace.noMatches") : "";
  return (0, import_jsx_runtime.jsxs)("div", { className: "absolute top-2 right-2 z-40 bg-popover border border-border rounded-lg shadow-lg p-3 text-xs text-foreground w-80 backdrop-blur", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between mb-2", children: [
      (0, import_jsx_runtime.jsxs)("span", { className: "font-semibold text-sm inline-flex items-center gap-1.5", children: [
        (0, import_jsx_runtime.jsx)(LuSearch, { className: ic2 }),
        t("pptx.findReplace.title")
      ] }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: btnGhost,
          onClick: onClose,
          title: t("pptx.findReplace.closeEscape"),
          "aria-label": t("pptx.findReplace.closeAriaLabel"),
          children: (0, import_jsx_runtime.jsx)(LuX, { className: ic2 })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 mb-1.5", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "relative flex-1", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            ref: searchInputRef,
            type: "text",
            value: findQuery,
            onChange: (e) => onSetFindQuery(e.target.value),
            onKeyDown: handleSearchKeyDown,
            placeholder: t("pptx.findReplace.findPlaceholder"),
            className: "w-full bg-muted border border-border rounded px-2 py-1 pr-7 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:outline-none",
            "aria-label": t("pptx.findReplace.searchText")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: `absolute right-1 top-1/2 -translate-y-1/2 p-0.5 rounded transition-colors ${findMatchCase ? "bg-primary/80 text-white" : "text-muted-foreground hover:text-foreground hover:bg-accent"}`,
            onClick: () => onSetFindMatchCase(!findMatchCase),
            title: t("pptx.findReplace.matchCase"),
            "aria-label": t("pptx.findReplace.toggleMatchCase"),
            "aria-pressed": findMatchCase,
            children: (0, import_jsx_runtime.jsx)(LuCaseSensitive, { className: ic2 })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: btnGhost,
          onClick: () => onNavigateResult(-1),
          disabled: !hasResults,
          title: t("pptx.findReplace.previousMatch"),
          "aria-label": t("pptx.findReplace.previousMatch"),
          children: (0, import_jsx_runtime.jsx)(LuChevronUp, { className: ic2 })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: btnGhost,
          onClick: () => onNavigateResult(1),
          disabled: !hasResults,
          title: t("pptx.findReplace.nextMatch"),
          "aria-label": t("pptx.findReplace.nextMatch"),
          children: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: ic2 })
        }
      )
    ] }),
    matchCountLabel && (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground mb-2 pl-0.5 tabular-nums", children: matchCountLabel }),
    (0, import_jsx_runtime.jsx)("div", { className: "flex items-center gap-1 mb-2", children: (0, import_jsx_runtime.jsxs)("div", { className: "relative flex-1", children: [
      (0, import_jsx_runtime.jsx)(LuReplace, { className: "absolute left-2 top-1/2 -translate-y-1/2 w-3 h-3 text-muted-foreground" }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          value: replaceQuery,
          onChange: (e) => onSetReplaceQuery(e.target.value),
          onKeyDown: handleReplaceKeyDown,
          placeholder: t("pptx.findReplace.replacePlaceholder"),
          className: "w-full bg-muted border border-border rounded pl-7 pr-2 py-1 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:outline-none",
          "aria-label": t("pptx.findReplace.replacementText")
        }
      )
    ] }) }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1.5", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: btnAction,
          onClick: onReplace,
          disabled: !hasResults,
          title: t("pptx.findReplace.replaceCurrent"),
          children: t("pptx.findReplace.replace")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: btnAction,
          onClick: onReplaceAll,
          disabled: !hasResults,
          title: t("pptx.findReplace.replaceAllMatches"),
          children: t("pptx.findReplace.replaceAll")
        }
      )
    ] })
  ] });
}
function SectionContextMenu({
  state,
  sectionGroups,
  totalSlides,
  onStartRename,
  onDeleteSection,
  onMoveSectionUp,
  onMoveSectionDown,
  onAddSection,
  onClose
}) {
  const { t } = useTranslation();
  const handleAddSectionAfter = () => {
    const group = sectionGroups.find((g) => g.id === state.sectionId);
    if (!group) {
      return;
    }
    const lastSlideIndex = group.slideIndexes[group.slideIndexes.length - 1] ?? 0;
    const nextSlideIndex = Math.min(lastSlideIndex + 1, totalSlides - 1);
    onAddSection == null ? void 0 : onAddSection(t("pptx.sections.defaultName"), nextSlideIndex);
    onClose();
  };
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "fixed z-50 min-w-[160px] rounded-md border border-border bg-popover py-1 shadow-xl",
      style: { left: state.x, top: state.y },
      onClick: (e) => e.stopPropagation(),
      children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
            onClick: () => {
              const group = sectionGroups.find((g) => g.id === state.sectionId);
              if (group) {
                onStartRename(state.sectionId, group.label);
              }
            },
            children: t("pptx.sections.rename")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
            onClick: () => {
              onDeleteSection == null ? void 0 : onDeleteSection(state.sectionId);
              onClose();
            },
            children: t("pptx.sections.delete")
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border" }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted disabled:opacity-40 disabled:cursor-not-allowed",
            disabled: state.sectionIndex === 0,
            onClick: () => {
              onMoveSectionUp == null ? void 0 : onMoveSectionUp(state.sectionId);
              onClose();
            },
            children: t("pptx.sections.moveUp")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted disabled:opacity-40 disabled:cursor-not-allowed",
            disabled: state.sectionIndex >= state.totalSections - 1,
            onClick: () => {
              onMoveSectionDown == null ? void 0 : onMoveSectionDown(state.sectionId);
              onClose();
            },
            children: t("pptx.sections.moveDown")
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border" }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
            onClick: handleAddSectionAfter,
            children: t("pptx.sections.addAfter")
          }
        )
      ]
    }
  );
}
function SectionHeader({
  sectionId,
  label,
  slideCount,
  isCollapsed,
  isRenaming,
  renameValue,
  canEdit,
  sectionIndex,
  totalSections,
  renameInputRef,
  onToggle,
  onContextMenu,
  onStartRename,
  onRenameValueChange,
  onCommitRename,
  onCancelRename
}) {
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      className: "flex w-full items-center gap-1 rounded px-1.5 py-1 text-[11px] uppercase tracking-wide text-muted-foreground hover:bg-muted/60 hover:text-foreground",
      onClick: () => onToggle(sectionId, isCollapsed),
      onContextMenu: (e) => canEdit ? onContextMenu(e, sectionId, sectionIndex, totalSections) : void 0,
      onDoubleClick: (e) => {
        if (!canEdit) {
          return;
        }
        e.stopPropagation();
        onStartRename(sectionId, label);
      },
      children: [
        (0, import_jsx_runtime.jsx)(
          LuChevronDown,
          {
            className: cn(
              "h-3 w-3 flex-shrink-0 transition-transform",
              isCollapsed ? "-rotate-90" : "rotate-0"
            )
          }
        ),
        isRenaming ? (0, import_jsx_runtime.jsx)(
          "input",
          {
            ref: renameInputRef,
            type: "text",
            className: "flex-1 bg-muted text-[11px] text-foreground rounded px-1 py-0.5 outline-none border border-primary",
            value: renameValue,
            onChange: (e) => onRenameValueChange(e.target.value),
            onKeyDown: (e) => {
              if (e.key === "Enter") {
                e.preventDefault();
                onCommitRename();
              } else if (e.key === "Escape") {
                e.preventDefault();
                onCancelRename();
              }
              e.stopPropagation();
            },
            onClick: (e) => e.stopPropagation(),
            onBlur: onCommitRename
          }
        ) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)("span", { className: "truncate text-left", children: label }),
          (0, import_jsx_runtime.jsx)("span", { className: "ml-auto text-[10px] text-muted-foreground", children: slideCount })
        ] })
      ]
    }
  );
}
function SlideContextMenu({
  state,
  onAddSection,
  onClose
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "fixed z-50 min-w-[180px] rounded-md border border-border bg-popover py-1 shadow-xl",
      style: { left: state.x, top: state.y },
      onClick: (e) => e.stopPropagation(),
      children: (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "flex w-full items-center px-3 py-1.5 text-xs text-foreground hover:bg-muted",
          onClick: () => {
            onAddSection == null ? void 0 : onAddSection(t("pptx.sections.defaultName"), state.slideIndex);
            onClose();
          },
          children: t("pptx.sections.addBefore")
        }
      )
    }
  );
}
function LazyThumbnailInner({
  slide,
  templateElements,
  canvasSize,
  previewHeight,
  fieldContext,
  tableStyleContext
}) {
  const containerRef = (0, import_react2.useRef)(null);
  const [isVisible, setIsVisible] = (0, import_react2.useState)(false);
  (0, import_react2.useEffect)(() => {
    const el = containerRef.current;
    if (!el) {
      return;
    }
    if (typeof IntersectionObserver === "undefined") {
      setIsVisible(true);
      return;
    }
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry == null ? void 0 : entry.isIntersecting) {
          setIsVisible(true);
          observer.disconnect();
        }
      },
      {
        // Pre-render thumbnails 200px before they enter the viewport
        rootMargin: "200px 0px"
      }
    );
    observer.observe(el);
    return () => {
      observer.disconnect();
    };
  }, []);
  return (0, import_jsx_runtime.jsx)("div", { ref: containerRef, children: isVisible ? (0, import_jsx_runtime.jsx)(
    SlideThumbnail,
    {
      slide,
      templateElements,
      canvasSize,
      fieldContext,
      tableStyleContext
    }
  ) : (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "relative w-full overflow-hidden rounded border border-border bg-muted/30 animate-pulse",
      style: {
        height: previewHeight,
        minWidth: SLIDE_NAV_THUMBNAIL_WIDTH
      }
    }
  ) });
}
var LazyThumbnail = import_react2.default.memo(LazyThumbnailInner);
function formatTimingMs(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
}
function buildFlatPaneItems(sectionGroups, showSectionHeaders, collapsedSections) {
  const items = [];
  for (let si = 0; si < sectionGroups.length; si++) {
    const section = sectionGroups[si];
    if (showSectionHeaders) {
      items.push({
        type: "section",
        sectionIndex: si,
        sectionId: section.id
      });
    }
    const isCollapsed = collapsedSections[section.id] ?? false;
    if (!isCollapsed) {
      for (const idx of section.slideIndexes) {
        items.push({ type: "slide", slideIndex: idx });
      }
    }
  }
  return items;
}
function estimateSlideItemHeight(canvasWidth, canvasHeight) {
  const safeW = Math.max(canvasWidth, 1);
  const safeH = Math.max(canvasHeight, 1);
  const scale = SLIDE_NAV_THUMBNAIL_WIDTH / safeW;
  const previewHeight = Math.max(56, Math.round(safeH * scale));
  return previewHeight + 30;
}
function SlideItemInner({
  slide,
  templateElements,
  slideIndex,
  isActive,
  canvasSize,
  canEdit,
  rehearsalTimings,
  presenceUsers,
  fieldContext,
  tableStyleContext,
  onSelectSlide,
  onSlideContextMenu,
  onAddSection,
  onOpenSlideCtxMenu,
  onDragStart,
  onDragOver,
  onDrop,
  slideRef
}) {
  var _a, _b2;
  const { t } = useTranslation();
  const isHidden = Boolean(slide.hidden);
  const cue = hiddenSlideCue(slide.hidden, "rail", slideIndex);
  const handleContextMenu = (0, import_react2.useCallback)(
    (e) => {
      if (canEdit && onAddSection) {
        e.preventDefault();
        e.stopPropagation();
        onOpenSlideCtxMenu(e.clientX, e.clientY, slideIndex);
      } else {
        onSlideContextMenu(e, slideIndex);
      }
    },
    [canEdit, onAddSection, onOpenSlideCtxMenu, onSlideContextMenu, slideIndex]
  );
  const safeCanvasWidth = Math.max(canvasSize.width, 1);
  const safeCanvasHeight = Math.max(canvasSize.height, 1);
  const scale = SLIDE_NAV_THUMBNAIL_WIDTH / safeCanvasWidth;
  const previewHeight = Math.max(56, Math.round(safeCanvasHeight * scale));
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      ref: slideRef,
      "aria-label": `Go to slide ${slideIndex + 1}`,
      "aria-current": isActive ? "true" : void 0,
      "aria-describedby": cue.labelId,
      "data-pptx-slide-hidden": cue.marker,
      className: cn(
        "group relative flex w-full items-center gap-1 cursor-pointer border-0 bg-transparent py-0.5 px-1 text-left transition-all",
        isActive && "bg-accent/40 before:absolute before:left-0 before:top-1 before:bottom-1 before:w-[3px] before:bg-primary before:rounded-r",
        isHidden && "opacity-50"
      ),
      draggable: canEdit,
      onClick: () => onSelectSlide(slideIndex),
      onContextMenu: handleContextMenu,
      onDragStart: (e) => onDragStart(e, slideIndex),
      onDragOver,
      onDrop: (e) => onDrop(e, slideIndex),
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5 w-5 shrink-0", children: [
          (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: cn(
                "text-[10px] tabular-nums text-right select-none w-full",
                isActive ? "text-primary font-medium" : "text-muted-foreground"
              ),
              style: isHidden ? { backgroundImage: HIDDEN_SLIDE_SLASH_GRADIENT } : void 0,
              children: slideIndex + 1
            }
          ),
          presenceUsers && presenceUsers.length > 0 && (0, import_jsx_runtime.jsx)("div", { className: "flex flex-wrap justify-center gap-px", children: presenceUsers.slice(0, 4).map((u, i) => (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: "w-[6px] h-[6px] rounded-full",
              style: { backgroundColor: u.userColor },
              title: u.userName
            },
            i
          )) })
        ] }),
        (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: cn(
              "relative flex-1 overflow-hidden border transition-colors bg-white",
              isActive ? "border-primary/60" : "border-transparent group-hover:border-border/40"
            ),
            children: [
              isHidden && (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 pointer-events-none z-10 bg-[repeating-linear-gradient(135deg,transparent,transparent_4px,rgba(255,255,255,0.04)_4px,rgba(255,255,255,0.04)_8px)]" }),
              (0, import_jsx_runtime.jsx)(
                LazyThumbnail,
                {
                  slide,
                  templateElements,
                  canvasSize,
                  previewHeight,
                  fieldContext,
                  tableStyleContext
                }
              ),
              (((_a = slide.comments) == null ? void 0 : _a.length) ?? 0) > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "absolute top-0.5 right-0.5 flex items-center gap-0.5 rounded bg-amber-500/90 px-1 py-0.5 text-[8px] font-medium text-white leading-none z-10", children: [
                (0, import_jsx_runtime.jsx)(LuMessageSquare, { className: "w-2 h-2" }),
                (_b2 = slide.comments) == null ? void 0 : _b2.length
              ] }),
              isHidden && (0, import_jsx_runtime.jsxs)("div", { className: "absolute bottom-0.5 right-0.5 z-10", id: cue.labelId, children: [
                (0, import_jsx_runtime.jsx)(LuEyeOff, { className: "w-3 h-3 text-muted-foreground" }),
                (0, import_jsx_runtime.jsx)("span", { className: "sr-only", children: t(HIDDEN_SLIDE_LABEL_KEY) })
              ] }),
              rehearsalTimings && typeof rehearsalTimings[slideIndex] === "number" && (0, import_jsx_runtime.jsx)("div", { className: "absolute bottom-0.5 left-0.5 z-10", children: (0, import_jsx_runtime.jsx)("span", { className: "text-[8px] font-mono text-amber-400/80 tabular-nums bg-black/50 px-0.5 rounded", children: formatTimingMs(rehearsalTimings[slideIndex]) }) })
            ]
          }
        )
      ]
    }
  );
}
var SlideItem = import_react2.default.memo(SlideItemInner);
function useSlidePaneCallbacks(onMoveSlide, onRenameSection, onToggleSectionCollapse) {
  const [collapsedSections, setCollapsedSections] = (0, import_react2.useState)({});
  const [renamingSectionId, setRenamingSectionId] = (0, import_react2.useState)(null);
  const [renameValue, setRenameValue] = (0, import_react2.useState)("");
  const [sectionContextMenu, setSectionContextMenu] = (0, import_react2.useState)(
    null
  );
  const [slideCtxMenu, setSlideCtxMenu] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    if (!sectionContextMenu && !slideCtxMenu) {
      return;
    }
    const handler = () => {
      setSectionContextMenu(null);
      setSlideCtxMenu(null);
    };
    document.addEventListener("click", handler);
    return () => document.removeEventListener("click", handler);
  }, [sectionContextMenu, slideCtxMenu]);
  const handleDragStart = (0, import_react2.useCallback)((e, slideIndex) => {
    e.dataTransfer.setData("text/plain", String(slideIndex));
    e.dataTransfer.effectAllowed = "move";
  }, []);
  const handleDragOver = (0, import_react2.useCallback)((e) => {
    e.preventDefault();
    e.dataTransfer.dropEffect = "move";
  }, []);
  const handleDrop = (0, import_react2.useCallback)(
    (e, toIndex) => {
      e.preventDefault();
      const fromStr = e.dataTransfer.getData("text/plain");
      const fromIndex = parseInt(fromStr, 10);
      if (!isNaN(fromIndex) && fromIndex !== toIndex) {
        onMoveSlide(fromIndex, toIndex);
      }
    },
    [onMoveSlide]
  );
  const toggleSection = (0, import_react2.useCallback)(
    (sectionId, isCollapsed) => {
      setCollapsedSections((prev) => ({
        ...prev,
        [sectionId]: !isCollapsed
      }));
      onToggleSectionCollapse == null ? void 0 : onToggleSectionCollapse(sectionId);
    },
    [onToggleSectionCollapse]
  );
  const startRename = (0, import_react2.useCallback)((sectionId, currentLabel) => {
    setRenamingSectionId(sectionId);
    setRenameValue(currentLabel);
    setSectionContextMenu(null);
  }, []);
  const commitRename = (0, import_react2.useCallback)(() => {
    if (renamingSectionId && renameValue.trim().length > 0) {
      onRenameSection == null ? void 0 : onRenameSection(renamingSectionId, renameValue.trim());
    }
    setRenamingSectionId(null);
    setRenameValue("");
  }, [renamingSectionId, renameValue, onRenameSection]);
  const cancelRename = (0, import_react2.useCallback)(() => {
    setRenamingSectionId(null);
    setRenameValue("");
  }, []);
  const handleSectionContextMenu = (0, import_react2.useCallback)(
    (e, sectionId, sectionIndex, totalSections) => {
      e.preventDefault();
      e.stopPropagation();
      setSectionContextMenu({
        x: e.clientX,
        y: e.clientY,
        sectionId,
        sectionIndex,
        totalSections
      });
    },
    []
  );
  const handleOpenSlideCtxMenu = (0, import_react2.useCallback)((x, y, slideIndex) => {
    setSlideCtxMenu({ x, y, slideIndex });
  }, []);
  const closeSectionContextMenu = (0, import_react2.useCallback)(() => {
    setSectionContextMenu(null);
  }, []);
  const closeSlideCtxMenu = (0, import_react2.useCallback)(() => {
    setSlideCtxMenu(null);
  }, []);
  return {
    collapsedSections,
    renamingSectionId,
    renameValue,
    sectionContextMenu,
    slideCtxMenu,
    setRenameValue,
    handleDragStart,
    handleDragOver,
    handleDrop,
    toggleSection,
    startRename,
    commitRename,
    cancelRename,
    handleSectionContextMenu,
    handleOpenSlideCtxMenu,
    closeSectionContextMenu,
    closeSlideCtxMenu
  };
}
var EMPTY_TEMPLATE_ELEMENTS = [];
var VIRTUALIZATION_THRESHOLD = SLIDE_VIRTUALIZATION_THRESHOLD;
function SlidesPaneSidebar({
  slides,
  templateElementsBySlideId,
  activeSlideIndex,
  canvasSize,
  sectionGroups,
  isOpen,
  canEdit,
  onSelectSlide,
  onSlideContextMenu,
  onMoveSlide,
  onAddSlide,
  onCollapse: _onCollapse,
  onAddSection,
  onRenameSection,
  onDeleteSection,
  onMoveSectionUp,
  onMoveSectionDown,
  onToggleSectionCollapse,
  rehearsalTimings,
  panelWidth,
  fieldContext,
  tableStyleContext
}) {
  const { t } = useTranslation();
  const collab = useCollaboration();
  const slideRefs = (0, import_react2.useRef)(/* @__PURE__ */ new Map());
  const renameInputRef = (0, import_react2.useRef)(null);
  const slidePresenceMap = (0, import_react2.useMemo)(() => {
    if (!collab || collab.remoteUsers.length === 0) {
      return void 0;
    }
    const map = /* @__PURE__ */ new Map();
    for (const user of collab.remoteUsers) {
      const idx = user.activeSlideIndex;
      const existing = map.get(idx);
      const entry = { userName: user.userName, userColor: user.userColor };
      if (existing) {
        existing.push(entry);
      } else {
        map.set(idx, [entry]);
      }
    }
    return map;
  }, [collab]);
  const estimatedItemHeight = (0, import_react2.useMemo)(
    () => estimateSlideItemHeight(canvasSize.width, canvasSize.height),
    [canvasSize.width, canvasSize.height]
  );
  const showSectionHeaders = sectionGroups.some((group) => group.id !== DEFAULT_SECTION_GROUP_ID);
  const {
    collapsedSections,
    renamingSectionId,
    renameValue,
    sectionContextMenu,
    slideCtxMenu,
    setRenameValue,
    handleDragStart,
    handleDragOver,
    handleDrop,
    toggleSection,
    startRename,
    commitRename,
    cancelRename,
    handleSectionContextMenu,
    handleOpenSlideCtxMenu,
    closeSectionContextMenu,
    closeSlideCtxMenu
  } = useSlidePaneCallbacks(onMoveSlide, onRenameSection, onToggleSectionCollapse);
  const effectiveCollapsed = (0, import_react2.useMemo)(() => {
    const map = {};
    for (const group of sectionGroups) {
      map[group.id] = collapsedSections[group.id] ?? group.defaultCollapsed ?? false;
    }
    return map;
  }, [sectionGroups, collapsedSections]);
  const flatItems = (0, import_react2.useMemo)(
    () => buildFlatPaneItems(sectionGroups, showSectionHeaders, effectiveCollapsed),
    [sectionGroups, showSectionHeaders, effectiveCollapsed]
  );
  const shouldVirtualize = slides.length >= VIRTUALIZATION_THRESHOLD;
  const { startIndex, endIndex, totalHeight, offsetY, scrollContainerRef, scrollToIndex } = useVirtualizedSlides({
    totalItems: shouldVirtualize ? flatItems.length : 0,
    itemHeight: estimatedItemHeight
  });
  (0, import_react2.useEffect)(() => {
    if (shouldVirtualize) {
      const flatIdx = flatItems.findIndex(
        (item) => item.type === "slide" && item.slideIndex === activeSlideIndex
      );
      if (flatIdx >= 0) {
        scrollToIndex(flatIdx);
      }
    } else {
      const el = slideRefs.current.get(activeSlideIndex);
      if (el) {
        el.scrollIntoView({ behavior: "smooth", block: "nearest" });
      }
    }
  }, [activeSlideIndex, shouldVirtualize, scrollToIndex, flatItems]);
  (0, import_react2.useEffect)(() => {
    if (renamingSectionId && renameInputRef.current) {
      renameInputRef.current.focus();
      renameInputRef.current.select();
    }
  }, [renamingSectionId]);
  const setSlideRef = (0, import_react2.useCallback)(
    (idx) => (el) => {
      if (el) {
        slideRefs.current.set(idx, el);
      } else {
        slideRefs.current.delete(idx);
      }
    },
    []
  );
  if (!isOpen) {
    return null;
  }
  const renderVirtualized = () => {
    const visibleItems = flatItems.slice(startIndex, endIndex + 1);
    return (0, import_jsx_runtime.jsx)("div", { ref: scrollContainerRef, className: "flex-1 overflow-y-auto px-1.5 pb-2", children: (0, import_jsx_runtime.jsx)("div", { style: { height: totalHeight, position: "relative" }, children: (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: {
          position: "absolute",
          top: offsetY,
          left: 0,
          right: 0
        },
        children: (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: visibleItems.map((item) => {
          if (item.type === "section") {
            const section = sectionGroups[item.sectionIndex];
            if (!section) {
              return null;
            }
            const isCollapsed = effectiveCollapsed[section.id] ?? false;
            return (0, import_jsx_runtime.jsx)(
              SectionHeader,
              {
                sectionId: section.id,
                label: section.label,
                slideCount: section.slideIndexes.length,
                isCollapsed,
                isRenaming: renamingSectionId === section.id,
                renameValue,
                canEdit,
                sectionIndex: item.sectionIndex,
                totalSections: sectionGroups.length,
                renameInputRef,
                onToggle: toggleSection,
                onContextMenu: handleSectionContextMenu,
                onStartRename: startRename,
                onRenameValueChange: setRenameValue,
                onCommitRename: commitRename,
                onCancelRename: cancelRename
              },
              `section-${section.id}`
            );
          }
          const slide = slides[item.slideIndex];
          if (!slide) {
            return null;
          }
          return (0, import_jsx_runtime.jsx)(
            SlideItem,
            {
              slide,
              templateElements: templateElementsBySlideId[slide.id] ?? EMPTY_TEMPLATE_ELEMENTS,
              slideIndex: item.slideIndex,
              isActive: item.slideIndex === activeSlideIndex,
              canvasSize,
              canEdit,
              rehearsalTimings,
              presenceUsers: slidePresenceMap == null ? void 0 : slidePresenceMap.get(item.slideIndex),
              fieldContext,
              tableStyleContext,
              onSelectSlide,
              onSlideContextMenu,
              onAddSection,
              onOpenSlideCtxMenu: handleOpenSlideCtxMenu,
              onDragStart: handleDragStart,
              onDragOver: handleDragOver,
              onDrop: handleDrop,
              slideRef: setSlideRef(item.slideIndex)
            },
            slide.id ?? item.slideIndex
          );
        }) })
      }
    ) }) });
  };
  const renderNonVirtualized = () => (0, import_jsx_runtime.jsx)("div", { className: "flex-1 space-y-1 overflow-y-auto px-1.5 pb-2", children: sectionGroups.map((section, sectionIndex) => {
    const isCollapsed = effectiveCollapsed[section.id] ?? false;
    return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
      showSectionHeaders && (0, import_jsx_runtime.jsx)(
        SectionHeader,
        {
          sectionId: section.id,
          label: section.label,
          slideCount: section.slideIndexes.length,
          isCollapsed,
          isRenaming: renamingSectionId === section.id,
          renameValue,
          canEdit,
          sectionIndex,
          totalSections: sectionGroups.length,
          renameInputRef,
          onToggle: toggleSection,
          onContextMenu: handleSectionContextMenu,
          onStartRename: startRename,
          onRenameValueChange: setRenameValue,
          onCommitRename: commitRename,
          onCancelRename: cancelRename
        }
      ),
      !isCollapsed && section.slideIndexes.map((idx) => {
        const slide = slides[idx];
        if (!slide) {
          return null;
        }
        return (0, import_jsx_runtime.jsx)(
          SlideItem,
          {
            slide,
            templateElements: templateElementsBySlideId[slide.id] ?? EMPTY_TEMPLATE_ELEMENTS,
            slideIndex: idx,
            isActive: idx === activeSlideIndex,
            canvasSize,
            canEdit,
            rehearsalTimings,
            presenceUsers: slidePresenceMap == null ? void 0 : slidePresenceMap.get(idx),
            fieldContext,
            tableStyleContext,
            onSelectSlide,
            onSlideContextMenu,
            onAddSection,
            onOpenSlideCtxMenu: handleOpenSlideCtxMenu,
            onDragStart: handleDragStart,
            onDragOver: handleDragOver,
            onDrop: handleDrop,
            slideRef: setSlideRef(idx)
          },
          slide.id ?? idx
        );
      })
    ] }, section.id);
  }) });
  return (0, import_jsx_runtime.jsxs)(
    "aside",
    {
      role: "navigation",
      "aria-label": t("pptx.sections.slides"),
      className: "flex h-full flex-col border-r border-border bg-secondary/30",
      style: panelWidth ? { width: panelWidth, flexShrink: 0 } : void 0,
      children: [
        shouldVirtualize ? renderVirtualized() : renderNonVirtualized(),
        canEdit && (0, import_jsx_runtime.jsx)("div", { className: "border-t border-border/60 px-2 py-1.5", children: (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center justify-center gap-1 rounded-sm px-2 py-1 text-[11px] text-muted-foreground hover:bg-accent hover:text-foreground transition-colors disabled:cursor-not-allowed disabled:opacity-40",
            disabled: !canEdit,
            onClick: onAddSlide,
            children: [
              (0, import_jsx_runtime.jsx)(LuPlus, { className: "h-3 w-3" }),
              t("pptx.sections.addSlide")
            ]
          }
        ) }),
        sectionContextMenu && (0, import_jsx_runtime.jsx)(
          SectionContextMenu,
          {
            state: sectionContextMenu,
            sectionGroups,
            totalSlides: slides.length,
            onStartRename: startRename,
            onDeleteSection,
            onMoveSectionUp,
            onMoveSectionDown,
            onAddSection,
            onClose: closeSectionContextMenu
          }
        ),
        slideCtxMenu && (0, import_jsx_runtime.jsx)(
          SlideContextMenu,
          {
            state: slideCtxMenu,
            onAddSection,
            onClose: closeSlideCtxMenu
          }
        )
      ]
    }
  );
}
var HANDOUT_SLIDES_PER_PAGE_OPTIONS = [1, 2, 3, 4, 6, 9];
function HandoutMasterPanel({
  handoutMaster,
  slidesPerPage,
  onSlidesPerPageChange,
  onBackgroundChange
}) {
  const { t } = useTranslation();
  if (!handoutMaster) {
    return (0, import_jsx_runtime.jsx)("div", { className: "px-2 py-4 text-center text-xs text-muted-foreground", children: t("pptx.master.noHandoutMaster") });
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2 px-1", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1.5", children: t("pptx.master.handoutSlidesPerPage") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-3 gap-1", children: HANDOUT_SLIDES_PER_PAGE_OPTIONS.map((count) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          "aria-pressed": slidesPerPage === count,
          className: cn(
            "px-2 py-1.5 rounded text-[11px] font-medium transition-colors",
            slidesPerPage === count ? "bg-primary text-white" : "bg-accent/50 text-muted-foreground hover:bg-accent/70 hover:text-foreground"
          ),
          onClick: () => onSlidesPerPageChange(count),
          children: count
        },
        count
      )) })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.master.handoutBackground") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          "aria-label": t("pptx.master.backgroundColorLabel"),
          className: "w-full h-8 rounded border border-border",
          value: handoutMaster.backgroundColor ?? "#ffffff",
          onChange: (event) => onBackgroundChange(event.target.value)
        }
      )
    ] }),
    handoutMaster.placeholders && handoutMaster.placeholders.length > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1.5", children: t("pptx.master.handoutPlaceholders") }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: handoutMaster.placeholders.map((ph) => (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "flex items-center gap-2 rounded px-1.5 py-1 bg-background/50 text-[10px] text-foreground",
          children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-2 h-2 rounded-full bg-purple-500/60 flex-shrink-0" }),
            ph.type
          ]
        },
        `${ph.type}-${ph.idx ?? "default"}`
      )) })
    ] })
  ] });
}
function NotesMasterPanel({
  notesMaster,
  onBackgroundChange
}) {
  const { t } = useTranslation();
  const placeholderLabels = (0, import_react2.useMemo)(() => {
    if (!(notesMaster == null ? void 0 : notesMaster.placeholders)) {
      return [];
    }
    return notesMaster.placeholders.map((ph) => {
      const typeLabel = ph.type === "body" ? t("pptx.master.notesMasterBody") : ph.type === "sldImg" ? t("pptx.master.notesMasterSlideImage") : ph.type === "hdr" ? t("pptx.master.notesMasterHeader") : ph.type === "ftr" ? t("pptx.master.notesMasterFooter") : ph.type === "dt" ? t("pptx.master.notesMasterDate") : ph.type === "sldNum" ? t("pptx.master.notesMasterPageNumber") : ph.type;
      return { type: ph.type, label: typeLabel, idx: ph.idx };
    });
  }, [notesMaster == null ? void 0 : notesMaster.placeholders, t]);
  if (!notesMaster) {
    return (0, import_jsx_runtime.jsx)("div", { className: "px-2 py-4 text-center text-xs text-muted-foreground", children: t("pptx.master.noNotesMaster") });
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2 px-1", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.master.notesMasterBackground") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          "aria-label": t("pptx.master.backgroundColorLabel"),
          className: "w-full h-8 rounded border border-border",
          value: notesMaster.backgroundColor ?? "#ffffff",
          onChange: (event) => onBackgroundChange(event.target.value)
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "rounded-md border border-border/60 bg-muted/40 p-2", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1.5", children: t("pptx.master.notesMasterPlaceholders") }),
      placeholderLabels.length > 0 ? (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: placeholderLabels.map((ph) => (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "flex items-center gap-2 rounded px-1.5 py-1 bg-background/50 text-[10px] text-foreground",
          children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-2 h-2 rounded-full bg-green-500/60 flex-shrink-0" }),
            ph.label
          ]
        },
        `${ph.type}-${ph.idx ?? "default"}`
      )) }) : (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.master.noPlaceholders") })
    ] })
  ] });
}
function partToSlide(master, layoutIndex) {
  return masterViewPseudoSlide(
    { slideMasters: [master] },
    { tab: "slides", masterIndex: 0, layoutIndex }
  );
}
var EMPTY_SLIDE = { id: "", rId: "", slideNumber: 0, elements: [] };
function SlideMastersList({
  slideMasters,
  activeMasterIndex,
  activeLayoutIndex,
  canvasSize,
  onSelectMaster,
  onSelectLayout
}) {
  const activeRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    var _a;
    (_a = activeRef.current) == null ? void 0 : _a.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }, [activeMasterIndex, activeLayoutIndex]);
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    slideMasters.map((master, masterIdx) => {
      const isMasterActive = masterIdx === activeMasterIndex && activeLayoutIndex === null;
      const layouts = master.layouts ?? [];
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            ref: isMasterActive ? activeRef : void 0,
            "aria-pressed": isMasterActive,
            "aria-label": master.name || t("pptx.master.master"),
            className: cn(
              "group relative block w-full cursor-pointer rounded-lg border-2 p-1 text-left transition-all",
              isMasterActive ? "border-amber-500 bg-amber-500/10" : "border-border bg-background/40 hover:border-border"
            ),
            onClick: () => onSelectMaster(masterIdx),
            children: [
              (0, import_jsx_runtime.jsx)("div", { className: "relative overflow-hidden rounded bg-white", children: (0, import_jsx_runtime.jsx)(
                SlideThumbnail,
                {
                  slide: partToSlide(master, null) ?? EMPTY_SLIDE,
                  templateElements: [],
                  canvasSize
                }
              ) }),
              (0, import_jsx_runtime.jsx)("div", { className: "mt-1 px-1", children: (0, import_jsx_runtime.jsx)(
                "span",
                {
                  className: cn(
                    "text-[10px] font-medium",
                    isMasterActive ? "text-amber-400" : "text-muted-foreground"
                  ),
                  children: master.name || t("pptx.master.master")
                }
              ) })
            ]
          }
        ),
        layouts.length > 0 && (0, import_jsx_runtime.jsx)("div", { className: "ml-3 space-y-1 border-l border-border/40 pl-2", children: layouts.map((layout, layoutIdx) => {
          const isLayoutActive = masterIdx === activeMasterIndex && layoutIdx === activeLayoutIndex;
          return (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              ref: isLayoutActive ? activeRef : void 0,
              "aria-pressed": isLayoutActive,
              "aria-label": layout.name || t("pptx.master.layout"),
              className: cn(
                "group relative block w-full cursor-pointer rounded-md border-2 p-0.5 text-left transition-all",
                isLayoutActive ? "border-primary bg-primary/10" : "border-border bg-background/40 hover:border-border"
              ),
              onClick: () => onSelectLayout(masterIdx, layoutIdx),
              children: [
                (0, import_jsx_runtime.jsx)("div", { className: "relative overflow-hidden rounded bg-white", children: (0, import_jsx_runtime.jsx)(
                  SlideThumbnail,
                  {
                    slide: partToSlide(master, layoutIdx) ?? EMPTY_SLIDE,
                    templateElements: [],
                    canvasSize
                  }
                ) }),
                (0, import_jsx_runtime.jsx)("div", { className: "mt-0.5 px-0.5", children: (0, import_jsx_runtime.jsx)(
                  "span",
                  {
                    className: cn(
                      "text-[9px]",
                      isLayoutActive ? "text-primary" : "text-muted-foreground"
                    ),
                    children: layout.name || t("pptx.master.layout")
                  }
                ) })
              ]
            },
            layout.path
          );
        }) })
      ] }, master.path);
    }),
    slideMasters.length === 0 && (0, import_jsx_runtime.jsx)("div", { className: "px-2 py-4 text-center text-xs text-muted-foreground", children: t("pptx.master.noMasters") })
  ] });
}
var TABS = [
  { key: "slides", labelKey: "pptx.sections.slides" },
  { key: "notes", labelKey: "pptx.notes.title" },
  { key: "handout", labelKey: "pptx.masterView.tabHandout" }
];
function MasterViewSidebar({
  slideMasters,
  activeMasterIndex,
  activeLayoutIndex,
  canvasSize,
  masterViewTab,
  notesMaster,
  handoutMaster,
  handoutSlidesPerPage,
  canEdit,
  onSelectMaster,
  onSelectLayout,
  onCollapse,
  onTabChange,
  crudActions,
  onCrudAction,
  onHandoutSlidesPerPageChange,
  onNotesMasterBackgroundChange,
  onHandoutMasterBackgroundChange,
  onSlidesBackgroundChange
}) {
  const scrollRef = (0, import_react2.useRef)(null);
  const { t } = useTranslation();
  const slidesBackground = masterViewBackgroundColor(
    { slideMasters },
    { tab: "slides", masterIndex: activeMasterIndex, layoutIndex: activeLayoutIndex }
  );
  const handleMasterClick = (0, import_react2.useCallback)(
    (index) => {
      onSelectMaster(index);
    },
    [onSelectMaster]
  );
  const handleLayoutClick = (0, import_react2.useCallback)(
    (masterIdx, layoutIdx) => {
      onSelectLayout(masterIdx, layoutIdx);
    },
    [onSelectLayout]
  );
  return (0, import_jsx_runtime.jsxs)(
    "aside",
    {
      "aria-label": t("pptx.view.masterViews"),
      className: "flex h-full flex-col border-r border-border/80 bg-background/70 backdrop-blur-sm w-56",
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-3 py-2", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-xs uppercase tracking-wide text-muted-foreground", children: masterViewTab === "slides" ? t("pptx.master.title") : masterViewTab === "notes" ? t("pptx.master.notesMasterTitle") : t("pptx.master.handoutMasterTitle") }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "rounded p-1 text-muted-foreground hover:bg-muted hover:text-foreground",
              title: t("pptx.master.collapseMasterPane"),
              onClick: onCollapse,
              children: (0, import_jsx_runtime.jsx)(LuPanelLeftClose, { className: "h-3.5 w-3.5" })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            role: "tablist",
            "aria-label": t("pptx.mode.masterView"),
            className: "flex border-b border-border/60 px-1",
            children: TABS.map((tab) => (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                role: "tab",
                "aria-selected": masterViewTab === tab.key,
                className: cn(
                  "flex-1 px-1 py-1.5 text-[10px] font-medium transition-colors border-b-2",
                  masterViewTab === tab.key ? "border-amber-500 text-amber-400" : "border-transparent text-muted-foreground hover:text-foreground"
                ),
                onClick: () => onTabChange(tab.key),
                children: t(tab.labelKey)
              },
              tab.key
            ))
          }
        ),
        (0, import_jsx_runtime.jsxs)("div", { ref: scrollRef, className: "flex-1 space-y-2 overflow-y-auto px-1.5 pb-2 pt-1", children: [
          masterViewTab === "slides" && canEdit && (0, import_jsx_runtime.jsxs)("section", { className: "space-y-1.5 rounded-md border border-border/60 p-2", children: [
            (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: t("pptx.master.notesMasterBackground") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "color",
                "aria-label": t("pptx.master.backgroundColorLabel"),
                className: "h-7 w-full cursor-pointer rounded border border-border/60 bg-transparent",
                value: slidesBackground ?? "#ffffff",
                onChange: (event) => onSlidesBackgroundChange(event.target.value)
              }
            )
          ] }),
          masterViewTab === "slides" && canEdit && crudActions.length > 0 && (0, import_jsx_runtime.jsx)("section", { className: "grid grid-cols-2 gap-1 rounded-md border border-border/60 p-2", children: crudActions.map((action) => (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "data-testid": `pptx-master-crud-${action.id}`,
              disabled: !action.enabled,
              title: action.disabledReasonKey ? t(action.disabledReasonKey) : void 0,
              onClick: () => onCrudAction(action.id),
              className: "rounded border border-border/60 px-1.5 py-1 text-[10px] hover:bg-accent/60 disabled:cursor-not-allowed disabled:opacity-40",
              children: t(action.labelKey)
            },
            action.id
          )) }),
          masterViewTab === "slides" && (0, import_jsx_runtime.jsx)(
            SlideMastersList,
            {
              slideMasters,
              activeMasterIndex,
              activeLayoutIndex,
              canvasSize,
              onSelectMaster: handleMasterClick,
              onSelectLayout: handleLayoutClick
            }
          ),
          masterViewTab === "notes" && (0, import_jsx_runtime.jsx)(
            NotesMasterPanel,
            {
              notesMaster,
              onBackgroundChange: onNotesMasterBackgroundChange
            }
          ),
          masterViewTab === "handout" && (0, import_jsx_runtime.jsx)(
            HandoutMasterPanel,
            {
              handoutMaster,
              slidesPerPage: handoutSlidesPerPage,
              onSlidesPerPageChange: onHandoutSlidesPerPageChange,
              onBackgroundChange: onHandoutMasterBackgroundChange
            }
          )
        ] })
      ]
    }
  );
}
var DEFAULT_NOTES_WIDTH = 720;
var DEFAULT_NOTES_HEIGHT = 960;
var PLACEHOLDER_LABEL_KEYS = {
  sldImg: "pptx.master.notesMasterSlideImage",
  body: "pptx.master.notesMasterBody",
  hdr: "pptx.master.notesMasterHeader",
  ftr: "pptx.master.notesMasterFooter",
  dt: "pptx.master.notesMasterDate",
  sldNum: "pptx.master.notesMasterPageNumber"
};
var DEFAULT_POSITIONS = {
  sldImg: { x: 0.1, y: 0.05, w: 0.8, h: 0.4 },
  body: { x: 0.1, y: 0.5, w: 0.8, h: 0.4 },
  hdr: { x: 0, y: 0, w: 0.4, h: 0.04 },
  ftr: { x: 0, y: 0.96, w: 0.4, h: 0.04 },
  dt: { x: 0.6, y: 0, w: 0.4, h: 0.04 },
  sldNum: { x: 0.6, y: 0.96, w: 0.4, h: 0.04 }
};
function elementText(element) {
  var _a;
  if ("textSegments" in element && ((_a = element.textSegments) == null ? void 0 : _a.length)) {
    return element.textSegments.map((segment) => segment.text).join("");
  }
  return "text" in element ? element.text ?? "" : "";
}
function NotesMasterCanvas({
  notesMaster,
  canvasSize,
  notesCanvasSize,
  slideThumbnail,
  notesText,
  slideNumber
}) {
  var _a;
  const { t } = useTranslation();
  const pageWidth = (notesCanvasSize == null ? void 0 : notesCanvasSize.width) ?? DEFAULT_NOTES_WIDTH;
  const pageHeight = (notesCanvasSize == null ? void 0 : notesCanvasSize.height) ?? DEFAULT_NOTES_HEIGHT;
  const scale = (0, import_react2.useMemo)(() => {
    const scaleX = canvasSize.width / pageWidth;
    const scaleY = canvasSize.height / pageHeight;
    return Math.min(scaleX, scaleY, 1) * 0.85;
  }, [canvasSize.width, canvasSize.height, pageWidth, pageHeight]);
  const scaledWidth = pageWidth * scale;
  const scaledHeight = pageHeight * scale;
  const bodyFontSize = (0, import_react2.useMemo)(
    () => resolveNotesSchematicBodyFontSizePx(notesMaster == null ? void 0 : notesMaster.notesStyle, scale),
    [notesMaster == null ? void 0 : notesMaster.notesStyle, scale]
  );
  const placeholders = (0, import_react2.useMemo)(() => {
    if (!(notesMaster == null ? void 0 : notesMaster.placeholders)) {
      return [
        { type: "sldImg", idx: void 0 },
        { type: "body", idx: void 0 },
        { type: "hdr", idx: void 0 },
        { type: "ftr", idx: void 0 },
        { type: "dt", idx: void 0 },
        { type: "sldNum", idx: void 0 }
      ];
    }
    return notesMaster.placeholders;
  }, [notesMaster == null ? void 0 : notesMaster.placeholders]);
  if (!notesMaster) {
    return (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-full text-muted-foreground text-sm", children: t("pptx.master.noNotesMaster") });
  }
  return (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-full w-full", children: (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "relative bg-white shadow-lg border border-border rounded",
      style: { width: scaledWidth, height: scaledHeight },
      children: [
        notesMaster.backgroundColor && (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute inset-0 rounded",
            style: { backgroundColor: notesMaster.backgroundColor }
          }
        ),
        placeholders.map((ph) => {
          const pos = DEFAULT_POSITIONS[ph.type];
          if (!pos) {
            return null;
          }
          const labelKey = PLACEHOLDER_LABEL_KEYS[ph.type] ?? ph.type;
          const isSlideArea = ph.type === "sldImg";
          const isBody = ph.type === "body";
          const isPageNum = ph.type === "sldNum";
          if (isSlideArea && slideThumbnail) {
            return (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute overflow-hidden border border-solid border-gray-300",
                style: {
                  left: pos.x * scaledWidth,
                  top: pos.y * scaledHeight,
                  width: pos.w * scaledWidth,
                  height: pos.h * scaledHeight
                },
                children: (0, import_jsx_runtime.jsx)(
                  "img",
                  {
                    src: slideThumbnail,
                    alt: slideNumber ? `Slide ${slideNumber}` : "Slide",
                    className: "w-full h-full object-contain"
                  }
                )
              },
              `${ph.type}-${ph.idx ?? "default"}`
            );
          }
          if (isBody && notesText !== void 0) {
            return (0, import_jsx_runtime.jsx)(
              "div",
              {
                "data-region": ph.type,
                className: "absolute overflow-auto border border-solid border-gray-200 p-2",
                style: {
                  left: pos.x * scaledWidth,
                  top: pos.y * scaledHeight,
                  width: pos.w * scaledWidth,
                  height: pos.h * scaledHeight,
                  fontSize: bodyFontSize,
                  lineHeight: 1.4,
                  color: "#374151",
                  whiteSpace: "pre-wrap"
                },
                children: notesText || t("pptx.notes.noNotes")
              },
              `${ph.type}-${ph.idx ?? "default"}`
            );
          }
          if (isPageNum && slideNumber !== void 0) {
            return (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute flex items-center justify-center",
                style: {
                  left: pos.x * scaledWidth,
                  top: pos.y * scaledHeight,
                  width: pos.w * scaledWidth,
                  height: pos.h * scaledHeight,
                  fontSize: Math.max(8, Math.min(12, scaledWidth * 0.018)),
                  color: "rgba(156, 163, 175, 0.6)"
                },
                children: slideNumber
              },
              `${ph.type}-${ph.idx ?? "default"}`
            );
          }
          return (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute flex items-center justify-center border border-dashed",
              style: {
                left: pos.x * scaledWidth,
                top: pos.y * scaledHeight,
                width: pos.w * scaledWidth,
                height: pos.h * scaledHeight,
                borderColor: isSlideArea ? "rgba(59, 130, 246, 0.5)" : isBody ? "rgba(34, 197, 94, 0.5)" : "rgba(156, 163, 175, 0.4)",
                backgroundColor: isSlideArea ? "rgba(59, 130, 246, 0.05)" : isBody ? "rgba(34, 197, 94, 0.05)" : "transparent"
              },
              children: (0, import_jsx_runtime.jsx)(
                "span",
                {
                  className: "text-center px-1",
                  style: {
                    fontSize: Math.max(8, Math.min(12, scaledWidth * 0.018)),
                    color: isSlideArea ? "rgba(59, 130, 246, 0.7)" : isBody ? "rgba(34, 197, 94, 0.7)" : "rgba(156, 163, 175, 0.6)"
                  },
                  children: t(labelKey)
                }
              )
            },
            `${ph.type}-${ph.idx ?? "default"}`
          );
        }),
        (_a = notesMaster.elements) == null ? void 0 : _a.map((element) => (0, import_jsx_runtime.jsx)(
          "div",
          {
            "data-pptx-element": "true",
            "data-element-id": element.id,
            className: "absolute overflow-hidden",
            style: {
              left: element.x * scale,
              top: element.y * scale,
              width: element.width * scale,
              height: element.height * scale,
              zIndex: 2
            },
            children: elementText(element)
          },
          element.id
        ))
      ]
    }
  ) });
}
var PAGE_WIDTH = 720;
var PAGE_HEIGHT = 960;
var MARGIN = 0.06;
function elementText2(element) {
  var _a;
  if ("textSegments" in element && ((_a = element.textSegments) == null ? void 0 : _a.length)) {
    return element.textSegments.map((segment) => segment.text).join("");
  }
  return "text" in element ? element.text ?? "" : "";
}
function computeSlotLayout(slidesPerPage, slideAspect) {
  const mx = MARGIN;
  const my = MARGIN;
  const contentW = 1 - 2 * mx;
  const contentH = 1 - 2 * my;
  const GAP = 0.02;
  switch (slidesPerPage) {
    case 1: {
      const w = contentW * 0.8;
      const h = w / slideAspect;
      return [{ x: mx + (contentW - w) / 2, y: my + (contentH - h) / 2, w, h }];
    }
    case 2: {
      const w = contentW * 0.75;
      const h = w / slideAspect;
      const totalH = h * 2 + GAP;
      const startY = my + (contentH - totalH) / 2;
      return [0, 1].map((i) => ({
        x: mx + (contentW - w) / 2,
        y: startY + i * (h + GAP),
        w,
        h
      }));
    }
    case 3: {
      const w = contentW * 0.5;
      const h = w / slideAspect;
      const totalH = h * 3 + GAP * 2;
      const startY = my + (contentH - totalH) / 2;
      return [0, 1, 2].map((i) => ({
        x: mx,
        y: startY + i * (h + GAP),
        w,
        h
      }));
    }
    case 4: {
      const cols = 2;
      const rows = 2;
      const w = (contentW - GAP) / cols;
      const h = w / slideAspect;
      const totalH = h * rows + GAP * (rows - 1);
      const startY = my + (contentH - totalH) / 2;
      return Array.from({ length: 4 }, (_, i) => ({
        x: mx + i % cols * (w + GAP),
        y: startY + Math.floor(i / cols) * (h + GAP),
        w,
        h
      }));
    }
    case 6: {
      const cols = 2;
      const rows = 3;
      const w = (contentW - GAP) / cols;
      const h = w / slideAspect;
      const totalH = h * rows + GAP * (rows - 1);
      const startY = my + (contentH - totalH) / 2;
      return Array.from({ length: 6 }, (_, i) => ({
        x: mx + i % cols * (w + GAP),
        y: startY + Math.floor(i / cols) * (h + GAP),
        w,
        h
      }));
    }
    case 9: {
      const cols = 3;
      const rows = 3;
      const w = (contentW - GAP * 2) / cols;
      const h = w / slideAspect;
      const totalH = h * rows + GAP * (rows - 1);
      const startY = my + (contentH - totalH) / 2;
      return Array.from({ length: 9 }, (_, i) => ({
        x: mx + i % cols * (w + GAP),
        y: startY + Math.floor(i / cols) * (h + GAP),
        w,
        h
      }));
    }
    default:
      return [];
  }
}
function HandoutMasterCanvas({
  handoutMaster,
  canvasSize,
  slidesPerPage,
  slideThumbnails,
  pageNumber
}) {
  var _a;
  const { t } = useTranslation();
  const slideAspect = 4 / 3;
  const scale = (0, import_react2.useMemo)(() => {
    const scaleX = canvasSize.width / PAGE_WIDTH;
    const scaleY = canvasSize.height / PAGE_HEIGHT;
    return Math.min(scaleX, scaleY, 1) * 0.85;
  }, [canvasSize.width, canvasSize.height]);
  const scaledWidth = PAGE_WIDTH * scale;
  const scaledHeight = PAGE_HEIGHT * scale;
  const slots = (0, import_react2.useMemo)(
    () => computeSlotLayout(slidesPerPage, slideAspect),
    [slidesPerPage, slideAspect]
  );
  if (!handoutMaster) {
    return (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-full text-muted-foreground text-sm", children: t("pptx.master.noHandoutMaster") });
  }
  return (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-full w-full", children: (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "relative bg-white shadow-lg border border-gray-300 rounded",
      style: { width: scaledWidth, height: scaledHeight },
      children: [
        handoutMaster.backgroundColor && (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute inset-0 rounded",
            style: { backgroundColor: handoutMaster.backgroundColor }
          }
        ),
        slots.map((slot, i) => {
          const thumbnail = slideThumbnails == null ? void 0 : slideThumbnails[i];
          return (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: `absolute flex items-center justify-center overflow-hidden ${thumbnail ? "border border-solid border-gray-300" : "border border-dashed border-blue-400/50 bg-blue-50/30"}`,
              style: {
                left: slot.x * scaledWidth,
                top: slot.y * scaledHeight,
                width: slot.w * scaledWidth,
                height: slot.h * scaledHeight
              },
              children: thumbnail ? (0, import_jsx_runtime.jsx)(
                "img",
                {
                  src: thumbnail,
                  alt: `Slide ${(pageNumber ? (pageNumber - 1) * slidesPerPage : 0) + i + 1}`,
                  className: "w-full h-full object-contain"
                }
              ) : (0, import_jsx_runtime.jsx)(
                "span",
                {
                  className: "text-blue-400/60 font-medium",
                  style: {
                    fontSize: Math.max(8, Math.min(14, slot.w * scaledWidth * 0.08))
                  },
                  children: t("pptx.master.handoutSlideSlot", { number: i + 1 })
                }
              )
            },
            i
          );
        }),
        (_a = handoutMaster.elements) == null ? void 0 : _a.map((element) => (0, import_jsx_runtime.jsx)(
          "div",
          {
            "data-pptx-element": "true",
            "data-element-id": element.id,
            className: "absolute overflow-hidden",
            style: {
              left: element.x * scale,
              top: element.y * scale,
              width: element.width * scale,
              height: element.height * scale,
              zIndex: 2
            },
            children: elementText2(element)
          },
          element.id
        )),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute left-0 top-0 px-1 text-gray-400/50 border-b border-r border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: t("pptx.master.notesMasterHeader")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute right-0 top-0 px-1 text-gray-400/50 border-b border-l border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: t("pptx.master.notesMasterDate")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute left-0 bottom-0 px-1 text-gray-400/50 border-t border-r border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: t("pptx.master.notesMasterFooter")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute right-0 bottom-0 px-1 text-gray-400/50 border-t border-l border-dashed border-gray-300/40",
            style: { fontSize: Math.max(6, 8 * scale) },
            children: pageNumber !== void 0 ? String(pageNumber) : t("pptx.master.notesMasterPageNumber")
          }
        )
      ]
    }
  ) });
}
function SectionBlock({
  section,
  slides,
  gridCols,
  zoomScale: _zoomScale,
  renderSlideCard
}) {
  const [collapsed, setCollapsed] = (0, import_react2.useState)(section.defaultCollapsed ?? false);
  return (0, import_jsx_runtime.jsxs)("div", { className: "mb-4", children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: "flex w-full items-center gap-1.5 rounded px-2 py-1.5 text-[11px] uppercase tracking-wide text-muted-foreground hover:bg-muted/60 hover:text-foreground mb-2",
        onClick: () => setCollapsed((p) => !p),
        children: [
          section.color && (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: "inline-block h-2.5 w-2.5 shrink-0 rounded-full",
              style: { backgroundColor: section.color }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            LuChevronDown,
            {
              className: cn("h-3 w-3 transition-transform", collapsed ? "-rotate-90" : "rotate-0")
            }
          ),
          (0, import_jsx_runtime.jsx)("span", { className: "truncate text-left", children: section.label }),
          (0, import_jsx_runtime.jsx)("span", { className: "ml-auto text-[10px] text-muted-foreground", children: section.slideIndexes.length })
        ]
      }
    ),
    !collapsed && (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "grid gap-4",
        style: {
          gridTemplateColumns: `repeat(${gridCols}, minmax(0, 1fr))`
        },
        children: section.slideIndexes.map((idx) => {
          const slide = slides[idx];
          if (!slide) {
            return null;
          }
          return renderSlideCard(slide, idx);
        })
      }
    )
  ] });
}
function SlideCardImpl({
  slide,
  index,
  isActive,
  isDragTarget,
  isSelected,
  selectedCount,
  selectionOrder,
  canvasSize,
  canEdit,
  onSlideClick,
  onDoubleClick,
  onContextMenu,
  onDragStart,
  onDragOver,
  onDragLeave,
  onDrop
}) {
  const { t } = useTranslation();
  const cue = hiddenSlideCue(slide.hidden, "sorter", index);
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: cn(
        "group relative cursor-pointer rounded-lg border-2 p-1 transition-all",
        isDragTarget ? "border-primary bg-primary/20" : isSelected ? "border-primary bg-primary/10 ring-1 ring-primary/50" : isActive ? "border-primary/50 bg-primary/5" : "border-border bg-background/50 hover:border-border",
        slide.hidden && "opacity-40"
      ),
      "data-pptx-slide-hidden": cue.marker,
      "aria-describedby": cue.labelId,
      onClick: (e) => onSlideClick(e, index),
      onDoubleClick: () => onDoubleClick(index),
      onContextMenu: (e) => onContextMenu(e, index),
      draggable: canEdit,
      onDragStart: (e) => onDragStart(e, index),
      onDragOver: (e) => onDragOver(e, index),
      onDragLeave,
      onDrop: (e) => onDrop(e, index),
      children: [
        (0, import_jsx_runtime.jsx)("div", { className: "aspect-video overflow-hidden rounded bg-white", children: (0, import_jsx_runtime.jsx)(SlideThumbnail, { slide, templateElements: [], canvasSize }) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "mt-1 flex items-center justify-between px-0.5", children: [
          (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: cn(
                "text-[11px] font-medium px-0.5",
                isSelected ? "text-primary" : isActive ? "text-primary/70" : "text-muted-foreground"
              ),
              style: cue.hidden ? { backgroundImage: HIDDEN_SLIDE_SLASH_GRADIENT } : void 0,
              children: index + 1
            }
          ),
          cue.hidden && (0, import_jsx_runtime.jsxs)("span", { className: "flex items-center gap-1", id: cue.labelId, children: [
            (0, import_jsx_runtime.jsx)(LuEyeOff, { className: "h-3 w-3 text-muted-foreground" }),
            (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] uppercase tracking-wide text-muted-foreground", children: t(HIDDEN_SLIDE_LABEL_KEY) })
          ] })
        ] }),
        isSelected && selectedCount > 1 && (0, import_jsx_runtime.jsx)("div", { className: "absolute top-2 right-2 flex h-5 w-5 items-center justify-center rounded-full bg-primary text-white text-[10px] font-bold", children: selectionOrder })
      ]
    }
  );
}
function arePropsEqual3(prev, next) {
  if (prev.slide.id !== next.slide.id) {
    return false;
  }
  if (prev.slide.isDirty !== next.slide.isDirty) {
    return false;
  }
  if (prev.slide.hidden !== next.slide.hidden) {
    return false;
  }
  if (prev.slide.elements !== next.slide.elements) {
    return false;
  }
  if (prev.index !== next.index) {
    return false;
  }
  if (prev.isActive !== next.isActive) {
    return false;
  }
  if (prev.isDragTarget !== next.isDragTarget) {
    return false;
  }
  if (prev.isSelected !== next.isSelected) {
    return false;
  }
  if (prev.selectedCount !== next.selectedCount) {
    return false;
  }
  if (prev.selectionOrder !== next.selectionOrder) {
    return false;
  }
  if (prev.canEdit !== next.canEdit) {
    return false;
  }
  if (prev.canvasSize.width !== next.canvasSize.width || prev.canvasSize.height !== next.canvasSize.height) {
    return false;
  }
  if (prev.onSlideClick !== next.onSlideClick || prev.onDoubleClick !== next.onDoubleClick || prev.onContextMenu !== next.onContextMenu || prev.onDragStart !== next.onDragStart || prev.onDragOver !== next.onDragOver || prev.onDragLeave !== next.onDragLeave || prev.onDrop !== next.onDrop) {
    return false;
  }
  return true;
}
var SlideCard = import_react2.default.memo(SlideCardImpl, arePropsEqual3);
function SorterContextMenu({
  x,
  y,
  selectedCount,
  hasClipboard,
  hasHiddenInSelection,
  hasVisibleInSelection,
  sectionGroups: _sectionGroups,
  onDelete,
  onDuplicate,
  onCopy,
  onPaste,
  onToggleHide,
  onClose
}) {
  const { t } = useTranslation();
  const menuX = Math.max(8, Math.min(x, window.innerWidth - 220));
  const menuY = Math.max(8, Math.min(y, window.innerHeight - 300));
  const countLabel = selectedCount > 1 ? ` (${selectedCount})` : "";
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "fixed inset-0 z-[119]",
        onClick: onClose,
        onContextMenu: (e) => {
          e.preventDefault();
          onClose();
        }
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: "fixed z-[120] min-w-[200px] rounded border border-border bg-popover shadow-2xl py-1.5 text-xs text-foreground",
        style: { left: menuX, top: menuY },
        children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onCopy,
              children: [
                (0, import_jsx_runtime.jsx)(LuCopy, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.copy"),
                countLabel
              ]
            }
          ),
          hasClipboard && (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onPaste,
              children: [
                (0, import_jsx_runtime.jsx)(LuClipboardPaste, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.paste")
              ]
            }
          ),
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onDuplicate,
              children: [
                (0, import_jsx_runtime.jsx)(LuCopyPlus, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.duplicate"),
                countLabel
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border" }),
          hasVisibleInSelection && (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onToggleHide,
              children: [
                (0, import_jsx_runtime.jsx)(LuEyeOff, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.hideSlides"),
                countLabel
              ]
            }
          ),
          hasHiddenInSelection && (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left hover:bg-muted",
              onClick: onToggleHide,
              children: [
                (0, import_jsx_runtime.jsx)(LuEye, { className: "h-3.5 w-3.5 text-muted-foreground" }),
                t("pptx.slideSorter.contextMenu.showSlides"),
                countLabel
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border" }),
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-left text-red-300 hover:bg-red-900/40",
              onClick: onDelete,
              children: [
                (0, import_jsx_runtime.jsx)(LuTrash2, { className: "h-3.5 w-3.5" }),
                t("pptx.slideSorter.contextMenu.delete"),
                countLabel
              ]
            }
          )
        ]
      }
    )
  ] });
}
var MIN_ZOOM = SORTER_MIN_ZOOM;
var MAX_ZOOM = SORTER_MAX_ZOOM;
var DEFAULT_ZOOM = 100;
var ZOOM_STEP = SORTER_ZOOM_STEP;
function useKeyboardShortcuts2(params) {
  const paramsRef = (0, import_react2.useRef)(params);
  paramsRef.current = params;
  (0, import_react2.useEffect)(() => {
    const handleKeyDown = (e) => {
      const current = paramsRef.current;
      if (current.contextMenu) {
        current.setContextMenu(null);
      }
      const { action } = mapSlideSorterKey(e, {
        canEdit: current.canEdit,
        hasMultiSelection: current.selectedSlideIds.length > 1,
        isTextInputTarget: isEditorTextInputTarget(e.target)
      });
      if (action === null) {
        return;
      }
      switch (action) {
        case "collapseSelection": {
          e.stopPropagation();
          const activeSlide = current.slides[current.activeSlideIndex];
          current.setSelectedSlideIds((activeSlide == null ? void 0 : activeSlide.id) ? [activeSlide.id] : []);
          return;
        }
        case "close":
          e.stopPropagation();
          current.onClose();
          return;
        case "delete":
          e.preventDefault();
          if (current.selectedIndexes.length > 0) {
            current.handleDeleteSelected();
          }
          return;
        case "copy":
          e.preventDefault();
          current.handleCopySelected();
          return;
        case "paste":
          e.preventDefault();
          current.handlePaste();
          return;
        case "duplicate":
          e.preventDefault();
          current.handleDuplicateSelected();
          return;
        case "selectAll":
          e.preventDefault();
          current.handleSelectAll();
          return;
        case "zoomIn":
          e.preventDefault();
          current.setZoom((z) => clampSorterZoom(z + ZOOM_STEP));
          return;
        case "zoomOut":
          e.preventDefault();
          current.setZoom((z) => clampSorterZoom(z - ZOOM_STEP));
          break;
      }
    };
    window.addEventListener("keydown", handleKeyDown);
    return () => window.removeEventListener("keydown", handleKeyDown);
  }, []);
}
function useSlideSorterState(params) {
  const {
    slides,
    activeSlideIndex,
    canEdit,
    sectionGroups,
    onSelectSlide,
    onMoveSlide,
    onDeleteSlides,
    onDuplicateSlides,
    onToggleHideSlides,
    onClose
  } = params;
  const [dragOverIndex, setDragOverIndex] = (0, import_react2.useState)(null);
  const [selectedSlideIds, setSelectedSlideIds] = (0, import_react2.useState)(() => {
    const activeSlide = slides[activeSlideIndex];
    return (activeSlide == null ? void 0 : activeSlide.id) ? [activeSlide.id] : [];
  });
  const [lastClickedIndex, setLastClickedIndex] = (0, import_react2.useState)(activeSlideIndex);
  const [contextMenu, setContextMenu] = (0, import_react2.useState)(null);
  const [zoom, setZoom] = (0, import_react2.useState)(DEFAULT_ZOOM);
  const [clipboardSlideIds, setClipboardSlideIds] = (0, import_react2.useState)([]);
  const backdropRef = (0, import_react2.useRef)(null);
  const slideIdToIndex = (0, import_react2.useMemo)(() => {
    const map = /* @__PURE__ */ new Map();
    slides.forEach((s, i) => map.set(s.id, i));
    return map;
  }, [slides]);
  const selectedIndexes = (0, import_react2.useMemo)(
    () => selectedSlideIds.map((id) => slideIdToIndex.get(id)).filter((i) => i !== void 0).sort((a, b) => a - b),
    [selectedSlideIds, slideIdToIndex]
  );
  const isSelected = (0, import_react2.useCallback)(
    (slideId) => selectedSlideIds.includes(slideId),
    [selectedSlideIds]
  );
  const handleSlideClick = (0, import_react2.useCallback)(
    (e, index) => {
      var _a;
      const slide = slides[index];
      if (!slide) {
        return;
      }
      if (e.ctrlKey || e.metaKey) {
        setSelectedSlideIds(
          (prev) => prev.includes(slide.id) ? prev.filter((id) => id !== slide.id) : [...prev, slide.id]
        );
        setLastClickedIndex(index);
      } else if (e.shiftKey) {
        const start = Math.min(lastClickedIndex, index);
        const end = Math.max(lastClickedIndex, index);
        const rangeIds = [];
        for (let i = start; i <= end; i++) {
          if ((_a = slides[i]) == null ? void 0 : _a.id) {
            rangeIds.push(slides[i].id);
          }
        }
        setSelectedSlideIds(rangeIds);
      } else {
        setSelectedSlideIds([slide.id]);
        setLastClickedIndex(index);
      }
    },
    [slides, lastClickedIndex]
  );
  const handleContextMenu = (0, import_react2.useCallback)(
    (e, index) => {
      e.preventDefault();
      e.stopPropagation();
      const slide = slides[index];
      if (!slide) {
        return;
      }
      if (!selectedSlideIds.includes(slide.id)) {
        setSelectedSlideIds([slide.id]);
        setLastClickedIndex(index);
      }
      setContextMenu({ x: e.clientX, y: e.clientY, slideIndex: index });
    },
    [slides, selectedSlideIds]
  );
  const closeContextMenu = (0, import_react2.useCallback)(() => {
    setContextMenu(null);
  }, []);
  const handleDeleteSelected = (0, import_react2.useCallback)(() => {
    if (selectedIndexes.length === 0) {
      return;
    }
    onDeleteSlides(selectedIndexes);
    setSelectedSlideIds([]);
    closeContextMenu();
  }, [selectedIndexes, onDeleteSlides, closeContextMenu]);
  const handleDuplicateSelected = (0, import_react2.useCallback)(() => {
    if (selectedIndexes.length === 0) {
      return;
    }
    onDuplicateSlides(selectedIndexes);
    closeContextMenu();
  }, [selectedIndexes, onDuplicateSlides, closeContextMenu]);
  const handleCopySelected = (0, import_react2.useCallback)(() => {
    setClipboardSlideIds([...selectedSlideIds]);
    closeContextMenu();
  }, [selectedSlideIds, closeContextMenu]);
  const handlePaste = (0, import_react2.useCallback)(() => {
    if (clipboardSlideIds.length === 0) {
      return;
    }
    const indexes = clipboardSlideIds.map((id) => slideIdToIndex.get(id)).filter((i) => i !== void 0).sort((a, b) => a - b);
    if (indexes.length > 0) {
      onDuplicateSlides(indexes);
    }
    closeContextMenu();
  }, [clipboardSlideIds, slideIdToIndex, onDuplicateSlides, closeContextMenu]);
  const handleToggleHideSelected = (0, import_react2.useCallback)(() => {
    if (selectedIndexes.length === 0) {
      return;
    }
    onToggleHideSlides(selectedIndexes);
    closeContextMenu();
  }, [selectedIndexes, onToggleHideSlides, closeContextMenu]);
  const handleSelectAll = (0, import_react2.useCallback)(() => {
    setSelectedSlideIds(slides.map((s) => s.id));
  }, [slides]);
  useKeyboardShortcuts2({
    slides,
    activeSlideIndex,
    canEdit,
    selectedSlideIds,
    selectedIndexes,
    contextMenu,
    setContextMenu,
    setSelectedSlideIds,
    setZoom,
    onClose,
    handleDeleteSelected,
    handleCopySelected,
    handlePaste,
    handleDuplicateSelected,
    handleSelectAll
  });
  const handleBackdropClick = (0, import_react2.useCallback)(
    (e) => {
      if (e.target === backdropRef.current) {
        onClose();
      }
    },
    [onClose]
  );
  const handleDragStart = (0, import_react2.useCallback)((e, index) => {
    e.dataTransfer.setData("text/plain", String(index));
    e.dataTransfer.effectAllowed = "move";
  }, []);
  const handleDragOver = (0, import_react2.useCallback)((e, index) => {
    e.preventDefault();
    e.dataTransfer.dropEffect = "move";
    setDragOverIndex(index);
  }, []);
  const handleDragLeave = (0, import_react2.useCallback)(() => setDragOverIndex(null), []);
  const handleDrop = (0, import_react2.useCallback)(
    (e, toIndex) => {
      e.preventDefault();
      setDragOverIndex(null);
      const fromIndex = parseInt(e.dataTransfer.getData("text/plain"), 10);
      if (!isNaN(fromIndex) && fromIndex !== toIndex) {
        onMoveSlide(fromIndex, toIndex);
      }
    },
    [onMoveSlide]
  );
  const handleDoubleClick = (0, import_react2.useCallback)((index) => onSelectSlide(index), [onSelectSlide]);
  const zoomScale = zoom / 100;
  const gridCols = (0, import_react2.useMemo)(() => {
    if (zoomScale >= 1.8) {
      return 2;
    }
    if (zoomScale >= 1.4) {
      return 3;
    }
    if (zoomScale >= 1) {
      return 4;
    }
    if (zoomScale >= 0.7) {
      return 5;
    }
    return 6;
  }, [zoomScale]);
  const showSectionHeaders = sectionGroups.length > 1;
  const hasHiddenInSelection = (0, import_react2.useMemo)(
    () => selectedIndexes.some((i) => {
      var _a;
      return (_a = slides[i]) == null ? void 0 : _a.hidden;
    }),
    [selectedIndexes, slides]
  );
  const hasVisibleInSelection = (0, import_react2.useMemo)(
    () => selectedIndexes.some((i) => {
      var _a;
      return !((_a = slides[i]) == null ? void 0 : _a.hidden);
    }),
    [selectedIndexes, slides]
  );
  return {
    dragOverIndex,
    selectedSlideIds,
    contextMenu,
    zoom,
    setZoom,
    clipboardSlideIds,
    backdropRef,
    selectedIndexes,
    isSelected,
    handleSlideClick,
    handleContextMenu,
    closeContextMenu,
    handleDeleteSelected,
    handleDuplicateSelected,
    handleCopySelected,
    handlePaste,
    handleToggleHideSelected,
    handleBackdropClick,
    handleDragStart,
    handleDragOver,
    handleDragLeave,
    handleDrop,
    handleDoubleClick,
    zoomScale,
    gridCols,
    showSectionHeaders,
    hasHiddenInSelection,
    hasVisibleInSelection
  };
}
function SlideSorterOverlay({
  slides,
  activeSlideIndex,
  canvasSize,
  canEdit,
  sectionGroups,
  onSelectSlide,
  onMoveSlide,
  onDeleteSlides,
  onDuplicateSlides,
  onToggleHideSlides,
  onClose
}) {
  const { t } = useTranslation();
  const state = useSlideSorterState({
    slides,
    activeSlideIndex,
    canEdit,
    sectionGroups,
    onSelectSlide,
    onMoveSlide,
    onDeleteSlides,
    onDuplicateSlides,
    onToggleHideSlides,
    onClose
  });
  const renderSlideCard = (slide, index) => (0, import_jsx_runtime.jsx)(
    SlideCard,
    {
      slide,
      index,
      isActive: index === activeSlideIndex,
      isDragTarget: state.dragOverIndex === index,
      isSelected: state.isSelected(slide.id),
      selectedCount: state.selectedSlideIds.length,
      selectionOrder: state.selectedIndexes.indexOf(index) + 1,
      canvasSize,
      canEdit,
      onSlideClick: state.handleSlideClick,
      onDoubleClick: state.handleDoubleClick,
      onContextMenu: state.handleContextMenu,
      onDragStart: state.handleDragStart,
      onDragOver: state.handleDragOver,
      onDragLeave: state.handleDragLeave,
      onDrop: state.handleDrop
    },
    slide.id || index
  );
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: state.backdropRef,
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex flex-col bg-black/70 backdrop-blur-sm",
      onClick: state.handleBackdropClick,
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-6 py-3 border-b border-border/50", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3", children: [
            (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-medium text-foreground", children: t("pptx.slideSorter.title") }),
            state.selectedSlideIds.length > 1 && (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: t("pptx.slideSorter.selectedCount", {
              count: state.selectedSlideIds.length
            }) })
          ] }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "flex h-8 w-8 items-center justify-center rounded-full bg-muted text-foreground hover:bg-accent hover:text-white transition-colors",
              "aria-label": t("pptx.slideSorter.close"),
              children: (0, import_jsx_runtime.jsxs)(
                "svg",
                {
                  xmlns: "http://www.w3.org/2000/svg",
                  width: "16",
                  height: "16",
                  viewBox: "0 0 24 24",
                  fill: "none",
                  stroke: "currentColor",
                  strokeWidth: "2",
                  strokeLinecap: "round",
                  strokeLinejoin: "round",
                  children: [
                    (0, import_jsx_runtime.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
                    (0, import_jsx_runtime.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
                  ]
                }
              )
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-y-auto px-6 py-6", children: (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "mx-auto",
            style: {
              maxWidth: `${Math.max(600, state.gridCols * 220 * state.zoomScale)}px`
            },
            children: state.showSectionHeaders ? sectionGroups.map((section) => (0, import_jsx_runtime.jsx)(
              SectionBlock,
              {
                section,
                slides,
                gridCols: state.gridCols,
                zoomScale: state.zoomScale,
                renderSlideCard
              },
              section.id
            )) : (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "grid gap-4",
                style: {
                  gridTemplateColumns: `repeat(${state.gridCols}, minmax(0, 1fr))`
                },
                children: slides.map((slide, index) => renderSlideCard(slide, index))
              }
            )
          }
        ) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-6 py-2.5 border-t border-border/50 bg-background/50", children: [
          (0, import_jsx_runtime.jsx)("div", { className: "text-xs text-muted-foreground", children: t("pptx.slideSorter.slideCount", { count: slides.length }) }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "rounded p-1 text-muted-foreground hover:bg-accent hover:text-foreground disabled:opacity-40",
                disabled: state.zoom <= MIN_ZOOM,
                onClick: () => state.setZoom((z) => Math.max(MIN_ZOOM, z - ZOOM_STEP)),
                "aria-label": t("pptx.slideSorter.zoomOut"),
                children: (0, import_jsx_runtime.jsx)(LuMinus, { className: "h-3.5 w-3.5" })
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "range",
                min: MIN_ZOOM,
                max: MAX_ZOOM,
                step: ZOOM_STEP,
                value: state.zoom,
                onChange: (e) => state.setZoom(Number(e.target.value)),
                className: "h-1 w-24 cursor-pointer appearance-none rounded-full bg-accent accent-primary",
                "aria-label": t("pptx.slideSorter.zoom")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "rounded p-1 text-muted-foreground hover:bg-accent hover:text-foreground disabled:opacity-40",
                disabled: state.zoom >= MAX_ZOOM,
                onClick: () => state.setZoom((z) => Math.min(MAX_ZOOM, z + ZOOM_STEP)),
                "aria-label": t("pptx.slideSorter.zoomIn"),
                children: (0, import_jsx_runtime.jsx)(LuPlus, { className: "h-3.5 w-3.5" })
              }
            ),
            (0, import_jsx_runtime.jsxs)("span", { className: "min-w-[3ch] text-right text-xs text-muted-foreground", children: [
              state.zoom,
              "%"
            ] })
          ] })
        ] }),
        state.contextMenu && canEdit && (0, import_jsx_runtime.jsx)(
          SorterContextMenu,
          {
            x: state.contextMenu.x,
            y: state.contextMenu.y,
            selectedCount: state.selectedSlideIds.length,
            hasClipboard: state.clipboardSlideIds.length > 0,
            hasHiddenInSelection: state.hasHiddenInSelection,
            hasVisibleInSelection: state.hasVisibleInSelection,
            sectionGroups: state.showSectionHeaders ? sectionGroups : [],
            onDelete: state.handleDeleteSelected,
            onDuplicate: state.handleDuplicateSelected,
            onCopy: state.handleCopySelected,
            onPaste: state.handlePaste,
            onToggleHide: state.handleToggleHideSelected,
            onClose: state.closeContextMenu
          }
        )
      ]
    }
  );
}
function useOutlineView(input) {
  const { slides, canvasSize, canEdit, setSlides, setActiveSlideIndex, bumpHistory } = input;
  const rows = (0, import_react2.useMemo)(() => buildOutline(slides), [slides]);
  const containerRef = (0, import_react2.useRef)(null);
  const pendingFocusRef = (0, import_react2.useRef)(null);
  const run = (0, import_react2.useCallback)(
    (edit) => {
      if (!canEdit) {
        return;
      }
      const result = applyOutlineEdit(slides, edit, { canvas: canvasSize });
      if (!result.changed) {
        return;
      }
      setSlides(result.slides);
      setActiveSlideIndex(result.activeSlideIndex);
      bumpHistory();
      pendingFocusRef.current = result.focusKey;
    },
    [bumpHistory, canEdit, canvasSize, setActiveSlideIndex, setSlides, slides]
  );
  const onRowKeyDown = (0, import_react2.useCallback)(
    (event, rowKey) => {
      const { edit, preventDefault } = mapOutlineKey(event, rowKey);
      if (preventDefault) {
        event.preventDefault();
      }
      if (edit) {
        run(edit);
      }
    },
    [run]
  );
  (0, import_react2.useEffect)(() => {
    var _a;
    const key = pendingFocusRef.current;
    if (!key) {
      return;
    }
    pendingFocusRef.current = null;
    const selector = `[${OUTLINE_ROW_ATTR}="${CSS.escape(key)}"]`;
    const target = (_a = containerRef.current) == null ? void 0 : _a.querySelector(selector);
    if (target && document.activeElement !== target) {
      target.focus();
      target.setSelectionRange(target.value.length, target.value.length);
    }
  }, [rows]);
  return { rows, containerRef, run, onRowKeyDown };
}
var INDENT_PX2 = 22;
function OutlineViewOverlay({
  slides,
  canvasSize,
  canEdit,
  setSlides,
  setActiveSlideIndex,
  bumpHistory,
  onClose
}) {
  const { t } = useTranslation();
  const { rows, containerRef, run, onRowKeyDown } = useOutlineView({
    slides,
    canvasSize,
    canEdit,
    setSlides,
    setActiveSlideIndex,
    bumpHistory
  });
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ...{ [OUTLINE_VIEW_ATTR]: "true" },
      role: "region",
      "aria-label": t("pptx.view.outlineView"),
      className: "fixed inset-0 z-[1300] flex flex-col bg-neutral-900 text-neutral-100",
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 border-b border-white/10 px-4 py-2", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-sm font-semibold", children: t("pptx.view.outlineView") }),
          (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate text-[11px] text-white/50", children: t("pptx.outline.hint") }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "inline-flex h-8 w-8 items-center justify-center rounded text-white/80 transition-colors hover:bg-white/15 hover:text-white",
              "aria-label": t("pptx.statusBar.normalView"),
              title: t("pptx.statusBar.normalView"),
              onClick: onClose,
              children: (0, import_jsx_runtime.jsx)(LuX, {})
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)("div", { ref: containerRef, className: "min-h-0 flex-1 overflow-auto px-4 py-3", children: rows.map((row) => (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: "flex items-center gap-2 py-0.5",
            style: { paddingLeft: row.level * INDENT_PX2 },
            children: [
              (0, import_jsx_runtime.jsx)("span", { className: "w-6 shrink-0 text-right text-[10px] tabular-nums text-white/40", children: row.kind === "title" ? row.slideIndex + 1 : "" }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  ...{
                    [OUTLINE_ROW_ATTR]: row.key,
                    [OUTLINE_SLIDE_ATTR]: String(row.slideIndex + 1),
                    [OUTLINE_LEVEL_ATTR]: String(row.level)
                  },
                  type: "text",
                  value: row.text,
                  readOnly: !canEdit,
                  "aria-label": t(
                    row.kind === "title" ? "pptx.outline.titleLine" : "pptx.outline.bodyLine"
                  ),
                  className: row.kind === "title" ? "w-full rounded bg-transparent px-1 py-0.5 text-sm font-semibold outline-none focus:bg-white/10" : "w-full rounded bg-transparent px-1 py-0.5 text-[13px] text-white/80 outline-none focus:bg-white/10",
                  onChange: (event) => run({ type: "setText", key: row.key, text: event.target.value }),
                  onKeyDown: (event) => onRowKeyDown(event, row.key)
                }
              )
            ]
          },
          row.key
        )) })
      ]
    }
  );
}
function ReadingViewStage({
  slide,
  templateElements,
  canvasSize,
  scale
}) {
  if (scale <= 0) {
    return null;
  }
  const width = Math.max(canvasSize.width, 1);
  const height = Math.max(canvasSize.height, 1);
  const elements = [...templateElements, ...slide.elements];
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ...{ [READING_VIEW_STAGE_ATTR]: "true" },
      "aria-roledescription": "slide",
      className: "relative overflow-hidden bg-white shadow-2xl",
      style: { width: width * scale, height: height * scale },
      children: [
        slide.backgroundColor && slide.backgroundColor !== "transparent" && (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute inset-0",
            style: { backgroundColor: normalizeHexColor2(slide.backgroundColor, "#ffffff") }
          }
        ),
        slide.backgroundImage && (0, import_jsx_runtime.jsx)(
          "img",
          {
            src: slide.backgroundImage,
            alt: "",
            className: "absolute inset-0 h-full w-full object-cover",
            draggable: false
          }
        ),
        slide.backgroundGradient && (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0", style: { backgroundImage: slide.backgroundGradient } }),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute left-0 top-0 origin-top-left",
            style: { width, height, transform: `scale(${scale})` },
            children: elements.map((element, index) => (0, import_jsx_runtime.jsx)(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index
              },
              element.id
            ))
          }
        )
      ]
    }
  );
}
var READING_VIEW_PADDING = 24;
function useReadingView(input) {
  const { slideCount, canvasSize, initialSlideIndex, onExit } = input;
  const [state, setState] = (0, import_react2.useState)(
    () => openReadingView(initialSlideIndex, slideCount)
  );
  const viewportRef = (0, import_react2.useRef)(null);
  const [viewport, setViewport] = (0, import_react2.useState)({ width: 0, height: 0 });
  const onExitRef = (0, import_react2.useRef)(onExit);
  onExitRef.current = onExit;
  const run = (0, import_react2.useCallback)(
    (command) => {
      setState((previous) => {
        const next = applyReadingViewCommand(previous, command, slideCount);
        if (previous.open && !next.open) {
          onExitRef.current(previous.slideIndex);
        }
        return next;
      });
    },
    [slideCount]
  );
  (0, import_react2.useEffect)(() => {
    const buffer = createPresentationKeyBuffer();
    const handle = (event) => {
      const { command, swallow, preventDefault } = handleReadingViewKey(event, buffer);
      if (swallow) {
        event.stopPropagation();
      }
      if (preventDefault) {
        event.preventDefault();
      }
      if (command.command !== "none") {
        run(command);
      }
    };
    window.addEventListener("keydown", handle, true);
    return () => window.removeEventListener("keydown", handle, true);
  }, [run]);
  (0, import_react2.useEffect)(() => {
    const element = viewportRef.current;
    if (!element) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      var _a;
      const rect = (_a = entries[0]) == null ? void 0 : _a.contentRect;
      if (rect) {
        setViewport({ width: rect.width, height: rect.height });
      }
    });
    observer.observe(element);
    return () => observer.disconnect();
  }, []);
  const scale = (0, import_react2.useMemo)(
    () => readingViewFitScale(canvasSize, viewport, READING_VIEW_PADDING),
    [canvasSize, viewport]
  );
  return { state, viewportRef, scale, run };
}
function ReadingViewOverlay({
  slides,
  templateElements,
  canvasSize,
  activeSlideIndex,
  onExit
}) {
  const { t } = useTranslation();
  const { state, viewportRef, scale, run } = useReadingView({
    slideCount: slides.length,
    canvasSize,
    initialSlideIndex: activeSlideIndex,
    onExit
  });
  const slide = slides[state.slideIndex];
  if (!state.open || !slide) {
    return null;
  }
  const control = "inline-flex h-8 w-8 items-center justify-center rounded text-white/80 transition-colors hover:bg-white/15 hover:text-white disabled:cursor-default disabled:opacity-30 disabled:hover:bg-transparent";
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ...{ [READING_VIEW_ATTR]: "true" },
      role: "region",
      "aria-label": t("pptx.view.readingView"),
      className: "fixed inset-0 z-[1300] flex flex-col bg-neutral-900",
      children: [
        (0, import_jsx_runtime.jsx)("div", { ref: viewportRef, className: "flex min-h-0 flex-1 items-center justify-center", children: (0, import_jsx_runtime.jsx)(
          ReadingViewStage,
          {
            slide,
            templateElements,
            canvasSize,
            scale
          }
        ) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-center gap-3 border-t border-white/10 px-4 py-2", children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: control,
              "aria-label": t("pptx.common.previous"),
              title: t("pptx.common.previous"),
              disabled: !canGoPrevious(state),
              onClick: () => run({ command: "previous" }),
              children: (0, import_jsx_runtime.jsx)(LuChevronLeft, {})
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "span",
            {
              ...{ [READING_VIEW_COUNTER_ATTR]: "true" },
              className: "min-w-16 text-center text-xs tabular-nums text-white/70",
              children: formatSlideCounter(state.slideIndex, slides.length)
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: control,
              "aria-label": t("pptx.common.next"),
              title: t("pptx.common.next"),
              disabled: !canGoNext(state, slides.length),
              onClick: () => run({ command: "next" }),
              children: (0, import_jsx_runtime.jsx)(LuChevronRight, {})
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: control,
              "aria-label": t("pptx.statusBar.normalView"),
              title: t("pptx.statusBar.normalView"),
              onClick: () => run({ command: "exit" }),
              children: (0, import_jsx_runtime.jsx)(LuX, {})
            }
          )
        ] })
      ]
    }
  );
}
function renderRichNotesSegments(segments) {
  const paragraphs = segmentsToParagraphs(segments);
  const nodes = [];
  let numberedCounter = 0;
  paragraphs.forEach((para, pIdx) => {
    if (para.bulletType === "numbered") {
      numberedCounter++;
    } else {
      numberedCounter = 0;
    }
    const divStyle = {};
    if (para.indentLevel > 0) {
      divStyle.paddingLeft = para.indentLevel * INDENT_PX;
    }
    const children = [];
    if (para.bulletType === "bullet") {
      children.push(
        (0, import_jsx_runtime.jsx)("span", { style: { marginRight: 6, color: "#9ca3af" }, children: "•" }, "bullet")
      );
    } else if (para.bulletType === "numbered") {
      children.push(
        (0, import_jsx_runtime.jsxs)("span", { style: { marginRight: 6, color: "#9ca3af" }, children: [
          numberedCounter,
          "."
        ] }, "num")
      );
    }
    para.segments.forEach((segment, sIdx) => {
      if (segment.isParagraphBreak) {
        return;
      }
      const style = {};
      if (segment.style.bold) {
        style.fontWeight = "bold";
      }
      if (segment.style.italic) {
        style.fontStyle = "italic";
      }
      if (segment.style.underline) {
        style.textDecoration = "underline";
      }
      if (segment.style.strikethrough) {
        style.textDecoration = `${style.textDecoration ? `${style.textDecoration} ` : ""}line-through`;
      }
      if (segment.style.color) {
        style.color = segment.style.color;
      }
      if (segment.style.fontSize) {
        style.fontSize = `${segment.style.fontSize}pt`;
      }
      if (segment.style.fontFamily) {
        style.fontFamily = segment.style.fontFamily;
      }
      if (segment.style.hyperlink && isUrlSafe(segment.style.hyperlink)) {
        const safeHref = segment.style.hyperlink;
        style.color = "#4a9eff";
        style.textDecoration = "underline";
        style.cursor = "pointer";
        children.push(
          (0, import_jsx_runtime.jsx)(
            "a",
            {
              href: safeHref,
              style,
              onClick: (e) => {
                e.preventDefault();
                safeOpenUrl(safeHref);
              },
              children: segment.text
            },
            `seg-${pIdx}-${sIdx}`
          )
        );
      } else {
        children.push(
          (0, import_jsx_runtime.jsx)("span", { style, children: segment.text }, `seg-${pIdx}-${sIdx}`)
        );
      }
    });
    nodes.push(
      (0, import_jsx_runtime.jsx)("div", { style: divStyle, children: children.length > 0 ? children : (0, import_jsx_runtime.jsx)("br", {}) }, `p-${pIdx}`)
    );
  });
  return nodes;
}
function NotesPrintDialog({
  slides,
  onClose,
  notesStyle
}) {
  const { t } = useTranslation();
  const printFrameRef = (0, import_react2.useRef)(null);
  const handlePrint = (0, import_react2.useCallback)(() => {
    const iframe = printFrameRef.current;
    if (!(iframe == null ? void 0 : iframe.contentWindow)) {
      return;
    }
    const doc = iframe.contentWindow.document;
    doc.open();
    doc.write(buildNotesPrintHtml(slides, (n) => t("pptx.notes.slideN", { n }), notesStyle));
    doc.close();
    setTimeout(() => {
      var _a;
      (_a = iframe.contentWindow) == null ? void 0 : _a.print();
    }, 200);
  }, [slides, t, notesStyle]);
  return (0, import_jsx_runtime.jsx)("div", { className: "fixed inset-0 z-50 flex items-center justify-center bg-black/60", children: (0, import_jsx_runtime.jsxs)("div", { className: "bg-background border border-border rounded-lg shadow-xl w-[640px] max-h-[80vh] flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-4 py-3 border-b border-border", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-sm font-medium text-foreground", children: t("pptx.notes.printNotes") }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "text-muted-foreground hover:text-foreground text-sm",
          children: t("pptx.common.close")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-y-auto p-4 space-y-4", children: slides.map((slide) => {
      const segs = resolveNotesSegments(slide, notesStyle);
      const hasText = segmentsToPlainText(segs).trim().length > 0;
      return (0, import_jsx_runtime.jsxs)("div", { className: "border border-border/50 rounded p-3", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "text-xs font-medium text-muted-foreground mb-2", children: t("pptx.notes.slideN", { n: slide.slideNumber }) }),
        (0, import_jsx_runtime.jsx)("div", { className: "w-full aspect-video bg-muted rounded mb-2 flex items-center justify-center text-muted-foreground text-sm", children: t("pptx.notes.slideN", { n: slide.slideNumber }) }),
        (0, import_jsx_runtime.jsx)("div", { className: "text-xs text-foreground whitespace-pre-wrap", children: hasText ? renderRichNotesSegments(segs) : (0, import_jsx_runtime.jsx)("span", { className: "italic text-muted-foreground", children: t("pptx.notes.noNotes") }) })
      ] }, slide.id);
    }) }),
    (0, import_jsx_runtime.jsx)("div", { className: "flex justify-end px-4 py-3 border-t border-border", children: (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: handlePrint,
        className: "px-3 py-1.5 text-xs bg-primary hover:bg-primary/80 text-white rounded",
        children: t("pptx.notes.print")
      }
    ) }),
    (0, import_jsx_runtime.jsx)(
      "iframe",
      {
        ref: printFrameRef,
        title: "print-notes",
        className: "hidden",
        sandbox: "allow-same-origin"
      }
    )
  ] }) });
}
function HyperlinkPopover({
  initialText,
  onInsert,
  onClose
}) {
  const { t } = useTranslation();
  const [url, setUrl] = (0, import_react2.useState)("");
  const [displayText, setDisplayText] = (0, import_react2.useState)(initialText);
  const urlRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    var _a;
    (_a = urlRef.current) == null ? void 0 : _a.focus();
  }, []);
  const handleSubmit = (0, import_react2.useCallback)(
    (e) => {
      e.preventDefault();
      if (url.trim().length === 0) {
        return;
      }
      const finalUrl = url.startsWith("http") ? url : `https://${url}`;
      onInsert(finalUrl, displayText || finalUrl);
    },
    [url, displayText, onInsert]
  );
  return (0, import_jsx_runtime.jsx)("div", { className: "absolute bottom-full left-0 mb-1 z-10 bg-muted border border-border rounded-lg shadow-lg p-3 w-72", children: (0, import_jsx_runtime.jsxs)("form", { onSubmit: handleSubmit, className: "space-y-2", children: [
    (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-[10px] text-muted-foreground mb-0.5", children: t("pptx.notes.linkUrl") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          ref: urlRef,
          type: "text",
          value: url,
          onChange: (e) => setUrl(e.target.value),
          placeholder: "https://...",
          className: "w-full px-2 py-1 text-xs bg-background border border-border rounded text-foreground focus:outline-none focus:border-primary"
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-[10px] text-muted-foreground mb-0.5", children: t("pptx.notes.linkDisplayText") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          value: displayText,
          onChange: (e) => setDisplayText(e.target.value),
          placeholder: t("pptx.notes.linkDisplayText"),
          className: "w-full px-2 py-1 text-xs bg-background border border-border rounded text-foreground focus:outline-none focus:border-primary"
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "px-2 py-1 text-[10px] text-muted-foreground hover:text-foreground",
          children: t("pptx.common.cancel")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "submit",
          className: "px-2 py-1 text-[10px] bg-primary hover:bg-primary/80 text-white rounded",
          children: t("pptx.notes.insertLink")
        }
      )
    ] })
  ] }) });
}
function NotesToolbar({
  isRichEditEnabled,
  showLinkPopover,
  savedSelectionText,
  hasAllSlides,
  onApplyRichCommand,
  onToggleBulletList,
  onToggleNumberedList,
  onIndent,
  onOutdent,
  onLinkButtonClick,
  onInsertLink,
  onCloseLinkPopover,
  onPrintClick,
  onToggleRichEdit
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "mb-1 flex items-center justify-between gap-2", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "inline-flex items-center rounded bg-muted text-xs overflow-hidden border border-border/60 relative", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.bold"),
          onClick: () => onApplyRichCommand("bold"),
          children: (0, import_jsx_runtime.jsx)(LuBold, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.italic"),
          onClick: () => onApplyRichCommand("italic"),
          children: (0, import_jsx_runtime.jsx)(LuItalic, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.underline"),
          onClick: () => onApplyRichCommand("underline"),
          children: (0, import_jsx_runtime.jsx)(LuUnderline, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.strikethrough"),
          onClick: () => onApplyRichCommand("strikeThrough"),
          children: (0, import_jsx_runtime.jsx)(LuStrikethrough, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "w-px h-4 bg-border mx-0.5" }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.bulletList"),
          onClick: onToggleBulletList,
          children: (0, import_jsx_runtime.jsx)(LuList, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.numberedList"),
          onClick: onToggleNumberedList,
          children: (0, import_jsx_runtime.jsx)(LuListOrdered, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "w-px h-4 bg-border mx-0.5" }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.indent"),
          onClick: onIndent,
          children: (0, import_jsx_runtime.jsx)(LuIndentIncrease, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.outdent"),
          onClick: onOutdent,
          children: (0, import_jsx_runtime.jsx)(LuIndentDecrease, { className: "w-3.5 h-3.5" })
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "w-px h-4 bg-border mx-0.5" }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent",
          title: t("pptx.notes.insertLink"),
          onClick: onLinkButtonClick,
          children: (0, import_jsx_runtime.jsx)(LuLink, { className: "w-3.5 h-3.5" })
        }
      ),
      hasAllSlides && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "px-2 py-1 hover:bg-accent border-l border-border/60",
          title: t("pptx.notes.printNotes"),
          onClick: onPrintClick,
          children: (0, import_jsx_runtime.jsx)(LuPrinter, { className: "w-3.5 h-3.5" })
        }
      ),
      showLinkPopover && (0, import_jsx_runtime.jsx)(
        HyperlinkPopover,
        {
          initialText: savedSelectionText,
          onInsert: onInsertLink,
          onClose: onCloseLinkPopover
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: "text-[10px] px-2 py-1 rounded bg-muted hover:bg-accent border border-border/60",
        onClick: onToggleRichEdit,
        title: t("pptx.notes.toggleRichPlainEditor"),
        children: isRichEditEnabled ? t("pptx.notes.plainEditor") : t("pptx.notes.richEditor")
      }
    )
  ] });
}
function useSlideNotes({
  activeSlide,
  isExpanded,
  canEdit,
  onToggle,
  onUpdateNotes,
  notesStyle
}) {
  const initialSegments = (0, import_react2.useMemo)(
    () => resolveNotesSegments(activeSlide, notesStyle),
    [activeSlide, notesStyle]
  );
  const [draft, setDraft] = (0, import_react2.useState)((activeSlide == null ? void 0 : activeSlide.notes) ?? "");
  const [draftSegments, setDraftSegments] = (0, import_react2.useState)(initialSegments);
  const [isRichEditEnabled, setIsRichEditEnabled] = (0, import_react2.useState)(initialSegments.length > 0);
  const [showLinkPopover, setShowLinkPopover] = (0, import_react2.useState)(false);
  const [showPrintDialog, setShowPrintDialog] = (0, import_react2.useState)(false);
  const debounceRef = (0, import_react2.useRef)(null);
  const textareaRef = (0, import_react2.useRef)(null);
  const richEditorRef = (0, import_react2.useRef)(null);
  const savedSelectionRef = (0, import_react2.useRef)(null);
  const isInternalInputRef = (0, import_react2.useRef)(false);
  const lastSavedTextRef = (0, import_react2.useRef)(null);
  const activeSlideRef = (0, import_react2.useRef)(activeSlide);
  activeSlideRef.current = activeSlide;
  (0, import_react2.useEffect)(() => {
    const nextText = (activeSlide == null ? void 0 : activeSlide.notes) ?? "";
    if (nextText === lastSavedTextRef.current) {
      return;
    }
    const nextSegments = resolveNotesSegments(activeSlideRef.current, notesStyle);
    setDraft(nextText);
    setDraftSegments(nextSegments);
    setIsRichEditEnabled(nextSegments.length > 0);
  }, [activeSlide == null ? void 0 : activeSlide.id, activeSlide == null ? void 0 : activeSlide.notes, activeSlide == null ? void 0 : activeSlide.notesSegments, notesStyle]);
  (0, import_react2.useEffect)(() => {
    if (!isRichEditEnabled || !isExpanded || !canEdit || !richEditorRef.current) {
      return;
    }
    if (isInternalInputRef.current) {
      isInternalInputRef.current = false;
      return;
    }
    richEditorRef.current.innerHTML = segmentsToEditorHtml(draftSegments);
  }, [canEdit, draftSegments, isExpanded, isRichEditEnabled]);
  const flush = (0, import_react2.useCallback)(
    (value, segments) => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
        debounceRef.current = null;
      }
      lastSavedTextRef.current = value;
      onUpdateNotes(value, segments);
    },
    [onUpdateNotes]
  );
  const scheduleSave = (0, import_react2.useCallback)(
    (value, segments) => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
      }
      debounceRef.current = setTimeout(() => {
        lastSavedTextRef.current = value;
        onUpdateNotes(value, segments);
        debounceRef.current = null;
      }, DEBOUNCE_MS);
    },
    [onUpdateNotes]
  );
  (0, import_react2.useEffect)(() => {
    return () => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
      }
    };
  }, []);
  const handlePlainChange = (0, import_react2.useCallback)(
    (e) => {
      const value = e.target.value;
      const segments = createPlainNotesSegments(value);
      setDraft(value);
      setDraftSegments(segments);
      scheduleSave(value, segments);
    },
    [scheduleSave]
  );
  const handleKeyDownPlain = (0, import_react2.useCallback)(
    (e) => {
      e.stopPropagation();
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
        flush(draft, draftSegments);
        onToggle();
      }
    },
    [draft, draftSegments, flush, onToggle]
  );
  const handleRichInput = (0, import_react2.useCallback)(() => {
    if (!richEditorRef.current) {
      return;
    }
    isInternalInputRef.current = true;
    const segments = parseSegmentsFromRichEditor(richEditorRef.current);
    const text = segmentsToPlainText(segments);
    setDraft(text);
    setDraftSegments(segments);
    scheduleSave(text, segments);
  }, [scheduleSave]);
  const handleBlur = (0, import_react2.useCallback)(() => {
    flush(draft, draftSegments);
  }, [draft, draftSegments, flush]);
  const updateParagraphProperty = (0, import_react2.useCallback)(
    (updater) => {
      if (!richEditorRef.current) {
        return;
      }
      const paraIdx = getCurrentParagraphIndex(richEditorRef.current, draftSegments);
      const paragraphs = segmentsToParagraphs(draftSegments);
      if (paraIdx >= 0 && paraIdx < paragraphs.length) {
        paragraphs[paraIdx] = updater(paragraphs[paraIdx], paraIdx);
      }
      const newSegments = paragraphsToSegments(paragraphs);
      const newText = segmentsToPlainText(newSegments);
      setDraft(newText);
      setDraftSegments(newSegments);
      scheduleSave(newText, newSegments);
    },
    [draftSegments, scheduleSave]
  );
  const applyRichCommand = (0, import_react2.useCallback)(
    (command) => {
      var _a;
      document.execCommand(command);
      handleRichInput();
      (_a = richEditorRef.current) == null ? void 0 : _a.focus();
    },
    [handleRichInput]
  );
  const toggleBulletList = (0, import_react2.useCallback)(() => {
    var _a;
    updateParagraphProperty((para) => ({
      ...para,
      bulletType: para.bulletType === "bullet" ? "none" : "bullet"
    }));
    (_a = richEditorRef.current) == null ? void 0 : _a.focus();
  }, [updateParagraphProperty]);
  const toggleNumberedList = (0, import_react2.useCallback)(() => {
    var _a;
    updateParagraphProperty((para) => ({
      ...para,
      bulletType: para.bulletType === "numbered" ? "none" : "numbered"
    }));
    (_a = richEditorRef.current) == null ? void 0 : _a.focus();
  }, [updateParagraphProperty]);
  const handleIndent = (0, import_react2.useCallback)(() => {
    var _a;
    updateParagraphProperty((para) => ({
      ...para,
      indentLevel: Math.min(MAX_INDENT_LEVEL, para.indentLevel + 1)
    }));
    (_a = richEditorRef.current) == null ? void 0 : _a.focus();
  }, [updateParagraphProperty]);
  const handleOutdent = (0, import_react2.useCallback)(() => {
    var _a;
    updateParagraphProperty((para) => ({
      ...para,
      indentLevel: Math.max(0, para.indentLevel - 1)
    }));
    (_a = richEditorRef.current) == null ? void 0 : _a.focus();
  }, [updateParagraphProperty]);
  const handleKeyDownRich = (0, import_react2.useCallback)(
    (e) => {
      e.stopPropagation();
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
        flush(draft, draftSegments);
        onToggle();
        return;
      }
      if (e.key === "Tab") {
        e.preventDefault();
        if (e.shiftKey) {
          handleOutdent();
        } else {
          handleIndent();
        }
      }
    },
    [draft, draftSegments, flush, onToggle, handleIndent, handleOutdent]
  );
  const handleLinkButtonClick = (0, import_react2.useCallback)(() => {
    const sel = window.getSelection();
    savedSelectionRef.current = { text: (sel == null ? void 0 : sel.toString()) ?? "" };
    setShowLinkPopover(true);
  }, []);
  const handleInsertLink = (0, import_react2.useCallback)(
    (url, displayText) => {
      setShowLinkPopover(false);
      if (!richEditorRef.current) {
        return;
      }
      richEditorRef.current.focus();
      const sel = window.getSelection();
      if (sel && sel.rangeCount > 0) {
        const range = sel.getRangeAt(0);
        range.deleteContents();
        const anchor = document.createElement("a");
        anchor.href = url;
        anchor.textContent = displayText;
        anchor.style.color = "#4a9eff";
        anchor.style.textDecoration = "underline";
        anchor.style.cursor = "pointer";
        anchor.setAttribute("data-hyperlink", url);
        range.insertNode(anchor);
        range.setStartAfter(anchor);
        range.collapse(true);
        sel.removeAllRanges();
        sel.addRange(range);
      }
      handleRichInput();
    },
    [handleRichInput]
  );
  const handleEditorClick = (0, import_react2.useCallback)((e) => {
    const target = e.target;
    if (!(target instanceof HTMLAnchorElement)) {
      return;
    }
    const href = target.getAttribute("data-hyperlink") || target.getAttribute("href");
    if (href && (e.ctrlKey || e.metaKey)) {
      e.preventDefault();
      safeOpenUrl(href);
    }
  }, []);
  return {
    draft,
    draftSegments,
    isRichEditEnabled,
    setIsRichEditEnabled,
    showLinkPopover,
    setShowLinkPopover,
    showPrintDialog,
    setShowPrintDialog,
    textareaRef,
    richEditorRef,
    savedSelectionRef,
    handlePlainChange,
    handleRichInput,
    handleBlur,
    handleKeyDownPlain,
    handleKeyDownRich,
    applyRichCommand,
    toggleBulletList,
    toggleNumberedList,
    handleIndent,
    handleOutdent,
    handleLinkButtonClick,
    handleInsertLink,
    handleEditorClick
  };
}
function SlideNotesPanel({
  activeSlide,
  allSlides,
  isExpanded,
  canEdit,
  onToggle,
  onUpdateNotes,
  panelHeight,
  notesStyle
}) {
  var _a;
  const { t } = useTranslation();
  const {
    draft,
    draftSegments,
    isRichEditEnabled,
    setIsRichEditEnabled,
    showLinkPopover,
    setShowLinkPopover,
    showPrintDialog,
    setShowPrintDialog,
    textareaRef,
    richEditorRef,
    savedSelectionRef,
    handlePlainChange,
    handleRichInput,
    handleBlur,
    handleKeyDownPlain,
    handleKeyDownRich,
    applyRichCommand,
    toggleBulletList,
    toggleNumberedList,
    handleIndent,
    handleOutdent,
    handleLinkButtonClick,
    handleInsertLink,
    handleEditorClick
  } = useSlideNotes({
    activeSlide,
    isExpanded,
    canEdit,
    onToggle,
    onUpdateNotes,
    notesStyle
  });
  const hasNotes = draft.trim().length > 0;
  const slideLabel = activeSlide ? t("pptx.notes.slideN", { n: activeSlide.slideNumber }) : t("pptx.notes.noSlide");
  const { dragY, handlers: dragHandlers } = useSheetDismissDrag(onToggle);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    isExpanded && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.common.close"),
        onClick: onToggle,
        className: "md:hidden fixed inset-0 z-20 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150"
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: cn(
          "flex flex-col border-t border-border/60 bg-background select-none",
          // On mobile, hide the entire notes strip when collapsed: the
          // MobileBottomBar's Notes button is the entry point instead.
          !isExpanded && "max-md:hidden",
          // On mobile, expanded notes render as a swipe-dismissable bottom
          // sheet (mirrors the Format/Comments sheets), not an inline strip.
          "max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:z-30 max-md:max-h-[75dvh] max-md:rounded-t-2xl max-md:border max-md:border-border max-md:shadow-2xl max-md:pb-[max(env(safe-area-inset-bottom),0px)]"
        ),
        style: dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : void 0,
        children: [
          isExpanded && (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "md:hidden flex items-center justify-center pt-3 pb-2 cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: dragHandlers.onPointerDown,
              onPointerMove: dragHandlers.onPointerMove,
              onPointerUp: dragHandlers.onPointerUp,
              onPointerCancel: dragHandlers.onPointerCancel,
              children: (0, import_jsx_runtime.jsx)("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" })
            }
          ),
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              onClick: onToggle,
              className: "flex items-center gap-1.5 px-3 py-1 text-[11px] text-muted-foreground hover:text-foreground hover:bg-accent/30 transition-colors w-full text-left shrink-0 max-md:hidden",
              "aria-expanded": isExpanded,
              "aria-controls": "slide-notes-content",
              children: [
                "Notes",
                !isExpanded && hasNotes && (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground/50 text-[10px]", children: "(has notes)" })
              ]
            }
          ),
          isExpanded && (0, import_jsx_runtime.jsxs)(
            "div",
            {
              id: "slide-notes-content",
              className: "px-3 pb-2 overflow-y-auto",
              style: { maxHeight: panelHeight ?? EXPANDED_MAX_HEIGHT + 40 },
              children: [
                (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: slideLabel }),
                canEdit ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                  (0, import_jsx_runtime.jsx)(
                    NotesToolbar,
                    {
                      isRichEditEnabled,
                      showLinkPopover,
                      savedSelectionText: ((_a = savedSelectionRef.current) == null ? void 0 : _a.text) ?? "",
                      hasAllSlides: allSlides !== void 0 && allSlides.length > 0,
                      onApplyRichCommand: applyRichCommand,
                      onToggleBulletList: toggleBulletList,
                      onToggleNumberedList: toggleNumberedList,
                      onIndent: handleIndent,
                      onOutdent: handleOutdent,
                      onLinkButtonClick: handleLinkButtonClick,
                      onInsertLink: handleInsertLink,
                      onCloseLinkPopover: () => setShowLinkPopover(false),
                      onPrintClick: () => setShowPrintDialog(true),
                      onToggleRichEdit: () => setIsRichEditEnabled((prev) => !prev)
                    }
                  ),
                  isRichEditEnabled ? (0, import_jsx_runtime.jsx)(
                    "div",
                    {
                      ref: richEditorRef,
                      contentEditable: true,
                      suppressContentEditableWarning: true,
                      onInput: handleRichInput,
                      onBlur: handleBlur,
                      onKeyDown: handleKeyDownRich,
                      onClick: handleEditorClick,
                      className: "w-full overflow-y-auto rounded-md border border-border/50 bg-muted/60 px-2.5 py-1.5 text-xs text-foreground focus:border-primary/50 focus:outline-none focus:ring-1 focus:ring-primary/30 transition-colors whitespace-pre-wrap",
                      style: { maxHeight: EXPANDED_MAX_HEIGHT - 8, minHeight: 72 }
                    }
                  ) : (0, import_jsx_runtime.jsx)(
                    "textarea",
                    {
                      ref: textareaRef,
                      name: "slide-notes",
                      value: draft,
                      onChange: handlePlainChange,
                      onBlur: handleBlur,
                      onKeyDown: handleKeyDownPlain,
                      placeholder: t("pptx.notes.clickToAddNotes"),
                      rows: 4,
                      className: "w-full resize-none rounded-md border border-border/50 bg-muted/60 px-2.5 py-1.5 text-xs text-foreground placeholder:text-muted-foreground focus:border-primary/50 focus:outline-none focus:ring-1 focus:ring-primary/30 transition-colors",
                      style: { maxHeight: EXPANDED_MAX_HEIGHT - 8 }
                    }
                  )
                ] }) : (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: "w-full rounded-md border border-border/30 bg-muted/40 px-2.5 py-1.5 text-xs text-muted-foreground overflow-y-auto whitespace-pre-wrap",
                    style: { maxHeight: EXPANDED_MAX_HEIGHT - 32, minHeight: 60 },
                    children: hasNotes ? renderRichNotesSegments(draftSegments) : (0, import_jsx_runtime.jsx)("span", { className: "italic text-muted-foreground", children: t("pptx.notes.noNotes") })
                  }
                )
              ]
            }
          ),
          showPrintDialog && allSlides && (0, import_jsx_runtime.jsx)(
            NotesPrintDialog,
            {
              slides: allSlides,
              onClose: () => setShowPrintDialog(false),
              notesStyle
            }
          )
        ]
      }
    )
  ] });
}
var DB_NAME = "pptx-viewer-autosave";
var DB_VERSION = 1;
var STORE_NAME = "recoveryVersions";
function openAutosaveDb() {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);
    req.onupgradeneeded = () => {
      const db = req.result;
      if (!db.objectStoreNames.contains(STORE_NAME)) {
        db.createObjectStore(STORE_NAME, { keyPath: "key" });
      }
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}
async function getVersions(filePath) {
  try {
    const db = await openAutosaveDb();
    return new Promise((resolve) => {
      const tx = db.transaction(STORE_NAME, "readonly");
      const store = tx.objectStore(STORE_NAME);
      const req = store.get(filePath);
      req.onsuccess = () => {
        db.close();
        const result = req.result;
        resolve(result ? [result] : []);
      };
      req.onerror = () => {
        db.close();
        resolve([]);
      };
    });
  } catch {
    return [];
  }
}
async function deleteVersion(filePath) {
  try {
    const db = await openAutosaveDb();
    return new Promise((resolve) => {
      const tx = db.transaction(STORE_NAME, "readwrite");
      const store = tx.objectStore(STORE_NAME);
      store.delete(filePath);
      tx.oncomplete = () => {
        db.close();
        resolve();
      };
      tx.onerror = () => {
        db.close();
        resolve();
      };
    });
  } catch {
  }
}
function formatFileSize(bytes) {
  if (bytes < 1024) {
    return `${bytes} B`;
  }
  if (bytes < 1024 * 1024) {
    return `${(bytes / 1024).toFixed(1)} KB`;
  }
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
function VersionHistoryPanel({
  isOpen,
  filePath,
  onClose,
  onRestore
}) {
  const { t } = useTranslation();
  const [versions, setVersions] = (0, import_react2.useState)([]);
  const [loading, setLoading] = (0, import_react2.useState)(false);
  const [restoringKey, setRestoringKey] = (0, import_react2.useState)(null);
  const [deletingKey, setDeletingKey] = (0, import_react2.useState)(null);
  const fetchVersions = (0, import_react2.useCallback)(async () => {
    if (!filePath) {
      return;
    }
    setLoading(true);
    try {
      const result = await getVersions(filePath);
      setVersions(result);
    } catch {
      setVersions([]);
    } finally {
      setLoading(false);
    }
  }, [filePath]);
  (0, import_react2.useEffect)(() => {
    if (isOpen) {
      void fetchVersions();
    }
  }, [isOpen, fetchVersions]);
  const handleRestore = (0, import_react2.useCallback)(
    async (version) => {
      setRestoringKey(version.key);
      try {
        if (version.data) {
          onRestore(version.data);
          onClose();
        }
      } finally {
        setRestoringKey(null);
      }
    },
    // `onRestore`/`onClose` ARE read above (inside the `try`), but the analyzer
    // doesn't see through a try/finally block and flags them as unused;
    // verified as a false positive with a minimal repro (see useLayoutSwitching.ts).
    // oxlint-disable-next-line react/memo-dependencies -- see comment above
    [onRestore, onClose]
  );
  const handleDelete = (0, import_react2.useCallback)(
    async (version) => {
      setDeletingKey(version.key);
      try {
        await deleteVersion(version.key);
        await fetchVersions();
      } finally {
        setDeletingKey(null);
      }
    },
    // `fetchVersions` IS read above (inside the `try`), but the analyzer
    // doesn't see through an `await` call wrapped in try/finally and flags it
    // as unused; verified as a false positive with a minimal repro (see
    // useLayoutSwitching.ts).
    // oxlint-disable-next-line react/memo-dependencies -- see comment above
    [fetchVersions]
  );
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "absolute inset-y-0 right-0 w-80 bg-background border-l border-border z-50 flex flex-col shadow-xl", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-3 py-2 border-b border-border", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-sm font-medium text-foreground", children: [
        (0, import_jsx_runtime.jsx)(LuClock, { className: "w-4 h-4" }),
        t("pptx.versionHistory.title")
      ] }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          onClick: onClose,
          className: "p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground",
          children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 overflow-y-auto", children: [
      loading && (0, import_jsx_runtime.jsx)("div", { className: "px-3 py-8 text-center text-xs text-muted-foreground", children: t("pptx.common.loading") }),
      !loading && versions.length === 0 && (0, import_jsx_runtime.jsx)("div", { className: "px-3 py-8 text-center text-xs text-muted-foreground", children: t("pptx.versionHistory.noVersions") }),
      !loading && versions.map((version) => (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "px-3 py-2.5 border-b border-border hover:bg-muted/50 group",
          children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
              (0, import_jsx_runtime.jsx)("div", { className: "text-xs text-foreground", children: formatVersionTimestamp(version.timestamp) }),
              (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground", children: formatRelativeTime(version.timestamp) })
            ] }),
            (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mt-0.5", children: formatFileSize(version.size) }),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 mt-1.5 opacity-0 group-hover:opacity-100 transition-opacity", children: [
              (0, import_jsx_runtime.jsxs)(
                "button",
                {
                  onClick: () => void handleRestore(version),
                  disabled: restoringKey === version.key,
                  className: "inline-flex items-center gap-1 px-2 py-1 rounded text-[10px] bg-primary/20 text-primary hover:bg-primary/30 disabled:opacity-40",
                  children: [
                    (0, import_jsx_runtime.jsx)(LuDownload, { className: "w-3 h-3" }),
                    restoringKey === version.key ? t("pptx.common.loading") : t("pptx.versionHistory.restore")
                  ]
                }
              ),
              (0, import_jsx_runtime.jsxs)(
                "button",
                {
                  onClick: () => void handleDelete(version),
                  disabled: deletingKey === version.key,
                  className: "inline-flex items-center gap-1 px-2 py-1 rounded text-[10px] bg-red-600/20 text-red-400 hover:bg-red-600/30 disabled:opacity-40",
                  children: [
                    (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-3 h-3" }),
                    t("pptx.common.delete")
                  ]
                }
              )
            ] })
          ]
        },
        version.key
      ))
    ] })
  ] });
}
function ChangeKindIcon({ kind }) {
  switch (kind) {
    case "added":
      return (0, import_jsx_runtime.jsx)(LuPlus, { className: "w-3 h-3 text-green-400" });
    case "removed":
      return (0, import_jsx_runtime.jsx)(LuMinus, { className: "w-3 h-3 text-red-400" });
    case "moved":
      return (0, import_jsx_runtime.jsx)(LuMove, { className: "w-3 h-3 text-primary" });
    case "resized":
      return (0, import_jsx_runtime.jsx)(LuMove, { className: "w-3 h-3 text-amber-400" });
    case "textChanged":
      return (0, import_jsx_runtime.jsx)(LuType, { className: "w-3 h-3 text-purple-400" });
  }
}
function SlideDiffRow({
  diff,
  diffIndex,
  canvasSize,
  accepted,
  rejected,
  onAccept,
  onReject
}) {
  const { t } = useTranslation();
  const [expanded, setExpanded] = (0, import_react2.useState)(diff.status === "changed");
  const statusLabel = (() => {
    switch (diff.status) {
      case "added":
        return t("pptx.compare.statusAdded");
      case "removed":
        return t("pptx.compare.statusRemoved");
      case "changed":
        return t("pptx.compare.statusChanged");
      case "unchanged":
        return t("pptx.compare.statusUnchanged");
    }
  })();
  const statusColor = (() => {
    switch (diff.status) {
      case "added":
        return "text-green-400 bg-green-900/30";
      case "removed":
        return "text-red-400 bg-red-900/30";
      case "changed":
        return "text-amber-400 bg-amber-900/30";
      case "unchanged":
        return "text-muted-foreground bg-muted/30";
    }
  })();
  if (diff.status === "unchanged") {
    return null;
  }
  const isResolved = accepted || rejected;
  const slideNumber = diff.baseIndex >= 0 ? diff.baseIndex + 1 : diff.compareIndex + 1;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: cn(
        "rounded-lg border transition-colors",
        isResolved ? "border-border/60 bg-card/40 opacity-60" : "border-border bg-background/70"
      ),
      children: [
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            className: "flex items-center gap-2 w-full px-3 py-2 text-left",
            onClick: () => setExpanded((p) => !p),
            children: [
              expanded ? (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3.5 h-3.5 text-muted-foreground flex-shrink-0" }) : (0, import_jsx_runtime.jsx)(LuChevronRight, { className: "w-3.5 h-3.5 text-muted-foreground flex-shrink-0" }),
              (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-foreground", children: t("pptx.compare.slideNumber", { number: slideNumber }) }),
              (0, import_jsx_runtime.jsx)("span", { className: cn("rounded-full px-2 py-0.5 text-[10px] font-medium", statusColor), children: statusLabel }),
              diff.changes.length > 0 && (0, import_jsx_runtime.jsxs)("span", { className: "text-[10px] text-muted-foreground", children: [
                diff.changes.length,
                " ",
                diff.changes.length === 1 ? "change" : "changes"
              ] }),
              (0, import_jsx_runtime.jsx)("span", { className: "flex-1" }),
              isResolved && (0, import_jsx_runtime.jsx)(
                "span",
                {
                  className: cn(
                    "text-[10px] font-medium",
                    accepted ? "text-green-400" : "text-muted-foreground"
                  ),
                  children: accepted ? t("pptx.compare.accepted") : t("pptx.compare.rejected")
                }
              )
            ]
          }
        ),
        expanded && (0, import_jsx_runtime.jsxs)("div", { className: "px-3 pb-3 space-y-2", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-2", children: [
            diff.baseSlide && (0, import_jsx_runtime.jsxs)("div", { className: "flex-1", children: [
              (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.compare.current") }),
              (0, import_jsx_runtime.jsx)("div", { className: "rounded border border-border overflow-hidden", children: (0, import_jsx_runtime.jsx)(
                SlideThumbnail,
                {
                  slide: diff.baseSlide,
                  templateElements: [],
                  canvasSize
                }
              ) })
            ] }),
            diff.compareSlide && (0, import_jsx_runtime.jsxs)("div", { className: "flex-1", children: [
              (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.compare.incoming") }),
              (0, import_jsx_runtime.jsx)(
                "div",
                {
                  className: cn(
                    "rounded border overflow-hidden",
                    diff.status === "added" ? "border-green-700/60" : diff.status === "changed" ? "border-amber-700/60" : "border-border"
                  ),
                  children: (0, import_jsx_runtime.jsx)(
                    SlideThumbnail,
                    {
                      slide: diff.compareSlide,
                      templateElements: [],
                      canvasSize
                    }
                  )
                }
              )
            ] })
          ] }),
          diff.changes.length > 0 && (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: diff.changes.map((change, ci) => (0, import_jsx_runtime.jsxs)(
            "div",
            {
              className: "flex items-start gap-2 rounded bg-muted/60 px-2 py-1.5 text-[11px]",
              children: [
                (0, import_jsx_runtime.jsx)(ChangeKindIcon, { kind: change.kind }),
                (0, import_jsx_runtime.jsx)("span", { className: "text-foreground", children: change.description })
              ]
            },
            `${change.elementId}-${change.kind}-${ci}`
          )) }),
          !isResolved && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 pt-1", children: [
            (0, import_jsx_runtime.jsxs)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-green-700/80 px-2.5 py-1 text-[11px] text-green-50 hover:bg-green-600 transition-colors",
                onClick: () => onAccept(diffIndex),
                children: [
                  (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3 h-3" }),
                  t("pptx.compare.accept")
                ]
              }
            ),
            (0, import_jsx_runtime.jsxs)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-accent px-2.5 py-1 text-[11px] text-foreground hover:bg-accent/80 transition-colors",
                onClick: () => onReject(diffIndex),
                children: [
                  (0, import_jsx_runtime.jsx)(LuX, { className: "w-3 h-3" }),
                  t("pptx.compare.reject")
                ]
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function ComparePanel({
  isOpen,
  compareResult,
  canvasSize,
  onClose,
  onAcceptSlide,
  onRejectSlide,
  onAcceptAll
}) {
  const { t } = useTranslation();
  const [accepted, setAccepted] = (0, import_react2.useState)({});
  const [rejected, setRejected] = (0, import_react2.useState)({});
  const handleAccept = (0, import_react2.useCallback)(
    (index) => {
      setAccepted((p) => ({ ...p, [index]: true }));
      setRejected((p) => {
        const next = { ...p };
        delete next[index];
        return next;
      });
      onAcceptSlide(index);
    },
    [onAcceptSlide]
  );
  const handleReject = (0, import_react2.useCallback)(
    (index) => {
      setRejected((p) => ({ ...p, [index]: true }));
      setAccepted((p) => {
        const next = { ...p };
        delete next[index];
        return next;
      });
      onRejectSlide(index);
    },
    [onRejectSlide]
  );
  const handleAcceptAll = (0, import_react2.useCallback)(() => {
    if (!compareResult) {
      return;
    }
    const acc = {};
    compareResult.diffs.forEach((d, i) => {
      if (d.status !== "unchanged") {
        acc[i] = true;
      }
    });
    setAccepted(acc);
    setRejected({});
    onAcceptAll();
  }, [compareResult, onAcceptAll]);
  if (!isOpen || !compareResult) {
    return null;
  }
  const nonTrivialDiffs = compareResult.diffs.filter((d) => d.status !== "unchanged");
  return (0, import_jsx_runtime.jsxs)("div", { className: "fixed inset-y-0 right-0 z-50 flex flex-col w-[440px] max-w-full border-l border-border bg-popover backdrop-blur-lg shadow-2xl", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-4 py-3 border-b border-border", children: [
      (0, import_jsx_runtime.jsxs)("div", { children: [
        (0, import_jsx_runtime.jsx)("h3", { className: "text-sm font-medium text-foreground", children: t("pptx.compare.title") }),
        (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground mt-0.5", children: t("pptx.compare.summary", {
          added: compareResult.addedCount,
          removed: compareResult.removedCount,
          changed: compareResult.changedCount
        }) })
      ] }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "rounded p-1.5 text-muted-foreground hover:bg-muted hover:text-foreground transition-colors",
          onClick: onClose,
          title: t("pptx.compare.close"),
          children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
        }
      )
    ] }),
    nonTrivialDiffs.length > 0 && (0, import_jsx_runtime.jsx)("div", { className: "px-4 py-2 border-b border-border/60", children: (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: "inline-flex items-center gap-1.5 rounded bg-green-700/80 px-3 py-1.5 text-xs text-green-50 hover:bg-green-600 transition-colors",
        onClick: handleAcceptAll,
        children: [
          (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3.5 h-3.5" }),
          t("pptx.compare.acceptAll")
        ]
      }
    ) }),
    (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-y-auto px-3 py-3 space-y-2", children: nonTrivialDiffs.length === 0 ? (0, import_jsx_runtime.jsx)("div", { className: "text-center py-8 text-xs text-muted-foreground", children: t("pptx.compare.noDifferences") }) : compareResult.diffs.map((diff, i) => (0, import_jsx_runtime.jsx)(
      SlideDiffRow,
      {
        diff,
        diffIndex: i,
        canvasSize,
        accepted: Boolean(accepted[i]),
        rejected: Boolean(rejected[i]),
        onAccept: handleAccept,
        onReject: handleReject
      },
      `diff-${i}-${diff.status}`
    )) })
  ] });
}
function getPasswordStrength(password) {
  if (!password) {
    return 0;
  }
  let score = 0;
  if (password.length >= 8) {
    score++;
  }
  if (password.length >= 12) {
    score++;
  }
  if (/[A-Z]/u.test(password) && /[a-z]/u.test(password)) {
    score++;
  }
  if (/\d/u.test(password)) {
    score++;
  }
  if (/[^A-Za-z0-9]/u.test(password)) {
    score++;
  }
  return Math.min(score, 4);
}
var STRENGTH_COLORS = [
  "bg-red-500",
  "bg-orange-500",
  "bg-yellow-500",
  "bg-lime-500",
  "bg-green-500"
];
function PasswordProtectionDialog({
  isOpen,
  isCurrentlyProtected,
  onClose,
  onSetPassword,
  onRemovePassword
}) {
  const { t } = useTranslation();
  const [password, setPassword] = (0, import_react2.useState)("");
  const [confirmPassword, setConfirmPassword] = (0, import_react2.useState)("");
  const [showPassword, setShowPassword] = (0, import_react2.useState)(false);
  const [error, setError] = (0, import_react2.useState)("");
  const dialogRef = (0, import_react2.useRef)(null);
  const passwordRef = (0, import_react2.useRef)(null);
  const strength = (0, import_react2.useMemo)(() => getPasswordStrength(password), [password]);
  const strengthLabel = (0, import_react2.useMemo)(() => {
    const labels = [
      t("pptx.security.strengthVeryWeak"),
      t("pptx.security.strengthWeak"),
      t("pptx.security.strengthFair"),
      t("pptx.security.strengthStrong"),
      t("pptx.security.strengthVeryStrong")
    ];
    return password ? labels[strength] : "";
  }, [strength, password, t]);
  const handleSubmit = (0, import_react2.useCallback)(() => {
    setError("");
    if (!password) {
      setError(t("pptx.security.errorPasswordRequired"));
      return;
    }
    if (password !== confirmPassword) {
      setError(t("pptx.security.errorPasswordMismatch"));
      return;
    }
    if (password.length < 4) {
      setError(t("pptx.security.errorPasswordTooShort"));
      return;
    }
    onSetPassword(password);
    setPassword("");
    setConfirmPassword("");
    onClose();
  }, [password, confirmPassword, onSetPassword, onClose, t]);
  const handleRemove = (0, import_react2.useCallback)(() => {
    onRemovePassword();
    setPassword("");
    setConfirmPassword("");
    onClose();
  }, [onRemovePassword, onClose]);
  const handleClose = (0, import_react2.useCallback)(() => {
    setPassword("");
    setConfirmPassword("");
    setError("");
    onClose();
  }, [onClose]);
  useModalFocus(isOpen, dialogRef, handleClose, passwordRef);
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: handleClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            ref: dialogRef,
            role: "dialog",
            "aria-modal": "true",
            "aria-labelledby": "pptx-password-title",
            tabIndex: -1,
            className: "pointer-events-auto w-[420px] overscroll-contain rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
                (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
                  (0, import_jsx_runtime.jsx)(LuLock, { className: "w-5 h-5 text-primary", "aria-hidden": "true" }),
                  (0, import_jsx_runtime.jsx)("h2", { id: "pptx-password-title", className: "text-sm font-semibold text-foreground", children: t("pptx.security.protectPresentation") })
                ] }),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: handleClose,
                    className: "p-1 rounded hover:bg-accent transition-colors",
                    "aria-label": t("pptx.common.close"),
                    children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4 text-muted-foreground", "aria-hidden": "true" })
                  }
                )
              ] }),
              (0, import_jsx_runtime.jsxs)("div", { className: "px-5 py-4 space-y-4", children: [
                isCurrentlyProtected && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 rounded-lg bg-green-900/30 border border-green-700/40 px-3 py-2", children: [
                  (0, import_jsx_runtime.jsx)(LuShieldCheck, { className: "w-4 h-4 text-green-400 shrink-0", "aria-hidden": "true" }),
                  (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-green-300", children: t("pptx.security.currentlyProtected") })
                ] }),
                (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground", children: t("pptx.security.description") }),
                (0, import_jsx_runtime.jsxs)("div", { children: [
                  (0, import_jsx_runtime.jsx)("label", { htmlFor: "pptx-password", className: "block text-xs text-foreground mb-1", children: t("pptx.security.password") }),
                  (0, import_jsx_runtime.jsxs)("div", { className: "relative", children: [
                    (0, import_jsx_runtime.jsx)(
                      "input",
                      {
                        ref: passwordRef,
                        id: "pptx-password",
                        name: "presentation-password",
                        autoComplete: "new-password",
                        "aria-invalid": Boolean(error),
                        "aria-describedby": error ? "pptx-password-error" : "pptx-password-strength",
                        type: showPassword ? "text" : "password",
                        className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                        placeholder: t("pptx.security.passwordPlaceholder"),
                        value: password,
                        onChange: (e) => {
                          setPassword(e.target.value);
                          setError("");
                        }
                      }
                    ),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: () => setShowPassword(!showPassword),
                        className: "absolute right-2 top-1/2 -translate-y-1/2 p-1 text-muted-foreground hover:text-foreground",
                        "aria-label": showPassword ? t("pptx.security.hidePassword") : t("pptx.security.showPassword"),
                        children: showPassword ? (0, import_jsx_runtime.jsx)(LuEyeOff, { className: "w-4 h-4", "aria-hidden": "true" }) : (0, import_jsx_runtime.jsx)(LuEye, { className: "w-4 h-4", "aria-hidden": "true" })
                      }
                    )
                  ] })
                ] }),
                password && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
                  (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: Array.from({ length: 5 }).map((_, i) => (0, import_jsx_runtime.jsx)(
                    "div",
                    {
                      className: `h-1 flex-1 rounded-full transition-colors ${i <= strength ? STRENGTH_COLORS[strength] : "bg-accent"}`
                    },
                    `str-${i}`
                  )) }),
                  (0, import_jsx_runtime.jsx)(
                    "p",
                    {
                      id: "pptx-password-strength",
                      className: "text-[11px] text-muted-foreground",
                      role: "status",
                      "aria-live": "polite",
                      children: strengthLabel
                    }
                  )
                ] }),
                (0, import_jsx_runtime.jsxs)("div", { children: [
                  (0, import_jsx_runtime.jsx)("label", { htmlFor: "pptx-password-confirm", className: "block text-xs text-foreground mb-1", children: t("pptx.security.confirmPassword") }),
                  (0, import_jsx_runtime.jsx)(
                    "input",
                    {
                      id: "pptx-password-confirm",
                      name: "presentation-password-confirmation",
                      autoComplete: "new-password",
                      "aria-invalid": Boolean(error),
                      "aria-describedby": error ? "pptx-password-error" : void 0,
                      type: showPassword ? "text" : "password",
                      className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                      placeholder: t("pptx.security.confirmPasswordPlaceholder"),
                      value: confirmPassword,
                      onChange: (e) => {
                        setConfirmPassword(e.target.value);
                        setError("");
                      }
                    }
                  )
                ] }),
                error && (0, import_jsx_runtime.jsx)("p", { id: "pptx-password-error", className: "text-xs text-red-400", role: "alert", children: error })
              ] }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-3 border-t border-border/60", children: [
                (0, import_jsx_runtime.jsx)("div", { children: isCurrentlyProtected && (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: handleRemove,
                    className: "text-xs text-red-400 hover:text-red-300 transition-colors",
                    children: t("pptx.security.removePassword")
                  }
                ) }),
                (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-2", children: [
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: handleClose,
                      className: "px-3 py-1.5 text-xs rounded-lg border border-border text-foreground hover:bg-muted transition-colors",
                      children: t("pptx.common.cancel")
                    }
                  ),
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: handleSubmit,
                      className: "px-3 py-1.5 text-xs rounded-lg bg-primary text-white hover:bg-primary/80 transition-colors",
                      children: isCurrentlyProtected ? t("pptx.security.updatePassword") : t("pptx.security.setPassword")
                    }
                  )
                ] })
              ] })
            ]
          }
        )
      }
    )
  ] });
}
var CUSTOM_PROPERTY_TYPES = [
  { value: "lpwstr", labelKey: "pptx.documentProperties.custom.typeText" },
  { value: "i4", labelKey: "pptx.documentProperties.custom.typeNumber" },
  { value: "filetime", labelKey: "pptx.documentProperties.custom.typeDate" },
  { value: "bool", labelKey: "pptx.documentProperties.custom.typeYesNo" }
];
function DocumentPropertiesCustomTab({
  customProperties,
  onUpdate
}) {
  const { t } = useTranslation();
  const handleAdd = (0, import_react2.useCallback)(() => {
    onUpdate([...customProperties, { name: "", value: "", type: "lpwstr" }]);
  }, [customProperties, onUpdate]);
  const handleDelete = (0, import_react2.useCallback)(
    (index) => {
      onUpdate(customProperties.filter((_, i) => i !== index));
    },
    [customProperties, onUpdate]
  );
  const handleChangeName = (0, import_react2.useCallback)(
    (index, name) => {
      onUpdate(customProperties.map((prop, i) => i === index ? { ...prop, name } : prop));
    },
    [customProperties, onUpdate]
  );
  const handleChangeValue = (0, import_react2.useCallback)(
    (index, value) => {
      onUpdate(customProperties.map((prop, i) => i === index ? { ...prop, value } : prop));
    },
    [customProperties, onUpdate]
  );
  const handleChangeType = (0, import_react2.useCallback)(
    (index, type) => {
      onUpdate(customProperties.map((prop, i) => i === index ? { ...prop, type } : prop));
    },
    [customProperties, onUpdate]
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-3", children: [
    (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground", children: t("pptx.documentProperties.custom.description") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[1fr_1fr_100px_32px] gap-1 text-[11px] font-medium text-muted-foreground px-1", children: [
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.documentProperties.custom.name") }),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.documentProperties.custom.value") }),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.documentProperties.custom.type") }),
      (0, import_jsx_runtime.jsx)("span", {})
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "max-h-[240px] overflow-y-auto space-y-1", children: customProperties.map((prop, index) => (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: "grid grid-cols-[1fr_1fr_100px_32px] gap-1 items-center",
        children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "text",
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              placeholder: t("pptx.documentProperties.custom.namePlaceholder"),
              value: prop.name,
              onChange: (e) => handleChangeName(index, e.target.value)
            }
          ),
          prop.type === "bool" ? (0, import_jsx_runtime.jsxs)(
            "select",
            {
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              value: prop.value,
              onChange: (e) => handleChangeValue(index, e.target.value),
              children: [
                (0, import_jsx_runtime.jsx)("option", { value: "true", children: t("pptx.documentProperties.custom.yes") }),
                (0, import_jsx_runtime.jsx)("option", { value: "false", children: t("pptx.documentProperties.custom.no") })
              ]
            }
          ) : (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: prop.type === "i4" ? "number" : "text",
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              placeholder: t("pptx.documentProperties.custom.valuePlaceholder"),
              value: prop.value,
              onChange: (e) => handleChangeValue(index, e.target.value)
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              className: "w-full rounded border border-border bg-muted px-2 py-1 text-xs text-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
              value: prop.type,
              onChange: (e) => handleChangeType(index, e.target.value),
              children: CUSTOM_PROPERTY_TYPES.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: () => handleDelete(index),
              className: "p-1 rounded hover:bg-red-500/20 text-muted-foreground hover:text-red-400 transition-colors",
              "aria-label": t("pptx.documentProperties.custom.deleteProperty"),
              children: (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-3.5 h-3.5" })
            }
          )
        ]
      },
      `custom-prop-${index}`
    )) }),
    customProperties.length === 0 && (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground/60 text-center py-4", children: t("pptx.documentProperties.custom.empty") }),
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: handleAdd,
        className: "inline-flex items-center gap-1.5 px-2.5 py-1.5 text-xs rounded-lg border border-border text-foreground hover:bg-muted transition-colors",
        children: [
          (0, import_jsx_runtime.jsx)(LuPlus, { className: "w-3.5 h-3.5" }),
          t("pptx.documentProperties.custom.addProperty")
        ]
      }
    )
  ] });
}
var STATISTICS_FIELDS = [
  {
    key: "application",
    labelKey: "pptx.documentProperties.statistics.application",
    editable: false
  },
  {
    key: "appVersion",
    labelKey: "pptx.documentProperties.statistics.appVersion",
    editable: false
  },
  {
    key: "presentationFormat",
    labelKey: "pptx.documentProperties.statistics.presentationFormat",
    editable: false
  },
  {
    key: "slides",
    labelKey: "pptx.documentProperties.statistics.slides",
    editable: false
  },
  {
    key: "hiddenSlides",
    labelKey: "pptx.documentProperties.statistics.hiddenSlides",
    editable: false
  },
  {
    key: "notes",
    labelKey: "pptx.documentProperties.statistics.notes",
    editable: false
  },
  {
    key: "totalTime",
    labelKey: "pptx.documentProperties.statistics.totalTime",
    editable: false
  },
  {
    key: "words",
    labelKey: "pptx.documentProperties.statistics.words",
    editable: false
  },
  {
    key: "paragraphs",
    labelKey: "pptx.documentProperties.statistics.paragraphs",
    editable: false
  },
  {
    key: "template",
    labelKey: "pptx.documentProperties.statistics.template",
    editable: false
  },
  {
    key: "company",
    labelKey: "pptx.documentProperties.summary.company",
    editable: true
  },
  {
    key: "manager",
    labelKey: "pptx.documentProperties.summary.manager",
    editable: true
  }
];
function formatTotalTime(minutes, t) {
  if (minutes === void 0 || minutes === null) {
    return t("pptx.documentProperties.statistics.notAvailable");
  }
  const hours = Math.floor(minutes / 60);
  const remainingMinutes = minutes % 60;
  return t("pptx.documentProperties.statistics.hoursMinutes", {
    hours,
    minutes: remainingMinutes
  });
}
function formatFieldValue(key, value, t) {
  if (key === "totalTime") {
    return formatTotalTime(value, t);
  }
  if (value === void 0 || value === null) {
    return t("pptx.documentProperties.statistics.notAvailable");
  }
  if (typeof value === "boolean") {
    return t(value ? "pptx.documentProperties.custom.yes" : "pptx.documentProperties.custom.no");
  }
  return String(value);
}
function DocumentPropertiesStatisticsTab({
  appProperties,
  onUpdateAppProperties,
  canEdit
}) {
  const { t } = useTranslation();
  const handleEditableChange = (0, import_react2.useCallback)(
    (key, value) => {
      onUpdateAppProperties({ [key]: value });
    },
    [onUpdateAppProperties]
  );
  return (0, import_jsx_runtime.jsx)("div", { className: "space-y-3", children: STATISTICS_FIELDS.map(({ key, labelKey, editable }) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3", children: [
    (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-foreground w-[140px] shrink-0", children: t(labelKey) }),
    editable && canEdit ? (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "text",
        className: "flex-1 rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
        value: String(appProperties[key] ?? ""),
        onChange: (e) => handleEditableChange(key, e.target.value)
      }
    ) : (0, import_jsx_runtime.jsx)("span", { className: "flex-1 px-3 py-2 text-sm text-muted-foreground", children: formatFieldValue(key, appProperties[key], t) })
  ] }, key)) });
}
var SUMMARY_FIELDS = [
  { key: "title", labelKey: "pptx.properties.titleLabel" },
  { key: "subject", labelKey: "pptx.properties.subject" },
  { key: "creator", labelKey: "pptx.properties.author" },
  { key: "keywords", labelKey: "pptx.properties.keywords" },
  {
    key: "description",
    labelKey: "pptx.documentProperties.summary.description",
    multiline: true
  },
  { key: "category", labelKey: "pptx.documentProperties.summary.category" }
];
function DocumentPropertiesDialog({
  isOpen,
  coreProperties,
  customProperties,
  appProperties,
  onClose,
  onSave
}) {
  const { t } = useTranslation();
  const [activeTab, setActiveTab] = (0, import_react2.useState)("summary");
  const [draftCore, setDraftCore] = (0, import_react2.useState)({});
  const [draftCustom, setDraftCustom] = (0, import_react2.useState)([]);
  const [draftApp, setDraftApp] = (0, import_react2.useState)({});
  const [prevOpen, setPrevOpen] = (0, import_react2.useState)(false);
  if (isOpen && !prevOpen) {
    setDraftCore({ ...coreProperties });
    setDraftCustom(customProperties.map((p) => ({ ...p })));
    setDraftApp({
      company: appProperties == null ? void 0 : appProperties.company,
      manager: appProperties == null ? void 0 : appProperties.manager
    });
    setActiveTab("summary");
  }
  if (isOpen !== prevOpen) {
    setPrevOpen(isOpen);
  }
  const handleCoreFieldChange = (0, import_react2.useCallback)((key, value) => {
    setDraftCore((prev) => ({ ...prev, [key]: value }));
  }, []);
  const isDirty = (0, import_react2.useMemo)(() => {
    const coreChanged = SUMMARY_FIELDS.some(
      ({ key }) => (draftCore[key] ?? "") !== (coreProperties[key] ?? "")
    );
    if (coreChanged) {
      return true;
    }
    const appCompanyChanged = (draftApp.company ?? "") !== ((appProperties == null ? void 0 : appProperties.company) ?? "");
    const appManagerChanged = (draftApp.manager ?? "") !== ((appProperties == null ? void 0 : appProperties.manager) ?? "");
    if (appCompanyChanged || appManagerChanged) {
      return true;
    }
    if (draftCustom.length !== customProperties.length) {
      return true;
    }
    return draftCustom.some(
      (p, i) => {
        var _a, _b2, _c;
        return p.name !== ((_a = customProperties[i]) == null ? void 0 : _a.name) || p.value !== ((_b2 = customProperties[i]) == null ? void 0 : _b2.value) || p.type !== ((_c = customProperties[i]) == null ? void 0 : _c.type);
      }
    );
  }, [draftCore, draftCustom, draftApp, coreProperties, customProperties, appProperties]);
  const handleUpdateDraftApp = (0, import_react2.useCallback)((updates) => {
    setDraftApp((prev) => ({ ...prev, ...updates }));
  }, []);
  const handleSave = (0, import_react2.useCallback)(() => {
    const appChanged = (draftApp.company ?? "") !== ((appProperties == null ? void 0 : appProperties.company) ?? "") || (draftApp.manager ?? "") !== ((appProperties == null ? void 0 : appProperties.manager) ?? "");
    onSave(draftCore, draftCustom, appChanged ? draftApp : void 0);
    onClose();
  }, [draftCore, draftCustom, draftApp, appProperties, onSave, onClose]);
  const handleClose = (0, import_react2.useCallback)(() => {
    onClose();
  }, [onClose]);
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(handleClose);
  if (!isOpen) {
    return null;
  }
  const TABS2 = [
    { id: "summary", labelKey: "pptx.documentProperties.tabs.general" },
    { id: "custom", labelKey: "pptx.documentProperties.tabs.custom" },
    { id: "statistics", labelKey: "pptx.documentProperties.tabs.statistics" }
  ];
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: handleClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            style: panelStyle,
            className: "pointer-events-auto w-[520px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-5 py-4 border-b border-border/60 touch-none",
                  children: [
                    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
                      (0, import_jsx_runtime.jsx)(LuFileText, { className: "w-5 h-5 text-primary" }),
                      (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.documentProperties.dialogTitle") })
                    ] }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: handleClose,
                        className: "p-1 rounded hover:bg-accent transition-colors",
                        "aria-label": t("pptx.common.close"),
                        children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4 text-muted-foreground" })
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsx)("div", { className: "flex border-b border-border/60", children: TABS2.map((tab) => (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  onClick: () => setActiveTab(tab.id),
                  className: `px-4 py-2 text-xs font-medium transition-colors ${activeTab === tab.id ? "text-primary border-b-2 border-primary" : "text-muted-foreground hover:text-foreground"}`,
                  children: t(tab.labelKey)
                },
                tab.id
              )) }),
              (0, import_jsx_runtime.jsxs)("div", { className: "px-5 py-4 min-h-[280px]", children: [
                activeTab === "summary" && (0, import_jsx_runtime.jsx)("div", { className: "space-y-3", children: SUMMARY_FIELDS.map(({ key, labelKey, multiline }) => (0, import_jsx_runtime.jsxs)("div", { children: [
                  (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-foreground mb-1", children: t(labelKey) }),
                  multiline ? (0, import_jsx_runtime.jsx)(
                    "textarea",
                    {
                      className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none resize-none",
                      rows: 3,
                      value: draftCore[key] ?? "",
                      onChange: (e) => handleCoreFieldChange(key, e.target.value)
                    }
                  ) : (0, import_jsx_runtime.jsx)(
                    "input",
                    {
                      type: "text",
                      className: "w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm text-foreground placeholder-muted-foreground focus:border-primary focus:ring-1 focus:ring-primary outline-none",
                      value: draftCore[key] ?? "",
                      onChange: (e) => handleCoreFieldChange(key, e.target.value)
                    }
                  )
                ] }, key)) }),
                activeTab === "custom" && (0, import_jsx_runtime.jsx)(
                  DocumentPropertiesCustomTab,
                  {
                    customProperties: draftCustom,
                    onUpdate: setDraftCustom
                  }
                ),
                activeTab === "statistics" && (0, import_jsx_runtime.jsx)(
                  DocumentPropertiesStatisticsTab,
                  {
                    appProperties: {
                      ...appProperties,
                      company: draftApp.company,
                      manager: draftApp.manager
                    },
                    onUpdateAppProperties: handleUpdateDraftApp,
                    canEdit: true
                  }
                )
              ] }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 px-5 py-3 border-t border-border/60", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: handleClose,
                    className: "px-3 py-1.5 text-xs rounded-lg border border-border text-foreground hover:bg-muted transition-colors",
                    children: t("pptx.common.cancel")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: handleSave,
                    disabled: !isDirty,
                    className: "px-3 py-1.5 text-xs rounded-lg bg-primary text-white hover:bg-primary/80 transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
                    children: t("pptx.common.save")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function FontEmbeddingPanel({
  isOpen,
  embedFontsEnabled,
  usedFontFamilies,
  embeddedFonts,
  canEmbedFonts = true,
  embedUnavailableKey,
  onClose,
  onToggleEmbedFonts
}) {
  const { t } = useTranslation();
  const [availableFamilies, setAvailableFamilies] = (0, import_react2.useState)(/* @__PURE__ */ new Set());
  const [scanning, setScanning] = (0, import_react2.useState)(false);
  const [scanned, setScanned] = (0, import_react2.useState)(false);
  const scanFonts = (0, import_react2.useCallback)(async () => {
    setScanning(true);
    try {
      setAvailableFamilies(await scanAvailableFontFamilies(usedFontFamilies));
      setScanned(true);
    } catch {
    } finally {
      setScanning(false);
    }
  }, [usedFontFamilies]);
  (0, import_react2.useEffect)(() => {
    if (isOpen && !scanned) {
      void scanFonts();
    }
  }, [isOpen, scanned, scanFonts]);
  if (!isOpen) {
    return null;
  }
  const missingFamilies = usedFontFamilies.filter((f) => !availableFamilies.has(f));
  const embeddedSet = new Set(embeddedFonts);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)("div", { className: "pointer-events-auto w-[460px] max-h-[80vh] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60 shrink-0", children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
              (0, import_jsx_runtime.jsx)(LuType, { className: "w-5 h-5 text-primary" }),
              (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.fonts.embedFonts") })
            ] }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "px-5 py-4 space-y-4 overflow-y-auto flex-1", children: [
            (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground", children: t("pptx.fonts.embedDescription") }),
            (0, import_jsx_runtime.jsxs)(
              "label",
              {
                className: `flex items-center gap-3 ${canEmbedFonts ? "cursor-pointer" : "cursor-not-allowed opacity-60"}`,
                children: [
                  (0, import_jsx_runtime.jsxs)("div", { className: "relative", children: [
                    (0, import_jsx_runtime.jsx)(
                      "input",
                      {
                        type: "checkbox",
                        className: "sr-only",
                        checked: embedFontsEnabled,
                        disabled: !canEmbedFonts,
                        onChange: (e) => onToggleEmbedFonts(e.target.checked)
                      }
                    ),
                    (0, import_jsx_runtime.jsx)(
                      "div",
                      {
                        className: `w-9 h-5 rounded-full transition-colors ${embedFontsEnabled ? "bg-primary" : "bg-muted-foreground"}`
                      }
                    ),
                    (0, import_jsx_runtime.jsx)(
                      "div",
                      {
                        className: `absolute top-0.5 left-0.5 w-4 h-4 rounded-full bg-white transition-transform ${embedFontsEnabled ? "translate-x-4" : ""}`
                      }
                    )
                  ] }),
                  (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-foreground", children: t("pptx.fonts.enableEmbedding") })
                ]
              }
            ),
            (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: canEmbedFonts ? t("pptx.fonts.embedKeepsExisting") : t(embedUnavailableKey ?? "pptx.fonts.embedUnavailable") }),
            (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
              (0, import_jsx_runtime.jsxs)("h3", { className: "text-xs font-medium text-foreground", children: [
                t("pptx.fonts.usedFonts"),
                " (",
                usedFontFamilies.length,
                ")"
              ] }),
              scanning ? (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 py-4 justify-center", children: [
                (0, import_jsx_runtime.jsx)(LuLoader, { className: "w-4 h-4 text-muted-foreground animate-spin" }),
                (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: t("pptx.fonts.scanning") })
              ] }) : (0, import_jsx_runtime.jsx)("div", { className: "space-y-1 max-h-[280px] overflow-y-auto", children: usedFontFamilies.map((family) => {
                const found = availableFamilies.has(family);
                const embedded = embeddedSet.has(family);
                return (0, import_jsx_runtime.jsxs)(
                  "div",
                  {
                    className: "flex items-center justify-between px-3 py-2 rounded-lg bg-muted/60",
                    children: [
                      (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-foreground", children: family }),
                      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
                        embedded && (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] px-1.5 py-0.5 rounded bg-green-900/40 text-green-400 border border-green-700/40", children: t("pptx.fonts.embedded") }),
                        found ? (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3.5 h-3.5 text-green-400" }) : (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-yellow-400", children: t("pptx.fonts.notFound") })
                      ] })
                    ]
                  },
                  family
                );
              }) })
            ] }),
            missingFamilies.length > 0 && !scanning && (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-yellow-400/80", children: t("pptx.fonts.missingWarning", {
              count: missingFamilies.length
            }) })
          ] }),
          (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-end px-5 py-3 border-t border-border/60 shrink-0", children: (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 text-xs rounded-lg bg-primary text-white hover:bg-primary/80 transition-colors",
              children: t("pptx.common.done")
            }
          ) })
        ] })
      }
    )
  ] });
}
function DigitalSignaturesDialog({
  isOpen,
  onClose,
  hasSignatures,
  signatureCount
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)("div", { className: "pointer-events-auto w-[420px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
              hasSignatures ? (0, import_jsx_runtime.jsx)(LuShieldCheck, { className: "w-5 h-5 text-green-400" }) : (0, import_jsx_runtime.jsx)(LuShieldAlert, { className: "w-5 h-5 text-amber-400" }),
              (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.digitalSignatures.title") })
            ] }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          (0, import_jsx_runtime.jsx)("div", { className: "px-5 py-6 space-y-4", children: hasSignatures ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-3 rounded-lg bg-green-900/20 border border-green-700/30 px-4 py-3", children: [
              (0, import_jsx_runtime.jsx)(LuSignature, { className: "w-5 h-5 text-green-400 shrink-0 mt-0.5" }),
              (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
                (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-green-200", children: t("pptx.digitalSignatures.signed") }),
                (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-green-300/70", children: t("pptx.digitalSignatures.signatureCount", {
                  count: signatureCount
                }) })
              ] })
            ] }),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-3 rounded-lg bg-amber-900/20 border border-amber-700/30 px-4 py-3", children: [
              (0, import_jsx_runtime.jsx)(LuInfo, { className: "w-5 h-5 text-amber-400 shrink-0 mt-0.5" }),
              (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-amber-200", children: t("pptx.digitalSignatures.editWarning") })
            ] })
          ] }) : (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-3 rounded-lg bg-muted/30 border border-border/40 px-4 py-3", children: [
            (0, import_jsx_runtime.jsx)(LuInfo, { className: "w-5 h-5 text-muted-foreground shrink-0 mt-0.5" }),
            (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground", children: t("pptx.digitalSignatures.noSignatures") })
          ] }) }) }),
          (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-end px-5 py-3 border-t border-border/60", children: (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 text-xs rounded-lg bg-accent text-foreground hover:bg-accent/80 transition-colors",
              children: t("pptx.common.close")
            }
          ) })
        ] })
      }
    )
  ] });
}
function SignatureStatusBadge({
  hasSignatures,
  signatureCount,
  onClick
}) {
  const { t } = useTranslation();
  if (!hasSignatures || signatureCount === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      onClick,
      className: "inline-flex items-center gap-1 px-2 py-0.5 text-[11px] font-medium rounded-md bg-green-900/30 border border-green-700/40 text-green-300 hover:bg-green-900/50 transition-colors",
      title: t("pptx.digitalSignatures.signatureCount", {
        count: signatureCount
      }),
      children: [
        (0, import_jsx_runtime.jsx)(LuShieldCheck, { className: "w-3.5 h-3.5" }),
        (0, import_jsx_runtime.jsx)("span", { children: t("pptx.digitalSignatures.badge") })
      ]
    }
  );
}
function SignatureStrippedDialog({
  isOpen,
  signatureCount,
  onConfirm,
  onCancel
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onCancel
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)("div", { className: "pointer-events-auto w-[440px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
              (0, import_jsx_runtime.jsx)(LuTriangleAlert, { className: "w-5 h-5 text-amber-400" }),
              (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.digitalSignatures.strippedTitle") })
            ] }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onCancel,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          (0, import_jsx_runtime.jsx)("div", { className: "px-5 py-6", children: (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-3 rounded-lg bg-amber-900/20 border border-amber-700/30 px-4 py-3", children: [
            (0, import_jsx_runtime.jsx)(LuTriangleAlert, { className: "w-5 h-5 text-amber-400 shrink-0 mt-0.5" }),
            (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
              (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-amber-200", children: t("pptx.digitalSignatures.strippedMessage", {
                count: signatureCount
              }) }),
              (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-amber-300/70", children: t("pptx.digitalSignatures.editWarning") })
            ] })
          ] }) }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 px-5 py-3 border-t border-border/60", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onCancel,
                className: "px-3 py-1.5 text-xs rounded-lg bg-accent text-foreground hover:bg-accent/80 transition-colors",
                children: t("pptx.common.cancel")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onConfirm,
                className: "px-3 py-1.5 text-xs rounded-lg bg-amber-600 text-white hover:bg-amber-500 transition-colors",
                children: t("pptx.digitalSignatures.strippedConfirm")
              }
            )
          ] })
        ] })
      }
    )
  ] });
}
function EncryptedFileDialog({
  isOpen,
  onClose
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.common.close"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)("div", { className: "pointer-events-auto w-[420px] rounded-xl border border-border bg-popover backdrop-blur-xl shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border/60", children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
              (0, import_jsx_runtime.jsx)(LuLock, { className: "w-5 h-5 text-red-400" }),
              (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.encryptedFile.title") })
            ] }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "p-1 rounded hover:bg-accent transition-colors",
                "aria-label": t("pptx.common.close"),
                children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4 text-muted-foreground" })
              }
            )
          ] }),
          (0, import_jsx_runtime.jsx)("div", { className: "px-5 py-6 space-y-4", children: (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-3 rounded-lg bg-red-900/20 border border-red-700/30 px-4 py-3", children: [
            (0, import_jsx_runtime.jsx)(LuInfo, { className: "w-5 h-5 text-red-400 shrink-0 mt-0.5" }),
            (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
              (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-red-200", children: t("pptx.encryptedFile.message") }),
              (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-red-300/70", children: t("pptx.encryptedFile.instructions") })
            ] })
          ] }) }),
          (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-end px-5 py-3 border-t border-border/60", children: (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 text-xs rounded-lg bg-accent text-foreground hover:bg-accent/80 transition-colors",
              children: t("pptx.common.close")
            }
          ) })
        ] })
      }
    )
  ] });
}
function PresenterSlideFrame({
  canvasSize,
  zoomScale = 1,
  zoomOriginX = 0.5,
  zoomOriginY = 0.5,
  inkProps,
  children
}) {
  const paneRef = (0, import_react2.useRef)(null);
  const [pane, setPane] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    const el = paneRef.current;
    if (!el) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      var _a;
      const rect = (_a = entries[0]) == null ? void 0 : _a.contentRect;
      if (rect && rect.width > 0 && rect.height > 0) {
        setPane({ width: rect.width, height: rect.height });
      }
    });
    observer.observe(el);
    const initial = el.getBoundingClientRect();
    if (initial.width > 0 && initial.height > 0) {
      setPane({ width: initial.width, height: initial.height });
    }
    return () => {
      observer.disconnect();
    };
  }, []);
  const safeWidth = Math.max(canvasSize.width, 1);
  const safeHeight = Math.max(canvasSize.height, 1);
  const fittedWidth = pane ? Math.max(1, Math.min(pane.width, pane.height * safeWidth / safeHeight)) : 0;
  return (0, import_jsx_runtime.jsx)("div", { ref: paneRef, className: "flex min-h-0 w-full flex-1 items-center justify-center", children: (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "relative transition-transform duration-200",
      style: {
        width: fittedWidth || void 0,
        transform: `scale(${zoomScale})`,
        transformOrigin: `${zoomOriginX * 100}% ${zoomOriginY * 100}%`,
        touchAction: "none"
      },
      ...inkProps,
      children: fittedWidth > 0 && children
    }
  ) });
}
function PresentationAudienceEffects({ snapshot }) {
  var _a, _b2;
  const strokes = ((_a = snapshot.inkStrokes) == null ? void 0 : _a.filter((stroke) => stroke.slideIndex === snapshot.slideIndex)) ?? [];
  return (0, import_jsx_runtime.jsxs)("div", { className: "pointer-events-none absolute inset-0 z-[75] overflow-hidden", children: [
    snapshot.blackout !== "none" && (0, import_jsx_runtime.jsx)(
      "div",
      {
        "data-pptx-blackout": true,
        className: `absolute inset-0 ${snapshot.blackout === "black" ? "bg-black" : "bg-white"}`
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "svg",
      {
        className: "absolute inset-0 h-full w-full",
        viewBox: "0 0 100 100",
        preserveAspectRatio: "none",
        "aria-hidden": "true",
        children: strokes.map((stroke) => (0, import_jsx_runtime.jsx)(
          "path",
          {
            d: presentationInkPath(stroke.points),
            fill: "none",
            stroke: stroke.color,
            strokeWidth: stroke.width / 10,
            strokeLinecap: "round",
            strokeLinejoin: "round",
            opacity: stroke.tool === "highlighter" ? 0.42 : 1
          },
          stroke.id
        ))
      }
    ),
    ((_b2 = snapshot.pointer) == null ? void 0 : _b2.tool) === "laser" && (0, import_jsx_runtime.jsx)(
      "span",
      {
        className: "absolute h-5 w-5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-500 shadow-[0_0_20px_8px_rgba(239,68,68,.55)]",
        style: { left: `${snapshot.pointer.x * 100}%`, top: `${snapshot.pointer.y * 100}%` }
      }
    ),
    snapshot.subtitlesVisible && snapshot.caption && (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-x-[10%] bottom-8 rounded-lg bg-black/80 px-6 py-3 text-center text-xl font-medium text-white", children: snapshot.caption })
  ] });
}
function PresentationEndOverlay({ onExit }) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    "button",
    {
      type: "button",
      "data-pptx-end-of-show": "",
      onClick: onExit,
      className: "absolute inset-0 z-[90] flex cursor-default items-start bg-black text-left",
      children: (0, import_jsx_runtime.jsx)("span", { className: "px-4 py-3 text-[12px] text-white/70", children: t("pptx.presentation.endOfSlideShow", "End of slide show, click to exit.") })
    }
  );
}
function PresentationSubtitleBar({
  visible,
  onCaptionChange
}) {
  const { t } = useTranslation();
  const [captionText, setCaptionText] = (0, import_react2.useState)("");
  const [supportState, setSupportState] = (0, import_react2.useState)(
    "unknown"
  );
  const recognitionRef = (0, import_react2.useRef)(null);
  const shouldRunRef = (0, import_react2.useRef)(false);
  (0, import_react2.useEffect)(() => {
    var _a;
    if (!visible) {
      shouldRunRef.current = false;
      (_a = recognitionRef.current) == null ? void 0 : _a.stop();
      recognitionRef.current = null;
      setCaptionText("");
      return;
    }
    shouldRunRef.current = true;
    const speechWindow = window;
    const RecognitionCtor = speechWindow.SpeechRecognition ?? speechWindow.webkitSpeechRecognition;
    if (!RecognitionCtor) {
      setSupportState("unsupported");
      return;
    }
    setSupportState("supported");
    const recognition = new RecognitionCtor();
    recognition.continuous = true;
    recognition.interimResults = true;
    recognition.lang = navigator.language || "en-US";
    recognition.onresult = (event) => {
      var _a2;
      let finalText = "";
      let interimText = "";
      for (let index = event.resultIndex; index < event.results.length; index += 1) {
        const result = event.results[index];
        const fragment = ((_a2 = result == null ? void 0 : result[0]) == null ? void 0 : _a2.transcript) ?? "";
        if (result == null ? void 0 : result.isFinal) {
          finalText += fragment;
        } else {
          interimText += fragment;
        }
      }
      const merged = `${finalText} ${interimText}`.trim();
      if (merged.length > 0) {
        setCaptionText(merged);
        onCaptionChange == null ? void 0 : onCaptionChange(merged);
      }
    };
    recognition.onerror = () => {
    };
    recognition.onend = () => {
      if (!shouldRunRef.current) {
        return;
      }
      try {
        recognition.start();
      } catch {
      }
    };
    recognitionRef.current = recognition;
    try {
      recognition.start();
    } catch {
      setSupportState("unsupported");
    }
    return () => {
      shouldRunRef.current = false;
      recognition.stop();
      recognitionRef.current = null;
    };
  }, [visible, onCaptionChange]);
  if (!visible) {
    return null;
  }
  const renderedText = supportState === "unsupported" ? t("pptx.subtitles.notSupported") : captionText.length > 0 ? captionText : t("pptx.subtitles.listening");
  return (0, import_jsx_runtime.jsx)("div", { className: "absolute bottom-14 left-1/2 -translate-x-1/2 z-[70] max-w-[80%] min-w-[300px]", children: (0, import_jsx_runtime.jsx)("div", { className: "px-6 py-3 rounded-lg bg-black/75 backdrop-blur-sm border border-white/10", children: (0, import_jsx_runtime.jsx)("p", { className: "text-center text-[15px] text-white/70 italic", children: renderedText }) }) });
}
function renderNotesSegments(segments) {
  return notesSegmentsToSpans(segments).map((span) => {
    if (span.kind === "break") {
      return import_react2.default.createElement("br", { key: span.key });
    }
    return import_react2.default.createElement(
      "span",
      { key: span.key, style: span.style },
      span.text
    );
  });
}
var CONSOLE_ICONS = {
  "arrow-left-right": LuArrowLeftRight,
  captions: LuCaptions,
  "circle-pause": LuCirclePause,
  "circle-play": LuCirclePlay,
  eraser: LuEraser,
  "grid-2x2": LuGrid2X2,
  highlighter: LuHighlighter,
  monitor: LuMonitor,
  "monitor-off": LuMonitorOff,
  "mouse-pointer-2": LuMousePointer2,
  "pen-tool": LuPenTool,
  "rotate-ccw": LuRotateCcw,
  scan: LuScan,
  x: LuX,
  "zoom-in": LuZoomIn,
  "zoom-out": LuZoomOut
};
var POINTER_TOOLS = {
  laser: "laser",
  pen: "pen",
  highlighter: "highlighter",
  eraser: "eraser"
};
function slotState(control, props) {
  var _a, _b2;
  const snapshot = props.snapshot;
  const pointerTool = ((_a = snapshot.pointer) == null ? void 0 : _a.tool) ?? "none";
  const tool = POINTER_TOOLS[control.id];
  if (tool) {
    return {
      active: pointerTool === tool,
      onClick: () => props.onPointerTool(pointerTool === tool ? "none" : tool)
    };
  }
  switch (control.id) {
    case "timer-toggle":
      return { active: Boolean(snapshot.paused), onClick: props.onToggleTimer };
    case "timer-reset":
      return { active: false, onClick: props.onResetTimer };
    case "all-slides":
      return { active: false, onClick: props.onShowSlides };
    case "zoom-in":
      return { active: (((_b2 = snapshot.zoom) == null ? void 0 : _b2.scale) ?? 1) > 1, onClick: () => props.onStepZoom(1) };
    case "zoom-out":
      return { active: false, onClick: () => props.onStepZoom(-1) };
    case "zoom-reset":
      return { active: false, onClick: props.onResetZoom };
    case "blackout-black":
      return {
        active: snapshot.blackout === "black",
        onClick: () => props.onBlackout(snapshot.blackout === "black" ? "none" : "black")
      };
    case "blackout-white":
      return {
        active: snapshot.blackout === "white",
        onClick: () => props.onBlackout(snapshot.blackout === "white" ? "none" : "white")
      };
    case "captions":
      return { active: Boolean(snapshot.subtitlesVisible), onClick: props.onToggleSubtitles };
    case "audience":
      return { active: props.audienceOpen, onClick: props.onToggleAudience };
    case "swap-displays":
      return { active: false, onClick: props.onSwapDisplays, disabled: !props.audienceOpen };
    case "end":
      return { active: false, onClick: props.onExit };
    default:
      return { active: false, onClick: () => void 0 };
  }
}
function PresenterConsoleToolbar(props) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)("div", { className: PRESENTER_CONSOLE_CLASSES.strip, "data-pptx-presenter-toolbar": true, children: PRESENTER_CONSOLE_CONTROLS.map((control) => {
    if (control.kind === "divider" || control.kind === "spacer") {
      return (0, import_jsx_runtime.jsx)(
        "span",
        {
          "data-pptx-presenter-control": control.id,
          className: control.kind === "divider" ? PRESENTER_CONSOLE_CLASSES.divider : PRESENTER_CONSOLE_CLASSES.spacer
        },
        control.id
      );
    }
    const state = slotState(control, props);
    const labelKey = state.active && control.activeLabelKey ? control.activeLabelKey : control.labelKey;
    const iconName = state.active && control.activeIcon ? control.activeIcon : control.icon;
    const Icon = iconName === void 0 ? void 0 : CONSOLE_ICONS[iconName];
    const label = labelKey === void 0 ? void 0 : t(labelKey);
    return (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        "data-pptx-presenter-control": control.id,
        className: state.active ? PRESENTER_CONSOLE_CLASSES.controlActive : PRESENTER_CONSOLE_CLASSES.control,
        onClick: state.onClick,
        disabled: state.disabled,
        "aria-label": label,
        title: label,
        "aria-pressed": control.kind === "toggle" ? state.active : void 0,
        children: [
          Icon ? (0, import_jsx_runtime.jsx)(Icon, {}) : null,
          control.glyph
        ]
      },
      control.id
    );
  }) });
}
function ScaledSlidePreviewImpl({
  slide,
  templateElements,
  canvasSize,
  className
}) {
  const containerRef = (0, import_react2.useRef)(null);
  const [containerWidth, setContainerWidth] = (0, import_react2.useState)(0);
  (0, import_react2.useEffect)(() => {
    const el = containerRef.current;
    if (!el) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      const entry = entries[0];
      if (entry) {
        setContainerWidth(entry.contentRect.width);
      }
    });
    observer.observe(el);
    return () => observer.disconnect();
  }, []);
  const safeCanvasWidth = Math.max(canvasSize.width, 1);
  const safeCanvasHeight = Math.max(canvasSize.height, 1);
  const scale = containerWidth > 0 ? containerWidth / safeCanvasWidth : 0.2;
  const previewHeight = Math.max(40, Math.round(safeCanvasHeight * scale));
  const previewElements = [
    ...visibleTemplateElements(slide, templateElements),
    ...slide.elements
  ].slice(0, 80);
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: containerRef,
      className: `relative w-full overflow-hidden rounded border border-border bg-white ${className ?? ""}`,
      style: { height: previewHeight },
      children: [
        slide.backgroundColor && slide.backgroundColor !== "transparent" && (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute inset-0",
            style: {
              backgroundColor: normalizeHexColor2(slide.backgroundColor, "#ffffff")
            }
          }
        ),
        slide.backgroundImage && (0, import_jsx_runtime.jsx)(
          "img",
          {
            src: slide.backgroundImage,
            alt: "",
            className: "absolute inset-0 w-full h-full object-cover pointer-events-none",
            draggable: false
          }
        ),
        slide.backgroundGradient && (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0", style: { backgroundImage: slide.backgroundGradient } }),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute top-0 left-0 origin-top-left",
            style: {
              width: safeCanvasWidth,
              height: safeCanvasHeight,
              transform: `scale(${scale})`,
              transformOrigin: "top left"
            },
            children: previewElements.map((element, index) => (0, import_jsx_runtime.jsx)(
              StaticElementRenderer,
              {
                element,
                activeSlide: slide,
                allSlides: [slide],
                zIndex: index
              },
              element.id
            ))
          }
        )
      ]
    }
  );
}
function arePropsEqual4(prev, next) {
  if (prev.slide.id !== next.slide.id) {
    return false;
  }
  if (prev.slide.isDirty !== next.slide.isDirty) {
    return false;
  }
  if (prev.slide.hidden !== next.slide.hidden) {
    return false;
  }
  if (prev.slide.elements !== next.slide.elements) {
    return false;
  }
  if (prev.slide.backgroundColor !== next.slide.backgroundColor) {
    return false;
  }
  if (prev.slide.backgroundImage !== next.slide.backgroundImage) {
    return false;
  }
  if (prev.slide.backgroundGradient !== next.slide.backgroundGradient) {
    return false;
  }
  if (prev.templateElements !== next.templateElements) {
    return false;
  }
  if (prev.canvasSize.width !== next.canvasSize.width || prev.canvasSize.height !== next.canvasSize.height) {
    return false;
  }
  if (prev.className !== next.className) {
    return false;
  }
  return true;
}
var ScaledSlidePreview = import_react2.default.memo(ScaledSlidePreviewImpl, arePropsEqual4);
function PresenterNotesRail({
  slides,
  current,
  canvasSize,
  templateElements,
  now,
  elapsed,
  activeCustomShow,
  authoredRange,
  onMove,
  onUpdateNotes
}) {
  const { t } = useTranslation();
  const slide = slides[current];
  const nextSlide = nextPresentedSlide(slides, current, activeCustomShow, authoredRange);
  const notesText = (slide == null ? void 0 : slide.notes) ?? "";
  const notesSegments = slide == null ? void 0 : slide.notesSegments;
  const [notesDraft, setNotesDraft] = (0, import_react2.useState)(notesText);
  const [fontSize, setFontSize] = (0, import_react2.useState)(NOTES_FONT_SIZE_DEFAULT);
  (0, import_react2.useEffect)(() => setNotesDraft(notesText), [current, notesText]);
  const notes = (0, import_react2.useMemo)(
    () => (notesSegments == null ? void 0 : notesSegments.length) ? renderNotesSegments(notesSegments) : notesText.trim() ? notesText : (0, import_jsx_runtime.jsx)("span", { className: "italic text-muted-foreground", children: t("pptx.presenter.noNotes") }),
    [notesSegments, notesText, t]
  );
  return (0, import_jsx_runtime.jsxs)("aside", { className: PRESENTER_CONSOLE_CLASSES.rail, children: [
    (0, import_jsx_runtime.jsxs)("header", { className: "flex items-center justify-between border-b border-border/60 px-4 py-3", children: [
      (0, import_jsx_runtime.jsxs)("div", { children: [
        (0, import_jsx_runtime.jsx)("div", { className: PRESENTER_CONSOLE_CLASSES.railHeading, children: t("pptx.presenter.currentTime") }),
        (0, import_jsx_runtime.jsx)("div", { className: "font-mono text-lg tabular-nums", children: formatTime(new Date(now)) })
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "text-right", children: [
        (0, import_jsx_runtime.jsx)("div", { className: PRESENTER_CONSOLE_CLASSES.railHeading, children: t("pptx.presenter.elapsed") }),
        (0, import_jsx_runtime.jsx)("div", { className: "font-mono text-lg tabular-nums text-primary", children: formatElapsed(elapsed) })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("nav", { className: "flex items-center justify-between border-b border-border/60 px-4 py-2", children: [
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          onClick: () => onMove(-1),
          disabled: presenterPrevDisabled(current),
          "data-pptx-presenter-control": "prev",
          className: "inline-flex items-center gap-1 rounded bg-muted px-3 py-1.5 text-xs disabled:opacity-40",
          children: [
            (0, import_jsx_runtime.jsx)(LuChevronLeft, {}),
            " ",
            t("pptx.presenter.prev")
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)("span", { className: "font-mono text-sm tabular-nums", children: [
        current + 1,
        " / ",
        slides.length
      ] }),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          onClick: () => onMove(1),
          disabled: presenterNextDisabled(),
          "data-pptx-presenter-control": "next",
          className: "inline-flex items-center gap-1 rounded bg-muted px-3 py-1.5 text-xs disabled:opacity-40",
          children: [
            t("pptx.presenter.next"),
            " ",
            (0, import_jsx_runtime.jsx)(LuChevronRight, {})
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("section", { className: "border-b border-border/60 px-4 py-3", "data-pptx-presenter-next-preview": true, children: [
      (0, import_jsx_runtime.jsx)("div", { className: `mb-2 ${PRESENTER_CONSOLE_CLASSES.railHeading}`, children: t("pptx.presenter.nextSlidePreview") }),
      nextSlide ? (0, import_jsx_runtime.jsx)(
        ScaledSlidePreview,
        {
          slide: nextSlide,
          templateElements,
          canvasSize
        }
      ) : (0, import_jsx_runtime.jsx)("div", { className: "flex h-16 items-center justify-center rounded bg-muted/40 text-xs italic text-muted-foreground", children: t("pptx.presenter.endOfPresentation") })
    ] }),
    (0, import_jsx_runtime.jsxs)("section", { className: "flex min-h-0 flex-1 flex-col px-4 py-3", "data-pptx-presenter-notes": true, children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "mb-2 flex items-center justify-between", children: [
        (0, import_jsx_runtime.jsx)("div", { className: PRESENTER_CONSOLE_CLASSES.railHeading, children: t("pptx.presenter.speakerNotes") }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: () => setFontSize(clampNotesFontSize(fontSize - NOTES_FONT_SIZE_STEP)),
              disabled: fontSize <= NOTES_FONT_SIZE_MIN,
              "data-pptx-presenter-control": "notes-font-decrease",
              "aria-label": t("pptx.presenter.decreaseFontSize"),
              children: (0, import_jsx_runtime.jsx)(LuMinus, {})
            }
          ),
          (0, import_jsx_runtime.jsxs)("span", { className: "min-w-8 text-center font-mono text-[10px]", children: [
            fontSize,
            "px"
          ] }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: () => setFontSize(clampNotesFontSize(fontSize + NOTES_FONT_SIZE_STEP)),
              disabled: fontSize >= NOTES_FONT_SIZE_MAX,
              "data-pptx-presenter-control": "notes-font-increase",
              "aria-label": t("pptx.presenter.increaseFontSize"),
              children: (0, import_jsx_runtime.jsx)(LuPlus, {})
            }
          )
        ] })
      ] }),
      onUpdateNotes ? (0, import_jsx_runtime.jsx)(
        "textarea",
        {
          className: "min-h-0 flex-1 resize-none rounded border border-border/30 bg-muted/40 px-3 py-2 leading-relaxed",
          style: { fontSize },
          value: notesDraft,
          onChange: (event) => setNotesDraft(event.target.value),
          onBlur: () => onUpdateNotes(notesDraft),
          "aria-label": t("pptx.presenter.speakerNotes")
        }
      ) : (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: "min-h-0 flex-1 overflow-y-auto whitespace-pre-wrap rounded border border-border/30 bg-muted/40 px-3 py-2 leading-relaxed",
          style: { fontSize },
          children: notes
        }
      )
    ] })
  ] });
}
function PresenterSlideNavigator(props) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: `${PRESENTER_CONSOLE_CLASSES.navigator} text-foreground`, children: [
    (0, import_jsx_runtime.jsxs)("header", { className: "mb-4 flex items-center justify-between border-b border-border pb-4", children: [
      (0, import_jsx_runtime.jsxs)("div", { children: [
        (0, import_jsx_runtime.jsx)("p", { className: "text-xs uppercase tracking-[0.22em] text-sky-300", children: t(PRESENTER_NAVIGATOR_LABEL_KEYS.title) }),
        (0, import_jsx_runtime.jsx)("h2", { className: "text-xl font-semibold", children: t(PRESENTER_NAVIGATOR_LABEL_KEYS.subtitle) })
      ] }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "rounded-md bg-muted px-4 py-2 hover:bg-accent",
          onClick: props.onClose,
          children: t(PRESENTER_NAVIGATOR_LABEL_KEYS.close)
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: PRESENTER_CONSOLE_CLASSES.navigatorGrid, children: props.slides.map((slide, index) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: `group text-left ${index === props.current ? "ring-2 ring-sky-400" : ""} ${slide.hidden ? "opacity-45" : ""}`,
        onClick: () => props.onSelect(index),
        children: [
          (0, import_jsx_runtime.jsx)(
            ScaledSlidePreview,
            {
              slide,
              templateElements: props.templateElements,
              canvasSize: props.canvasSize
            }
          ),
          (0, import_jsx_runtime.jsxs)("span", { className: "mt-2 block text-xs tabular-nums text-muted-foreground", children: [
            index + 1,
            slide.hidden ? " - hidden" : ""
          ] })
        ]
      },
      slide.id ?? index
    )) })
  ] });
}
function point(event) {
  const rect = event.currentTarget.getBoundingClientRect();
  return {
    x: (event.clientX - rect.left) / rect.width,
    y: (event.clientY - rect.top) / rect.height
  };
}
function usePresenterInk(snapshot, onUpdate) {
  const drawingId = (0, import_react2.useRef)(null);
  const onPointerDown = (0, import_react2.useCallback)(
    (event) => {
      const pointer = snapshot.pointer;
      if (!pointer || pointer.tool === "none" || pointer.tool === "laser") {
        return;
      }
      const nextPoint = point(event);
      if (pointer.tool === "eraser") {
        onUpdate({
          inkStrokes: erasePresentationInkAt(
            snapshot.inkStrokes ?? [],
            snapshot.slideIndex,
            nextPoint
          )
        });
        return;
      }
      event.currentTarget.setPointerCapture(event.pointerId);
      const stroke = {
        id: crypto.randomUUID(),
        slideIndex: snapshot.slideIndex,
        tool: pointer.tool,
        color: pointer.color,
        width: pointer.tool === "highlighter" ? 12 : 4,
        points: [nextPoint]
      };
      drawingId.current = stroke.id;
      onUpdate({ inkStrokes: [...snapshot.inkStrokes ?? [], stroke] });
    },
    [onUpdate, snapshot]
  );
  const onPointerMove = (0, import_react2.useCallback)(
    (event) => {
      const nextPoint = point(event);
      const pointer = snapshot.pointer;
      if (!pointer) {
        return;
      }
      if (pointer.tool === "laser") {
        onUpdate({ pointer: movePresenterPointer(pointer, nextPoint.x, nextPoint.y) });
      }
      if (pointer.tool === "eraser" && event.buttons === 1) {
        onUpdate({
          inkStrokes: erasePresentationInkAt(
            snapshot.inkStrokes ?? [],
            snapshot.slideIndex,
            nextPoint
          )
        });
      }
      if (drawingId.current && event.buttons === 1) {
        onUpdate({
          inkStrokes: (snapshot.inkStrokes ?? []).map(
            (stroke) => stroke.id === drawingId.current ? appendPresentationInkPoint(stroke, nextPoint) : stroke
          )
        });
      }
    },
    [onUpdate, snapshot]
  );
  const onPointerUp = (0, import_react2.useCallback)(() => {
    drawingId.current = null;
  }, []);
  return { onPointerDown, onPointerMove, onPointerUp };
}
function PresenterView({
  slides,
  currentSlideIndex,
  canvasSize,
  templateElements,
  presentationStartTime,
  activeCustomShow,
  authoredRange,
  onMovePresentationSlide,
  onExit,
  onOpenAudienceWindow,
  onCloseAudienceWindow,
  isAudienceWindowOpen,
  endOfShowVisible,
  snapshot,
  onNavigateToSlide,
  onToggleTimer,
  onResetTimer,
  onStepZoom,
  onResetZoom,
  onSetBlackout,
  onUpdateSnapshot,
  onToggleSubtitles,
  onSwapDisplays,
  onUpdateNotes
}) {
  var _a, _b2, _c, _d;
  const { t } = useTranslation();
  const [now, setNow] = (0, import_react2.useState)(Date.now());
  (0, import_react2.useEffect)(() => {
    const interval = window.setInterval(() => setNow(Date.now()), 1e3);
    return () => window.clearInterval(interval);
  }, []);
  const elapsed = snapshot.elapsedMs ?? (presentationStartTime ? now - presentationStartTime : 0);
  const [showSlides, setShowSlides] = (0, import_react2.useState)(false);
  const ink = usePresenterInk(snapshot, onUpdateSnapshot);
  const paneAdvancesOnClick = presenterPaneAdvancesOnClick((_a = snapshot.pointer) == null ? void 0 : _a.tool) && !endOfShowVisible;
  const handleSlidePaneClick = (0, import_react2.useCallback)(() => {
    if (!paneAdvancesOnClick) {
      return;
    }
    onMovePresentationSlide(1, "click");
  }, [paneAdvancesOnClick, onMovePresentationSlide]);
  const handleCaptionChange = (0, import_react2.useCallback)(
    (caption) => onUpdateSnapshot({ caption }),
    [onUpdateSnapshot]
  );
  const currentSlide = slides[currentSlideIndex];
  if (!currentSlide) {
    return (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 z-50 flex items-center justify-center bg-card text-muted-foreground", children: t("pptx.presenter.noSlides") });
  }
  const timerProgress = presenterTimerProgress(elapsed);
  return (0, import_jsx_runtime.jsxs)("div", { className: PRESENTER_CONSOLE_CLASSES.root, children: [
    (0, import_jsx_runtime.jsx)(
      PresenterConsoleToolbar,
      {
        snapshot,
        audienceOpen: Boolean(isAudienceWindowOpen),
        onToggleAudience: () => isAudienceWindowOpen ? onCloseAudienceWindow == null ? void 0 : onCloseAudienceWindow() : onOpenAudienceWindow == null ? void 0 : onOpenAudienceWindow(),
        onSwapDisplays,
        onToggleTimer,
        onResetTimer,
        onShowSlides: () => setShowSlides(true),
        onStepZoom,
        onResetZoom,
        onBlackout: onSetBlackout,
        onPointerTool: (tool) => onUpdateSnapshot({
          pointer: { ...snapshot.pointer ?? { x: 0.5, y: 0.5, color: "#ef4444" }, tool }
        }),
        onToggleSubtitles,
        onExit
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: PRESENTER_CONSOLE_CLASSES.body, children: [
      (0, import_jsx_runtime.jsxs)(
        "div",
        {
          role: "presentation",
          "data-pptx-presenter-slide": true,
          onClick: handleSlidePaneClick,
          className: `${PRESENTER_CONSOLE_CLASSES.main} ${paneAdvancesOnClick ? "cursor-pointer" : ""}`,
          children: [
            endOfShowVisible && (0, import_jsx_runtime.jsx)(PresentationEndOverlay, { onExit: () => onMovePresentationSlide(1) }),
            (0, import_jsx_runtime.jsxs)(
              PresenterSlideFrame,
              {
                canvasSize,
                zoomScale: ((_b2 = snapshot.zoom) == null ? void 0 : _b2.scale) ?? 1,
                zoomOriginX: ((_c = snapshot.zoom) == null ? void 0 : _c.originX) ?? 0.5,
                zoomOriginY: ((_d = snapshot.zoom) == null ? void 0 : _d.originY) ?? 0.5,
                inkProps: ink,
                children: [
                  (0, import_jsx_runtime.jsx)(
                    ScaledSlidePreview,
                    {
                      slide: currentSlide,
                      templateElements,
                      canvasSize
                    }
                  ),
                  (0, import_jsx_runtime.jsx)(PresentationAudienceEffects, { snapshot })
                ]
              }
            ),
            (0, import_jsx_runtime.jsx)("div", { className: "mt-3 text-xs font-mono tabular-nums text-white/50 select-none", children: t("pptx.presenter.slideLabel", {
              current: currentSlideIndex + 1,
              total: slides.length,
              defaultValue: `Slide ${currentSlideIndex + 1} of ${slides.length}`
            }) })
          ]
        }
      ),
      (0, import_jsx_runtime.jsx)(
        PresenterNotesRail,
        {
          slides,
          current: currentSlideIndex,
          canvasSize,
          templateElements,
          activeCustomShow,
          authoredRange,
          now,
          elapsed,
          onMove: onMovePresentationSlide,
          onUpdateNotes
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      PresentationSubtitleBar,
      {
        visible: Boolean(snapshot.subtitlesVisible),
        onCaptionChange: handleCaptionChange
      }
    ),
    showSlides && (0, import_jsx_runtime.jsx)(
      PresenterSlideNavigator,
      {
        slides,
        current: currentSlideIndex,
        canvasSize,
        templateElements,
        onSelect: (index) => {
          onNavigateToSlide(index);
          setShowSlides(false);
        },
        onClose: () => setShowSlides(false)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: PRESENTER_CONSOLE_CLASSES.progressTrack,
        role: "progressbar",
        "aria-valuenow": Math.round(timerProgress.percent),
        "aria-valuemin": 0,
        "aria-valuemax": 100,
        "aria-label": t("pptx.presenter.timerProgress"),
        title: t("pptx.presenter.timerTitle", {
          elapsed: formatElapsed(elapsed),
          segment: timerProgress.segment + 1
        }),
        children: (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: PRESENTER_CONSOLE_CLASSES.progressFill,
            style: { width: `${timerProgress.percent}%` }
          }
        )
      }
    )
  ] });
}
function MobilePresenterView({
  slides,
  currentSlideIndex,
  canvasSize,
  templateElements,
  activeCustomShow,
  authoredRange,
  presentationStartTime,
  onMovePresentationSlide,
  onExit
}) {
  const { t } = useTranslation();
  const [now, setNow] = (0, import_react2.useState)(Date.now());
  (0, import_react2.useEffect)(() => {
    const interval = window.setInterval(() => setNow(Date.now()), 1e3);
    return () => window.clearInterval(interval);
  }, []);
  const elapsed = mobileElapsedSince(presentationStartTime, now);
  const currentSlide = slides[currentSlideIndex];
  const nextSlide = nextPresentedSlide(slides, currentSlideIndex, activeCustomShow, authoredRange);
  if (!currentSlide) {
    return (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 z-50 flex items-center justify-center bg-card text-muted-foreground", children: t("pptx.presenter.noSlides") });
  }
  const notesText = currentSlide.notes ?? "";
  const notesSegments = currentSlide.notesSegments;
  const hasRichNotes = notesSegments && notesSegments.length > 0;
  const prevDisabled = presenterPrevDisabled(currentSlideIndex);
  const nextDisabled = presenterNextDisabled();
  const insetStyle = {
    paddingTop: "env(safe-area-inset-top, 0px)",
    paddingBottom: "env(safe-area-inset-bottom, 0px)",
    paddingLeft: "env(safe-area-inset-left, 0px)",
    paddingRight: "env(safe-area-inset-right, 0px)"
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "absolute inset-0 z-50 flex flex-col bg-card text-foreground", style: insetStyle, children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2 px-4 py-2 border-b border-border/60", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wider", children: t("pptx.presenter.elapsed") }),
        (0, import_jsx_runtime.jsx)("span", { className: "text-lg font-mono tabular-nums text-primary", children: formatMobileElapsed(elapsed) })
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-sm font-mono tabular-nums text-foreground", children: mobileSlideCounter(currentSlideIndex, slides.length) }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onExit,
          className: "flex items-center justify-center w-11 h-11 min-w-11 min-h-11 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors",
          title: t("pptx.presenter.endPresentation"),
          "aria-label": t("pptx.presenter.endPresentation"),
          children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-5 h-5" })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center bg-black px-3 py-3", children: (0, import_jsx_runtime.jsx)("div", { className: "w-full max-w-[640px]", children: (0, import_jsx_runtime.jsx)(
      ScaledSlidePreview,
      {
        slide: currentSlide,
        templateElements,
        canvasSize
      }
    ) }) }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 px-4 py-2 border-b border-border/60", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wider whitespace-nowrap", children: t("pptx.presenter.nextSlidePreview") }),
      (0, import_jsx_runtime.jsx)("div", { className: "w-[132px] flex-shrink-0", children: nextSlide ? (0, import_jsx_runtime.jsx)(
        ScaledSlidePreview,
        {
          slide: nextSlide,
          templateElements,
          canvasSize
        }
      ) : (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-12 rounded border border-border/30 bg-muted/40 text-[10px] text-muted-foreground italic", children: t("pptx.presenter.endOfPresentation") }) })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 flex flex-col min-h-0 px-4 py-2", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground uppercase tracking-wider mb-1", children: t("pptx.presenter.speakerNotes") }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-y-auto rounded border border-border/30 bg-muted/40 px-3 py-2 text-foreground whitespace-pre-wrap leading-relaxed text-[15px]", children: hasRichNotes ? renderNotesSegments(notesSegments) : notesText.trim().length > 0 ? notesText : (0, import_jsx_runtime.jsx)("span", { className: "italic text-muted-foreground", children: t("pptx.presenter.noNotes") }) })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-3 px-4 py-2 border-t border-border/60", children: [
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          "data-pptx-presenter-control": "prev",
          onClick: () => onMovePresentationSlide(-1),
          disabled: prevDisabled,
          className: "inline-flex items-center justify-center gap-1.5 flex-1 h-11 rounded bg-muted hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed text-sm transition-colors",
          title: t("pptx.presenter.previousSlide"),
          "aria-label": t("pptx.presenter.previousSlide"),
          children: [
            (0, import_jsx_runtime.jsx)(LuChevronLeft, { className: "w-5 h-5" }),
            t("pptx.presenter.prev")
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          "data-pptx-presenter-control": "next",
          onClick: () => onMovePresentationSlide(1),
          disabled: nextDisabled,
          className: "inline-flex items-center justify-center gap-1.5 flex-1 h-11 rounded bg-muted hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed text-sm transition-colors",
          title: t("pptx.presenter.nextSlide"),
          "aria-label": t("pptx.presenter.nextSlide"),
          children: [
            t("pptx.presenter.next"),
            (0, import_jsx_runtime.jsx)(LuChevronRight, { className: "w-5 h-5" })
          ]
        }
      )
    ] })
  ] });
}
function formatMs(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
}
function RehearseTimingsHud({
  presentationStartTime,
  slideStartTime,
  paused,
  onTogglePause
}) {
  const { t } = useTranslation();
  const [tick, setTick] = (0, import_react2.useState)(0);
  (0, import_react2.useEffect)(() => {
    if (paused) {
      return;
    }
    const id = window.setInterval(() => setTick((p) => p + 1), 250);
    return () => window.clearInterval(id);
  }, [paused]);
  const now = Date.now();
  const slideElapsed = slideStartTime !== null ? now - slideStartTime : 0;
  const totalElapsed = presentationStartTime !== null ? now - presentationStartTime : 0;
  return (0, import_jsx_runtime.jsxs)("div", { className: "fixed bottom-4 left-4 z-[9999] flex items-center gap-3 rounded-lg bg-black/80 px-4 py-2 text-white shadow-xl backdrop-blur-sm select-none", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col text-xs leading-tight", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.rehearse.slideTime") }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-lg font-mono tabular-nums", children: formatMs(slideElapsed) })
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "w-px h-8 bg-border" }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col text-xs leading-tight", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.rehearse.totalTime") }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-lg font-mono tabular-nums", children: formatMs(totalElapsed) })
    ] }),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onTogglePause,
        className: "ml-1 rounded p-1.5 hover:bg-white/20 transition-colors",
        title: paused ? t("pptx.rehearse.resume") : t("pptx.rehearse.pause"),
        "aria-label": paused ? t("pptx.rehearse.resume") : t("pptx.rehearse.pause"),
        children: paused ? (0, import_jsx_runtime.jsx)(LuPlay, { className: "w-4 h-4" }) : (0, import_jsx_runtime.jsx)(LuPause, { className: "w-4 h-4" })
      }
    )
  ] });
}
function formatMs2(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
}
function RehearseTimingsSummary({
  slides,
  canvasSize,
  recordedTimings,
  onSave,
  onDiscard
}) {
  const { t } = useTranslation();
  const totalMs = Object.values(recordedTimings).reduce((sum, ms) => sum + ms, 0);
  const sortedEntries = Object.entries(recordedTimings).map(([idx, ms]) => ({ slideIndex: Number(idx), ms })).sort((a, b) => a.slideIndex - b.slideIndex);
  return (0, import_jsx_runtime.jsx)("div", { className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 backdrop-blur-sm", children: (0, import_jsx_runtime.jsxs)("div", { className: "w-full max-w-lg rounded-xl border border-border bg-background shadow-2xl", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "border-b border-border px-5 py-4", children: [
      (0, import_jsx_runtime.jsx)("h2", { className: "text-base font-semibold text-foreground", children: t("pptx.rehearse.summaryTitle") }),
      (0, import_jsx_runtime.jsxs)("p", { className: "mt-1 text-sm text-muted-foreground", children: [
        t("pptx.rehearse.totalPresentationTime"),
        ":",
        " ",
        (0, import_jsx_runtime.jsx)("span", { className: "font-mono text-foreground", children: formatMs2(totalMs) })
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "max-h-72 overflow-y-auto px-5 py-3", children: (0, import_jsx_runtime.jsxs)("table", { className: "w-full text-xs", children: [
      (0, import_jsx_runtime.jsx)("thead", { children: (0, import_jsx_runtime.jsxs)("tr", { className: "text-muted-foreground uppercase tracking-wide", children: [
        (0, import_jsx_runtime.jsx)("th", { className: "pb-2 text-left", children: "#" }),
        (0, import_jsx_runtime.jsx)("th", { className: "pb-2 text-left", children: t("pptx.rehearse.slide") }),
        (0, import_jsx_runtime.jsx)("th", { className: "pb-2 text-right", children: t("pptx.rehearse.time") })
      ] }) }),
      (0, import_jsx_runtime.jsx)("tbody", { children: sortedEntries.map(({ slideIndex, ms }) => {
        const slide = slides[slideIndex];
        return (0, import_jsx_runtime.jsxs)("tr", { className: "border-t border-border", children: [
          (0, import_jsx_runtime.jsx)("td", { className: "py-2 text-muted-foreground tabular-nums", children: slideIndex + 1 }),
          (0, import_jsx_runtime.jsx)("td", { className: "py-2", children: slide && (0, import_jsx_runtime.jsx)("div", { className: "w-20 rounded overflow-hidden bg-white", children: (0, import_jsx_runtime.jsx)(
            SlideThumbnail,
            {
              slide,
              templateElements: [],
              canvasSize
            }
          ) }) }),
          (0, import_jsx_runtime.jsx)("td", { className: "py-2 text-right font-mono text-foreground tabular-nums", children: formatMs2(ms) })
        ] }, slideIndex);
      }) })
    ] }) }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 border-t border-border px-5 py-3", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onDiscard,
          className: "rounded-lg px-4 py-2 text-sm text-foreground hover:bg-muted transition-colors",
          children: t("pptx.rehearse.discard")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onSave,
          className: "rounded-lg bg-primary px-4 py-2 text-sm text-white hover:bg-primary/80 transition-colors",
          children: t("pptx.rehearse.saveTimings")
        }
      )
    ] })
  ] }) });
}
var TYPE_LABELS = {
  text: "Text Box",
  shape: "Shape",
  connector: "Connector",
  image: "Image",
  picture: "Picture",
  chart: "Chart",
  table: "Table",
  smartArt: "SmartArt",
  media: "Media",
  group: "Group",
  ink: "Ink",
  ole: "Object",
  unknown: "Object"
};
function getElementDisplayName(element, index) {
  if (element.name && element.name.trim().length > 0) {
    return element.name.trim();
  }
  if (hasTextProperties(element) && element.text && element.text.trim().length > 0) {
    return element.text.trim().slice(0, 32);
  }
  const label = TYPE_LABELS[element.type] ?? "Object";
  return `${label} ${index + 1}`;
}
function SelectionPane({
  slides,
  activeSlideIndex,
  selectedElementId,
  selectedElementIds,
  canEdit,
  setSelectedElementId,
  setSelectedElementIds,
  setSlides,
  markDirty,
  onClose
}) {
  const { t } = useTranslation();
  const activeSlide = slides[activeSlideIndex];
  const elements = (activeSlide == null ? void 0 : activeSlide.elements) ?? [];
  const [editingId, setEditingId] = (0, import_react2.useState)(null);
  const [editingName, setEditingName] = (0, import_react2.useState)("");
  const inputRef = (0, import_react2.useRef)(null);
  const editSeedRef = (0, import_react2.useRef)("");
  const [dragIndex, setDragIndex] = (0, import_react2.useState)(null);
  const [dragOverIndex, setDragOverIndex] = (0, import_react2.useState)(null);
  const handleSelect = (0, import_react2.useCallback)(
    (elementId) => {
      setSelectedElementId(elementId);
      setSelectedElementIds([elementId]);
    },
    [setSelectedElementId, setSelectedElementIds]
  );
  const handleToggleVisibility = (0, import_react2.useCallback)(
    (elementId) => {
      setSlides((prevSlides) => {
        const nextSlides = [...prevSlides];
        const slide = nextSlides[activeSlideIndex];
        if (!slide) {
          return prevSlides;
        }
        const nextElements = [...slide.elements ?? []];
        const idx = nextElements.findIndex((e) => e.id === elementId);
        if (idx === -1) {
          return prevSlides;
        }
        nextElements[idx] = {
          ...nextElements[idx],
          hidden: !nextElements[idx].hidden
        };
        nextSlides[activeSlideIndex] = { ...slide, elements: nextElements };
        return nextSlides;
      });
      markDirty();
    },
    [activeSlideIndex, setSlides, markDirty]
  );
  const handleDoubleClick = (0, import_react2.useCallback)(
    (element, displayName) => {
      if (!canEdit) {
        return;
      }
      setEditingId(element.id);
      setEditingName(displayName);
      editSeedRef.current = displayName;
      setTimeout(() => {
        var _a;
        return (_a = inputRef.current) == null ? void 0 : _a.select();
      }, 0);
    },
    [canEdit]
  );
  const commitRename = (0, import_react2.useCallback)(() => {
    const elementId = editingId;
    setEditingId(null);
    if (elementId === null) {
      return;
    }
    const commit = resolveSelectionPaneRename(editSeedRef.current, editingName);
    if (!commit) {
      return;
    }
    setSlides((prevSlides) => {
      const nextSlides = [...prevSlides];
      const slide = nextSlides[activeSlideIndex];
      if (!slide) {
        return prevSlides;
      }
      const nextElements = [...slide.elements ?? []];
      const idx = nextElements.findIndex((e) => e.id === elementId);
      if (idx === -1) {
        return prevSlides;
      }
      nextElements[idx] = {
        ...nextElements[idx],
        name: commit.name
      };
      nextSlides[activeSlideIndex] = { ...slide, elements: nextElements };
      return nextSlides;
    });
    markDirty();
  }, [editingId, editingName, activeSlideIndex, setSlides, markDirty]);
  const handleDragStart = (0, import_react2.useCallback)((index) => {
    setDragIndex(index);
  }, []);
  const handleDragOver = (0, import_react2.useCallback)(
    (e, index) => {
      e.preventDefault();
      if (dragIndex !== null && dragIndex !== index) {
        setDragOverIndex(index);
      }
    },
    [dragIndex]
  );
  const handleDrop = (0, import_react2.useCallback)(
    (targetIndex) => {
      if (dragIndex === null || dragIndex === targetIndex) {
        setDragIndex(null);
        setDragOverIndex(null);
        return;
      }
      setSlides((prevSlides) => {
        const nextSlides = [...prevSlides];
        const slide = nextSlides[activeSlideIndex];
        if (!slide) {
          return prevSlides;
        }
        const nextElements = [...slide.elements ?? []];
        const [moved] = nextElements.splice(dragIndex, 1);
        nextElements.splice(targetIndex, 0, moved);
        nextSlides[activeSlideIndex] = { ...slide, elements: nextElements };
        return nextSlides;
      });
      markDirty();
      setDragIndex(null);
      setDragOverIndex(null);
    },
    [dragIndex, activeSlideIndex, setSlides, markDirty]
  );
  const reversed = [...elements].reverse();
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-pptx-selection-pane": true,
      className: "flex flex-col h-full bg-popover border-l border-border w-56",
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-3 py-2 border-b border-border", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-xs font-medium text-foreground", children: t("pptx.selectionPane.title") }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "text-muted-foreground hover:text-foreground text-xs",
              title: t("pptx.selectionPane.close"),
              children: "×"
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-y-auto py-1", children: reversed.length === 0 ? (0, import_jsx_runtime.jsx)("div", { className: "px-3 py-4 text-xs text-muted-foreground italic", children: t("pptx.selectionPane.empty") }) : reversed.map((element, reversedIdx) => {
          const realIndex = elements.length - 1 - reversedIdx;
          const isSelected = selectedElementId === element.id || selectedElementIds.includes(element.id);
          const displayName = getElementDisplayName(element, realIndex);
          const isEditing = editingId === element.id;
          return (0, import_jsx_runtime.jsxs)(
            "div",
            {
              draggable: canEdit,
              onDragStart: () => handleDragStart(realIndex),
              onDragOver: (e) => handleDragOver(e, realIndex),
              onDrop: () => handleDrop(realIndex),
              onDragEnd: () => {
                setDragIndex(null);
                setDragOverIndex(null);
              },
              className: cn(
                "flex items-center gap-1.5 px-2 py-1 text-xs cursor-pointer transition-colors",
                isSelected ? "bg-primary/30 text-primary" : "hover:bg-muted text-foreground",
                dragOverIndex === realIndex ? "border-t-2 border-primary" : ""
              ),
              onClick: () => handleSelect(element.id),
              onDoubleClick: () => handleDoubleClick(element, displayName),
              children: [
                canEdit && (0, import_jsx_runtime.jsx)(LuGripVertical, { className: "w-3 h-3 text-muted-foreground flex-shrink-0 cursor-grab" }),
                (0, import_jsx_runtime.jsx)("span", { "data-pptx-selection-name": true, className: "flex-1 truncate", children: isEditing ? (0, import_jsx_runtime.jsx)(
                  "input",
                  {
                    ref: inputRef,
                    type: "text",
                    "aria-label": t("pptx.selectionPane.renameElement"),
                    value: editingName,
                    onChange: (e) => setEditingName(e.target.value),
                    onBlur: commitRename,
                    onKeyDown: (e) => {
                      if (e.key === "Enter") {
                        commitRename();
                      }
                      if (e.key === "Escape") {
                        setEditingId(null);
                      }
                    },
                    className: "w-full bg-muted text-xs px-1 py-0.5 rounded border border-border outline-none",
                    onClick: (e) => e.stopPropagation()
                  }
                ) : displayName }),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: "text-muted-foreground hover:text-foreground flex-shrink-0",
                    title: element.hidden ? t("pptx.selectionPane.show") : t("pptx.selectionPane.hide"),
                    onClick: (e) => {
                      e.stopPropagation();
                      handleToggleVisibility(element.id);
                    },
                    children: element.hidden ? (0, import_jsx_runtime.jsx)(LuEyeOff, { className: "w-3.5 h-3.5" }) : (0, import_jsx_runtime.jsx)(LuEye, { className: "w-3.5 h-3.5 opacity-50" })
                  }
                )
              ]
            },
            element.id
          );
        }) })
      ]
    }
  );
}
var INPUT_CLS = "bg-muted border border-border rounded px-2 py-1 w-full text-[11px]";
var SELECT_CLS = "bg-muted border border-border rounded px-2 py-1 w-full text-[11px]";
var ENTRANCE_PRESETS = ANIMATION_PRESET_OPTIONS.filter(
  (o) => o.value === "fadeIn" || o.value === "flyIn" || o.value === "zoomIn"
);
var EXIT_PRESETS = ANIMATION_PRESET_OPTIONS.filter(
  (o) => o.value === "fadeOut" || o.value === "flyOut" || o.value === "zoomOut"
);
var EMPHASIS_PRESETS = ANIMATION_PRESET_OPTIONS.filter(
  (o) => o.value === "spin" || o.value === "pulse" || o.value === "colorWave" || o.value === "bounce" || o.value === "flash" || o.value === "growShrink" || o.value === "teeter"
);
var TRIGGER_OPTIONS = [
  { value: "onClick", labelKey: "pptx.animation.trigger.onClick" },
  { value: "onShapeClick", labelKey: "pptx.animation.trigger.onShapeClick" },
  { value: "onHover", labelKey: "pptx.animation.trigger.onHover" },
  { value: "afterPrevious", labelKey: "pptx.animation.trigger.afterPrevious" },
  { value: "withPrevious", labelKey: "pptx.animation.trigger.withPrevious" }
];
var TIMING_CURVE_OPTIONS = [
  { value: "ease", labelKey: "pptx.animation.timingCurve.ease" },
  { value: "ease-in", labelKey: "pptx.animation.timingCurve.easeIn" },
  { value: "ease-out", labelKey: "pptx.animation.timingCurve.easeOut" },
  { value: "linear", labelKey: "pptx.animation.timingCurve.linear" }
];
var REPEAT_MODE_OPTIONS = [
  { value: "none", labelKey: "pptx.animation.repeatUntil.none" },
  {
    value: "untilNextClick",
    labelKey: "pptx.animation.repeatUntil.untilNextClick"
  },
  {
    value: "untilEndOfSlide",
    labelKey: "pptx.animation.repeatUntil.untilEndOfSlide"
  }
];
var DIRECTION_OPTIONS = [
  {
    value: "fromTop",
    labelKey: "pptx.animation.direction.fromTop",
    icon: LuArrowDown
  },
  {
    value: "fromBottom",
    labelKey: "pptx.animation.direction.fromBottom",
    icon: LuArrowUp
  },
  {
    value: "fromLeft",
    labelKey: "pptx.animation.direction.fromLeft",
    icon: LuArrowRight
  },
  {
    value: "fromRight",
    labelKey: "pptx.animation.direction.fromRight",
    icon: LuArrowLeft
  }
];
var SEQUENCE_OPTIONS = [
  { value: "asOne", labelKey: "pptx.animation.sequence.asOne" },
  { value: "byParagraph", labelKey: "pptx.animation.sequence.byParagraph" },
  { value: "byWord", labelKey: "pptx.animation.sequence.byWord" },
  { value: "byLetter", labelKey: "pptx.animation.sequence.byLetter" }
];
var AFTER_ANIMATION_OPTIONS = [
  { value: "none", labelKey: "pptx.animation.afterAnimation.none" },
  { value: "dimToColor", labelKey: "pptx.animation.afterAnimation.dimToColor" },
  { value: "hideAfterAnimation", labelKey: "pptx.animation.afterAnimation.hideAfterAnimation" },
  { value: "hideOnNextClick", labelKey: "pptx.animation.afterAnimation.hideOnNextClick" }
];
function AfterAnimationRow({
  action,
  color,
  canEdit,
  onActionChange,
  onColorChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.afterAnimation") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.animation.afterAnimation"),
          value: action,
          onChange: (event) => onActionChange(event.target.value),
          disabled: !canEdit,
          className: SELECT_CLS,
          children: AFTER_ANIMATION_OPTIONS.map((option) => (0, import_jsx_runtime.jsx)("option", { value: option.value, children: t(option.labelKey) }, option.value))
        }
      )
    ] }),
    action === "dimToColor" && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.afterAnimation.color") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          "aria-label": t("pptx.animation.afterAnimation.color"),
          value: color ?? "#808080",
          onChange: (event) => {
            onColorChange(event.target.value);
            pushColor(event.target.value);
          },
          disabled: !canEdit,
          className: "h-6 w-10 rounded border border-border bg-transparent p-0"
        }
      )
    ] })
  ] });
}
function animationTypeColor(anim) {
  if (anim.entrance) {
    return "bg-green-500/60";
  }
  if (anim.emphasis) {
    return "bg-yellow-500/60";
  }
  if (anim.exit) {
    return "bg-red-500/60";
  }
  return "bg-muted-foreground/40";
}
function AnimationTimelineSection({
  selectedElementId,
  canEdit,
  handlers
}) {
  const { t } = useTranslation();
  const effectLabel = (anim) => animationEffectLabel(anim, (key) => t(key));
  const {
    sortedAnimations,
    timelineRows,
    dragIndex,
    dragOverIndex,
    timelineBarData,
    handleAnimationHover,
    handleAnimationHoverEnd,
    handleDragStart,
    handleDragOver,
    handleDragEnter,
    handleDragLeave,
    handleDrop,
    handleDragEnd,
    handleMoveUp,
    handleMoveDown,
    getTimelineLabel,
    getNativeRowLabel
  } = handlers;
  if (timelineRows.length === 0 && timelineBarData.length === 0) {
    return null;
  }
  const animationByElementId = new Map(sortedAnimations.map((anim) => [anim.elementId, anim]));
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    timelineBarData.length > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 pt-2 border-t border-border", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] uppercase tracking-wide text-muted-foreground mb-1", children: t("pptx.animation.timelineBar") }),
      (0, import_jsx_runtime.jsx)("div", { className: "relative h-6 rounded bg-muted/50 border border-border overflow-hidden", children: timelineBarData.map((bar) => (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: cn(
            "absolute top-0.5 bottom-0.5 rounded-sm transition-colors",
            animationTypeColor(bar.anim),
            bar.anim.elementId === selectedElementId && "ring-1 ring-primary"
          ),
          style: {
            left: `${bar.leftPercent}%`,
            width: `${Math.max(bar.widthPercent, 2)}%`
          },
          title: `${getTimelineLabel(bar.anim)} - ${effectLabel(bar.anim)} (${bar.anim.durationMs ?? 500}ms)`,
          onMouseEnter: () => handleAnimationHover(bar.anim),
          onMouseLeave: handleAnimationHoverEnd
        },
        bar.anim.elementId
      )) })
    ] }),
    timelineRows.length > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 pt-2 border-t border-border", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] uppercase tracking-wide text-muted-foreground mb-1", children: t("pptx.animation.timeline") }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-0.5 max-h-40 overflow-y-auto", children: timelineRows.map((row, index) => {
        const isDragOver = dragOverIndex === index;
        if (row.kind === "native") {
          return (0, import_jsx_runtime.jsxs)(
            "div",
            {
              onDragOver: (e) => handleDragOver(index, e),
              onDragEnter: () => handleDragEnter(index),
              onDragLeave: handleDragLeave,
              onDrop: (e) => handleDrop(index, e),
              title: t("pptx.animation.nativeEffectHint"),
              className: cn(
                "flex items-center gap-1 px-1 py-0.5 rounded text-[10px] italic",
                "bg-muted/20 text-muted-foreground/70",
                isDragOver && "border-t-2 border-primary"
              ),
              children: [
                (0, import_jsx_runtime.jsx)("span", { className: "w-3 h-3 shrink-0" }),
                (0, import_jsx_runtime.jsxs)("span", { className: "text-muted-foreground/70 w-4 shrink-0", children: [
                  index + 1,
                  "."
                ] }),
                (0, import_jsx_runtime.jsxs)("span", { className: "truncate flex-1", children: [
                  t("pptx.animation.nativeEffect"),
                  ": ",
                  getNativeRowLabel(row.targetIds)
                ] })
              ]
            },
            row.key
          );
        }
        const anim = animationByElementId.get(row.elementId);
        if (!anim) {
          return null;
        }
        const isSelected = selectedElementId === anim.elementId;
        const isDragging = dragIndex === index;
        return (0, import_jsx_runtime.jsxs)(
          "div",
          {
            draggable: canEdit,
            onDragStart: (e) => handleDragStart(index, e),
            onDragOver: (e) => handleDragOver(index, e),
            onDragEnter: () => handleDragEnter(index),
            onDragLeave: handleDragLeave,
            onDrop: (e) => handleDrop(index, e),
            onDragEnd: handleDragEnd,
            onMouseEnter: () => handleAnimationHover(anim),
            onMouseLeave: handleAnimationHoverEnd,
            className: cn(
              "flex items-center gap-1 px-1 py-0.5 rounded text-[10px] cursor-grab transition-colors",
              isSelected ? "bg-primary/30 text-primary" : "bg-muted/50 text-muted-foreground",
              isDragging && "opacity-40",
              isDragOver && "border-t-2 border-primary"
            ),
            children: [
              canEdit && (0, import_jsx_runtime.jsx)(LuGripVertical, { className: "w-3 h-3 text-muted-foreground/50 shrink-0" }),
              (0, import_jsx_runtime.jsxs)("span", { className: "text-muted-foreground w-4 shrink-0", children: [
                index + 1,
                "."
              ] }),
              (0, import_jsx_runtime.jsx)("span", { className: "truncate flex-1", children: getTimelineLabel(anim) }),
              anim.entrance && (0, import_jsx_runtime.jsx)(LuMoveRight, { className: "w-3 h-3 text-green-400/70 shrink-0" }),
              anim.emphasis && (0, import_jsx_runtime.jsx)(LuRotateCw, { className: "w-3 h-3 text-yellow-400/70 shrink-0" }),
              anim.exit && (0, import_jsx_runtime.jsx)(LuMoveRight, { className: "w-3 h-3 text-red-400/70 rotate-180 shrink-0" }),
              canEdit && (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-0.5 shrink-0", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    disabled: index === 0,
                    className: "text-muted-foreground hover:text-foreground disabled:opacity-30 transition-colors",
                    onClick: (e) => {
                      e.stopPropagation();
                      handleMoveUp(index);
                    },
                    title: t("pptx.animation.moveUp"),
                    children: (0, import_jsx_runtime.jsx)(LuChevronUp, { className: "w-3 h-3" })
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    disabled: index === timelineRows.length - 1,
                    className: "text-muted-foreground hover:text-foreground disabled:opacity-30 transition-colors",
                    onClick: (e) => {
                      e.stopPropagation();
                      handleMoveDown(index);
                    },
                    title: t("pptx.animation.moveDown"),
                    children: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3" })
                  }
                )
              ] })
            ]
          },
          row.key
        );
      }) })
    ] })
  ] });
}
function EffectSoundRow({
  soundState,
  canEdit,
  onPick
}) {
  const { t } = useTranslation();
  const fileInputRef = import_react2.default.useRef(null);
  const handleSelectChange = (event) => {
    var _a;
    if (event.target.value === "custom") {
      (_a = fileInputRef.current) == null ? void 0 : _a.click();
      return;
    }
    onPick(void 0);
  };
  const handleFileChange = (event) => {
    var _a;
    const file = (_a = event.target.files) == null ? void 0 : _a[0];
    event.target.value = "";
    if (!file) {
      return;
    }
    const reader = new FileReader();
    reader.onload = () => {
      if (typeof reader.result === "string") {
        onPick({ dataUrl: reader.result, fileName: file.name });
      }
    };
    reader.readAsDataURL(file);
  };
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.sound") }),
    (0, import_jsx_runtime.jsxs)(
      "select",
      {
        "aria-label": t("pptx.animation.sound"),
        value: soundState.hasSound ? "custom" : "none",
        onChange: handleSelectChange,
        disabled: !canEdit,
        className: SELECT_CLS,
        children: [
          (0, import_jsx_runtime.jsx)("option", { value: "none", children: t("pptx.animation.sound.none") }),
          (0, import_jsx_runtime.jsx)("option", { value: "custom", children: soundState.hasSound && soundState.fileName ? soundState.fileName : t("pptx.animation.sound.custom") })
        ]
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        ref: fileInputRef,
        type: "file",
        accept: "audio/*",
        "aria-label": t("pptx.animation.sound.chooseFile"),
        className: "hidden",
        onChange: handleFileChange,
        tabIndex: -1
      }
    )
  ] });
}
function MotionPathRow({
  motionPath,
  canEdit,
  onChange
}) {
  const { t } = useTranslation();
  const presetId = motionPathPresetIdForPath(motionPath);
  const isCustom = Boolean(motionPath) && !presetId;
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.motionPath.label") }),
    (0, import_jsx_runtime.jsxs)(
      "select",
      {
        "aria-label": t("pptx.animation.motionPath.label"),
        value: isCustom ? "custom" : presetId ?? "none",
        onChange: (event) => onChange(event.target.value),
        disabled: !canEdit,
        className: SELECT_CLS,
        children: [
          (0, import_jsx_runtime.jsx)("option", { value: "none", children: t("pptx.animation.motionPath.none") }),
          isCustom && (0, import_jsx_runtime.jsx)("option", { value: "custom", children: t("pptx.animation.motionPath.custom") }),
          MOTION_PATH_FAMILIES.map((family) => (0, import_jsx_runtime.jsx)("optgroup", { label: t(motionPathFamilyLabelKey(family)), children: motionPathPresetsByFamily(family).map((preset) => (0, import_jsx_runtime.jsx)("option", { value: preset.id, children: t(motionPathPresetLabelKey(preset.id)) }, preset.id)) }, family))
        ]
      }
    ),
    motionPath && (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.animation.motionPath.editHint") })
  ] });
}
function useAnimationDragDrop({
  canEdit,
  rows,
  updateAnimations
}) {
  const [dragIndex, setDragIndex] = (0, import_react2.useState)(null);
  const [dragOverIndex, setDragOverIndex] = (0, import_react2.useState)(null);
  const dragCounterRef = (0, import_react2.useRef)(0);
  const handleDragStart = (0, import_react2.useCallback)(
    (index, event) => {
      var _a;
      if (!canEdit || ((_a = rows[index]) == null ? void 0 : _a.kind) !== "editor") {
        return;
      }
      setDragIndex(index);
      event.dataTransfer.effectAllowed = "move";
      event.dataTransfer.setData("text/plain", String(index));
    },
    [canEdit, rows]
  );
  const handleDragOver = (0, import_react2.useCallback)((_index, event) => {
    event.preventDefault();
    event.dataTransfer.dropEffect = "move";
    setDragOverIndex(_index);
  }, []);
  const handleDragEnter = (0, import_react2.useCallback)((index) => {
    dragCounterRef.current++;
    setDragOverIndex(index);
  }, []);
  const handleDragLeave = (0, import_react2.useCallback)(() => {
    dragCounterRef.current--;
    if (dragCounterRef.current <= 0) {
      setDragOverIndex(null);
      dragCounterRef.current = 0;
    }
  }, []);
  const reorderRows = (0, import_react2.useCallback)(
    (sourceIndex, targetIndex) => {
      const sourceRow = rows[sourceIndex];
      if (!sourceRow || sourceRow.kind !== "editor") {
        return;
      }
      const nextRows = reorderAnimationTimelineRows(rows, sourceRow.key, targetIndex);
      updateAnimations((anims) => applyAnimationTimelineOrder(anims, nextRows));
    },
    [rows, updateAnimations]
  );
  const handleDrop = (0, import_react2.useCallback)(
    (targetIndex, event) => {
      event.preventDefault();
      dragCounterRef.current = 0;
      const sourceIndex = dragIndex;
      setDragIndex(null);
      setDragOverIndex(null);
      if (sourceIndex === null || sourceIndex === targetIndex) {
        return;
      }
      reorderRows(sourceIndex, targetIndex);
    },
    [dragIndex, reorderRows]
  );
  const handleDragEnd = (0, import_react2.useCallback)(() => {
    setDragIndex(null);
    setDragOverIndex(null);
    dragCounterRef.current = 0;
  }, []);
  const handleMoveUp = (0, import_react2.useCallback)(
    (rowIndex) => {
      if (rowIndex <= 0) {
        return;
      }
      reorderRows(rowIndex, rowIndex - 1);
    },
    [reorderRows]
  );
  const handleMoveDown = (0, import_react2.useCallback)(
    (rowIndex) => {
      reorderRows(rowIndex, rowIndex + 1);
    },
    [reorderRows]
  );
  return {
    dragIndex,
    dragOverIndex,
    handleDragStart,
    handleDragOver,
    handleDragEnter,
    handleDragLeave,
    handleDrop,
    handleDragEnd,
    handleMoveUp,
    handleMoveDown
  };
}
function useAnimationPreview({
  selectedElementId,
  selectedElementAnimation
}) {
  const handleAnimationHover = (0, import_react2.useCallback)((anim) => {
    if (anim.motionPath) {
      startMotionPathPreview(anim.elementId, anim.motionPath, {
        durationMs: anim.durationMs,
        delayMs: anim.delayMs,
        timingCurve: anim.timingCurve
      });
      return;
    }
    const preset = anim.entrance ?? anim.emphasis ?? anim.exit;
    if (!preset || preset === "none") {
      return;
    }
    startPreviewAnimation(anim.elementId, preset, {
      direction: anim.direction,
      durationMs: anim.durationMs ?? 500,
      timingCurve: anim.timingCurve
    });
  }, []);
  const handleAnimationHoverEnd = (0, import_react2.useCallback)(() => stopPreviewAnimation(), []);
  const handlePreviewClick = (0, import_react2.useCallback)(() => {
    if (!selectedElementAnimation) {
      return;
    }
    if (selectedElementAnimation.motionPath) {
      startMotionPathPreview(selectedElementId, selectedElementAnimation.motionPath, {
        durationMs: selectedElementAnimation.durationMs,
        delayMs: selectedElementAnimation.delayMs,
        timingCurve: selectedElementAnimation.timingCurve
      });
      return;
    }
    const preset = selectedElementAnimation.entrance ?? selectedElementAnimation.emphasis ?? selectedElementAnimation.exit;
    if (!preset || preset === "none") {
      return;
    }
    startPreviewAnimation(selectedElementId, preset, {
      direction: selectedElementAnimation.direction,
      durationMs: selectedElementAnimation.durationMs ?? 500,
      timingCurve: selectedElementAnimation.timingCurve
    });
  }, [selectedElementId, selectedElementAnimation]);
  return {
    handleAnimationHover,
    handleAnimationHoverEnd,
    handlePreviewClick
  };
}
function useAnimationHandlers({
  selectedElement,
  activeSlide,
  canEdit,
  onUpdateSlide
}) {
  const selectedElementAnimation = (0, import_react2.useMemo)(
    () => (activeSlide.animations ?? []).find((a) => a.elementId === selectedElement.id),
    [activeSlide, selectedElement]
  );
  const sortedAnimations = (0, import_react2.useMemo)(
    () => [...activeSlide.animations ?? []].sort((a, b) => (a.order ?? 0) - (b.order ?? 0)),
    [activeSlide.animations]
  );
  const timelineRows = (0, import_react2.useMemo)(
    () => buildAnimationTimelineRows(
      activeSlide.animations ?? [],
      activeSlide.animationTimelineAnchors ?? []
    ),
    [activeSlide.animations, activeSlide.animationTimelineAnchors]
  );
  const updateAnimations = (0, import_react2.useCallback)(
    (updater) => {
      if (!canEdit) {
        return;
      }
      const current = activeSlide.animations ?? [];
      onUpdateSlide({ animations: updater([...current]) });
    },
    [canEdit, activeSlide.animations, onUpdateSlide]
  );
  const updateAnimationField = (0, import_react2.useCallback)(
    (field, value) => {
      updateAnimations(
        (anims) => anims.map((a) => a.elementId === selectedElement.id ? { ...a, [field]: value } : a)
      );
    },
    [updateAnimations, selectedElement.id]
  );
  const setAnimationPreset = (0, import_react2.useCallback)(
    (entrance, exit, emphasis) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => {
        const idx = anims.findIndex((a) => a.elementId === selectedElement.id);
        const hasEffect = entrance || exit || emphasis;
        if (idx >= 0) {
          if (!hasEffect && !anims[idx].motionPath) {
            return anims.filter((a) => a.elementId !== selectedElement.id);
          }
          anims[idx] = {
            ...anims[idx],
            entrance,
            exit,
            emphasis
          };
        } else if (hasEffect) {
          anims.push({
            elementId: selectedElement.id,
            entrance,
            exit,
            emphasis,
            durationMs: 500,
            order: anims.length,
            trigger: "onClick"
          });
        }
        return anims;
      });
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleEntranceChange = (0, import_react2.useCallback)(
    (event) => {
      const v = event.target.value;
      setAnimationPreset(
        v === "none" ? void 0 : v,
        selectedElementAnimation == null ? void 0 : selectedElementAnimation.exit,
        selectedElementAnimation == null ? void 0 : selectedElementAnimation.emphasis
      );
    },
    [setAnimationPreset, selectedElementAnimation]
  );
  const handleExitChange = (0, import_react2.useCallback)(
    (event) => {
      const v = event.target.value;
      setAnimationPreset(
        selectedElementAnimation == null ? void 0 : selectedElementAnimation.entrance,
        v === "none" ? void 0 : v,
        selectedElementAnimation == null ? void 0 : selectedElementAnimation.emphasis
      );
    },
    [setAnimationPreset, selectedElementAnimation]
  );
  const handleEmphasisChange = (0, import_react2.useCallback)(
    (event) => {
      const v = event.target.value;
      setAnimationPreset(
        selectedElementAnimation == null ? void 0 : selectedElementAnimation.entrance,
        selectedElementAnimation == null ? void 0 : selectedElementAnimation.exit,
        v === "none" ? void 0 : v
      );
    },
    [setAnimationPreset, selectedElementAnimation]
  );
  const handleMotionPathChange = (0, import_react2.useCallback)(
    (presetId) => {
      if (!canEdit) {
        return;
      }
      if (presetId === "custom") {
        return;
      }
      updateAnimations(
        (anims) => presetId === "none" ? clearMotionPath(anims, selectedElement.id) : applyMotionPathPreset(anims, selectedElement.id, presetId)
      );
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleTriggerChange = (0, import_react2.useCallback)(
    (event) => {
      const newTrigger = event.target.value;
      updateAnimationField("trigger", newTrigger);
      if (newTrigger !== "onShapeClick") {
        updateAnimationField("triggerShapeId", void 0);
      }
    },
    [updateAnimationField]
  );
  const handleTriggerShapeChange = (0, import_react2.useCallback)(
    (e) => updateAnimationField("triggerShapeId", e.target.value || void 0),
    [updateAnimationField]
  );
  const handleTimingCurveChange = (0, import_react2.useCallback)(
    (e) => updateAnimationField("timingCurve", e.target.value),
    [updateAnimationField]
  );
  const handleDurationChange = (0, import_react2.useCallback)(
    (e) => updateAnimationField(
      "durationMs",
      Math.max(100, Math.min(1e4, Number(e.target.value) || 450))
    ),
    [updateAnimationField]
  );
  const handleDelayChange = (0, import_react2.useCallback)(
    (e) => updateAnimationField("delayMs", Math.max(0, Math.min(1e4, Number(e.target.value) || 0))),
    [updateAnimationField]
  );
  const handleRepeatCountChange = (0, import_react2.useCallback)(
    (e) => updateAnimationField("repeatCount", Math.max(1, Math.min(100, Number(e.target.value) || 1))),
    [updateAnimationField]
  );
  const handleRepeatModeChange = (0, import_react2.useCallback)(
    (e) => {
      const v = e.target.value;
      updateAnimationField("repeatMode", v === "none" ? void 0 : v);
    },
    [updateAnimationField]
  );
  const handleDirectionChange = (0, import_react2.useCallback)(
    (dir) => updateAnimationField("direction", dir),
    [updateAnimationField]
  );
  const handleSequenceChange = (0, import_react2.useCallback)(
    (e) => updateAnimationField("sequence", e.target.value),
    [updateAnimationField]
  );
  const effectSoundState = (0, import_react2.useMemo)(
    () => getEffectSoundState(activeSlide.animations ?? [], selectedElement.id),
    [activeSlide.animations, selectedElement.id]
  );
  const handleEffectSoundPick = (0, import_react2.useCallback)(
    (pick) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => setEffectSound(anims, selectedElement.id, pick));
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleAfterAnimationChange = (0, import_react2.useCallback)(
    (action) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => setAfterAnimation(anims, selectedElement.id, action));
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const handleAfterAnimationColorChange = (0, import_react2.useCallback)(
    (color) => {
      if (!canEdit) {
        return;
      }
      updateAnimations((anims) => setAfterAnimationColor(anims, selectedElement.id, color));
    },
    [canEdit, updateAnimations, selectedElement.id]
  );
  const preview = useAnimationPreview({
    selectedElementId: selectedElement.id,
    selectedElementAnimation
  });
  const dragDrop = useAnimationDragDrop({ canEdit, rows: timelineRows, updateAnimations });
  const getTimelineLabel = (0, import_react2.useCallback)(
    (anim) => {
      var _a;
      const el = (_a = activeSlide.elements) == null ? void 0 : _a.find((e) => e.id === anim.elementId);
      if (!el) {
        return anim.elementId.slice(0, 8);
      }
      const text = hasTextProperties(el) ? el.text : void 0;
      return text || getElementLabel(el);
    },
    [activeSlide.elements]
  );
  const getNativeRowLabel = (0, import_react2.useCallback)(
    (targetIds) => targetIds.map((id) => {
      var _a;
      const el = (_a = activeSlide.elements) == null ? void 0 : _a.find((e) => e.id === id);
      if (!el) {
        return id.slice(0, 8);
      }
      const text = hasTextProperties(el) ? el.text : void 0;
      return text || getElementLabel(el);
    }).join(", "),
    [activeSlide.elements]
  );
  const hasAnimation = Boolean(
    (selectedElementAnimation == null ? void 0 : selectedElementAnimation.entrance) || (selectedElementAnimation == null ? void 0 : selectedElementAnimation.exit) || (selectedElementAnimation == null ? void 0 : selectedElementAnimation.emphasis) || (selectedElementAnimation == null ? void 0 : selectedElementAnimation.motionPath)
  );
  const showDirectionPicker = hasAnimation && (DIRECTIONAL_PRESETS.has((selectedElementAnimation == null ? void 0 : selectedElementAnimation.entrance) ?? "") || DIRECTIONAL_PRESETS.has((selectedElementAnimation == null ? void 0 : selectedElementAnimation.exit) ?? ""));
  const timelineBarData = (0, import_react2.useMemo)(() => {
    const bars = buildAnimationTimelineBars(sortedAnimations);
    const barsByElementId = new Map(bars.map((bar) => [bar.elementId, bar]));
    return sortedAnimations.flatMap((anim) => {
      const bar = barsByElementId.get(anim.elementId);
      return bar ? [{ anim, leftPercent: bar.leftPercent, widthPercent: bar.widthPercent }] : [];
    });
  }, [sortedAnimations]);
  return {
    selectedElementAnimation,
    sortedAnimations,
    timelineRows,
    hasAnimation,
    showDirectionPicker,
    timelineBarData,
    handleEntranceChange,
    handleExitChange,
    handleEmphasisChange,
    handleTriggerChange,
    handleTriggerShapeChange,
    handleTimingCurveChange,
    handleDurationChange,
    handleDelayChange,
    handleRepeatCountChange,
    handleRepeatModeChange,
    handleDirectionChange,
    handleSequenceChange,
    handleMotionPathChange,
    effectSoundState,
    handleEffectSoundPick,
    handleAfterAnimationChange,
    handleAfterAnimationColorChange,
    getTimelineLabel,
    getNativeRowLabel,
    ...preview,
    ...dragDrop
  };
}
function AnimationPanel({
  selectedElement,
  activeSlide,
  canEdit,
  onUpdateSlide
}) {
  const { t } = useTranslation();
  const handlers = useAnimationHandlers({
    selectedElement,
    activeSlide,
    canEdit,
    onUpdateSlide
  });
  const {
    selectedElementAnimation,
    hasAnimation,
    showDirectionPicker,
    handleEntranceChange,
    handleExitChange,
    handleEmphasisChange,
    handleTriggerChange,
    handleTriggerShapeChange,
    handleTimingCurveChange,
    handleDurationChange,
    handleDelayChange,
    handleRepeatCountChange,
    handleRepeatModeChange,
    handleDirectionChange,
    handleSequenceChange,
    handleMotionPathChange,
    effectSoundState,
    handleEffectSoundPick,
    handleAfterAnimationChange,
    handleAfterAnimationColorChange,
    handlePreviewClick
  } = handlers;
  return (0, import_jsx_runtime.jsxs)("div", { className: "rounded border border-border bg-card p-2 space-y-2", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.animation.title") }),
      hasAnimation && (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          className: "flex items-center gap-1 text-[10px] text-primary hover:text-primary/80 transition-colors",
          onClick: handlePreviewClick,
          title: t("pptx.animation.preview"),
          children: [
            (0, import_jsx_runtime.jsx)(LuPlay, { className: "w-3 h-3" }),
            t("pptx.animation.preview")
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.entrance") }),
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          "aria-label": t("pptx.animation.entrance"),
          value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.entrance) ?? "none",
          onChange: handleEntranceChange,
          disabled: !canEdit,
          className: SELECT_CLS,
          children: [
            (0, import_jsx_runtime.jsx)("option", { value: "none", children: t("pptx.animation.none") }),
            ENTRANCE_PRESETS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(`pptx.animation.preset.${o.value}`) }, o.value))
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.emphasis") }),
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          "aria-label": t("pptx.animation.emphasis"),
          value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.emphasis) ?? "none",
          onChange: handleEmphasisChange,
          disabled: !canEdit,
          className: SELECT_CLS,
          children: [
            (0, import_jsx_runtime.jsx)("option", { value: "none", children: t("pptx.animation.none") }),
            EMPHASIS_PRESETS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(`pptx.animation.preset.${o.value}`) }, o.value))
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.exit") }),
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          "aria-label": t("pptx.animation.exit"),
          value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.exit) ?? "none",
          onChange: handleExitChange,
          disabled: !canEdit,
          className: SELECT_CLS,
          children: [
            (0, import_jsx_runtime.jsx)("option", { value: "none", children: t("pptx.animation.none") }),
            EXIT_PRESETS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(`pptx.animation.preset.${o.value}`) }, o.value))
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      MotionPathRow,
      {
        motionPath: selectedElementAnimation == null ? void 0 : selectedElementAnimation.motionPath,
        canEdit,
        onChange: handleMotionPathChange
      }
    ),
    hasAnimation && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      showDirectionPicker && (0, import_jsx_runtime.jsxs)("div", { className: "pt-1 border-t border-border", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px] block mb-1", children: t("pptx.animation.direction") }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: DIRECTION_OPTIONS.map((opt) => {
          const Icon = opt.icon;
          const isActive = (selectedElementAnimation == null ? void 0 : selectedElementAnimation.direction) === opt.value;
          return (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              disabled: !canEdit,
              className: cn(
                "flex items-center justify-center w-7 h-7 rounded border transition-colors",
                isActive ? "border-primary bg-primary/20 text-primary" : "border-border bg-muted text-muted-foreground hover:bg-accent"
              ),
              onClick: () => handleDirectionChange(opt.value),
              title: t(opt.labelKey),
              children: (0, import_jsx_runtime.jsx)(Icon, { className: "w-3.5 h-3.5" })
            },
            opt.value
          );
        }) })
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.sequence") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.animation.sequence"),
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.sequence) ?? "asOne",
            onChange: handleSequenceChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: SEQUENCE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "pt-1 border-t border-border flex flex-col gap-2", children: [
        (0, import_jsx_runtime.jsx)(
          EffectSoundRow,
          {
            soundState: effectSoundState,
            canEdit,
            onPick: handleEffectSoundPick
          }
        ),
        (0, import_jsx_runtime.jsx)(
          AfterAnimationRow,
          {
            action: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.afterAnimation) ?? "none",
            color: selectedElementAnimation == null ? void 0 : selectedElementAnimation.afterAnimationColor,
            canEdit,
            onActionChange: handleAfterAnimationChange,
            onColorChange: handleAfterAnimationColorChange
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground pt-1 border-t border-border", children: t("pptx.animation.timing") }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.trigger") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.animation.trigger"),
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.trigger) ?? "onClick",
            onChange: handleTriggerChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: TRIGGER_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] }),
      (selectedElementAnimation == null ? void 0 : selectedElementAnimation.trigger) === "onShapeClick" && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.trigger.shapeLabel") }),
        (0, import_jsx_runtime.jsxs)(
          "select",
          {
            "aria-label": t("pptx.animation.trigger.shapeLabel"),
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.triggerShapeId) ?? "",
            onChange: handleTriggerShapeChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: [
              (0, import_jsx_runtime.jsx)("option", { value: "", children: t("pptx.animation.trigger.selectShape") }),
              activeSlide.elements.filter((el) => el.id !== selectedElement.id).map((el) => (0, import_jsx_runtime.jsx)("option", { value: el.id, children: getElementLabel(el) }, el.id))
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.duration") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 100,
            max: 1e4,
            step: 50,
            disabled: !canEdit,
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.durationMs) ?? 450,
            onChange: handleDurationChange,
            className: INPUT_CLS
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.delay") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 1e4,
            step: 50,
            disabled: !canEdit,
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.delayMs) ?? 0,
            onChange: handleDelayChange,
            className: INPUT_CLS
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.timingCurve") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.animation.timingCurve"),
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.timingCurve) ?? "ease",
            onChange: handleTimingCurveChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: TIMING_CURVE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.repeatCount") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 1,
            max: 100,
            step: 1,
            disabled: !canEdit,
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.repeatCount) ?? 1,
            onChange: handleRepeatCountChange,
            className: INPUT_CLS
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.animation.repeatUntil") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.animation.repeatUntil"),
            value: (selectedElementAnimation == null ? void 0 : selectedElementAnimation.repeatMode) ?? "none",
            onChange: handleRepeatModeChange,
            disabled: !canEdit,
            className: SELECT_CLS,
            children: REPEAT_MODE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.labelKey) }, o.value))
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)(
      AnimationTimelineSection,
      {
        selectedElementId: selectedElement.id,
        canEdit,
        handlers
      }
    )
  ] });
}
var INSPECTOR_TABS = [
  { key: "elements", label: "Elements", icon: LuLayers },
  { key: "properties", label: "Properties", icon: LuSettings2 },
  { key: "comments", label: "Comments", icon: LuMessageSquare }
];
var HEADING = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD = "rounded border border-border bg-card p-2 space-y-2";
var INPUT = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var POS_FIELDS = [
  ["X", "x"],
  ["Y", "y"],
  ["W", "width"],
  ["H", "height"]
];
var URL_LABEL_KEY = {
  url: "pptx.action.gotoUrl",
  openFile: "pptx.hyperlink.actionOpenFile",
  openPresentation: "pptx.hyperlink.actionOpenPresentation"
};
function ActionTargetFields({
  type,
  canEdit,
  slideCount,
  customShows,
  url,
  slideIndex,
  customShowId,
  returnAfter,
  onChange
}) {
  const { t } = useTranslation();
  const urlLabelKey = URL_LABEL_KEY[type];
  if (urlLabelKey) {
    return (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "text",
        disabled: !canEdit,
        "aria-label": t(urlLabelKey),
        className: cn(INPUT, "w-full"),
        placeholder: "https://...",
        value: url ?? "",
        onChange: (e) => onChange({ url: e.target.value })
      }
    );
  }
  if (type === "slide") {
    return (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "number",
        disabled: !canEdit,
        "aria-label": t("pptx.action.gotoSlide"),
        className: cn(INPUT, "w-full"),
        placeholder: t("pptx.action.slideNumberPlaceholder"),
        min: 1,
        max: slideCount,
        value: (slideIndex ?? 0) + 1,
        onChange: (e) => {
          const idx = toSlideIndex(Number(e.target.value), slideCount);
          if (idx !== void 0) {
            onChange({ slideIndex: idx });
          }
        }
      }
    );
  }
  if (type === "customShow") {
    return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          "data-testid": "pptx-action-custom-show",
          disabled: !canEdit,
          "aria-label": t("pptx.hyperlink.customShowLabel"),
          className: cn(INPUT, "w-full"),
          value: customShowId ?? "",
          onChange: (e) => onChange({ customShowId: e.target.value, returnAfter }),
          children: [
            (0, import_jsx_runtime.jsx)("option", { value: "", children: t("pptx.hyperlink.customShowLabel") }),
            customShows.map((show) => (0, import_jsx_runtime.jsx)("option", { value: show.id, children: show.name }, show.id))
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1.5 text-muted-foreground", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            "data-testid": "pptx-action-custom-show-return",
            disabled: !canEdit,
            checked: returnAfter ?? false,
            onChange: (e) => onChange({ customShowId, returnAfter: e.target.checked })
          }
        ),
        t("pptx.hyperlink.customShowReturn")
      ] })
    ] });
  }
  return null;
}
function ActionSettingsPanel({
  selectedElement,
  slides,
  canEdit,
  customShows,
  onUpdateElement
}) {
  var _a, _b2, _c, _d;
  const { t } = useTranslation();
  const clickAction = selectedElement.actionClick ? pptxActionToElementAction(selectedElement.actionClick, "click") : void 0;
  const hoverAction = selectedElement.actionHover ? pptxActionToElementAction(selectedElement.actionHover, "hover") : void 0;
  const updateAction = (trigger, type, patch) => {
    const ea = { trigger, type, ...patch };
    const pa = elementActionToPptxAction(ea);
    if (trigger === "click") {
      onUpdateElement({ actionClick: pa });
    } else {
      onUpdateElement({ actionHover: pa });
    }
  };
  const changeType = (trigger, type) => {
    const current = trigger === "click" ? clickAction : hoverAction;
    const target = {
      url: current == null ? void 0 : current.url,
      slideIndex: current == null ? void 0 : current.slideIndex,
      customShowId: current == null ? void 0 : current.customShowId
    };
    if (canCommitActionType(type, target)) {
      updateAction(trigger, type, target);
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, "data-pptx-action-settings": true, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.action.title", "Action") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)(
        ActionTriggerSection,
        {
          label: t("pptx.action.onClick", "On Click"),
          trigger: "click",
          activeType: clickAction == null ? void 0 : clickAction.type,
          action: clickAction,
          fallbackUrl: (_a = selectedElement.actionClick) == null ? void 0 : _a.url,
          fallbackSlideIndex: (_b2 = selectedElement.actionClick) == null ? void 0 : _b2.targetSlideIndex,
          canEdit,
          slideCount: slides.length,
          customShows,
          onChangeType: (type) => changeType("click", type),
          onChangeTarget: (type, patch) => updateAction("click", type, patch)
        },
        `click-${selectedElement.id}`
      ),
      (0, import_jsx_runtime.jsx)(
        ActionTriggerSection,
        {
          label: t("pptx.action.onHover", "On Hover"),
          trigger: "hover",
          activeType: hoverAction == null ? void 0 : hoverAction.type,
          action: hoverAction,
          fallbackUrl: (_c = selectedElement.actionHover) == null ? void 0 : _c.url,
          fallbackSlideIndex: (_d = selectedElement.actionHover) == null ? void 0 : _d.targetSlideIndex,
          canEdit,
          slideCount: slides.length,
          customShows,
          onChangeType: (type) => changeType("hover", type),
          onChangeTarget: (type, patch) => updateAction("hover", type, patch)
        },
        `hover-${selectedElement.id}`
      )
    ] })
  ] });
}
function ActionTriggerSection({
  label,
  trigger,
  activeType,
  action,
  fallbackUrl,
  fallbackSlideIndex,
  canEdit,
  slideCount,
  customShows,
  onChangeType,
  onChangeTarget
}) {
  const { t } = useTranslation();
  const [pendingType, setPendingType] = (0, import_react2.useState)(void 0);
  const effectiveType = resolveActionType(pendingType, activeType);
  const changeType = (type) => {
    setPendingType(type);
    onChangeType(type);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", "data-pptx-action-trigger": trigger, children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground font-medium", children: label }),
    (0, import_jsx_runtime.jsx)(
      "select",
      {
        disabled: !canEdit,
        "aria-label": label,
        className: cn(INPUT, "w-full"),
        value: effectiveType,
        onChange: (e) => changeType(e.target.value),
        children: ELEMENT_ACTION_TYPE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.labelKey) }, o.value))
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ActionTargetFields,
      {
        type: effectiveType,
        canEdit,
        slideCount,
        customShows,
        url: (action == null ? void 0 : action.url) ?? fallbackUrl,
        slideIndex: (action == null ? void 0 : action.slideIndex) ?? fallbackSlideIndex,
        customShowId: action == null ? void 0 : action.customShowId,
        returnAfter: action == null ? void 0 : action.returnAfter,
        onChange: (patch) => onChangeTarget(effectiveType, patch)
      }
    )
  ] });
}
var HEADING2 = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD2 = "rounded border border-border bg-card p-2 space-y-2";
var INPUT2 = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN2 = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var CELL_INPUT = "bg-muted border border-border rounded px-1 py-0.5 text-[11px] w-full text-center";
var GROUPING_OPTIONS = [
  { value: "clustered", labelKey: "pptx.chart.groupingClustered" },
  { value: "stacked", labelKey: "pptx.chart.groupingStacked" },
  { value: "percentStacked", labelKey: "pptx.chart.groupingPercentStacked" }
];
var LEGEND_POSITION_OPTIONS = [
  { value: "t", labelKey: "pptx.chart.legendTop" },
  { value: "b", labelKey: "pptx.chart.legendBottom" },
  { value: "l", labelKey: "pptx.chart.legendLeft" },
  { value: "r", labelKey: "pptx.chart.legendRight" }
];
var TICK_LABEL_POSITION_OPTIONS = [
  { value: "nextTo", labelKey: "pptx.chart.tickNextTo" },
  { value: "high", labelKey: "pptx.chart.tickHigh" },
  { value: "low", labelKey: "pptx.chart.tickLow" },
  { value: "none", labelKey: "pptx.chart.tickNone" }
];
var DISPLAY_UNITS_OPTIONS = [
  { value: "", labelKey: "pptx.chart.unitsNone" },
  { value: "hundreds", labelKey: "pptx.chart.unitsHundreds" },
  { value: "thousands", labelKey: "pptx.chart.unitsThousands" },
  { value: "tenThousands", labelKey: "pptx.chart.unitsTenThousands" },
  { value: "hundredThousands", labelKey: "pptx.chart.unitsHundredThousands" },
  { value: "millions", labelKey: "pptx.chart.unitsMillions" },
  { value: "tenMillions", labelKey: "pptx.chart.unitsTenMillions" },
  { value: "hundredMillions", labelKey: "pptx.chart.unitsHundredMillions" },
  { value: "billions", labelKey: "pptx.chart.unitsBillions" },
  { value: "trillions", labelKey: "pptx.chart.unitsTrillions" }
];
var DATA_LABEL_CONTENT_OPTIONS = [
  { key: "showValue", labelKey: "pptx.chart.labelValue" },
  { key: "showCategory", labelKey: "pptx.chart.labelCategory" },
  { key: "showSeriesName", labelKey: "pptx.chart.labelSeriesName" },
  { key: "showPercent", labelKey: "pptx.chart.labelPercent" },
  { key: "showLegendKey", labelKey: "pptx.chart.labelLegendKey" }
];
var DATA_LABEL_POSITION_OPTIONS = [
  { value: "", labelKey: "pptx.chart.labelPosDefault" },
  { value: "ctr", labelKey: "pptx.chart.labelPosCenter" },
  { value: "inEnd", labelKey: "pptx.chart.labelPosInsideEnd" },
  { value: "inBase", labelKey: "pptx.chart.labelPosInsideBase" },
  { value: "outEnd", labelKey: "pptx.chart.labelPosOutsideEnd" },
  { value: "bestFit", labelKey: "pptx.chart.labelPosBestFit" }
];
var TRENDLINE_TYPE_OPTIONS = [
  { value: "", labelKey: "pptx.chart.trendlineNone" },
  { value: "linear", labelKey: "pptx.chart.trendlineLinear" },
  { value: "exponential", labelKey: "pptx.chart.trendlineExponential" },
  { value: "logarithmic", labelKey: "pptx.chart.trendlineLogarithmic" },
  { value: "polynomial", labelKey: "pptx.chart.trendlinePolynomial" },
  { value: "power", labelKey: "pptx.chart.trendlinePower" },
  { value: "movingAvg", labelKey: "pptx.chart.trendlineMovingAvg" }
];
var TRENDLINE_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "bar",
  "line",
  "area",
  "scatter",
  "bubble"
]);
var ERROR_BAR_VALTYPE_OPTIONS = [
  { value: "", labelKey: "pptx.chart.errorBarNone" },
  { value: "fixedVal", labelKey: "pptx.chart.errorBarFixed" },
  { value: "percentage", labelKey: "pptx.chart.errorBarPercentage" },
  { value: "stdDev", labelKey: "pptx.chart.errorBarStdDev" },
  { value: "stdErr", labelKey: "pptx.chart.errorBarStdErr" }
];
var ERROR_BAR_TYPE_OPTIONS = [
  { value: "both", labelKey: "pptx.chart.errorBarBoth" },
  { value: "plus", labelKey: "pptx.chart.errorBarPlus" },
  { value: "minus", labelKey: "pptx.chart.errorBarMinus" }
];
var ERROR_BAR_VALUE_TYPES = /* @__PURE__ */ new Set(["fixedVal", "percentage", "stdDev"]);
var ERROR_BAR_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "bar",
  "line",
  "area",
  "scatter",
  "bubble"
]);
var GROUPING_SUPPORTED_TYPES = /* @__PURE__ */ new Set(["bar", "line", "area"]);
var MARKER_SYMBOL_OPTIONS = [
  { value: "", labelKey: "pptx.chart.markerAuto" },
  { value: "none", labelKey: "pptx.chart.markerNone" },
  { value: "circle", labelKey: "pptx.chart.markerCircle" },
  { value: "square", labelKey: "pptx.chart.markerSquare" },
  { value: "diamond", labelKey: "pptx.chart.markerDiamond" },
  { value: "triangle", labelKey: "pptx.chart.markerTriangle" },
  { value: "x", labelKey: "pptx.chart.markerX" },
  { value: "star", labelKey: "pptx.chart.markerStar" },
  { value: "plus", labelKey: "pptx.chart.markerPlus" },
  { value: "dot", labelKey: "pptx.chart.markerDot" },
  { value: "dash", labelKey: "pptx.chart.markerDash" }
];
var MARKER_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "line",
  "scatter",
  "bubble",
  "radar"
]);
var GRIDLINE_DASH_OPTIONS = [
  { value: "", labelKey: "pptx.chart.dashDefault" },
  { value: "solid", labelKey: "pptx.chart.dashSolid" },
  { value: "dash", labelKey: "pptx.chart.dashDash" },
  { value: "dot", labelKey: "pptx.chart.dashDot" },
  { value: "dashDot", labelKey: "pptx.chart.dashDashDot" },
  { value: "lgDash", labelKey: "pptx.chart.dashLong" }
];
var COMBO_SERIES_TYPE_OPTIONS = [
  { value: "", labelKey: "pptx.chart.comboDefault" },
  { value: "bar", labelKey: "pptx.chart.typeBar" },
  { value: "line", labelKey: "pptx.chart.typeLine" },
  { value: "area", labelKey: "pptx.chart.typeArea" },
  { value: "scatter", labelKey: "pptx.chart.typeScatter" }
];
var COMBO_SUPPORTED_TYPES = /* @__PURE__ */ new Set(["bar", "line", "area", "combo"]);
var EXPLOSION_SUPPORTED_TYPES = /* @__PURE__ */ new Set([
  "pie",
  "pie3D",
  "doughnut",
  "ofPie"
]);
var AXIS_ROWS = [
  { type: "valAx", labelKey: "pptx.chart.valueAxis", hasScale: true },
  { type: "dateAx", labelKey: "pptx.chart.dateAxis", hasScale: true },
  { type: "catAx", labelKey: "pptx.chart.categoryAxis", hasScale: false }
];
function ChartAxisOptions({ axes, canEdit, onUpdateAxis }) {
  const { t } = useTranslation();
  const rows = AXIS_ROWS.map((row) => ({
    ...row,
    axis: axes == null ? void 0 : axes.find((a) => a.axisType === row.type)
  })).filter((row) => Boolean(row.axis));
  if (rows.length === 0) {
    return null;
  }
  const numberField = (axisType, labelKey, value, key) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t(labelKey) }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "number",
        disabled: !canEdit,
        className: INPUT2,
        value: value ?? "",
        placeholder: t("pptx.chart.auto"),
        onChange: (e) => {
          const raw = e.target.value;
          if (raw === "") {
            onUpdateAxis(axisType, { [key]: void 0 });
            return;
          }
          const num = Number.parseFloat(raw);
          if (Number.isFinite(num)) {
            onUpdateAxis(axisType, { [key]: num });
          }
        }
      }
    )
  ] });
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.axes") }),
    rows.map(({ type, labelKey, hasScale, axis }) => {
      var _a;
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] font-medium", children: t(labelKey) }),
        hasScale && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 ml-2", children: [
          numberField(type, "pptx.chart.min", axis.min, "min"),
          numberField(type, "pptx.chart.max", axis.max, "max"),
          numberField(type, "pptx.chart.majorUnit", axis.majorUnit, "majorUnit"),
          numberField(type, "pptx.chart.minorUnit", axis.minorUnit, "minorUnit"),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.displayUnits") }),
            (0, import_jsx_runtime.jsx)(
              "select",
              {
                "aria-label": t("pptx.chart.displayUnits"),
                disabled: !canEdit,
                className: INPUT2,
                value: axis.displayUnits ?? "",
                onChange: (e) => onUpdateAxis(type, {
                  displayUnits: e.target.value || void 0
                }),
                children: DISPLAY_UNITS_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
              }
            )
          ] })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 ml-2", children: [
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.axisTitle") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                disabled: !canEdit,
                className: INPUT2,
                value: axis.titleText ?? "",
                placeholder: t("pptx.chart.axisTitlePlaceholder"),
                onChange: (e) => onUpdateAxis(type, { titleText: e.target.value })
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.numberFormat") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                disabled: !canEdit,
                className: INPUT2,
                value: ((_a = axis.numFmt) == null ? void 0 : _a.formatCode) ?? "",
                placeholder: t("pptx.settings.general"),
                onChange: (e) => onUpdateAxis(type, {
                  numFmt: e.target.value ? { formatCode: e.target.value, sourceLinked: false } : void 0
                })
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.tickLabels") }),
            (0, import_jsx_runtime.jsx)(
              "select",
              {
                "aria-label": t("pptx.chart.tickLabels"),
                disabled: !canEdit,
                className: INPUT2,
                value: axis.tickLblPos ?? "nextTo",
                onChange: (e) => onUpdateAxis(type, {
                  tickLblPos: e.target.value
                }),
                children: TICK_LABEL_POSITION_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: axis.majorGridlines ?? false,
                onChange: (e) => onUpdateAxis(type, { majorGridlines: e.target.checked }),
                className: "accent-primary"
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.majorGridlines") })
          ] }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: axis.minorGridlines ?? false,
                onChange: (e) => onUpdateAxis(type, { minorGridlines: e.target.checked }),
                className: "accent-primary"
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.minorGridlines") })
          ] })
        ] })
      ] }, type);
    })
  ] });
}
var SCALE_AXES = [
  { type: "valAx", labelKey: "pptx.chart.valueAxis" },
  { type: "dateAx", labelKey: "pptx.chart.dateAxis" },
  { type: "catAx", labelKey: "pptx.chart.categoryAxis" }
];
function ChartAxisStyleOptions({
  axes,
  canEdit,
  onSetLogScale,
  onSetTitleStyle,
  onSetGridlineStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const rows = SCALE_AXES.map((row) => ({
    ...row,
    axis: axes == null ? void 0 : axes.find((a) => a.axisType === row.type)
  })).filter((row) => Boolean(row.axis));
  if (rows.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.axisStyling") }),
    rows.map(({ type, labelKey, axis }) => {
      const hasScale = type === "valAx" || type === "dateAx";
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] font-medium", children: t(labelKey) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 ml-2", children: [
          hasScale && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
            (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "checkbox",
                  disabled: !canEdit,
                  checked: axis.logScale ?? false,
                  onChange: (e) => onSetLogScale(type, { enabled: e.target.checked, base: axis.logBase }),
                  className: "accent-primary"
                }
              ),
              (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.logScale") })
            ] }),
            axis.logScale && (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "number",
                min: 2,
                disabled: !canEdit,
                title: t("pptx.chart.logBase"),
                className: "w-16 bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
                value: axis.logBase ?? 10,
                onChange: (e) => {
                  const num = Number.parseFloat(e.target.value);
                  if (Number.isFinite(num) && num > 1) {
                    onSetLogScale(type, { enabled: true, base: num });
                  }
                }
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.titleFont") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                disabled: !canEdit,
                className: INPUT2,
                placeholder: t("pptx.chart.auto"),
                value: axis.fontFamily ?? "",
                onChange: (e) => onSetTitleStyle(type, { fontFamily: e.target.value || null })
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "number",
                min: 4,
                max: 96,
                disabled: !canEdit,
                title: t("pptx.chart.fontSize"),
                className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
                value: axis.fontSize ?? "",
                placeholder: t("pptx.chart.auto"),
                onChange: (e) => {
                  const raw = e.target.value;
                  const num = Number.parseFloat(raw);
                  onSetTitleStyle(type, { fontSize: Number.isFinite(num) ? num : null });
                }
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 text-[11px]", children: [
            (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 cursor-pointer", children: [
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "checkbox",
                  disabled: !canEdit,
                  checked: axis.fontBold ?? false,
                  onChange: (e) => onSetTitleStyle(type, { fontBold: e.target.checked }),
                  className: "accent-primary"
                }
              ),
              (0, import_jsx_runtime.jsx)("span", { children: t("pptx.chart.bold") })
            ] }),
            (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
              (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.titleColor") }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "color",
                  disabled: !canEdit,
                  className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
                  value: axis.fontColor ?? "#000000",
                  onChange: (e) => {
                    onSetTitleStyle(type, { fontColor: e.target.value });
                    pushColor(e.target.value);
                  }
                }
              )
            ] })
          ] }),
          ["major", "minor"].map((which) => {
            const spPr = which === "major" ? axis.majorGridlinesSpPr : axis.minorGridlinesSpPr;
            const enabled = which === "major" ? axis.majorGridlines : axis.minorGridlines;
            if (!enabled) {
              return null;
            }
            return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
              (0, import_jsx_runtime.jsx)("span", { className: "w-12 text-muted-foreground shrink-0", children: t(
                which === "major" ? "pptx.chart.majorGridlines" : "pptx.chart.minorGridlines"
              ) }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "color",
                  disabled: !canEdit,
                  title: t("pptx.chart.gridlineColor"),
                  className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
                  value: (spPr == null ? void 0 : spPr.strokeColor) ?? "#d9d9d9",
                  onChange: (e) => {
                    onSetGridlineStyle(type, which, { color: e.target.value });
                    pushColor(e.target.value);
                  }
                }
              ),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "number",
                  min: 0.25,
                  step: 0.25,
                  disabled: !canEdit,
                  title: t("pptx.chart.gridlineWidth"),
                  className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
                  value: (spPr == null ? void 0 : spPr.strokeWidth) ?? "",
                  placeholder: t("pptx.chart.auto"),
                  onChange: (e) => {
                    const num = Number.parseFloat(e.target.value);
                    onSetGridlineStyle(type, which, {
                      width: Number.isFinite(num) ? num : null
                    });
                  }
                }
              ),
              (0, import_jsx_runtime.jsx)(
                "select",
                {
                  disabled: !canEdit,
                  title: t("pptx.chart.gridlineDash"),
                  className: INPUT2,
                  value: (spPr == null ? void 0 : spPr.strokeDashStyle) ?? "",
                  onChange: (e) => onSetGridlineStyle(type, which, { dashStyle: e.target.value || null }),
                  children: GRIDLINE_DASH_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
                }
              )
            ] }, which);
          })
        ] })
      ] }, type);
    })
  ] });
}
function ChartComboTypeOptions({
  chartType,
  series,
  canEdit,
  onSetSeriesType
}) {
  const { t } = useTranslation();
  if (!COMBO_SUPPORTED_TYPES.has(chartType) || series.length < 2) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.comboTypes") }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-1.5", children: series.map((s, i) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          disabled: !canEdit,
          className: INPUT2,
          value: s.seriesChartType ?? "",
          onChange: (e) => {
            const value = e.target.value;
            onSetSeriesType(i, value === "" ? null : value);
          },
          children: COMBO_SERIES_TYPE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
        }
      )
    ] }, `${s.name}-${i}`)) })
  ] });
}
function ChartDataGrid({
  series,
  categories,
  canEdit,
  highlightCell,
  onUpdateSeries,
  onUpdateCategoryLabel,
  onUpdateValue,
  onAddCategory,
  onRemoveCategory,
  onAddSeries,
  onRemoveSeries
}) {
  const { t } = useTranslation();
  const highlightRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    var _a;
    (_a = highlightRef.current) == null ? void 0 : _a.scrollIntoView({ block: "nearest", inline: "nearest" });
  }, [highlightCell == null ? void 0 : highlightCell.seriesIndex, highlightCell == null ? void 0 : highlightCell.pointIndex]);
  const highlightClass = " ring-1 ring-primary";
  const isSeriesHighlight = (si) => (highlightCell == null ? void 0 : highlightCell.seriesIndex) === si && highlightCell.pointIndex === void 0;
  const isValueHighlight = (si, ci) => (highlightCell == null ? void 0 : highlightCell.seriesIndex) === si && highlightCell.pointIndex === ci;
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.data") }),
      canEdit && (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1", children: [
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            className: BTN2,
            title: t("pptx.chart.addCategory"),
            onClick: onAddCategory,
            children: [
              (0, import_jsx_runtime.jsx)(LuPlus, { className: "inline w-3 h-3 mr-0.5" }),
              t("pptx.chart.cat")
            ]
          }
        ),
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            className: BTN2,
            title: t("pptx.chart.addSeries"),
            onClick: onAddSeries,
            children: [
              (0, import_jsx_runtime.jsx)(LuPlus, { className: "inline w-3 h-3 mr-0.5" }),
              t("pptx.chart.seriesShort")
            ]
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "overflow-x-auto", children: (0, import_jsx_runtime.jsxs)("table", { className: "w-full text-[11px] border-collapse", children: [
      (0, import_jsx_runtime.jsx)("thead", { children: (0, import_jsx_runtime.jsxs)("tr", { children: [
        (0, import_jsx_runtime.jsx)(
          "th",
          {
            "aria-label": t("pptx.chart.categories"),
            className: "text-muted-foreground p-0.5 text-left min-w-[60px]"
          }
        ),
        series.map((s, si) => (0, import_jsx_runtime.jsx)("th", { className: "p-0.5 font-normal min-w-[72px]", children: (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-0.5", children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "text",
              disabled: !canEdit,
              ref: isSeriesHighlight(si) ? highlightRef : null,
              className: `${CELL_INPUT}${isSeriesHighlight(si) ? highlightClass : ""}`,
              value: s.name,
              onChange: (e) => onUpdateSeries(si, { name: e.target.value })
            }
          ),
          canEdit && series.length > 1 && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "text-muted-foreground hover:text-red-400 shrink-0",
              title: t("pptx.chart.removeSeries"),
              onClick: () => onRemoveSeries(si),
              children: (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-3 h-3" })
            }
          )
        ] }) }, si))
      ] }) }),
      (0, import_jsx_runtime.jsx)("tbody", { children: categories.map((cat, ci) => (0, import_jsx_runtime.jsxs)("tr", { children: [
        (0, import_jsx_runtime.jsx)("td", { className: "p-0.5", children: (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-0.5", children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "text",
              disabled: !canEdit,
              className: CELL_INPUT,
              value: cat,
              onChange: (e) => onUpdateCategoryLabel(ci, e.target.value)
            }
          ),
          canEdit && categories.length > 1 && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "text-muted-foreground hover:text-red-400 shrink-0",
              title: t("pptx.chart.removeCategory"),
              onClick: () => onRemoveCategory(ci),
              children: (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-3 h-3" })
            }
          )
        ] }) }),
        series.map((s, si) => (0, import_jsx_runtime.jsx)("td", { className: "p-0.5", children: (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            "aria-label": `${s.name} value ${ci + 1}`,
            disabled: !canEdit,
            ref: isValueHighlight(si, ci) ? highlightRef : null,
            className: `${CELL_INPUT}${isValueHighlight(si, ci) ? highlightClass : ""}`,
            value: s.values[ci] ?? 0,
            onChange: (e) => onUpdateValue(si, ci, e.target.value)
          }
        ) }, si))
      ] }, ci)) })
    ] }) })
  ] });
}
function ChartDataLabelOptions({
  style,
  canEdit,
  onUpdateStyle
}) {
  const { t } = useTranslation();
  if (!(style == null ? void 0 : style.hasDataLabels)) {
    return null;
  }
  const labels = style.dataLabels ?? {};
  const patchLabels = (patch) => onUpdateStyle({ dataLabels: { ...labels, ...patch } });
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.dataLabels") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      DATA_LABEL_CONTENT_OPTIONS.map((opt) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: labels[opt.key] ?? false,
            onChange: (e) => patchLabels({ [opt.key]: e.target.checked }),
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t(opt.labelKey) })
      ] }, opt.key)),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.labelPosition") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.chart.labelPosition"),
            disabled: !canEdit,
            className: INPUT2,
            value: labels.position ?? "",
            onChange: (e) => patchLabels({
              position: e.target.value || void 0
            }),
            children: DATA_LABEL_POSITION_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] })
    ] })
  ] });
}
var SYMBOL_OPTIONS = MARKER_SYMBOL_OPTIONS.filter((o) => o.value !== "");
function ChartDataPointMarkerOptions({
  chartType,
  categories,
  series,
  canEdit,
  onSetPointMarker
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const [seriesIndex, setSeriesIndex] = (0, import_react2.useState)(0);
  if (!MARKER_SUPPORTED_TYPES.has(chartType) || series.length === 0 || categories.length === 0) {
    return null;
  }
  const activeSeries = series[Math.min(seriesIndex, series.length - 1)];
  const pointFor = (idx) => {
    var _a;
    return (_a = activeSeries.dataPoints) == null ? void 0 : _a.find((p) => p.idx === idx);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.pointMarkers") }),
    series.length > 1 && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.series") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.chart.series"),
          disabled: !canEdit,
          className: "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full",
          value: Math.min(seriesIndex, series.length - 1),
          onChange: (e) => setSeriesIndex(Number.parseInt(e.target.value, 10)),
          children: series.map((s, i) => (0, import_jsx_runtime.jsx)("option", { value: i, children: s.name }, `${s.name}-${i}`))
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-2", children: categories.map((cat, idx) => {
      var _a;
      const point2 = pointFor(idx);
      const hasOverride = (point2 == null ? void 0 : point2.marker) !== void 0;
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: cat, children: cat }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 shrink-0", children: [
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: hasOverride,
                onChange: (e) => {
                  onSetPointMarker(
                    seriesIndex,
                    idx,
                    e.target.checked ? { symbol: "circle" } : null
                  );
                }
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.markerOverride") })
          ] })
        ] }),
        hasOverride && (point2 == null ? void 0 : point2.marker) && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 ml-2 flex-wrap", children: [
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              disabled: !canEdit,
              className: "bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
              value: point2.marker.symbol,
              onChange: (e) => onSetPointMarker(seriesIndex, idx, {
                symbol: e.target.value
              }),
              children: SYMBOL_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 1,
              max: 20,
              disabled: !canEdit,
              title: t("pptx.chart.markerSize"),
              className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
              value: point2.marker.size ?? "",
              placeholder: t("pptx.chart.auto"),
              onChange: (e) => {
                const num = Number.parseInt(e.target.value, 10);
                onSetPointMarker(seriesIndex, idx, {
                  size: Number.isFinite(num) ? num : void 0
                });
              }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              title: t("pptx.chart.markerFill"),
              className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
              value: ((_a = point2.marker.spPr) == null ? void 0 : _a.fillColor) ?? "#4472c4",
              onChange: (e) => {
                onSetPointMarker(seriesIndex, idx, { fillColor: e.target.value });
                pushColor(e.target.value);
              }
            }
          )
        ] })
      ] }, `${cat}-${idx}`);
    }) })
  ] });
}
function ChartDataPointOptions({
  chartType,
  categories,
  series,
  canEdit,
  onSetPointFill,
  onSetPointExplosion,
  onSetPointLabel
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const [seriesIndex, setSeriesIndex] = (0, import_react2.useState)(0);
  if (series.length === 0 || categories.length === 0) {
    return null;
  }
  const activeSeries = series[Math.min(seriesIndex, series.length - 1)];
  const showExplosion = EXPLOSION_SUPPORTED_TYPES.has(chartType);
  const pointFor = (idx) => {
    var _a;
    return (_a = activeSeries.dataPoints) == null ? void 0 : _a.find((p) => p.idx === idx);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.dataPoints") }),
    series.length > 1 && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.series") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.chart.series"),
          disabled: !canEdit,
          className: INPUT2,
          value: Math.min(seriesIndex, series.length - 1),
          onChange: (e) => setSeriesIndex(Number.parseInt(e.target.value, 10)),
          children: series.map((s, i) => (0, import_jsx_runtime.jsx)("option", { value: i, children: s.name }, `${s.name}-${i}`))
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-1.5", children: categories.map((cat, idx) => {
      var _a, _b2, _c, _d;
      const point2 = pointFor(idx);
      return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: cat, children: cat }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            disabled: !canEdit,
            title: t("pptx.chart.pointLabelOverride"),
            className: "w-20 bg-muted border border-border rounded px-1.5 py-0.5 text-[11px]",
            value: ((_b2 = (_a = activeSeries.dataLabels) == null ? void 0 : _a.find((l) => l.idx === idx)) == null ? void 0 : _b2.text) ?? "",
            placeholder: t("pptx.chart.auto"),
            onChange: (e) => {
              const raw = e.target.value;
              onSetPointLabel(seriesIndex, idx, raw === "" ? null : raw);
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            title: t("pptx.chart.pointFill"),
            className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
            value: ((_c = point2 == null ? void 0 : point2.spPr) == null ? void 0 : _c.fillColor) ?? activeSeries.color ?? "#4472c4",
            onChange: (e) => {
              onSetPointFill(seriesIndex, idx, e.target.value);
              pushColor(e.target.value);
            }
          }
        ),
        ((_d = point2 == null ? void 0 : point2.spPr) == null ? void 0 : _d.fillColor) && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            title: t("pptx.chart.pointFillClear"),
            className: "text-muted-foreground hover:text-foreground",
            onClick: () => onSetPointFill(seriesIndex, idx, null),
            children: "×"
          }
        ),
        showExplosion && (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 100,
            disabled: !canEdit,
            title: t("pptx.chart.pointExplosion"),
            className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
            value: (point2 == null ? void 0 : point2.explosion) ?? "",
            placeholder: "0",
            onChange: (e) => {
              const raw = e.target.value;
              if (raw === "") {
                onSetPointExplosion(seriesIndex, idx, null);
                return;
              }
              const num = Number.parseInt(raw, 10);
              if (Number.isFinite(num)) {
                onSetPointExplosion(seriesIndex, idx, num);
              }
            }
          }
        )
      ] }, `${cat}-${idx}`);
    }) })
  ] });
}
function ChartDisplayOptions({
  style,
  canEdit,
  onUpdateStyle,
  hasGridlines,
  onToggleGridlines
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.display") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: (style == null ? void 0 : style.hasTitle) ?? false,
            onChange: (e) => onUpdateStyle({ hasTitle: e.target.checked }),
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.showTitle") })
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: (style == null ? void 0 : style.hasLegend) ?? false,
            onChange: (e) => onUpdateStyle({ hasLegend: e.target.checked }),
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.showLegend") })
      ] }),
      (style == null ? void 0 : style.hasLegend) && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px] ml-4", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-12 text-muted-foreground shrink-0", children: t("pptx.chart.legendPosition") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.chart.legendPosition"),
            disabled: !canEdit,
            className: INPUT2,
            value: style.legendPosition ?? "b",
            onChange: (e) => onUpdateStyle({
              legendPosition: e.target.value
            }),
            children: LEGEND_POSITION_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: hasGridlines,
            onChange: (e) => onToggleGridlines(e.target.checked),
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.showGridlines") })
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: (style == null ? void 0 : style.hasDataLabels) ?? false,
            onChange: (e) => onUpdateStyle({
              hasDataLabels: e.target.checked
            }),
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t("pptx.chart.showDataLabels") })
      ] })
    ] })
  ] });
}
function ChartErrorBarOptions({
  chartType,
  series,
  canEdit,
  onSetErrorBars
}) {
  const { t } = useTranslation();
  if (!ERROR_BAR_SUPPORTED_TYPES.has(chartType) || series.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.errorBars") }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-2", children: series.map((s, i) => {
      var _a;
      const bars = (_a = s.errBars) == null ? void 0 : _a[0];
      const valType = (bars == null ? void 0 : bars.valType) ?? "";
      const showValue = ERROR_BAR_VALUE_TYPES.has(valType);
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: valType,
              onChange: (e) => {
                const value = e.target.value;
                if (!value) {
                  onSetErrorBars(i, null);
                  return;
                }
                onSetErrorBars(i, {
                  direction: (bars == null ? void 0 : bars.direction) ?? "y",
                  barType: (bars == null ? void 0 : bars.barType) ?? "both",
                  valType: value,
                  val: bars == null ? void 0 : bars.val
                });
              },
              children: ERROR_BAR_VALTYPE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        bars && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 ml-2", children: [
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: bars.barType,
              onChange: (e) => onSetErrorBars(i, {
                ...bars,
                barType: e.target.value
              }),
              children: ERROR_BAR_TYPE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          ),
          showValue && (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              disabled: !canEdit,
              className: INPUT2,
              value: bars.val ?? "",
              placeholder: t("pptx.chart.errorBarAmount"),
              onChange: (e) => {
                const raw = e.target.value;
                const num = raw === "" ? void 0 : Number.parseFloat(raw);
                if (raw !== "" && !Number.isFinite(num)) {
                  return;
                }
                onSetErrorBars(i, { ...bars, val: num });
              }
            }
          )
        ] })
      ] }, `${s.name}-${i}`);
    }) })
  ] });
}
function ChartMarkerOptions({
  chartType,
  series,
  canEdit,
  onSetMarker
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  if (!MARKER_SUPPORTED_TYPES.has(chartType) || series.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.markers") }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-2", children: series.map((s, i) => {
      var _a;
      const marker = s.marker;
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: (marker == null ? void 0 : marker.symbol) ?? "",
              onChange: (e) => {
                const value = e.target.value;
                if (value === "") {
                  onSetMarker(i, null);
                  return;
                }
                onSetMarker(i, { symbol: value });
              },
              children: MARKER_SYMBOL_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        marker && marker.symbol !== "none" && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 ml-2", children: [
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.markerSize") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "number",
                min: 2,
                max: 72,
                disabled: !canEdit,
                className: "w-14 bg-muted border border-border rounded px-1.5 py-0.5",
                value: marker.size ?? "",
                placeholder: t("pptx.chart.auto"),
                onChange: (e) => {
                  const raw = e.target.value;
                  const num = Number.parseInt(raw, 10);
                  onSetMarker(i, { size: Number.isFinite(num) ? num : void 0 });
                }
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.markerFill") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "color",
                disabled: !canEdit,
                className: "h-6 w-8 cursor-pointer rounded border border-border bg-transparent",
                value: ((_a = marker.spPr) == null ? void 0 : _a.fillColor) ?? "#4472c4",
                onChange: (e) => {
                  onSetMarker(i, { fillColor: e.target.value });
                  pushColor(e.target.value);
                }
              }
            )
          ] })
        ] })
      ] }, `${s.name}-${i}`);
    }) })
  ] });
}
function DebouncedColorInput({
  value,
  disabled,
  className,
  ariaLabel,
  onCommit
}) {
  const [local, setLocal] = (0, import_react2.useState)(value);
  const commitRef = (0, import_react2.useRef)(onCommit);
  commitRef.current = onCommit;
  const { pushColor } = useRecentColors();
  (0, import_react2.useEffect)(() => {
    setLocal(value);
  }, [value]);
  const handleChange = (0, import_react2.useCallback)(
    (e) => {
      const next = e.target.value;
      setLocal(next);
      commitRef.current(next);
      pushColor(next);
    },
    [pushColor]
  );
  return (0, import_jsx_runtime.jsx)(
    "input",
    {
      type: "color",
      "aria-label": ariaLabel ?? "Color",
      disabled,
      value: local,
      className,
      onChange: handleChange
    }
  );
}
var DEFAULT_SWATCH = "#4472c4";
function ChartSeriesColorOptions({
  chartData,
  canEdit,
  onSetColor,
  onToggleSecondaryAxis
}) {
  const { t } = useTranslation();
  const series = chartData.series;
  if (series.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.seriesColors") }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: series.map((s, i) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 text-muted-foreground shrink-0", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: isSeriesUsingSecondaryAxis(chartData, i),
            onChange: (e) => onToggleSecondaryAxis(i, e.target.checked),
            className: "accent-primary"
          }
        ),
        t("pptx.chart.secondaryAxis")
      ] }),
      (0, import_jsx_runtime.jsx)(
        DebouncedColorInput,
        {
          value: s.color ?? DEFAULT_SWATCH,
          disabled: !canEdit,
          ariaLabel: t("pptx.chart.seriesColor", { name: s.name }),
          className: "h-6 w-8 cursor-pointer rounded border border-border bg-muted p-0",
          onCommit: (hex) => onSetColor(i, hex)
        }
      ),
      canEdit && s.color && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "text-muted-foreground hover:text-red-400 shrink-0",
          title: t("pptx.chart.clearSeriesColor"),
          onClick: () => onSetColor(i, null),
          children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-3 h-3" })
        }
      )
    ] }, `${s.name}-${i}`)) })
  ] });
}
function ChartSubtypeOptions({
  chartData,
  canEdit,
  onUpdateChartData
}) {
  const { t } = useTranslation();
  if (chartData.chartType !== "bar3D" && chartData.chartType !== "radar" && chartData.chartType !== "surface") {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.display") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      chartData.chartType === "bar3D" && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.bar3DShapeLabel") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "data-testid": "pptx-chart-bar3d-shape",
            "aria-label": t("pptx.chart.bar3DShapeLabel"),
            disabled: !canEdit,
            className: INPUT2,
            value: chartData.barShape ?? "box",
            onChange: (e) => onUpdateChartData(bar3DShapePatch(chartData, e.target.value)),
            children: BAR3D_SHAPE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] }),
      chartData.chartType === "radar" && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.radarStyleLabel") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "data-testid": "pptx-chart-radar-style",
            "aria-label": t("pptx.chart.radarStyleLabel"),
            disabled: !canEdit,
            className: INPUT2,
            value: chartData.radarStyle ?? "standard",
            onChange: (e) => onUpdateChartData(
              radarStylePatch(
                chartData,
                e.target.value
              )
            ),
            children: RADAR_STYLE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] }),
      chartData.chartType === "surface" && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-16 text-muted-foreground shrink-0", children: t("pptx.chart.surfaceWireframeLabel") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "data-testid": "pptx-chart-surface-wireframe",
            "aria-label": t("pptx.chart.surfaceWireframeLabel"),
            disabled: !canEdit,
            className: INPUT2,
            value: chartData.wireframe ? "true" : "false",
            onChange: (e) => onUpdateChartData(surfaceWireframePatch(chartData, e.target.value === "true")),
            children: SURFACE_WIREFRAME_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
          }
        )
      ] })
    ] })
  ] });
}
function ChartTrendlineOptions({
  chartType,
  series,
  canEdit,
  onSetTrendline
}) {
  const { t } = useTranslation();
  if (!TRENDLINE_SUPPORTED_TYPES.has(chartType) || series.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.trendlines") }),
    (0, import_jsx_runtime.jsx)("div", { className: "space-y-2", children: series.map((s, i) => {
      var _a;
      const trendline = (_a = s.trendlines) == null ? void 0 : _a[0];
      return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", title: s.name, children: s.name }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              disabled: !canEdit,
              className: INPUT2,
              value: (trendline == null ? void 0 : trendline.trendlineType) ?? "",
              onChange: (e) => {
                const value = e.target.value;
                if (!value) {
                  onSetTrendline(i, null);
                  return;
                }
                onSetTrendline(i, {
                  ...trendline,
                  trendlineType: value
                });
              },
              children: TRENDLINE_TYPE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
            }
          )
        ] }),
        trendline && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 ml-2", children: [
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 text-[11px] cursor-pointer", children: [
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: trendline.displayEq ?? false,
                onChange: (e) => onSetTrendline(i, { ...trendline, displayEq: e.target.checked }),
                className: "accent-primary"
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { children: t("pptx.chart.trendlineEquation") })
          ] }),
          (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 text-[11px] cursor-pointer", children: [
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "checkbox",
                disabled: !canEdit,
                checked: trendline.displayRSq ?? false,
                onChange: (e) => onSetTrendline(i, { ...trendline, displayRSq: e.target.checked }),
                className: "accent-primary"
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { children: t("pptx.chart.trendlineRSquared") })
          ] })
        ] })
      ] }, `${s.name}-${i}`);
    }) })
  ] });
}
function ChartTypeSelector({
  title,
  chartType,
  chartData,
  grouping,
  seriesCount,
  categoryCount,
  canEdit,
  onUpdateChartData,
  onTitleChange
}) {
  const { t } = useTranslation();
  const supportsGrouping = GROUPING_SUPPORTED_TYPES.has(chartType);
  const displayedType = resolveDisplayedChartType(chartData);
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.heading") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "text-[11px] text-muted-foreground mb-1", children: [
      seriesCount,
      " ",
      t("pptx.chart.series"),
      " · ",
      categoryCount,
      " ",
      t("pptx.chart.categories")
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "w-10 text-muted-foreground shrink-0", children: t("pptx.chart.title") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          disabled: !canEdit,
          className: INPUT2,
          value: title ?? "",
          onChange: (e) => onTitleChange(e.target.value)
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "w-10 text-muted-foreground shrink-0", children: t("pptx.chart.type") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.chart.type"),
          disabled: !canEdit,
          className: INPUT2,
          value: displayedType,
          onChange: (e) => onUpdateChartData({
            chartType: e.target.value
          }),
          children: CHART_TYPE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
        }
      )
    ] }),
    supportsGrouping && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "w-10 text-muted-foreground shrink-0", children: t("pptx.chart.grouping") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.chart.grouping"),
          disabled: !canEdit,
          className: INPUT2,
          value: grouping ?? "clustered",
          onChange: (e) => onUpdateChartData({
            grouping: e.target.value
          }),
          children: GROUPING_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value, children: t(opt.labelKey) }, opt.value))
        }
      )
    ] })
  ] });
}
function NumberField({ value, canEdit, fraction, nonNegative, onChange }) {
  return (0, import_jsx_runtime.jsx)(
    "input",
    {
      type: "number",
      step: fraction ? "0.01" : void 0,
      min: fraction || nonNegative ? 0 : void 0,
      max: fraction ? 1 : void 0,
      disabled: !canEdit,
      className: INPUT2,
      value,
      onChange: (e) => onChange(Number(e.target.value))
    }
  );
}
function RotationField({
  row,
  canEdit,
  onRotationPatch
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeRotation") }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "number",
        step: "1",
        "aria-label": t("pptx.chart.userShapeRotation"),
        disabled: !canEdit,
        className: INPUT2,
        value: row.rotation ?? 0,
        onChange: (e) => onRotationPatch(row.path, Number(e.target.value) || void 0)
      }
    )
  ] });
}
function FlipFields({
  row,
  canEdit,
  onFlipPatch
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          "aria-label": t("pptx.arrange.flipHorizontally"),
          disabled: !canEdit,
          className: "accent-primary",
          checked: row.flipH ?? false,
          onChange: (e) => onFlipPatch(row.path, { flipH: e.target.checked })
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.arrange.flipHorizontally") })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          "aria-label": t("pptx.arrange.flipVertically"),
          disabled: !canEdit,
          className: "accent-primary",
          checked: row.flipV ?? false,
          onChange: (e) => onFlipPatch(row.path, { flipV: e.target.checked })
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.arrange.flipVertically") })
    ] })
  ] });
}
function ChartUserShapePositionFields({
  row,
  userShapes,
  canEdit,
  onPatch,
  onBoxPatch,
  onRotationPatch,
  onFlipPatch
}) {
  const { t } = useTranslation();
  const update = (patch) => onPatch(row.path, patch);
  if (row.depth === 0) {
    const from = row.from;
    return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-wrap items-center gap-1 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeFrom") }),
      (0, import_jsx_runtime.jsx)(
        NumberField,
        {
          fraction: true,
          canEdit,
          value: from.x,
          onChange: (x) => update({ from: { ...from, x } })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        NumberField,
        {
          fraction: true,
          canEdit,
          value: from.y,
          onChange: (y) => update({ from: { ...from, y } })
        }
      ),
      row.anchor === "rel" && row.to ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeTo") }),
        (0, import_jsx_runtime.jsx)(
          NumberField,
          {
            fraction: true,
            canEdit,
            value: row.to.x,
            onChange: (x) => update({ to: { ...row.to, x } })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          NumberField,
          {
            fraction: true,
            canEdit,
            value: row.to.y,
            onChange: (y) => update({ to: { ...row.to, y } })
          }
        )
      ] }) : null,
      row.anchor === "abs" && row.ext ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeSize") }),
        (0, import_jsx_runtime.jsx)(
          NumberField,
          {
            nonNegative: true,
            canEdit,
            value: row.ext.cx,
            onChange: (cx) => update({ ext: { ...row.ext, cx } })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          NumberField,
          {
            nonNegative: true,
            canEdit,
            value: row.ext.cy,
            onChange: (cy) => update({ ext: { ...row.ext, cy } })
          }
        )
      ] }) : null,
      (0, import_jsx_runtime.jsx)(RotationField, { row, canEdit, onRotationPatch }),
      (0, import_jsx_runtime.jsx)(FlipFields, { row, canEdit, onFlipPatch })
    ] });
  }
  const box = getChartUserShapeRowChartBox(userShapes, row.path);
  if (!box) {
    return null;
  }
  const patchBox = (next) => onBoxPatch(row.path, { from: box.from, to: box.to, ...next });
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-wrap items-center gap-1 text-[11px]", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeFrom") }),
    (0, import_jsx_runtime.jsx)(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.from.x,
        onChange: (x) => patchBox({ from: { ...box.from, x } })
      }
    ),
    (0, import_jsx_runtime.jsx)(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.from.y,
        onChange: (y) => patchBox({ from: { ...box.from, y } })
      }
    ),
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeTo") }),
    (0, import_jsx_runtime.jsx)(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.to.x,
        onChange: (x) => patchBox({ to: { ...box.to, x } })
      }
    ),
    (0, import_jsx_runtime.jsx)(
      NumberField,
      {
        fraction: true,
        canEdit,
        value: box.to.y,
        onChange: (y) => patchBox({ to: { ...box.to, y } })
      }
    ),
    (0, import_jsx_runtime.jsx)(RotationField, { row, canEdit, onRotationPatch }),
    (0, import_jsx_runtime.jsx)(FlipFields, { row, canEdit, onFlipPatch })
  ] });
}
var PATH_KEY = (path) => path.join(",");
function ChartUserShapeOptions({
  chartData,
  canEdit,
  onUpdateChartData
}) {
  const { t } = useTranslation();
  const shapes = chartData.userShapes;
  const rows = listChartUserShapeRows(shapes);
  const kindLabel = (kind) => {
    const key = `pptx.chart.userShapeKind${kind.charAt(0).toUpperCase()}${kind.slice(1)}`;
    return t(key);
  };
  const update = (path, patch) => onUpdateChartData({ userShapes: withChartUserShapeRowUpdated(shapes, path, patch) });
  const updateBox = (path, box) => onUpdateChartData({ userShapes: withChartUserShapeRowChartBoxUpdated(shapes, path, box) });
  const updateRotation = (path, rotation) => onUpdateChartData({ userShapes: withChartUserShapeRowRotationUpdated(shapes, path, rotation) });
  const updateFlip = (path, flip) => onUpdateChartData({ userShapes: withChartUserShapeRowFlipUpdated(shapes, path, flip) });
  const addIntoGroup = (path) => {
    const transform = getChartUserShapeGroupTransform(shapes, path);
    if (!transform) {
      return;
    }
    onUpdateChartData({
      userShapes: withChartUserShapeGroupChildAdded(
        shapes,
        path,
        createDefaultChartUserShapeGroupChild(transform)
      )
    });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD2, children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING2, children: t("pptx.chart.userShapes") }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: BTN2,
          onClick: () => onUpdateChartData({
            userShapes: withChartUserShapeAdded(shapes, createDefaultChartUserShape())
          }),
          children: t("pptx.chart.userShapeAddTextBox")
        }
      )
    ] }),
    rows.length === 0 ? (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: t("pptx.chart.userShapesEmpty") }) : (0, import_jsx_runtime.jsx)("div", { className: "space-y-2", children: rows.map((row) => (0, import_jsx_runtime.jsxs)(
      "div",
      {
        "data-chart-user-shape-path": PATH_KEY(row.path),
        style: { marginLeft: row.depth * 12 },
        className: "space-y-1 rounded border border-border p-1.5",
        children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
            (0, import_jsx_runtime.jsxs)("span", { className: "flex-1 truncate", children: [
              kindLabel(row.kind),
              row.text ? ` - ${row.text}` : ""
            ] }),
            row.isGroup ? (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: BTN2,
                onClick: () => addIntoGroup(row.path),
                children: t("pptx.chart.userShapeAddIntoGroup")
              }
            ) : null,
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                "aria-label": t("pptx.chart.userShapeDelete"),
                className: BTN2,
                onClick: () => onUpdateChartData({
                  userShapes: withChartUserShapeRowRemoved(shapes, row.path)
                }),
                children: "✕"
              }
            )
          ] }),
          row.editableVisuals ? (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeText") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                "aria-label": t("pptx.chart.userShapeText"),
                disabled: !canEdit,
                className: INPUT2,
                value: row.text ?? "",
                onChange: (e) => onUpdateChartData({
                  userShapes: withChartUserShapeRowTextUpdated(
                    shapes,
                    row.path,
                    e.target.value
                  )
                })
              }
            )
          ] }) : null,
          row.editableVisuals ? (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 text-[11px]", children: [
            (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
              (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeFill") }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "color",
                  "aria-label": t("pptx.chart.userShapeFill"),
                  disabled: !canEdit,
                  value: row.fill ?? "#ffffff",
                  onChange: (e) => update(row.path, { fill: e.target.value })
                }
              )
            ] }),
            (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
              (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeStroke") }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "color",
                  "aria-label": t("pptx.chart.userShapeStroke"),
                  disabled: !canEdit,
                  value: row.stroke ?? "#000000",
                  onChange: (e) => update(row.path, { stroke: e.target.value })
                }
              )
            ] })
          ] }) : null,
          row.editableAltText ? (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 text-[11px]", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.chart.userShapeAltText") }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                "aria-label": t("pptx.chart.userShapeAltText"),
                disabled: !canEdit,
                className: INPUT2,
                value: row.altText ?? "",
                onChange: (e) => update(row.path, { altText: e.target.value })
              }
            )
          ] }) : null,
          (0, import_jsx_runtime.jsx)(
            ChartUserShapePositionFields,
            {
              row,
              userShapes: shapes,
              canEdit,
              onPatch: update,
              onBoxPatch: updateBox,
              onRotationPatch: updateRotation,
              onFlipPatch: updateFlip
            }
          )
        ]
      },
      PATH_KEY(row.path)
    )) })
  ] });
}
function ChartDataPanel({ selectedElement, canEdit, onUpdateElement }) {
  const chartData = selectedElement.chartData;
  const followDataPoint = useViewerOptionsContext().advanced.chartPropertiesFollowDataPoint;
  const { selection: partSelection } = useChartPartSelection();
  const canvasPart = (partSelection == null ? void 0 : partSelection.elementId) === selectedElement.id ? partSelection.part : null;
  const title = chartData == null ? void 0 : chartData.title;
  const chartType = chartData == null ? void 0 : chartData.chartType;
  const categories = chartData == null ? void 0 : chartData.categories;
  const series = chartData == null ? void 0 : chartData.series;
  const style = chartData == null ? void 0 : chartData.style;
  const grouping = chartData == null ? void 0 : chartData.grouping;
  const replaceChartData = (0, import_react2.useCallback)(
    (newData) => {
      onUpdateElement({
        chartData: newData
      });
    },
    [onUpdateElement]
  );
  const updateChartData = (0, import_react2.useCallback)(
    (patch) => {
      if (!chartData) {
        return;
      }
      replaceChartData(patchChartData(chartData, patch));
    },
    [chartData, replaceChartData]
  );
  const updateTitle = (0, import_react2.useCallback)(
    (text) => {
      if (!chartData) {
        return;
      }
      updateChartData(collapseChartTitleRunsForEdit(chartData, text));
    },
    [chartData, updateChartData]
  );
  const updateStyle = (0, import_react2.useCallback)(
    (patch) => {
      if (!chartData) {
        return;
      }
      onUpdateElement({
        chartData: {
          ...chartData,
          style: { ...style, ...patch }
        }
      });
    },
    [chartData, style, onUpdateElement]
  );
  const hasGridlines = chartData ? chartGridlinesState(chartData) : false;
  const toggleGridlines = (0, import_react2.useCallback)(
    (show) => {
      if (!chartData) {
        return;
      }
      updateChartData(chartGridlinesPatch(chartData, show));
    },
    [chartData, updateChartData]
  );
  const toggleSecondaryAxis = (0, import_react2.useCallback)(
    (seriesIndex, useSecondary) => {
      if (!chartData) {
        return;
      }
      updateChartData(seriesSecondaryAxisPatch(chartData, seriesIndex, useSecondary));
    },
    [chartData, updateChartData]
  );
  const updateAxis = (0, import_react2.useCallback)(
    (axisType, patch) => {
      if (!chartData) {
        return;
      }
      const axes = chartData.axes ? [...chartData.axes] : [];
      const index = axes.findIndex((a) => a.axisType === axisType);
      if (index === -1) {
        axes.push({ axisType, ...patch });
      } else {
        axes[index] = { ...axes[index], ...patch };
      }
      updateChartData({ axes });
    },
    [chartData, updateChartData]
  );
  const updateSeries = (0, import_react2.useCallback)(
    (index, patch) => {
      if (!series) {
        return;
      }
      const updated = series.map((s, i) => i === index ? { ...s, ...patch } : s);
      updateChartData({ series: updated });
    },
    [series, updateChartData]
  );
  const setSeriesTrendline = (0, import_react2.useCallback)(
    (index, trendline) => {
      if (!series) {
        return;
      }
      const updated = series.map(
        (s, i) => i === index ? { ...s, trendlines: trendline ? [trendline] : [] } : s
      );
      updateChartData({ series: updated });
    },
    [series, updateChartData]
  );
  const setSeriesErrorBars = (0, import_react2.useCallback)(
    (index, errBars) => {
      if (!series) {
        return;
      }
      const updated = series.map(
        (s, i) => i === index ? { ...s, errBars: errBars ? [errBars] : [] } : s
      );
      updateChartData({ series: updated });
    },
    [series, updateChartData]
  );
  const setSeriesColor = (0, import_react2.useCallback)(
    (index, color) => {
      updateSeries(index, { color: color ?? void 0 });
    },
    [updateSeries]
  );
  const updateCategoryLabel = (0, import_react2.useCallback)(
    (catIndex, value) => {
      const next = chartData && setChartCategoryLabel(chartData, catIndex, value);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, replaceChartData]
  );
  const updateValue = (0, import_react2.useCallback)(
    (seriesIndex, catIndex, raw) => {
      const next = chartData && setChartCellValue(chartData, seriesIndex, catIndex, raw);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, replaceChartData]
  );
  const addCategory = (0, import_react2.useCallback)(() => {
    if (chartData) {
      replaceChartData(addChartCategory(chartData));
    }
  }, [chartData, replaceChartData]);
  const removeCategory = (0, import_react2.useCallback)(
    (catIndex) => {
      const next = chartData && removeChartCategory(chartData, catIndex, followDataPoint);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, followDataPoint, replaceChartData]
  );
  const addSeries = (0, import_react2.useCallback)(() => {
    if (chartData) {
      replaceChartData(addChartSeries(chartData));
    }
  }, [chartData, replaceChartData]);
  const removeSeries = (0, import_react2.useCallback)(
    (seriesIndex) => {
      const next = chartData && removeChartSeries(chartData, seriesIndex);
      if (next) {
        replaceChartData(next);
      }
    },
    [chartData, replaceChartData]
  );
  const applyChartOp = (0, import_react2.useCallback)(
    (mutate) => {
      if (!chartData) {
        return;
      }
      const clone = {
        ...selectedElement,
        chartData: structuredClone(chartData)
      };
      mutate(clone);
      replaceChartData(clone.chartData);
    },
    [chartData, selectedElement, replaceChartData]
  );
  const setAxisLogScale = (0, import_react2.useCallback)(
    (axisType, opts) => applyChartOp((el) => setChartAxisLogScale(el, axisType, opts)),
    [applyChartOp]
  );
  const setAxisTitleStyle = (0, import_react2.useCallback)(
    (axisType, edit) => applyChartOp((el) => setChartAxisTitleStyle(el, axisType, edit)),
    [applyChartOp]
  );
  const setGridlineStyle = (0, import_react2.useCallback)(
    (axisType, which, edit) => applyChartOp((el) => setChartAxisGridlineStyle(el, axisType, which, edit)),
    [applyChartOp]
  );
  const setSeriesMarker = (0, import_react2.useCallback)(
    (index, marker) => applyChartOp((el) => setChartSeriesMarker(el, index, marker)),
    [applyChartOp]
  );
  const setSeriesType = (0, import_react2.useCallback)(
    (index, seriesType) => applyChartOp((el) => setChartSeriesChartType(el, index, seriesType)),
    [applyChartOp]
  );
  const setPointFill = (0, import_react2.useCallback)(
    (seriesIndex, pointIndex, color) => applyChartOp((el) => setChartDataPointFill(el, seriesIndex, pointIndex, color)),
    [applyChartOp]
  );
  const setPointExplosion = (0, import_react2.useCallback)(
    (seriesIndex, pointIndex, explosion) => applyChartOp((el) => setChartDataPointExplosion(el, seriesIndex, pointIndex, explosion)),
    [applyChartOp]
  );
  const setPointMarker = (0, import_react2.useCallback)(
    (seriesIndex, pointIndex, marker) => applyChartOp((el) => setChartDataPointMarker(el, seriesIndex, pointIndex, marker)),
    [applyChartOp]
  );
  const setPointLabel = (0, import_react2.useCallback)(
    (seriesIndex, pointIndex, text) => applyChartOp(
      (el) => setChartDataPointLabel(el, seriesIndex, pointIndex, text !== null ? { text } : null)
    ),
    [applyChartOp]
  );
  if (!chartData || !categories || !series) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      ChartTypeSelector,
      {
        title,
        chartType,
        chartData,
        grouping,
        seriesCount: series.length,
        categoryCount: categories.length,
        canEdit,
        onUpdateChartData: updateChartData,
        onTitleChange: updateTitle
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartDisplayOptions,
      {
        style,
        canEdit,
        onUpdateStyle: updateStyle,
        hasGridlines,
        onToggleGridlines: toggleGridlines
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartSubtypeOptions,
      {
        chartData,
        canEdit,
        onUpdateChartData: updateChartData
      }
    ),
    (0, import_jsx_runtime.jsx)(ChartDataLabelOptions, { style, canEdit, onUpdateStyle: updateStyle }),
    (0, import_jsx_runtime.jsx)(ChartAxisOptions, { axes: chartData.axes, canEdit, onUpdateAxis: updateAxis }),
    (0, import_jsx_runtime.jsx)(
      ChartAxisStyleOptions,
      {
        axes: chartData.axes,
        canEdit,
        onSetLogScale: setAxisLogScale,
        onSetTitleStyle: setAxisTitleStyle,
        onSetGridlineStyle: setGridlineStyle
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartMarkerOptions,
      {
        chartType,
        series,
        canEdit,
        onSetMarker: setSeriesMarker
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartComboTypeOptions,
      {
        chartType,
        series,
        canEdit,
        onSetSeriesType: setSeriesType
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartDataPointOptions,
      {
        chartType,
        categories,
        series,
        canEdit,
        onSetPointFill: setPointFill,
        onSetPointExplosion: setPointExplosion,
        onSetPointLabel: setPointLabel
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartDataPointMarkerOptions,
      {
        chartType,
        categories,
        series,
        canEdit,
        onSetPointMarker: setPointMarker
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartTrendlineOptions,
      {
        chartType,
        series,
        canEdit,
        onSetTrendline: setSeriesTrendline
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartErrorBarOptions,
      {
        chartType,
        series,
        canEdit,
        onSetErrorBars: setSeriesErrorBars
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartSeriesColorOptions,
      {
        chartData,
        canEdit,
        onSetColor: setSeriesColor,
        onToggleSecondaryAxis: toggleSecondaryAxis
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartUserShapeOptions,
      {
        chartData,
        canEdit,
        onUpdateChartData: updateChartData
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ChartDataGrid,
      {
        categories,
        series,
        canEdit,
        highlightCell: canvasPart ? { seriesIndex: canvasPart.seriesIndex, pointIndex: canvasPart.pointIndex } : null,
        onUpdateSeries: updateSeries,
        onUpdateCategoryLabel: updateCategoryLabel,
        onUpdateValue: updateValue,
        onAddCategory: addCategory,
        onRemoveCategory: removeCategory,
        onAddSeries: addSeries,
        onRemoveSeries: removeSeries
      }
    )
  ] });
}
var SELECT_CLS2 = "bg-muted border border-border rounded px-2 py-1";
var BTN_CLS = "inline-flex items-center justify-center gap-1 rounded bg-muted hover:bg-accent px-2 py-1";
var END_LABEL_KEYS = {
  Start: {
    arrow: "pptx.connectorArrows.startArrow",
    width: "pptx.connectorArrows.startWidth",
    length: "pptx.connectorArrows.startLength"
  },
  End: {
    arrow: "pptx.connectorArrows.endArrow",
    width: "pptx.connectorArrows.endWidth",
    length: "pptx.connectorArrows.endLength"
  }
};
function ConnectorArrowsSection({
  selectedShapeStyle,
  canEdit = true,
  onUpdateShapeStyle,
  markDirty
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2", children: [
    ["Start", "End"].map((end) => {
      const key = end === "Start" ? "connectorStartArrow" : "connectorEndArrow";
      return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t(END_LABEL_KEYS[end].arrow) }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t(END_LABEL_KEYS[end].arrow),
            value: (selectedShapeStyle == null ? void 0 : selectedShapeStyle[key]) || "none",
            onChange: (e) => {
              onUpdateShapeStyle({
                [key]: e.target.value
              });
              markDirty == null ? void 0 : markDirty();
            },
            disabled: !canEdit,
            className: SELECT_CLS2,
            children: CONNECTOR_ARROW_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.i18nKey) }, o.value))
          }
        )
      ] }, end);
    }),
    ["Start", "End"].map((end) => {
      const widthKey = end === "Start" ? "connectorStartArrowWidth" : "connectorEndArrowWidth";
      const lengthKey = end === "Start" ? "connectorStartArrowLength" : "connectorEndArrowLength";
      return (0, import_jsx_runtime.jsxs)(import_react2.default.Fragment, { children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t(END_LABEL_KEYS[end].width) }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              "aria-label": t(END_LABEL_KEYS[end].width),
              value: (selectedShapeStyle == null ? void 0 : selectedShapeStyle[widthKey]) || "med",
              onChange: (e) => {
                onUpdateShapeStyle({
                  [widthKey]: e.target.value
                });
                markDirty == null ? void 0 : markDirty();
              },
              disabled: !canEdit,
              className: SELECT_CLS2,
              children: ARROW_SIZE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.i18nKey) }, o.value))
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t(END_LABEL_KEYS[end].length) }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              "aria-label": t(END_LABEL_KEYS[end].length),
              value: (selectedShapeStyle == null ? void 0 : selectedShapeStyle[lengthKey]) || "med",
              onChange: (e) => {
                onUpdateShapeStyle({
                  [lengthKey]: e.target.value
                });
                markDirty == null ? void 0 : markDirty();
              },
              disabled: !canEdit,
              className: SELECT_CLS2,
              children: ARROW_SIZE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.i18nKey) }, o.value))
            }
          )
        ] })
      ] }, `${end}-size`);
    })
  ] });
}
function ConnectorPanel({
  selectedElement,
  canEdit,
  onUpdateElementStyle
}) {
  if (selectedElement.type !== "connector") {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: "Connector" }),
    (0, import_jsx_runtime.jsx)(
      ConnectorArrowsSection,
      {
        selectedShapeStyle: selectedElement.shapeStyle,
        canEdit: canEdit && canInteractWithElement(selectedElement, "changeArrowheads"),
        onUpdateShapeStyle: onUpdateElementStyle
      }
    )
  ] });
}
function GroupInfoPanel({
  selectedElement
}) {
  const { t } = useTranslation();
  if (selectedElement.type !== "group") {
    return null;
  }
  const group = selectedElement;
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.elementType.group") }),
    (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: Array.isArray(group.children) ? t("pptx.group.childCount", { count: group.children.length }) : t("pptx.group.groupedElement") })
  ] });
}
function OlePropertiesPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  if (selectedElement.type !== "ole") {
    return null;
  }
  const ole = selectedElement;
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.ole.title") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 text-[11px]", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.ole.objectName") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            disabled: !canEdit,
            className: INPUT,
            value: ole.oleName ?? "",
            placeholder: t("pptx.ole.objectNamePlaceholder"),
            onChange: (e) => onUpdateElement(buildOleObjectNamePatch(e.target.value))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.ole.type") }),
        (0, import_jsx_runtime.jsx)("span", { className: "text-foreground truncate", children: getOleObjectTypeLabel(ole.oleObjectType) })
      ] }),
      ole.fileName && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.ole.fileName") }),
        (0, import_jsx_runtime.jsx)("span", { className: "text-foreground truncate", title: ole.fileName, children: ole.fileName })
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.ole.linkStatus") }),
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: cn(
              "inline-flex items-center rounded-full px-2 py-0.5 text-[10px] font-medium",
              ole.isLinked ? "bg-blue-500/20 text-blue-400" : "bg-green-500/20 text-green-400"
            ),
            children: ole.isLinked ? t("pptx.ole.linked") : t("pptx.ole.embedded")
          }
        )
      ] })
    ] })
  ] });
}
function TransformPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: "Transform" }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Rotation (°)" }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: INPUT,
            value: Math.round(selectedElement.rotation || 0),
            onChange: (e) => onUpdateElement({ rotation: Number(e.target.value) })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Opacity" }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            disabled: !canEdit,
            min: 0,
            max: 100,
            value: Math.round((selectedElement.opacity ?? 1) * 100),
            className: "accent-primary",
            onChange: (e) => onUpdateElement({ opacity: Number(e.target.value) / 100 })
          }
        )
      ] })
    ] })
  ] });
}
function LayerOrderButtons({
  canEdit,
  onMoveLayer
}) {
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1", children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: cn("flex-1", BTN),
        disabled: !canEdit,
        onClick: () => onMoveLayer("forward"),
        children: "↑ Forward"
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: cn("flex-1", BTN),
        disabled: !canEdit,
        onClick: () => onMoveLayer("backward"),
        children: "↓ Backward"
      }
    )
  ] });
}
function ArtisticEffectsGallery({
  imgSrc: imgSrc2,
  fx,
  canEdit,
  updateEffects
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1 text-[11px]", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.artisticEffects") }),
    (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-4 gap-1", children: ARTISTIC_EFFECTS.map(([effectName, tKey, cssFilter]) => {
      const isActive = effectName === "none" ? !(fx == null ? void 0 : fx.artisticEffect) : (fx == null ? void 0 : fx.artisticEffect) === effectName;
      return (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "flex flex-col items-center gap-0.5 rounded border p-0.5 hover:bg-accent/50",
            isActive ? "border-primary bg-primary/10" : "border-border"
          ),
          title: t(tKey),
          onClick: () => updateEffects({
            artisticEffect: effectName === "none" ? void 0 : effectName,
            ...effectName === "grayscale" ? { grayscale: void 0 } : {}
          }),
          children: [
            (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "w-10 h-7 rounded overflow-hidden bg-muted",
                style: {
                  backgroundImage: imgSrc2 ? `url(${imgSrc2})` : void 0,
                  backgroundSize: "cover",
                  backgroundPosition: "center",
                  filter: cssFilter || void 0
                }
              }
            ),
            (0, import_jsx_runtime.jsx)("span", { className: "text-[8px] text-muted-foreground truncate w-full text-center", children: t(tKey) })
          ]
        },
        effectName
      );
    }) })
  ] });
}
function ColorChangeSection({
  fx,
  canEdit,
  updateEffects
}) {
  const { t } = useTranslation();
  const cc = fx == null ? void 0 : fx.clrChange;
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1 text-[11px]", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.colorChange") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: Boolean(cc),
          onChange: (e) => updateEffects({
            clrChange: e.target.checked ? {
              clrFrom: "#ffffff",
              clrTo: "#000000",
              clrToTransparent: false
            } : void 0
          })
        }
      )
    ] }),
    cc && (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.colorChangeFrom") }),
        (0, import_jsx_runtime.jsx)(
          DebouncedColorInput,
          {
            disabled: !canEdit,
            value: normalizeHexColor2(cc.clrFrom, "#ffffff"),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => updateEffects({
              clrChange: {
                ...cc,
                clrFrom: hex,
                clrTo: cc.clrTo ?? "#000000"
              }
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.colorChangeTo") }),
        (0, import_jsx_runtime.jsx)(
          DebouncedColorInput,
          {
            disabled: !canEdit || Boolean(cc.clrToTransparent),
            value: normalizeHexColor2(cc.clrTo, "#000000"),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => updateEffects({
              clrChange: {
                ...cc,
                clrFrom: cc.clrFrom ?? "#ffffff",
                clrTo: hex
              }
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2 col-span-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.colorChangeTransparent") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(cc.clrToTransparent),
            onChange: (e) => updateEffects({
              clrChange: {
                clrFrom: cc.clrFrom ?? "#ffffff",
                clrTo: cc.clrTo ?? "#000000",
                clrToTransparent: e.target.checked
              }
            })
          }
        )
      ] })
    ] })
  ] });
}
function ColorWashSection({
  fx,
  canEdit,
  updateEffects
}) {
  const { t } = useTranslation();
  const wash = fx == null ? void 0 : fx.colorWash;
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1 text-[11px]", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.colorWash") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: Boolean(wash),
          onChange: (e) => updateEffects({
            colorWash: e.target.checked ? { color: "#0066cc", opacity: 40 } : void 0
          })
        }
      )
    ] }),
    wash && (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.washColor") }),
        (0, import_jsx_runtime.jsx)(
          DebouncedColorInput,
          {
            disabled: !canEdit,
            value: normalizeHexColor2(wash.color, "#0066cc"),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => updateEffects({
              colorWash: { color: hex, opacity: wash.opacity ?? 40 }
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.washOpacity") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            min: 0,
            max: 100,
            disabled: !canEdit,
            className: "accent-primary",
            value: wash.opacity,
            onChange: (e) => updateEffects({
              colorWash: {
                color: wash.color ?? "#0066cc",
                opacity: Number(e.target.value)
              }
            })
          }
        )
      ] })
    ] })
  ] });
}
function DebouncedColorInput2({
  value,
  disabled,
  className,
  onCommit
}) {
  const [local, setLocal] = (0, import_react2.useState)(value);
  const commitRef = (0, import_react2.useRef)(onCommit);
  commitRef.current = onCommit;
  const { pushColor } = useRecentColors();
  (0, import_react2.useEffect)(() => {
    setLocal(value);
  }, [value]);
  const handleChange = (0, import_react2.useCallback)((e) => {
    setLocal(e.target.value);
  }, []);
  const handleBlur = (0, import_react2.useCallback)(() => {
    commitRef.current(local);
    pushColor(local);
  }, [local, pushColor]);
  const inputRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    const el = inputRef.current;
    if (!el) {
      return;
    }
    const handler = () => {
      commitRef.current(el.value);
      pushColor(el.value);
    };
    el.addEventListener("change", handler);
    return () => el.removeEventListener("change", handler);
  }, [pushColor]);
  return (0, import_jsx_runtime.jsx)(
    "input",
    {
      ref: inputRef,
      type: "color",
      disabled,
      className,
      value: local,
      onChange: handleChange,
      onBlur: handleBlur
    }
  );
}
function DuotonePanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  var _a, _b2;
  const { t } = useTranslation();
  const effects = "imageEffects" in selectedElement ? selectedElement.imageEffects : void 0;
  const commitDuotone = (0, import_react2.useCallback)(
    (color1, color2) => {
      onUpdateElement({
        imageEffects: {
          ...effects,
          duotone: { color1, color2 }
        }
      });
    },
    [effects, onUpdateElement]
  );
  const clearDuotone = (0, import_react2.useCallback)(() => {
    onUpdateElement({
      imageEffects: {
        ...effects,
        duotone: void 0
      }
    });
  }, [effects, onUpdateElement]);
  const shadowColor = normalizeHexColor2((_a = effects == null ? void 0 : effects.duotone) == null ? void 0 : _a.color1, "#000000");
  const highlightColor = normalizeHexColor2((_b2 = effects == null ? void 0 : effects.duotone) == null ? void 0 : _b2.color2, "#ffffff");
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2 text-[11px]", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "text-muted-foreground font-medium", children: t("pptx.image.duotone") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.duotoneShadows") }),
        (0, import_jsx_runtime.jsx)(
          DebouncedColorInput2,
          {
            disabled: !canEdit,
            value: shadowColor,
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => commitDuotone(hex, highlightColor)
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.duotoneHighlights") }),
        (0, import_jsx_runtime.jsx)(
          DebouncedColorInput2,
          {
            disabled: !canEdit,
            value: highlightColor,
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer",
            onCommit: (hex) => commitDuotone(shadowColor, hex)
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-muted-foreground text-[10px]", children: t("pptx.image.duotonePresets") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-4 gap-1", children: DUOTONE_PRESETS.map((preset) => (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: "flex flex-col items-center gap-0.5 rounded border border-border hover:bg-accent p-1 transition-colors",
          title: t(preset.labelKey),
          onClick: () => commitDuotone(preset.shadow, preset.highlight),
          children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex h-4 w-full rounded overflow-hidden", children: [
              (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: preset.shadow } }),
              (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: preset.highlight } })
            ] }),
            (0, import_jsx_runtime.jsx)("span", { className: "text-[8px] text-muted-foreground truncate w-full text-center", children: t(preset.labelKey) })
          ]
        },
        preset.labelKey
      )) })
    ] }),
    (effects == null ? void 0 : effects.duotone) && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: "w-full rounded bg-muted hover:bg-accent border border-border px-2 py-1 text-foreground transition-colors",
        onClick: clearDuotone,
        children: t("pptx.image.duotoneClear")
      }
    )
  ] });
}
function ImageAdjustmentsPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const effects = (0, import_react2.useMemo)(
    () => "imageEffects" in selectedElement ? selectedElement.imageEffects : void 0,
    [selectedElement]
  );
  const alphaModFix = (effects == null ? void 0 : effects.alphaModFix) ?? 100;
  const transparency = 100 - alphaModFix;
  const biLevelThreshold = (effects == null ? void 0 : effects.biLevel) ?? 0;
  const hasValues = (effects == null ? void 0 : effects.alphaModFix) !== void 0 || (effects == null ? void 0 : effects.biLevel) !== void 0;
  const commitEffects = (0, import_react2.useCallback)(
    (patch) => {
      onUpdateElement({
        imageEffects: {
          ...effects,
          ...patch
        }
      });
    },
    [effects, onUpdateElement]
  );
  const handleTransparencyChange = (0, import_react2.useCallback)(
    (e) => {
      const val = Number(e.target.value);
      commitEffects({ alphaModFix: 100 - val });
    },
    [commitEffects]
  );
  const handleBiLevelChange = (0, import_react2.useCallback)(
    (e) => {
      commitEffects({ biLevel: Number(e.target.value) });
    },
    [commitEffects]
  );
  const handleReset = (0, import_react2.useCallback)(() => {
    commitEffects({ alphaModFix: void 0, biLevel: void 0 });
  }, [commitEffects]);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-3 text-[11px]", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground font-medium", children: t("pptx.imageAdjustments.title") }),
      hasValues && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: "rounded bg-muted hover:bg-accent border border-border px-1.5 py-0.5 text-[10px] text-foreground disabled:opacity-50",
          onClick: handleReset,
          children: t("pptx.common.reset")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.imageAdjustments.transparency") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            min: 0,
            max: 100,
            step: 1,
            disabled: !canEdit,
            className: "accent-primary flex-1",
            value: transparency,
            onChange: handleTransparencyChange
          }
        ),
        (0, import_jsx_runtime.jsxs)("span", { className: "w-8 text-right tabular-nums text-muted-foreground", children: [
          transparency,
          "%"
        ] })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.imageAdjustments.biLevelThreshold") }),
      (0, import_jsx_runtime.jsx)("p", { className: "text-[10px] text-muted-foreground/70", children: t("pptx.imageAdjustments.biLevelDescription") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            min: 0,
            max: 100,
            step: 1,
            disabled: !canEdit,
            className: "accent-primary flex-1",
            value: biLevelThreshold,
            onChange: handleBiLevelChange
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "w-8 text-right tabular-nums text-muted-foreground", children: biLevelThreshold })
      ] })
    ] })
  ] });
}
var CROP_SIDES = ["Left", "Top", "Right", "Bottom"];
var MAX_CROP_PERCENT = 80;
function ImageCropSection({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  if (!isImageLikeElement(selectedElement)) {
    return null;
  }
  const croppable = canEdit && canInteractWithElement(selectedElement, "crop");
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", "data-pptx-image-crop": true, children: [
    CROP_SIDES.map((side) => {
      const key = `crop${side}`;
      return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 text-[11px]", children: [
        (0, import_jsx_runtime.jsxs)("span", { className: "text-muted-foreground", children: [
          "Crop ",
          side
        ] }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            min: 0,
            max: MAX_CROP_PERCENT,
            disabled: !croppable,
            className: "accent-primary",
            value: Math.round(clampCropValue(selectedElement[key]) * 100),
            onChange: (e) => onUpdateElement({
              [key]: Number(e.target.value) / 100
            })
          }
        )
      ] }, side);
    }),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !croppable,
        className: `${BTN_CLS} w-full`,
        onClick: () => onUpdateElement({
          cropLeft: 0,
          cropTop: 0,
          cropRight: 0,
          cropBottom: 0
        }),
        children: t("pptx.image.resetCrop")
      }
    )
  ] });
}
function RangeSlider({
  label,
  disabled,
  value,
  onChange
}) {
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: label }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "range",
        min: -100,
        max: 100,
        disabled,
        className: "accent-primary",
        value,
        onChange: (e) => onChange(Number(e.target.value))
      }
    )
  ] });
}
function ImagePropertiesPanel({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const fx = selectedElement.imageEffects;
  const updateEffects = (patch) => {
    onUpdateElement({
      imageEffects: { ...fx, ...patch }
    });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.image.properties") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.image.altText") }),
        (0, import_jsx_runtime.jsx)(
          "textarea",
          {
            rows: 2,
            disabled: !canEdit,
            className: cn(INPUT, "resize-none text-[11px]"),
            value: selectedElement.altText ?? "",
            onChange: (e) => onUpdateElement({
              altText: e.target.value
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)(
          RangeSlider,
          {
            label: t("pptx.imageAdjustments.brightness"),
            disabled: !canEdit,
            value: (fx == null ? void 0 : fx.brightness) ?? 0,
            onChange: (v) => updateEffects({ brightness: v })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RangeSlider,
          {
            label: t("pptx.imageAdjustments.contrast"),
            disabled: !canEdit,
            value: (fx == null ? void 0 : fx.contrast) ?? 0,
            onChange: (v) => updateEffects({ contrast: v })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          RangeSlider,
          {
            label: t("pptx.image.saturation"),
            disabled: !canEdit,
            value: (fx == null ? void 0 : fx.saturation) ?? 0,
            onChange: (v) => updateEffects({ saturation: v })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        ImageCropSection,
        {
          selectedElement,
          canEdit,
          onUpdateElement
        }
      ),
      (0, import_jsx_runtime.jsx)(
        ImageAdjustmentsPanel,
        {
          selectedElement,
          canEdit,
          onUpdateElement
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Grayscale" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: Boolean(fx == null ? void 0 : fx.grayscale),
              onChange: (e) => updateEffects({ grayscale: e.target.checked })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)(
          DuotonePanel,
          {
            selectedElement,
            canEdit,
            onUpdateElement
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(ColorWashSection, { fx, canEdit, updateEffects }),
      (0, import_jsx_runtime.jsx)(ColorChangeSection, { fx, canEdit, updateEffects }),
      (0, import_jsx_runtime.jsx)(
        ArtisticEffectsGallery,
        {
          imgSrc: selectedElement.imageData,
          fx,
          canEdit,
          updateEffects
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: "w-full text-[11px] rounded bg-muted hover:bg-accent border border-border px-2 py-1 text-foreground",
          onClick: () => onUpdateElement({
            imageEffects: {
              brightness: 0,
              contrast: 0,
              saturation: 0,
              grayscale: false,
              artisticEffect: void 0,
              colorWash: void 0,
              alphaModFix: void 0,
              biLevel: void 0,
              duotone: void 0
            },
            cropShape: "none"
          }),
          children: t("pptx.image.resetImage")
        }
      )
    ] })
  ] });
}
var HEADING3 = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD3 = "rounded border border-border bg-card p-2 space-y-2";
var INPUT3 = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN3 = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var LABEL_CLS = "flex items-center justify-between gap-2";
var LABEL_TEXT = "text-muted-foreground";
function msToMmSs(ms) {
  if (!Number.isFinite(ms) || ms < 0) {
    return "00:00";
  }
  const totalSeconds = Math.round(ms / 1e3);
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`;
}
function mmSsToMs(value) {
  const trimmed = value.trim();
  if (trimmed.length === 0) {
    return void 0;
  }
  const colonIdx = trimmed.indexOf(":");
  if (colonIdx >= 0) {
    const minPart = trimmed.slice(0, colonIdx);
    const secPart = trimmed.slice(colonIdx + 1);
    const minutes = parseInt(minPart, 10);
    const seconds2 = parseInt(secPart, 10);
    if (!Number.isFinite(minutes) || !Number.isFinite(seconds2)) {
      return void 0;
    }
    if (minutes < 0 || seconds2 < 0 || seconds2 >= 60) {
      return void 0;
    }
    return (minutes * 60 + seconds2) * 1e3;
  }
  const seconds = parseInt(trimmed, 10);
  if (!Number.isFinite(seconds) || seconds < 0) {
    return void 0;
  }
  return seconds * 1e3;
}
var TRIM_ERROR_KEYS = {
  negative: "pptx.media.trimErrorNegative",
  startAfterEnd: "pptx.media.trimErrorStartAfterEnd",
  beyondDuration: "pptx.media.trimErrorBeyondDuration"
};
function validateTrimRange(trimStartMs, trimEndMs, durationMs) {
  const error = validateMediaTrimRange(trimStartMs, trimEndMs, durationMs);
  return error ? TRIM_ERROR_KEYS[error] : null;
}
function TrimTimeInput({
  label,
  valueMs,
  maxMs,
  disabled,
  onChange
}) {
  const [editValue, setEditValue] = (0, import_react2.useState)(null);
  const [hasError, setHasError] = (0, import_react2.useState)(false);
  const displayValue = editValue ?? msToMmSs(valueMs);
  const handleFocus = (0, import_react2.useCallback)(() => {
    setEditValue(msToMmSs(valueMs));
    setHasError(false);
  }, [valueMs]);
  const handleBlur = (0, import_react2.useCallback)(() => {
    if (editValue !== null) {
      const parsed = mmSsToMs(editValue);
      if (parsed !== void 0 && parsed >= 0 && (maxMs <= 0 || parsed <= maxMs)) {
        onChange(parsed);
        setHasError(false);
      } else {
        setHasError(false);
      }
    }
    setEditValue(null);
  }, [editValue, maxMs, onChange]);
  const handleChange = (0, import_react2.useCallback)((e) => {
    const val = e.target.value;
    setEditValue(val);
    const parsed = mmSsToMs(val);
    setHasError(parsed === void 0);
  }, []);
  const handleKeyDown = (0, import_react2.useCallback)((e) => {
    if (e.key === "Enter") {
      e.currentTarget.blur();
    } else if (e.key === "Escape") {
      setEditValue(null);
      setHasError(false);
    }
  }, []);
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
    (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT}`, children: label }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "text",
        disabled,
        className: `${INPUT3} text-[11px] tabular-nums ${hasError ? "border-red-400" : ""}`,
        value: displayValue,
        placeholder: "00:00",
        onFocus: handleFocus,
        onBlur: handleBlur,
        onChange: handleChange,
        onKeyDown: handleKeyDown
      }
    )
  ] });
}
function MediaInspector({
  element,
  canEdit,
  durationSeconds,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const trimStartMs = element.trimStartMs ?? 0;
  const trimEndMs = element.trimEndMs ?? 0;
  const durationMs = durationSeconds * 1e3;
  const trimmedDuration = (0, import_react2.useMemo)(
    () => msToMmSs(trimmedMediaDurationMs(trimStartMs, trimEndMs, durationMs)),
    [trimStartMs, trimEndMs, durationMs]
  );
  const trimEndAbsoluteMs = mediaTrimEndAbsoluteMs(durationMs, trimEndMs);
  const validationError = (0, import_react2.useMemo)(
    () => validateTrimRange(trimStartMs, trimEndMs, durationMs),
    [trimStartMs, trimEndMs, durationMs]
  );
  const hasTrim = trimStartMs > 0 || trimEndMs > 0;
  const handleTrimStartChange = (0, import_react2.useCallback)(
    (ms) => {
      onUpdateElement({
        trimStartMs: ms
      });
    },
    [onUpdateElement]
  );
  const handleTrimEndChange = (0, import_react2.useCallback)(
    (ms) => {
      onUpdateElement({
        trimEndMs: durationMs > 0 ? mediaTrimEndMsFromAbsoluteMs(durationMs, ms) : ms
      });
    },
    [durationMs, onUpdateElement]
  );
  const handleResetTrim = (0, import_react2.useCallback)(() => {
    onUpdateElement({
      trimStartMs: 0,
      trimEndMs: 0
    });
  }, [onUpdateElement]);
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD3, children: [
    (0, import_jsx_runtime.jsxs)("div", { className: HEADING3, children: [
      (0, import_jsx_runtime.jsx)(LuScissors, { className: "inline w-3 h-3 mr-1" }),
      t("pptx.media.trim")
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
      (0, import_jsx_runtime.jsx)(
        TrimTimeInput,
        {
          label: t("pptx.media.trimStartTime"),
          valueMs: trimStartMs,
          maxMs: durationMs,
          disabled: !canEdit,
          onChange: handleTrimStartChange
        }
      ),
      (0, import_jsx_runtime.jsx)(
        TrimTimeInput,
        {
          label: t("pptx.media.trimEndTime"),
          valueMs: trimEndAbsoluteMs,
          maxMs: durationMs,
          disabled: !canEdit,
          onChange: handleTrimEndChange
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: LABEL_CLS, children: [
      (0, import_jsx_runtime.jsxs)("span", { className: `text-[11px] ${LABEL_TEXT}`, children: [
        (0, import_jsx_runtime.jsx)(LuClock, { className: "inline w-3 h-3 mr-1" }),
        t("pptx.media.trimmedDuration")
      ] }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] tabular-nums font-medium", children: trimmedDuration })
    ] }),
    validationError && (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-red-400", children: t(validationError) }),
    canEdit && hasTrim && (0, import_jsx_runtime.jsx)("button", { type: "button", className: `${BTN3} w-full text-center`, onClick: handleResetTrim, children: t("pptx.media.resetTrim") })
  ] });
}
function formatTime2(seconds) {
  const m = Math.floor(seconds / 60);
  const s = Math.floor(seconds % 60);
  const ms = Math.round(seconds % 1 * 10);
  return `${m}:${String(s).padStart(2, "0")}.${ms}`;
}
function generateBookmarkId() {
  return `bmk-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`;
}
var HEADING4 = "text-[11px] uppercase tracking-wide text-muted-foreground";
var CARD4 = "rounded border border-border bg-card p-2 space-y-2";
var INPUT4 = "flex-1 bg-muted border border-border rounded px-1.5 py-0.5 w-full";
var BTN4 = "rounded bg-muted hover:bg-accent px-2 py-1 text-[11px] transition-colors";
var LABEL_CLS2 = "flex items-center justify-between gap-2";
var LABEL_TEXT2 = "text-muted-foreground";
function TrimTimeline({
  duration,
  trimStartMs,
  trimEndMs,
  currentTime,
  bookmarks,
  canEdit,
  onTrimChange,
  onSeek
}) {
  const barRef = (0, import_react2.useRef)(null);
  const [dragging, setDragging] = (0, import_react2.useState)(null);
  const trimStartSec = trimStartMs / 1e3;
  const trimEndSec = mediaTrimEndSeconds(duration, trimEndMs);
  const safeDuration = duration > 0 ? duration : 1;
  const startPct = trimStartSec / safeDuration * 100;
  const endPct = trimEndSec / safeDuration * 100;
  const playheadPct = currentTime / safeDuration * 100;
  const getTimeFromPointer = (0, import_react2.useCallback)(
    (clientX) => {
      const bar = barRef.current;
      if (!bar) {
        return 0;
      }
      const rect = bar.getBoundingClientRect();
      const ratio = Math.max(0, Math.min(1, (clientX - rect.left) / rect.width));
      return ratio * safeDuration;
    },
    [safeDuration]
  );
  (0, import_react2.useEffect)(() => {
    if (!dragging) {
      return;
    }
    const handlePointerMove = (e) => {
      const t = getTimeFromPointer(e.clientX);
      if (dragging === "start") {
        const newStart = Math.min(t, trimEndSec - 0.1);
        onTrimChange(Math.max(0, newStart) * 1e3, trimEndMs);
      } else {
        const newEnd = Math.max(t, trimStartSec + 0.1);
        onTrimChange(trimStartMs, mediaTrimEndMsFromSeconds(duration, newEnd));
      }
    };
    const handlePointerUp = () => {
      setDragging(null);
    };
    window.addEventListener("pointermove", handlePointerMove);
    window.addEventListener("pointerup", handlePointerUp);
    return () => {
      window.removeEventListener("pointermove", handlePointerMove);
      window.removeEventListener("pointerup", handlePointerUp);
    };
  }, [
    dragging,
    duration,
    getTimeFromPointer,
    onTrimChange,
    trimEndMs,
    trimEndSec,
    trimStartMs,
    trimStartSec
  ]);
  const handleBarClick = (e) => {
    const t = getTimeFromPointer(e.clientX);
    onSeek(t);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between text-[10px] text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)("span", { children: formatTime2(trimStartSec) }),
      (0, import_jsx_runtime.jsx)("span", { children: formatTime2(trimEndSec) })
    ] }),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        ref: barRef,
        className: "relative h-5 rounded bg-muted cursor-pointer select-none",
        onClick: handleBarClick,
        children: [
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute top-0 bottom-0 bg-primary/30 rounded",
              style: { left: `${startPct}%`, width: `${endPct - startPct}%` }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute top-0 bottom-0 w-0.5 bg-white z-10",
              style: { left: `${Math.min(playheadPct, 100)}%` }
            }
          ),
          canEdit && (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute top-0 bottom-0 w-2 bg-primary rounded-l cursor-ew-resize z-20 hover:bg-primary/80",
              style: { left: `calc(${startPct}% - 4px)` },
              onPointerDown: (e) => {
                e.stopPropagation();
                setDragging("start");
              }
            }
          ),
          canEdit && (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute top-0 bottom-0 w-2 bg-primary rounded-r cursor-ew-resize z-20 hover:bg-primary/80",
              style: { left: `calc(${endPct}% - 4px)` },
              onPointerDown: (e) => {
                e.stopPropagation();
                setDragging("end");
              }
            }
          ),
          bookmarks.map((bmk) => {
            const pct = bmk.time / safeDuration * 100;
            return (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute top-0 bottom-0 w-1 bg-yellow-400/70 z-10 cursor-pointer",
                style: { left: `${pct}%` },
                title: bmk.label,
                onClick: (e) => {
                  e.stopPropagation();
                  onSeek(bmk.time);
                }
              },
              bmk.id
            );
          })
        ]
      }
    )
  ] });
}
function MediaPlaybackBookmarks({
  element,
  canEdit,
  currentTime,
  bookmarks,
  onUpdateElement,
  onSeekTo
}) {
  const { t } = useTranslation();
  const [newBookmarkLabel, setNewBookmarkLabel] = (0, import_react2.useState)("");
  const volumePercent = Math.round((element.volume ?? 1) * 100);
  const handleAddBookmark = (0, import_react2.useCallback)(() => {
    const label = newBookmarkLabel.trim() || `${t("pptx.media.bookmark")} ${bookmarks.length + 1}`;
    const newBmk = {
      id: generateBookmarkId(),
      time: currentTime,
      label
    };
    onUpdateElement({
      bookmarks: [...bookmarks, newBmk]
    });
    setNewBookmarkLabel("");
  }, [bookmarks, currentTime, newBookmarkLabel, onUpdateElement, t]);
  const handleRemoveBookmark = (0, import_react2.useCallback)(
    (bmkId) => {
      onUpdateElement({
        bookmarks: bookmarks.filter((b) => b.id !== bmkId)
      });
    },
    [bookmarks, onUpdateElement]
  );
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: CARD4, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING4, children: t("pptx.media.playback") }),
      (0, import_jsx_runtime.jsxs)("label", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.volume") }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 flex-1 max-w-[140px]", children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "range",
              min: 0,
              max: 100,
              step: 1,
              disabled: !canEdit,
              className: "flex-1",
              value: volumePercent,
              onChange: (e) => onUpdateElement({
                volume: Number(e.target.value) / 100
              })
            }
          ),
          (0, import_jsx_runtime.jsxs)("span", { className: "text-[10px] text-muted-foreground w-7 text-right tabular-nums", children: [
            volumePercent,
            "%"
          ] })
        ] })
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.speed") }),
        (0, import_jsx_runtime.jsxs)(
          "select",
          {
            "aria-label": t("pptx.media.speed"),
            disabled: !canEdit,
            className: `${INPUT4} text-[11px] max-w-[100px]`,
            value: element.playbackSpeed ?? 1,
            onChange: (e) => onUpdateElement({
              playbackSpeed: Number(e.target.value)
            }),
            children: [
              (0, import_jsx_runtime.jsx)("option", { value: 0.25, children: "0.25x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 0.5, children: "0.5x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 0.75, children: "0.75x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 1, children: "1x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 1.25, children: "1.25x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 1.5, children: "1.5x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 2, children: "2x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 3, children: "3x" }),
              (0, import_jsx_runtime.jsx)("option", { value: 4, children: "4x" })
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
          (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.fadeIn") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              step: 0.1,
              disabled: !canEdit,
              className: `${INPUT4} text-[11px]`,
              value: element.fadeInDuration ?? 0,
              onChange: (e) => onUpdateElement({
                fadeInDuration: Number(e.target.value) || void 0
              })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
          (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.fadeOut") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              step: 0.1,
              disabled: !canEdit,
              className: `${INPUT4} text-[11px]`,
              value: element.fadeOutDuration ?? 0,
              onChange: (e) => onUpdateElement({
                fadeOutDuration: Number(e.target.value) || void 0
              })
            }
          )
        ] })
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.loop") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.loop),
            onChange: (e) => onUpdateElement({
              loop: e.target.checked || void 0
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.startTrigger") }),
        (0, import_jsx_runtime.jsxs)(
          "select",
          {
            "aria-label": t("pptx.media.startTrigger"),
            disabled: !canEdit,
            className: "text-[11px] bg-transparent border border-border rounded px-1 py-0.5",
            value: element.autoPlay ? "auto" : "onClick",
            onChange: (e) => onUpdateElement({
              autoPlay: e.target.value === "auto" || void 0
            }),
            children: [
              (0, import_jsx_runtime.jsx)("option", { value: "onClick", children: t("pptx.media.startOnClick") }),
              (0, import_jsx_runtime.jsx)("option", { value: "auto", children: t("pptx.media.startAutomatically") })
            ]
          }
        )
      ] }),
      element.mediaType === "audio" && (0, import_jsx_runtime.jsxs)("label", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.playAcrossSlides") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.playAcrossSlides),
            onChange: (e) => onUpdateElement({
              playAcrossSlides: e.target.checked || void 0,
              ...e.target.checked ? { autoPlay: true } : {}
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.fullScreen") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.fullScreen),
            onChange: (e) => onUpdateElement({
              fullScreen: e.target.checked || void 0
            })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.hideWhenNotPlaying") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(element.hideWhenNotPlaying),
            onChange: (e) => onUpdateElement({
              hideWhenNotPlaying: e.target.checked || void 0
            })
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: CARD4, children: [
      (0, import_jsx_runtime.jsxs)("div", { className: HEADING4, children: [
        (0, import_jsx_runtime.jsx)(LuBookmark, { className: "inline w-3 h-3 mr-1" }),
        t("pptx.media.bookmarks")
      ] }),
      bookmarks.length > 0 && (0, import_jsx_runtime.jsx)("div", { className: "space-y-1 max-h-32 overflow-y-auto", children: bookmarks.slice().sort((a, b) => a.time - b.time).map((bmk) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 text-[11px] group", children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "text-primary hover:text-primary/80 truncate flex-1 text-left",
            onClick: () => onSeekTo(bmk.time),
            title: t("pptx.media.seekToBookmark"),
            children: bmk.label
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground tabular-nums text-[10px]", children: formatTime2(bmk.time) }),
        canEdit && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "opacity-0 group-hover:opacity-100 text-red-400 hover:text-red-300 transition-opacity",
            onClick: () => handleRemoveBookmark(bmk.id),
            title: t("pptx.common.remove"),
            children: (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-3 h-3" })
          }
        )
      ] }, bmk.id)) }),
      canEdit && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            className: `${INPUT4} text-[11px]`,
            placeholder: t("pptx.media.bookmarkLabel"),
            value: newBookmarkLabel,
            onChange: (e) => setNewBookmarkLabel(e.target.value),
            onKeyDown: (e) => {
              if (e.key === "Enter") {
                e.preventDefault();
                handleAddBookmark();
              }
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN4,
            onClick: handleAddBookmark,
            title: t("pptx.media.addBookmark"),
            children: (0, import_jsx_runtime.jsx)(LuPlus, { className: "w-3 h-3" })
          }
        )
      ] })
    ] })
  ] });
}
function MediaPropertiesPanel({
  element,
  mediaDataUrls,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const mediaRef = (0, import_react2.useRef)(null);
  const [isPlaying, setIsPlaying] = (0, import_react2.useState)(false);
  const [currentTime, setCurrentTime] = (0, import_react2.useState)(0);
  const [duration, setDuration] = (0, import_react2.useState)(0);
  const dataUrl = element.mediaData ?? (element.mediaPath ? mediaDataUrls.get(element.mediaPath) : void 0);
  const bookmarks = element.bookmarks ?? [];
  const isVideo = element.mediaType === "video";
  (0, import_react2.useEffect)(() => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    const onTimeUpdate = () => setCurrentTime(el.currentTime);
    const onDurationChange = () => {
      if (Number.isFinite(el.duration)) {
        setDuration(el.duration);
      }
    };
    const onPlay = () => setIsPlaying(true);
    const onPause = () => setIsPlaying(false);
    const onEnded = () => setIsPlaying(false);
    el.addEventListener("timeupdate", onTimeUpdate);
    el.addEventListener("durationchange", onDurationChange);
    el.addEventListener("loadedmetadata", onDurationChange);
    el.addEventListener("play", onPlay);
    el.addEventListener("pause", onPause);
    el.addEventListener("ended", onEnded);
    return () => {
      el.removeEventListener("timeupdate", onTimeUpdate);
      el.removeEventListener("durationchange", onDurationChange);
      el.removeEventListener("loadedmetadata", onDurationChange);
      el.removeEventListener("play", onPlay);
      el.removeEventListener("pause", onPause);
      el.removeEventListener("ended", onEnded);
    };
  }, [dataUrl]);
  const togglePlay = (0, import_react2.useCallback)(() => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    if (el.paused) {
      void el.play();
    } else {
      el.pause();
    }
  }, []);
  const seekTo = (0, import_react2.useCallback)((time) => {
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    el.currentTime = time;
    setCurrentTime(time);
  }, []);
  const handleTrimChange = (0, import_react2.useCallback)(
    (trimStartMs, trimEndMs) => {
      onUpdateElement({ trimStartMs, trimEndMs });
    },
    [onUpdateElement]
  );
  const [extractedMetadata, setExtractedMetadata] = (0, import_react2.useState)(
    element.metadata
  );
  (0, import_react2.useEffect)(() => {
    var _a;
    const el = mediaRef.current;
    if (!el) {
      return;
    }
    if (((_a = element.metadata) == null ? void 0 : _a.duration) !== void 0) {
      setExtractedMetadata(element.metadata);
      return;
    }
    const extract = () => {
      if (!Number.isFinite(el.duration) || el.duration === 0) {
        return;
      }
      const meta = { duration: el.duration };
      if (el instanceof HTMLVideoElement) {
        if (el.videoWidth > 0) {
          meta.videoWidth = el.videoWidth;
        }
        if (el.videoHeight > 0) {
          meta.videoHeight = el.videoHeight;
        }
      }
      const sourceEl = el.querySelector("source");
      if (sourceEl == null ? void 0 : sourceEl.type) {
        meta.codecInfo = sourceEl.type;
      }
      element.metadata = meta;
      setExtractedMetadata(meta);
    };
    el.addEventListener("loadedmetadata", extract);
    if (el.readyState >= 1) {
      extract();
    }
    return () => {
      el.removeEventListener("loadedmetadata", extract);
    };
  }, [dataUrl, element]);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-3", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: CARD4, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING4, children: t("pptx.media.title") }),
      (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: isVideo ? t("pptx.media.videoClip") : t("pptx.media.audioClip") }),
      dataUrl && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        isVideo ? (0, import_jsx_runtime.jsx)(
          "video",
          {
            ref: mediaRef,
            className: "w-full rounded bg-black max-h-32 object-contain",
            src: dataUrl,
            preload: "metadata"
          }
        ) : (0, import_jsx_runtime.jsx)(
          "audio",
          {
            ref: mediaRef,
            className: "w-full",
            src: dataUrl,
            preload: "metadata"
          }
        ),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: BTN4,
              onClick: togglePlay,
              title: isPlaying ? t("pptx.media.pause") : t("pptx.media.play"),
              children: isPlaying ? (0, import_jsx_runtime.jsx)(LuPause, { className: "w-3 h-3" }) : (0, import_jsx_runtime.jsx)(LuPlay, { className: "w-3 h-3" })
            }
          ),
          (0, import_jsx_runtime.jsxs)("span", { className: "text-[10px] text-muted-foreground tabular-nums", children: [
            formatTime2(currentTime),
            " / ",
            formatTime2(duration)
          ] })
        ] })
      ] }),
      duration > 0 && (0, import_jsx_runtime.jsx)(
        TrimTimeline,
        {
          duration,
          trimStartMs: element.trimStartMs ?? 0,
          trimEndMs: element.trimEndMs ?? 0,
          currentTime,
          bookmarks,
          canEdit,
          onTrimChange: handleTrimChange,
          onSeek: seekTo
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      MediaInspector,
      {
        element,
        canEdit,
        durationSeconds: duration,
        onUpdateElement
      }
    ),
    (extractedMetadata || element.posterFrameData) && (0, import_jsx_runtime.jsxs)("div", { className: CARD4, children: [
      (0, import_jsx_runtime.jsxs)("div", { className: HEADING4, children: [
        (0, import_jsx_runtime.jsx)(LuInfo, { className: "inline w-3 h-3 mr-1" }),
        t("pptx.media.info")
      ] }),
      element.posterFrameData && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsx)("div", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.posterFrame") }),
        (0, import_jsx_runtime.jsx)(
          "img",
          {
            src: element.posterFrameData,
            alt: t("pptx.media.posterFrame"),
            className: "w-full max-h-20 object-contain rounded bg-black/20"
          }
        )
      ] }),
      (extractedMetadata == null ? void 0 : extractedMetadata.duration) !== void 0 && (0, import_jsx_runtime.jsxs)("div", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.duration") }),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] tabular-nums", children: formatTime2(extractedMetadata.duration) })
      ] }),
      (extractedMetadata == null ? void 0 : extractedMetadata.videoWidth) !== void 0 && (extractedMetadata == null ? void 0 : extractedMetadata.videoHeight) !== void 0 && (0, import_jsx_runtime.jsxs)("div", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.resolution") }),
        (0, import_jsx_runtime.jsxs)("span", { className: "text-[11px] tabular-nums", children: [
          extractedMetadata.videoWidth,
          " x ",
          extractedMetadata.videoHeight
        ] })
      ] }),
      (extractedMetadata == null ? void 0 : extractedMetadata.codecInfo) && (0, import_jsx_runtime.jsxs)("div", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.format") }),
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: "text-[11px] truncate max-w-[140px]",
            title: extractedMetadata.codecInfo,
            children: extractedMetadata.codecInfo
          }
        )
      ] }),
      element.mediaPath && (0, import_jsx_runtime.jsxs)("div", { className: LABEL_CLS2, children: [
        (0, import_jsx_runtime.jsx)("span", { className: `text-[11px] ${LABEL_TEXT2}`, children: t("pptx.media.filePath") }),
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: "text-[10px] truncate max-w-[140px] text-muted-foreground",
            title: element.mediaPath,
            children: element.mediaPath.split("/").pop()
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)(
      MediaPlaybackBookmarks,
      {
        element,
        canEdit,
        currentTime,
        bookmarks,
        onUpdateElement,
        onSeekTo: seekTo
      }
    )
  ] });
}
function AccessibilityTextSection({
  selectedElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const fields = getNonVisualDescriptionFields(selectedElement);
  if (!fields.showAltText && !fields.showTitle) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, "data-pptx-accessibility-text": true, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.accessibility.heading", "Accessibility") }),
    fields.showAltText && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.elementAccessibility.altText") }),
      (0, import_jsx_runtime.jsx)(
        "textarea",
        {
          rows: 2,
          disabled: !canEdit,
          placeholder: t("pptx.elementAccessibility.altTextPlaceholder"),
          className: cn(INPUT, "resize-none text-[11px]"),
          value: fields.altText,
          onChange: (e) => onUpdateElement({ altText: e.target.value })
        }
      )
    ] }),
    fields.showTitle && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.elementAccessibility.title") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          disabled: !canEdit,
          placeholder: t("pptx.elementAccessibility.titlePlaceholder"),
          className: cn(INPUT, "text-[11px]"),
          value: fields.title,
          onChange: (e) => onUpdateElement({ title: e.target.value })
        }
      )
    ] })
  ] });
}
var COMPOUND_LINE_OPTIONS2 = COMPOUND_LINE_OPTIONS;
var LINE_JOIN_OPTIONS2 = LINE_JOIN_OPTIONS;
var LINE_CAP_OPTIONS2 = LINE_CAP_OPTIONS;
var PATTERN_PRESET_OPTIONS2 = PATTERN_PRESET_OPTIONS;
var FILL_MODE_OPTIONS2 = [
  { value: "solid", label: "Solid", i18nKey: "pptx.fill.solid" },
  { value: "gradient", label: "Gradient", i18nKey: "pptx.fill.gradient" },
  { value: "pattern", label: "Pattern", i18nKey: "pptx.table.fillPattern" },
  { value: "image", label: "Image", i18nKey: "pptx.inspector.image" },
  { value: "none", label: "None", i18nKey: "pptx.fill.none" }
];
var GRADIENT_TYPE_OPTIONS2 = [
  { value: "linear", label: "Linear", i18nKey: "pptx.gradient.linear" },
  { value: "radial", label: "Radial", i18nKey: "pptx.gradient.radial" }
];
var IMAGE_MODE_OPTIONS = [
  { value: "stretch", label: "Stretch", i18nKey: "pptx.image.stretch" },
  { value: "tile", label: "Tile", i18nKey: "pptx.image.tile" }
];
function getCompoundLinePreviewStyle(type) {
  const baseColor = "#6b7280";
  switch (type) {
    case "sng":
      return {
        borderTop: `2px solid ${baseColor}`,
        width: "100%"
      };
    case "dbl": {
      const lineWidth = 2;
      const gap = 2;
      return {
        position: "relative",
        height: `${lineWidth * 2 + gap}px`,
        width: "100%",
        boxShadow: `inset 0 ${lineWidth + gap}px 0 ${-lineWidth}px ${baseColor}, inset 0 ${-4}px 0 ${-lineWidth}px ${baseColor}`
      };
    }
    case "thickThin": {
      const thickWidth = 3;
      const thinWidth = 1;
      const gap = 1;
      return {
        position: "relative",
        height: `${thickWidth + thinWidth + gap}px`,
        width: "100%",
        boxShadow: `inset 0 ${thickWidth / 2 + gap}px 0 ${-thickWidth}px ${baseColor}, inset 0 ${-3.5}px 0 ${-thinWidth}px ${baseColor}`
      };
    }
    case "thinThick": {
      const thinWidth = 1;
      const thickWidth = 3;
      const gap = 1;
      return {
        position: "relative",
        height: `${thinWidth + thickWidth + gap}px`,
        width: "100%",
        boxShadow: `inset 0 ${thickWidth / 2 + gap}px 0 ${-thinWidth}px ${baseColor}, inset 0 ${-3.5}px 0 ${-thickWidth}px ${baseColor}`
      };
    }
    case "tri": {
      const lineWidth = 1;
      const gap = 1;
      const offset1 = lineWidth + gap;
      return {
        position: "relative",
        height: `${lineWidth * 3 + gap * 2}px`,
        width: "100%",
        boxShadow: `inset 0 0 0 ${-lineWidth}px ${baseColor}, inset 0 ${offset1}px 0 ${-lineWidth}px ${baseColor}, inset 0 ${-4}px 0 ${-lineWidth}px ${baseColor}`
      };
    }
    default:
      return {};
  }
}
var BEVEL_TYPE_OPTIONS = [
  { value: "circle", label: "Circle", i18nKey: "pptx.bevelTypes.circle" },
  { value: "relaxedInset", label: "Relaxed Inset", i18nKey: "pptx.bevelTypes.relaxedInset" },
  { value: "cross", label: "Cross", i18nKey: "pptx.bevelTypes.cross" },
  { value: "slope", label: "Slope", i18nKey: "pptx.bevelTypes.slope" },
  { value: "convex", label: "Convex", i18nKey: "pptx.bevelTypes.convex" },
  { value: "coolSlant", label: "Cool Slant", i18nKey: "pptx.bevelTypes.coolSlant" },
  { value: "angle", label: "Angle", i18nKey: "pptx.bevelTypes.angle" },
  { value: "softRound", label: "Soft Round", i18nKey: "pptx.bevelTypes.softRound" },
  { value: "riblet", label: "Riblet", i18nKey: "pptx.bevelTypes.riblet" },
  { value: "hardEdge", label: "Hard Edge", i18nKey: "pptx.bevelTypes.hardEdge" },
  { value: "artDeco", label: "Art Deco", i18nKey: "pptx.bevelTypes.artDeco" },
  { value: "divot", label: "Divot", i18nKey: "pptx.bevelTypes.divot" }
];
var clamp = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
var SHADOW_EFFECT_CONFIGS = [
  /* Shadow */
  {
    label: "Shadow",
    isOn: (s) => Boolean(s == null ? void 0 : s.shadowColor) && (s == null ? void 0 : s.shadowColor) !== "transparent",
    onEnable: (s) => {
      let angle = (s == null ? void 0 : s.shadowAngle) ?? 315;
      let distance = (s == null ? void 0 : s.shadowDistance) ?? 5.66;
      const offsetX = (s == null ? void 0 : s.shadowOffsetX) ?? 4;
      const offsetY = (s == null ? void 0 : s.shadowOffsetY) ?? 4;
      if ((s == null ? void 0 : s.shadowAngle) === null && (s == null ? void 0 : s.shadowDistance) === null && (offsetX !== 0 || offsetY !== 0)) {
        distance = Math.sqrt(offsetX * offsetX + offsetY * offsetY);
        angle = (Math.atan2(offsetY, offsetX) * 180 / Math.PI + 360) % 360;
      }
      return {
        shadowColor: normalizeHexColor2(s == null ? void 0 : s.shadowColor, "#000000"),
        shadowOpacity: (s == null ? void 0 : s.shadowOpacity) ?? 0.35,
        shadowBlur: (s == null ? void 0 : s.shadowBlur) ?? 6,
        shadowAngle: angle,
        shadowDistance: distance,
        shadowOffsetX: offsetX,
        shadowOffsetY: offsetY,
        shadowRotateWithShape: (s == null ? void 0 : s.shadowRotateWithShape) ?? true
      };
    },
    onDisable: () => ({ shadowColor: "transparent" }),
    fields: [
      {
        key: "sc",
        label: "Shadow Color",
        type: "color",
        read: (s) => normalizeHexColor2(s == null ? void 0 : s.shadowColor, "#000000"),
        write: (v) => ({ shadowColor: String(v) })
      },
      {
        key: "so",
        label: "Shadow Opacity",
        type: "range",
        min: 0,
        max: 100,
        isPercent: true,
        read: (s) => Math.round(((s == null ? void 0 : s.shadowOpacity) ?? 0.35) * 100),
        write: (v) => ({ shadowOpacity: Number(v) / 100 })
      },
      {
        key: "sb",
        label: "Shadow Blur",
        type: "number",
        min: 0,
        max: 96,
        read: (s) => Math.round((s == null ? void 0 : s.shadowBlur) ?? 6),
        write: (v) => ({ shadowBlur: clamp(Number(v), 0, 96) })
      },
      {
        key: "sa",
        label: "Shadow Angle",
        type: "number",
        min: 0,
        max: 359,
        read: (s) => {
          if (typeof (s == null ? void 0 : s.shadowAngle) === "number") {
            return Math.round(s.shadowAngle) % 360;
          }
          const x = (s == null ? void 0 : s.shadowOffsetX) ?? 4;
          const y = (s == null ? void 0 : s.shadowOffsetY) ?? 4;
          return Math.round((Math.atan2(y, x) * 180 / Math.PI + 360) % 360);
        },
        write: (v) => {
          const angle = Number(v) % 360;
          return (s) => {
            const dist = typeof (s == null ? void 0 : s.shadowDistance) === "number" ? s.shadowDistance : 5.66;
            const angleRad = angle * Math.PI / 180;
            return {
              shadowAngle: angle,
              shadowDistance: dist,
              shadowOffsetX: Math.cos(angleRad) * dist,
              shadowOffsetY: Math.sin(angleRad) * dist
            };
          };
        }
      },
      {
        key: "sd",
        label: "Shadow Distance",
        type: "number",
        min: 0,
        max: 100,
        read: (s) => {
          if (typeof (s == null ? void 0 : s.shadowDistance) === "number") {
            return Math.round(s.shadowDistance * 10) / 10;
          }
          const x = (s == null ? void 0 : s.shadowOffsetX) ?? 4;
          const y = (s == null ? void 0 : s.shadowOffsetY) ?? 4;
          return Math.round(Math.sqrt(x * x + y * y) * 10) / 10;
        },
        write: (v) => {
          return (s) => {
            const distance = clamp(Number(v), 0, 100);
            const angle = typeof (s == null ? void 0 : s.shadowAngle) === "number" ? s.shadowAngle : 315;
            const angleRad = angle * Math.PI / 180;
            return {
              shadowAngle: angle,
              shadowDistance: distance,
              shadowOffsetX: Math.cos(angleRad) * distance,
              shadowOffsetY: Math.sin(angleRad) * distance
            };
          };
        }
      },
      {
        key: "sr",
        label: "Rotate with Shape",
        i18nKey: "pptx.effects.rotateWithShape",
        type: "checkbox",
        read: (s) => (s == null ? void 0 : s.shadowRotateWithShape) ?? true,
        write: (v) => ({ shadowRotateWithShape: Boolean(v) })
      },
      {
        key: "sx",
        label: "Shadow X",
        type: "number",
        min: -96,
        max: 96,
        read: (s) => Math.round((s == null ? void 0 : s.shadowOffsetX) ?? 4),
        write: (v) => {
          return (s) => {
            const offsetX = clamp(Number(v), -96, 96);
            const offsetY = (s == null ? void 0 : s.shadowOffsetY) ?? 4;
            const distance = Math.sqrt(offsetX * offsetX + offsetY * offsetY);
            const angle = (Math.atan2(offsetY, offsetX) * 180 / Math.PI + 360) % 360;
            return {
              shadowOffsetX: offsetX,
              shadowAngle: angle,
              shadowDistance: distance
            };
          };
        }
      },
      {
        key: "sy",
        label: "Shadow Y",
        type: "number",
        min: -96,
        max: 96,
        span2: true,
        read: (s) => Math.round((s == null ? void 0 : s.shadowOffsetY) ?? 4),
        write: (v) => {
          return (s) => {
            const offsetY = clamp(Number(v), -96, 96);
            const offsetX = (s == null ? void 0 : s.shadowOffsetX) ?? 4;
            const distance = Math.sqrt(offsetX * offsetX + offsetY * offsetY);
            const angle = (Math.atan2(offsetY, offsetX) * 180 / Math.PI + 360) % 360;
            return {
              shadowOffsetY: offsetY,
              shadowAngle: angle,
              shadowDistance: distance
            };
          };
        }
      }
    ]
  },
  /* Inner Shadow */
  {
    label: "Inner Shadow",
    isOn: (s) => Boolean(s == null ? void 0 : s.innerShadowColor) && (s == null ? void 0 : s.innerShadowColor) !== "transparent",
    onEnable: (s) => ({
      innerShadowColor: normalizeHexColor2(s == null ? void 0 : s.innerShadowColor, "#000000"),
      innerShadowOpacity: (s == null ? void 0 : s.innerShadowOpacity) ?? 0.5,
      innerShadowBlur: (s == null ? void 0 : s.innerShadowBlur) ?? 5,
      innerShadowOffsetX: (s == null ? void 0 : s.innerShadowOffsetX) ?? 0,
      innerShadowOffsetY: (s == null ? void 0 : s.innerShadowOffsetY) ?? 0
    }),
    onDisable: () => ({ innerShadowColor: "transparent" }),
    fields: [
      {
        key: "isc",
        label: "Color",
        type: "color",
        read: (s) => normalizeHexColor2(s == null ? void 0 : s.innerShadowColor, "#000000"),
        write: (v) => ({ innerShadowColor: String(v) })
      },
      {
        key: "iso",
        label: "Opacity",
        type: "range",
        min: 0,
        max: 100,
        isPercent: true,
        read: (s) => Math.round(((s == null ? void 0 : s.innerShadowOpacity) ?? 0.5) * 100),
        write: (v) => ({ innerShadowOpacity: Number(v) / 100 })
      },
      {
        key: "isb",
        label: "Blur",
        type: "number",
        min: 0,
        max: 96,
        read: (s) => Math.round((s == null ? void 0 : s.innerShadowBlur) ?? 5),
        write: (v) => ({ innerShadowBlur: clamp(Number(v), 0, 96) })
      },
      {
        key: "isx",
        label: "Offset X",
        type: "number",
        min: -96,
        max: 96,
        read: (s) => Math.round((s == null ? void 0 : s.innerShadowOffsetX) ?? 0),
        write: (v) => ({
          innerShadowOffsetX: clamp(Number(v), -96, 96)
        })
      },
      {
        key: "isy",
        label: "Offset Y",
        type: "number",
        min: -96,
        max: 96,
        span2: true,
        read: (s) => Math.round((s == null ? void 0 : s.innerShadowOffsetY) ?? 0),
        write: (v) => ({
          innerShadowOffsetY: clamp(Number(v), -96, 96)
        })
      }
    ]
  }
];
var clamp2 = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
var VISUAL_EFFECT_CONFIGS = [
  /* Glow */
  {
    label: "Glow",
    isOn: (s) => Boolean(s == null ? void 0 : s.glowColor) && (s == null ? void 0 : s.glowColor) !== "transparent",
    onEnable: (s) => ({
      glowColor: (s == null ? void 0 : s.glowColor) && s.glowColor !== "transparent" ? s.glowColor : "#ffff00",
      glowOpacity: (s == null ? void 0 : s.glowOpacity) ?? 0.75,
      glowRadius: (s == null ? void 0 : s.glowRadius) ?? 6
    }),
    onDisable: () => ({ glowColor: "transparent", glowRadius: 0 }),
    fields: [
      {
        key: "gc",
        label: "Glow Color",
        type: "color",
        read: (s) => (s == null ? void 0 : s.glowColor) && s.glowColor !== "transparent" ? s.glowColor : "#ffff00",
        write: (v) => ({ glowColor: String(v) })
      },
      {
        key: "go",
        label: "Glow Opacity",
        type: "range",
        min: 0,
        max: 100,
        isPercent: true,
        read: (s) => Math.round(((s == null ? void 0 : s.glowOpacity) ?? 0.75) * 100),
        write: (v) => ({ glowOpacity: Number(v) / 100 })
      },
      {
        key: "gr",
        label: "Glow Radius",
        type: "number",
        min: 0,
        max: 96,
        span2: true,
        read: (s) => Math.round((s == null ? void 0 : s.glowRadius) ?? 6),
        write: (v) => ({ glowRadius: clamp2(Number(v), 0, 96) })
      }
    ]
  },
  /* Soft Edge */
  {
    label: "Soft Edge",
    i18nKey: "pptx.inspector.softEdge",
    isOn: (s) => typeof (s == null ? void 0 : s.softEdgeRadius) === "number" && s.softEdgeRadius > 0,
    onEnable: () => ({ softEdgeRadius: 6 }),
    onDisable: () => ({ softEdgeRadius: 0 }),
    fields: [
      {
        key: "se",
        label: "Soft Edge Radius",
        i18nKey: "pptx.inspector.softEdgeRadius",
        type: "number",
        min: 0,
        max: 96,
        span2: true,
        read: (s) => Math.round((s == null ? void 0 : s.softEdgeRadius) ?? 6),
        write: (v) => ({ softEdgeRadius: clamp2(Number(v), 0, 96) })
      }
    ]
  },
  /* Reflection */
  {
    label: "Reflection",
    isOn: (s) => ((s == null ? void 0 : s.reflectionBlurRadius) ?? 0) > 0 || ((s == null ? void 0 : s.reflectionStartOpacity) ?? 0) > 0,
    onEnable: () => ({
      reflectionBlurRadius: 3,
      reflectionStartOpacity: 50,
      reflectionEndOpacity: 0,
      reflectionEndPosition: 50,
      reflectionDirection: 90,
      reflectionDistance: 0
    }),
    onDisable: () => ({
      reflectionBlurRadius: 0,
      reflectionStartOpacity: 0,
      reflectionEndOpacity: 0,
      reflectionEndPosition: 0,
      reflectionDirection: 0,
      reflectionDistance: 0
    }),
    fields: [
      {
        key: "rb",
        label: "Blur",
        type: "number",
        min: 0,
        max: 20,
        step: 0.5,
        read: (s) => (s == null ? void 0 : s.reflectionBlurRadius) ?? 3,
        write: (v) => ({ reflectionBlurRadius: clamp2(Number(v), 0, 20) })
      },
      {
        key: "rs",
        label: "Start Opacity %",
        type: "number",
        min: 0,
        max: 100,
        read: (s) => (s == null ? void 0 : s.reflectionStartOpacity) ?? 50,
        write: (v) => ({ reflectionStartOpacity: clamp2(Number(v), 0, 100) })
      },
      {
        key: "re",
        label: "End Opacity %",
        type: "number",
        min: 0,
        max: 100,
        read: (s) => (s == null ? void 0 : s.reflectionEndOpacity) ?? 0,
        write: (v) => ({ reflectionEndOpacity: clamp2(Number(v), 0, 100) })
      },
      {
        key: "rd",
        label: "Distance",
        type: "number",
        min: 0,
        max: 50,
        read: (s) => (s == null ? void 0 : s.reflectionDistance) ?? 0,
        write: (v) => ({ reflectionDistance: clamp2(Number(v), 0, 50) })
      },
      {
        key: "rdir",
        label: "Direction",
        type: "number",
        min: 0,
        max: 360,
        read: (s) => (s == null ? void 0 : s.reflectionDirection) ?? 90,
        write: (v) => ({ reflectionDirection: clamp2(Number(v), 0, 360) })
      },
      {
        key: "rrot",
        label: "Rotation",
        type: "number",
        min: 0,
        max: 360,
        span2: true,
        read: (s) => (s == null ? void 0 : s.reflectionRotation) ?? 0,
        write: (v) => ({ reflectionRotation: clamp2(Number(v), 0, 360) })
      }
    ]
  },
  /* Blur */
  {
    label: "Blur",
    isOn: (s) => ((s == null ? void 0 : s.blurRadius) ?? 0) > 0,
    onEnable: () => ({ blurRadius: 4 }),
    onDisable: () => ({ blurRadius: 0 }),
    fields: [
      {
        key: "bl",
        label: "Blur Radius",
        type: "number",
        min: 0,
        max: 50,
        span2: true,
        read: (s) => Math.round((s == null ? void 0 : s.blurRadius) ?? 4),
        write: (v) => ({ blurRadius: clamp2(Number(v), 0, 50) })
      }
    ]
  },
  /* Bevel / 3D */
  {
    label: "Bevel / 3D",
    isOn: (s) => {
      var _a, _b2;
      return Boolean((_a = s == null ? void 0 : s.shape3d) == null ? void 0 : _a.bevelTopType) && ((_b2 = s == null ? void 0 : s.shape3d) == null ? void 0 : _b2.bevelTopType) !== "none";
    },
    onEnable: (s) => {
      var _a, _b2, _c;
      return {
        shape3d: {
          ...s == null ? void 0 : s.shape3d,
          bevelTopType: ((_a = s == null ? void 0 : s.shape3d) == null ? void 0 : _a.bevelTopType) || "circle",
          bevelTopWidth: ((_b2 = s == null ? void 0 : s.shape3d) == null ? void 0 : _b2.bevelTopWidth) ?? 76200,
          bevelTopHeight: ((_c = s == null ? void 0 : s.shape3d) == null ? void 0 : _c.bevelTopHeight) ?? 76200
        }
      };
    },
    onDisable: (s) => ({
      shape3d: {
        ...s == null ? void 0 : s.shape3d,
        bevelTopType: void 0,
        bevelTopWidth: void 0,
        bevelTopHeight: void 0
      }
    }),
    fields: [
      {
        key: "btype",
        label: "Bevel Type",
        type: "select",
        span2: true,
        options: BEVEL_TYPE_OPTIONS,
        read: (s) => {
          var _a;
          return ((_a = s == null ? void 0 : s.shape3d) == null ? void 0 : _a.bevelTopType) ?? "circle";
        },
        write: (v, s) => ({
          shape3d: { ...s == null ? void 0 : s.shape3d, bevelTopType: String(v) }
        })
      },
      {
        key: "bw",
        label: "Bevel Width (EMU)",
        type: "number",
        min: 0,
        max: 5e5,
        step: 12700,
        read: (s) => {
          var _a;
          return ((_a = s == null ? void 0 : s.shape3d) == null ? void 0 : _a.bevelTopWidth) ?? 76200;
        },
        write: (v, s) => ({
          shape3d: {
            ...s == null ? void 0 : s.shape3d,
            bevelTopWidth: clamp2(Number(v), 0, 5e5)
          }
        })
      },
      {
        key: "bh",
        label: "Bevel Height (EMU)",
        type: "number",
        min: 0,
        max: 5e5,
        step: 12700,
        span2: true,
        read: (s) => {
          var _a;
          return ((_a = s == null ? void 0 : s.shape3d) == null ? void 0 : _a.bevelTopHeight) ?? 76200;
        },
        write: (v, s) => ({
          shape3d: {
            ...s == null ? void 0 : s.shape3d,
            bevelTopHeight: clamp2(Number(v), 0, 5e5)
          }
        })
      }
    ]
  }
];
var EFFECT_CONFIGS = [...SHADOW_EFFECT_CONFIGS, ...VISUAL_EFFECT_CONFIGS];
var SEL2 = "bg-muted border border-border rounded px-2 py-1";
var NUM2 = SEL2;
var RNG = "accent-primary";
var SWATCH = "h-4 w-4 rounded border border-border";
var DIS = "disabled:opacity-40 disabled:cursor-not-allowed";
var LBL2 = "text-muted-foreground";
var COL2 = "col-span-2";
var isLineish = (el, st) => el.type === "connector" || st === "line";
var safeNum = (raw, fallback) => {
  const n = Number(raw);
  return Number.isFinite(n) ? n : fallback;
};
function EffectField({
  field,
  style,
  onUpdate
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const fieldLabel = field.i18nKey ? t(field.i18nKey) : field.label;
  const val = field.read(style);
  const cls = `flex flex-col gap-1 ${field.span2 ? COL2 : ""}`;
  if (field.type === "select" && field.options) {
    return (0, import_jsx_runtime.jsxs)("label", { className: cls, children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: fieldLabel }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": fieldLabel,
          value: String(val),
          onChange: (e) => {
            const result = field.write(e.target.value, style);
            onUpdate(typeof result === "function" ? result(style) : result);
          },
          className: SEL2,
          children: field.options.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: o.label }, o.value))
        }
      )
    ] });
  }
  if (field.type === "color") {
    return (0, import_jsx_runtime.jsxs)("label", { className: cls, children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: fieldLabel }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          value: String(val),
          onChange: (e) => {
            const result = field.write(e.target.value, style);
            onUpdate(typeof result === "function" ? result(style) : result);
            pushColor(e.target.value);
          },
          className: `h-8 ${SEL2} px-1`
        }
      )
    ] });
  }
  if (field.type === "checkbox") {
    return (0, import_jsx_runtime.jsxs)("label", { className: `flex items-center gap-2 ${field.span2 ? COL2 : ""}`, children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: Boolean(val),
          onChange: (e) => {
            const result = field.write(e.target.checked, style);
            onUpdate(typeof result === "function" ? result(style) : result);
          },
          className: "h-4 w-4"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: fieldLabel })
    ] });
  }
  if (field.type === "range") {
    return (0, import_jsx_runtime.jsxs)("label", { className: cls, children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: fieldLabel }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "range",
          min: field.min ?? 0,
          max: field.max ?? 100,
          value: Number(val),
          onChange: (e) => {
            const result = field.write(Number(e.target.value), style);
            onUpdate(typeof result === "function" ? result(style) : result);
          },
          className: RNG
        }
      )
    ] });
  }
  return (0, import_jsx_runtime.jsxs)("label", { className: cls, children: [
    (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: fieldLabel }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "number",
        min: field.min,
        max: field.max,
        step: field.step,
        value: Number(val),
        onChange: (e) => {
          const n = safeNum(e.target.value, Number(val));
          const result = field.write(n, style);
          onUpdate(typeof result === "function" ? result(style) : result);
        },
        className: NUM2
      }
    )
  ] });
}
function SelectRow({
  label,
  value,
  span2,
  options,
  onChange
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("label", { className: `flex flex-col gap-1 ${span2 ? COL2 : ""}`, children: [
    (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: label }),
    (0, import_jsx_runtime.jsx)(
      "select",
      {
        "aria-label": label,
        value,
        onChange: (e) => onChange(e.target.value),
        className: SEL2,
        children: options.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: o.i18nKey ? t(o.i18nKey) : o.label }, o.value))
      }
    )
  ] });
}
function ColorPickerRow({
  label,
  value,
  selectedRef,
  disabled,
  prefix,
  onChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const commit = (color, ref) => {
    onChange(color, ref);
    pushColor(color);
  };
  const handleEyedropper = async () => {
    const color = await openNativeEyeDropper();
    if (color) {
      commit(color);
    }
  };
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: label }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          value,
          disabled,
          onChange: (e) => commit(e.target.value),
          className: `h-8 flex-1 ${SEL2} px-1 ${DIS}`
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled,
          className: "h-8 w-8 flex items-center justify-center rounded border border-border bg-muted hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed transition-colors",
          title: t("pptx.fillStroke.eyedropperTooltip"),
          onClick: (e) => {
            e.preventDefault();
            void handleEyedropper();
          },
          children: (0, import_jsx_runtime.jsx)(LuPipette, { className: "w-3.5 h-3.5" })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      ThemeColorSwatchGrid,
      {
        prefix,
        disabled,
        selectedRef,
        selectedHex: value,
        onPick: (c) => commit(c.hex, c.ref)
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "mt-1", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground mb-1", children: t("pptx.colorPicker.standardColors") }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex flex-wrap gap-1", children: OFFICE_COLOR_SWATCHES.map((c) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: `${SWATCH} ${DIS}`,
          style: { backgroundColor: c.hex },
          title: `${label} ${c.label}`,
          "aria-label": `${label} ${c.label}`,
          "data-pptx-compact": true,
          disabled,
          onClick: () => commit(c.hex)
        },
        `${prefix}-standard-${c.hex}`
      )) })
    ] }),
    (0, import_jsx_runtime.jsx)(RecentColorsRow, { prefix, disabled, onCommit: commit })
  ] });
}
function GradientStopRow({
  stop,
  index,
  total,
  onUpdate,
  allStops
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const patchStop = (patch) => {
    const next = allStops.map((s, i) => i === index ? { ...s, ...patch } : s);
    onUpdate(next);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[auto,1fr,auto] items-center gap-2", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          value: normalizeHexColor2(stop.color, "#3b82f6"),
          onChange: (e) => {
            const hex = normalizeHexColor2(e.target.value, "#3b82f6");
            patchStop({ color: hex, colorRef: void 0 });
            pushColor(hex);
          },
          className: "h-7 w-10 rounded border border-border bg-muted"
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          value: Math.round(stop.position),
          onChange: (e) => patchStop({ position: Number(e.target.value) }),
          className: RNG
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: total <= 2,
          className: "rounded bg-muted px-2 py-1 text-[11px] hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed",
          onClick: () => onUpdate(allStops.filter((_, i) => i !== index)),
          children: t("pptx.comments.remove")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      ThemeColorSwatchGrid,
      {
        prefix: `gradient-stop-${index}`,
        selectedRef: stop.colorRef,
        selectedHex: stop.color,
        onPick: (c) => {
          patchStop({ color: c.hex, colorRef: c.ref });
          pushColor(c.hex);
        }
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[auto,1fr,auto] items-center gap-2 pl-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground w-10 text-center", children: "Opacity" }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          value: Math.round((stop.opacity ?? 1) * 100),
          onChange: (e) => patchStop({ opacity: Number(e.target.value) / 100 }),
          className: RNG
        }
      ),
      (0, import_jsx_runtime.jsxs)("span", { className: "text-[10px] text-muted-foreground w-[52px] text-right", children: [
        Math.round((stop.opacity ?? 1) * 100),
        "%"
      ] })
    ] })
  ] });
}
function FillAdvancedControls({
  style,
  gradientStops,
  showGradient,
  gradType,
  isLine,
  onUpdateShapeStyle,
  onUpdateGradientStops,
  onSetFillColor: _onSetFillColor
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    showGradient && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        SelectRow,
        {
          label: t("pptx.fillAdvanced.gradientType"),
          span2: true,
          value: gradType,
          options: GRADIENT_TYPE_OPTIONS2,
          onChange: (v) => onUpdateShapeStyle({
            fillMode: "gradient",
            fillGradientType: v
          })
        }
      ),
      gradType === "linear" && (0, import_jsx_runtime.jsxs)("label", { className: `flex flex-col gap-1 ${COL2}`, children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.fillAdvanced.gradientAngle") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            min: 0,
            max: 360,
            value: Math.round((style == null ? void 0 : style.fillGradientAngle) ?? 90),
            onChange: (e) => onUpdateShapeStyle({
              fillMode: "gradient",
              fillGradientAngle: Number(e.target.value)
            }),
            className: RNG
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: `${COL2} space-y-1.5 rounded border border-border p-2`, children: [
        (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.fillAdvanced.gradientStops") }),
        gradientStops.map((stop, i) => (0, import_jsx_runtime.jsx)(
          GradientStopRow,
          {
            stop,
            index: i,
            total: gradientStops.length,
            allStops: gradientStops,
            onUpdate: onUpdateGradientStops
          },
          `gradient-stop-${i}`
        )),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: "inline-flex items-center justify-center rounded bg-muted px-2 py-1 text-[11px] hover:bg-accent",
            onClick: () => onUpdateGradientStops([
              ...gradientStops,
              {
                color: normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR),
                position: 50,
                opacity: 1
              }
            ]),
            children: t("pptx.gradient.addStop")
          }
        )
      ] })
    ] }),
    (style == null ? void 0 : style.fillMode) === "pattern" && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsxs)("div", { className: `${COL2} flex flex-col gap-2`, children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: "Pattern" }),
        (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-8 gap-1 max-h-48 overflow-y-auto rounded border border-border p-2", children: PATTERN_PRESET_OPTIONS2.map((opt) => {
          const fgColor = normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR);
          const bgColor = normalizeHexColor2(style == null ? void 0 : style.fillPatternBackgroundColor, "#ffffff");
          const svgPattern = getPatternSvg(opt.value, fgColor, bgColor);
          const isSelected = (style.fillPatternPreset || "pct20") === opt.value;
          return (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              title: t(opt.labelKey),
              className: `h-8 w-8 rounded border transition-all ${isSelected ? "border-primary border-2 ring-2 ring-primary/20" : "border-border hover:border-primary/50"}`,
              onClick: () => onUpdateShapeStyle({
                fillMode: "pattern",
                fillPatternPreset: opt.value
              }),
              children: svgPattern && (0, import_jsx_runtime.jsx)(
                "div",
                {
                  className: "w-full h-full rounded-sm",
                  style: {
                    backgroundImage: `url('data:image/svg+xml;utf8,${encodeURIComponent(svgPattern)}')`,
                    backgroundRepeat: "repeat",
                    backgroundSize: "8px 8px"
                  }
                }
              )
            },
            opt.value
          );
        }) })
      ] }),
      (0, import_jsx_runtime.jsx)(
        ColorPickerRow,
        {
          label: t("pptx.fillAdvanced.foregroundColor"),
          prefix: "pattern-fg",
          value: normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR),
          disabled: isLine,
          onChange: (color) => onUpdateShapeStyle({ fillMode: "pattern", fillColor: color })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        ColorPickerRow,
        {
          label: t("pptx.fillAdvanced.backgroundColor"),
          prefix: "pattern-bg",
          value: normalizeHexColor2(style == null ? void 0 : style.fillPatternBackgroundColor, "#ffffff"),
          disabled: isLine,
          onChange: (color) => onUpdateShapeStyle({
            fillMode: "pattern",
            fillPatternBackgroundColor: color
          })
        }
      )
    ] }),
    (style == null ? void 0 : style.fillMode) === "image" && (0, import_jsx_runtime.jsxs)("div", { className: `${COL2} space-y-2`, children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.fillAdvanced.imageUrl") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            placeholder: "https://example.com/image.png",
            value: (style == null ? void 0 : style.fillImageUrl) || "",
            onChange: (e) => onUpdateShapeStyle({ fillImageUrl: e.target.value }),
            className: `${SEL2} text-xs`
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.fillAdvanced.orChooseFile") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "file",
            accept: "image/*",
            onChange: (e) => {
              var _a;
              const file = (_a = e.target.files) == null ? void 0 : _a[0];
              if (!file) {
                return;
              }
              const reader = new FileReader();
              reader.onload = () => onUpdateShapeStyle({ fillImageUrl: reader.result });
              reader.readAsDataURL(file);
            },
            className: "text-xs text-foreground file:bg-accent file:border-0 file:rounded file:px-2 file:py-1 file:text-xs file:text-foreground file:mr-2"
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        SelectRow,
        {
          label: t("pptx.fillAdvanced.imageMode"),
          value: (style == null ? void 0 : style.fillImageMode) || "stretch",
          options: IMAGE_MODE_OPTIONS,
          onChange: (v) => onUpdateShapeStyle({ fillImageMode: v })
        }
      ),
      (style == null ? void 0 : style.fillImageUrl) && (0, import_jsx_runtime.jsx)("div", { className: "mt-1 rounded border border-border overflow-hidden h-16", children: (0, import_jsx_runtime.jsx)(
        "img",
        {
          src: style.fillImageUrl,
          alt: t("pptx.fillAdvanced.fillPreviewAlt"),
          className: "w-full h-full object-cover"
        }
      ) })
    ] })
  ] });
}
function QuickStylesGallery({
  onUpdateShapeStyle
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.shape.quickStyles") }),
    (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-6 gap-1", children: SHAPE_QUICK_STYLES.map((qs, idx) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        title: qs.name,
        "aria-label": qs.name,
        className: "h-7 w-full rounded border border-border hover:border-primary transition-colors",
        style: quickStyleSwatchCss(qs),
        onClick: () => onUpdateShapeStyle(qs.style)
      },
      idx
    )) })
  ] });
}
function StrokeEffectsSection({
  style,
  isLine,
  onUpdateShapeStyle,
  onSetStrokeColor
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      ColorPickerRow,
      {
        label: t("pptx.inspector.stroke"),
        prefix: "stroke",
        value: normalizeHexColor2(style == null ? void 0 : style.strokeColor, DEFAULT_STROKE_COLOR),
        selectedRef: style == null ? void 0 : style.strokeColorRef,
        onChange: onSetStrokeColor
      }
    ),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.strokeEffects.strokeWidth") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "number",
          min: 0,
          value: Math.round((style == null ? void 0 : style.strokeWidth) || 0),
          onChange: (e) => {
            const v = safeNum(e.target.value, 0);
            onUpdateShapeStyle({ strokeWidth: Math.max(0, v) });
          },
          className: NUM2
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.strokeEffects.fillOpacity") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          disabled: isLine,
          value: Math.round(((style == null ? void 0 : style.fillOpacity) ?? 1) * 100),
          onChange: (e) => onUpdateShapeStyle({ fillOpacity: Number(e.target.value) / 100 }),
          className: `${RNG} ${DIS}`
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.strokeEffects.strokeOpacity") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "range",
          min: 0,
          max: 100,
          value: Math.round(((style == null ? void 0 : style.strokeOpacity) ?? 1) * 100),
          onChange: (e) => onUpdateShapeStyle({
            strokeOpacity: Number(e.target.value) / 100
          }),
          className: RNG
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: `flex flex-col gap-1 ${COL2}`, children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.strokeEffects.strokeDash") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.strokeEffects.strokeDash"),
          value: (style == null ? void 0 : style.strokeDash) || "solid",
          onChange: (e) => onUpdateShapeStyle({
            strokeDash: e.target.value
          }),
          className: "bg-muted border border-border rounded px-2 py-1",
          children: STROKE_DASH_OPTIONS2.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.i18nKey) }, o.value))
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: `flex flex-col gap-1 ${COL2}`, children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL2, children: t("pptx.strokeEffects.compoundLine") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-5 gap-1", children: COMPOUND_LINE_OPTIONS2.map((opt) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          title: t(opt.i18nKey),
          "aria-label": t(opt.i18nKey),
          className: `h-10 px-2 rounded border transition-all ${((style == null ? void 0 : style.compoundLine) || "sng") === opt.value ? "border-primary bg-accent" : "border-border hover:border-primary/50"}`,
          onClick: () => onUpdateShapeStyle({
            compoundLine: opt.value
          }),
          children: (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "w-full h-full flex items-center justify-center",
              style: getCompoundLinePreviewStyle(opt.value)
            }
          )
        },
        opt.value
      )) })
    ] }),
    EFFECT_CONFIGS.map((cfg) => {
      const on = cfg.isOn(style);
      return (0, import_jsx_runtime.jsxs)(import_react2.default.Fragment, { children: [
        (0, import_jsx_runtime.jsxs)("label", { className: `inline-flex items-center gap-2 text-foreground ${COL2}`, children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "checkbox",
              checked: on,
              onChange: (e) => {
                onUpdateShapeStyle(e.target.checked ? cfg.onEnable(style) : cfg.onDisable(style));
              }
            }
          ),
          cfg.i18nKey ? t(cfg.i18nKey) : cfg.label
        ] }),
        on && cfg.fields.map((f) => (0, import_jsx_runtime.jsx)(EffectField, { field: f, style, onUpdate: onUpdateShapeStyle }, f.key))
      ] }, cfg.label);
    }),
    (0, import_jsx_runtime.jsx)(
      SelectRow,
      {
        label: t("pptx.strokeEffects.lineJoin"),
        value: (style == null ? void 0 : style.lineJoin) || "round",
        options: LINE_JOIN_OPTIONS2,
        onChange: (v) => onUpdateShapeStyle({ lineJoin: v })
      }
    ),
    (0, import_jsx_runtime.jsx)(
      SelectRow,
      {
        label: t("pptx.strokeEffects.lineCap"),
        value: (style == null ? void 0 : style.lineCap) || "flat",
        options: LINE_CAP_OPTIONS2,
        onChange: (v) => onUpdateShapeStyle({ lineCap: v })
      }
    )
  ] });
}
function FillStrokeProperties({
  selectedElement,
  selectedShapeStyle,
  selectedShapeType,
  selectedGradientStops,
  onUpdateShapeStyle,
  onSetFillColor,
  onSetStrokeColor
}) {
  const { t } = useTranslation();
  const line = isLineish(selectedElement, selectedShapeType);
  const style = selectedShapeStyle;
  const handleFillModeChange = (nextMode) => {
    if (nextMode === "none") {
      onUpdateShapeStyle({ fillMode: "none", fillColor: "transparent" });
      return;
    }
    if (nextMode === "image") {
      onUpdateShapeStyle({
        fillMode: "image",
        fillImageMode: (style == null ? void 0 : style.fillImageMode) || "stretch"
      });
      return;
    }
    if (nextMode === "gradient") {
      const existing = sanitizeGradientStops(style == null ? void 0 : style.fillGradientStops);
      const stops = existing.length >= 2 ? existing : [
        {
          color: normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR),
          position: 0,
          opacity: 1
        },
        { color: "#ffffff", position: 100, opacity: 1 }
      ];
      onUpdateShapeStyle({
        fillMode: "gradient",
        fillGradientType: (style == null ? void 0 : style.fillGradientType) || "linear",
        fillGradientAngle: (style == null ? void 0 : style.fillGradientAngle) ?? 90,
        fillGradientStops: stops
      });
      return;
    }
    if (nextMode === "pattern") {
      onUpdateShapeStyle({
        fillMode: "pattern",
        fillPatternPreset: (style == null ? void 0 : style.fillPatternPreset) || "pct20",
        fillPatternBackgroundColor: (style == null ? void 0 : style.fillPatternBackgroundColor) || "#ffffff"
      });
      return;
    }
    onUpdateShapeStyle({ fillMode: "solid" });
  };
  const gradientStops = selectedGradientStops.length > 0 ? selectedGradientStops : [
    { color: DEFAULT_FILL_COLOR, position: 0 },
    { color: "#ffffff", position: 100 }
  ];
  const showGradient = (style == null ? void 0 : style.fillMode) === "gradient" || selectedGradientStops.length > 0;
  const gradType = (style == null ? void 0 : style.fillGradientType) || "linear";
  const updateGradientStops = (stops) => {
    onUpdateShapeStyle({ fillMode: "gradient", fillGradientStops: stops });
  };
  const showQuickStyles = selectedElement.type === "shape" || selectedElement.type === "text";
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    showQuickStyles && (0, import_jsx_runtime.jsx)(QuickStylesGallery, { onUpdateShapeStyle }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2", children: [
      (0, import_jsx_runtime.jsx)(
        SelectRow,
        {
          label: t("pptx.table.fillMode"),
          span2: true,
          value: (style == null ? void 0 : style.fillMode) || ((style == null ? void 0 : style.fillColor) === "transparent" ? "none" : "solid"),
          options: FILL_MODE_OPTIONS2,
          onChange: handleFillModeChange
        }
      ),
      (0, import_jsx_runtime.jsx)(
        ColorPickerRow,
        {
          label: t("pptx.fill.fill"),
          prefix: "fill",
          value: normalizeHexColor2(style == null ? void 0 : style.fillColor, DEFAULT_FILL_COLOR),
          selectedRef: style == null ? void 0 : style.fillColorRef,
          disabled: line,
          onChange: onSetFillColor
        }
      ),
      (0, import_jsx_runtime.jsx)(
        FillAdvancedControls,
        {
          style,
          gradientStops,
          showGradient,
          gradType,
          isLine: line,
          onUpdateShapeStyle,
          onUpdateGradientStops: updateGradientStops,
          onSetFillColor
        }
      ),
      (0, import_jsx_runtime.jsx)(
        StrokeEffectsSection,
        {
          style,
          isLine: line,
          onUpdateShapeStyle,
          onSetStrokeColor
        }
      )
    ] })
  ] });
}
var INPUT_CLS2 = "bg-muted border border-border rounded px-2 py-1";
var COLOR_CLS = "h-8 bg-muted border border-border rounded px-1";
function Text3DProperties({
  ts,
  onUpdateTextStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const t3d = ts == null ? void 0 : ts.text3d;
  const hasExtrusion = Boolean((t3d == null ? void 0 : t3d.extrusionHeight) && t3d.extrusionHeight > 0);
  const update3d = (partial) => {
    const merged = { ...ts == null ? void 0 : ts.text3d, ...partial };
    onUpdateTextStyle({ text3d: merged });
  };
  const toggleExtrusion = (checked) => {
    onUpdateTextStyle({ text3d: toggleText3dExtrusion(ts == null ? void 0 : ts.text3d, checked) });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", "data-pptx-text-3d": true, children: [
    (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.text3d.title") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            checked: hasExtrusion,
            onChange: (e) => toggleExtrusion(e.target.checked)
          }
        ),
        t("pptx.text3d.extrusion")
      ] }),
      hasExtrusion && (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text3d.extrusionDepth") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              max: 100,
              step: 1,
              value: text3dEmuToPt(t3d == null ? void 0 : t3d.extrusionHeight),
              onChange: (e) => {
                const v = Number(e.target.value);
                if (Number.isFinite(v)) {
                  update3d({
                    extrusionHeight: text3dPtToEmu(Math.max(0, Math.min(100, v)))
                  });
                }
              },
              className: INPUT_CLS2
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text3d.extrusionColor") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              value: normalizeHexColor2(t3d == null ? void 0 : t3d.extrusionColor, "#888888"),
              onChange: (e) => {
                update3d({ extrusionColor: e.target.value });
                pushColor(e.target.value);
              },
              className: COLOR_CLS
            }
          )
        ] })
      ] })
    ] }),
    hasExtrusion && (0, import_jsx_runtime.jsx)(
      BevelSection,
      {
        label: t("pptx.text3d.bevelTop"),
        bevelType: t3d == null ? void 0 : t3d.bevelTopType,
        bevelWidth: t3d == null ? void 0 : t3d.bevelTopWidth,
        bevelHeight: t3d == null ? void 0 : t3d.bevelTopHeight,
        onTypeChange: (v) => update3d({
          bevelTopType: v || void 0
        }),
        onWidthChange: (v) => update3d({ bevelTopWidth: text3dPtToEmu(v) }),
        onHeightChange: (v) => update3d({ bevelTopHeight: text3dPtToEmu(v) }),
        t
      }
    ),
    hasExtrusion && (0, import_jsx_runtime.jsx)(
      BevelSection,
      {
        label: t("pptx.text3d.bevelBottom"),
        bevelType: t3d == null ? void 0 : t3d.bevelBottomType,
        bevelWidth: t3d == null ? void 0 : t3d.bevelBottomWidth,
        bevelHeight: t3d == null ? void 0 : t3d.bevelBottomHeight,
        onTypeChange: (v) => update3d({
          bevelBottomType: v || void 0
        }),
        onWidthChange: (v) => update3d({ bevelBottomWidth: text3dPtToEmu(v) }),
        onHeightChange: (v) => update3d({ bevelBottomHeight: text3dPtToEmu(v) }),
        t
      }
    ),
    hasExtrusion && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 pl-4", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text3d.material") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.text3d.material"),
          value: (t3d == null ? void 0 : t3d.presetMaterial) ?? "",
          onChange: (e) => {
            const v = e.target.value;
            update3d({
              presetMaterial: v ? v : void 0
            });
          },
          className: INPUT_CLS2,
          children: MATERIAL_PRESETS.map(({ value, label }) => (0, import_jsx_runtime.jsx)("option", { value, children: label }, value))
        }
      )
    ] })
  ] });
}
function BevelSection({
  label,
  bevelType,
  bevelWidth,
  bevelHeight,
  onTypeChange,
  onWidthChange,
  onHeightChange,
  t
}) {
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1 pl-4", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] text-muted-foreground", children: label }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-3 gap-2", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text3d.bevelType") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.text3d.bevelType"),
            value: bevelType ?? "none",
            onChange: (e) => onTypeChange(e.target.value),
            className: INPUT_CLS2,
            children: BEVEL_PRESETS.map(({ value, label: l }) => (0, import_jsx_runtime.jsx)("option", { value, children: l }, value))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text3d.bevelWidth") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 50,
            step: 1,
            value: text3dEmuToPt(bevelWidth),
            onChange: (e) => {
              const v = Number(e.target.value);
              if (Number.isFinite(v)) {
                onWidthChange(Math.max(0, Math.min(50, v)));
              }
            },
            className: INPUT_CLS2
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text3d.bevelHeight") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 50,
            step: 1,
            value: text3dEmuToPt(bevelHeight),
            onChange: (e) => {
              const v = Number(e.target.value);
              if (Number.isFinite(v)) {
                onHeightChange(Math.max(0, Math.min(50, v)));
              }
            },
            className: INPUT_CLS2
          }
        )
      ] })
    ] })
  ] });
}
var INPUT_CLS3 = "bg-muted border border-border rounded px-2 py-1";
var COLOR_CLS2 = "h-8 bg-muted border border-border rounded px-1";
var UNDERLINE_STYLES = [
  ["sng", "Single"],
  ["dbl", "Double"],
  ["heavy", "Heavy"],
  ["dotted", "Dotted"],
  ["dottedHeavy", "Dotted Heavy"],
  ["dash", "Dash"],
  ["dashHeavy", "Dash Heavy"],
  ["dashLong", "Long Dash"],
  ["dashLongHeavy", "Long Dash Heavy"],
  ["dotDash", "Dot Dash"],
  ["dotDashHeavy", "Dot Dash Heavy"],
  ["dotDotDash", "Dot Dot Dash"],
  ["dotDotDashHeavy", "Dot Dot Dash Heavy"],
  ["wavy", "Wavy"],
  ["wavyHeavy", "Wavy Heavy"],
  ["wavyDbl", "Wavy Double"],
  ["none", "None"]
];
var BASELINE_TOGGLES = [
  ["Superscript", 3e4],
  ["Subscript", -25e3]
];
function createNumericChangeHandler(onUpdateTextStyle) {
  return (fn) => (e) => {
    const v = Number(e.target.value);
    if (Number.isFinite(v)) {
      onUpdateTextStyle(fn(v));
    }
  };
}
function AdvancedTextFormatting({
  ts,
  canEdit,
  onUpdateTextStyle,
  numChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.textProperties.advancedTextHeading") }),
    (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-2", children: BASELINE_TOGGLES.map(([label, baseline]) => (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: (ts == null ? void 0 : ts.baseline) === baseline,
          onChange: () => onUpdateTextStyle({
            baseline: (ts == null ? void 0 : ts.baseline) === baseline ? 0 : baseline
          })
        }
      ),
      label
    ] }, label)) }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Highlight" }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          value: normalizeHexColor2(ts == null ? void 0 : ts.highlightColor, "#ffff00"),
          onChange: (e) => {
            onUpdateTextStyle({ highlightColor: e.target.value });
            pushColor(e.target.value);
          },
          disabled: !canEdit,
          className: COLOR_CLS2
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textProperties.underlineStyle") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.textProperties.underlineStyle"),
            value: (ts == null ? void 0 : ts.underlineStyle) || "sng",
            disabled: !canEdit,
            onChange: (e) => onUpdateTextStyle({
              underline: true,
              underlineStyle: e.target.value
            }),
            className: INPUT_CLS3,
            children: UNDERLINE_STYLES.map(([v, l]) => (0, import_jsx_runtime.jsx)("option", { value: v, children: l }, v))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textProperties.underlineColour") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            value: (ts == null ? void 0 : ts.underlineColor) || (ts == null ? void 0 : ts.color) || "#000000",
            onChange: (e) => {
              onUpdateTextStyle({ underlineColor: e.target.value });
              pushColor(e.target.value);
            },
            className: COLOR_CLS2
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textProperties.outlineWidth") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 10,
            step: 0.5,
            value: (ts == null ? void 0 : ts.textOutlineWidth) ?? 0,
            onChange: (e) => onUpdateTextStyle({
              textOutlineWidth: parseFloat(e.target.value) || 0
            }),
            className: INPUT_CLS3
          }
        )
      ] }),
      ((ts == null ? void 0 : ts.textOutlineWidth) ?? 0) > 0 && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textProperties.outlineColour") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            value: (ts == null ? void 0 : ts.textOutlineColor) || "#000000",
            onChange: (e) => {
              onUpdateTextStyle({ textOutlineColor: e.target.value });
              pushColor(e.target.value);
            },
            className: COLOR_CLS2
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.text.characterSpacing") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "number",
          min: -1e3,
          max: 5e3,
          step: 50,
          value: Math.round((ts == null ? void 0 : ts.characterSpacing) ?? 0),
          disabled: !canEdit,
          onChange: numChange((v) => ({ characterSpacing: v })),
          className: INPUT_CLS3
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textProperties.leftMargin") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 500,
            step: 4,
            value: Math.round((ts == null ? void 0 : ts.paragraphMarginLeft) ?? 0),
            disabled: !canEdit,
            onChange: numChange((v) => ({
              paragraphMarginLeft: Math.max(0, Math.min(500, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textProperties.firstIndent") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: -500,
            max: 500,
            step: 4,
            value: Math.round((ts == null ? void 0 : ts.paragraphIndent) ?? 0),
            disabled: !canEdit,
            onChange: numChange((v) => ({
              paragraphIndent: Math.max(-500, Math.min(500, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: Boolean(ts == null ? void 0 : ts.autoFit),
          disabled: !canEdit,
          onChange: (e) => onUpdateTextStyle({ autoFit: e.target.checked })
        }
      ),
      "Auto-fit text to shape"
    ] })
  ] });
}
function TextReflectionSection({
  ts,
  onUpdateTextStyle,
  numChange
}) {
  const { t } = useTranslation();
  const hasReflection = Boolean(ts == null ? void 0 : ts.textReflection);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: hasReflection,
          onChange: (e) => {
            if (e.target.checked) {
              onUpdateTextStyle({
                textReflection: true,
                textReflectionBlur: 1,
                textReflectionStartOpacity: 0.5,
                textReflectionEndOpacity: 0,
                textReflectionOffset: 3
              });
            } else {
              onUpdateTextStyle({
                textReflection: void 0,
                textReflectionBlur: void 0,
                textReflectionStartOpacity: void 0,
                textReflectionEndOpacity: void 0,
                textReflectionOffset: void 0
              });
            }
          }
        }
      ),
      t("pptx.textEffects.reflection")
    ] }),
    hasReflection && (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Blur" }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 20,
            step: 0.5,
            value: Number((ts == null ? void 0 : ts.textReflectionBlur) ?? 1).toFixed(1),
            onChange: numChange((v) => ({
              textReflectionBlur: Math.max(0, Math.min(20, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Offset" }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 20,
            step: 1,
            value: Math.round((ts == null ? void 0 : ts.textReflectionOffset) ?? 3),
            onChange: numChange((v) => ({
              textReflectionOffset: Math.max(0, Math.min(20, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textEffects.startOpacity") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 1,
            step: 0.05,
            value: Number((ts == null ? void 0 : ts.textReflectionStartOpacity) ?? 0.5).toFixed(2),
            onChange: numChange((v) => ({
              textReflectionStartOpacity: Math.max(0, Math.min(1, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textEffects.endOpacity") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 0,
            max: 1,
            step: 0.05,
            value: Number((ts == null ? void 0 : ts.textReflectionEndOpacity) ?? 0).toFixed(2),
            onChange: numChange((v) => ({
              textReflectionEndOpacity: Math.max(0, Math.min(1, v))
            })),
            className: INPUT_CLS3
          }
        )
      ] })
    ] })
  ] });
}
function TextEffectsPanel({
  ts,
  onUpdateTextStyle,
  numChange
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const hasShadow = Boolean(
    (ts == null ? void 0 : ts.textShadowColor) || typeof (ts == null ? void 0 : ts.textShadowBlur) === "number" && ts.textShadowBlur > 0
  );
  const hasGlow = Boolean(
    (ts == null ? void 0 : ts.textGlowColor) || typeof (ts == null ? void 0 : ts.textGlowRadius) === "number" && ts.textGlowRadius > 0
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.textEffects.title") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            checked: hasShadow,
            onChange: (e) => {
              if (e.target.checked) {
                onUpdateTextStyle({
                  textShadowColor: "#000000",
                  textShadowBlur: 4,
                  textShadowOffsetX: 2,
                  textShadowOffsetY: 2,
                  textShadowOpacity: 0.5
                });
              } else {
                onUpdateTextStyle({
                  textShadowColor: void 0,
                  textShadowBlur: void 0,
                  textShadowOffsetX: void 0,
                  textShadowOffsetY: void 0,
                  textShadowOpacity: void 0
                });
              }
            }
          }
        ),
        t("pptx.textEffects.shadow")
      ] }),
      hasShadow && (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Color" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              value: normalizeHexColor2(ts == null ? void 0 : ts.textShadowColor, "#000000"),
              onChange: (e) => {
                onUpdateTextStyle({ textShadowColor: e.target.value });
                pushColor(e.target.value);
              },
              className: COLOR_CLS2
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Opacity" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              max: 1,
              step: 0.05,
              value: Number((ts == null ? void 0 : ts.textShadowOpacity) ?? 0.5).toFixed(2),
              onChange: numChange((v) => ({
                textShadowOpacity: Math.max(0, Math.min(1, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Blur" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              max: 50,
              step: 1,
              value: Math.round((ts == null ? void 0 : ts.textShadowBlur) ?? 4),
              onChange: numChange((v) => ({
                textShadowBlur: Math.max(0, Math.min(50, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textEffects.offsetX") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: -50,
              max: 50,
              step: 1,
              value: Math.round((ts == null ? void 0 : ts.textShadowOffsetX) ?? 2),
              onChange: numChange((v) => ({ textShadowOffsetX: v })),
              className: INPUT_CLS3
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.textEffects.offsetY") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: -50,
              max: 50,
              step: 1,
              value: Math.round((ts == null ? void 0 : ts.textShadowOffsetY) ?? 2),
              onChange: numChange((v) => ({ textShadowOffsetY: v })),
              className: INPUT_CLS3
            }
          )
        ] })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            checked: hasGlow,
            onChange: (e) => {
              if (e.target.checked) {
                onUpdateTextStyle({
                  textGlowColor: "#ffff00",
                  textGlowRadius: 6,
                  textGlowOpacity: 0.6
                });
              } else {
                onUpdateTextStyle({
                  textGlowColor: void 0,
                  textGlowRadius: void 0,
                  textGlowOpacity: void 0
                });
              }
            }
          }
        ),
        t("pptx.textEffects.glow")
      ] }),
      hasGlow && (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2 pl-4", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Color" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              value: normalizeHexColor2(ts == null ? void 0 : ts.textGlowColor, "#ffff00"),
              onChange: (e) => {
                onUpdateTextStyle({ textGlowColor: e.target.value });
                pushColor(e.target.value);
              },
              className: COLOR_CLS2
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Opacity" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              max: 1,
              step: 0.05,
              value: Number((ts == null ? void 0 : ts.textGlowOpacity) ?? 0.6).toFixed(2),
              onChange: numChange((v) => ({
                textGlowOpacity: Math.max(0, Math.min(1, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Size" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 1,
              max: 50,
              step: 1,
              value: Math.round((ts == null ? void 0 : ts.textGlowRadius) ?? 6),
              onChange: numChange((v) => ({
                textGlowRadius: Math.max(1, Math.min(50, v))
              })),
              className: INPUT_CLS3
            }
          )
        ] })
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)(TextReflectionSection, { ts, onUpdateTextStyle, numChange })
  ] });
}
function TextWarpGallery({
  ts,
  onUpdateTextStyle
}) {
  var _a;
  const { t } = useTranslation();
  const [expanded, setExpanded] = import_react2.default.useState(false);
  const currentPreset = (ts == null ? void 0 : ts.textWarpPreset) || "textNoShape";
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 rounded border border-border bg-card p-2 space-y-2", children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: "flex w-full items-center justify-between text-[11px] uppercase tracking-wide text-muted-foreground",
        onClick: () => setExpanded(!expanded),
        children: [
          (0, import_jsx_runtime.jsx)("span", { children: t("pptx.textWarp.title") }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: expanded ? "−" : "+" })
        ]
      }
    ),
    !expanded && (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: ((_a = TEXT_WARP_PRESETS.find((p) => p.value === currentPreset)) == null ? void 0 : _a.label) || currentPreset }),
    expanded && (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-5 gap-1", children: TEXT_WARP_PRESETS.map(({ value, label }) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        title: label,
        "aria-label": label,
        className: `flex items-center justify-center rounded p-1 ${currentPreset === value ? "bg-primary ring-1 ring-primary" : "bg-muted hover:bg-accent"}`,
        onClick: () => onUpdateTextStyle({
          textWarpPreset: value === "textNoShape" ? void 0 : value
        }),
        children: (0, import_jsx_runtime.jsx)("svg", { width: 40, height: 20, viewBox: "0 0 40 20", children: (0, import_jsx_runtime.jsx)(
          "path",
          {
            d: warpPreviewPath(value),
            stroke: "currentColor",
            strokeWidth: 1.5,
            fill: "none",
            className: currentPreset === value ? "text-white" : "text-muted-foreground"
          }
        ) })
      },
      value
    )) })
  ] });
}
function TextAdvancedSections({
  selectedElement,
  canEdit,
  onUpdateTextStyle
}) {
  if (!hasTextProperties(selectedElement)) {
    return null;
  }
  const ts = selectedElement.textStyle;
  const numChange = createNumericChangeHandler(onUpdateTextStyle);
  return (0, import_jsx_runtime.jsxs)("div", { "data-pptx-text-advanced": true, children: [
    (0, import_jsx_runtime.jsx)(
      AdvancedTextFormatting,
      {
        ts,
        canEdit,
        onUpdateTextStyle,
        numChange
      }
    ),
    (0, import_jsx_runtime.jsx)(TextWarpGallery, { ts, onUpdateTextStyle }),
    (0, import_jsx_runtime.jsx)(TextEffectsPanel, { ts, onUpdateTextStyle, numChange }),
    (0, import_jsx_runtime.jsx)(Text3DProperties, { ts, onUpdateTextStyle })
  ] });
}
function ShapeTextPanels({
  selectedElement,
  canEdit,
  onUpdateElement,
  onUpdateElementStyle,
  onUpdateTextStyle
}) {
  var _a, _b2, _c, _d, _e, _f, _g, _h;
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    hasShapeProperties(selectedElement) && (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.shape.type", "Shape Type") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          value: selectedElement.shapeType || "rect",
          disabled: !canEdit,
          className: cn(INPUT, "w-full"),
          onChange: (e) => onUpdateElement({
            shapeType: e.target.value
          }),
          children: SHAPE_PRESETS.filter((p) => p.type !== "connector").map((p) => (0, import_jsx_runtime.jsx)("option", { value: p.type, children: t(p.i18nKey) }, p.type))
        }
      )
    ] }),
    hasShapeProperties(selectedElement) && (0, import_jsx_runtime.jsxs)("div", { className: CARD, "data-pptx-fill-stroke": true, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.shape.fillStroke", "Fill & Stroke") }),
      (0, import_jsx_runtime.jsx)(
        FillStrokeProperties,
        {
          selectedElement,
          selectedShapeStyle: selectedElement.shapeStyle,
          selectedShapeType: selectedElement.shapeType,
          selectedGradientStops: sanitizeGradientStops(
            (_a = selectedElement.shapeStyle) == null ? void 0 : _a.fillGradientStops
          ),
          canEdit,
          onUpdateShapeStyle: onUpdateElementStyle,
          onSetFillColor: (hex, ref) => onUpdateElementStyle({ fillColor: hex, fillMode: "solid", fillColorRef: ref }),
          onSetStrokeColor: (hex, ref) => onUpdateElementStyle({ strokeColor: hex, strokeColorRef: ref })
        }
      )
    ] }),
    hasTextProperties(selectedElement) && (0, import_jsx_runtime.jsxs)("div", { className: CARD, "data-pptx-text-card": true, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.text.title", "Text") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Size" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              disabled: !canEdit,
              className: INPUT,
              min: 6,
              max: 200,
              step: "any",
              value: ((_b2 = selectedElement.textStyle) == null ? void 0 : _b2.fontSize) !== void 0 ? textFontSizePxToPt(selectedElement.textStyle.fontSize) : 18,
              onChange: (e) => onUpdateTextStyle({ fontSize: textFontSizePtToPx(Number(e.target.value)) })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "Color" }),
          (0, import_jsx_runtime.jsx)(
            DebouncedColorInput,
            {
              disabled: !canEdit,
              ariaLabel: "Text Color",
              value: normalizeHexColor2((_c = selectedElement.textStyle) == null ? void 0 : _c.color, "#000000"),
              className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
              onCommit: (hex) => onUpdateTextStyle({ color: hex, colorRef: void 0 })
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: "col-span-2", children: (0, import_jsx_runtime.jsx)(
          ThemeColorSwatchGrid,
          {
            prefix: "text-color",
            disabled: !canEdit,
            selectedRef: (_d = selectedElement.textStyle) == null ? void 0 : _d.colorRef,
            selectedHex: (_e = selectedElement.textStyle) == null ? void 0 : _e.color,
            onPick: (c) => onUpdateTextStyle({ color: c.hex, colorRef: c.ref })
          }
        ) }),
        (0, import_jsx_runtime.jsx)("div", { className: "col-span-2", children: (0, import_jsx_runtime.jsx)(
          RecentColorsRow,
          {
            prefix: "text-color",
            disabled: !canEdit,
            onCommit: (hex) => onUpdateTextStyle({ color: hex, colorRef: void 0 })
          }
        ) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1 col-span-2", children: [
          (0, import_jsx_runtime.jsx)(
            TextFormatToggle,
            {
              label: "B",
              active: Boolean((_f = selectedElement.textStyle) == null ? void 0 : _f.bold),
              disabled: !canEdit,
              onClick: () => {
                var _a2;
                return onUpdateTextStyle({ bold: !((_a2 = selectedElement.textStyle) == null ? void 0 : _a2.bold) });
              }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            TextFormatToggle,
            {
              label: "I",
              active: Boolean((_g = selectedElement.textStyle) == null ? void 0 : _g.italic),
              disabled: !canEdit,
              italic: true,
              onClick: () => {
                var _a2;
                return onUpdateTextStyle({
                  italic: !((_a2 = selectedElement.textStyle) == null ? void 0 : _a2.italic)
                });
              }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            TextFormatToggle,
            {
              label: "U",
              active: Boolean((_h = selectedElement.textStyle) == null ? void 0 : _h.underline),
              disabled: !canEdit,
              underline: true,
              onClick: () => {
                var _a2;
                return onUpdateTextStyle({
                  underline: !((_a2 = selectedElement.textStyle) == null ? void 0 : _a2.underline)
                });
              }
            }
          )
        ] })
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)(
      TextAdvancedSections,
      {
        selectedElement,
        canEdit,
        onUpdateTextStyle
      }
    ),
    shouldShowAccessibilitySection(selectedElement) && (0, import_jsx_runtime.jsx)(
      AccessibilityTextSection,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    )
  ] });
}
function TextFormatToggle({
  label,
  active,
  disabled,
  italic,
  underline,
  onClick
}) {
  return (0, import_jsx_runtime.jsx)(
    "button",
    {
      type: "button",
      disabled,
      className: cn(
        "px-2 py-1 rounded text-[11px] transition-colors",
        italic && "italic",
        underline && "underline",
        active ? "bg-primary text-white" : "bg-muted hover:bg-accent"
      ),
      onClick,
      children: label
    }
  );
}
function ProcessThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "2", y: "10", width: "10", height: "12", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("path", { d: "M14 16 L17 16", stroke: "currentColor", strokeWidth: "1.5", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "19", y: "10", width: "10", height: "12", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("path", { d: "M31 16 L34 16", stroke: "currentColor", strokeWidth: "1.5", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "36", y: "10", width: "10", height: "12", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function HierarchyThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "17", y: "2", width: "14", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("path", { d: "M24 10 L12 18 M24 10 L36 18", stroke: "currentColor", strokeWidth: "1", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "18", width: "14", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "30", y: "18", width: "14", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function CycleThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("circle", { cx: "24", cy: "5", r: "4", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "37", cy: "20", r: "4", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "11", cy: "20", r: "4", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)(
      "path",
      {
        d: "M28 6 L34 17 M33 23 L14 23 M14 18 L20 7",
        stroke: "currentColor",
        strokeWidth: "1",
        opacity: 0.4,
        fill: "none"
      }
    )
  ] });
}
function MatrixThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "3", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "26", y: "3", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "18", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "26", y: "18", width: "18", height: "11", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function PyramidThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "16", y: "2", width: "16", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "10", y: "12", width: "28", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "22", width: "40", height: "8", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function ListThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "3", width: "40", height: "7", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "13", width: "40", height: "7", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "4", y: "23", width: "40", height: "7", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
function FunnelThumb() {
  return (0, import_jsx_runtime.jsx)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: (0, import_jsx_runtime.jsx)("path", { d: "M4 3 L44 3 L28 16 L28 29 L20 29 L20 16 Z", fill: "currentColor", opacity: 0.7 }) });
}
function TargetThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("circle", { cx: "24", cy: "16", r: "14", fill: "currentColor", opacity: 0.3 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "24", cy: "16", r: "9", fill: "currentColor", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "24", cy: "16", r: "4", fill: "currentColor", opacity: 0.8 })
  ] });
}
function GearThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("circle", { cx: "16", cy: "16", r: "9", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "16", cy: "16", r: "3", fill: "white", opacity: 0.9 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "33", cy: "16", r: "6", fill: "currentColor", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "33", cy: "16", r: "2", fill: "white", opacity: 0.9 })
  ] });
}
function VennThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("circle", { cx: "19", cy: "16", r: "11", fill: "currentColor", opacity: 0.45 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "29", cy: "16", r: "11", fill: "currentColor", opacity: 0.45 })
  ] });
}
function TimelineThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("path", { d: "M4 16 L44 16", stroke: "currentColor", strokeWidth: "1.5", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "10", cy: "16", r: "3", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "24", cy: "16", r: "3", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "38", cy: "16", r: "3", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "6", y: "4", width: "8", height: "6", rx: "1", fill: "currentColor", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("rect", { x: "34", y: "4", width: "8", height: "6", rx: "1", fill: "currentColor", opacity: 0.5 })
  ] });
}
function RelationshipThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("circle", { cx: "14", cy: "16", r: "10", fill: "currentColor", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("circle", { cx: "34", cy: "16", r: "10", fill: "currentColor", opacity: 0.5 }),
    (0, import_jsx_runtime.jsx)("path", { d: "M14 6 L34 26 M34 6 L14 26", stroke: "currentColor", strokeWidth: "1", opacity: 0.3 })
  ] });
}
function ChevronThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("path", { d: "M2 8 L14 8 L20 16 L14 24 L2 24 L8 16 Z", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("path", { d: "M17 8 L29 8 L35 16 L29 24 L17 24 L23 16 Z", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)("path", { d: "M32 8 L44 8 L44 24 L32 24 L38 16 Z", fill: "currentColor", opacity: 0.7 })
  ] });
}
function BendingThumb() {
  return (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 48 32", className: "w-full h-full", children: [
    (0, import_jsx_runtime.jsx)("rect", { x: "2", y: "3", width: "11", height: "9", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)(
      "path",
      {
        d: "M13 7 L20 7 L20 16",
        stroke: "currentColor",
        strokeWidth: "1.5",
        fill: "none",
        opacity: 0.5
      }
    ),
    (0, import_jsx_runtime.jsx)("rect", { x: "19", y: "12", width: "11", height: "9", rx: "2", fill: "currentColor", opacity: 0.7 }),
    (0, import_jsx_runtime.jsx)(
      "path",
      {
        d: "M30 16 L37 16 L37 25",
        stroke: "currentColor",
        strokeWidth: "1.5",
        fill: "none",
        opacity: 0.5
      }
    ),
    (0, import_jsx_runtime.jsx)("rect", { x: "35", y: "21", width: "11", height: "9", rx: "2", fill: "currentColor", opacity: 0.7 })
  ] });
}
var THUMB_COMPONENTS = {
  process: ProcessThumb,
  hierarchy: HierarchyThumb,
  cycle: CycleThumb,
  matrix: MatrixThumb,
  pyramid: PyramidThumb,
  list: ListThumb,
  funnel: FunnelThumb,
  target: TargetThumb,
  gear: GearThumb,
  venn: VennThumb,
  timeline: TimelineThumb,
  relationship: RelationshipThumb,
  chevron: ChevronThumb,
  bending: BendingThumb
};
var LAYOUT_DESCRIPTORS = SWITCHABLE_LAYOUT_TYPES.map((type) => ({
  type,
  i18nKey: `pptx.smartart.category.${type}`
}));
function SmartArtLayoutSwitcher({
  smartArtData,
  canEdit,
  onUpdateSmartArt
}) {
  const { t } = useTranslation();
  const currentLayout = smartArtData.resolvedLayoutType ?? "list";
  const handleLayoutSwitch = (newLayout) => {
    if (!canEdit || newLayout === currentLayout) {
      return;
    }
    const updated = switchSmartArtLayout(smartArtData, newLayout);
    onUpdateSmartArt({
      layoutType: updated.layoutType,
      resolvedLayoutType: updated.resolvedLayoutType,
      layout: updated.layout,
      layoutDirty: updated.layoutDirty,
      drawingDirty: updated.drawingDirty,
      drawingShapes: updated.drawingShapes
    });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] text-muted-foreground", children: t("pptx.smartart.switchLayout") }),
    (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-3 gap-1.5", children: LAYOUT_DESCRIPTORS.map(({ type, i18nKey }) => {
      const isActive = currentLayout === type;
      const ThumbComponent = THUMB_COMPONENTS[type];
      return (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          "data-testid": `smartart-layout-${type}`,
          disabled: !canEdit,
          className: cn(
            "flex flex-col items-center gap-0.5 rounded border p-1.5 transition-colors",
            "text-[9px] leading-tight",
            isActive ? "border-primary bg-primary/15 text-primary" : "border-border text-muted-foreground hover:bg-muted hover:text-foreground",
            !canEdit && "opacity-50 cursor-not-allowed"
          ),
          onClick: () => handleLayoutSwitch(type),
          title: t(i18nKey),
          children: [
            (0, import_jsx_runtime.jsx)("div", { className: "w-10 h-7", children: ThumbComponent ? (0, import_jsx_runtime.jsx)(ThumbComponent, {}) : null }),
            (0, import_jsx_runtime.jsx)("span", { className: "truncate w-full text-center", children: t(i18nKey) })
          ]
        },
        type
      );
    }) })
  ] });
}
var DEFAULT_FILL = "#3b82f6";
var DEFAULT_FONT = "#ffffff";
var EMPHASIS_BTN = "px-1.5 py-0.5 text-[10px] rounded border transition-colors disabled:opacity-40 disabled:cursor-not-allowed";
function SmartArtNodeStyleControls({
  nodeId,
  style,
  label,
  canEdit,
  onChangeStyle
}) {
  const { pushColor } = useRecentColors();
  const bold = Boolean(style == null ? void 0 : style.bold);
  const italic = Boolean(style == null ? void 0 : style.italic);
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "flex items-center gap-1.5 mt-1 pl-4",
      role: "group",
      "aria-label": `Style for ${label}`,
      children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-0.5 text-[9px] text-muted-foreground", children: [
          (0, import_jsx_runtime.jsx)("span", { children: "Fill" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              "aria-label": `Fill colour for ${label}`,
              className: "h-4 w-5 cursor-pointer rounded border border-border bg-transparent p-0 disabled:cursor-not-allowed",
              value: (style == null ? void 0 : style.fillColor) ?? DEFAULT_FILL,
              onChange: (e) => {
                onChangeStyle(nodeId, { fillColor: e.target.value });
                pushColor(e.target.value);
              }
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-0.5 text-[9px] text-muted-foreground", children: [
          (0, import_jsx_runtime.jsx)("span", { children: "Text" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              "aria-label": `Font colour for ${label}`,
              className: "h-4 w-5 cursor-pointer rounded border border-border bg-transparent p-0 disabled:cursor-not-allowed",
              value: (style == null ? void 0 : style.fontColor) ?? DEFAULT_FONT,
              onChange: (e) => {
                onChangeStyle(nodeId, { fontColor: e.target.value });
                pushColor(e.target.value);
              }
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            "aria-label": `Bold ${label}`,
            "aria-pressed": bold,
            className: cn(
              EMPHASIS_BTN,
              "font-bold",
              bold ? "border-primary bg-primary/20 text-primary" : "border-border text-muted-foreground"
            ),
            onClick: () => onChangeStyle(nodeId, { bold: !bold }),
            children: "B"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            "aria-label": `Italic ${label}`,
            "aria-pressed": italic,
            className: cn(
              EMPHASIS_BTN,
              "italic",
              italic ? "border-primary bg-primary/20 text-primary" : "border-border text-muted-foreground"
            ),
            onClick: () => onChangeStyle(nodeId, { italic: !italic }),
            children: "I"
          }
        )
      ]
    }
  );
}
var MINI_BTN = "text-[9px] text-muted-foreground hover:text-primary px-1 disabled:opacity-40 disabled:cursor-not-allowed";
function SmartArtNodeRow({
  nodeId,
  text,
  displayIndex,
  isChild,
  canEdit,
  removeDisabled,
  moveUpDisabled,
  moveDownDisabled,
  inputRef,
  style,
  onChangeText,
  onKeyDown,
  onAddSubItem,
  onMoveUp,
  onMoveDown,
  onRemove,
  onChangeStyle
}) {
  const { t } = useTranslation();
  const label = isChild ? `Sub-item: ${text || "empty"}` : `Item ${displayIndex}: ${text || "empty"}`;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      role: "listitem",
      className: cn(
        "rounded border bg-background/60 p-1.5",
        isChild ? "border-border/60 ml-4" : "border-border"
      ),
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground w-3 shrink-0", "aria-hidden": "true", children: isChild ? "•" : `${displayIndex}` }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              ref: inputRef,
              type: "text",
              "data-testid": "smartart-node-text",
              disabled: !canEdit,
              "aria-label": label,
              className: cn(INPUT, "flex-1 text-[11px] py-0.5"),
              value: text,
              onChange: (e) => onChangeText(nodeId, e.target.value),
              onKeyDown: (e) => onKeyDown(e, nodeId),
              placeholder: t("pptx.smartArt.nodePlaceholder")
            }
          ),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-0.5 shrink-0", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit || moveUpDisabled,
                className: MINI_BTN,
                onClick: () => onMoveUp(nodeId),
                "aria-label": `Move ${label} up`,
                title: t("pptx.smartArt.moveUp"),
                children: "↑"
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit || moveDownDisabled,
                className: MINI_BTN,
                onClick: () => onMoveDown(nodeId),
                "aria-label": `Move ${label} down`,
                title: t("pptx.smartArt.moveDown"),
                children: "↓"
              }
            ),
            !isChild && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                className: MINI_BTN,
                onClick: () => onAddSubItem(nodeId),
                "aria-label": `Add sub-item under ${label}`,
                title: t("pptx.smartArt.addSubItem"),
                children: "+Sub"
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit || removeDisabled,
                className: cn(MINI_BTN, "hover:text-red-400"),
                onClick: () => onRemove(nodeId),
                "aria-label": `Remove ${label}`,
                title: removeDisabled ? t("pptx.smartArt.layoutMinimum") : t("pptx.smartArt.remove"),
                children: "x"
              }
            )
          ] })
        ] }),
        (0, import_jsx_runtime.jsx)(
          SmartArtNodeStyleControls,
          {
            nodeId,
            style,
            label,
            canEdit,
            onChangeStyle
          }
        )
      ]
    }
  );
}
function SmartArtStyleControls({
  colorSchemes,
  styleOptions,
  colorScheme,
  style,
  canEdit,
  onChangeColorScheme,
  onChangeStyle
}) {
  const { t } = useTranslation();
  const translate = import_react2.default.useCallback((key) => t(key), [t]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.smartart.colorScheme") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          disabled: !canEdit,
          "data-testid": "smartart-color-scheme",
          "aria-label": t("pptx.smartart.colorScheme"),
          className: cn(INPUT, "w-full"),
          value: colorScheme,
          onChange: (e) => onChangeColorScheme(e.target.value),
          children: colorSchemes.map((cs) => (0, import_jsx_runtime.jsx)("option", { value: cs, children: schemaLabel(SMARTART_COLOR_SCHEME_LABEL_KEYS, cs, translate) }, cs))
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.smartart.style") }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", role: "group", "aria-label": t("pptx.smartart.style"), children: styleOptions.map((s) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          "aria-pressed": style === s,
          className: cn(
            "flex-1 px-2 py-1 text-[10px] rounded border transition-colors",
            style === s ? "border-primary bg-primary/20 text-primary" : "border-border text-muted-foreground hover:bg-muted"
          ),
          onClick: () => onChangeStyle(s),
          children: schemaLabel(SMARTART_STYLE_LABEL_KEYS, s, translate)
        },
        s
      )) })
    ] })
  ] });
}
var COLOR_SCHEMES = [
  "colorful1",
  "colorful2",
  "colorful3",
  "monochromatic1",
  "monochromatic2"
];
var STYLE_OPTIONS = ["flat", "moderate", "intense"];
function SmartArtPropertiesPanel({
  smartArtData,
  canEdit,
  onUpdateElement,
  box
}) {
  const { t } = useTranslation();
  const nodes = smartArtData.nodes ?? [];
  const layout = smartArtData.resolvedLayoutType;
  const topLevelCount = nodes.filter((n) => !n.parentId).length;
  const inputRefs = import_react2.default.useRef(/* @__PURE__ */ new Map());
  const pendingFocusId = import_react2.default.useRef(null);
  import_react2.default.useEffect(() => {
    var _a;
    const id = pendingFocusId.current;
    if (id) {
      (_a = inputRefs.current.get(id)) == null ? void 0 : _a.focus();
      pendingFocusId.current = null;
    }
  });
  const applySmartArtData = (newData, focusId) => {
    if (focusId) {
      pendingFocusId.current = focusId;
    }
    const reflowed = box ? rebuildDrawingShapesIfCleared(
      newData,
      newData.layout,
      resolveSmartArtDataPalette(newData),
      newData.style ?? "flat",
      "inspector",
      box
    ) : newData;
    onUpdateElement({ smartArtData: reflowed });
  };
  const updateSmartArt = (patch) => {
    applySmartArtData({ ...smartArtData, ...patch });
  };
  const handleUpdateNodeText = (nodeId, text) => {
    applySmartArtData(updateSmartArtNodeText(smartArtData, nodeId, text));
  };
  const handleChangeNodeStyle = (nodeId, patch) => {
    const next = setSmartArtNodeStyle(smartArtData, nodeId, patch);
    if (next !== smartArtData) {
      applySmartArtData(next);
    }
  };
  const addNode = () => {
    if (!canAddTopLevelNode(layout, topLevelCount)) {
      return;
    }
    applySmartArtData(addSmartArtNodeAsChild(smartArtData));
  };
  const addSubItem = (parentId) => {
    applySmartArtData(addSmartArtNodeAsChild(smartArtData, parentId, "Sub-item"));
  };
  const removeNode = (nodeId) => {
    var _a;
    const isTopLevel = !((_a = nodes.find((n) => n.id === nodeId)) == null ? void 0 : _a.parentId);
    if (isTopLevel && !canRemoveTopLevelNode(layout, topLevelCount)) {
      return;
    }
    applySmartArtData(removeSmartArtNode(smartArtData, nodeId));
  };
  const moveNode = (nodeId, direction) => {
    const next = reorder2(smartArtData, nodeId, direction);
    if (next) {
      applySmartArtData(next, nodeId);
    }
  };
  const handleKeyDown = (e, nodeId) => {
    const node = nodes.find((n) => n.id === nodeId);
    const isEmpty = !(node == null ? void 0 : node.text);
    if (e.key === "Enter") {
      e.preventDefault();
      const result = addSiblingAfter(smartArtData, nodeId);
      if (result) {
        applySmartArtData(result.data, result.focusNodeId);
      }
    } else if ((e.key === "Backspace" || e.key === "Delete") && isEmpty) {
      const isTop = !(node == null ? void 0 : node.parentId);
      if (isTop && !canRemoveTopLevelNode(layout, topLevelCount)) {
        return;
      }
      e.preventDefault();
      const result = removeEmptyNode(smartArtData, nodeId);
      if (result) {
        applySmartArtData(result.data, result.focusNodeId);
      }
    } else if (e.key === "Tab" && !e.shiftKey) {
      e.preventDefault();
      const next = demote(smartArtData, nodeId);
      if (next) {
        applySmartArtData(next, nodeId);
      }
    } else if (e.key === "Tab" && e.shiftKey) {
      e.preventDefault();
      const next = promote(smartArtData, nodeId);
      if (next) {
        applySmartArtData(next, nodeId);
      }
    }
  };
  const setInputRef = (nodeId) => (el) => {
    if (el) {
      inputRefs.current.set(nodeId, el);
    } else {
      inputRefs.current.delete(nodeId);
    }
  };
  const boundsHint = describeSmartArtBounds(layout);
  const addDisabled = !canEdit || !canAddTopLevelNode(layout, topLevelCount);
  const extraConnections = classifyExtraConnections(smartArtData);
  const extraConnectionSummary = Array.from(
    new Set(extraConnections.map((c) => c.type ?? "unknown"))
  ).join(", ");
  let topDisplayIndex = 0;
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, role: "group", "aria-label": t("pptx.smartart.title"), children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.smartart.title") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
      (0, import_jsx_runtime.jsx)(
        SmartArtLayoutSwitcher,
        {
          smartArtData,
          canEdit,
          onUpdateSmartArt: updateSmartArt
        }
      ),
      (0, import_jsx_runtime.jsx)(
        SmartArtStyleControls,
        {
          colorSchemes: COLOR_SCHEMES,
          styleOptions: STYLE_OPTIONS,
          colorScheme: smartArtData.colorScheme ?? "colorful1",
          style: smartArtData.style ?? "flat",
          canEdit,
          onChangeColorScheme: (colorScheme) => updateSmartArt({ colorScheme }),
          onChangeStyle: (style) => updateSmartArt({ style })
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
        (0, import_jsx_runtime.jsxs)("span", { className: "text-[11px] text-muted-foreground", children: [
          t("pptx.smartart.textPane"),
          " (",
          nodes.length,
          ")"
        ] }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: addDisabled,
            className: BTN,
            onClick: addNode,
            title: addDisabled ? boundsHint : void 0,
            children: t("pptx.smartart.addItem")
          }
        )
      ] }),
      boundsHint && (0, import_jsx_runtime.jsx)("div", { className: "text-[9px] text-muted-foreground", role: "note", children: boundsHint }),
      (0, import_jsx_runtime.jsx)("div", { className: "max-h-52 overflow-y-auto space-y-1 pr-1", role: "list", children: nodes.map((node) => {
        const isChild = Boolean(node.parentId);
        if (!isChild) {
          topDisplayIndex += 1;
        }
        const sIdx = siblingIndex(smartArtData, node.id);
        const sCount = siblingCount(smartArtData, node.id);
        const removeDisabled = nodes.length <= 1 || !isChild && !canRemoveTopLevelNode(layout, topLevelCount);
        return (0, import_jsx_runtime.jsx)(
          SmartArtNodeRow,
          {
            nodeId: node.id,
            text: node.text,
            displayIndex: topDisplayIndex,
            isChild,
            canEdit,
            removeDisabled,
            moveUpDisabled: sIdx <= 0,
            moveDownDisabled: sIdx < 0 || sIdx >= sCount - 1,
            inputRef: setInputRef(node.id),
            style: node.style,
            onChangeText: handleUpdateNodeText,
            onKeyDown: handleKeyDown,
            onAddSubItem: addSubItem,
            onMoveUp: (id) => moveNode(id, -1),
            onMoveDown: (id) => moveNode(id, 1),
            onRemove: removeNode,
            onChangeStyle: handleChangeNodeStyle
          },
          node.id
        );
      }) }),
      extraConnections.length > 0 && (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "text-[9px] text-muted-foreground",
          role: "note",
          "aria-label": `${extraConnections.length} non-tree relationship connection(s): ${extraConnectionSummary}`,
          children: [
            t("pptx.smartart.extraConnections", { count: extraConnections.length }),
            (0, import_jsx_runtime.jsx)("span", { className: "block opacity-80", children: extraConnectionSummary })
          ]
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "text-[9px] text-muted-foreground mt-1", children: t("pptx.smartart.tabHint") })
    ] })
  ] });
}
var HEADER_CELL = "flex items-center justify-center gap-0.5 bg-muted text-muted-foreground border border-border -m-px px-1 py-0.5 whitespace-nowrap";
var CELL_INPUT2 = "w-full box-border bg-muted px-1 py-0.5 text-[11px] border-0 outline-none focus:bg-accent disabled:opacity-60";
var REMOVE_BTN = "px-0.5 leading-none text-destructive hover:opacity-80";
function TableDataGrid({
  tableElement,
  canEdit,
  onUpdateElement
}) {
  const { t } = useTranslation();
  const grid = buildTableDataGrid(tableElement);
  if (grid.rowCount === 0 || grid.colCount === 0) {
    return null;
  }
  const commit = (next) => {
    if (next === tableElement) {
      return;
    }
    onUpdateElement({ tableData: next.tableData, rawXml: next.rawXml });
  };
  return (0, import_jsx_runtime.jsx)("div", { className: CARD, children: (0, import_jsx_runtime.jsxs)("section", { "aria-label": t("pptx.tableDataEditor.ariaLabel"), children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-1 mb-1.5", children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.inspector.tableData") }),
      canEdit && (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-wrap gap-0.5", children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            title: t("pptx.tableDataEditor.addRowTitle"),
            onClick: () => commit(appendTableElementRow(tableElement)),
            children: t("pptx.tableDataEditor.addRowLabel")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !grid.canRemoveRow,
            title: t("pptx.tableDataEditor.removeRowTitle"),
            onClick: () => commit(removeLastTableElementRow(tableElement)),
            children: t("pptx.tableDataEditor.removeRowLabel")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            title: t("pptx.tableDataEditor.addColumnTitle"),
            onClick: () => commit(appendTableElementColumn(tableElement)),
            children: t("pptx.tableDataEditor.addColumnLabel")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !grid.canRemoveColumn,
            title: t("pptx.tableDataEditor.removeColumnTitle"),
            onClick: () => commit(removeLastTableElementColumn(tableElement)),
            children: t("pptx.tableDataEditor.removeColumnLabel")
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)("div", { className: "overflow-x-auto", children: (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col text-[11px] w-max min-w-full", role: "grid", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex", role: "row", children: [
        (0, import_jsx_runtime.jsx)("div", { className: `${HEADER_CELL} flex-none w-10`, "aria-hidden": "true" }),
        grid.colIndices.map((colIndex) => (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: `${HEADER_CELL} flex-1 basis-16`,
            role: "columnheader",
            children: [
              (0, import_jsx_runtime.jsx)("span", { children: colIndex + 1 }),
              canEdit && grid.canRemoveColumn && (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  className: REMOVE_BTN,
                  "aria-label": t("pptx.tableDataEditor.removeColumnN", {
                    number: colIndex + 1
                  }),
                  title: t("pptx.tableDataEditor.removeColumnN", { number: colIndex + 1 }),
                  onClick: () => commit(removeTableElementColumn(tableElement, colIndex)),
                  children: "×"
                }
              )
            ]
          },
          colIndex
        ))
      ] }),
      grid.rows.map((row) => (0, import_jsx_runtime.jsxs)("div", { className: "flex", role: "row", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: `${HEADER_CELL} flex-none w-10`, role: "rowheader", children: [
          (0, import_jsx_runtime.jsx)("span", { children: row.rowIndex + 1 }),
          canEdit && grid.canRemoveRow && (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: REMOVE_BTN,
              "aria-label": t("pptx.tableDataEditor.removeRowN", {
                number: row.rowIndex + 1
              }),
              title: t("pptx.tableDataEditor.removeRowN", { number: row.rowIndex + 1 }),
              onClick: () => commit(removeTableElementRow(tableElement, row.rowIndex)),
              children: "×"
            }
          )
        ] }),
        row.cells.map((cell) => (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "flex flex-1 basis-16 p-px border border-border -m-px",
            role: "gridcell",
            children: (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                className: CELL_INPUT2,
                disabled: !canEdit,
                "aria-label": t("pptx.tableDataEditor.cellAriaLabel", {
                  row: cell.rowIndex + 1,
                  column: cell.colIndex + 1
                }),
                value: cell.text,
                onChange: (event) => commit(
                  setTableElementCellText(
                    tableElement,
                    cell.rowIndex,
                    cell.colIndex,
                    event.target.value
                  )
                )
              }
            )
          },
          cell.colIndex
        ))
      ] }, row.rowIndex))
    ] }) })
  ] }) });
}
function TableCellPatternFill({
  cellStyle,
  canEdit,
  onUpdateCellStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const translate = (key) => t(key);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.patternPreset") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.table.patternPreset"),
          disabled: !canEdit,
          className: SEL,
          value: cellStyle.patternFillPreset ?? "ltDnDiag",
          onChange: (e) => onUpdateCellStyle({ patternFillPreset: e.target.value }),
          children: PATTERN_OPTIONS.map((p) => (0, import_jsx_runtime.jsx)("option", { value: p, children: schemaLabel(FILL_PATTERN_LABEL_KEYS, p, translate) }, p))
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.patternForeground") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
            value: cellStyle.patternFillForeground ?? "#000000",
            onChange: (e) => {
              onUpdateCellStyle({ patternFillForeground: e.target.value });
              pushColor(e.target.value);
            }
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.patternBackground") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
            value: cellStyle.patternFillBackground ?? "#FFFFFF",
            onChange: (e) => {
              onUpdateCellStyle({ patternFillBackground: e.target.value });
              pushColor(e.target.value);
            }
          }
        )
      ] })
    ] })
  ] });
}
function TableCellAdvancedFill({
  cellStyle,
  canEdit,
  onUpdateCellStyle
}) {
  const { t } = useTranslation();
  const fillMode = cellStyle.fillMode ?? "solid";
  const handleFillModeChange = (mode) => {
    const next = mode;
    if (next === "gradient") {
      onUpdateCellStyle({
        fillMode: "gradient",
        gradientFillType: cellStyle.gradientFillType ?? "linear",
        gradientFillAngle: cellStyle.gradientFillAngle ?? 90,
        gradientFillStops: cellStyle.gradientFillStops ?? [
          { color: "#FF0000", position: 0 },
          { color: "#0000FF", position: 100 }
        ]
      });
    } else if (next === "pattern") {
      onUpdateCellStyle({
        fillMode: "pattern",
        patternFillPreset: cellStyle.patternFillPreset ?? "ltDnDiag",
        patternFillForeground: cellStyle.patternFillForeground ?? "#000000",
        patternFillBackground: cellStyle.patternFillBackground ?? "#FFFFFF"
      });
    } else {
      onUpdateCellStyle({ fillMode: next });
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.fillMode") }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": t("pptx.table.fillMode"),
          disabled: !canEdit,
          className: SEL,
          value: fillMode,
          onChange: (e) => handleFillModeChange(e.target.value),
          children: FILL_MODE_OPTIONS.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.value ?? "", children: t(opt.i18nKey) }, opt.value))
        }
      )
    ] }),
    fillMode === "gradient" && (0, import_jsx_runtime.jsx)(
      GradientControls,
      {
        cellStyle,
        canEdit,
        onUpdateCellStyle
      }
    ),
    fillMode === "pattern" && (0, import_jsx_runtime.jsx)(
      TableCellPatternFill,
      {
        cellStyle,
        canEdit,
        onUpdateCellStyle
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
      (0, import_jsx_runtime.jsx)("span", { className: SECTION_HEADING, children: t("pptx.table.margins") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-1.5", children: [
        ["marginTop", "pptx.table.marginTop"],
        ["marginBottom", "pptx.table.marginBottom"],
        ["marginLeft", "pptx.table.marginLeft"],
        ["marginRight", "pptx.table.marginRight"]
      ].map(([key, i18nKey]) => (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t(i18nKey) }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: NUM,
            min: 0,
            max: 200,
            value: cellStyle[key] ?? 0,
            onChange: (e) => onUpdateCellStyle({
              [key]: Number(e.target.value)
            })
          }
        )
      ] }, key)) })
    ] })
  ] });
}
function GradientControls({
  cellStyle,
  canEdit,
  onUpdateCellStyle
}) {
  const { t } = useTranslation();
  const { pushColor } = useRecentColors();
  const stops = cellStyle.gradientFillStops ?? [];
  const gradType = cellStyle.gradientFillType ?? "linear";
  const updateStop = (index, patch) => {
    const next = stops.map((s, i) => i === index ? { ...s, ...patch } : s);
    onUpdateCellStyle({ gradientFillStops: next });
  };
  const addStop = () => {
    onUpdateCellStyle({
      gradientFillStops: [...stops, { color: "#888888", position: 50 }]
    });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.gradientType") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.table.gradientType"),
            disabled: !canEdit,
            className: SEL,
            value: gradType,
            onChange: (e) => onUpdateCellStyle({
              gradientFillType: e.target.value
            }),
            children: GRADIENT_TYPE_OPTIONS.map((o) => (0, import_jsx_runtime.jsx)("option", { value: o.value, children: t(o.i18nKey) }, o.value))
          }
        )
      ] }),
      gradType === "linear" && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.gradientAngle") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: NUM,
            min: 0,
            max: 360,
            value: cellStyle.gradientFillAngle ?? 90,
            onChange: (e) => onUpdateCellStyle({
              gradientFillAngle: Number(e.target.value)
            })
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)("span", { className: LBL, children: t("pptx.table.gradientStops") }),
    stops.map((stop, idx) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "color",
          disabled: !canEdit,
          className: "h-6 w-6 rounded border border-border cursor-pointer",
          value: stop.color,
          onChange: (e) => {
            updateStop(idx, { color: e.target.value });
            pushColor(e.target.value);
          }
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "number",
          disabled: !canEdit,
          className: NUM,
          min: 0,
          max: 100,
          value: Math.round(stop.position),
          onChange: (e) => updateStop(idx, {
            position: Number(e.target.value)
          })
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: "%" })
    ] }, idx)),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: "text-[10px] text-primary hover:underline",
        onClick: addStop,
        children: t("pptx.table.gradientAddStop")
      }
    )
  ] });
}
function TableCellColorField({
  label,
  prefix,
  value,
  fallback,
  selectedRef,
  disabled,
  onCommit
}) {
  const hex = normalizeHexColor2(value, fallback);
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: label }),
    (0, import_jsx_runtime.jsx)(
      DebouncedColorInput,
      {
        disabled,
        value: hex,
        className: "w-full h-7 rounded border border-border bg-transparent cursor-pointer",
        onCommit: (next) => onCommit(next, void 0)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ThemeColorSwatchGrid,
      {
        prefix,
        disabled,
        selectedRef,
        selectedHex: hex,
        onPick: (c) => onCommit(c.hex, c.ref)
      }
    )
  ] });
}
function TableCellFormattingPanel({
  tableData: td,
  tableEditorState,
  canEdit,
  onUpdateTableData
}) {
  var _a;
  const { t } = useTranslation();
  const { rowIndex, columnIndex } = tableEditorState;
  const cell = (_a = td.rows[rowIndex]) == null ? void 0 : _a.cells[columnIndex];
  if (!cell) {
    return null;
  }
  const cs = cell.style ?? {};
  const updateCellStyle = (updates) => {
    const newRows = td.rows.map((row, ri) => {
      if (ri !== rowIndex) {
        return row;
      }
      return {
        ...row,
        cells: row.cells.map((c, ci) => {
          if (ci !== columnIndex) {
            return c;
          }
          return { ...c, style: { ...cs, ...updates } };
        })
      };
    });
    onUpdateTableData({ rows: newRows });
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.table.cell", { row: rowIndex + 1, col: columnIndex + 1 }) }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-10 text-muted-foreground", children: t("pptx.table.fontSize") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: INPUT,
            min: 6,
            max: 200,
            value: cs.fontSize ?? 14,
            onChange: (e) => updateCellStyle({ fontSize: Number(e.target.value) })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
        (0, import_jsx_runtime.jsx)(
          TableCellColorField,
          {
            label: t("pptx.table.color"),
            prefix: "table-cell-text",
            value: cs.color,
            fallback: "#000000",
            selectedRef: cs.colorRef,
            disabled: !canEdit,
            onCommit: (hex, ref) => updateCellStyle({ color: hex, colorRef: ref })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          TableCellColorField,
          {
            label: t("pptx.table.background"),
            prefix: "table-cell-bg",
            value: cs.backgroundColor,
            fallback: "#ffffff",
            selectedRef: cs.backgroundColorRef,
            disabled: !canEdit,
            onCommit: (hex, ref) => updateCellStyle({ backgroundColor: hex, backgroundColorRef: ref })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        TableCellAdvancedFill,
        {
          cellStyle: cs,
          canEdit,
          onUpdateCellStyle: updateCellStyle
        }
      ),
      (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: [
        ["bold", "B", "font-bold"],
        ["italic", "I", "italic"],
        ["underline", "U", "underline"]
      ].map(([key, lbl, cls]) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "px-2 py-1 rounded text-[11px] transition-colors",
            cls,
            cs[key] ? "bg-primary text-white" : "bg-muted hover:bg-accent"
          ),
          onClick: () => updateCellStyle({ [key]: !cs[key] }),
          children: lbl
        },
        key
      )) }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: [
        ["left", "L"],
        ["center", "C"],
        ["right", "R"]
      ].map(([val, lbl]) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "px-2 py-1 rounded text-[11px] transition-colors",
            cs.align === val ? "bg-primary text-white" : "bg-muted hover:bg-accent"
          ),
          onClick: () => updateCellStyle({ align: val }),
          children: lbl
        },
        val
      )) }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: [
        ["top", "T"],
        ["middle", "M"],
        ["bottom", "B"]
      ].map(([val, lbl]) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: cn(
            "px-2 py-1 rounded text-[11px] transition-colors",
            cs.vAlign === val ? "bg-primary text-white" : "bg-muted hover:bg-accent"
          ),
          onClick: () => updateCellStyle({ vAlign: val }),
          children: lbl
        },
        val
      )) }),
      (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[11px]", children: t("pptx.table.cellBorders") }),
        (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-1.5", children: [
          ["Top", "borderTopColor", "borderTopWidth"],
          ["Bottom", "borderBottomColor", "borderBottomWidth"],
          ["Left", "borderLeftColor", "borderLeftWidth"],
          ["Right", "borderRightColor", "borderRightWidth"]
        ].map(([edge, colorKey, widthKey]) => (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-0.5", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t(`pptx.table.border${edge}`) }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1 items-center", children: [
            (0, import_jsx_runtime.jsx)(
              DebouncedColorInput,
              {
                disabled: !canEdit,
                value: normalizeHexColor2(cs[colorKey], "#374151"),
                className: "w-7 h-6 rounded border border-border bg-transparent cursor-pointer shrink-0",
                onCommit: (hex) => updateCellStyle({ [colorKey]: hex })
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "number",
                disabled: !canEdit,
                className: cn(INPUT, "w-14"),
                min: 0,
                max: 10,
                value: cs[widthKey] ?? 1,
                onChange: (e) => updateCellStyle({ [widthKey]: Number(e.target.value) })
              }
            )
          ] })
        ] }, edge)) })
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-3 gap-1", children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: cn(BTN, "text-center"),
            onClick: () => {
              const newRows = computeMergeCellRight(td, rowIndex, columnIndex);
              if (newRows) {
                onUpdateTableData({ rows: newRows });
              }
            },
            children: t("pptx.table.mergeRight")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: cn(BTN, "text-center"),
            onClick: () => {
              const newRows = computeMergeCellDown(td, rowIndex, columnIndex);
              if (newRows) {
                onUpdateTableData({ rows: newRows });
              }
            },
            children: t("pptx.table.mergeDown")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: cn(BTN, "text-center"),
            onClick: () => {
              const newRows = computeSplitCell(td, rowIndex, columnIndex);
              if (newRows) {
                onUpdateTableData({ rows: newRows });
              }
            },
            children: t("pptx.table.split")
          }
        )
      ] })
    ] })
  ] });
}
function TableStyleEditorFields({
  descriptor,
  canEdit,
  onEdit
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.tableStyleEditor.fillSection") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            value: descriptor.fill.color.hex,
            onChange: (e) => onEdit({ kind: "fillColor", hex: e.target.value, ref: void 0 }),
            className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer"
          }
        ),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: descriptor.fill.noFill,
              onChange: (e) => onEdit({ kind: "fillNone", noFill: e.target.checked })
            }
          ),
          t("pptx.tableStyleEditor.noFill")
        ] })
      ] }),
      (0, import_jsx_runtime.jsx)(
        ThemeColorSwatchGrid,
        {
          prefix: "table-style-fill",
          disabled: !canEdit,
          selectedRef: descriptor.fill.color.ref,
          selectedHex: descriptor.fill.color.hex,
          onPick: (c) => onEdit({ kind: "fillColor", hex: c.hex, ref: c.ref })
        }
      )
    ] }),
    descriptor.hasTextAndBorders && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.tableStyleEditor.textSection") }),
        (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: ["bold", "italic", "underline"].map((flag) => (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: `${BTN} ${descriptor.text[flag] ? "bg-accent" : ""}`,
            onClick: () => onEdit(textFlagEdit(flag, !descriptor.text[flag])),
            children: t(`pptx.format.${flag}`)
          },
          flag
        )) }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
          (0, import_jsx_runtime.jsx)("span", { children: t("pptx.tableStyleEditor.textColor") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "color",
              disabled: !canEdit,
              value: descriptor.text.color.hex,
              onChange: (e) => onEdit({ kind: "textColor", hex: e.target.value, ref: void 0 }),
              className: "h-6 w-8 rounded border border-border bg-transparent cursor-pointer"
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)(
          ThemeColorSwatchGrid,
          {
            prefix: "table-style-text",
            disabled: !canEdit,
            selectedRef: descriptor.text.color.ref,
            selectedHex: descriptor.text.color.hex,
            onPick: (c) => onEdit({ kind: "textColor", hex: c.hex, ref: c.ref })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.tableStyleEditor.bordersSection") }),
        TABLE_STYLE_BORDER_SIDES.map((side) => (0, import_jsx_runtime.jsx)(
          BorderSideRow,
          {
            side,
            state: descriptor.borders[side],
            canEdit,
            onEdit
          },
          side
        ))
      ] })
    ] })
  ] });
}
function textFlagEdit(flag, value) {
  if (flag === "bold") {
    return { kind: "textBold", value };
  }
  if (flag === "italic") {
    return { kind: "textItalic", value };
  }
  return { kind: "textUnderline", value };
}
function BorderSideRow({
  side,
  state,
  canEdit,
  onEdit
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1.5 text-[11px]", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "w-28 shrink-0 text-muted-foreground", children: t(TABLE_STYLE_BORDER_SIDE_LABEL_KEYS[side]) }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "color",
        disabled: !canEdit,
        value: state.color.hex,
        onChange: (e) => onEdit({ kind: "borderColor", side, hex: e.target.value, ref: void 0 }),
        className: "h-6 w-7 rounded border border-border bg-transparent cursor-pointer"
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "number",
        min: 0,
        max: 20,
        disabled: !canEdit,
        value: state.width,
        onChange: (e) => onEdit({ kind: "borderWidth", side, width: Number(e.target.value) }),
        className: `${INPUT} w-12`
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "select",
      {
        disabled: !canEdit,
        value: state.dash,
        onChange: (e) => onEdit({ kind: "borderDash", side, dash: e.target.value }),
        className: INPUT,
        children: TABLE_STYLE_DASH_PRESETS.map((dash) => (0, import_jsx_runtime.jsx)("option", { value: dash, children: dash }, dash))
      }
    ),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1 shrink-0", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: state.noFill,
          onChange: (e) => onEdit({ kind: "borderNone", side, noFill: e.target.checked })
        }
      ),
      t("pptx.tableStyleEditor.noBorder")
    ] })
  ] });
}
function TableStyleEditor({
  styleMap,
  styleId,
  canEdit,
  onStyleMapChange,
  onDeleteStyle,
  onAssignStyle,
  onClose
}) {
  const { t } = useTranslation();
  const themeColorMap = useThemeColorMap();
  const [activeStyleId, setActiveStyleId] = (0, import_react2.useState)(
    styleId ? normalizeTableStyleGuid(styleId) : ""
  );
  const [selectedPart, setSelectedPart] = (0, import_react2.useState)("wholeTbl");
  const entry = activeStyleId ? styleMap == null ? void 0 : styleMap[activeStyleId] : void 0;
  const descriptor = describeTableStyleEditor(entry, selectedPart, themeColorMap);
  function edit(fieldEdit) {
    if (!entry || !styleMap) {
      return;
    }
    const { entry: nextEntry } = applyTableStyleFieldEdit(entry, selectedPart, fieldEdit);
    onStyleMapChange({ ...styleMap, [nextEntry.styleId]: nextEntry });
  }
  function createFromCurrent() {
    const name = window.prompt(
      t("pptx.tableStyleEditor.newStyleNamePrompt"),
      entry ? `${entry.styleName ?? ""} Copy`.trim() : ""
    );
    if (!name) {
      return;
    }
    const nextMap = { ...styleMap ?? {} };
    const created = createTableStyleEntry(nextMap, { styleName: name, basedOn: entry });
    addTableStyleToMap(nextMap, created);
    onStyleMapChange(nextMap);
    setActiveStyleId(created.styleId);
    onAssignStyle == null ? void 0 : onAssignStyle(created.styleId);
  }
  function handleDelete() {
    if (!entry || !styleMap || !window.confirm(t("pptx.tableStyleEditor.deleteConfirm"))) {
      return;
    }
    const nextMap = { ...styleMap };
    deleteTableStyleFromMap(nextMap, entry.styleId);
    onStyleMapChange(nextMap);
    onDeleteStyle(entry.styleId);
    onClose();
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, "data-testid": "table-style-editor", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.tableStyleEditor.title") }),
      (0, import_jsx_runtime.jsx)("button", { type: "button", className: BTN, onClick: onClose, children: t("pptx.tableStyleEditor.close") })
    ] }),
    !entry && (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: t("pptx.tableStyleEditor.noStyleSelected") }),
    entry && (0, import_jsx_runtime.jsx)("div", { className: "flex flex-wrap gap-1", children: TABLE_STYLE_EDITOR_PARTS.map((part) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: `${BTN} ${selectedPart === part.id ? "bg-accent" : ""}`,
        onClick: () => setSelectedPart(part.id),
        children: t(part.labelKey)
      },
      part.id
    )) }),
    descriptor && (0, import_jsx_runtime.jsx)(TableStyleEditorFields, { descriptor, canEdit, onEdit: edit }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1.5 pt-1 border-t border-border", children: [
      (0, import_jsx_runtime.jsx)("button", { type: "button", className: BTN, disabled: !canEdit, onClick: createFromCurrent, children: entry ? t("pptx.tableStyleEditor.newFromCurrent") : t("pptx.tableStyleEditor.newStyle") }),
      entry && (0, import_jsx_runtime.jsx)("button", { type: "button", className: BTN, disabled: !canEdit, onClick: handleDelete, children: t("pptx.tableStyleEditor.deleteStyle") })
    ] })
  ] });
}
function TablePropertiesPanel({
  tableElement,
  canEdit,
  onUpdateElement,
  tableEditorState,
  tableStyleMap,
  onTableStyleMapChange,
  onDeleteTableStyle
}) {
  const { t } = useTranslation();
  const [showStyleEditor, setShowStyleEditor] = (0, import_react2.useState)(false);
  const td = tableElement.tableData;
  if (!td) {
    return null;
  }
  const rowCount = td.rows.length;
  const colCount = td.columnWidths.length;
  const updateTableData = (patch) => {
    onUpdateElement({
      tableData: { ...td, ...patch }
    });
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.table.title") }),
      (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground mb-2", children: t("pptx.table.rowsColumns", { rows: rowCount, cols: colCount }) }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: [
        ["bandedRows", "pptx.table.bandedRows"],
        ["firstRowHeader", "pptx.table.headerRow"],
        ["bandedColumns", "pptx.table.bandedColumns"],
        ["firstCol", "pptx.table.firstColumn"],
        ["lastCol", "pptx.table.lastColumn"],
        ["lastRow", "pptx.table.lastRow"]
      ].map(([key, i18nKey]) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            disabled: !canEdit,
            checked: Boolean(td[key]),
            onChange: (e) => updateTableData({ [key]: e.target.checked }),
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px]", children: t(i18nKey) })
      ] }, key)) }),
      td.bandedRows && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 mt-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] text-muted-foreground", children: t("pptx.table.bandRowCycle") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 1,
            max: 99,
            disabled: !canEdit,
            value: td.bandRowCycle ?? 1,
            onChange: (e) => updateTableData({
              bandRowCycle: Math.max(1, parseInt(e.target.value, 10) || 1)
            }),
            className: "w-14 rounded border border-border bg-background px-1 py-0.5 text-[11px]"
          }
        )
      ] }),
      td.bandedColumns && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 mt-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] text-muted-foreground", children: t("pptx.table.bandColCycle") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 1,
            max: 99,
            disabled: !canEdit,
            value: td.bandColCycle ?? 1,
            onChange: (e) => updateTableData({
              bandColCycle: Math.max(1, parseInt(e.target.value, 10) || 1)
            }),
            className: "w-14 rounded border border-border bg-background px-1 py-0.5 text-[11px]"
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.table.stylePresets") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-3 gap-1.5", children: TABLE_STYLE_PRESETS.map((preset) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          title: preset.label,
          "aria-label": preset.label,
          className: "rounded border border-border hover:border-primary overflow-hidden h-10 transition-colors",
          onClick: () => {
            updateTableData({ rows: applyTableStylePreset(td, preset) });
          },
          children: (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col h-full", children: [
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: preset.headerBg } }),
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: preset.bandBg } }),
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1 border-t", style: { borderColor: preset.borderColor } })
          ] })
        },
        preset.id
      )) }),
      onTableStyleMapChange && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: `${BTN} mt-1.5`,
          disabled: !canEdit,
          onClick: () => setShowStyleEditor((v) => !v),
          children: t("pptx.tableStyleEditor.editButton")
        }
      )
    ] }),
    showStyleEditor && onTableStyleMapChange && (0, import_jsx_runtime.jsx)(
      TableStyleEditor,
      {
        styleMap: tableStyleMap,
        styleId: td.tableStyleId,
        canEdit,
        onStyleMapChange: onTableStyleMapChange,
        onDeleteStyle: (styleId) => onDeleteTableStyle == null ? void 0 : onDeleteTableStyle(styleId),
        onAssignStyle: (styleId) => updateTableData(tableStyleAssignmentUpdate(styleId)),
        onClose: () => setShowStyleEditor(false)
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between mb-1", children: [
        (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.table.columnWidths") }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit,
            onClick: () => {
              updateTableData({ columnWidths: evenColumnWidths(colCount) });
            },
            children: t("pptx.table.even")
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: td.columnWidths.map((w, ci) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-6 text-muted-foreground shrink-0", children: ci + 1 }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "range",
            disabled: !canEdit,
            min: 5,
            max: 80,
            value: Math.round(w * 100),
            className: "flex-1 accent-primary",
            onChange: (e) => {
              const newPct = Number(e.target.value) / 100;
              updateTableData({
                columnWidths: redistributeColumnWidth(td.columnWidths, ci, newPct)
              });
            }
          }
        ),
        (0, import_jsx_runtime.jsxs)("span", { className: "w-10 text-right text-muted-foreground", children: [
          Math.round(w * 100),
          "%"
        ] })
      ] }, ci)) })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between mb-1", children: [
        (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.table.rowHeights") }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit,
            onClick: () => {
              updateTableData({ rows: evenRowHeights(td.rows) });
            },
            children: t("pptx.table.even")
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-1", children: td.rows.map((row, ri) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-6 text-muted-foreground shrink-0", children: ri + 1 }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            disabled: !canEdit,
            className: INPUT,
            min: 16,
            max: 500,
            value: row.height ?? 32,
            onChange: (e) => {
              const newRows = td.rows.map(
                (r, i) => i === ri ? { ...r, height: Number(e.target.value) } : r
              );
              updateTableData({ rows: newRows });
            }
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "w-6 text-muted-foreground", children: "px" })
      ] }, ri)) })
    ] }),
    tableEditorState && (0, import_jsx_runtime.jsx)(
      TableCellFormattingPanel,
      {
        tableData: td,
        tableEditorState,
        canEdit,
        onUpdateTableData: updateTableData
      }
    )
  ] });
}
function ElementInspectorBody({
  selectedElement,
  canEdit,
  slides,
  customShows,
  tableEditorState,
  mediaDataUrls,
  tableStyleMap,
  onTableStyleMapChange,
  onDeleteTableStyle,
  onUpdateElement,
  onUpdateElementStyle,
  onUpdateTextStyle,
  onMoveLayer
}) {
  const { t } = useTranslation();
  const isLocked = isElementLocked(selectedElement);
  const handleToggleLock = () => {
    if (!canEdit) {
      return;
    }
    onUpdateElement({ locks: elementLockTogglePatch(!isLocked) });
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
        (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.inspector.element") }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: handleToggleLock,
            disabled: !canEdit,
            title: isLocked ? t("pptx.inspector.unlock") : t("pptx.inspector.lock"),
            className: "p-1 rounded hover:bg-accent disabled:opacity-40 disabled:cursor-not-allowed transition-colors",
            "aria-pressed": isLocked,
            children: isLocked ? (0, import_jsx_runtime.jsx)(LuLock, { className: "w-3.5 h-3.5 text-amber-400" }) : (0, import_jsx_runtime.jsx)(LuLockOpen, { className: "w-3.5 h-3.5 text-muted-foreground" })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-1.5 text-[11px]", children: POS_FIELDS.map(([label, key]) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-4 text-muted-foreground", children: label }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            className: INPUT,
            disabled: !canEdit,
            value: Math.round(selectedElement[key] ?? 0),
            onChange: (e) => onUpdateElement({
              [key]: Number(e.target.value)
            })
          }
        )
      ] }, key)) })
    ] }),
    selectedElement.type === "table" && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        TableDataGrid,
        {
          tableElement: selectedElement,
          canEdit,
          onUpdateElement
        }
      ),
      (0, import_jsx_runtime.jsx)(
        TablePropertiesPanel,
        {
          tableElement: selectedElement,
          canEdit,
          onUpdateElement,
          tableEditorState,
          tableStyleMap,
          onTableStyleMapChange,
          onDeleteTableStyle
        }
      )
    ] }),
    selectedElement.type === "chart" && (0, import_jsx_runtime.jsx)(
      ChartDataPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    selectedElement.type === "smartArt" && selectedElement.smartArtData && (0, import_jsx_runtime.jsx)(
      SmartArtPropertiesPanel,
      {
        smartArtData: selectedElement.smartArtData,
        canEdit,
        onUpdateElement,
        box: { width: selectedElement.width, height: selectedElement.height }
      }
    ),
    isImageLikeElement(selectedElement) && (0, import_jsx_runtime.jsx)(
      ImagePropertiesPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    selectedElement.type === "media" && (0, import_jsx_runtime.jsx)(
      MediaPropertiesPanel,
      {
        element: selectedElement,
        mediaDataUrls: mediaDataUrls ?? /* @__PURE__ */ new Map(),
        canEdit,
        onUpdateElement
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ConnectorPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElementStyle
      }
    ),
    (0, import_jsx_runtime.jsx)(GroupInfoPanel, { selectedElement }),
    (0, import_jsx_runtime.jsx)(
      OlePropertiesPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ShapeTextPanels,
      {
        selectedElement,
        canEdit,
        onUpdateElement,
        onUpdateElementStyle,
        onUpdateTextStyle
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ActionSettingsPanel,
      {
        selectedElement,
        slides,
        canEdit,
        customShows,
        onUpdateElement
      }
    ),
    (0, import_jsx_runtime.jsx)(
      TransformPanel,
      {
        selectedElement,
        canEdit,
        onUpdateElement
      }
    ),
    (0, import_jsx_runtime.jsx)(LayerOrderButtons, { canEdit, onMoveLayer })
  ] });
}
function CommentMentionTextarea({
  value,
  mentions,
  authors,
  onChange,
  onSubmitShortcut,
  placeholder,
  rows = 2,
  className,
  ariaLabel
}) {
  const { t } = useTranslation();
  const textareaRef = (0, import_react2.useRef)(null);
  const [caret, setCaret] = (0, import_react2.useState)(value.length);
  const [highlighted, setHighlighted] = (0, import_react2.useState)(0);
  const [dismissedAt, setDismissedAt] = (0, import_react2.useState)(null);
  const query = (0, import_react2.useMemo)(() => commentMentionQuery(value, caret), [value, caret]);
  const activeQuery = query && query.start !== dismissedAt ? query : null;
  const matches = (0, import_react2.useMemo)(
    () => activeQuery ? matchCommentMentionAuthors(authors, activeQuery.query) : [],
    [activeQuery, authors]
  );
  const safeHighlighted = matches.length > 0 ? highlighted % matches.length : 0;
  const syncCaret = (target) => {
    setCaret(target.selectionStart ?? target.value.length);
    setHighlighted(0);
  };
  const acceptAuthor = (author) => {
    const result = insertCommentMention(value, mentions, caret, author);
    onChange(result.text, result.mentions);
    setCaret(result.caret);
    setDismissedAt(null);
    requestAnimationFrame(() => {
      var _a, _b2;
      (_a = textareaRef.current) == null ? void 0 : _a.setSelectionRange(result.caret, result.caret);
      (_b2 = textareaRef.current) == null ? void 0 : _b2.focus();
    });
  };
  const handleChange = (event) => {
    onChange(event.target.value, mentions);
    syncCaret(event.target);
  };
  const handleKeyDown = (event) => {
    if (matches.length > 0) {
      if (event.key === "ArrowDown") {
        event.preventDefault();
        setHighlighted((index) => (index + 1) % matches.length);
        return;
      }
      if (event.key === "ArrowUp") {
        event.preventDefault();
        setHighlighted((index) => (index - 1 + matches.length) % matches.length);
        return;
      }
      if (event.key === "Enter" || event.key === "Tab") {
        event.preventDefault();
        acceptAuthor(matches[safeHighlighted]);
        return;
      }
      if (event.key === "Escape") {
        event.preventDefault();
        setDismissedAt((activeQuery == null ? void 0 : activeQuery.start) ?? null);
        return;
      }
    }
    if (onSubmitShortcut && event.key === "Enter" && (event.metaKey || event.ctrlKey)) {
      event.preventDefault();
      onSubmitShortcut();
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "relative", children: [
    (0, import_jsx_runtime.jsx)(
      "textarea",
      {
        ref: textareaRef,
        "aria-label": ariaLabel,
        rows,
        placeholder: placeholder ?? t("pptx.comments.mentionPlaceholder"),
        value,
        className,
        onChange: handleChange,
        onSelect: (event) => syncCaret(event.currentTarget),
        onClick: (event) => syncCaret(event.currentTarget),
        onKeyDown: handleKeyDown,
        onKeyUp: (event) => syncCaret(event.currentTarget)
      }
    ),
    activeQuery && matches.length > 0 && (0, import_jsx_runtime.jsx)(
      "div",
      {
        "data-testid": "pptx-comment-mention-suggestions",
        role: "listbox",
        "aria-label": t("pptx.comments.mentionSuggestions"),
        className: "absolute z-10 mt-0.5 max-h-40 w-full overflow-y-auto rounded border border-border bg-popover shadow-lg",
        children: matches.map((author, index) => (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-testid": "pptx-comment-mention-option",
            role: "option",
            "aria-selected": index === safeHighlighted,
            className: cn(
              "block w-full px-2 py-1 text-left text-[11px] hover:bg-accent",
              index === safeHighlighted && "bg-accent"
            ),
            onMouseDown: (event) => {
              event.preventDefault();
              acceptAuthor(author);
            },
            children: author.name
          },
          author.id
        ))
      }
    )
  ] });
}
function CommentBody({ text, mentions, className }) {
  const segments = commentTextSegments(text, mentions);
  if (!segments.some((segment) => segment.kind === "mention")) {
    return (0, import_jsx_runtime.jsx)("div", { className, children: text });
  }
  return (0, import_jsx_runtime.jsx)("div", { className, children: segments.map(
    (segment, index) => segment.kind === "mention" ? (0, import_jsx_runtime.jsx)(
      "span",
      {
        className: `${COMMENT_MENTION_CLASS} rounded bg-primary/15 px-0.5 font-semibold text-primary`,
        ...{ [COMMENT_MENTION_ATTRIBUTE]: segment.personId || "" },
        title: segment.authorName,
        children: segment.text
      },
      index
    ) : (
      // eslint-disable-next-line react/no-array-index-key -- segments are positional
      (0, import_jsx_runtime.jsx)(import_react2.default.Fragment, { children: segment.text }, index)
    )
  ) });
}
function InspectorCommentReplyForm({
  commentId,
  authorName,
  draft,
  mentions,
  authors,
  onDraftChange,
  onSubmit,
  onCancel
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 space-y-1.5 pl-3 border-l-2 border-l-primary/40", children: [
    (0, import_jsx_runtime.jsx)(
      CommentMentionTextarea,
      {
        value: draft,
        mentions,
        authors,
        rows: 2,
        placeholder: t("pptx.comments.replyPlaceholder", { author: authorName || "Author" }),
        className: "w-full rounded border border-border bg-background px-2 py-1.5 text-xs text-foreground outline-none focus:border-primary resize-y",
        onChange: (text, next) => onDraftChange(commentId, text, next),
        onSubmitShortcut: () => onSubmit(commentId)
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1.5", children: [
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          className: "inline-flex items-center gap-1 rounded bg-primary px-2 py-1 text-[11px] text-white hover:bg-primary/80 disabled:opacity-40 disabled:cursor-not-allowed",
          onClick: () => onSubmit(commentId),
          disabled: draft.trim().length === 0,
          children: [
            (0, import_jsx_runtime.jsx)(LuReply, { className: "h-3 w-3" }),
            t("pptx.comments.addReply")
          ]
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: "inline-flex items-center gap-1 rounded bg-muted px-2 py-1 text-[11px] text-foreground hover:bg-accent",
          onClick: onCancel,
          children: t("pptx.comments.cancel")
        }
      )
    ] })
  ] });
}
var EMPTY_REPLY_MENTIONS = {};
var EMPTY_AUTHORS = [];
function InspectorCommentRow({
  comment,
  canEdit,
  activeSlide,
  editingCommentId,
  commentEditDraft,
  replyingToCommentId,
  replyDraftByCommentId,
  replyDraftMentionsByCommentId = EMPTY_REPLY_MENTIONS,
  commentAuthors = EMPTY_AUTHORS,
  onStartEditComment,
  onSaveEditComment,
  onCancelEditComment,
  onSetCommentEditDraft,
  onDeleteComment,
  onToggleCommentResolved,
  onStartReply,
  onCancelReply,
  onReplyDraftChange,
  onSubmitReply,
  onSelectElement,
  depth = 0
}) {
  var _a;
  const { t } = useTranslation();
  const isEditing = editingCommentId === comment.id;
  const isReplying = replyingToCommentId === comment.id;
  const replyDraft = replyDraftByCommentId[comment.id] ?? "";
  const [showReplies, setShowReplies] = (0, import_react2.useState)(true);
  const replies = comment.replies ?? [];
  const targetElement = comment.elementId ? ((_a = activeSlide == null ? void 0 : activeSlide.elements) == null ? void 0 : _a.find((el) => el.id === comment.elementId)) ?? null : null;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: cn(
        "rounded border border-border bg-card p-2",
        depth > 0 && "ml-3 border-l-2 border-l-primary/40"
      ),
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1.5 min-w-0", children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] font-medium text-foreground truncate", children: comment.author || "Author" }),
            comment.resolved && (0, import_jsx_runtime.jsxs)("span", { className: "inline-flex items-center gap-0.5 rounded-full bg-green-900/40 px-1.5 py-0.5 text-[9px] font-medium text-green-300 flex-shrink-0", children: [
              (0, import_jsx_runtime.jsx)(LuCheck, { className: "h-2.5 w-2.5" }),
              t("pptx.comments.resolved")
            ] })
          ] }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground flex-shrink-0", children: formatCommentTimestamp(comment.createdAt) })
        ] }),
        comment.elementId && (targetElement ? (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            className: "mt-1 inline-flex items-center gap-1 rounded bg-primary/20 px-1.5 py-0.5 text-[10px] text-primary hover:bg-primary/30 transition-colors",
            title: t("pptx.comments.commentingOn", {
              element: getElementLabel(targetElement)
            }),
            onClick: () => onSelectElement(targetElement.id),
            children: [
              (0, import_jsx_runtime.jsx)(LuType, { className: "h-2.5 w-2.5" }),
              getElementLabel(targetElement)
            ]
          }
        ) : (0, import_jsx_runtime.jsx)("span", { className: "mt-1 inline-flex items-center gap-1 rounded bg-muted/60 px-1.5 py-0.5 text-[10px] text-muted-foreground", children: t("pptx.comments.deletedElement") })),
        isEditing ? (0, import_jsx_runtime.jsxs)("div", { className: "mt-1.5 space-y-1.5", children: [
          (0, import_jsx_runtime.jsx)(
            "textarea",
            {
              value: commentEditDraft,
              onChange: (e) => onSetCommentEditDraft(e.target.value),
              rows: 3,
              className: "w-full rounded border border-border bg-background px-2 py-1.5 text-xs text-foreground outline-none focus:border-primary resize-y"
            }
          ),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1.5", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-primary px-2 py-1 text-[11px] text-white hover:bg-primary/80 disabled:opacity-40 disabled:cursor-not-allowed",
                onClick: () => onSaveEditComment(comment.id),
                disabled: String(commentEditDraft).trim().length === 0,
                children: t("pptx.comments.save")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center gap-1 rounded bg-muted px-2 py-1 text-[11px] text-foreground hover:bg-accent",
                onClick: onCancelEditComment,
                children: t("pptx.comments.cancel")
              }
            )
          ] })
        ] }) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)(
            CommentBody,
            {
              text: comment.text,
              mentions: comment.mentions,
              className: "mt-1 whitespace-pre-wrap break-words text-[11px] text-foreground"
            }
          ),
          canEdit && (0, import_jsx_runtime.jsxs)("div", { className: "mt-1.5 inline-flex items-center rounded bg-muted overflow-hidden", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center justify-center p-1.5 hover:bg-accent text-foreground hover:text-foreground transition-colors",
                onClick: () => onStartEditComment(comment.id),
                title: t("pptx.comments.edit"),
                children: (0, import_jsx_runtime.jsx)(LuPencil, { className: "h-3 w-3" })
              }
            ),
            onStartReply && depth === 0 && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center justify-center p-1.5 border-l border-border hover:bg-accent text-foreground hover:text-foreground transition-colors",
                onClick: () => onStartReply(comment.id),
                title: t("pptx.comments.reply"),
                children: (0, import_jsx_runtime.jsx)(LuReply, { className: "h-3 w-3" })
              }
            ),
            onToggleCommentResolved && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: cn(
                  "inline-flex items-center justify-center p-1.5 border-l border-border transition-colors",
                  comment.resolved ? "bg-green-900/40 text-green-300 hover:bg-green-900/60" : "hover:bg-accent text-foreground hover:text-foreground"
                ),
                onClick: () => onToggleCommentResolved(comment.id),
                title: comment.resolved ? t("pptx.comments.unresolve") : t("pptx.comments.resolve"),
                children: (0, import_jsx_runtime.jsx)(LuCheck, { className: "h-3 w-3" })
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: "inline-flex items-center justify-center p-1.5 border-l border-border hover:bg-red-900/40 text-muted-foreground hover:text-red-300 transition-colors",
                onClick: () => onDeleteComment(comment.id),
                title: t("pptx.comments.delete"),
                children: (0, import_jsx_runtime.jsx)(LuTrash2, { className: "h-3 w-3" })
              }
            )
          ] })
        ] }),
        replies.length > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "mt-2", children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex items-center gap-1 text-[10px] text-muted-foreground hover:text-foreground transition-colors",
              onClick: () => setShowReplies((p) => !p),
              children: [
                showReplies ? (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "h-3 w-3" }) : (0, import_jsx_runtime.jsx)(LuChevronRight, { className: "h-3 w-3" }),
                t("pptx.comments.repliesCount", { count: replies.length })
              ]
            }
          ),
          showReplies && (0, import_jsx_runtime.jsx)("div", { className: "mt-1.5 space-y-1.5", children: replies.map((reply, ri) => (0, import_jsx_runtime.jsx)(
            InspectorCommentRow,
            {
              comment: reply,
              canEdit,
              activeSlide,
              editingCommentId,
              commentEditDraft,
              replyingToCommentId,
              replyDraftByCommentId,
              replyDraftMentionsByCommentId,
              commentAuthors,
              onStartEditComment,
              onSaveEditComment,
              onCancelEditComment,
              onSetCommentEditDraft,
              onDeleteComment,
              onToggleCommentResolved,
              onStartReply,
              onCancelReply,
              onReplyDraftChange,
              onSubmitReply,
              onSelectElement,
              depth: depth + 1
            },
            `${reply.id}-${ri}`
          )) })
        ] }),
        isReplying && onCancelReply && onReplyDraftChange && onSubmitReply && (0, import_jsx_runtime.jsx)(
          InspectorCommentReplyForm,
          {
            commentId: comment.id,
            authorName: comment.author ?? "",
            draft: replyDraft,
            mentions: replyDraftMentionsByCommentId[comment.id] ?? [],
            authors: commentAuthors,
            onDraftChange: onReplyDraftChange,
            onSubmit: onSubmitReply,
            onCancel: onCancelReply
          }
        )
      ]
    }
  );
}
function InspectorCommentsSection({
  comments,
  canEdit,
  activeSlide,
  selectedElement,
  editingCommentId,
  commentEditDraft,
  commentDraft,
  commentDraftMentions,
  commentAuthors,
  replyingToCommentId,
  replyDraftByCommentId,
  replyDraftMentionsByCommentId,
  onSetCommentDraft,
  onAddComment,
  onDeleteComment,
  onStartEditComment,
  onSaveEditComment,
  onCancelEditComment,
  onSetCommentEditDraft,
  onToggleCommentResolved,
  onStartReply,
  onCancelReply,
  onReplyDraftChange,
  onSubmitReply,
  onSelectElement
}) {
  const { t } = useTranslation();
  const HEADING_CLS = "text-[11px] uppercase tracking-wide text-muted-foreground";
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-3", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING_CLS, children: t("pptx.comments.slideComments") }),
      (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] text-muted-foreground", children: comments.length })
    ] }),
    comments.length === 0 ? (0, import_jsx_runtime.jsx)("div", { className: "text-xs text-muted-foreground", children: t("pptx.comments.noComments") }) : (0, import_jsx_runtime.jsx)("div", { className: "space-y-2 max-h-[42vh] overflow-y-auto pr-1", children: comments.map((c, idx) => (0, import_jsx_runtime.jsx)(
      InspectorCommentRow,
      {
        comment: c,
        canEdit,
        activeSlide,
        editingCommentId,
        commentEditDraft,
        replyingToCommentId,
        replyDraftByCommentId,
        replyDraftMentionsByCommentId,
        commentAuthors,
        onStartEditComment,
        onSaveEditComment,
        onCancelEditComment,
        onSetCommentEditDraft,
        onDeleteComment,
        onToggleCommentResolved,
        onStartReply,
        onCancelReply,
        onReplyDraftChange,
        onSubmitReply,
        onSelectElement
      },
      `${c.id}-${idx}`
    )) }),
    canEdit && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 pt-1 border-t border-border", children: [
      selectedElement && (0, import_jsx_runtime.jsxs)("div", { className: "inline-flex items-center gap-1 rounded bg-primary/20 px-1.5 py-0.5 text-[10px] text-primary", children: [
        (0, import_jsx_runtime.jsx)(LuType, { className: "h-2.5 w-2.5" }),
        t("pptx.comments.commentingOn", {
          element: getElementLabel(selectedElement)
        })
      ] }),
      (0, import_jsx_runtime.jsx)(
        CommentMentionTextarea,
        {
          value: commentDraft,
          mentions: commentDraftMentions,
          authors: commentAuthors,
          rows: 2,
          placeholder: selectedElement ? t("pptx.comments.addOnElement", {
            element: getElementLabel(selectedElement)
          }) : t("pptx.comments.addPlaceholder"),
          className: "w-full bg-muted border border-border rounded px-2 py-1 text-xs resize-y outline-none focus:border-primary",
          onChange: onSetCommentDraft,
          onSubmitShortcut: onAddComment
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          onClick: onAddComment,
          disabled: !commentDraft.trim(),
          className: cn(
            "w-full inline-flex items-center justify-center gap-1 rounded px-2 py-1 text-[11px] font-medium transition-colors",
            commentDraft.trim() ? "bg-primary hover:bg-primary/80 text-white" : "bg-muted text-muted-foreground cursor-not-allowed"
          ),
          children: [
            (0, import_jsx_runtime.jsx)(LuMessageSquare, { className: "w-3.5 h-3.5" }),
            t("pptx.comments.addComment")
          ]
        }
      )
    ] })
  ] });
}
function elementLabel(element) {
  const text = hasTextProperties(element) ? (element.text || "").slice(0, 24) : void 0;
  return text || element.type;
}
function InspectorElementsTab({
  activeSlide,
  selectedElementId,
  selectedElementIds,
  canEdit,
  onSelectElement,
  onUpdateSlide
}) {
  const { t } = useTranslation();
  const toggleHidden = (elementId) => {
    const elements2 = activeSlide == null ? void 0 : activeSlide.elements;
    if (!canEdit || !elements2) {
      return;
    }
    const index = elements2.findIndex((el) => el.id === elementId);
    if (index === -1) {
      return;
    }
    const next = [...elements2];
    next[index] = { ...next[index], hidden: !next[index].hidden };
    onUpdateSlide({ elements: next });
  };
  if (!activeSlide) {
    return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
      (0, import_jsx_runtime.jsx)("div", { className: cn(HEADING, "mb-2"), children: t("pptx.inspector.layerOrder") }),
      (0, import_jsx_runtime.jsx)("div", { className: "text-muted-foreground italic", children: t("pptx.inspector.noSlideSelected") })
    ] });
  }
  const elements = activeSlide.elements || [];
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", "data-pptx-elements-tab": true, children: [
    (0, import_jsx_runtime.jsx)("div", { className: cn(HEADING, "mb-2"), children: t("pptx.inspector.layerOrder") }),
    [...elements].reverse().map((el, reversedIndex) => {
      const index = elements.length - 1 - reversedIndex;
      const selected = selectedElementId === el.id || selectedElementIds.includes(el.id);
      return (0, import_jsx_runtime.jsxs)(
        "div",
        {
          title: `${el.type} - ${el.id}`,
          className: cn(
            "flex items-center gap-2 px-2 py-1 rounded cursor-pointer transition-colors",
            selected ? "bg-primary/30 text-primary" : "hover:bg-muted text-foreground"
          ),
          onClick: () => onSelectElement(el.id),
          children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground w-4 text-right", children: index + 1 }),
            (0, import_jsx_runtime.jsx)("span", { className: "flex-1 truncate", children: elementLabel(el) }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                "data-pptx-element-visibility": el.id,
                "aria-pressed": Boolean(el.hidden),
                className: "text-muted-foreground hover:text-foreground disabled:opacity-40 disabled:cursor-not-allowed",
                title: el.hidden ? t("pptx.selectionPane.showElement") : t("pptx.selectionPane.hideElement"),
                onClick: (event) => {
                  event.stopPropagation();
                  toggleHidden(el.id);
                },
                children: el.hidden ? (0, import_jsx_runtime.jsx)(LuEyeOff, { className: "w-3.5 h-3.5" }) : (0, import_jsx_runtime.jsx)(LuEye, { className: "w-3.5 h-3.5 opacity-50" })
              }
            )
          ]
        },
        el.id
      );
    })
  ] });
}
function InspectorPaneHeader({
  activeTab,
  onSetActiveTab,
  onClose
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2 px-3 py-2 border-b border-border", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "flex items-center gap-1 rounded bg-muted p-0.5", children: INSPECTOR_TABS.map(({ key, label, icon: Icon }) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        title: label,
        className: cn(
          "flex items-center gap-1 px-2 py-1 rounded text-[11px] transition-colors",
          activeTab === key ? "bg-primary text-white" : "text-muted-foreground hover:text-foreground hover:bg-accent"
        ),
        onClick: () => onSetActiveTab(key),
        children: [
          (0, import_jsx_runtime.jsx)(Icon, { className: "w-3.5 h-3.5" }),
          (0, import_jsx_runtime.jsx)("span", { className: "hidden sm:inline", children: label })
        ]
      },
      key
    )) }),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onClose,
        title: t("pptx.common.close"),
        className: "p-1 rounded text-muted-foreground hover:text-foreground hover:bg-muted transition-colors",
        children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
      }
    )
  ] });
}
function InfoRow({ label, value }) {
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2", children: [
    (0, import_jsx_runtime.jsx)("span", { children: label }),
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: value })
  ] });
}
function TextFieldRow({
  label,
  disabled,
  value,
  onChange
}) {
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: label }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "text",
        className: INPUT,
        disabled,
        value,
        onChange: (e) => onChange(e.target.value)
      }
    )
  ] });
}
function CustomPropertiesBlock({
  customProperties,
  canEdit,
  onUpdate
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.documentProperties.custom.heading") }),
      canEdit && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: BTN,
          onClick: () => onUpdate([
            ...customProperties,
            {
              name: `Property ${customProperties.length + 1}`,
              value: "",
              type: "lpwstr"
            }
          ]),
          children: t("pptx.documentProperties.custom.add")
        }
      )
    ] }),
    customProperties.length === 0 ? (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.documentProperties.custom.empty") }) : customProperties.map((entry, index) => (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[1fr,1fr,auto] gap-1", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          className: INPUT,
          disabled: !canEdit,
          value: entry.name,
          onChange: (e) => onUpdate(
            customProperties.map(
              (c, i) => i === index ? { ...c, name: e.target.value } : c
            )
          )
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          className: INPUT,
          disabled: !canEdit,
          value: entry.value,
          onChange: (e) => onUpdate(
            customProperties.map(
              (c, i) => i === index ? { ...c, value: e.target.value } : c
            )
          )
        }
      ),
      canEdit && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: cn(BTN, "px-1.5 text-red-400 hover:text-red-300"),
          onClick: () => onUpdate(customProperties.filter((_, i) => i !== index)),
          children: "×"
        }
      )
    ] }, `${entry.name}-${index}`))
  ] });
}
function NotesHandoutCard({
  notesCanvasSize,
  notesMaster,
  handoutMaster
}) {
  var _a, _b2;
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.documentProperties.notesHandoutHeading") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1 text-[11px] text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)(
        InfoRow,
        {
          label: t("pptx.documentProperties.notesSize"),
          value: notesCanvasSize ? `${notesCanvasSize.width} × ${notesCanvasSize.height}px` : t("pptx.digitalSignatures.notAvailable")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        InfoRow,
        {
          label: t("pptx.master.notesMasterTitle"),
          value: notesMaster ? `${((_a = notesMaster.placeholders) == null ? void 0 : _a.length) ?? 0} placeholders` : t("pptx.digitalSignatures.notAvailable")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        InfoRow,
        {
          label: t("pptx.master.handoutMasterTitle"),
          value: handoutMaster ? `${((_b2 = handoutMaster.placeholders) == null ? void 0 : _b2.length) ?? 0} placeholders` : t("pptx.digitalSignatures.notAvailable")
        }
      )
    ] })
  ] });
}
function DocumentPropertiesCard({
  coreProperties,
  appProperties,
  customProperties,
  canEdit,
  onUpdateCoreProperties,
  onUpdateAppProperties,
  onUpdateCustomProperties
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.documentProperties.documentHeading") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2 text-[11px] text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)(
        TextFieldRow,
        {
          label: t("pptx.properties.titleLabel"),
          disabled: !canEdit,
          value: (coreProperties == null ? void 0 : coreProperties.title) ?? "",
          onChange: (v) => onUpdateCoreProperties({ title: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        TextFieldRow,
        {
          label: t("pptx.properties.author"),
          disabled: !canEdit,
          value: (coreProperties == null ? void 0 : coreProperties.creator) ?? "",
          onChange: (v) => onUpdateCoreProperties({ creator: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        TextFieldRow,
        {
          label: t("pptx.documentProperties.summary.company"),
          disabled: !canEdit,
          value: (appProperties == null ? void 0 : appProperties.company) ?? "",
          onChange: (v) => onUpdateAppProperties({ company: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        TextFieldRow,
        {
          label: t("pptx.documentProperties.statistics.application"),
          disabled: !canEdit,
          value: (appProperties == null ? void 0 : appProperties.application) ?? "",
          onChange: (v) => onUpdateAppProperties({ application: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        CustomPropertiesBlock,
        {
          customProperties,
          canEdit,
          onUpdate: onUpdateCustomProperties
        }
      )
    ] })
  ] });
}
function SlideSizeRescalePrompt({
  onChoose
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 rounded border border-border bg-muted/40 p-2 text-[11px]", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "font-medium", children: t("pptx.slideSize.rescaleTitle") }),
    (0, import_jsx_runtime.jsx)("div", { className: "text-muted-foreground", children: t("pptx.slideSize.rescaleDescription") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5 pt-1", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          "data-testid": "pptx-slide-size-rescale-maximize",
          title: t("pptx.slideSize.rescaleMaximizeHint"),
          className: "rounded bg-primary px-2 py-1 text-primary-foreground transition-colors hover:opacity-90",
          onClick: () => onChoose("maximize"),
          children: t("pptx.slideSize.rescaleMaximize")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          "data-testid": "pptx-slide-size-rescale-ensure-fit",
          title: t("pptx.slideSize.rescaleEnsureFitHint"),
          className: "rounded bg-muted px-2 py-1 transition-colors hover:bg-accent",
          onClick: () => onChoose("ensureFit"),
          children: t("pptx.slideSize.rescaleEnsureFit")
        }
      )
    ] })
  ] });
}
var CUSTOM_PRESET_VALUE = "";
function sizesDiffer(a, b) {
  return a.widthEmu !== b.widthEmu || a.heightEmu !== b.heightEmu;
}
function SlideSizeCard({
  canvasSize,
  slideSizeEmu,
  canEdit,
  onUpdate,
  onUpdateSlideSize,
  hasContent
}) {
  var _a;
  const { t } = useTranslation();
  const selection = resolveSlideSizeSelection({ current: slideSizeEmu, canvas: canvasSize });
  const presetDisabled = !canEdit || !onUpdateSlideSize;
  const [pendingSize, setPendingSize] = (0, import_react2.useState)(null);
  const applyOrPromptSize = (nextSize) => {
    if (hasContent && sizesDiffer(selection.size, nextSize)) {
      setPendingSize(nextSize);
      return;
    }
    onUpdateSlideSize == null ? void 0 : onUpdateSlideSize(nextSize);
  };
  const handlePreset = (labelKey) => {
    const preset = SLIDE_SIZE_PRESETS.find((candidate) => candidate.labelKey === labelKey);
    if (!preset) {
      return;
    }
    applyOrPromptSize(slideSizeFromPreset(preset, selection.orientation));
  };
  const handleOrientation = (orientation) => {
    applyOrPromptSize(withSlideSizeOrientation(selection.size, orientation));
  };
  const handleRescale = (mode) => {
    if (!pendingSize) {
      return;
    }
    onUpdateSlideSize == null ? void 0 : onUpdateSlideSize(pendingSize, mode);
    setPendingSize(null);
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.slideSize.title") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 text-[11px]", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.slideSize.presets") }),
        (0, import_jsx_runtime.jsxs)(
          "select",
          {
            "aria-label": t("pptx.slideSize.presets"),
            "data-pptx-slide-size-preset": true,
            disabled: presetDisabled,
            className: INPUT,
            value: ((_a = selection.preset) == null ? void 0 : _a.labelKey) ?? CUSTOM_PRESET_VALUE,
            onChange: (e) => handlePreset(e.target.value),
            children: [
              selection.preset === void 0 && (0, import_jsx_runtime.jsx)("option", { value: CUSTOM_PRESET_VALUE, children: t("pptx.slideSize.customSize") }),
              SLIDE_SIZE_PRESETS.map((preset) => (0, import_jsx_runtime.jsx)("option", { value: preset.labelKey, children: t(`pptx.slideSize.preset.${preset.labelKey}`) }, preset.labelKey))
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.slideSize.orientation") }),
        (0, import_jsx_runtime.jsxs)(
          "select",
          {
            "aria-label": t("pptx.slideSize.orientation"),
            "data-pptx-slide-size-orientation": true,
            disabled: presetDisabled,
            className: INPUT,
            value: selection.orientation,
            onChange: (e) => handleOrientation(e.target.value),
            children: [
              (0, import_jsx_runtime.jsx)("option", { value: "landscape", children: t("pptx.slideSize.landscape") }),
              (0, import_jsx_runtime.jsx)("option", { value: "portrait", children: t("pptx.slideSize.portrait") })
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-1.5", children: [
        ["W", "width"],
        ["H", "height"]
      ].map(([label, key]) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: label }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            className: INPUT,
            disabled: !canEdit,
            value: canvasSize[key],
            onChange: (e) => onUpdate({ ...canvasSize, [key]: Number(e.target.value) })
          }
        )
      ] }, key)) }),
      pendingSize && (0, import_jsx_runtime.jsx)(SlideSizeRescalePrompt, { onChoose: handleRescale })
    ] })
  ] });
}
function CheckboxRow({
  label,
  disabled,
  checked,
  onChange
}) {
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: label }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "checkbox",
        disabled,
        checked,
        onChange: (e) => onChange(e.target.checked)
      }
    )
  ] });
}
function PresentationSettingsCard({
  presentationProperties,
  canEdit,
  onUpdate
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.slideInspector.presentation") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5 text-[11px]", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.presentationSettings.showType") }),
        (0, import_jsx_runtime.jsxs)(
          "select",
          {
            "aria-label": t("pptx.presentationSettings.showType"),
            disabled: !canEdit,
            className: cn(INPUT, "w-28"),
            value: presentationProperties.showType ?? "presented",
            onChange: (e) => onUpdate({
              showType: e.target.value
            }),
            children: [
              (0, import_jsx_runtime.jsx)("option", { value: "presented", children: t("pptx.presentationSettings.showTypePresented") }),
              (0, import_jsx_runtime.jsx)("option", { value: "browsed", children: t("pptx.presentationSettings.showTypeBrowsed") }),
              (0, import_jsx_runtime.jsx)("option", { value: "kiosk", children: t("pptx.presentationSettings.showTypeKiosk") })
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.loopContinuously"),
          disabled: !canEdit,
          checked: Boolean(presentationProperties.loopContinuously),
          onChange: (v) => onUpdate({ loopContinuously: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.showNarration"),
          disabled: !canEdit,
          checked: presentationProperties.showWithNarration !== false,
          onChange: (v) => onUpdate({ showWithNarration: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.showAnimation"),
          disabled: !canEdit,
          checked: presentationProperties.showWithAnimation !== false,
          onChange: (v) => onUpdate({ showWithAnimation: v })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        CheckboxRow,
        {
          label: t("pptx.presentationSettings.frameSlides"),
          disabled: !canEdit,
          checked: printPropertiesFrameSlides(presentationProperties.printProperties),
          onChange: (v) => onUpdate({
            printProperties: withFrameSlides(presentationProperties.printProperties, v)
          })
        }
      ),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center justify-between gap-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.presentationSettings.slidesPerPage") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 1,
            max: 16,
            disabled: !canEdit,
            className: cn(INPUT, "w-20"),
            value: printPropertiesSlidesPerPage(presentationProperties.printProperties),
            onChange: (e) => onUpdate({
              printProperties: withSlidesPerPage(
                presentationProperties.printProperties,
                Number(e.target.value)
              )
            })
          }
        )
      ] })
    ] })
  ] });
}
function ThemeSelectorCard({
  themeOptions,
  selectedThemePath,
  setSelectedThemePath,
  canEdit,
  onApplyTheme
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.documentProperties.themeHeading") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.documentProperties.themeHeading") }),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            "aria-label": t("pptx.documentProperties.themeHeading"),
            disabled: themeOptions.length === 0,
            className: INPUT,
            value: selectedThemePath,
            onChange: (e) => setSelectedThemePath(e.target.value),
            children: themeOptions.length === 0 ? (0, import_jsx_runtime.jsx)("option", { value: "", children: t("pptx.documentProperties.noThemesOption") }) : themeOptions.map((opt) => (0, import_jsx_runtime.jsx)("option", { value: opt.path, children: opt.name || opt.path.split("/").pop() }, opt.path))
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-1.5", children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit || !selectedThemePath,
            onClick: () => onApplyTheme(selectedThemePath, false),
            children: t("pptx.documentProperties.applyFirstMaster")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            className: BTN,
            disabled: !canEdit || !selectedThemePath,
            onClick: () => onApplyTheme(selectedThemePath, true),
            children: t("pptx.documentProperties.applyAllMasters")
          }
        )
      ] })
    ] })
  ] });
}
var ALIAS_LABELS = {
  bg1: "Background 1",
  tx1: "Text 1",
  bg2: "Background 2",
  tx2: "Text 2",
  accent1: "Accent 1",
  accent2: "Accent 2",
  accent3: "Accent 3",
  accent4: "Accent 4",
  accent5: "Accent 5",
  accent6: "Accent 6",
  hlink: "Hyperlink",
  folHlink: "Followed Hyperlink"
};
function SlideThemeOverridePanel({
  activeSlide,
  theme,
  canEdit,
  onUpdateSlide
}) {
  const { t } = useTranslation();
  const translate = (0, import_react2.useCallback)((key) => t(key), [t]);
  const override = activeSlide == null ? void 0 : activeSlide.clrMapOverride;
  const isOverrideActive = override !== void 0;
  const targetSlotOptions = (0, import_react2.useMemo)(() => [...THEME_COLOR_SCHEME_KEYS], []);
  const commitOverride = (0, import_react2.useCallback)(
    (nextOverride) => {
      if (!activeSlide || !(theme == null ? void 0 : theme.colorScheme)) {
        onUpdateSlide({ clrMapOverride: nextOverride });
        return;
      }
      const nextSlide = applyThemeOverrideToSlide(activeSlide, theme.colorScheme, nextOverride);
      onUpdateSlide({
        clrMapOverride: nextSlide.clrMapOverride,
        backgroundColor: nextSlide.backgroundColor,
        elements: nextSlide.elements
      });
    },
    [activeSlide, onUpdateSlide, theme == null ? void 0 : theme.colorScheme]
  );
  const handleToggle = (0, import_react2.useCallback)(
    (enabled) => {
      if (enabled) {
        const defaultMap = {};
        for (const key of COLOR_MAP_ALIAS_KEYS) {
          defaultMap[key] = DEFAULT_COLOR_MAP[key];
        }
        commitOverride(defaultMap);
      } else {
        commitOverride(void 0);
      }
    },
    [commitOverride]
  );
  const handleAliasChange = (0, import_react2.useCallback)(
    (alias, targetSlot) => {
      const current = override ?? {};
      const next = { ...current, [alias]: targetSlot };
      for (const key of COLOR_MAP_ALIAS_KEYS) {
        if (!next[key]) {
          next[key] = DEFAULT_COLOR_MAP[key];
        }
      }
      commitOverride(next);
    },
    [commitOverride, override]
  );
  const resolveSlotColor = (0, import_react2.useCallback)(
    (slot) => {
      if (!(theme == null ? void 0 : theme.colorScheme)) {
        return void 0;
      }
      const cs = theme.colorScheme;
      return cs[slot];
    },
    [theme]
  );
  if (!activeSlide) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
    (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-xs", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          disabled: !canEdit,
          checked: isOverrideActive,
          onChange: (e) => handleToggle(e.target.checked)
        }
      ),
      t("pptx.themeOverride.enableOverride")
    ] }),
    isOverrideActive && override && (0, import_jsx_runtime.jsx)("div", { className: "space-y-1.5", children: COLOR_MAP_ALIAS_KEYS.map((alias) => {
      const currentTarget = override[alias] ?? DEFAULT_COLOR_MAP[alias];
      const resolvedHex = resolveSlotColor(currentTarget);
      return (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "w-24 truncate text-muted-foreground", title: ALIAS_LABELS[alias], children: ALIAS_LABELS[alias] }),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "h-4 w-4 rounded-sm border border-border shrink-0",
            style: {
              backgroundColor: resolvedHex ? `#${resolvedHex.replace(/^#/, "")}` : void 0
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "select",
          {
            disabled: !canEdit,
            className: "flex-1 bg-muted border border-border rounded px-1 py-0.5 text-[11px]",
            value: currentTarget,
            onChange: (e) => handleAliasChange(alias, e.target.value),
            children: targetSlotOptions.map((slot) => (0, import_jsx_runtime.jsx)("option", { value: slot, children: schemaLabel(THEME_COLOR_SLOT_LABEL_KEYS, slot, translate) }, slot))
          }
        )
      ] }, alias);
    }) })
  ] });
}
var DIR_ARROWS = TRANSITION_DIR_ARROWS;
function DirectionPicker({
  directions,
  value,
  disabled,
  onChange
}) {
  const { t } = useTranslation();
  if (directions.length <= 3) {
    return (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: directions.map((dir) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        disabled,
        onClick: () => onChange(dir),
        className: `px-2 py-1 rounded text-xs border ${value === dir ? "bg-primary text-white border-primary" : "bg-muted border-border hover:bg-accent"}`,
        title: t(`pptx.transition.dir.${dir}`, dir),
        children: DIR_ARROWS[dir] ?? dir
      },
      dir
    )) });
  }
  const cells = buildDirectionGrid(directions);
  return (0, import_jsx_runtime.jsx)("div", { className: "inline-grid grid-cols-3 gap-0.5", children: cells.flatMap(
    (row, ri) => row.map((cell, ci) => {
      if (!cell) {
        return (0, import_jsx_runtime.jsx)("div", { className: "w-6 h-6" }, `${ri}-${ci}`);
      }
      return (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled,
          onClick: () => onChange(cell),
          className: `w-6 h-6 rounded text-xs flex items-center justify-center border ${value === cell ? "bg-primary text-white border-primary" : "bg-muted border-border hover:bg-accent"}`,
          title: t(`pptx.transition.dir.${cell}`, cell),
          children: DIR_ARROWS[cell] ?? cell
        },
        cell
      );
    })
  ) });
}
function TransitionPreview({
  transition
}) {
  const previewRef = (0, import_react2.useRef)(null);
  const [playing, setPlaying] = (0, import_react2.useState)(false);
  const [animKey, setAnimKey] = (0, import_react2.useState)(0);
  const { t } = useTranslation();
  const durationMs = transition.durationMs ?? 500;
  const animations = (0, import_react2.useMemo)(
    () => getSlideTransitionAnimations(
      transition.type,
      durationMs,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern
    ),
    [
      transition.type,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern,
      durationMs
    ]
  );
  const handlePlay = (0, import_react2.useCallback)(() => {
    setPlaying(true);
    setAnimKey((k) => k + 1);
  }, []);
  (0, import_react2.useEffect)(() => {
    if (!playing) {
      return;
    }
    const timer = window.setTimeout(() => setPlaying(false), durationMs + 100);
    return () => {
      window.clearTimeout(timer);
    };
  }, [playing, durationMs, animKey]);
  if (transition.type === "none" || transition.type === "cut") {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.transition.preview") }),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        ref: previewRef,
        className: "relative w-full h-16 rounded border border-border overflow-hidden bg-muted cursor-pointer",
        onClick: handlePlay,
        title: t("pptx.transition.preview"),
        children: [
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute inset-0 bg-primary/20 flex items-center justify-center text-[9px] text-muted-foreground",
              style: {
                animation: playing && animations.incoming !== "none" ? animations.incoming : void 0
              },
              children: "B"
            },
            `in-${animKey}`
          ),
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "absolute inset-0 bg-card flex items-center justify-center text-[9px] text-muted-foreground",
              style: {
                zIndex: animations.outgoingOnTop ? 2 : 0,
                animation: playing && animations.outgoing !== "none" ? animations.outgoing : !playing ? void 0 : `pptx-tr-fade-out ${durationMs}ms ease-in-out forwards`
              },
              children: "A"
            },
            `out-${animKey}`
          )
        ]
      }
    ),
    (0, import_jsx_runtime.jsx)("style", { children: SLIDE_TRANSITION_KEYFRAMES })
  ] });
}
var DEFAULT_DURATION_MS = 320;
var MAX_DURATION_MS = 1e4;
var MIN_SPOKES = 1;
var MAX_SPOKES = 8;
function SlideTransitionSection({
  activeSlide,
  canEdit = true,
  onTransitionChange
}) {
  var _a, _b2, _c, _d, _e, _f, _g, _h, _i;
  const { t } = useTranslation();
  if (!activeSlide) {
    return null;
  }
  const transitionType = ((_a = activeSlide.transition) == null ? void 0 : _a.type) ?? "none";
  const validDirections = TRANSITION_VALID_DIRECTIONS[transitionType];
  const hasDirections = validDirections !== void 0 && validDirections.length > 0;
  const usesOrientation = TRANSITION_ORIENTATION_TYPES.has(transitionType);
  const isWheel = transitionType === "wheel";
  const isMorph = transitionType === "morph";
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "mb-3 rounded border border-border bg-card p-2 space-y-2",
      "data-pptx-slide-transition": true,
      children: [
        (0, import_jsx_runtime.jsx)("div", { className: "text-[11px] uppercase tracking-wide text-muted-foreground", children: t("pptx.slideInspector.slideTransition") }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.type") }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              value: ((_b2 = activeSlide.transition) == null ? void 0 : _b2.type) || "none",
              disabled: !canEdit,
              "aria-label": t("pptx.transition.type"),
              onChange: (e) => onTransitionChange({
                type: e.target.value
              }),
              className: "bg-muted border border-border rounded px-2 py-1",
              children: SLIDE_TRANSITION_OPTIONS.map((option) => (0, import_jsx_runtime.jsx)("option", { value: option.value, children: t(option.i18nKey) }, option.value))
            }
          )
        ] }),
        hasDirections && !usesOrientation && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.direction") }),
          (0, import_jsx_runtime.jsx)(
            DirectionPicker,
            {
              directions: validDirections,
              value: (_c = activeSlide.transition) == null ? void 0 : _c.direction,
              disabled: !canEdit,
              onChange: (dir) => onTransitionChange({ direction: dir })
            }
          )
        ] }),
        usesOrientation && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.orientation") }),
          (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1", children: ["horz", "vert"].map((o) => {
            var _a2;
            return (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                disabled: !canEdit,
                onClick: () => onTransitionChange({ orient: o }),
                className: `px-2 py-1 rounded text-xs border ${(((_a2 = activeSlide.transition) == null ? void 0 : _a2.orient) ?? "horz") === o ? "bg-primary text-white border-primary" : "bg-muted border-border hover:bg-accent"}`,
                children: t(
                  o === "horz" ? "pptx.slideInspector.horizontal" : "pptx.slideInspector.vertical"
                )
              },
              o
            );
          }) })
        ] }),
        isWheel && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.spokes") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: MIN_SPOKES,
              max: MAX_SPOKES,
              disabled: !canEdit,
              value: ((_d = activeSlide.transition) == null ? void 0 : _d.spokes) ?? 4,
              onChange: (e) => {
                const spokes = clampTransitionNumber(Number(e.target.value), MIN_SPOKES, MAX_SPOKES);
                if (spokes !== null) {
                  onTransitionChange({ spokes });
                }
              },
              className: "bg-muted border border-border rounded px-2 py-1 text-xs w-16"
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.duration") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 0,
              max: MAX_DURATION_MS,
              disabled: !canEdit,
              "aria-label": t("pptx.transition.duration"),
              value: Math.round(((_e = activeSlide.transition) == null ? void 0 : _e.durationMs) || DEFAULT_DURATION_MS),
              onChange: (e) => {
                const durationMs = clampTransitionNumber(Number(e.target.value), 0, MAX_DURATION_MS);
                if (durationMs !== null) {
                  onTransitionChange({ durationMs });
                }
              },
              className: "bg-muted border border-border rounded px-2 py-1"
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.speed") }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              value: ((_f = activeSlide.transition) == null ? void 0 : _f.speed) ?? "fast",
              disabled: !canEdit,
              "aria-label": t("pptx.transition.speed"),
              onChange: (e) => onTransitionChange({
                speed: e.target.value
              }),
              className: "bg-muted border border-border rounded px-2 py-1",
              children: TRANSITION_SPEED_OPTIONS.map((option) => (0, import_jsx_runtime.jsx)("option", { value: option.value, children: t(option.i18nKey) }, option.value))
            }
          )
        ] }),
        isMorph && (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-1", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-xs", children: t("pptx.transition.morphOption") }),
          (0, import_jsx_runtime.jsx)(
            "select",
            {
              value: ((_g = activeSlide.transition) == null ? void 0 : _g.morphOption) ?? "byObject",
              disabled: !canEdit,
              "aria-label": t("pptx.transition.morphOption"),
              onChange: (e) => onTransitionChange({
                morphOption: e.target.value
              }),
              className: "bg-muted border border-border rounded px-2 py-1",
              children: TRANSITION_MORPH_OPTIONS.map((option) => (0, import_jsx_runtime.jsx)("option", { value: option.value, children: t(option.i18nKey) }, option.value))
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "inline-flex items-center gap-2 text-foreground text-xs", children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "checkbox",
              disabled: !canEdit,
              checked: ((_h = activeSlide.transition) == null ? void 0 : _h.advanceOnClick) !== false,
              onChange: (e) => onTransitionChange({ advanceOnClick: e.target.checked })
            }
          ),
          t("pptx.transition.advanceOnClick")
        ] }),
        ((_i = activeSlide.transition) == null ? void 0 : _i.soundFileName) && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-1 text-xs text-muted-foreground", children: [
          (0, import_jsx_runtime.jsxs)("span", { className: "text-muted-foreground", children: [
            t("pptx.transition.sound"),
            ":"
          ] }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-foreground truncate", title: activeSlide.transition.soundFileName, children: activeSlide.transition.soundFileName })
        ] }),
        activeSlide.transition && (0, import_jsx_runtime.jsx)(TransitionPreview, { transition: activeSlide.transition })
      ]
    }
  );
}
function TagsSection({
  tagCollections,
  onUpdateTagCollections,
  canEdit
}) {
  const { t } = useTranslation();
  const [collapsed, setCollapsed] = (0, import_react2.useState)(true);
  const allTags = flattenTagCollections(tagCollections);
  const updateTag = (colIdx, tagIdx, field, newValue) => {
    onUpdateTagCollections(updateTagInCollections(tagCollections, colIdx, tagIdx, field, newValue));
  };
  const deleteTag = (colIdx, tagIdx) => {
    onUpdateTagCollections(deleteTagFromCollections(tagCollections, colIdx, tagIdx));
  };
  const addTag = () => {
    onUpdateTagCollections(addTagToCollections(tagCollections));
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        className: "flex items-center gap-1 w-full",
        onClick: () => setCollapsed(!collapsed),
        children: [
          collapsed ? (0, import_jsx_runtime.jsx)(LuChevronRight, { className: "w-3 h-3 text-muted-foreground" }) : (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "w-3 h-3 text-muted-foreground" }),
          (0, import_jsx_runtime.jsx)("span", { className: HEADING, children: t("pptx.tags.title") }),
          (0, import_jsx_runtime.jsx)("span", { className: "ml-auto text-[10px] text-muted-foreground", children: allTags.length })
        ]
      }
    ),
    !collapsed && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      allTags.length === 0 ? (0, import_jsx_runtime.jsx)("div", { className: "text-[10px] text-muted-foreground", children: t("pptx.tags.noTags") }) : allTags.map((tag, idx) => (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "grid grid-cols-[1fr,1fr,auto] gap-1 text-[11px]",
          children: [
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                className: INPUT,
                disabled: !canEdit,
                placeholder: t("pptx.tags.name"),
                value: tag.name,
                onChange: (e) => updateTag(tag.colIdx, tag.tagIdx, "name", e.target.value)
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                className: INPUT,
                disabled: !canEdit,
                placeholder: t("pptx.tags.value"),
                value: tag.value,
                onChange: (e) => updateTag(tag.colIdx, tag.tagIdx, "value", e.target.value)
              }
            ),
            canEdit && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                className: cn(BTN, "px-1.5 text-red-400 hover:text-red-300"),
                title: t("pptx.tags.deleteTag"),
                onClick: () => deleteTag(tag.colIdx, tag.tagIdx),
                children: (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-3 h-3" })
              }
            )
          ]
        },
        `${tag.colIdx}-${tag.tagIdx}-${idx}`
      )),
      canEdit && (0, import_jsx_runtime.jsx)("button", { type: "button", className: BTN, onClick: addTag, children: t("pptx.tags.addTag") })
    ] })
  ] });
}
function PresentationPropertiesPanel({
  presentationProperties,
  canEdit,
  onUpdatePresentationProperties,
  themeOptions,
  selectedThemePath,
  setSelectedThemePath,
  onApplyTheme,
  activeSlide,
  theme,
  onUpdateSlide,
  canvasSize,
  onUpdateCanvasSize,
  slideSizeEmu,
  onUpdateSlideSize,
  hasContent,
  notesCanvasSize,
  notesMaster,
  handoutMaster,
  coreProperties,
  onUpdateCoreProperties,
  appProperties,
  onUpdateAppProperties,
  customProperties,
  onUpdateCustomProperties,
  tagCollections,
  onUpdateTagCollections
}) {
  var _a;
  const { t } = useTranslation();
  const handleTransitionChange = import_react2.default.useCallback(
    (updates) => {
      onUpdateSlide({
        transition: mergeSlideTransition(activeSlide == null ? void 0 : activeSlide.transition, updates)
      });
    },
    [activeSlide == null ? void 0 : activeSlide.transition, onUpdateSlide]
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-3", children: [
    (0, import_jsx_runtime.jsx)(
      PresentationSettingsCard,
      {
        presentationProperties,
        canEdit,
        onUpdate: onUpdatePresentationProperties
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ThemeSelectorCard,
      {
        themeOptions,
        selectedThemePath,
        setSelectedThemePath,
        canEdit,
        onApplyTheme
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.themeOverride.heading") }),
      (0, import_jsx_runtime.jsx)(
        SlideThemeOverridePanel,
        {
          activeSlide,
          theme,
          canEdit,
          onUpdateSlide
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      SlideSizeCard,
      {
        canvasSize,
        slideSizeEmu,
        canEdit,
        onUpdate: onUpdateCanvasSize,
        onUpdateSlideSize,
        hasContent
      }
    ),
    (0, import_jsx_runtime.jsx)(
      SlideTransitionSection,
      {
        activeSlide: activeSlide ?? null,
        canEdit,
        onTransitionChange: handleTransitionChange
      }
    ),
    (0, import_jsx_runtime.jsx)(
      NotesHandoutCard,
      {
        notesCanvasSize,
        notesMaster,
        handoutMaster
      }
    ),
    (0, import_jsx_runtime.jsx)(
      DocumentPropertiesCard,
      {
        coreProperties,
        appProperties,
        customProperties,
        canEdit,
        onUpdateCoreProperties,
        onUpdateAppProperties,
        onUpdateCustomProperties
      }
    ),
    tagCollections && onUpdateTagCollections && (0, import_jsx_runtime.jsx)(
      TagsSection,
      {
        tagCollections,
        onUpdateTagCollections,
        canEdit
      }
    ),
    activeSlide && (0, import_jsx_runtime.jsxs)("div", { className: cn(CARD, "space-y-1"), children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: "Slide" }),
      (0, import_jsx_runtime.jsxs)("div", { className: "text-[11px] text-muted-foreground", children: [
        ((_a = activeSlide.elements) == null ? void 0 : _a.length) ?? 0,
        " elements"
      ] })
    ] })
  ] });
}
function SlideBackgroundPanel({
  activeSlide,
  canEdit,
  onUpdateSlide,
  editTemplateMode,
  slideMasters,
  onSetTemplateBackground,
  onGetTemplateBackgroundColor
}) {
  const { t } = useTranslation();
  const bgImageInputRef = (0, import_react2.useRef)(null);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: cn(CARD, "space-y-2"), children: [
      (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: "Background" }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground w-10 shrink-0", children: "Colour" }),
        (0, import_jsx_runtime.jsx)(
          DebouncedColorInput,
          {
            value: normalizeHexColor2(activeSlide.backgroundColor, "#ffffff"),
            disabled: !canEdit,
            className: "h-6 w-8 rounded border border-border bg-muted cursor-pointer",
            onCommit: (hex) => onUpdateSlide({ backgroundColor: hex })
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[10px] truncate", children: activeSlide.backgroundColor || "none" })
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 text-[11px]", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground w-10 shrink-0", children: "Image" }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              ref: bgImageInputRef,
              type: "file",
              accept: "image/png,image/jpeg,image/gif,image/webp,image/svg+xml",
              className: "hidden",
              disabled: !canEdit,
              onChange: (e) => {
                var _a;
                const file = (_a = e.target.files) == null ? void 0 : _a[0];
                if (!file) {
                  return;
                }
                const reader = new FileReader();
                reader.onload = () => {
                  if (typeof reader.result === "string") {
                    onUpdateSlide({ backgroundImage: reader.result });
                  }
                };
                reader.readAsDataURL(file);
                e.target.value = "";
              }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: cn(BTN, "flex-1 text-center"),
              disabled: !canEdit,
              onClick: () => {
                var _a;
                return (_a = bgImageInputRef.current) == null ? void 0 : _a.click();
              },
              children: activeSlide.backgroundImage ? t("pptx.slideBackground.replaceImage") : t("pptx.slideBackground.chooseImage")
            }
          )
        ] }),
        activeSlide.backgroundImage && (0, import_jsx_runtime.jsxs)("div", { className: "relative mt-1", children: [
          (0, import_jsx_runtime.jsx)(
            "img",
            {
              src: activeSlide.backgroundImage,
              alt: t("pptx.slideBackground.backgroundPreview"),
              className: "w-full h-16 object-cover rounded border border-border"
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              className: "absolute top-0.5 right-0.5 rounded bg-background/80 hover:bg-red-700 p-0.5 text-[10px] transition-colors",
              disabled: !canEdit,
              title: t("pptx.slideBackground.removeBackgroundImage"),
              onClick: () => onUpdateSlide({ backgroundImage: void 0 }),
              children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-3 h-3" })
            }
          )
        ] })
      ] }),
      (activeSlide.backgroundColor || activeSlide.backgroundImage || activeSlide.backgroundGradient) && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: cn(BTN, "w-full text-center text-red-400 hover:text-red-300"),
          disabled: !canEdit,
          onClick: () => onUpdateSlide({
            backgroundColor: void 0,
            backgroundImage: void 0,
            backgroundGradient: void 0
          }),
          children: t("pptx.slideBackground.clearBackground")
        }
      ),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            checked: activeSlide.showMasterShapes === false,
            disabled: !canEdit,
            onChange: (e) => onUpdateSlide({ showMasterShapes: !e.target.checked })
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.slideBackground.hideBackgroundGraphics") })
      ] })
    ] }),
    editTemplateMode && onSetTemplateBackground && onGetTemplateBackgroundColor && (0, import_jsx_runtime.jsx)(
      TemplateBackgroundCard,
      {
        activeSlide,
        slideMasters,
        canEdit,
        onSetTemplateBackground,
        onGetTemplateBackgroundColor
      }
    )
  ] });
}
function TemplateBackgroundCard({
  activeSlide,
  slideMasters,
  canEdit,
  onSetTemplateBackground,
  onGetTemplateBackgroundColor
}) {
  const { t } = useTranslation();
  const rows = resolveTemplateBackgroundRows(
    activeSlide,
    slideMasters,
    t("pptx.master.layout"),
    t("pptx.master.master")
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: cn(CARD, "space-y-2"), children: [
    (0, import_jsx_runtime.jsx)("div", { className: HEADING, children: t("pptx.slideBackground.templateBackgroundsHeading") }),
    rows.layout && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground w-14 shrink-0 truncate", title: rows.layout.title, children: t("pptx.master.layout") }),
      (0, import_jsx_runtime.jsx)(
        DebouncedColorInput,
        {
          value: normalizeHexColor2(onGetTemplateBackgroundColor(rows.layout.path), "#ffffff"),
          disabled: !canEdit,
          className: "h-6 w-8 rounded border border-border bg-muted cursor-pointer",
          onCommit: (hex) => onSetTemplateBackground(rows.layout.path, hex)
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[10px] truncate", children: rows.layout.label })
    ] }),
    rows.master && (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-[11px]", children: [
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground w-14 shrink-0 truncate", title: rows.master.title, children: t("pptx.master.master") }),
      (0, import_jsx_runtime.jsx)(
        DebouncedColorInput,
        {
          value: normalizeHexColor2(onGetTemplateBackgroundColor(rows.master.path), "#ffffff"),
          disabled: !canEdit,
          className: "h-6 w-8 rounded border border-border bg-muted cursor-pointer",
          onCommit: (hex) => onSetTemplateBackground(rows.master.path, hex)
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground text-[10px] truncate", children: rows.master.label })
    ] })
  ] });
}
var ANIMATION_PANEL_MIN_HEIGHT = 80;
var ANIMATION_PANEL_MAX_HEIGHT = 500;
var ANIMATION_PANEL_DEFAULT_HEIGHT = 220;
function useInspectorPaneState(themeOptions, slideMasters, theme) {
  var _a;
  const activeThemePath = (_a = slideMasters == null ? void 0 : slideMasters[0]) == null ? void 0 : _a.themePath;
  const effectiveThemeOptions = (0, import_react2.useMemo)(() => {
    if (themeOptions.length > 0 || !activeThemePath) {
      return themeOptions;
    }
    return [{ path: activeThemePath, name: theme == null ? void 0 : theme.name }];
  }, [activeThemePath, theme == null ? void 0 : theme.name, themeOptions]);
  const [selectedThemePath, setSelectedThemePath] = (0, import_react2.useState)("");
  (0, import_react2.useEffect)(() => {
    var _a2;
    setSelectedThemePath(activeThemePath ?? ((_a2 = effectiveThemeOptions[0]) == null ? void 0 : _a2.path) ?? "");
  }, [activeThemePath, effectiveThemeOptions]);
  const [animationPanelHeight, setAnimationPanelHeight] = (0, import_react2.useState)(ANIMATION_PANEL_DEFAULT_HEIGHT);
  const onResizeAnimationPanel = (0, import_react2.useCallback)((delta) => {
    setAnimationPanelHeight(
      (height) => Math.min(ANIMATION_PANEL_MAX_HEIGHT, Math.max(ANIMATION_PANEL_MIN_HEIGHT, height - delta))
    );
  }, []);
  return {
    animationPanelHeight,
    effectiveThemeOptions,
    onResizeAnimationPanel,
    selectedThemePath,
    setSelectedThemePath
  };
}
function InspectorPane(props) {
  const {
    isOpen,
    canEdit,
    activeSlide,
    slides,
    customShows,
    canvasSize,
    selectedElement,
    selectedElementIds,
    tableEditorState,
    activeTab,
    onSetActiveTab,
    onClose,
    onUpdateElement,
    onSelectElement,
    onMoveLayer,
    presentationProperties,
    onUpdatePresentationProperties,
    notesMaster,
    handoutMaster,
    notesCanvasSize,
    coreProperties,
    appProperties,
    customProperties,
    themeOptions,
    onUpdateCoreProperties,
    onUpdateAppProperties,
    onUpdateCustomProperties,
    tagCollections,
    onUpdateTagCollections,
    onApplyTheme,
    comments,
    commentDraft,
    commentDraftMentions,
    commentAuthors,
    editingCommentId,
    commentEditDraft,
    onSetCommentDraft,
    onAddComment,
    onDeleteComment,
    onStartEditComment,
    onSaveEditComment,
    onCancelEditComment,
    onSetCommentEditDraft,
    onToggleCommentResolved,
    onStartReply,
    onCancelReply,
    onReplyDraftChange,
    onSubmitReply,
    replyingToCommentId,
    replyDraftByCommentId,
    replyDraftMentionsByCommentId,
    onUpdateCanvasSize,
    slideSizeEmu,
    onUpdateSlideSize,
    onUpdateElementStyle,
    onUpdateTextStyle,
    onUpdateSlide,
    editTemplateMode,
    slideMasters,
    onSetTemplateBackground,
    onGetTemplateBackgroundColor,
    mediaDataUrls,
    theme,
    tableStyleMap,
    onTableStyleMapChange,
    onDeleteTableStyle,
    panelWidth
  } = props;
  const hasSelection = selectedElement !== null;
  const { t } = useTranslation();
  const { dragY, handlers: dragHandlers } = useSheetDismissDrag(onClose);
  const {
    animationPanelHeight,
    effectiveThemeOptions,
    onResizeAnimationPanel,
    selectedThemePath,
    setSelectedThemePath
  } = useInspectorPaneState(themeOptions, slideMasters, theme);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    isOpen && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        "aria-label": t("pptx.common.close"),
        onClick: onClose,
        className: "md:hidden fixed inset-0 z-20 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150"
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: cn(
          // Shared styles
          "bg-background flex flex-col text-xs text-foreground shadow-xl",
          // Mobile: absolute bottom sheet overlay sized via dvh so it
          // adapts to the on-screen keyboard / dynamic browser chrome.
          "max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[75dvh] max-md:rounded-t-2xl max-md:border-t max-md:border-border max-md:z-30 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
          "max-md:transition-transform max-md:duration-200 max-md:ease-in-out",
          isOpen ? "max-md:translate-y-0" : "max-md:translate-y-full",
          // Desktop: flow-based flex child (takes space from canvas)
          "md:h-full md:border-l md:border-border",
          !panelWidth && "md:w-72"
        ),
        style: {
          ...panelWidth ? { width: panelWidth } : {},
          ...dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : {}
        },
        children: [
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "md:hidden flex items-center justify-center pt-3 pb-2 cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: dragHandlers.onPointerDown,
              onPointerMove: dragHandlers.onPointerMove,
              onPointerUp: dragHandlers.onPointerUp,
              onPointerCancel: dragHandlers.onPointerCancel,
              children: (0, import_jsx_runtime.jsx)("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" })
            }
          ),
          (0, import_jsx_runtime.jsx)(
            InspectorPaneHeader,
            {
              activeTab,
              onSetActiveTab,
              onClose
            }
          ),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 overflow-y-auto p-3 space-y-3", children: [
            activeTab === "elements" && (0, import_jsx_runtime.jsx)(
              InspectorElementsTab,
              {
                activeSlide,
                selectedElementId: (selectedElement == null ? void 0 : selectedElement.id) ?? null,
                selectedElementIds,
                canEdit,
                onSelectElement,
                onUpdateSlide
              }
            ),
            activeTab === "properties" && (0, import_jsx_runtime.jsx)("div", { className: "space-y-3", children: hasSelection && selectedElement ? (0, import_jsx_runtime.jsx)(
              ElementInspectorBody,
              {
                selectedElement,
                canEdit,
                slides,
                customShows,
                tableEditorState,
                mediaDataUrls,
                tableStyleMap,
                onTableStyleMapChange,
                onDeleteTableStyle,
                onUpdateElement,
                onUpdateElementStyle,
                onUpdateTextStyle,
                onMoveLayer
              }
            ) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              (0, import_jsx_runtime.jsx)(
                PresentationPropertiesPanel,
                {
                  canEdit,
                  canvasSize,
                  presentationProperties,
                  onUpdatePresentationProperties,
                  notesMaster,
                  handoutMaster,
                  notesCanvasSize,
                  coreProperties,
                  appProperties,
                  customProperties,
                  themeOptions: effectiveThemeOptions,
                  selectedThemePath,
                  setSelectedThemePath,
                  onApplyTheme,
                  onUpdateCoreProperties,
                  onUpdateAppProperties,
                  onUpdateCustomProperties,
                  tagCollections,
                  onUpdateTagCollections,
                  onUpdateCanvasSize,
                  slideSizeEmu,
                  onUpdateSlideSize,
                  hasContent: slides.some((slide) => slide.elements.length > 0),
                  activeSlide,
                  theme,
                  onUpdateSlide
                }
              ),
              activeSlide && (0, import_jsx_runtime.jsx)(
                SlideBackgroundPanel,
                {
                  activeSlide,
                  canEdit,
                  onUpdateSlide,
                  editTemplateMode,
                  slideMasters,
                  onSetTemplateBackground,
                  onGetTemplateBackgroundColor
                }
              )
            ] }) }),
            activeTab === "comments" && (0, import_jsx_runtime.jsx)(
              InspectorCommentsSection,
              {
                comments,
                canEdit,
                activeSlide,
                selectedElement,
                editingCommentId,
                commentEditDraft,
                commentDraft,
                commentDraftMentions: commentDraftMentions ?? [],
                commentAuthors: commentAuthors ?? [],
                replyingToCommentId: replyingToCommentId ?? null,
                replyDraftByCommentId: replyDraftByCommentId ?? {},
                replyDraftMentionsByCommentId: replyDraftMentionsByCommentId ?? {},
                onSetCommentDraft,
                onAddComment,
                onDeleteComment,
                onStartEditComment,
                onSaveEditComment,
                onCancelEditComment,
                onSetCommentEditDraft,
                onToggleCommentResolved,
                onStartReply,
                onCancelReply,
                onReplyDraftChange,
                onSubmitReply,
                onSelectElement
              }
            )
          ] }),
          hasSelection && selectedElement && activeSlide && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            (0, import_jsx_runtime.jsx)(ResizeHandle, { direction: "vertical", onResize: onResizeAnimationPanel }),
            (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "border-t border-border p-3 overflow-y-auto flex-shrink-0",
                style: { height: animationPanelHeight },
                children: (0, import_jsx_runtime.jsx)(
                  AnimationPanel,
                  {
                    selectedElement,
                    activeSlide,
                    canEdit,
                    onUpdateSlide
                  }
                )
              }
            )
          ] })
        ]
      }
    )
  ] });
}
function PresentationAnnotationOverlay({
  canvasSize,
  editorScale,
  presentationTool,
  blackout,
  annotationStrokes,
  currentStroke,
  laserPosition,
  onPointerDown,
  onPointerMove,
  onPointerUp,
  onLaserMove,
  onLaserLeave,
  onEraseAtPoint
}) {
  const svgRef = (0, import_react2.useRef)(null);
  const isErasingRef = (0, import_react2.useRef)(false);
  const toSlideCoords = (0, import_react2.useCallback)(
    (clientX, clientY) => {
      const svg = svgRef.current;
      if (!svg) {
        return null;
      }
      const rect = svg.getBoundingClientRect();
      const x = (clientX - rect.left) / editorScale;
      const y = (clientY - rect.top) / editorScale;
      return { x, y };
    },
    [editorScale]
  );
  const handlePointerDown = (0, import_react2.useCallback)(
    (e) => {
      if (presentationTool === "none") {
        return;
      }
      e.preventDefault();
      e.stopPropagation();
      const coords = toSlideCoords(e.clientX, e.clientY);
      if (!coords) {
        return;
      }
      if (presentationTool === "eraser") {
        isErasingRef.current = true;
        onEraseAtPoint(coords.x, coords.y);
        return;
      }
      if (presentationTool === "pen" || presentationTool === "highlighter") {
        onPointerDown(coords.x, coords.y);
      }
    },
    [presentationTool, toSlideCoords, onPointerDown, onEraseAtPoint]
  );
  const handlePointerMove = (0, import_react2.useCallback)(
    (e) => {
      if (presentationTool === "none") {
        return;
      }
      const coords = toSlideCoords(e.clientX, e.clientY);
      if (!coords) {
        return;
      }
      if (presentationTool === "laser") {
        onLaserMove(coords.x, coords.y);
        return;
      }
      if (presentationTool === "eraser" && isErasingRef.current) {
        onEraseAtPoint(coords.x, coords.y);
        return;
      }
      if (presentationTool === "pen" || presentationTool === "highlighter") {
        onPointerMove(coords.x, coords.y);
      }
    },
    [presentationTool, toSlideCoords, onPointerMove, onLaserMove, onEraseAtPoint]
  );
  const handlePointerUp = (0, import_react2.useCallback)(
    (e) => {
      if (presentationTool === "none") {
        return;
      }
      e.preventDefault();
      if (presentationTool === "eraser") {
        isErasingRef.current = false;
        return;
      }
      onPointerUp();
    },
    [presentationTool, onPointerUp]
  );
  const handlePointerLeave = (0, import_react2.useCallback)(() => {
    if (presentationTool === "laser") {
      onLaserLeave();
    }
    if (presentationTool === "eraser") {
      isErasingRef.current = false;
    }
    onPointerUp();
  }, [presentationTool, onLaserLeave, onPointerUp]);
  const allStrokes = currentStroke ? [...annotationStrokes, currentStroke] : annotationStrokes;
  const isCapturing = presentationTool !== "none";
  if (!isCapturing && allStrokes.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-pptx-annotation-overlay": true,
      className: "absolute inset-0",
      style: {
        zIndex: annotationOverlayZIndex(blackout),
        cursor: cursorForTool(presentationTool),
        pointerEvents: isCapturing ? "auto" : "none"
      },
      children: [
        (0, import_jsx_runtime.jsx)(
          "svg",
          {
            ref: svgRef,
            className: "absolute",
            style: {
              width: canvasSize.width,
              height: canvasSize.height,
              transformOrigin: "top left",
              transform: `scale(${editorScale})`
            },
            viewBox: `0 0 ${canvasSize.width} ${canvasSize.height}`,
            onPointerDown: handlePointerDown,
            onPointerMove: handlePointerMove,
            onPointerUp: handlePointerUp,
            onPointerLeave: handlePointerLeave,
            children: allStrokes.map((stroke) => (0, import_jsx_runtime.jsx)(
              "path",
              {
                d: buildStrokePathD(stroke.points),
                fill: "none",
                stroke: stroke.color,
                strokeWidth: stroke.width,
                strokeLinecap: "round",
                strokeLinejoin: "round",
                opacity: stroke.opacity
              },
              stroke.id
            ))
          }
        ),
        presentationTool === "laser" && laserPosition && (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "absolute rounded-full pointer-events-none",
            style: {
              width: 24,
              height: 24,
              left: laserPosition.x * editorScale - 12,
              top: laserPosition.y * editorScale - 12,
              backgroundColor: "rgba(255, 0, 0, 0.85)",
              boxShadow: "0 0 12px 6px rgba(255, 0, 0, 0.5), 0 0 24px 12px rgba(255, 0, 0, 0.25)",
              filter: "drop-shadow(0 0 8px rgba(255, 0, 0, 0.7))",
              zIndex: 70
            }
          }
        )
      ]
    }
  );
}
function PresentationToolbar({
  presentationTool,
  penColor,
  highlighterColor,
  hasAnnotations,
  onSetTool,
  onSetPenColor,
  onSetHighlighterColor,
  onClearAnnotations,
  blackout,
  onToggleBlackboard,
  currentSlideIndex,
  totalSlides,
  onMovePresentationSlide,
  presentationStartTime,
  onEndPresentation,
  onTogglePresenterView,
  presenterMode
}) {
  const { t } = useTranslation();
  const [showPenColors, setShowPenColors] = (0, import_react2.useState)(false);
  const [showHighlighterColors, setShowHighlighterColors] = (0, import_react2.useState)(false);
  const toolbarRef = (0, import_react2.useRef)(null);
  const [now, setNow] = (0, import_react2.useState)(Date.now());
  (0, import_react2.useEffect)(() => {
    if (!presentationStartTime) {
      return;
    }
    const interval = window.setInterval(() => setNow(Date.now()), 1e3);
    return () => window.clearInterval(interval);
  }, [presentationStartTime]);
  const elapsed = presentationStartTime ? now - presentationStartTime : 0;
  (0, import_react2.useEffect)(() => {
    const handler = (e) => {
      if (toolbarRef.current && !toolbarRef.current.contains(e.target)) {
        setShowPenColors(false);
        setShowHighlighterColors(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, []);
  const handleToolClick = (0, import_react2.useCallback)(
    (tool) => {
      onSetTool(tool);
      setShowPenColors(false);
      setShowHighlighterColors(false);
    },
    [onSetTool]
  );
  const handlePenRightClick = (0, import_react2.useCallback)((e) => {
    e.preventDefault();
    setShowPenColors((prev) => !prev);
    setShowHighlighterColors(false);
  }, []);
  const handleHighlighterRightClick = (0, import_react2.useCallback)((e) => {
    e.preventDefault();
    setShowHighlighterColors((prev) => !prev);
    setShowPenColors(false);
  }, []);
  const toolBtnClass = (tool) => presentationTool === tool ? PRESENT_TOOLBAR_CLASSES.toggleActive : PRESENT_TOOLBAR_CLASSES.toggle;
  const navBtnClass = PRESENT_TOOLBAR_CLASSES.button;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: toolbarRef,
      "data-pptx-present-toolbar": true,
      role: "toolbar",
      tabIndex: -1,
      "aria-label": t("pptx.toolbar.presentationToolbarAria"),
      className: PRESENT_TOOLBAR_CLASSES.container,
      onClick: (e) => e.stopPropagation(),
      children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "previous",
            className: navBtnClass,
            onClick: () => onMovePresentationSlide(-1),
            disabled: currentSlideIndex === 0,
            title: t("pptx.presenter.previousSlide"),
            "aria-label": t("pptx.presenter.previousSlide"),
            children: (0, import_jsx_runtime.jsx)(LuChevronLeft, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { "data-pptx-present-control": "counter", className: PRESENT_TOOLBAR_CLASSES.counter, children: formatSlideCounter(currentSlideIndex, totalSlides) }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "next",
            className: navBtnClass,
            onClick: () => onMovePresentationSlide(1),
            disabled: currentSlideIndex >= totalSlides - 1,
            title: t("pptx.presenter.nextSlide"),
            "aria-label": t("pptx.presenter.nextSlide"),
            children: (0, import_jsx_runtime.jsx)(LuChevronRight, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            "data-pptx-present-control": "divider-navigation",
            className: PRESENT_TOOLBAR_CLASSES.divider
          }
        ),
        (0, import_jsx_runtime.jsxs)(
          "div",
          {
            "data-pptx-present-control": "timer",
            className: PRESENT_TOOLBAR_CLASSES.timer,
            title: t("pptx.presenter.elapsed"),
            "aria-label": t("pptx.presenter.elapsed"),
            children: [
              (0, import_jsx_runtime.jsx)(LuTimer, { size: 14 }),
              (0, import_jsx_runtime.jsx)("span", { children: formatElapsed(elapsed) })
            ]
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { "data-pptx-present-control": "divider-timer", className: PRESENT_TOOLBAR_CLASSES.divider }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "laser",
            className: toolBtnClass("laser"),
            onClick: () => handleToolClick("laser"),
            title: t("pptx.presentation.laserPointer"),
            "aria-label": t("pptx.presentation.laserPointer"),
            children: (0, import_jsx_runtime.jsx)(LuMousePointer2, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative flex items-center", children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "pen",
              className: toolBtnClass("pen"),
              onClick: () => handleToolClick("pen"),
              onContextMenu: handlePenRightClick,
              title: t("pptx.presentation.pen"),
              "aria-label": t("pptx.presentation.pen"),
              children: [
                (0, import_jsx_runtime.jsx)(LuPenTool, { size: 18 }),
                (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: PRESENT_TOOLBAR_CLASSES.swatchBar,
                    style: { backgroundColor: penColor }
                  }
                )
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "pen-color",
              className: PRESENT_TOOLBAR_CLASSES.caret,
              onClick: () => {
                setShowPenColors((prev) => !prev);
                setShowHighlighterColors(false);
              },
              title: t("pptx.presentationToolbar.penColor"),
              "aria-label": t("pptx.presentationToolbar.penColor"),
              children: (0, import_jsx_runtime.jsx)(LuChevronDown, { size: 12 })
            }
          ),
          showPenColors && (0, import_jsx_runtime.jsx)("div", { className: PRESENT_TOOLBAR_CLASSES.palette, children: PEN_COLORS.map((color) => (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "aria-label": t("pptx.presentationToolbar.penColorValue", { color }),
              className: `${PRESENT_TOOLBAR_CLASSES.swatch} ${penColor === color ? "border-white" : "border-white/20"}`,
              style: { backgroundColor: color },
              onClick: () => {
                onSetPenColor(color);
                setShowPenColors(false);
                if (presentationTool !== "pen") {
                  onSetTool("pen");
                }
              }
            },
            color
          )) })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "relative flex items-center", children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "highlighter",
              className: toolBtnClass("highlighter"),
              onClick: () => handleToolClick("highlighter"),
              onContextMenu: handleHighlighterRightClick,
              title: t("pptx.presentation.highlighter"),
              "aria-label": t("pptx.presentation.highlighter"),
              children: [
                (0, import_jsx_runtime.jsx)(LuHighlighter, { size: 18 }),
                (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: PRESENT_TOOLBAR_CLASSES.swatchBar,
                    style: { backgroundColor: highlighterColor }
                  }
                )
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "data-pptx-present-control": "highlighter-color",
              className: PRESENT_TOOLBAR_CLASSES.caret,
              onClick: () => {
                setShowHighlighterColors((prev) => !prev);
                setShowPenColors(false);
              },
              title: t("pptx.presentationToolbar.highlighterColor"),
              "aria-label": t("pptx.presentationToolbar.highlighterColor"),
              children: (0, import_jsx_runtime.jsx)(LuChevronDown, { size: 12 })
            }
          ),
          showHighlighterColors && (0, import_jsx_runtime.jsx)("div", { className: PRESENT_TOOLBAR_CLASSES.palette, children: HIGHLIGHTER_COLORS.map((color) => (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "aria-label": t("pptx.presentationToolbar.highlighterColorValue", { color }),
              className: `${PRESENT_TOOLBAR_CLASSES.swatch} ${highlighterColor === color ? "border-white" : "border-white/20"}`,
              style: { backgroundColor: color },
              onClick: () => {
                onSetHighlighterColor(color);
                setShowHighlighterColors(false);
                if (presentationTool !== "highlighter") {
                  onSetTool("highlighter");
                }
              }
            },
            color
          )) })
        ] }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "eraser",
            className: toolBtnClass("eraser"),
            onClick: () => handleToolClick("eraser"),
            title: t("pptx.presentation.eraser"),
            "aria-label": t("pptx.presentation.eraser"),
            children: (0, import_jsx_runtime.jsx)(LuEraser, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "blackboard",
            className: isBlackboardActive(blackout, presentationTool) ? PRESENT_TOOLBAR_CLASSES.toggleActive : PRESENT_TOOLBAR_CLASSES.toggle,
            onClick: onToggleBlackboard,
            title: t("pptx.presentation.blackboard"),
            "aria-label": t("pptx.presentation.blackboard"),
            children: (0, import_jsx_runtime.jsx)(LuPresentation, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "clear",
            className: `${PRESENT_TOOLBAR_CLASSES.button} ${hasAnnotations ? "hover:text-red-400" : ""}`,
            onClick: hasAnnotations ? onClearAnnotations : void 0,
            title: t("pptx.presentation.clearAnnotations"),
            "aria-label": t("pptx.presentation.clearAnnotations"),
            disabled: !hasAnnotations,
            children: (0, import_jsx_runtime.jsx)(LuTrash2, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsx)("div", { "data-pptx-present-control": "divider-tools", className: PRESENT_TOOLBAR_CLASSES.divider }),
        onTogglePresenterView && (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "presenter-view",
            className: presenterMode ? PRESENT_TOOLBAR_CLASSES.toggleActive : PRESENT_TOOLBAR_CLASSES.toggle,
            onClick: onTogglePresenterView,
            title: t("pptx.presenter.presenterView"),
            "aria-label": t("pptx.presenter.presenterView"),
            children: (0, import_jsx_runtime.jsx)(LuPanelRight, { size: 18 })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "data-pptx-present-control": "end",
            className: `${PRESENT_TOOLBAR_CLASSES.button} hover:text-red-400`,
            onClick: onEndPresentation,
            title: t("pptx.presenter.endPresentation"),
            "aria-label": t("pptx.presenter.endPresentation"),
            children: (0, import_jsx_runtime.jsx)(LuX, { size: 18 })
          }
        )
      ]
    }
  );
}
function PresentationTouchControls({
  currentSlideIndex,
  totalSlides,
  onMovePresentationSlide,
  onEndPresentation,
  hideNavigation = false
}) {
  const { t } = useTranslation();
  const { isTouchDevice } = useIsMobile();
  if (!isTouchDevice) {
    return null;
  }
  const btnClass = "pointer-events-auto flex items-center justify-center w-11 h-11 min-w-11 min-h-11 rounded-full bg-black/55 text-white shadow-lg active:bg-black/75";
  const stop = (e) => {
    e.stopPropagation();
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: `fixed z-[90] ${btnClass}`,
        style: {
          top: "calc(env(safe-area-inset-top, 0px) + 0.5rem)",
          right: "calc(env(safe-area-inset-right, 0px) + 0.5rem)",
          touchAction: "manipulation"
        },
        onClick: (e) => {
          stop(e);
          onEndPresentation();
        },
        "aria-label": t("pptx.presenter.endPresentation"),
        children: (0, import_jsx_runtime.jsx)(LuX, { size: 22 })
      }
    ),
    !hideNavigation && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: `fixed z-[90] top-1/2 -translate-y-1/2 ${btnClass} disabled:opacity-30`,
          style: {
            left: "calc(env(safe-area-inset-left, 0px) + 0.5rem)",
            touchAction: "manipulation"
          },
          disabled: currentSlideIndex <= 0,
          onClick: (e) => {
            stop(e);
            onMovePresentationSlide(-1);
          },
          "aria-label": t("pptx.presenter.previousSlide"),
          children: (0, import_jsx_runtime.jsx)(LuChevronLeft, { size: 26 })
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          className: `fixed z-[90] top-1/2 -translate-y-1/2 ${btnClass} disabled:opacity-30`,
          style: {
            right: "calc(env(safe-area-inset-right, 0px) + 0.5rem)",
            touchAction: "manipulation"
          },
          disabled: currentSlideIndex >= totalSlides - 1,
          onClick: (e) => {
            stop(e);
            onMovePresentationSlide(1);
          },
          "aria-label": t("pptx.presenter.nextSlide"),
          children: (0, import_jsx_runtime.jsx)(LuChevronRight, { size: 26 })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      "span",
      {
        className: "fixed z-[90] left-1/2 -translate-x-1/2 px-3 py-1 rounded-full bg-black/55 text-white text-xs font-mono tabular-nums pointer-events-none",
        style: { bottom: "calc(env(safe-area-inset-bottom, 0px) + 0.5rem)" },
        children: totalSlides === 0 ? "0 / 0" : `${currentSlideIndex + 1} / ${totalSlides}`
      }
    )
  ] });
}
function SlideLayer({ slide, templateElements, canvasSize }) {
  const safeWidth = Math.max(canvasSize.width, 1);
  const safeHeight = Math.max(canvasSize.height, 1);
  const elements = [...visibleTemplateElements(slide, templateElements), ...slide.elements];
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      className: "relative overflow-hidden",
      style: {
        width: safeWidth,
        height: safeHeight,
        backgroundColor: slide.backgroundColor ? normalizeHexColor2(slide.backgroundColor, "#ffffff") : "#ffffff",
        backgroundImage: slide.backgroundImage ? `url(${slide.backgroundImage})` : slide.backgroundGradient ? slide.backgroundGradient : void 0,
        backgroundSize: slide.backgroundImage ? "cover" : void 0,
        backgroundPosition: slide.backgroundImage ? "center" : void 0
      },
      children: [
        (0, import_jsx_runtime.jsx)(SlideBackgroundImageLayer, { slide }),
        elements.map((element, index) => (0, import_jsx_runtime.jsx)(
          StaticElementRenderer,
          {
            element,
            activeSlide: slide,
            allSlides: [slide],
            zIndex: index
          },
          element.id
        ))
      ]
    }
  );
}
function PresentationTransitionOverlay({
  outgoingSlide,
  templateElements,
  canvasSize,
  transition,
  durationMs,
  scale: stageScale,
  morphPlan,
  incomingSlide,
  onComplete
}) {
  const containerRef = (0, import_react2.useRef)(null);
  const [containerSize, setContainerSize] = (0, import_react2.useState)(null);
  const resolvedStageScale = typeof stageScale === "number" && Number.isFinite(stageScale) && stageScale > 0 ? stageScale : null;
  (0, import_react2.useEffect)(() => {
    if (resolvedStageScale !== null) {
      return;
    }
    const el = containerRef.current;
    if (!el) {
      return;
    }
    const rect = el.getBoundingClientRect();
    setContainerSize({ width: rect.width, height: rect.height });
  }, [resolvedStageScale]);
  (0, import_react2.useEffect)(() => {
    const timer = window.setTimeout(onComplete, durationMs + 50);
    return () => {
      window.clearTimeout(timer);
    };
  }, [durationMs, onComplete]);
  const animations = (0, import_react2.useMemo)(
    () => getSlideTransitionAnimations(
      transition.type,
      durationMs,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern
    ),
    [
      transition.type,
      transition.direction,
      transition.orient,
      transition.spokes,
      transition.pattern,
      durationMs
    ]
  );
  const scale = (0, import_react2.useMemo)(() => {
    if (resolvedStageScale !== null) {
      return resolvedStageScale;
    }
    if (!containerSize) {
      return 1;
    }
    const scaleX = containerSize.width / Math.max(canvasSize.width, 1);
    const scaleY = containerSize.height / Math.max(canvasSize.height, 1);
    return Math.min(scaleX, scaleY);
  }, [resolvedStageScale, containerSize, canvasSize]);
  const outgoingZIndex = animations.outgoingOnTop ? 40 : 20;
  if (morphPlan) {
    return (0, import_jsx_runtime.jsxs)(
      "div",
      {
        ref: containerRef,
        "data-pptx-transition-overlay": true,
        "data-pptx-transition-morph": "true",
        className: "pptx-react-transition-overlay absolute inset-0 pointer-events-none overflow-hidden",
        style: { zIndex: 40 },
        children: [
          (0, import_jsx_runtime.jsx)("style", { children: morphPlan.keyframesCss }),
          (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 flex items-center justify-center", children: (0, import_jsx_runtime.jsxs)(
            "div",
            {
              style: {
                width: canvasSize.width,
                height: canvasSize.height,
                // The slide box is a flex ITEM, so it shrinks to the container
                // unless told not to. A show surface narrower than the deck's own
                // canvas (a windowed show, or a display scaled past 100%) squeezed
                // the unscaled 1280px box down to the container width and only
                // then scaled it, landing the whole outgoing slide up to ~77px to
                // the right of the live one for the length of the morph (issue
                // #161). The stage below positions its slide box absolutely and is
                // unaffected, so the two layers disagreed.
                flexShrink: 0,
                transform: `scale(${scale})`,
                transformOrigin: "center",
                position: "relative"
              },
              children: [
                morphPlan.outgoingElements.map((element, index) => (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    "data-pptx-morph-outgoing": element.id,
                    style: { position: "absolute", inset: 0 },
                    children: (0, import_jsx_runtime.jsx)(
                      StaticElementRenderer,
                      {
                        element,
                        activeSlide: outgoingSlide,
                        allSlides: [outgoingSlide],
                        zIndex: index,
                        animation: morphPlan.outgoingAnimations.get(element.id),
                        imageAnimation: morphPlan.outgoingImageAnimations.get(element.id)
                      }
                    )
                  },
                  element.id
                )),
                incomingSlide && morphPlan.overlayIncomingElements.map((element, index) => (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    "data-pptx-morph-lifted": element.id,
                    style: { position: "absolute", inset: 0 },
                    children: (0, import_jsx_runtime.jsx)(
                      StaticElementRenderer,
                      {
                        element,
                        activeSlide: incomingSlide,
                        allSlides: [incomingSlide],
                        zIndex: morphPlan.outgoingElements.length + index,
                        animation: morphPlan.overlayIncomingAnimations.get(element.id)
                      }
                    )
                  },
                  element.id
                )),
                incomingSlide && morphPlan.crossfadeGroups.map((group, index) => (0, import_jsx_runtime.jsxs)(
                  "div",
                  {
                    "data-pptx-morph-crossfade": group.incoming.id,
                    style: {
                      ...MORPH_CROSSFADE_GROUP_STYLE,
                      zIndex: morphPlan.outgoingElements.length + morphPlan.overlayIncomingElements.length + index
                    },
                    children: [
                      (0, import_jsx_runtime.jsx)(
                        "div",
                        {
                          "data-pptx-morph-outgoing": group.outgoing.id,
                          style: { ...MORPH_CROSSFADE_HALF_STYLE, animation: group.outgoingAnimation },
                          children: (0, import_jsx_runtime.jsx)(
                            StaticElementRenderer,
                            {
                              element: group.outgoing,
                              activeSlide: outgoingSlide,
                              allSlides: [outgoingSlide],
                              zIndex: 0,
                              animation: morphPlan.outgoingAnimations.get(group.outgoing.id),
                              imageAnimation: morphPlan.outgoingImageAnimations.get(group.outgoing.id)
                            }
                          )
                        }
                      ),
                      (0, import_jsx_runtime.jsx)(
                        "div",
                        {
                          "data-pptx-morph-lifted": group.incoming.id,
                          style: { ...MORPH_CROSSFADE_HALF_STYLE, animation: group.incomingAnimation },
                          children: (0, import_jsx_runtime.jsx)(
                            StaticElementRenderer,
                            {
                              element: group.incoming,
                              activeSlide: incomingSlide,
                              allSlides: [incomingSlide],
                              zIndex: 0,
                              animation: morphPlan.overlayIncomingAnimations.get(group.incoming.id)
                            }
                          )
                        }
                      )
                    ]
                  },
                  group.incoming.id
                ))
              ]
            }
          ) })
        ]
      }
    );
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: containerRef,
      "data-pptx-transition-overlay": true,
      className: "pptx-react-transition-overlay absolute inset-0 pointer-events-none overflow-hidden",
      style: { zIndex: outgoingZIndex },
      children: [
        (0, import_jsx_runtime.jsx)("style", { children: SLIDE_TRANSITION_KEYFRAMES }),
        !morphPlan && animations.incoming !== "none" && incomingSlide ? (0, import_jsx_runtime.jsx)(
          "div",
          {
            "data-pptx-transition-layer": "incoming",
            className: "pptx-react-transition-layer absolute inset-0 flex items-center justify-center",
            style: {
              animation: animations.incoming,
              zIndex: animations.outgoingOnTop ? 0 : 10
            },
            children: (0, import_jsx_runtime.jsx)(
              "div",
              {
                style: {
                  width: canvasSize.width,
                  height: canvasSize.height,
                  flexShrink: 0,
                  transform: `scale(${scale})`,
                  transformOrigin: "center"
                },
                children: (0, import_jsx_runtime.jsx)(
                  SlideLayer,
                  {
                    slide: incomingSlide,
                    templateElements,
                    canvasSize
                  }
                )
              }
            )
          }
        ) : null,
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            "data-pptx-transition-layer": "outgoing",
            className: "pptx-react-transition-layer absolute inset-0 flex items-center justify-center",
            style: {
              animation: animations.outgoing !== "none" ? animations.outgoing : void 0,
              zIndex: animations.outgoingOnTop ? 10 : 0
            },
            children: (0, import_jsx_runtime.jsx)(
              "div",
              {
                style: {
                  width: canvasSize.width,
                  height: canvasSize.height,
                  // See the morph layer above: a flex item shrinks to its container,
                  // which offsets the whole outgoing slide on a show surface narrower
                  // than the deck's canvas.
                  flexShrink: 0,
                  transform: `scale(${scale})`,
                  transformOrigin: "center"
                },
                children: (0, import_jsx_runtime.jsx)(
                  SlideLayer,
                  {
                    slide: outgoingSlide,
                    templateElements,
                    canvasSize
                  }
                )
              }
            )
          }
        )
      ]
    }
  );
}
function KeepAnnotationsDialog({
  isOpen,
  annotationCount,
  slideCount,
  onKeep,
  onDiscard
}) {
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/50",
      children: (0, import_jsx_runtime.jsxs)("div", { className: "bg-background border border-border rounded-lg shadow-xl w-[420px] max-w-[90vw] p-6 animate-in fade-in zoom-in-95 duration-200", children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 mb-4", children: [
          (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center w-10 h-10 rounded-full bg-primary/10", children: (0, import_jsx_runtime.jsx)(LuPenTool, { className: "w-5 h-5 text-primary" }) }),
          (0, import_jsx_runtime.jsxs)("div", { children: [
            (0, import_jsx_runtime.jsx)("h2", { className: "text-base font-semibold text-foreground", children: t("pptx.presentation.keepAnnotationsTitle") }),
            (0, import_jsx_runtime.jsx)("p", { className: "text-sm text-muted-foreground", children: t("pptx.presentation.keepAnnotationsDescription", {
              count: annotationCount,
              slides: slideCount
            }) })
          ] })
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 mt-6", children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md border border-border bg-background text-foreground hover:bg-accent transition-colors",
              onClick: onDiscard,
              children: [
                (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-4 h-4" }),
                t("pptx.presentation.discardAnnotations")
              ]
            }
          ),
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md bg-primary text-white hover:bg-primary/90 transition-colors",
              onClick: onKeep,
              children: [
                (0, import_jsx_runtime.jsx)(LuPenTool, { className: "w-4 h-4" }),
                t("pptx.presentation.keepAnnotations")
              ]
            }
          )
        ] })
      ] })
    }
  );
}
var PREVIEW_SCALE2 = 0.9;
var NOTE_LINE_COUNT = 5;
function NotesPagePreview({
  slideIndices,
  slides,
  frameSlides,
  maxPages = 3
}) {
  const { t } = useTranslation();
  const pages = (0, import_react2.useMemo)(() => computeAllNotesPages(slideIndices), [slideIndices]);
  const printable = (0, import_react2.useMemo)(() => getNotesPrintableArea(), []);
  const visiblePages = pages.slice(0, maxPages);
  const scaledW = printable.width * PREVIEW_SCALE2;
  const scaledH = printable.height * PREVIEW_SCALE2;
  if (slideIndices.length === 0) {
    return (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-full text-xs text-muted-foreground", children: t("pptx.print.preview.noSlides") });
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-3 py-2", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wide", children: t("pptx.print.preview.title") }),
    visiblePages.map((page) => {
      const slide = slides[page.slideIndex];
      const hasNotes = Boolean(slide == null ? void 0 : slide.notes);
      const borderCls = frameSlides ? "border-2 border-gray-800" : "border border-gray-300";
      return (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: "relative bg-white border border-gray-200 shadow-sm rounded",
          style: {
            width: scaledW,
            height: scaledH,
            overflow: "hidden"
          },
          children: [
            (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: `absolute flex items-center justify-center rounded-sm bg-gray-50 ${borderCls}`,
                style: {
                  left: page.slideArea.x * PREVIEW_SCALE2,
                  top: page.slideArea.y * PREVIEW_SCALE2,
                  width: page.slideArea.width * PREVIEW_SCALE2,
                  height: page.slideArea.height * PREVIEW_SCALE2
                },
                children: (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] font-medium text-gray-400", children: page.slideIndex + 1 })
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute left-1 right-1 border-b border-gray-200",
                style: {
                  top: page.textArea.y * PREVIEW_SCALE2 - 1
                }
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: "absolute",
                style: {
                  left: page.textArea.x * PREVIEW_SCALE2 + 4,
                  top: page.textArea.y * PREVIEW_SCALE2,
                  width: page.textArea.width * PREVIEW_SCALE2 - 8,
                  height: page.textArea.height * PREVIEW_SCALE2
                },
                children: Array.from({ length: NOTE_LINE_COUNT }, (_, li) => {
                  const lineSpacing = page.textArea.height * PREVIEW_SCALE2 / (NOTE_LINE_COUNT + 1);
                  return (0, import_jsx_runtime.jsx)(
                    "div",
                    {
                      className: `absolute left-0 right-0 border-b ${hasNotes ? "border-gray-300" : "border-gray-200"}`,
                      style: { top: lineSpacing * (li + 1) }
                    },
                    li
                  );
                })
              }
            )
          ]
        },
        page.pageIndex
      );
    }),
    pages.length > maxPages && (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.print.preview.morePages", {
      count: pages.length - maxPages
    }) })
  ] });
}
var PREVIEW_SCALE3 = 0.9;
function PrintPreview({
  slideIndices,
  slidesPerPage,
  orientation,
  frameSlides,
  maxPages = 3
}) {
  const { t } = useTranslation();
  const pages = (0, import_react2.useMemo)(
    () => computeHandoutLayout(slideIndices, slidesPerPage, orientation),
    [slideIndices, slidesPerPage, orientation]
  );
  const printable = (0, import_react2.useMemo)(() => getPrintableArea(orientation), [orientation]);
  const visiblePages = pages.slice(0, maxPages);
  const isThreePerPage = slidesPerPage === 3;
  const noteLineCount = generateNoteLineCount();
  const scaledW = printable.width * PREVIEW_SCALE3;
  const scaledH = printable.height * PREVIEW_SCALE3;
  const noteAreaFraction = 0.5;
  if (slideIndices.length === 0) {
    return (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center h-full text-xs text-muted-foreground", children: t("pptx.print.preview.noSlides") });
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-3 py-2", children: [
    (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground uppercase tracking-wide", children: t("pptx.print.preview.title") }),
    visiblePages.map((page) => (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "relative bg-white border border-gray-200 shadow-sm rounded",
        style: {
          width: scaledW,
          height: scaledH,
          overflow: "hidden"
        },
        children: page.cells.map((cell, idx) => {
          const isEmpty = cell.slideIndex < 0;
          const cx = cell.x * PREVIEW_SCALE3;
          const cy = cell.y * PREVIEW_SCALE3;
          const cw = cell.width * PREVIEW_SCALE3;
          const ch = cell.height * PREVIEW_SCALE3;
          const borderCls = frameSlides ? "border-2 border-gray-800" : "border border-gray-300";
          return (0, import_jsx_runtime.jsxs)(
            "div",
            {
              className: "absolute flex items-start",
              style: {
                left: cx,
                top: cy,
                gap: isThreePerPage ? 2 : 0
              },
              children: [
                (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: `flex items-center justify-center rounded-sm bg-gray-50 ${isEmpty ? "opacity-30" : ""} ${borderCls}`,
                    style: { width: cw, height: ch },
                    children: !isEmpty && (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] font-medium text-gray-400", children: cell.slideIndex + 1 })
                  }
                ),
                isThreePerPage && !isEmpty && (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: "relative border-l border-gray-200 pl-1",
                    style: {
                      width: scaledW * noteAreaFraction,
                      height: ch
                    },
                    children: Array.from({ length: noteLineCount }, (_, li) => {
                      const lineY = ch / (noteLineCount + 1) * (li + 1);
                      return (0, import_jsx_runtime.jsx)(
                        "div",
                        {
                          className: "absolute left-1 right-0 border-b border-gray-200",
                          style: { top: lineY }
                        },
                        li
                      );
                    })
                  }
                )
              ]
            },
            idx
          );
        })
      },
      page.pageIndex
    )),
    pages.length > maxPages && (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.print.preview.morePages", {
      count: pages.length - maxPages
    }) })
  ] });
}
var radioClass = (active) => `flex items-center gap-2 px-3 py-2 rounded-lg border text-sm cursor-pointer transition-colors ${active ? "border-primary bg-primary/10 text-foreground" : "border-border bg-background text-muted-foreground hover:border-primary/40"}`;
function PrintSettingsPanel({
  printWhat,
  onPrintWhatChange,
  orientation,
  onOrientationChange,
  colorMode,
  onColorModeChange,
  frameSlides,
  onFrameSlidesChange,
  slidesPerPage,
  onSlidesPerPageChange,
  slideRange,
  onSlideRangeChange,
  customFrom,
  onCustomFromChange,
  customTo,
  onCustomToChange,
  totalSlides,
  activeSlideIndex
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 space-y-5 min-w-0", children: [
    (0, import_jsx_runtime.jsxs)("fieldset", { children: [
      (0, import_jsx_runtime.jsx)("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.printWhat") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-2 gap-2", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(printWhat === "slides"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "slides",
              onChange: () => onPrintWhatChange("slides")
            }
          ),
          (0, import_jsx_runtime.jsx)(LuFileText, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.fullPageSlides")
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(printWhat === "handouts"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "handouts",
              onChange: () => onPrintWhatChange("handouts")
            }
          ),
          (0, import_jsx_runtime.jsx)(LuGrid2X2, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.handouts")
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(printWhat === "notes"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "notes",
              onChange: () => onPrintWhatChange("notes")
            }
          ),
          (0, import_jsx_runtime.jsx)(LuStickyNote, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.notesPages")
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(printWhat === "outline"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "printWhat",
              className: "sr-only",
              checked: printWhat === "outline",
              onChange: () => onPrintWhatChange("outline")
            }
          ),
          (0, import_jsx_runtime.jsx)(LuList, { className: "w-4 h-4 shrink-0" }),
          t("pptx.print.outline")
        ] })
      ] })
    ] }),
    printWhat === "handouts" && (0, import_jsx_runtime.jsxs)("fieldset", { children: [
      (0, import_jsx_runtime.jsx)("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.slidesPerPage") }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex gap-1.5 flex-wrap", children: HANDOUT_OPTIONS.map((n) => (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: () => onSlidesPerPageChange(n),
          className: `px-3 py-1.5 rounded-md text-sm font-medium border transition-colors ${slidesPerPage === n ? "border-primary bg-primary/10 text-foreground" : "border-border bg-background text-muted-foreground hover:border-primary/40"}`,
          children: n
        },
        n
      )) })
    ] }),
    (0, import_jsx_runtime.jsxs)("fieldset", { children: [
      (0, import_jsx_runtime.jsx)("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.slideRange") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "space-y-2", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(slideRange === "all"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "slideRange",
              className: "sr-only",
              checked: slideRange === "all",
              onChange: () => onSlideRangeChange("all")
            }
          ),
          t("pptx.print.allSlides"),
          " (",
          totalSlides,
          ")"
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(slideRange === "current"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "slideRange",
              className: "sr-only",
              checked: slideRange === "current",
              onChange: () => onSlideRangeChange("current")
            }
          ),
          t("pptx.print.currentSlide"),
          " (",
          activeSlideIndex + 1,
          ")"
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(slideRange === "custom"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "slideRange",
              className: "sr-only",
              checked: slideRange === "custom",
              onChange: () => onSlideRangeChange("custom")
            }
          ),
          t("pptx.print.customRange")
        ] }),
        slideRange === "custom" && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 pl-6", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: t("pptx.print.from") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 1,
              max: totalSlides,
              value: customFrom,
              onChange: (e) => onCustomFromChange(Math.max(1, parseInt(e.target.value, 10) || 1)),
              className: "w-16 px-2 py-1 text-sm border border-border rounded bg-background text-foreground"
            }
          ),
          (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: t("pptx.print.to") }),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "number",
              min: 1,
              max: totalSlides,
              value: customTo,
              onChange: (e) => onCustomToChange(Math.max(1, parseInt(e.target.value, 10) || 1)),
              className: "w-16 px-2 py-1 text-sm border border-border rounded bg-background text-foreground"
            }
          )
        ] })
      ] })
    ] }),
    printWhat === "slides" && (0, import_jsx_runtime.jsxs)("fieldset", { children: [
      (0, import_jsx_runtime.jsx)("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.orientation") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-2", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(orientation === "landscape"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "orientation",
              className: "sr-only",
              checked: orientation === "landscape",
              onChange: () => onOrientationChange("landscape")
            }
          ),
          t("pptx.print.landscape")
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(orientation === "portrait"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "orientation",
              className: "sr-only",
              checked: orientation === "portrait",
              onChange: () => onOrientationChange("portrait")
            }
          ),
          t("pptx.print.portrait")
        ] })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("fieldset", { children: [
      (0, import_jsx_runtime.jsx)("legend", { className: "text-xs font-medium text-muted-foreground mb-2 uppercase tracking-wide", children: t("pptx.print.colorMode") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-2 flex-wrap", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(colorMode === "color"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "colorMode",
              className: "sr-only",
              checked: colorMode === "color",
              onChange: () => onColorModeChange("color")
            }
          ),
          t("pptx.print.color")
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(colorMode === "grayscale"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "colorMode",
              className: "sr-only",
              checked: colorMode === "grayscale",
              onChange: () => onColorModeChange("grayscale")
            }
          ),
          t("pptx.print.grayscale")
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: radioClass(colorMode === "blackAndWhite"), children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "radio",
              name: "colorMode",
              className: "sr-only",
              checked: colorMode === "blackAndWhite",
              onChange: () => onColorModeChange("blackAndWhite")
            }
          ),
          t("pptx.print.blackAndWhite")
        ] })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: frameSlides,
          onChange: (e) => onFrameSlidesChange(e.target.checked),
          className: "rounded border-border"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "text-sm text-foreground", children: t("pptx.print.frameSlides") })
    ] })
  ] });
}
function PrintDialog({
  open,
  onClose,
  onPrint,
  slides,
  activeSlideIndex,
  defaultSlidesPerPage,
  defaultFrameSlides
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const printOptionDefaults = resolveDefaultPrintSettings(useViewerOptionsContext());
  const [printWhat, setPrintWhat] = (0, import_react2.useState)((printOptionDefaults == null ? void 0 : printOptionDefaults.printWhat) ?? "slides");
  const [orientation, setOrientation] = (0, import_react2.useState)("landscape");
  const [colorMode, setColorMode] = (0, import_react2.useState)(
    (printOptionDefaults == null ? void 0 : printOptionDefaults.colorMode) ?? "color"
  );
  const [frameSlides, setFrameSlides] = (0, import_react2.useState)(
    (printOptionDefaults == null ? void 0 : printOptionDefaults.frameSlides) ?? defaultFrameSlides ?? false
  );
  const [slidesPerPage, setSlidesPerPage] = (0, import_react2.useState)(
    HANDOUT_OPTIONS.includes(defaultSlidesPerPage) ? defaultSlidesPerPage : 6
  );
  const [slideRange, setSlideRange] = (0, import_react2.useState)("all");
  const [customFrom, setCustomFrom] = (0, import_react2.useState)(1);
  const [customTo, setCustomTo] = (0, import_react2.useState)(slides.length);
  const effectiveOrientation = (0, import_react2.useMemo)(() => {
    if (printWhat === "notes" || printWhat === "outline") {
      return "portrait";
    }
    if (printWhat === "handouts") {
      return "portrait";
    }
    return orientation;
  }, [printWhat, orientation]);
  const slideCount = (0, import_react2.useMemo)(() => {
    if (slideRange === "all") {
      return slides.length;
    }
    if (slideRange === "current") {
      return 1;
    }
    const from = Math.max(1, Math.min(customFrom, slides.length));
    const to = Math.max(from, Math.min(customTo, slides.length));
    return to - from + 1;
  }, [slideRange, slides.length, customFrom, customTo]);
  const pageCount = (0, import_react2.useMemo)(() => {
    if (printWhat === "slides") {
      return slideCount;
    }
    if (printWhat === "notes") {
      return slideCount;
    }
    if (printWhat === "outline") {
      return 1;
    }
    return Math.ceil(slideCount / slidesPerPage);
  }, [printWhat, slideCount, slidesPerPage]);
  const previewSlideIndices = (0, import_react2.useMemo)(() => {
    if (slideRange === "current") {
      return [activeSlideIndex];
    }
    if (slideRange === "custom") {
      const from = Math.max(0, customFrom - 1);
      const to = Math.min(slides.length - 1, customTo - 1);
      return Array.from({ length: Math.max(0, to - from + 1) }, (_, i) => from + i);
    }
    return Array.from({ length: slides.length }, (_, i) => i);
  }, [slideRange, activeSlideIndex, customFrom, customTo, slides.length]);
  const handlePrint = (0, import_react2.useCallback)(() => {
    onPrint({
      printWhat,
      orientation: effectiveOrientation,
      colorMode,
      frameSlides,
      slidesPerPage,
      slideRange,
      customRangeFrom: Math.max(1, Math.min(customFrom, slides.length)),
      customRangeTo: Math.max(1, Math.min(customTo, slides.length)),
      // Options > Advanced > "Print scale to fit"; no dialog control of its
      // own (PowerPoint keeps this an Options default, not a per-job choice).
      scaleToFit: (printOptionDefaults == null ? void 0 : printOptionDefaults.scaleToFit) ?? true
    });
  }, [
    printWhat,
    effectiveOrientation,
    colorMode,
    frameSlides,
    slidesPerPage,
    slideRange,
    customFrom,
    customTo,
    slides.length,
    printOptionDefaults == null ? void 0 : printOptionDefaults.scaleToFit,
    onPrint
  ]);
  if (!open) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/60 backdrop-blur-sm",
      children: (0, import_jsx_runtime.jsxs)(
        "div",
        {
          style: panelStyle,
          className: "w-[780px] max-h-[90vh] rounded-xl border border-border bg-background shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
          children: [
            (0, import_jsx_runtime.jsxs)(
              "div",
              {
                ...dragHandlers,
                className: "flex items-center justify-between px-5 py-4 border-b border-border touch-none",
                children: [
                  (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
                    (0, import_jsx_runtime.jsx)(LuPrinter, { className: "w-4 h-4 text-muted-foreground" }),
                    (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.print.title") })
                  ] }),
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: onClose,
                      className: "p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors",
                      children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
                    }
                  )
                ]
              }
            ),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 overflow-y-auto px-5 py-4 flex gap-5", children: [
              (0, import_jsx_runtime.jsx)(
                PrintSettingsPanel,
                {
                  printWhat,
                  onPrintWhatChange: setPrintWhat,
                  orientation,
                  onOrientationChange: setOrientation,
                  colorMode,
                  onColorModeChange: setColorMode,
                  frameSlides,
                  onFrameSlidesChange: setFrameSlides,
                  slidesPerPage,
                  onSlidesPerPageChange: setSlidesPerPage,
                  slideRange,
                  onSlideRangeChange: setSlideRange,
                  customFrom,
                  onCustomFromChange: setCustomFrom,
                  customTo,
                  onCustomToChange: setCustomTo,
                  totalSlides: slides.length,
                  activeSlideIndex
                }
              ),
              printWhat === "handouts" && (0, import_jsx_runtime.jsx)("div", { className: "w-[230px] shrink-0 border-l border-border pl-4 overflow-y-auto", children: (0, import_jsx_runtime.jsx)(
                PrintPreview,
                {
                  slideIndices: previewSlideIndices,
                  slidesPerPage,
                  orientation: effectiveOrientation,
                  frameSlides
                }
              ) }),
              printWhat === "notes" && (0, import_jsx_runtime.jsx)("div", { className: "w-[230px] shrink-0 border-l border-border pl-4 overflow-y-auto", children: (0, import_jsx_runtime.jsx)(
                NotesPagePreview,
                {
                  slideIndices: previewSlideIndices,
                  slides,
                  frameSlides
                }
              ) })
            ] }),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-3 border-t border-border", children: [
              (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: t("pptx.print.pageEstimate", {
                pages: pageCount,
                slides: slideCount
              }) }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-2", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-4 py-2 text-sm rounded-lg border border-border text-muted-foreground hover:text-foreground hover:bg-accent transition-colors",
                    children: t("pptx.common.cancel")
                  }
                ),
                (0, import_jsx_runtime.jsxs)(
                  "button",
                  {
                    type: "button",
                    onClick: handlePrint,
                    className: "px-4 py-2 text-sm rounded-lg bg-primary text-white hover:bg-primary/90 transition-colors flex items-center gap-1.5",
                    children: [
                      (0, import_jsx_runtime.jsx)(LuPrinter, { className: "w-3.5 h-3.5" }),
                      t("pptx.print.printButton")
                    ]
                  }
                )
              ] })
            ] })
          ]
        }
      )
    }
  );
}
function ShowOptionsFieldset({
  draft,
  update
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("fieldset", { className: "space-y-1.5", children: [
    (0, import_jsx_runtime.jsx)("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.showOptions") }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: Boolean(draft.loopContinuously),
          onChange: (e) => update({
            loopContinuously: e.target.checked
          }),
          className: "accent-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.loopContinuously") })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: draft.showWithNarration === false,
          onChange: (e) => update({
            showWithNarration: !e.target.checked
          }),
          className: "accent-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.showWithoutNarration") })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: draft.showWithAnimation === false,
          onChange: (e) => update({
            showWithAnimation: !e.target.checked
          }),
          className: "accent-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.showWithoutAnimation") })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: Boolean(draft.showSubtitles),
          onChange: (e) => update({ showSubtitles: e.target.checked }),
          className: "accent-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.showSubtitles") })
    ] })
  ] });
}
function ShowSlidesFieldset({
  draft,
  update,
  showSlidesMode,
  slideCount,
  customShows
}) {
  var _a;
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("fieldset", { className: "space-y-1.5", children: [
    (0, import_jsx_runtime.jsx)("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.showSlides") }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "radio",
          name: "showSlides",
          value: "all",
          checked: showSlidesMode === "all",
          onChange: () => update({ showSlidesMode: "all" }),
          className: "accent-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.allSlides") })
    ] }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "radio",
          name: "showSlides",
          value: "range",
          checked: showSlidesMode === "range",
          onChange: () => update({
            showSlidesMode: "range",
            showSlidesFrom: draft.showSlidesFrom ?? 1,
            showSlidesTo: draft.showSlidesTo ?? slideCount
          }),
          className: "accent-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.fromTo") })
    ] }),
    showSlidesMode === "range" && (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 ml-6", children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.slideShow.from") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 1,
            max: slideCount,
            value: draft.showSlidesFrom ?? 1,
            onChange: (e) => update({
              showSlidesFrom: Math.max(1, Number.parseInt(e.target.value, 10) || 1)
            }),
            className: "w-14 px-1.5 py-0.5 rounded bg-muted border border-border text-foreground text-[11px]"
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: t("pptx.slideShow.to") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            min: 1,
            max: slideCount,
            value: draft.showSlidesTo ?? slideCount,
            onChange: (e) => update({
              showSlidesTo: Math.min(
                slideCount,
                Number.parseInt(e.target.value, 10) || slideCount
              )
            }),
            className: "w-14 px-1.5 py-0.5 rounded bg-muted border border-border text-foreground text-[11px]"
          }
        )
      ] })
    ] }),
    customShows.length > 0 && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "radio",
            name: "showSlides",
            value: "customShow",
            checked: showSlidesMode === "customShow",
            onChange: () => {
              var _a2;
              return update({
                showSlidesMode: "customShow",
                showSlidesCustomShowId: draft.showSlidesCustomShowId ?? ((_a2 = customShows[0]) == null ? void 0 : _a2.id)
              });
            },
            className: "accent-primary"
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.customShow") })
      ] }),
      showSlidesMode === "customShow" && (0, import_jsx_runtime.jsx)("div", { className: "ml-6", children: (0, import_jsx_runtime.jsx)(
        "select",
        {
          value: draft.showSlidesCustomShowId ?? ((_a = customShows[0]) == null ? void 0 : _a.id) ?? "",
          onChange: (e) => update({
            showSlidesCustomShowId: e.target.value
          }),
          className: "w-full px-2 py-1 rounded bg-muted border border-border text-foreground text-[11px]",
          children: customShows.map((cs) => (0, import_jsx_runtime.jsx)("option", { value: cs.id, children: cs.name }, cs.id))
        }
      ) })
    ] })
  ] });
}
function SetUpSlideShowDialog({
  open,
  onClose,
  properties,
  onSave,
  customShows,
  slideCount
}) {
  const { t } = useTranslation();
  const [draft, setDraft] = (0, import_react2.useState)({
    ...properties
  });
  const update = (0, import_react2.useCallback)((patch) => {
    setDraft((prev) => ({ ...prev, ...patch }));
  }, []);
  const handleSave = (0, import_react2.useCallback)(() => {
    onSave(draft);
    onClose();
  }, [draft, onSave, onClose]);
  if (!open) {
    return null;
  }
  const showType = draft.showType ?? "presented";
  const showSlidesMode = draft.showSlidesMode ?? "all";
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        "aria-label": t("pptx.share.closeDialog"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)("div", { className: "pointer-events-auto flex flex-col w-[440px] max-h-[90vh] rounded-xl border border-border bg-background shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-5 py-3 border-b border-border shrink-0", children: [
            (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.slideShow.setUpTitle") }),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "text-muted-foreground hover:text-foreground text-lg leading-none",
                "aria-label": t("pptx.common.close"),
                children: "×"
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 px-5 py-4 space-y-5 text-[12px] text-foreground overflow-y-auto", children: [
            (0, import_jsx_runtime.jsxs)("fieldset", { className: "space-y-1.5", children: [
              (0, import_jsx_runtime.jsx)("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.showType") }),
              [
                ["presented", t("pptx.slideShow.presentedBySpeaker")],
                ["browsed", t("pptx.slideShow.browsedByIndividual")],
                ["kiosk", t("pptx.slideShow.browsedAtKiosk")]
              ].map(([value, label]) => (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
                (0, import_jsx_runtime.jsx)(
                  "input",
                  {
                    type: "radio",
                    name: "showType",
                    value,
                    checked: showType === value,
                    onChange: () => update({
                      showType: value,
                      ...value === "kiosk" ? { loopContinuously: true } : {}
                    }),
                    className: "accent-primary"
                  }
                ),
                (0, import_jsx_runtime.jsx)("span", { children: label })
              ] }, value))
            ] }),
            (0, import_jsx_runtime.jsx)(
              ShowSlidesFieldset,
              {
                draft,
                update,
                showSlidesMode,
                slideCount,
                customShows
              }
            ),
            (0, import_jsx_runtime.jsxs)("fieldset", { className: "space-y-1.5", children: [
              (0, import_jsx_runtime.jsx)("legend", { className: "text-[11px] font-medium text-muted-foreground uppercase tracking-wide mb-1", children: t("pptx.slideShow.advanceSlides") }),
              (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
                (0, import_jsx_runtime.jsx)(
                  "input",
                  {
                    type: "radio",
                    name: "advanceMode",
                    value: "manual",
                    checked: draft.advanceMode === "manual",
                    onChange: () => update({ advanceMode: "manual" }),
                    className: "accent-primary"
                  }
                ),
                (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.manually") })
              ] }),
              (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 cursor-pointer", children: [
                (0, import_jsx_runtime.jsx)(
                  "input",
                  {
                    type: "radio",
                    name: "advanceMode",
                    value: "useTimings",
                    checked: (draft.advanceMode ?? "useTimings") === "useTimings",
                    onChange: () => update({ advanceMode: "useTimings" }),
                    className: "accent-primary"
                  }
                ),
                (0, import_jsx_runtime.jsx)("span", { children: t("pptx.slideShow.useTimings") })
              ] })
            ] }),
            (0, import_jsx_runtime.jsx)(ShowOptionsFieldset, { draft, update })
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 px-5 py-3 border-t border-border shrink-0", children: [
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onClose,
                className: "px-3 py-1.5 rounded bg-muted hover:bg-accent text-[12px] text-foreground transition-colors",
                children: t("pptx.common.cancel")
              }
            ),
            (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: handleSave,
                className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/80 text-[12px] text-white transition-colors",
                children: t("pptx.common.ok")
              }
            )
          ] })
        ] })
      }
    )
  ] });
}
function BroadcastDialog({
  open,
  onClose,
  onStartBroadcast,
  onStopBroadcast,
  onStartPresenting,
  defaultRoomId,
  defaultUserName,
  defaultServerUrl
}) {
  const { t } = useTranslation();
  const collab = useCollaboration();
  const isBroadcasting = collab !== null && collab.status !== "disconnected" && collab.status !== "error" && collab.config.role === "owner";
  const [roomId, setRoomId] = (0, import_react2.useState)("");
  const [userName, setUserName] = (0, import_react2.useState)("");
  const [serverUrl, setServerUrl] = (0, import_react2.useState)("");
  const [copied, setCopied] = (0, import_react2.useState)(false);
  const dialogRef = (0, import_react2.useRef)(null);
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  useModalFocus(open, dialogRef, onClose);
  (0, import_react2.useEffect)(() => {
    if (open && !isBroadcasting) {
      const broadcastRoom = defaultRoomId ? `broadcast-${defaultRoomId}` : generateBroadcastRoomId();
      setRoomId(broadcastRoom);
      setUserName(defaultUserName ?? "");
      setServerUrl(defaultServerUrl ?? DEFAULT_BROADCAST_SERVER_URL);
    }
  }, [open, isBroadcasting, defaultRoomId, defaultUserName, defaultServerUrl]);
  const activeRoomId = isBroadcasting ? collab.config.roomId : roomId;
  const activeServerUrl = isBroadcasting ? collab.config.serverUrl : serverUrl;
  const broadcastUrl = typeof window !== "undefined" ? buildBroadcastViewerUrl(activeRoomId, activeServerUrl, window.location) : activeRoomId;
  const handleCopyUrl = (0, import_react2.useCallback)(() => {
    void navigator.clipboard.writeText(broadcastUrl).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 2e3);
      return void 0;
    });
  }, [broadcastUrl]);
  const handleStartBroadcast = (0, import_react2.useCallback)(() => {
    if (!roomId.trim() || !userName.trim()) {
      return;
    }
    const trimmedServer = serverUrl.trim();
    onStartBroadcast == null ? void 0 : onStartBroadcast({
      roomId: roomId.trim(),
      serverUrl: trimmedServer,
      userName: userName.trim(),
      role: "owner",
      // A blank server broadcasts over the serverless peer-to-peer transport.
      transport: resolveTransportForServerUrl(trimmedServer)
    });
    setTimeout(() => {
      onStartPresenting == null ? void 0 : onStartPresenting();
    }, 100);
    onClose();
  }, [roomId, userName, serverUrl, onStartBroadcast, onStartPresenting, onClose]);
  const handleStopBroadcast = (0, import_react2.useCallback)(() => {
    onStopBroadcast == null ? void 0 : onStopBroadcast();
    onClose();
  }, [onStopBroadcast, onClose]);
  const canStart = roomId.trim().length > 0 && userName.trim().length > 0;
  if (!open) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        "aria-label": t("pptx.share.closeDialog"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            ref: dialogRef,
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.broadcast.title"),
            tabIndex: -1,
            style: panelStyle,
            className: "pointer-events-auto w-full max-w-md rounded-xl border border-border bg-popover text-foreground shadow-2xl outline-none max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:max-w-none max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-5 py-3 border-b border-border touch-none",
                  children: [
                    (0, import_jsx_runtime.jsxs)("h2", { className: "text-sm font-semibold text-foreground flex items-center gap-2", children: [
                      (0, import_jsx_runtime.jsx)(LuCast, { className: "w-4 h-4" }),
                      isBroadcasting ? t("pptx.broadcast.broadcasting") : t("pptx.broadcast.title")
                    ] }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "text-muted-foreground hover:text-foreground text-lg leading-none",
                        "aria-label": t("pptx.common.close"),
                        children: "×"
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsx)("div", { className: "px-5 py-4", children: isBroadcasting ? (0, import_jsx_runtime.jsx)(
                ActiveBroadcastView,
                {
                  collab,
                  broadcastUrl,
                  copied,
                  onCopyUrl: handleCopyUrl,
                  onStopBroadcast: handleStopBroadcast
                }
              ) : (0, import_jsx_runtime.jsx)(
                StartBroadcastForm,
                {
                  roomId,
                  userName,
                  serverUrl,
                  onRoomIdChange: setRoomId,
                  onUserNameChange: setUserName,
                  onServerUrlChange: setServerUrl
                }
              ) }),
              !isBroadcasting && (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 px-5 py-3 border-t border-border", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 rounded bg-muted hover:bg-accent text-[12px] text-foreground transition-colors",
                    children: t("pptx.common.close")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    disabled: !canStart,
                    onClick: handleStartBroadcast,
                    className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/90 text-[12px] text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
                    children: t("pptx.broadcast.startBroadcast")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function StartBroadcastForm({
  roomId,
  userName,
  serverUrl,
  onRoomIdChange,
  onUserNameChange,
  onServerUrlChange
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
    (0, import_jsx_runtime.jsx)("p", { className: "text-[13px] text-muted-foreground leading-relaxed", children: t("pptx.broadcast.description") }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)(
        "label",
        {
          htmlFor: "broadcast-room-id",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.broadcast.sessionName")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "broadcast-room-id",
          type: "text",
          value: roomId,
          onChange: (e) => onRoomIdChange(e.target.value),
          placeholder: "broadcast-abc123",
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)(
        "label",
        {
          htmlFor: "broadcast-user-name",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.broadcast.displayName")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "broadcast-user-name",
          type: "text",
          value: userName,
          onChange: (e) => onUserNameChange(e.target.value),
          placeholder: t("pptx.broadcast.presenterPlaceholder"),
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)(
        "label",
        {
          htmlFor: "broadcast-server-url",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.broadcast.serverLabel")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "broadcast-server-url",
          type: "text",
          value: serverUrl,
          onChange: (e) => onServerUrlChange(e.target.value),
          placeholder: "ws://localhost:1234",
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground/70 leading-relaxed", children: serverUrl.trim().length === 0 ? t("pptx.broadcast.p2pHint") : t("pptx.broadcast.hint") })
  ] });
}
function ActiveBroadcastView({
  collab,
  broadcastUrl,
  copied,
  onCopyUrl,
  onStopBroadcast
}) {
  const { t } = useTranslation();
  const statusColor = collab.status === "connected" ? "text-green-400" : collab.status === "connecting" ? "text-yellow-400" : "text-red-400";
  const statusIcon = collab.status === "connected" || collab.status === "connecting" ? (0, import_jsx_runtime.jsx)(LuWifi, { className: "w-4 h-4" }) : (0, import_jsx_runtime.jsx)(LuWifiOff, { className: "w-4 h-4" });
  const viewerCount = collab.remoteUsers.filter((u) => u.role === "viewer").length;
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
      (0, import_jsx_runtime.jsx)("span", { className: statusColor, children: statusIcon }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[13px] font-medium text-foreground capitalize", children: collab.status }),
      (0, import_jsx_runtime.jsxs)("span", { className: "text-[12px] text-muted-foreground ml-auto flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)(LuUsers, { className: "w-3.5 h-3.5" }),
        t("pptx.broadcast.viewerCount", { count: viewerCount })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.broadcast.viewerLink") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "flex-1 px-3 py-1.5 rounded border border-border bg-background text-[11px] text-foreground select-all font-mono truncate", children: broadcastUrl }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onCopyUrl,
            className: "flex items-center gap-1 px-2.5 py-1.5 rounded border border-border bg-muted hover:bg-accent text-[12px] text-foreground transition-colors shrink-0",
            title: t("pptx.broadcast.copyLink"),
            children: copied ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3.5 h-3.5 text-green-400" }),
              (0, import_jsx_runtime.jsx)("span", { children: t("pptx.share.copied") })
            ] }) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              (0, import_jsx_runtime.jsx)(LuCopy, { className: "w-3.5 h-3.5" }),
              (0, import_jsx_runtime.jsx)("span", { children: t("pptx.share.copyUrl") })
            ] })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.broadcast.shareHint") })
    ] }),
    collab.remoteUsers.length > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.broadcast.viewers") }),
      (0, import_jsx_runtime.jsx)("div", { className: "rounded border border-border bg-background divide-y divide-border max-h-[120px] overflow-y-auto", children: collab.remoteUsers.map((user) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 px-3 py-2", children: [
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "w-5 h-5 rounded-full flex items-center justify-center text-[8px] font-semibold text-white shrink-0",
            style: { backgroundColor: user.userColor },
            children: user.userName.slice(0, 2).toUpperCase()
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[12px] text-foreground truncate", children: user.userName }),
        (0, import_jsx_runtime.jsxs)("span", { className: "text-[10px] text-muted-foreground ml-auto", children: [
          "Slide ",
          user.activeSlideIndex + 1
        ] })
      ] }, user.clientId)) })
    ] }),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onStopBroadcast,
        className: "w-full px-3 py-2 rounded border border-red-500/30 bg-red-500/10 hover:bg-red-500/20 text-[12px] text-red-400 font-medium transition-colors",
        children: t("pptx.broadcast.stopBroadcast")
      }
    )
  ] });
}
function isP2P(config) {
  var _a;
  return config.transport === "webrtc" || !((_a = config.serverUrl) == null ? void 0 : _a.trim());
}
function shareUrl(config) {
  return buildCollaborationShareUrl(
    config,
    typeof window === "undefined" ? void 0 : { origin: window.location.origin, pathname: window.location.pathname }
  );
}
function ActiveSessionView({
  collab,
  activeCollaboration,
  copied,
  onCopyRoomId,
  onStopCollaboration
}) {
  const { t } = useTranslation(), viewerOptions = useViewerOptionsContext(), statusColor = collab.status === "connected" ? "text-green-400" : collab.status === "connecting" ? "text-yellow-400" : "text-red-400", statusIcon = collab.status === "connected" || collab.status === "connecting" ? (0, import_jsx_runtime.jsx)(LuWifi, { className: "w-4 h-4" }) : (0, import_jsx_runtime.jsx)(LuWifiOff, { className: "w-4 h-4" }), sessionUsers = buildActiveSessionUsers({
    localUserName: (activeCollaboration == null ? void 0 : activeCollaboration.userName) ?? collab.config.userName,
    localUserInitials: viewerOptions.general.userInitials,
    localUserColor: collab.config.userColor,
    remoteUsers: collab.remoteUsers
  });
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
      (0, import_jsx_runtime.jsx)("span", { className: statusColor, children: statusIcon }),
      (0, import_jsx_runtime.jsx)("span", { className: "text-[13px] font-medium text-foreground capitalize", children: collab.status }),
      (0, import_jsx_runtime.jsxs)("span", { className: "text-[12px] text-muted-foreground ml-auto flex items-center gap-1", children: [
        (0, import_jsx_runtime.jsx)(LuUsers, { className: "w-3.5 h-3.5" }),
        t("pptx.collaboration.userCount", { count: collab.connectedCount })
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.shareLink") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "flex-1 px-3 py-1.5 rounded border border-border bg-background text-[11px] text-foreground select-all font-mono truncate", children: shareUrl(collab.config) }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onCopyRoomId,
            className: "flex items-center gap-1 px-2.5 py-1.5 rounded border border-border bg-muted hover:bg-accent text-[12px] text-foreground transition-colors shrink-0",
            title: t("pptx.share.copyLink"),
            children: copied ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3.5 h-3.5 text-green-400" }),
              (0, import_jsx_runtime.jsx)("span", { children: t("pptx.share.copied") })
            ] }) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              (0, import_jsx_runtime.jsx)(LuCopy, { className: "w-3.5 h-3.5" }),
              (0, import_jsx_runtime.jsx)("span", { children: t("pptx.share.copyUrl") })
            ] })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.share.shareHint") })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 text-[11px] text-muted-foreground", children: [
      (0, import_jsx_runtime.jsxs)("span", { children: [
        t("pptx.share.room"),
        " ",
        (0, import_jsx_runtime.jsx)("code", { className: "font-mono text-foreground", children: collab.config.roomId })
      ] }),
      (0, import_jsx_runtime.jsxs)("span", { children: [
        t("pptx.share.server"),
        " ",
        (0, import_jsx_runtime.jsx)("code", { className: "font-mono text-foreground", children: isP2P(collab.config) ? t("pptx.share.p2pServerValue") : collab.config.serverUrl })
      ] })
    ] }),
    collab.remoteUsers.length > 0 && (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.connectedUsers") }),
      (0, import_jsx_runtime.jsx)("div", { className: "rounded border border-border bg-background divide-y divide-border max-h-[140px] overflow-y-auto", children: sessionUsers.map((user) => (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 px-3 py-2", children: [
        (0, import_jsx_runtime.jsx)(
          "div",
          {
            className: "w-6 h-6 rounded-full flex items-center justify-center text-[9px] font-semibold text-white shrink-0",
            style: { backgroundColor: user.color },
            children: user.avatarUrl ? (0, import_jsx_runtime.jsx)(
              "img",
              {
                src: user.avatarUrl,
                alt: "",
                className: "w-full h-full rounded-full object-cover"
              }
            ) : user.initials
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[12px] text-foreground truncate", children: user.name }),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground ml-auto", children: user.isLocal ? t("pptx.share.you") : t("pptx.notes.slideN", { n: user.slideNumber }) })
      ] }, user.id)) })
    ] }),
    onStopCollaboration && (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onStopCollaboration,
        className: "w-full px-3 py-2 rounded border border-red-500/30 bg-red-500/10 hover:bg-red-500/20 text-[12px] text-red-400 font-medium transition-colors",
        children: t("pptx.share.stopSharing")
      }
    )
  ] });
}
function StartSessionForm({
  mode,
  invitation,
  roomId,
  userName,
  serverUrl,
  onRoomIdChange,
  onUserNameChange,
  onServerUrlChange,
  onModeChange,
  onInvitationChange,
  preconfigured,
  connectionFailed,
  onRetry
}) {
  const { t } = useTranslation();
  const inputReadOnlyClass = preconfigured ? " opacity-70 cursor-not-allowed" : "";
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
    (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-1 rounded-lg bg-muted p-1", role: "tablist", children: ["create", "join"].map((candidate) => (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        role: "tab",
        "aria-selected": mode === candidate,
        onClick: () => onModeChange(candidate),
        className: `rounded-md px-3 py-1.5 text-[12px] font-medium transition-colors ${mode === candidate ? "bg-background text-foreground shadow-sm" : "text-muted-foreground hover:text-foreground"}`,
        children: t(candidate === "create" ? "pptx.share.createSession" : "pptx.share.joinSession")
      },
      candidate
    )) }),
    (0, import_jsx_runtime.jsx)("p", { className: "text-[13px] text-muted-foreground leading-relaxed", children: mode === "join" ? t("pptx.share.joinDescription") : preconfigured ? t("pptx.share.preconfiguredDescription") : t("pptx.share.description") }),
    connectionFailed && (0, import_jsx_runtime.jsxs)(
      "div",
      {
        role: "alert",
        className: "flex items-start gap-2 rounded border border-red-500/30 bg-red-500/10 px-3 py-2 text-[12px] text-red-400",
        children: [
          (0, import_jsx_runtime.jsx)(LuWifiOff, { className: "w-4 h-4 mt-0.5 shrink-0" }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 space-y-1", children: [
            (0, import_jsx_runtime.jsx)("p", { className: "font-medium", children: t("pptx.share.connectionError") }),
            onRetry && (0, import_jsx_runtime.jsx)(
              "button",
              {
                type: "button",
                onClick: onRetry,
                className: "underline underline-offset-2 hover:no-underline",
                children: t("pptx.collaboration.retry")
              }
            )
          ] })
        ]
      }
    ),
    mode === "join" ? (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)(
        "label",
        {
          htmlFor: "share-invitation",
          className: "block text-[12px] font-medium text-foreground",
          children: t("pptx.share.invitationLabel")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "share-invitation",
          type: "text",
          value: invitation,
          onChange: (event) => onInvitationChange(event.target.value),
          placeholder: t("pptx.share.invitationPlaceholder"),
          className: "w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
        }
      ),
      (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.share.invitationHint") })
    ] }) : (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { htmlFor: "share-room-id", className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.sessionName") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "share-room-id",
          type: "text",
          "aria-label": t("pptx.share.sessionName"),
          value: roomId,
          onChange: (e) => onRoomIdChange(e.target.value),
          readOnly: preconfigured,
          placeholder: t("pptx.share.sessionPlaceholder"),
          className: `w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary${inputReadOnlyClass}`
        }
      ),
      (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: t("pptx.share.sessionHint") })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { htmlFor: "share-user-name", className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.displayName") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "share-user-name",
          type: "text",
          "aria-label": t("pptx.share.displayName"),
          value: userName,
          onChange: (e) => onUserNameChange(e.target.value),
          readOnly: preconfigured,
          placeholder: t("pptx.share.namePlaceholder"),
          className: `w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary${inputReadOnlyClass}`
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
      (0, import_jsx_runtime.jsx)("label", { htmlFor: "share-server-url", className: "block text-[12px] font-medium text-foreground", children: t("pptx.share.serverLabel") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          id: "share-server-url",
          type: "text",
          "aria-label": t("pptx.share.serverLabel"),
          value: serverUrl,
          onChange: (e) => onServerUrlChange(e.target.value),
          readOnly: preconfigured,
          placeholder: t("pptx.share.serverPlaceholder"),
          className: `w-full px-3 py-1.5 rounded border border-border bg-background text-foreground text-[13px] placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary${inputReadOnlyClass}`
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)("p", { className: "text-[11px] text-muted-foreground", children: serverUrl.trim().length === 0 ? t("pptx.share.p2pHint") : t("pptx.share.serverHint") })
  ] });
}
function shareUrl2(config) {
  return buildCollaborationShareUrl(
    config,
    typeof window === "undefined" ? void 0 : { origin: window.location.origin, pathname: window.location.pathname }
  );
}
function ShareDialog({
  open,
  onClose,
  activeCollaboration,
  onStartCollaboration,
  onStopCollaboration,
  preconfigured,
  defaultRoomId,
  defaultUserName,
  defaultServerUrl
}) {
  const collab = useCollaboration();
  const isActive = collab !== null && collab.status !== "disconnected" && collab.status !== "error";
  const { t } = useTranslation();
  const [roomId, setRoomId] = (0, import_react2.useState)(defaultRoomId ?? "");
  const [userName, setUserName] = (0, import_react2.useState)(defaultUserName ?? "");
  const [serverUrl, setServerUrl] = (0, import_react2.useState)(defaultServerUrl ?? "");
  const [mode, setMode] = (0, import_react2.useState)("create");
  const [invitation, setInvitation] = (0, import_react2.useState)("");
  const [copied, setCopied] = (0, import_react2.useState)(false);
  const dialogRef = (0, import_react2.useRef)(null);
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  useModalFocus(open, dialogRef, onClose);
  (0, import_react2.useEffect)(() => {
    if (activeCollaboration) {
      setRoomId(activeCollaboration.roomId);
      setUserName(activeCollaboration.userName);
      setServerUrl(activeCollaboration.serverUrl);
    }
  }, [activeCollaboration]);
  const handleCopyRoomId = (0, import_react2.useCallback)(() => {
    const config = activeCollaboration ?? { roomId, serverUrl };
    void navigator.clipboard.writeText(shareUrl2(config)).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 2e3);
      return void 0;
    });
  }, [activeCollaboration, roomId, serverUrl]);
  const handleStartSharing = (0, import_react2.useCallback)(() => {
    const config = mode === "join" ? buildJoinCollaborationConfig({ invitation, userName, serverUrl }) : buildCreateCollaborationConfig({ roomId, userName, serverUrl });
    if (config) {
      onStartCollaboration == null ? void 0 : onStartCollaboration(config);
    }
  }, [invitation, mode, roomId, userName, serverUrl, onStartCollaboration]);
  const canStart = (mode === "join" ? buildJoinCollaborationConfig({ invitation, userName, serverUrl }) : buildCreateCollaborationConfig({ roomId, userName, serverUrl })) !== null;
  if (!open) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        "aria-label": t("pptx.share.closeDialog"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            ref: dialogRef,
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.share.title"),
            tabIndex: -1,
            style: panelStyle,
            className: "pointer-events-auto w-full max-w-md rounded-xl border border-border bg-popover text-foreground shadow-2xl outline-none max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:max-w-none max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-5 py-3 border-b border-border touch-none",
                  children: [
                    (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: isActive ? t("pptx.share.collaborationActive") : t("pptx.share.title") }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "text-muted-foreground hover:text-foreground text-lg leading-none",
                        "aria-label": t("pptx.share.close"),
                        children: "×"
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsx)("div", { className: "px-5 py-4", children: isActive ? (0, import_jsx_runtime.jsx)(
                ActiveSessionView,
                {
                  collab,
                  activeCollaboration,
                  copied,
                  onCopyRoomId: handleCopyRoomId,
                  onStopCollaboration
                }
              ) : (0, import_jsx_runtime.jsx)(
                StartSessionForm,
                {
                  mode,
                  invitation,
                  roomId,
                  userName,
                  serverUrl,
                  onRoomIdChange: setRoomId,
                  onUserNameChange: setUserName,
                  onServerUrlChange: setServerUrl,
                  onModeChange: setMode,
                  onInvitationChange: setInvitation,
                  preconfigured,
                  connectionFailed: (collab == null ? void 0 : collab.status) === "error",
                  onRetry: collab == null ? void 0 : collab.retry
                }
              ) }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 px-5 py-3 border-t border-border", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 rounded bg-muted hover:bg-accent text-[12px] text-foreground transition-colors",
                    children: isActive ? t("pptx.share.close") : t("pptx.share.cancel")
                  }
                ),
                !isActive && (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    disabled: !canStart,
                    onClick: handleStartSharing,
                    className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/90 text-[12px] text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
                    children: t(mode === "join" ? "pptx.share.joinSession" : "pptx.share.startSharing")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function HyperlinkTabContent({
  targetType,
  url,
  setUrl,
  tooltip,
  setTooltip,
  emailAddress,
  setEmailAddress,
  emailSubject,
  setEmailSubject,
  slideNumber,
  setSlideNumber,
  slideCount,
  filePath,
  setFilePath,
  actionVerb,
  setActionVerb,
  inputRef,
  inputCls,
  onEnterConfirm
}) {
  const { t } = useTranslation();
  const handleEnterKey = (e) => {
    if (e.key === "Enter") {
      onEnterConfirm();
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "px-4 py-3 space-y-3", children: [
    targetType === "url" && (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.urlLabel") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          ref: inputRef,
          type: "url",
          className: inputCls,
          placeholder: "https://example.com",
          value: url,
          onChange: (e) => setUrl(e.target.value),
          onKeyDown: handleEnterKey
        }
      )
    ] }),
    targetType === "email" && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsxs)("div", { children: [
        (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.emailLabel") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            ref: inputRef,
            type: "email",
            className: inputCls,
            placeholder: "user@example.com",
            value: emailAddress,
            onChange: (e) => setEmailAddress(e.target.value),
            onKeyDown: handleEnterKey
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)("div", { children: [
        (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.subjectLabel") }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            className: inputCls,
            value: emailSubject,
            onChange: (e) => setEmailSubject(e.target.value)
          }
        )
      ] })
    ] }),
    targetType === "slide" && (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.slideLabel") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          ref: inputRef,
          type: "number",
          min: 1,
          max: slideCount,
          className: inputCls,
          value: slideNumber,
          onChange: (e) => setSlideNumber(parseInt(e.target.value, 10) || 1),
          onKeyDown: handleEnterKey
        }
      ),
      (0, import_jsx_runtime.jsx)("p", { className: "text-[10px] text-muted-foreground mt-1", children: t("pptx.hyperlink.slideRange", { max: slideCount }) })
    ] }),
    targetType === "file" && (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.fileLabel") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          ref: inputRef,
          type: "text",
          className: inputCls,
          placeholder: "../documents/file.docx",
          value: filePath,
          onChange: (e) => setFilePath(e.target.value),
          onKeyDown: handleEnterKey
        }
      )
    ] }),
    targetType === "action" && (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.actionLabel") }),
      (0, import_jsx_runtime.jsxs)(
        "select",
        {
          className: inputCls,
          value: actionVerb,
          onChange: (e) => setActionVerb(e.target.value),
          children: [
            (0, import_jsx_runtime.jsx)("option", { value: "nextSlide", children: t("pptx.hyperlink.actionNextSlide") }),
            (0, import_jsx_runtime.jsx)("option", { value: "prevSlide", children: t("pptx.hyperlink.actionPrevSlide") }),
            (0, import_jsx_runtime.jsx)("option", { value: "firstSlide", children: t("pptx.hyperlink.actionFirstSlide") }),
            (0, import_jsx_runtime.jsx)("option", { value: "lastSlide", children: t("pptx.hyperlink.actionLastSlide") }),
            (0, import_jsx_runtime.jsx)("option", { value: "endShow", children: t("pptx.hyperlink.actionEndShow") })
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { children: [
      (0, import_jsx_runtime.jsx)("label", { className: "block text-xs text-muted-foreground mb-1", children: t("pptx.hyperlink.tooltipLabel") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          className: inputCls,
          value: tooltip,
          onChange: (e) => setTooltip(e.target.value)
        }
      )
    ] })
  ] });
}
function HyperlinkEditDialog({
  open,
  initialUrl,
  initialTooltip,
  initialAction,
  slideCount,
  onConfirm,
  onCancel
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onCancel);
  const inputRef = (0, import_react2.useRef)(null);
  const detectedType = detectTargetType(initialUrl, initialAction);
  const emailParts = parseEmailUrl(initialUrl || "");
  const detectedSlide = parseSlideFromUrl(initialUrl, initialAction);
  const detectedVerb = initialAction ? ACTION_VERB_MAP[initialAction.toLowerCase()] || "nextSlide" : "nextSlide";
  const [targetType, setTargetType] = (0, import_react2.useState)(detectedType);
  const [url, setUrl] = (0, import_react2.useState)(
    detectedType === "email" || detectedType === "slide" || detectedType === "action" ? "" : initialUrl || ""
  );
  const [tooltip, setTooltip] = (0, import_react2.useState)(initialTooltip || "");
  const [emailAddress, setEmailAddress] = (0, import_react2.useState)(emailParts.address);
  const [emailSubject, setEmailSubject] = (0, import_react2.useState)(emailParts.subject);
  const [slideNumber, setSlideNumber] = (0, import_react2.useState)(detectedSlide);
  const [filePath, setFilePath] = (0, import_react2.useState)(detectedType === "file" ? initialUrl || "" : "");
  const [actionVerb, setActionVerb] = (0, import_react2.useState)(detectedVerb);
  (0, import_react2.useEffect)(() => {
    if (open) {
      setTimeout(() => {
        var _a;
        return (_a = inputRef.current) == null ? void 0 : _a.focus();
      }, 50);
    }
  }, [open]);
  if (!open) {
    return null;
  }
  const handleConfirm = () => {
    onConfirm({
      targetType,
      url,
      tooltip,
      emailAddress,
      emailSubject,
      slideNumber,
      filePath,
      actionVerb
    });
  };
  const tabCls = (active) => cn(
    "px-3 py-1.5 text-xs rounded-t border-b-2 transition-colors",
    active ? "border-primary text-primary font-medium" : "border-transparent text-muted-foreground hover:text-foreground hover:border-border"
  );
  const inputCls = "w-full rounded border border-border bg-background px-2.5 py-1.5 text-xs text-foreground outline-none focus:ring-1 focus:ring-primary";
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("div", { style: { zIndex: 1200 }, className: "fixed inset-0 bg-black/40", onClick: onCancel }),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            style: panelStyle,
            className: "pointer-events-auto w-[440px] rounded-lg border border-border bg-popover shadow-2xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:overflow-y-auto max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            onClick: (e) => e.stopPropagation(),
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-4 py-3 border-b border-border touch-none",
                  children: [
                    (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.hyperlink.editTitle") }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        className: "text-muted-foreground hover:text-foreground",
                        onClick: onCancel,
                        children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1 px-4 pt-2 border-b border-border", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "url"),
                    onClick: () => setTargetType("url"),
                    children: t("pptx.hyperlink.tabUrl")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "email"),
                    onClick: () => setTargetType("email"),
                    children: t("pptx.hyperlink.tabEmail")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "slide"),
                    onClick: () => setTargetType("slide"),
                    children: t("pptx.hyperlink.tabSlide")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "file"),
                    onClick: () => setTargetType("file"),
                    children: t("pptx.hyperlink.tabFile")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: tabCls(targetType === "action"),
                    onClick: () => setTargetType("action"),
                    children: t("pptx.hyperlink.tabAction")
                  }
                )
              ] }),
              (0, import_jsx_runtime.jsx)(
                HyperlinkTabContent,
                {
                  targetType,
                  url,
                  setUrl,
                  tooltip,
                  setTooltip,
                  emailAddress,
                  setEmailAddress,
                  emailSubject,
                  setEmailSubject,
                  slideNumber,
                  setSlideNumber,
                  slideCount,
                  filePath,
                  setFilePath,
                  actionVerb,
                  setActionVerb,
                  inputRef,
                  inputCls,
                  onEnterConfirm: handleConfirm
                }
              ),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 px-4 py-3 border-t border-border", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: "px-3 py-1.5 text-xs rounded border border-border text-foreground hover:bg-muted",
                    onClick: onCancel,
                    children: t("pptx.common.cancel")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    className: "px-3 py-1.5 text-xs rounded bg-primary text-white hover:bg-primary/90",
                    onClick: handleConfirm,
                    children: t("pptx.common.apply")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function ViewerOverlays({
  isShortcutHelpOpen,
  isAccessibilityPanelOpen,
  showSlideSorter,
  showReadingView,
  showOutlineView,
  templateElements,
  accessibilityIssues,
  slides,
  activeSlideIndex,
  canvasSize,
  canEdit,
  sectionGroups,
  onCloseShortcuts,
  onCloseAccessibility,
  onSelectSlide,
  onMoveSlide,
  onDeleteSlides,
  onDuplicateSlides,
  onToggleHideSlides,
  onCloseSorter,
  onCloseReadingView,
  onCloseOutlineView,
  setSlides,
  setActiveSlideIndex,
  bumpHistory,
  reducedMotion,
  onToggleReducedMotion
}) {
  const hasOverlay = isShortcutHelpOpen || isAccessibilityPanelOpen || showSlideSorter || showReadingView || showOutlineView;
  if (!hasOverlay) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    isShortcutHelpOpen && (0, import_jsx_runtime.jsx)(ShortcutPanel, { isOpen: isShortcutHelpOpen, onClose: onCloseShortcuts }),
    isAccessibilityPanelOpen && (0, import_jsx_runtime.jsx)(
      AccessibilityPanel,
      {
        isOpen: isAccessibilityPanelOpen,
        issues: accessibilityIssues,
        onClose: onCloseAccessibility,
        reducedMotion,
        onToggleReducedMotion
      }
    ),
    showSlideSorter && (0, import_jsx_runtime.jsx)(
      SlideSorterOverlay,
      {
        slides,
        activeSlideIndex,
        canvasSize,
        canEdit,
        sectionGroups,
        onSelectSlide,
        onMoveSlide,
        onDeleteSlides,
        onDuplicateSlides,
        onToggleHideSlides,
        onClose: onCloseSorter
      }
    ),
    showReadingView && (0, import_jsx_runtime.jsx)(
      ReadingViewOverlay,
      {
        slides,
        templateElements,
        canvasSize,
        activeSlideIndex,
        onExit: onCloseReadingView
      }
    ),
    showOutlineView && (0, import_jsx_runtime.jsx)(
      OutlineViewOverlay,
      {
        slides,
        canvasSize,
        canEdit,
        setSlides,
        setActiveSlideIndex,
        bumpHistory,
        onClose: onCloseOutlineView
      }
    )
  ] });
}
function ViewerBottomPanels({
  activeSlide,
  allSlides,
  isSlideNotesCollapsed,
  canEdit,
  slideCount,
  activeSlideIndex,
  isDirty,
  autosaveStatus,
  onToggleNotes,
  onUpdateNotes,
  collaborationSlot,
  notesPanelHeight,
  onResizeBottom,
  scale,
  onZoomIn,
  onZoomOut,
  onZoomToFit,
  mode,
  onSetMode,
  onToggleSlideSorter,
  hideStatusBar = false,
  hiddenActions,
  notesStyle
}) {
  const { isHidden } = useToolbarVisibility(hiddenActions);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    onResizeBottom && !isSlideNotesCollapsed && (0, import_jsx_runtime.jsx)(ResizeHandle, { direction: "vertical", onResize: onResizeBottom }),
    (0, import_jsx_runtime.jsx)(
      SlideNotesPanel,
      {
        activeSlide,
        allSlides,
        isExpanded: !isSlideNotesCollapsed,
        canEdit,
        onToggle: onToggleNotes,
        onUpdateNotes,
        panelHeight: notesPanelHeight,
        notesStyle
      }
    ),
    !hideStatusBar && (0, import_jsx_runtime.jsx)(
      StatusBar,
      {
        slideCount,
        activeSlideIndex,
        isDirty,
        autosaveStatus,
        scale,
        onZoomIn,
        onZoomOut,
        onZoomToFit,
        isNotesExpanded: !isSlideNotesCollapsed,
        onToggleNotes,
        mode,
        onSetMode,
        onToggleSlideSorter,
        collaborationSlot,
        hideZoomControls: isHidden("zoom"),
        hideNotesToggle: isHidden("notes"),
        hideFullscreenToggle: isHidden("fullscreen")
      }
    )
  ] });
}
function ViewerInspector({
  isOpen,
  canEdit,
  mode,
  activeSlide,
  slides,
  customShows,
  canvasSize,
  selectedElement,
  effectiveSelectedIds,
  tableEditorState,
  sidebarPanelMode,
  activeSlideIndex,
  comments,
  commentAuthors,
  onSetSidebarPanelMode,
  onClose,
  onUpdateElementStyle,
  onUpdateTextStyle,
  onUpdateElement,
  onApplySelection,
  onSetCanvasSize,
  slideSizeEmu,
  onSetSlideSize,
  onMoveLayer,
  onMoveLayerToEdge,
  onDeleteElement,
  onUpdateSlide,
  presentationProperties,
  onUpdatePresentationProperties,
  notesMaster,
  handoutMaster,
  notesCanvasSize,
  coreProperties,
  appProperties,
  customProperties,
  themeOptions,
  onUpdateCoreProperties,
  onUpdateAppProperties,
  onUpdateCustomProperties,
  tagCollections,
  onUpdateTagCollections,
  onApplyTheme,
  editTemplateMode,
  slideMasters,
  onSetTemplateBackground,
  onGetTemplateBackgroundColor,
  mediaDataUrls,
  theme,
  tableStyleMap,
  onTableStyleMapChange,
  onDeleteTableStyle,
  panelWidth
}) {
  var _a;
  const { t } = useTranslation();
  if (!isOpen) {
    return null;
  }
  const slideId = (activeSlide == null ? void 0 : activeSlide.id) ?? "";
  const commentDraft = comments.commentDraftBySlideId[slideId] ?? "";
  const editingCommentId = comments.editingCommentIdBySlideId[slideId] ?? null;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      "data-pptx-inspector": true,
      role: "complementary",
      "aria-label": t("pptx.inspector.properties"),
      className: "h-full flex-shrink-0 max-md:absolute max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:z-30",
      style: panelWidth ? { width: panelWidth } : void 0,
      children: (0, import_jsx_runtime.jsx)(
        InspectorPane,
        {
          isOpen,
          canEdit,
          mode,
          activeSlide,
          slides,
          customShows,
          canvasSize,
          selectedElement,
          selectedElementIds: effectiveSelectedIds,
          tableEditorState,
          activeTab: sidebarPanelMode,
          onSetActiveTab: (nextTab) => onSetSidebarPanelMode(nextTab),
          onClose,
          onUpdateElementStyle,
          onUpdateTextStyle,
          onUpdateElement,
          onUpdateSlide,
          onSelectElement: (id) => onApplySelection(id),
          onMoveLayer,
          onMoveLayerToEdge,
          onDeleteElement,
          comments: (activeSlide == null ? void 0 : activeSlide.comments) ?? [],
          presentationProperties,
          onUpdatePresentationProperties,
          notesMaster,
          handoutMaster,
          notesCanvasSize,
          coreProperties,
          appProperties,
          customProperties,
          themeOptions,
          onUpdateCoreProperties,
          onUpdateAppProperties,
          onUpdateCustomProperties,
          tagCollections,
          onUpdateTagCollections,
          onApplyTheme,
          commentDraft,
          commentDraftMentions: ((_a = comments.commentDraftMentionsBySlideId) == null ? void 0 : _a[slideId]) ?? [],
          commentAuthors: commentAuthors ?? [],
          editingCommentId,
          commentEditDraft: editingCommentId ? comments.commentEditDraftByCommentId[editingCommentId] ?? "" : "",
          onSetCommentDraft: (draft, mentions) => comments.handleCommentDraftChange(slideId, draft, mentions),
          onAddComment: () => comments.handleAddSlideComment(activeSlideIndex),
          onDeleteComment: (id) => comments.handleDeleteSlideComment(activeSlideIndex, id),
          onStartEditComment: (id) => comments.handleStartCommentEdit(slideId, id),
          onSaveEditComment: (id) => comments.handleSaveCommentEdit(activeSlideIndex, id),
          onCancelEditComment: () => comments.handleCancelCommentEdit(slideId),
          onSetCommentEditDraft: (draft) => comments.handleSetCommentEditDraft(
            comments.editingCommentIdBySlideId[slideId] ?? "",
            draft
          ),
          onToggleCommentResolved: (id) => comments.handleToggleCommentResolved(activeSlideIndex, id),
          onStartReply: (id) => comments.handleStartReply(activeSlideIndex, id),
          onCancelReply: comments.handleCancelReply,
          onReplyDraftChange: (commentId, draft, mentions) => comments.handleReplyDraftChange(commentId, draft, mentions),
          onSubmitReply: (commentId) => comments.handleSubmitReply(activeSlideIndex, commentId),
          replyingToCommentId: comments.replyingToCommentId,
          replyDraftByCommentId: comments.replyDraftByCommentId,
          replyDraftMentionsByCommentId: comments.replyDraftMentionsByCommentId,
          onUpdateCanvasSize: onSetCanvasSize,
          slideSizeEmu,
          onUpdateSlideSize: onSetSlideSize,
          editTemplateMode,
          slideMasters,
          onSetTemplateBackground,
          onGetTemplateBackgroundColor,
          mediaDataUrls,
          theme,
          tableStyleMap,
          onTableStyleMapChange,
          onDeleteTableStyle,
          panelWidth
        }
      )
    }
  );
}
function TitleBar(p) {
  var _a, _b2, _c, _d;
  const { t } = useTranslation();
  const editing = (p.mode === "edit" || p.mode === "master") && p.canEdit;
  const { isHidden } = useToolbarVisibility(p.hiddenActions);
  const quickAccess = useViewerOptionsContext().quickAccess;
  const [searchQuery, setSearchQuery] = (0, import_react2.useState)("");
  const [searchFocused, setSearchFocused] = (0, import_react2.useState)(false);
  const searchRef = (0, import_react2.useRef)(null);
  const commandResults = filterCommands(searchQuery, t);
  const handleCommandSelect = (0, import_react2.useCallback)(
    (entry) => {
      var _a2;
      (_a2 = p.onCommandSearch) == null ? void 0 : _a2.call(p, entry.command);
      setSearchQuery("");
      setSearchFocused(false);
    },
    [p]
  );
  const handleSearchKeyDown = (0, import_react2.useCallback)(
    (e) => {
      if (e.key === "Enter" && searchQuery.trim()) {
        if (commandResults.length > 0) {
          handleCommandSelect(commandResults[0]);
        } else {
          p.onToggleFindReplace();
          setSearchFocused(false);
        }
      } else if (e.key === "Escape") {
        setSearchQuery("");
        setSearchFocused(false);
      }
    },
    [searchQuery, commandResults, handleCommandSelect, p]
  );
  (0, import_react2.useEffect)(() => {
    const handler = (e) => {
      if (searchRef.current && !searchRef.current.contains(e.target)) {
        setSearchFocused(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, []);
  const statusKey = resolveTitleBarStatusKey({
    autosaveState: ((_a = p.autosaveStatus) == null ? void 0 : _a.state) ?? "idle",
    isDirty: p.isDirty,
    autosaveEnabled: p.autosaveEnabled,
    disabledReason: ((_b2 = p.autosaveStatus) == null ? void 0 : _b2.state) === "disabled" ? p.autosaveStatus.reason : void 0
  });
  return (0, import_jsx_runtime.jsxs)("div", { className: TITLE_BAR_CLASSES.container, "data-pptx-title-bar": "", children: [
    (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.logo, "aria-hidden": "true", children: "P" }),
    editing && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsxs)("span", { className: TITLE_BAR_CLASSES.autosaveGroup, children: [
        (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.autosaveLabel, children: t("pptx.titleBar.autoSave") }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            role: "switch",
            "aria-checked": p.autosaveEnabled,
            onClick: p.onToggleAutosave,
            className: cn(
              TITLE_BAR_CLASSES.toggleTrack,
              p.autosaveEnabled ? TITLE_BAR_CLASSES.toggleTrackOn : TITLE_BAR_CLASSES.toggleTrackOff
            ),
            title: t("pptx.titleBar.toggleAutoSave"),
            "aria-label": t("pptx.titleBar.toggleAutoSave"),
            children: (0, import_jsx_runtime.jsx)(
              "span",
              {
                className: cn(
                  TITLE_BAR_CLASSES.toggleKnob,
                  p.autosaveEnabled ? TITLE_BAR_CLASSES.toggleKnobOn : TITLE_BAR_CLASSES.toggleKnobOff
                )
              }
            )
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.autosaveLabel, children: t(p.autosaveEnabled ? "pptx.titleBar.autoSaveOn" : "pptx.titleBar.autoSaveOff") })
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: TITLE_BAR_CLASSES.separator }),
      p.onSave && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: p.onSave,
          className: TITLE_BAR_CLASSES.quickButton,
          title: t("pptx.titleBar.save"),
          "aria-label": t("pptx.titleBar.save"),
          children: (0, import_jsx_runtime.jsx)(LuSave, { className: "w-3.5 h-3.5" })
        }
      ),
      !isHidden("undo") && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: p.onUndo,
          disabled: !p.canUndo,
          className: TITLE_BAR_CLASSES.quickButton,
          title: p.undoLabel ? t("pptx.toolbar.undoAction", { action: p.undoLabel }) : t("pptx.toolbar.undo"),
          "aria-label": t("pptx.toolbar.undo"),
          children: (0, import_jsx_runtime.jsx)(LuUndo, { className: "w-3.5 h-3.5" })
        }
      ),
      !isHidden("redo") && (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: p.onRedo,
          disabled: !p.canRedo,
          className: TITLE_BAR_CLASSES.quickButton,
          title: p.redoLabel ? t("pptx.toolbar.redoAction", { action: p.redoLabel }) : t("pptx.toolbar.redo"),
          "aria-label": t("pptx.toolbar.redo"),
          children: (0, import_jsx_runtime.jsx)(LuRedo, { className: "w-3.5 h-3.5" })
        }
      ),
      quickAccess.position !== "below" && (0, import_jsx_runtime.jsx)(TitleBarQuickExtras, { quickAccess, onCommand: p.onQuickCommand }),
      (0, import_jsx_runtime.jsx)("div", { className: TITLE_BAR_CLASSES.separator })
    ] }),
    (0, import_jsx_runtime.jsxs)("span", { className: TITLE_BAR_CLASSES.fileGroup, children: [
      (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.fileName, children: p.fileName || t(TITLE_BAR_DEFAULT_FILE_KEY) }),
      editing && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.statusDot, "aria-hidden": "true", children: "•" }),
        (0, import_jsx_runtime.jsx)(
          "span",
          {
            className: cn(
              TITLE_BAR_CLASSES.statusText,
              ((_c = p.autosaveStatus) == null ? void 0 : _c.state) === "error" && p.autosaveEnabled && TITLE_BAR_CLASSES.statusError,
              ((_d = p.autosaveStatus) == null ? void 0 : _d.state) === "saving" && p.autosaveEnabled && TITLE_BAR_CLASSES.statusSaving
            ),
            children: t(statusKey)
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.searchWrap, children: (p.mode === "edit" || p.mode === "master") && (0, import_jsx_runtime.jsxs)("div", { ref: searchRef, className: "relative w-full max-w-md", children: [
      (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: cn(
            TITLE_BAR_CLASSES.searchBox,
            (searchFocused || p.findReplaceOpen) && "text-foreground bg-background"
          ),
          children: [
            (0, import_jsx_runtime.jsx)(LuSearch, { className: TITLE_BAR_CLASSES.searchIcon }),
            (0, import_jsx_runtime.jsx)(
              "input",
              {
                type: "text",
                value: searchQuery,
                onChange: (e) => setSearchQuery(e.target.value),
                onFocus: () => setSearchFocused(true),
                onKeyDown: handleSearchKeyDown,
                placeholder: t("pptx.titleBar.searchPlaceholder"),
                className: "flex-1 bg-transparent text-[11px] outline-none placeholder:text-muted-foreground/60",
                "aria-label": t("pptx.titleBar.search")
              }
            )
          ]
        }
      ),
      searchFocused && searchQuery.trim() && (0, import_jsx_runtime.jsxs)("div", { className: "absolute left-0 right-0 top-full z-50 mt-1 rounded-lg border border-border bg-popover shadow-xl max-h-64 overflow-y-auto", children: [
        commandResults.length > 0 ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)("div", { className: "px-3 py-1.5 text-[10px] font-semibold text-muted-foreground uppercase tracking-wider", children: t("pptx.titleBar.searchCommands") }),
          commandResults.slice(0, 8).map((entry) => (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              className: "flex w-full items-center gap-2 px-3 py-1.5 text-xs text-foreground hover:bg-accent transition-colors",
              onMouseDown: () => handleCommandSelect(entry),
              children: [
                (0, import_jsx_runtime.jsx)("span", { className: "truncate", children: t(entry.labelKey) }),
                (0, import_jsx_runtime.jsx)("span", { className: "ml-auto text-[10px] text-muted-foreground capitalize", children: entry.category })
              ]
            },
            entry.command
          ))
        ] }) : (0, import_jsx_runtime.jsx)("div", { className: "px-3 py-2 text-xs text-muted-foreground", children: t("pptx.titleBar.searchNoResults") }),
        (0, import_jsx_runtime.jsx)("div", { className: "border-t border-border/60", children: (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            className: "flex w-full items-center gap-2 px-3 py-1.5 text-xs text-foreground hover:bg-accent transition-colors",
            onMouseDown: () => {
              p.onToggleFindReplace();
              setSearchFocused(false);
              setSearchQuery("");
            },
            children: [
              (0, import_jsx_runtime.jsx)(LuSearch, { className: "w-3 h-3 shrink-0" }),
              (0, import_jsx_runtime.jsxs)("span", { children: [
                t("pptx.titleBar.searchContent"),
                " “",
                searchQuery,
                "”"
              ] })
            ]
          }
        ) })
      ] })
    ] }) }),
    (0, import_jsx_runtime.jsx)("span", { className: TITLE_BAR_CLASSES.rightSpacer })
  ] });
}
function ViewerToolbarSection(props) {
  var _a, _b2, _c, _d, _e, _f, _g, _h;
  const {
    mode,
    canEdit,
    state: s,
    selectedElement,
    activeSlide,
    activeSlideIndex,
    zoom,
    history,
    findReplace,
    manipulation,
    insertHandlers,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    dialogs,
    slideOps,
    sectionOps,
    onApplyLayout,
    loadLayoutPreviews,
    customFontFamilies,
    ops,
    onSetMode,
    onPresentFromBeginning,
    onEnterPresenterView,
    onEnterRehearsalMode,
    onOpenSettings,
    onOpenHeaderFooter,
    onOpenShareDialog,
    onOpenFile,
    onOpenRecentFile,
    onToggleFormatPainter: onToggleFormatPainterProp,
    fileName,
    autosaveStatus,
    autosaveEnabled = true,
    onToggleAutosave,
    hiddenActions,
    recentPresentationsCount,
    aiEnabled,
    isAiPanelOpen,
    onToggleAiPanel,
    isProtectedView,
    onEnableEditing,
    onSetSnapToGrid,
    onSetSnapToShape,
    onSetShowGuides
  } = props;
  const { t } = useTranslation();
  const handleAddAnimation = (0, import_react2.useCallback)(
    (preset, group) => {
      if (!selectedElement || !activeSlide) {
        return;
      }
      const current = activeSlide.animations ?? [];
      if (group === "motionPath") {
        propertyHandlers.handleUpdateSlide({
          animations: applyMotionPathPreset(current, selectedElement.id, preset)
        });
        return;
      }
      const existing = current.find((a) => a.elementId === selectedElement.id);
      const presetValue = preset;
      if (existing) {
        const updated = current.map(
          (a) => a.elementId === selectedElement.id ? { ...a, [group]: presetValue } : a
        );
        propertyHandlers.handleUpdateSlide({ animations: updated });
      } else {
        const newAnim = {
          elementId: selectedElement.id,
          [group]: presetValue,
          durationMs: 500,
          order: current.length,
          trigger: "onClick"
        };
        propertyHandlers.handleUpdateSlide({ animations: [...current, newAnim] });
      }
    },
    [selectedElement, activeSlide, propertyHandlers]
  );
  const handleRemoveAnimation = (0, import_react2.useCallback)(() => {
    if (!selectedElement || !activeSlide) {
      return;
    }
    const current = activeSlide.animations ?? [];
    const filtered = current.filter((a) => a.elementId !== selectedElement.id);
    propertyHandlers.handleUpdateSlide({ animations: filtered });
  }, [selectedElement, activeSlide, propertyHandlers]);
  const handleTransitionChange = (0, import_react2.useCallback)(
    (updates) => {
      if (!activeSlide) {
        return;
      }
      const current = activeSlide.transition ?? { type: "none" };
      propertyHandlers.handleUpdateSlide({ transition: { ...current, ...updates } });
    },
    [activeSlide, propertyHandlers]
  );
  const scopedLayoutOptions = import_react2.default.useMemo(
    () => scopeLayoutOptionsToActiveSlide(s.layoutOptions, activeSlide),
    [s.layoutOptions, activeSlide]
  );
  const handleResetSlide = (0, import_react2.useCallback)(() => {
    const path = resetSlideLayoutPath(activeSlide);
    if (path) {
      onApplyLayout == null ? void 0 : onApplyLayout(path);
    }
  }, [activeSlide, onApplyLayout]);
  const handleAddSection = (0, import_react2.useCallback)(() => {
    sectionOps == null ? void 0 : sectionOps.addSection(t("pptx.sections.defaultName"), activeSlideIndex);
  }, [sectionOps, activeSlideIndex, t]);
  const handleOpenSlideSize = (0, import_react2.useCallback)(() => {
    s.setSelectedElementId(null);
    s.setSelectedElementIds([]);
    s.setSidebarPanelMode("properties");
    s.setIsInspectorPaneOpen(true);
  }, [s]);
  const handleSelectAll = (0, import_react2.useCallback)(() => {
    const allIds = (activeSlide == null ? void 0 : activeSlide.elements.map((element) => element.id)) ?? [];
    if (allIds.length > 0) {
      ops.applySelection(allIds[0], allIds);
    }
  }, [activeSlide, ops]);
  const handlePresentationPropertiesChange = (0, import_react2.useCallback)(
    (updates) => {
      s.setPresentationProperties((prev) => ({ ...prev, ...updates }));
      history.markDirty();
    },
    [s, history]
  );
  const handleApplyTransitionToAll = (0, import_react2.useCallback)(() => {
    const transition = activeSlide == null ? void 0 : activeSlide.transition;
    if (!transition) {
      return;
    }
    ops.updateSlides((prev) => prev.map((sl) => ({ ...sl, transition })));
    history.markDirty();
  }, [activeSlide, ops, history]);
  const handleCommandSearch = (0, import_react2.useCallback)(
    (command) => {
      var _a2, _b3;
      const [category, action] = command.split(".");
      switch (category) {
        case "format":
          switch (action) {
            case "bold":
              ops.updateSelectedTextStyle({ bold: true });
              break;
            case "italic":
              ops.updateSelectedTextStyle({ italic: true });
              break;
            case "underline":
              ops.updateSelectedTextStyle({ underline: true });
              break;
            case "alignLeft":
              ops.updateSelectedTextStyle({ align: "left" });
              break;
            case "alignCenter":
              ops.updateSelectedTextStyle({ align: "center" });
              break;
            case "alignRight":
              ops.updateSelectedTextStyle({ align: "right" });
              break;
            case "clear":
              ops.updateSelectedTextStyle({
                bold: false,
                italic: false,
                underline: false,
                strikethrough: false
              });
              break;
          }
          break;
        case "insert":
          switch (action) {
            case "textBox":
              insertHandlers.handleAddTextBox();
              break;
            case "shape":
              insertHandlers.handleAddShape();
              break;
            case "image":
              (_a2 = s.imageInputRef.current) == null ? void 0 : _a2.click();
              break;
            case "media":
              (_b3 = s.mediaInputRef.current) == null ? void 0 : _b3.click();
              break;
            case "table":
              insertHandlers.handleAddTable();
              break;
            case "chart":
              insertHandlers.handleAddChart(DEFAULT_INSERT_CHART_KIND);
              break;
            case "smartArt":
              dialogs.setIsSmartArtDialogOpen(true);
              break;
            case "equation":
              dialogs.setIsEquationDialogOpen(true);
              break;
            case "link":
              dialogs.setIsHyperlinkDialogOpen(true);
              break;
          }
          break;
        case "view":
          switch (action) {
            case "toggleGrid":
              s.setShowGrid((p) => !p);
              break;
            case "toggleRulers":
              s.setShowRulers((p) => !p);
              break;
            case "slideSorter":
              s.setShowSlideSorter((p) => !p);
              break;
            case "zoomToFit":
              zoom.handleZoomToFit();
              break;
          }
          break;
        case "slideShow":
          switch (action) {
            case "fromBeginning":
              (onPresentFromBeginning ?? (() => onSetMode("present")))();
              break;
            case "presenterView":
              onEnterPresenterView();
              break;
          }
          break;
        case "design":
          switch (action) {
            case "browseThemes":
              s.setIsThemeGalleryOpen((p) => !p);
              break;
            case "slideSize":
              handleOpenSlideSize();
              break;
          }
          break;
        case "arrange":
          switch (action) {
            case "bringToFront":
              manipulation.handleMoveLayerToEdge("front");
              break;
            case "sendToBack":
              manipulation.handleMoveLayerToEdge("back");
              break;
            case "duplicate":
              manipulation.handleDuplicate();
              break;
          }
          break;
        case "review":
          switch (action) {
            case "spelling":
              s.setSpellCheckEnabled((p) => !p);
              break;
            case "accessibility":
              dialogs.handleRunAccessibilityCheck();
              break;
          }
          break;
      }
    },
    [
      ops,
      insertHandlers,
      s,
      dialogs,
      zoom,
      onSetMode,
      onPresentFromBeginning,
      onEnterPresenterView,
      manipulation,
      handleOpenSlideSize
    ]
  );
  const handleQuickAccessCommand = (0, import_react2.useCallback)(
    (id) => {
      var _a2;
      const handlers = {
        presentFromStart: onPresentFromBeginning ?? (() => onSetMode("present")),
        print: printHandlers.handlePrint,
        exportPdf: exportHandlers.handleExportPdf,
        newSlide: slideOps.handleAddSlide,
        spellCheck: () => s.setSpellCheckEnabled((p) => !p),
        zoomIn: zoom.handleZoomIn,
        zoomOut: zoom.handleZoomOut
      };
      (_a2 = handlers[id]) == null ? void 0 : _a2.call(handlers);
    },
    [onSetMode, onPresentFromBeginning, printHandlers, exportHandlers, slideOps, s, zoom]
  );
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    !dialogs.isNarrowViewport && (0, import_jsx_runtime.jsx)(
      TitleBar,
      {
        mode,
        canEdit,
        fileName,
        isDirty: s.isDirty,
        autosaveStatus,
        autosaveEnabled,
        onToggleAutosave: onToggleAutosave ?? (() => {
        }),
        canUndo: history.canUndo,
        canRedo: history.canRedo,
        undoLabel: history.undoLabel,
        redoLabel: history.redoLabel,
        onUndo: history.handleUndo,
        onRedo: history.handleRedo,
        onSave: exportHandlers.handleSaveAsPptx,
        findReplaceOpen: findReplace.findReplaceOpen,
        onToggleFindReplace: () => findReplace.setFindReplaceOpen(!findReplace.findReplaceOpen),
        onCommandSearch: handleCommandSearch,
        onQuickCommand: handleQuickAccessCommand,
        hiddenActions
      }
    ),
    (0, import_jsx_runtime.jsx)(
      Toolbar,
      {
        fileName,
        mode,
        canEdit,
        isProtectedView,
        onEnableEditing,
        onQuickCommand: handleQuickAccessCommand,
        hiddenActions,
        recentPresentationsCount,
        isNarrowViewport: dialogs.isNarrowViewport,
        isSidebarCollapsed: !s.isSlidesPaneOpen,
        isInspectorPaneOpen: s.isInspectorPaneOpen,
        isCompactToolbarOpen: s.isCompactToolbarOpen,
        toolbarSection: s.toolbarSection,
        scale: zoom.scale,
        canUndo: history.canUndo,
        canRedo: history.canRedo,
        undoLabel: history.undoLabel,
        redoLabel: history.redoLabel,
        findReplaceOpen: findReplace.findReplaceOpen,
        selectedElement,
        tableEditorState: s.tableEditorState,
        editTemplateMode: s.editTemplateMode,
        newShapeType: s.newShapeType,
        activeTool: s.activeTool,
        drawingColor: s.drawingColor,
        drawingWidth: s.drawingWidth,
        clipboardPayload: s.clipboardPayload,
        onSetMode,
        onPresentFromBeginning,
        onToggleSidebar: () => s.setIsSlidesPaneOpen((p) => !p),
        onToggleInspector: () => s.setIsInspectorPaneOpen((p) => !p),
        onOpenAnimationPanel: () => {
          s.setIsInspectorPaneOpen(true);
          s.setSidebarPanelMode("properties");
        },
        onAddAnimation: handleAddAnimation,
        onRemoveAnimation: handleRemoveAnimation,
        onToggleCompactToolbar: () => s.setIsCompactToolbarOpen((p) => !p),
        onSetToolbarSection: s.setToolbarSection,
        onZoomIn: zoom.handleZoomIn,
        onZoomOut: zoom.handleZoomOut,
        onZoomToFit: zoom.handleZoomToFit,
        onUndo: history.handleUndo,
        onRedo: history.handleRedo,
        onToggleFindReplace: () => findReplace.setFindReplaceOpen(!findReplace.findReplaceOpen),
        onSetNewShapeType: s.setNewShapeType,
        onAddTextBox: insertHandlers.handleAddTextBox,
        onAddShape: insertHandlers.handleAddShape,
        onAddTable: insertHandlers.handleAddTable,
        onAddChart: insertHandlers.handleAddChart,
        onAddSmartArt: () => dialogs.setIsSmartArtDialogOpen(true),
        onAddEquation: () => {
          dialogs.setEditingEquationOmml(null);
          dialogs.setIsEquationDialogOpen(true);
        },
        onAddActionButton: insertHandlers.handleAddActionButton,
        onInsertField: insertHandlers.handleInsertField,
        onOpenHeaderFooter,
        onOpenImagePicker: () => {
          var _a2;
          return (_a2 = s.imageInputRef.current) == null ? void 0 : _a2.click();
        },
        onOpenMediaPicker: () => {
          var _a2;
          return (_a2 = s.mediaInputRef.current) == null ? void 0 : _a2.click();
        },
        onSetActiveTool: s.setActiveTool,
        onSetDrawingColor: s.setDrawingColor,
        onSetDrawingWidth: s.setDrawingWidth,
        onSetEditTemplateMode: s.setEditTemplateMode,
        spellCheckEnabled: s.spellCheckEnabled,
        showGrid: s.showGrid,
        showRulers: s.showRulers,
        showGuides: s.showGuides,
        snapToGrid: s.snapToGrid,
        snapToShape: s.snapToShape,
        onSetSpellCheckEnabled: s.setSpellCheckEnabled,
        onSetShowGrid: s.setShowGrid,
        onSetShowRulers: s.setShowRulers,
        onSetShowGuides: onSetShowGuides ?? s.setShowGuides,
        onSetSnapToGrid: onSetSnapToGrid ?? s.setSnapToGrid,
        onSetSnapToShape: onSetSnapToShape ?? s.setSnapToShape,
        onAddGuide: dialogs.handleAddGuide,
        onAlignElements: manipulation.handleAlignElements,
        onDistributeElements: manipulation.handleDistributeElements,
        canDistribute: manipulation.canDistribute,
        onCopy: manipulation.handleCopy,
        onCut: manipulation.handleCut,
        onPaste: manipulation.handlePaste,
        onFlip: manipulation.handleFlip,
        onMoveLayer: manipulation.handleMoveLayer,
        onMoveLayerToEdge: manipulation.handleMoveLayerToEdge,
        onGroupElements: manipulation.handleGroupElements,
        onUngroupElement: manipulation.handleUngroupElement,
        onUpdateElementStyle: ops.updateSelectedShapeStyle,
        selectedCount: s.selectedElementIds.length > 0 ? s.selectedElementIds.length : selectedElement ? 1 : 0,
        selectionGroupable: manipulation.selectionGroupable,
        onOpenHyperlinkDialog: () => dialogs.setIsHyperlinkDialogOpen(true),
        onDuplicate: manipulation.handleDuplicate,
        onDelete: manipulation.handleDelete,
        onExportPng: exportHandlers.handleExportPng,
        onExportPdf: exportHandlers.handleExportPdf,
        onExportVideo: exportHandlers.handleExportVideo,
        onExportGif: exportHandlers.handleExportGif,
        onExportJson: exportHandlers.handleExportJson,
        onOpenFile,
        onOpenRecentFile,
        onCreatePresentation: (templateId) => {
          s.setSlides(createBackstagePresentation(templateId));
          s.setActiveSlideIndex(0);
          s.setSelectedElementId(null);
          s.setSelectedElementIds([]);
          s.setTemplateElementsBySlideId({});
          s.setIsDirty(true);
        },
        onOpenShareDialog,
        onSaveAsPptx: exportHandlers.handleSaveAsPptx,
        onSaveAsPpsx: exportHandlers.handleSaveAsPpsx,
        onSaveAsPptm: exportHandlers.handleSaveAsPptm,
        hasMacros: s.hasMacros,
        onCopySlideAsImage: exportHandlers.handleCopySlideAsImage,
        onPrint: printHandlers.handlePrint,
        onToggleShortcuts: () => s.setIsShortcutHelpOpen((p) => !p),
        onOpenSettings,
        onRunAccessibilityCheck: dialogs.handleRunAccessibilityCheck,
        onToggleSlideSorter: () => s.setShowSlideSorter((p) => !p),
        onOpenReadingView: () => s.setShowReadingView(true),
        onOpenOutlineView: () => s.setShowOutlineView(true),
        onUpdateTextStyle: ops.updateSelectedTextStyle,
        onTransformTextCase: ops.updateSelectedTextCase,
        isOverflowMenuOpen: s.isOverflowMenuOpen,
        onSetOverflowMenuOpen: s.setIsOverflowMenuOpen,
        layoutOptions: scopedLayoutOptions,
        currentLayoutPath: activeSlide == null ? void 0 : activeSlide.layoutPath,
        loadLayoutPreviews,
        themeFonts: {
          heading: (_c = (_b2 = (_a = s.theme) == null ? void 0 : _a.fontScheme) == null ? void 0 : _b2.majorFont) == null ? void 0 : _c.latin,
          body: (_f = (_e = (_d = s.theme) == null ? void 0 : _d.fontScheme) == null ? void 0 : _e.minorFont) == null ? void 0 : _f.latin
        },
        embeddedFontFamilies: s.embeddedFonts.map((font) => font.name),
        customFontFamilies,
        onInsertSlideFromLayout: slideOps.handleInsertSlideFromLayout,
        onApplyLayout,
        onInsertSlideFromTemplate: slideOps.handleInsertSlideFromTemplate,
        templateScheme: templateSchemeFromTheme((_g = s.theme) == null ? void 0 : _g.colorScheme),
        customShows: s.customShows,
        activeCustomShowId: s.activeCustomShowId,
        onSetActiveCustomShowId: s.setActiveCustomShowId,
        onCreateCustomShow: dialogs.handleCreateCustomShow,
        onRenameActiveCustomShow: dialogs.handleRenameActiveCustomShow,
        onDeleteActiveCustomShow: dialogs.handleDeleteActiveCustomShow,
        onToggleCurrentSlideInActiveShow: dialogs.handleToggleCurrentSlideInActiveShow,
        isCurrentSlideInActiveShow: dialogs.isCurrentSlideInActiveShow,
        onEnterMasterView: dialogs.handleEnterMasterView,
        onCloseMasterView: dialogs.handleCloseMasterView,
        onToggleVersionHistory: () => propertyHandlers.setIsVersionHistoryOpen((p) => !p),
        onOpenPasswordProtection: () => dialogs.setIsPasswordDialogOpen(true),
        onOpenDocumentProperties: () => dialogs.setIsDocPropsDialogOpen(true),
        onOpenSlideSize: handleOpenSlideSize,
        onOpenFontEmbedding: () => dialogs.setIsFontEmbeddingOpen(true),
        onOpenDigitalSignatures: () => dialogs.setIsDigitalSigDialogOpen(true),
        onEnterPresenterView,
        onEnterRehearsalMode,
        onToggleThemeEditor: () => s.setIsThemeEditorOpen((p) => !p),
        isThemeEditorOpen: s.isThemeEditorOpen,
        onToggleThemeGallery: () => s.setIsThemeGalleryOpen((p) => !p),
        isThemeGalleryOpen: s.isThemeGalleryOpen,
        onCompare: propertyHandlers.handleCompare,
        onToggleComments: () => {
          s.setSidebarPanelMode("comments");
          if (!s.isInspectorPaneOpen) {
            s.setIsInspectorPaneOpen(true);
          }
        },
        isCommentsPanelOpen: s.isInspectorPaneOpen,
        slideCommentCount: ((_h = activeSlide == null ? void 0 : activeSlide.comments) == null ? void 0 : _h.length) ?? 0,
        formatPainterActive: s.formatPainterActive,
        canActivateFormatPainter: hasCopyableFormat(selectedElement),
        onToggleFormatPainter: onToggleFormatPainterProp ?? (() => s.setFormatPainterActive((p) => !p)),
        isSelectionPaneOpen: s.isSelectionPaneOpen,
        onToggleSelectionPane: () => s.setIsSelectionPaneOpen((p) => !p),
        eyedropperActive: s.eyedropperActive,
        onToggleEyedropper: () => s.setEyedropperActive((p) => !p),
        onOpenSetUpSlideShow: () => dialogs.setIsSetUpSlideShowOpen(true),
        onToggleHideSlide: () => slideOps.handleToggleHideSlides([activeSlideIndex]),
        activeSlideHidden: Boolean(activeSlide == null ? void 0 : activeSlide.hidden),
        onOpenBroadcastDialog: () => dialogs.setIsBroadcastDialogOpen(true),
        onToggleSubtitles: dialogs.handleToggleSubtitles,
        showSubtitles: Boolean(s.presentationProperties.showSubtitles),
        activeSlide,
        onTransitionChange: handleTransitionChange,
        onApplyTransitionToAll: handleApplyTransitionToAll,
        onResetSlide: handleResetSlide,
        onAddSection: sectionOps ? handleAddSection : void 0,
        onSelectAll: handleSelectAll,
        presentationProperties: s.presentationProperties,
        onPresentationPropertiesChange: handlePresentationPropertiesChange,
        aiEnabled,
        isAiPanelOpen,
        onToggleAiPanel
      }
    ),
    s.hasDigitalSignatures && (0, import_jsx_runtime.jsx)("div", { className: "flex items-center px-3 py-1 z-10", children: (0, import_jsx_runtime.jsx)(
      SignatureStatusBadge,
      {
        hasSignatures: s.hasDigitalSignatures,
        signatureCount: s.digitalSignatureCount,
        onClick: () => dialogs.setIsDigitalSigDialogOpen(true)
      }
    ) }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        ref: s.imageInputRef,
        type: "file",
        name: "image-upload",
        accept: "image/*",
        className: "hidden",
        onChange: insertHandlers.handleImageFileChange
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        ref: s.mediaInputRef,
        type: "file",
        name: "media-upload",
        accept: "video/*,audio/*",
        className: "hidden",
        onChange: insertHandlers.handleMediaFileChange
      }
    )
  ] });
}
var TEMPLATES = EQUATION_TEMPLATES;
var TEMPLATE_MATHML = compileEquationTemplateMathMl();
function MathMlPreview({ mathml }) {
  const containerRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    if (!containerRef.current) {
      return;
    }
    if (mathml) {
      containerRef.current.innerHTML = sanitizeMathMl(mathml);
    } else {
      containerRef.current.innerHTML = "";
    }
  }, [mathml]);
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      ref: containerRef,
      className: "flex items-center justify-center min-h-[80px] text-2xl text-foreground",
      style: { fontFamily: '"Cambria Math", "STIX Two Math", serif' }
    }
  );
}
function EquationEditorDialog({
  isOpen,
  onClose,
  onInsert,
  existingOmml
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const initialLatex = (0, import_react2.useMemo)(() => {
    if (!existingOmml) {
      return "";
    }
    return convertOmmlToLatex(existingOmml);
  }, [existingOmml]);
  const [latex, setLatex] = (0, import_react2.useState)(initialLatex);
  const deferredLatex = (0, import_react2.useDeferredValue)(latex);
  const textareaRef = (0, import_react2.useRef)(null);
  const dialogRef = (0, import_react2.useRef)(null);
  useModalFocus(isOpen, dialogRef, onClose, textareaRef);
  (0, import_react2.useEffect)(() => {
    if (isOpen) {
      setLatex(initialLatex);
    }
  }, [isOpen, initialLatex]);
  const { mathml, omml } = (0, import_react2.useMemo)(() => compileLatexEquation(deferredLatex), [deferredLatex]);
  const handleInsert = (0, import_react2.useCallback)(() => {
    if (!latex.trim()) {
      return;
    }
    let insertOmml = omml;
    try {
      const fresh = convertLatexToOmml(latex);
      if (fresh && Object.keys(fresh).length > 0) {
        insertOmml = fresh;
      }
    } catch {
    }
    onInsert(insertOmml);
    onClose();
  }, [latex, omml, onInsert, onClose]);
  const handleTemplateClick = (0, import_react2.useCallback)((templateLatex) => {
    setLatex(templateLatex);
  }, []);
  const handleKeyDown = (0, import_react2.useCallback)(
    (e) => {
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        handleInsert();
      }
      if (e.key === "Escape") {
        e.preventDefault();
        onClose();
      }
    },
    [handleInsert, onClose]
  );
  if (!isOpen) {
    return null;
  }
  const isEditing = Boolean(existingOmml);
  const hasContent = latex.trim().length > 0 && Object.keys(omml).length > 0;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 backdrop-blur-sm",
      onClick: (e) => {
        if (e.target === e.currentTarget) {
          onClose();
        }
      },
      onKeyDown: handleKeyDown,
      children: (0, import_jsx_runtime.jsxs)(
        "div",
        {
          ref: dialogRef,
          style: panelStyle,
          className: "bg-background border border-border rounded-xl shadow-2xl w-[640px] max-h-[85vh] flex flex-col overflow-hidden max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
          onClick: (e) => e.stopPropagation(),
          role: "dialog",
          "aria-modal": "true",
          tabIndex: -1,
          "aria-label": isEditing ? t("pptx.equation.editTitle", "Edit Equation") : t("pptx.equation.insertTitle", "Insert Equation"),
          children: [
            (0, import_jsx_runtime.jsxs)(
              "div",
              {
                ...dragHandlers,
                className: "flex items-center justify-between px-5 py-3 border-b border-border touch-none",
                children: [
                  (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: isEditing ? t("pptx.equation.editTitle", "Edit Equation") : t("pptx.equation.insertTitle", "Insert Equation") }),
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: onClose,
                      className: "p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors",
                      "aria-label": t("pptx.settings.close"),
                      children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
                    }
                  )
                ]
              }
            ),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 overflow-y-auto px-5 py-4 space-y-4", children: [
              (0, import_jsx_runtime.jsx)("div", { className: "rounded-lg border border-border bg-muted/60 p-4 min-h-[80px] flex items-center justify-center", children: hasContent ? (0, import_jsx_runtime.jsx)(MathMlPreview, { mathml }) : (0, import_jsx_runtime.jsx)("span", { className: "text-sm text-muted-foreground italic", children: t("pptx.equation.previewPlaceholder", "Equation preview will appear here") }) }),
              (0, import_jsx_runtime.jsxs)("div", { children: [
                (0, import_jsx_runtime.jsx)("label", { className: "block text-xs font-medium text-muted-foreground mb-1.5", children: t("pptx.equation.latexInput", "LaTeX Input") }),
                (0, import_jsx_runtime.jsx)(
                  "textarea",
                  {
                    ref: textareaRef,
                    value: latex,
                    onChange: (e) => setLatex(e.target.value),
                    placeholder: "\\\\frac{a}{b} + \\\\sqrt{c}",
                    className: "w-full h-24 px-3 py-2 rounded-lg border border-border bg-muted text-foreground text-sm font-mono resize-none focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary placeholder:text-muted-foreground",
                    spellCheck: false
                  }
                ),
                (0, import_jsx_runtime.jsx)("p", { className: "mt-1 text-[11px] text-muted-foreground", children: t("pptx.equation.latexHint", "Use LaTeX syntax. Ctrl+Enter to insert.") })
              ] }),
              (0, import_jsx_runtime.jsxs)("div", { children: [
                (0, import_jsx_runtime.jsx)("h3", { className: "text-xs font-medium text-muted-foreground mb-2", children: t("pptx.equation.templates", "Common Templates") }),
                (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-4 gap-1.5", children: TEMPLATES.map((tmpl, idx) => {
                  const tmplMathml = TEMPLATE_MATHML[idx];
                  return (0, import_jsx_runtime.jsxs)(
                    "button",
                    {
                      type: "button",
                      onClick: () => handleTemplateClick(tmpl.latex),
                      className: cn(
                        "flex flex-col items-center gap-1 p-2 rounded-lg border transition-colors",
                        latex === tmpl.latex ? "border-primary bg-primary/10" : "border-border bg-muted/40 hover:bg-accent/60 hover:border-border"
                      ),
                      title: t(tmpl.i18nKey, tmpl.label),
                      children: [
                        (0, import_jsx_runtime.jsx)(
                          "div",
                          {
                            className: "text-sm text-foreground h-8 flex items-center justify-center overflow-hidden",
                            style: {
                              fontFamily: '"Cambria Math", "STIX Two Math", serif'
                            },
                            dangerouslySetInnerHTML: { __html: tmplMathml }
                          }
                        ),
                        (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground truncate w-full text-center", children: t(tmpl.i18nKey, tmpl.label) })
                      ]
                    },
                    tmpl.latex
                  );
                }) })
              ] })
            ] }),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 px-5 py-3 border-t border-border", children: [
              (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  onClick: onClose,
                  className: "px-4 py-1.5 rounded-lg text-xs text-foreground hover:bg-accent transition-colors",
                  children: t("pptx.equation.cancel", "Cancel")
                }
              ),
              (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  onClick: handleInsert,
                  disabled: !hasContent,
                  className: "px-4 py-1.5 rounded-lg text-xs font-medium bg-primary hover:bg-primary/80 text-white disabled:opacity-40 disabled:cursor-not-allowed transition-colors",
                  children: isEditing ? t("pptx.equation.update", "Update") : t("pptx.equation.insert", "Insert")
                }
              )
            ] })
          ]
        }
      )
    }
  );
}
var PREVIEW_TILE_WIDTH2 = 64;
function getPreviewForLayout(layout) {
  const scale = PREVIEW_TILE_WIDTH2 / SMARTART_PREVIEW_ELEMENT_WIDTH;
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      "aria-hidden": true,
      className: "overflow-hidden pointer-events-none",
      style: {
        width: PREVIEW_TILE_WIDTH2,
        height: Math.round(SMARTART_PREVIEW_ELEMENT_HEIGHT * scale)
      },
      children: (0, import_jsx_runtime.jsx)(
        "div",
        {
          style: {
            width: SMARTART_PREVIEW_ELEMENT_WIDTH,
            height: SMARTART_PREVIEW_ELEMENT_HEIGHT,
            transform: `scale(${scale})`,
            transformOrigin: "top left"
          },
          children: (0, import_jsx_runtime.jsx)(SmartArtRenderer, { element: buildSmartArtPreviewElement(layout) })
        }
      )
    }
  );
}
function InsertSmartArtDialog({
  isOpen,
  onClose,
  onInsert
}) {
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const [activeCategory, setActiveCategory] = (0, import_react2.useState)("list");
  const [selectedLayout, setSelectedLayout] = (0, import_react2.useState)(null);
  const dialogRef = (0, import_react2.useRef)(null);
  useModalFocus(isOpen, dialogRef, onClose);
  const filteredPresets = PRESETS.filter((p) => p.category === activeCategory);
  const handleInsert = (0, import_react2.useCallback)(() => {
    if (!selectedLayout) {
      return;
    }
    const preset = PRESETS.find((p) => p.layout === selectedLayout);
    if (!preset) {
      return;
    }
    onInsert(preset.layout, preset.defaultItems);
    onClose();
  }, [selectedLayout, onInsert, onClose]);
  if (!isOpen) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/50",
        onClick: onClose,
        "aria-label": t("pptx.smartart.close")
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            ref: dialogRef,
            style: panelStyle,
            className: "pointer-events-auto w-[600px] max-w-[90vw] max-h-[80vh] rounded-lg border border-border bg-background shadow-2xl flex flex-col max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:max-w-none max-md:max-h-[88dvh] max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.smartart.insertTitle"),
            tabIndex: -1,
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between px-4 py-3 border-b border-border touch-none",
                  children: [
                    (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-medium text-foreground", children: t("pptx.smartart.insertTitle") }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "p-1 rounded hover:bg-muted transition-colors",
                        "aria-label": t("pptx.smartart.close"),
                        children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-1 overflow-hidden", children: [
                (0, import_jsx_runtime.jsx)(
                  "nav",
                  {
                    className: "w-40 border-r border-border py-2",
                    "aria-label": t("pptx.insertSmartArt.categories"),
                    children: CATEGORIES.map((cat) => (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: () => {
                          setActiveCategory(cat.id);
                          setSelectedLayout(null);
                        },
                        className: cn(
                          "w-full text-left px-3 py-1.5 text-xs transition-colors",
                          activeCategory === cat.id ? "bg-primary text-white" : "text-foreground hover:bg-muted"
                        ),
                        children: t(cat.label)
                      },
                      cat.id
                    ))
                  }
                ),
                (0, import_jsx_runtime.jsx)("div", { className: "flex-1 p-3 overflow-y-auto", children: (0, import_jsx_runtime.jsx)(
                  "div",
                  {
                    className: "grid grid-cols-3 gap-2",
                    role: "listbox",
                    "aria-label": t("pptx.insertSmartArt.layouts"),
                    children: filteredPresets.map((preset) => (0, import_jsx_runtime.jsxs)(
                      "button",
                      {
                        type: "button",
                        role: "option",
                        "aria-selected": selectedLayout === preset.layout,
                        onClick: () => setSelectedLayout(preset.layout),
                        onDoubleClick: () => {
                          setSelectedLayout(preset.layout);
                          onInsert(preset.layout, preset.defaultItems);
                          onClose();
                        },
                        className: cn(
                          "flex flex-col items-center gap-1 p-2 rounded border transition-colors",
                          selectedLayout === preset.layout ? "border-primary bg-primary/20" : "border-border hover:border-border hover:bg-muted/50"
                        ),
                        children: [
                          (0, import_jsx_runtime.jsx)("div", { className: "w-16 h-12 flex items-center justify-center bg-muted rounded", children: getPreviewForLayout(preset.layout) }),
                          (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-foreground text-center leading-tight", children: t(preset.labelKey) })
                        ]
                      },
                      preset.layout
                    ))
                  }
                ) })
              ] }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 px-4 py-3 border-t border-border", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: onClose,
                    className: "px-3 py-1.5 text-xs rounded bg-muted hover:bg-accent text-foreground transition-colors",
                    children: t("pptx.smartart.cancel")
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: handleInsert,
                    disabled: !selectedLayout,
                    className: cn(
                      "px-3 py-1.5 text-xs rounded transition-colors",
                      selectedLayout ? "bg-primary hover:bg-primary/80 text-white" : "bg-muted text-muted-foreground cursor-not-allowed"
                    ),
                    children: t("pptx.smartart.insert")
                  }
                )
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function ViewerDialogGroup(props) {
  var _a, _b2, _c, _d, _e, _f;
  const {
    dialogs,
    insertHandlers,
    exportHandlers,
    printHandlers,
    propertyHandlers,
    annotations,
    slides,
    activeSlideIndex,
    canvasSize,
    filePath,
    coreProperties,
    customProperties,
    appProperties,
    embeddedFonts,
    hasDigitalSignatures,
    digitalSignatureCount,
    presentationProperties,
    customShows,
    selectedElements,
    isEncryptedDialogOpen,
    setIsEncryptedDialogOpen,
    showKeepAnnotationsDialog,
    onKeepAnnotations,
    onDiscardAnnotations
  } = props;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      VersionHistoryPanel,
      {
        isOpen: propertyHandlers.isVersionHistoryOpen,
        filePath,
        onClose: () => propertyHandlers.setIsVersionHistoryOpen(false),
        onRestore: propertyHandlers.handleRestoreVersion
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ComparePanel,
      {
        isOpen: propertyHandlers.isComparePanelOpen,
        compareResult: propertyHandlers.compareResult,
        canvasSize,
        onClose: () => propertyHandlers.setIsComparePanelOpen(false),
        onAcceptSlide: propertyHandlers.handleAcceptSlide,
        onRejectSlide: propertyHandlers.handleRejectSlide,
        onAcceptAll: propertyHandlers.handleAcceptAllSlides
      }
    ),
    (0, import_jsx_runtime.jsx)(
      PasswordProtectionDialog,
      {
        isOpen: dialogs.isPasswordDialogOpen,
        isCurrentlyProtected: dialogs.isPasswordProtected,
        onClose: () => dialogs.setIsPasswordDialogOpen(false),
        onSetPassword: dialogs.handleSetPassword,
        onRemovePassword: dialogs.handleRemovePassword
      }
    ),
    (0, import_jsx_runtime.jsx)(
      DocumentPropertiesDialog,
      {
        isOpen: dialogs.isDocPropsDialogOpen,
        coreProperties: coreProperties ?? {},
        customProperties,
        appProperties,
        onClose: () => dialogs.setIsDocPropsDialogOpen(false),
        onSave: (core, custom, app) => {
          propertyHandlers.handleUpdateCoreProperties(core);
          propertyHandlers.handleUpdateCustomProperties(custom);
          if (app) {
            propertyHandlers.handleUpdateAppProperties(app);
          }
        }
      }
    ),
    (0, import_jsx_runtime.jsx)(
      FontEmbeddingPanel,
      {
        isOpen: dialogs.isFontEmbeddingOpen,
        embedFontsEnabled: dialogs.embedFontsEnabled,
        usedFontFamilies: propertyHandlers.usedFontFamilies,
        embeddedFonts: embeddedFonts.map((f) => f.name),
        canEmbedFonts: dialogs.fontEmbedding.interactive,
        embedUnavailableKey: dialogs.fontEmbedding.disabledReasonKey,
        onClose: () => dialogs.setIsFontEmbeddingOpen(false),
        onToggleEmbedFonts: dialogs.setEmbedFontsEnabled
      }
    ),
    (0, import_jsx_runtime.jsx)(
      DigitalSignaturesDialog,
      {
        isOpen: dialogs.isDigitalSigDialogOpen,
        onClose: () => dialogs.setIsDigitalSigDialogOpen(false),
        hasSignatures: hasDigitalSignatures,
        signatureCount: digitalSignatureCount
      }
    ),
    (0, import_jsx_runtime.jsx)(
      SignatureStrippedDialog,
      {
        isOpen: dialogs.isSignatureStrippedDialogOpen,
        signatureCount: digitalSignatureCount,
        onConfirm: () => dialogs.setIsSignatureStrippedDialogOpen(false),
        onCancel: () => dialogs.setIsSignatureStrippedDialogOpen(false)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      EncryptedFileDialog,
      {
        isOpen: isEncryptedDialogOpen,
        onClose: () => setIsEncryptedDialogOpen(false)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      InsertSmartArtDialog,
      {
        isOpen: dialogs.isSmartArtDialogOpen,
        onClose: () => dialogs.setIsSmartArtDialogOpen(false),
        onInsert: insertHandlers.handleInsertSmartArt
      }
    ),
    (0, import_jsx_runtime.jsx)(
      EquationEditorDialog,
      {
        isOpen: dialogs.isEquationDialogOpen,
        onClose: () => dialogs.setIsEquationDialogOpen(false),
        onInsert: dialogs.editingEquationOmml ? insertHandlers.handleUpdateEquation : insertHandlers.handleInsertEquation,
        existingOmml: dialogs.editingEquationOmml
      }
    ),
    (0, import_jsx_runtime.jsx)(
      HyperlinkEditDialog,
      {
        open: dialogs.isHyperlinkDialogOpen,
        initialUrl: (_b2 = (_a = selectedElements[0]) == null ? void 0 : _a.actionClick) == null ? void 0 : _b2.url,
        initialTooltip: (_d = (_c = selectedElements[0]) == null ? void 0 : _c.actionClick) == null ? void 0 : _d.tooltip,
        initialAction: (_f = (_e = selectedElements[0]) == null ? void 0 : _e.actionClick) == null ? void 0 : _f.action,
        slideCount: slides.length,
        onConfirm: insertHandlers.handleHyperlinkConfirm,
        onCancel: () => dialogs.setIsHyperlinkDialogOpen(false)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ExportProgressModal,
      {
        isOpen: exportHandlers.exportModalOpen,
        title: exportHandlers.exportModalTitle,
        progress: exportHandlers.exportProgress,
        statusMessage: exportHandlers.exportStatusMessage,
        onCancel: exportHandlers.handleCancelExport
      }
    ),
    (0, import_jsx_runtime.jsx)(
      SetUpSlideShowDialog,
      {
        open: dialogs.isSetUpSlideShowOpen,
        onClose: () => dialogs.setIsSetUpSlideShowOpen(false),
        properties: presentationProperties,
        onSave: dialogs.handleSaveSlideShowSettings,
        customShows,
        slideCount: slides.length
      }
    ),
    (0, import_jsx_runtime.jsx)(
      PrintDialog,
      {
        open: printHandlers.isPrintDialogOpen,
        onClose: () => printHandlers.setIsPrintDialogOpen(false),
        onPrint: printHandlers.handlePrintSvg,
        slides,
        activeSlideIndex,
        defaultSlidesPerPage: printPropertiesSlidesPerPage(presentationProperties.printProperties),
        defaultFrameSlides: printPropertiesFrameSlides(presentationProperties.printProperties)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      KeepAnnotationsDialog,
      {
        isOpen: showKeepAnnotationsDialog,
        annotationCount: Array.from(annotations.allSlideAnnotations.values()).reduce(
          (sum, strokes) => sum + strokes.length,
          0
        ),
        slideCount: annotations.allSlideAnnotations.size,
        onKeep: onKeepAnnotations,
        onDiscard: onDiscardAnnotations
      }
    )
  ] });
}
function ViewerPresentationLayer(props) {
  const {
    mode,
    slides,
    canvasSize,
    templateElements,
    presentation,
    activeCustomShow,
    authoredRange,
    onExitPresentation,
    isMobile,
    onUpdateNotes
  } = props;
  const presenterActive = mode === "present" && presentation.presenterMode;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    presenterActive && (isMobile ? (0, import_jsx_runtime.jsx)(
      MobilePresenterView,
      {
        slides,
        currentSlideIndex: presentation.presentationSlideIndex,
        canvasSize,
        templateElements,
        activeCustomShow,
        authoredRange,
        presentationStartTime: presentation.presentationStartTime,
        onMovePresentationSlide: presentation.movePresentationSlide,
        onExit: onExitPresentation
      }
    ) : (0, import_jsx_runtime.jsx)(
      PresenterView,
      {
        slides,
        currentSlideIndex: presentation.presentationSlideIndex,
        canvasSize,
        templateElements,
        activeCustomShow,
        authoredRange,
        presentationStartTime: presentation.presentationStartTime,
        onMovePresentationSlide: presentation.movePresentationSlide,
        onExit: onExitPresentation,
        onOpenAudienceWindow: presentation.openAudienceWindow,
        onCloseAudienceWindow: presentation.closeAudienceWindow,
        isAudienceWindowOpen: presentation.isAudienceWindowOpen(),
        endOfShowVisible: presentation.endOfShowVisible,
        snapshot: presentation.presenterSnapshot,
        onNavigateToSlide: presentation.navigateToSlide,
        onToggleTimer: presentation.togglePresenterTimer,
        onResetTimer: presentation.resetPresenterTimer,
        onStepZoom: presentation.stepPresenterZoom,
        onResetZoom: presentation.resetPresenterZoom,
        onSetBlackout: presentation.setPresenterBlackout,
        onUpdateSnapshot: presentation.updatePresenterSnapshot,
        onToggleSubtitles: () => presentation.setPresenterSubtitlesVisible(
          !presentation.presenterSnapshot.subtitlesVisible
        ),
        onSwapDisplays: () => void presentation.swapPresenterDisplays(),
        onUpdateNotes
      }
    )),
    mode === "present" && presentation.rehearsing && (0, import_jsx_runtime.jsx)(
      RehearseTimingsHud,
      {
        presentationStartTime: presentation.presentationStartTime,
        slideStartTime: presentation.slideStartTime,
        paused: presentation.rehearsalPaused,
        onTogglePause: presentation.toggleRehearsalPause
      }
    ),
    presentation.showRehearsalSummary && (0, import_jsx_runtime.jsx)(
      RehearseTimingsSummary,
      {
        slides,
        canvasSize,
        recordedTimings: presentation.recordedTimings,
        onSave: presentation.saveRehearsalTimings,
        onDiscard: presentation.dismissRehearsalSummary
      }
    )
  ] });
}
function PresentationStage({
  activeSlide,
  templateElements,
  canvasSize,
  mediaDataUrls,
  presentationElementStates,
  presentationKeyframesCss,
  onActionClick,
  onHyperlinkClick,
  allSlides,
  onZoomClick,
  sourceSlideIndex,
  fieldContext,
  tableStyleContext,
  onStageClick,
  children,
  screenOverlay
}) {
  const containerRef = (0, import_react2.useRef)(null);
  const [box, setBox] = (0, import_react2.useState)(null);
  (0, import_react2.useEffect)(() => {
    const el = containerRef.current;
    if (!el) {
      return;
    }
    const observer = new ResizeObserver((entries) => {
      var _a;
      const rect = (_a = entries[0]) == null ? void 0 : _a.contentRect;
      if (rect && rect.width > 0 && rect.height > 0) {
        setBox({ width: rect.width, height: rect.height });
      }
    });
    observer.observe(el);
    const initial = el.getBoundingClientRect();
    if (initial.width > 0 && initial.height > 0) {
      setBox({ width: initial.width, height: initial.height });
    }
    return () => {
      observer.disconnect();
    };
  }, []);
  const safeWidth = Math.max(canvasSize.width, 1);
  const safeHeight = Math.max(canvasSize.height, 1);
  const scale = (0, import_react2.useMemo)(() => {
    if (!box) {
      return 1;
    }
    const fit = Math.min(box.width / safeWidth, box.height / safeHeight);
    return fit > 0 ? fit : 1;
  }, [box, safeWidth, safeHeight]);
  const scaledWidth = safeWidth * scale;
  const scaledHeight = safeHeight * scale;
  return (
    // oxlint-disable-next-line jsx-a11y/no-static-element-interactions, jsx-a11y/click-events-have-key-events -- the show surface is not a control; it carries PowerPoint's click-to-advance, whose keyboard equivalent (Space / arrows / PageDown) is bound globally by usePresentationKeyboard
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        ref: containerRef,
        "data-pptx-presentation-stage": true,
        className: "relative flex-1 min-h-0 overflow-hidden bg-black select-none",
        onClick: onStageClick,
        children: [
          (0, import_jsx_runtime.jsxs)(
            "div",
            {
              className: "absolute",
              style: {
                width: scaledWidth,
                height: scaledHeight,
                // Centre the letterboxed slide without flexbox so the box keeps a
                // definite size for the overlays parented to it.
                left: box ? Math.max(0, (box.width - scaledWidth) / 2) : 0,
                top: box ? Math.max(0, (box.height - scaledHeight) / 2) : 0
              },
              children: [
                (0, import_jsx_runtime.jsxs)(
                  "div",
                  {
                    role: "region",
                    "aria-roledescription": "slide",
                    "aria-label": `Slide ${(sourceSlideIndex ?? 0) + 1}`,
                    ...{ [PRESENTATION_STAGE_ATTRIBUTE]: "true" },
                    className: "absolute top-0 left-0 overflow-hidden",
                    style: {
                      width: safeWidth,
                      height: safeHeight,
                      transform: `scale(${scale})`,
                      transformOrigin: "top left",
                      // Motion-path keyframes translate by a fraction of the SLIDE, so
                      // the stage publishes its own size for those calc() offsets.
                      "--pptx-slide-w": `${safeWidth}px`,
                      "--pptx-slide-h": `${safeHeight}px`,
                      ...getReactSlideBackgroundStyle(activeSlide, {
                        widthPx: canvasSize.width,
                        heightPx: canvasSize.height
                      })
                    },
                    children: [
                      (0, import_jsx_runtime.jsx)(SlideBackgroundImageLayer, { slide: activeSlide }),
                      (0, import_jsx_runtime.jsx)("style", { children: PRESENTATION_HIT_TEST_CSS }),
                      presentationKeyframesCss && (0, import_jsx_runtime.jsx)("style", { children: presentationKeyframesCss }),
                      templateElements.map((element, index) => (0, import_jsx_runtime.jsx)(
                        ElementRenderer,
                        {
                          element,
                          activeSlide,
                          isSelected: false,
                          isInlineEditing: false,
                          inlineEditingText: "",
                          canInteract: false,
                          presenting: true,
                          spellCheckEnabled: false,
                          mediaDataUrls,
                          selectionColorClass: "blue-400",
                          showHoverBorder: false,
                          zIndex: index,
                          imageAltText: "Template element",
                          showResizeHandles: false,
                          renderInk: false,
                          renderGroups: true,
                          adjustmentHandles: [],
                          onResizePointerDown: noop2,
                          onAdjustmentPointerDown: noop2,
                          onInlineEditChange: noop2,
                          onInlineEditCommit: noop2,
                          onInlineEditCancel: noop2,
                          onActionClick,
                          onHyperlinkClick,
                          animationState: presentationElementStates == null ? void 0 : presentationElementStates.get(element.id),
                          presentationElementStates,
                          allSlides,
                          onZoomClick,
                          sourceSlideIndex,
                          fieldContext,
                          tableStyleContext
                        },
                        `tpl-${element.id}`
                      )),
                      activeSlide == null ? void 0 : activeSlide.elements.map((element, index) => (0, import_jsx_runtime.jsx)(
                        ElementRenderer,
                        {
                          element,
                          activeSlide,
                          isSelected: false,
                          isInlineEditing: false,
                          inlineEditingText: "",
                          canInteract: false,
                          presenting: true,
                          spellCheckEnabled: false,
                          mediaDataUrls,
                          selectionColorClass: "blue-500",
                          showHoverBorder: false,
                          zIndex: templateElements.length + index,
                          imageAltText: "Slide element",
                          showResizeHandles: false,
                          renderInk: true,
                          renderGroups: true,
                          adjustmentHandles: [],
                          onResizePointerDown: noop2,
                          onAdjustmentPointerDown: noop2,
                          onInlineEditChange: noop2,
                          onInlineEditCommit: noop2,
                          onInlineEditCancel: noop2,
                          onActionClick,
                          onHyperlinkClick,
                          animationState: presentationElementStates == null ? void 0 : presentationElementStates.get(element.id),
                          presentationElementStates,
                          allSlides,
                          onZoomClick,
                          sourceSlideIndex,
                          fieldContext,
                          tableStyleContext
                        },
                        element.id
                      ))
                    ]
                  }
                ),
                children == null ? void 0 : children(scale)
              ]
            }
          ),
          screenOverlay
        ]
      }
    )
  );
}
function noop2() {
}
var itemClass = "block w-full whitespace-nowrap px-3 py-1.5 text-left text-xs text-foreground transition-colors hover:bg-accent";
var headingClass = "px-3 pb-0.5 pt-1.5 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground";
function PresentationContextMenu(p) {
  const { t } = useTranslation();
  const run = (action) => {
    action();
    p.onClose();
  };
  const item = (label, action) => action ? (0, import_jsx_runtime.jsx)("button", { type: "button", role: "menuitem", className: itemClass, onClick: () => run(action), children: label }) : null;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "fixed inset-0 z-[1299]",
        onClick: p.onClose,
        onContextMenu: (e) => {
          e.preventDefault();
          p.onClose();
        }
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        "data-pptx-presentation-menu": "",
        role: "menu",
        tabIndex: -1,
        className: "fixed z-[1300] min-w-[180px] rounded-md border border-border bg-popover py-1 shadow-xl",
        style: { left: p.state.x, top: p.state.y },
        onContextMenu: (e) => e.preventDefault(),
        children: [
          item(t("pptx.presenter.nextSlide"), p.onNext),
          item(t("pptx.presenter.previousSlide"), p.onPrevious),
          item(t("pptx.presenter.seeAllSlides"), p.onSeeAllSlides),
          item(t("pptx.presenter.presenterView"), p.onShowPresenterView),
          (p.onPointerTool || p.onEraseInk) && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border/60" }),
            (0, import_jsx_runtime.jsx)("div", { className: headingClass, children: t("pptx.presentation.pointerTools") }),
            item(
              t("pptx.presenter.pointerArrow"),
              p.onPointerTool && (() => {
                var _a;
                return (_a = p.onPointerTool) == null ? void 0 : _a.call(p, "none");
              })
            ),
            item(
              t("pptx.presenter.pointerPen"),
              p.onPointerTool && (() => {
                var _a;
                return (_a = p.onPointerTool) == null ? void 0 : _a.call(p, "pen");
              })
            ),
            item(
              t("pptx.presenter.pointerHighlighter"),
              p.onPointerTool && (() => {
                var _a;
                return (_a = p.onPointerTool) == null ? void 0 : _a.call(p, "highlighter");
              })
            ),
            item(
              t("pptx.presentation.laserPointer"),
              p.onPointerTool && (() => {
                var _a;
                return (_a = p.onPointerTool) == null ? void 0 : _a.call(p, "laser");
              })
            ),
            item(t("pptx.presenter.eraseAllInk"), p.onEraseInk)
          ] }),
          p.onBlank && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border/60" }),
            (0, import_jsx_runtime.jsx)("div", { className: headingClass, children: t("pptx.presenter.screen") }),
            item(t("pptx.presenter.blackScreen"), () => {
              var _a;
              return (_a = p.onBlank) == null ? void 0 : _a.call(p, "black");
            }),
            item(t("pptx.presenter.whiteScreen"), () => {
              var _a;
              return (_a = p.onBlank) == null ? void 0 : _a.call(p, "white");
            })
          ] }),
          (0, import_jsx_runtime.jsx)("div", { className: "my-1 border-t border-border/60" }),
          item(t("pptx.presenter.endPresentation"), p.onEndShow)
        ]
      }
    )
  ] });
}
function ViewerCanvasArea(props) {
  var _a;
  const {
    mode,
    canEdit,
    slides,
    activeSlide,
    masterPseudoSlide,
    templateElements,
    canvasSize,
    activeSlideIndex,
    gridSpacingPx,
    zoom,
    state: s,
    selectedElement,
    canvasHandlers,
    insertHandlers,
    tableOps,
    annotations,
    presentation,
    onEndPresentation,
    findReplace,
    hiddenActions,
    aiPickMode = false,
    onAiPickElement,
    aiHighlightOverlay,
    aiCanvasActive = false
  } = props;
  const { t } = useTranslation();
  const { isHidden } = useToolbarVisibility(hiddenActions);
  const viewerOptions = useViewerOptionsContext();
  const effectiveSlide = mode === "master" ? masterPseudoSlide : activeSlide;
  const effectiveTemplateElements = mode === "master" ? [] : templateElements;
  const fieldContext = (0, import_react2.useMemo)(
    () => buildFieldSubstitutionContext({
      headerFooter: s.headerFooter,
      customProperties: s.customProperties,
      slide: activeSlide
    }),
    [s.headerFooter, s.customProperties, activeSlide]
  );
  const tableStyleContext = (0, import_react2.useMemo)(() => {
    if (!s.theme && !s.tableStyleMap) {
      return void 0;
    }
    return { theme: s.theme, tableStyleMap: s.tableStyleMap };
  }, [s.theme, s.tableStyleMap]);
  const openExternalUrl = (0, import_react2.useCallback)(
    (url) => {
      if (shouldConfirmExternalHyperlink(viewerOptions, url) && !window.confirm(`${t("pptx.options.trust.confirmHyperlinks")}

${url}`)) {
        return;
      }
      safeOpenUrl(url);
    },
    [viewerOptions, t]
  );
  const handleActionClick = (0, import_react2.useCallback)(
    (elementId, action) => {
      if (mode === "present") {
        const resolved = resolvePresentationAction(action, { slideCount: slides.length });
        if (resolved.intent.kind === "openUrl") {
          openExternalUrl(resolved.intent.url);
          return;
        }
        presentation.handlePresentationAction(action, elementId);
      } else if (action.url) {
        openExternalUrl(action.url);
      }
    },
    [mode, presentation, openExternalUrl, slides.length]
  );
  const handleHyperlinkClick = (0, import_react2.useCallback)(
    (url) => {
      if (isPpactionUrl(url)) {
        if (mode === "present") {
          const parsed = parsePpactionUrl(url);
          if (parsed) {
            const action = {
              action: parsed.action,
              targetSlideIndex: parsed.targetSlideIndex
            };
            presentation.handlePresentationAction(action);
          }
        }
        return;
      }
      openExternalUrl(url);
    },
    [mode, presentation, openExternalUrl]
  );
  const [presentationMenu, setPresentationMenu] = (0, import_react2.useState)(
    null
  );
  const handlePresentationContextMenu = (0, import_react2.useCallback)(
    (e) => {
      if (mode !== "present") {
        return;
      }
      e.preventDefault();
      if (!viewerOptions.advanced.slideShowShowMenuOnRightClick) {
        return;
      }
      setPresentationMenu({ x: e.clientX, y: e.clientY });
    },
    [mode, viewerOptions.advanced.slideShowShowMenuOnRightClick]
  );
  const swipeHandledAtRef = (0, import_react2.useRef)(0);
  const handleStageClick = (0, import_react2.useCallback)(
    (e) => {
      if (mode !== "present") {
        return;
      }
      if (Date.now() - swipeHandledAtRef.current < 700) {
        return;
      }
      if (annotations.presentationTool !== "none") {
        return;
      }
      if (!isPresentationAdvanceClick(e.target)) {
        return;
      }
      presentation.movePresentationSlide(1, "click");
    },
    [annotations.presentationTool, mode, presentation]
  );
  const handleToggleBlackboard = (0, import_react2.useCallback)(() => {
    const next = toggleBlackboard(
      presentation.presenterSnapshot.blackout,
      annotations.presentationTool
    );
    presentation.setPresenterBlackout(next.blackout);
    if (next.tool !== annotations.presentationTool) {
      annotations.setPresentationTool(next.tool);
    }
  }, [annotations, presentation]);
  const toolbarHoveringRef = (0, import_react2.useRef)(false);
  const handleToolbarMouseEnter = (0, import_react2.useCallback)(() => {
    toolbarHoveringRef.current = true;
    annotations.setToolbarVisible(true);
  }, [annotations]);
  const handleToolbarMouseLeave = (0, import_react2.useCallback)(() => {
    toolbarHoveringRef.current = false;
  }, []);
  const swipeEnabled = mode === "preview" || mode === "present";
  const handleSwipeNext = (0, import_react2.useCallback)(() => {
    if (mode === "present") {
      swipeHandledAtRef.current = Date.now();
      presentation.movePresentationSlide(1, "click");
    } else {
      s.setActiveSlideIndex((i) => Math.min(slides.length - 1, i + 1));
    }
  }, [mode, presentation, s, slides.length]);
  const handleSwipePrev = (0, import_react2.useCallback)(() => {
    if (mode === "present") {
      swipeHandledAtRef.current = Date.now();
      presentation.movePresentationSlide(-1);
    } else {
      s.setActiveSlideIndex((i) => Math.max(0, i - 1));
    }
  }, [mode, presentation, s]);
  const swipe = useSwipeNavigation({
    enabled: swipeEnabled,
    onNext: handleSwipeNext,
    onPrev: handleSwipePrev
  });
  const pickActive = aiPickMode && Boolean(onAiPickElement) && mode === "edit";
  const handleElementClick = pickActive ? (elementId, e) => {
    e.stopPropagation();
    onAiPickElement == null ? void 0 : onAiPickElement(activeSlideIndex, elementId);
  } : canvasHandlers.handleElementClick;
  const handleElementMouseDown = pickActive ? (_elementId, e) => {
    e.stopPropagation();
  } : canvasHandlers.handleElementMouseDown;
  return (
    // eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions -- the canvas area hosts swipe navigation and the presentation-mode right-click menu; it has no interactive semantics of its own
    (0, import_jsx_runtime.jsxs)(
      "main",
      {
        "aria-label": t("pptx.viewer.slideEditorAria"),
        "data-ai-pick-mode": pickActive ? "true" : void 0,
        className: `flex-1 min-w-0 relative flex flex-col bg-background${pickActive ? " cursor-crosshair" : ""}`,
        onTouchStart: swipe.onTouchStart,
        onTouchEnd: swipe.onTouchEnd,
        onContextMenu: mode === "present" ? handlePresentationContextMenu : void 0,
        children: [
          findReplace.findReplaceOpen && (0, import_jsx_runtime.jsx)(
            FindReplacePanel,
            {
              findQuery: findReplace.findQuery,
              replaceQuery: findReplace.replaceQuery,
              findMatchCase: findReplace.findMatchCase,
              findResults: findReplace.findResults,
              findResultIndex: findReplace.findResultIndex,
              onSetFindQuery: findReplace.setFindQuery,
              onSetReplaceQuery: findReplace.setReplaceQuery,
              onSetFindMatchCase: findReplace.setFindMatchCase,
              onPerformFind: findReplace.performFind,
              onNavigateResult: findReplace.navigateFindResult,
              onReplace: findReplace.handleReplace,
              onReplaceAll: findReplace.handleReplaceAll,
              onClose: () => findReplace.setFindReplaceOpen(false)
            }
          ),
          mode === "present" ? (0, import_jsx_runtime.jsx)(
            PresentationStage,
            {
              activeSlide,
              templateElements,
              canvasSize,
              mediaDataUrls: s.mediaDataUrls,
              presentationElementStates: presentation.presentationElementStates,
              presentationKeyframesCss: presentation.presentationKeyframesCss,
              onActionClick: handleActionClick,
              onHyperlinkClick: handleHyperlinkClick,
              allSlides: slides,
              onZoomClick: presentation.handleZoomClick,
              sourceSlideIndex: activeSlideIndex,
              fieldContext,
              tableStyleContext,
              onStageClick: handleStageClick,
              screenOverlay: (
                // Blackout/whiteout, audience ink, laser and captions. Rendered on
                // the stage rather than beside the viewer, because the fullscreen
                // element is the viewer's inner container: anything mounted outside
                // it simply is not painted while a real fullscreen show is running.
                (0, import_jsx_runtime.jsx)(PresentationAudienceEffects, { snapshot: presentation.presenterSnapshot })
              ),
              children: (stageScale) => (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                presentation.transitionOverlay && slides[presentation.transitionOverlay.outgoingSlideIndex] && (0, import_jsx_runtime.jsx)(
                  PresentationTransitionOverlay,
                  {
                    outgoingSlide: slides[presentation.transitionOverlay.outgoingSlideIndex],
                    templateElements,
                    canvasSize,
                    transition: presentation.transitionOverlay.transition,
                    durationMs: presentation.transitionOverlay.durationMs,
                    scale: stageScale,
                    morphPlan: presentation.morphPlan,
                    incomingSlide: slides[presentation.transitionOverlay.incomingSlideIndex],
                    onComplete: presentation.handleTransitionOverlayComplete
                  },
                  `${presentation.transitionOverlay.outgoingSlideIndex}-${presentation.transitionOverlay.incomingSlideIndex}`
                ),
                annotations.inkMarkupVisible && (0, import_jsx_runtime.jsx)(
                  PresentationAnnotationOverlay,
                  {
                    canvasSize,
                    editorScale: stageScale,
                    presentationTool: annotations.presentationTool,
                    blackout: presentation.presenterSnapshot.blackout,
                    annotationStrokes: annotations.annotationStrokes,
                    currentStroke: annotations.currentStroke,
                    laserPosition: annotations.laserPosition,
                    onPointerDown: annotations.handlePointerDown,
                    onPointerMove: annotations.handlePointerMove,
                    onPointerUp: annotations.handlePointerUp,
                    onLaserMove: annotations.handleLaserMove,
                    onLaserLeave: annotations.handleLaserLeave,
                    onEraseAtPoint: annotations.eraseAtPoint
                  }
                )
              ] })
            }
          ) : mode === "master" && s.masterViewTab === "notes" ? (0, import_jsx_runtime.jsx)(
            NotesMasterCanvas,
            {
              notesMaster: s.notesMaster,
              canvasSize,
              notesCanvasSize: s.notesCanvasSize
            }
          ) : mode === "master" && s.masterViewTab === "handout" ? (0, import_jsx_runtime.jsx)(
            HandoutMasterCanvas,
            {
              handoutMaster: s.handoutMaster,
              canvasSize,
              slidesPerPage: ((_a = s.handoutMaster) == null ? void 0 : _a.slidesPerPage) ?? s.handoutSlidesPerPage
            }
          ) : (0, import_jsx_runtime.jsx)(
            SlideCanvas,
            {
              onUpdateSlideAnimations: props.onUpdateSlideAnimations,
              activeSlide: effectiveSlide,
              templateElements: effectiveTemplateElements,
              canvasSize,
              zoom,
              mode,
              canEdit,
              editTemplateMode: mode === "master" || s.editTemplateMode,
              selectedElementIdSet: s.selectedElementIdSet,
              selectedElement,
              inlineEditingElementId: s.inlineEditingElementId,
              inlineEditingText: s.inlineEditingText,
              spellCheckEnabled: s.spellCheckEnabled,
              mediaDataUrls: s.mediaDataUrls,
              tableEditorState: s.tableEditorState,
              marqueeSelectionState: s.marqueeSelectionState,
              snapLines: s.snapLines,
              showGrid: s.showGrid,
              gridSpacingPx,
              showRulers: s.showRulers,
              guides: s.guides,
              onClick: handleElementClick,
              onDoubleClick: canvasHandlers.handleElementDoubleClick,
              onMouseDown: handleElementMouseDown,
              onContextMenu: canvasHandlers.handleElementContextMenu,
              onCanvasMouseDown: canvasHandlers.handleCanvasMouseDown,
              onResizePointerDown: canvasHandlers.handleResizePointerDown,
              onAdjustmentPointerDown: canvasHandlers.handleAdjustmentPointerDown,
              onRotate: canvasHandlers.handleRotate,
              onInlineEditChange: s.setInlineEditingText,
              onInlineEditCommit: canvasHandlers.handleInlineEditCommit,
              onInlineEditCancel: () => s.setInlineEditingElementId(null),
              onTableCellSelect: (cell, elementId) => s.setTableEditorState(cell ? { ...cell, elementId } : null),
              onCommitCellEdit: tableOps.handleCommitCellEdit,
              onUpdateSmartArtElement: canvasHandlers.handleUpdateSmartArtElement,
              onFormatText: canvasHandlers.handleFormatText,
              onResizeTableColumns: tableOps.handleResizeTableColumns,
              onResizeTableRow: tableOps.handleResizeTableRow,
              findResults: findReplace.findResults,
              findResultIndex: findReplace.findResultIndex,
              activeSlideIndex,
              activeTool: s.activeTool,
              drawingColor: s.drawingColor,
              drawingWidth: s.drawingWidth,
              isDrawingRef: s.isDrawingRef,
              onAddInkElement: insertHandlers.handleAddInkElement,
              onAddFreeformShape: insertHandlers.handleAddFreeformShape,
              onEraseInkElement: insertHandlers.handleEraseInkElement,
              onActionClick: handleActionClick,
              onHyperlinkClick: handleHyperlinkClick,
              fieldContext,
              tableStyleContext,
              aiActive: aiCanvasActive,
              collaborationOverlay: (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                (0, import_jsx_runtime.jsx)(
                  RemoteSelectionOverlay,
                  {
                    elements: (effectiveSlide == null ? void 0 : effectiveSlide.elements) ?? [],
                    activeSlideIndex
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  CollaborationCursorOverlay,
                  {
                    activeSlideIndex,
                    canvasWidth: canvasSize.width,
                    canvasHeight: canvasSize.height,
                    selectedElementId: s.selectedElementId
                  }
                ),
                aiHighlightOverlay
              ] }),
              comments: activeSlide == null ? void 0 : activeSlide.comments,
              showCommentMarkers: s.sidebarPanelMode === "comments",
              onCommentMarkerClick: () => s.setSidebarPanelMode("comments"),
              onMoveGuide: (guideId, position) => {
                s.setGuides(
                  (prev) => prev.map(
                    (guide) => guide.id === guideId ? {
                      ...guide,
                      position: guide.axis === "h" ? Math.max(0, Math.min(canvasSize.height, position)) : Math.max(0, Math.min(canvasSize.width, position))
                    } : guide
                  )
                );
              },
              onDeleteGuide: (guideId) => {
                s.setGuides((prev) => prev.filter((guide) => guide.id !== guideId));
              },
              onCreateGuideFromRuler: (axis, positionPx) => {
                s.setGuides((prev) => [
                  ...prev,
                  {
                    id: `guide-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
                    axis,
                    position: positionPx
                  }
                ]);
              }
            }
          ),
          mode === "present" && presentation.allSlidesOpen && (0, import_jsx_runtime.jsx)(
            PresenterSlideNavigator,
            {
              slides,
              current: presentation.presentationSlideIndex,
              canvasSize,
              templateElements,
              onSelect: (index) => {
                presentation.navigateToSlide(index);
                presentation.closeAllSlides();
              },
              onClose: presentation.closeAllSlides
            }
          ),
          mode === "present" && presentation.endOfShowVisible && (0, import_jsx_runtime.jsx)(PresentationEndOverlay, { onExit: () => presentation.movePresentationSlide(1) }),
          mode === "present" && presentationMenu && (0, import_jsx_runtime.jsx)(
            PresentationContextMenu,
            {
              state: presentationMenu,
              onNext: () => presentation.movePresentationSlide(1),
              onPrevious: () => presentation.movePresentationSlide(-1),
              onEndShow: onEndPresentation ?? (() => {
              }),
              onClose: () => setPresentationMenu(null),
              onSeeAllSlides: presentation.openAllSlides,
              onShowPresenterView: presentation.togglePresenterView,
              onBlank: (value) => presentation.setPresenterBlackout(
                presentation.presenterSnapshot.blackout === value ? "none" : value
              ),
              onPointerTool: (tool) => annotations.setPresentationTool(tool === "none" ? "none" : tool),
              onEraseInk: annotations.clearAnnotations
            }
          ),
          mode === "present" && (0, import_jsx_runtime.jsx)(PresentationSubtitleBar, { visible: Boolean(s.presentationProperties.showSubtitles) }),
          mode === "present" && (0, import_jsx_runtime.jsx)(
            PresentationTouchControls,
            {
              currentSlideIndex: presentation.presentationSlideIndex,
              totalSlides: slides.length,
              onMovePresentationSlide: presentation.movePresentationSlide,
              onEndPresentation: onEndPresentation ?? (() => {
              }),
              hideNavigation: isHidden("navigation")
            }
          ),
          mode === "present" && (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: PRESENT_TOOLBAR_CLASSES.wrapper,
              style: {
                opacity: annotations.toolbarVisible ? 1 : 0,
                pointerEvents: annotations.toolbarVisible ? "auto" : "none"
              },
              onMouseEnter: handleToolbarMouseEnter,
              onMouseLeave: handleToolbarMouseLeave,
              children: (0, import_jsx_runtime.jsx)(
                PresentationToolbar,
                {
                  presentationTool: annotations.presentationTool,
                  penColor: annotations.penColor,
                  highlighterColor: annotations.highlighterColor,
                  hasAnnotations: annotations.annotationStrokes.length > 0,
                  onSetTool: annotations.setPresentationTool,
                  onSetPenColor: annotations.setPenColor,
                  onSetHighlighterColor: annotations.setHighlighterColor,
                  onClearAnnotations: annotations.clearAnnotations,
                  blackout: presentation.presenterSnapshot.blackout,
                  onToggleBlackboard: handleToggleBlackboard,
                  currentSlideIndex: presentation.presentationSlideIndex,
                  totalSlides: slides.length,
                  onMovePresentationSlide: presentation.movePresentationSlide,
                  presentationStartTime: presentation.presentationStartTime,
                  onEndPresentation: onEndPresentation ?? (() => {
                  }),
                  onTogglePresenterView: presentation.togglePresenterView,
                  presenterMode: presentation.presenterMode
                }
              )
            }
          )
        ]
      }
    )
  );
}
function useTableStyleMapHandlers(input) {
  const { tableStyleMap, setTableStyleMap, tableStylesToDelete, setTableStylesToDelete } = input;
  const handleTableStyleMapChange = (nextMap) => {
    const result = applyTableStyleMapChange({ tableStyleMap, tableStylesToDelete }, nextMap);
    setTableStyleMap(result.tableStyleMap);
    setTableStylesToDelete(result.tableStylesToDelete);
  };
  const handleDeleteTableStyle = (styleId) => {
    const result = applyTableStyleDelete({ tableStyleMap, tableStylesToDelete }, styleId);
    setTableStyleMap(result.tableStyleMap);
    setTableStylesToDelete(result.tableStylesToDelete);
  };
  return { handleTableStyleMapChange, handleDeleteTableStyle };
}
var AiChatPanel = (0, import_react2.lazy)(async () => {
  const [{ useChat }, { default: Panel }] = await Promise.all([
    import("./react_pptx-react-viewer-FG4E7SHS.js"),
    import("./AiChatPanel-II6VOOAL-BJCAH4KG.js")
  ]);
  function BoundAiChatPanel(props) {
    return (0, import_jsx_runtime.jsx)(Panel, { ...props, useChat });
  }
  return { default: BoundAiChatPanel };
});
function AiChatPanelLazy(props) {
  return (0, import_jsx_runtime.jsx)(
    import_react2.Suspense,
    {
      fallback: (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: "absolute right-0 top-0 z-30 flex h-full w-80 items-center justify-center border-l border-border bg-card text-muted-foreground max-md:inset-x-0 max-md:top-auto max-md:bottom-0 max-md:h-[75dvh] max-md:w-full max-md:rounded-t-2xl max-md:border-l-0 max-md:border-t max-md:shadow-2xl",
          style: props.panelWidth ? { width: props.panelWidth } : void 0,
          children: (0, import_jsx_runtime.jsx)(LuLoaderCircle, { className: "w-5 h-5 animate-spin" })
        }
      ),
      children: (0, import_jsx_runtime.jsx)(AiChatPanel, { ...props })
    }
  );
}
var OVERLAY_CSS = `
@keyframes pptx-ai-ring-pulse {
	0% { box-shadow: 0 0 0 0 rgba(59,130,246,0.55), 0 0 0 0 rgba(59,130,246,0.35); }
	70% { box-shadow: 0 0 0 6px rgba(59,130,246,0), 0 0 14px 4px rgba(59,130,246,0.28); }
	100% { box-shadow: 0 0 0 0 rgba(59,130,246,0), 0 0 10px 2px rgba(59,130,246,0.22); }
}
@keyframes pptx-ai-ring-in {
	0% { opacity: 0; transform: scale(1.04); }
	100% { opacity: 1; transform: scale(1); }
}
/* While the AI is active, tween colour changes on slide elements so an edit
   fades from its old value to the new one instead of snapping. */
[data-pptx-ai-active='true'] [data-element-id],
[data-pptx-ai-active='true'] [data-element-id] * {
	transition: color 0.5s ease, fill 0.5s ease, stroke 0.5s ease, background-color 0.5s ease,
		border-color 0.5s ease;
}
`;
function AiFocusHighlightOverlay({
  highlights,
  elements,
  activeSlideIndex
}) {
  if (highlights.length === 0) {
    return null;
  }
  const byId = new Map(elements.map((el) => [el.id, el]));
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("style", { children: OVERLAY_CSS }),
    highlights.map((hl) => {
      if (hl.slideIndex !== activeSlideIndex) {
        return null;
      }
      const el = byId.get(hl.elementId);
      if (!el) {
        return null;
      }
      const active = hl.variant === "active";
      return (0, import_jsx_runtime.jsx)(
        "div",
        {
          "data-testid": `ai-focus-highlight-${hl.elementId}`,
          "data-ai-highlight": hl.variant,
          "data-export-ignore": "true",
          className: "absolute pointer-events-none rounded-[3px]",
          style: {
            left: el.x - 3,
            top: el.y - 3,
            width: el.width + 6,
            height: el.height + 6,
            zIndex: 9998,
            border: active ? "2px solid rgba(59,130,246,0.9)" : "2px solid rgba(59,130,246,0.55)",
            animation: active ? "pptx-ai-ring-in 0.18s ease-out, pptx-ai-ring-pulse 1s ease-out infinite" : "pptx-ai-ring-in 0.9s ease-out",
            boxShadow: active ? void 0 : "0 0 10px 2px rgba(59,130,246,0.18)"
          }
        },
        `ai-hl-${hl.variant}-${hl.elementId}`
      );
    })
  ] });
}
function AiChangeOverlay({ batch, activeSlideIndex }) {
  const [phase, setPhase] = (0, import_react2.useState)("start");
  const nonce = (batch == null ? void 0 : batch.nonce) ?? 0;
  (0, import_react2.useEffect)(() => {
    if (!batch) {
      return;
    }
    setPhase("start");
    let inner = 0;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setPhase("end"));
    });
    return () => {
      cancelAnimationFrame(outer);
      cancelAnimationFrame(inner);
    };
  }, [nonce, batch]);
  if (!batch) {
    return null;
  }
  const changes = batch.changes.filter((c) => c.slideIndex === activeSlideIndex);
  if (changes.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("style", { children: aiChangeAnimationCss(batch.config) }),
    changes.map((change) => (0, import_jsx_runtime.jsx)(
      "div",
      {
        "data-testid": `ai-change-${change.elementId}`,
        "data-ai-change": change.kind,
        "data-export-ignore": "true",
        style: changeGhostStyle(change, phase, batch.config)
      },
      `ai-change-${change.elementId}-${nonce}`
    ))
  ] });
}
var THEME_EDITOR_CARD = "rounded bg-muted/50 p-2.5 space-y-2";
var THEME_EDITOR_HEADING = "text-[11px] font-medium uppercase tracking-wide text-muted-foreground mb-1.5";
var THEME_EDITOR_INPUT = "w-full rounded border border-border bg-background px-2 py-1 text-xs text-foreground focus:border-primary focus:outline-none";
var THEME_EDITOR_BTN = "rounded bg-primary px-3 py-1.5 text-xs font-medium text-white hover:bg-primary/80 disabled:opacity-40 disabled:cursor-not-allowed transition-colors";
var THEME_EDITOR_BTN_SECONDARY = "rounded bg-accent px-3 py-1.5 text-xs font-medium text-foreground hover:bg-accent/80 disabled:opacity-40 disabled:cursor-not-allowed transition-colors";
function ThemeColorSchemeEditor({
  editColors,
  canEdit,
  activePickerKey,
  majorFont,
  minorFont,
  onSetActivePickerKey,
  onColorChange,
  onMajorFontChange,
  onMinorFontChange
}) {
  const { t } = useTranslation();
  const slotLabel = (0, import_react2.useCallback)((key) => themeColorLabel(key, t), [t]);
  const previewGrid = (0, import_react2.useMemo)(() => buildThemeColorGrid(editColors, t), [editColors, t]);
  const handleColorText = (0, import_react2.useCallback)(
    (val) => {
      if (activePickerKey && /^#[0-9a-fA-F]{6}$/.test(val)) {
        onColorChange(activePickerKey, val);
      }
    },
    [activePickerKey, onColorChange]
  );
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)("div", { className: THEME_EDITOR_CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.colorScheme") }),
      (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-4 gap-1.5", children: THEME_COLOR_SCHEME_KEYS.map((key) => (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col items-center gap-0.5", children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            disabled: !canEdit,
            className: `h-6 w-full rounded-sm border transition-colors ${activePickerKey === key ? "border-primary ring-1 ring-primary" : "border-border hover:border-muted-foreground"} disabled:opacity-40 disabled:cursor-not-allowed`,
            style: { backgroundColor: editColors[key] },
            title: `${slotLabel(key)}: ${editColors[key]}`,
            "aria-label": `${slotLabel(key)}: ${editColors[key]}`,
            onClick: () => onSetActivePickerKey(activePickerKey === key ? null : key)
          }
        ),
        (0, import_jsx_runtime.jsx)("span", { className: "text-[9px] text-muted-foreground truncate w-full text-center", children: slotLabel(key) })
      ] }, key)) }),
      activePickerKey && (0, import_jsx_runtime.jsxs)("div", { className: "mt-2 flex items-center gap-2 rounded bg-background p-2", children: [
        (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground min-w-[60px]", children: slotLabel(activePickerKey) }),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "color",
            disabled: !canEdit,
            className: "h-6 w-8 cursor-pointer rounded border-0 bg-transparent p-0",
            value: editColors[activePickerKey],
            onChange: (e) => onColorChange(activePickerKey, e.target.value)
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            disabled: !canEdit,
            className: "flex-1 rounded border border-border bg-muted px-1.5 py-0.5 text-[10px] text-foreground font-mono",
            value: editColors[activePickerKey],
            onChange: (e) => handleColorText(e.target.value)
          }
        )
      ] })
    ] }),
    previewGrid && (0, import_jsx_runtime.jsxs)("div", { className: THEME_EDITOR_CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.preview") }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex flex-col gap-px", children: previewGrid.map((row, rowIdx) => (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-12 gap-px", children: row.map((cell) => (0, import_jsx_runtime.jsx)(
        "div",
        {
          className: "h-3.5 rounded-sm",
          style: { backgroundColor: cell.hex },
          title: `${cell.colLabel} -- ${cell.rowLabel} (${cell.hex})`
        },
        `${cell.schemeKey}-${rowIdx}`
      )) }, rowIdx)) })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: THEME_EDITOR_CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.fonts") }),
      (0, import_jsx_runtime.jsxs)("div", { className: "space-y-1.5", children: [
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.themeEditor.headingFont") }),
          (0, import_jsx_runtime.jsxs)(
            "select",
            {
              "aria-label": t("pptx.themeEditor.headingFont"),
              className: THEME_EDITOR_INPUT,
              disabled: !canEdit,
              value: COMMON_FONTS.includes(majorFont) ? majorFont : "__custom__",
              onChange: (e) => {
                if (e.target.value !== "__custom__") {
                  onMajorFontChange(e.target.value);
                }
              },
              children: [
                COMMON_FONTS.map((f) => (0, import_jsx_runtime.jsx)("option", { value: f, children: f }, f)),
                !COMMON_FONTS.includes(majorFont) && (0, import_jsx_runtime.jsx)("option", { value: "__custom__", children: majorFont })
              ]
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("label", { className: "flex flex-col gap-0.5", children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground", children: t("pptx.themeEditor.bodyFont") }),
          (0, import_jsx_runtime.jsxs)(
            "select",
            {
              "aria-label": t("pptx.themeEditor.bodyFont"),
              className: THEME_EDITOR_INPUT,
              disabled: !canEdit,
              value: COMMON_FONTS.includes(minorFont) ? minorFont : "__custom__",
              onChange: (e) => {
                if (e.target.value !== "__custom__") {
                  onMinorFontChange(e.target.value);
                }
              },
              children: [
                COMMON_FONTS.map((f) => (0, import_jsx_runtime.jsx)("option", { value: f, children: f }, f)),
                !COMMON_FONTS.includes(minorFont) && (0, import_jsx_runtime.jsx)("option", { value: "__custom__", children: minorFont })
              ]
            }
          )
        ] }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2 pt-1 text-[10px] text-muted-foreground", children: [
          (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: majorFont }, className: "text-foreground", children: t("pptx.themeEditor.headingSample") }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-muted-foreground", children: "|" }),
          (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: minorFont }, className: "text-foreground", children: t("pptx.themeEditor.bodySample") })
        ] })
      ] })
    ] })
  ] });
}
var ACCENT_KEYS = ["accent1", "accent2", "accent3", "accent4", "accent5", "accent6"];
function ThemePresetGallery({
  canEdit,
  currentThemeName,
  onSelectPreset
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: THEME_EDITOR_CARD, children: [
    (0, import_jsx_runtime.jsx)("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.presetThemes") }),
    (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-1.5", children: PRESET_THEMES.map((preset) => (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        disabled: !canEdit,
        className: `flex flex-col items-start gap-1 rounded border p-1.5 text-left transition-colors ${currentThemeName === preset.name ? "border-primary bg-primary/10" : "border-border hover:border-muted-foreground hover:bg-muted"} disabled:opacity-40 disabled:cursor-not-allowed`,
        onClick: () => onSelectPreset(preset),
        title: preset.name,
        children: [
          (0, import_jsx_runtime.jsx)("div", { className: "flex gap-px w-full", children: ACCENT_KEYS.map((key) => (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "h-3 flex-1 first:rounded-l-sm last:rounded-r-sm",
              style: { backgroundColor: preset.colorScheme[key] }
            },
            key
          )) }),
          (0, import_jsx_runtime.jsx)("span", { className: "text-[10px] text-muted-foreground truncate w-full", children: preset.name })
        ]
      },
      preset.name
    )) })
  ] });
}
function ThemeEditorPanel({
  theme,
  canEdit,
  onUpdateColorScheme,
  onUpdateFontScheme,
  onUpdateThemeName,
  onApplyToPresentation,
  onClose
}) {
  const { t } = useTranslation();
  const [editColors, setEditColors] = (0, import_react2.useState)(
    () => (theme == null ? void 0 : theme.colorScheme) ?? PRESET_THEMES[0].colorScheme
  );
  const [majorFont, setMajorFont] = (0, import_react2.useState)(
    () => {
      var _a, _b2;
      return ((_b2 = (_a = theme == null ? void 0 : theme.fontScheme) == null ? void 0 : _a.majorFont) == null ? void 0 : _b2.latin) ?? "Calibri Light";
    }
  );
  const [minorFont, setMinorFont] = (0, import_react2.useState)(
    () => {
      var _a, _b2;
      return ((_b2 = (_a = theme == null ? void 0 : theme.fontScheme) == null ? void 0 : _a.minorFont) == null ? void 0 : _b2.latin) ?? "Calibri";
    }
  );
  const [themeName, setThemeName] = (0, import_react2.useState)(() => (theme == null ? void 0 : theme.name) ?? "Custom Theme");
  const [activePickerKey, setActivePickerKey] = (0, import_react2.useState)(null);
  const handleColorChange = (0, import_react2.useCallback)(
    (key, hex) => {
      const next = { ...editColors, [key]: hex };
      setEditColors(next);
      onUpdateColorScheme(next);
    },
    [editColors, onUpdateColorScheme]
  );
  const handleSelectPreset = (0, import_react2.useCallback)(
    (preset) => {
      setEditColors(preset.colorScheme);
      setMajorFont(preset.majorFont);
      setMinorFont(preset.minorFont);
      setThemeName(preset.name);
      onUpdateColorScheme(preset.colorScheme);
      onUpdateFontScheme({
        majorFont: { latin: preset.majorFont },
        minorFont: { latin: preset.minorFont }
      });
      onUpdateThemeName(preset.name);
    },
    [onUpdateColorScheme, onUpdateFontScheme, onUpdateThemeName]
  );
  const handleReset = (0, import_react2.useCallback)(() => {
    var _a, _b2, _c, _d, _e, _f, _g, _h;
    if (!(theme == null ? void 0 : theme.colorScheme)) {
      return;
    }
    setEditColors(theme.colorScheme);
    setMajorFont(((_b2 = (_a = theme.fontScheme) == null ? void 0 : _a.majorFont) == null ? void 0 : _b2.latin) ?? "Calibri Light");
    setMinorFont(((_d = (_c = theme.fontScheme) == null ? void 0 : _c.minorFont) == null ? void 0 : _d.latin) ?? "Calibri");
    setThemeName(theme.name ?? "Custom Theme");
    onUpdateColorScheme(theme.colorScheme);
    onUpdateFontScheme({
      majorFont: {
        latin: ((_f = (_e = theme.fontScheme) == null ? void 0 : _e.majorFont) == null ? void 0 : _f.latin) ?? "Calibri Light"
      },
      minorFont: { latin: ((_h = (_g = theme.fontScheme) == null ? void 0 : _g.minorFont) == null ? void 0 : _h.latin) ?? "Calibri" }
    });
    onUpdateThemeName(theme.name ?? "Custom Theme");
  }, [theme, onUpdateColorScheme, onUpdateFontScheme, onUpdateThemeName]);
  const handleMajorFontChange = (0, import_react2.useCallback)(
    (font) => {
      setMajorFont(font);
      onUpdateFontScheme({
        majorFont: { latin: font },
        minorFont: { latin: minorFont }
      });
    },
    [minorFont, onUpdateFontScheme]
  );
  const handleMinorFontChange = (0, import_react2.useCallback)(
    (font) => {
      setMinorFont(font);
      onUpdateFontScheme({
        majorFont: { latin: majorFont },
        minorFont: { latin: font }
      });
    },
    [majorFont, onUpdateFontScheme]
  );
  const handleNameChange = (0, import_react2.useCallback)(
    (name) => {
      setThemeName(name);
      onUpdateThemeName(name);
    },
    [onUpdateThemeName]
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col gap-2 overflow-y-auto max-h-[calc(100vh-120px)]", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between", children: [
      (0, import_jsx_runtime.jsx)("h3", { className: "text-sm font-semibold text-foreground", children: t("pptx.themeEditor.title") }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "rounded p-1 text-muted-foreground hover:bg-accent hover:text-foreground",
          title: t("pptx.themeEditor.close"),
          "aria-label": t("pptx.themeEditor.close"),
          children: (0, import_jsx_runtime.jsx)(
            "svg",
            {
              className: "h-4 w-4",
              fill: "none",
              viewBox: "0 0 24 24",
              stroke: "currentColor",
              strokeWidth: 2,
              children: (0, import_jsx_runtime.jsx)("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M6 18L18 6M6 6l12 12" })
            }
          )
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: THEME_EDITOR_CARD, children: [
      (0, import_jsx_runtime.jsx)("div", { className: THEME_EDITOR_HEADING, children: t("pptx.themeEditor.themeName") }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          className: THEME_EDITOR_INPUT,
          disabled: !canEdit,
          value: themeName,
          onChange: (e) => handleNameChange(e.target.value),
          placeholder: t("pptx.themeEditor.themeNamePlaceholder")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(
      ThemePresetGallery,
      {
        canEdit,
        currentThemeName: themeName,
        onSelectPreset: handleSelectPreset
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ThemeColorSchemeEditor,
      {
        editColors,
        canEdit,
        activePickerKey,
        majorFont,
        minorFont,
        onSetActivePickerKey: setActivePickerKey,
        onColorChange: handleColorChange,
        onMajorFontChange: handleMajorFontChange,
        onMinorFontChange: handleMinorFontChange
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex gap-1.5 pt-1", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: THEME_EDITOR_BTN,
          onClick: onApplyToPresentation,
          children: t("pptx.themeEditor.applyToPresentation")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          disabled: !canEdit,
          className: THEME_EDITOR_BTN_SECONDARY,
          onClick: handleReset,
          children: t("pptx.themeEditor.reset")
        }
      )
    ] })
  ] });
}
function MobileDismissSheet({
  onClose,
  className,
  children
}) {
  const { dragY, handlers } = useSheetDismissDrag(onClose);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        "aria-label": "Close",
        onClick: onClose,
        className: "md:hidden fixed inset-0 z-20 bg-black/40 backdrop-blur-[2px] animate-in fade-in duration-150"
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className,
        style: dragY > 0 ? { transform: `translateY(${dragY}px)`, transition: "none" } : void 0,
        children: [
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              className: "md:hidden flex items-center justify-center pt-2 pb-1 cursor-grab active:cursor-grabbing touch-none",
              onPointerDown: handlers.onPointerDown,
              onPointerMove: handlers.onPointerMove,
              onPointerUp: handlers.onPointerUp,
              onPointerCancel: handlers.onPointerCancel,
              children: (0, import_jsx_runtime.jsx)("div", { className: "h-1 w-10 rounded-full bg-muted-foreground/40" })
            }
          ),
          children
        ]
      }
    )
  ] });
}
var BUILT_IN_THEMES = [
  {
    id: "office",
    name: "Office",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#44546A",
      lt2: "#E7E6E6",
      accent1: "#4472C4",
      accent2: "#ED7D31",
      accent3: "#A5A5A5",
      accent4: "#FFC000",
      accent5: "#5B9BD5",
      accent6: "#70AD47",
      hlink: "#0563C1",
      folHlink: "#954F72"
    },
    fontScheme: {
      majorFont: "Calibri Light",
      minorFont: "Calibri"
    }
  },
  {
    id: "facet",
    name: "Facet",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#2C3C43",
      lt2: "#CFD7D9",
      accent1: "#90C226",
      accent2: "#54A021",
      accent3: "#E6B91E",
      accent4: "#E76618",
      accent5: "#C42F1A",
      accent6: "#918655",
      hlink: "#99CA3C",
      folHlink: "#B9D181"
    },
    fontScheme: {
      majorFont: "Trebuchet MS",
      minorFont: "Trebuchet MS"
    }
  },
  {
    id: "integral",
    name: "Integral",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#335B74",
      lt2: "#DFE3E5",
      accent1: "#1CADE4",
      accent2: "#2683C6",
      accent3: "#27CED7",
      accent4: "#42BA97",
      accent5: "#3E8853",
      accent6: "#62A39F",
      hlink: "#6BA5D1",
      folHlink: "#9DCCE0"
    },
    fontScheme: {
      majorFont: "Tw Cen MT Condensed",
      minorFont: "Tw Cen MT"
    }
  },
  {
    id: "ion",
    name: "Ion",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#262626",
      lt2: "#E1E1E1",
      accent1: "#B01513",
      accent2: "#EA6312",
      accent3: "#E6B729",
      accent4: "#6AAC90",
      accent5: "#5F9C9D",
      accent6: "#9B6BF2",
      hlink: "#F09415",
      folHlink: "#D14D0F"
    },
    fontScheme: {
      majorFont: "Century Gothic",
      minorFont: "Century Gothic"
    }
  },
  {
    id: "retrospect",
    name: "Retrospect",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#637052",
      lt2: "#CCDDEA",
      accent1: "#E48312",
      accent2: "#BD582C",
      accent3: "#865640",
      accent4: "#9B8357",
      accent5: "#C2BC80",
      accent6: "#94A088",
      hlink: "#3A7CCB",
      folHlink: "#8C8C8C"
    },
    fontScheme: {
      majorFont: "Calibri Light",
      minorFont: "Calibri"
    }
  },
  {
    id: "organic",
    name: "Organic",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#283138",
      lt2: "#E3DED1",
      accent1: "#83992A",
      accent2: "#3C9770",
      accent3: "#44709D",
      accent4: "#A23C33",
      accent5: "#D97828",
      accent6: "#DEB340",
      hlink: "#A8B97F",
      folHlink: "#89C2A3"
    },
    fontScheme: {
      majorFont: "Garamond",
      minorFont: "Garamond"
    }
  },
  {
    id: "wisp",
    name: "Wisp",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#46393B",
      lt2: "#E8DCD8",
      accent1: "#A53010",
      accent2: "#DE7E18",
      accent3: "#9F8351",
      accent4: "#728653",
      accent5: "#92AA4C",
      accent6: "#6AAC91",
      hlink: "#FB4F14",
      folHlink: "#E25839"
    },
    fontScheme: {
      majorFont: "Century Gothic",
      minorFont: "Century Gothic"
    }
  },
  {
    id: "berlin",
    name: "Berlin",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#3E5C61",
      lt2: "#D5E0D0",
      accent1: "#F09415",
      accent2: "#C1B56B",
      accent3: "#89C2A3",
      accent4: "#729BBE",
      accent5: "#9A90C2",
      accent6: "#BE8DBE",
      hlink: "#25A0DA",
      folHlink: "#FF7F00"
    },
    fontScheme: {
      majorFont: "Trebuchet MS",
      minorFont: "Trebuchet MS"
    }
  },
  {
    id: "slice",
    name: "Slice",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#2E3B4C",
      lt2: "#C7CDD3",
      accent1: "#E2751D",
      accent2: "#C53B23",
      accent3: "#A13D69",
      accent4: "#58599E",
      accent5: "#36749D",
      accent6: "#4EA39A",
      hlink: "#FD6B32",
      folHlink: "#D14D0F"
    },
    fontScheme: {
      majorFont: "Century Gothic",
      minorFont: "Century Gothic"
    }
  },
  {
    id: "dividend",
    name: "Dividend",
    colorScheme: {
      dk1: "#000000",
      lt1: "#FFFFFF",
      dk2: "#3D3D3D",
      lt2: "#E1E1E1",
      accent1: "#4D1434",
      accent2: "#903163",
      accent3: "#B2324B",
      accent4: "#D34817",
      accent5: "#E8971C",
      accent6: "#FDCB00",
      hlink: "#A03068",
      folHlink: "#C34882"
    },
    fontScheme: {
      majorFont: "Gill Sans MT",
      minorFont: "Gill Sans MT"
    }
  }
];
function ThemeThumbnail({
  theme,
  selected,
  onClick
}) {
  const { colorScheme } = theme;
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type: "button",
      onClick,
      className: cn(
        "group relative flex flex-col rounded-lg border-2 transition-all overflow-hidden",
        selected ? "border-primary shadow-lg scale-[1.02]" : "border-border hover:border-primary/50 hover:shadow-md"
      ),
      title: theme.name,
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "h-24 flex flex-col", children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "h-10 flex", children: [
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: colorScheme.accent1 } }),
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: colorScheme.accent2 } }),
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: colorScheme.accent3 } })
          ] }),
          (0, import_jsx_runtime.jsxs)("div", { className: "flex-1 flex", children: [
            (0, import_jsx_runtime.jsx)("div", { className: "w-1/3", style: { backgroundColor: colorScheme.dk2 } }),
            (0, import_jsx_runtime.jsx)("div", { className: "flex-1", style: { backgroundColor: colorScheme.lt2 } })
          ] })
        ] }),
        (0, import_jsx_runtime.jsx)("div", { className: "bg-background border-t border-border px-2 py-1.5", children: (0, import_jsx_runtime.jsx)("p", { className: "text-xs font-medium text-foreground text-center", children: theme.name }) }),
        selected && (0, import_jsx_runtime.jsx)("div", { className: "absolute top-1 right-1 bg-primary text-white rounded-full p-1", children: (0, import_jsx_runtime.jsx)(LuCheck, { className: "w-3 h-3" }) })
      ]
    }
  );
}
function ThemeGallery({
  open,
  currentTheme,
  canEdit,
  onClose,
  onApplyTheme,
  onImportTheme
}) {
  const { t } = useTranslation();
  const [selectedTheme, setSelectedTheme] = (0, import_react2.useState)(currentTheme ?? null);
  const fileInputRef = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    if (open) {
      setSelectedTheme(currentTheme ?? null);
    }
  }, [currentTheme, open]);
  if (!open) {
    return null;
  }
  const handleApply = () => {
    if (selectedTheme) {
      onApplyTheme(selectedTheme);
      onClose();
    }
  };
  const handleImportClick = () => {
    var _a;
    (_a = fileInputRef.current) == null ? void 0 : _a.click();
  };
  const handleFileChange = (event) => {
    var _a;
    const file = (_a = event.target.files) == null ? void 0 : _a[0];
    if (file && onImportTheme) {
      onImportTheme(file);
    }
    event.target.value = "";
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        className: "fixed inset-0 z-[100] bg-black/60 backdrop-blur-sm",
        onClick: onClose,
        "aria-label": t("pptx.common.close")
      }
    ),
    (0, import_jsx_runtime.jsx)("div", { className: "fixed inset-0 z-[101] flex items-center justify-center p-4", children: (0, import_jsx_runtime.jsxs)("div", { className: "bg-background border border-border rounded-xl shadow-2xl max-w-5xl w-full max-h-[90vh] flex flex-col", children: [
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-6 py-4 border-b border-border", children: [
        (0, import_jsx_runtime.jsxs)("div", { children: [
          (0, import_jsx_runtime.jsx)("h2", { className: "text-lg font-semibold text-foreground", children: t("pptx.themes.gallery.title") }),
          (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground mt-0.5", children: t("pptx.themes.gallery.description") })
        ] }),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            onClick: onClose,
            className: "p-2 rounded hover:bg-accent transition-colors",
            "aria-label": t("pptx.common.close"),
            children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-5 h-5" })
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)("div", { className: "flex-1 overflow-auto p-6", children: (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4", children: BUILT_IN_THEMES.map((theme) => (0, import_jsx_runtime.jsx)(
        ThemeThumbnail,
        {
          theme,
          selected: (selectedTheme == null ? void 0 : selectedTheme.id) === theme.id,
          onClick: () => setSelectedTheme(theme)
        },
        theme.id
      )) }) }),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-6 py-4 border-t border-border", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "flex items-center gap-2", children: onImportTheme && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsxs)(
            "button",
            {
              type: "button",
              onClick: handleImportClick,
              disabled: !canEdit,
              className: "inline-flex items-center gap-2 px-3 py-1.5 rounded bg-accent hover:bg-accent/80 text-xs font-medium text-foreground transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
              children: [
                (0, import_jsx_runtime.jsx)(LuUpload, { className: "w-3.5 h-3.5" }),
                t("pptx.themes.gallery.importTheme")
              ]
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              ref: fileInputRef,
              type: "file",
              accept: ".thmx,.pptx",
              className: "hidden",
              onChange: handleFileChange
            }
          )
        ] }) }),
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: onClose,
              className: "px-3 py-1.5 rounded bg-accent hover:bg-accent/80 text-xs font-medium text-foreground transition-colors",
              children: t("pptx.common.cancel")
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              onClick: handleApply,
              disabled: !canEdit || !selectedTheme,
              className: "px-3 py-1.5 rounded bg-primary hover:bg-primary/80 text-xs font-medium text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed",
              children: t("pptx.common.apply")
            }
          )
        ] })
      ] })
    ] }) })
  ] });
}
function ViewerSidePanels(props) {
  const {
    mode,
    canEdit,
    activeSlide,
    masterPseudoSlide,
    slides,
    canvasSize,
    activeSlideIndex,
    selectedElement,
    state: s,
    comments,
    ops,
    manipulation,
    propertyHandlers,
    themeHandlers,
    history,
    panelWidth,
    onResizeRight,
    aiConfig,
    aiBridge,
    aiPanel
  } = props;
  const handleSetSlideSize = (size, rescaleMode) => {
    if (rescaleMode && s.slideSizeEmu) {
      s.setSlides(scaleSlidesForSizeChange(slides, s.slideSizeEmu, size, rescaleMode));
    }
    s.setSlideSizeEmu(size);
    s.setCanvasSize(slideSizeToCanvasPx(size));
    history.markDirty();
  };
  const { handleTableStyleMapChange, handleDeleteTableStyle } = useTableStyleMapHandlers({
    tableStyleMap: s.tableStyleMap,
    setTableStyleMap: s.setTableStyleMap,
    tableStylesToDelete: s.tableStylesToDelete,
    setTableStylesToDelete: s.setTableStylesToDelete
  });
  const effectiveSlide = mode === "master" ? masterPseudoSlide : activeSlide;
  const currentBuiltInTheme = BUILT_IN_THEMES.find(
    (candidate) => {
      var _a;
      return themeColorSchemesEqual((_a = s.theme) == null ? void 0 : _a.colorScheme, candidate.colorScheme);
    }
  ) ?? null;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (mode === "edit" || mode === "master") && s.isInspectorPaneOpen && onResizeRight && (0, import_jsx_runtime.jsx)(ResizeHandle, { direction: "horizontal", onResize: onResizeRight }),
    (0, import_jsx_runtime.jsx)(
      ViewerInspector,
      {
        isOpen: (mode === "edit" || mode === "master") && s.isInspectorPaneOpen,
        canEdit,
        mode,
        activeSlide: effectiveSlide,
        slides,
        customShows: s.customShows,
        canvasSize,
        selectedElement,
        effectiveSelectedIds: s.effectiveSelectedIds,
        tableEditorState: s.tableEditorState,
        sidebarPanelMode: s.sidebarPanelMode,
        activeSlideIndex,
        comments,
        commentAuthors: s.modernCommentAuthors,
        onSetSidebarPanelMode: s.setSidebarPanelMode,
        onClose: () => s.setIsInspectorPaneOpen(false),
        onUpdateElementStyle: ops.updateSelectedShapeStyle,
        onUpdateTextStyle: ops.updateSelectedTextStyle,
        onUpdateElement: ops.updateSelectedElement,
        onApplySelection: ops.applySelection,
        onSetCanvasSize: s.setCanvasSize,
        slideSizeEmu: s.slideSizeEmu,
        onSetSlideSize: handleSetSlideSize,
        onMoveLayer: manipulation.handleMoveLayer,
        onMoveLayerToEdge: manipulation.handleMoveLayerToEdge,
        onDeleteElement: manipulation.handleDelete,
        onUpdateSlide: propertyHandlers.handleUpdateSlide,
        presentationProperties: s.presentationProperties,
        onUpdatePresentationProperties: propertyHandlers.handleUpdatePresentationProperties,
        editTemplateMode: s.editTemplateMode,
        slideMasters: s.slideMasters,
        themeOptions: s.themeOptions,
        notesMaster: s.notesMaster,
        handoutMaster: s.handoutMaster,
        notesCanvasSize: s.notesCanvasSize,
        coreProperties: s.coreProperties,
        appProperties: s.appProperties,
        customProperties: s.customProperties,
        tagCollections: s.tagCollections,
        onUpdateTagCollections: s.setTagCollections,
        onUpdateCoreProperties: propertyHandlers.handleUpdateCoreProperties,
        onUpdateAppProperties: propertyHandlers.handleUpdateAppProperties,
        onUpdateCustomProperties: propertyHandlers.handleUpdateCustomProperties,
        onApplyTheme: themeHandlers.handleApplyTheme,
        onSetTemplateBackground: themeHandlers.handleSetTemplateBackground,
        onGetTemplateBackgroundColor: themeHandlers.handleGetTemplateBackgroundColor,
        mediaDataUrls: s.mediaDataUrls,
        theme: s.theme,
        tableStyleMap: s.tableStyleMap,
        onTableStyleMapChange: handleTableStyleMapChange,
        onDeleteTableStyle: handleDeleteTableStyle,
        panelWidth
      }
    ),
    s.isSelectionPaneOpen && (mode === "edit" || mode === "master") && (0, import_jsx_runtime.jsx)(
      MobileDismissSheet,
      {
        onClose: () => s.setIsSelectionPaneOpen(false),
        className: "absolute right-0 top-0 z-30 h-full max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:h-auto max-md:max-h-[50vh] max-md:rounded-t-xl max-md:border-t max-md:border-border max-md:shadow-2xl max-md:bg-background",
        children: (0, import_jsx_runtime.jsx)(
          SelectionPane,
          {
            slides,
            activeSlideIndex,
            selectedElementId: s.selectedElementId,
            selectedElementIds: s.selectedElementIds,
            canEdit,
            setSelectedElementId: s.setSelectedElementId,
            setSelectedElementIds: s.setSelectedElementIds,
            setSlides: s.setSlides,
            markDirty: history.markDirty,
            onClose: () => s.setIsSelectionPaneOpen(false)
          }
        )
      }
    ),
    s.isThemeEditorOpen && mode === "edit" && (0, import_jsx_runtime.jsx)(
      MobileDismissSheet,
      {
        onClose: () => s.setIsThemeEditorOpen(false),
        className: "absolute right-0 top-0 z-30 h-full w-72 overflow-y-auto border-l border-border bg-card p-2.5 shadow-xl max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:w-full max-md:h-auto max-md:max-h-[60vh] max-md:rounded-t-xl max-md:border-t max-md:border-l-0",
        children: (0, import_jsx_runtime.jsx)(
          ThemeEditorPanel,
          {
            theme: s.theme,
            canEdit,
            onUpdateColorScheme: themeHandlers.handleUpdateThemeColorScheme,
            onUpdateFontScheme: themeHandlers.handleUpdateThemeFontScheme,
            onUpdateThemeName: themeHandlers.handleUpdateThemeName,
            onApplyToPresentation: themeHandlers.handleApplyThemeToPresentation,
            onClose: () => s.setIsThemeEditorOpen(false)
          }
        )
      }
    ),
    (aiPanel == null ? void 0 : aiPanel.isOpen) && aiConfig && aiBridge && (mode === "edit" || mode === "master") && (0, import_jsx_runtime.jsx)(
      AiChatPanelLazy,
      {
        bridge: aiBridge,
        config: aiConfig,
        aiPanel,
        onClose: aiPanel.close,
        panelWidth
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ThemeGallery,
      {
        open: s.isThemeGalleryOpen,
        currentTheme: currentBuiltInTheme,
        canEdit,
        onClose: () => s.setIsThemeGalleryOpen(false),
        onApplyTheme: (theme) => {
          themeHandlers.handleApplyThemeData(
            theme.colorScheme,
            {
              majorFont: {
                latin: theme.fontScheme.majorFont,
                eastAsia: theme.fontScheme.majorFont,
                complexScript: theme.fontScheme.majorFont
              },
              minorFont: {
                latin: theme.fontScheme.minorFont,
                eastAsia: theme.fontScheme.minorFont,
                complexScript: theme.fontScheme.minorFont
              }
            },
            theme.name
          );
        }
      }
    )
  ] });
}
function ViewerMainContent(props) {
  var _a;
  const {
    mode,
    canEdit,
    slides,
    activeSlide,
    masterPseudoSlide,
    activeSlideIndex,
    canvasSize,
    gridSpacingPx,
    slideSectionGroups,
    showSlidesPane,
    showMasterPane,
    selectedElement,
    state,
    editorOps,
    dialogs,
    presentation,
    annotations,
    propertyHandlers,
    themeHandlers,
    history,
    comments,
    masterViewCrud,
    zoom,
    isMobile: _isMobile = false,
    isTouchDevice: _isTouchDevice = false,
    onEndPresentation,
    leftPanelWidth,
    onResizeLeft,
    rightPanelWidth,
    onResizeRight,
    hiddenActions,
    aiConfig,
    aiBridge,
    aiPanel
  } = props;
  const {
    ops,
    sectionOps,
    canvasHandlers,
    insertHandlers,
    manipulation,
    slideOps,
    tableOps,
    findReplace
  } = editorOps;
  const hf = state.headerFooter;
  const thumbnailFieldContext = (0, import_react2.useMemo)(
    () => ({
      dateTimeText: hf.dateTimeText,
      dateFormat: hf.dateFormat,
      footerText: hf.footerText,
      headerText: hf.headerText,
      customProperties: state.customProperties.map((p) => ({ name: p.name, value: p.value }))
    }),
    [hf.dateTimeText, hf.dateFormat, hf.footerText, hf.headerText, state.customProperties]
  );
  const thumbnailTableStyleContext = (0, import_react2.useMemo)(
    () => state.theme || state.tableStyleMap ? { theme: state.theme, tableStyleMap: state.tableStyleMap } : void 0,
    [state.theme, state.tableStyleMap]
  );
  return (0, import_jsx_runtime.jsx)(ChartPartSelectionProvider, { children: (0, import_jsx_runtime.jsxs)("div", { className: "relative z-10 flex flex-1 min-h-0", children: [
    showSlidesPane && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        SlidesPaneSidebar,
        {
          slides,
          templateElementsBySlideId: state.templateElementsBySlideId,
          activeSlideIndex,
          canvasSize,
          sectionGroups: slideSectionGroups,
          isOpen: state.isSlidesPaneOpen,
          canEdit,
          onSelectSlide: state.setActiveSlideIndex,
          onSlideContextMenu: slideOps.handleSlideContextMenu,
          onMoveSlide: slideOps.handleMoveSlide,
          onAddSlide: slideOps.handleAddSlide,
          onCollapse: () => state.setIsSlidesPaneOpen(false),
          onAddSection: sectionOps.addSection,
          onRenameSection: sectionOps.renameSection,
          onDeleteSection: sectionOps.deleteSection,
          onMoveSectionUp: sectionOps.moveSectionUp,
          onMoveSectionDown: sectionOps.moveSectionDown,
          onToggleSectionCollapse: sectionOps.toggleSectionCollapse,
          rehearsalTimings: Object.keys(presentation.recordedTimings).length > 0 ? presentation.recordedTimings : void 0,
          panelWidth: leftPanelWidth,
          fieldContext: thumbnailFieldContext,
          tableStyleContext: thumbnailTableStyleContext
        }
      ),
      onResizeLeft && (0, import_jsx_runtime.jsx)(ResizeHandle, { direction: "horizontal", onResize: onResizeLeft })
    ] }),
    showMasterPane && (0, import_jsx_runtime.jsx)(
      MasterViewSidebar,
      {
        slideMasters: state.slideMasters,
        activeMasterIndex: state.activeMasterIndex,
        activeLayoutIndex: state.activeLayoutIndex,
        canvasSize,
        masterViewTab: state.masterViewTab,
        notesMaster: state.notesMaster,
        handoutMaster: state.handoutMaster,
        handoutSlidesPerPage: ((_a = state.handoutMaster) == null ? void 0 : _a.slidesPerPage) ?? state.handoutSlidesPerPage,
        onSelectMaster: dialogs.handleSelectMaster,
        onSelectLayout: dialogs.handleSelectLayout,
        onCollapse: dialogs.handleCloseMasterView,
        onTabChange: state.setMasterViewTab,
        crudActions: masterViewCrud.crudActions,
        onCrudAction: masterViewCrud.handleCrudAction,
        onHandoutSlidesPerPageChange: (count) => {
          state.setHandoutSlidesPerPage(count);
          state.setHandoutMaster(
            (master) => master ? { ...master, slidesPerPage: count } : master
          );
          state.setIsDirty(true);
        },
        onNotesMasterBackgroundChange: (backgroundColor) => {
          state.setNotesMaster((master) => master ? { ...master, backgroundColor } : master);
          state.setIsDirty(true);
        },
        onHandoutMasterBackgroundChange: (backgroundColor) => {
          state.setHandoutMaster(
            (master) => master ? { ...master, backgroundColor } : master
          );
          state.setIsDirty(true);
        },
        canEdit,
        onSlidesBackgroundChange: (backgroundColor) => {
          const write = setMasterViewBackgroundColor(
            { slideMasters: state.slideMasters },
            {
              tab: "slides",
              masterIndex: state.activeMasterIndex,
              layoutIndex: state.activeLayoutIndex
            },
            backgroundColor
          );
          if (write == null ? void 0 : write.slideMasters) {
            state.setSlideMasters(write.slideMasters);
            state.setIsDirty(true);
          }
        }
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ViewerCanvasArea,
      {
        onUpdateSlideAnimations: (animations) => propertyHandlers.handleUpdateSlide({ animations }),
        mode,
        canEdit,
        slides,
        activeSlide,
        masterPseudoSlide,
        templateElements: state.templateElements,
        canvasSize,
        activeSlideIndex,
        gridSpacingPx,
        zoom,
        state,
        selectedElement,
        canvasHandlers,
        insertHandlers,
        tableOps,
        annotations,
        presentation,
        onEndPresentation,
        findReplace,
        hiddenActions,
        aiPickMode: aiPanel == null ? void 0 : aiPanel.pickMode,
        onAiPickElement: aiPanel ? aiPanel.addPick : void 0,
        aiCanvasActive: aiPanel == null ? void 0 : aiPanel.canvasAnimating,
        aiHighlightOverlay: aiPanel ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)(
            AiFocusHighlightOverlay,
            {
              highlights: aiPanel.canvasHighlights,
              elements: (activeSlide == null ? void 0 : activeSlide.elements) ?? [],
              activeSlideIndex
            }
          ),
          (0, import_jsx_runtime.jsx)(AiChangeOverlay, { batch: aiPanel.changeBatch, activeSlideIndex })
        ] }) : void 0
      }
    ),
    state.contextMenuState && (0, import_jsx_runtime.jsx)(
      ContextMenu,
      {
        contextMenuState: state.contextMenuState,
        mode,
        selectedElement,
        tableEditorState: state.tableEditorState,
        hasMultiSelection: state.effectiveSelectedIds.length > 1,
        selectionGroupable: manipulation.selectionGroupable,
        onAction: manipulation.handleContextMenuAction,
        onInsertTableRow: tableOps.handleInsertTableRow,
        onDeleteTableRow: tableOps.handleDeleteTableRow,
        onInsertTableColumn: tableOps.handleInsertTableColumn,
        onDeleteTableColumn: tableOps.handleDeleteTableColumn,
        onMergeCellRight: tableOps.handleMergeCellRight,
        onMergeCellDown: tableOps.handleMergeCellDown,
        onMergeSelectedCells: tableOps.handleMergeSelectedCells,
        onSplitCell: tableOps.handleSplitCell,
        onAskAi: aiPanel && selectedElement ? () => {
          aiPanel.askAboutSelection();
          state.setContextMenuState(null);
        } : void 0,
        onFixAi: aiPanel && selectedElement ? () => {
          aiPanel.fixSelection();
          state.setContextMenuState(null);
        } : void 0,
        onClose: () => state.setContextMenuState(null)
      }
    ),
    (0, import_jsx_runtime.jsx)(
      ViewerSidePanels,
      {
        mode,
        canEdit,
        activeSlide,
        masterPseudoSlide,
        slides,
        canvasSize,
        activeSlideIndex,
        selectedElement,
        state,
        comments,
        ops,
        manipulation,
        propertyHandlers,
        themeHandlers,
        history,
        panelWidth: rightPanelWidth,
        onResizeRight,
        aiConfig,
        aiBridge,
        aiPanel
      }
    )
  ] }) });
}
function AutosaveRecoveryDialog({
  prompt,
  onRestore,
  onDiscard
}) {
  const { t } = useTranslation();
  if (!prompt) {
    return null;
  }
  const title = t(prompt.titleKey);
  const when = t(prompt.ageKey, prompt.ageParams);
  return (0, import_jsx_runtime.jsx)(
    "div",
    {
      style: { zIndex: 1200 },
      className: "fixed inset-0 flex items-center justify-center bg-black/50",
      "data-pptx-autosave-recovery": "true",
      children: (0, import_jsx_runtime.jsxs)(
        "div",
        {
          role: "dialog",
          "aria-modal": "true",
          "aria-label": title,
          className: "bg-background border border-border rounded-lg shadow-xl w-[440px] max-w-[90vw] p-6",
          children: [
            (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-3 mb-3", children: [
              (0, import_jsx_runtime.jsx)("div", { className: "flex items-center justify-center w-10 h-10 rounded-full bg-primary/10", children: (0, import_jsx_runtime.jsx)(LuHistory, { className: "w-5 h-5 text-primary" }) }),
              (0, import_jsx_runtime.jsx)("h2", { className: "text-base font-semibold text-foreground", children: title })
            ] }),
            (0, import_jsx_runtime.jsx)("p", { className: "text-sm text-muted-foreground", children: t(prompt.messageKey, prompt.messageParams) }),
            (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground mt-2", children: t("pptx.autosave.recovery.savedLabel", { when }) }),
            (0, import_jsx_runtime.jsxs)("div", { className: "flex justify-end gap-2 mt-6", children: [
              (0, import_jsx_runtime.jsxs)(
                "button",
                {
                  type: "button",
                  className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md border border-border bg-background text-foreground hover:bg-accent transition-colors",
                  onClick: onDiscard,
                  children: [
                    (0, import_jsx_runtime.jsx)(LuTrash2, { className: "w-4 h-4" }),
                    t(prompt.discardKey)
                  ]
                }
              ),
              (0, import_jsx_runtime.jsxs)(
                "button",
                {
                  type: "button",
                  className: "inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium rounded-md bg-primary text-white hover:bg-primary/90 transition-colors",
                  onClick: onRestore,
                  children: [
                    (0, import_jsx_runtime.jsx)(LuHistory, { className: "w-4 h-4" }),
                    t(prompt.restoreKey)
                  ]
                }
              )
            ] })
          ]
        }
      )
    }
  );
}
function CompatibilityToasts({ toasts, onDismiss, onDismissAll }) {
  const { t } = useTranslation();
  if (toasts.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "pptx-compat-toasts",
      className: "max-h-[60%] overflow-y-auto",
      style: compatToastStackStyle(),
      children: [
        (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between px-1", style: { pointerEvents: "auto" }, children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-[11px] font-medium uppercase tracking-wide text-muted-foreground", children: t("pptx.compatibility.toastTitle") }),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "data-testid": "pptx-compat-toasts-dismiss-all",
              onClick: onDismissAll,
              className: "text-[11px] text-muted-foreground underline-offset-2 hover:underline",
              children: t("pptx.compatibility.dismissAll")
            }
          )
        ] }),
        toasts.map((toast) => (0, import_jsx_runtime.jsxs)(
          "div",
          {
            "data-testid": "pptx-compat-toast",
            "data-code": toast.code,
            "data-severity": toast.severity,
            className: "flex items-start gap-2 rounded border border-border bg-card p-2 text-[11px] shadow-md",
            style: { pointerEvents: "auto" },
            children: [
              (0, import_jsx_runtime.jsx)("span", { className: "flex-1 min-w-0", children: t(toast.messageKey, toast.params) }),
              (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  "data-testid": "pptx-compat-toast-dismiss",
                  onClick: () => onDismiss(toast.id),
                  "aria-label": t("pptx.compatibility.dismiss"),
                  className: "shrink-0 text-muted-foreground hover:text-foreground",
                  children: "×"
                }
              )
            ]
          },
          toast.id
        ))
      ]
    }
  );
}
function ToggleRow({ checked, onChange, icon, label, testId }) {
  return (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2.5 cursor-pointer group select-none", children: [
    (0, import_jsx_runtime.jsxs)("span", { className: "relative flex items-center justify-center w-4 h-4", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked,
          onChange: (e) => onChange(e.target.checked),
          "data-testid": testId,
          className: "peer sr-only"
        }
      ),
      (0, import_jsx_runtime.jsx)("span", { className: "absolute inset-0 rounded border border-border bg-muted transition-colors peer-checked:border-primary peer-checked:bg-primary peer-focus-visible:ring-2 peer-focus-visible:ring-primary/50" }),
      checked && (0, import_jsx_runtime.jsx)(LuCheck, { className: "relative z-10 w-3 h-3 text-white" })
    ] }),
    (0, import_jsx_runtime.jsxs)("span", { className: "flex items-center gap-1.5 text-xs text-foreground group-hover:text-foreground transition-colors", children: [
      icon,
      label
    ] })
  ] });
}
function HeaderFooterPanel({
  headerFooter,
  onUpdate,
  onApplyToAll,
  onApplyToCurrent,
  onClose
}) {
  const { t } = useTranslation();
  const showDateTime = headerFooter.hasDateTime ?? false;
  const dateTimeAuto = headerFooter.dateTimeAuto ?? false;
  const showDateText = isHeaderFooterDateTextVisible(headerFooter);
  const showSlideNumber = headerFooter.hasSlideNumber ?? false;
  const showHeader = headerFooter.hasHeader ?? false;
  const showHeaderText = isHeaderFooterHeaderTextVisible(headerFooter);
  const showFooter = headerFooter.hasFooter ?? false;
  const showFooterText = isHeaderFooterFooterTextVisible(headerFooter);
  const handleDateTextChange = (0, import_react2.useCallback)(
    (e) => onUpdate({ dateTimeText: e.target.value }),
    [onUpdate]
  );
  const handleHeaderTextChange = (0, import_react2.useCallback)(
    (e) => onUpdate({ headerText: e.target.value }),
    [onUpdate]
  );
  const handleFooterTextChange = (0, import_react2.useCallback)(
    (e) => onUpdate({ footerText: e.target.value }),
    [onUpdate]
  );
  return (0, import_jsx_runtime.jsx)("div", { className: "absolute inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm", children: (0, import_jsx_runtime.jsxs)("div", { className: "w-full max-w-sm rounded-lg border border-border bg-background shadow-2xl", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between border-b border-border px-4 py-3", children: [
      (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.headerFooter.title") }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "rounded p-1 text-muted-foreground hover:bg-muted hover:text-foreground transition-colors",
          "aria-label": t("pptx.headerFooter.close"),
          "data-testid": "header-footer-close",
          children: (0, import_jsx_runtime.jsx)(LuX, { className: "w-4 h-4" })
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4 px-4 py-4", children: [
      (0, import_jsx_runtime.jsx)(
        ToggleRow,
        {
          checked: showDateTime,
          onChange: (hasDateTime) => onUpdate({ hasDateTime }),
          icon: (0, import_jsx_runtime.jsx)(LuCalendarDays, { className: "w-3.5 h-3.5" }),
          label: t("pptx.headerFooter.dateAndTime"),
          testId: "hf-date-time"
        }
      ),
      showDateTime && (0, import_jsx_runtime.jsxs)("div", { className: "pl-6 space-y-2", children: [
        (0, import_jsx_runtime.jsx)(
          ToggleRow,
          {
            checked: dateTimeAuto,
            onChange: (auto) => onUpdate({ dateTimeAuto: auto }),
            icon: (0, import_jsx_runtime.jsx)(LuClock, { className: "w-3.5 h-3.5" }),
            label: t("pptx.headerFooter.updateAutomatically"),
            testId: "hf-date-auto"
          }
        ),
        showDateText && (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "text",
            value: headerFooter.dateTimeText ?? "",
            onChange: handleDateTextChange,
            placeholder: t("pptx.headerFooter.fixedDate"),
            "data-testid": "hf-date-text",
            className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground placeholder-muted-foreground outline-none transition-colors focus:border-primary focus:ring-1 focus:ring-primary/30"
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        ToggleRow,
        {
          checked: showSlideNumber,
          onChange: (hasSlideNumber) => onUpdate({ hasSlideNumber }),
          icon: (0, import_jsx_runtime.jsx)(LuHash, { className: "w-3.5 h-3.5" }),
          label: t("pptx.headerFooter.slideNumber"),
          testId: "hf-slide-number"
        }
      ),
      (0, import_jsx_runtime.jsx)(
        ToggleRow,
        {
          checked: showHeader,
          onChange: (hasHeader) => onUpdate({ hasHeader }),
          icon: (0, import_jsx_runtime.jsx)(LuFileText, { className: "w-3.5 h-3.5" }),
          label: t("pptx.field.header"),
          testId: "hf-header"
        }
      ),
      showHeaderText && (0, import_jsx_runtime.jsx)("div", { className: "pl-6", children: (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          value: headerFooter.headerText ?? "",
          onChange: handleHeaderTextChange,
          placeholder: t("pptx.headerFooter.headerText"),
          "data-testid": "hf-header-text",
          className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground placeholder-muted-foreground outline-none transition-colors focus:border-primary focus:ring-1 focus:ring-primary/30"
        }
      ) }),
      (0, import_jsx_runtime.jsx)(
        ToggleRow,
        {
          checked: showFooter,
          onChange: (hasFooter) => onUpdate({ hasFooter }),
          icon: (0, import_jsx_runtime.jsx)(LuText, { className: "w-3.5 h-3.5" }),
          label: t("pptx.headerFooter.footer"),
          testId: "hf-footer"
        }
      ),
      showFooterText && (0, import_jsx_runtime.jsx)("div", { className: "pl-6", children: (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "text",
          value: headerFooter.footerText ?? "",
          onChange: handleFooterTextChange,
          placeholder: t("pptx.headerFooter.footerPlaceholder"),
          "data-testid": "hf-footer-text",
          className: "w-full rounded border border-border bg-muted px-2.5 py-1.5 text-xs text-foreground placeholder-muted-foreground outline-none transition-colors focus:border-primary focus:ring-1 focus:ring-primary/30"
        }
      ) })
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-2 border-t border-border px-4 py-3", children: [
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onApplyToAll,
          "data-testid": "hf-apply-all",
          className: "rounded bg-accent px-3 py-1.5 text-xs font-medium text-foreground hover:bg-accent/80 transition-colors",
          children: t("pptx.headerFooter.applyToAll")
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onApplyToCurrent,
          "data-testid": "hf-apply-current",
          className: "rounded bg-primary px-3 py-1.5 text-xs font-medium text-white hover:bg-primary/80 transition-colors",
          children: t("pptx.headerFooter.applyToCurrent")
        }
      )
    ] })
  ] }) });
}
function isActionKey(key) {
  return key === "slides" || key === "insert" || key === "inspector" || key === "comments" || key === "notes";
}
var LABEL_BY_KEY = {
  slides: "pptx.sections.slides",
  insert: "pptx.mobileBar.insert",
  inspector: "pptx.field.format",
  comments: "pptx.toolbar.comments",
  notes: "pptx.notes.title"
};
var ICON_BY_KEY = {
  slides: LuLayers,
  insert: LuPlus,
  inspector: LuSettings2,
  comments: LuMessageSquare,
  notes: LuStickyNote
};
function MobileBottomBar({
  slideCount,
  onOpenSlides,
  onOpenInsert,
  onOpenInspector,
  onOpenComments,
  onToggleNotes,
  activeSheet,
  commentCount
}) {
  const { t } = useTranslation(), onClickByKey = {
    slides: onOpenSlides,
    insert: onOpenInsert,
    inspector: onOpenInspector,
    comments: onOpenComments,
    notes: onToggleNotes
  }, actions = buildBarActions({ slideCount }).filter((descriptor) => isActionKey(descriptor.key)).map((descriptor) => {
    const key = descriptor.key;
    return {
      key,
      label: LABEL_BY_KEY[key],
      icon: ICON_BY_KEY[key],
      onClick: onClickByKey[key],
      disabled: descriptor.disabled,
      badge: key === "comments" ? commentCount : void 0
    };
  });
  return (0, import_jsx_runtime.jsx)(
    "nav",
    {
      "aria-label": t("pptx.mobileBar.ariaLabel"),
      className: "flex items-stretch justify-around border-t border-border bg-secondary/80 backdrop-blur supports-[backdrop-filter]:bg-secondary/60 pb-[max(env(safe-area-inset-bottom),0px)]",
      children: actions.map(({ key, label, icon: Icon, onClick, disabled, badge }) => {
        const active = activeSheet === key;
        return (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            onClick,
            disabled,
            className: cn(
              "relative flex flex-col items-center justify-center gap-0.5 flex-1 min-h-[56px] py-1.5 text-[10px] font-medium transition-colors active:scale-95 disabled:opacity-40 disabled:pointer-events-none",
              active ? "text-primary" : "text-muted-foreground hover:text-foreground"
            ),
            "aria-pressed": active,
            "aria-label": key === "notes" ? t("pptx.statusBar.toggleNotes") : void 0,
            children: [
              (0, import_jsx_runtime.jsx)(Icon, { className: "w-5 h-5" }),
              (0, import_jsx_runtime.jsx)("span", { children: t(label) }),
              badge !== void 0 && badge > 0 && (0, import_jsx_runtime.jsx)("span", { className: "absolute top-1 right-1/4 flex items-center justify-center min-w-[16px] h-4 px-1 rounded-full bg-primary text-[9px] font-semibold text-white", children: badge > 99 ? "99+" : badge }),
              active && (0, import_jsx_runtime.jsx)("span", { className: "absolute top-0 left-1/2 -translate-x-1/2 w-8 h-0.5 rounded-full bg-primary" })
            ]
          },
          key
        );
      })
    }
  );
}
function MobileSlidesSheet({
  open,
  onClose,
  ...sidebar
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    MobileSheet,
    {
      open,
      onClose,
      heightFraction: 0.7,
      title: t("pptx.sections.slides"),
      children: (0, import_jsx_runtime.jsx)("div", { className: "h-full", children: (0, import_jsx_runtime.jsx)(SlidesPaneSidebar, { ...sidebar }) })
    }
  );
}
function nextMobileSheet(current, tapped) {
  const next = toggleSheet(current, tapped);
  return next === tapped ? tapped : null;
}
function MobileChromeOverlay(props) {
  const {
    state: s,
    editorOps,
    presentation,
    slides,
    activeSlideIndex,
    canvasSize,
    slideSectionGroups,
    canEdit,
    commentCount
  } = props, { keyboardInset, isKeyboardOpen: isKeyboardOpen2 } = useKeyboardInsets(), activeSheet = s.isSlidesPaneOpen ? "slides" : s.isInspectorPaneOpen ? s.sidebarPanelMode === "comments" ? "comments" : "inspector" : !s.isSlideNotesCollapsed ? "notes" : null, closeAllSheets = () => {
    s.setIsSlidesPaneOpen(false);
    s.setIsInspectorPaneOpen(false);
    s.setIsSlideNotesCollapsed(true);
  }, openSheet = (which) => {
    closeAllSheets();
    switch (which) {
      case "slides":
        s.setIsSlidesPaneOpen(true);
        break;
      case "inspector":
        s.setSidebarPanelMode("properties");
        s.setIsInspectorPaneOpen(true);
        break;
      case "comments":
        s.setSidebarPanelMode("comments");
        s.setIsInspectorPaneOpen(true);
        break;
      case "notes":
        s.setIsSlideNotesCollapsed(false);
        break;
    }
  }, applySheetTap = (tapped) => {
    const next = nextMobileSheet(activeSheet, tapped);
    closeAllSheets();
    if (next) {
      openSheet(next);
    }
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      MobileSlidesSheet,
      {
        open: s.isSlidesPaneOpen,
        onClose: () => s.setIsSlidesPaneOpen(false),
        slides,
        templateElementsBySlideId: s.templateElementsBySlideId,
        activeSlideIndex,
        canvasSize,
        sectionGroups: slideSectionGroups,
        isOpen: true,
        canEdit,
        onSelectSlide: (index) => {
          s.setActiveSlideIndex(index);
          s.setIsSlidesPaneOpen(false);
        },
        onSlideContextMenu: editorOps.slideOps.handleSlideContextMenu,
        onMoveSlide: editorOps.slideOps.handleMoveSlide,
        onAddSlide: editorOps.slideOps.handleAddSlide,
        onCollapse: () => s.setIsSlidesPaneOpen(false),
        onAddSection: editorOps.sectionOps.addSection,
        onRenameSection: editorOps.sectionOps.renameSection,
        onDeleteSection: editorOps.sectionOps.deleteSection,
        onMoveSectionUp: editorOps.sectionOps.moveSectionUp,
        onMoveSectionDown: editorOps.sectionOps.moveSectionDown,
        onToggleSectionCollapse: editorOps.sectionOps.toggleSectionCollapse,
        rehearsalTimings: Object.keys(presentation.recordedTimings).length > 0 ? presentation.recordedTimings : void 0
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "contents",
        style: keyboardInset > 0 ? {
          display: "block",
          transform: `translateY(-${keyboardInset}px)`,
          transition: "transform 150ms ease-out",
          willChange: "transform"
        } : void 0,
        "data-keyboard-open": isKeyboardOpen2 ? "true" : void 0,
        children: (0, import_jsx_runtime.jsx)(
          MobileBottomBar,
          {
            slideCount: slides.length,
            activeSheet,
            commentCount,
            onOpenSlides: () => applySheetTap("slides"),
            onOpenInsert: () => {
              editorOps.insertHandlers.handleAddTextBox();
            },
            onOpenInspector: () => applySheetTap("inspector"),
            onOpenComments: () => applySheetTap("comments"),
            onToggleNotes: () => applySheetTap("notes")
          }
        )
      }
    )
  ] });
}
function ReadOnlyBanner({
  recommendation,
  onEditAnyway,
  onDismiss,
  passwordPromptOpen = false,
  passwordError = null,
  checkingPassword = false,
  onSubmitPassword,
  onCancelPassword
}) {
  const { t } = useTranslation();
  const [password, setPassword] = (0, import_react2.useState)("");
  const inputId = (0, import_react2.useId)();
  const errorId = (0, import_react2.useId)();
  const handleSubmit = (event) => {
    event.preventDefault();
    onSubmitPassword == null ? void 0 : onSubmitPassword(password);
  };
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      "data-testid": "pptx-readonly-banner",
      "data-kind": recommendation.kind ?? void 0,
      className: "flex items-center gap-3 px-3 py-1.5 bg-amber-600/10 border-b border-amber-600/30 text-[12px] text-amber-900 dark:text-amber-200",
      children: [
        (0, import_jsx_runtime.jsx)("span", { className: "flex-1 min-w-0", children: t(recommendation.messageKey) }),
        passwordPromptOpen ? (0, import_jsx_runtime.jsxs)(
          "form",
          {
            "data-testid": "pptx-readonly-password-form",
            onSubmit: handleSubmit,
            className: "flex shrink-0 items-center gap-2",
            children: [
              (0, import_jsx_runtime.jsx)("label", { htmlFor: inputId, className: "sr-only", children: t("pptx.readOnly.passwordLabel") }),
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  id: inputId,
                  "data-testid": "pptx-readonly-password-input",
                  type: "password",
                  autoFocus: true,
                  value: password,
                  disabled: checkingPassword,
                  onChange: (event) => setPassword(event.target.value),
                  placeholder: t("pptx.readOnly.passwordPlaceholder"),
                  "aria-invalid": passwordError !== null,
                  "aria-describedby": passwordError !== null ? errorId : void 0,
                  className: "rounded-sm border border-amber-600/40 bg-white/80 px-1.5 py-0.5 text-[11px] text-amber-950 dark:bg-black/20 dark:text-amber-100"
                }
              ),
              (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "submit",
                  "data-testid": "pptx-readonly-unlock",
                  disabled: checkingPassword,
                  className: "shrink-0 rounded-sm bg-amber-600/90 px-2 py-0.5 text-[11px] text-amber-50 transition-colors hover:bg-amber-600 disabled:opacity-60",
                  children: t("pptx.readOnly.unlock")
                }
              ),
              (0, import_jsx_runtime.jsx)(
                "button",
                {
                  type: "button",
                  "data-testid": "pptx-readonly-password-cancel",
                  onClick: onCancelPassword,
                  className: "shrink-0 rounded-sm px-1.5 py-0.5 text-[11px] text-amber-900/80 transition-colors hover:bg-amber-600/20 dark:text-amber-200/80",
                  children: t("pptx.common.cancel")
                }
              ),
              passwordError !== null && (0, import_jsx_runtime.jsx)(
                "span",
                {
                  id: errorId,
                  role: "alert",
                  "data-testid": "pptx-readonly-password-error",
                  className: "shrink-0 text-[11px] text-red-700 dark:text-red-300",
                  children: t(
                    passwordError === "wrong-password" ? "pptx.readOnly.wrongPassword" : "pptx.readOnly.unsupportedAlgorithm"
                  )
                }
              )
            ]
          }
        ) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "data-testid": "pptx-readonly-edit-anyway",
              onClick: onEditAnyway,
              className: "shrink-0 rounded-sm bg-amber-600/90 px-2 py-0.5 text-[11px] text-amber-50 transition-colors hover:bg-amber-600",
              children: t("pptx.readOnly.editAnyway")
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              "data-testid": "pptx-readonly-dismiss",
              onClick: onDismiss,
              "aria-label": t("pptx.readOnly.dismiss"),
              className: "shrink-0 rounded-sm px-1.5 py-0.5 text-[11px] text-amber-900/80 transition-colors hover:bg-amber-600/20 dark:text-amber-200/80",
              children: t("pptx.readOnly.dismiss")
            }
          )
        ] })
      ]
    }
  );
}
function AddinTable({
  title,
  rows,
  selectedId,
  onSelect
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("section", { children: [
    (0, import_jsx_runtime.jsx)("h4", { className: "mb-1 text-xs font-semibold text-foreground", children: title }),
    rows.length === 0 ? (0, import_jsx_runtime.jsx)("p", { className: "px-2 py-1 text-xs italic text-muted-foreground", children: t("pptx.options.addIns.description") }) : (0, import_jsx_runtime.jsx)("table", { className: "w-full border-collapse text-left", children: (0, import_jsx_runtime.jsx)("tbody", { children: rows.map((row) => (0, import_jsx_runtime.jsxs)(
      "tr",
      {
        onClick: () => onSelect(row.id),
        className: cn(
          "cursor-pointer border-b border-border/40 transition-colors",
          selectedId === row.id ? "bg-primary/10" : "hover:bg-accent"
        ),
        children: [
          (0, import_jsx_runtime.jsx)("td", { className: "px-2 py-1.5 text-xs text-foreground", children: t(row.nameKey) }),
          (0, import_jsx_runtime.jsx)("td", { className: "px-2 py-1.5 font-mono text-[11px] text-muted-foreground", children: row.location }),
          (0, import_jsx_runtime.jsx)("td", { className: "px-2 py-1.5 text-xs text-muted-foreground", children: t(`pptx.options.addInType.${row.type}`) })
        ]
      },
      row.id
    )) }) })
  ] });
}
function OptionsAddInsPane({ addinStatus }) {
  const { t } = useTranslation();
  const [selectedId, setSelectedId] = (0, import_react2.useState)(null);
  const rows = resolveViewerAddinRows(addinStatus);
  const active = rows.filter((row) => row.active);
  const inactive = rows.filter((row) => !row.active);
  const selected = rows.find((row) => row.id === selectedId);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "grid grid-cols-[1fr_auto_auto] gap-x-2 border-b border-border pb-1 pl-2 text-[11px] font-semibold uppercase tracking-wide text-muted-foreground", children: [
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.options.addIns.name") }),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.options.addIns.location") }),
      (0, import_jsx_runtime.jsx)("span", { children: t("pptx.options.addIns.type") })
    ] }),
    (0, import_jsx_runtime.jsx)(
      AddinTable,
      {
        title: t("pptx.options.addIns.active"),
        rows: active,
        selectedId,
        onSelect: setSelectedId
      }
    ),
    (0, import_jsx_runtime.jsx)(
      AddinTable,
      {
        title: t("pptx.options.addIns.inactive"),
        rows: inactive,
        selectedId,
        onSelect: setSelectedId
      }
    ),
    selected && (0, import_jsx_runtime.jsxs)("div", { className: "rounded border border-border/60 bg-muted/40 p-3", children: [
      (0, import_jsx_runtime.jsx)("p", { className: "text-xs font-semibold text-foreground", children: t(selected.nameKey) }),
      (0, import_jsx_runtime.jsx)("p", { className: "mt-1 text-xs text-muted-foreground", children: t(selected.descriptionKey) }),
      (0, import_jsx_runtime.jsx)("p", { className: "mt-1 font-mono text-[11px] text-muted-foreground", children: selected.location })
    ] })
  ] });
}
function readValue(options, control) {
  const group = options[control.group];
  const value = group[control.key];
  return typeof value === "boolean" || typeof value === "number" || typeof value === "string" ? value : void 0;
}
function InfoTip({ text }) {
  return (0, import_jsx_runtime.jsx)("span", { title: text, className: "inline-flex cursor-help align-middle", children: (0, import_jsx_runtime.jsx)(LuInfo, { className: "ml-1 h-3.5 w-3.5 text-primary/70", "aria-label": text }) });
}
function ControlRow({
  control,
  options,
  onOptionChange
}) {
  const { t } = useTranslation();
  const value = readValue(options, control);
  const label = t(control.labelKey);
  const info = control.infoKey ? (0, import_jsx_runtime.jsx)(InfoTip, { text: t(control.infoKey) }) : null;
  const rowClass = cn("flex items-center justify-between gap-3 py-1.5", control.indent && "pl-6");
  if (control.kind === "toggle") {
    return (0, import_jsx_runtime.jsxs)("label", { className: cn(rowClass, "cursor-pointer select-none"), children: [
      (0, import_jsx_runtime.jsxs)("span", { className: "text-sm text-foreground", children: [
        label,
        info
      ] }),
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          className: "h-4 w-4 shrink-0 accent-[var(--pptx-primary,#6366f1)]",
          checked: value === true,
          onChange: (event) => onOptionChange(control.group, control.key, event.target.checked)
        }
      )
    ] });
  }
  if (control.kind === "select") {
    return (0, import_jsx_runtime.jsxs)("div", { className: rowClass, children: [
      (0, import_jsx_runtime.jsxs)("span", { className: "text-sm text-foreground", children: [
        label,
        info
      ] }),
      (0, import_jsx_runtime.jsx)(
        "select",
        {
          "aria-label": label,
          className: "max-w-[55%] rounded border border-border bg-background px-2 py-1 text-xs text-foreground",
          value: typeof value === "string" ? value : "",
          onChange: (event) => onOptionChange(control.group, control.key, event.target.value),
          children: control.choices.map((choice) => (0, import_jsx_runtime.jsx)("option", { value: choice.value, children: t(choice.labelKey) }, choice.value))
        }
      )
    ] });
  }
  if (control.kind === "number") {
    return (0, import_jsx_runtime.jsxs)("div", { className: rowClass, children: [
      (0, import_jsx_runtime.jsxs)("span", { className: "text-sm text-foreground", children: [
        label,
        info
      ] }),
      (0, import_jsx_runtime.jsxs)("span", { className: "flex items-center gap-1.5", children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "number",
            "aria-label": label,
            className: "w-20 rounded border border-border bg-background px-2 py-1 text-right text-xs text-foreground",
            min: control.min,
            max: control.max,
            step: control.step ?? 1,
            value: typeof value === "number" ? value : control.min,
            onChange: (event) => {
              const clamped = clampOptionNumber(event.target.value, control.min, control.max);
              if (clamped !== void 0) {
                onOptionChange(control.group, control.key, clamped);
              }
            }
          }
        ),
        control.unitKey && (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: t(control.unitKey) })
      ] })
    ] });
  }
  return (0, import_jsx_runtime.jsxs)("div", { className: rowClass, children: [
    (0, import_jsx_runtime.jsxs)("span", { className: "text-sm text-foreground", children: [
      label,
      info
    ] }),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        type: "text",
        "aria-label": label,
        className: "w-48 max-w-[55%] rounded border border-border bg-background px-2 py-1 text-xs text-foreground",
        maxLength: control.maxLength,
        value: typeof value === "string" ? value : "",
        onChange: (event) => onOptionChange(control.group, control.key, event.target.value)
      }
    )
  ] });
}
function OptionsPane({
  tab,
  options,
  onOptionChange,
  renderSpecial,
  children
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-5", children: [
    (0, import_jsx_runtime.jsx)("p", { className: "text-sm font-medium text-foreground", children: t(tab.descriptionKey) }),
    tab.sections.map((section) => (0, import_jsx_runtime.jsxs)("section", { children: [
      (0, import_jsx_runtime.jsx)("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t(section.titleKey) }),
      section.descriptionKey && (0, import_jsx_runtime.jsx)("p", { className: "mb-2 text-xs text-muted-foreground", children: t(section.descriptionKey) }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-0.5", children: section.controls.map((control) => (0, import_jsx_runtime.jsx)(
        ControlRow,
        {
          control,
          options,
          onOptionChange
        },
        `${control.group}.${control.key}.${section.id}`
      )) }),
      section.special && (renderSpecial == null ? void 0 : renderSpecial(section))
    ] }, section.id)),
    children
  ] });
}
function CommandList({
  title,
  commandIds,
  selectedId,
  onSelect
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)("div", { className: "flex-1", children: [
    (0, import_jsx_runtime.jsx)("p", { className: "mb-1 text-xs font-medium text-muted-foreground", children: title }),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        role: "listbox",
        "aria-label": title,
        className: "h-48 space-y-0.5 overflow-y-auto rounded border border-border/60 p-1",
        children: commandIds.map((id) => {
          const command = QUICK_ACCESS_COMMAND_CATALOG.find((entry) => entry.id === id);
          if (!command) {
            return null;
          }
          return (0, import_jsx_runtime.jsx)(
            "button",
            {
              type: "button",
              role: "option",
              "aria-selected": selectedId === id,
              onClick: () => onSelect(id),
              className: cn(
                "flex w-full items-center rounded px-2 py-1.5 text-left text-sm transition-colors",
                selectedId === id ? "bg-primary/15 text-primary" : "text-foreground hover:bg-accent"
              ),
              children: t(command.labelKey)
            },
            id
          );
        })
      }
    )
  ] });
}
function OptionsQuickAccessPane({
  options,
  onQuickAccessCommandsChange,
  onResetQuickAccess
}) {
  const { t } = useTranslation();
  const [selectedAvailable, setSelectedAvailable] = (0, import_react2.useState)(null);
  const [selectedCurrent, setSelectedCurrent] = (0, import_react2.useState)(null);
  const current = options.quickAccess.commandIds;
  const available = availableQuickAccessCommands(current).map((entry) => entry.id);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-3", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-stretch gap-3", children: [
      (0, import_jsx_runtime.jsx)(
        CommandList,
        {
          title: t("pptx.options.quickAccess.chooseCommands"),
          commandIds: available,
          selectedId: selectedAvailable,
          onSelect: setSelectedAvailable
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col justify-center gap-2", children: [
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            disabled: !selectedAvailable,
            onClick: () => {
              if (selectedAvailable) {
                onQuickAccessCommandsChange(addQuickAccessCommand(current, selectedAvailable));
                setSelectedAvailable(null);
              }
            },
            className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: [
              t("pptx.options.quickAccess.add"),
              " >>"
            ]
          }
        ),
        (0, import_jsx_runtime.jsxs)(
          "button",
          {
            type: "button",
            disabled: !selectedCurrent,
            onClick: () => {
              if (selectedCurrent) {
                onQuickAccessCommandsChange(removeQuickAccessCommand(current, selectedCurrent));
                setSelectedCurrent(null);
              }
            },
            className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: [
              "<< ",
              t("pptx.options.quickAccess.remove")
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime.jsx)(
        CommandList,
        {
          title: t("pptx.options.quickAccess.currentCommands"),
          commandIds: current,
          selectedId: selectedCurrent,
          onSelect: setSelectedCurrent
        }
      ),
      (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-col justify-center gap-2", children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "aria-label": t("pptx.options.quickAccess.moveUp"),
            disabled: !selectedCurrent,
            onClick: () => {
              if (selectedCurrent) {
                onQuickAccessCommandsChange(moveQuickAccessCommand(current, selectedCurrent, "up"));
              }
            },
            className: "rounded border border-border p-1.5 text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: (0, import_jsx_runtime.jsx)(LuChevronUp, { className: "h-4 w-4" })
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            type: "button",
            "aria-label": t("pptx.options.quickAccess.moveDown"),
            disabled: !selectedCurrent,
            onClick: () => {
              if (selectedCurrent) {
                onQuickAccessCommandsChange(
                  moveQuickAccessCommand(current, selectedCurrent, "down")
                );
              }
            },
            className: "rounded border border-border p-1.5 text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
            children: (0, import_jsx_runtime.jsx)(LuChevronDown, { className: "h-4 w-4" })
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        onClick: onResetQuickAccess,
        className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
        children: t("pptx.options.quickAccess.reset")
      }
    )
  ] });
}
function OptionsRibbonPane({
  options,
  onRibbonTabHiddenChange,
  onResetRibbon
}) {
  const { t } = useTranslation();
  const hidden = new Set(options.ribbon.hiddenTabIds);
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-5", children: [
    (0, import_jsx_runtime.jsxs)("section", { children: [
      (0, import_jsx_runtime.jsx)("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.options.ribbon.tabsTitle") }),
      (0, import_jsx_runtime.jsx)("p", { className: "mb-2 text-xs text-muted-foreground", children: t("pptx.options.ribbon.tabsDescription") }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-0.5 rounded border border-border/60 p-2", children: TOOLBAR_TABS.map((tab) => {
        const isFile = tab.id === "file";
        const isVisible = isFile || !hidden.has(tab.id);
        return (0, import_jsx_runtime.jsxs)(
          "label",
          {
            className: cn(
              "flex items-center gap-2 rounded px-2 py-1.5",
              isFile ? "cursor-not-allowed opacity-60" : "cursor-pointer hover:bg-accent"
            ),
            children: [
              (0, import_jsx_runtime.jsx)(
                "input",
                {
                  type: "checkbox",
                  className: "h-4 w-4 accent-[var(--pptx-primary,#6366f1)]",
                  checked: isVisible,
                  disabled: isFile,
                  onChange: (event) => onRibbonTabHiddenChange(tab.id, !event.target.checked)
                }
              ),
              (0, import_jsx_runtime.jsx)("span", { className: "text-sm text-foreground", children: t(tab.labelKey) })
            ]
          },
          tab.id
        );
      }) }),
      (0, import_jsx_runtime.jsx)(
        "button",
        {
          type: "button",
          onClick: onResetRibbon,
          className: "mt-2 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
          children: t("pptx.options.ribbon.reset")
        }
      )
    ] }),
    (0, import_jsx_runtime.jsxs)("section", { children: [
      (0, import_jsx_runtime.jsx)("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.settings.keyboardShortcuts") }),
      (0, import_jsx_runtime.jsx)("div", { className: "space-y-0.5", children: SHORTCUT_REFERENCE_ITEMS.map((shortcut, i) => (0, import_jsx_runtime.jsxs)(
        "div",
        {
          className: cn(
            "flex items-center justify-between gap-3 rounded px-3 py-2",
            i % 2 === 0 ? "bg-muted/60" : ""
          ),
          children: [
            (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-foreground", children: t(shortcut.actionKey) }),
            (0, import_jsx_runtime.jsx)("span", { className: "whitespace-nowrap font-mono text-[11px] text-muted-foreground", children: shortcut.shortcut })
          ]
        },
        shortcut.actionKey
      )) })
    ] })
  ] });
}
async function exportAiChatLogs2(options) {
  const store = (options == null ? void 0 : options.store) ?? createChatHistoryStore();
  const chats = await collectStoredChats(store);
  return exportAiChatLogs(
    chats,
    { format: options == null ? void 0 : options.format, detailed: options == null ? void 0 : options.detailed, now: options == null ? void 0 : options.now },
    (filename, content, mime) => {
      downloadBlob(new Blob([content], { type: mime }), filename);
    }
  );
}
function SettingsAiTab({ store }) {
  const { t } = useTranslation();
  const activeStore = (0, import_react2.useMemo)(() => store ?? createChatHistoryStore(), [store]);
  const [chatCount, setChatCount] = (0, import_react2.useState)(null);
  const [detailed, setDetailed] = (0, import_react2.useState)(true);
  const [status, setStatus] = (0, import_react2.useState)({ kind: "idle" });
  (0, import_react2.useEffect)(() => {
    let cancelled = false;
    const load = async () => {
      try {
        const chats = await activeStore.listChats();
        if (!cancelled) {
          setChatCount(chats.length);
        }
      } catch {
        if (!cancelled) {
          setChatCount(0);
        }
      }
    };
    void load();
    return () => {
      cancelled = true;
    };
  }, [activeStore]);
  const handleExport = (0, import_react2.useCallback)(
    async (format) => {
      setStatus({ kind: "busy" });
      try {
        const count = await exportAiChatLogs2({ store: activeStore, format, detailed });
        setStatus({ kind: "done", count });
      } catch {
        setStatus({ kind: "done", count: 0 });
      }
    },
    [activeStore, detailed]
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-2 rounded-lg border border-border/60 bg-muted/30 p-3", children: [
      (0, import_jsx_runtime.jsx)(LuBug, { className: "mt-0.5 h-4 w-4 shrink-0 text-primary" }),
      (0, import_jsx_runtime.jsxs)("div", { children: [
        (0, import_jsx_runtime.jsx)("p", { className: "text-sm font-medium text-foreground", children: t("pptx.ai.settingsSectionTitle") }),
        (0, import_jsx_runtime.jsx)("p", { className: "mt-1 text-xs text-muted-foreground", children: t("pptx.ai.exportLogsHint") })
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground", children: chatCount === null ? t("pptx.ai.exportLogsCounting") : t("pptx.ai.exportLogsStoredCount", { count: chatCount }) }),
    (0, import_jsx_runtime.jsxs)("label", { className: "flex items-center gap-2 text-xs text-foreground", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          checked: detailed,
          onChange: (e) => setDetailed(e.target.checked),
          className: "h-3.5 w-3.5 rounded border-border"
        }
      ),
      t("pptx.ai.exportLogsDetailed")
    ] }),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex flex-wrap items-center gap-2", children: [
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          onClick: () => void handleExport("json"),
          disabled: status.kind === "busy",
          className: "inline-flex items-center gap-1.5 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:opacity-50",
          children: [
            (0, import_jsx_runtime.jsx)(LuDownload, { className: "h-3.5 w-3.5" }),
            t("pptx.ai.exportLogsJson")
          ]
        }
      ),
      (0, import_jsx_runtime.jsxs)(
        "button",
        {
          type: "button",
          onClick: () => void handleExport("markdown"),
          disabled: status.kind === "busy",
          className: "inline-flex items-center gap-1.5 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:opacity-50",
          children: [
            (0, import_jsx_runtime.jsx)(LuDownload, { className: "h-3.5 w-3.5" }),
            t("pptx.ai.exportLogsMarkdown")
          ]
        }
      )
    ] }),
    status.kind === "done" && (0, import_jsx_runtime.jsx)("p", { className: "text-xs text-muted-foreground", role: "status", children: status.count > 0 ? t("pptx.ai.exportLogsDone", { count: status.count }) : t("pptx.ai.noChatsToExport") })
  ] });
}
function SettingsAppearanceTab({
  activeThemeKey,
  themes,
  onSelectTheme
}) {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)("div", { className: "grid grid-cols-2 gap-3", children: themes.map((entry) => {
    var _a, _b2, _c, _d;
    const isActive = entry.key === activeThemeKey;
    const previewBackground = ((_b2 = (_a = entry.theme) == null ? void 0 : _a.colors) == null ? void 0 : _b2.background) ?? "#0b0f19";
    const previewPrimary = ((_d = (_c = entry.theme) == null ? void 0 : _c.colors) == null ? void 0 : _d.primary) ?? "#6366f1";
    return (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: () => onSelectTheme(entry.key),
        "aria-pressed": isActive,
        className: cn(
          "flex items-center gap-3 rounded-lg border px-3 py-2.5 text-left transition-colors",
          isActive ? "border-primary bg-primary/10" : "border-border hover:border-primary/50 hover:bg-accent"
        ),
        children: [
          (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: "h-8 w-8 shrink-0 rounded-full border border-border/60 shadow-inner",
              style: {
                background: `linear-gradient(135deg, ${previewBackground} 50%, ${previewPrimary} 50%)`
              }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "span",
            {
              className: cn("text-xs font-medium", isActive ? "text-primary" : "text-foreground"),
              children: t(entry.labelKey)
            }
          )
        ]
      },
      entry.key
    );
  }) });
}
function SettingsCustomFontsSection({
  enabled,
  families,
  onRegistered
}) {
  const { t } = useTranslation();
  const inputRef = (0, import_react2.useRef)(null);
  const [error, setError] = (0, import_react2.useState)(false);
  const handleFile = async (file) => {
    setError(false);
    try {
      const registration = await registerCustomFont(file);
      if (registration) {
        onRegistered(registration.family);
      } else {
        setError(true);
      }
    } catch {
      setError(true);
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2", children: [
    (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        disabled: !enabled,
        onClick: () => {
          var _a;
          return (_a = inputRef.current) == null ? void 0 : _a.click();
        },
        className: "inline-flex items-center gap-2 rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent disabled:cursor-not-allowed disabled:opacity-50",
        children: [
          (0, import_jsx_runtime.jsx)(LuUpload, { className: "h-3.5 w-3.5" }),
          t("pptx.options.general.addFontFile")
        ]
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "input",
      {
        ref: inputRef,
        type: "file",
        accept: CUSTOM_FONT_ACCEPT,
        className: "hidden",
        onChange: (event) => {
          var _a;
          const file = (_a = event.currentTarget.files) == null ? void 0 : _a[0];
          event.currentTarget.value = "";
          if (file) {
            void handleFile(file);
          }
        }
      }
    ),
    !enabled && (0, import_jsx_runtime.jsx)("p", { className: "mt-2 text-xs text-muted-foreground", children: t("pptx.options.general.customFontsDisabled") }),
    error && (0, import_jsx_runtime.jsx)("p", { role: "alert", className: "mt-2 text-xs text-destructive", children: t("pptx.options.general.customFontError") }),
    (0, import_jsx_runtime.jsx)("p", { className: "mt-3 text-[11px] font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.options.general.customFontsAdded") }),
    families.length === 0 ? (0, import_jsx_runtime.jsx)("p", { className: "mt-1 text-xs text-muted-foreground", children: t("pptx.options.general.customFontsEmpty") }) : (0, import_jsx_runtime.jsx)("ul", { className: "mt-1 flex flex-col gap-0.5", children: families.map((family) => (0, import_jsx_runtime.jsx)("li", { className: "text-xs text-foreground", style: { fontFamily: family }, children: family }, family)) })
  ] });
}
function SettingsLanguageTab({
  activeLocale,
  locales,
  onSelectLocale
}) {
  return (0, import_jsx_runtime.jsx)("div", { className: "space-y-0.5", children: locales.map((locale) => {
    const isActive = locale.code === activeLocale;
    return (0, import_jsx_runtime.jsxs)(
      "button",
      {
        type: "button",
        onClick: () => onSelectLocale(locale.code),
        "aria-pressed": isActive,
        className: cn(
          "flex w-full items-center justify-between rounded px-3 py-2.5 text-left transition-colors",
          isActive ? "bg-primary/10 text-primary" : "text-foreground hover:bg-accent"
        ),
        children: [
          (0, import_jsx_runtime.jsx)("span", { className: "text-sm", children: locale.nativeLabel }),
          locale.nativeLabel !== locale.label && (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-muted-foreground", children: locale.label })
        ]
      },
      locale.code
    );
  }) });
}
var AI_TAB_ID = "ai";
var NO_CUSTOM_FONT_FAMILIES = [];
function SettingsDialog({
  isOpen,
  onClose,
  options,
  onOptionChange,
  onRestoreOptions,
  onRibbonTabHiddenChange,
  onQuickAccessCommandsChange,
  onResetOptions,
  onClearCache,
  addinStatus,
  themeKey,
  availableThemes,
  onSelectTheme,
  localeCode,
  availableLocales,
  onSelectLocale,
  aiEnabled,
  chatStore,
  customFontFamilies = NO_CUSTOM_FONT_FAMILIES,
  onCustomFontRegistered
}) {
  const [activeTabId, setActiveTabId] = (0, import_react2.useState)("general");
  const { t } = useTranslation();
  const { panelStyle, handlers: dragHandlers } = useModalDismissDrag(onClose);
  const snapshotRef = (0, import_react2.useRef)(null);
  const wasOpenRef = (0, import_react2.useRef)(false);
  (0, import_react2.useEffect)(() => {
    if (isOpen && !wasOpenRef.current) {
      snapshotRef.current = options;
    }
    wasOpenRef.current = isOpen;
  }, [isOpen, options]);
  const handleCancel = (0, import_react2.useCallback)(() => {
    if (snapshotRef.current) {
      onRestoreOptions(snapshotRef.current);
    }
    onClose();
  }, [onClose, onRestoreOptions]);
  const handleKeyDown = (0, import_react2.useCallback)(
    (e) => {
      if (e.key === "Escape") {
        onClose();
      }
    },
    [onClose]
  );
  (0, import_react2.useEffect)(() => {
    if (isOpen) {
      document.addEventListener("keydown", handleKeyDown);
      return () => document.removeEventListener("keydown", handleKeyDown);
    }
  }, [isOpen, handleKeyDown]);
  if (!isOpen) {
    return null;
  }
  const activeTab = VIEWER_OPTIONS_TABS.find((tab) => tab.id === activeTabId) ?? VIEWER_OPTIONS_TABS[0];
  if (!activeTab) {
    return null;
  }
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      "button",
      {
        type: "button",
        style: { zIndex: 1200 },
        className: "fixed inset-0 bg-black/60",
        "aria-label": t("pptx.settings.closeSettings"),
        onClick: onClose
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        style: { zIndex: 1201 },
        className: "fixed inset-0 flex items-center justify-center pointer-events-none",
        children: (0, import_jsx_runtime.jsxs)(
          "div",
          {
            style: panelStyle,
            role: "dialog",
            "aria-modal": "true",
            "aria-label": t("pptx.options.title"),
            className: "pointer-events-auto flex max-h-[85vh] w-[min(56rem,calc(100%-2rem))] flex-col rounded-xl border border-border bg-popover shadow-2xl backdrop-blur-xl max-md:fixed max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:max-h-[88dvh] max-md:w-full max-md:rounded-t-2xl max-md:rounded-b-none max-md:border-x-0 max-md:border-b-0 max-md:pb-[max(env(safe-area-inset-bottom),0px)]",
            children: [
              (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  ...dragHandlers,
                  className: "flex items-center justify-between border-b border-border/60 px-5 py-4 touch-none",
                  children: [
                    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
                      (0, import_jsx_runtime.jsx)(LuSettings, { className: "h-5 w-5 text-primary" }),
                      (0, import_jsx_runtime.jsx)("h2", { className: "text-sm font-semibold text-foreground", children: t("pptx.options.title") })
                    ] }),
                    (0, import_jsx_runtime.jsx)(
                      "button",
                      {
                        type: "button",
                        onClick: onClose,
                        className: "rounded p-1 transition-colors hover:bg-accent",
                        "aria-label": t("pptx.settings.close"),
                        children: (0, import_jsx_runtime.jsx)(LuX, { className: "h-4 w-4 text-muted-foreground" })
                      }
                    )
                  ]
                }
              ),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex min-h-0 flex-1 max-md:flex-col", children: [
                (0, import_jsx_runtime.jsxs)(
                  "nav",
                  {
                    "aria-label": t("pptx.options.title"),
                    className: "w-44 shrink-0 space-y-0.5 overflow-y-auto border-r border-border/60 p-2 max-md:flex max-md:w-full max-md:space-y-0 max-md:gap-1 max-md:overflow-x-auto max-md:border-b max-md:border-r-0",
                    children: [
                      VIEWER_OPTIONS_TABS.map((tab) => (0, import_jsx_runtime.jsx)(
                        "button",
                        {
                          type: "button",
                          onClick: () => setActiveTabId(tab.id),
                          "aria-current": activeTabId === tab.id,
                          className: cn(
                            "block w-full whitespace-nowrap rounded px-3 py-2 text-left text-sm transition-colors max-md:w-auto",
                            activeTabId === tab.id ? "bg-primary/10 font-medium text-primary" : "text-foreground hover:bg-accent"
                          ),
                          children: t(tab.labelKey)
                        },
                        tab.id
                      )),
                      aiEnabled && (0, import_jsx_runtime.jsx)(
                        "button",
                        {
                          type: "button",
                          onClick: () => setActiveTabId(AI_TAB_ID),
                          "aria-current": activeTabId === AI_TAB_ID,
                          className: cn(
                            "block w-full whitespace-nowrap rounded px-3 py-2 text-left text-sm transition-colors max-md:w-auto",
                            activeTabId === AI_TAB_ID ? "bg-primary/10 font-medium text-primary" : "text-foreground hover:bg-accent"
                          ),
                          children: t("pptx.ai.settingsSectionTitle")
                        }
                      )
                    ]
                  }
                ),
                (0, import_jsx_runtime.jsx)("div", { className: "min-h-0 flex-1 overflow-y-auto px-5 py-4", children: activeTabId === AI_TAB_ID ? (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
                  (0, import_jsx_runtime.jsx)("p", { className: "text-sm font-medium text-foreground", children: t("pptx.ai.settingsSectionTitle") }),
                  (0, import_jsx_runtime.jsx)(SettingsAiTab, { store: chatStore })
                ] }) : activeTab.custom === "language" ? (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
                  (0, import_jsx_runtime.jsx)("p", { className: "text-sm font-medium text-foreground", children: t(activeTab.descriptionKey) }),
                  (0, import_jsx_runtime.jsxs)("section", { children: [
                    (0, import_jsx_runtime.jsx)("h3", { className: "mb-1 border-b border-border/60 pb-1 text-xs font-semibold uppercase tracking-wide text-muted-foreground", children: t("pptx.options.language.displayLanguage") }),
                    (0, import_jsx_runtime.jsx)("p", { className: "mb-2 text-xs text-muted-foreground", children: t("pptx.options.language.displayLanguageDescription") }),
                    (0, import_jsx_runtime.jsx)(
                      SettingsLanguageTab,
                      {
                        activeLocale: localeCode,
                        locales: availableLocales,
                        onSelectLocale
                      }
                    )
                  ] })
                ] }) : activeTab.custom === "ribbon" ? (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
                  (0, import_jsx_runtime.jsx)("p", { className: "text-sm font-medium text-foreground", children: t(activeTab.descriptionKey) }),
                  (0, import_jsx_runtime.jsx)(
                    OptionsRibbonPane,
                    {
                      options,
                      onRibbonTabHiddenChange,
                      onResetRibbon: () => onResetOptions("ribbon")
                    }
                  )
                ] }) : activeTab.custom === "addIns" ? (0, import_jsx_runtime.jsxs)("div", { className: "space-y-4", children: [
                  (0, import_jsx_runtime.jsx)("p", { className: "text-sm font-medium text-foreground", children: t(activeTab.descriptionKey) }),
                  (0, import_jsx_runtime.jsx)(OptionsAddInsPane, { addinStatus })
                ] }) : (0, import_jsx_runtime.jsx)(
                  OptionsPane,
                  {
                    tab: activeTab,
                    options,
                    onOptionChange,
                    renderSpecial: (section) => {
                      if (section.special === "themePicker") {
                        return (0, import_jsx_runtime.jsx)("div", { className: "mt-2", children: (0, import_jsx_runtime.jsx)(
                          SettingsAppearanceTab,
                          {
                            activeThemeKey: themeKey,
                            themes: availableThemes,
                            onSelectTheme
                          }
                        ) });
                      }
                      if (section.special === "customFonts") {
                        return (0, import_jsx_runtime.jsx)(
                          SettingsCustomFontsSection,
                          {
                            enabled: options.general.enableCustomFontUpload,
                            families: customFontFamilies,
                            onRegistered: (family) => onCustomFontRegistered == null ? void 0 : onCustomFontRegistered(family)
                          }
                        );
                      }
                      if (section.special === "clearCache") {
                        return (0, import_jsx_runtime.jsxs)("div", { className: "mt-2", children: [
                          (0, import_jsx_runtime.jsx)("p", { className: "mb-2 text-xs text-muted-foreground", children: t("pptx.options.save.clearCacheDescription") }),
                          (0, import_jsx_runtime.jsx)(
                            "button",
                            {
                              type: "button",
                              onClick: onClearCache,
                              className: "rounded border border-border px-3 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
                              children: t("pptx.options.save.clearCacheNow")
                            }
                          )
                        ] });
                      }
                      return null;
                    },
                    children: activeTab.custom === "quickAccess" && (0, import_jsx_runtime.jsx)(
                      OptionsQuickAccessPane,
                      {
                        options,
                        onQuickAccessCommandsChange,
                        onResetQuickAccess: () => onQuickAccessCommandsChange([...DEFAULT_QUICK_ACCESS_COMMAND_IDS])
                      }
                    )
                  }
                ) })
              ] }),
              (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-2 border-t border-border/60 px-5 py-3", children: [
                (0, import_jsx_runtime.jsx)(
                  "button",
                  {
                    type: "button",
                    onClick: () => onResetOptions(),
                    className: "rounded border border-border px-3 py-1.5 text-xs text-muted-foreground transition-colors hover:bg-accent hover:text-foreground",
                    children: t("pptx.options.resetAll")
                  }
                ),
                (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center gap-2", children: [
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: handleCancel,
                      className: "rounded border border-border px-4 py-1.5 text-xs text-foreground transition-colors hover:bg-accent",
                      children: t("pptx.common.cancel")
                    }
                  ),
                  (0, import_jsx_runtime.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: onClose,
                      className: "rounded bg-primary px-4 py-1.5 text-xs font-medium text-primary-foreground transition-colors hover:bg-primary/90",
                      children: t("pptx.common.ok")
                    }
                  )
                ] })
              ] })
            ]
          }
        )
      }
    )
  ] });
}
function useAiBridge(input) {
  const ref = (0, import_react2.useRef)(input);
  ref.current = input;
  return (0, import_react2.useMemo)(() => {
    const applySlidesUpdate = (updater, _label) => {
      const live = ref.current;
      live.setSlides((prev) => updater(structuredClone(prev)));
      live.bumpHistory();
      live.markDirty();
    };
    const readDeckData = (live) => ({
      slides: live.slides,
      width: live.canvasSize.width,
      height: live.canvasSize.height,
      theme: live.theme,
      sections: live.sections,
      presentationProperties: live.presentationProperties,
      customProperties: live.customProperties,
      coreProperties: live.coreProperties,
      appProperties: live.appProperties,
      viewProperties: live.viewProperties,
      tableStyleMap: live.tableStyleMap,
      tableStylesDefaultId: live.tableStylesDefaultId,
      tags: live.tagCollections
    });
    const differs = deckDataFieldChanged;
    return {
      getDeckMeta() {
        var _a;
        const live = ref.current;
        const firstTitle = (_a = live.slides[0]) == null ? void 0 : _a.elements.find(
          (el) => "text" in el && typeof el.text === "string" && el.text.trim().length > 0
        );
        return {
          slideCount: live.slides.length,
          activeSlideIndex: live.activeSlideIndex,
          title: live.fileName ?? (firstTitle && "text" in firstTitle ? String(firstTitle.text) : void 0),
          width: live.canvasSize.width,
          height: live.canvasSize.height
        };
      },
      getSlides: () => ref.current.slides,
      getActiveSlideIndex: () => ref.current.activeSlideIndex,
      getTheme: () => ref.current.theme,
      getHandler: () => ref.current.handlerRef.current ?? void 0,
      goToSlide(index) {
        ref.current.setActiveSlideIndex(index);
      },
      selectElements(slideIndex, elementIds) {
        const live = ref.current;
        if (slideIndex !== live.activeSlideIndex) {
          live.setActiveSlideIndex(slideIndex);
        }
        live.applySelection(elementIds[0] ?? null, elementIds);
      },
      applySlidesUpdate,
      updateElement(slideIndex, elementId, updates) {
        applySlidesUpdate((slides) => {
          var _a;
          const el = (_a = slides[slideIndex]) == null ? void 0 : _a.elements.find((e) => e.id === elementId);
          if (el) {
            applyElementUpdate(el, updates);
          }
          return slides;
        });
      },
      applyTheme(updates) {
        ref.current.applyThemeUpdates(updates);
      },
      getDeckData() {
        return readDeckData(ref.current);
      },
      applyDeckData(updater, _label) {
        const live = ref.current;
        const before = readDeckData(live);
        const after = updater(structuredClone(before));
        if (differs(before.slides, after.slides)) {
          live.setSlides(() => after.slides);
          live.bumpHistory();
        }
        if (before.width !== after.width || before.height !== after.height) {
          live.setCanvasSize({ width: after.width, height: after.height });
        }
        const nextSections = after.sections ?? before.sections ?? [];
        if (differs(before.sections, nextSections)) {
          live.setSections(nextSections);
        }
        const nextPresProps = after.presentationProperties ?? live.presentationProperties;
        if (differs(before.presentationProperties, nextPresProps)) {
          live.setPresentationProperties(nextPresProps);
        }
        const nextCustomProps = after.customProperties ?? before.customProperties ?? [];
        if (differs(before.customProperties, nextCustomProps)) {
          live.setCustomProperties(nextCustomProps);
        }
        if (differs(before.coreProperties, after.coreProperties)) {
          live.setCoreProperties(after.coreProperties);
        }
        if (differs(before.appProperties, after.appProperties)) {
          live.setAppProperties(after.appProperties);
        }
        if (differs(before.viewProperties, after.viewProperties)) {
          live.setViewProperties(after.viewProperties);
        }
        if (differs(before.tableStyleMap, after.tableStyleMap)) {
          live.setTableStyleMap(after.tableStyleMap);
        }
        if (differs(before.tableStylesDefaultId, after.tableStylesDefaultId)) {
          live.setTableStylesDefaultId(after.tableStylesDefaultId);
        }
        const nextTags = after.tags ?? before.tags ?? [];
        if (differs(before.tags, nextTags)) {
          live.setTagCollections(nextTags);
        }
        live.markDirty();
      },
      getFocusedTargets() {
        const live = ref.current;
        if (live.pickedFocus && live.pickedFocus.length > 0) {
          return live.pickedFocus;
        }
        if (live.pinnedFocus && live.pinnedFocus.length > 0) {
          return live.pinnedFocus;
        }
        return computeFocusTargets({
          activeSlideIndex: live.activeSlideIndex,
          selectedElementIds: live.selectedElementIds,
          selectedElementId: live.selectedElementId
        });
      },
      notify(message, level) {
        var _a, _b2;
        (_b2 = (_a = ref.current).notify) == null ? void 0 : _b2.call(_a, message, level);
      }
    };
  }, []);
}
var TOOL_FLASH_MS = 2600;
function fixDirective(element, slideIndex) {
  return `Review and fix any issues with this ${element.type} (id=${element.id}) on slide ${slideIndex + 1}.`;
}
function useAiPanelController(input) {
  const { activeSlideIndex, selectedElementId, selectedElementIds, selectedElement } = input;
  const [isOpen, setIsOpen] = (0, import_react2.useState)(false);
  const [pinnedFocus, setPinnedFocus] = (0, import_react2.useState)(null);
  const [prefill, setPrefill] = (0, import_react2.useState)({ text: "", nonce: 0 });
  const [pickMode, setPickMode] = (0, import_react2.useState)(false);
  const [pickTargets, setPickTargets] = (0, import_react2.useState)([]);
  const [toolFocus, setToolFocus] = (0, import_react2.useState)(
    null
  );
  const [flashTick, setFlashTick] = (0, import_react2.useState)(0);
  const flashTimer = (0, import_react2.useRef)(null);
  const [changeBatch, setChangeBatch] = (0, import_react2.useState)(null);
  const showChangeBatch = (0, import_react2.useCallback)((batch) => setChangeBatch(batch), []);
  const liveFocusTargets = (0, import_react2.useMemo)(
    () => computeFocusTargets({ activeSlideIndex, selectedElementIds, selectedElementId }),
    [activeSlideIndex, selectedElementIds, selectedElementId]
  );
  const open = (0, import_react2.useCallback)(() => setIsOpen(true), []);
  const close = (0, import_react2.useCallback)(() => setIsOpen(false), []);
  const toggle = (0, import_react2.useCallback)(() => setIsOpen((p) => !p), []);
  const pinFocus = (0, import_react2.useCallback)(() => setPinnedFocus(liveFocusTargets), [liveFocusTargets]);
  const clearPinnedFocus = (0, import_react2.useCallback)(() => setPinnedFocus(null), []);
  const askAboutSelection = (0, import_react2.useCallback)(() => {
    setPinnedFocus(liveFocusTargets);
    setPrefill((p) => ({ text: "", nonce: p.nonce + 1 }));
    setIsOpen(true);
  }, [liveFocusTargets]);
  const fixSelection = (0, import_react2.useCallback)(() => {
    setPinnedFocus(liveFocusTargets);
    const text = selectedElement ? fixDirective(selectedElement, activeSlideIndex) : "";
    setPrefill((p) => ({ text, nonce: p.nonce + 1 }));
    setIsOpen(true);
  }, [liveFocusTargets, selectedElement, activeSlideIndex]);
  const startPicking = (0, import_react2.useCallback)(() => {
    setPickMode(true);
    setIsOpen(true);
  }, []);
  const stopPicking = (0, import_react2.useCallback)(() => setPickMode(false), []);
  const addPick = (0, import_react2.useCallback)((slideIndex, elementId) => {
    setPickTargets((prev) => {
      if (prev.some((t) => t.kind === "element" && t.elementId === elementId)) {
        return prev;
      }
      return [...prev, { kind: "element", slideIndex, elementId }];
    });
  }, []);
  const clearPicks = (0, import_react2.useCallback)(() => {
    setPickTargets([]);
    setPickMode(false);
  }, []);
  const flashToolTarget = (0, import_react2.useCallback)((target) => {
    if (target && target.slideIndex !== void 0 && target.elementIds.length > 0) {
      setToolFocus({ slideIndex: target.slideIndex, elementIds: target.elementIds });
    } else {
      setToolFocus(null);
    }
    setFlashTick((n) => n + 1);
    if (flashTimer.current) {
      clearTimeout(flashTimer.current);
    }
    flashTimer.current = setTimeout(() => {
      setToolFocus(null);
      setFlashTick(0);
    }, TOOL_FLASH_MS);
  }, []);
  (0, import_react2.useEffect)(
    () => () => {
      if (flashTimer.current) {
        clearTimeout(flashTimer.current);
      }
    },
    []
  );
  const canvasHighlights = (0, import_react2.useMemo)(() => {
    const out = [];
    for (const t of pickTargets) {
      if (t.kind === "element") {
        out.push({ slideIndex: t.slideIndex, elementId: t.elementId, variant: "pick" });
      }
    }
    if (toolFocus) {
      for (const elementId of toolFocus.elementIds) {
        out.push({ slideIndex: toolFocus.slideIndex, elementId, variant: "active" });
      }
    }
    return out;
  }, [pickTargets, toolFocus]);
  const canvasAnimating = canvasHighlights.length > 0 || flashTick > 0;
  return {
    isOpen,
    open,
    close,
    toggle,
    liveFocusTargets,
    pinnedFocus,
    pinFocus,
    clearPinnedFocus,
    prefill,
    askAboutSelection,
    fixSelection,
    pickMode,
    startPicking,
    stopPicking,
    pickTargets,
    addPick,
    clearPicks,
    canvasHighlights,
    canvasAnimating,
    flashToolTarget,
    changeBatch,
    showChangeBatch
  };
}
function useMasterViewCrud(input) {
  const {
    handlerRef,
    slides,
    slideMasters,
    target,
    setSlides,
    setSlideMasters,
    setActiveMasterIndex,
    setActiveLayoutIndex,
    markDirty,
    pushToast
  } = input;
  const { t } = useTranslation();
  const data = (0, import_react2.useMemo)(
    () => ({ slides, slideMasters }),
    [slides, slideMasters]
  );
  const crudActions = (0, import_react2.useMemo)(() => masterViewCrudActions(data, target), [data, target]);
  const handleCrudAction = (0, import_react2.useCallback)(
    async (id) => {
      var _a, _b2, _c;
      const handler = handlerRef.current;
      if (!handler || !target) {
        return;
      }
      const action = crudActions.find((candidate) => candidate.id === id);
      if (!(action == null ? void 0 : action.enabled)) {
        return;
      }
      let name;
      if (id === "renameLayout" || id === "renameMaster") {
        const master = slideMasters[target.masterIndex];
        const current = id === "renameLayout" ? ((_b2 = (_a = master == null ? void 0 : master.layouts) == null ? void 0 : _a[target.layoutIndex ?? -1]) == null ? void 0 : _b2.name) ?? "" : (master == null ? void 0 : master.name) ?? "";
        const next = (_c = safePrompt(t("pptx.masterView.renamePrompt"), current)) == null ? void 0 : _c.trim();
        if (!next) {
          return;
        }
        name = next;
      }
      const result = await applyMasterViewCrudAction(
        handler,
        data,
        id,
        target,
        name !== void 0 ? { name } : void 0
      );
      if (!result.ok) {
        pushToast({
          id: `master-crud-${Date.now()}`,
          code: `masterViewCrud:${result.reason}`,
          severity: "warning",
          messageKey: masterViewCrudFailureKey(id, result.reason)
        });
        return;
      }
      handlerRef.current = result.handler;
      setSlides(result.data.slides);
      setSlideMasters(result.data.slideMasters ?? []);
      setActiveMasterIndex(result.target.masterIndex);
      setActiveLayoutIndex(result.target.layoutIndex);
      markDirty();
    },
    [
      handlerRef,
      target,
      crudActions,
      slideMasters,
      t,
      data,
      pushToast,
      setSlides,
      setSlideMasters,
      setActiveMasterIndex,
      setActiveLayoutIndex,
      markDirty
    ]
  );
  return { crudActions, handleCrudAction };
}
function applyRecentColorPick(recent, hex) {
  const next = pushRecentColor(recent, hex);
  if (next === recent) {
    return null;
  }
  return { recentColors: next, patch: mruColorsPatch(next) };
}
function useRecentColorsSync(input) {
  const { setRecentColors, setPresentationProperties } = input;
  const pushColor = (0, import_react2.useCallback)(
    (hex) => {
      setRecentColors((prev) => {
        const applied = applyRecentColorPick(prev, hex);
        if (!applied) {
          return prev;
        }
        setPresentationProperties((properties) => ({ ...properties, ...applied.patch }));
        return applied.recentColors;
      });
    },
    [setRecentColors, setPresentationProperties]
  );
  return { pushColor };
}
var PowerPointViewer = (0, import_react2.forwardRef)(
  // oxlint-disable-next-line prefer-arrow-callback -- named fn gives the forwardRef component its displayName
  function PowerPointViewer2(props, ref) {
    var _a, _b2, _c, _d;
    const {
      content: incomingContent,
      fonts = [],
      filePath,
      fileName,
      canEdit: hostCanEdit = false,
      autosave: hostAutosave,
      autosaveIntervalMs: hostAutosaveIntervalMs,
      onContentChange,
      onDirtyChange,
      onActiveSlideChange,
      onModeChange,
      onZoomChange,
      onSelectionChange,
      onSlideCountChange,
      onOpenFile: hostOpenFile,
      theme,
      defaultThemeKey,
      availableThemes,
      onThemeChange,
      defaultLocale,
      availableLocales,
      onLocaleChange,
      accountAuth,
      authorName,
      collaboration,
      onStartCollaboration,
      onStopCollaboration,
      shareDefaults,
      smartArt3D = false,
      surfaceChart3D = false,
      barChart3D = false,
      lineChart3D = false,
      areaChart3D = false,
      pieChart3D = false,
      hiddenActions,
      ai
    } = props;
    (0, import_react2.useEffect)(() => {
      const css = buildUserFontFaceStyles(fonts);
      if (!css) {
        return;
      }
      const style = document.createElement("style");
      style.dataset.pptxUserFonts = "true";
      style.textContent = css;
      document.head.appendChild(style);
      return () => style.remove();
    }, [fonts]);
    const [themeKey, setThemeKey] = (0, import_react2.useState)(
      () => defaultThemeKey ?? readStoredViewerPrefs().themeKey ?? "default"
    );
    const themeCatalog = availableThemes ?? THEME_CATALOG;
    const effectiveTheme = theme ?? resolveThemeCatalogEntry(themeKey, themeCatalog);
    const handleThemeChange = (0, import_react2.useCallback)(
      (key) => {
        setThemeKey(key);
        if (onThemeChange) {
          onThemeChange(key);
        } else {
          writeStoredViewerPrefs({ themeKey: key });
        }
      },
      [onThemeChange]
    );
    const themeStyle = useThemeStyle(effectiveTheme);
    const [customFontFamilies, setCustomFontFamilies] = (0, import_react2.useState)([]);
    const handleCustomFontRegistered = (0, import_react2.useCallback)((family) => {
      setCustomFontFamilies(
        (current) => current.includes(family) ? current : [...current, family]
      );
    }, []);
    const { i18n } = useTranslation();
    const [localeCode, setLocaleCode] = (0, import_react2.useState)(
      () => defaultLocale ?? readStoredViewerPrefs().localeCode ?? "en"
    );
    (0, import_react2.useEffect)(() => {
      if (localeCode !== "en" && !onLocaleChange) {
        void i18n.changeLanguage(localeCode);
      }
    }, []);
    const resolvedLocales = (0, import_react2.useMemo)(() => {
      if (availableLocales) {
        return availableLocales;
      }
      const registeredCodes = i18n.options.resources ? Object.keys(i18n.options.resources) : i18n.languages ?? ["en"];
      return registeredCodes.map(
        (code) => LOCALE_CATALOG.find((entry) => entry.code === code) ?? {
          code,
          label: code,
          nativeLabel: code
        }
      );
    }, [availableLocales, i18n]);
    const handleLocaleChange = (0, import_react2.useCallback)(
      (code) => {
        setLocaleCode(code);
        if (onLocaleChange) {
          onLocaleChange(code);
        } else {
          void i18n.changeLanguage(code);
          writeStoredViewerPrefs({ localeCode: code });
        }
      },
      [onLocaleChange, i18n]
    );
    const [content, setContent] = (0, import_react2.useState)(incomingContent);
    const [loadOrigin, setLoadOrigin] = (0, import_react2.useState)("bootstrap");
    (0, import_react2.useEffect)(() => {
      setContent(incomingContent);
      setLoadOrigin("bootstrap");
    }, [incomingContent]);
    const handleOpenFile = (0, import_react2.useCallback)(() => {
      if (hostOpenFile) {
        hostOpenFile();
        return;
      }
      void (async () => {
        const picked = await openPptxFile();
        if (picked) {
          setLoadOrigin("user");
          setContent(picked.buffer);
        }
      })();
    }, [hostOpenFile]);
    const handleOpenRecentFile = (0, import_react2.useCallback)((key) => {
      void (async () => {
        const bytes = await readBackstageRecentFile(key);
        if (bytes) {
          setLoadOrigin("user");
          setContent(bytes);
        }
      })();
    }, []);
    const { optionsStore, options: viewerOptions } = useViewerOptions();
    const [protectedViewOverridden, setProtectedViewOverridden] = (0, import_react2.useState)(false);
    (0, import_react2.useEffect)(() => {
      setProtectedViewOverridden(false);
    }, [content]);
    const isProtectedView = viewerOptions.trust.openInProtectedView && !protectedViewOverridden;
    const handleEnableEditing = (0, import_react2.useCallback)(() => {
      setProtectedViewOverridden(true);
    }, []);
    const readOnlyRec = useReadOnlyRecommendationState(content);
    const compatToastsState = useCompatibilityToastsState();
    const canEdit = hostCanEdit && !isProtectedView && !readOnlyRec.locked;
    const [isSettingsOpen, setIsSettingsOpen] = (0, import_react2.useState)(false);
    const [isHeaderFooterOpen, setIsHeaderFooterOpen] = (0, import_react2.useState)(false);
    const [autosaveEnabled, setAutosaveEnabled] = (0, import_react2.useState)(true);
    const autosaveActivation = resolveAutosaveActivation({
      hostAutosave,
      userEnabled: autosaveEnabled,
      canEdit,
      filePath
    });
    const [isShareDialogOpen, setIsShareDialogOpen] = (0, import_react2.useState)(false);
    const { reducedMotion, toggleReducedMotion } = useReducedMotion();
    const state = useViewerState({ content, canEdit });
    const {
      containerRef,
      mode,
      slides,
      canvasSize,
      loading,
      error,
      activeSlideIndex,
      selectedElementId,
      selectedElementIds,
      templateElementsBySlideId,
      activeSlide,
      selectedElement
    } = state;
    const { pushColor } = useRecentColorsSync({
      setRecentColors: state.setRecentColors,
      setPresentationProperties: state.setPresentationProperties
    });
    const recentColorsValue = (0, import_react2.useMemo)(
      () => ({ recentColors: state.recentColors, pushColor }),
      [state.recentColors, pushColor]
    );
    const themeColorMapValue = (0, import_react2.useMemo)(
      () => {
        var _a2;
        return ((_a2 = state.theme) == null ? void 0 : _a2.colorScheme) ? buildThemeColorMap(state.theme.colorScheme) : void 0;
      },
      [(_a = state.theme) == null ? void 0 : _a.colorScheme]
    );
    const settings = (0, import_react2.useMemo)(
      () => ({
        autoSave: autosaveEnabled,
        spellCheck: state.spellCheckEnabled,
        showGrid: state.showGrid,
        showRulers: state.showRulers,
        snapToGrid: state.snapToGrid,
        reducedMotion
      }),
      [
        autosaveEnabled,
        state.spellCheckEnabled,
        state.showGrid,
        state.showRulers,
        state.snapToGrid,
        reducedMotion
      ]
    );
    const handleSettingsChange = (0, import_react2.useCallback)(
      (key, value) => {
        switch (key) {
          case "autoSave":
            setAutosaveEnabled(value);
            break;
          case "spellCheck":
            state.setSpellCheckEnabled(value);
            break;
          case "showGrid":
            state.setShowGrid(value);
            break;
          case "showRulers":
            state.setShowRulers(value);
            break;
          case "snapToGrid":
            state.setSnapToGrid(value);
            break;
          case "reducedMotion":
            if (value !== reducedMotion) {
              toggleReducedMotion();
            }
            break;
        }
      },
      [state, reducedMotion, toggleReducedMotion]
    );
    const settingsRef = (0, import_react2.useRef)(settings);
    settingsRef.current = settings;
    const handleSettingsChangeRef = (0, import_react2.useRef)(handleSettingsChange);
    handleSettingsChangeRef.current = handleSettingsChange;
    const syncingFromOptionsRef = (0, import_react2.useRef)(false);
    (0, import_react2.useEffect)(() => {
      const mapped = viewerOptionsToPreferences(viewerOptions);
      let changed = false;
      for (const key of Object.keys(mapped)) {
        if (mapped[key] !== settingsRef.current[key]) {
          changed = true;
          handleSettingsChangeRef.current(key, mapped[key]);
        }
      }
      if (changed) {
        syncingFromOptionsRef.current = true;
      }
    }, [viewerOptions]);
    (0, import_react2.useEffect)(() => {
      const current = optionsStore.getOptions();
      const mapped = viewerOptionsToPreferences(current);
      const keys = Object.keys(mapped);
      if (syncingFromOptionsRef.current) {
        if (keys.every((key) => mapped[key] === settings[key])) {
          syncingFromOptionsRef.current = false;
        }
        return;
      }
      let next = current;
      for (const key of keys) {
        if (mapped[key] !== settings[key]) {
          next = applyPreferenceToOptions(next, key, settings[key]);
        }
      }
      if (next !== current) {
        optionsStore.setOptions(next);
      }
    }, [settings, optionsStore]);
    const handleClearOptionsCache = (0, import_react2.useCallback)(() => {
      void (async () => {
        const snapshots = await listAutosaveSnapshots();
        await Promise.all(snapshots.map((entry) => deleteAutosaveSnapshot(entry.key)));
      })();
    }, []);
    (0, import_react2.useEffect)(() => {
      void (async () => {
        try {
          const snapshots = await listAutosaveSnapshots();
          const expired = resolveExpiredAutosaveSnapshots(snapshots, viewerOptions);
          await Promise.all(expired.map((key) => deleteAutosaveSnapshot(key)));
        } catch {
        }
      })();
    }, []);
    (0, import_react2.useEffect)(() => {
      const clearIfRequested = () => {
        if (shouldClearAutosaveCacheOnClose(viewerOptions)) {
          handleClearOptionsCache();
        }
      };
      window.addEventListener("beforeunload", clearIfRequested);
      return () => {
        window.removeEventListener("beforeunload", clearIfRequested);
        clearIfRequested();
      };
    }, [viewerOptions, handleClearOptionsCache]);
    const mobile = useIsMobile();
    const { isMobile, isTouchDevice, isVirtualKeyboardOpen } = mobile;
    const resizablePanels = useResizablePanels();
    const {
      gridSpacingPx,
      visibleSlideIndexes,
      slideSectionGroups,
      masterPseudoSlide,
      authoredRange
    } = useDerivedSlideState({
      slides,
      sections: state.sections,
      customShows: state.customShows,
      activeCustomShowId: state.activeCustomShowId,
      presentationProperties: state.presentationProperties,
      mode,
      activeLayout: state.activeLayout,
      activeMaster: state.activeMaster,
      documentGridSpacing: (_b2 = state.viewProperties) == null ? void 0 : _b2.gridSpacing
    });
    const activeCustomShow = state.customShows.find((show) => show.id === state.activeCustomShowId) ?? null;
    const hasActivePointerInteraction = (0, import_react2.useCallback)(
      () => Boolean(
        state.dragStateRef.current || state.resizeStateRef.current || state.marqueeStateRef.current || state.shapeAdjustmentDragStateRef.current || state.isDrawingRef.current
      ),
      [
        state.dragStateRef,
        state.resizeStateRef,
        state.marqueeStateRef,
        state.shapeAdjustmentDragStateRef,
        state.isDrawingRef
      ]
    );
    const zoom = useZoomViewport({
      canvasSize,
      selectedElements: state.selectedElements
    });
    const { setIsDirty } = state;
    const markDocumentDirty = (0, import_react2.useCallback)(() => {
      setIsDirty(true);
    }, [setIsDirty]);
    const history = useEditorHistory({
      slides,
      canvasSize,
      activeSlideIndex,
      templateElementsBySlideId,
      selectedElementId,
      selectedElementIds,
      editTemplateMode: state.editTemplateMode,
      headerFooter: state.headerFooter,
      loading,
      error,
      // File > Options > Advanced > "Maximum number of undos", re-read every
      // render so a mid-session change reaches the undo stack.
      maxHistoryEntries: resolveHistoryDepth(viewerOptions),
      hasActivePointerInteraction,
      pointerCommitNonce: state.pointerCommitNonce,
      onDirty: markDocumentDirty,
      setSlides: state.setSlides,
      setCanvasSize: state.setCanvasSize,
      setActiveSlideIndex: state.setActiveSlideIndex,
      setTemplateElementsBySlideId: state.setTemplateElementsBySlideId,
      setSelectedElementId: state.setSelectedElementId,
      setSelectedElementIds: state.setSelectedElementIds,
      setEditTemplateMode: state.setEditTemplateMode,
      setHeaderFooter: state.setHeaderFooter
    });
    const { presentation, annotations, actionSoundHandlerRef, setExitModeHandler } = usePresentationSetup({
      mode,
      slides,
      visibleSlideIndexes,
      // File > Options > Advanced > "End with black slide". Off means the show
      // exits straight to the editor instead of raising the black end screen.
      endWithBlackSlide: viewerOptions.advanced.slideShowEndWithBlackSlide,
      // File > Options > Advanced > "Prompt to keep ink annotations when
      // exiting". Off skips the keep/discard dialog entirely.
      promptKeepInkAnnotations: viewerOptions.advanced.slideShowPromptKeepInkAnnotations,
      // File > Options > Advanced > "Show popup toolbar" while presenting.
      popupToolbarEnabled: viewerOptions.advanced.slideShowShowPopupToolbar,
      activeSlideIndex,
      containerRef,
      content,
      mediaDataUrls: state.mediaDataUrls,
      presentationProperties: state.presentationProperties,
      setMode: state.setMode,
      setActiveSlideIndex: state.setActiveSlideIndex,
      setSlides: state.setSlides,
      history,
      // PowerPoint's bare `J` during a show toggles live captions. It resolves
      // in the shared slide-show keymap; this is the state it has to reach.
      onToggleSubtitles: () => state.setPresentationProperties((prev) => ({
        ...prev,
        showSubtitles: !prev.showSubtitles
      })),
      customShows: state.customShows,
      activeCustomShowId: state.activeCustomShowId,
      onSetActiveCustomShowId: state.setActiveCustomShowId
    });
    useTouchGestures({
      targetRef: zoom.canvasViewportRef,
      currentScale: zoom.scale,
      callbacks: {
        onPinchZoom: (newScale) => zoom.setScale(newScale),
        onSwipe: mode === "present" ? (direction) => presentation.movePresentationSlide(direction === 1 ? -1 : 1) : void 0,
        onLongPress: (clientX, clientY) => {
          if (mode !== "edit" || !canEdit) {
            return;
          }
          if (!state.selectedElementId) {
            return;
          }
          state.setContextMenuState({
            x: clientX,
            y: clientY,
            elementId: state.selectedElementId
          });
        }
      },
      enabled: isTouchDevice
    });
    const dialogs = useViewerDialogs({
      mode,
      slides,
      activeSlide,
      activeSlideIndex,
      canvasSize,
      containerRef,
      customShows: state.customShows,
      activeCustomShowId: state.activeCustomShowId,
      setCustomShows: state.setCustomShows,
      setActiveCustomShowId: state.setActiveCustomShowId,
      setGuides: state.setGuides,
      setPresentationProperties: state.setPresentationProperties,
      setAccessibilityIssues: state.setAccessibilityIssues,
      setIsAccessibilityPanelOpen: state.setIsAccessibilityPanelOpen,
      setMode: state.setMode,
      setPreMasterMode: state.setPreMasterMode,
      setActiveMasterIndex: state.setActiveMasterIndex,
      setActiveLayoutIndex: state.setActiveLayoutIndex,
      setSelectedElementId: state.setSelectedElementId,
      setSelectedElementIds: state.setSelectedElementIds,
      preMasterMode: state.preMasterMode,
      hasDigitalSignatures: state.hasDigitalSignatures,
      isDirty: state.isDirty,
      history,
      embeddedFontFamilies: state.embeddedFonts.map((font) => font.name)
    });
    (0, import_react2.useEffect)(() => {
      state.setSelectedElementId(null);
      state.setSelectedElementIds([]);
      state.setInlineEditingElementId(null);
    }, [activeSlideIndex]);
    (0, import_react2.useEffect)(() => {
      var _a2;
      if (mode !== "present") {
        return;
      }
      state.setSelectedElementId(null);
      state.setSelectedElementIds([]);
      state.setInlineEditingElementId(null);
      zoom.handleZoomToFit();
      (_a2 = zoom.canvasViewportRef.current) == null ? void 0 : _a2.scrollTo({ left: 0, top: 0 });
    }, [mode]);
    const editorOps = useEditorOperations({
      state,
      history,
      zoom,
      mode,
      canEdit,
      slides,
      activeSlide,
      activeSlideIndex,
      selectedElement,
      selectedElementId,
      selectedElementIds,
      canvasSize,
      dialogs,
      presentation,
      // Comment/reply authorship: an explicit host `authorName` or an active
      // collaboration session's identity wins; otherwise fall back to the
      // user's own Options > General > "User name" before the generic "You".
      userName: authorName ?? (collaboration == null ? void 0 : collaboration.userName) ?? (viewerOptions.general.userName || void 0),
      handlerRef: actionSoundHandlerRef,
      // Options > Proofing > AutoCorrect, applied to committed inline-edit text.
      transformCommittedText: (text) => applyAutoCorrect(text, viewerOptions.proofing)
    });
    const {
      exportHandlers: rawExportHandlers,
      printHandlers,
      themeHandlers,
      propertyHandlers,
      showKeepAnnotationsDialog,
      handleSetMode,
      handleKeepAnnotations,
      handleDiscardAnnotations,
      handleEnterPresenterView,
      handleEnterRehearsalMode,
      autosaveStatus,
      recovery,
      isEncryptedDialogOpen,
      setIsEncryptedDialogOpen,
      handlerRef,
      loadVersion
    } = useViewerIntegration({
      state,
      zoom,
      history,
      presentation,
      annotations,
      actionSoundHandlerRef,
      editorOps,
      dialogs,
      gridSpacingPx,
      content,
      filePath,
      autosaveEnabled: autosaveActivation.active,
      autosaveAllowed: hostAutosave !== false,
      // Host prop first (an explicit policy), else File > Options > Save >
      // "Save AutoRecover information every N minutes", else the shared 120s.
      autosaveIntervalMs: resolveAutosaveIntervalMs({
        hostIntervalMs: hostAutosaveIntervalMs,
        optionsIntervalSeconds: resolveAutosaveIntervalSeconds(viewerOptions)
      }),
      // Trust Center > "Allow external content": core defaults this to false
      // (SSRF/privacy-safe), so only an explicit `true` here lets remote
      // http(s) image sources actually load.
      allowExternalImages: viewerOptions.trust.allowExternalContent,
      setReadOnlyRecommendation: readOnlyRec.setRecommendation,
      setModifyVerifier: readOnlyRec.setModifyVerifier,
      setCompatToasts: compatToastsState.setToasts,
      canEdit,
      promptKeepInkAnnotations: viewerOptions.advanced.slideShowPromptKeepInkAnnotations,
      // File > Options > Advanced > "Image Size and Quality" (do-not-compress /
      // default resolution), resolved to a raster-scale multiplier for
      // PNG/PDF export and copy-slide-as-image. Multiplied against the
      // pre-existing 2x baseline (not used outright) so the default "High
      // fidelity" preset (raw multiplier 1) keeps today's export quality
      // instead of silently downgrading it; the explicit ppi presets still
      // scale proportionally from that baseline. Mirrors the other bindings.
      imageExportScale: 2 * resolveImageResolutionScale(viewerOptions),
      mode,
      slides,
      activeSlide,
      activeSlideIndex,
      canvasSize,
      loading,
      error,
      ref,
      setContent,
      onContentChange,
      onDirtyChange,
      onActiveSlideChange,
      onModeChange,
      onZoomChange,
      onSelectionChange,
      onSlideCountChange
    });
    const viewPreferencesSync = useViewPreferencesSync({
      loadVersion,
      viewProperties: state.viewProperties,
      setViewProperties: state.setViewProperties,
      snapToGrid: state.snapToGrid,
      setSnapToGrid: state.setSnapToGrid,
      snapToShape: state.snapToShape,
      setSnapToShape: state.setSnapToShape,
      showGuides: state.showGuides,
      setShowGuides: state.setShowGuides
    });
    const exportHandlers = (0, import_react2.useMemo)(() => {
      const afterSuccessfulSave = (format) => {
        playFeedbackSound(viewerOptions);
        if (format === "pptx" && filePath && shouldDiscardAutosaveOnSuccessfulSave(viewerOptions)) {
          void deleteAutosaveSnapshot(filePath);
        }
      };
      const handleSaveAsFormat = async (format) => {
        await rawExportHandlers.handleSaveAsFormat(format);
        afterSuccessfulSave(format);
      };
      return {
        ...rawExportHandlers,
        handleSaveAsFormat,
        handleSaveAsPptx: () => void handleSaveAsFormat("pptx"),
        handleSaveAsPpsx: () => void handleSaveAsFormat("ppsx"),
        handleSaveAsPptm: () => void handleSaveAsFormat("pptm")
      };
    }, [rawExportHandlers, viewerOptions, filePath]);
    setExitModeHandler(handleSetMode);
    const handleTemplateElementsChanged = (0, import_react2.useCallback)(
      (slideId, elements) => {
        state.setTemplateElementsBySlideId((prev) => ({ ...prev, [slideId]: elements }));
      },
      [state]
    );
    const layoutSwitching = useLayoutSwitching({
      handler: handlerRef.current,
      slides,
      activeSlideIndex,
      ops: editorOps.ops,
      history,
      onTemplateElementsChanged: handleTemplateElementsChanged
    });
    const masterViewCrud = useMasterViewCrud({
      handlerRef,
      slides,
      slideMasters: state.slideMasters,
      target: {
        tab: state.masterViewTab,
        masterIndex: state.activeMasterIndex,
        layoutIndex: state.activeLayoutIndex
      },
      setSlides: state.setSlides,
      setSlideMasters: state.setSlideMasters,
      setActiveMasterIndex: state.setActiveMasterIndex,
      setActiveLayoutIndex: state.setActiveLayoutIndex,
      markDirty: history.markDirty,
      pushToast: (toast) => compatToastsState.setToasts((prev) => [...prev, toast])
    });
    const applyAiTheme = (0, import_react2.useCallback)(
      (updates) => {
        if (updates.colorScheme) {
          void themeHandlers.handleUpdateThemeColorScheme(updates.colorScheme);
        }
        if (updates.fontScheme) {
          void themeHandlers.handleUpdateThemeFontScheme(updates.fontScheme);
        }
      },
      [themeHandlers]
    );
    const bumpAiHistory = (0, import_react2.useCallback)(() => {
      state.setPointerCommitNonce((n) => n + 1);
    }, [state]);
    const aiPanel = useAiPanelController({
      activeSlideIndex,
      selectedElementId,
      selectedElementIds,
      selectedElement
    });
    const aiBridge = useAiBridge({
      slides,
      activeSlideIndex,
      canvasSize,
      theme: state.theme,
      fileName,
      selectedElementId,
      selectedElementIds,
      pinnedFocus: aiPanel.pinnedFocus,
      pickedFocus: aiPanel.pickTargets.length > 0 ? aiPanel.pickTargets : null,
      handlerRef,
      sections: state.sections,
      presentationProperties: state.presentationProperties,
      customProperties: state.customProperties,
      coreProperties: state.coreProperties,
      appProperties: state.appProperties,
      viewProperties: state.viewProperties,
      tableStyleMap: state.tableStyleMap,
      tableStylesDefaultId: state.tableStylesDefaultId,
      tagCollections: state.tagCollections,
      setCanvasSize: state.setCanvasSize,
      setSections: state.setSections,
      setPresentationProperties: state.setPresentationProperties,
      setCustomProperties: state.setCustomProperties,
      setCoreProperties: state.setCoreProperties,
      setAppProperties: state.setAppProperties,
      setViewProperties: state.setViewProperties,
      setTableStyleMap: state.setTableStyleMap,
      setTableStylesDefaultId: state.setTableStylesDefaultId,
      setTagCollections: state.setTagCollections,
      setSlides: state.setSlides,
      setActiveSlideIndex: state.setActiveSlideIndex,
      applySelection: editorOps.ops.applySelection,
      bumpHistory: bumpAiHistory,
      markDirty: history.markDirty,
      applyThemeUpdates: applyAiTheme
    });
    const showSlidesPane = mode === "edit" && !isMobile && !dialogs.isNarrowViewport && state.isSlidesPaneOpen;
    const showMasterPane = mode === "master" && !isMobile && state.isSlidesPaneOpen;
    const addinStatus = {
      smartArt3d: smartArt3D && !viewerOptions.advanced.disable3DRendering,
      collaboration: Boolean(collaboration),
      locales: resolvedLocales.length > 1
    };
    const viewerContent = (0, import_jsx_runtime.jsxs)(
      "div",
      {
        style: themeStyle,
        "data-pptx-viewer": "",
        "aria-busy": loading,
        className: cn(
          "h-full w-full bg-background text-foreground relative",
          ...resolveOptionRootClasses(viewerOptions, "pptx")
        ),
        children: [
          (0, import_jsx_runtime.jsx)(
            "div",
            {
              ref: containerRef,
              tabIndex: 0,
              className: "h-full w-full flex flex-col overflow-hidden outline-none",
              children: loading ? (0, import_jsx_runtime.jsx)(LoadingState, {}) : error ? (0, import_jsx_runtime.jsx)(ErrorState, { error }) : (0, import_jsx_runtime.jsx)(ThemeColorMapProvider, { value: themeColorMapValue, children: (0, import_jsx_runtime.jsxs)(RecentColorsProvider, { value: recentColorsValue, children: [
                mode !== "present" && (0, import_jsx_runtime.jsx)(
                  ViewerToolbarSection,
                  {
                    mode,
                    canEdit,
                    state,
                    selectedElement,
                    activeSlide,
                    activeSlideIndex,
                    zoom,
                    history,
                    findReplace: editorOps.findReplace,
                    manipulation: editorOps.manipulation,
                    insertHandlers: editorOps.insertHandlers,
                    exportHandlers,
                    printHandlers,
                    propertyHandlers,
                    dialogs,
                    slideOps: editorOps.slideOps,
                    sectionOps: editorOps.sectionOps,
                    onApplyLayout: (path) => void layoutSwitching.applyLayout(path),
                    loadLayoutPreviews: layoutSwitching.loadLayoutPreviews,
                    customFontFamilies,
                    ops: editorOps.ops,
                    onSetMode: handleSetMode,
                    onPresentFromBeginning: presentation.enterPresentModeFromBeginning,
                    onEnterPresenterView: handleEnterPresenterView,
                    onEnterRehearsalMode: handleEnterRehearsalMode,
                    onOpenSettings: () => setIsSettingsOpen(true),
                    onOpenHeaderFooter: () => setIsHeaderFooterOpen(true),
                    onOpenShareDialog: () => setIsShareDialogOpen(true),
                    onOpenFile: handleOpenFile,
                    onOpenRecentFile: handleOpenRecentFile,
                    fileName,
                    autosaveStatus,
                    autosaveEnabled: autosaveActivation.active,
                    onToggleAutosave: () => {
                      if (autosaveActivation.toggleAvailable) {
                        setAutosaveEnabled((p) => !p);
                      }
                    },
                    hiddenActions,
                    recentPresentationsCount: viewerOptions.advanced.recentPresentationsCount,
                    aiEnabled: Boolean(ai),
                    isAiPanelOpen: aiPanel.isOpen,
                    onToggleAiPanel: aiPanel.toggle,
                    isProtectedView,
                    onEnableEditing: hostCanEdit ? handleEnableEditing : void 0,
                    onSetSnapToGrid: viewPreferencesSync.handleSetSnapToGrid,
                    onSetSnapToShape: viewPreferencesSync.handleSetSnapToShape,
                    onSetShowGuides: viewPreferencesSync.handleSetShowGuides
                  }
                ),
                mode !== "present" && readOnlyRec.bannerVisible && (0, import_jsx_runtime.jsx)(
                  ReadOnlyBanner,
                  {
                    recommendation: readOnlyRec.recommendation,
                    onEditAnyway: readOnlyRec.editAnyway,
                    onDismiss: readOnlyRec.dismiss,
                    passwordPromptOpen: readOnlyRec.passwordPromptOpen,
                    passwordError: readOnlyRec.passwordError,
                    checkingPassword: readOnlyRec.checkingPassword,
                    onSubmitPassword: readOnlyRec.submitPassword,
                    onCancelPassword: readOnlyRec.cancelPasswordPrompt
                  }
                ),
                (0, import_jsx_runtime.jsx)(
                  ViewerMainContent,
                  {
                    mode,
                    canEdit,
                    slides,
                    activeSlide,
                    masterPseudoSlide,
                    activeSlideIndex,
                    canvasSize,
                    gridSpacingPx,
                    slideSectionGroups,
                    showSlidesPane,
                    showMasterPane,
                    selectedElement,
                    state,
                    editorOps,
                    dialogs,
                    presentation,
                    annotations,
                    propertyHandlers,
                    themeHandlers,
                    history,
                    comments: editorOps.comments,
                    masterViewCrud,
                    zoom,
                    isMobile,
                    isTouchDevice,
                    onEndPresentation: () => handleSetMode("edit"),
                    leftPanelWidth: isMobile ? void 0 : resizablePanels.leftWidth,
                    onResizeLeft: isMobile ? void 0 : resizablePanels.onResizeLeft,
                    rightPanelWidth: isMobile ? void 0 : resizablePanels.rightWidth,
                    onResizeRight: isMobile ? void 0 : resizablePanels.onResizeRight,
                    hiddenActions,
                    aiConfig: ai,
                    aiBridge: ai ? aiBridge : void 0,
                    aiPanel: ai ? aiPanel : void 0
                  }
                ),
                mode !== "present" && (!isVirtualKeyboardOpen || !state.isSlideNotesCollapsed) && (0, import_jsx_runtime.jsx)(
                  ViewerBottomPanels,
                  {
                    activeSlide,
                    allSlides: slides,
                    isSlideNotesCollapsed: state.isSlideNotesCollapsed,
                    canEdit,
                    slideCount: slides.length,
                    activeSlideIndex,
                    isDirty: state.isDirty,
                    autosaveStatus,
                    onToggleNotes: () => state.setIsSlideNotesCollapsed((p) => !p),
                    onUpdateNotes: propertyHandlers.handleUpdateNotes,
                    collaborationSlot: collaboration ? (0, import_jsx_runtime.jsx)(CollaborationStatusStrip, {}) : void 0,
                    notesPanelHeight: isMobile ? void 0 : resizablePanels.bottomHeight,
                    onResizeBottom: isMobile ? void 0 : resizablePanels.onResizeBottom,
                    scale: zoom.scale,
                    onZoomIn: zoom.handleZoomIn,
                    onZoomOut: zoom.handleZoomOut,
                    onZoomToFit: zoom.handleZoomToFit,
                    mode,
                    onSetMode: handleSetMode,
                    onToggleSlideSorter: () => state.setShowSlideSorter((p) => !p),
                    hideStatusBar: isMobile,
                    hiddenActions,
                    notesStyle: (_c = state.notesMaster) == null ? void 0 : _c.notesStyle
                  }
                ),
                mode !== "present" && isMobile && (0, import_jsx_runtime.jsx)(
                  MobileChromeOverlay,
                  {
                    state,
                    editorOps,
                    presentation,
                    slides,
                    activeSlideIndex,
                    canvasSize,
                    slideSectionGroups,
                    canEdit,
                    commentCount: ((_d = activeSlide == null ? void 0 : activeSlide.comments) == null ? void 0 : _d.length) ?? 0
                  }
                )
              ] }) })
            }
          ),
          mode !== "present" && (0, import_jsx_runtime.jsx)(
            CompatibilityToasts,
            {
              toasts: compatToastsState.toasts,
              onDismiss: compatToastsState.dismiss,
              onDismissAll: compatToastsState.dismissAll
            }
          ),
          (0, import_jsx_runtime.jsx)(
            ViewerDialogGroup,
            {
              dialogs,
              insertHandlers: editorOps.insertHandlers,
              exportHandlers,
              printHandlers,
              propertyHandlers,
              annotations,
              slides,
              activeSlideIndex,
              canvasSize,
              filePath,
              coreProperties: state.coreProperties,
              customProperties: state.customProperties,
              appProperties: state.appProperties,
              embeddedFonts: state.embeddedFonts,
              hasDigitalSignatures: state.hasDigitalSignatures,
              digitalSignatureCount: state.digitalSignatureCount,
              presentationProperties: state.presentationProperties,
              customShows: state.customShows,
              selectedElements: state.selectedElements,
              isEncryptedDialogOpen,
              setIsEncryptedDialogOpen,
              showKeepAnnotationsDialog,
              onKeepAnnotations: handleKeepAnnotations,
              onDiscardAnnotations: handleDiscardAnnotations
            }
          ),
          (0, import_jsx_runtime.jsx)(
            AutosaveRecoveryDialog,
            {
              prompt: shouldShowAutosaveRecoveryPrompt({
                prompt: recovery.prompt,
                presenting: mode === "present"
              }) ? recovery.prompt : null,
              onRestore: recovery.restore,
              onDiscard: recovery.discard
            }
          ),
          (0, import_jsx_runtime.jsx)(
            SettingsDialog,
            {
              isOpen: isSettingsOpen,
              onClose: () => setIsSettingsOpen(false),
              options: viewerOptions,
              onOptionChange: (group, key, value) => optionsStore.setValue(group, key, value),
              onRestoreOptions: (snapshot) => optionsStore.setOptions(snapshot),
              onRibbonTabHiddenChange: (tabId, hidden) => optionsStore.setRibbonTabHidden(tabId, hidden),
              onQuickAccessCommandsChange: (commandIds) => optionsStore.setQuickAccessCommands(commandIds),
              onResetOptions: (group) => optionsStore.reset(group),
              onClearCache: handleClearOptionsCache,
              addinStatus,
              themeKey,
              availableThemes: themeCatalog,
              onSelectTheme: handleThemeChange,
              localeCode,
              availableLocales: resolvedLocales,
              onSelectLocale: handleLocaleChange,
              aiEnabled: Boolean(ai),
              customFontFamilies,
              onCustomFontRegistered: handleCustomFontRegistered
            }
          ),
          isHeaderFooterOpen && (0, import_jsx_runtime.jsx)(
            HeaderFooterPanel,
            {
              headerFooter: state.headerFooter,
              onUpdate: (patch) => state.setHeaderFooter((current) => ({ ...current, ...patch })),
              onApplyToAll: () => {
                history.markDirty();
                setIsHeaderFooterOpen(false);
              },
              onApplyToCurrent: () => {
                history.markDirty();
                setIsHeaderFooterOpen(false);
              },
              onClose: () => setIsHeaderFooterOpen(false)
            }
          ),
          (0, import_jsx_runtime.jsx)(
            ShareDialog,
            {
              open: isShareDialogOpen,
              onClose: () => setIsShareDialogOpen(false),
              activeCollaboration: collaboration,
              onStartCollaboration,
              onStopCollaboration,
              preconfigured: Boolean(collaboration),
              defaultRoomId: shareDefaults == null ? void 0 : shareDefaults.roomId,
              defaultUserName: shareDefaults == null ? void 0 : shareDefaults.userName,
              defaultServerUrl: shareDefaults == null ? void 0 : shareDefaults.serverUrl
            }
          ),
          (0, import_jsx_runtime.jsx)(
            BroadcastDialog,
            {
              open: dialogs.isBroadcastDialogOpen,
              onClose: () => dialogs.setIsBroadcastDialogOpen(false),
              onStartBroadcast: onStartCollaboration,
              onStopBroadcast: onStopCollaboration,
              onStartPresenting: () => handleSetMode("present"),
              defaultRoomId: shareDefaults == null ? void 0 : shareDefaults.roomId,
              defaultUserName: shareDefaults == null ? void 0 : shareDefaults.userName,
              defaultServerUrl: shareDefaults == null ? void 0 : shareDefaults.serverUrl
            }
          ),
          (0, import_jsx_runtime.jsx)(
            ViewerOverlays,
            {
              isShortcutHelpOpen: state.isShortcutHelpOpen,
              isAccessibilityPanelOpen: state.isAccessibilityPanelOpen,
              showSlideSorter: state.showSlideSorter,
              showReadingView: state.showReadingView,
              showOutlineView: state.showOutlineView,
              templateElements: state.templateElements,
              accessibilityIssues: state.accessibilityIssues,
              slides,
              activeSlideIndex,
              canvasSize,
              canEdit,
              sectionGroups: slideSectionGroups,
              onCloseShortcuts: () => state.setIsShortcutHelpOpen(false),
              onCloseAccessibility: () => state.setIsAccessibilityPanelOpen(false),
              onSelectSlide: (i) => {
                state.setActiveSlideIndex(i);
                state.setShowSlideSorter(false);
              },
              onMoveSlide: editorOps.slideOps.handleMoveSlide,
              onDeleteSlides: editorOps.slideOps.handleDeleteSlides,
              onDuplicateSlides: editorOps.slideOps.handleDuplicateSlides,
              onToggleHideSlides: editorOps.slideOps.handleToggleHideSlides,
              onCloseSorter: () => state.setShowSlideSorter(false),
              onCloseReadingView: (slideIndex) => {
                state.setShowReadingView(false);
                state.setActiveSlideIndex(slideIndex);
              },
              onCloseOutlineView: () => state.setShowOutlineView(false),
              setSlides: state.setSlides,
              setActiveSlideIndex: state.setActiveSlideIndex,
              bumpHistory: () => state.setPointerCommitNonce((n) => n + 1),
              reducedMotion,
              onToggleReducedMotion: toggleReducedMotion
            }
          ),
          (0, import_jsx_runtime.jsx)(
            ViewerPresentationLayer,
            {
              mode,
              slides,
              canvasSize,
              templateElements: state.templateElements,
              presentation,
              activeCustomShow,
              authoredRange,
              onExitPresentation: () => handleSetMode("edit"),
              onUpdateNotes: propertyHandlers.handleUpdateNotes,
              isMobile
            }
          )
        ]
      }
    );
    const effective3D = resolve3DRenderingFlags(
      { smartArt3D, surfaceChart3D, barChart3D, lineChart3D, areaChart3D, pieChart3D },
      viewerOptions
    );
    return (
      // `ViewerOptionsContext` is how every deeply-nested chrome piece (the
      // toolbar, title bar, Quick Access strip, print dialog, canvas
      // hyperlink gate, share dialog) reads the live File > Options
      // snapshot without threading it through each intermediate prop list.
      // Nothing previously provided it, so every one of those consumers was
      // silently reading `DEFAULT_VIEWER_OPTIONS` forever, no matter what
      // the user changed in the Options dialog.
      (0, import_jsx_runtime.jsx)(ViewerOptionsContext.Provider, { value: viewerOptions, children: (0, import_jsx_runtime.jsx)(AccountAuthContext.Provider, { value: accountAuth, children: (0, import_jsx_runtime.jsx)(SmartArt3DContext.Provider, { value: effective3D.smartArt3D, children: (0, import_jsx_runtime.jsx)(SurfaceChart3DContext.Provider, { value: effective3D.surfaceChart3D, children: (0, import_jsx_runtime.jsx)(BarChart3DContext.Provider, { value: effective3D.barChart3D, children: (0, import_jsx_runtime.jsx)(LineChart3DContext.Provider, { value: effective3D.lineChart3D, children: (0, import_jsx_runtime.jsx)(AreaChart3DContext.Provider, { value: effective3D.areaChart3D, children: (0, import_jsx_runtime.jsx)(PieChart3DContext.Provider, { value: effective3D.pieChart3D, children: (0, import_jsx_runtime.jsx)(ViewerThemeProvider, { theme: effectiveTheme, children: (0, import_jsx_runtime.jsxs)(
        CollaborationProvider,
        {
          config: collaboration,
          canvasWidth: canvasSize.width,
          canvasHeight: canvasSize.height,
          children: [
            (0, import_jsx_runtime.jsx)(
              CollaborationDocumentSync,
              {
                slides,
                templateElementsBySlideId,
                setSlides: state.setSlides,
                config: collaboration,
                content,
                loadVersion,
                loadOrigin,
                livePatcher: state.livePatcher,
                deckSaveState: {
                  headerFooter: state.headerFooter,
                  presentationProperties: state.presentationProperties,
                  viewProperties: state.viewProperties,
                  customShows: state.customShows,
                  sections: state.sections,
                  coreProperties: state.coreProperties,
                  appProperties: state.appProperties,
                  customProperties: state.customProperties,
                  tagCollections: state.tagCollections,
                  slideMasters: state.slideMasters,
                  notesMaster: state.notesMaster,
                  handoutMaster: state.handoutMaster,
                  slideSize: resolveSlideSizeSelection({
                    current: state.slideSizeEmu,
                    canvas: canvasSize
                  }).size,
                  tableStyleMap: state.tableStyleMap,
                  tableStylesDefaultId: state.tableStylesDefaultId,
                  tableStylesToDelete: state.tableStylesToDelete,
                  embedFonts: dialogs.embedFontsEnabled
                }
              }
            ),
            (0, import_jsx_runtime.jsx)(
              CollaborationFollowLayer,
              {
                activeSlideIndex,
                setActiveSlideIndex: state.setActiveSlideIndex,
                slideCount: slides.length
              }
            ),
            viewerContent
          ]
        }
      ) }) }) }) }) }) }) }) }) })
    );
  }
);
PowerPointViewer.displayName = "PowerPointViewer";
function CollaborationStatusStrip() {
  const collab = useCollaboration();
  if (!collab) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(
    CollaborationStatusIndicator,
    {
      status: collab.status,
      connectedCount: collab.connectedCount,
      onRetry: collab.retry
    }
  );
}
function CollaborationDocumentSync({
  slides,
  templateElementsBySlideId,
  setSlides,
  config,
  content,
  loadVersion,
  loadOrigin,
  livePatcher,
  deckSaveState
}) {
  const collab = useCollaboration();
  const contentRef = (0, import_react2.useRef)(content);
  contentRef.current = content;
  const getSourceBytes = (0, import_react2.useCallback)(() => {
    const bytes = contentRef.current;
    if (!bytes) {
      return null;
    }
    return bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes);
  }, []);
  const deckSaveStateRef = (0, import_react2.useRef)(deckSaveState);
  deckSaveStateRef.current = deckSaveState;
  const getSaveOptions = (0, import_react2.useCallback)(() => buildDeckSaveOptions(deckSaveStateRef.current), []);
  useYjsDocumentSync({
    doc: (collab == null ? void 0 : collab.doc) ?? null,
    slides,
    templateElementsBySlideId,
    setSlides,
    isConnected: (collab == null ? void 0 : collab.status) === "connected",
    isSynced: (collab == null ? void 0 : collab.synced) ?? true,
    config,
    getSourceBytes,
    getSaveOptions,
    loadVersion,
    loadOrigin
  });
  useCollaborationLivePatch({
    patcher: livePatcher,
    doc: (collab == null ? void 0 : collab.doc) ?? null,
    isConnected: (collab == null ? void 0 : collab.status) === "connected",
    isSynced: (collab == null ? void 0 : collab.synced) ?? true
  });
  return null;
}
function CollaborationFollowLayer({
  activeSlideIndex,
  setActiveSlideIndex,
  slideCount
}) {
  const collab = useCollaboration();
  const { followedClientId, followUser } = useFollowMode({
    collab,
    activeSlideIndex,
    setActiveSlideIndex,
    slideCount
  });
  useBroadcastFollower({
    collab,
    activeSlideIndex,
    setActiveSlideIndex,
    slideCount,
    paused: followedClientId !== null
  });
  if (!collab) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)("div", { className: "pointer-events-none fixed inset-x-0 top-2 z-[1100] flex justify-center px-2", children: (0, import_jsx_runtime.jsx)("div", { className: "pointer-events-auto", children: (0, import_jsx_runtime.jsx)(
    FollowModeBar,
    {
      presences: collab.remoteUsers,
      followedClientId,
      onFollow: followUser
    }
  ) }) });
}
export {
  AVATAR_COLOR_SWATCHES,
  DEFAULT_VIEWER_PROFILE,
  LOCALE_CATALOG,
  PPTX_OPEN_ACCEPT,
  PRESENTATION_OPEN_EXTENSIONS,
  PowerPointViewer,
  SLIDE_TEMPLATES,
  SlideCanvas,
  SlideTemplateGalleryDialog,
  SlideTemplatePreview,
  THEME_CATALOG,
  Toolbar,
  VIEWER_PREFS_STORAGE_KEY,
  ViewerThemeProvider,
  buildSlideTemplateContent,
  buildSlideTemplateSlide,
  clearAllLocalViewerData,
  clearStoredViewerPrefs,
  defaultCssVars,
  defaultRadius,
  defaultThemeColors,
  getAnimationInitialStyle2 as getAnimationInitialStyle,
  getLocalStorageUsageSummary,
  isLegacyBinaryPresentation,
  isSupportedPresentationFile,
  presentationBaseName,
  readStoredViewerPrefs,
  renderToCanvas,
  resolveProfileInitial,
  resolveThemeCatalogEntry,
  saveViewerProfile,
  savedPresentationFileName,
  themeToCssVars,
  useViewerBuildingBlocks,
  useViewerTheme,
  vermilionDarkColors,
  vermilionDarkTheme,
  vermilionLightColors,
  vermilionLightTheme,
  vermilionRadius,
  writeStoredViewerPrefs
};
//# sourceMappingURL=pptx-react-viewer.js.map
