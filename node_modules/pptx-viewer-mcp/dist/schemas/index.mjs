import { z } from 'zod';

// src/schemas/slide-schemas.ts
var GetSlideSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index")
});
var AddSlideSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  insertAfterIndex: z.number().int().min(0).optional().describe("Insert after this slide index"),
  backgroundColor: z.string().optional().describe("Background color (hex)")
});
var DeleteSlidesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndexes: z.array(z.number().int().min(0)).min(1).describe("Slide indexes to delete")
});
var ReorderSlidesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  newOrder: z.array(z.number().int().min(0)).min(1).describe("New slide order as array of indexes")
});
var DuplicateSlideSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Slide to duplicate"),
  targetIndex: z.number().int().min(0).optional().describe("Where to insert the duplicate")
});
var UpdateSlidePropertiesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  backgroundColor: z.string().optional().describe("Background color (hex)"),
  backgroundGradient: z.string().optional().describe("Background gradient CSS"),
  backgroundImage: z.string().optional().describe("Background image data URL"),
  notes: z.string().optional().describe("Speaker notes text"),
  hidden: z.boolean().optional().describe("Whether slide is hidden")
});
var SetSlideTransitionSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  type: z.string().describe('Transition type (e.g. "fade", "wipe", "none")'),
  durationMs: z.number().int().min(0).optional().describe("Duration in ms"),
  direction: z.string().optional().describe('Direction (e.g. "left", "right")'),
  advanceOnClick: z.boolean().optional().describe("Advance on mouse click"),
  advanceAfterMs: z.number().int().min(0).optional().describe("Auto-advance after ms")
});
var SetCanvasSizeSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  width: z.number().positive().describe("Canvas width in points"),
  height: z.number().positive().describe("Canvas height in points")
});
var ConvertToMarkdownSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  outputDir: z.string().optional().describe("Output directory for markdown and media"),
  mediaFolderName: z.string().optional().describe("Name for extracted media folder"),
  includeMetadata: z.boolean().optional().describe("Include presentation metadata"),
  slideRange: z.object({
    start: z.number().int().min(0).optional(),
    end: z.number().int().min(0).optional()
  }).optional().describe("Slide range to convert"),
  includeSpeakerNotes: z.boolean().optional().describe("Include speaker notes"),
  semanticMode: z.boolean().optional().describe("Use semantic (clean) markdown mode")
});
var AccessibilityCheckSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var ThemeColorRefSchema = z.object({
  scheme: z.enum([
    "dk1",
    "lt1",
    "dk2",
    "lt2",
    "accent1",
    "accent2",
    "accent3",
    "accent4",
    "accent5",
    "accent6",
    "hlink",
    "folHlink",
    "bg1",
    "tx1",
    "bg2",
    "tx2",
    "phClr"
  ]),
  lumMod: z.number().optional().describe("Multiply HSL luminance (0..1)"),
  lumOff: z.number().optional().describe("Add to HSL luminance after lumMod (0..1)"),
  tint: z.number().optional().describe("Blend towards white (0..1)"),
  shade: z.number().optional().describe("Blend towards black (0..1)"),
  alpha: z.number().optional().describe("Opacity fraction, 1 = opaque")
});
var AddElementSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  type: z.enum(["text", "shape", "image", "table", "connector"]).describe("Element type to add"),
  x: z.number().optional().describe("X position in points"),
  y: z.number().optional().describe("Y position in points"),
  width: z.number().optional().describe("Width in points"),
  height: z.number().optional().describe("Height in points"),
  text: z.string().optional().describe("Text content"),
  fontSize: z.number().optional().describe("Font size in points"),
  fontFamily: z.string().optional().describe("Font family name"),
  fontColor: z.string().optional().describe("Font color (hex)"),
  bold: z.boolean().optional().describe("Bold text"),
  italic: z.boolean().optional().describe("Italic text"),
  underline: z.boolean().optional().describe("Underline text"),
  alignment: z.enum(["left", "center", "right", "justify"]).optional().describe("Text alignment"),
  shapeType: z.string().optional().describe('Shape type (e.g. "rect", "ellipse")'),
  fillColor: z.string().optional().describe("Fill color (hex)"),
  strokeColor: z.string().optional().describe("Stroke color (hex)"),
  strokeWidth: z.number().optional().describe("Stroke width in points"),
  imageData: z.string().optional().describe("Image as base64 data URL"),
  altText: z.string().optional().describe("Alt text for accessibility"),
  rows: z.number().int().min(1).optional().describe("Number of table rows"),
  columns: z.number().int().min(1).optional().describe("Number of table columns"),
  cellData: z.array(z.array(z.string())).optional().describe("Table cell data as 2D array"),
  headerRow: z.boolean().optional().describe("Style first row as header"),
  startArrow: z.string().optional().describe("Start arrow type"),
  endArrow: z.string().optional().describe("End arrow type"),
  startShapeId: z.string().optional().describe("Connect start to shape ID"),
  endShapeId: z.string().optional().describe("Connect end to shape ID")
});
var UpdateElementSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Element ID to update"),
  x: z.number().optional(),
  y: z.number().optional(),
  width: z.number().optional(),
  height: z.number().optional(),
  rotation: z.number().optional(),
  text: z.string().optional(),
  fontSize: z.number().optional(),
  fontFamily: z.string().optional(),
  fontColor: z.string().optional(),
  /**
   * A theme colour for the run text. Wins on save; resolves `fontColor`
   * immediately when `fontColor` was not also given. Passing `fontColor`
   * alone clears a previously-set text theme colour.
   */
  fontThemeColor: ThemeColorRefSchema.optional(),
  bold: z.boolean().optional(),
  italic: z.boolean().optional(),
  underline: z.boolean().optional(),
  alignment: z.enum(["left", "center", "right", "justify"]).optional(),
  fillColor: z.string().optional(),
  strokeColor: z.string().optional(),
  strokeWidth: z.number().optional(),
  opacity: z.number().min(0).max(1).optional(),
  hidden: z.boolean().optional(),
  flipH: z.boolean().optional(),
  flipV: z.boolean().optional(),
  altText: z.string().optional().describe(
    "Accessibility description (p:cNvPr/@descr) for image, picture, table, chart, smartArt, ole, media, text, shape and connector elements"
  ),
  title: z.string().optional().describe(
    "Accessibility title (p:cNvPr/@title) for table, chart, smartArt, ole, media, text, shape and connector elements (not pictures)"
  )
});
var RenameElementSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Element ID to rename"),
  name: z.string().describe("New element name (empty string clears the name)")
});
var DeleteElementsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementIds: z.array(z.string()).min(1).describe("Element IDs to delete")
});
var ArrangeElementsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  action: z.enum(["align", "reorderLayer"]),
  elementIds: z.array(z.string()).optional().describe("Element IDs (for align)"),
  alignment: z.enum(["left", "right", "top", "bottom", "centerH", "centerV"]).optional(),
  elementId: z.string().optional().describe("Element ID (for reorderLayer)"),
  layerAction: z.enum(["bringForward", "sendBackward", "bringToFront", "sendToBack"]).optional()
});
var CloneElementSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementId: z.string(),
  targetSlideIndexes: z.array(z.number().int().min(0)).optional(),
  offsetX: z.number().optional(),
  offsetY: z.number().optional()
});
var SetElementAnimationSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementId: z.string(),
  entrance: z.string().optional(),
  exit: z.string().optional(),
  durationMs: z.number().optional(),
  delayMs: z.number().optional(),
  order: z.number().optional()
});
var GroupElementsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementIds: z.array(z.string()).min(2).describe("At least 2 element IDs to group")
});
var UngroupElementsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  groupElementId: z.string().describe("Group element ID to ungroup")
});
var BatchUpdateElementsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementIds: z.array(z.string()).min(1),
  x: z.number().optional(),
  y: z.number().optional(),
  width: z.number().optional(),
  height: z.number().optional(),
  rotation: z.number().optional(),
  opacity: z.number().min(0).max(1).optional(),
  hidden: z.boolean().optional()
});
var ThemeColorRefSchema2 = z.object({
  scheme: z.enum([
    "dk1",
    "lt1",
    "dk2",
    "lt2",
    "accent1",
    "accent2",
    "accent3",
    "accent4",
    "accent5",
    "accent6",
    "hlink",
    "folHlink",
    "bg1",
    "tx1",
    "bg2",
    "tx2",
    "phClr"
  ]),
  lumMod: z.number().optional().describe("Multiply HSL luminance (0..1)"),
  lumOff: z.number().optional().describe("Add to HSL luminance after lumMod (0..1)"),
  tint: z.number().optional().describe("Blend towards white (0..1)"),
  shade: z.number().optional().describe("Blend towards black (0..1)"),
  alpha: z.number().optional().describe("Opacity fraction, 1 = opaque")
});
var UpdateTableCellsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementId: z.string().describe("Table element ID"),
  cells: z.array(
    z.object({
      row: z.number().int().min(0),
      col: z.number().int().min(0),
      text: z.string()
    })
  ).min(1).describe("Cells to update")
});
var ManageTableStructureSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementId: z.string().describe("Table element ID"),
  action: z.enum(["insertRow", "deleteRow", "insertColumn", "deleteColumn"]),
  position: z.number().int().min(0).optional().describe("Insertion position index"),
  referenceIndex: z.number().int().min(0).optional(),
  cellTexts: z.array(z.string()).optional()
});
var UpdateElementStyleSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementId: z.string(),
  fillColor: z.string().optional(),
  /**
   * A theme colour for the fill (e.g. `{ scheme: 'accent1', lumMod: 0.8 }`
   * for "Accent 1, Lighter 80%"). When set, the shape saves as
   * `<a:schemeClr>` and keeps following the theme after a later theme
   * change, instead of freezing today's resolved hex. Setting `fillColor`
   * without this clears any previously-set fill theme colour.
   */
  fillThemeColor: ThemeColorRefSchema2.optional(),
  fillMode: z.string().optional(),
  fillGradientStops: z.array(
    z.object({
      color: z.string(),
      position: z.number(),
      opacity: z.number().optional()
    })
  ).optional(),
  fillGradientAngle: z.number().optional(),
  fillGradientType: z.string().optional(),
  fillOpacity: z.number().optional(),
  strokeColor: z.string().optional(),
  /** A theme colour for the outline; see `fillThemeColor` above. */
  strokeThemeColor: ThemeColorRefSchema2.optional(),
  strokeWidth: z.number().optional(),
  strokeDash: z.string().optional(),
  strokeOpacity: z.number().optional(),
  shadowColor: z.string().optional(),
  shadowBlur: z.number().optional(),
  shadowOffsetX: z.number().optional(),
  shadowOffsetY: z.number().optional(),
  shadowOpacity: z.number().optional(),
  glowColor: z.string().optional(),
  glowRadius: z.number().optional(),
  glowOpacity: z.number().optional(),
  softEdgeRadius: z.number().optional(),
  cropLeft: z.number().optional(),
  cropTop: z.number().optional(),
  cropRight: z.number().optional(),
  cropBottom: z.number().optional(),
  brightness: z.number().optional(),
  contrast: z.number().optional(),
  grayscale: z.boolean().optional(),
  altText: z.string().optional().describe(
    "Accessibility description (p:cNvPr/@descr) for image, picture, table, chart, smartArt, ole, media, text, shape and connector elements"
  ),
  title: z.string().optional().describe(
    "Accessibility title (p:cNvPr/@title) for table, chart, smartArt, ole, media, text, shape and connector elements (not pictures)"
  )
});
var FindTextSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  query: z.string().describe("Text to search for"),
  useRegex: z.boolean().optional().describe("Treat query as regex"),
  caseSensitive: z.boolean().optional().describe("Case-sensitive search"),
  slideIndexes: z.array(z.number().int().min(0)).optional().describe("Limit search to specific slides")
});
var ReplaceTextSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  query: z.string().describe("Text to find"),
  replacement: z.string().describe("Replacement text"),
  useRegex: z.boolean().optional(),
  caseSensitive: z.boolean().optional(),
  slideIndexes: z.array(z.number().int().min(0)).optional()
});
var ManageCommentsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  action: z.enum(["list", "add", "delete", "resolve"]),
  slideIndex: z.number().int().min(0).optional(),
  text: z.string().optional(),
  author: z.string().optional(),
  commentId: z.string().optional(),
  resolved: z.boolean().optional(),
  includeResolved: z.boolean().optional()
});
var TABLE_STYLE_SECTIONS = [
  "wholeTbl",
  "band1H",
  "band2H",
  "band1V",
  "band2V",
  "lastCol",
  "firstCol",
  "lastRow",
  "seCell",
  "swCell",
  "firstRow",
  "neCell",
  "nwCell"
];
var TableStyleSimpleColorSchema = z.object({
  schemeColor: z.string().describe('Theme colour key (e.g. "accent1"), or "" for a non-scheme fill'),
  tint: z.number().optional().describe("0-100000"),
  shade: z.number().optional().describe("0-100000"),
  color: z.string().optional().describe('Explicit hex colour, e.g. "#FF8800"')
});
var TableStyleFillSchema = z.object({
  schemeColor: z.string(),
  tint: z.number().optional(),
  shade: z.number().optional(),
  color: z.string().optional(),
  noFill: z.boolean().optional(),
  gradient: z.object({
    type: z.enum(["linear", "radial"]),
    angle: z.number().optional(),
    stops: z.array(z.object({ position: z.number(), fill: TableStyleSimpleColorSchema }))
  }).optional(),
  pattern: z.object({
    preset: z.string(),
    foreground: TableStyleSimpleColorSchema.optional(),
    background: TableStyleSimpleColorSchema.optional()
  }).optional(),
  image: z.object({ path: z.string().optional(), data: z.string().optional() }).optional()
});
var TableStyleTextSchema = z.object({
  bold: z.boolean().optional(),
  italic: z.boolean().optional(),
  underline: z.boolean().optional(),
  fontSchemeColor: z.string().optional(),
  fontTint: z.number().optional(),
  fontShade: z.number().optional(),
  fontColor: z.string().optional(),
  fontFace: z.string().optional(),
  fontRefIdx: z.string().optional()
});
var TableStyleBorderSideSchema = z.object({
  width: z.number().optional(),
  dash: z.string().optional(),
  fill: TableStyleSimpleColorSchema.optional(),
  color: z.string().optional(),
  noFill: z.boolean().optional()
});
var TableStyleBordersSchema = z.object({
  left: TableStyleBorderSideSchema.optional(),
  right: TableStyleBorderSideSchema.optional(),
  top: TableStyleBorderSideSchema.optional(),
  bottom: TableStyleBorderSideSchema.optional(),
  insideH: TableStyleBorderSideSchema.optional(),
  insideV: TableStyleBorderSideSchema.optional(),
  tl2br: TableStyleBorderSideSchema.optional(),
  tr2bl: TableStyleBorderSideSchema.optional()
});
var TableStyleCell3DSchema = z.object({
  bevelWidth: z.number().optional(),
  bevelHeight: z.number().optional(),
  bevelPreset: z.string().optional(),
  material: z.string().optional(),
  lightRig: z.string().optional(),
  lightRigDirection: z.string().optional()
});
var SetTableStyleSectionSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  styleId: z.string().describe('Table style GUID, e.g. "{5C22544A-7EE6-4342-B048-85BDC9FD1C3A}"'),
  section: z.enum(TABLE_STYLE_SECTIONS).describe("Which of the 13 CT_TableStyle parts to patch"),
  styleName: z.string().optional().describe("Rename the style"),
  fill: TableStyleFillSchema.optional(),
  text: TableStyleTextSchema.optional(),
  borders: TableStyleBordersSchema.optional(),
  cell3D: TableStyleCell3DSchema.optional()
});
var CreateTableStyleSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  styleName: z.string().describe("Display name for the new style"),
  basedOnStyleId: z.string().optional().describe("Deep-clone every section from this existing style GUID as the starting point"),
  setAsDefault: z.boolean().optional().describe("Repoint ppt/tableStyles.xml's default style at the new one")
});
var DeleteTableStyleSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  styleId: z.string().describe("Table style GUID to remove"),
  force: z.boolean().optional().describe("Delete even though a table on this deck still references the style")
});
var AssignTableStyleSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0),
  elementId: z.string().describe("Table element ID"),
  styleId: z.string().optional().describe(
    "Table style GUID to assign. Defaults to the presentation's current default table style when omitted."
  ),
  bandedRows: z.boolean().optional(),
  bandedColumns: z.boolean().optional(),
  firstRowHeader: z.boolean().optional(),
  lastRow: z.boolean().optional(),
  firstCol: z.boolean().optional(),
  lastCol: z.boolean().optional()
});
var GetThemeInfoSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var ApplyThemePresetSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  presetName: z.string().describe(
    'Theme preset name (e.g. "OFFICE", "MODERN_BLUE", "EARTH", "MONOCHROME", "VIBRANT", "CORPORATE", "MINIMAL", "DARK")'
  )
});
var UpdateThemeColorsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  dk1: z.string().optional().describe("Dark 1 color (hex)"),
  lt1: z.string().optional().describe("Light 1 color (hex)"),
  dk2: z.string().optional().describe("Dark 2 color (hex)"),
  lt2: z.string().optional().describe("Light 2 color (hex)"),
  accent1: z.string().optional().describe("Accent 1 color (hex)"),
  accent2: z.string().optional().describe("Accent 2 color (hex)"),
  accent3: z.string().optional().describe("Accent 3 color (hex)"),
  accent4: z.string().optional().describe("Accent 4 color (hex)"),
  accent5: z.string().optional().describe("Accent 5 color (hex)"),
  accent6: z.string().optional().describe("Accent 6 color (hex)"),
  hlink: z.string().optional().describe("Hyperlink color (hex)"),
  folHlink: z.string().optional().describe("Followed hyperlink color (hex)")
});
var UpdateThemeFontsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  majorFont: z.string().optional().describe("Heading (major) font family"),
  minorFont: z.string().optional().describe("Body (minor) font family")
});
var MarkerSymbolSchema = z.enum([
  "circle",
  "dash",
  "diamond",
  "dot",
  "none",
  "picture",
  "plus",
  "square",
  "star",
  "triangle",
  "x",
  "auto"
]);
var DataLabelPositionSchema = z.enum([
  "bestFit",
  "b",
  "ctr",
  "inBase",
  "inEnd",
  "l",
  "outEnd",
  "r",
  "t"
]);
var FormatChartDataPointSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  seriesIndex: z.number().int().min(0).describe("Zero-based series index"),
  pointIndex: z.number().int().min(0).describe("Zero-based data point (category) index"),
  fillColor: z.string().optional().describe("Fill colour (hex) for this point only"),
  strokeColor: z.string().optional().describe("Outline colour (hex) for this point only"),
  strokeWidth: z.number().optional().describe("Outline width in points"),
  strokeDashStyle: z.string().optional().describe("Outline dash style, e.g. solid, dash, dashDot, sysDot"),
  clearStyle: z.boolean().optional().describe("Remove all per-point shape formatting"),
  explosion: z.number().nullable().optional().describe("Pie/doughnut slice pull-out distance 0-100; null clears it"),
  marker: z.object({
    symbol: MarkerSymbolSchema.optional(),
    size: z.number().optional(),
    fillColor: z.string().optional()
  }).nullable().optional().describe("Per-point marker override (line/scatter/bubble); null clears it")
});
var FormatChartDataLabelSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  seriesIndex: z.number().int().min(0).describe("Zero-based series index"),
  pointIndex: z.number().int().min(0).describe("Zero-based data point (category) index"),
  remove: z.boolean().optional().describe("Remove the per-point label override entirely"),
  showValue: z.boolean().optional(),
  showCategory: z.boolean().optional(),
  showSeriesName: z.boolean().optional(),
  showPercent: z.boolean().optional(),
  showLegendKey: z.boolean().optional(),
  position: DataLabelPositionSchema.optional(),
  text: z.string().optional().describe("Custom label text override; '' clears it"),
  spPr: z.object({
    fillColor: z.string().optional(),
    strokeColor: z.string().optional(),
    strokeWidth: z.number().optional(),
    strokeDashStyle: z.string().optional()
  }).nullable().optional().describe("This label's own fill/line formatting; null removes it"),
  txPr: z.object({
    fontFamily: z.string().optional(),
    fontSize: z.number().optional(),
    bold: z.boolean().optional(),
    italic: z.boolean().optional(),
    color: z.string().optional()
  }).nullable().optional().describe("This label's own font; null removes it")
});
var FormatChartSeriesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  seriesIndex: z.number().int().min(0).describe("Zero-based series index"),
  marker: z.object({
    symbol: MarkerSymbolSchema.optional(),
    size: z.number().optional(),
    fillColor: z.string().optional(),
    strokeColor: z.string().optional(),
    strokeWidth: z.number().optional(),
    strokeDashStyle: z.string().optional()
  }).nullable().optional().describe("Series marker (line/scatter/bubble/radar); null removes it"),
  trendline: z.object({
    trendlineType: z.enum([
      "linear",
      "exponential",
      "logarithmic",
      "polynomial",
      "power",
      "movingAvg"
    ]),
    color: z.string().optional(),
    lineWidth: z.number().optional().describe("Trendline width in points"),
    lineDashStyle: z.string().optional(),
    order: z.number().optional().describe("Polynomial order, 2-6"),
    period: z.number().optional().describe("Moving-average period"),
    displayEq: z.boolean().optional(),
    displayRSq: z.boolean().optional()
  }).nullable().optional().describe("Series trendline; null removes it"),
  errorBars: z.object({
    direction: z.enum(["x", "y"]),
    barType: z.enum(["both", "minus", "plus"]),
    valType: z.enum(["cust", "fixedVal", "percentage", "stdDev", "stdErr"]),
    val: z.number().optional(),
    customPlus: z.array(z.number()).optional(),
    customMinus: z.array(z.number()).optional(),
    noEndCap: z.boolean().optional(),
    color: z.string().optional(),
    width: z.number().optional().describe("Error-bar line width in points"),
    dashStyle: z.string().optional()
  }).nullable().optional().describe("Series error bars; null removes them")
});
var SetChartHelperLineSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  line: z.enum(["dropLines", "hiLowLines"]).describe("Which helper line to set"),
  style: z.object({
    color: z.string().optional(),
    width: z.number().optional(),
    dashStyle: z.string().optional()
  }).nullable().describe("Line colour/width/dash; {} shows the line with defaults, null removes it")
});
var SetChartColorMapOverrideSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  overrides: z.record(z.string(), z.string()).nullable().describe(
    'Theme colour role remap (bg1/tx1/bg2/tx2/accent1-6/hlink/folHlink -> role), e.g. { "accent1": "accent2" }; null removes the override'
  )
});
var UpdateChartSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  chartType: z.string().optional().describe(
    'New chart type (bar, line, pie, scatter, area, doughnut, radar, histogram, waterfall, funnel, treemap, sunburst, boxWhisker, regionMap). "pareto" is also accepted: it is authored as a histogram chart with an added cumulative-percentage line series.'
  ),
  title: z.string().optional().describe("Chart title"),
  grouping: z.enum(["clustered", "stacked", "percentStacked"]).optional().describe("Bar/column grouping"),
  legend: z.object({
    show: z.boolean().optional(),
    position: z.string().optional().describe("Legend position: b, t, l, r, tr")
  }).optional().describe("Legend configuration"),
  dataLabels: z.object({
    show: z.boolean().optional(),
    showValue: z.boolean().optional(),
    showCategory: z.boolean().optional(),
    showSeriesName: z.boolean().optional(),
    showPercent: z.boolean().optional()
  }).optional().describe("Data label configuration"),
  axis: z.object({
    type: z.string().describe("Axis type: valAx, catAx, dateAx, serAx"),
    edit: z.object({
      min: z.number().nullable().optional(),
      max: z.number().nullable().optional(),
      majorUnit: z.number().nullable().optional(),
      title: z.string().nullable().optional(),
      numberFormat: z.string().optional(),
      majorGridlines: z.boolean().optional(),
      minorGridlines: z.boolean().optional()
    })
  }).optional().describe("Axis configuration"),
  categories: z.array(z.string()).optional().describe("Category axis labels")
});
var AddChartSeriesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  name: z.string().describe("Series name"),
  values: z.array(z.number()).describe("Data values"),
  color: z.string().optional().describe("Series color (hex)")
});
var RemoveChartSeriesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  seriesIndex: z.number().int().min(0).describe("Zero-based series index to remove")
});
var UpdateChartSeriesDataSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  seriesIndex: z.number().int().min(0).describe("Zero-based series index"),
  values: z.array(z.number()).describe("New data values")
});
var CreateChartSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  chartType: z.string().describe(
    'Chart type (bar, line, pie, scatter, area, doughnut, radar, histogram, waterfall, funnel, treemap, sunburst, boxWhisker, regionMap). "pareto" is also accepted: it is authored as a histogram chart with an added cumulative-percentage line series.'
  ),
  x: z.number().optional().describe("X position in points"),
  y: z.number().optional().describe("Y position in points"),
  width: z.number().optional().describe("Width in points"),
  height: z.number().optional().describe("Height in points"),
  title: z.string().optional().describe("Chart title"),
  categories: z.array(z.string()).optional().describe("Category labels"),
  series: z.array(
    z.object({
      name: z.string(),
      values: z.array(z.number()),
      color: z.string().optional()
    })
  ).optional().describe("Data series"),
  legend: z.object({
    show: z.boolean(),
    position: z.string().optional()
  }).optional().describe("Legend configuration")
});
var PointSchema = z.object({ x: z.number(), y: z.number() });
var ExtentSchema = z.object({ cx: z.number(), cy: z.number() });
var ChartUserShapeInputSchema = z.object({
  kind: z.enum(["sp", "cxnSp"]).optional().describe('Overlay shape kind: "sp" (text/preset shape, default) or "cxnSp" (connector).'),
  anchor: z.enum(["rel", "abs"]).optional().describe(
    'Anchor kind: "rel" (default, resizes with the chart; needs `to`) or "abs" (fixed EMU size; needs `ext`).'
  ),
  from: PointSchema.describe("Top-left corner as chart-relative fractions (0-1 each)."),
  to: PointSchema.optional().describe(
    "Bottom-right corner as chart-relative fractions; rel anchors only."
  ),
  ext: ExtentSchema.optional().describe("Width/height in EMU; abs anchors only."),
  prst: z.string().optional().describe('Preset geometry name (e.g. "rect", "roundRect"); default "rect".'),
  fill: z.string().optional().describe('Solid fill colour, hex (e.g. "#FFFF00").'),
  stroke: z.string().optional().describe("Line colour, hex."),
  strokeWidth: z.number().optional().describe("Line width in points."),
  text: z.string().optional().describe("Plain text content, centred in the shape.")
});
var ListChartUserShapesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID")
});
var AddChartUserShapeSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  shape: ChartUserShapeInputSchema.describe("The overlay shape to add.")
});
var UpdateChartUserShapeSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  index: z.number().int().min(0).describe("Zero-based overlay-shape index (from chart_user_shape_list)"),
  patch: ChartUserShapeInputSchema.partial().describe(
    "Fields to overwrite; anything omitted is left as-is."
  )
});
var RemoveChartUserShapeSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Chart element ID"),
  index: z.number().int().min(0).describe("Zero-based overlay-shape index to remove")
});
var ManageSmartArtSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("SmartArt element ID"),
  action: z.enum([
    "getNodes",
    "addNode",
    "removeNode",
    "updateNodeText",
    "reorderNode",
    "promoteNode",
    "demoteNode",
    "decompose"
  ]).describe("SmartArt action"),
  nodeId: z.string().optional().describe("Node ID (required for most actions)"),
  text: z.string().optional().describe("Text for addNode or updateNodeText"),
  afterNodeId: z.string().optional().describe("Insert after this node ID"),
  direction: z.number().optional().describe("Reorder direction: 1 (forward) or -1 (backward)")
});
var FindPlaceholdersSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var ApplyTemplateSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  data: z.record(z.string(), z.unknown()).describe("Template data: keys are placeholder names, values are replacement values")
});
var GetMetadataSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var UpdateMetadataSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  title: z.string().optional().describe("Presentation title"),
  subject: z.string().optional().describe("Subject"),
  creator: z.string().optional().describe("Creator/author name"),
  keywords: z.string().optional().describe("Keywords (comma-separated)"),
  description: z.string().optional().describe("Description/comments"),
  lastModifiedBy: z.string().optional().describe("Last modified by"),
  category: z.string().optional().describe("Category"),
  company: z.string().optional().describe("Company name"),
  manager: z.string().optional().describe("Manager name"),
  customProperties: z.array(
    z.object({
      name: z.string(),
      value: z.union([z.string(), z.number(), z.boolean()])
    })
  ).optional().describe("Custom properties to set")
});
var ManageSectionsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  action: z.enum(["list", "add", "remove", "reorder", "moveSlides", "getForSlide"]).describe("Section action"),
  name: z.string().optional().describe("Section name (for add)"),
  slideIndices: z.array(z.number().int().min(0)).optional().describe("Slide indices (for add, moveSlides)"),
  sectionId: z.string().optional().describe("Section ID (for remove, moveSlides)"),
  sectionIds: z.array(z.string()).optional().describe("Ordered section IDs (for reorder)"),
  slideIndex: z.number().int().min(0).optional().describe("Slide index (for getForSlide)")
});
var ExportToSvgSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndices: z.array(z.number().int().min(0)).optional().describe("Specific slides to export (all if omitted)"),
  includeHidden: z.boolean().optional().describe("Include hidden slides"),
  defaultFontFamily: z.string().optional().describe("Default font family for SVG"),
  defaultFontSize: z.number().optional().describe("Default font size")
});
var ExportSlideSvgSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index to export"),
  defaultFontFamily: z.string().optional().describe("Default font family for SVG"),
  defaultFontSize: z.number().optional().describe("Default font size")
});
var ExportToJsonSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  pretty: z.boolean().optional().describe("Pretty-print the JSON document with 2-space indentation (default true)")
});
var ImportFromJsonSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  json: z.string().describe("The pptx-viewer-json document text to import (replaces the deck content)")
});
var ManageHyperlinksSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  action: z.enum(["list", "set", "remove"]).describe("Hyperlink action"),
  elementId: z.string().optional().describe("Element ID (for set/remove)"),
  trigger: z.enum(["click", "hover"]).optional().describe("Action trigger (default: click)"),
  url: z.string().optional().describe("URL for the hyperlink"),
  tooltip: z.string().optional().describe("Tooltip text"),
  targetSlideIndex: z.number().int().min(0).optional().describe("Target slide index for internal links"),
  actionType: z.enum(["url", "slide", "nextSlide", "prevSlide", "firstSlide", "lastSlide", "endShow"]).optional().describe("Link action type")
});
var ReplaceGeometrySchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Shape element ID"),
  shapeType: z.string().optional().describe('Preset shape type (e.g. "roundRect", "star5", "ellipse", "diamond")'),
  svgPath: z.string().optional().describe("Custom SVG path data (mutually exclusive with shapeType)"),
  pathWidth: z.number().optional().describe("Width of SVG path coordinate space"),
  pathHeight: z.number().optional().describe("Height of SVG path coordinate space"),
  adjustments: z.record(z.string(), z.number()).optional().describe("Shape adjustment values")
});
var SetElementLockSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  elementId: z.string().describe("Element ID to lock/unlock"),
  locked: z.boolean().describe("Whether the element should be locked"),
  noMove: z.boolean().optional().describe("Prevent moving"),
  noResize: z.boolean().optional().describe("Prevent resizing"),
  noRotation: z.boolean().optional().describe("Prevent rotation"),
  noSelect: z.boolean().optional().describe("Prevent selection"),
  noTextEdit: z.boolean().optional().describe("Prevent text editing")
});
var ValidatePresentationSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var RepairPresentationSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var GetPresentationPropertiesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var UpdatePresentationPropertiesSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  showType: z.enum(["presented", "browsed", "kiosk"]).optional().describe("Slideshow mode"),
  loopContinuously: z.boolean().optional().describe("Loop the slideshow"),
  showWithNarration: z.boolean().optional().describe("Play narration"),
  showWithAnimation: z.boolean().optional().describe("Play animations"),
  advanceMode: z.enum(["manual", "useTimings"]).optional().describe("Slide advance mode"),
  showSlidesMode: z.enum(["all", "customShow", "range"]).optional().describe("Which slides to show"),
  showSlidesFrom: z.number().int().min(1).optional().describe("Range start (1-based)"),
  showSlidesTo: z.number().int().min(1).optional().describe("Range end (1-based)"),
  penColor: z.string().optional().describe("Annotation pen color (hex)")
});
var GetLayoutsSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file")
});
var ApplyLayoutSchema = z.object({
  filePath: z.string().describe("Path to the PPTX file"),
  slideIndex: z.number().int().min(0).describe("Zero-based slide index"),
  layoutName: z.string().optional().describe("Layout name to apply"),
  layoutType: z.string().optional().describe("Layout type to apply (alternative to name)")
});

export { AccessibilityCheckSchema, AddChartSeriesSchema, AddChartUserShapeSchema, AddElementSchema, AddSlideSchema, ApplyLayoutSchema, ApplyTemplateSchema, ApplyThemePresetSchema, ArrangeElementsSchema, AssignTableStyleSchema, BatchUpdateElementsSchema, CloneElementSchema, ConvertToMarkdownSchema, CreateChartSchema, CreateTableStyleSchema, DeleteElementsSchema, DeleteSlidesSchema, DeleteTableStyleSchema, DuplicateSlideSchema, ExportSlideSvgSchema, ExportToJsonSchema, ExportToSvgSchema, FindPlaceholdersSchema, FindTextSchema, FormatChartDataLabelSchema, FormatChartDataPointSchema, FormatChartSeriesSchema, GetLayoutsSchema, GetMetadataSchema, GetPresentationPropertiesSchema, GetSlideSchema, GetThemeInfoSchema, GroupElementsSchema, ImportFromJsonSchema, ListChartUserShapesSchema, ManageCommentsSchema, ManageHyperlinksSchema, ManageSectionsSchema, ManageSmartArtSchema, ManageTableStructureSchema, RemoveChartSeriesSchema, RemoveChartUserShapeSchema, RenameElementSchema, ReorderSlidesSchema, RepairPresentationSchema, ReplaceGeometrySchema, ReplaceTextSchema, SetCanvasSizeSchema, SetChartColorMapOverrideSchema, SetChartHelperLineSchema, SetElementAnimationSchema, SetElementLockSchema, SetSlideTransitionSchema, SetTableStyleSectionSchema, TABLE_STYLE_SECTIONS, TableStyleBordersSchema, TableStyleCell3DSchema, TableStyleFillSchema, TableStyleTextSchema, UngroupElementsSchema, UpdateChartSchema, UpdateChartSeriesDataSchema, UpdateChartUserShapeSchema, UpdateElementSchema, UpdateElementStyleSchema, UpdateMetadataSchema, UpdatePresentationPropertiesSchema, UpdateSlidePropertiesSchema, UpdateTableCellsSchema, UpdateThemeColorsSchema, UpdateThemeFontsSchema, ValidatePresentationSchema };
